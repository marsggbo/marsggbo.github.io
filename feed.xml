<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://marsggbo.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://marsggbo.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-17T06:20:54+00:00</updated><id>https://marsggbo.github.io/feed.xml</id><title type="html">HE Xin</title><subtitle>&quot;SSS: Study, Sleep, Slim&quot; </subtitle><entry><title type="html">Eurosys26 | FineMoE如何用「细粒度」打破MoE推理的显存-延迟死局 - marsggbo</title><link href="https://marsggbo.github.io/blog/2026/eurosys26-finemoemoe-marsggbo/" rel="alternate" type="text/html" title="Eurosys26 | FineMoE如何用「细粒度」打破MoE推理的显存-延迟死局 - marsggbo"/><published>2026-05-16T03:28:00+00:00</published><updated>2026-05-16T03:28:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/eurosys26--finemoemoe----marsggbo</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/eurosys26-finemoemoe-marsggbo/"><![CDATA[]]></content><author><name></name></author><summary type="html"><![CDATA[layout: post title: &quot;延迟降47%！FineMoE如何用「细粒度」打破MoE推理的显存-延迟死局&quot; date: 2026-05-14 tags: [LLM, MoE, 论文解读, 推理优化, Expert Offloading] Eurosys26 | FineMoE如何用「细粒]]></summary></entry><entry><title type="html">LoRA fine-tune吞吐量提升1.96倍！LoRAFusion如何把内存带宽浪费和pipeline bubble一起干掉 - marsggbo</title><link href="https://marsggbo.github.io/blog/2026/lora-fine-tune196lorafusionpipeline-bubble-marsggbo/" rel="alternate" type="text/html" title="LoRA fine-tune吞吐量提升1.96倍！LoRAFusion如何把内存带宽浪费和pipeline bubble一起干掉 - marsggbo"/><published>2026-05-16T03:27:00+00:00</published><updated>2026-05-16T03:27:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/lora-fine-tune196lorafusionpipeline-bubble---marsggbo</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/lora-fine-tune196lorafusionpipeline-bubble-marsggbo/"><![CDATA[]]></content><author><name></name></author><summary type="html"><![CDATA[layout: post title: &quot;LoRA fine-tune吞吐量提升1.96倍！LoRAFusion如何把内存带宽浪费和pipeline bubble一起干掉&quot; date: 2026-05-14 tags: [LLM, LoRA, 训练优化, Kernel Fusion, 分布式训练,]]></summary></entry><entry><title type="html">Fast26 | LLM 推理启动慢？华为用一个「可编程 Page Cache」把模型加载砍了 79% - marsggbo</title><link href="https://marsggbo.github.io/blog/2026/fast26-llm-page-cache-79-marsggbo/" rel="alternate" type="text/html" title="Fast26 | LLM 推理启动慢？华为用一个「可编程 Page Cache」把模型加载砍了 79% - marsggbo"/><published>2026-05-16T03:26:00+00:00</published><updated>2026-05-16T03:26:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/fast26--llm--page-cache-79---marsggbo</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/fast26-llm-page-cache-79-marsggbo/"><![CDATA[]]></content><author><name></name></author><summary type="html"><![CDATA[layout: post title: &quot;LLM 推理启动慢？华为用一个「可编程 Page Cache」把模型加载砍了 79%&quot; date: 2026-05-14 tags: [LLM, 推理优化, 系统, 论文解读, FAST] LLM 推理启动慢？华为用一个「可编程 Page Cache」把模型]]></summary></entry><entry><title type="html">KV Cache 的两层存储到底卡在哪？FAST’26 这篇论文给出了答案 - marsggbo</title><link href="https://marsggbo.github.io/blog/2026/kv-cache-fast26-marsggbo/" rel="alternate" type="text/html" title="KV Cache 的两层存储到底卡在哪？FAST’26 这篇论文给出了答案 - marsggbo"/><published>2026-05-16T03:26:00+00:00</published><updated>2026-05-16T03:26:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/kv-cache-fast26----marsggbo</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/kv-cache-fast26-marsggbo/"><![CDATA[]]></content><author><name></name></author><summary type="html"><![CDATA[layout: post title: &quot;KV Cache 的两层存储到底卡在哪？FAST&#39;26 这篇论文给出了答案&quot; date: 2026-05-14 tags: [LLM, KV Cache, 论文解读, 系统优化, FAST] KV Cache 的两层存储到底卡在哪？FAST&#39;26 这篇论文给]]></summary></entry><entry><title type="html">延迟降47%！FineMoE如何用「细粒度」打破MoE推理的显存-延迟死局</title><link href="https://marsggbo.github.io/blog/2026/2502_05370/" rel="alternate" type="text/html" title="延迟降47%！FineMoE如何用「细粒度」打破MoE推理的显存-延迟死局"/><published>2026-05-14T00:00:00+00:00</published><updated>2026-05-14T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/2502_05370</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/2502_05370/"><![CDATA[<h1 id="延迟降47finemoe如何用细粒度打破moe推理的显存-延迟死局">延迟降47%！FineMoE如何用「细粒度」打破MoE推理的显存-延迟死局</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2502.05370">Taming Latency-Memory Trade-Off in MoE-Based LLM Serving via Fine-Grained Expert Offloading</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>今天想和大家聊聊这篇刚被 EuroSys ‘26 接收的工作——FineMoE。</p> <p>研究方向是 MoE 推理优化的同学应该都知道，这个领域有一个结结实实的大坑：MoE 模型的参数很多，但推理时真正用到的 expert 只是少数，<strong>绝大多数 expert 参数全程闲着</strong>。以 Mixtral-8×7B 为例，每次推理激活的 expert 比例只有 28%，也就是说有 <strong>72% 的参数在 GPU 显存里白占着位置</strong>，换算下来是 67 GB 的显存在”陪跑”。</p> <p>一个自然的思路是：把不用的 expert 卸载到 CPU 内存，用的时候再加载回来。但问题来了——怎么知道下一步要用哪个 expert？预测准了就能提前 prefetch，预测错了就得临时从 CPU 搬运，这个搬运延迟可能高达数百毫秒，直接拖垮推理速度。</p> <p>这就是 <strong>显存-延迟 trade-off</strong> 的核心矛盾：要么全部放在 GPU 里（不卸载，显存爆炸），要么卸载到 CPU（显存省了，但 hit miss 时延迟暴增）。</p> <p>现有方案在这个 trade-off 上踩了坑——它们要么低延迟但显存占用大，要么显存小但延迟高。FineMoE 的切入点很简单：<strong>现有方案太”粗粒度”了，预测准确率不够高，所以才陷入两难</strong>。</p> <hr/> <h2 id="2-背景llm-serving-和-moe-的基本逻辑">2. 背景：LLM Serving 和 MoE 的基本逻辑</h2> <p>先交代下背景。</p> <p>LLM serving 分两个阶段：<strong>prefill</strong> 和 <strong>decode</strong>。</p> <ul> <li><strong>Prefill</strong>：一次性处理所有输入 token，生成第一个输出 token，只跑一次，是 compute-bound 的，用 TTFT（Time-To-First-Token）衡量。</li> <li><strong>Decode</strong>：自回归逐 token 生成，每次用之前的输出预测下一个 token，是 memory-bound 的，用 TPOT（Time-Per-Output-Token）衡量。</li> </ul> <p>MoE 层的结构是：每个 Transformer block 里，把原来的 FFN 替换成一组 expert 网络 + 一个 gate 网络。gate 网络决定当前 token 激活哪 Top-K 个 expert。比如 Mixtral-8×7B 每层有 8 个 expert，每次只激活 2 个（Top-2）。</p> <p>如下图，Mixtral、Qwen1.5-MoE、Phi-3.5-MoE 三个典型 MoE 模型的参数统计：</p> <p><img src="/assets/img/posts/2502_05370/001_a.png" alt="MoE 推理流程与显存-延迟 trade-off 示意"/></p> <table> <thead> <tr> <th>模型</th> <th>激活参数 / 总参数</th> <th>Top-K / 专家数</th> <th>MoE层数</th> </tr> </thead> <tbody> <tr> <td>Mixtral-8×7B</td> <td>12.9B / 46.7B</td> <td>2 / 8</td> <td>32</td> </tr> <tr> <td>Qwen1.5-MoE</td> <td>2.7B / 14.3B</td> <td>4 / 60</td> <td>24</td> </tr> <tr> <td>Phi-3.5-MoE</td> <td>6.6B / 42B</td> <td>2 / 16</td> <td>32</td> </tr> </tbody> </table> <p><strong>72%、81%、84%</strong> 的参数在推理中从不被激活，分别对应 67 GB、23 GB、70 GB 的显存浪费。</p> <hr/> <h2 id="3-现有方案为什么不够好">3. 现有方案为什么不够好</h2> <h3 id="31-设计空间概览">3.1 设计空间概览</h3> <p>如下图，MoE serving 的设计空间分两大类：</p> <p><img src="/assets/img/posts/2502_05370/003_Figure_2.png" alt="MoE serving 设计空间"/></p> <ul> <li><strong>Lossy serving</strong>：压缩、剪枝、量化——省显存，但损精度</li> <li><strong>Lossless serving</strong>：卸载权重，保精度——这是 FineMoE 的赛道</li> </ul> <p>Lossless offloading 里，有几个代表性方案：</p> <ul> <li><strong>DeepSpeed-Inference</strong>：按层卸载，没有 expert 预取，每次都需要 on-demand 加载，延迟最差</li> <li><strong>Mixtral-Offloading</strong>：用 LRU 缓存 + 投机式预取，同步预取会拖慢推理</li> <li><strong>MoE-Infinity</strong>：追踪 request 级别的 expert 激活矩阵，异步预取，但预测粒度太粗</li> <li><strong>ProMoE</strong>：训练预测器来预测 expert，准确率高但需要训练，开销大</li> </ul> <p>这些方案的问题，一句话总结：<strong>它们都在 request 级别（粗粒度）追踪 expert 激活模式，而不是 iteration 级别（细粒度）</strong>。</p> <h3 id="32-粗粒度-vs-细粒度信息熵说明一切">3.2 粗粒度 vs 细粒度：信息熵说明一切</h3> <p>为什么粗粒度不好用？论文用 Shannon 熵量化了这个问题。</p> <p>如下图，分别展示了粗粒度（request 级别）和细粒度（iteration 级别）的 expert 激活热图：</p> <p><img src="/assets/img/posts/2502_05370/004_a.png" alt="粗粒度 vs 细粒度 expert 激活热图及熵分析"/></p> <p><strong>核心结论：粗粒度的 expert 激活熵显著高于细粒度</strong>，也就是说粗粒度的模式更难预测。更致命的是，随着 decode 迭代次数增加，request 级别的熵还会持续上升——因为越来越多的 expert 激活信息被聚合进去，模式越来越”平均化”，越来越难以区分。</p> <p>如下图，细粒度 offloading 的 expert hit rate 在不同 prefetch distance 下都显著高于粗粒度：</p> <p><img src="/assets/img/posts/2502_05370/007_Figure_4.png" alt="细粒度 vs 粗粒度的 expert hit rate 对比"/></p> <p>这个分析直接支撑了 FineMoE 的核心论点：<strong>要想准确预测 expert 激活，必须做细粒度的 iteration 级别分析，而不是 request 级别</strong>。</p> <hr/> <h2 id="4-finemoe-的方案">4. FineMoE 的方案</h2> <p>FineMoE 的设计围绕三个目标展开：</p> <ol> <li>低显存占用 + 低推理延迟（打破 trade-off）</li> <li>最小化 expert miss（减少 on-demand 加载）</li> <li>自适应不同 MoE 模型和不同 prompt（不搞 one-fits-all）</li> </ol> <h3 id="41-整体架构">4.1 整体架构</h3> <p>如下图，FineMoE 由三个核心组件构成：</p> <p><img src="/assets/img/posts/2502_05370/008_Figure_5.png" alt="FineMoE 系统架构和工作流"/></p> <ul> <li><strong>Expert Map Store</strong>：存储历史 expert 激活模式（以 expert map 为单位）</li> <li><strong>Expert Map Searcher</strong>：收到新 prompt 时，搜索最相似的历史 expert map</li> <li><strong>Expert Cache</strong>：GPU 上缓存 expert 权重，管理 prefetch 和 eviction</li> </ul> <p>五步工作流：</p> <ol> <li>收集推理上下文（语义 embedding + 历史 expert 激活轨迹）</li> <li>搜索最匹配的 expert map</li> <li>根据搜索结果决定 prefetch 哪些 expert</li> <li>推理执行（hit → 直接用；miss → on-demand 加载）</li> <li>更新 Expert Map Store</li> </ol> <h3 id="42-expert-map细粒度的核心数据结构">4.2 Expert Map：细粒度的核心数据结构</h3> <p><strong>Expert Map 是 FineMoE 的核心创新</strong>。</p> <p>传统方案（如 MoE-Infinity）只记录 expert 的 hit count（某个 expert 在 request 里被激活了多少次）。FineMoE 不一样——它记录每个 iteration 里，gate 网络输出的<strong>完整概率分布</strong>。</p> <p>如下图，expert map 记录了每一层 gate 输出的概率分布：</p> <p><img src="/assets/img/posts/2502_05370/009_Figure_6.png" alt="Expert Map 数据结构示意"/></p> <p>具体来说，第 i 次 iteration 的 expert map 定义为：</p> \[\text{map}_i = \{\mathbf{P}_1^{(i)}, \mathbf{P}_2^{(i)}, \ldots, \mathbf{P}_L^{(i)}\}\] <p>其中 $\mathbf{P}_l^{(i)} \in \mathbb{R}^J$ 是第 l 层 gate 网络在第 i 次 iteration 的输出概率分布，$J$ 是 expert 总数。</p> <p>和只记录 hit count 相比，expert map 有两个优势：</p> <ul> <li><strong>粒度更细</strong>：iteration 级别，不是 request 级别</li> <li><strong>信息更丰富</strong>：完整概率分布，不只是 0/1 的 hit/miss</li> </ul> <h3 id="43-双路搜索语义--轨迹">4.3 双路搜索：语义 + 轨迹</h3> <p>知道了 expert map 长什么样，下一个问题是：给定一个新 prompt，怎么找到最相似的历史 expert map？</p> <p>FineMoE 设计了两路搜索，分别对应推理的不同阶段：</p> <p>如下图，两路搜索的工作流：</p> <p><img src="/assets/img/posts/2502_05370/010_Figure_7.png" alt="Expert Map 搜索工作流（语义搜索 + 轨迹搜索）"/></p> <p><strong>语义搜索（用于前 d 层）</strong></p> <p>在 prefill 阶段开始时，还没有任何 expert 激活历史，没法用轨迹。此时用 embedding 层的输出（语义 embedding）来查找相似的历史 prompt，找到对应的 expert map，指导前 d 层（d 是 prefetch distance）的 expert prefetch。</p> <p>相似度用 cosine similarity 计算：</p> \[\text{score}_{x,y}^{\text{sem}} = \frac{\text{sem}_x^{\text{new}} \cdot \text{sem}_y^{\text{old}}}{\|\text{sem}_x^{\text{new}}\| \cdot \|\text{sem}_y^{\text{old}}\|}\] <p><strong>轨迹搜索（用于第 d+1 层之后）</strong></p> <p>从第 d+1 层开始，已经有了前面若干层的 expert 激活概率轨迹。这时用当前轨迹去匹配历史的 expert map，预测后续层的 expert 激活。同样是 cosine similarity：</p> \[\text{score}_{x,y}^{\text{traj}} = \frac{\text{map}_x^{\text{new}} \cdot \text{map}_y^{\text{old}}}{\|\text{map}_x^{\text{new}}\| \cdot \|\text{map}_y^{\text{old}}\|}\] <p>如下图，两路搜索的 Pearson 相关系数分析证明，不管是语义相似度还是轨迹相似度，与 expert hit rate 都有显著的正相关：</p> <p><img src="/assets/img/posts/2502_05370/012_Figure_9.png" alt="语义/轨迹相似度与 expert hit rate 的 Pearson 相关系数"/></p> <p>这个分析很关键——它从理论上说明了”用相似 prompt 的历史 expert map 来指导当前的 expert prefetch”这个思路是站得住脚的。</p> <h3 id="44-自适应-prefetch相似度越高prefetch-越少">4.4 自适应 prefetch：相似度越高，prefetch 越少</h3> <p>找到最相似的历史 expert map 后，怎么决定 prefetch 哪些 expert？</p> <p><strong>FineMoE 根据相似度分数动态调整预取数量</strong>，设计了一个动态阈值 $\delta_l$：</p> \[\delta_l = \text{Clip}(1 - \text{score}, 0, 1)\] <p>逻辑很直观：</p> <ul> <li>相似度高 → score 接近 1 → $\delta$ 接近 0 → 按概率选少量高把握的 expert prefetch</li> <li>相似度低 → score 接近 0 → $\delta$ 接近 1 → 多 prefetch 一些以对冲不确定性</li> </ul> <p>然后按概率从高到低累加，直到超过 $\delta$ 为止，这些 expert 就是要 prefetch 的集合。</p> <h3 id="45-异步架构搜索不阻塞推理">4.5 异步架构：搜索不阻塞推理</h3> <p>现有方案（如 MoE-Infinity）的一个大问题是同步预取——要等 expert 预测和 prefetch 完成后才能继续 forward，等待时间直接加进延迟里。</p> <p>FineMoE 用 <strong>Publisher-Subscriber 异步架构</strong>解决了这个问题：</p> <ul> <li>推理进程负责向 Expert Map Store 发布上下文数据（语义 embedding、expert 概率分布）</li> <li>Expert Map Searcher 订阅数据，异步进行相似度计算和 expert prefetch</li> <li>两个过程并行，互不阻塞</li> </ul> <p>系统测量结果：<strong>异步操作引入的额外延迟不超过 50ms（约占单次 iteration 总延迟的 1%）</strong>，基本可以忽略不计。</p> <h3 id="46-expert-map-store-去重保持多样性">4.6 Expert Map Store 去重：保持多样性</h3> <p>Expert Map Store 有容量限制，不能无限存。当新的 iteration 数据进来时，FineMoE 计算它与历史数据的”冗余度”，用两路相似度的加权组合：</p> \[\text{RDY}_{x,y} = \frac{d}{L} \cdot \text{score}_{x,y}^{\text{sem}} + \frac{L-d}{L} \cdot \text{score}_{x,y}^{\text{traj}}\] <p>冗余度高的旧 map 会被新 map 替换，保证 store 里的 expert map 尽量多样、有代表性。</p> <p>论文还做了理论分析：保持至少 $\frac{1}{2}LJ \ln(LJ)$ 个 expert map，可以保证 98% 的相似度下界（即对于任意新 prompt，都能找到至少 98% 相似的历史 map）。实测下来，<strong>1K 大小的 store 足够，对应不到 200 MB CPU 内存</strong>。</p> <hr/> <h2 id="5-实验设置和结果">5. 实验设置和结果</h2> <h3 id="51-实验设置">5.1 实验设置</h3> <ul> <li><strong>硬件</strong>：6 × NVIDIA RTX 3090（24GB 显存），PCIe 4.0 互联，AMD Ryzen Threadripper PRO 3955WX + 480 GB CPU 内存</li> <li><strong>模型</strong>：Mixtral-8×7B、Qwen1.5-MoE、Phi-3.5-MoE</li> <li><strong>数据集</strong>：LMSYS-Chat-1M（真实用户对话）、ShareGPT</li> <li><strong>Baseline</strong>：DeepSpeed-Inference、Mixtral-Offloading、ProMoE、MoE-Infinity</li> <li><strong>指标</strong>：TTFT（prefill 阶段）、TPOT（decode 阶段）、expert hit rate</li> </ul> <h3 id="52-整体性能offline-serving">5.2 整体性能（Offline Serving）</h3> <p>如下图，FineMoE 在三个模型、两个数据集上全面领先：</p> <p><img src="/assets/img/posts/2502_05370/013_Figure_10.png" alt="Prefill 和 Decode 阶段整体性能对比"/></p> <p>核心数字：</p> <ul> <li>相比 DeepSpeed-Inference：<strong>TTFT 平均降低 74%，TPOT 平均降低 46%</strong></li> <li>相比 MoE-Infinity（最强 baseline）：<strong>TTFT 平均降低 53%，TPOT 平均降低 22%</strong></li> <li>Expert hit rate 相比 Mixtral-Offloading/ProMoE/MoE-Infinity 分别提升 <strong>14%/37%/68%</strong></li> </ul> <p>离谱的是，即使和”完全不卸载（No-offload）”的全量 GPU 方案相比，FineMoE 在显存大幅节省的同时，延迟也没有显著恶化。</p> <h3 id="53-在线服务online-serving">5.3 在线服务（Online Serving）</h3> <p>如下图，在模拟真实在线流量（Azure LLM 推理 trace，256 个请求，2.91 req/s）下，FineMoE 的端到端请求延迟 CDF 曲线明显优于所有 baseline：</p> <p><img src="/assets/img/posts/2502_05370/014_Figure_11.png" alt="在线服务请求延迟 CDF"/></p> <p>特别是 P90/P99 尾延迟，FineMoE 的优势更明显。这对实际线上服务来说非常关键——SLA 通常是看尾延迟的。</p> <h3 id="54-不同显存限制下的表现">5.4 不同显存限制下的表现</h3> <p>如下图，在不同 GPU 显存 cache 限制（6 GB 到 96 GB）下，FineMoE 始终保持领先：</p> <p><img src="/assets/img/posts/2502_05370/015_Figure_12.png" alt="不同显存限制下的 TPOT 对比"/></p> <p><strong>在显存极度受限（6 GB cache）时，FineMoE 相比 MoE-Infinity 的 TPOT 降低 29%</strong>。随着显存增大，各方案差距缩小（因为大家都能缓存更多 expert），但 FineMoE 始终在帕累托前沿。</p> <h3 id="55-高端-gpua100上的表现">5.5 高端 GPU（A100）上的表现</h3> <p>如下图，在 80 GB HBM2e A100 上测试，FineMoE 的相对优势有所收窄（因为高端 GPU 的 expert on-demand 加载速度更快），但<strong>仍然全面优于所有 baseline</strong>：</p> <p><img src="/assets/img/posts/2502_05370/016_Figure_13.png" alt="A100 上的性能对比"/></p> <p>这也说明 FineMoE 的优势主要来自”更准的预测”（expert hit rate），而不是依赖特定硬件。</p> <h3 id="56-消融实验">5.6 消融实验</h3> <p>如下图，逐步开启 FineMoE 各个设计组件后，expert hit rate 的变化：</p> <p><img src="/assets/img/posts/2502_05370/017_a.png" alt="消融实验：不同 expert 追踪方式的 hit rate 对比"/></p> <p>从 Speculate → Hit count → Map(T) → Map(T+S) → Map(T+S+δ)，hit rate 逐级提升：</p> <ul> <li>投机预测（Mixtral-Offloading 的方式）在 prefetch distance 小时效果不错，但 distance 增大后急剧退化</li> <li>request 级别 hit count（MoE-Infinity 的方式）最差</li> <li>加上轨迹搜索（T）→ 加上语义搜索（T+S）→ 加上动态阈值（T+S+δ），每一步都有提升</li> </ul> <p><strong>每个设计决策都是有贡献的，没有凑数的模块。</strong></p> <hr/> <h2 id="6-一些值得关注的设计细节">6. 一些值得关注的设计细节</h2> <p><strong>为什么不用 NN-based 预测器？</strong></p> <p>ProMoE 用了训练好的 predictor 来预测每层的 expert。论文给出了不用 NN 的理由：</p> <ol> <li>推理延迟高，predictor 本身的延迟有时和 MoE 推理延迟本身差不多</li> <li>需要 per-layer 训练，每次换模型或 workload 都要重新训</li> <li>参数量大，占用额外 GPU 显存</li> <li>和细粒度设计不兼容：在细粒度数据上训练 NN 收敛困难</li> </ol> <p>FineMoE 的 heuristic 方案（相似度搜索）不需要训练，开箱即用。</p> <p><strong>prefetch distance 怎么选？</strong></p> <p>三个模型的最优 prefetch distance 分别设为：</p> <ul> <li>Mixtral-8×7B：d = 3</li> <li>Qwen1.5-MoE：d = 6</li> <li>Phi-3.5-MoE：d = 4</li> </ul> <p>distance 太小，异步操作来不及完成，还是会引入等待延迟；distance 太大，预测依据不足，hit rate 下降。需要 profile 一下找到最优点。</p> <hr/> <h2 id="7-总结与个人感受">7. 总结与个人感受</h2> <p>FineMoE 这篇工作的思路很清晰：</p> <ol> <li><strong>发现了一个被忽视的问题</strong>：现有 MoE offloading 方案都在用粗粒度（request 级别）的 expert 激活模式，而 iteration 级别的细粒度模式预测准确率更高</li> <li><strong>设计了一个轻量的数据结构</strong>（expert map）来追踪细粒度模式，不需要额外训练</li> <li><strong>双路搜索</strong>（语义 + 轨迹）覆盖了 prefill 初期没有历史轨迹的盲区</li> <li><strong>异步架构</strong>让搜索和 prefetch 不阻塞推理</li> <li><strong>动态阈值</strong>让 prefetch 数量自适应搜索置信度</li> </ol> <p>实验结果很扎实，47% 的延迟降低、39% 的 hit rate 提升，而且 Expert Map Store 只需要不到 200 MB 的 CPU 内存，系统 overhead 也控制在 1% 以内。</p> <p>这类工作的价值在于：<strong>不改模型权重，不需要训练，直接插入现有 serving 框架（HuggingFace Transformers）</strong>，落地成本低，工程可行性强。</p> <p>对我们做 MoE 推理优化的人来说，FineMoE 提供了一个很好的视角：<strong>粒度是关键</strong>。很多时候系统设计在”粗粒度够用了”的假设下偷懒，但 FineMoE 用数据证明了细粒度确实有用，而且代价并不大。</p> <p>感兴趣的同学可以去看一下原文，目前代码似乎还没有开源（至少文中没有提到代码仓库），期待后续放出。</p> <p>欢迎评论区交流～</p> <hr/> <p><em>作者：Hanfei Yu, Xingqi Cui, Hong Zhang, Hao Wang, Hao Wang（Stevens Institute of Technology / Rice University / University of Waterloo / Rutgers University）</em> <em>发表于：EuroSys ‘26（2026年4月）</em></p>]]></content><author><name></name></author><category term="LLM"/><category term="MoE"/><category term="论文解读"/><category term="推理优化"/><category term="Expert Offloading"/><summary type="html"><![CDATA[延迟降47%！FineMoE如何用「细粒度」打破MoE推理的显存-延迟死局]]></summary></entry><entry><title type="html">LoRA fine-tune吞吐量提升1.96倍！LoRAFusion如何把内存带宽浪费和pipeline bubble一起干掉</title><link href="https://marsggbo.github.io/blog/2026/2510_00206/" rel="alternate" type="text/html" title="LoRA fine-tune吞吐量提升1.96倍！LoRAFusion如何把内存带宽浪费和pipeline bubble一起干掉"/><published>2026-05-14T00:00:00+00:00</published><updated>2026-05-14T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/2510_00206</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/2510_00206/"><![CDATA[<h1 id="lora-fine-tune吞吐量提升196倍lorafusion如何把内存带宽浪费和pipeline-bubble一起干掉">LoRA fine-tune吞吐量提升1.96倍！LoRAFusion如何把内存带宽浪费和pipeline bubble一起干掉</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2510.00206">LoRAFusion: Efficient LoRA Fine-Tuning for LLMs</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>做过 LLM fine-tune 的同学应该都知道，LoRA 是当前 PEFT 方法里最主流的选择。只引入少量低秩参数，显存省下来了，效果也不差。</p> <p>但你有没有想过：<strong>LoRA 参数只有全参数的 0.3% 不到，为什么加上 LoRA 之后训练速度反而掉了 40%？</strong></p> <p>这个数字是真实测出来的，不是理论估算。Fine-tune LLaMa-3.1-70B，加上 LoRA（rank=16）之后，训练 throughput 直接下降 40%。本质上是一个让人有些细思极恐的问题：参数只加了 0.3%，计算量也没增加多少，但速度掉了这么多，钱花在哪了？</p> <p>今天这篇 EuroSys ‘26 的工作 LoRAFusion 把这个问题剖析得很清楚，顺带还解决了另一个在多任务 fine-tune 场景里被严重忽视的问题：<strong>多个 LoRA job 并行时的 pipeline bubble 和 GPU 负载不均衡</strong>。</p> <p>代码已开源：<a href="https://github.com/CentML/lorafusion">github.com/CentML/lorafusion</a></p> <hr/> <h2 id="2-背景lora-的参数结构和-fine-tune-痛点">2. 背景：LoRA 的参数结构和 fine-tune 痛点</h2> <p>先简单回顾一下 LoRA 的公式。对于一个预训练权重矩阵 $W \in \mathbb{R}^{k \times n}$，LoRA 注入两个低秩矩阵 $A \in \mathbb{R}^{k \times r}$ 和 $B \in \mathbb{R}^{r \times n}$（$r \ll k, n$）：</p> \[Y = XW + \alpha \cdot (XA)B = XW + \alpha \cdot SB\] <p>其中 $S = XA$ 是中间结果（维度小），$\alpha$ 是缩放系数。fine-tune 过程中 $W$ 冻结不更新，只更新 $A$ 和 $B$。</p> <p>以 LLaMa-3.1-70B 为例：</p> <ul> <li>全参数 fine-tune：仅模型状态就需要约 <strong>1120 GB</strong> 显存（参数、梯度、优化器状态）</li> <li>LoRA（rank=16）fine-tune：只需约 <strong>142 GB</strong> 显存，额外参数量仅占 <strong>0.29%</strong></li> </ul> <p>显存省了将近 8 倍，但训练速度呢？</p> <p>如下图，LoRA linear 层和普通 frozen linear 层的吞吐量对比：</p> <p><img src="/assets/img/posts/2510_00206/003_Figure_3.png" alt="LoRA vs Frozen Linear 前向/反向吞吐量对比"/></p> <p><strong>不管 token 数和 rank 是多少，LoRA linear 比 frozen linear 慢了约 40%（forward）和 36%（backward）</strong>。torch.compile 几乎没有帮助。这是为什么？</p> <hr/> <h2 id="3-问题分析罪魁祸首是内存带宽不是计算量">3. 问题分析：罪魁祸首是内存带宽，不是计算量</h2> <h3 id="31-内存访问是瓶颈">3.1 内存访问是瓶颈</h3> <p>如下图，一个 LoRA linear 层的运行时细分：</p> <p><img src="/assets/img/posts/2510_00206/005_Figure_4.png" alt="LoRA 模块运行时分布分析"/></p> <p>运行时主要分三类：</p> <ol> <li><strong>Frozen GEMM</strong>（$XW$）：compute-bound，占 59%/60%（前向/反向）</li> <li><strong>LoRA GEMM</strong>（$XA$ 和 $SB$）：memory-bound，前向占 10.76%，反向占 20.37%</li> <li><strong>Element-wise 操作</strong>（dropout、乘法、加法）：memory-bound，前向占 30.46%，反向占 17.49%</li> </ol> <p>LoRA 的下投影 $S = XA$ 的算术强度 $\mathbb{I}$ 是：</p> \[\mathbb{I} = \frac{1}{\frac{1}{r} + \frac{1}{n} + \frac{1}{m}} \ll \mathbb{B}\] <p>由于 $r$ 很小（比如 16），这个算术强度远低于 H100 的硬件 balance 点（FP16 约 295），确认是 <strong>memory-bandwidth-bound</strong>。</p> <p>更直接的数据：用 NVIDIA Nsight Compute 测量，<strong>LoRA 模块的 GPU global memory 读写流量比 frozen linear 增加了 2.64 倍</strong>。</p> <p>结论很明确：LoRA 的 runtime overhead 根本原因是<strong>大 activation tensor 的重复内存访问</strong>，不是 FLOPs 增加。</p> <h3 id="32-multi-lora-的机会被忽视了">3.2 Multi-LoRA 的机会被忽视了</h3> <p>现实中的 fine-tune 场景经常同时跑多个 LoRA job——超参搜索、多任务适配、多租户服务。这些 job 共用同一个 base model，显存开销很小，完全可以放在同一批 GPU 上跑。</p> <p>但现有系统（比如 mLoRA）基本是各跑各的，错过了两个重要的优化机会：</p> <p>如下图，随 global batch size 增大，理想吞吐量的提升幅度：</p> <p><img src="/assets/img/posts/2510_00206/006_Figure_5.png" alt="增大 global batch size 对 LLaMa-3.1-70B 吞吐量的影响"/></p> <p><strong>机会 1：增大 global batch size，降低分布式并行 overhead</strong></p> <p>把多个 job 的 sample 合并，global batch size 从 4 增到 32，FSDP 的理想吞吐量提升 84%，Pipeline Parallelism 提升 45%。原因是更大的 batch 能更好地 overlap 通信和计算（FSDP），以及填满 pipeline stages 减少 bubble（PP）。</p> <p><strong>机会 2：跨 job 打包样本，缓解 GPU 负载不均衡</strong></p> <p>实际数据集的 sequence length 分布差异很大（XSum/CNN/WikiSum 各不相同），导致每个 microbatch 的 token 数波动剧烈。如下图，同一 microbatch_size=4 下，token 数在不同样本间差异悬殊：</p> <p>这种不均衡在 FSDP 下导致各 rank 等待最慢的那个，在 PP 下形成更多 bubble。如果把多个 job 的样本混合打包，可以有效平衡每个 microbatch 的 token 数，<strong>理论上可以带来最高 2.28 倍的吞吐量提升</strong>。</p> <p>如下图，实际测量到的 LoRA fine-tune 相比理想固定长度场景的性能下降（最高约 30%）：</p> <p><img src="/assets/img/posts/2510_00206/008_Figure_7.png" alt="实际 LoRA fine-tune vs 理想场景的性能差距"/></p> <p><strong>这是整个 multi-LoRA fine-tune 领域的核心矛盾</strong>：现有系统要么不支持多 job 并行，要么支持了但忽略了 load balance 和 kernel 层面的 redundant memory access。</p> <hr/> <h2 id="4-lorafusion-的方案">4. LoRAFusion 的方案</h2> <p>两个层面的优化，对应两个问题：</p> <p>如下图，LoRAFusion 整体系统架构：</p> <p><img src="/assets/img/posts/2510_00206/009_Figure_8.png" alt="LoRAFusion 系统架构和工作流"/></p> <h3 id="41-kernel-层面fusedlora-和-fusedmultilora">4.1 Kernel 层面：FusedLoRA 和 FusedMultiLoRA</h3> <p><strong>核心思路：图分裂（Graph Splitting）+ 横向融合</strong></p> <p>一个直觉上的做法是把整个 LoRA 计算图 fuse 成一个 kernel。但这有两个问题：</p> <ul> <li>Frozen GEMM（$XW$）是 compute-bound，需要精心 tiling，如果和其他操作混在一起，共享的寄存器/shared memory 会破坏最优 tiling</li> <li>$XA$ 和 $(XA)B$ 之间有 producer-consumer 依赖，naive fusion 要么 recompute，要么跨 thread block 同步，都会引入额外开销</li> </ul> <p>LoRAFusion 的解法是<strong>在中间张量 $S = XA$（维度小，rank 维度）处分裂计算图</strong>：</p> <p>如下图，三种融合策略的对比（recompute / synchronization / graph-split）：</p> <p><img src="/assets/img/posts/2510_00206/010_Figure_9.png" alt="LoRA 融合策略对比：全图融合 vs 分裂图融合"/></p> <p>$S$ 的大小是 $m \times r$（$r$ 很小），读写它代价很低。在 $S$ 处分裂后：</p> <ul> <li>$S$ 之前的操作（dropout + $XA$）fuse 成一个 kernel，消除大 activation tensor 的重复加载</li> <li>$S$ 之后的操作（$XW$ 和 $\alpha \cdot SB$）fuse 成另一个 kernel，避免 frozen GEMM 被干扰</li> </ul> <p>如下图，FusedLoRA 的 kernel 设计（前向和反向）：</p> <p><img src="/assets/img/posts/2510_00206/011_Figure_10.png" alt="FusedLoRA kernel 设计示意图"/></p> <p>具体实现：</p> <ul> <li><strong>Forward 前向</strong>：① dropout + 下投影（$X \to S$）fuse 成一个 kernel；② frozen GEMM（$XW$）和 LoRA 上投影（$\alpha SB$）fuse 成一个 kernel，直接累加输出，消除一次大张量的读写</li> <li><strong>Backward 反向</strong>：③ fuse $dS$ 和 $dB$ 的计算，消除 $dY$ 的重复加载；⑤ fuse base model 的梯度计算和 LoRA path 操作，消除 partial output 梯度的重复读写</li> </ul> <p><strong>内存流量实测减少 34%~37%</strong>，不需要 recompute，不需要 synchronization。</p> <p>接下来，扩展到 FusedMultiLoRA，支持同时处理来自不同 job 的 adapter：</p> <p>如下图，FusedMultiLoRA 的 tile 级 routing 机制：</p> <p><img src="/assets/img/posts/2510_00206/012_Figure_11.png" alt="FusedMultiLoRA：tile 级 adapter 路由"/></p> <p>每个 input tile 带一个 adapter ID，kernel 执行时动态查表，加载对应的 $A$ 和 $B$ 矩阵并应用正确的 dropout 和 scaling。Base model 的计算在所有 token 间共享，adapter-specific 逻辑按 tile 独立处理。反向传播时，梯度同样按 adapter ID 路由回各自的 adapter，互不干扰。</p> <h3 id="42-scheduling-层面multi-lora-自适应调度">4.2 Scheduling 层面：Multi-LoRA 自适应调度</h3> <p>这是 LoRAFusion 的另一个核心贡献，解决的是如何把多个 job 的样本打包成均衡的 microbatch。</p> <p>如下图，Multi-LoRA 调度器的三步工作流：</p> <p><img src="/assets/img/posts/2510_00206/013_Figure_12.png" alt="Multi-LoRA 调度工作流（adapter 分组 + MILP 打包 + 跨 batch 合并）"/></p> <p>整个调度分三步：</p> <p><strong>Step 1：Adapter 分组（Bubble Lemma）</strong></p> <p>Pipeline 并行下有一个依赖约束：adapter $i$ 的 global batch $j$ 的 sample，在它的 backward 完成之前，同一 adapter 的 global batch $j+1$ 的 sample 不能开始 backward。简单说就是：同一个 adapter 的相邻两个 global batch，在时间线上必须间隔至少 $S-1$ 个 microbatch（$S$ 是 pipeline stages 数）。</p> <p>LoRAFusion 通过把 adapter 按 sequence length 分布分组，并在相邻组之间保持这个间隔，来满足 Bubble Lemma。组内采用”头尾配对”——长 sequence 和短 sequence 的 adapter 配对，提升负载均衡。</p> <p><strong>Step 2：两阶段 MILP bin-packing</strong></p> <p>组内的样本需要被打包到 microbatch 里（每个 microbatch 有 token 容量上限）。目标是：①最小化 microbatch 数；②让最空的 microbatch 尽量空（为后续合并留空间）。</p> <p>这是一个 bin-packing 问题，用两阶段 MILP 求解：</p> <ul> <li>Stage 1：最小化 bin（microbatch）数 $B^*$</li> <li>Stage 2：固定 $B = B^*$，最大化最空的 bin 的空闲空间</li> </ul> <p>MILP 设置超时，超时则 fallback 到贪心算法。多个 global batch 的打包并行用 multiprocessing 执行。</p> <p><strong>Step 3：跨 batch 合并</strong></p> <p>每个 global batch 的最后一个 microbatch 往往是欠填充的（last microbatch 问题）。LoRAFusion 在满足 Bubble Lemma 的前提下，把下一个 global batch 的开头 token 挪进来填充，减少 pipeline bubble。</p> <p><strong>系统异步性保障</strong>：调度时间线性增长（每个样本约 4ms CPU 时间），但调度本身和 GPU 训练并行执行，overhead 被隐藏在 GPU 计算时间内。</p> <hr/> <h2 id="5-实验设置和结果">5. 实验设置和结果</h2> <h3 id="51-实验设置">5.1 实验设置</h3> <ul> <li><strong>GPU</strong>：NVIDIA H100（80GB，NVLink）和 L40S（48GB，PCIe）</li> <li><strong>模型</strong>：LLaMa-3.1-8B、Qwen-2.5-32B、LLaMa-3.1-70B</li> <li><strong>数据集</strong>：XSum、CNN/DailyMail、WikiSum（摘要任务）</li> <li><strong>实验配置</strong>：同时 fine-tune 4 个 LoRA adapter</li> <li><strong>Baseline</strong>：Megatron-LM（FSDP 和 PP）、mLoRA</li> </ul> <h3 id="52-端到端结果">5.2 端到端结果</h3> <p>如下图，在 H100 上训练 4 个 LoRA adapter 的端到端 throughput 对比：</p> <p><img src="/assets/img/posts/2510_00206/015_Figure_14.png" alt="H100 上端到端训练吞吐量对比"/></p> <p><strong>核心数字：</strong></p> <ul> <li>相比 Megatron-LM：最高 <strong>1.96×</strong>，平均 <strong>1.47×</strong> 提升</li> <li>相比 mLoRA（SOTA multi-LoRA）：最高 <strong>1.46×</strong>，平均 <strong>1.29×</strong> 提升</li> </ul> <p>不同模型的收益规律：</p> <ul> <li><strong>LLaMa-3.1-8B（单卡）</strong>：平均 1.26×，主要来自 FusedLoRA kernel，因为单卡不存在 pipeline bubble</li> <li><strong>Qwen-2.5-32B / LLaMa-3.1-70B（多卡分布式）</strong>：平均 1.42×/1.64×，调度层面的优化贡献更大</li> <li><strong>WikiSum 数据集</strong>（sequence length 方差极大）：LoRAFusion 在 baseline 出现 OOM 的情况下仍能稳定运行</li> </ul> <p>如下图，在 L40S 上的对比结果：</p> <p><img src="/assets/img/posts/2510_00206/016_Figure_15.png" alt="L40S 上端到端训练吞吐量对比"/></p> <p>L40S 的绝对提升略小（单卡显存有限，限制了 batch size），但 LoRAFusion 依然保持领先。</p> <h3 id="53-扩展性">5.3 扩展性</h3> <p>如下图，在 4、8、16 卡 H100 上的扩展性对比（DP scaling vs Job scaling）：</p> <p><img src="/assets/img/posts/2510_00206/017_Figure_16.png" alt="LoRAFusion 在 4/8/16 张 H100 上的扩展性"/></p> <p><strong>关键发现：Job-level scaling（用更多 GPU 跑更多并行 job）优于 DP scaling（用更多 GPU 扩展同一 job）</strong>，在 8 和 16 卡时分别高出 1.18× 和 1.25×。</p> <p>原因很直接：更多的 job 提供了更大的 global batch，调度灵活性更高，pipeline bubble 更少。LoRAFusion 同时兼容 DP scaling，在 DP scaling 场景下也比 Megatron-LM 快 1.78×，比 mLoRA 快 1.50×。</p> <h3 id="54-kernel-性能">5.4 Kernel 性能</h3> <p>如下图，FusedLoRA 和 FusedMultiLoRA kernel 的吞吐量对比：</p> <p><img src="/assets/img/posts/2510_00206/018_Figure_17.png" alt="FusedLoRA kernel 前向/反向吞吐量对比"/></p> <ul> <li><strong>FusedLoRA</strong>：平均 <strong>1.27×</strong>（最高 1.39×）</li> <li><strong>FusedMultiLoRA</strong>：平均 <strong>1.17×</strong>（最高 1.24×）</li> </ul> <p>FusedMultiLoRA 在反向传播时需要额外累加多个 adapter 的梯度，有轻微 overhead，但整体依然显著优于 baseline。</p> <p>如下图，内存流量实测（Nsight Compute）：</p> <p><img src="/assets/img/posts/2510_00206/020_Figure_19.png" alt="GPU DRAM 内存流量对比（不同 kernel）"/></p> <p><strong>DRAM 总流量减少 34%~37%</strong>，和理论分析完全对应。</p> <h3 id="55-pipeline-bubble-消减">5.5 Pipeline Bubble 消减</h3> <p>如下图，不同 adapter 数量下 LoRAFusion 的 pipeline bubble 比率：</p> <p><img src="/assets/img/posts/2510_00206/021_Figure_20.png" alt="不同方法下的 pipeline bubble 比率"/></p> <ul> <li>Megatron-LM（单 adapter）：<strong>48.79%</strong></li> <li>mLoRA（4 adapter）：<strong>34.11%</strong></li> <li>LoRAFusion（4 adapter）：<strong>11.09%</strong></li> </ul> <p>bubble 从将近一半降到一成，调度的效果非常明显。</p> <h3 id="56-speedup-分解">5.6 Speedup 分解</h3> <p>如下图，LoRAFusion 在 LLaMa-3.1-70B（4 H100）上的加速分解：</p> <p><img src="/assets/img/posts/2510_00206/023_Figure_22.png" alt="LLaMa-3.1-70B 上各组件的加速贡献分解"/></p> <p>从 Megatron-LM baseline 开始，各组件的贡献：</p> <table> <thead> <tr> <th>累加组件</th> <th>相对 Megatron-LM 加速</th> </tr> </thead> <tbody> <tr> <td>+ FusedLoRA</td> <td>1.13×</td> </tr> <tr> <td>+ Multi-LoRA Zero-Bubble PP</td> <td>1.50×</td> </tr> <tr> <td>+ FusedMultiLoRA</td> <td>1.72×</td> </tr> <tr> <td>+ Adaptive Scheduling</td> <td>2.05×</td> </tr> </tbody> </table> <p><strong>kernel fusion + pipeline 调度 + 负载均衡，三个维度缺一不可，组合起来才是最终的 2× 提升。</strong></p> <hr/> <h2 id="6-一些延伸思考">6. 一些延伸思考</h2> <p><strong>为什么 torch.compile 解决不了这个问题？</strong></p> <p>torch.compile 有 compiler-based fusion，但测试结果是对 forward pass 几乎零收益，backward 只有可以忽略的微小改善。原因是 LoRA 的计算图结构对 torch.compile 的 fusion 规则来说不够”友好”——大 activation tensor 在多个分支之间共享，编译器难以自动找到减少内存 traffic 的最优分裂点。LoRAFusion 的 graph-splitting 策略本质上是一个领域特定的 kernel 设计，需要对 LoRA 结构有深入理解。</p> <p><strong>Kernel 可以直接插入现有系统？</strong></p> <p>是的，FusedLoRA kernel 可以作为 plug-and-play 替换，直接接到 PEFT Library、LLaMa-Factory 等现有系统上。论文中验证了数值精度等价，不影响模型质量。这个”即插即用”的设计思路值得借鉴——<strong>优化和算法分离，降低落地门槛。</strong></p> <p><strong>LoRA 变体怎么办（DoRA/VeRA）？</strong></p> <p>论文讨论了可扩展性：这些变体的核心 LoRA 计算结构不变，只是在外面套了 prologue/epilogue 函数，LoRAFusion 的 kernel 可以通过添加这些函数来扩展。更通用的方案是集成进 torch.compile 的 compiler 框架，未来工作。</p> <hr/> <h2 id="7-总结">7. 总结</h2> <p>LoRAFusion 找到了两个被现有系统忽视的问题，并给出了系统性的解法：</p> <ol> <li><strong>内存带宽浪费</strong>：LoRA 参数只有 0.3%，但大 activation tensor 的重复加载导致 40% 吞吐量下降。LoRAFusion 用 graph-splitting + 横向 kernel fusion 把内存流量减少 34%~37%</li> <li><strong>Multi-LoRA 机会未被利用</strong>：多 job 并行时的 pipeline bubble 和 load imbalance。LoRAFusion 用 Bubble Lemma 约束的 adapter 分组 + 两阶段 MILP bin-packing，把 bubble 从 48% 压到 11%</li> </ol> <p>两者结合，端到端吞吐量最高提升 1.96×（平均 1.47×），而且代码已经开源，kernel 部分可以直接插到现有系统里。</p> <p>对做 LLM fine-tune 的工程师来说，这篇工作的实用价值很直接。感兴趣的可以去试一下：<a href="https://github.com/CentML/lorafusion">github.com/CentML/lorafusion</a></p> <p>欢迎评论区交流～</p> <hr/> <p><em>作者：Zhanda Zhu, Qidong Su, Yaoyao Ding, Kevin Song, Shang Wang, Gennady Pekhimenko（University of Toronto / Vector Institute / NVIDIA）</em> <em>发表于：EuroSys ‘26（2026年4月）</em></p>]]></content><author><name></name></author><category term="LLM"/><category term="LoRA"/><category term="训练优化"/><category term="Kernel Fusion"/><category term="分布式训练"/><category term="论文解读"/><summary type="html"><![CDATA[LoRA fine-tune吞吐量提升1.96倍！LoRAFusion如何把内存带宽浪费和pipeline bubble一起干掉]]></summary></entry><entry><title type="html">KV Cache 的两层存储到底卡在哪？FAST’26 这篇论文给出了答案</title><link href="https://marsggbo.github.io/blog/2026/fast26-hu-shipeng/" rel="alternate" type="text/html" title="KV Cache 的两层存储到底卡在哪？FAST’26 这篇论文给出了答案"/><published>2026-05-14T00:00:00+00:00</published><updated>2026-05-14T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/fast26-hu-shipeng</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/fast26-hu-shipeng/"><![CDATA[<h1 id="kv-cache-的两层存储到底卡在哪fast26-这篇论文给出了答案">KV Cache 的两层存储到底卡在哪？FAST’26 这篇论文给出了答案</h1> <blockquote> <p>原文：<a href="https://www.usenix.org/system/files/fast26-hu-shipeng.pdf">Bidaw: Enhancing Key-Value Caching for Interactive LLM Serving via Bidirectional Computation–Storage Awareness</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有想过，像 Replika、Duolingo 这类依赖多轮对话的 LLM 应用，在底层到底有多”重”？</p> <p>用户问一句话，LLM 要回一段话。下一轮用户再问，LLM 不仅要理解新问题，还要”记住”之前说过的所有内容——这个”记住”在工程上的实现，就是把历史轮次产生的 <strong>KV tensor</strong> 全部加载进 GPU 重新计算。</p> <p>问题来了：GPU 显存就那么大，每轮对话产生的 KV 不可能一直堆在 GPU 里。所以业界的常规做法是，把历史 KV 缓存在一个<strong>两层存储</strong>（host memory + SSD）里，下次用到的时候再从存储里 load 进来。</p> <p>听起来很合理对吧？但现实是——<strong>用了两层存储之后，latency 能高出 3.8×，throughput 能跌掉一半。</strong></p> <p>这篇来自 FAST’26 的工作（清华大学团队）提出了 Bidaw，核心洞察是：现有方案里，<strong>compute engine 和两层存储完全互不知情</strong>，一个 I/O 盲调度 + 一个不懂对话规律的 eviction，凑在一起把性能搞塌了。</p> <hr/> <h2 id="2-背景多轮对话的-kv-缓存有多贵">2. 背景：多轮对话的 KV 缓存有多贵？</h2> <p>先交代下背景。</p> <p>多轮对话的 LLM serving，每一轮的计算都需要用到前面所有轮次的 KV tensor（Key 和 Value 矩阵）。如果这些 KV 不缓存，每次都要重算，开销是指数级上涨的。论文里用真实工业 workload 测了一下：</p> <blockquote> <p><strong>平均每个用户有 22.4 轮对话，冗余计算占到总计算量的 93.1%！</strong></p> </blockquote> <p>就是说，绝大多数 GPU 算力都在做无效的重复工作。</p> <p>为了解决这个问题，有两类方向：</p> <ul> <li><strong>分布式内存池</strong>（如基于 RDMA 的方案）：性能好，但需要专用硬件，成本高，很多垂直领域的公司根本部署不起</li> <li><strong>本地两层存储</strong>（host memory + SSD）：容量大、成本低，CachedAttention 和 FlashGen 是这条路上的代表工作</li> </ul> <p>下图是两层存储的基本架构——GPU 做计算，KV 从 host memory（快层）或 SSD（慢层）里 load 进来：</p> <p><img src="/assets/img/posts/fast26-hu-shipeng/fig_04_Figure_2.png" alt="两层存储 KV 缓存系统架构"/></p> <p>但问题是，<strong>这条路上的性能实际上被卡死了</strong>。</p> <p>论文在同一个 workload 上跑了一组对比实验：CachedAttention 和 FlashGen 的 response latency 比”理想情况下所有 KV 都放在 host memory 里”高出 <strong>3.8×</strong>，throughput 低 <strong>2.0×</strong>。</p> <p>这个 gap 不小，足以让人怀疑两层存储这条路是否走得通。</p> <hr/> <h2 id="3-根因分析compute-和-storage-各自为政">3. 根因分析：compute 和 storage 各自为政</h2> <p>论文花了大量篇幅刻画真实 workload 的特征，找出了<strong>两个根本问题</strong>，都指向同一个根因：<strong>compute engine 和两层存储完全不通信。</strong></p> <h3 id="31-kv-加载时间差异极大">3.1 KV 加载时间差异极大</h3> <p>不同请求的历史对话长度差异非常大，加上 host memory 和 SSD 之间带宽差了好几个量级，导致 KV loading time 的<strong>变异系数（CV）高于 90%</strong>。</p> <p><img src="/assets/img/posts/fast26-hu-shipeng/fig_06_Figure_8.png" alt="KV 加载时间变异系数极高"/></p> <p>现有的 I/O-oblivious 调度策略（FCFS）完全无视这件事。一个历史很长、KV 在 SSD 里的请求被排到最前面，GPU 就干等着——后面那些 KV 在 host memory 里、几十毫秒就能 load 完的请求，也只能傻等。这就是所谓的<strong>请求阻塞（request blocking）</strong>问题。</p> <h3 id="32-kv-访问时间局部性极差命中率惨不忍睹">3.2 KV 访问时间局部性极差，命中率惨不忍睹</h3> <p>多轮对话有个特性：用户问完一个问题，得等模型回答，然后再思考下一个问题。这个时间窗口里，服务系统会处理大量其他用户的请求。</p> <p>论文引入了”加权重用距离”（weighted reuse distance）——即两次访问同一用户 KV 之间，其他 KV 的总访问量。结果发现：<strong>80% 的 KV 访问的加权重用距离超过了 host memory 的容量（200GB）</strong>。</p> <p><img src="/assets/img/posts/fast26-hu-shipeng/fig_05_Figure_6.png" alt="KV 访问时间局部性极差及命中率"/></p> <p>换句话说，两次访问之间已经有太多其他用户的 KV 进来又出去了，传统的 LRU/FIFO 根本没法利用这种局部性。结果就是：即使 host memory 能容纳 40.1% 的 KV，命中率也只有大约 <strong>20%</strong>，大量请求的 KV 要从慢速 SSD 里 load。</p> <p>这也是整个行业在两层 KV 存储上<strong>面临的核心矛盾</strong>：容量层和性能层的带宽鸿沟本来就存在，再加上调度和 eviction 都不考虑对话语义，性能就彻底垮了。</p> <hr/> <h2 id="4-bidaw双向感知的-kv-缓存">4. Bidaw：双向感知的 KV 缓存</h2> <p>Bidaw 的核心思路很直接：<strong>让 compute engine 和 storage 互相知道对方在干什么</strong>。</p> <ul> <li>Compute engine 侧：调度时感知 KV 的 I/O latency，避免 blocking</li> <li>Storage 侧：利用模型回答的长度来预测下次 KV 被访问的时间，指导 eviction</li> </ul> <p>下图是 Bidaw 的系统架构：</p> <p><img src="/assets/img/posts/fast26-hu-shipeng/fig_06_Figure_9.png" alt="Bidaw 系统总体架构"/></p> <h3 id="41-io-aware-请求调度双队列--disk-hrrn">4.1 I/O-aware 请求调度：双队列 + Disk-HRRN</h3> <p><strong>双队列分离</strong></p> <p>Bidaw 把请求按 KV 所在存储层分成两个队列：</p> <ul> <li><strong>Ready Queue</strong>：KV 在 host memory 里，可以直接调度上 GPU</li> <li><strong>Preparing Queue</strong>：KV 在 SSD 里，正在 load 中，load 完才会升到 ready queue</li> </ul> <p>GPU 只调度 ready queue 里的请求，preparing queue 里的请求在 SSD I/O 完成后才升队。这样，不会因为一个 SSD I/O 慢的请求挡住一堆快请求。</p> <p>下图对比了 I/O-oblivious 和 I/O-aware 调度的效果——同样 5 个请求到来，I/O-aware 策略让 req3/4/5（KV 在 host memory）先跑起来，req1/2 在 SSD load 的同时 GPU 不空转：</p> <p><img src="/assets/img/posts/fast26-hu-shipeng/fig_07_Figure_11.png" alt="I/O-aware 调度减少请求阻塞"/></p> <p><strong>Disk-HRRN 优先级策略</strong></p> <p>Preparing queue 里的请求在 SSD 上发 I/O 也有先后顺序。直觉上应该优先 load 小 KV（load 快，早点升到 ready queue），但纯粹按大小来会饿死大 KV 请求。</p> <p>Bidaw 借鉴了 HRRN 调度算法（Highest Response Ratio Next），设计了一个 disk-HRRN：</p> \[\text{Response Ratio} = 1 + \frac{\text{Request waiting time}}{\text{KV size}}\] <p>等待时间越长，优先级越高；KV 越小，优先级越高。这样既能快速处理小请求，又不会让大请求一直饿着。</p> <h3 id="42-基于模型回答长度的-kv-eviction-策略">4.2 基于模型回答长度的 KV Eviction 策略</h3> <p>这是论文里我觉得最有意思的一个观察。</p> <p><strong>观察：回答越长，下次访问越晚</strong></p> <p>模型回答越长，用户读/听/理解的时间越长，再发下一个请求的间隔就越长。在这段时间里，其他用户的请求会不断进来，加权重用距离自然就越大。</p> <p>论文在不同时间段、不同压力下跑了 12 组实验，计算了”加权重用距离的下界”与”上一轮回答长度”的 Spearman 相关系数：<strong>全部在 0.94 到 0.98 之间</strong>，强正相关。</p> <p><img src="/assets/img/posts/fast26-hu-shipeng/fig_08_Figure_12.png" alt="回答长度与重用距离正相关"/></p> <p><strong>Hit Potential 驱动的 Eviction</strong></p> <p>有了对加权重用距离的预测，还需要判断：一个 KV 的”下次访问还能命中 host memory 吗？”</p> <p>论文定义了三种重用距离范围：</p> <ul> <li><strong>small</strong>：重用距离小于 host memory 容量，命中率 1.0</li> <li><strong>promising</strong>：重用距离超出容量但还在一定范围内，有概率命中</li> <li><strong>extreme</strong>：距离太远，即使 Belady 最优算法也没法命中，命中率 0.0</li> </ul> <p><img src="/assets/img/posts/fast26-hu-shipeng/fig_09_Figure_13.png" alt="不同重用距离范围的命中率"/></p> <p>对于 promising 区间，Bidaw 维护了一个后台运行的 <strong>ghost cache</strong>（用 Belady 最优算法做 eviction），从历史 trace 里统计各个距离 bucket 的命中率。</p> <p>最终，每个用户 KV 的 hit potential 是这样算的：</p> \[\text{Overall\_potential} = p_{\text{small}} \times 1.0 + p_{\text{extreme}} \times 0.0 + \sum_{i=1}^{m} p_{\text{promising}(i)} \times \text{hit\_promising}(i)\] <p><strong>hit potential 最低的 KV 优先 evict</strong>，而不是按时间顺序或访问频率来。</p> <h3 id="43-存储高效的-tensor-缓存">4.3 存储高效的 Tensor 缓存</h3> <p>还有一个工程优化值得单独说一下。</p> <p>现有方案（CachedAttention、FlashGen）直接缓存 KV tensor。但 Bidaw 发现，LLM 推理过程中有多种中间 tensor，它们大小不同，转换成 KV 所需的计算量也不同。</p> <p>如果我们缓存的不是 KV tensor，而是<strong>某个更小、转换代价低的中间 tensor</strong>，就可以在同样的存储空间里放更多用户的数据，host memory 命中率自然上去了。</p> <p><img src="/assets/img/posts/fast26-hu-shipeng/fig_10_Figure_14.png" alt="不同中间 tensor 的存储效率对比"/></p> <p>论文定义了 cost efficiency（saved FLOPs / required space），发现 <strong>tensor 6（归一化激活值）的 cost efficiency 为 51.0，远高于直接缓存 KV tensor 的 30.5</strong>。</p> <p>实现上，把 storage-efficient tensor 转换成 KV tensor 的操作，分配给一个低优先级 CUDA stream，利用推理时闲置的 GPU SM 来做，对 latency 影响可以忽略不计（实测只有几十毫秒，而整体延迟是几百到几千毫秒量级）。</p> <blockquote> <p>需要注意：这个优化对 MHA-based LLM（Llama、Qwen、OPT 等）适用；对于 GQA-based LLM，KV 本身已经比较小了，直接缓存 KV 更合适。</p> </blockquote> <hr/> <h2 id="5-实验设置">5. 实验设置</h2> <p><strong>硬件环境</strong>：</p> <ul> <li>单卡 A800 80GB GPU，PCIe Gen 4 连接</li> <li>200GB host memory（performance layer）</li> <li>4 块 SATA SSD 组成 RAID-5，带宽约 1.5 GB/s（capacity layer）</li> </ul> <p><strong>测试 workload</strong>：</p> <ul> <li>自有工业 workload（100 万+ 轮对话，平均 22.4 轮/用户）</li> <li>公开 ShareGPT workload（平均 5.7 轮/用户）</li> </ul> <p><strong>对比 baseline</strong>：</p> <ul> <li>vLLM（re-computation，不缓存 KV）</li> <li>CachedAttention（ATC’24，两层存储 + queue-enhanced eviction）</li> <li>FlashGen（两层存储 + inclusive caching）</li> <li>理想上界：所有 KV 都在 host memory 里的仿真场景</li> </ul> <p><strong>测试模型</strong>：OPT-6.7B、Qwen-7B、OPT-13B、Qwen-14B、OPT-30B</p> <hr/> <h2 id="6-实验结果">6. 实验结果</h2> <h3 id="61-整体性能">6.1 整体性能</h3> <p>下图展示了在各个模型上，随着用户到达率增加，平均响应延迟的变化：</p> <p><img src="/assets/img/posts/fast26-hu-shipeng/fig_11_Figure_15.png" alt="各模型上的平均响应延迟"/></p> <p>几个关键数字：</p> <ul> <li><strong>Bidaw 最高能把响应延迟压低 3.58×</strong>（OPT-13B，对比 state-of-the-art）</li> <li>对比 CachedAttention 和 FlashGen，<strong>延迟平均降低 83.9%</strong></li> <li><strong>吞吐量提升 1.43× 到 1.83×</strong>（用每分钟支持的用户数量来衡量）</li> <li>即使把 SSD 带宽提升到 5 GB/s，FlashGen 从 15.18 users/min 提升到 20.23，而 Bidaw 从 27.81 提升到 30.35——差距依然显著</li> </ul> <p><strong>注意</strong>：Bidaw 是 lossless 的——只是重新排了请求调度顺序，不影响模型回答的准确性。</p> <h3 id="62-host-memory-大小敏感性">6.2 host memory 大小敏感性</h3> <p>在 host memory 从 120GB 到 200GB（对应 GPU 显存的 1.5× 到 2.5×）变化时，Bidaw 依然明显优于 CachedAttention 和 FlashGen——<strong>在各种 memory 配置下，Bidaw 支撑的用户到达率是对比方案的 1.75× 到 2.19×</strong>。</p> <h3 id="63-公开-workloadsharegpt">6.3 公开 workload（ShareGPT）</h3> <p>在 ShareGPT 上，Bidaw 对 FlashGen 的提升约为 <strong>1.40× 吞吐量</strong>，响应延迟最高压低 56.9%。提升幅度略低于自有 workload，原因是 ShareGPT 缺少真实时间戳（只能用 Poisson 分布仿真），previous-answer-based eviction 的效果有所打折。</p> <h3 id="64-命中率对比">6.4 命中率对比</h3> <p>Bidaw 的 eviction 策略把 host memory 的 <strong>miss rate 压低了 57.6%</strong>（对比 queue-enhanced）和 <strong>69.9%</strong>（对比 FIFO/LRU/LFU 等通用策略）。</p> <h3 id="65-请求排队时间">6.5 请求排队时间</h3> <p>I/O-aware scheduler 把请求的平均排队时间从 5.76s 压低到 <strong>2.45s，降低了 57.5%</strong>。</p> <h3 id="66-系统开销">6.6 系统开销</h3> <p>Bidaw 的 overhead 非常低：</p> <ul> <li>调度操作：平均 0.62ms/次</li> <li>eviction 操作：0.35ms/次（ghost cache 后台运行：2.86ms/次）</li> <li>storage-efficient tensor 转换：几十毫秒，用低优先级 CUDA stream 并行执行</li> </ul> <h3 id="67-消融实验">6.7 消融实验</h3> <p>论文把三个组件分别拆开测了一遍（OPT-30B）：</p> <p><img src="/assets/img/posts/fast26-hu-shipeng/fig_14_Figure_21.png" alt="消融实验：各组件贡献"/></p> <ul> <li>只加 I/O-aware 调度：latency 降低 <strong>1.58×</strong></li> <li>再加 previous-answer-based eviction：throughput 进一步提升 <strong>1.25×</strong></li> <li>再加 storage-efficient tensor caching：throughput 再提升 <strong>1.10×</strong></li> </ul> <p>三个技术叠加才是完整的 Bidaw，缺一不可。</p> <hr/> <h2 id="7-个人-take">7. 个人 take</h2> <p>这篇工作的切入点很务实——<strong>不搞新的模型压缩，不搞分布式，就专注于”本地两层存储方案的实际瓶颈在哪”这一个问题</strong>，然后给出了系统性的解法。</p> <p>几个让我印象深刻的点：</p> <p><strong>第一</strong>，”compute 和 storage 互不知情”这个 root cause 分析得很准。很多系统优化工作喜欢把 I/O 优化和 scheduling 优化当成独立问题，但在多轮对话场景下，两者耦合得非常紧——I/O latency 的方差高达 90% CV，任何不感知 I/O 的调度策略都是在赌运气。</p> <p><strong>第二</strong>，用”模型回答长度”来预测下次 KV 访问时间，这个洞察很 elegant。它背后的直觉是：<strong>LLM 回答越长 → 用户思考时间越长 → 两次访问间隔越长 → 重用距离越大</strong>。Spearman 相关系数 0.94-0.98，这种规律在 12 组不同时段的实验里都成立，说明这不是 overfitting，是 interactive LLM serving 场景的一个基本性质。</p> <p><strong>第三</strong>，storage-efficient tensor caching 这个 trick 挺有意思。不是所有中间 tensor 都一样大，选对缓存的 tensor 可以让同样空间装更多用户的数据。当然这需要 transformer 架构支持（MHA 系列），GQA 的话 KV 本来就小，用处不大。</p> <p>一个值得讨论的局限：<strong>论文的 eviction 策略依赖真实时间戳</strong>，在有时间戳的工业 workload 上效果显著，但 ShareGPT 这种时间戳仿真的公开数据集上提升就弱了。这其实说明这个方案对 workload 的假设比较强——用户确实在”交互式”地使用，而不是脚本化批量发请求。</p> <p>总体来说，这是一篇偏系统的实实在在的工程优化工作，代码开源在 GitHub（interactive-conversation-workload），感兴趣的可以去看看。</p> <hr/> <p>欢迎评论区交流！如果你们在自己的 LLM serving 系统里也踩过类似的两层存储坑，欢迎分享一下经验。</p>]]></content><author><name></name></author><category term="LLM"/><category term="KV Cache"/><category term="论文解读"/><category term="系统优化"/><category term="FAST"/><summary type="html"><![CDATA[KV Cache 的两层存储到底卡在哪？FAST’26 这篇论文给出了答案]]></summary></entry><entry><title type="html">LLM 推理启动慢？华为用一个「可编程 Page Cache」把模型加载砍了 79%</title><link href="https://marsggbo.github.io/blog/2026/fast26-liu-yubo/" rel="alternate" type="text/html" title="LLM 推理启动慢？华为用一个「可编程 Page Cache」把模型加载砍了 79%"/><published>2026-05-14T00:00:00+00:00</published><updated>2026-05-14T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/fast26-liu-yubo</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/fast26-liu-yubo/"><![CDATA[<h1 id="llm-推理启动慢华为用一个可编程-page-cache把模型加载砍了-79">LLM 推理启动慢？华为用一个「可编程 Page Cache」把模型加载砍了 79%</h1> <blockquote> <p>原文：<a href="https://www.usenix.org/system/files/fast26-liu-yubo.pdf">Accelerating Model Loading in LLM Inference by Programmable Page Cache</a>（USENIX FAST 2026）</p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有遇到过这种情况：一个 LLM 推理服务，业务流量一上来，需要紧急扩容，但新起一个实例，光是模型加载就要等一两分钟，有时甚至更长？</p> <p>这不是什么小概率踩坑，这是 MaaS（Model-as-a-Service）场景下普遍存在的痛点。Qwen2.5-72B 这类大模型，光文件就 130+ GB，加载到 NPU/GPU 上是个实实在在的 I/O 密集操作。论文里测了一组数据：<strong>模型加载的开销占推理服务整个启动时延的 50% 以上</strong>，这还是算上了容器初始化、KV cache 初始化的情况。</p> <p>更细思极恐的是：SSD 的理论最大带宽是 5.93 GB/s，但实测过程中<strong>平均带宽只有 1.05 GB/s</strong>，只用了约 <strong>17%</strong>！SSD 在那儿闲着，但模型就是加载不快。</p> <p>这是整个行业在 LLM 大规模部署上面临的核心矛盾之一：存储硬件的性能远没有被充分利用，瓶颈卡在了软件层——具体来说，是 Linux 内核的 <strong>page cache 策略</strong>。</p> <p>这篇来自华为的论文（发表在 FAST 2026）就是专门冲着这个问题去的。他们提出了 <strong>PPC（Programmable Page Cache）</strong> 框架 + <strong>MAIO（Model-Accelerated I/O）</strong> 缓存策略，最终实现模型加载时延降低 <strong>79%</strong>，在弹性部署场景下 inference throughput 提升 <strong>36%</strong>。</p> <hr/> <h2 id="2-背景为什么是-page-cache">2. 背景：为什么是 page cache？</h2> <p>先交代下背景。</p> <p>LLM 推理服务启动分三个阶段：</p> <ol> <li><strong>Framework Init</strong>：容器初始化、推理框架启动</li> <li><strong>Model Loading</strong>：从 SSD 文件系统把模型权重逐 tensor 加载到 XPU（NPU/GPU）</li> <li><strong>Service Start</strong>：KV cache 初始化、推理服务就绪</li> </ol> <p>瓶颈在 Model Loading 阶段。模型文件存在 SSD 上，通过内核文件系统读取，数据先进 <strong>page cache（内存）</strong>，再传到 XPU 显存。</p> <p>内核默认的 page cache 策略对通用场景设计，对模型加载这种特殊 I/O 场景非常不友好：</p> <p><strong>观察一：prefetch 机制无法充分利用 SSD 并发带宽</strong></p> <p>如下图，可以很清晰地看到 SSD 带宽利用率的变化曲线——峰值能到 5.93 GB/s，均值只有 1.05 GB/s，足足差了 5.64x。原因是内核的 prefetch 策略受限于 kworker 线程数量，SSD 本身的高并发能力根本发挥不出来。另外，prefetch 的时机也不对，在 framework init 阶段（这段时间 CPU 在忙别的，I/O 本来可以提前做）根本没有 prefetch。</p> <p><img src="/assets/img/posts/fast26-liu-yubo/fig1_ssd_bandwidth.png" alt="SSD 带宽利用率vs时间"/></p> <p><strong>观察二：prefetch 的精度很低，大量无效数据被预取</strong></p> <p>内核 page cache 只会对同一文件内相邻偏移的数据做顺序预取，完全感知不到模型加载的实际 I/O 顺序——不同 tensor parallelism 配置下，每个 XPU worker 的读取顺序是不一样的。预取了不该预取的数据，反而挤占了内存。</p> <p><strong>观察三：eviction 策略在内存受限场景下引发 cache thrashing</strong></p> <p>当可用内存不够放下整个模型时（比如只给模型加载留了 64GB 内存），内核用 LRU 驱逐页面，但完全不知道哪些数据”用了就不会再用”，导致后面要用的数据被驱逐，引发 cache thrashing，加载反而更慢。</p> <hr/> <h2 id="3-现有方案的问题性能好但兼容性差">3. 现有方案的问题：性能好但兼容性差</h2> <p>行业里不是没有人尝试优化这个问题。ServerlessLLM 通过改造推理框架的 model loading 逻辑来做 pipeline prefetch；BlitzScale 利用 NVLink/RDMA 实现多机间模型共享。</p> <p>但这些方案有个共同问题：<strong>为了性能牺牲了兼容性</strong>。</p> <ul> <li>依赖特定推理框架（比如只支持 vLLM，不支持 Transformers）</li> <li>依赖特定硬件（NVLink、HCCS 等互连硬件）</li> <li>需要修改内核，而内核升级在生产集群里是几年才能完成的大工程</li> <li>需要转换模型格式，额外存储开销</li> </ul> <p>华为的出发点是：在 MaaS 生产环境里，你不能假设所有节点都是同款硬件，也不能随意改推理框架。<strong>必须做到高性能、高兼容性两手都要抓</strong>。</p> <hr/> <h2 id="4-解法ppc--maio">4. 解法：PPC + MAIO</h2> <h3 id="41-ppc可编程-page-cache-框架">4.1 PPC：可编程 Page Cache 框架</h3> <p>PPC 的核心思路是——<strong>在不修改内核、不改推理框架的前提下，让用户可以自定义 page cache 策略</strong>。</p> <p>如下图，有几种控制 page cache 的技术路线：</p> <table> <thead> <tr> <th>方案</th> <th>是否非侵入</th> <th>灵活性</th> <th>轻量性</th> </tr> </thead> <tbody> <tr> <td>FUSE-based（RFUSE、XFUSE）</td> <td>✓</td> <td>✓</td> <td>✗（overhead 高）</td> </tr> <tr> <td>eBPF-based（PageFlex、FetchBPF）</td> <td>✗（需要改内核）</td> <td>✗（策略复杂度受限）</td> <td>✓</td> </tr> <tr> <td>fadvise（内核原生）</td> <td>✗</td> <td>✗（无法深度协作前端 I/O）</td> <td>✓</td> </tr> <tr> <td><strong>PPC（本文）</strong></td> <td><strong>✓</strong></td> <td><strong>✓</strong></td> <td><strong>✓</strong></td> </tr> </tbody> </table> <p><img src="/assets/img/posts/fast26-liu-yubo/table1_comparison.png" alt="PPC 方案对比"/></p> <p>PPC 分两个核心组件：</p> <p><strong>RFS（Routing File System）</strong>：基于 stacked filesystem 机制（类似 OverlayFS），在不修改底层文件系统的情况下，通过重写 VFS 接口来拦截 I/O 操作。当应用发起文件读取，RFS 检查 page cache 是否命中：命中则直接返回；miss 则通过 <strong>UPC（Userspace Procedure Call）</strong> 把事件异步发送到用户态，同时调用底层文件系统的原生读取流程——整个过程<strong>非阻塞</strong>。</p> <p><strong>CPRT（Cache Policy Runtime）</strong>：运行在用户态的策略执行引擎。它维护一个线程池，监听 RFS 发来的 I/O miss 事件，解析事件信息（文件、偏移、长度、PID），然后调用用户自定义的 prefetch/evict 函数。用户只需要编译一个动态链接库，通过 <code class="language-plaintext highlighter-rouge">reg_policy</code> 注册进来就行，策略可以随时热切换。</p> <p><img src="/assets/img/posts/fast26-liu-yubo/fig3_rfs.png" alt="PPC 架构与 RFS 实现"/></p> <p>这个设计非常干净：PPC 作为一个独立内核模块存在，不需要修改现有内核模块，对前端 I/O 性能影响极小（实测引入的额外 overhead 只有 3.7%~6.4%，而 RFUSE 有 14%~15%）。</p> <h3 id="42-maio面向模型加载的-io-策略">4.2 MAIO：面向模型加载的 I/O 策略</h3> <p>有了 PPC 这个编程框架，接下来就是设计具体的缓存策略 MAIO。</p> <p>MAIO 的核心洞见是：<strong>同一个 LLM 推理服务实例，每次启动时的 I/O 序列是完全一致的</strong>。模型、tensor parallelism 配置确定之后，每个 XPU worker 按照固定顺序读取固定的 tensor 文件分片，这是可以预测的。</p> <p>基于这个观察，MAIO 做了三件事：</p> <p><strong>① I/O 模板生成（第一次启动时）</strong></p> <p>MAIO 通过 PPC 的 miss 事件机制，透明地追踪每个 XPU worker 的 I/O 序列，生成一个 <strong>I/O 模板文件</strong>。这个模板记录了每个 XPU worker 按时间顺序读取的文件路径、偏移、长度。模板存储极度紧凑——即使是 DeepSeek-R1-671B（662B 参数，双节点 16 NPU），I/O 模板也只有 <strong>545KB</strong>。</p> <p><strong>② 基于模板的精确 prefetch + XPU afﬁnity loading</strong></p> <p>有了 I/O 模板，MAIO 在 framework init 阶段就能提前知道后面 model loading 要读什么、哪个 XPU worker 要用。它实现了<strong>可中断的 prefetch</strong>：维护一个高并发线程池充分利用 SSD 的并发能力，当内存不足时可以立即中断当前 prefetch，避免资源浪费。</p> <p>更关键的是 <strong>XPU afﬁnity loading</strong>：prefetch 的数据会直接放到目标 XPU 所在的 NUMA node 的内存里，减少 host-to-device 传输的跨 NUMA 开销。</p> <p><strong>③ BAR（Burn-After-Reading）驱逐</strong></p> <p>这是 MAIO 在内存受限场景的杀手锏。传统 LRU 驱逐策略不知道哪些数据”用完就不会再用”；MAIO 利用 I/O 模板，<strong>精确知道哪些数据已经被对应的 XPU worker 消费完了</strong>，可以立即将其标记为可回收，为后续 prefetch 腾出空间，彻底避免 cache thrashing。</p> <p>如下图是 MAIO 的整体执行流程：</p> <p><img src="/assets/img/posts/fast26-liu-yubo/fig5_maio.png" alt="MAIO 整体实现"/></p> <hr/> <h2 id="5-实验设置">5. 实验设置</h2> <p>实验平台：4 个 48 核 Kunpeng 920 CPUs，<strong>8 个 Ascend 910B2 NPUs</strong>，1TB DRAM，3.75TB SSD。软件栈：vLLM-Ascend 0.9.2，PyTorch 2.5.1，Linux kernel 5.10。推理服务部署为容器，默认跑在 4 个 NPU 上。</p> <p>测试模型：Qwen2.5-7B/32B/72B，Llama-7B/70B。</p> <p>对比基线：</p> <ul> <li><strong>Native</strong>：原始内核 page cache 策略，模型存 SSD</li> <li><strong>PreCache</strong>：直接把整个模型预缓存进内存（高性能但吃内存）</li> <li><strong>EagerLoad</strong>：第一次 I/O miss 时触发全量 prefetch，无 eviction 优化</li> <li><strong>SLLM-NPU</strong>：ServerlessLLM 的 NPU 适配版本（不兼容方案代表）</li> </ul> <p>两种内存场景：充足内存（无限制）vs 内存受限（64GB 限制，用 cgroup 控制）</p> <hr/> <h2 id="6-实验结果分析">6. 实验结果分析</h2> <h3 id="61-模型加载时延">6.1 模型加载时延</h3> <p>如下图：</p> <p><img src="/assets/img/posts/fast26-liu-yubo/fig6_model_loading.png" alt="各模型加载时延对比"/></p> <p><strong>充足内存场景（上图 a）</strong>：</p> <ul> <li>MAIO vs Native：<strong>最多快 79%</strong></li> <li>MAIO vs EagerLoad：最多快 32%（EagerLoad 没有精确 prefetch，不知道 XPU afﬁnity）</li> <li>MAIO vs PreCache：最多快 37%（PreCache 没有 XPU afﬁnity 感知）</li> <li>MAIO vs SLLM-NPU（不兼容方案）：大模型场景还要快 17%！原因是 MAIO 在 framework init 阶段就开始 prefetch，而 SLLM-NPU 只在 model loading 阶段才做，错过了大量可以利用的带宽窗口。</li> </ul> <p><strong>内存受限场景（上图 b）</strong>：</p> <ul> <li>MAIO vs 其他所有方案：<strong>最多快 74%</strong></li> <li>PreCache 和 EagerLoad 在这个场景下几乎没有优势，甚至比 Native 更差——原因是它们的盲目 prefetch 触发了 kernel 原生 eviction，产生严重 cache thrashing。</li> <li>MAIO 的 BAR eviction 精确感知了 I/O 位置，evict 已用数据，只 prefetch 接下来要用的，彻底绕开了 cache thrashing。</li> </ul> <h3 id="62-推理服务总启动时延">6.2 推理服务总启动时延</h3> <p><img src="/assets/img/posts/fast26-liu-yubo/fig7_startup_latency.png" alt="推理服务总启动时延"/></p> <p>充足内存场景下，MAIO 比 Native <strong>降低最多 38%</strong> 的总启动时延；内存受限场景下，比其他所有方案<strong>降低最多 51%</strong>。</p> <h3 id="63-ssd-带宽利用情况">6.3 SSD 带宽利用情况</h3> <p>如下图：</p> <p><img src="/assets/img/posts/fast26-liu-yubo/fig8_ssd_bandwidth.png" alt="SSD 带宽利用率"/></p> <p>上图直观展示了 MAIO 和其他方案的 SSD 带宽利用曲线：MAIO 在 framework init 阶段就开始高并发 prefetch，几乎打满了 SSD 带宽，到 model loading 阶段大部分数据都已经 cache 命中了。而 Native 只在 model loading 阶段才有 I/O，还跑不满带宽。</p> <h3 id="64-maio-各组件消融分析">6.4 MAIO 各组件消融分析</h3> <p>如下图：</p> <p><img src="/assets/img/posts/fast26-liu-yubo/fig11_perf_breakdown.png" alt="MAIO 组件消融实验"/></p> <p>充足内存场景：interruptible prefetch（Base+P）贡献了 <strong>65%+</strong> 的降幅，XPU afﬁnity loading（+A）再砍 8.5%，BAR eviction（+E）影响不大（因为内存够用，eviction 不是瓶颈）。</p> <p>内存受限场景：三个组件各有显著作用，BAR eviction 在这里的贡献达到 <strong>19%~23%</strong>，是内存受限场景的关键设计。</p> <h3 id="65-弹性部署端到端-throughput">6.5 弹性部署端到端 throughput</h3> <p>在弹性部署场景（Qwen2.5-72B → Llama-70B → Qwen2.5-72B 循环，不同空闲时间间隔），MAIO 的 token throughput 比 Native <strong>最多提升 36%</strong>。这个场景最贴近实际 MaaS 生产环境——模型频繁热换，快速加载直接影响整体算力效率。</p> <h3 id="66-真实业务验证deepseek-r1-671b">6.6 真实业务验证：DeepSeek-R1-671B</h3> <p>华为在自家 Intelligence BooM 平台上部署 DeepSeek-R1-671B（662B 参数，双节点，16 NPUs），验证了 MAIO 的实际效果：</p> <ul> <li>原始直接加载：<strong>649 秒</strong></li> <li>MAIO 优化后：<strong>452 秒</strong>（降低约 30%）</li> <li>有趣的对比：把模型整个 cache 进 DRAM 的时延是 <strong>561 秒</strong>，MAIO 比全量内存缓存还快！</li> </ul> <p>原因在于 MAIO 的 XPU afﬁnity loading 加速了 host-to-device 传输，而全量 DRAM 缓存方案缺乏这个优化。</p> <hr/> <h2 id="7-个人-take">7. 个人 take</h2> <p>这篇论文技术上挺扎实的，几个点值得关注：</p> <p><strong>选了一个被忽视的优化角度</strong>。大家做模型加载优化都在改推理框架、改硬件互联，这篇直接从 OS page cache 层入手，而且不修改内核、不改推理框架，兼容性是真的强。在生产集群里这种约束现实存在，所以这个出发点很有价值。</p> <p><strong>I/O 模板这个 insight 简洁有力</strong>。”同一推理服务的 I/O 序列是确定性可重复的”——这个观察直接打通了精确 prefetch 的可能性，模板生成完全透明，只有第一次启动时有开销，后续使用几乎零代价（DeepSeek-R1-671B 模板才 545KB）。</p> <p><strong>XPU afﬁnity loading 是个容易被忽视的细节</strong>。很多人优化 prefetch 只关注带宽，但数据放在哪个 NUMA node 的内存里影响很大，这个细节被单独提出来并做了消融实验，说明作者是真的在生产环境里踩过坑。</p> <p><strong>BAR eviction 在内存受限场景是关键</strong>。内存受限是现实部署中常见的约束，特别是大模型多实例共存时。传统方法在这里直接翻车（cache thrashing），MAIO 的 BAR 策略实现了”用完即扔”，精准控制内存水位。</p> <p>当然也有些局限：目前 RFS 是只读的，只支持 model loading 这类读场景；I/O 模板依赖推理服务规格不变，一旦 tensor parallelism 改变就需要重新生成。不过这些在 MaaS 场景里算是可接受的约束。</p> <hr/> <p>欢迎评论区交流，有踩过类似坑的可以聊聊～</p>]]></content><author><name></name></author><category term="LLM"/><category term="推理优化"/><category term="系统"/><category term="论文解读"/><category term="FAST"/><summary type="html"><![CDATA[LLM 推理启动慢？华为用一个「可编程 Page Cache」把模型加载砍了 79%]]></summary></entry><entry><title type="html">笔记本也能跑 128k 上下文 LLM？SolidAttention 把 SSD 玩出了新花样</title><link href="https://marsggbo.github.io/blog/2026/fast26-zheng/" rel="alternate" type="text/html" title="笔记本也能跑 128k 上下文 LLM？SolidAttention 把 SSD 玩出了新花样"/><published>2026-05-14T00:00:00+00:00</published><updated>2026-05-14T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/fast26-zheng</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/fast26-zheng/"><![CDATA[<h1 id="笔记本也能跑-128k-上下文-llmsolidattention-把-ssd-玩出了新花样">笔记本也能跑 128k 上下文 LLM？SolidAttention 把 SSD 玩出了新花样</h1> <blockquote> <p>原文：<a href="https://www.usenix.org/system/files/fast26-zheng.pdf">SolidAttention: Low-Latency SSD-based Serving on Memory-Constrained PCs</a>（FAST 2026，上海交通大学 IPADS 实验室）</p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有想过，在自己的笔记本上本地跑一个长上下文的 LLM，体验一下”私有 AI 助手”到底是什么感觉？</p> <p>这个想法很美好，现实很骨感。</p> <p>现在主流的笔记本——就是那种配个 16GB 内存、8GB 显存的标配机器——在面对 128k token 上下文时，连 KV cache 都装不下。一个 8B 参数的模型，光 KV cache 就需要 <strong>16GB 内存</strong>，比模型权重本身多 4 倍多。</p> <p>所以现在的本地部署方案（llama.cpp、Ollama 这些）基本默认你的内存够用，但大多数人的机器其实根本不够用。</p> <p>今天想和大家聊聊 FAST 2026 的这篇工作——<strong>SolidAttention</strong>。他们的思路是：内存不够，SSD 来凑。但怎么凑，里面有很多讲究。</p> <hr/> <h2 id="2-核心矛盾kv-cache-的内存危机">2. 核心矛盾：KV Cache 的内存危机</h2> <p>先交代下背景。</p> <p>LLM 的 autoregressive decode 阶段，每生成一个 token，都需要把之前所有 token 的 Key 和 Value 矩阵拿出来做 attention。为了不重复计算，就把这些 K/V 缓存下来，这就是 KV cache。</p> <p>问题在于，<strong>KV cache 随着序列长度线性增长</strong>。如下图左，Llama-3.1-8B（INT4 量化权重）在 128k context 下，KV cache 需要 16GB，是模型权重的 4 倍多。而右图说明了 SSD 的特性：读取单元越大，带宽越高。4KB 小块随机读约 1GB/s，1MB 以上大块读能到 6GB/s——相差 6 倍。</p> <p><img src="/assets/img/posts/fast26-zheng/fig01.png" alt="KV Cache 内存增长和 SSD 带宽特性"/></p> <p>更难受的是，市面上大部分笔记本只有 8-16GB 内存、6-8GB 显存。在这种机器上，128k context 不是说”慢一点”，是直接装不下。</p> <p><strong>那量化 KV cache 行不行？</strong></p> <p>INT4 量化之后效果很差——KV cache 里有很多异常值（outlier），激进量化直接把精度打烂了（Qwen-2.5-7B 的精度从 71.39% 暴跌到 18.63%）。</p> <p><strong>那把 KV cache 卸载到 SSD 呢？</strong></p> <p>这个方向是对的，但之前的工作（比如 FlexGen）有一个核心假设：系统里有足够多的并发请求，让 SSD 加载数据的时候，GPU 还有别的请求可以算。</p> <p>如下图，高并发场景（上）：Request 1 在等 KV cache 的时候，GPU 可以跑 Request 2 的 FFN——I/O 延迟被”藏住”了。低并发场景（下）：只有一个请求，KV cache 加载期间 GPU 完全空转（GPU Idle），SSD 等 GPU 算完也在空转（SSD Idle）。</p> <p><img src="/assets/img/posts/fast26-zheng/fig02.png" alt="高并发 vs 低并发场景下的 SSD 访问延迟"/></p> <p>笔记本本地推理的场景，<strong>根本就没有并发</strong>。只有你一个人在用，batch size=1。SSD 加载数据的时候，GPU 只能干等。加载 1k token 的 KV cache（128MB）大约需要 40ms，这能占一次 decode step 将近一半的时间。</p> <hr/> <h2 id="3-问题的根源ssd-和稀疏-attention-天生相克">3. 问题的根源：SSD 和稀疏 Attention 天生相克</h2> <p>现有 SSD offloading + attention sparsity 方案的毛病，不只是”并发不够”这一个。</p> <p><strong>更深层的矛盾是：动态稀疏 attention 的访问模式，和 SSD 的特性天生相克。</strong></p> <p>动态稀疏 attention 每个 decode step 都要根据当前 query，动态选出需要的 KV 块，加载进来。这些块是<strong>按 token 粒度选的</strong>，单块可能只有几 KB，产生大量细粒度随机 I/O，SSD 带宽利用率极低。</p> <p>SolidAttention 的核心 insight：<strong>要把稀疏 attention 算法和 SSD 存储管理协同设计（co-design）</strong>，让数据访问的粒度对齐 SSD 的特性，同时精细编排计算和 I/O 的重叠。</p> <hr/> <h2 id="4-solidattention-整体架构">4. SolidAttention 整体架构</h2> <p>三个核心挑战，三个解决模块：</p> <ul> <li><strong>挑战 1</strong>：如何在不牺牲精度的情况下，把细粒度访问变成粗粒度访问？→ <strong>KV Consolidator</strong></li> <li><strong>挑战 2</strong>：动态 attention 在算完才知道需要哪些块，来不及提前加载，怎么办？→ <strong>Speculative Prefetcher</strong></li> <li><strong>挑战 3</strong>：内存受限时，GPU 读数据和 SSD 预取数据可能发生冲突，怎么安全调度？→ <strong>SSD-aware Scheduler</strong></li> </ul> <p>如下图，SolidAttention 系统架构：整个 KV cache 存在 SSD 里，decode 时按需动态加载到 GPU VRAM。三个模块协同工作，把 I/O 延迟尽量藏在计算背后。</p> <p><img src="/assets/img/posts/fast26-zheng/fig03.png" alt="SolidAttention 系统架构"/></p> <p><strong>Block-wise 注意力稀疏</strong>。SolidAttention 把 KV cache 按 context 长度切成三类 block。如下图，用当前 query Q 和每个 block 的 representative vector（代表向量）做相似度计算，选出 Top-K 个 block 参与 attention 计算：</p> <ul> <li><strong>Init Block</strong>（绿色）：固定覆盖开头几个 token，处理 attention sink 现象</li> <li><strong>Local Block</strong>（蓝色）：滑动窗口，覆盖最近生成的 token</li> <li><strong>Selected Block</strong>（棕色）：每次 decode 时动态选出的高相关 block</li> </ul> <p><img src="/assets/img/posts/fast26-zheng/fig04.png" alt="Block-wise 动态稀疏 Attention 的分块和选择流程"/></p> <p>类比理解：把整本书按段落分好，每次提问不是把整本书都翻一遍，而是先看每段的”摘要”（representative vector），只把最相关的几段拿出来精读（attention 计算）。大幅减少了需要从 SSD 加载的 KV cache 量。</p> <hr/> <h2 id="5-kv-consolidator让-k-和-v-手拉手走翻倍带宽">5. KV Consolidator：让 K 和 V “手拉手”走，翻倍带宽</h2> <p><strong>问题</strong>：Block 越大，SSD 读的粒度越大，带宽利用率越高——但每个 block 的 representative vector 要概括更多 token，精度会下降。</p> <p>如下图，block size 从 32 增大到 256，两个模型的 recall rate 都明显下降，从约 90% 跌到约 60%：</p> <p><img src="/assets/img/posts/fast26-zheng/fig06.png" alt="Block Size 对 Recall Rate 的影响"/></p> <p>所以 SolidAttention 选用 block size=32，精度损失很小。但 32 token 的 block 在 SSD 上读起来还是太碎了——怎么在不增大 block 的情况下提高传输粒度？</p> <p><strong>解法</strong>：把 K 和 V 交织在一起，把每次 I/O 的传输单元翻倍。</p> <p>K 和 V 的形状完全一样（都是 N×H 的矩阵）。如下图：</p> <ul> <li><strong>Typical</strong>（上）：K 和 V 分开存、分开读，同一块 block 要读两次 SSD</li> <li><strong>Interleaved</strong>（下）：按 token 粒度交织，K₁V₁K₂V₂…，一次 SSD 读把 K 和 V 都搬过来</li> </ul> <p><img src="/assets/img/posts/fast26-zheng/fig07.png" alt="分离存储 vs 交织存储的 KV cache I/O 对比"/></p> <p>传输单元翻倍，I/O 次数减半，SSD 带宽利用率 double。<strong>精度完全不受影响</strong>——每个 block 里的 token 数量没变，representative vector 质量不变。</p> <p><strong>工程实现</strong>：如下图，对比两种 projection 计算方式：</p> <p><img src="/assets/img/posts/fast26-zheng/fig08.png" alt="分离权重 vs 拼接权重的 projection 计算"/></p> <p>在模型初始化时把 K projection 和 V projection 的权重矩阵预先拼接成一个矩阵（Weight K+V），用一次矩阵乘法直接生成交织的 K/V 输出，避免运行时 reshape 开销。attention kernel 里用 stride=2H 的访问模式来逻辑上分开 K 和 V，实测延迟开销 ≤2%。</p> <hr/> <h2 id="6-speculative-prefetcher历史会重演让-ssd-提前读">6. Speculative Prefetcher：历史会重演，让 SSD 提前读</h2> <p><strong>问题</strong>：每一层选哪些 KV block，是根据这一层的 query 计算出来的。在计算这一层 attention 之前，下一层要哪些 block 根本不知道。</p> <p>等到上一层算完再去 SSD 读，就产生了 <strong>blocking latency</strong>——GPU 干等 SSD。</p> <p><strong>突破口：相邻层之间的 block 选择结果高度相似。</strong></p> <p>如下图，在 LongBench 的 8 个 benchmark 上统计，跨层的 block 选择相似度平均在 <strong>75%~90%</strong> 之间，两个模型都一致：</p> <p><img src="/assets/img/posts/fast26-zheng/fig09.png" alt="跨层 Block 选择相似度（Llama-3.1-8B 和 Qwen-2.5-7B）"/></p> <p>直觉也说得通：模型在处理同一个输入时，不同层关注的 context 区域往往差不多（比如处理代码补全，每层都在关注函数定义那几个 token）。</p> <p>有了这个观察，就可以做 <strong>speculative prefetching</strong>：用上一层的选择结果预测下一层需要哪些 block，提前发起 I/O。如下图：</p> <ul> <li><strong>(a) 预取命中</strong>：上一层选的块 {1,3}，提前加载到 DRAM，下一层实际也选 {1,3}，直接用</li> <li><strong>(b) 预取失误</strong>：预取了块 6，但实际需要的是块 3，把块 3 直接覆写上去，不需要重排序</li> </ul> <p><img src="/assets/img/posts/fast26-zheng/fig10.png" alt="Speculative Prefetch 的命中和 miss 处理"/></p> <p>整个单层的数据流时间线如下图。可以看到，SSD 预取（蓝色箭头）是在上一层的 FFN 计算期间就提前开始了：</p> <p><img src="/assets/img/posts/fast26-zheng/fig05.png" alt="单层 LLM decode 的数据流时间线"/></p> <p>时间线解读（以 Layer 1 为例）：</p> <ul> <li><strong>t₁→t₂</strong>：Layer 0 的 FFN 运行期间，SolidAttention 就开始 prefetch Layer 1 需要的 KV 块（VRAM←SSD）</li> <li><strong>t₂→t₃</strong>：Layer 1 执行 token selection（select），确认命中 {a,c}，miss 了 {b}（橙色虚线框）</li> <li><strong>t₃→t₄</strong>：补传 miss 的块 {b}，同时 GPU 算当前 token 的 kv proj，新 KV pair 写到 DRAM</li> <li><strong>t₄→t₅</strong>：Layer 1 self-attention 计算，同时异步 prefetch Layer 2 的 KV 块（DRAM←SSD）</li> <li><strong>FFN 期间</strong>：把 DRAM 里预取的数据传到 VRAM，同时把 Layer 1 新 KV pair 写回 SSD</li> </ul> <p>GPU、CPU、SSD 几乎全程都在忙，没有大段空闲。</p> <hr/> <h2 id="7-ssd-aware-schedulerdag-调度同步点复用">7. SSD-aware Scheduler：DAG 调度，同步点复用</h2> <p><strong>问题</strong>：内存受限时，同一块 DRAM 缓冲区既要给 GPU 提供数据，又要接收 SSD 预取来的数据。Naive 实现是等所有 I/O 完成 GPU 再开始算——GPU 和 SSD 串行，效率极低。</p> <p>如下图，三种调度方案对比（一层 attention module 的完整时间线）：</p> <p><img src="/assets/img/posts/fast26-zheng/fig11.png" alt="三种调度方案的时间线对比"/></p> <ul> <li><strong>(a) 不用 attention sparsity</strong>：没有 select/load 步骤，但 GPU 和 SSD 之间有多个 Sync 等待点</li> <li><strong>(b) Naive + attention sparsity</strong>：有 5 个 Sync 点，大量 GPU 空等，是最慢的方案</li> <li><strong>(c) SSD-aware schedule</strong>（SolidAttention）：只有 3 个 Sync 点，GPU 计算和 SSD I/O 大量重叠，关键路径更短</li> </ul> <p><strong>两个核心设计</strong>：</p> <p><strong>1. 微任务分解 + 细粒度重叠</strong></p> <p>把 attention module 分解成微任务：q proj → kv proj → select → prefetch → load → attention → store</p> <p>用 DAG（有向无环图）建模任务依赖关系。调度器找出关键路径，优先执行关键路径上的任务。非关键任务（比如写回新 KV 到 SSD）在不影响关键路径的情况下尽量并行。</p> <p><strong>2. 同步点复用</strong></p> <p>多个 microtask 共享一个同步点，减少 CPU-GPU 之间的握手频率。例如：把”等待 selected block 加载完成”和”等待 init/local block 加载完成”两个同步点合并成一个。少同步 = 少停顿 = 更快。</p> <hr/> <h2 id="8-实验到底快了多少">8. 实验：到底快了多少？</h2> <h3 id="81-实验设置">8.1 实验设置</h3> <p>两台真实笔记本：</p> <ul> <li><strong>CUDA 后端</strong>：Intel Ultra 9 185H + RTX 4070 Laptop GPU（8GB VRAM）+ 64GB DDR5 + 1TB Samsung 990 PRO PCIe 4.0 SSD</li> <li><strong>SYCL 后端</strong>：Intel Ultra 7 255H + Intel Arc 140T 集成显卡 + 64GB DDR5 + 同款 SSD</li> </ul> <p>测试模型：Llama-3.2-3B、Llama-3.1-8B、Qwen-2.5-7B（权重 INT4，KV cache FP16），DRAM 限制 16GB，batch size=1。</p> <h3 id="82-端到端性能">8.2 端到端性能</h3> <p>如下图，CUDA 后端三个模型在不同 context 长度下的推理速度（token/s）：</p> <p><img src="/assets/img/posts/fast26-zheng/fig12.png" alt="CUDA 后端端到端推理速度"/></p> <p>如下图，SYCL 后端（集成显卡）的性能：</p> <p><img src="/assets/img/posts/fast26-zheng/fig13.png" alt="SYCL 后端端到端推理速度"/></p> <p>关键数字汇总：</p> <table> <thead> <tr> <th>对比</th> <th>加速比</th> </tr> </thead> <tbody> <tr> <td>CUDA + 128k，vs Offload+Sparse（3个模型）</td> <td><strong>2.4×、2.8×、3.1×</strong></td> </tr> <tr> <td>CUDA + 16k，vs FlexGen</td> <td><strong>58.9×</strong></td> </tr> <tr> <td>SYCL + 128k，vs Offload+Sparse</td> <td><strong>1.9×~2.5×</strong></td> </tr> </tbody> </table> <p>FlexGen 在 batch=1 的情况下惨不忍睹——大量 page fault 触发的细粒度 SSD 随机访问，带宽浪费严重，没有并发可以隐藏 I/O 延迟。</p> <h3 id="83-内存占用">8.3 内存占用</h3> <p>SolidAttention 只在 DRAM 里保留一层 1k token 的 KV cache buffer，其余全在 SSD：</p> <p><img src="/assets/img/posts/fast26-zheng/fig14.png" alt="KV Cache 内存占用对比"/></p> <p>KV cache 内存占用比 llama.cpp 降低 <strong>62×（约 98%）</strong>，三个模型全都一样。</p> <h3 id="84-精度">8.4 精度</h3> <p>如下图，Table 1 对比精度，Figure 16 展示投机预取的 blocking latency 降低效果：</p> <p><img src="/assets/img/posts/fast26-zheng/table01_fig16.png" alt="精度对比（Table 1）和投机预取效果（Figure 16）"/></p> <p>精度方面：</p> <ul> <li><strong>INT4 KV 量化（Quant）</strong>：Qwen-2.5-7B 从 71.39% → 18.63%，直接废了</li> <li><strong>SolidAttention</strong>：各项 benchmark 精度与原始模型差距 ≤2%</li> </ul> <p>投机预取的 blocking latency 降低：</p> <ul> <li>SYCL 后端：<strong>3.1×</strong></li> <li>CUDA 后端：<strong>3.9×</strong></li> </ul> <p>CUDA 收益更大是因为 RTX 4070 算得快，对 I/O blocking 更敏感。</p> <h3 id="85-和纯内存方案对比">8.5 和纯内存方案对比</h3> <p>SolidAttention 把 KV cache 全部卸到 SSD，但和纯内存推理（InDRAM）差多少？</p> <p><img src="/assets/img/posts/fast26-zheng/fig15.png" alt="SolidAttention vs 纯内存推理速度对比（SYCL）"/></p> <p>结果：仅 ≤<strong>11%</strong> 的吞吐量损失。把 KV cache 从内存搬到 SSD，速度几乎不变，但内存节省了 98%。</p> <h3 id="86-消融实验">8.6 消融实验</h3> <p>如下图，左：KV 交织 vs 不交织的 attention 延迟对比；右：调度方案的逐步叠加效果：</p> <p><img src="/assets/img/posts/fast26-zheng/fig17.png" alt="消融实验：KV交织和SSD-aware调度的效果"/></p> <p>三个模块的独立贡献：</p> <table> <thead> <tr> <th>模块</th> <th>效果</th> </tr> </thead> <tbody> <tr> <td>KV Consolidator（交织）</td> <td>attention 延迟降低 <strong>22%</strong></td> </tr> <tr> <td>SSD-aware Scheduler（细粒度 overlap）</td> <td>性能提升 <strong>25%</strong></td> </tr> <tr> <td>同步点复用</td> <td>attention 延迟再降 <strong>22%</strong></td> </tr> </tbody> </table> <hr/> <h2 id="9-总结">9. 总结</h2> <p>SolidAttention 这篇工作的切入点很准：</p> <ol> <li><strong>确认了一个核心矛盾</strong>：动态稀疏 attention 和 SSD 特性天生不匹配，细粒度随机 I/O 导致 SSD 带宽严重浪费</li> <li><strong>co-design 思路</strong>：不把 attention 算法和存储系统看作两个独立的问题，而是协同设计，让数据访问粒度对齐 SSD 特性</li> <li><strong>三个技术点都很实在</strong>：KV 交织（纯工程技巧，不影响精度）、投机预取（利用跨层选择相似性）、DAG 调度（精细化计算/I/O 重叠）</li> </ol> <p>工程完成度也不错：CUDA + SYCL 双后端，约 25k 行代码，在真实笔记本上跑出了这些数字。</p> <p>对做本地推理的同学来说，这个工作提供了一种不量化 KV cache 就能大幅降低内存占用的路子。现在还是研究原型，能不能接进 llama.cpp/Ollama 主线还是个问题，但思路是对的。</p> <p>欢迎评论区聊聊你对本地部署 LLM 的看法——有没有在自己的笔记本上实际跑过长上下文推理，感受如何？</p>]]></content><author><name></name></author><category term="LLM"/><category term="KV Cache"/><category term="推理优化"/><category term="SSD"/><category term="论文解读"/><summary type="html"><![CDATA[笔记本也能跑 128k 上下文 LLM？SolidAttention 把 SSD 玩出了新花样]]></summary></entry><entry><title type="html">说人话理解 IBP：不用量化，也能缓解 GPU 内存搬运瓶颈？</title><link href="https://marsggbo.github.io/blog/2026/ibp/" rel="alternate" type="text/html" title="说人话理解 IBP：不用量化，也能缓解 GPU 内存搬运瓶颈？"/><published>2026-05-14T00:00:00+00:00</published><updated>2026-05-14T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/ibp</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/ibp/"><![CDATA[<h1 id="说人话理解-ibp不用量化也能缓解-gpu-内存搬运瓶颈">说人话理解 IBP：不用量化，也能缓解 GPU 内存搬运瓶颈？</h1> <blockquote> <p>原文：<a href="https://dl.acm.org/doi/epdf/10.1145/3767295.3803595">Reducing the GPU Memory Bottleneck with Lossless Compression for ML</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有想过，很多 ML workload 慢，并不是 GPU 算不动，而是数据到不了 GPU？</p> <p>GNN training 的 node features、DLRM 的 embedding tables、LLM long-context inference 的 KVCache，经常大到 GPU HBM 放不下。于是系统只能把数据放在 CPU memory、disk 或 network storage，需要时再通过 PCIe 搬到 GPU。</p> <p>这就很尴尬了：A100/H100 算力很猛，但 PCIe 带宽就那么点。GPU 在那边等数据，牛马在这边看 profiler，大家都不开心（笑）。</p> <p>这篇 EuroSys 2026 的 IBP 工作关注的就是这个问题：能不能用 compression 减少 CPU-to-GPU transfer？</p> <p>以往大家一提压缩，首先想到 quantization、pruning、lossy compression。但工业系统里 lossy compression 很烦，因为它可能影响 accuracy，而且影响程度和 workload/model 强相关。IBP 选择了一条更保守的路：</p> <p><strong>做 lossless compression，不牺牲任何数据 fidelity，只压掉 ML tensor 里跨样本不变的 bits。</strong></p> <h2 id="2-背景为什么-pcie-是瓶颈">2. 背景：为什么 PCIe 是瓶颈？</h2> <p>论文讨论了三类典型 workload。</p> <p>第一是 GNN training。GNN 的 graph topology 和 feature vectors 通常很大，训练时每个 mini-batch 都要 sample 一批节点/边，再把对应 feature 搬到 GPU。很多系统会做 GPU cache，但 cache 放不下全部数据，miss 后仍要走 PCIe。</p> <p>第二是 DLRM inference。推荐模型里的 embedding tables 可以有上亿行，GPU memory 放不下时，lookup 需要从 CPU memory 搬 embedding entries 到 GPU。</p> <p>第三是 LLM KV-cache offloading。长上下文 LLM 中 KVCache 可能远超 HBM，系统会把部分 KVCache offload 到 CPU，再在 decode 时按需搬回 GPU。对于 long-context inference，KV transfer 本身就会变成 critical path。</p> <p>传统思路是用 lossy compression，比如 quantization。但问题是：lossy 方法需要针对模型和数据调参，出了 accuracy regression 很难 debug。lossless compression 看起来安全，但很多通用算法如 Huffman、LZ4、zStd、GDeflate 在 GPU critical path 上 decompression overhead 太高，反而变慢。</p> <p>论文前面有一个很有意思的表：很多算法压缩率不错，但 transfer + decompression 后速度不一定快。比如 zStd 在 sparse GNN 上 space saving 很高，但 transfer speedup 并不好。IBP 的目标不是追求最高压缩率，而是追求<strong>压缩后搬得更快，解得更快</strong>。</p> <h2 id="3-核心观察ml-tensor-的-bit-pattern-有结构">3. 核心观察：ML tensor 的 bit pattern 有结构</h2> <p>普通 bit packing 会看一个 tensor 内部的数值范围，删除高位不需要的 bits。但 ML tensor 里的浮点值范围常常比较大，所以传统 bit packing 空间有限。</p> <p>IBP 的观察是：很多 ML 数据虽然数值看起来分散，但在 bit level 上有低熵结构。比如 floating-point 的 sign bit、exponent bits，在很多 tensors 之间可能高度一致。也就是说，同一个 bit position 在大量 tensor 中都是同一个值。</p> <p>论文把这种 bits 叫 invariant bits。IBP 会把这些跨 tensor 不变的 bits 记录在很小的 metadata 里，然后在每个 tensor 的 compressed data 中删掉它们。</p> <p>如下图，左边是 conventional bit packing，右边是 invariant bit packing。红色 bits 被移除，蓝色部分是真正传输的数据。</p> <p><img src="/assets/img/posts/ibp/emb_001_p4.png" alt="IBP bit packing"/></p> <p>这个设计很适合 ML pipeline：metadata 很小，可以常驻 GPU；tensor data 仍在 CPU memory，GPU 需要时发起读取并解压。</p> <h2 id="4-方法ibp-怎么压怎么解">4. 方法：IBP 怎么压、怎么解？</h2> <p>IBP 的 metadata 主要有两个：</p> <ul> <li><code class="language-plaintext highlighter-rouge">Mask</code>：哪些 bit positions 是 invariant。</li> <li><code class="language-plaintext highlighter-rouge">Bitval</code>：这些 invariant bits 的具体值。</li> </ul> <p>压缩时，IBP 会扫描一批 tensors，找出满足 threshold 的 invariant bit positions。之后每个 tensor 只保存非 invariant bits，加上一点 participation bits，用来处理某些 tensor 不适合压缩的情况。如果压缩后不省空间，IBP 直接保留原始 tensor，避免 compressed dataset 反而变大。</p> <p>解压更关键，因为它在 critical path 上。IBP 用一个 warp 解压一个 tensor，每个 thread 负责一个 chunk，整个过程尽量只在 warp 内通信，避免跨 warp synchronization。</p> <p>解压大概分几步：</p> <ol> <li>warp 协作从 CPU memory 读一段 compressed data 到 workspace；</li> <li>每个 thread 读自己的 Mask chunk，统计需要恢复多少 bits；</li> <li>warp 内做 exclusive scan，确定每个 thread 的 offset；</li> <li>异步预取下一段 data；</li> <li>根据 Mask/Bitval 把 invariant bits 填回去。</li> </ol> <p>这套流程的重点是：<strong>PCIe transfer 和 decompression overlap，且 CPU 不在 critical path 上。</strong></p> <p>论文还专门优化了 GPU-initiated PCIe transfer。相比 CPU 发 DMA，GPU zero-copy 对小块随机 tensor transfer 更合适，但要做 aligned access。IBP 保证 warp 尽量发起 128B-aligned transactions，减少 PCIe transaction overhead。</p> <p>如下图是 IBP 集成到 ML 应用中的两种方式：一种压缩 GPU cache，一种压缩 CPU memory 中等待搬运的数据。</p> <p><img src="/assets/img/posts/ibp/emb_002_p4.png" alt="IBP 集成方式"/></p> <h2 id="5-实验设置">5. 实验设置</h2> <p>IBP 的实验平台是一张 A100 GPU，CPU memory 300GB，CPU-GPU interconnect 是 16-lane PCIe Gen 4，理论单向 32GB/s，实测约 25GB/s。软件栈是 CUDA 11.7、Python 3.8、PyTorch 1.13.1。</p> <p>baseline 包括 NVIDIA nvCOMP v3.0.1 和 ndzip-gpu。系统集成上，作者把 IBP 接到三个方向：</p> <ul> <li>GNN：DGL v2.1 和 Legion；</li> <li>DLRM：Colossal-AI 的 CachedEmbeddingBag；</li> <li>LLM：InfiniGen，一个 KV-cache offload 系统。</li> </ul> <p>数据集也覆盖了几类：</p> <ul> <li>GNN：Pubmed、Citeseer、Cora、Reddit、Products、MAG240M；</li> <li>DLRM：Criteo 1TB 上训练的 NVIDIA DLRM embedding weights；</li> <li>LLM：Meta OPT 系列，输入 1920 tokens，输出 128 tokens，batch size 20。</li> </ul> <h2 id="6-结果一压缩率不是最高但-transfer-最快">6. 结果一：压缩率不是最高，但 transfer 最快</h2> <p>IBP 在 sparse GNN 上空间节省很高，在 dense GNN、DLRM、LLM KVCache 上空间节省相对 modest，但很稳定。论文报告 IBP 在 dense GNN 上大约 10% 到 12% space saving，在 DLRM 上约 8%，LLM KVCache 上最高约 10%。</p> <p>这听起来不夸张，但关键是 transfer + decompression throughput。IBP 在各类数据上都有比较好的 transfer speedup：Table 1 中 IBP 在 Sparse GNN 上 9.7×，Dense GNN 1.2×，DLRM 1.1×，LLM KVCache 1.0 到 1.1×。很多通用算法虽然压得更小，但解压开销会抵消收益。</p> <p>论文还做了 sampling 实验：只采样 1%、5%、10% 数据来生成 Mask/Bitval，compression ratio 基本和全量接近。这对 streaming 或大规模 dataset 很重要，因为 preprocessing 不能太重。</p> <h2 id="7-结果二gnndlrmllm-都有收益">7. 结果二：GNN、DLRM、LLM 都有收益</h2> <p>GNN 上，IBP 接入 DGL 后平均加速 56%；接入 Legion 后平均加速 74%。在 Legion 中，IBP 同时做 compressed cache 和 PCIe traffic compression，最大 throughput 提升到 2.7×。</p> <p>如下图，IBP 对不同 GNN dataset 的训练 epoch speedup 有明显提升，尤其是可压缩性高的数据。</p> <p><img src="/assets/img/posts/ibp/emb_003_p13.png" alt="GNN 加速结果"/></p> <p>DLRM 上，IBP 的收益主要来自 GPU-based lookup/transfer。baseline 需要 CPU 做 table lookup，然后把结果搬到 GPU；IBP 用 GPU zero-copy 直接并行读 CPU memory。随着 batch size 和 embedding entry size 增大，IBP 的优势更明显。论文摘要里给出的平均提升是 DLRM embedding lookup 快 180%。</p> <p>LLM 上，作者把 IBP 接入 InfiniGen 的 KV-cache fetching 路径。实验使用 OPT-2.7B、6.7B、13B、30B，1920 input tokens，128 output tokens，batch size 20。IBP 让 KV-cache movement latency 最高降低 37%，整体 inference latency 最高降低 27%，平均约 25%。</p> <p>有个细节值得注意：LLM 场景下 IBP 不提前 preprocess 全部数据，而是在每次 prefill 阶段采样 prompt tokens 的 K/V 生成 Mask/Bitval，用于 decode 阶段压缩 output token K/V entries。这样 prefill 会增加约 10% 开销，但因为 prefill 占总时间不到 20%，decode transfer 省下来的时间覆盖了这部分成本。</p> <h2 id="8-结果三为什么-gpu-initiated-transfer-很关键">8. 结果三：为什么 GPU-initiated transfer 很关键？</h2> <p>论文专门比较了 CPU fine-grained copy、CPU bulk copy、GPU zero-copy、GPU aligned copy。对于小块随机 tensor，CPU fine-grained DMA overhead 很高；CPU bulk copy 要先在 CPU 侧 gather 成连续 buffer；GPU zero-copy 可以让 GPU warp 直接发起读取。</p> <p>更进一步，aligned transfer 比普通 GPU copy 高 25% throughput。这个点很工程，但很关键。很多时候系统 paper 的优化不是一个惊天动地的新算法，而是把数据路径上的每个 transaction overhead 都算清楚。</p> <p>IBP 的 decompression throughput 在 space saving 小于 50% 时能达到 ideal 的 95% 以上。也就是说，对于 dense ML tensors 这种压缩率不算极高的场景，decompression overhead 基本被 GPU parallelism 和 PCIe transfer overlap 掩盖掉了。</p> <h2 id="9-我的-take">9. 我的 take</h2> <p>IBP 这篇 paper 的味道很系统：它没有承诺“压缩率最高”，而是明确盯着 ML pipeline 里的真实瓶颈：PCIe transfer latency。</p> <p>很多 compression 方法只看 compression ratio，其实对 serving/training 不够。因为数据压小了以后还要解，解压如果在 critical path 上太慢，就会把省下来的 PCIe 时间吃回去。IBP 的核心取舍是：<strong>牺牲一点理论压缩率，换取极低 metadata、warp-level decompression、GPU-initiated aligned transfer。</strong></p> <p>我觉得这个思路对 LLM inference 特别有意思。现在 KVCache 优化有很多 lossy 方向，比如 eviction、quantization、sparsification。但 production 里有时候你就是不想动模型语义，不想解释为什么某个 prompt 下质量掉了。lossless 方法虽然收益可能不如 aggressive quantization，但落地阻力小很多。</p> <p>当然，IBP 也不是银弹。LLM KVCache 上的 compression ratio 只有几个百分点到 10% 左右，OPT 这种每个 head 的 K/V entry 又小，压缩收益有限。论文也提到，新模型如果 K/V entry 更大，收益可能更明显。另一个边界是 preprocessing 和 metadata 对 workload distribution 的依赖，如果数据分布变化大，Mask/Bitval 可能需要重新估计。</p> <p>但总体看，这篇工作提醒我们一个很朴素的事实：<strong>GPU 系统优化不只是算得快，还要搬得聪明。</strong> 在 GNN、DLRM、LLM 这些越来越依赖大规模 memory hierarchy 的 workload 里，lossless tensor compression 可能会成为一个很实用的系统组件。</p>]]></content><author><name></name></author><category term="GPU"/><category term="Compression"/><category term="LLM Inference"/><category term="GNN"/><category term="DLRM"/><category term="论文解读"/><summary type="html"><![CDATA[说人话理解 IBP：不用量化，也能缓解 GPU 内存搬运瓶颈？]]></summary></entry></feed>