<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://marsggbo.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://marsggbo.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-07-30T07:35:48+00:00</updated><id>https://marsggbo.github.io/feed.xml</id><title type="html">HE Xin</title><subtitle>&quot;SSS: Study, Sleep, Slim&quot; </subtitle><entry><title type="html"></title><link href="https://marsggbo.github.io/blog/2026/2026-06-07-2605_29350/" rel="alternate" type="text/html" title=""/><published>2026-07-30T07:35:48+00:00</published><updated>2026-07-30T07:35:48+00:00</updated><id>https://marsggbo.github.io/blog/2026/2026-06-07-2605_29350</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/2026-06-07-2605_29350/"><![CDATA[<blockquote> <p>插播：之前写的<a href="https://item.jd.com/14945889.html">《动手学 AutoML》</a>终于出版了，从 NAS 到超参优化都有覆盖，适合想系统入门 AutoML 的同学。好了广告结束，现在进入正题。</p> <p><img src="/assets/img/book_cover_automl.png" alt="动手学AutoML书籍封面"/></p> </blockquote> <h1 id="moe-压缩新思路别删专家别合并专家把它们重映射就够了">MoE 压缩新思路：别删专家、别合并专家，把它们”重映射”就够了</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2605.29350">ConMoE: Expert-Pool Consolidation via Prototype Reassignment for MoE Compression</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有想过，MoE 架构最骄傲的地方，恰恰也是最烫手的地方？</p> <p>MoE（Mixture-of-Experts）的核心设计是稀疏激活——每个 token 只走少数几个 expert，理论上参数量很大但计算量很小。Qwen3-30B-A3B 的名字里 A3B 的意思就是”激活参数 3B”，但总参数有 30B。听起来很美，对吧？</p> <p>但问题来了：<strong>这 30B 参数，你一个都不能少地加载进显存</strong>。</p> <p>不管一个 token 实际上只用了 3B 参数，那剩下 27B 的专家还是得老老实实待在显存里候命。这就是 MoE 推理的核心矛盾：稀疏激活省了算力，但显存一分没省。随着模型越做越大，expert 存储变成了 MoE 落地的最大障碍之一。</p> <p>现有的压缩思路主要两条路：<strong>剪掉专家（pruning）</strong> 和 <strong>合并专家（merging）</strong>。前者直接删掉一些专家，后者把多个专家的权重”揉”成一个新专家。</p> <p>今天要聊的这篇论文提了第三条路：<strong>什么都不删，什么都不合并，只是把原来的专家引用”重新指向”一个更小的专家池</strong>。</p> <p>听起来像绕口令？别急，慢慢来。</p> <hr/> <h2 id="2-现有方案的痛点">2. 现有方案的痛点</h2> <p>在聊 ConMoE 之前，先交代下背景，说说 pruning 和 merging 各自的问题。</p> <p><strong>Expert Pruning（专家剪枝）</strong>：直接删掉贡献低的专家。简单粗暴，但问题是删了就是删了，原来指向这个专家的 router 决策没有任何”容错”——被删专家的那些 token 只能丢给其他留下来的专家，不管合不合适。</p> <p><strong>Expert Merging（专家合并）</strong>：把多个专家的权重融合成一个新专家。听起来更优雅，但实际上构造了一个”原来不存在”的新权重，这个新权重可能引入了原本没有的错误，而且融合方式很敏感，调参难度大。</p> <p>这两类方法有一个共同问题：<strong>把”保留哪些专家参数”和”原来的 expert slot 该怎么被表示”这两件事耦合在一起了</strong>。</p> <p>ConMoE 的作者们说，等等，这两个问题应该分开来看。</p> <hr/> <h2 id="3-conmoe-的核心思路专家池整合expert-pool-consolidation">3. ConMoE 的核心思路：专家池整合（Expert-Pool Consolidation）</h2> <p>论文提出了一个新的框架视角，叫 <strong>expert-pool consolidation</strong>（专家池整合）。</p> <p>核心思想拆成两步：</p> <ol> <li>从原来的专家池里<strong>挑出一个更小的子集</strong>作为”原型（prototype）”</li> <li>把每个原来的专家引用<strong>确定性地重映射</strong>到某个 prototype 上</li> </ol> <p>这样，router 对外还是看到原来那些 expert slot，内部只是把调用转发给对应的 prototype。<strong>专家的权重没有被修改，没有被融合，prototype 就是原来的 pretrained 专家</strong>。</p> <p>如下图，直观地展示了 ConMoE 的整体流程：</p> <p><img src="/assets/img/posts/2605_29350/figure1_p3.png" alt="ConMoE 整体架构示意"/></p> <p>图里的流程很清晰：左边是预训练的 MoE，有若干层，每层有自己的专家池。ConMoE 在若干相邻层构成的 local scope 里，用 calibration 统计数据和专家间的距离来选出 prototype，然后把原来的每个专家 slot 都确定性地指向某个 prototype。最右边压缩后的 MoE 对外接口和原来一样（router 不变），只是内部的专家池变小了。</p> <p>这里有一个很巧妙的地方：<strong>prototype 可以跨层共享</strong>。也就是说，相邻几层的专家可以共用一个 prototype 池，这让压缩比更高，同时也利用了相邻层之间专家的冗余性。</p> <hr/> <h2 id="4-怎么挑-prototype两个信号的乘积">4. 怎么挑 prototype？两个信号的乘积</h2> <p>这是方法里最关键的设计。对于每个 local scope $G$，ConMoE 给每个专家算一个分数 $s_e$，然后取 top-K 作为 prototype。</p> <p>分数由两个信号构成：</p> <p><strong>信号一：路由条件贡献分（contribution score）$a_e$</strong></p> \[a_e = \frac{1}{|\mathcal{D}_e|} \sum_{t \in \mathcal{D}_e} g_e(t) \cdot \|e(h_t)\|_2\] <p>说人话：在 calibration 数据上，这个专家被 router 选中的那些 token，它的输出强度乘以路由权重的平均值。分数高 = 这个专家被用到时贡献大。</p> <p><strong>信号二：可替换性分（replaceability score）$b_e$</strong></p> \[b_e = \min_{e' \in \mathcal{E}_G \setminus \{e\}} d(e, e')\] <p>就是这个专家在 scope 内，到其最近邻专家的距离。距离越大 = 越难被其他专家替代 = 越不可替换。</p> <p>两个分数各自在 scope 内做 min-max 归一化后相乘：</p> \[s_e = \bar{a}_e \cdot \bar{b}_e\] <p><strong>直觉很清晰</strong>：一个好的 prototype，既要在原来模型里有实质性的贡献（不能是闲置专家），又要在参数空间里相对独特（不能被其他专家轻易替代）。两个维度同时满足，才是值得被保留的 prototype。</p> <p>选好 prototype 后，每个原始专家就找自己最近的 prototype：</p> \[m_G(e) = \arg\min_{p \in P_G} d(e, p)\] <p>这就是重映射。干净，确定，不需要梯度，不需要 fine-tune。</p> <hr/> <h2 id="5-跨层共享scope-大了不一定好">5. 跨层共享：scope 大了不一定好</h2> <p>ConMoE 允许相邻几层共享一个 prototype 候选池（local scope），这让压缩更灵活。但 scope 到底设多大合适？</p> <p>如下图是在 Qwen3-30B-A3B 上做的消融，50% expert reduction 的情况下，不同 scope size 的效果：</p> <p><img src="/assets/img/posts/2605_29350/figure2_p7.png" alt="scope size 对性能的影响"/></p> <p>结果很有趣：scope 1（只在单层内选）、scope 2、scope 4 的表现非常接近，scope 4 在这个代表性子集上平均分最高。但 scope 一旦扩到 8 或 16 层，三个任务的性能都出现明显下降，HellaSwag 和 MMLU 掉得尤其多。</p> <p>这说明什么？<strong>相邻层之间确实存在可复用的冗余，但这种冗余是局部的</strong>。距离太远的层，对应的是模型不同深度的语义转换，强行共享 prototype 会引入深度不匹配的问题。</p> <p>这也解释了为什么 ConMoE 选择 bounded local scope，而不是一个全局的 expert pool。</p> <hr/> <h2 id="6-主实验结果">6. 主实验结果</h2> <p>实验在三个 MoE 模型上做了对比：Qwen3-30B-A3B、deepseek-moe-16b-base、OLMoE-1B-7B-0125，各自测了 25% 和 50% 的 expert reduction。Baseline 包括：</p> <ul> <li><strong>Pruning 类</strong>：Frequency pruning（按路由频率删）、REAP pruning（基于贡献分删）</li> <li><strong>Merging 类</strong>：M-SMoE（路由统计引导合并）、HC-SMoE（输出特征聚类合并）</li> </ul> <p>如下表是主要实验结果（6 个多选题 benchmark 的平均分）：</p> <p><img src="/assets/img/posts/2605_29350/table1_p6.png" alt="主实验结果表"/></p> <p>几个关键观察：</p> <ul> <li><strong>DeepSeek 上，ConMoE 在 25% 和 50% reduction 两个设置下都拿到最佳平均分</strong>（0.691 vs 0.681/0.676，0.651 vs 0.642/0.613），这是最亮眼的结果。</li> <li><strong>OLMoE 上，25% reduction 同样是 ConMoE 最好</strong>（0.676 vs 0.669/0.662），50% 时和最强 baseline 很接近。</li> <li><strong>Qwen3 上，pruning 方法特别强</strong>，尤其是 50% reduction 时 Frequency pruning 达到 0.733，ConMoE 是 0.717。但 ConMoE 在 WinoGrande 和 BoolQ 上保持了很强的表现。</li> </ul> <p>整体来说，ConMoE 作为一种全新的”不删不合并、只重映射”的压缩范式，<strong>在和 pruning/merging 同等压缩比下能打平甚至超过</strong>，这本身就是一个有意义的结论。</p> <hr/> <h2 id="7-消融实验哪个设计最重要">7. 消融实验：哪个设计最重要？</h2> <h3 id="71-整合结构consolidation-structure">7.1 整合结构（Consolidation Structure）</h3> <p>如下表对比了不同的整合方式：</p> <p><img src="/assets/img/posts/2605_29350/table2_p6_04.png" alt="整合结构消融实验"/></p> <p>三种 variant：</p> <ul> <li><strong>Layer-local</strong>：只在单层内选 prototype，不跨层</li> <li><strong>Cross-layer fixed-k</strong>：跨层候选池，但每层的 prototype 数量固定</li> <li><strong>Adaptive prototype</strong>：ConMoE 默认方案，用贡献分+可替换性自适应分配</li> </ul> <p>有意思的是，layer-local 已经很强了（ARC-C 0.515，HellaSwag 0.715，MMLU 0.651）。允许跨层候选池（Cross-layer fixed-k）在 HellaSwag 上略有提升，Adaptive prototype 和 Cross-layer fixed-k 表现接近。</p> <p><strong>结论：确定性重映射本身才是最重要的成分</strong>，跨层和自适应选择是锦上添花。</p> <h3 id="72-prototype-选择策略">7.2 Prototype 选择策略</h3> <p>如下表比较了不同的 prototype 选择信号：</p> <p><img src="/assets/img/posts/2605_29350/table3_p7_08.png" alt="prototype 选择消融"/></p> <ul> <li><strong>Distance-only</strong>（只看可替换性）：ARC-C 0.242，HellaSwag 0.319，MMLU 0.231 ——直接崩了</li> <li><strong>Usage top-k</strong>（只看路由频率）：HellaSwag 掉到 0.586，比较弱</li> <li><strong>REAP top-k</strong>（基于贡献分）：HellaSwag 回到 0.718，接近 ConMoE 默认</li> <li><strong>Adaptive prototype</strong>（ConMoE 默认）：HellaSwag 0.716，与 REAP top-k 相当</li> </ul> <p><strong>路由条件贡献分是主导信号</strong>，可替换性是辅助正则化（防止选出重复的 prototype），单独用距离作为选择标准完全行不通。</p> <h3 id="73-事后权重融合诊断">7.3 事后权重融合诊断</h3> <p>有人会问：选好 prototype 之后，能不能再做一下权重融合（把分配给同一 prototype 的那些原始专家的权重平均一下），效果会不会更好？</p> <p>如下表给出了答案：</p> <p><img src="/assets/img/posts/2605_29350/table4_p7.png" alt="事后权重融合诊断"/></p> <p>对比三种方案：</p> <ul> <li><strong>None（ConMoE 默认）</strong>：不做任何融合，ARC-C 0.504，HellaSwag 0.716，MMLU 0.639</li> <li><strong>Arcee fusion</strong>：HellaSwag 从 0.716 直接掉到 0.531，MMLU 从 0.639 掉到 0.512</li> <li><strong>Weighted average</strong>：更惨，HellaSwag 0.469，MMLU 0.358</li> </ul> <p><strong>ConMoE 的收益来自确定性重映射，而不是权重融合</strong>。直接复用原始的 pretrained prototype 才是最稳的策略，加了融合反而破坏了原始权重。</p> <hr/> <h2 id="8-跨层专家可替换性分析">8. 跨层专家可替换性分析</h2> <p>为什么跨层 scope 有效，但又不能太大？论文专门做了一个几何分析。</p> <p>在 Qwen3-30B-A3B 上，用 4 层 scope 计算每个 expert 在参数空间的最近邻，统计最近邻是否在同一层还是不同层：</p> <p><img src="/assets/img/posts/2605_29350/figure3_p8.png" alt="跨层最近邻分析"/></p> <p>左图是热力图，展示从 source layer 到最近邻 layer 的分布。可以看到主要沿对角线集中，说明最近邻大概率在附近的层，而非全局随机分布。</p> <p>右图是每层中”最近邻在不同层”的比例。<strong>整体上有 50.4% 的 expert，其参数空间的最近邻在另一层</strong>，部分中间层超过 70%。</p> <p>这两个观察共同说明：<strong>专家的可替换性不是严格层内的，相邻层之间存在真实的冗余</strong>——这是跨层共享 prototype 的几何依据。但这种冗余是局部的，不支持全局共享。</p> <hr/> <h2 id="9-总结">9. 总结</h2> <p>ConMoE 提供了一个很干净的框架来思考 MoE 压缩：</p> <ul> <li><strong>不删</strong>（pruning 的做法）</li> <li><strong>不改权重</strong>（merging 的做法）</li> <li><strong>只重映射</strong>：选出 pretrained 专家子集作为 prototype，把其他专家的引用指过去</li> </ul> <p>三个设计原则从实验里验证：</p> <ol> <li><strong>确定性重映射是最稳定的成分</strong>，单层内做都已经很强</li> <li><strong>跨层复用要保持局部</strong>，相邻层可以，太远的层会因深度不匹配掉性能</li> <li><strong>路由条件贡献分是主导信号</strong>，可替换性是辅助的正则化</li> </ol> <p>个人觉得这篇论文最有价值的点，是把”保留哪些专家参数”和”原来的 expert slot 怎么被表示”这两个问题<strong>显式地解耦</strong>了。这个视角在之前的 pruning/merging 框架里是隐含的，ConMoE 把它明确化之后，整个设计空间就清晰多了。</p> <p>当然 limitation 也直接：这套方案依赖 calibration 数据，跨层 scope 不能太大，而且现在的”logical reduction ratio”和实际的显存节省还需要专门的 runtime 支持（shared-prototype checkpoint 格式）才能落地。最后这点是整个 prototype sharing 方案的工程挑战，不只是这篇论文的问题。</p> <p>感兴趣的可以去翻原文，代码和复现脚本作者也有提到会开源。</p> <p>欢迎评论区交流，有什么遗漏或者理解有误的地方也欢迎指出~</p>]]></content><author><name></name></author></entry><entry><title type="html">ISCA’26 最佳论文 | 44 亿种专家组合背后，其实藏着可预测的规律</title><link href="https://marsggbo.github.io/blog/2026/20260730-moe-chaos/" rel="alternate" type="text/html" title="ISCA’26 最佳论文 | 44 亿种专家组合背后，其实藏着可预测的规律"/><published>2026-07-30T00:00:00+00:00</published><updated>2026-07-30T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260730-moe-chaos</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260730-moe-chaos/"><![CDATA[<h1 id="isca26-最佳论文--44-亿种专家组合背后其实藏着可预测的规律">ISCA’26 最佳论文 | 44 亿种专家组合背后，其实藏着可预测的规律</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2510.05497">Patterns behind Chaos: Forecasting Data Movement for Efficient Large-Scale MoE LLM Inference</a></p> </blockquote> <hr/> <h2 id="1-前言吐槽">1. 前言吐槽</h2> <p>做 MoE 推理优化，有个问题绕不过去：<strong>专家选择到底是不是随机的？</strong></p> <p>这个问题看起来很学术，但它直接决定了你能做什么优化。如果专家选择完全随机，那 prefetch、cache、replication 这一整套系统优化的思路全都不成立——你没法预测下一步要用哪个专家，就只能傻等数据搬过来。DeepSeek V3 每层 256 个专家选 8 个，$C_{256}^{8}$ = <strong>4,426,165,368</strong> 种组合。44 亿。面对这个数字，”随机”几乎是个下意识的结论。</p> <p>但我们做优化的人心里一直有个直觉：<strong>它不该是随机的</strong>。因为 MoE 的 gating 是学出来的，学出来的东西就该有结构。问题是没人拿大模型系统性地验证过——之前的工作最多profile一两个 Mixtral 8x7B 这种量级的小模型，报几个表面观察就结束了。</p> <p>这篇 ISCA’26 最佳论文把这件事做实了。<strong>四个 2025 年的 SOTA 大模型（235B 到 1000B），24000+ 条请求，2000+ GPU 小时，150GB 的 expert selection trace</strong>，然后给出六条可以直接指导系统设计的 insight。今天想和大家聊聊这篇工作到底测出了什么，以及为什么这些结论对做 MoE serving 的人很重要。</p> <hr/> <h2 id="2-先交代背景为什么数据搬运是-moe-的核心矛盾">2. 先交代背景：为什么数据搬运是 MoE 的核心矛盾</h2> <p>MoE 的宣传语一直是”稀疏激活，参数量涨了但推理开销不涨”。听着很美好，落地的时候第一个撞上的墙就是<strong>数据搬运</strong>。</p> <p>先看这篇论文给的 latency breakdown。DeepSeek V3、4K 序列长度，在三种真实 serving 配置下：</p> <p><img src="/assets/img/posts/20260730-moe-chaos/figure2_p2.png" alt="DeepSeekV3 不同 serving 配置下的延迟拆解"/></p> <p>MoE 相关的数据搬运（MoE All-to-All + MoE Weight）<strong>占了总延迟的 60%-90%</strong>。注意这不是 attention 慢、不是 GEMM 慢，是”把数据搬到该去的地方”这件事本身慢。</p> <p>为什么会这样？因为 MoE 的稀疏激活是<strong>动态</strong>的。token 走到 gating 之前，你不知道它要去哪个专家。而现代大模型的部署方式必然是多单元的——DeepSeek V3 671B 参数要 32+ 张卡，专家分散在不同 GPU 上。于是每一层 MoE 都要做一次全局的 token 重排（all-to-all），再把结果搬回来。<strong>这也是整个行业推 MoE 落地时绕不过去的核心矛盾：稀疏计算的收益，被动态搬运的开销吃掉了。</strong></p> <p>模型还在往大走。看这张图，横轴是发布时间，纵轴是参数量，气泡大小是每层专家数：</p> <p><img src="/assets/img/posts/20260730-moe-chaos/fig1_full.png" alt="MoE 模型参数量与发布时间趋势"/></p> <p>2025 年这一波（红色气泡）是 DeepSeek V3、Kimi K2、Llama4-Maverick、Qwen3-235B，也就是这篇论文 profile 的四个。橙色的 DeepSeek V4 和 GLM-5 是论文写完之后才出的。趋势非常明确：<strong>参数量和专家数都在快速涨，搬运问题只会越来越严重</strong>。</p> <hr/> <h2 id="3-论文的方法论从-system-centric-翻转到-model-centric">3. 论文的方法论：从 system-centric 翻转到 model-centric</h2> <p>这篇工作我最欣赏的一点是它的定位。</p> <p>以前的 MoE 优化工作是 <strong>system-centric</strong> 的：先选定一个平台（CPU-GPU offload、多 GPU 集群、PIM 架构），然后针对这个平台的数据搬运特性做优化。MoE-Lightning 做 CPU 内存 offload，Comet 和 MegaScale-Infer 做多 GPU 通信重叠，Duplex 上 processing-in-memory。这些工作都很好，但它们的 insight 是<strong>平台绑定</strong>的——换个部署形态就不一定成立，而且每篇只看到整体规律的一个切片。</p> <p>这篇论文把流程反过来，做 <strong>model-centric</strong> 的分析：<strong>不绑定任何系统，只 profile 模型本身的专家选择行为</strong>，得出的规律因此是 system-agnostic 的，能往各种平台上套。</p> <p>具体怎么切？作者把 expert selection 的规律分成两个维度：</p> <p><img src="/assets/img/posts/20260730-moe-chaos/fig3_full.png" alt="MoE 推理流程与两个维度的划分方式"/></p> <ul> <li><strong>Temporal（时间维度）</strong>：当前的专家选择能不能预测未来的选择？这决定了你能不能做 prefetch / cache / migration，是<strong>单个计算单元内部</strong>的优化。图里左半部分标了三个观察尺度：Ob1 层级（相邻两层之间）、Ob2 token 级（相邻两个 token 的同一层）、Ob3 prefill-decode 级（两个阶段之间）。</li> <li><strong>Spatial（空间维度）</strong>：在一个时间窗口内，专家激活怎么分布到各个单元上？这决定了你怎么做 expert placement 和负载均衡，是<strong>跨单元</strong>的优化。图里右半部分是 Ob4（单个专家的激活倾斜）和 Ob5（专家对的共激活亲和性）。</li> </ul> <p>这个切法很清晰：<strong>时间维度管”预取什么”，空间维度管”放在哪”</strong>。下面按这两个维度看结论。</p> <p>整篇论文的逻辑链我画了一张图，可以对着往下读：</p> <p><img src="/assets/img/posts/20260730-moe-chaos/diagram_insights.png" alt="从 profiling 到六条 insight 的推导关系"/></p> <hr/> <h2 id="4-temporal专家选择在时间上确实可预测">4. Temporal：专家选择在时间上确实可预测</h2> <h3 id="41-ob1-层间相关性">4.1 Ob1 层间相关性</h3> <p>先看相邻两层。热力图的含义是：<strong>上一层激活了专家 i 的条件下，下一层激活专家 j 的条件概率</strong>，亮的地方概率高。</p> <p><img src="/assets/img/posts/20260730-moe-chaos/figure4_p3.png" alt="相邻层专家选择的条件概率热力图和 CDF"/></p> <p>看 (a) DeepSeek V3 和 (b) Qwen3 的热力图，能看出几个东西：</p> <ul> <li><strong>白点</strong>：某些特定的专家对，跨层选择概率显著高于其他组合</li> <li><strong>亮的竖线</strong>：某些专家不管上一层选了谁都会被频繁选中——这就是”热门专家”，后面空间维度会专门讲</li> <li>Qwen3 的热力图整体更亮，说明它的层间相关性比 DeepSeek 更强</li> <li>同一个模型不同层的模式也不一样，第 3-4 层和第 30-31 层长得不同</li> </ul> <p>光看热力图容易主观，作者用条件 CDF 量化了一下。(c) 图的读法：横轴是”取下一层候选专家的前百分之多少”，纵轴是”这些候选覆盖了多少条件概率质量”。结论是<strong>取 top 20% 的候选，就能覆盖 50%（DeepSeek V3）、65%（Qwen3）、77%（Llama 4）、56%（Kimi K2）的概率质量</strong>。</p> <p>换句话说，如果你愿意预取 20% 的专家，就有一半以上的把握命中。这个数字对 prefetch 设计来说完全够用了。相关性强度是 <strong>Llama 4 最强、DeepSeek 最弱</strong>。</p> <h3 id="42-ob2-token-间相关性">4.2 Ob2 token 间相关性</h3> <p>再看同一层、相邻两个 token 之间：</p> <p><img src="/assets/img/posts/20260730-moe-chaos/fig5_full.png" alt="相邻 token 专家选择的条件概率热力图"/></p> <p>白点、亮竖线这些跟层间类似，但<strong>多了一个所有模型共有的新特征：明显的对角线</strong>。</p> <p>对角线意味着什么？意味着<strong>相邻 token 倾向于选同一个专家</strong>。这个特征对 cache 设计极其有价值——你把这一个 token 用过的专家留在本地，下一个 token 有相当概率直接命中。</p> <p>更有意思的是这个对角线<strong>只在高层出现（17 层、43 层），低层（1 层、3 层）没有</strong>，而且四个模型都是这个规律。我的理解是：低层还在处理比较通用的词法/句法特征，token 之间差异大；到了高层已经在处理语义了，相邻 token 在语义上高度连续，所以路由到同一批专家。</p> <p>CDF 量化的结果：top 20% 的下一个 token 候选专家覆盖 <strong>47%（DeepSeek V3）、62%（Qwen3）、80%（Llama 4）、53%（Kimi K2）</strong> 的累积条件概率。同样是 Llama 4 最强、DeepSeek 最弱。</p> <h3 id="43-ob3-prefill-和-decode-的一致性">4.3 Ob3 prefill 和 decode 的一致性</h3> <p>这是我认为<strong>最有工程价值的一条</strong>。作者对比了 prefill 阶段和 decode 阶段的专家选择模式：</p> <p><img src="/assets/img/posts/20260730-moe-chaos/figure6_p4.png" alt="prefill 与 decode 阶段的热力图对比及 Spearman 相关性"/></p> <table> <tbody> <tr> <td>(a)(b) 是 cross-layer 热力图的 prefill / decode 版本，(c)(d) 是 cross-token 的，肉眼看亮点分布高度相似。(e)(f) 用 <strong>Spearman 相关系数</strong>做了量化——这个系数衡量两个变量的单调关系，范围 -1 到 1，一般</td> <td>ρ</td> <td>&gt; 0.7 算强相关，0.4 &lt;</td> <td>ρ</td> <td>≤ 0.7 算中等相关。结果是<strong>大部分层都落在强相关区间</strong>，少数层是中等相关。</td> </tr> </tbody> </table> <p>除了专家对的热力图，单个专家的频率分布也一致：</p> <p><img src="/assets/img/posts/20260730-moe-chaos/fig7_full.png" alt="prefill 与 decode 的专家频率分布及 top 专家重叠率"/></p> <p>(a) 是 Qwen3 第 66 层的专家选择频率，蓝色 prefill、橙色 decode，两条分布基本贴在一起（低频专家那里有些偏差）。(b) 是 top 专家的重叠率：<strong>prefill 的 top-5 专家覆盖了 decode top-5 的约 60%，top-10 覆盖 75%，top-20 覆盖 90%</strong>。(c) 确认这个关系在四个模型上都成立。</p> <p>这条为什么重要？因为它解决了一个<strong>真实的工程空窗期</strong>。现在大规模 MoE serving 用 EPLB（Expert Parallelism Load Balancer）来动态调整专家放置，但 EPLB 需要先攒够 profiling 数据才能生效——<strong>要 3000+ 步才触发一次</strong>。那前面这 1000 个 decode token 怎么办？尤其是短输出的请求，EPLB 从头到尾都攒不够数据。</p> <p>Ob3 给的答案是：<strong>用 prefill 阶段的 trace 去预测 decode</strong>。这在现在流行的 PD 分离架构里特别顺——prefill 和 decode 本来就在不同机器上跑，prefill 机器顺手把 trace 传过去就行。</p> <h3 id="44-从时间维度出来的两条-insight">4.4 从时间维度出来的两条 insight</h3> <p><strong>Insight 1：用 prefill 数据驱动预测（来自 Ob3）</strong>。拿 prefill 阶段的专家选择 trace 去预测 decode 阶段的选择，尤其适合 decode 刚开始、历史数据还很少的时候。</p> <p><strong>Insight 2：跨层次内存管理（来自 Ob1、Ob2）</strong>。这条的巧思在于把两种相关性映射到不同的内存层级上，理由是<strong>reuse distance 不一样</strong>：</p> <ul> <li><strong>层级相关性 reuse distance 短</strong>——相邻 MoE 层是紧接着执行的，中间隔的时间很短</li> <li><strong>token 级相关性 reuse distance 长</strong>——要遍历完所有层才生成下一个 token</li> </ul> <p>而现代系统的内存层级刚好是分层的。以多 chiplet 架构为例，每个 die 上有 LLC（小而快）和 local DRAM（大而慢）：<strong>用层级相关性管 LLC，用 token 级相关性管 DRAM</strong>。这个原则能往其他配置上套——CXL 系统（本地 DRAM + 远端 CXL 内存）、SSD offload 系统（DRAM + flash）、PIM 系统（本地和远端 DRAM die），都是”层级相关性管快的那层，token 级相关性管慢的那层”。</p> <hr/> <h2 id="5-spatial负载倾斜和专家对亲和性">5. Spatial：负载倾斜和专家对亲和性</h2> <h3 id="51-ob4-单专家激活倾斜">5.1 Ob4 单专家激活倾斜</h3> <p>看 Llama4 第 7 层各专家的激活频率：</p> <p><img src="/assets/img/posts/20260730-moe-chaos/fig8_full.png" alt="Llama4 第 7 层的专家激活倾斜与任务相关性"/></p> <p>(a) 图非常直观：<strong>少数专家的激活频率超过平均值的 16 倍</strong>（红线是平均值）。这种倾斜直接导致负载不均衡——存着热门专家的那个单元被打爆，其他单元在等它。</p> <p>(b)(c) 是我觉得这篇论文里更有意思的部分。作者跑了 MMLU 全部 57 个学科（生物、历史、数学等等），画出每个学科的 top 10 热门专家：</p> <ul> <li><strong>横向亮线</strong>：某些专家不管什么学科都热门——这是全局热点</li> <li>除此之外，各学科的热门专家<strong>差异明显</strong>——说明确实存在任务特化</li> </ul> <p>然后作者做了个很漂亮的对照实验：用 MMLU Pro 的<strong>中文版本</strong>，题目内容完全相同，只换语言。结果 (c) 图的模式<strong>明显不同</strong>——虽然也有 5-6 个专家跨学科热门，但<strong>只有两个和英文 MMLU 的热门专家重叠</strong>。</p> <p><strong>内容相同、只换语言，激活的专家就换了一批</strong>。这个发现挺反直觉的，说明 gating 学到的不只是”语义领域”，还包含了语言层面的表面特征。对系统设计来说这是个好消息：<strong>语言/任务类型是可以提前知道的元信息</strong>，可以拿来做主动的专家放置。</p> <h3 id="52-ob5-专家对共激活亲和性">5.2 Ob5 专家对共激活亲和性</h3> <p>超越单个专家，看专家<strong>两两之间</strong>是否倾向于同时被激活：</p> <p><img src="/assets/img/posts/20260730-moe-chaos/figure9_p6.png" alt="专家对共激活热力图与 CDF"/></p> <p>热力图的每个像素是一个专家对，值是共激活频率<strong>除以理论随机概率</strong> $p = \frac{2}{n(n-1)}$（$n$ 是专家数）。所以值等于 1 就是”和随机没区别”。</p> <p>结果是<strong>亮点的概率达到理论值的 20-40 倍</strong>。这远远超出随机能解释的范围。</p> <p>(a) 图里 DeepSeek 的热力图有个特别的结构：<strong>频繁激活的专家对集中在红线之间形成的亮方块里</strong>。这不是自然涌现的，是 DeepSeek 的路由限制导致的——它限制 token 只路由到相邻节点来减少通信开销，所以共激活自然集中在同组内。</p> <p>(c) 图量化：<strong>top 10% 的专家对占了总共激活量的 60-80%</strong>。倾斜非常强。</p> <p>（Llama 每层只选一个专家，所以没有共激活关系，这里只分析了 DeepSeek 和 Qwen。）</p> <h3 id="53-从空间维度出来的四条-insight">5.3 从空间维度出来的四条 insight</h3> <p>空间维度对应的是<strong>粗粒度、静态</strong>的策略，可以在系统启动时或者周期性重分布（比如每 10 分钟）的时候应用。</p> <p><strong>Insight 3：专家放置感知的负载分配（Ob4、Ob5）</strong>。分配任务的时候要考虑专家实际放在哪。这里有个思路上的转变值得注意：传统多 GPU 系统倾向于”把计算分配到专家所在的本地 GPU”，避免跨单元通信。但在 multi-chiplet GPU 上，<strong>inter-unit 通信变快了，所以可以考虑把任务分给远端 die 来换取更好的负载均衡</strong>。设计空间比以前大。</p> <p><strong>Insight 4：热门专家去中心化（Ob4）</strong>。把热门专家在多个单元上复制，摊平负载。另外要<strong>避免把多个热门专家放在同一个单元</strong>。</p> <p><strong>Insight 5：专家对分离（Ob5）</strong>。把频繁共激活的专家对分到不同计算单元，最大化并行度。但论文很诚实地指出这条有 trade-off：<strong>分离会引入跨单元通信开销</strong>，是否值得取决于系统拓扑和 batch size。</p> <p><strong>Insight 6：workload 感知的 serving 系统（Ob4）</strong>。利用任务类型、语言这些元信息，在 serving 之前就做专家迁移。比如已知当前是英文流量，就提前复制/重分配英文相关的专家。关键是这个 task-to-expert 映射<strong>只需要一次离线 profiling，之后整个部署周期都能复用</strong>，成本很低。</p> <hr/> <h2 id="6-case-study-1wafer-scale-gpu-上-66-提速">6. Case Study 1：wafer-scale GPU 上 6.6× 提速</h2> <p>Insight 光说不练没意思，论文做了两个 case study。第一个是未来的 wafer-scale GPU。</p> <h3 id="61-为什么是-wafer-scale">6.1 为什么是 wafer-scale</h3> <p>这个背景值得交代一下。单 die 面积受 photomask 限制卡在 800-1000 mm²，Moore’s Law 也快到头了，所以厂商都在往 multi-chiplet 走：AMD MI300 集成 8 个计算 chiplet，NVIDIA Blackwell 是 2 个，即将出的 Rubin 预计 4 个。再往前一步就是 wafer-scale——<strong>TSMC 的 System-on-Wafer 能在一片晶圆上放 24 个计算 die 和 96 个 HBM die，面积超过 200,000 mm²</strong>，HBM 容量 3TB+，算力到 PFLOPS 级别。</p> <p><img src="/assets/img/posts/20260730-moe-chaos/fig10_full.png" alt="wafer-scale GPU 架构、SoW 技术与 Global CP 的数据结构"/></p> <p>(b) 是 SoW 的物理结构：每个 GPU die 通过 LSI（local silicon interconnect）连本地 HBM，相邻 GPU die 之间纵向走 LSI、横向走 XSR SerDes。虽然 LSI 和 SerDes 带宽都到 Tb 级，<strong>inter-die 通信仍然是主要瓶颈</strong>——远端访问要跨多跳，延迟高；多个 die 同时访问远端 HBM 还会带宽争抢。<strong>本地 vs 远端访问的开销差距可以到 15 倍</strong>。</p> <h3 id="62-编程模型的选择">6.2 编程模型的选择</h3> <p>这里有个设计决策值得单独说。wafer-scale 芯片的编程模型现在是开放问题，有两条路：</p> <p><strong>Multi-GPU-like</strong>：把整片晶圆暴露成一个多 GPU 系统，程序员能细粒度控制每个 die。WSC-LLM、MoEntwine 走这条。灵活，但和产业趋势不符——Blackwell 和 Rubin 都是多 die，但 NVIDIA <strong>根本没暴露 die 级细粒度控制的工具链</strong>。MIG 倒是能把多 die GPU 切成独立实例，但这个模式下<strong>高速 D2D 链路是被禁用的</strong>，inter-die 通信被迫走慢 10-100 倍的 NVLink 或 PCIe，多 die 封装的好处就没了。</p> <p><strong>Single-GPU-like</strong>：把整片晶圆暴露成一个统一的 GPU，完全屏蔽多 die 拓扑和数据放置。这篇论文选的是这条，理由有两个：和 Blackwell/Rubin 这些商用多 chiplet GPU 对齐，有产业相关性；程序员不用管 NCCL/NVSHMEM 那套显式通信。</p> <p>但<strong>选了 single-GPU-like，优化负担就全压到硬件上了</strong>——程序员看不到跨 die 的数据搬运，那就只能靠架构层面去优化。这也是这个 case study 要改硬件的根本原因。</p> <h3 id="63-当前-gpu-架构的两个不足">6.3 当前 GPU 架构的两个不足</h3> <p><strong>任务分配太简单</strong>。现在 GPU 的 command processor 把所有 SM 一视同仁，<strong>完全不管 SM 的物理位置和数据放在哪</strong>。这种分配方式在 wafer-scale 上会产生大量 D2D 流量，而且忽略了专家选择的倾斜性，结果是大部分 die 闲着、少数 die 过载。</p> <p><strong>本地 HBM 管理不足</strong>。现在 GPU 把所有 HBM die 当成统一内存空间，但 wafer-scale 上每个计算 die 直连本地 HBM，<strong>本地访问比远端快得多</strong>。热门的远端专家本来可以缓存到本地，但当前架构不区分本地和远端，白白产生额外流量。</p> <h3 id="64-两个改动">6.4 两个改动</h3> <p>对应上面两个问题，论文做了两处架构扩展（上图 (a) 里橙色的部分）：</p> <p><strong>第一，Command Processor 改成两级</strong>：晶圆级的 Global CP + 每个 die 内的 Local CP。Global CP 维护全局的专家选择和放置信息。它管两个数据结构（上图 (c)）：</p> <ul> <li><strong>Expert Distribution Table</strong>：每个专家的初始 die ID + 一个 n 位二进制码，每位表示该专家在对应 die 上是否存在</li> <li><strong>Cross-token Heatmap</strong>：专家激活的历史模式，给预测用</li> </ul> <p><strong>第二，D2D controller 扩展出 ATU 和 PDU</strong>：</p> <ul> <li><strong>ATU（Address Translation Unit）</strong>：远端数据被复制到本地后，把远端地址翻译成本地地址</li> <li><strong>PDU（Prediction Unit）</strong>：判断哪些远端数据值得复制。每个 PDU 维护一张 prediction table，每个专家两个字段：<code class="language-plaintext highlighter-rouge">cp_en</code>（是否该缓存到本地，由 Global CP 算好下发）和 <code class="language-plaintext highlighter-rouge">is_local</code>（是否已经缓存在本地 HBM）</li> </ul> <p>远端数据读取的流程变成这样：SM 读一个不在本地的远端数据 → D2D controller 正常路由 → 数据返回时 PDU 查 prediction table 做复制决策，<strong>不管决策如何都先把数据给 SM</strong>（不阻塞）→ 如果要复制，PDU 写入 LLC 和本地 HBM，更新 ATU 里的地址，把 <code class="language-plaintext highlighter-rouge">is_local</code> 置 1。下次再读同一个数据，ATU 直接把远端地址翻译成本地地址，重定向到 LLC。</p> <h3 id="65-两个算法">6.5 两个算法</h3> <p><img src="/assets/img/posts/20260730-moe-chaos/figure11_p9.png" alt="任务分配算法与数据驱动预测器的示意"/></p> <p><strong>任务分配算法</strong>（对应 Insight 3）。精确的任务分配是 NP-hard 的，所以用两个启发式：候选机制缩小要考虑的 die 集合，块粒度分配在候选里搜近似解。</p> <p><img src="/assets/img/posts/20260730-moe-chaos/algo1_full.png" alt="任务分配算法伪代码"/></p> <p>走一遍这个算法：输入 <code class="language-plaintext highlighter-rouge">expert_reqs_dict</code> 是每个专家有多少请求，<code class="language-plaintext highlighter-rouge">expert_die_map</code> 是专家放在哪（来自 Expert Distribution Table）。</p> <ol> <li>先按请求数<strong>升序</strong>排序专家（第 2 行）——请求少的先分配</li> <li>对每个专家，生成候选 die 列表 = 存了这个专家权重的 die + 它们的相邻 die（上图 (a) 里绿色是本地 die，红色是远端 die）</li> <li>候选按当前负载排序，列表长度限制在 <code class="language-plaintext highlighter-rouge">maxsplitnum</code> 以内，这个值由该专家的请求数决定</li> <li>请求按<strong>块大小 50</strong> 分发到候选 die（第 6-11 行）——粒度太细搜索开销大，太粗均衡效果差，50 是效率和精度的折中</li> <li>每一块用 cost model 选最优 die，cost model 考虑 DRAM 访问、计算、die-to-die 通信三项</li> <li>最后把分到同一个 die 的块合并（第 12 行）</li> </ol> <p><strong>数据驱动预测器</strong>（对应 Insight 1、2）。用当前 MoE kernel 的专家选择去预测下一个 token 在每个 die 上会用哪些热门专家。看上图 (b)：当前 token 选了专家 (1, 4) → ① 从 cross-token heatmap 里取出对应的行（红色阴影）→ ② 每行取 top n 专家 → ③ 对应的下一 token 专家就是预测结果（绿色阴影），这里预测出 (2, 4, 6)。因为这个 die 当前只读了专家 1 和 4，<strong>所以只把专家 4 复制到它的本地 DRAM</strong>。</p> <h3 id="66-效果">6.6 效果</h3> <p>评测用自己写的 multi-chiplet 模拟器（现有工具都不合用：Gem5/gpgpusim/mgpusim 是 cycle-accurate 的，20+ die、batch 15000+ 的规模跑不动；ASTRA-sim 支持多 GPU 但微架构建模不够细，也不支持 single-GPU-like 编程模型）。模拟器<strong>对着 8×H100 DGX 实测校准过，误差在 5% 以内</strong>。</p> <p>硬件配置两种拓扑：Tesla Dojo（5×5 mesh）和 TSMC SoW（8×3 mesh），每个 chiplet 是 H100 级别（989 TFLOPS FP16、80GB HBM、3.35TB/s 本地带宽、1.7TB/s inter-die 带宽）。</p> <p><img src="/assets/img/posts/20260730-moe-chaos/fig12_full.png" alt="MoE 层吞吐（上）与跳数削减比（下）"/></p> <p><strong>跨模型看</strong>：Allo+Pred 在 DeepSeek、Kimi、Llama、Qwen 上分别拿到 <strong>7.0×、8.2×、7.3×、4.1×</strong> 吞吐提升（论文摘要里的 6.6× 是平均值）。DeepSeek 和 Kimi 收益更大，因为它们专家数更多（256 vs 128）、选择模式更复杂——<strong>优化空间本来就更大</strong>。</p> <p><strong>跨拓扑看</strong>：Dojo 上 6.0×，TSMC 上 7.5×。die 数量差不多（25 vs 24），但 <strong>TSMC 的长条形布局让 die 之间平均距离更远</strong>，不做策略性任务分配的话跨单元通信更多，所以我们的策略在这里收益更大。</p> <p><strong>和 EP 基线对比</strong>（EP = 把每个专家的计算都分到它所在的 die，完全消除 D2D 通信，但可能严重负载不均）：batch 4096 这种小 batch 下两者差不多——<strong>每个专家分到的 token 太少，执行是 memory-bound 的，把一个专家拆到多个 die 上没有收益，我们的策略退化成 EP</strong>。优势在大 batch 出现，batch 16384 时比 EP 快 <strong>1.44×</strong>。</p> <p><strong>跳数削减这里有个很有意思的现象</strong>，值得单独说。跳数是所有跨单元通信的曼哈顿距离之和：</p> <ul> <li><strong>Pred Only</strong>：跳数降 4.5×，性能提升 3.0×——比较接近，说明<strong>跨单元通信确实是 baseline 的主要瓶颈</strong>，减少跳数就能按比例提升性能</li> <li><strong>Allo Only</strong>：跳数降 <strong>142×</strong>，但性能只提升 6.3×——严重不成比例</li> <li><strong>Allo+Pred</strong>：跳数降 <strong>213×</strong>，性能提升 6.63×，比 Allo Only 只多了 1.1×</li> </ul> <p>这说明什么？<strong>跳数已经不是瓶颈了</strong>。任务分配算法把大部分任务分给了持有相关专家的本地 die，只有极热门的专家需要往远端分配，D2D 流量已经压到很低，<strong>瓶颈转移到了负载分配本身</strong>。这是个很好的例子：优化到一定程度，你盯的那个指标就不再是瓶颈了，继续压它收益递减。</p> <p>DRAM 访问的拆解印证了这一点：</p> <p><img src="/assets/img/posts/20260730-moe-chaos/figure14_p11.png" alt="DRAM 访问拆解：本地读/远端读/本地写"/></p> <p>baseline 里大部分读都是远端读，用了我们的策略之后<strong>大部分远端读被转成了本地读</strong>。Allo+Pred 相比 Pred Only 远端读更少（任务大多分到本地 die），相比 Allo Only 也更少（热门专家缓存在本地 HBM）。</p> <p><strong>为什么一定要改硬件？</strong> 论文做了个对照：任务分配算法理论上也能跑在 host CPU 上。结果在 Dojo 配置下，host CPU 方案的开销是 DeepSeek V3 的 5.2%-6.4%、Qwen3 的 11.1%-14.2%；换到 Dojo-Enhanced（die 换成 B300 级别，4500 TFLOPS）<strong>开销飙到 DeepSeek 的 19.3%-23.8%、Qwen3 的 42.0%-51.6%</strong>。</p> <p>两个原因：Qwen3 比 DeepSeek 差是因为它 MoE 层更多（94 vs 58），CPU-GPU 之间要过 PCIe 的次数更多，而且单层计算量更小，传输开销占比被放大。Dojo-Enhanced 比 Dojo 差 3.7 倍以上，是因为 die 变快了，<strong>固定的 PCIe 传输开销占比更重</strong>。结论很清楚：<strong>GPU 算力增长快于互连带宽增长，把分配器放进 GPU command processor 是越来越必要的</strong>。</p> <p>硬件开销方面，设计支持最多 100 层 × 512 专家（远超 Kimi-K2 的 61 层 384 专家）。完整 heatmap 50MB 放在 Global CP 的 DRAM 里，片上只留 0.5MB 缓存一层。<strong>总面积和功耗开销都低于 0.04%</strong>。</p> <hr/> <h2 id="7-case-study-2真实-8h100-上的-prefill-感知专家放置">7. Case Study 2：真实 8×H100 上的 prefill 感知专家放置</h2> <p>第二个 case study 不需要改硬件，是纯软件的，落地更近。</p> <p>问题是前面提过的 EPLB 空窗期：EPLB 3000+ 步才触发一次，<strong>最初 ~1000 个 decode token 没有 profiling 数据可用</strong>，短输出请求更是从头到尾都攒不够。Insight 1 说 prefill 和 decode 相关，那就用 prefill trace 来定 decode 的初始专家放置。</p> <p><img src="/assets/img/posts/20260730-moe-chaos/figure16_p12.png" alt="两种专家放置策略：Remap 与 Dup"/></p> <p>两个算法：</p> <p><strong>Remap-based</strong>：保持每个 GPU 的专家数量不变，重新分配专家的归属。做法是按 roofline cost 降序排列专家，贪心地把每个专家分给当前负载最小的 GPU，约束是每个 GPU 恰好 $E/G$ 个专家。</p> <p><strong>Duplication-based</strong>：在每个 GPU 上预留额外的专家槽位，用 prefill trace 把热门专家复制进去。从默认的连续布局出发（GPU 0 放专家 0-15、GPU 1 放 16-31……），贪心地每次加一个副本，选那个能<strong>最大程度降低瓶颈负载 $\max_g load_g$</strong> 的 (专家, GPU) 组合。被复制专家的 token 在所有副本之间平均分。</p> <p>两个算法都用 roofline cost model 估计每个 GPU 的负载。</p> <p>实验是 Qwen3-235B（94 个 MoE 层、每层 128 专家选 8）+ SGLang + 8×H100 NVLink，MoE 后端用 DeepEP，通过 SGLang 的 <code class="language-plaintext highlighter-rouge">init_expert_location</code> 接口控制放置，<code class="language-plaintext highlighter-rouge">ep_dispatch_algorithm</code> 设成 dynamic 保证副本间均分 token。指标只算 MoE 计算时间（三个 expert linear 层），不含 attention、all-to-all、top-k。</p> <p><img src="/assets/img/posts/20260730-moe-chaos/figure17_p13.png" alt="prefill 感知专家放置的性能"/></p> <p>Remap 和 Dup 相比 Default 分别快 <strong>15.5%</strong> 和 <strong>12.5%</strong>，相比 Worst 快 <strong>2 倍以上</strong>，而且<strong>都在 Best 的 10% 以内</strong>——注意 Best 是用 oracle decode 信息算出来的理论最优，实际拿不到。两个算法表现接近，可以按内存和系统约束自由选。</p> <p>这里论文有一段自我批评我觉得很实诚：<strong>8 卡的 EP 规模本身就限制了能拿到的提升</strong>。EP8 下每张卡放 16 个专家，冷热专家自然混在一起，<strong>即使用默认布局负载也相对均衡</strong>（最大/最小执行时间比只有约 1.3×）。所以真实的大规模部署（200+ GPU）里不均衡更严重，提升空间应该更大。这种不吹不黑的表述在最佳论文里挺常见的——<strong>审稿人最烦的就是过度声明</strong>。</p> <hr/> <h2 id="8-我的几点-take">8. 我的几点 take</h2> <p><strong>第一，这篇论文最大的价值不是那两个 case study，是 trace 本身</strong>。论文自己也这么定位——两个 case study 只是证明 insight 可用，主要贡献是 profiling。150GB 的 expert selection trace 和多 chiplet 模拟器都开源了（<a href="https://huggingface.co/datasets/core12345/MoE_expert_selection_trace">HuggingFace</a>），这意味着后面做 MoE serving 的人<strong>不用再花 2000 GPU 小时去重新 profile</strong>。这种”做基础设施”的工作在系统方向是最难被承认的，能拿最佳论文说明 ISCA 的审稿品味不错。</p> <p><strong>第二，”随机”这个假设值得反复质疑</strong>。44 亿种组合听起来是天文数字，但实际的条件概率分布高度集中——top 20% 覆盖 50%-80%。这提醒我一件事：<strong>组合空间大小和实际分布的熵是两个完全不同的东西</strong>。以后看到”这个空间太大不可能预测”的论断，我会先问一句实际分布长什么样。</p> <p><strong>第三，语言影响专家选择这个发现被低估了</strong>。内容完全相同、只换语言，热门专家就换了一批，只有两个重叠。这件事对多语言服务的部署有直接影响——<strong>如果你的流量是中英混合，按单一语言 profile 出来的专家放置可能是错的</strong>。而且这也让我对 MoE 的 gating 到底学到了什么更好奇了，它显然不只是在做语义路由。</p> <p><strong>第四，跳数降 213× 但性能只提升 6.63× 这个现象很有教育意义</strong>。做优化的人容易盯死一个 proxy 指标，但 proxy 和最终目标的关系是<strong>非线性且会随优化进程变化</strong>的。跳数在 baseline 阶段是好 proxy，优化到后期就完全脱钩了。所以<strong>优化过程中要反复确认瓶颈还在不在原来的地方</strong>。</p> <p><strong>第五，两个 case study 的落地难度差很多，值得区别看</strong>。wafer-scale 那个是模拟器上的结果，依赖尚不存在的硬件 + 架构改动，6.6× 这个数字要打折看；prefill 感知放置那个是真机 8×H100 实测，改的是 SGLang 的一个接口调用，<strong>今天就能用</strong>。1.25× 听起来不如 6.6× 性感，但对生产系统来说后者的价值更实在。</p> <hr/> <h2 id="9-一点吐槽和展望">9. 一点吐槽和展望</h2> <p>写完这篇我有个感慨：<strong>MoE 系统优化正在从”猜”变成”测”</strong>。</p> <p>之前做 MoE 相关的工作，很多设计决策是基于直觉的——”专家应该有局部性吧”、”热专家应该比较稳定吧”。有了这种规模的 trace，这些直觉可以被验证或推翻。我自己做 ExpertFlow 的时候就吃过没有可靠 profiling 数据的苦，很多参数只能靠试。</p> <p>还没被回答的问题也不少。比如<strong>这些规律在不同 batch size、不同并发下会不会变</strong>？论文的 trace 是特定配置下收集的，实际生产环境的请求混合更复杂。再比如<strong>模型更新之后 trace 要不要重新收</strong>？论文说 task-to-expert 映射一次离线 profiling 就够，但这个”一次”能撑多久，跨版本还成立吗？还有<strong>训练时的 aux loss 设计怎么影响这些规律</strong>——热专家倾斜 16 倍是不是意味着 load balancing loss 没起作用，如果调整 loss 让分布更均匀，是不是反而破坏了可预测性、让 prefetch 失效？这里可能有个有意思的 trade-off。</p> <p>欢迎评论区交流，尤其是踩过 MoE 部署坑的同学。</p> <hr/> <p>扯一句题外话。这篇论文的方法论其实很”AutoML”——<strong>先大规模 profiling 拿到经验数据，再从数据里提炼可自动化的策略，最后把策略变成系统里的一个组件</strong>。这个”用数据驱动设计决策”的思路和我们做 NAS 时的 benchmark 方法论一脉相承（NAS-Bench 那套东西存在的意义就是让架构搜索可复现、可比较）。</p> <p>我和合作者把这方面的积累整理成了《动手学 AutoML：从 NAS 到大语言模型优化实战》。这本书讲的是搜索空间设计、搜索策略（进化/贝叶斯/强化学习/DARTS）、架构评估这些自动化方法，以及 LLM 方向的架构自动化、压缩（剪枝/量化）、模型融合。<strong>它不涵盖 MoE serving 的系统工程</strong>——这篇论文里的 chiplet 架构、HBM 管理那些内容书里没有，方向相邻但不是一回事，我不想夸大。但如果你对”怎么让设计决策从拍脑袋变成数据驱动”这个方法论感兴趣，书里 NAS 和 benchmark 那几章可能有参考价值。</p> <blockquote> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="MoE"/><category term="推理优化"/><category term="论文解读"/><summary type="html"><![CDATA[ISCA’26 最佳论文 | 44 亿种专家组合背后，其实藏着可预测的规律]]></summary></entry><entry><title type="html">ASPLOS’26 最佳论文 | 让 0.5B 小模型读汇编，给 CPU 预取器当军师</title><link href="https://marsggbo.github.io/blog/2026/20260730-pf-llm/" rel="alternate" type="text/html" title="ASPLOS’26 最佳论文 | 让 0.5B 小模型读汇编，给 CPU 预取器当军师"/><published>2026-07-30T00:00:00+00:00</published><updated>2026-07-30T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260730-pf-llm</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260730-pf-llm/"><![CDATA[<h1 id="asplos26-最佳论文--让-05b-小模型读汇编给-cpu-预取器当军师">ASPLOS’26 最佳论文 | 让 0.5B 小模型读汇编，给 CPU 预取器当军师</h1> <blockquote> <p>原文：<a href="https://fact-lab.hkust.edu.hk/publications/conference-paper/2025/xu-2025-pf-llm/3779212.3790202.pdf">PF-LLM: Large Language Model Hinted Hardware Prefetching</a></p> </blockquote> <hr/> <h2 id="1-前言这篇论文把两个八竿子打不着的东西接上了">1. 前言：这篇论文把两个八竿子打不着的东西接上了</h2> <p>先说这篇论文让我眼前一亮的地方：<strong>它让一个 0.5B 的语言模型去读汇编代码，然后指挥 CPU 里的硬件预取器该怎么工作</strong>。</p> <p>我第一反应是”这不可能”，因为两边的时间尺度差得太远了。但看完发现思路很漂亮。这篇 ASPLOS’26 最佳论文来自港科大 + Duke，今天想把它的完整逻辑讲清楚。</p> <p>不过要讲明白这个工作，得先讲清楚<strong>CPU 预取器到底是干什么的、为什么它是个难题</strong>。这块属于 CPU 微架构，和大部分做 AI 的同学日常接触的东西距离比较远，所以下面两节我会从零开始铺，已经熟悉的可以直接跳到第 4 节。</p> <hr/> <h2 id="2-背景cpu-预取器要解决什么问题">2. 背景：CPU 预取器要解决什么问题</h2> <h3 id="21-一切的起点内存墙">2.1 一切的起点：内存墙</h3> <p>先看一个可能反直觉的事实：<strong>现代 CPU 里，一次 DRAM（主存）访问要花几百个周期</strong>。</p> <p>对比一下同一颗 CPU 里的其他操作有多快（这篇论文模拟的配置，后面会详细列）：</p> <table> <thead> <tr> <th>操作</th> <th>延迟</th> </tr> </thead> <tbody> <tr> <td>一次加法/乘法</td> <td>1 个周期左右</td> </tr> <tr> <td>L1 缓存命中</td> <td>2 个周期</td> </tr> <tr> <td>L2 缓存命中</td> <td>9 个周期</td> </tr> <tr> <td>L3 缓存命中</td> <td>20 个周期</td> </tr> <tr> <td><strong>DRAM 访问</strong></td> <td><strong>几百个周期</strong></td> </tr> </tbody> </table> <p>差了两个数量级。<strong>CPU 算得再快，数据没到就只能干等</strong>——这个”处理器速度和内存延迟之间的鸿沟”就是计算机体系结构里著名的 <strong>memory wall（内存墙）</strong>。</p> <p>为什么这件事在今天还重要？论文开头的论证是：虽然现在到处是多核、GPU、各种加速器，但<strong>Amdahl 定律说明单线程速度对降低应用延迟依然关键</strong>——程序里那些无法并行的部分，快慢完全取决于单核性能。而单核性能的传统来源（指令级并行 ILP）已经挖得差不多了，<strong>内存墙成了最主要的障碍</strong>。</p> <p>所以怎么办？既然消除不了这几百个周期的延迟，那就<strong>把它藏起来</strong>。</p> <h3 id="22-预取在-cpu-要用之前先把数据搬过来">2.2 预取：在 CPU 要用之前先把数据搬过来</h3> <p><strong>Data prefetching（数据预取）</strong> 的想法很朴素：<strong>在 CPU 真正需要某个数据之前，投机性地（speculatively）提前把它从慢的地方搬到快的地方</strong>。等 CPU 真的要用了，数据已经在 L1 缓存里躺着了，2 个周期就拿到，那几百个周期的延迟就被”藏”在了 CPU 干别的事的时间里。</p> <p><img src="/assets/img/posts/20260730-pf-llm/diagram_prefetch.png" alt="有预取和没有预取的时间线对比"/></p> <p>对着这张图走一遍：</p> <ul> <li><strong>左边没有预取</strong>：第 0 周期 CPU 执行一条 load 指令要读地址 A → L1D/L2/L3 层层未命中 → 请求发到 DRAM → <strong>接下来将近 300 个周期，CPU 就卡在这条指令上干等</strong>（后面依赖这个数据的指令都动不了）→ 数据到了才能继续。</li> <li><strong>右边有预取</strong>：第 0 周期，预取器从之前的访存规律里<strong>预测</strong>到”接下来会用 A” → 第 1 周期就发起搬运，<strong>而 CPU 此时在正常执行别的指令，一点没停</strong> → 300 个周期后 A 已经在 L1D 里了 → 第 320 周期 CPU 才真正执行那条 load，<strong>直接 L1D 命中，只花 2 个周期</strong>。</li> </ul> <p>延迟并没有消失（DRAM 该花的 300 周期照花），但它和 CPU 的正常执行<strong>重叠</strong>了，所以对程序来说是免费的。</p> <p><strong>注意这里的关键前提：预取器必须提前猜对要读哪个地址。</strong> 这就是整个领域的难点所在。而且猜错的代价不是”白忙一场”这么轻：</p> <ul> <li><strong>浪费内存带宽</strong>——搬了没人要的数据</li> <li><strong>缓存污染（cache pollution）</strong>——把有用的数据从缓存里挤出去了，导致本来会命中的访问变成了未命中</li> </ul> <p><strong>所以预取器猜错是会让程序变慢的</strong>，不是不赚而已。后面会看到，有些编排方法在某些负载上收益几乎归零，就是这个原因。</p> <p>再明确一点：<strong>预取器纯粹是硬件里的一个预测器，它看不到源代码</strong>。它能观察到的只有<strong>运行时经过它的地址流</strong>——”刚才读了 0x1000、0x1040、0x1080……”，然后从这串数字里找规律。这个信息局限是后面所有故事的根源。</p> <h3 id="23-各种专才预取器按访问模式分工">2.3 各种”专才”预取器：按访问模式分工</h3> <p>预取器怎么从地址流里找规律？答案是<strong>针对不同的内存访问模式设计专门的算法</strong>。学术界几十年里做出了一大堆，各管一类模式：</p> <ul> <li><strong>stream（流式）</strong>：顺序往后读，比如遍历一个字符串或数组。规律最简单：读了 A，那就预取 A+1、A+2……</li> <li><strong>stride（定步长）</strong>：固定间隔跳跃。比如遍历一个结构体数组时只取其中一个字段，每次地址前进的距离等于结构体大小。</li> <li><strong>spatial（空间局部性）</strong>：访问了某个结构体的一个字段，紧接着会访问同一个结构体的其他字段——地址在一小块区域内，但<strong>不是连续也不是等距的</strong>。这类预取器学的是”在一个内存区域里，哪些偏移量常常一起被访问”这种模板。</li> <li><strong>irregular（不规则）</strong>：链表、图遍历这种，地址之间没有算术关系，只能靠记住历史上的访问序列。</li> </ul> <p>具体的预取器名字后面会反复出现，先有个印象就行：Pythia、Bingo、SMS、DSPatch、Sandbox、AMPM、MLOP、Power7、PPF、Stride、Stream、Next Line。<strong>这些就是这篇论文 ensemble 里的候选零件</strong>，各自擅长的模式论文列了一张表，后面会看到。</p> <h3 id="24-从专才到组合ensemble-和它的编排难题">2.4 从”专才”到”组合”：ensemble 和它的编排难题</h3> <p><strong>Prefetcher ensemble（预取器组合）</strong> 的动机来自一个现实问题：<strong>真实程序不是单一模式的</strong>。</p> <p>论文的说法是：真实应用很少是”铁板一块”的，而是由<strong>多个动态切换的执行阶段</strong>组成，每个阶段有各自主导的内存行为。一个程序可能这一段在顺序扫数组（适合 stream），下一段在遍历链表（stream 完全没用），再下一段在做矩阵分块访问（适合 spatial）。<strong>所以任何单个专才预取器天生就覆盖不了整个负载</strong>。</p> <p>于是近年的设计思路是<strong>把多个专才组合成一个 ensemble</strong>。但这么一来，核心难题就从”怎么设计单个预取器”<strong>转移到了”怎么在运行时动态调度这一组预取器”</strong>。</p> <p>这个调度层叫 <strong>orchestration layer（编排层）</strong>：</p> <p><img src="/assets/img/posts/20260730-pf-llm/figure2_p3.png" alt="预取器 ensemble 的编排层结构"/></p> <p>图里的数据通路是这样：Core/LSU 发出访存请求 → 先查 L1D，未命中就往 L2/L3/DRAM 走（上面那条链）。同时这个请求<strong>分出一路进 Router</strong>，Router 决定把它送给哪些子预取器；各子预取器产出的预取请求汇到 <strong>Mux</strong>，Mux 决定放哪个真正发出去。<strong>(c) Orchestrator</strong> 就是同时控制 Router 和 Mux 的那个东西（图里两个红箭头），它要做两个决策：</p> <ol> <li> <p><strong>(a) Demand Request Training</strong>——一个真实的访存请求（demand request）来了，该送给哪些子预取器？</p> <p>这里要解释一下为什么”送给谁”是个决策。<strong>预取器是靠观察访存历史来”训练”自己内部状态的</strong>——比如 stride 预取器需要看到连续几次等距访问才能确认步长。所以喂给它什么请求，直接决定了它内部状态的质量。<strong>喂了不相关的请求，等于往它的训练数据里灌噪声。</strong></p> </li> <li> <p><strong>(b) Prefetch Request Issue</strong>——多个子预取器同时发出了预取请求，该选哪个（或哪些）发到下一级内存？带宽有限，不能都发。</p> </li> </ol> <p>论文提到已有工作有的只做前者、有的只做后者、有的两者都做。</p> <h3 id="25-编排层的两个死结">2.5 编排层的两个死结</h3> <p>有了 ensemble，整个系统的性能就取决于这个<strong>选择策略</strong>——必须把动态变化的程序阶段和最优的子预取器正确配对。配错了，就是前面说的挤掉缓存数据、浪费带宽，<strong>性能反而下降</strong>。</p> <p>现有方法靠<strong>在线学习</strong>（online learning）在运行时推导策略：试着用某个预取器，观察效果，再调整。这条路有两个绕不过的死结：</p> <p><strong>第一，试错收敛期躲不掉。</strong> 在线学习本质是 trial-and-error，得试错一段时间才能学到有效策略。<strong>而程序的阶段切换可能比这个收敛期还快</strong>——等它学明白该用哪个预取器，程序已经进入下一个阶段了。永远在追，永远追不上。</p> <p><strong>第二，硬件预算把它锁死在简单启发式上。</strong> 这里是我觉得最能体现”硬件约束有多残酷”的地方：<strong>现代 CPU 主频高达 5GHz，一个周期 0.2ns，一次预取决策的时间预算常常在亚纳秒级别</strong>。再加上核内的面积和功耗限制，<strong>在线机制只能用非常便宜的逻辑</strong>——几个计数器、一张小表，就这么多。它根本不可能去分析更长的程序上下文、做更有信息量的判断。</p> <p>论文还发现了一个更尴尬的现象：<strong>现有编排算法对付不了复杂的现代预取器</strong>。像 Pythia 这类 sophisticated 的预取器，内部状态是<strong>通过跟踪整个内存区域的模式</strong>建立起来的，而 Alecto、DOL 这些编排算法是 <strong>PC-centric</strong> 的——按指令（PC）粒度做选择和过滤。用指令粒度的逻辑去管区域粒度的状态，<strong>会破坏它的内部状态、干扰它的模式检测，导致显著的性能下降</strong>。后面实验会看到这个现象有多严重（严重到 ensemble 打不过单个预取器）。</p> <h3 id="26-那全搬到离线呢两条老路各有硬伤">2.6 那全搬到离线呢？两条老路各有硬伤</h3> <p>既然在线的算力预算这么紧，为什么不把分析搬到离线？离线有大得多的计算预算，可以做深得多的分析。这个思路早就有人走了，两条老路：</p> <p><strong>编译器方案</strong>：靠<strong>人工设计的启发式</strong>，在编译期决定往哪里插预取指令。比 baseline 有提升，但问题是<strong>依赖人类专家去设计规则</strong>；而且这些规则通常是被特定代码模式触发的，<strong>没法在程序级别做推理，对付不了复杂的代码结构</strong>。</p> <p><strong>PGO（Profile-Guided Optimization，性能剖析引导优化）</strong>：把程序编译并实际跑若干次，收集”如果这样预取会怎样”的 what-if 信息，直到找到有效策略。PGO 在很多程序上效果不错，但<strong>它必须有程序的输入数据才能收集到正确的 profile</strong>。而输入数据不总是有——<strong>程序的输入未知、或者不同运行之间变化很大的时候，PGO 就用不了</strong>。</p> <p><strong>而且这两条路还有一个共同的硬伤：都要重新编译程序</strong>（去注入预取指令或 hint）。<strong>如果应用是闭源的、手上只有一个二进制文件，这两种离线分析全都失效。</strong></p> <hr/> <h2 id="3-需要先记住的几个术语">3. 需要先记住的几个术语</h2> <p>上面铺完了背景，这里把后面高频出现的几个缩写集中列一下，免得后文空降：</p> <p><strong>Prefetch degree（预取激进度）</strong>：预取器一次往前预取多少个 cache line。值越大越激进——猜对了收益大，猜错了浪费也大。不同预取器支持的取值范围差别很大（有的 1-8，有的 0-64，有的干脆不支持调）。</p> <p><strong>Demand request（需求请求）</strong>：CPU 真实发出的访存请求，区别于预取器投机发出的 prefetch request。前面说过，它同时承担”喂给预取器做训练”的作用。</p> <p><strong>AMAT（Average Memory Access Time，平均内存访问时间）</strong>：以周期为单位。这篇论文用它衡量”某个预取策略对某条 load 指令好不好”，<strong>AMAT 越低越好</strong>。这是论文构造训练标签的依据。</p> <p><strong>IPC（Instructions Per Cycle）</strong>：每周期执行的指令数，衡量 CPU 整体性能的最终指标。<strong>IPC 提升多少，就是这篇论文最终的成绩单。</strong></p> <p><strong>PC（Program Counter，程序计数器）</strong>：这里用来<strong>唯一标识一条指令</strong>。后面反复出现的”per-PC 的 hint”就是”每条指令配一份 hint”。</p> <p><strong>L1D / L2 / L3</strong>：三级数据缓存，越靠前越小越快。这篇论文优化的是 <strong>L1D 的预取</strong>。</p> <hr/> <h2 id="4-pf-llm-的破局思路把决策搬到离线">4. PF-LLM 的破局思路：把决策搬到离线</h2> <p>回过头看，第 2 节的困境可以概括成一个矛盾：</p> <ul> <li><strong>在线</strong>：能看到运行时的真实访存行为，但<strong>算力预算极小</strong>（亚纳秒、几个计数器），看不到代码语义</li> <li><strong>离线</strong>：算力预算充足，但<strong>已有方法要么依赖人工规则、要么依赖输入数据、还都要重新编译</strong></li> </ul> <p>PF-LLM 的答案是：<strong>把”什么时候预取、怎么预取、多激进地预取”（when, how, and how aggressively to prefetch）这三个难决策全部从运行时硬件搬到离线的 LLM 分析里</strong>，运行时的硬件只负责<strong>按 PC 查表</strong>。</p> <p>这样时间尺度的矛盾就消解了。回到开头我的第一反应——LLM 推理要几毫秒，预取决策只有亚纳秒，差六七个数量级——<strong>但如果 LLM 的活是在程序运行之前就干完的，运行时只剩一次查表（单周期），预算就完全够了</strong>。</p> <p>论文把这个结果描述为：<strong>让片上预取器变成一个”零延迟、oracle 级”的系统，对每一条 load 指令都遵循最优的预取策略</strong>。”零延迟”指的就是没有在线试错的收敛期。</p> <p>而且相比前面两条老路，用 LLM 读<strong>汇编</strong>还顺手解决了另外两个硬伤：不需要输入数据（纯静态分析），不需要源码和重新编译（<strong>闭源二进制反汇编就能用</strong>）。</p> <hr/> <h2 id="5-为什么是-llm">5. 为什么是 LLM</h2> <p>上一节说的是”搬到离线”，但离线也可以用别的方法（前面那两条老路就是）。<strong>为什么偏偏是 LLM？</strong> 论文专门用一节论证这个，我觉得这几条论证是这篇工作真正的思想内核，值得完整过一遍。</p> <p><strong>核心洞察：静态代码上下文里已经包含了足够的信息来预测内存访问模式。</strong></p> <p>这个洞察怎么来的？作者的观察是：<strong>有经验的开发者看一眼代码就知道该怎么预取</strong>。看这个银行账户管理的 C 代码例子：</p> <p><img src="/assets/img/posts/20260730-pf-llm/fig1_full.png" alt="一段 C 代码里四种一眼可辨的内存访问模式"/></p> <p>图分两半：左边是 C 代码，右边是对应的内存布局。四种模式在<strong>同一个 for 循环里</strong>同时出现，而且都<strong>肉眼可辨</strong>：</p> <ul> <li><strong>(a) 无需预取</strong>：<code class="language-plaintext highlighter-rouge">mutex_lock(&amp;accounts[i].lock)</code> 访问的是锁字段，预取没意义</li> <li><strong>(b) 定步长</strong>：<code class="language-plaintext highlighter-rouge">total += accounts[i].amount</code> 遍历 <code class="language-plaintext highlighter-rouge">Account</code> 结构体数组。看右上角的内存布局，<code class="language-plaintext highlighter-rouge">acc[0]</code>、<code class="language-plaintext highlighter-rouge">acc[1]</code> 依次排开，每次跳一个结构体大小——图里标的是 <strong>stride 9</strong>（<code class="language-plaintext highlighter-rouge">is_valid</code> 1 字节 + <code class="language-plaintext highlighter-rouge">owner</code> 4 字节 + <code class="language-plaintext highlighter-rouge">lock</code> 2 字节，加上对齐）</li> <li><strong>(c) 空间局部性</strong>：<code class="language-plaintext highlighter-rouge">accounts[i].owner-&gt;age</code>、<code class="language-plaintext highlighter-rouge">-&gt;country_code</code>、<code class="language-plaintext highlighter-rouge">-&gt;name</code> 通过同一个 <code class="language-plaintext highlighter-rouge">Person*</code> 指针连续访问该结构体的多个字段。右图里 <code class="language-plaintext highlighter-rouge">*person</code> 那个框内的三个绿色箭头就是它</li> <li><strong>(d) 流式</strong>：<code class="language-plaintext highlighter-rouge">printf("%s", accounts[i].owner-&gt;name)</code> 顺序读一个字符串。右图最下面 <code class="language-plaintext highlighter-rouge">name[0][1][2][3]...</code> 那条直箭头</li> </ul> <p>这个例子说明两件事：<strong>即使很简单的程序也需要混合多种预取策略</strong>；而<strong>合适的策略常常能直接从静态代码结构推断出来</strong>。</p> <p>于是核心问题变成：<strong>既然有经验的开发者能从代码里识别这些模式，现代 LLM 能不能自动做同样的事？</strong></p> <p>论文给了四条论证：</p> <p><strong>深度代码理解</strong>。硬件预取器只能从<strong>地址流</strong>里找模式，因此局限于固定步长这类时序规律性。编译器启发式通常也只做局部循环分析或模式匹配。而 LLM 能从代码上下文推断<strong>底层语义</strong>——它能区分”遍历简单数组”、”遍历结构体数组”和”遍历链表”，<strong>这三者意味着完全不同的最优预取策略，但在地址流层面可能长得很像</strong>。另外现代 LLM 的大上下文窗口支持更高层次的程序结构分析，这在编译器或硬件里都做不到。</p> <p><strong>自动发现启发式</strong>。人工设计规则费力、需要领域专家、结果往往脆弱且难泛化。<strong>学出来的启发式还能适应新的编程习惯、新硬件特性、新负载特征</strong>——重新 fine-tune 就行。而人工规则在新一代硬件出来之后可能就过时了。</p> <p><strong>从集体经验中泛化</strong>。这条是对 PGO 的直接改进。PGO 的知识<strong>只针对被 profile 的那个应用</strong>，而且对 profiling 时用的具体输入敏感。PF-LLM 在一个多样化的语料上训练，学的是<strong>静态代码模式和动态内存行为之间的关系</strong>，而不是单个程序的行为。所以它能为<strong>没见过的新程序</strong>推断有效策略，不需要该应用的 profile，<strong>摆脱了对输入数据的依赖</strong>。</p> <p>关键的一步：<strong>PF-LLM 直接分析汇编代码</strong>。这让它同时独立于源码和程序输入，<strong>能用在预编译的闭源二进制上</strong>——PGO 和很多编译器方案在这里都失效。</p> <p><strong>复用预训练基础模型</strong>。这个方法可行的根基是：现在能 fine-tune 已经在数十亿行代码上训练过的编码模型，天然带着对编程语言和程序结构的理解。<strong>只需要专门化，不需要从零训练</strong>。</p> <p>我把新旧两条路的对比画了一张图：</p> <p><img src="/assets/img/posts/20260730-pf-llm/diagram_split.png" alt="传统在线 ensemble 与 PF-LLM 离线方案的对比"/></p> <hr/> <h2 id="6-pf-llm-的完整设计">6. PF-LLM 的完整设计</h2> <p>整个框架分两部分：离线的 PF-LLM 模型 + 运行时的 LMHint Prefetcher 硬件。</p> <p><img src="/assets/img/posts/20260730-pf-llm/fig3_full.png" alt="PF-LLM 整体流程：训练、离线生成 hint、运行时消费"/></p> <p>这张图分三块，对着看一遍：</p> <ul> <li><strong>左边 (a) 训练</strong>：一段汇编上下文（红色那行是目标 load 指令）走两条路——上路经 prompt template 变成训练输入，下路进模拟器跑出 ground-truth 策略（<code class="language-plaintext highlighter-rouge">PF Sel / PF Degree / Filter</code>）当训练标签，两者一起 fine-tune PF-LLM</li> <li><strong>中间 (b) 离线生成 hint</strong>：拿到一个二进制 → 反汇编 → 找出所有 load 指令 + 各自的上下文 → <strong>每条 load 跑一次 PF-LLM 推理</strong> → 结果汇总成 PHT（按 PC 索引）</li> <li><strong>右边 (c) 运行时消费</strong>：Core/LSU 发出访存 → L1D → 未命中往 L2/L3/DRAM。PHT 通过”TLB-like loading mechanism”把 hint 送进来，分三路作用在 ensemble 上：<strong>(1) Demand Request Filtering</strong> 作用在 Router（控制哪些子预取器能收到这条 demand request）、<strong>(2) Prefetch Degree</strong> 作用在各子预取器、<strong>(3) Prefetcher Selection</strong> 作用在 Mux（决定谁的预取请求真正发出去）。注意候选里还有一项 <strong>“No Prefetch”</strong>——模型可以显式判断这条 load 不该预取</li> </ul> <p>先说清楚<strong>适用范围</strong>：这篇工作聚焦于现代超标量处理器核内的 <strong>L1D 缓存数据预取</strong>，实验限定在<strong>单核</strong>、<strong>x86-64</strong> ISA。作者的说法是先在微架构层面证明有效性，多核留给未来工作。</p> <h3 id="61-模型为什么选-05b">6.1 模型：为什么选 0.5B</h3> <p><strong>模型选型</strong>：Qwen-2.5-Coder-0.5B-Instruct。理由是<strong>预取是个高度专门化的任务，小模型的能力已经够用</strong>，同时训练更快、显存占用更小。没有改动模型架构，就是在预取数据集上 fine-tune。</p> <p>我觉得这个选择很关键。后面会看到，<strong>离线推理成本是这套方案能否落地的决定性因素</strong>——每条 load 指令都要跑一次推理，模型大一点整个方案的成本就爆了。0.5B 是个经过考量的工程决策，不是随便选的。</p> <p><strong>输入</strong>：目标 load 指令周围的汇编代码上下文，具体是<strong>前 128 行 + 后 128 行，共 257 行</strong>。</p> <p>为什么用汇编而不是源码或 LLVM IR？论文在 discussion 里给了三个理由：</p> <ol> <li><strong>通用性</strong>：源码和 IR 不一定有（闭源二进制），但静态二进制总能反汇编</li> <li><strong>上下文长度可控</strong>：直接从二进制扒代码看似会破坏跨函数调用关系，但<strong>编译器通常会把调用亲和度高的函数放在相邻内存区域</strong>来改善指令缓存性能，所以不管函数边界直接扒上下文，反而能捕捉到跨函数信息。反过来，如果用源码，<strong>函数内联或循环展开会让上下文长度指数级增长</strong>，固定上下文窗口内反而抓不到有意义的语义模式</li> <li><strong>和运行时上下文最贴近</strong>：多一层抽象就多一层间接，LLM 更难学到”输入抽象”到”输出 hint”的映射</li> </ol> <p><strong>输出</strong>：三种 hint</p> <ul> <li><strong>Prefetcher Selection Hint</strong>：一个整数索引，指定用 ensemble 里哪个子预取器来为这条 load 发预取请求</li> <li><strong>Prefetch Degree Hint（可选）</strong>：控制激进度的整数，值越大越激进。选中的预取器不支持 degree 控制时这个 hint 不用</li> <li><strong>Demand Request Filtering Hint（可选）</strong>：指定<strong>不该</strong>接收这条 load 产生的 demand request 的子预取器。<strong>过滤掉不相关的 demand request 能减少某些子预取器的训练噪声</strong></li> </ul> <p>第三个 hint 值得多说一句，因为它对应的正是前面提到的”PC-centric 编排会破坏高级预取器状态”那个问题。<strong>如果某个预取器根本不是为这种访问模式设计的，喂给它这条 load 的访存记录只会污染它的内部状态</strong>，那就干脆别喂。</p> <h3 id="62-训练数据怎么造">6.2 训练数据怎么造</h3> <p>这是我认为整篇论文里最巧的一环。<strong>ground truth 从哪来？</strong> 答案是用架构模拟器暴力枚举。</p> <p>流程是这样：对每个 benchmark，<strong>为”子预取器 × 预取激进度”的每一种组合跑一次独立的 ChampSim 模拟</strong>。作者改了模拟器，让它记录<strong>每条 load 指令（按 PC 唯一标识）的 AMAT</strong>。</p> <p>举个具体例子：ensemble 里有 stream 和 stride 两个预取器，各支持 degree 1 和 2，那每个 benchmark 就要跑 2×2 = <strong>4 次模拟</strong>。跑完得到一张 per-PC 的表：</p> <p><img src="/assets/img/posts/20260730-pf-llm/table1_full.png" alt="数据集生成过程：按 AMAT 选最优和最差策略"/></p> <p>表的读法：PC <code class="language-plaintext highlighter-rouge">0x1234</code> 这条 load，用 stride + degree 1 时 AMAT 是 25.2 周期，stride + degree 2 是 28.3，stream + degree 1 是 101.0，stream + degree 2 是 103.9。</p> <p>然后用一个<strong>确定性启发式</strong>从这张表推出 ground truth：</p> <ol> <li><strong>最优策略</strong> = AMAT 最小的那个组合 → 上例中是 <code class="language-plaintext highlighter-rouge">PF Sel: stride, PF Degree: 1</code></li> <li><strong>要过滤的预取器</strong> = 表现最差的那个策略对应的预取器 → 上例中是 <code class="language-plaintext highlighter-rouge">Filter: stream</code></li> </ol> <p>第二条的逻辑是：<strong>如果某个预取器在这条 load 上表现极差，说明它不是为这种访问模式设计的，它的内部状态很可能正被这条 load 的 demand request 干扰，那它就是个理想的过滤候选</strong>。</p> <p>但这里有个例外，很重要：<strong>如果表现最差的预取器属于”高级组件”，就不生成 filtering hint</strong>。因为这些预取器内部状态复杂，<strong>对 demand request 过滤本身就很脆弱</strong>。具体来说 Pythia、Bingo、DSPatch、Sandbox、SMS 这五个被排除在过滤之外：</p> <p><img src="/assets/img/posts/20260730-pf-llm/table2_p8.png" alt="子预取器列表及其目标访问模式"/></p> <p>这张表带星号（*）的就是论文正文里排除在过滤之外的那几个（表里标星的是 Pythia、Bingo、DSPatch，另外 Sandbox 和 SMS 在正文 5.1 节里一并列出）。还能看到各预取器的 degree 支持情况差异很大——Pythia、Bingo、Next Line、PPF 完全不支持 degree 控制，支持的那些原生范围从 0-6、1-8 到 0-64 各不相同。</p> <p>顺便说下候选池的选取原则：ensemble 里<strong>没有放 Triage、Triangel 这类 temporal prefetcher</strong>，因为它们资源开销大、而且是专为 LLC 预取设计的，和这篇工作聚焦的 L1D 预取不匹配。</p> <p><strong>怎么统一这个异构的 degree 范围？</strong> 论文的做法是<strong>归一化成三态</strong>：1（保守）、2（中等）、3（激进），分别映射到每个子预取器原生范围的 <strong>Q1、中位数、Q3</strong>。举个例子：LMHint degree 3（激进）对 DSPatch（原生范围 0-64）来说，映射到 <code class="language-plaintext highlighter-rouge">round(0.75 × 64) = 48</code>。</p> <p>这个归一化同时简化了两件事：PHT 的实现（只需要 2 bit 存 degree）和 LLM 的学习任务（不用学 12 种不同的数值范围）。</p> <h3 id="63-prompt-格式">6.3 Prompt 格式</h3> <p>直接把裸汇编喂给 instruction-tuned 模型是次优的，Qwen 这类模型期望特定的 system/user/assistant 格式。论文用了基于 Qwen 官方模板的 prompt：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;|im_start|&gt;system
You are a helpful assistant that generates prefetch hints for a given load
instruction in a assembly code snippet.
The load instruction is marked with &lt;load&gt; and &lt;/load&gt;.
Your output should be a JSON object with "PFSel", "PF Degree" and "Filter"
as fields.
&lt;|im_end|&gt;

&lt;|im_start|&gt;user
movzbl 0x3adef0(%rip), %eax
xorl %r8d, %r8d
leaq 0x6dc8d(%rip), %rsi
&lt;load&gt;movq 0x10(%rsp), %rcx&lt;/load&gt;
movl 0x1c(%rsp), %edx
movq 0x3ab11d(%rip), %rdi
orl $2, %eax
&lt;|im_end|&gt;

&lt;|im_start|&gt;assistant {
  "PF Sel": "stride",
  "PF Degree": 2,
  "Filter": "stream",
}&lt;|im_end|&gt;
</code></pre></div></div> <p>对齐指令格式有两个好处：<strong>加速 fine-tune 收敛</strong>（system/user prompt 让基础模型能用零样本能力理解任务，把学习精力集中在预取的细微差别上而不是输入格式）；<strong>便于结构化输出</strong>（配合 vLLM 这类框架的 structured output 功能，可以<strong>强制</strong>输出严格符合 JSON schema）。</p> <p>两个实现细节：</p> <ul> <li><code class="language-plaintext highlighter-rouge">&lt;load&gt;</code> 和 <code class="language-plaintext highlighter-rouge">&lt;/load&gt;</code> 被加成了<strong>特殊 token</strong>（进 embedding table），让模型更平滑地学会识别 load 指令的位置</li> <li><strong>loss 只在输出部分计算</strong>——只算 JSON 输出那几个 token 的 loss，不算输入汇编和 system prompt 的。这让训练聚焦在生成 hint 这个主任务上，<strong>不会因为”复现输入汇编”的差异被惩罚</strong></li> </ul> <p>训练配置：LLaMA-Factory + DeepSpeed，8× NVIDIA H20，BF16，学习率 1e-5，有效 batch size 64，AdamW，2 个 epoch。</p> <h3 id="64-硬件侧hint-怎么送进-cpu">6.4 硬件侧：hint 怎么送进 CPU</h3> <p>这是另一个关键工程问题：<strong>离线生成的 hint 怎么在运行时被处理器用上？</strong></p> <p>论文的方案是套用 <strong>TLB 的思路</strong>：</p> <ul> <li><strong>PHT（Prefetch Hint Table）</strong>：完整的 hint 表，放在<strong>主存</strong>里，按每条 load 指令的虚拟 PC 索引。程序执行前加载进去</li> <li><strong>PHB（Prefetch Hint Buffer）</strong>：<strong>片上</strong>的小缓冲，缓存最近用过的 PHT 条目，<strong>类比 TLB 缓存页表项</strong>。用 <strong>256 项</strong>，在硬件开销和单周期访问之间取平衡</li> </ul> <p>hint 的编码压得很紧：每个 PC 一个 <strong>8 bit</strong> 值，其中 <strong>4 bit 预取器选择 + 2 bit 预取激进度 + 2 bit demand request 过滤策略</strong>。论文提了一句为什么是 8 bit 而不是更少——<strong>行宽小于 8 bit 的 SRAM blackbox 不常见</strong>，所以就用 8 bit。这种细节挺能体现作者是真的在做硬件设计。</p> <p>运行时流程：load 指令发出 → 用 PC 查 PHB → <strong>命中</strong>就取出 hint 策略，分三路去控制 ensemble：</p> <ol> <li><strong>选择 hint</strong> 决定哪个子预取器被允许为这条 load 发预取请求</li> <li><strong>degree hint</strong> 直接传给选中的子预取器控制激进度</li> <li><strong>过滤 hint</strong> 阻止这条 demand request 被送到指定的子预取器，<strong>保护它的内部状态不被无关训练信息污染</strong></li> </ol> <p><strong>PHB miss</strong> 怎么办？发请求去主存的 PHT 填充条目，<strong>期间用一个默认策略</strong>——存在 PHT 和 PHB 的零地址保留条目里。</p> <hr/> <h2 id="7-效果">7. 效果</h2> <h3 id="71-模型精度95">7.1 模型精度：95%</h3> <p>实验设置里有个细节值得表扬：<strong>训练用 SPEC 2006 的 memory-intensive benchmark，测试专门留 SPEC 2017</strong>，模型在评测前从没见过测试集。</p> <p>另外因为 PF-LLM 需要静态汇编，<strong>不能用公开的 ChampSim trace</strong>（那些 trace 用 Pin 生成，只含指令执行顺序、访存历史这些动态信息，<strong>没有静态汇编代码</strong>），所以作者自己从源码编译了所有 SPEC benchmark：Ubuntu 20.04 + GCC 9.4.0，<code class="language-plaintext highlighter-rouge">-O2 -fpermissive -march=x86-64</code>，每个 job 模拟 2 亿条指令。</p> <p>模拟器是 <strong>ChampSim 的 DPC-3 版本</strong>，核的配置对标 <strong>Arm Neoverse N2</strong>：5-wide fetch/decode、10-wide issue/commit、120 项 IQ、85/90 项 LQ/SQ、288 项 ROB；L1 I/D 各 64KB 4-way（2 周期命中，<strong>从 L2 预取</strong>），L2 512KB 8-way（9 周期），L3 2MB 16-way（20 周期），DRAM 是 LPDDR5-5500 单通道。<strong>注意 L1D 缓存只有 64KB、L3 只有 2MB</strong>——这个配置比服务器级 x86 小不少，缓存越小预取的相对收益越大，这是看那个 54.5% 时要记住的前提。</p> <p><img src="/assets/img/posts/20260730-pf-llm/figure4_p9.png" alt="PF-LLM 训练收敛曲线"/></p> <p>蓝线是 training loss（对数轴），橙线是 evaluation accuracy。loss 在最开始就断崖式下降，之后一路缓慢下行；accuracy 从 <strong>84% 左右起步</strong>——注意这个起点已经很高了，<strong>说明预训练模型带来的先验很强，fine-tune 主要是在做专门化</strong>，和论文”只需专门化、不需从零训练”的论证对得上。然后 accuracy 阶梯式往上爬，最后<strong>在留出测试集上达到 95.0%</strong>。</p> <p>论文说是因为算力和时间限制停在这里，更大的训练集精度应该还能涨。看曲线末端确实还在上扬、没有走平，这个说法可信。</p> <p>更细的分析在这里：</p> <p><img src="/assets/img/posts/20260730-pf-llm/fig5_full.png" alt="混淆矩阵与最优子预取器策略的频率分布"/></p> <p><strong>右边 (b) 图先看，这是理解左图的钥匙</strong>：最优预取器策略的分布<strong>极度不均匀</strong>。Sandbox 那一行的柱子长度远远压过其他所有预取器，而且颜色以保守/中等 degree 为主（论文正文的说法是 “Sandbox + moderate degree 远比其他组合更常成为最优”）。Streamer 排第二，其余的量级都很小。</p> <p>左边 (a) 是混淆矩阵，行是 ground truth、列是预测。<strong>对角线整体成立，但强弱差别很大</strong>：</p> <ul> <li>Sandbox <strong>0.98</strong>、Power7 <strong>1.00</strong>，几乎全对</li> <li>MLOP 0.87、Stride 0.85、Bingo 0.84、Pythia 0.77、SMS 0.76、DSPatch 0.75、AMPM 0.75，中上水平</li> <li><strong>Streamer 只有 0.53</strong>——将近一半判错</li> </ul> <p>而且<strong>误判的方向高度集中：几乎所有非对角线的重量都堆在 Sandbox 那一列</strong>（Streamer→Sandbox 0.34、DSPatch→Sandbox 0.25、AMPM→Sandbox 0.25、SMS→Sandbox 0.19、Pythia→Sandbox 0.14……）。</p> <p>论文对误判的解释是：<strong>PF-LLM 猜错的时候输出不是随机的，而是高度倾向于第二好的选项</strong>，性能接近最优；这说明模型学到的不只是”单个最优策略”，而是”什么样的预取策略对这种代码上下文有效”这个更一般的特征。</p> <p><strong>这个解释我只部分认同。</strong> 从矩阵看，误判集中流向 Sandbox 更直接的解释是<strong>它是频率上的多数类</strong>——(b) 图里 Sandbox 的样本量压倒性地大，模型在不确定的时候倒向多数类是分类任务的常见行为，不一定是”学会了识别次优解”。当然这两种解释在结果上会重合：因为 Sandbox 本身就是最常见的最优解，倒向它确实往往不会错得太远，这也解释了为什么 IPC 上没有崩。</p> <p>顺带说一句，<strong>对角线平均下来明显低于 95%</strong>，两个数字并不矛盾：95.0% 是在测试集上按样本算的准确率，而样本分布本身是倾斜的——Sandbox 这个占比最大的类准确率 0.98，把整体拉了上去。混淆矩阵是按行归一化的，等权重看每个类，所以显得低。<strong>看这类结果的时候，得分清”按样本”还是”按类别”</strong>。</p> <p>不过论文这个观察方向仍然值得肯定：<strong>对系统优化来说，误判的代价分布比准确率数字更重要</strong>。如果剩下 5% 的误判是灾难性的，整体收益可能被吃光；论文至少去看了误判落在哪里，这一步很多 ML for Systems 的工作是省掉的。</p> <h3 id="72-端到端性能">7.2 端到端性能</h3> <p>消融实验设了四个配置：</p> <ul> <li><strong>LMHint-S</strong>：只用预取器<strong>选择</strong>（Selection）hint</li> <li><strong>LMHint-SD</strong>：加上预取<strong>激进度</strong>（Degree）控制</li> <li><strong>LMHint-SDF</strong>：再加上 demand request <strong>过滤</strong>（Filtering）</li> <li><strong>LMHint-SDFR</strong>：<strong>降成本</strong>（Reduced）配置，三种 hint 都用，但硬件只实现 Figure 5(b) 里最常被选中的<strong>四个</strong>子预取器</li> </ul> <p>先看总览：</p> <p><img src="/assets/img/posts/20260730-pf-llm/figure7_p10.png" alt="各预取器配置在 SPEC 2017 上的几何平均加速比"/></p> <p>图里的数字是相对无预取基线的<strong>几何平均 IPC 提升</strong>。四个 LMHint 配置分别是 <strong>54.5%（SDFR）、54.5%（SDF）、54.2%（SD）、53.9%（S）</strong>，全部高于所有基线。基线里最好的单预取器是 <strong>Pythia 44.7%</strong>，其后 Bingo 43.2%、Sandbox 41.7%、PPF/Streamer 41.5%、AMPM 41.1%、Power7 38.6%、MLOP 36.0%；ensemble 方法里 <strong>DOL 35.6%</strong>、Conf. Based 29.8%、<strong>Alecto 21.9%</strong>；最差的 SW Prefetch 只有 <strong>0.2%</strong>。</p> <p>论文声明的提升是 <strong>比最强单预取器高 9.8 个百分点，比最强已有 ensemble 方法高 18.9 个百分点</strong>。这两个数字对得上 54.5 − 44.7 = 9.8 和 54.5 − 35.6 = 18.9。<strong>不过论文正文把这两个对照对象分别写成了 Sandbox 和 Alecto，和图里的数值排序不一致</strong>（Sandbox 是 41.7、Alecto 是 21.9，都不是各自类别里最强的）。我倾向于认为是正文笔误、数字本身没问题，但读的时候值得留个心。</p> <p>逐 benchmark 的细节（上半是对单预取器，下半是对 ensemble 方法）：</p> <p><img src="/assets/img/posts/20260730-pf-llm/fig6_full.png" alt="逐 benchmark 的 IPC 提升"/></p> <p>先看这张图能读出的东西：<strong>LMHint 在几乎每个 benchmark 上都是最高的那根柱子</strong>，而且收益分布极不均匀——bwaves_s 和 fotonik3d_s 能到 150% 左右，gcc_s、roms_s 在 100% 上下，而 omnetpp_s 几乎没有提升（所有预取器都不行，说明它的访存模式本身就不可预取）。<strong>几何平均 54.5% 这个数字主要由几个高收益负载拉起来的</strong>，看总均值容易误判单个应用的实际收益。</p> <p>下半图里 Alecto 和 Random 那两根在 mcf_s 上甚至掉到 0 附近，而 LMHint 是 30% 出头——<strong>编排策略选错的代价是实打实的负优化</strong>，不是”没优化好”而已。</p> <p>再看总结论：</p> <p><strong>第一，消融实验证明每种 hint 都有价值，但贡献不均</strong>。从 LMHint-S 到 LMHint-SDF 是递增的：加 degree 控制（SD）带来 <strong>0.3%</strong> 平均 IPC 提升，再加 demand request 过滤（SDF）又贡献 <strong>0.3%</strong>。<strong>预取器选择提供了最大的收益</strong>，另外两个是精细调优。</p> <p><strong>第二，降成本配置几乎不掉性能</strong>，LMHint-SDFR 甚至比 SDF 高 0.01%（在噪声范围内）。这个结果的意义是：<strong>硬件只需实现 4 个子预取器而不是 11 个</strong>，面积和复杂度大幅下降，性能不变。而且<strong>不需要重训模型</strong>——只要在生成 hint 时<strong>约束 vLLM 推理引擎的 JSON schema</strong>，只允许模型从这四个里选。这个 trick 很漂亮，充分利用了结构化输出的能力。</p> <p><strong>第三，也是我觉得最有意思的一点</strong>：看上面那张总览图，<strong>DOL（35.6%）和 Alecto（21.9%）这两个在线 ensemble 方法，居然比几乎所有单预取器都差</strong>——Pythia 44.7%、Bingo 43.2%、Sandbox 41.7%，连排在中游的 MLOP（36.0%）都比 DOL 高。<strong>Alecto 的 21.9% 甚至只比 SMS（12.6%）和 Random（10.9%）好</strong>。</p> <p>这印证了前面提到的问题。<strong>这些空间型子预取器通过跟踪整个内存区域的模式来构建复杂内部状态，而不是单条指令流</strong>。Alecto 这类方法的 PC-based 选择逻辑，要么<strong>用无关的 demand request 污染它们的训练</strong>，要么<strong>过滤掉了关键的 demand request</strong>，两头都会破坏它们的模式检测。</p> <p>PF-LLM 靠<strong>静态的、上下文感知的 hint</strong> 绕过了这个问题。论文特别强调<strong>过滤 demand request 的能力尤为关键</strong>，因为它能屏蔽这些高级预取器脆弱的状态不受干扰输入影响。</p> <h3 id="73-为什么能赢oracle-级的零延迟适应">7.3 为什么能赢：oracle 级的零延迟适应</h3> <p>这张图我觉得是全文最有说服力的一张：</p> <p><img src="/assets/img/posts/20260730-pf-llm/figure9_p11.png" alt="654.roms 的 IPC 加速轨迹"/></p> <p>这是 654.roms 这个负载的 heartbeat IPC 轨迹。(b) 是各个 baseline 预取器的轨迹，(a) 在它上面叠加了 LMHint 的轨迹。</p> <p><strong>LMHint 的曲线始终贴着所有子预取器的性能上包络线跑，而其他预取器在震荡和滞后</strong>。</p> <p>为什么？<strong>单体预取器天生受限于它承诺了单一固定策略，不可能适应程序变化的访存模式</strong>。而 LMHint 是<strong>主动</strong>的——靠 PF-LLM 离线生成的 per-PC hint，它在一段代码<strong>被执行之前就知道</strong>最有效的策略是什么。程序控制流在不同阶段之间切换时，<strong>LMHint 瞬间切换策略，没有任何运行时试错</strong>。</p> <p>看图里标的三个区域：</p> <ul> <li><strong>区域 ②</strong> 和 <strong>③</strong>：LMHint 立刻匹配上了该阶段最优预取器（分别是 MLOP 和 Sandbox）的性能。这就是 <strong>oracle 级选择</strong></li> <li><strong>区域 ①</strong>：更有意思——<strong>LMHint 超过了任何单个子预取器</strong>。这个阶段程序的访存模式太多样，没有单个预取器能有效处理。这时候细粒度控制的优势体现出来了：<strong>LMHint 用选择、degree、过滤三种 hint 在单条 load 指令的粒度上编排子预取器，让每个子预取器只专注于它擅长的访问流，最小化来自无关 demand request 的干扰</strong>。于是 ensemble 的性能变成<strong>可叠加的</strong>，超过任何单个组件的上限</li> </ul> <h3 id="74-真实-web-负载">7.4 真实 web 负载</h3> <p>SPEC 毕竟是合成 benchmark，论文还测了四个真实的 web serving 应用：Apache HTTP Server、MySQL、RocksDB、Xapian 搜索引擎。为了更贴近现代数据中心里拥挤的微服务共同部署环境，<strong>主存延迟调到 2 倍</strong>。</p> <p><img src="/assets/img/posts/20260730-pf-llm/figure8_p11.png" alt="真实 web serving 负载上的加速比"/></p> <p>LMHint-SDFR 仍然优于 Pythia 和 DOL 这两个对照，但<strong>提升幅度明显小得多</strong>：Apache <strong>约 3.7%</strong>、RocksDB 约 1.7%、MySQL 和 Xapian 都只有 <strong>0.5% 出头</strong>。论文老实分析了两个原因：</p> <ol> <li>这些应用<strong>更偏 I/O-bound 而非 CPU/memory-bound</strong>，预取优化的天花板本身就低</li> <li>这些应用<strong>已经被人工优化了很多年，很多自己实现了缓存和预取机制</strong>，留给自动预取的空间不大</li> </ol> <p>这段自我批评我挺欣赏。3.7% 和 SPEC 上的 54.5% 差距巨大，<strong>如果论文避而不谈只报 SPEC，读者就会对实际收益产生错误预期</strong>。</p> <p>另外注意这里只对了 Pythia 和 DOL 两个基线，没有像 SPEC 那样铺开全部 19 个。样本也是四个应用，说服力不如 SPEC 那部分。</p> <h3 id="75-开销这才是落地的关键">7.5 开销：这才是落地的关键</h3> <p><strong>离线推理开销</strong>。每条 load 指令跑一次 LLM 推理，听起来很贵。实测：</p> <p><img src="/assets/img/posts/20260730-pf-llm/figure10_p12.png" alt="PF-LLM 离线推理开销分析"/></p> <p>(a) 单张 H20 上用 vLLM 服务，吞吐<strong>最高 234.3 requests/秒</strong>（换成 L20 是 173.6）。可以看到吞吐随并发度显著上升，但<strong>在并发 8 之后基本走平</strong>——GPU 已经打满了，再加并发没用。</p> <p>高吞吐来自三个因素：0.5B 参数量小；输入上下文短，而且生成长度极短（就一个小 JSON）；<strong>分析二进制里所有 load 指令天然提供了大量独立请求</strong>，能充分利用 vLLM 的 batching。</p> <p>(b) 是输入上下文长度的分布，论文的说法是”大部分汇编上下文在 3000 token 以下”。<strong>看直方图我觉得这个描述偏乐观</strong>——分布的峰就在 2900-3000 附近，整体跨度 2000 到 4000，3000 以上还有相当一块。不过这不影响结论：这个量级的上下文对 0.5B 模型来说都很轻。</p> <p>(c) 是总推理时间 vs 二进制大小。论文说是<strong>线性增长</strong>，理由是推理次数由 load 指令数决定、而 load 指令数和代码规模成正比。这个逻辑没问题，但<strong>要注意这张图两个轴都是对数轴</strong>（横轴 100KB→100MB，纵轴 10⁰→10³ 秒），红色虚线是在 log-log 下拟合的。log-log 上的直线严格说是幂律关系，斜率约等于 1 时才是线性。图上的点离散度不小（同一量级的 binary 之间差了好几倍），所以<strong>当成”大致成正比”看就好，别当精确的线性模型</strong>。</p> <p>最关键的数字：<strong>整个 SPEC 2017 套件生成 hint，在 8 GPU 系统上需要 38.5 分钟</strong>。而<strong>用 16 核机器编译这套 benchmark 需要 25.4 分钟</strong>。</p> <p><strong>同一个量级。</strong> 这个对比是这套方案能否落地的核心论据——它把 LLM 推理成本放在了”和编译差不多”这个开发者早已接受的尺度上。</p> <p><strong>运行时存储开销</strong>。PHT 每条目存 48 bit 虚拟 PC（x86-64）+ 8 bit hint = <strong>56 bit（7 字节）</strong>。分析 SPEC 2017 二进制发现<strong>平均每 MB 可执行代码有 10.62K 条 load 指令</strong>，所以 PHT 开销是<strong>每 MB 二进制 74.34 KB</strong>，即<strong>静态程序体积增加 7.26%</strong>。</p> <p>论文的论证是：为了 10-20% 的 IPC 提升，这点开销是划算的——<strong>想靠传统硬件手段（加宽核、加深流水线、加大缓存）拿到同等提升，面积、功耗和设计复杂度的代价会高得多</strong>。</p> <hr/> <h2 id="8-论文自己承认的局限">8. 论文自己承认的局限</h2> <p>这部分论文写得很坦诚，我照实转述。</p> <p><strong>JIT 和字节码程序不支持</strong>。PF-LLM 依赖分析静态二进制，所以<strong>需要 JIT 编译的程序或跑在字节码上的程序（比如 Java）目前不支持</strong>。作者给的思路是扩展到分析运行前就有的静态 IR，训一个从 IR 生成 hint 的模型。</p> <p><strong>没处理 ASLR</strong>（地址空间布局随机化）。hint 是按静态 PC 索引的，<strong>运行时随机化会打断 hint 和指令的映射</strong>。解决思路是把 hinting 机制和 OS loader 集成——<strong>给 hint 表施加和代码段相同的随机化偏移</strong>，对应关系就保住了。</p> <p><strong>ISA 和机器配置依赖</strong>。当前只在 x86-64 上评测，但方法论本身是 ISA 无关的，换 ISA 重训就行。最优预取策略也会随缓存大小、带宽这些微架构参数变化，当前模型是为特定配置训的。论文的设想是：<strong>由 CPU 厂商提供 PF-LLM 模型</strong>——他们有详细的硬件知识和精确的模拟器来生成训练数据。或者把硬件参数作为额外的 prompt 输入，让模型生成配置感知的 hint。</p> <p>顺便，论文对 PGO 的对比有个论证值得记：虽然 PF-LLM 给特定二进制生成的 hint 是静态的，但<strong>整体方案对运行时环境是自适应的</strong>——因为<strong>内存访问模式主要由程序代码决定</strong>，给定 load 的模式在不同运行间基本反映代码语义；足够大的上下文窗口让 PF-LLM 能区分不同的输入/分支相关情况；剩下的数据和输入相关的变化，<strong>由被选中的那个预取器继续在运行时学习来处理</strong>。</p> <p>论文把这个总结为它的一个关键贡献：<strong>在离线 hint 生成和在线预取器选择之间定义了一个清晰、可泛化的接口</strong>，让静态的、优化过的、语义感知的 hint 去引导动态的、运行时自适应的硬件。</p> <p>另外实验还顺手证明了一件事：<strong>软件预取（往指令流里直接插预取指令）表现很差</strong>（图里 SW Prefetch 只有 0.2%）。原因是它既对运行时访存模式缺乏可见性，又<strong>增加了指令数量，给核的前端带来额外开销</strong>。</p> <hr/> <h2 id="9-我的几点-take">9. 我的几点 take</h2> <p><strong>第一，这篇工作真正的贡献是那个”接口”，不是 LLM</strong>。把”用 LLM 做预取”当成卖点的话就看小了。核心贡献是<strong>在离线深度分析和在线快速执行之间定义了一个 8 bit 宽的接口</strong>。8 bit 这个数字很关键——正是因为窄，它才能塞进 PHB 做到单周期访问；也正是因为语义上够表达（选哪个预取器 + 多激进 + 过滤谁），它才承载得住离线分析的价值。<strong>接口设计是这类 offline-online 协同工作的真正难点</strong>，不是模型训练。</p> <p><strong>第二，”离线做难决策、在线只查表”这个模式可以复制</strong>。论文在 implications 里也提到了：这套方法论能推广到其他动态硬件机制——<strong>LLM 分析复杂控制流给分支预测器提 hint，或者推断数据复用特性来指导更智能的缓存替换和插入策略</strong>。我觉得这个方向真的有搞头。分支预测器面临的困境和预取器几乎一模一样：决策预算极小、只能看到运行时历史、看不到代码语义。</p> <p><strong>第三，0.5B 这个选择比想象中重要</strong>。整套方案能不能落地，取决于”生成 hint 的时间”能不能和”编译时间”处在同一量级。38.5 分钟 vs 25.4 分钟这个对比，如果模型换成 7B，就完全不是一个故事了。<strong>这提醒我在做系统优化时，模型规模不是”越大越好”的自由变量，而是被端到端成本预算约束的设计参数</strong>。</p> <p><strong>第四，那个 95% 要拆开看</strong>。混淆矩阵按类别看，对角线从 Streamer 的 0.53 到 Power7 的 1.00 跨度很大，误判几乎全部流向 Sandbox 这个多数类。<strong>倾斜的标签分布让”按样本的准确率”和”按类别的准确率”差得很远</strong>，这是我读这篇论文时最需要自己动手核对的地方。但真正决定结论的不是这个数字——是 IPC 没崩这个事实，而它成立的原因恰好是 Sandbox 本身常常就是不错的选择。<strong>做 ML for Systems，别只报准确率，要报误判落在哪里、代价多大。</strong></p> <p><strong>第五，在线 ensemble 反而不如单预取器这个发现很有教育意义</strong>。Alecto（21.9%）和 DOL（35.6%）都是发表过的方法，各自的论文里肯定都报了正向结果。但<strong>当子预取器换成 Pythia 这类复杂的现代预取器，它们就失效甚至变成负优化</strong>（mcf_s 上 Alecto 几乎归零）。这是系统研究里典型的”baseline 组合爆炸”问题——<strong>方法 A 在环境 X 有效，方法 B 在环境 X 有效，不代表 A 在 B 的环境里还有效</strong>。这也说明为什么这类工作必须做全面的 baseline 对比。</p> <p><strong>第六，真实负载只有 3.7% 这件事要正视</strong>。SPEC 上 54.5% 很漂亮，但真实 web 服务里最好的 Apache 才 3.7%，MySQL 和 Xapian 是 0.5% 出头。论文给的两个理由（I/O-bound、已被人工深度优化）都站得住，但这也意味着<strong>这套方案的真实受益场景是特定的</strong>：计算密集、访存密集、且没被人工深度优化过的负载。科学计算、编译器、数据库的部分内核可能符合，但典型的微服务未必。</p> <hr/> <h2 id="10-一点展望">10. 一点展望</h2> <p>这篇论文我读完最兴奋的不是那 18.9%，而是它示范了一种<strong>新的 hardware-software co-design 分工方式</strong>。</p> <p>传统的分工是”编译器做静态优化、硬件做动态适应”，两边各自为战。<strong>PF-LLM 提出的分工是”离线的 AI 做语义理解、在线的硬件做快速执行”</strong>，中间用一个窄接口连接。这个分工里，LLM 承担的是”人类专家的代码直觉”这个角色——正好是它相对硬件和编译器启发式的比较优势。</p> <p>还没被回答的问题不少。<strong>hint 的粒度是不是最优的？</strong> 现在是 per-PC 的，但同一条 load 指令在不同调用上下文里最优策略可能不同，per-PC 会取一个平均。<strong>跨 ISA 和跨微架构配置的泛化到底能做到什么程度？</strong> 论文的设想是厂商各训一个，但如果每代 CPU 都要重新收集训练数据、重训模型，这个成本谁承担？还有<strong>多核场景</strong>——共享 LLC 上的预取干扰是个完全不同的问题，per-PC 的静态 hint 在多核竞争下还成立吗？</p> <p>欢迎评论区交流。搞 CPU 微架构的同学如果觉得我哪里理解偏了，欢迎指出来。</p> <hr/> <p>顺带一提，这篇工作的思路可以概括成一句话：<strong>把一个原本靠人工设计启发式的决策问题，交给模型从数据里自动学</strong>。ground truth 靠模拟器暴力枚举生成，模型学的是”代码上下文 → 最优配置”的映射。</p> <p>这个套路 AutoML 圈子的人应该会觉得很熟悉——它本质上就是<strong>用学习替代人工启发式设计</strong>，只不过搜索的对象从神经网络架构换成了预取策略。我们把这方面的积累整理成了《动手学 AutoML：从 NAS 到大语言模型优化实战》，书里搜索策略那部分（进化、贝叶斯、强化学习、DARTS）和架构评估那部分（权重共享、免训练评估、benchmark 构建）讲的就是这类”怎么高效地搜、怎么便宜地评估”的方法。</p> <p>不过要说清楚：<strong>这本书讲的是神经网络和 LLM 层面的自动化，CPU 微架构、预取器、ChampSim 这些内容书里完全没有</strong>。方向上算是共享方法论，但不是同一个领域，我不想扯得太硬。感兴趣的话可以当成另一个视角看看。</p> <blockquote> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="CPU微架构"/><category term="预取"/><category term="论文解读"/><summary type="html"><![CDATA[ASPLOS’26 最佳论文 | 让 0.5B 小模型读汇编，给 CPU 预取器当军师]]></summary></entry><entry><title type="html">OSDI’26 | ECHO：稀疏 attention 省了算力，却把 HBM 撑爆了</title><link href="https://marsggbo.github.io/blog/2026/20260729-echo/" rel="alternate" type="text/html" title="OSDI’26 | ECHO：稀疏 attention 省了算力，却把 HBM 撑爆了"/><published>2026-07-29T00:00:00+00:00</published><updated>2026-07-29T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260729-echo</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260729-echo/"><![CDATA[<h1 id="osdi26--echo稀疏-attention-省了算力却把-hbm-撑爆了">OSDI’26 | ECHO：稀疏 attention 省了算力，却把 HBM 撑爆了</h1> <blockquote> <p>原文：<a href="https://www.usenix.org/conference/osdi26/presentation/liu-guangda">ECHO: Efficient KV Cache Offloading with Lossless Prefetching for Serving Native Sparse Attention LLMs</a>（OSDI 2026，上海交通大学 + 华为 + 贵州大学）</p> <p>代码：<a href="https://github.com/sjtu-zhao-lab/ECHO">https://github.com/sjtu-zhao-lab/ECHO</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有想过一个略微反直觉的问题：<strong>稀疏 attention 明明是为了省资源，为什么部署起来反而更容易 OOM？</strong></p> <p>按常理讲，稀疏 attention 只算 top-k 个 token 的注意力，计算量从 $O(L^2)$ 降到 $O(Lk)$，KV cache 的访问量也少了一大截，听起来全是好处。但真拿 DeepSeek-V3.2 这种 native sparse attention 模型去跑长上下文 serving，你会发现瓶颈根本不在算力上——<strong>卡在 GPU HBM 容量上，而且比 dense attention 卡得更死</strong>。</p> <p>原因很简单也很气人：稀疏 attention 只是”少算”，没有”少存”。KV cache 该多大还是多大，随 context 线性增长。更离谱的是，DSA（DeepSeek Sparse Attention）为了挑 token，额外引入了一个 indexer 模块，这个 indexer 自己还要存一份 K cache。所以<strong>稀疏 attention 的 KV cache 不但没变小，斜率还比 dense 更陡</strong>。</p> <p>今天想和大家聊聊 OSDI 2026 的这篇 ECHO。它要解决的就是这个矛盾：把 KV cache 卸载（offload）到 host 内存去，从而突破 HBM 容量限制，把 batch size 撑起来。这个思路本身不新，但 ECHO 把两件之前没做好的事做成了——<strong>cache 管理全部塞进 CUDA Graph 里跑</strong>，以及<strong>无损（lossless）的 KV prefetch</strong>。</p> <p>先说结论：在 8×H20 上服务 DeepSeek-V3.2，长上下文 workload 下 ECHO 相比 SGLang 拿到最高 <strong>2.15×</strong> 的 generation throughput，HBM 更紧张的时候能到 <strong>4.12×</strong>。</p> <hr/> <h2 id="2-先建立概念">2. 先建立概念</h2> <p>这篇文章涉及的名词有点多，先用大白话过一遍，后面看设计才不会懵。</p> <p><strong>MLA（Multi-head Latent Attention）</strong>：DeepSeek 系列用的 attention 变体。它先把 hidden states 低秩压缩成 latent 向量 $C = W_{DKV}X \in \mathbb{R}^{L \times d_c}$，需要的时候再上投影还原出 K/V。好处是<strong>推理时只用缓存 latent 向量 $C$</strong>，比 GQA 更省。V3.2 里 $d_c = 512$，attention 头数 $H = 128$。</p> <p>有个关键点后面会反复用到：<strong>MLA 的 KV cache 没法用 tensor parallelism（TP）切开</strong>。因为 latent 向量是所有头共享的，切不了。所以实际部署里 MLA 模块走 data parallelism（DP），也就是<strong>每个 DP worker 都得存一份完整的 KV cache</strong>。</p> <p><strong>DSA 的 indexer</strong>：DeepSeek-V3.2 实现 training-time 稀疏 attention 的方式。它挂了一个轻量的 MQA-like 模块，给每个候选 token 算一个”重要性分数”（index score）。对于 query token $t$，token $s$ 的分数是：</p> \[\sum_{h=1}^{H_{index}} w^I_{h,t} \cdot \mathrm{ReLU}(q^I_{h,t}, k^I_s)\] <p>其中 $H_{index} = 64$、$d_{index} = 128$，都比 MLA 本体（$H=128$、$d_c=512$）小得多，而且 $q^I / k^I$ 用 FP8，ReLU 也比 softmax 便宜。<strong>算完分数取 top-k（V3.2 里 $k = 2048$）送进稀疏 MLA。</strong></p> <p>注意这里有个隐含成本：indexer 要给所有历史 token 算分，所以它自己那份 $k^I$ 也得全部缓存下来。这就是前面说的”额外一份 indexer K cache”。</p> <p><strong>offload / eviction / recall</strong>：这三个词是本文主线。</p> <ul> <li><strong>offload</strong>：把 KV cache 从 GPU 写到 host 内存（DRAM）里备份。</li> <li><strong>eviction（驱逐）</strong>：GPU pool 满了，把一些 token 的 KV 从 GPU 上踢掉腾位置。ECHO 里驱逐<strong>不需要传数据回 host</strong>，因为生成的时候就已经备份过了，改一下元数据就行。</li> <li><strong>recall（召回）</strong>：indexer 选中的 token 如果不在 GPU 上，得从 host 通过 PCIe 拉回来。这一步是延迟大头。</li> </ul> <p><strong>CUDA Graph / graph execution</strong>：把一串 kernel launch 录制成一张确定的图，之后一次 replay 就全跑完，省掉大量 CPU 侧 launch 开销。decode 阶段每步计算量很小，launch 开销占比极高，所以生产环境基本必开。但它有个硬约束：<strong>录进图里的东西必须全在 GPU 上跑完，中间不能有任何 CPU 控制或同步，也不能出现动态长度的 tensor。</strong> 这条约束是 ECHO 第一个设计的全部动机。</p> <p><strong>top-k vs top-p</strong>：top-k 是”取分数最高的 k 个”，必须<strong>算完所有分数</strong>才知道谁入选。top-p 是”分数超过阈值 p 就入选”，可以<strong>边算边定</strong>。这个差别是 ECHO 第二个设计的全部动机。</p> <hr/> <h2 id="3-动机一hbm-容量是怎么被吃干的">3. 动机一：HBM 容量是怎么被吃干的</h2> <h3 id="31-参数吃掉六成kv-cache-还不能切">3.1 参数吃掉六成，KV cache 还不能切</h3> <p>如下图，是 DeepSeek-V3.2 在两种配置下的 HBM 占用。左边是 AWQ 4-bit 量化跑在 8×H20（96GB），右边是原始 FP8 跑在 8×H200（141GB），参数都用 TP8 切开。</p> <p><img src="/assets/img/posts/20260729-echo/figure1_p5.png" alt="DeepSeek-V3.2 的 HBM 占用构成"/></p> <p>看数字：两种配置下<strong>参数都吃掉了 60% 以上的 HBM</strong>，剩下的空间分别只够 ~4.1M 和 ~5.2M token。</p> <p>4M token 听起来很多对吧？但前面说了，MLA 的 KV cache 走 DP 不走 TP，<strong>每个 worker 要存全量</strong>。所以除以 8 之后，每个 DP worker 实际只有 ~511K（AWQ）和 ~655K（FP8）token 的额度。</p> <p>实测更惨。作者在 8×H20 上用 SGLang（TP8 + MLA DP8）跑，因为还有 CUDA Graph 元数据这类运行时开销，<strong>每个 DP worker 最多只能装 380K token</strong>。这意味着面对 ~100K token 的请求，一个 DP worker 只能同时服务 <strong>3-4 个</strong>。</p> <p>并发上不去，硬件利用率自然上不去，吞吐就烂。</p> <h3 id="32-稀疏-kernel-让利用率问题雪上加霜">3.2 稀疏 kernel 让利用率问题雪上加霜</h3> <p>更难受的是，稀疏 attention kernel 本身的算术密度就低。如下图，是 FlashMLA 的 sparse 和 dense decode kernel 在 H20 上的实测 TFLOPS。</p> <p><img src="/assets/img/posts/20260729-echo/figure2_p5.png" alt="稀疏与稠密 FlashMLA kernel 的硬件利用率对比"/></p> <p>对着图看两条线的差距：<strong>dense kernel 算术密度高，batch size 很小就能把硬件喂饱</strong>——64K context 下 batch size 8 就到 97% 利用率了。而 sparse kernel 因为访存不连续，同样条件下只有 <strong>53%</strong>，32K context / batch 16 也只有 63%，得靠更大的 batch 才能追上来。</p> <p>这就形成了一个死结：<strong>稀疏 kernel 恰恰更需要大 batch，而 HBM 容量恰恰不给你大 batch。</strong> 论文里算了笔账：511K token 的额度撑 64K 上下文最多 8 个请求，利用率就卡在 53%。</p> <h3 id="33-现有-offloading-系统为什么不好用">3.3 现有 offloading 系统为什么不好用</h3> <p>Offload KV cache 到 host 不是新想法，但如下表，作者把代表性工作横向比了一遍：</p> <p><img src="/assets/img/posts/20260729-echo/table1_full.png" alt="KV cache offloading 系统横向对比"/></p> <p>两列是关键。<strong>Graph Execution 一列，除 ECHO 全是 ✘</strong>；<strong>KV Prefetch 一列，做了的全是 Lossy（有损）</strong>。另外注意最后一行 Model Scale：之前的动态 offloading 工作基本都在 7B~14B 的 dense 模型 + 单卡上验证，ECHO 是第一个做到 <strong>671B MoE + 多卡</strong>的。</p> <p>先说 graph execution 为什么重要。如下图 (a)，开 CUDA Graph 对 dense 的 DeepSeek-V3.1 是 <strong>1.2×</strong> 提升，而对稀疏的 V3.2 是 <strong>1.5×</strong>——<strong>稀疏模型算子更碎、kernel 更多，对 launch 开销更敏感，所以更依赖 graph</strong>。</p> <p><img src="/assets/img/posts/20260729-echo/figure3_full.png" alt="CUDA Graph 收益与 allocator 延迟对比"/></p> <p>再看 (b)。现有系统（如 SGLang）的 allocator 靠动态 tensor 切片/拼接实现，一次 allocate + free 约 <strong>12 µs</strong>，而 ECHO 的 graph-friendly allocator 只要 <strong>6-8 µs</strong>，快 <strong>1.6-1.9×</strong>。</p> <p>但真正致命的不是这 6 µs 的差距，而是<strong>动态切片这件事本身依赖 CPU 侧控制，会导致整条 decode 路径没法被录成 graph</strong>——也就是说，图 (a) 里那 1.5× 的收益直接就没了。</p> <p>那用 piecewise graph（动态部分 eager 跑、静态部分录成图片段）行不行？作者的回答是不行：<strong>eviction 和 recall 是每层、每个 decode step 都发生的，图会在所有层被打断</strong>，等于把 CPU 控制和 kernel launch 又请回了最怕它们的 decode 阶段。</p> <p><strong>所以第一个挑战是：要做动态 offloading，就必须让 cache 管理整个塞进 GPU graph 里。</strong></p> <h3 id="34-recall-开销以及-indexer-留下的时间窗口">3.4 Recall 开销，以及 indexer 留下的时间窗口</h3> <p>Host-GPU 互联（PCIe）带宽比 HBM 低一到两个数量级，recall 因此很贵。之前 InfiniGen / FreeKV / CLO 的办法是<strong>提前预测</strong>哪些 token 重要然后 prefetch，但它们靠的是”层间相似性”或”步间相似性”来近似分数——<strong>预测不准就选错 token，在稀疏 attention 本身的精度损失之上再叠一层损失</strong>，复杂任务上不敢用。</p> <p>那有没有可能做到无损 prefetch？作者的关键观察是一组复杂度差异：</p> <blockquote> <p><strong>indexer 计算是 $O(L^2)$，而 recall 只有 $O(Lk)$。</strong></p> </blockquote> <p>具体到 decode：indexer 必须给<strong>全部 $L$ 个历史 token</strong> 算分，而 recall 最多只搬 $k$ 个（$k=2048$ 是常数，$L$ 却随 context 一直长）。而且 GPU pool 本身缓存了一部分，实际要搬的往往远少于 $k$ 个。</p> <p>如下图，左右分别是 batch size 1 和 2 下，indexer 计算延迟（橙线，随 context 增长）和不同 hit rate 下的 recall 延迟（蓝色虚线）。</p> <p><img src="/assets/img/posts/20260729-echo/figure4_full.png" alt="indexer 计算延迟与不同命中率下的 recall 延迟"/></p> <p>对着图看：橙线一路往上爬，当 <strong>context 接近 100K、hit rate 到 0.9</strong> 时，橙线已经超过了对应的蓝虚线——<strong>indexer 算的时间比 recall 搬的时间还长</strong>。而且两者抢的是不同资源（indexer 吃 GPU 算力，recall 吃 PCIe 带宽），天然可以重叠。H20 这里是 64GB/s 的 PCIe Gen5，换成 NVLink/SXM（600-900 GB/s）recall 只会更快。</p> <p>结论就是：<strong>indexer 计算期间，PCIe 完全闲着，这是一个白白浪费的时间窗口。</strong></p> <p>问题是窗口用不上。因为 top-k 必须等所有分数算完才能确定谁入选，recall 只能排在 indexer 之后。</p> <p><strong>所以第二个挑战是：top-k 的全局依赖挡住了 prefetch，让 PCIe 在 indexer 计算期间白白空转。</strong></p> <hr/> <h2 id="4-echo-总体设计">4. ECHO 总体设计</h2> <p>两个挑战，两个模块。如下图是整体架构。</p> <p><img src="/assets/img/posts/20260729-echo/figure5_p7.png" alt="ECHO 总体架构"/></p> <p>对着图走一遍数据流：</p> <ol> <li>每个 <strong>DP attention worker</strong> 同时维护 GPU 和 host 两个 KV cache pool，host pool 可以远大于 GPU pool（实验里 host 1.8M token vs GPU 200K）。</li> <li><strong>indexer 的 K cache 常驻 GPU pool</strong>（它每步都要全量访问，放 host 不现实），而 MLA 的 KV cache <strong>两边都存</strong>：host 是全量备份，<strong>GPU pool 只是被选中 token 的一个 cache</strong>。</li> <li><strong>Graph-Friendly Cache Manager</strong> 维护两个 pool 的状态元数据。所有元数据都在 GPU 上，所有更新操作都能被录制/replay，<strong>decode 时整条路径在一张图里跑完</strong>。</li> <li>indexer 计算的同时，<strong>Fused Lossless Prefetch</strong> 已经开始把一部分重要 token 搬过来了，prefetch 直接融进 indexer kernel 并做软件流水。</li> <li>indexer 算完后，对”选中但还没到 GPU 上”的 token 补一次 <strong>Guaranteed Recall</strong>（保证召回）——这一步是无损的关键：<strong>prefetch 只是提前搬，不改变最终选中的集合</strong>。</li> <li>最后根据 index score 更新 token 的驱逐优先级。</li> </ol> <p><strong>关于 PD 分离的一个工程 take</strong>：ECHO 两种部署都支持，但作者建议配 PD 分离（prefill / decode 分开部署），理由挺实在。第一，prefill 阶段每个 query token 独立选 top-k，<strong>总选中集合是所有 token 的并集</strong>，最坏情况会访问全部历史 token，把别人的 cache 全挤掉。第二，prefill 期间要往 host 写大量 KV，实测<strong>会和 recall 抢 PCIe</strong>。所以 ECHO 在 prefill 实例上直接关掉 offloading（反正 prefill 并发低，单个 128K 请求 HBM 装得下），decode 实例才做 offload + prefetch。</p> <hr/> <h2 id="5-graph-friendly-cache-manager">5. Graph-Friendly Cache Manager</h2> <h3 id="51-元数据全部是定长整数-tensor">5.1 元数据：全部是定长整数 tensor</h3> <p>因为 graph execution 不允许动态创建或变长 tensor，所以所有元数据都<strong>静态预分配、定长</strong>，而且都是 GPU 上的整数 tensor：</p> <table> <thead> <tr> <th>元数据</th> <th>含义</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">GPUTokenFree</code></td> <td>bitmap，标记 GPU pool 每个 slot 是否空闲</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">GPUTokenPriority</code></td> <td>每个 slot 的驱逐优先级，低的先被踢；空闲 slot 初始化为 -1</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">GPUIndicesBuffer</code></td> <td>存放 allocate / free 操作产出的 GPU pool 索引</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">GPUTokenToHost</code></td> <td>GPU slot → host pool 索引的映射，初始 ∞ 表示空</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">HostTokenToGPU</code></td> <td>host slot → GPU pool 索引的映射，初始 ∞ 表示不在 GPU 上</td> </tr> </tbody> </table> <p>有个设计细节值得单独说：<strong>GPU pool 按层（per-layer）管理，host pool 按模型（per-model）管理。</strong></p> <p>为什么？因为动态稀疏 attention <strong>每层选中的 token 是不一样的</strong>，各层的 cache 状态天然分叉，没法像传统系统那样所有层统一分配/释放。而 host pool 存的是全量备份，不受 eviction/recall 影响，所以还能按模型统一管。</p> <p>代价是元数据存储开销。设 host pool 有 $N_H$ 个 token、GPU pool 有 $N_G$ 个，总开销是 $4N_H + 13N_G$ 字节。代入实验配置 $N_H = 2M$、$N_G = 200K$：<strong>每层约 10MB，DeepSeek-V3.2 全部 61 层加起来约 610MB</strong>。相比省下来的 HBM 空间，这笔账是划算的。</p> <h3 id="52-三个并行-in-graph-操作">5.2 三个并行 in-graph 操作</h3> <p>cache manager 只有三个基本操作：allocate、free、recall。因为改成 per-layer 管理，每层都要更新一次（而不是每次 forward 一次），所以每个操作都必须极致轻量。ECHO 的做法是<strong>全部用并行 GPU 操作实现，不引入任何变长 tensor</strong>。</p> <p>如下图，把三个操作的元数据变化画在了一起，我们对着走一遍。</p> <p><img src="/assets/img/posts/20260729-echo/figure6_full.png" alt="graph-friendly cache manager 的元数据与更新操作"/></p> <p><strong>Allocate（图中第二栏）</strong>：需要给新生成的 KV 或即将 recall 的 token 找位置时调用。传统实现靠动态切片，ECHO 换成了一个 atomic 技巧：</p> <ul> <li>所有 GPU 线程<strong>并行读</strong> <code class="language-plaintext highlighter-rouge">GPUTokenFree</code>；</li> <li>手上有空闲 slot 的线程对一个全局计数器做 <code class="language-plaintext highlighter-rouge">atomicAdd</code>；</li> <li><code class="language-plaintext highlighter-rouge">atomicAdd</code> 返回的是<strong>加之前的旧值</strong>，靠这个返回值就能定出”我是第几个中签的”，从而决定哪些线程真正分配。</li> </ul> <p>图里的例子是 <code class="language-plaintext highlighter-rouge">size=2, host indices=(3,4)</code>：6 个线程拿到的返回值是 <code class="language-plaintext highlighter-rouge">4 0 3 5 1 2</code>，其中返回值 &lt; 2 的是 thread 1（返回 0）和 thread 4（返回 1），所以<strong>分配 GPU slot 1 和 4</strong>。接着更新元数据：把这两个 slot 的优先级从 -1 抬起来，并在 <code class="language-plaintext highlighter-rouge">GPUTokenToHost</code> / <code class="language-plaintext highlighter-rouge">HostTokenToGPU</code> 之间建立 GPU slot (1,4) ↔ host slot (3,4) 的双向映射。最后把分到的索引写进 <code class="language-plaintext highlighter-rouge">GPUIndicesBuffer</code> 供后续使用。</p> <p><strong>关键在于全程没有变长 tensor</strong>——中签者的数量是动态的，但结果写进定长 buffer 里，靠 -1 表示无效。这就是能录进 graph 的原因。</p> <p><strong>Free（图中第三栏）</strong>：空间不够时驱逐。步骤是：</p> <ol> <li><strong>先保护</strong>：把 indexer 本轮选中的 token 的优先级抬高，避免刚选中就被踢（图中 <code class="language-plaintext highlighter-rouge">protected indices = (1, 5)</code>）；</li> <li><strong>并行 argtopk</strong>：找出优先级最低的若干 slot，索引同样写进 <code class="language-plaintext highlighter-rouge">GPUIndicesBuffer</code>；</li> <li><strong>并行 scatter</strong>：用这些索引更新 <code class="language-plaintext highlighter-rouge">GPUTokenFree</code>、<code class="language-plaintext highlighter-rouge">GPUTokenToHost</code>，并把 <code class="language-plaintext highlighter-rouge">HostTokenToGPU</code> 对应位置改回 ∞。</li> </ol> <p>优先级低的先走，效果上是个 LRU-like 策略。前面提过但值得再强调：<strong>驱逐不需要传任何数据回 host</strong>，因为生成时就备份过了，纯改元数据。host pool 里的 token 只在请求结束时才释放。</p> <p><strong>Recall（图中第四栏）</strong>：从 indexer 拿到选中 token 的 host 索引后，先用一次并行 scatter 查 <code class="language-plaintext highlighter-rouge">HostTokenToGPU</code>，<strong>∞ 的就是缺的</strong>；然后调 free + allocate 腾出 slot；最后更新双向映射，并把 KV 从 host 搬到 GPU。</p> <p>搬运这一步也有讲究：<strong>ECHO 用 unified virtual memory 让 GPU kernel 直接访问 host 内存</strong>，而不是走 host 侧发起的拷贝。这样既避免了 CPU 干预（保住了 graph execution），又提高了 PCIe 带宽利用率。</p> <p><strong>实现层面</strong>：元数据就是普通 PyTorch tensor，并行更新操作用 <strong>Triton</strong> 写。作者也承认，在 NVIDIA 卡上手写 CUDA kernel 能更快，但 Triton 兼容性更好，能覆盖更多硬件后端——这是个挺务实的取舍。</p> <hr/> <h2 id="6-fused-lossless-prefetching">6. Fused Lossless Prefetching</h2> <p>这一节是 ECHO 我觉得最漂亮的部分：<strong>怎么在不改变 top-k 结果的前提下提前搬数据。</strong></p> <h3 id="61-intra-query-prefetchingdecode把-top-k-变成-top-p">6.1 Intra-query prefetching（decode）：把 top-k 变成 top-p</h3> <p>思路的起点是那句”top-p 可以边算边定”。如果能找到一个阈值 $p$，让”分数 &gt; p”这件事等价于”进 top-k”，那 prefetch 就成立了。</p> <p>问题是这个阈值就是<strong>第 k 大的分数</strong>，而它得算完才知道。</p> <p>ECHO 发现的数值性质是：<strong>第 k 大的分数在相邻 decode step 之间高度可预测。</strong> 具体用 EMA（指数移动平均）：</p> \[\hat{s}_{t+1} = \alpha \cdot \hat{s}_t + (1 - \alpha) \cdot s_t\] <p>其中 $s_t$ 是第 $t$ 步实际的第 $k$ 大分数，$\alpha = 0.5$。</p> <p>如下图，是 DeepSeek-V3.2 第 3 层和第 50 层的第 2048 大分数（蓝线，ground truth）和 EMA 预测（橙线），用的是 ShareGPT 里一个 20K token 的请求。</p> <p><img src="/assets/img/posts/20260729-echo/figure7_full.png" alt="第 2048 大 index score 的真实值与 EMA 预测"/></p> <p>两层的分数量级完全不同（layer 3 在 2.0 附近，layer 50 在 -3.5 附近），但橙线都能贴着蓝线走。这也说明为什么阈值必须<strong>按层</strong>预测，不能全局取一个。</p> <p>预测出阈值之后，indexer 算分的过程中，<strong>任何分数超过预测阈值的 token 立刻开始 prefetch</strong>。如下图是这样筛出来的候选 token 数量。</p> <p><img src="/assets/img/posts/20260729-echo/figure8_p10.png" alt="prefetch 候选 token 数量"/></p> <p>蓝线是用 0 当阈值（相当于不预测），黄线是 EMA 预测。可以看到<strong>黄线大部分时候都贴在 2048 附近</strong>，说明预测出来的阈值确实卡得很准。</p> <p>有些步候选数会超过 2048（阈值低估了真实值）。prefetch 太多会拖慢 indexer 计算，所以 ECHO 给 prefetch 数量设了<strong>上界</strong>，用一个全局计数器强制执行，上界与 context 长度成正比（因为 indexer 计算时间也与 context 成正比）。</p> <p><strong>为什么这是无损的？</strong> 因为 EMA 只用来决定”提前搬谁”，不用来决定”最终选谁”。预测偏乐观就多搬几个（浪费一点带宽），偏保守就少搬几个（少省一点时间），但 indexer 算完之后那次 <strong>guaranteed recall 会把真正 top-k 里缺的补齐</strong>。所以最终参与 attention 的 token 集合和不做 prefetch 时<strong>完全一致</strong>。这和 InfiniGen 那类”预测错就真的选错”有本质区别。</p> <h3 id="62-inter-query-prefetchingprefill近似-top-k--分数平移">6.2 Inter-query prefetching（prefill）：近似 top-k + 分数平移</h3> <p>prefill 阶段天然有另一个窗口。indexer 会把输入的 Q 切成多个 block <strong>顺序处理</strong>，所以<strong>算 block $i+1$ 分数的同时，可以 prefetch block $i$ 选中的 token</strong>。</p> <p>但这里有个性能陷阱：在 kernel 里精确算 top-k 会严重拖慢 indexer。所以 ECHO 用<strong>近似 top-k</strong>，只求拿到 top-k 的一个子集去 prefetch（剩下的照样靠 guaranteed recall 兜底）。</p> <p>做法基于 radix select top-k：按分数最高 8 位映射到 <strong>256 个 bin</strong> 建直方图，然后取最大的几个 bin。设 bin 大小为 $S_0, \dots, S_{255}$，选最高 $j$ 个 bin 满足 $S_{256-j} + \dots + S_{255} \le k$ 而再多一个就超了，那么 $S_{255-j}$ 这个 bin 叫<strong>阈值 bin</strong>。</p> <p>关键是这个不等式：$S = S_{256-j} + \dots + S_{255} &gt; k - S_{255-j}$。也就是说，<strong>阈值 bin 越小，拿到的子集就越接近完整的 top-k</strong>。</p> <p>怎么把阈值 bin 做小？还是 EMA——<strong>用上一个 prefill chunk 末尾 token 的第 k 大分数预测当前 chunk 的阈值，然后建直方图时把所有分数减掉这个预测值</strong>。分数平移之后，关注区域正好落在直方图分辨率最高的地方。</p> <p>如下图，箱线图对比了平移前后阈值 bin 的大小分布。</p> <p><img src="/assets/img/posts/20260729-echo/figure9_p10.png" alt="平移前后阈值 bin 大小的分布"/></p> <p>平移之后阈值 bin 明显变小，近似 top-k 的精度就上来了。</p> <h3 id="63-融合-kernel三级软件流水">6.3 融合 kernel：三级软件流水</h3> <p>prefetch 要真的省时间，就不能作为独立 kernel 串在后面，得<strong>融进 indexer kernel 里和计算重叠</strong>。ECHO 基于 DeepGEMM 的 indexer kernel 改，用了两个技术：</p> <p><strong>Warp specialization（warp 专门化）</strong>：传统 SIMT 模型下一个 warp 内所有线程执行相同指令，但现代 GPU 有 tensor core、TMA（tensor memory accelerator）这些专用单元，SIMT 喂不满。warp specialization 就是给 CTA 内不同 warp group <strong>分派不同角色</strong>——比如一部分 warp 专门用 TMA 搬数据，另一部分专门用 tensor core 算。</p> <p><strong>软件流水</strong>：配合异步 barrier，让不同 warp group 的执行互相重叠，形成多级流水。原始 DeepGEMM kernel 是两级流水（TMA 生产者 → GEMM 消费者），ECHO 加了第三级。</p> <p>如下图是 decode 用的融合 kernel。</p> <p><img src="/assets/img/posts/20260729-echo/figure10_p11.png" alt="decode 阶段的融合 intra-query prefetch kernel"/></p> <p>对着图走一遍。decode 时 query token 很少，所以<strong>按 indexer K cache 切分</strong>任务：每个 SM 负责一个 query 向量和一部分 K cache。query 向量常驻 shared memory，然后三级流水转起来：</p> <ul> <li><strong>TMA warps</strong>：把 indexer K block 从 global memory 迭代搬进 shared memory（图中 K0、K1）；</li> <li><strong>GEMM warps</strong>：消费 K block，算出 index score 写回 shared memory；</li> <li><strong>Prefetch warps</strong>：消费这些分数，和预测阈值比较（图右下角那个 <code class="language-plaintext highlighter-rouge">Scores &gt; Est. TopK</code> 的示意），超过阈值的就从 host 内存直接加载。</li> </ul> <p>看右边时间线：TMA 算 K1 的时候 GEMM 在算 K0，GEMM 算 K1 的时候 prefetch 在处理 K0——<strong>三者叠起来了</strong>。为了防止 prefetch 拖慢主计算，ECHO 给 prefetch 分配了更多流水级，并用前面说的全局计数器卡住数量。</p> <p>prefill 的融合 kernel 如下图，差别在于<strong>按 query 切分</strong>：每个 SM 负责若干 Q block 和对应的全部 K block，TMA 外层循环搬 Q、内层循环搬 K。</p> <p><img src="/assets/img/posts/20260729-echo/figure11_p11.png" alt="prefill 阶段的融合 inter-query prefetch kernel"/></p> <p>prefetch warps 拿到 score block 后，先用预测值做平移，再扫一遍建直方图。<strong>当前 Q block 算完的瞬间，prefetch warps 查直方图把超阈值的 token 搬过来，同时 TMA 和 GEMM warps 已经在算下一个 Q block 了</strong>——这就是 inter-query 的重叠。</p> <hr/> <h2 id="7-实验结果">7. 实验结果</h2> <h3 id="71-配置">7.1 配置</h3> <ul> <li><strong>硬件</strong>：单节点 8×NVIDIA H20（96GB），PCIe Gen5 64GB/s，224 核 Intel Xeon Platinum 8480+，1.5TB DRAM。</li> <li><strong>对比系统</strong>：vLLM v0.11.1、SGLang v0.5.4（CUDA 12.9 + PyTorch 2.8）。ECHO 和 SGLang 都是 attention DP8 + MoE TP8；vLLM 只能全 TP8（它不支持 DP+TP 组合，而 MoE 用 EP 在 8×H20 上显存不够起不来）。chunked prefill 都设 2048，<strong>三者都开 CUDA Graph</strong>。</li> <li><strong>模型</strong>：FP8 原版在 8×H20 上装不下，所以用 4-bit 量化的 DeepSeek-V3.2-Exp，<strong>indexer 和第 0、1、2、60 层不量化</strong>以保精度。</li> <li><strong>ECHO host pool</strong>：1.8M token，约 1000GB host 内存。</li> <li><strong>Workload</strong>：长上下文用 InfiniteBench 采样 318 个 80K-100K token 的请求（共约 26M input token）；短上下文用 ShareGPT。</li> </ul> <h3 id="72-吞吐主结果">7.2 吞吐：主结果</h3> <p>如下图，是 PD 分离设定下的 generation throughput（8×H20 作为 decode 实例，KV cache 预先算好）。图上标注的数字是 <strong>effective batch size</strong>，即每个 decode step 平均处理的活跃请求数。</p> <p><img src="/assets/img/posts/20260729-echo/figure12_p12.png" alt="InfiniteBench 上不同请求率下的生成吞吐"/></p> <p>先看 (a)（输出固定 256 token）。低请求率下三家差不多，但随请求率上升 ECHO 一路往上爬，到 Inf 请求率（所有请求同时发进来）时相比 SGLang <strong>2.15×</strong>、相比 vLLM <strong>4.1×</strong>。</p> <p><strong>吞吐差距的来源就写在标注数字里</strong>：ECHO 的 effective batch size 是 <strong>102</strong>，SGLang 是 <strong>28</strong>，vLLM 只有 <strong>1.5</strong>。往回追：</p> <ul> <li>SGLang 的 GPU KV pool 是 325K token，每卡只能装 3-4 个 InfiniteBench 请求，跨 DP worker 加起来 effective batch 约 30；</li> <li>vLLM 因为纯 TP 部署，<strong>MLA KV cache 在 8 卡上重复存了 8 份</strong>，8 卡总 pool 只有 180K token，effective batch 掉到 1.5（不过 TP 部署单步延迟低，所以吞吐没按 batch 比例崩）；</li> <li>ECHO 的 host pool 有 1.8M token，batch 自然能撑起来。</li> </ul> <p>再看 (b)（不限制输出长度），ECHO 对 SGLang 是 <strong>2.08×</strong>。这里有两个有意思的反向效应：低请求率下更长的生成反而<strong>提高</strong>了 DP worker 利用率，两家吞吐都涨；但高请求率下<strong>长尾生成造成 DP worker 负载不均</strong>，有的 worker 空转，两家吞吐都掉。vLLM 纯 TP 反而不受 DP 负载不均影响。</p> <h3 id="73-hbm-越紧张echo-越占优">7.3 HBM 越紧张，ECHO 越占优</h3> <p>如果 MoE 参数更大、HBM 更紧张会怎样？如下图，作者把 GPU pool 压小重测。</p> <p><img src="/assets/img/posts/20260729-echo/figure13_p12.png" alt="受限 HBM 容量下的生成吞吐"/></p> <ul> <li>(a) GPU pool 压到 200K token（ECHO 和 SGLang 同配置）：优势从 2.15× 涨到 <strong>3.10×</strong>；</li> <li>(b) 进一步压到 110K token（三家同配置）：SGLang 和 vLLM 显著退化（它们的 batch 直接被 pool 大小卡死），ECHO 靠 host pool 维持，达到 <strong>4.12×</strong>。</li> </ul> <p>作者还提了一句挺反直觉的观察：<strong>ECHO 在 GPU pool 更小的时候性能还略有提升</strong>，因为 pool 小了元数据管理开销也小了。</p> <h3 id="74-分任务看收益并不均匀">7.4 分任务看：收益并不均匀</h3> <p>如下图是不打输出长度上限、按 InfiniteBench 单任务分别测的结果，下方直方图是各任务的输出长度分布。</p> <p><img src="/assets/img/posts/20260729-echo/figure14_p12.png" alt="InfiniteBench 各任务的生成吞吐"/></p> <p>ECHO 在 Code.Debug 上提升 <strong>27.07%</strong>、En.MC <strong>2.83%</strong>、En.QA <strong>7.11%</strong>，但在 Code.Run 上<strong>比 SGLang 低 1.74%</strong>。</p> <p>作者给的两个解释都很诚实：一是单任务请求数太少（分别只有 75、160、31、42 个），并发撑不起来，喂不满所有 DP worker；二是看下方直方图，<strong>单任务的输出长度分布更偏斜</strong>，少数长尾请求主导了整个任务的运行时间，这时候 SGLang 更低的 ITL 反而能抵消 ECHO 更大 batch 的优势。</p> <h3 id="75-延迟这是有代价的">7.5 延迟：这是有代价的</h3> <p>天下没有免费的午餐。如下图是 ShareGPT（短上下文、轻负载）下的延迟对比。</p> <p><img src="/assets/img/posts/20260729-echo/figure15_p13.png" alt="ShareGPT 上的 TTFT、ITL 和端到端延迟"/></p> <ul> <li><strong>TTFT</strong> 最多 <strong>+7.9%</strong>，主要来自 prefill 时把生成的 KV 备份到 host；</li> <li><strong>ITL</strong> 从 <strong>+2.7% 到 +27.8%</strong>，因为 decode 每层都要做 cache 管理和 recall；</li> <li><strong>端到端</strong>在请求率 0.1-0.2 时 <strong>+15.9%~19.2%</strong>，请求率 ≥0.3 时降到 <strong>≤7.2%</strong>，0.5 到 Inf 之间 <strong>&lt;4.6%</strong>。</li> </ul> <p>规律很清楚：<strong>开销在轻负载时最痛</strong>，因为每步处理的请求少，固定的管理和 recall 成本没东西可以摊薄。所以 ECHO 的定位就是<strong>面向吞吐的长上下文 serving</strong>，不是延迟敏感场景的银弹。这点作者在 discussion 里也直说了。</p> <h3 id="76-延迟拆解开销到底花在哪">7.6 延迟拆解：开销到底花在哪</h3> <p>如下图，是 Inf 请求率下的延迟拆解。</p> <p><img src="/assets/img/posts/20260729-echo/figure16_p13.png" alt="ECHO 与 SGLang 的延迟拆解"/></p> <p><strong>(a) prefill 每层延迟</strong>：6.82ms → 7.42ms，<strong>+8.8%</strong>，增量主要落在 attention 部分（cache 管理 + offload）。</p> <p><strong>(b) decode 全层延迟</strong>：398.5ms → 409.8ms，只 <strong>+2.8%</strong>。注意这张图最重要的信息是<strong>MoE 占了约 70%</strong>（275.9ms，含 dequant、GEMM、AllReduce）。ECHO 给 graph replay 加了 5.9ms（CPU 侧 launch 和元数据开销），剩下约 6.8ms 主要来自 DP worker 间 ReduceScatter 同步——因为 ECHO 并发更大，通信抖动也更大。</p> <p><strong>(c) 一个 decode step 的全层 offloading 开销</strong>：allocate 0.167ms、free 0.572ms、recall 0.145ms、KV offload 0.261ms，<strong>总共 1.15ms，只占全层 decode 延迟的 0.28%</strong>。</p> <p>这个 0.28% 就是 graph-friendly cache manager 的成绩单。具体到规模：allocate/free 平均每步只处理 <strong>37 个</strong> recall 进来的 token，KV offload 每个活跃请求写 1 个新 token，平均每 DP worker <strong>13 个</strong>。</p> <h3 id="77-命中率为什么-recall-量这么小">7.7 命中率：为什么 recall 量这么小</h3> <p>上面 37 个 token 的数字背后是很高的 GPU pool 命中率。如下图是各层的平均命中率。</p> <p><img src="/assets/img/posts/20260729-echo/figure17_p14.png" alt="各层 GPU pool 命中率"/></p> <p><strong>大部分层稳定在 0.97-0.99</strong>，只有第 12 层（0.95）和第 17 层（0.88）低一些。这和已知的 attention pattern 观察一致——注意力里存在”竖线”结构，相邻 query token 选中的 token 高度重叠，所以上一步搬进来的这一步大概率还能用。</p> <h3 id="78-prefetch-的消融">7.8 Prefetch 的消融</h3> <p>如下图是 intra-query prefetch 的微基准。</p> <p><img src="/assets/img/posts/20260729-echo/figure18_p14.png" alt="开关 intra-query prefetch 的延迟对比"/></p> <p>hit rate 0.5 时总延迟降低 <strong>1.29×</strong>，0.9 时 <strong>1.51×</strong>——<strong>0.9 的情况下 recall 开销几乎被完全隐藏</strong>。但 hit rate 0.97 时提升就很小了，因为大部分 token 本来就在 GPU 上，没什么需要搬的。</p> <p>端到端呢？如下图。</p> <p><img src="/assets/img/posts/20260729-echo/figure19_p14.png" alt="开关 intra-query prefetch 的端到端吞吐对比"/></p> <p>只有 <strong>最多 4%</strong>。作者自己解释了这个落差：一是 InfiniteBench 上命中率本来就很高（0.97-0.99），需要 recall 的比例小；二是回看图 16(c)，<strong>MoE 占了 decode 时间的 70%</strong>，省掉 recall 那一小块对总时间影响有限。作者认为换用 expert parallelism 或更高效的 MoE kernel 之后，prefetch 的端到端收益会更明显。</p> <p>inter-query prefetch（prefill）的收益更有限，如下图，最多 <strong>1.1×</strong>。</p> <p><img src="/assets/img/posts/20260729-echo/figure20_p14.png" alt="开关 inter-query prefetch 的延迟对比"/></p> <p>原因是 prefill 时每个 query token 独立选 top-k，<strong>总选中量最坏是 query token 数 × k</strong>，recall 量比 decode 大得多，prefetch 那点提前量占比就小了。不过作者也说这个结果<strong>低估了潜力</strong>——评测里假设不同 query token 选中的 top-k 是随机分布的，而已有工作观察到它们之间<strong>重叠很大</strong>，利用这个重叠能砍掉大量冗余 recall，这留给future work。</p> <hr/> <h2 id="8-几个-discussion-里的-take">8. 几个 discussion 里的 take</h2> <p><strong>能推广到别的稀疏 attention 吗？</strong> 作者分得很清楚：<strong>cache manager 可以，prefetch 不一定。</strong> cache manager 解决的是”动态稀疏模式下 KV 的驱逐与召回”这个通用系统问题，天然适配 token 级稀疏（如 DSA），改改元数据粒度、给 kernel 加上 GQA 的 KV-head 维度也能支持 block 级稀疏——它应该被看作<strong>动态稀疏 attention serving 的系统底座</strong>。但 intra/inter-query prefetch 依赖 DSA indexer 的两个具体性质（选择边界跨步可预测、prefill 按 query block 顺序暴露候选），换成别的机制就得看它有没有类似的”早期信号”。作者顺手提了个方向：<strong>能不能反过来在设计稀疏 attention 时就让选择机制 prefetch 友好</strong>——这个 co-design 的想法我觉得挺有意思。</p> <p><strong>吞吐-延迟的取舍会一直存在吗？</strong> 作者的判断是会收窄。管理开销可以靠更专门的 kernel 优化压下去，传输开销则吃互联带宽的红利——NVLink-C2C 在 Grace 上已经是 900GB/s，Vera 上预期 1.8TB/s。按这个趋势，offload 有机会从”吞吐优先”的技术慢慢变成延迟敏感场景也能用的技术。</p> <p><strong>KV cache 变便宜了，offload 是不是就没意义了？</strong> 这个问题问得好，毕竟量化、hybrid attention 都在降 per-token 成本，新卡 HBM 也上 200GB 了。作者的回答是<strong>两边同时在涨</strong>：一边 agentic workload 把 1M context 推成主流且还在往上走，另一边 MoE 参数在冲万亿规模、挤占本来给 KV cache 的空间。所以 KV cache 效率和 HBM 容量的改善会被 context 和模型规模的增长抵消掉，<strong>分层 KV cache 管理是个长期的系统问题</strong>。</p> <hr/> <h2 id="9-我的一点私货">9. 我的一点私货</h2> <p>读完这篇，我觉得最值得拿走的不是 2.1× 这个数字，而是两个方法论层面的东西。</p> <p><strong>第一，”能不能进 CUDA Graph”正在变成推理系统设计的一等约束。</strong> 这篇里 graph execution 不是锦上添花的优化，而是<strong>倒推整个数据结构设计的源头</strong>。因为不能有变长 tensor，所以元数据全定长 + 用 -1/∞ 做哨兵；因为不能有 CPU 控制，所以 allocate 要用 atomicAdd 的返回值做无锁定序、free 要用 argtopk + scatter；因为 KV 搬运不能走 host 发起的拷贝，所以用 UVM 让 GPU kernel 直接读 host 内存。<strong>换句话说，”必须能被录制”这条约束把一个看起来很朴素的内存管理器逼成了一个全并行的 GPU 数据结构。</strong> 这跟我之前在 MoE 推理上踩的坑是同一类问题——你以为瓶颈在算法，实际上卡你的是”这段逻辑能不能不打断 GPU 执行流”。</p> <p><strong>第二，”无损”这个词值得反复推敲。</strong> InfiniGen 那一路的 prefetch 也叫 prefetch，但它是<strong>用预测替代选择</strong>，预测错了 token 就真选错了，精度损失叠在稀疏 attention 本身的损失之上。ECHO 的做法是<strong>用预测决定”提前搬谁”，用精确计算决定”最终选谁”</strong>，中间靠一次 guaranteed recall 兜底。预测不准的代价只是多搬或少搬一点数据，<strong>不会传导到模型输出上</strong>。这个”把投机限制在性能维度、不让它泄漏到语义维度”的模式，其实和 speculative decoding 靠 verify 保证输出分布不变是同一个哲学。做系统优化的时候，<strong>先想清楚你的近似会污染哪一层</strong>，这个习惯比具体技巧值钱。</p> <p>再补一个吐槽。这篇的 ablation 挺实诚：intra-query prefetch 微基准 1.51×，端到端只剩 4%，作者没藏着，还老老实实分析了是因为 MoE 占了 70% 的 decode 时间。这其实暴露了一个更大的现实——<strong>在 MoE 模型上做 attention 侧的优化，天花板是被 MoE kernel 效率压着的</strong>。你把 attention 和 KV 传输优化到极致，端到端也就动那么几个点。作为研究 MoE 系统的牛马看到这里还是挺有共鸣的：<strong>MoE 的稀疏激活优势和它的全量参数加载/通信开销，仍然是整个行业的核心矛盾</strong>，attention 这边省下来的时间，很容易被 MoE 那边的 dequant + GEMM + AllReduce 吃回去。</p> <p>欢迎评论区交流，有理解错的地方也请指出。</p> <hr/> <p>顺带说一句题外话。这篇里让我印象最深的其实是”约束反推设计”这个思路——graph execution 这条硬约束，逼出了一整套全并行的元数据结构。这种”先把部署约束摆上桌，再倒推算法/结构怎么改”的做法，在自动化模型优化里也是一样的：搜索空间怎么定、什么算可行解，本质上都是被部署约束框出来的。我们把这方面的积累整理成了《动手学 AutoML：从 NAS 到大语言模型优化实战》，讲搜索空间设计、搜索策略、架构评估，以及 LLM 压缩（剪枝/量化）和模型融合这些偏落地的部分。需要说明的是，<strong>这本书里没有 KV cache offloading、CUDA Graph 这类推理框架工程内容</strong>，和本文是方向相邻而不重叠——如果你感兴趣的是”怎么自动化地把模型改小改快”，那它可能对得上；如果你想找的是 serving 系统源码级的东西，那它帮不上忙。</p> <blockquote> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="KV Cache"/><category term="稀疏注意力"/><category term="推理优化"/><category term="论文解读"/><summary type="html"><![CDATA[OSDI’26 | ECHO：稀疏 attention 省了算力，却把 HBM 撑爆了]]></summary></entry><entry><title type="html">EuroSys’26 | eLLM：显存被 KV Cache 榨干的时候，SM 却在摸鱼</title><link href="https://marsggbo.github.io/blog/2026/20260729-ellm/" rel="alternate" type="text/html" title="EuroSys’26 | eLLM：显存被 KV Cache 榨干的时候，SM 却在摸鱼"/><published>2026-07-29T00:00:00+00:00</published><updated>2026-07-29T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260729-ellm</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260729-ellm/"><![CDATA[<h1 id="eurosys26--ellm显存被-kv-cache-榨干的时候sm-却在摸鱼">EuroSys’26 | eLLM：显存被 KV Cache 榨干的时候，SM 却在摸鱼</h1> <blockquote> <p>原文：<a href="https://dl.acm.org/doi/10.1145/3767295.3803570">High Throughput and Low Latency LLM Serving via Adaptive KV Caching</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有观察过一件很割裂的事：跑 LLM 推理服务的时候，<code class="language-plaintext highlighter-rouge">nvidia-smi</code> 里显存条几乎顶满，但 SM 利用率却常年在三四十趴晃。</p> <p>这不是你的配置有问题，这是当前 LLM serving 系统的结构性矛盾。KV Cache 把显存吃干了 → 能同时跑的请求数被卡死 → batch 上不去 → 算力空转。<strong>显存是瓶颈，算力是浪费，两边同时不健康。</strong></p> <p>今天想和大家聊聊 EuroSys’26 这篇 eLLM（澳门大学 + 南洋理工 + 中科院深圳先进院），它的切入点很朴素：既然显存不够、算力有余，那就<strong>把一部分 KV Cache 从显存里扔掉，等要用的时候现算回来</strong>——用富余的算力去换紧缺的显存。</p> <p>听起来像是”这不就是 recomputation 吗，vLLM 早就有了”。但 vLLM 的 recompute 是<strong>请求粒度</strong>的：显存满了就把整个请求抢占掉，全部 KV 丢掉，之后从头 prefill 一遍。eLLM 做的是<strong>token 粒度 + layer 粒度</strong>的部分缓存，而且把重算的 kernel 和 decode 的 kernel <strong>横向融合</strong>到一起跑，让重算这件事几乎不占额外时间。</p> <p>最终结果：相比 baseline <strong>最高 3.03× 吞吐、2.63× 更低 TTFT、SM 利用率提升 15%</strong>。</p> <hr/> <h2 id="2-先建立概念">2. 先建立概念</h2> <p>后面反复出现的几个名词，先用大白话说清楚，不然读到中间会懵。</p> <h3 id="21-kv-cache-为什么必须存">2.1 KV Cache 为什么必须存</h3> <p>标准的 decoder-only 推理流程如下图：prefill 阶段把整个 prompt 一次性过一遍所有 transformer layer，每层算出 K、V 存下来；decoding 阶段每次只输入一个新 token，它的 Q 要和<strong>之前所有 token 的 K、V</strong> 做 attention。</p> <p><img src="/assets/img/posts/20260729-ellm/figure1_p2.png" alt="KV Cache 推理流程"/></p> <p>所以 KV Cache 不是可选的加速手段，它是”不想每步都把整个历史重算一遍”的必然产物。代价是显存占用随生成长度<strong>线性增长</strong>。一个请求的 KV Cache 大小是 <code class="language-plaintext highlighter-rouge">4 · L · h · s</code>（4 = K 和 V 各一份、半精度每元素 2 字节，L 是层数，h 是 hidden dim，s 是序列长度）。</p> <h3 id="22-all-or-nothing-caching现有方案的共同假设">2.2 all-or-nothing caching：现有方案的共同假设</h3> <p>现在主流的做法，本质都是”要么全存、要么全不存”：</p> <ul> <li><strong>vLLM-Swap</strong>：显存满了，把被抢占请求的 KV 整份换到 host memory，有空间了再换回来</li> <li><strong>vLLM-Recompute</strong>：显存满了，把被抢占请求的 KV 整份丢掉，有空间了重新 prefill</li> <li><strong>HCache</strong>：不存 KV，改存<strong>中间 hidden state</strong>（体积更小），恢复时从 hidden state 快速重算出 KV。加速的是”恢复”这一步</li> <li><strong>KVPR</strong>：从输入 activation 恢复被抢占请求</li> </ul> <p>前三类（全 token 全层恢复 / 全 token 部分层恢复 / 全层部分关键 token 恢复）有个共同点：<strong>正在 decode 的请求，必须在显存里持有完整 KV Cache</strong>。HCache 和 KVPR 优化的是”被踢出去的请求怎么快点回来”，而不是”在线请求能不能少占点显存”。</p> <p>这就是 eLLM 想撬的那块砖。</p> <h3 id="23-cache-ratio部分-token-缓存">2.3 cache ratio：部分 token 缓存</h3> <p>eLLM 的做法如下图 (a)：一个长度 <code class="language-plaintext highlighter-rouge">n</code> 的 prompt，decoding 期间把<strong>最前面 s 个 token</strong> 的 KV 从显存里踢掉，只保留后面的，于是 cache ratio = <code class="language-plaintext highlighter-rouge">(n − s) / n</code>。要 decode 新 token 时，先把这 s 个 token 的 KV <strong>现场重算</strong>出来，和缓存的部分拼在一起做 attention，算完立刻释放。</p> <p><img src="/assets/img/posts/20260729-ellm/figure3_full.png" alt="部分 token 缓存示意与效果"/></p> <p>关键点：<strong>踢掉最前面的 token 是有讲究的</strong>。重算 s 个 token 的代价随 s 的位置无关但随长度平方增长（attention 部分是 <code class="language-plaintext highlighter-rouge">4hs²r²</code>），而前缀 token 的重算是”短序列 attention”，本身就便宜。作者的原话是 recomputing early-sequence tokens incurs negligible latency。</p> <p>注意这里<strong>没有任何精度损失</strong>——重算出来的 KV 和原来存的一模一样，attention 语义完全保持。这一点和 InfiniGen / ArkVale 那类”只保留 critical token”的稀疏化方案有本质区别。</p> <h3 id="24-vertical-fusion-vs-horizontal-fusion">2.4 vertical fusion vs horizontal fusion</h3> <p>kernel fusion 大家熟的是<strong>纵向融合</strong>：把连续的多个 kernel 串成一个，省掉 kernel launch 开销和中间结果的 global memory 往返（FlashAttention 就是典型）。</p> <p><strong>横向融合</strong>不一样：把<strong>互相没有数据依赖</strong>的多个 kernel 塞进同一个 kernel 里并发执行，给它们分配不同的 thread group。它优化的不是访存，而是<strong>资源互补</strong>——如果一个 kernel 是 compute-bound、另一个是 memory-bound，合在一起跑就能同时把 SM 的计算单元和显存带宽吃满。</p> <p>eLLM 融的就是这么一对：<strong>K1 = 重算 kernel（compute-intensive）</strong>，<strong>K2 = decode kernel（memory-intensive）</strong>。</p> <hr/> <h2 id="3-动机显存和算力的失衡有多离谱">3. 动机：显存和算力的失衡有多离谱</h2> <p>作者在 A100 80GB 上跑 Llama2-13B（MHA），ShareGPT 数据集，50 req/s，测了显存和算力利用率的联合分布：</p> <p><img src="/assets/img/posts/20260729-ellm/figure2_full.png" alt="显存算力失衡分布"/></p> <p>左图是散点 + 边缘密度分布，横轴算力利用率、纵轴显存利用率。<strong>显存利用率稳定在 50%~100%，而算力利用率大部分时间只在 0%~10% 之间</strong>。右图更直观：随着请求涌入，KV Cache 用量迅速爬到满，而且<strong>即使有大量请求已经跑完、腾出了算力，KV Cache 占用依然高位不下</strong>——因为还在跑的那些请求的输出 token 在不断累积。</p> <p>有人会说这是 MHA 的锅，换成 MLA 就好了。作者顺手试了 DeepSeek-V2-Lite（16B，带 MLA）：<strong>在 97.4% 显存占用的情况下，GPU 利用率中位数仍然只有 35%</strong>。</p> <p>所以结论很硬：<strong>这不是 attention 变体的问题，是 serving 范式的问题。</strong> 架构层面的压缩只是把墙推远了一点，墙还在那儿。</p> <h3 id="31-部分缓存真的有用吗">3.1 部分缓存真的有用吗</h3> <p>作者做了个很干脆的实验（上面那张 (b) 图）：同样的模型、同样的 Azure trace，跑到 <strong>200 秒</strong>的时候，把所有在线请求的 cache ratio 一把改成 <strong>50%</strong>。</p> <p>结果：<strong>并发执行的请求数几乎翻倍，TTFT 降低 48.05%，SM 利用率提升 8.7%。</strong></p> <p>然后是完整的 cache ratio 扫描，横轴是缓存比例从 10% 到 90%：</p> <p><img src="/assets/img/posts/20260729-ellm/figure4_full.png" alt="缓存比例扫描"/></p> <p>读法是这样的：缓存的 KV 越少（横轴越往左），吞吐和 TTFT 的改善越大（最高 <strong>57%</strong> 吞吐提升）。代价是 TPOT 变差——毕竟要多算。但有意思的地方在于，<strong>随着 cache ratio 往上走，存在一个区间：TPOT 几乎不受影响，而 TTFT 和吞吐仍有显著改善</strong>。</p> <blockquote> <p><strong>Insight #1</strong>：部分 token 缓存提高了 batch 内的请求并发度，从而提升吞吐、降低 TTFT；而只要 cache ratio 选得好，TPOT 可以做到基本不退化。</p> </blockquote> <h3 id="32-横向融合的收益">3.2 横向融合的收益</h3> <p>重算这件事本身是”额外开销”，但如果它能藏进 decode 的空隙里呢？如下图 (a)，decode 第 <code class="language-plaintext highlighter-rouge">i</code> 层的 token T8 需要 T1~T7 在第 <code class="language-plaintext highlighter-rouge">i</code> 层的 KV；T4~T7 是缓存的，T1~T3 得重算。而<strong>下一层 <code class="language-plaintext highlighter-rouge">i+1</code> 的 T1~T3 重算（K1）和当前层的 decode（K2）之间没有数据依赖</strong>，可以并发。</p> <p><img src="/assets/img/posts/20260729-ellm/figure5_full.png" alt="横向核融合与收益"/></p> <p>右图是在 40% token 被踢掉的设定下测的收益，两种 K1:K2 线程分配比例（0.5:0.5 和 0.75:0.25，较大的那份给 K1，因为 K1 更吃算力）：<strong>吞吐提升最高 18.4%，TPOT 降低最高 25.0%</strong>。</p> <p>但注意 TTFT 是<strong>负收益</strong>（−13.9% / −11.4%）。原因很实在：横向融合需要额外拿出一层的显存空间来暂存 K1 产生的 KV，这部分显存挤压了 prefill 请求的空间，导致某些请求的 TTFT 变差。</p> <blockquote> <p><strong>Insight #2</strong>：横向融合通过合并资源需求互补的 kernel 来提升 GPU 利用率，配合线程分配的精细调节，能进一步放大部分 token 缓存的优势。</p> </blockquote> <h3 id="33-两个挑战">3.3 两个挑战</h3> <p>到这里方案的雏形有了，但真要落地有两个坑：</p> <p><strong>C1：请求到达和生成长度都在剧烈波动。</strong> 请求变多、序列变长 → 显存需求线性涨，而重算量是<strong>平方级</strong>涨的（<code class="language-plaintext highlighter-rouge">4hLs²r²</code> 那一项）。cache ratio 必须动态调；融合 kernel 的线程分配也必须跟着实时的重算负载和 decode 复杂度变。</p> <p><strong>C2：token 缓存和 kernel 融合互相耦合。</strong> cache ratio 决定了显存开销；kernel 融合虽然提升算力效率，但<strong>自己也要吃显存</strong>（存融合 kernel 的中间 KV）；而未缓存 token 的重算量又反过来决定了 K1/K2 的线程分配。这三者是一个闭环，不能分开优化。</p> <hr/> <h2 id="4-ellm-总体设计">4. eLLM 总体设计</h2> <p>针对 C1/C2，eLLM 设计成<strong>两级优化框架</strong>，如下图：</p> <p><img src="/assets/img/posts/20260729-ellm/figure6_p5.png" alt="eLLM 系统架构"/></p> <p><strong>Request Level（请求级）</strong>：</p> <ul> <li>❶ <strong>Request Batching</strong>：决定这一轮的 batch size <code class="language-plaintext highlighter-rouge">b</code>，在显存约束和算力约束之间取最大并发</li> <li>❷ <strong>Token-wise Caching</strong>：给每个请求分配 cache ratio，控制显存足迹</li> </ul> <p><strong>Layer Level（层级）</strong>：</p> <ul> <li>❸ <strong>Comm-Com Overlapping</strong>：用两条独立的 CUDA stream，让”未缓存 token 的重算”和”缓存 KV 在 host↔GPU 之间的搬运”重叠起来</li> <li>❹ <strong>Layer-wise Kernel Fusion</strong>：把重算 kernel 和 decode kernel 横向融合，并按实时负载调线程分配</li> </ul> <p>关键是这两级<strong>不是单向的</strong>。层级优化确定了融合配置之后，会把真实的额外显存需求 <code class="language-plaintext highlighter-rouge">M_o</code> <strong>反馈回请求级</strong>，请求级据此把 batch size 往上推、cache ratio 往下压——形成一个闭环。这就是它解 C2 的方式。</p> <hr/> <h2 id="5-新的-kv-管理机制为什么-pagedattention-不够用">5. 新的 KV 管理机制：为什么 PagedAttention 不够用</h2> <p>vLLM 的 PagedAttention 把<strong>一个序列的所有层</strong>打包放在一个 block 里，这个设计隐含假设了”token 和 layer 都是全缓存的”。eLLM 要做的是部分 token + 部分 layer 缓存，而且重算过程还需要给中间层开临时空间，这就和固定大小 block 打起来了：<strong>如果只往一个 block 里存少数几层少数几个 token，剩下的空间没法给别的请求用，内部碎片会非常严重。</strong></p> <p>eLLM 的改法如下图：</p> <p><img src="/assets/img/posts/20260729-ellm/figure7_p6.png" alt="KV 管理机制"/></p> <p>核心是把 block 的粒度从”全部 L 层”细化到”<strong>连续 F 层</strong>“，每个单元带上自己的 layer index 元数据，再用一张 map table 把逻辑地址映射到物理地址（这些物理区域可以是不连续的）。</p> <p>map table 的字段：<code class="language-plaintext highlighter-rouge">seq_id</code>、<code class="language-plaintext highlighter-rouge">token_id</code>、<code class="language-plaintext highlighter-rouge">layer_id</code>、<code class="language-plaintext highlighter-rouge">logical_block_id</code>、<code class="language-plaintext highlighter-rouge">physical_block_id</code>、<code class="language-plaintext highlighter-rouge">#filled</code>（这个 block 里存了几个 token）。host 侧用同样的 reshape 方式，保证两边一致。</p> <p>图里的例子走一遍：假设 <code class="language-plaintext highlighter-rouge">L = 8</code> 层，token block size = 4，那么原始 block 维度是 4×4。设 <code class="language-plaintext highlighter-rouge">F = 4</code>，原始 block table 被 reshape 成 8×4 的逻辑 block table，物理 block table 同样 reshape。请求 1 和请求 2 的前 4 个 token 未缓存，后面的 token 有 KV Cache。</p> <ul> <li><strong>Example A</strong>：Swap、Recompute、Decode 分别需要存 1、1、2 层 → 一共 2 个 block</li> <li><strong>Example B</strong>：分别需要 2、2、4 层 → 扩展到 4 个 block</li> </ul> <p><code class="language-plaintext highlighter-rouge">F</code> 是个 trade-off 参数，默认 4（后面 §7.3 有敏感性分析）。</p> <hr/> <h2 id="6-两级优化怎么求解">6. 两级优化怎么求解</h2> <h3 id="61-优化问题的形式化">6.1 优化问题的形式化</h3> <p>eLLM 要在 <code class="language-plaintext highlighter-rouge">N</code> 张 GPU（每张显存 <code class="language-plaintext highlighter-rouge">M_G</code>）上，最大化输出 token 吞吐，同时保证 TPOT SLO：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>max_{b,r,δ}   b / T(b, r, δ)
s.t.  T(b,r,δ) + WT_max ≤ SLO
      Σ_{i=1}^{b} 4·L·h·s_i·(1−r) + M_W + M_o ≤ M_G · N
      0 ≤ r ≤ 1,  1 ≤ b ≤ B
</code></pre></div></div> <p>三个决策变量：</p> <ul> <li><code class="language-plaintext highlighter-rouge">b</code>：batch size，这一轮并发处理多少请求</li> <li><code class="language-plaintext highlighter-rouge">r</code>：<strong>未缓存</strong> token 比例（<code class="language-plaintext highlighter-rouge">1−r</code> 是缓存比例）</li> <li><code class="language-plaintext highlighter-rouge">δ</code>：融合 kernel 里分给 K1 的线程数</li> </ul> <p>约束的含义：第一条是”处理时间 + 最大等待时间不超 SLO”；第二条是显存约束——缓存的 KV（<code class="language-plaintext highlighter-rouge">4Lhs_i(1−r)</code>）、模型权重 <code class="language-plaintext highlighter-rouge">M_W</code>、重叠和融合所需的额外空间 <code class="language-plaintext highlighter-rouge">M_o</code>，三者加起来不能超过集群总显存。</p> <p>这是一个在线的<strong>混合整数非线性规划</strong>，直接解代价太高。所以 eLLM 拆成两级：请求级只解 <code class="language-plaintext highlighter-rouge">b</code> 和 <code class="language-plaintext highlighter-rouge">r</code>（先忽略 <code class="language-plaintext highlighter-rouge">δ</code>），层级解 <code class="language-plaintext highlighter-rouge">δ</code> 和重叠配置，然后把精确的 <code class="language-plaintext highlighter-rouge">M_o</code> 喂回请求级。</p> <p><strong>这个反馈很关键</strong>：请求级第一次求解时，<code class="language-plaintext highlighter-rouge">M_o</code> 按最坏情况取 <code class="language-plaintext highlighter-rouge">40hbsr</code>（假设 swap 3 层、recompute 2 层）；层级优化定下来之后，<code class="language-plaintext highlighter-rouge">M_o</code> 可以按真实值 <code class="language-plaintext highlighter-rouge">8hbsr(l₁+l₂)</code> 算，通常小得多。省出来的显存让系统可以<strong>继续把 <code class="language-plaintext highlighter-rouge">b</code> 调大、把 <code class="language-plaintext highlighter-rouge">r</code> 调小</strong>——闭环就这么转起来了。</p> <h3 id="62-延迟估计模型">6.2 延迟估计模型</h3> <p>要解上面那个问题，得先能估 <code class="language-plaintext highlighter-rouge">T(b, r)</code>。eLLM 走的是纯 FLOPs 路线：算总 FLOPs，除以 GPU 的 FLOPS。</p> <p><strong>重算部分</strong>（相当于 prefill 了 <code class="language-plaintext highlighter-rouge">s_i·r</code> 个 token）：attention 是 <code class="language-plaintext highlighter-rouge">8s_i rh² + 4s_i²r²h</code>，MLP 两个线性层是 <code class="language-plaintext highlighter-rouge">16s_i rh²</code>，输出层映射到词表是 <code class="language-plaintext highlighter-rouge">2s_i rhV</code>。全 <code class="language-plaintext highlighter-rouge">L</code> 层、<code class="language-plaintext highlighter-rouge">b</code> 个请求加起来：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>V_rec(b,r) = Σ_i [ 24h²L·s_i·r + 4hL·s_i²r² + 2hV·s_i·r + ε₀ ]
</code></pre></div></div> <p><strong>decode 部分</strong>（KV 已缓存，attention 变便宜）：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>V_dec(b,r) = Σ_i [ 24h²L + 4hL(s_i + 1) + 2hV·s_i + ε₁ ]
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">ε₀</code>、<code class="language-plaintext highlighter-rouge">ε₁</code> 是 Layernorm 这类小算子的开销，理论模型里省掉了。最后 <code class="language-plaintext highlighter-rouge">T(b,r) = (V_rec + V_dec) / FLOPS</code>。</p> <p>把最大化转成等价的最小化形式之后，用 SciPy 的 <strong>SLSQP</strong> solver 求解。调度策略默认 FCFS（架构上支持换）。</p> <h3 id="63-层级重算和搬运怎么重叠">6.3 层级：重算和搬运怎么重叠</h3> <p>Comm-Com Overlapping 的思路如下图 (a)：一部分层的 KV 从 host 搬回来（swap），另一部分层直接在 GPU 上重算，两件事并发。</p> <p><img src="/assets/img/posts/20260729-ellm/figure8_full.png" alt="层级重叠与 swap 开销"/></p> <p>比如可以重算第 <code class="language-plaintext highlighter-rouge">i</code> 和 <code class="language-plaintext highlighter-rouge">i+1</code> 层、同时把 <code class="language-plaintext highlighter-rouge">i+2</code> 层从 host 搬过来；也可以只重算第 <code class="language-plaintext highlighter-rouge">i</code> 层、搬 <code class="language-plaintext highlighter-rouge">i+1</code> 和 <code class="language-plaintext highlighter-rouge">i+2</code> 层。但<strong>配错了就会有气泡</strong>——图里的 ① <code class="language-plaintext highlighter-rouge">T_R &lt; T_S</code> 和 ② <code class="language-plaintext highlighter-rouge">T_R &gt; T_S</code> 都会产生空转，只有 ③ <code class="language-plaintext highlighter-rouge">T_R ≈ T_S</code> 才是理想状态。</p> <p>所以准确估这两个时间很重要。</p> <p><strong>搬运延迟</strong>：作者在 A100 上 profile 了 Llama2-13B 的 KV 搬运，如上图 (b)，swap 时间和搬运的 token 数是<strong>干净的线性关系</strong>（0~4000 token 对应 0~4.5ms）。于是建模成：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>T_{k,l₁} = (α·k + β) · l₁ / L
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">k</code> 是搬运 token 数，<code class="language-plaintext highlighter-rouge">l₁</code> 是搬运层数，<code class="language-plaintext highlighter-rouge">α</code>、<code class="language-plaintext highlighter-rouge">β</code> 是针对具体模型和硬件预先 profile 出来的常数。</p> <p><strong>重算延迟</strong>：直接复用 §6.2 的模型，限定在 <code class="language-plaintext highlighter-rouge">l₂</code> 层：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>T_{b,r,l₂} = (1/FLOPS) · Σ_i [ (24h²s_i r + 4hs_i²r²)·l₂/L + 2s_i rhV ]
</code></pre></div></div> <p>搜索空间被刻意压得很小：<strong>重算最多 2 层，搬运最多 3 层，一共 6 种可行配置</strong>。算法就是把 6 种全试一遍，选 <code class="language-plaintext highlighter-rouge">T_R</code> 和 <code class="language-plaintext highlighter-rouge">T_S</code> 最接近的那个。</p> <h3 id="64-层级线程怎么分">6.4 层级：线程怎么分</h3> <p>融合的具体形态如下图：Stream A 跑 Swap（数据搬运），Stream B 跑融合后的 K1 + K2（计算）。相邻层之间用<strong>纵向融合</strong>省 launch 开销，K1 和 K2 之间用<strong>横向融合</strong>吃满 SM。</p> <p><img src="/assets/img/posts/20260729-ellm/figure9_p8.png" alt="层级优化"/></p> <p>额外显存开销算得很清楚：swap 层的 KV <code class="language-plaintext highlighter-rouge">4hbsr·l₁</code>、K1 重算层 <code class="language-plaintext highlighter-rouge">4hbsr·l₂</code>、K2 decode 层 <code class="language-plaintext highlighter-rouge">4hbsr(l₁+l₂)</code>，合计 <code class="language-plaintext highlighter-rouge">8hbsr(l₁+l₂)</code>——这就是前面 <code class="language-plaintext highlighter-rouge">M_o</code> 的精确值。</p> <p>线程分配的依据是<strong>计算量之比</strong>：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>V_K1 = Σ_i [ 24h²·l₂·s_i r + 4h·l₂·s_i²r² + 2hV·s_i r ]
V_K2 = Σ_i [ 24h²(l₁+l₂) + 4h(l₁+l₂)(s_i+1) + 2hV·s_i ]
</code></pre></div></div> <p>按 <code class="language-plaintext highlighter-rouge">V_K1 / V_K2</code> 的比例，从每个 block 的总线程数 <code class="language-plaintext highlighter-rouge">T</code>（A100 上上限 1024）里切 <code class="language-plaintext highlighter-rouge">δ</code> 个给 K1，剩下 <code class="language-plaintext highlighter-rouge">T − δ</code> 给 K2。而且<strong>必须对齐到 32 的倍数</strong>——NVIDIA 的 warp scheduler 一次调度 32 个线程，不对齐就浪费执行单元。</p> <h3 id="65-工程实现">6.5 工程实现</h3> <p>基于 vLLM 扩展，<strong>3500 行 Python + 1700 行 CUDA</strong>，保留 vLLM 的核心 API 保证向后兼容。</p> <p>有个细节挺有意思：kernel fusion 部分，eLLM 把 CUDA kernel <strong>预编译成 31 个 <code class="language-plaintext highlighter-rouge">.so</code> 文件</strong>，对应 <code class="language-plaintext highlighter-rouge">[32, 1024]</code> 区间内步长 32 的所有线程配置。运行时按当前层的计算需求动态选一个加载。这是在”线程分配要能随负载变”和”CUDA kernel 的线程配置必须编译期确定”之间的一个很务实的折中。</p> <hr/> <h2 id="7-实验结果">7. 实验结果</h2> <h3 id="71-setup">7.1 setup</h3> <p>4×A100 80GB，<strong>注意是 PCIe 4.0×16 互联，没有 NVLink</strong>（所以 swap 带宽是真实瓶颈，不是理想环境）。CUDA 12.4，Driver 550.107.02，96 核 Xeon Gold 6342，256GB host memory。给 swap 预留的 host memory：Llama2-13B 40GB，Llama2-70B 160GB。</p> <p><img src="/assets/img/posts/20260729-ellm/table1_full.png" alt="模型与数据集配置"/></p> <p>两个数据集的长度差异非常大：<strong>ShareGPT 平均输入 222 / 输出 1346</strong>；<strong>L-Eval（paper assistant 子集）平均输入 35956 / 输出 5189</strong>——后者是长上下文场景。</p> <p>负载用的是 DynamoLLM 里的 <strong>Azure LLM 真实调用 trace</strong>（平均请求间隔 22.15ms，标准差 37.49ms，波动极大），取前 3000 个时间间隔样本，对应 25 req/s。ShareGPT 因为 prompt 短，把间隔砍半（→ 50 req/s）；L-Eval 因为 prompt 长，把间隔翻倍（→ 12.5 req/s）。ShareGPT 端到端跑约 1.25 小时，L-Eval 约 2.92 小时，含 2 分钟 warm-up、5 次测量。</p> <p>Baseline 三个：<strong>vLLM-Recompute</strong>、<strong>vLLM-Swap</strong>、<strong>HCache</strong>（作者自己在 vLLM 里复现了 HCache 的 StatePartitionAlgorithm）。为了公平，作者<strong>把 Eq.(5) 以 <code class="language-plaintext highlighter-rouge">r = 0</code> 代入到三个 baseline 里</strong>，让它们也能在 TPOT SLO 约束下估算最大 batch size——这个处理挺讲究的，避免了”我有 batch 优化你没有”的不对等比较。</p> <h3 id="72-端到端sharegpt">7.2 端到端：ShareGPT</h3> <p><img src="/assets/img/posts/20260729-ellm/figure10_p10.png" alt="ShareGPT 端到端结果"/></p> <p><strong>吞吐</strong>：Llama2-13B 上 eLLM 比 vLLM-Recompute 快 <strong>2.64×</strong>、比 vLLM-Swap 快 <strong>2.61×</strong>、比 HCache 快 <strong>1.91×</strong>。Llama2-70B 上分别是 <strong>2.0×</strong>（对两个 vLLM 变体）和 <strong>1.6×</strong>（对 HCache）。</p> <p><strong>TTFT</strong>：13B 上比 Recompute 低 <strong>2.63×</strong>、比 Swap 低 2.59×、比 HCache 低 1.7×；70B 上是 1.62× / 1.61× / 1.21×。</p> <p>这里的逻辑链条要理清楚：<strong>TTFT 变好不是因为 prefill 变快了，而是因为并发度上去了，请求在队列里等的时间变短了。</strong> 再加上层级的重叠和融合把 TPOT 压下来，请求周转更快，进一步反哺 TTFT。</p> <p><strong>SLO 达标率</strong>：eLLM 97.3%（13B）/ 98.6%（70B）；两个 vLLM 变体都在 88% 以下；HCache 92.8% / 92.9%。平均 TPOT 的绝对值：13B 上 Recompute / Swap / HCache / eLLM 分别是 <strong>63.91 / 64.57 / 54.54 / 48.39 ms</strong>，70B 上是 <strong>680.82 / 667.01 / 522.2 / 501.74 ms</strong>。</p> <h3 id="73-运行时行为并发数和-sm-利用率">7.3 运行时行为：并发数和 SM 利用率</h3> <p>这张图我觉得是全文最有说服力的一张，因为它直接验证了 motivation：</p> <p><img src="/assets/img/posts/20260729-ellm/figure11_p11.png" alt="运行时行为分布"/></p> <p><strong>并发请求数</strong>（每 5 秒采样一次）：Recompute 223.16、Swap 220.94、HCache 240.71、<strong>eLLM 254.64</strong>，而且 eLLM 的<strong>峰值达到 448</strong>，远超其他。原因就是 token 级 + layer 级缓存降低了单请求的显存占用——<strong>eLLM 的平均 cached ratio 是 0.64，也就是省了超过 36% 的显存</strong>。</p> <p><strong>SM 利用率</strong>（<code class="language-plaintext highlighter-rouge">nvidia dmon</code> 每秒采样）：Recompute 58.65%、Swap 58.20%、HCache 61.20%、<strong>eLLM 67.02%</strong>，提升约 <strong>15%</strong>。看右图的密度分布，在 SM 利用率 &gt; 80% 的区间，eLLM 的密度是最高的。</p> <p><strong>能效</strong>：重算确实多耗电，但吞吐涨得更多，所以每 token 的能耗反而更低——eLLM 的 token/watt 比 HCache 高 <strong>1.43×</strong>，比 vLLM-Swap 高 <strong>2.21×</strong>。</p> <h3 id="74-端到端长上下文l-eval">7.4 端到端：长上下文（L-Eval）</h3> <p><img src="/assets/img/posts/20260729-ellm/figure12_p11.png" alt="L-Eval 端到端结果"/></p> <p>长上下文场景下 eLLM 的优势<strong>更明显</strong>：平均只缓存前缀长度的 <strong>0.53</strong>（省了超过 47% 显存），吞吐最高 <strong>3.03×</strong>，TTFT 最高低 <strong>1.79×</strong>，SLO 达标率 96.6%（13B）/ 97.4%（70B）。</p> <p>拆开看：对 vLLM-Recompute 吞吐 <strong>2.73×</strong> / TTFT <strong>1.76×</strong>；对 vLLM-Swap 吞吐 <strong>3.03×</strong> / TTFT <strong>1.71×</strong>；对 HCache 吞吐 <strong>1.61×</strong> / TTFT <strong>1.37×</strong>。</p> <p>这个”越长越强”的趋势是合理的：序列越长，KV Cache 占比越大，能省的显存绝对量越多；同时前缀 token 的重算相对总计算量的占比反而更小。</p> <h3 id="75-消融两个模块各贡献多少">7.5 消融：两个模块各贡献多少</h3> <p><img src="/assets/img/posts/20260729-ellm/figure13_p11.png" alt="模块消融"/></p> <p><strong>关掉 Comm-Com Overlapping</strong>（w/o Overlap）：搬运必须等当前层重算完，串行化 → TPOT 变差。但吞吐仍有 1.67×（13B）/ 1.61×（70B），TTFT 仍降低最高 1.73×（13B）/ 1.4×（70B）。</p> <p><strong>关掉 Kernel Fusion</strong>（w/o KF）：重算和 decode 串行执行，SM 效率下降。吞吐仍有 1.85× 和 1.83×（对两个 vLLM 变体）、1.34×（13B）/ 1.46×（70B）（对 HCache），TTFT 最高快 1.96×。</p> <p>有个细节值得注意：<strong>w/o KF 的表现比 w/o Overlap 更好</strong>。这说明在这套设计里，Comm-Com Overlapping 的边际贡献比 Kernel Fusion 更大——重叠配错了会产生气泡，代价比”不融合 kernel”更大。</p> <h3 id="76-负载压力测试">7.6 负载压力测试</h3> <p>把 Azure trace 缩放到 0.5×~3.0×（12.5~75 req/s，步长 12.5），测 TTFT 的拐点在哪：</p> <p><img src="/assets/img/posts/20260729-ellm/figure14_p12.png" alt="不同负载下的 TTFT"/></p> <p><strong>Llama2-70B</strong>：eLLM 到 <strong>2.5×</strong> 负载才出现拐点，HCache / Swap / Recompute 在 <strong>2×</strong> 就崩了 → 请求吞吐高 <strong>1.25×</strong>。</p> <p><strong>Llama2-13B</strong>：eLLM 和 HCache 的拐点都在 2×——13B 在高请求率下先撞到的是<strong>算力饱和</strong>，不是显存。但即便如此，eLLM 的部分 token 缓存让它能塞进更多请求做并发 decode，所以 token 吞吐仍显著更高，TTFT 也更低。</p> <p><strong>Goodput</strong>（按 DistServe 的定义：SLO 达标率 ≥ 90% 时能承受的最大请求率）：</p> <p><img src="/assets/img/posts/20260729-ellm/figure15_p12.png" alt="不同负载下的 SLO 达标率"/></p> <p>3× 负载下，13B 上 eLLM 还能维持约 90% SLO 达标，是 HCache 的 <strong>1.5×</strong>、两个 vLLM 变体的 <strong>6×</strong>；70B 上是 HCache 的 <strong>1.67×</strong>、vLLM 的 <strong>5× 以上</strong>。</p> <h3 id="77-参数-f-的敏感性">7.7 参数 F 的敏感性</h3> <p><img src="/assets/img/posts/20260729-ellm/table2_full.png" alt="F 敏感性分析"/></p> <p><code class="language-plaintext highlighter-rouge">F = 2 / 4 / 8</code> 对应显存碎片率 1.18% / 1.28% / 2.30%，map table 访问开销占请求处理时间的 6.67% / 3.84% / 1.47%，归一化吞吐 1.13× / 1.31× / 1.0。</p> <p><strong><code class="language-plaintext highlighter-rouge">F = 4</code> 是最优点</strong>：<code class="language-plaintext highlighter-rouge">F</code> 太小碎片少但 map table 查询次数多、开销大；<code class="language-plaintext highlighter-rouge">F</code> 太大查询便宜但碎片严重。比 <code class="language-plaintext highlighter-rouge">F=2</code> 高 1.14×，比 <code class="language-plaintext highlighter-rouge">F=8</code> 高 1.31×。</p> <p>作者还额外解释了长时间运行的显存稳定性：eLLM 和 PagedAttention 一样用固定大小 block，请求结束就把 block 还回全局 free list，<strong>不需要周期性的 compaction 或 GC</strong>。碎片被严格限制在”每个活跃请求不到一个 block 的尾部内部碎片”，外部碎片靠 block 粒度复用规避掉。</p> <h3 id="78-延迟模型的鲁棒性">7.8 延迟模型的鲁棒性</h3> <p>这块作者写得很坦诚，值得单独说。§6.2 的延迟模型<strong>只考虑了计算，没考虑访存带宽</strong>，而 decoding 阶段本质是 memory-bandwidth-bound，所以模型会<strong>低估</strong> decoding 延迟。</p> <p>作者的辩护有三点：(1) 总延迟 = 重算 + decode，decode 的误差只影响一部分；(2) <code class="language-plaintext highlighter-rouge">b</code> 和 <code class="language-plaintext highlighter-rouge">r</code> 是通过显存约束耦合在一起的，延迟估计不需要那么精确；(3) 选 <code class="language-plaintext highlighter-rouge">b</code>、<code class="language-plaintext highlighter-rouge">r</code> 只是端到端延迟优化的一个环节，还有别的机制在兜。</p> <p>然后给了实测：V100 上跑 Llama2-13B，<code class="language-plaintext highlighter-rouge">T(b,r)</code> 的 <strong>MAPE 是 0.13</strong>。更狠的是把 A100 的 FLOPs 和峰值带宽人为扰动 <strong>±20%</strong>：+20% 高估导致吞吐降 1.6%、TTFT 涨 5.4%；−20% 低估导致吞吐降 1.9%、TTFT 涨 3.4%。</p> <p><strong>结论是估计误差只会让工作点稍微偏移，不会导致系统不稳定。</strong> 这个论证方式比硬说”我的模型很准”要有说服力得多。</p> <h3 id="79-系统开销">7.9 系统开销</h3> <p><img src="/assets/img/posts/20260729-ellm/figure16_p13.png" alt="系统开销 CDF"/></p> <p><strong>请求级</strong>（解 <code class="language-plaintext highlighter-rouge">b</code> 和 <code class="language-plaintext highlighter-rouge">r</code>）：所有配置下都 &lt; 21ms，90% &lt; 10ms。13B 上 ShareGPT / L-Eval 平均 4.58ms / 2.91ms，70B 上 6.04ms / 3.25ms。扩展性也测了：70B 从 4 卡到 8 卡、全局 batch 上限从 512 到 1024，平均求解开销只从 <strong>6.04ms 涨到 7.41ms</strong>。</p> <p><strong>层级</strong>：map table 查询平均 &lt; 0.015ms，90% &lt; 0.025ms，峰值 0.105ms；算 <code class="language-plaintext highlighter-rouge">V_K1 / V_K2</code> 比例约 0.03ms。基本可以忽略。</p> <hr/> <h2 id="8-泛化性讨论">8. 泛化性讨论</h2> <p>这一节作者主动讨论了几个”你可能会问”的问题，我觉得比很多论文的 discussion 都实在：</p> <p><strong>MoE 会怎样？</strong> 更有利。在 DeepSeek-V2 这类稀疏 FFN 的模型里，计算瓶颈往 attention 偏移了——4×A100（TP=4）上 profile 显示 <strong>attention 占 DeepSeek-V2 执行时间的 54%，而 Llama2-70B 只有 37%</strong>；FFN 通信开销相当（7.3% vs 6.5%），但 attention 模块内部开销更低（6.4% vs 10.0%）。所以 KV 为中心的优化在 MoE 上收益更大。</p> <p><strong>分布式？</strong> 有 NVLink 时带宽更充裕，能支持更高的 swap 比例和更大 batch。多机场景下 eLLM 是<strong>节点内的局部优化器</strong>，防止某个节点因显存瓶颈变成 pipeline 并行里的 straggler。和 collective 型的 Sequence Parallelism 天然兼容（缓存和重算都在本地 shard 上做）；但和 <strong>Ring-based SP 不好搭</strong>——Ring Attention 的流水线特性要求缺失的 KV block 必须预先物化，否则会产生气泡。这块留给未来工作。</p> <p><strong>和 chunked-prefill 冲突吗？</strong> 正交。chunked-prefill 优化的是 batching 和 GPU 利用率，<strong>不直接降低 decode 阶段的 KV 驻留量</strong>；eLLM 补的正是这个缺口。</p> <p><strong>和 KV 压缩兼容吗？</strong> 分情况——<strong>和 KV 量化完全兼容</strong>（量化只改变计算/带宽常数，cache-vs-recompute 机制不变）；<strong>和 importance-based token pruning 不兼容</strong>，因为 eLLM 保持精确的 attention 语义（要么留着、要么重算回来），而 pruning 是永久删掉 token、不可逆地改变了 attention 上下文。</p> <hr/> <h2 id="9-我的一点私货">9. 我的一点私货</h2> <p>几个 take：</p> <p><strong>第一，”重算换显存”这个方向被低估了。</strong> HCache 那篇论文里明确说过 token 级重算不实际（overhead 太大），eLLM 打破这个判断的关键动作不是算法，是<strong>把所有在线请求的重算 token 攒到一起 batch 掉</strong>。单个请求重算几十个 token 是小 GEMM，GPU 跑起来极其低效；几百个请求的重算 token 拼成一个大 GEMM，效率就完全不一样了。这个思路很朴素，但它是整个方案能成立的地基。</p> <p><strong>第二，横向融合的价值可能比大家想的大。</strong> 现在做 kernel 优化，绝大多数精力花在纵向融合（FlashAttention 那条路）。但纵向融合优化的是访存，当你的 workload 里同时存在 compute-bound 和 memory-bound 的算子时，<strong>横向融合是在利用一个完全不同的空隙</strong>——SM 里计算单元和带宽的空闲是错位的。POD-Attention 也在做类似的事（hybrid batch 的 attention kernel），我觉得这个方向后面会热。</p> <p><strong>第三，闭环反馈是这篇工作真正的工程含量。</strong> 论文里那个 <code class="language-plaintext highlighter-rouge">M_o</code> 从 <code class="language-plaintext highlighter-rouge">40hbsr</code> 修正到 <code class="language-plaintext highlighter-rouge">8hbsr(l₁+l₂)</code> 再反哺请求级的循环，看着不起眼，但它解决的是”两个优化目标互相占用同一份资源”这类问题的通用范式。很多系统论文遇到这种耦合就直接固定一边的参数了事，eLLM 老老实实做了迭代。</p> <p><strong>第四，和同期工作的对照。</strong> 我今天正好也写了 OSDI’26 的 ECHO（KV Cache offloading for sparse attention）。两篇解的是同一堵墙，路子完全相反：<strong>ECHO 是”全存 host、按需搬回”，eLLM 是”存一部分、算一部分”。</strong> ECHO 赌的是 sparse attention 下真正需要的 KV 只是一小部分，所以搬运量可控；eLLM 赌的是算力有大量富余，重算比搬运更划算（尤其在没有 NVLink 的 PCIe 环境下，这个赌注更容易赢）。这两个假设在不同硬件配置下的胜负关系会翻转，我认为它们是互补而不是竞争。</p> <p><strong>最后一个疑问</strong>：eLLM 的延迟模型是纯 FLOPs 的，作者的鲁棒性论证我认可，但那都是在 A100/V100 上做的。到了 H100/B200 这种算力增速远快于带宽增速的架构上，”decode 是 memory-bound”这件事会更严重，<code class="language-plaintext highlighter-rouge">T(b,r)</code> 的低估幅度会不会大到影响工作点选择？作者说”只要更新架构相关常数就行”，但纯计算模型缺的是带宽项，不是常数——这可能不是换个常数能补上的。有跑过类似实验的朋友欢迎在评论区聊聊。</p> <hr/> <p>顺带一提，这篇文章里 <code class="language-plaintext highlighter-rouge">b</code>、<code class="language-plaintext highlighter-rouge">r</code>、<code class="language-plaintext highlighter-rouge">δ</code> 三个变量的联合求解，本质上就是一个带约束的在线超参优化问题——eLLM 用 SLSQP 解它，因为目标函数有解析形式。但如果目标函数不可导、或者搜索空间是离散的（比如上面那个”31 个预编译 <code class="language-plaintext highlighter-rouge">.so</code> 里选哪个”），就得换成搜索类方法了。我和几位朋友把这类”自动化搜索最优配置”的方法整理成了《动手学 AutoML：从 NAS 到大语言模型优化实战》，涵盖进化算法、贝叶斯优化、强化学习、DARTS 这些搜索策略，以及 LLM 压缩、剪枝、量化的实战。不过要说清楚：<strong>这本书里没有 KV Cache 管理、CUDA kernel 融合这类推理框架的工程内容</strong>，eLLM 这种系统级优化不在覆盖范围内，只能说是方向相邻。感兴趣的话可以看看。</p> <blockquote> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="KV Cache"/><category term="推理优化"/><category term="vLLM"/><category term="论文解读"/><summary type="html"><![CDATA[EuroSys’26 | eLLM：显存被 KV Cache 榨干的时候，SM 却在摸鱼]]></summary></entry><entry><title type="html">DAC’26 | ExpertFlow 让 MoE 推理不再被显存卡脖子：单卡跑 Mixtral-8x7B，吞吐最高提升 10 倍</title><link href="https://marsggbo.github.io/blog/2026/20260728-expertflow/" rel="alternate" type="text/html" title="DAC’26 | ExpertFlow 让 MoE 推理不再被显存卡脖子：单卡跑 Mixtral-8x7B，吞吐最高提升 10 倍"/><published>2026-07-28T00:00:00+00:00</published><updated>2026-07-28T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260728-expertflow</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260728-expertflow/"><![CDATA[<h1 id="dac26--expertflow-让-moe-推理不再被显存卡脖子单卡跑-mixtral-8x7b吞吐最高提升-10-倍">DAC’26 | ExpertFlow 让 MoE 推理不再被显存卡脖子：单卡跑 Mixtral-8x7B，吞吐最高提升 10 倍</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2410.17954">ExpertFlow: Efficient Mixture-of-Experts Inference via Predictive Expert Caching and Token Scheduling</a></p> <p>代码：<a href="https://github.com/expertflow-dac/expertflow">github.com/expertflow-dac/expertflow</a> ｜ 权重：<a href="https://huggingface.co/expertflow-dac/models">huggingface.co/expertflow-dac/models</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>先交代下背景：我们团队的 ExpertFlow 被 <strong>DAC 2026</strong> 录用了，而且大会这两天（7 月 26–29 日）正在 Long Beach 进行中，所以趁热和大家聊聊这篇工作到底解决了什么真问题。代码和训好的 predictor 权重都已经开源（链接见文章开头），欢迎大家来踩。</p> <p>这篇工作的出发点其实很朴素：<strong>MoE 模型号称”稀疏激活”，但显存占用一点都不稀疏</strong>。以 Mixtral-8×7B 为例，跑一次推理需要 96 GB 以上的 GPU 显存，一张 80 GB 的 A100 都塞不下。但离谱的是，它 46.7B 参数里有 45.1B 属于 expert 模块，每个 token 实际只激活其中 2/8 的 expert——也就是说，<strong>90% 以上的参数在大部分时间里全程闲着，却要全程占着显存</strong>。</p> <p>“计算稀疏、内存稠密”，这就是 MoE 落地绕不过去的核心矛盾。最直接的思路是 offloading：把不用的 expert 放在 CPU 内存，需要时再搬进 GPU。想法很自然，但真做起来会结结实实踩三个坑，这也正是 ExpertFlow 要解决的三个问题。</p> <h2 id="2-moe-offloading-的三个坑">2. MoE offloading 的三个坑</h2> <p><strong>坑一：expert 预测不准、不及时。</strong> offloading 要想不拖慢推理，就必须提前知道”接下来哪些 expert 会被用到”，才能提前把它们搬上 GPU。已有方法分两派：回归派直接拟合 router 打分，但打分的微小误差就会改变路由结果，往往需要大量 fine-tune 来补救；分类派里的启发式方法（基于 token-expert 统计）轻量但抓不住输入相关的路由行为，学习式方法（如 ProMoE）精度上去了，却是逐层预测——要等上一层跑完才知道下一层的 expert，调度空间被压得很死。</p> <p><strong>坑二：expert 利用率低。</strong> decoding 阶段 batch 通常不大，token 在 expert 之间的分布可能极度不均，最坏情况是每个 expert 只分到 1 个 token。而 expert kernel 处理少量 token 时开销几乎是常数，这种碎片化的分配等于让 GPU 空转。</p> <p><strong>坑三：缓存策略失效。</strong> GPU 上的 expert cache 常用 LRU 策略，但 LRU 只看”最近用过谁”，完全无视 MoE 动态路由的模式，命中率很不稳定。SE-MoE 用 ring-buffer 缓存相邻两层的所有 expert，locality 是好了，但对 Switch-128 这种一层 128 个 expert 的模型，内存开销直接爆炸，还会反复搬运根本不会被激活的 expert。</p> <h2 id="3-expertflow预测驱动的三件套">3. ExpertFlow：预测驱动的三件套</h2> <p>我们的解法是把整个推理过程重构成一条”先预测、再调度、后执行”的流水线，三个组件互相咬合。</p> <p><img src="/assets/img/posts/20260728-expertflow/figure1_p2.png" alt="ExpertFlow 系统总览"/></p> <p>如上图，给定两个输入 batch：① Routing Path Predictor（RPP）一次性预测所有 token 在<strong>所有层</strong>的 expert 激活；② Token Scheduler（TS）根据预测结果跨 batch 重组 token，把路由路径相似的 token 聚到一起；③ Expert Cache Engine（ECE）只把需要的 expert 从 CPU 预取进 GPU；④ 模型带着优化后的 token 流和 CPU/GPU 异构的 expert 放置开始执行。</p> <h3 id="31-routing-path-predictor一次前向预测全部层">3.1 Routing Path Predictor：一次前向，预测全部层</h3> <p><img src="/assets/img/posts/20260728-expertflow/figure2_p3.png" alt="RPP 结构示意"/></p> <p>RPP 用的是 T5 风格的 encoder-decoder：encoder 编码完整输入序列，decoder 上挂 L 个轻量预测头，每个头输出对应 MoE 层的 E 个 expert 的激活概率，最终得到形状为 (B, S, L, E) 的概率矩阵。训练时把每个 token 的路由路径编码成 {0,1} 的二值矩阵，当成 multi-label 分类任务用 binary cross-entropy 学。</p> <p>和逐层预测的方案比，关键区别在于：<strong>第一个 MoE 层还没开始算，整条路由计划就已经出来了</strong>，prefetching 和内存规划可以从推理一开始就全局展开。而且这个 predictor 非常小——网格搜索后的最终配置只有 <strong>7.21 MB</strong>，相对于动辄几十 GB 的 MoE 本体几乎可以忽略。</p> <h3 id="32-token-scheduler把人以群分用在-token-上">3.2 Token Scheduler：把”人以群分”用在 token 上</h3> <p><img src="/assets/img/posts/20260728-expertflow/figure3_full.png" alt="Token Scheduler 重组 token 示意"/></p> <p>如上图左侧的最坏情况：4 个 expert、两个 batch 各 4 个 token，每个 batch 都把所有 expert 激活了一遍，但每个 expert 只算 1 个 token——搬运成本拉满，计算效率拉胯。TS 拿到 RPP 的预测后，把两个相邻 batch 的 token 合并再重新划分，让路由路径相似的 token 进同一个 batch。重组后（右侧）每个 batch 只激活一半 expert，每个 expert 一次算 2 个 token。</p> <p>严格求解这个划分问题是 intractable 的，所以我们用 K-means 风格的聚类做近似：以路由路径的 Hamming 距离构造相似度矩阵，迭代地把 token 分成两个等大的簇。整个过程跑在 CPU 上，开销 <strong>&lt;10ms</strong>。重组会打乱 KV cache 的顺序，所以 TS 还配了两个轻量原语：<strong>Merge</strong> 按全局 token 顺序重建 KV cache，<strong>Reindex</strong> 更新 token 索引，保证 attention 语义不变。</p> <h3 id="33-expert-cache-engine预测性缓存--运行时纠错">3.3 Expert Cache Engine：预测性缓存 + 运行时纠错</h3> <p><img src="/assets/img/posts/20260728-expertflow/figure5_full.png" alt="Expert Cache Engine 工作流"/></p> <p>ECE 包含两部分。<strong>PLEC（Predictive Locality-aware Expert Caching）</strong>不像 LRU 那样按”最近使用”被动淘汰，而是按预测的 expert 需求主动分配 cache slot：哪层预测要用的 expert 多，就多给几个坑位，然后提前把最可能激活的 expert 搬上 GPU。<strong>Real-time Correction</strong> 负责兜底——预测不可能 100% 准，一旦发现预取错了（比如上图里多搬了 e23、漏了 e24），就在其他 expert 计算的同时用高优先级队列做 swap，把 I/O 藏进计算里。</p> <p>另外，RPP 和 TS 本身也有开销，为此我们设计了 <strong>Dual-Batch Pipeline</strong>：把两个 batch 绑成一个调度单元，当前单元在 GPU 上做 prefill/decode 时，下一个单元的 RPP 预测和 TS 重组在旁路并行执行，互相不阻塞。</p> <p><img src="/assets/img/posts/20260728-expertflow/figure4_p4.png" alt="Sequential vs Dual-Batch 流水线对比"/></p> <h2 id="4-实验数字说话">4. 实验：数字说话</h2> <p>实验在单张 <strong>A40（48 GB）</strong>上做，覆盖 6 个 MoE 模型（Switch-32/64/128、Mixtral-8×7B、Qwen1.5-MoE、Deepseek-MoE）和 4 个数据集（Alpaca、WMT16、XSUM、AIME2024）。</p> <p><img src="/assets/img/posts/20260728-expertflow/table1_p4.png" alt="各 MoE 模型配置"/></p> <h3 id="41-吞吐">4.1 吞吐</h3> <p><img src="/assets/img/posts/20260728-expertflow/figure6_full.png" alt="不同模型和数据集上的吞吐对比"/></p> <p>如上图，在 Switch 系列上，expert 数越多、显存越紧，收益越大：(CS=16, BS=32) 时对 SE-MoE 的加速比从 Switch-32 的 2.01× 涨到 Switch-128 的 5.86×；在 Switch-128 上进一步把 cache size 压到 4，加速比拉到 <strong>9.99×</strong>。对 Mixtral-8、Qwen1.5、Deepseek-MoE，相对最强 baseline Cache-MoE 的提升分别最高 1.99×、2.12×、1.94×。</p> <p>这里说句实在话：<strong>10× 这个数字出现在 expert 数量极多、cache 极小的 Switch-128 场景，属于我们方法的”主场”</strong>；在 Mixtral 这类 expert 少而大的模型上，收益是 2× 左右——依然可观，但不是处处 10×，大家看数字的时候可以留意下配置。</p> <h3 id="42-跨域泛化">4.2 跨域泛化</h3> <p>一个自然的质疑是：predictor 是在特定数据集上训的，换个任务是不是就废了？我们用 Alpaca 上训的 RPP 直接去跑 WMT16 和 XSUM：</p> <p><img src="/assets/img/posts/20260728-expertflow/figure7_full.png" alt="跨域 predictor 的吞吐表现"/></p> <p>加速比依然稳定在 1.78×–2.21×，说明 RPP 学到的 expert 激活模式有相当的任务无关性。预测精度上，in-domain 场景大多 90% 以上，cross-domain 一般掉 5–10 个点。</p> <p><img src="/assets/img/posts/20260728-expertflow/figure9_p6.png" alt="各层 expert 预测精度对比"/></p> <p>如上图，对比两个启发式 baseline——TLP（拿上一个 decoding step 的路由当预测）和 SLP（拿上一层的路由当预测）——RPP 全场领先，在 Switch-64 上 baseline 精度不足 20% 时 RPP 还能维持 80–90%。</p> <h3 id="43-显存">4.3 显存</h3> <p><img src="/assets/img/posts/20260728-expertflow/figure8_full.png" alt="GPU 显存占用对比"/></p> <p>对比把全部参数放 GPU 的 All-in-GPU（AIG）：Switch-128 从 15.26 GB 降到 <strong>1.03 GB</strong>（约 93% 的削减），Deepseek-MoE 从 31.35 GB 降到 6.38 GB，Qwen1.5 从 35.21 GB 降到 6.52 GB。最有意思的是 Mixtral-8×7B：AIG 直接 OOM，我们的系统 <strong>15.99 GB</strong> 就能跑完——这意味着一张消费级的 24 GB 卡理论上也能伺候得动。</p> <h3 id="44-消融">4.4 消融</h3> <p>cache 策略上，PLEC 对 LRU 的命中率优势在 batch 变大时越拉越开：CS=8、BS=16 时 PLEC 命中率 71.89%，LRU 只有 36.22%；CS=16 时 PLEC 最高到 91.96%。</p> <p><img src="/assets/img/posts/20260728-expertflow/figure10_p6.png" alt="PLEC 与 LRU 缓存命中率对比"/></p> <p>TS 的贡献单独拆出来看：Switch-32/64/128 上分别带来 1.03×、1.15×、1.17× 的吞吐提升。趋势也符合直觉——<strong>expert 越多，token 分配越碎，重组 batch 的收益就越大</strong>。</p> <p><img src="/assets/img/posts/20260728-expertflow/table2_p6.png" alt="Token Scheduler 消融结果"/></p> <h2 id="5-一些冷静的讨论">5. 一些冷静的讨论</h2> <p>自夸完了，也聊聊边界，免得大家以为这是银弹：</p> <ol> <li><strong>RPP 需要为每个模型单独训练</strong>。虽然只有 7.21 MB、训练数据靠跑 10000 条输入自动收集，成本不高，但换模型就得重训，做不到即插即用。</li> <li><strong>收益和 expert 粒度强相关</strong>。expert 多而小的模型（Switch 系列）收益最大；expert 少而大的模型（Mixtral）单个 expert 的搬运延迟更难藏，收益就收敛到 2× 附近。</li> <li>目前实验集中在<strong>单卡 offloading</strong> 场景。不过路由预测这个信号本身是通用的，往分布式 expert placement、routing-guided pruning、多级缓存上延伸都是很自然的方向，我们也在往这边推进。</li> </ol> <p>总结一下，ExpertFlow 的核心思想一句话就能说清：<strong>与其被动应对 MoE 的动态路由，不如先把路由预测出来，让调度和缓存都”未卜先知”</strong>。预测（RPP）、调度（TS）、缓存（ECE）三件套环环相扣，最终换来最高 93.72% 的显存削减和 10× 的吞吐提升。</p> <p>论文正在 DAC’26 现场展示（7 月 26–29 日，Long Beach），在场的朋友欢迎来交流。代码和权重都在：</p> <ul> <li>论文：<a href="https://arxiv.org/abs/2410.17954">arxiv.org/abs/2410.17954</a></li> <li>代码：<a href="https://github.com/expertflow-dac/expertflow">github.com/expertflow-dac/expertflow</a></li> <li>权重：<a href="https://huggingface.co/expertflow-dac/models">huggingface.co/expertflow-dac/models</a></li> </ul> <p>有问题欢迎评论区交流，也欢迎直接在 GitHub 提 issue。</p> <hr/> <p>顺带扯句题外话：ExpertFlow 里”用一个小模型去预测大模型的行为，再据此做系统优化”的思路，和 AutoML 里”用 proxy 评估架构好坏”其实是一脉相承的——都是用便宜的预测换昂贵的执行。我们把这些年在 AutoML/NAS 和 LLM 效率优化上的积累整理成了一本书《动手学 AutoML：从 NAS 到大语言模型优化实战》，里面有 LLM 压缩（剪枝/量化）、模型融合的章节，和本文的推理加速角度不同，但目标一致：让大模型跑得更省。感兴趣的朋友可以翻翻。</p> <blockquote> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="MoE"/><category term="推理优化"/><category term="论文解读"/><summary type="html"><![CDATA[DAC’26 | ExpertFlow 让 MoE 推理不再被显存卡脖子：单卡跑 Mixtral-8x7B，吞吐最高提升 10 倍]]></summary></entry><entry><title type="html">106K star 的开源 ASR 老祖宗 | 重读 Whisper 原论文的每一个技术细节</title><link href="https://marsggbo.github.io/blog/2026/20260728-whisper/" rel="alternate" type="text/html" title="106K star 的开源 ASR 老祖宗 | 重读 Whisper 原论文的每一个技术细节"/><published>2026-07-28T00:00:00+00:00</published><updated>2026-07-28T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260728-whisper</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260728-whisper/"><![CDATA[<h1 id="106k-star-的开源-asr-老祖宗--重读-whisper-原论文的每一个技术细节">106K star 的开源 ASR 老祖宗 | 重读 Whisper 原论文的每一个技术细节</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2212.04356">Robust Speech Recognition via Large-Scale Weak Supervision</a>（OpenAI, 2022）</p> </blockquote> <hr/> <h2 id="1-前言给老祖宗上香之前先把它的家谱读明白">1. 前言：给老祖宗上香之前，先把它的家谱读明白</h2> <p>今天想和大家聊聊 Whisper 这篇论文。</p> <p>先摆一个数字：<code class="language-plaintext highlighter-rouge">openai/whisper</code> 这个仓库现在（2026 年 7 月）有 <strong>105.8K star、12.8K fork</strong>。这个量级在整个 AI 开源生态里都是头部，在语音这个方向上更是断层第一 —— 后面所有你听过的名字，faster-whisper、whisper.cpp、WhisperX、distil-whisper、insanely-fast-whisper，全是它的下游。<strong>你现在用的绝大多数开源 ASR 方案，往上追两代就是这个仓库。</strong></p> <p>问题是，正因为它太好用了，大部分人（包括我）跟它的关系一直停留在 <code class="language-plaintext highlighter-rouge">pip install openai-whisper</code> 然后 <code class="language-plaintext highlighter-rouge">model.transcribe()</code>。<strong>用了三年，论文没认真读过。</strong> 这次算是回来补作业 —— 认真把这个老祖宗的家谱翻一遍，不是”了解一下 Whisper 是什么”，而是逐节抠它每个技术决定背后的理由。</p> <p>促使我回来补作业的是最近的活。我在做中英混说（code-switching）的 ASR 评测，测了十来个引擎，Whisper 是里面的一个 baseline。跑完之后我盯着数字发了一会儿呆 —— 一个 2022 年 12 月放出来的模型，在 2026 年的评测里依然是个”你必须解释为什么没打赢它”的对照组。更离谱的是，当我想给评测定归一化口径（”9:30” 和 “九点三十” 算不算同一个答案）的时候，翻了一圈，发现业界最常被引用的那套规则，就是 Whisper 论文附录 C 里那 12 条。<strong>连评测的尺子都是它留下的。</strong></p> <p>所以这篇论文有两层价值：<strong>一层是它的方法（弱监督 scaling），另一层是它顺手立下的评测规范。</strong> 后者被引用得远比前者少，但对做工程的人来说可能更有用。</p> <p>作为研究 AI 方向的牛马，我读论文有个习惯：先问”它在跟谁吵架”。Whisper 吵架的对象非常明确 —— wav2vec 2.0 那一条路线。这条路线在当时是绝对主流：自监督预训练一个 audio encoder，然后在下游数据集上 fine-tune。它的问题论文在 Introduction 里说得很直白：</p> <blockquote> <p>这些预训练 audio encoder 学到了高质量的语音表示，但因为它们是纯无监督的，<strong>缺一个同等水平的 decoder</strong> 把表示映射成能用的输出，因此必须有一个 fine-tune 阶段才能真正做语音识别这件事。</p> </blockquote> <p>而 fine-tune 有个更隐蔽的风险，论文举了一个计算机视觉的例子：Radford et al. (2021) 发现在 ImageNet 上 fine-tune 能让物体分类准确率涨 9.2%，但在另外七个自然图像数据集上，平均准确率<strong>一点没涨</strong>。也就是说涨的那 9.2% 全是在学 ImageNet 的数据集特质。</p> <p>论文由此给 ASR 系统提了一个目标，这句话我觉得是整篇文章的立意所在：</p> <blockquote> <p>语音识别系统的目标应该是<strong>开箱即用</strong>地在广泛的环境下可靠工作，而不是对每一个部署分布都要重新监督 fine-tune 一遍 decoder。</p> </blockquote> <p>那怎么做到？当时的思路是把已有的高质量数据集混起来训（SpeechStew 混了 7 个数据集共 5,140 小时）。但 5,140 小时对比 wav2vec 那条路线用的 1,000,000 小时无标注数据，实在太小。</p> <p>Whisper 的选择是：<strong>放弃 gold-standard 标注，去互联网上捞（音频, 字幕）配对，把有监督数据规模拉到 680,000 小时。</strong> 论文管这叫 weak supervision（弱监督）—— 标注是有的，但质量不保证。</p> <p>下面按论文的顺序，把每个技术细节拆开讲。</p> <hr/> <h2 id="2-数据这篇论文真正的护城河">2. 数据：这篇论文真正的护城河</h2> <h3 id="21-一句话总结数据处理哲学">2.1 一句话总结数据处理哲学</h3> <p>论文 2.1 节的第一句话就定了调：</p> <blockquote> <p>我们采取极简的数据预处理方式。与大量语音识别工作相反，我们训练 Whisper 去预测转录文本的<strong>原始形态</strong>，不做任何显著的标准化，依赖 seq2seq 模型的表达力去学会音频到转录形态的映射。</p> </blockquote> <p>这句话的分量要放到当时的语境里才看得出来。传统 ASR 的输出是”标准化”的 —— 全小写、无标点、数字写成单词（<code class="language-plaintext highlighter-rouge">twenty twenty two</code>），然后需要一个独立的 <strong>inverse text normalization（ITN，逆文本标准化）</strong> 模块把它变回人能读的形态（<code class="language-plaintext highlighter-rouge">2022</code>、加标点、加大小写）。ITN 通常是规则系统，是一坨脏活。</p> <p>Whisper 直接让模型预测带标点、带大小写、数字是阿拉伯形态的原始文本，<strong>ITN 这个模块就消失了</strong>。这是”用 scaling 换掉一个工程模块”的典型操作。</p> <h3 id="22-过滤流水线怎么从互联网垃圾里捞出-68-万小时">2.2 过滤流水线：怎么从互联网垃圾里捞出 68 万小时</h3> <p>数据多样性对鲁棒性有好处，但论文明确区分了两种多样性：</p> <blockquote> <p>音频质量的多样性可以帮模型变得鲁棒，但<strong>转录质量的多样性并不同样有益</strong>。</p> </blockquote> <p>初步检查发现原始数据里有大量劣质转录。所以他们上了一整套自动过滤。这是全文我认为最有工程价值的一段，我把它画成了流程图：</p> <p><img src="/assets/img/posts/20260728-whisper/diagram_data_pipeline.png" alt="数据过滤流水线"/></p> <p>对着图从左往右走一遍：</p> <p><strong>第一道闸：字幕是不是机器转写的？</strong> 这是个反直觉但极其关键的设计。互联网上很多”字幕”其实是别家 ASR 系统的输出。论文引 Ghorbani et al. (2021) 说，在人写和机器生成混合的数据上训练会显著损害翻译系统的性能。他们把这个要避免的东西起名叫 <strong>“transcript-ese”</strong>（转录腔）—— 我觉得这个词起得很妙，就像 translationese（翻译腔）一样，是一种被机器污染的语言变体。</p> <p>怎么检测？靠机器 ASR 输出的”缺陷指纹”：</p> <ul> <li>现有 ASR 系统只输出书面语言的一个受限子集，会把难以从音频信号预测的东西<strong>抹掉或标准化掉</strong> —— 复杂标点（感叹号、逗号、问号）、段落空白、大小写</li> <li>全大写或全小写的转录，几乎不可能是人写的</li> <li>即使某些 ASR 带了 ITN，那个 ITN 通常简单或基于规则，从”从不出现逗号”这类未处理的方面依然能识别出来</li> </ul> <p><strong>第二道闸：音频语言 vs 字幕语言。</strong> 他们用一个音频语言检测器（在 VoxLingua107 上 fine-tune 出来的），和字幕文本经 CLD2 判定的语言对比。不一致就不作为语音识别样本。</p> <p>但这里有个漂亮的转折 —— <strong>不一致且字幕是英文的，转成 X→en 翻译训练样本</strong>。翻译能力不是特意去收集数据训出来的，是从”语言不匹配的废料”里回收出来的。这 125,739 小时翻译数据就是这么来的。</p> <p><strong>第三道闸：模糊去重 + 切 30 秒片段。</strong> 音频切成 30 秒段，配上落在这个时间窗内的字幕子集。注意一个细节：<strong>无人声的片段也训练（虽然是降采样后的概率），并把它们当作 VAD（voice activity detection，人声活动检测）的训练数据。</strong> 后面第 4 节会看到 VAD 是怎么白捡的。</p> <p><strong>第四道闸（最容易被忽略）：训完初版模型再回头洗数据。</strong> 原文：</p> <blockquote> <p>训练一个初版模型之后，我们聚合它在各个训练数据源上的错误率信息，并按<strong>错误率高 + 数据源体积大</strong>的组合排序，人工检查这些数据源，以高效地识别并移除低质量数据源。</p> </blockquote> <p>排序键设计得很讲究：只按错误率排会去查一堆边边角角的小数据源；乘上体积，就变成”修一个能收益最大”的优先级。检查结果发现了大量只转录了一部分的、对齐错位的，以及过滤规则没抓到的残留机器字幕。</p> <p>最后还有防污染的一步：与他们认为重叠风险较高的评测集（主要是 TED-LIUM 3）做了 transcript 级别的去重。</p> <h3 id="23-最终的数据构成这张图解释了后面所有实验结果">2.3 最终的数据构成：这张图解释了后面所有实验结果</h3> <p><img src="/assets/img/posts/20260728-whisper/fig11_full.png" alt="训练数据统计"/></p> <p>这是论文附录 E 的图，三块要分开看：</p> <p><strong>中间那块</strong>是总构成 —— 65% 英文语音识别（438,218 小时）、17% 多语言语音识别（117,113 小时）、18% 翻译（125,739 小时）。</p> <p><strong>左边那块</strong>是多语言转写数据按语言排序的柱状图，注意 x 轴是<strong>对数轴</strong>且<strong>从左到右递减</strong>。最上面中文 23,446 小时、德语 13,344、西班牙语 11,100，到最下面老挝语只有 <strong>0.1 小时</strong>。跨度是六个数量级。</p> <p><strong>右边那块</strong>是翻译数据，同样跨度极大：韩语 19,938 小时到土库曼语 1 小时。</p> <p>这张图为什么重要？因为<strong>后面第 3 节所有”某语言表现差”的结果，答案都在这张图里</strong>。论文在 Limitations 里也承认了：由于数据收集流水线偏向互联网的英语中心部分，<strong>大多数语言的训练数据不到 1000 小时</strong>。</p> <hr/> <h2 id="3-模型故意不创新的架构">3. 模型：故意不创新的架构</h2> <h3 id="31-为什么用最普通的-transformer">3.1 为什么用最普通的 Transformer</h3> <p>论文 2.2 节开门见山：</p> <blockquote> <p>由于我们工作的重点是研究大规模有监督预训练对语音识别的能力，我们使用<strong>现成的架构</strong>，以避免把我们的发现与模型改进混淆。</p> </blockquote> <p>这是很干净的实验设计思路：如果同时改数据和改架构，涨点归谁就说不清了。所以他们选了 encoder-decoder Transformer（Vaswani et al., 2017），理由只有一个 —— 这个架构已经被充分验证过能可靠 scale。</p> <h3 id="32-前端和骨架每个数字都对应一个设计决定">3.2 前端和骨架，每个数字都对应一个设计决定</h3> <p><img src="/assets/img/posts/20260728-whisper/fig1_top_full.png" alt="Whisper 整体架构"/></p> <p>对着这张图从下往上、从左往右走一遍。</p> <p><strong>左边的灰框</strong>是训练数据的四种形态，也就是第 2 节那套流水线的产物：英文转写、任意语言→英文翻译、非英文转写、无人声（音频是背景音乐，目标文本是 ∅ 空集）。注意每一条都是「🗣️ 音频 → 📝 文本」的配对，而<strong>四种任务共用同一个模型</strong>，这是下一小节多任务格式要解决的问题。</p> <p><strong>中间偏下的彩色条带</strong>是 Log-Mel Spectrogram，也就是模型真正的输入。具体参数：</p> <ul> <li>所有音频重采样到 <strong>16,000 Hz</strong></li> <li><strong>80 通道</strong>的 log-magnitude Mel 频谱</li> <li><strong>25 毫秒</strong>窗口，<strong>10 毫秒</strong> stride</li> <li>特征归一化：全局缩放到 [−1, 1]，在预训练数据集上均值约为零</li> </ul> <p>10ms stride 意味着 30 秒音频 → 3000 帧。这个数字要记住，因为下一步就是要把它压掉。</p> <p><strong>再往上是紫色的 <code class="language-plaintext highlighter-rouge">2 × Conv1D + GELU</code></strong> —— encoder 的 stem（干），两个卷积层，filter width = 3，激活函数 GELU，<strong>第二个卷积层 stride = 2</strong>。作用就是把 3000 帧压成 1500 帧。为什么必须压？因为 self-attention 是 O(n²)，3000 长度的序列 attention 矩阵是 900 万个元素，1500 就降到 225 万，直接省 4 倍。这一步是纯粹的算力妥协，没什么理论深意，但少了它模型训不起来。</p> <p><strong>stem 输出加上 Sinusoidal Positional Encoding</strong>（图里那个 <code class="language-plaintext highlighter-rouge">~</code> 圆圈符号），然后进 encoder 的 Transformer blocks（图中青色）。两个细节：</p> <ul> <li>Transformer 用 <strong>pre-activation residual blocks</strong>（Child et al., 2019），也就是 LayerNorm 放在残差分支的前面而不是后面 —— 这是深层 Transformer 训练稳定性的标配做法</li> <li>encoder 输出还要过<strong>一个最终的 LayerNorm</strong></li> </ul> <p><strong>右边红色是 decoder blocks</strong>，每个 block 三件套：self attention → cross attention → MLP。cross attention 就是图中那些从 encoder 横着连过来的箭头 —— decoder 通过它去看音频表示。</p> <p><strong>decoder 的两个和 encoder 不同的地方</strong>：</p> <ul> <li>用 <strong>learned position embeddings</strong>（图右下的圆柱形符号），不是 sinusoidal</li> <li><strong>tied input-output token representations</strong>（Press &amp; Wolf, 2017）—— 输入 embedding 矩阵和输出投影矩阵共享权重，省参数</li> </ul> <p><strong>encoder 和 decoder 的宽度、block 数完全相同。</strong></p> <p><strong>图最右上角</strong>是 <code class="language-plaintext highlighter-rouge">next-token prediction</code>：给定 <code class="language-plaintext highlighter-rouge">SOT EN TRANSCRIBE 0.0 The quick</code>，预测下一个 token <code class="language-plaintext highlighter-rouge">brown</code>。是的，decoder 侧就是个标准的自回归语言模型，只不过它是 audio-conditional 的。</p> <p><strong>tokenizer</strong>：英文模型直接用 GPT-2 那套 byte-level BPE；多语言模型<strong>保持词表大小不变、重新拟合词表</strong>，因为 GPT-2 的 BPE 词表是纯英文的，直接用在其他语言上会导致过度碎片化（一个字被切成好几个 byte token）。</p> <p>这个”重新拟合但不扩容”的决定后面留了个尾巴：论文 3.4 节讨论为什么希伯来语（HE）、泰卢固语（TE）、中文（ZH）、韩语（KO）是显著的负向离群点时，给出的一个候选解释就是 <strong>byte level BPE tokenizer 对这些语言是个糟糕的匹配</strong>。词表大小不变、要塞进 99 种语言，这个 trade-off 是有代价的。</p> <h3 id="33-模型家族">3.3 模型家族</h3> <p><img src="/assets/img/posts/20260728-whisper/table1_p5.png" alt="Whisper 模型家族的架构参数"/></p> <p>五个尺寸，layers / width / heads / 参数量。Tiny 是 4 层 384 宽 6 头 39M，一路到 Large 的 32 层 1280 宽 20 头 1550M。注意这里的 layers 是 encoder 和 decoder <strong>各自</strong>的层数（因为两边同深同宽）。</p> <p><strong>训练细节</strong>（2.4 节）：</p> <ul> <li>数据并行，FP16 + dynamic loss scaling + activation checkpointing</li> <li>AdamW + gradient norm clipping，前 2048 步 warmup 后线性衰减到 0</li> <li>batch size = 256 segments，训 2²⁰ = 1,048,576 步，相当于<strong>在数据集上过 2~3 遍</strong></li> <li><strong>不用任何数据增强和正则化</strong> —— 原文理由：由于只训几个 epoch，过拟合不是大问题，转而依赖如此大数据集内部的多样性来鼓励泛化和鲁棒性</li> </ul> <p>Large V2 是原始版本发布之后追加训的：多训了 2.5 倍的 epoch，并加上了 SpecAugment、Stochastic Depth、BPE Dropout 做正则化。论文里除特别说明外，报告的结果都更新成了这个改进模型。</p> <p><strong>一个有意思的失败案例</strong>（2.4 节末尾）：早期开发中他们发现 Whisper 倾向于给说话人名字编造出<strong>看起来合理但几乎总是错的</strong>猜测。原因是预训练数据里很多转录带说话人姓名，模型就学着去预测它 —— 但这个信息通常无法只从最近 30 秒的音频里推断出来。这是”数据里有什么，模型就学什么”的经典体现，也是后面 hallucination 问题的一个具体来源。</p> <hr/> <h2 id="4-多任务格式全文最精巧的设计">4. 多任务格式：全文最精巧的设计</h2> <h3 id="41-要解决的问题">4.1 要解决的问题</h3> <p>完整的语音识别系统不止”预测说了哪些词”，还包含 VAD、说话人分离（diarization）、逆文本标准化等组件。这些组件通常各自独立处理，导致核心识别模型外面裹着一个相当复杂的系统。</p> <p>Whisper 想让<strong>一个模型干完整条流水线</strong>。但同一段音频上可以做的任务有很多种：转写、翻译、VAD、对齐、语言识别。这是个一对多映射，必须有某种形式的<strong>任务指定（task specification）</strong>。</p> <p>Whisper 的答案：<strong>把所有任务和条件信息都编码成 decoder 的输入 token 序列。</strong> 不加任务专属的 head，不加 adapter，就是 token。</p> <h3 id="42-token-序列长什么样">4.2 token 序列长什么样</h3> <p><img src="/assets/img/posts/20260728-whisper/fig1_format_full.png" alt="多任务训练格式"/></p> <p>这是论文 Figure 1 的下半部分。从最左边那个黑点开始，沿着箭头走，每条路径就是一种任务：</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">PREV</code> + <code class="language-plaintext highlighter-rouge">previous text tokens</code></strong>（可选的前置分支）：以一定概率把当前音频段之前的转录文本加进 decoder 的上下文。图里标注了它的两个用途 ——「Custom vocabulary / prompting」。这就是后来大家用的 <code class="language-plaintext highlighter-rouge">initial_prompt</code> 参数的出处：<strong>你可以往这里塞热词表。</strong></li> <li><strong><code class="language-plaintext highlighter-rouge">START OF TRANSCRIPT</code></strong>（就是 <code class="language-plaintext highlighter-rouge">&lt;|startoftranscript|&gt;</code>）：预测开始</li> <li>接下来是<strong>第一个岔路口</strong>：预测<strong>语言 tag</strong>（训练集里每种语言一个唯一 token，共 99 个，标签来自前面提到的 VoxLingua107 模型），<strong>或者</strong>预测 <code class="language-plaintext highlighter-rouge">NO SPEECH</code>。图中这两个框上各挂了一个红色箭头指出它们的对外能力：语言 tag 那条是「Language identification」，NO SPEECH 那条是「Voice activity detection (VAD)」</li> <li>第二个岔路口：<code class="language-plaintext highlighter-rouge">TRANSCRIBE</code>（图上标注 X → X Transcription）或 <code class="language-plaintext highlighter-rouge">TRANSLATE</code>（X → English Translation）</li> <li>第三个岔路口：要不要时间戳。走上路 = 带时间戳，走 <code class="language-plaintext highlighter-rouge">NO TIMESTAMPS</code> = 纯文本</li> <li>带时间戳的路径（图上方红色大括号标注 Time-aligned transcription）：<code class="language-plaintext highlighter-rouge">begin time</code> → <code class="language-plaintext highlighter-rouge">text tokens</code> → <code class="language-plaintext highlighter-rouge">end time</code> → <code class="language-plaintext highlighter-rouge">begin time</code> → … 交替</li> <li>不带时间戳的路径（图下方红色大括号标注 Text-only transcription，并注明 allows dataset-specific fine-tuning）：一整块 <code class="language-plaintext highlighter-rouge">text tokens</code></li> <li>最后汇到 <code class="language-plaintext highlighter-rouge">EOT</code>（end of transcript）</li> </ul> <p>我把四种任务各自的完整 token 序列写出来，对着看更清楚：</p> <p><img src="/assets/img/posts/20260728-whisper/diagram_token_format.png" alt="四种任务的 token 序列"/></p> <p><strong>时间戳 token 的量化规则</strong>：时间是相对当前音频段的，<strong>量化到最近的 20 毫秒</strong>（这匹配 Whisper 模型的原生时间分辨率 —— 10ms stride 经过 stride-2 卷积后就是 20ms），词表里为每一个这样的时刻加一个 token。start time token 在每段 caption 文本之前预测，end time token 在之后。</p> <p><strong>一个边界情况的处理很讲究</strong>：当最后一个转录片段只有一部分落在当前 30 秒音频块内时，时间戳模式下<strong>只预测它的 start time token</strong>，用来指示后续解码应该在与那个时刻对齐的音频窗口上进行；非时间戳模式下则直接把这段音频截掉。这一条就是长音频滑窗解码的地基。</p> <h3 id="43-这个设计为什么聪明">4.3 这个设计为什么聪明</h3> <p>我认为有三个点值得单独拎出来：</p> <p><strong>第一，VAD 和语言识别是白捡的。</strong> 它们不是额外训练的任务，而是”语言 tag 这个位置的分类结果”的两种读法。nospeech 和 99 个语言 tag 抢的是<strong>同一个 slot</strong> —— 所以你想做 VAD，不需要跑一个额外的模型，只要取这个位置 softmax 里 <code class="language-plaintext highlighter-rouge">&lt;|nospeech|&gt;</code> 的概率就行。第 6 节会看到官方推荐的 VAD 判据就是这么用的。</p> <p><strong>第二，「要不要时间戳」是可控的。</strong> 这不只是输出格式的开关，它让同一个模型能服务两种下游需求：需要字幕对齐的场景开时间戳，纯转写的场景关掉省 token。论文特别在图上标了「allows dataset-specific fine-tuning」—— 因为学术数据集的 label 通常没有时间戳。</p> <p><strong>第三，PREV 机制是一个可编程接口。</strong> 论文的动机写的是「希望它学会利用更长的文本上下文来消解有歧义的音频」，但实际上它同时给了用户一个注入先验的口子。</p> <p><strong>训练时的 loss 有一个关键细节</strong>（论文 2.3 节最后）：<strong>只在当前音频段的 token 上算 loss，把之前上下文的部分 mask 掉。</strong> 这个设计是必要的 —— 否则模型会把一部分容量花在”复述我已经知道的上文”上，而那个能力对推理毫无用处。</p> <hr/> <h2 id="5-实验结果数字与它们的读法">5. 实验结果：数字与它们的读法</h2> <h3 id="51-effective-robustness这篇论文最重要的评测概念">5.1 Effective robustness：这篇论文最重要的评测概念</h3> <p>先说结论：Whisper 在 LibriSpeech test-clean 上的 zero-shot WER 是 <strong>2.5</strong>，而当时的 SOTA 是 <strong>1.4</strong> —— <strong>Whisper 输了</strong>，还输得不算少。</p> <p>但论文的立论是这个比较本身就有问题。它引入 Taori et al. (2020) 的两个概念：</p> <ul> <li><strong>overall robustness（整体鲁棒性）</strong>：在众多分布/数据集上的平均表现</li> <li><strong>effective robustness（有效鲁棒性）</strong>：在<strong>参考数据集</strong>（通常是分布内）上的表现与<strong>一个或多个分布外数据集</strong>上的表现之间的差距</li> </ul> <p>一个 effective robustness 高的模型，在分布外数据集上的表现<strong>好于由它在参考数据集上的表现所预期的水平</strong>，也就是逼近「在所有数据集上表现相同」这个理想。他们用 LibriSpeech 做参考数据集，用 12 个学术 ASR 数据集研究分布外行为。</p> <p><img src="/assets/img/posts/20260728-whisper/fig2_full.png" alt="zero-shot Whisper 逼近人类的鲁棒性前沿"/></p> <p>这张图我认为是全文最值得盯着看的一张。逐个元素解释：</p> <ul> <li><strong>x 轴</strong>：LibriSpeech dev-clean 上的 WER（分布内表现，越左越好）</li> <li><strong>y 轴</strong>：Common Voice / CHiME-6 / TED-LIUM 三个数据集上的平均 WER（分布外表现）</li> <li><strong>黑色虚线</strong> <code class="language-plaintext highlighter-rouge">y = x</code>：理想鲁棒性 —— 分布内外表现一致</li> <li><strong>蓝色点和蓝色回归线</strong>：有监督的 LibriSpeech 模型</li> <li><strong>紫色点和紫色回归线</strong>：zero-shot Whisper 模型（五个尺寸各一个点）</li> <li><strong>橙色点带误差棒</strong>：一个人类（Alec）的 zero-shot 表现，误差棒是 95% 置信区间</li> </ul> <p>关键在于<strong>看斜率而不是看点的位置</strong>。蓝色线又高又陡：LibriSpeech 上从 1.5 走到 3.5，分布外 WER 从 24 飙到 46。紫色线又低又平：LibriSpeech 上从 2.6 走到 7.8，分布外 WER 只从 12 爬到 26.5。</p> <p>最有说服力的地方在 x ≈ 2.6 附近：那里蓝色点的 y 值约 27，紫色点的 y 值约 12。<strong>同样的 LibriSpeech 成绩，分布外错误率差 2.25 倍。</strong></p> <p>而橙色那个人类点，落在紫色线上。论文的措辞是：zero-shot Whisper 模型的估计鲁棒性前沿<strong>包含了这位特定人类的 95% 置信区间</strong>。</p> <p><img src="/assets/img/posts/20260728-whisper/table2_p6.png" alt="effective robustness 的逐数据集对比"/></p> <p>这张表把上图具象化成数字。wav2vec 2.0 Large（no LM）vs Whisper Large V2，RER 是相对错误率下降：</p> <ul> <li><strong>LibriSpeech Clean：两边都是 2.7，RER = 0.0</strong> —— 参考数据集上打成平手</li> <li>Artie：24.5 → 6.2，RER <strong>74.7%</strong></li> <li>Common Voice：29.9 → 9.0，RER 69.9%</li> <li>Fleurs En：14.6 → 4.4，RER 69.9%</li> <li>CHiME6：65.8 → 25.5，RER 61.2%</li> <li>AMI SDM1：67.6 → 36.4，RER 46.2%（最小的一档，也是绝对 WER 最高的一档 —— 远场单麦克风会议录音，公认最难）</li> <li><strong>平均：29.3 → 12.8，RER 55.2%</strong></li> </ul> <p>这张表的结构就是论证本身：<strong>在参考集上打成平手（0.0%），在其他所有 12 个集上平均少错 55.2%。</strong> 这个差距不可能用”模型更好”解释，只能用”训练分布更宽”解释。</p> <p>论文由此提出一个方法论建议：</p> <blockquote> <p>这一发现建议强调 zero-shot 和分布外评测，特别是在试图与人类表现比较的时候，以避免由于误导性的比较而夸大机器学习系统的能力。</p> </blockquote> <h3 id="52-多语言数据量决定一切">5.2 多语言：数据量决定一切</h3> <p><img src="/assets/img/posts/20260728-whisper/table3_full.png" alt="多语言语音识别对比"/></p> <p>MLS（Multilingual LibriSpeech）上 Whisper zero-shot 拿到 <strong>7.3</strong>，好过 XLS-R (1B) 的 10.9 和 mSLAM-CTC (2B) 的 9.7。但 VoxPopuli 上 Whisper 是 <strong>13.6</strong>，输给几乎所有对手（Maestro 8.1、mSLAM 9.1、XLS-R 10.6）。</p> <p>论文自己给的解释很诚实：VoxPopuli 上表现差可能是因为<strong>其他模型把这个分布作为无监督预训练数据的主要来源</strong>，并且该数据集有显著更多的监督数据可供 fine-tune。数据支撑：MLS 每种语言 10 小时训练数据，而 VoxPopuli 的每语言平均训练数据大约<strong>高 10 倍</strong>。</p> <p>而且这两个 benchmark 都很窄 —— 只覆盖 15 种语言，几乎全在印欧语系，大部分是高资源语言。所以他们转到 Fleurs（102 种语言）做更广的分析：</p> <p><img src="/assets/img/posts/20260728-whisper/figure3_p7.png" alt="训练数据量与识别 WER 的关系"/></p> <ul> <li><strong>x 轴</strong>：该语言的转录音频小时数（对数轴，从 0.1 小时到 1M 小时）</li> <li><strong>y 轴</strong>：Fleurs 上的 WER（对数轴）</li> <li>每个点是一种语言，标着两字母语言代码</li> <li>蓝线是线性拟合，浅蓝带是置信区间</li> <li>左下角标着 <strong>r² = 0.83</strong></li> </ul> <p><strong>r² = 0.83 是个很强的相关。</strong> 论文从拟合的回归系数推出一个好记的经验规律：<strong>训练数据每增加 16 倍，WER 减半。</strong></p> <p>再看离群点，这是这张图最有信息量的部分。右侧偏上的 <strong>ZH（中文）</strong>：训练数据超过 20,000 小时（在图上位于 x 轴很右边），但 WER 还在 15 左右，明显在拟合线上方。<strong>KO（韩语）</strong> 同样。还有 <strong>HE（希伯来语）</strong>、<strong>TE（泰卢固语）</strong>。论文给的三个候选解释：</p> <ol> <li>由于语言距离远，<strong>缺少迁移</strong></li> <li>我们的 byte level BPE tokenizer 对这些语言是个糟糕的匹配</li> <li>数据质量的差异</li> </ol> <p>第 2 条我前面提过 —— 词表大小不变塞 99 种语言的代价，在这里显形了。对做中文 ASR 的人来说这一条尤其值得注意：<strong>Whisper 在中文上表现不及其数据量应有的水平，一部分原因是 tokenizer 层面的，不是数据量能补回来的。</strong></p> <h3 id="53-翻译同一套分析r-却掉到-024">5.3 翻译：同一套分析，r² 却掉到 0.24</h3> <p><img src="/assets/img/posts/20260728-whisper/table4_full.png" alt="X→en 语音翻译"/></p> <p>CoVoST2 的 X→en 子集，Whisper zero-shot 总分 <strong>29.1 BLEU</strong>，是新的 SOTA（对手 Maestro 25.2、mSLAM-CTC 24.8、XLS-R 22.1），而且<strong>没用 CoVoST2 的任何训练数据</strong>。</p> <p>按资源分档看更有意思：</p> <ul> <li><strong>High（高资源）：36.2</strong>，输给 Maestro 的 38.2</li> <li><strong>Mid：32.6</strong>，最优</li> <li><strong>Low（低资源）：25.2</strong>，最优，比 mSLAM 高出 <strong>6.7 BLEU</strong></li> </ul> <p>论文的归因：他们预训练数据集里有 68,000 小时的 X→en 翻译数据（针对这些语言），虽然噪声大，但远多于 CoVoST2 里 861 小时的训练数据。<strong>低资源档赢得最多、高资源档反而输</strong>，这个模式和”数据量驱动”的解释完全一致。</p> <p><img src="/assets/img/posts/20260728-whisper/figure4_p7.png" alt="训练数据量与翻译 BLEU 的关系"/></p> <p>同样的分析换成翻译，<strong>r² 掉到了 0.24</strong>（识别是 0.83）。同样的方法、同样的模型，相关性弱这么多，说明翻译的表现被数据量之外的因素支配得更多。</p> <p>论文怀疑这部分是由于<strong>翻译训练数据的噪声更大</strong>，源于语言识别环节的错误，然后举了一个我读完印象最深的例子：</p> <blockquote> <p>威尔士语（CY）是一个显著的离群点，表现远差于预期 —— 在号称有 <strong>9,000 小时</strong>翻译数据的情况下只有 <strong>13 BLEU</strong>。这个巨大的威尔士语翻译数据量令人惊讶，在全部翻译数据里排到第 4，超过了法语、西班牙语、俄语这些世界上使用人数最多的语言。检查发现，绝大多数所谓的威尔士语翻译数据实际上是<strong>带英文字幕的英文音频</strong>，被语言识别系统误分类成了威尔士语，于是按照他们的数据集构建规则，被当成翻译训练数据而不是转写训练数据收进来了。</p> </blockquote> <p>这个 case 值得所有做数据流水线的人抄在本子上。<strong>语言识别的一个错误分类，会顺着自动化规则被放大成 9000 小时的错误标签。</strong> 而且它在总量统计里看起来完全正常 —— 威尔士语排第 4，只有当你觉得”这不对，威尔士语凭什么排在法语前面”的时候才会去查。第 2.2 节那个”训完初版模型再洗数据”的步骤没能抓到它，因为这批数据的音频和文本其实是<strong>匹配</strong>的（都是英文），错的只是任务标签。</p> <h3 id="54-语言识别一个被数据集设定坑了的结果">5.4 语言识别：一个被数据集设定坑了的结果</h3> <p><img src="/assets/img/posts/20260728-whisper/table5_full.png" alt="语言识别准确率"/></p> <p>Fleurs 上 Whisper zero-shot 是 <strong>64.5%</strong>，输给有监督 SOTA 的 77.7%（mSLAM-CTC 2B）和 71.4%（w2v-bert-51）。论文承认不 competitive，但同时指出一个口径问题：</p> <p>Whisper 数据集里 Fleurs 的 <strong>102 种语言中有 20 种完全没有训练数据</strong>，所以准确率的<strong>上界只有 80.4%</strong>。在 82 种重叠语言上，最好的 Whisper 模型达到 <strong>80.3%</strong> 准确率 —— 也就是说在它见过的语言里，几乎打满了上界。</p> <p>这是一个读表时容易踩的坑：64.5 这个数字里，有 19.6 个百分点是结构性不可能的。</p> <h3 id="55-噪声鲁棒性">5.5 噪声鲁棒性</h3> <p><img src="/assets/img/posts/20260728-whisper/fig5_full.png" alt="不同 SNR 下的 WER"/></p> <p>两个子图：左边加<strong>白噪声</strong>，右边加<strong>酒吧噪声</strong>（pub noise，来自 Audio Degradation Toolbox，模拟嘈杂餐厅或酒吧里的环境噪声和含混交谈）。x 轴是信噪比 SNR，<strong>从左往右递减</strong>（40dB 干净 → −10dB 极噪）；y 轴是 LibriSpeech test-clean 的 WER，对数轴。</p> <ul> <li><strong>红色星星折线</strong>是 Whisper</li> <li>绿色圆点是 NVIDIA STT 系列</li> <li>蓝色三角是 wav2vec2-large-960h-lv60-self</li> <li>其余细线是另外十来个 LibriSpeech 训练的模型</li> </ul> <p>读法：在图的<strong>左侧</strong>（低噪声，40dB），红星在 2.5 左右，被绿点（1.5）和蓝三角（1.9）压在下面 —— 干净音频上 Whisper 不是最优的，这不意外，因为那些模型主要在 LibriSpeech 上训练。</p> <p>但往右走，所有模型都快速退化，而<strong>红星的曲线更平</strong>。论文的结论：在 SNR 低于 <strong>10 dB</strong> 的加性酒吧噪声下，所有模型的表现都比 Whisper 模型更差。低噪声下第二好的模型（蓝色三角，只在 LibriSpeech 上 fine-tune 过）退化得<strong>更快</strong>。</p> <p>这张图和 5.1 节那张 effective robustness 图讲的是同一件事的两个侧面：<strong>Whisper 的优势不在最优点上，在斜率上。</strong></p> <h3 id="56-长音频转写">5.6 长音频转写</h3> <p>Whisper 在 30 秒块上训练，一次吞不下更长的音频。他们的做法是<strong>连续转写 30 秒段，并按模型预测的时间戳来移动窗口</strong>。论文强调：</p> <blockquote> <p>我们观察到，<strong>基于模型预测的重复性和 log 概率来做 beam search 和温度调度是至关重要的</strong>，才能可靠地转写长音频。</p> </blockquote> <p>七个长音频数据集，长度从几分钟到几小时：TED-LIUM3（拼接成完整 TED talk）、Meanwhile（Stephen Colbert 秀里术语密集的片段）、Rev16 和 Kincaid46（在线博客里被用作 ASR benchmark 的视频/播客）、Earnings-21/22（财报电话会议录音）、CORAAL（地区非裔美国人语言语料库的完整访谈）。</p> <p><img src="/assets/img/posts/20260728-whisper/fig6_full.png" alt="长音频转写与商业系统对比"/></p> <p>箱线图，每个数据集下 6 个箱子：Whisper（蓝）、Company A~D（4 家商业 ASR 服务）、NVIDIA STT CTC large（棕，开源里最好的）。箱子是逐样本 WER 的四分位数，箱上标注的数字是<strong>该数据集的聚合 WER</strong>。</p> <p>逐个数据集读 Whisper 的数字和它的排位：</p> <ul> <li>TED-LIUM3：<strong>3.52</strong>，全场最低（对手 3.61 / 3.78 / 4.81 / 4.02 / 4.00）</li> <li>Meanwhile：<strong>5.09</strong>，第二名 8.23，<strong>领先 38%</strong> —— 论文特别点出这个数据集”术语密集（heavy with uncommon words）”</li> <li>Kincaid46：<strong>8.80</strong>，第二名 9.94</li> <li>Rev16：<strong>11.3</strong>，Company B 是 10.9，<strong>Whisper 第二</strong></li> <li>Earnings-21：<strong>9.72</strong>，Company A 是 8.94、B 是 9.27，<strong>Whisper 第三</strong></li> <li>Earnings-22：<strong>12.6</strong>，最低</li> <li>CORAAL：<strong>19.6</strong>，Company B 是 14.6，<strong>Whisper 第二</strong>，且和最差的 30.0 差距巨大</li> </ul> <p>所以准确的说法是：Whisper 在多数数据集上优于被比较的模型，<strong>在所有数据集上优于最好的开源模型（NVIDIA STT）</strong>，多数情况下也优于商业 ASR 系统。不是全胜。</p> <p>论文还主动提了一个 caveat，我觉得这一句体现了作者的诚实：</p> <blockquote> <p>我们注意到某些商业 ASR 系统有可能已经在这些公开可用的数据集上训练过，因此这些结果可能没有准确反映系统的相对鲁棒性。</p> </blockquote> <h3 id="57-和人类比">5.7 和人类比</h3> <p><img src="/assets/img/posts/20260728-whisper/figure7_p10.png" alt="Whisper 与专业人工转写对比"/></p> <p>Kincaid46 里选 25 段录音，覆盖念稿和即兴的广播、电话和 VoIP 通话、会议等多种录制条件。横轴 10 个箱子：Whisper、A~D（同上的 4 家商业 ASR）、<strong>E（一家计算机辅助的人工转写服务）</strong>、<strong>F~I（4 家纯人工转写服务）</strong>。图下方的括号标出了分组：左边 5 个是 ASR，E 单独标为 computer-assisted，右边 4 个是 human transcription。</p> <p>聚合 WER：</p> <ul> <li>Whisper <strong>8.81</strong></li> <li>商业 ASR：A 9.66、B 9.74、C 10.9、D 12.2 —— <strong>全部落后于 Whisper</strong></li> <li>E（计算机辅助人工）<strong>7.61</strong> —— 全场最低，比 Whisper 好 <strong>1.15 个百分点</strong></li> <li>F~I（纯人工）8.14、8.65、8.96、10.5</li> </ul> <p>纯人工那四家里，<strong>有两家（8.96、10.5）比 Whisper 差</strong>。论文的措辞很克制：纯人类的表现只比 Whisper 好<strong>不到一个百分点</strong>（a fraction of a percentage point）。</p> <p>这里有个细节值得注意：为什么要拿人类来比？因为<strong>由于含混或不清晰的语音以及标注错误，每个数据集都有不同程度的不可约错误（irreducible error）</strong>，光看 ASR 系统之间的 WER，很难判断每个数据集还剩多少提升空间。人类的成绩就是那把标尺。</p> <hr/> <h2 id="6-消融实验哪些东西真的有用">6. 消融实验：哪些东西真的有用</h2> <h3 id="61-模型规模">6.1 模型规模</h3> <p><img src="/assets/img/posts/20260728-whisper/fig8_full.png" alt="zero-shot 表现随模型规模的变化"/></p> <p>四个子图，x 轴都是模型参数量（38M → 1549M），淡蓝细线是单个数据集/语言，粗蓝线是平均，橙色虚线单独标出 Large V2（因为它包含了小模型没有的若干改动）：</p> <ol> <li><strong>English Speech Recognition</strong>：12 个数据集平均 WER，18.5 → 9.5，Large V2 到 ~8.8</li> <li><strong>Multilingual Speech Recognition (Fleurs)</strong>：67 种语言平均 WER，79 → 39，Large V2 到 ~33</li> <li><strong>X→En Translation (CoVoST2)</strong>：21 种语言平均 BLEU，2.5 → 25.5，Large V2 到 ~29</li> <li><strong>Language Identification (Fleurs)</strong>：102 种语言准确率，45 → 61，Large V2 到 ~64.5</li> </ol> <p>论文的结论：随规模增大，表现在所有任务上都可靠改善，<strong>除了英文语音识别在从 768M 到 1.5B 时呈现出收益递减</strong>。而 Whisper 的鲁棒性随规模持续提升 —— 这否证了”在弱监督数据上训练，模型会饱和在数据集固有质量水平上”这个担忧，也否证了”随算力增加模型会学去利用数据集特质”的担忧。</p> <p>顺便说一句，第 1 个子图那个”英文识别在 768M 之后趋平”的现象，论文猜测原因是<strong>接近人类水平表现时的饱和效应</strong> —— 呼应了 5.7 节的结果。</p> <h3 id="62-数据集规模这张表适合贴在墙上">6.2 数据集规模：这张表适合贴在墙上</h3> <p><img src="/assets/img/posts/20260728-whisper/table6_p11.png" alt="数据集规模的影响"/></p> <p>从 3,405 小时（全量的 1/200）到 681,070 小时（全量），每档翻倍：</p> <table> <thead> <tr> <th>数据量(h)</th> <th>英文 WER↓</th> <th>多语言 WER↓</th> <th>X→En BLEU↑</th> </tr> </thead> <tbody> <tr> <td>3,405</td> <td>30.5</td> <td>92.4</td> <td>0.2</td> </tr> <tr> <td>6,811</td> <td>19.6</td> <td>72.7</td> <td>1.7</td> </tr> <tr> <td>13,621</td> <td>14.4</td> <td>56.6</td> <td>7.9</td> </tr> <tr> <td>27,243</td> <td>12.3</td> <td>45.0</td> <td>13.9</td> </tr> <tr> <td>54,486</td> <td>10.9</td> <td>36.4</td> <td>19.2</td> </tr> <tr> <td><strong>681,070</strong></td> <td><strong>9.9</strong></td> <td><strong>29.2</strong></td> <td><strong>24.8</strong></td> </tr> </tbody> </table> <p>三列的形状完全不同，值得分开读：</p> <p><strong>英文 WER</strong>：3,000 → 13,000 小时<strong>快速改善</strong>（30.5 → 14.4），13,000 → 54,000 之间<strong>明显放缓</strong>（14.4 → 10.9），从 54,000 到全量（<strong>又是 12.5 倍</strong>）只再降 <strong>1 个点</strong>。</p> <p><strong>多语言 WER</strong>：到 54,000 小时都遵循幂律趋势，之后偏离，从 36.4 只再改善 7 个点到 29.2。</p> <p><strong>X→En BLEU</strong>：在 7,000 小时或更少的音频上训练时，翻译表现<strong>几乎是零</strong>（0.2 → 1.7）—— 存在一个明显的能力涌现门槛。然后到 54,000 小时之前大致呈<strong>对数线性</strong>改善，之后也进入递减。</p> <p>论文对”全量数据收益递减”给了两个方向截然不同的解读，并明确说无法区分：</p> <ol> <li><strong>数据没吃完</strong>：当前最好的 Whisper 模型相对数据集规模而言是<strong>训练不足（under-trained）</strong>的，用更长训练和更大模型可以进一步改善</li> <li><strong>数据吃到底了</strong>：我们正在接近语音识别从数据集规模 scaling 中能获得的性能改进的终点</li> </ol> <p>原文：「需要进一步分析来刻画语音识别的 scaling law，以便在这些解释之间做决定。」这个问题据我所知到现在也没有被干净地回答。</p> <h3 id="63-多任务多语言迁移小模型有害大模型有益">6.3 多任务/多语言迁移：小模型有害，大模型有益</h3> <p><img src="/assets/img/posts/20260728-whisper/fig9_full.png" alt="多任务多语言迁移随规模的变化"/></p> <ul> <li><strong>x 轴</strong>：花在英文语音识别上的训练 FLOPs（对数轴，10e19 → 10e22）</li> <li><strong>y 轴</strong>：11 个英文语音识别数据集的平均 WER</li> <li><strong>蓝色点/线</strong>：English Only 模型</li> <li><strong>橙色点/线</strong>：Multilingual and Multitask 模型</li> <li>阴影带是 95% bootstrap 置信区间</li> </ul> <p>关键是<strong>两条线交叉了</strong>。在图的左侧（低算力），橙线在蓝线<strong>上方</strong> —— 联合训练更差。往右走，两条线在 10e20 附近交叉，之后橙线在蓝线<strong>下方</strong> —— 联合训练更好，而且橙线<strong>斜率更陡</strong>。</p> <p>这里有个方法论细节必须说清楚，否则这张图会被读错。论文做了一个校正：</p> <blockquote> <p>我们对花在英文语音识别任务上的 FLOPs 数量做了调整，因为在联合训练设定下<strong>只有 65% 的算力花在这个任务上</strong>；否则分析会被”任务上的训练不足”所混淆，相比同尺寸的纯英文模型不公平。</p> </blockquote> <p>也就是说 x 轴不是”总算力”，是”折算到英文识别上的有效算力”。没有这个校正，联合模型会因为分心而看起来更差，那就说不清是”负迁移”还是”算力被分走了”。</p> <p>结论：<strong>对于用中等算力训练的小模型，确实存在跨任务和跨语言的负迁移</strong> —— 联合模型表现不如同等算力的纯英文模型。<strong>但联合模型 scale 得更好</strong>，在他们最大的实验里超过了纯英文对手，展示出正迁移。而且在最大规模下，即使<strong>不校正</strong>每任务算力，联合模型也略微超过纯英文模型。</p> <p>这个结论对做多语言/多任务系统的人很实用：<strong>小规模实验上观察到的负迁移，不能直接外推到大规模。</strong> 如果 Whisper 团队只在 1e19 FLOPs 规模上做消融，他们会得到”多任务联合训练有害”的结论然后放弃这条路。</p> <h3 id="64-文本标准化这一节被引用得太少了">6.4 文本标准化：这一节被引用得太少了</h3> <p>我在开头说这篇论文有第二层价值，就是指这里。</p> <p>由于他们的文本标准化器是<strong>和 Whisper 一起开发</strong>的，用来折掉无关紧要的词形错误，存在一个风险：<strong>这个标准化器可能过拟合到了 Whisper 的特性上，而不是在处理转写中的一般性差异。</strong> 为了检查，他们把自己的标准化器和 FairSpeech 项目独立开发的一个做了对比。</p> <p><img src="/assets/img/posts/20260728-whisper/fig10_full.png" alt="两个标准化器的效果对比"/></p> <ul> <li><strong>y 轴</strong>：13 个数据集，按下方效果差异排序</li> <li><strong>x 轴</strong>：相对于 FairSpeech 标准化器的相对 WER 降幅（%），注意<strong>从左往右递减</strong>，右边红色竖线是 0（无差异）</li> <li><strong>蓝色箱</strong>：开源模型</li> <li><strong>橙色箱</strong>：Whisper 模型</li> </ul> <p>读法：如果某个数据集上橙箱明显比蓝箱更靠左，说明他们的标准化器给 Whisper 的优惠比给别人的多，那就是过拟合的证据。</p> <p>结果：<strong>大多数数据集上两个箱子重叠得很好</strong>（CORAAL、CommonVoice、AMI-SDM1、Artie、Fleurs 等基本都贴在 0 附近）。但有三个数据集橙箱明显更靠左 —— <strong>WSJ、CallHome、Switchboard</strong>，他们的标准化器把 Whisper 模型的 WER 降低得显著更多，最大接近 <strong>50%</strong>。</p> <p>论文诚实地追查了原因，并且解释得很具体：</p> <blockquote> <p>降幅差异可以追溯到 ground truth 使用的不同格式，以及两个标准化器惩罚它们的方式。例如在 CallHome 和 Switchboard 上，我们的标准化器<strong>没有惩罚常见英文缩写的差异</strong>，比如 “you’re” 对 “you are”；而在 WSJ 上，我们的标准化器<strong>标准化了数值和货币表达的书面形式与口语形式</strong>，比如 “sixty-eight million dollars” 对 “$68 million”。</p> </blockquote> <p>附录 C 里给出了完整的 12 条英文标准化规则，我列一下（这个清单可以直接当工程 checklist 用）：</p> <ol> <li>去掉方括号 <code class="language-plaintext highlighter-rouge">[ ]</code> 之间的短语</li> <li>去掉圆括号 <code class="language-plaintext highlighter-rouge">( )</code> 之间的短语</li> <li>去掉这些词：hmm, mm, mhm, mmm, uh, um</li> <li>去掉撇号 <code class="language-plaintext highlighter-rouge">'</code> 前的空白字符</li> <li>把英文的标准或非正式<strong>缩写形式还原</strong>成原形</li> <li>去掉数字之间的逗号</li> <li>去掉后面不跟数字的句点</li> <li>去掉符号和音调符号（Unicode category 以 M/S/P 开头的字符），但保留句点、百分号和货币符号（下一步要用）</li> <li><strong>检测数字和货币的数值表达，替换成阿拉伯数字形式</strong> —— 例如 “Ten thousand dollars” → “$10000”</li> <li><strong>英式拼写转美式拼写</strong></li> <li>去掉不属于任何数值表达的剩余符号</li> <li>连续空白字符压成一个空格</li> </ol> <p>非英文语言，他们承认缺乏为所有语言构建这类标准化器的语言学知识，所以只做基础处理：去括号内容、把 M/S/P 类字符替换成空格（在 NFKC 归一化的字符串上判定）、转小写、压缩空白。<strong>另外，对不用空格分词的语言（中文、日文、泰文、老挝文、缅甸文），在每个字符之间插入一个空格，实际上等于在测字错误率（CER）。</strong></p> <p>最后论文自己给了一个很到位的声明：</p> <blockquote> <p>我们注意到上述方案是不完美的，有时会产生意外和非预期的输出。我们<strong>不主张</strong>上述过程得到的文本格式在任何意义上更”正确”。相反，上述流程的设计目标是更好地区分<strong>无害的措辞差异</strong>和<strong>真正的转写错误</strong>。</p> </blockquote> <p>这段话我觉得应该刻在每个 ASR 评测报告的第一页。评测口径不是”正确”与否的问题，是”你想测什么”的问题 —— 而这个选择会改变排名。我自己做混说评测时踩过这个坑：同一批模型，开不开填充词过滤（嗯/呃/那个），排名会变。</p> <h3 id="65-长音频解码的启发式规则">6.5 长音频解码的启发式规则</h3> <p>Whisper 的长音频转写依赖时间戳 token 的准确预测来决定把 30 秒窗口往后移多少，<strong>任一窗口内转写不准都可能负面影响后续窗口的转写</strong>。所以他们攒了一套启发式规则专门避开长音频的失败模式：</p> <p><img src="/assets/img/posts/20260728-whisper/table7_full.png" alt="长音频解码启发式的逐项消融"/></p> <p>七个数据集 + 平均列，从贪心解码开始逐项累加：</p> <table> <thead> <tr> <th>配置</th> <th>平均 WER</th> </tr> </thead> <tbody> <tr> <td>Greedy decoding only</td> <td>11.0</td> </tr> <tr> <td>+ Beam search</td> <td>10.6</td> </tr> <tr> <td>+ Temperature fallback</td> <td>10.6</td> </tr> <tr> <td>+ Voice activity detection</td> <td>10.2</td> </tr> <tr> <td>+ Previous text conditioning</td> <td>10.0</td> </tr> <tr> <td>+ Initial timestamp constraint</td> <td>10.0</td> </tr> </tbody> </table> <p>每条规则的具体参数（4.5 节）：</p> <p><img src="/assets/img/posts/20260728-whisper/diagram_decode_loop.png" alt="解码启发式的控制流"/></p> <p>对着图走一遍：</p> <ol> <li><strong>Beam search，5 个 beam，用 log 概率作为打分函数</strong> —— 目的是减少贪心解码中更频繁出现的重复循环</li> <li><strong>温度 fallback</strong>：从 T = 0 开始（即总是选概率最高的 token），当<strong>两个信号任一命中</strong>时把温度增加 0.2，最高到 1.0： <ul> <li>生成 token 的<strong>平均 log 概率低于 −1</strong>（模型自己对结果没信心）</li> <li>生成文本的 <strong>gzip 压缩率高于 2.4</strong>（文本重复到了能被高效压缩的程度 —— 这是个很聪明的循环检测器，不用写规则去匹配重复模式，直接用压缩率当”重复度”的代理指标）</li> </ul> </li> <li><strong>前文条件</strong>：把前一个窗口的转写文本作为 prev 提供给模型，<strong>但只在当前温度低于 0.5 时才做</strong>。这个条件很关键 —— 高温意味着当前窗口的输出本身就不可信，把它传下去会污染后续窗口，错误会级联</li> <li><strong>VAD</strong>：他们发现<strong>只用 <code class="language-plaintext highlighter-rouge">&lt;|nospeech|&gt;</code> token 的概率不足以</strong>区分一个段落有没有语音，把 nospeech 概率阈值 <strong>0.6</strong> 和平均 log 概率阈值 <strong>−1</strong> 组合起来，才使 Whisper 的 VAD 更可靠</li> <li><strong>初始时间戳约束</strong>：为了避免模型忽略输入前几个词的失败模式，把初始时间戳 token 约束在 <strong>0.0 到 1.0 秒之间</strong></li> </ol> <p>从表看，Beam search 贡献 0.4，Temperature fallback 在平均上是 0.0（但看单列，它在 Meanwhile 上从 5.71 没变、在 Kincaid46 上 9.42 没变 —— 它的作用是<strong>防灾</strong>而不是提分，只在触发失败模式的样本上起作用，会被平均掉），VAD 贡献 0.4，前文条件贡献 0.2，初始时间戳约束在平均上 0.0。总共 11.0 → 10.0。</p> <p>论文对这套东西的定位说得很直白：</p> <blockquote> <p>增加这些干预中的每一个通常都会整体降低 WER，但<strong>在数据集上并不均匀</strong>。这些启发式规则作为一个针对模型噪声预测的 <strong>workaround</strong>，需要更多研究来进一步改善长音频解码的可靠性。</p> </blockquote> <p><strong>用了 workaround 这个词。</strong> 这是我最欣赏这篇论文的地方之一 —— 没有把一堆调出来的阈值包装成”我们提出了一个新颖的解码框架”。</p> <hr/> <h2 id="7-局限论文自己承认的问题">7. 局限：论文自己承认的问题</h2> <p>第 6 节 Limitations 里，我觉得最值得关注的是解码策略这一段：</p> <blockquote> <p>随着我们 scale Whisper，我们观察到更大的模型在减少<strong>感知类错误</strong>（比如混淆发音相似的词）上取得了稳定可靠的进展。许多剩余的错误，特别是在长音频转写中，性质上似乎更顽固，且<strong>明显不是人类式的/感知性的</strong>。它们是 seq2seq 模型、语言模型、以及文本-音频对齐的失败模式的组合，包括<strong>卡在重复循环里</strong>、<strong>不转写音频段落开头或结尾的前几个词</strong>、或者<strong>完全的幻觉</strong> —— 模型输出一段与实际音频完全无关的转写。</p> </blockquote> <p>这段话把错误分成了两类，这个分类我认为比任何单一指标都有用：</p> <ul> <li><strong>感知类错误</strong>（同音词混淆）：scaling 能解决</li> <li><strong>生成类错误</strong>（重复循环、掉头尾、幻觉）：scaling 不解决，因为它们来自 seq2seq/LM 侧而不是听觉侧</li> </ul> <p>这解释了为什么第 6.5 节那套启发式是必需的 —— 它们全都是在治第二类病。论文猜测在高质量监督数据集上 fine-tune、或者用强化学习更直接优化解码表现，可能进一步减少这些错误。</p> <p>其他几条：</p> <ul> <li><strong>低资源语言数据太少</strong>：数据集因为收集流水线的偏向而<strong>非常英语中心</strong>，大多数语言不到 1000 小时。而 Figure 3 的 r² = 0.83 说明这条路很明确 —— 有针对性地给这些稀有语言加数据，即使总数据量只小幅增加，也能大幅改善平均识别表现</li> <li><strong>没研究 fine-tune</strong>：本文只研究 zero-shot 迁移，但对很多有高质量监督数据的领域，fine-tune 大概能进一步改善</li> <li><strong>不清楚鲁棒性来自 encoder 还是 decoder</strong>：他们怀疑 Whisper 的鲁棒性部分来自它强大的 decoder（一个音频条件语言模型），但目前不清楚收益来自训练 encoder、decoder、还是两者。可以通过训练一个无 decoder 的 CTC 模型来消融，或者研究把 wav2vec 2.0 这类现有 encoder 配上语言模型后表现如何变化</li> <li><strong>没有辅助训练目标</strong>：Whisper 明显偏离了大多数近期 SOTA 系统，因为它缺少无监督预训练或自训练方法。虽然他们没发现这些是达到好表现的必需品，但纳入它们也许能进一步改善结果</li> </ul> <hr/> <h2 id="8-读完之后我自己的几个-take">8. 读完之后我自己的几个 take</h2> <p><strong>第一，这篇论文最硬的贡献是数据流水线，不是模型。</strong> 模型是”现成的 encoder-decoder Transformer”，作者自己都强调是为了避免混淆变量才刻意不创新的。真正难复现的是第 2.2 节那四道闸 —— 尤其是”检测机器生成字幕”和”训完初版模型再按错误率×体积排序洗数据”。这两件事没有论文可以引用，是纯手工活。<strong>这也是为什么 Whisper 好复现架构、难复现效果。</strong></p> <p><strong>第二，effective robustness 这个评测框架的价值超过 Whisper 本身。</strong> 「在参考集上打平，在其他 12 个集上平均少错 55.2%」这种论证结构，比”我在 benchmark X 上刷到了 SOTA”有说服力得多。我做混说评测时借了这个思路：先在共同交集上排名，再单独看每家在难子集上掉多少，两个数字一起看才有意义。</p> <p><strong>第三，Figure 9 那个交叉点是个方法论警告。</strong> 小规模实验上多任务联合训练是<strong>负迁移</strong>，大规模上是<strong>正迁移</strong>。如果只在小规模做消融就下结论，会把一条正确的路线砍掉。这个陷阱在 MoE 领域也有同构的版本 —— 小规模上 sparse 打不过 dense，规模上去了才反过来。<strong>规模是会改变结论的方向，不只是改变数值。</strong></p> <p><strong>第四，威尔士语那 9000 小时是我读到的最好的数据质量案例。</strong> 一个语言识别的误分类，被自动化规则放大成了 9000 小时的错误标签，而且在总量统计里看起来完全正常（排第 4）。<strong>自动化流水线不会犯随机错误，它会犯系统性错误</strong> —— 而系统性错误在聚合统计里是隐形的。唯一发现它的方式是有人觉得”威尔士语凭什么排在法语前面”然后去查了。</p> <p><strong>第五，附录 C 那 12 条应该成为常识。</strong> 做 ASR 评测的人如果不定归一化口径，报出来的 WER/CER 数字基本没有可比性。而口径的选择本身会改变排名 —— 论文自己在 4.4 节展示了同一批模型换个标准化器 WER 能差 50%。<strong>任何一份 ASR 评测报告的第一节都应该是口径声明，不是结论。</strong></p> <p><strong>第六，一个我没想到的细节：中文在 Figure 3 里是负向离群点。</strong> 训练数据量排第一（23,446 小时），但 WER 明显高于拟合线预期。论文给的候选解释里，byte-level BPE tokenizer 对中文是糟糕匹配这一条最值得注意 —— 因为这不是加数据能补的。做中文 ASR 的话，这一条比”Whisper 中文数据最多”重要。</p> <hr/> <p>扯一句题外话。这篇论文的核心逻辑是”用规模换掉工程复杂度”—— ITN 模块被 scaling 换掉了，VAD 和语言识别被 token 格式换掉了。这个思路在别的方向上也在反复出现：手工设计的模块被自动化流程和规模取代。我这几年主要做的 NAS / AutoML 和 LLM 效率优化，本质上也在同一个方向上使劲，只是换了个入口 —— 不是”把模块塞进模型让它自己学”，而是”把设计模块这件事本身自动化掉”。</p> <p>我们把这方面的积累整理成了一本书，《动手学 AutoML：从 NAS 到大语言模型优化实战》。ASR 不在书的覆盖范围内（书讲的是搜索空间、搜索策略、架构评估，以及 LLM 架构自动化、压缩剪枝量化、LLM 驱动的 AutoML 这些），所以这里不硬扯关系 —— 只是如果你读这篇文章时对”哪些设计决定其实可以交给搜索”这个问题起了兴趣，那本书讲的就是这件事。</p> <blockquote> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote> <p>欢迎评论区交流，有讲错的地方也请指出来。</p>]]></content><author><name></name></author><category term="ASR"/><category term="语音识别"/><category term="Whisper"/><category term="论文解读"/><category term="弱监督"/><summary type="html"><![CDATA[106K star 的开源 ASR 老祖宗 | 重读 Whisper 原论文的每一个技术细节]]></summary></entry><entry><title type="html">OSDI’26 | 双路 CPU + 两张 5090，本地跑满血 671B DeepSeek：云端 SLO 是怎么在消费级硬件上达成的 - marsggbo</title><link href="https://marsggbo.github.io/blog/2026/osdi26-cpu-5090-671b-deepseek-slo-marsggbo/" rel="alternate" type="text/html" title="OSDI’26 | 双路 CPU + 两张 5090，本地跑满血 671B DeepSeek：云端 SLO 是怎么在消费级硬件上达成的 - marsggbo"/><published>2026-07-27T03:25:00+00:00</published><updated>2026-07-27T03:25:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/osdi26---cpu---5090-671b-deepseek-slo----marsggbo</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/osdi26-cpu-5090-671b-deepseek-slo-marsggbo/"><![CDATA[]]></content><author><name></name></author><summary type="html"><![CDATA[OSDI&#39;26 | 双路 CPU + 两张 5090，本地跑满血 671B DeepSeek：云端 SLO 是怎么在消费级硬件上达成的 原文：Achieving Cloud-Grade SLOs for Local Mixture-of-Experts Inference through CPU–G]]></summary></entry><entry><title type="html">arXiv’25 | SMoE：expert 不在 GPU 上？别搬了，找个’平替’直接算 - marsggbo</title><link href="https://marsggbo.github.io/blog/2026/arxiv25-smoeexpert-gpu-marsggbo/" rel="alternate" type="text/html" title="arXiv’25 | SMoE：expert 不在 GPU 上？别搬了，找个’平替’直接算 - marsggbo"/><published>2026-07-27T03:23:00+00:00</published><updated>2026-07-27T03:23:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/arxiv25--smoeexpert--gpu----marsggbo</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/arxiv25-smoeexpert-gpu-marsggbo/"><![CDATA[]]></content><author><name></name></author><summary type="html"><![CDATA[arXiv&#39;25 | SMoE：expert 不在 GPU 上？别搬了，找个&quot;平替&quot;直接算 原文：SMoE: An Algorithm-System Co-Design for Pushing MoE to the Edge via Expert Substitution 1. 前言 MoE off]]></summary></entry><entry><title type="html">arXiv’26 | 无损压缩逼近 Shannon 极限：你的 LLM 权重里一半以上的 bit 是白存的 - marsggbo</title><link href="https://marsggbo.github.io/blog/2026/arxiv26-shannon-llm-bit-marsggbo/" rel="alternate" type="text/html" title="arXiv’26 | 无损压缩逼近 Shannon 极限：你的 LLM 权重里一半以上的 bit 是白存的 - marsggbo"/><published>2026-07-27T03:23:00+00:00</published><updated>2026-07-27T03:23:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/arxiv26---shannon--llm--bit----marsggbo</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/arxiv26-shannon-llm-bit-marsggbo/"><![CDATA[]]></content><author><name></name></author><summary type="html"><![CDATA[arXiv&#39;26 | 无损压缩逼近 Shannon 极限：你的 LLM 权重里一半以上的 bit 是白存的 原文：Approaching Shannon Bound with Lossless LLM Weight Compression 1. 前言 你有没有想过，当你把一个 bf16 的模型权重加]]></summary></entry></feed>