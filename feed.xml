<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://marsggbo.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://marsggbo.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-06-07T06:58:50+00:00</updated><id>https://marsggbo.github.io/feed.xml</id><title type="html">HE Xin</title><subtitle>&quot;SSS: Study, Sleep, Slim&quot; </subtitle><entry><title type="html">MoE 变 Dense：剪枝+蒸馏能救内存瓶颈吗？</title><link href="https://marsggbo.github.io/blog/2026/2605_28207/" rel="alternate" type="text/html" title="MoE 变 Dense：剪枝+蒸馏能救内存瓶颈吗？"/><published>2026-06-07T00:00:00+00:00</published><updated>2026-06-07T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/2605_28207</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/2605_28207/"><![CDATA[<h1 id="moe-变-dense剪枝蒸馏能救内存瓶颈吗">MoE 变 Dense：剪枝+蒸馏能救内存瓶颈吗？</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2605.28207">Pruning and Distilling Mixture-of-Experts into Dense Language Models</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>作为一个天天和 MoE 模型打交道的牛马，我最近被一个问题折磨得不轻：</p> <p><strong>Qwen3-30B-A3B 推理起来爽，但你得先把 30B 参数全部塞进显存——哪怕每个 token 只激活 3B。</strong></p> <p>这就是整个 MoE 时代最核心的矛盾：稀疏激活带来了计算上的高效，但内存瓶颈根本没有解决。MoE 能让你省算力，却没法省显存。</p> <p>所以自然就有人问：<strong>能不能把训练好的 MoE 直接”变成”一个 dense 模型？</strong> 不是从头训 dense，也不是压缩成小 MoE，而是把 expert 的知识直接浓缩到一个标准的 FFN 里？</p> <p>今天想和大家聊聊这篇来自 KRAFTON 的工作——据我所知，这是第一个系统性研究 MoE-to-dense 转换的框架，而且结论相当有意思。</p> <hr/> <h2 id="2-背景moe-的内存困境">2. 背景：MoE 的内存困境</h2> <p>先交代下背景。</p> <p>现在主流的大模型基本都是 MoE 架构：DeepSeek-V3/V4、Qwen3、Llama 4、GLM-5……这些模型的共同设计思路是：<strong>有很多 expert，但每个 token 只路由到其中少数几个</strong>。</p> <p>以 Qwen3-30B-A3B 为例：</p> <ul> <li>总参数：30B</li> <li>每个 MoE 层有 128 个 expert</li> <li>每个 token 激活其中 8 个</li> <li>实际参与计算的参数：~3B</li> </ul> <p>从计算量看，30B 的 MoE 用起来接近 3B dense 的成本。但从内存来看——你得把 30B 全部加载进来，不然 router 不知道往哪路由。</p> <p>这就结结实实踩了个大坑：<strong>你买单卡 A100 跑不了，买多卡又觉得没必要</strong>，因为大多数参数在推理时根本用不上，就是坐在那里吃显存。</p> <hr/> <h2 id="3-现有方案的痛点">3. 现有方案的痛点</h2> <p>面对这个问题，学术界已经有不少工作了：</p> <h3 id="31-expert-剪枝把-moe-压缩成小-moe">3.1 Expert 剪枝（把 MoE 压缩成小 MoE）</h3> <p>REAP、MC-SMoE、HC-SMoE、Sub-MoE、MergeMoE……这些工作的思路是<strong>减少 expert 数量</strong>，把 128 个 expert 压缩成更少。但有一个本质问题没有解决：</p> <blockquote> <p><strong>输出还是 MoE 模型</strong>。你还是需要 router，还是需要把所有保留的 expert 全部加载进显存。内存瓶颈的根本矛盾没有消失。</p> </blockquote> <p>如下图，Table 1 对现有方法做了梳理——所有 prior work 都只能产出更小的 MoE，而这篇工作第一个把目标定在了完全的 dense 模型上：</p> <p><img src="/assets/img/posts/2605_28207/table1_p2.png" alt="现有MoE压缩方法对比"/></p> <h3 id="32-dense-模型剪枝dense-to-dense">3.2 Dense 模型剪枝（Dense-to-Dense）</h3> <p>另一条路是先训一个大 dense 模型（比如 Qwen3-32B），再剪枝蒸馏到小 dense 模型。Minitron 就是走这条路。</p> <p>问题是：</p> <ol> <li>你得先有个大 dense teacher，训练成本本身就不低</li> <li>大 dense teacher（32B）在推理时每个 token 全量激活，给学生做蒸馏时计算效率低</li> <li>dense 剪枝在高压缩比下效果很差（后面实验数据会说明）</li> </ol> <hr/> <h2 id="4-这篇工作的核心思路">4. 这篇工作的核心思路</h2> <p>这篇论文提出了一个三步走的框架，把 MoE 直接转成 dense：</p> <p><strong>第一步</strong>：对 MoE 层里的所有 expert 打分，选出最重要的 K 个。</p> <p><strong>第二步</strong>：把 K 个 expert 分成 k 组（k 是原来 MoE 的 top-k 数），每组内做 score-weighted 平均得到一个代表性 FFN 块，再把 k 个 FFN 块拼接成一个完整的 dense FFN。</p> <p><strong>第三步</strong>：用原始 MoE 作为 teacher，对 dense 学生做知识蒸馏，把蒸馏过程中的精度损失补回来。</p> <p>如下图是完整的 pipeline 和与其他方法的对比：</p> <p><img src="/assets/img/posts/2605_28207/figure1_p1.png" alt="MoE-to-dense pipeline 和效果对比"/></p> <p>这张图非常关键——左边对比了三种产出 3B dense 模型的方式（MoE-to-dense、Dense-to-dense pruning、Random Init），MoE-to-dense 大幅领先；右边是 pipeline 示意图，三步转换一目了然。</p> <hr/> <h2 id="5-方法细节打分才是关键">5. 方法细节：打分才是关键</h2> <h3 id="51-expert-打分七种方法的对决">5.1 Expert 打分：七种方法的对决</h3> <p>这篇工作花了大量篇幅研究<strong>如何给 expert 打分</strong>——这是整个 pipeline 中最关键的决策，比怎么分组更重要。</p> <p>论文一共评测了 7 种打分方法，大致分三类：</p> <p><strong>频率类</strong>（SF/PP/PS）：看 expert 被选中了多少次、路由概率多高。这类方法偏爱”generalist”expert——服务的 token 多，但选的 expert 高度重叠，没什么多样性。</p> <p><strong>条件概率类</strong>（CP/ACP）：CP 只统计被路由到时的平均概率，过滤掉频率信息，专注于 router 真正”有把握”时的分数；ACP 在 CP 基础上再乘以输出范数，把 expert 的实际输出贡献也算进去。</p> <p><strong>D-Optimal 类</strong>（DO-CP/DO-ACP）：这是这篇论文自己提出的方法。核心思想是：独立给每个 expert 打分会选出很多”同质”的 expert，浪费了 dense FFN 的表示能力。D-Optimal 准则用 Gram 矩阵的 log-determinant 来同时衡量每个 expert 的重要性和 expert 之间的多样性，保证选出来的 k 个 expert 尽可能”各司其职”。</p> <p>如下图展示了不同打分方法选出的 top-8 expert 之间的重叠度：</p> <p><img src="/assets/img/posts/2605_28207/figure2_p5.png" alt="不同打分方法选择的expert重叠度"/></p> <p>频率类方法（SF/PP/PS）选出来的 expert 几乎完全重叠——本质上它们在选同一批”万金油”expert。DO-ACP 和其他方法的重叠度只有 ≤0.08，选出的 expert 要多样得多。</p> <p>下图则是 effective rank（有效秩，衡量多样性的指标，满分 8）的对比：</p> <p><img src="/assets/img/posts/2605_28207/figure3_p5.png" alt="Expert集合的有效秩对比"/></p> <p>DO-Optimal 选出的 expert 集合有效秩从 6.07 提升到 7.37（基于 CP），或从 6.31 提升到 6.93（基于 ACP）。换句话说，它真的让 dense FFN 的每个”槽位”都发挥了不同的作用。</p> <h3 id="52-分组策略五种方法差距不大">5.2 分组策略：五种方法，差距不大</h3> <p>把 K 个 expert 分成 k 组，每组合并为一个 FFN 块，共有五种策略：轮询（RR）、权重聚类（WC）、router 聚类（RC）、Anchor-based（AB）、输出聚类（OC）。</p> <p>实验结论很干脆：<strong>分组策略对最终结果的影响远小于打分方法</strong>，只贡献约 1 pp 的差距。而且当 K=k 时（纯剪枝，不做合并），分组根本不重要——因为每组只有一个 expert，没有”组内平均”这回事。</p> <h3 id="53-down-projection-缩放弥合静态权重和动态路由的差距">5.3 Down-projection 缩放：弥合静态权重和动态路由的差距</h3> <p>MoE 里 router 是 token-dependent 的，而 dense FFN 的权重是静态的。怎么弥合？</p> <p>论文用了两种简单的静态缩放方案：均匀缩放（每组权重 ×1/k）和比例缩放（按 expert 重要性分配权重）。实验发现差距很小，但打分越极端（比如 CP 选的 expert 概率差异大），比例缩放更好。</p> <hr/> <h2 id="6-实验结果350-种配置的大扫描">6. 实验结果：350 种配置的大扫描</h2> <p>这篇工作一口气跑了 <strong>350 种组合</strong>（7 打分 × 5 分组 × 2 缩放 × 5 个 K 值），主要在 Qwen3-30B-A3B 上做评测，然后在 DeepSeek-V2-Lite 和 GPT-OSS-20B 上做验证。</p> <h3 id="61-打分策略决定上限">6.1 打分策略决定上限</h3> <p>如下图展示了各配置的下游任务平均准确率（0.3B token 蒸馏后）：</p> <p><img src="/assets/img/posts/2605_28207/figure4_p8.png" alt="打分和分组方法的边际效应"/></p> <p>结论非常清晰：</p> <ul> <li>最好的打分（DO-ACP）vs 最差的打分（PP），差距 <strong>5.7 pp</strong></li> <li>最好的分组（RR）vs 最差的分组（AB），差距只有 <strong>1.2 pp</strong></li> </ul> <p>打分是核心。</p> <h3 id="62-纯剪枝kk胜过合并">6.2 纯剪枝（K=k）胜过合并</h3> <p>最好的 7 个配置全部使用 K=8 的纯剪枝——就是选出最好的 8 个 expert，直接拼接，完全不做组内平均。虽然合并（K&gt;k）在蒸馏前 PPL 更低，但蒸馏之后排名逆转：DO-ACP 在 K=8 下达到 43.41% 平均准确率，而 ACP×OC K=16 只有 40.50%。</p> <p>为什么？直觉上，DO-ACP 选出的 expert 本来就已经足够多样，强行合并反而把有用的个性化信息抹掉了；而频率类方法选出一堆同质 expert，合并反而有助于去冗余。</p> <h3 id="63-和其他方法的对比">6.3 和其他方法的对比</h3> <p>如下表，跟各种 baseline 的硬碰硬对比：</p> <p><img src="/assets/img/posts/2605_28207/table3_p8.png" alt="与其他方法的对比"/></p> <ul> <li>DO-ACP（43.41%）vs MC-SMoE 打分方式（36.31%）：<strong>+7.1 pp</strong></li> <li>DO-ACP vs Dense-to-Dense 剪枝（33.28%）：<strong>+10.1 pp</strong></li> <li>DO-ACP vs Random FFN（32.70%）：<strong>+10.7 pp</strong></li> </ul> <p>Dense-to-Dense 剪枝在这个压缩比（30B→3B）下几乎和随机初始化一样，只比 random 好 0.6 pp。这说明 dense 剪枝在极高压缩比下结构优势很弱，而 MoE expert 提供了一个强得多的初始化。</p> <h3 id="64-扩展训练4b-token-后效果">6.4 扩展训练：4B token 后效果</h3> <p>蒸馏量放大到 4B token，结论依然稳定：</p> <p><img src="/assets/img/posts/2605_28207/table6_p10.png" alt="扩展训练结果"/></p> <p>DO-ACP 达到 <strong>58.10%</strong> 平均准确率，比 D2D 剪枝（51.84%）高 <strong>+6.3 pp</strong>，而且训练速度还快 <strong>1.6 倍</strong>。</p> <p>速度快这件事很重要：MoE teacher（Qwen3-30B-A3B）每个 token 只激活 3B 参数，而 Dense teacher（Qwen3-32B）需要跑全量 32B——MoE teacher 蒸馏更省算力。</p> <p>验证损失曲线如下：</p> <p><img src="/assets/img/posts/2605_28207/figure5_p10.png" alt="扩展训练的验证损失曲线"/></p> <p>DO-ACP 在整个训练过程中始终保持最低的验证损失，优势没有随训练量扩大而消失。</p> <h3 id="65-定性分析错误模式对比">6.5 定性分析：错误模式对比</h3> <p>论文还做了 MMLU chain-of-thought 的定性分析，用 Claude Opus 4.6 作为 judge 对 567 道题的回答做分类。</p> <p>如下图展示了各模型的错误分布：</p> <p><img src="/assets/img/posts/2605_28207/figure6_p11.png" alt="MMLU错误分布分析"/></p> <p>从左到右模型质量提升，灾难性失败（语无伦次、重复循环）比例下降。DO-ACP 的灾难失败率（54.5%）是所有方法中最低的，同时知识错误率也最低（4.2% vs D2D 的 12.5%）。</p> <p>MMLU 人文类题目上差距最大：DO-ACP 达到 49.2%，SF 只有 25.4%，D2D 只有 19.2%。知识密集型任务下多样性打分的优势更明显——选出多样的 expert 确实更好地保留了世界知识。</p> <h3 id="66-跨模型验证">6.6 跨模型验证</h3> <p>在 DeepSeek-V2-Lite 和 GPT-OSS-20B 上的验证结果：</p> <p><img src="/assets/img/posts/2605_28207/figure7_p12.png" alt="跨模型验证结果"/></p> <p>核心结论在三个模型上都成立：DO-ACP 最好，纯剪枝（K=k）最好。但有一个有趣的规律：<strong>expert 池越大，多样性打分的优势越明显</strong>。</p> <ul> <li>Qwen3（128 experts）：最好 vs 最差打分差 <strong>7.1 pp</strong></li> <li>DeepSeek-V2-Lite（64 experts）：差距 <strong>4.3 pp</strong></li> <li>GPT-OSS-20B（32 experts）：差距 <strong>1.6 pp</strong></li> </ul> <p>直觉上也说得通：expert 越多，高质量 expert 之间冗余越严重，多样性筛选的价值越大；expert 数少时每个 expert 覆盖的 token 模式更不一样，独立打分也能选出比较多样的集合。</p> <hr/> <h2 id="7-这个工作值不值得前景如何">7. 这个工作值不值得？前景如何？</h2> <p>说几点个人判断。</p> <h3 id="71-内存问题确实是真问题">7.1 内存问题确实是真问题</h3> <p>这不是为了发论文而造的问题。MoE 在实际部署中内存占用确实是卡脖子的点——尤其是单卡 serving、边缘设备、on-device 推理。把 30B MoE 变成等效 3B dense，这个需求非常真实。</p> <h3 id="72-moe-是更好的起点">7.2 MoE 是更好的起点</h3> <p>这篇论文最重要的发现不是某个具体方法，而是<strong>MoE-to-dense 比 Dense-to-dense 剪枝强得多</strong>这个事实。</p> <p>在相同参数量下，MoE 的 expert 天然提供了更好的初始化：每个 expert 虽然参数少，但它们捕捉了不同的 token 模式，把它们拼在一起就已经构成了一个质量不错的 dense FFN，蒸馏只是锦上添花。而 dense 剪枝从一个”大而全”的网络暴力剪到小，在极高压缩比下结构信息丢失严重。</p> <h3 id="73-d-optimal-的思路有普适性">7.3 D-Optimal 的思路有普适性</h3> <p>把 D-optimality（实验设计里的经典准则）用到 expert 选择上，是个挺优雅的迁移。它本质上是在问”我用有限的 budget，怎么选一个信息量最大的子集”——这个问题在 active learning、coreset 选取、模型压缩里都有对应。</p> <p>这篇工作的理论部分也是认真的——证明了 greedy 选择的 (1-1/e) 近似比，有冗余时独立打分会失败的反例，以及有限样本下的稳定性保证。不是随便套了个公式。</p> <h3 id="74-局限和还没解决的问题">7.4 局限和还没解决的问题</h3> <p>说实话，这篇工作还有几个明显的限制：</p> <p><strong>压缩比很高</strong>：Qwen3-30B-A3B → 3.3B，压缩了近 10 倍。4B token 蒸馏之后 58.10% 的均分，而同规模 Qwen3-1.7B（比这个学生还小）预训练后能到 66.63%。差距还有 8.5 pp。如果目标是部署，还需要更多的蒸馏数据。</p> <p><strong>蒸馏量有限</strong>：4B token 对于 LLM 来说仍然是很少的量。论文自己也承认，需要扩到数十亿 token 级别才能真正评估质量天花板。</p> <p><strong>专家池大小影响收益</strong>：GPT-OSS-20B（32 experts）上 MoE-to-dense 相对 random baseline 只有 +3.7 pp，而 Qwen3 上有 +10.7 pp。如果 MoE 的 expert 数本来就不多，转换成 dense 的优势就没那么显著了。</p> <p><strong>合并策略还可以优化</strong>：当前方案把合并权重和打分权重绑定，论文承认解耦可能更好。</p> <h3 id="75-这个方向有没有前景">7.5 这个方向有没有前景？</h3> <p>我觉得是有的，但需要看清楚它解决的是哪个问题。</p> <p>它<strong>不是</strong>想训练出一个最强的 3B 模型——从头预训练肯定能做得更好。它解决的是”<strong>我已经有了一个训练好的强 MoE，我不想重新训，但我需要一个能跑在内存受限环境里的 dense 版本</strong>“这个问题。</p> <p>这个场景在 on-device、私有部署、边缘计算场景下非常常见。而且随着 MoE 成为旗舰模型的标配，MoE-to-dense 的”原材料”会越来越多——每一个 MoE 都是潜在的 dense 小模型工厂。</p> <p>比较有意思的后续方向：</p> <ul> <li>能不能在蒸馏时引入更多数据（domain-specific）来做领域适配？</li> <li>MoE-to-dense 和 LoRA 微调结合，能不能进一步弥合质量差距？</li> <li>expert 粒度更细时（如 DeepSeek 的 fine-grained MoE），转换策略需要怎么调整？</li> </ul> <hr/> <h2 id="8-总结">8. 总结</h2> <p>一句话：<strong>把 MoE 剪成 dense，打分方法是核心，多样性比频率更重要，最终产物比 Dense-to-Dense 剪枝强 +6.3 pp，还快 1.6 倍。</strong></p> <p>这篇工作做的事情很朴素——系统地扫了一遍所有可能的设计选择，找出哪个因素最关键，然后把结论在多个模型上验证了。没有花哨的创新，但结论实在，实验扎实。</p> <p>MoE 的内存瓶颈是整个行业的核心矛盾之一，MoE-to-dense 提供了一个新的思路。值得关注。</p> <p>欢迎评论区交流，如果有踩过类似坑的也欢迎分享。</p> <hr/> <p><em>不排除穿插了个人私货哈哈，有问题欢迎指出</em></p>]]></content><author><name></name></author><category term="LLM"/><category term="MoE"/><category term="论文解读"/><category term="模型压缩"/><summary type="html"><![CDATA[MoE 变 Dense：剪枝+蒸馏能救内存瓶颈吗？]]></summary></entry><entry><title type="html">ReMoE：只动 Router 就让 MoE 推理快 2 倍？这才是端侧 MoE 部署该有的姿势</title><link href="https://marsggbo.github.io/blog/2026/2605_27081/" rel="alternate" type="text/html" title="ReMoE：只动 Router 就让 MoE 推理快 2 倍？这才是端侧 MoE 部署该有的姿势"/><published>2026-06-03T00:00:00+00:00</published><updated>2026-06-03T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/2605_27081</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/2605_27081/"><![CDATA[<h1 id="remoe只动-router-就让-moe-推理快-2-倍这才是端侧-moe-部署该有的姿势">ReMoE：只动 Router 就让 MoE 推理快 2 倍？这才是端侧 MoE 部署该有的姿势</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2605.27081">ReMoE: Boosting Expert Reuse through Router Fine-Tuning in Memory-Constrained MoE LLM Inference</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有想过一个问题：MoE 模型的”稀疏激活”理论上是推理效率的银弹——每个 token 只用 Top-K 个 expert，计算量小得很。但<strong>当你把 MoE 模型塞到手机/边缘设备上的时候</strong>，为什么体验反而比同等计算量的 dense 模型还卡？</p> <p>答案是<strong>内存瓶颈</strong>。</p> <p>以 DeepSeek-V2-Lite 为例，它有 64 个 routed expert，每个 token 选 6 个。但如果你设备的快速内存只能装下 6 个 expert 的权重，那每生成一个 token，只要选中的 expert 集合跟上一步不一样，就得从慢速存储（UFS/NVMe SSD）把新 expert 搬进来。搬一次就是几百 MB 的权重 I/O。</p> <p><strong>这也是整个 MoE 端侧部署的核心矛盾：模型的稀疏激活降低了计算量，但 expert 的频繁切换导致 I/O 成了新的瓶颈。</strong></p> <p>今天想和大家聊聊这篇被 ICML 2026 接收的工作——<strong>ReMoE</strong>。它的思路非常优雅：不改模型结构、不改推理引擎、不加运行时策略，<strong>只 fine-tune router（gate 参数）</strong>，就让 MoE 在边缘设备上的推理速度提升 1.77–1.99×。</p> <hr/> <h2 id="2-问题定义moe-offloading-的-locality-gap">2. 问题定义：MoE Offloading 的 Locality Gap</h2> <h3 id="21-端侧-moe-推理的困境">2.1 端侧 MoE 推理的困境</h3> <p>先交代下背景。在内存受限的设备上跑 MoE 模型，通常只有少量 expert 能常驻快速内存（比如 GPU 显存、DRAM），剩下的 expert 要从慢速存储（UFS 4.0、NVMe SSD）按需加载。这种模式叫 <strong>expert offloading</strong>。</p> <p>问题是：标准 MoE 训练时用了 load-balancing loss，目的是让 token 尽量均匀分配给所有 expert，防止训练时 expert parallelism 出问题。这个优化目标在训练时很合理，但到了内存受限的单请求推理场景，就变成了灾难——router 被训练得尽可能”散”地选 expert，导致相邻 token 几乎不复用同一批 expert。</p> <p>如下图，对比了 Baseline 和 ReMoE 的 expert 访问模式：</p> <p><img src="/assets/img/posts/2605_27081/figure1_p2.png" alt="Expert I/O 动态对比"/></p> <p>左边是标准 router 的行为：每一步选的 expert 集合和上一步差异很大，cache 被频繁淘汰替换，产生大量 I/O thrashing。右边是 ReMoE：相邻步之间 expert 复用率大幅提高，cache 替换频率下降，I/O overhead 显著减少。</p> <h3 id="22-量化指标eor">2.2 量化指标：EOR</h3> <p>为了量化这个”缓存友好度”，论文定义了两个指标：</p> <p><strong>Instantaneous Reuse (IR)</strong>：相邻两步 expert 集合的重叠比例：</p> \[IR_t = \frac{|E_t \cap E_{t-1}|}{K}\] <p><strong>Expert Overlap Ratio (EOR)</strong>：整个序列的平均 IR：</p> \[EOR = \frac{1}{T-1}\sum_{t=2}^{T} IR_t\] <p>EOR 越高，意味着 cache miss 越少。论文还给出了一个关键命题：在标准 LRU cache（容量 $C \geq K$）下，平均 expert fetch 次数的上界是 $K(1 - EOR)$。<strong>所以提高 EOR，就是直接减少 I/O。</strong></p> <h3 id="23-训练-推理的-mismatch">2.3 训练-推理的 Mismatch</h3> <p>如下图，可以清楚看到 baseline router 的 routing trajectory：</p> <p><img src="/assets/img/posts/2605_27081/figure2_p5.png" alt="Routing trajectory 对比"/></p> <p>这是 DeepSeek-V2-Lite 第 21 层在 teacher forcing 下的 expert 选择轨迹。Baseline（左）可以看到短暂的复用，但频繁切换。ReMoE（右）通过 gate-only fine-tuning 把这些短暂的复用”拉长”了，形成明显的水平条纹——同一个 expert 被连续多步选中。</p> <p>重点：输入是固定的（teacher forcing），所以差异完全来自 routing policy 的改变，不是生成内容不同。</p> <hr/> <h2 id="3-remoe-方法详解">3. ReMoE 方法详解</h2> <h3 id="31-总览冻结一切只调-gate">3.1 总览：冻结一切，只调 Gate</h3> <p>ReMoE 的核心思路极其简洁：<strong>冻结所有非 router 参数</strong>（embedding、attention、expert FFN 全部不动），只 fine-tune gate 参数 $\theta_{\text{gate}}$。</p> <p>如下图展示了 ReMoE 在单个 MoE 层内的工作流程：</p> <p><img src="/assets/img/posts/2605_27081/figure3_p5.png" alt="ReMoE 方法概览"/></p> <p>具体步骤：</p> <ol> <li> <p><strong>Step 1</strong>：给定当前 hidden state $h_t$，同时跑两个 router——一个是冻结的 reference router（用预训练权重 $\theta^0_{\text{gate}}$ 的 FP32 快照），产出 $P^{\text{ref}}_t$；一个是可训练的 router，产出 $P_t$。</p> </li> <li><strong>Step 2</strong>：用两个信号优化可训练 router： <ul> <li><strong>Semantic Anchor</strong>（Trust-KL）：让 $P_t$ 不要偏离 $P^{\text{ref}}_t$ 太远</li> <li><strong>Temporal Locality Signal</strong>：让 $P_t$ 倾向于复用最近几步选过的 expert</li> </ul> </li> <li> <p><strong>Step 3-4</strong>：记录当前选择的 Top-K expert 到历史 buffer，供后续步骤使用</p> </li> <li><strong>Step 5</strong>：正常执行 Top-K expert，和 baseline 完全一致</li> </ol> <p><strong>关键洞察：训练时多了 reference router 的前向计算，但部署时完全没有任何额外开销</strong>——用的就是标准 MoE 推理图，只是 gate 权重被替换成了 fine-tune 后的版本。</p> <h3 id="32-训练目标">3.2 训练目标</h3> <p>总目标函数：</p> \[\mathcal{L} = L_{\text{CE}} + \lambda_{\text{KL}} L_{\text{Trust}} + \alpha_t L_{\text{Loc}}\] <p>其中 $L_{\text{CE}}$ 是标准 next-token cross entropy，$L_{\text{Trust}}$ 是语义锚点，$L_{\text{Loc}}$ 是时域局部性正则，$\alpha_t$ 是 warmup 系数。</p> <p>注意一个细节：<strong>训练时把标准 MoE load-balancing loss 关掉了</strong>（$L_{\text{aux}} = 0$），因为 load-balancing 的目标是让 expert 使用尽可能均匀——这和 cache locality 的目标直接冲突。</p> <h3 id="33-semantic-anchortrust-kl-loss">3.3 Semantic Anchor：Trust-KL Loss</h3> <p>说人话就是：不能让 router 为了提高复用率就乱飘，得”锚住”预训练学到的路由语义。</p> <p>具体做法是用 KL 散度约束当前 $P_t$ 和冻结参考分布 $P^{\text{ref}}_t$ 的距离：</p> \[L_{\text{Trust}} = \frac{1}{T}\sum_{t=1}^{T} D_{\text{KL}}(P_t \| \text{stop\_gradient}(P^{\text{ref}}_t))\] <p>这里用 stop_gradient 是因为 $P^{\text{ref}}_t$ 来自冻结分支，不需要梯度回传。</p> <p>为什么选 KL 而不是 L2 之类的？因为 routing 本身就是概率分布，KL 散度天然适合衡量分布偏移，而且对高概率 expert（实际会被 Top-K 选中的）赋予更大权重。</p> <h3 id="34-temporal-locality-regularization">3.4 Temporal Locality Regularization</h3> <p>这是 ReMoE 的核心技术贡献，由四个子项组成：</p> \[L_{\text{Loc}} = \lambda_{\text{Reuse}} L_{\text{Reuse}} + \lambda_{\text{Smooth}} L_{\text{Smooth}} + \lambda_{\text{Lag}} L_{\text{Lag}} + \lambda_{\text{WS}} L_{\text{WS}}\] <p>逐一解释：</p> <p><strong>① Reuse Loss $L_{\text{Reuse}}$</strong>——直接提高短期复用</p> <p>先定义”reuse mass”：当前步分配给上一步选中 expert 的概率质量：</p> \[m_t = \frac{1}{K}\sum_{k \in \tilde{E}_{t-1}} P_t(k)\] <p>其中 $\tilde{E}<em>{t-1} = \text{stop_gradient}(E</em>{t-1})$，即上一步的 Top-K 集合作为固定目标。然后对整个序列取平均并取负对数：</p> \[\rho = \frac{1}{T-1}\sum_{t=2}^{T} m_t, \quad L_{\text{Reuse}} = -\log(\rho + 10^{-8})\] <p>直觉上就是：鼓励当前步把更多概率分配给上一步用过的 expert，增加复用的可能性。</p> <p><strong>② Smoothness Loss $L_{\text{Smooth}}$</strong>——抑制高频抖动</p> <p>用对称 KL 散度惩罚相邻步分布的突变：</p> \[L_{\text{Smooth}} = \frac{1}{T-1}\sum_{t=2}^{T} \text{SymKL}(P_t, P_{t-1})\] <p>如果 $P_t$ 相邻步变化越小，Top-K 边界被跨越的可能性就越低。注意这里<strong>没有</strong> stop_gradient，因为希望双向约束：$P_t$ 要靠近 $P_{t-1}$，反过来也一样。</p> <p><strong>③ Lagged Inertia Loss $L_{\text{Lag}}$</strong>——抑制慢漂移</p> <p>只看相邻步可能忽略”温水煮青蛙”的漂移——每步变化很小但累积起来 expert 集合完全不同了。用一个 lag set $\mathcal{D} = {1, 2, 4, 8, 16}$ 对齐多个历史步：</p> \[L_{\text{Lag}} = \frac{1}{T-1}\sum_{t=2}^{T} \frac{1}{|\mathcal{D}|}\sum_{d \in \mathcal{D}, t-d \geq 1} \text{SymKL}(P_t, P_{t-d})\] <p><strong>④ Working-Set Compression Loss $L_{\text{WS}}$</strong>——压缩局部工作集</p> <p>前三个都不能防止 router 在一个稍大的窗口内访问太多不同 expert。这个 loss 对窗口内平均分布做熵最小化：</p> \[\bar{P}_b = \frac{1}{W}\sum_{j=1}^{W} P_{(b-1)W+j}, \quad L_{\text{WS}} = \frac{1}{n}\sum_{b=1}^{n} H(\bar{P}_b)\] <p>鼓励局部窗口内只依赖少量 expert，让 routing 适配小 cache 容量。</p> <hr/> <h2 id="4-实验设置">4. 实验设置</h2> <ul> <li><strong>模型</strong>：DeepSeek-V2-Lite（15.7B 总参数，2.4B 激活参数，64 routed expert + 2 shared expert，Top-6 routing）</li> <li><strong>数据</strong>：OpenHermes-2.5，100K 样本训练，1K 验证</li> <li><strong>训练</strong>：2000 步，AdamW，lr=5e-5，序列长度 2048，micro-batch=1 + grad_accum=8</li> <li><strong>超参</strong>：$\lambda_{\text{KL}}=0.45$，$\lambda_{\text{reuse}}=0.2$，$\lambda_{\text{smooth}}=0.05$，$\lambda_{\text{lag}}=0.05$，$\lambda_{\text{ws}}=0.01$</li> <li><strong>训练成本</strong>：单张 80GB GPU 约 7.9 小时。<strong>一次性成本，推理时零开销。</strong></li> </ul> <hr/> <h2 id="5-实验结果">5. 实验结果</h2> <h3 id="51-routing-locality-提升">5.1 Routing Locality 提升</h3> <p>如下图展示了定量结果：</p> <p><img src="/assets/img/posts/2605_27081/table2_p7.png" alt="Routing 指标对比"/></p> <p>核心数字：</p> <ul> <li><strong>EOR 从 27.3% 提升到 34.5%</strong>，相对提升 +26.4%</li> <li>作为对照，CE-only（用相同数据和设置只优化交叉熵，不加 locality loss）的 EOR 反而降到了 22.9%——说明单纯 continued fine-tuning 不仅没用，反而有害</li> <li>Routing entropy 从 0.9998 降到 0.9971（微降），负载均衡 CV 从 0.04 升到 0.16（可接受，因为 B=1 场景没有 expert parallelism）</li> <li><strong>序列级 expert 多样性几乎不变</strong>（64.000 → 63.997），说明 concentration 是 step-level 的局部集中，而不是全局 routing collapse</li> </ul> <h3 id="52-cache-效率提升">5.2 Cache 效率提升</h3> <p><img src="/assets/img/posts/2605_27081/table9_p22.png" alt="Cache 效率数据"/></p> <p>在 LRU cache 下，不同容量 $C$ 的结果：</p> <ul> <li>$C=6$（和 Top-K 匹配）：uHR 从 0.3187 提升到 0.3687（+15.7%），#uMiss 从 0.87M 降到 0.81M（-7.3%）</li> <li>$C=12$：uHR 从 0.4519 提升到 0.5035（+11.4%），#uMiss 从 0.70M 降到 0.63M（-9.4%）</li> <li>在 LFU、FIFO、甚至 Belady 最优策略下都有类似提升</li> </ul> <h3 id="53-真实系统评测">5.3 真实系统评测</h3> <p><strong>vLLM Expert Offloading（RTX 3090）</strong></p> <p><img src="/assets/img/posts/2605_27081/table4_p8.png" alt="vLLM 评测结果"/></p> <ul> <li>输出吞吐从 3.58 提升到 3.88 tok/s（<strong>+8.4%</strong>）</li> <li>TPOT 从 254.31ms 降到 242.99ms（-4.5%）</li> <li>uHR 从 39.4% 提升到 43.4%</li> <li>CE-only 反而比 baseline 还差（2.95 tok/s），再次证明 locality loss 的必要性</li> </ul> <p><strong>Jetson Orin NX 16GB 边缘设备（llama.cpp + NVMe SSD）</strong></p> <p><img src="/assets/img/posts/2605_27081/table5_p8.png" alt="Jetson 评测结果"/></p> <p>这才是 ReMoE 真正大显身手的场景——SSD 后端的 expert miss 代价更高：</p> <ul> <li><strong>ShareGPT</strong>：TPOT 从 554.69ms 降到 306.27ms，<strong>减少 44.8%</strong>，decode speedup <strong>1.81×</strong></li> <li><strong>GSM8K</strong>：TPOT 从 613.73ms 降到 346.04ms，<strong>减少 43.6%</strong>，decode speedup <strong>1.77×</strong></li> <li><strong>HumanEval</strong>：TPOT 从 672.68ms 降到 337.61ms，<strong>减少 49.8%</strong>，decode speedup <strong>1.99×</strong></li> </ul> <p>细思极恐：<strong>只改了 router 的权重，没动任何其他东西</strong>，推理速度翻倍。</p> <h3 id="54-模型能力保持">5.4 模型能力保持</h3> <p><img src="/assets/img/posts/2605_27081/table6_p8.png" alt="Benchmark 结果"/></p> <ul> <li>MMLU：57.72 → 57.81（+0.09 pp），几乎不变</li> <li>HumanEval：26.83 → 29.27（+2.44 pp），反而提升了</li> <li>GSM8K（strict）：38.89 → 38.13（-0.76 pp），在误差范围内</li> <li>IFEval：基本持平</li> </ul> <p><strong>结论：locality gain 不以牺牲模型能力为代价。</strong></p> <hr/> <h2 id="6-消融实验">6. 消融实验</h2> <p><img src="/assets/img/posts/2605_27081/table7_p9.png" alt="Ablation 结果"/></p> <p>消融研究逐一移除各组件，结论清晰：</p> <table> <thead> <tr> <th>变体</th> <th>EOR</th> <th>PPL</th> <th>解读</th> </tr> </thead> <tbody> <tr> <td>Full ReMoE</td> <td>0.3453</td> <td>3.2280</td> <td>完整方法</td> </tr> <tr> <td>w/o Reuse</td> <td>0.2831</td> <td>3.2222</td> <td>EOR 暴跌 -0.062，<strong>Reuse 是核心驱动力</strong></td> </tr> <tr> <td>w/o Consistency</td> <td>0.3290</td> <td>3.2254</td> <td>EOR 小降 -0.016，辅助稳定作用</td> </tr> <tr> <td>w/o Trust</td> <td>0.3877</td> <td>3.2629</td> <td>EOR 最高但 PPL 劣化，<strong>Trust 防止过度集中</strong></td> </tr> </tbody> </table> <p>三个关键发现：</p> <ol> <li><strong>Reuse loss 是 locality 提升的主力</strong>：去掉它 EOR 直接回到接近 baseline 水平</li> <li><strong>Consistency terms（Smooth + Lag + WS）起辅助稳定作用</strong>：主要抑制 routing 抖动和慢漂移</li> <li><strong>Trust-KL 是质量守门员</strong>：没有它 EOR 能更高（0.3877），但 PPL 开始劣化（3.2280 → 3.2629），routing 过度集中（CV: 0.16 → 0.21）</li> </ol> <hr/> <h2 id="7-超参敏感性">7. 超参敏感性</h2> <p><img src="/assets/img/posts/2605_27081/figure5_p25.png" alt="超参敏感性分析"/></p> <p>论文做了 $\lambda_{\text{reuse}}$ 和 $\lambda_{\text{KL}}$ 的 sweep：</p> <ul> <li> <p><strong>增大 $\lambda_{\text{reuse}}$</strong>：reuse score 从 0.283 线性增长到 0.370，同时 trust deviation 也增大（0.0098 → 0.0667）。但 PPL 和 Acc@1 在整个范围内保持稳定（PPL ≈ 3.22-3.24），说明在合理范围内提高复用不影响能力。</p> </li> <li> <p><strong>增大 $\lambda_{\text{KL}}$</strong>：trust deviation 从 0.308（$\lambda_{\text{KL}}=0$）降到 0.016（$\lambda_{\text{KL}}=0.7$），但 reuse 也从 0.386 降到 0.321。$\lambda_{\text{KL}}=0.45$ 是一个甜点：既有强复用增益，drift 也可控。</p> </li> </ul> <hr/> <h2 id="8-泛化性验证">8. 泛化性验证</h2> <p>论文不只在 DeepSeek 上做了验证：</p> <ul> <li> <p><strong>Qwen1.5-MoE-A2.7B</strong>：同样的 recipe，EOR 从 0.1695 提升到 0.2156（+27.2%），下游 benchmark 保持（MMLU 61.10 → 61.20，HumanEval 35.37 → 35.98）</p> </li> <li> <p><strong>Iluvatar BI-V150 GPU（国产卡）</strong>：EOR +25.0%，prefill 吞吐 2.47×，decode 吞吐 1.54×</p> </li> <li> <p><strong>华为昇腾 910B3 NPU + 鲲鹏 920</strong>：EOR +30.2%，生成吞吐从 4.3 提升到 4.8 tok/s（+11.6%）</p> </li> </ul> <p>跨模型、跨硬件平台都能 work，这说明方法的 generalization 不错。</p> <hr/> <h2 id="9-与推理时-cache-aware-rerouting-的比较">9. 与推理时 Cache-Aware Rerouting 的比较</h2> <p>一个自然的问题：为什么不在推理时直接给 cache 里已有的 expert 加分？</p> <p>论文做了对比实验：在推理时给 cached expert 的 router score 加一个 bonus $\beta$：</p> <ul> <li>$\beta=1.0$：uHR 41.66%，但 PPL 从 6.35 飙到 10.60</li> <li>$\beta=4.0$：uHR 63.88%，但 <strong>PPL 炸到 3607.92</strong>——模型直接废了</li> </ul> <p>而 ReMoE 的 learned router 不加任何推理时干预，uHR 23.74%，PPL 6.35——在一个更安全的 quality-locality trade-off 点上。</p> <p>更有趣的是，<strong>两者可以叠加</strong>：ReMoE + mild rerouting（$\beta=0.5$）比 Baseline + 同等 rerouting 效果更好（uHR 34.07% vs 32.07%，PPL 6.51 vs 6.97）。</p> <hr/> <h2 id="10-个人-take">10. 个人 Take</h2> <p>这篇论文让我印象深刻的几点：</p> <ol> <li> <p><strong>问题定义精准</strong>：把 MoE 训练时的 load-balancing 和部署时的 cache locality 之间的 mismatch 抽出来，定义为一个可优化的问题。这个 insight 本身就很有价值。</p> </li> <li> <p><strong>方法极其轻量</strong>：只动 gate 参数，2000 步，7.9 小时，一张卡。推理时零开销。这种”训练-部署”分离的思路非常工程友好。</p> </li> <li> <p><strong>效果惊人</strong>：在 Jetson 上接近 2× 的 decode speedup，而且<strong>没有牺牲模型能力</strong>。这在 MoE efficiency 方向是很少见的”白捡”收益。</p> </li> <li> <p><strong>与现有系统正交</strong>：ReMoE 改的是上游 routing trace，和下游的 cache 策略（LRU/LFU）、prefetch 策略、推理时 rerouting 都是互补的，可以叠加。</p> </li> </ol> <p>如果你在做 MoE 模型的端侧部署，或者对 expert offloading 系统感兴趣，这篇值得仔细读。代码和 checkpoint 已开源：<a href="https://github.com/BUAA-OSCAR/ReMoE">https://github.com/BUAA-OSCAR/ReMoE</a>。</p> <hr/> <p>欢迎评论区交流 / 指出问题。</p>]]></content><author><name></name></author><category term="LLM"/><category term="MoE"/><category term="推理优化"/><category term="论文解读"/><category term="ICML"/><summary type="html"><![CDATA[ReMoE：只动 Router 就让 MoE 推理快 2 倍？这才是端侧 MoE 部署该有的姿势]]></summary></entry><entry><title type="html">LLM 时代，还有人搞 AutoML 吗？有，而且变得更难了</title><link href="https://marsggbo.github.io/blog/2026/automl-book/" rel="alternate" type="text/html" title="LLM 时代，还有人搞 AutoML 吗？有，而且变得更难了"/><published>2026-06-02T00:00:00+00:00</published><updated>2026-06-02T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/automl-book</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/automl-book/"><![CDATA[<h1 id="llm-时代还有人搞-automl-吗有而且变得更难了">LLM 时代，还有人搞 AutoML 吗？有，而且变得更难了</h1> <hr/> <p>前几天有人在评论区问我：「现在都是 prompt engineering 和 fine-tune，NAS / AutoML 这条路还有人走吗？」</p> <p>我当时回了一句：「你说的那些，本质上就是 AutoML。」</p> <p>对方沉默了。</p> <hr/> <h2 id="1-一个被误解了十年的领域">1. 一个被误解了十年的领域</h2> <p>AutoML 这个词在国内有点尴尬——说出去，一半人以为是”傻瓜调参工具”，另一半人觉得这玩意儿已经过时了。</p> <p>但如果你真的做过这个方向，你会发现它背后在问的是一个<strong>永不过时的问题</strong>：</p> <blockquote> <p><strong>在一个复杂的搜索空间里，怎么用最少的代价找到最好的配置？</strong></p> </blockquote> <p>这个问题从来没有消失，只是换了马甲——</p> <ul> <li>2016 年以前，它叫”超参数优化”</li> <li>2017-2020 年，它叫”神经架构搜索（NAS）”</li> <li>2021 年以后，它叫”LLM 结构设计、量化策略搜索、LoRA rank 选择、KV cache 压缩”</li> </ul> <p><strong>换了个名字，核心矛盾还是那个</strong>：搜索空间太大，评估太贵，资源有限。</p> <hr/> <h2 id="2-写这本书之前我踩了什么坑">2. 写这本书之前，我踩了什么坑</h2> <p><img src="/assets/img/posts/automl-book/book_cover.jpg" alt="书封面"/></p> <p>说回这本书：<strong>《动手学 AutoML：从 NAS 到大语言模型优化实战》</strong>，机械工业出版社。</p> <p>我在 A*STAR CFAR 做 LLM 效率研究，之前在 HKBU 读博期间做了好几年 NAS。现在回头看，入门这个方向的时候，中文资料的确很匮乏——有的只讲概念，没有代码；有的代码能跑，但不知道在搜索什么；还有一类，就是把英文综述直接翻译一遍，读完仍然一脸懵。</p> <p>我自己摸清楚这个领域，靠的是啃论文、开源 <code class="language-plaintext highlighter-rouge">hyperbox</code> 框架、在知乎写了一堆文章——基本上是把别人踩过的坑再踩一遍，然后记下来。</p> <p>写书的念头就是这么来的：<strong>同样的坑，没必要让后来的人再踩一遍</strong>。</p> <hr/> <h2 id="3-llm-时代-automl-的新挑战">3. LLM 时代 AutoML 的新挑战</h2> <p>如果你觉得”NAS 就是搜网络结构、和 LLM 没什么关系”，那我得在这里纠正一下。</p> <p>GPT 系列刚出来的时候，大家都在想：模型越来越大，怎么让它跑得更快、用更少资源？</p> <p>这就引出了几类核心问题：</p> <p><strong>量化（Quantization）</strong>：把 float32 的权重压成 INT8 甚至 INT4，精度损失有多少？能不能自动找到每层的最优 bit-width？这是个搜索问题。</p> <p><strong>剪枝（Pruning）</strong>：Transformer 里有多少头是冗余的？注意力层可以砍多少？哪些 FFN 神经元可以移除？哪些不能？还是个搜索问题。</p> <p><strong>MoE 架构设计</strong>：DeepSeek、Mixtral 这类 MoE 模型，expert 的数量、每次激活几个、router 怎么设计——这些超参数怎么定？One-shot NAS 的逻辑可以直接迁移过来。</p> <p><strong>LoRA / PEFT 的配置</strong>：rank 选多少？用哪些层？不同任务的最优配置是不一样的，但你总不能每次都手调吧——这又是个 AutoML 问题。</p> <p><strong>本质上，LLM 时代反而把 AutoML 的重要性放大了</strong>，因为搜索空间更大、评估更贵、错误成本更高。以前跑错一次 NAS 实验，顶多浪费几张 GPU；现在搜错一次 LLM 量化策略，可能是几百张 A100 的时间。</p> <hr/> <h2 id="4-这本书讲了什么">4. 这本书讲了什么</h2> <p>书的结构大致是三层递进：从”为什么要自动化”，到 NAS 的各个范式，再到 LLM 时代的具体落地。</p> <p><strong>第一层：AutoML 基础</strong></p> <p>问题定义、搜索空间、优化策略——贝叶斯优化、进化算法、强化学习各自的适用场景和局限。这部分是打底，不会写成 wikipedia 词条那种风格，会告诉你每个方法在什么条件下会出问题。</p> <p><strong>第二层：NAS 核心范式</strong></p> <p>这是全书最厚的部分，因为这里有最多坑：</p> <ul> <li><strong>Black-box NAS</strong>：每个候选架构独立训练评估。最准，最贵，搜一次可能要 3000 GPU-days（谷歌当年 NASNet 就是这么出来的）</li> <li><strong>One-shot NAS / Weight Sharing</strong>：让所有候选共享一套 supernet 权重，把搜索成本压到几十个 GPU-hours。但有个核心问题：supernet 里排名第一的架构，独立训练后不一定还是第一。这个”ranking correlation”问题到现在还没有完美的解法</li> <li><strong>Predictor-based NAS</strong>：训练一个代理模型去预测架构的性能，少做几百次昂贵的评估。样本效率高，但代理模型本身的泛化性是个问题</li> <li><strong>Efficiency-aware NAS</strong>：把 latency、内存占用这些硬件约束直接加进搜索目标。这是最靠近工业落地的方向</li> </ul> <p>书里专门有一章讲 NAS-Bench 系列——这是这个领域少有的可以在上面做可重复实验的 benchmark，想做 NAS 研究但没有大量 GPU 的同学，强烈建议把这章读透。</p> <p><strong>第三层：LLM 时代的模型压缩与优化</strong></p> <p>涵盖量化、剪枝、知识蒸馏、PEFT（LoRA / Adapter / Prefix-Tuning）。每种方法都有配套的可运行代码，不是 toy demo，是在真实模型上跑的。</p> <hr/> <h2 id="5-得说几句实在话">5. 得说几句实在话</h2> <p>写了这么多，有几点必须说清楚，不然你买了之后骂我。</p> <p><strong>一，领域跑太快了。</strong> 书稿定稿到现在，又出了不少新工作。书里的内容是截止写作时的主流范式，可以给你打下系统性基础，但不代表”最新”。书是跑不过 arxiv 的。</p> <p><strong>二，LLM 那部分是全书相对薄弱的地方。</strong> 我的研究重心在 NAS 和系统效率，LLM 压缩那块写得没有前面几章深。如果你是冲着”LLM 推理系统优化”（vLLM、PagedAttention 那个层级）来的，这本书不是最对口的选择。</p> <p><strong>三，有门槛。</strong> 假设你有基础的 Python 和 PyTorch 使用经验，以及机器学习基础知识。纯零基础建议先打好底再来。</p> <hr/> <h2 id="6-写在最后">6. 写在最后</h2> <p>我最近在做 MoE 系统的推理效率优化，ExpertFlow 刚在 DAC 2026 被接收，下周可能会单独写篇文章聊聊这个。</p> <p>顺带一提，书里有个我一直觉得被低估的观点——</p> <p><strong>NAS 和 RLHF 的优化逻辑，在结构上是同构的。</strong></p> <p>NAS：用 reward（架构性能）去指导搜索策略更新，最终收敛到好架构。<br/> RLHF：用 reward（人类偏好）去更新模型参数，最终让模型”更像人类期望的样子”。</p> <p>两者都是在高维空间里用稀疏 reward 做策略优化，只是搜索对象不同。这个角度如果你从来没有想过，书里第三章有专门的讨论，可以翻翻。</p> <hr/> <p>书在京东有售：<a href="https://item.jd.com/15384990.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a></p> <p>欢迎评论区聊，有任何问题——包括书里写得不清楚的地方——都可以直接来问。</p> <hr/> <p><em>作者：贺鑫（marsggbo），新加坡 A*STAR CFAR 研究员</em><br/> <em>知乎 / GitHub: marsggbo</em></p>]]></content><author><name></name></author><category term="AutoML"/><category term="NAS"/><category term="LLM"/><category term="论文解读"/><category term="新书"/><summary type="html"><![CDATA[LLM 时代，还有人搞 AutoML 吗？有，而且变得更难了]]></summary></entry><entry><title type="html">arXiv’26 | ELF：Flow Matching 生成文字，用 10 倍少的数据全面超越主流 Diffusion LM</title><link href="https://marsggbo.github.io/blog/2026/20260526-elf/" rel="alternate" type="text/html" title="arXiv’26 | ELF：Flow Matching 生成文字，用 10 倍少的数据全面超越主流 Diffusion LM"/><published>2026-05-26T00:00:00+00:00</published><updated>2026-05-26T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260526-elf</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260526-elf/"><![CDATA[<h1 id="arxiv26--elfflow-matching-生成文字用-10-倍少的数据全面超越主流-diffusion-lm">arXiv’26 | ELF：Flow Matching 生成文字，用 10 倍少的数据全面超越主流 Diffusion LM</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2605.10938">ELF: Embedded Language Flows</a><br/> 作者：Keya Hu<em>, Linlu Qiu</em>, Yiyang Lu, Hanhong Zhao, Tianhong Li, Yoon Kim, Jacob Andreas, Kaiming He（MIT）<br/> 代码：https://github.com/lillian039/ELF</p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有想过，为什么用 Diffusion model 生成图片这么自然，用来生成文字却这么别扭？</p> <p>Stable Diffusion、FLUX 满大街都是，图像生成效果惊艳。但 Diffusion 语言模型（DLM）一直活在 GPT/LLaMA 这类自回归模型的阴影下——又慢，效果又差，inference 还要跑几百步。这其中有什么根本原因？</p> <p>MIT 这篇 ELF（Embedded Language Flows，Kaiming He 是 co-author）给了一个很干净的答案：<strong>连续 Diffusion LM 打不过离散方案，不是因为”文字本身是离散的”这个本质问题，而是之前大家的算法设计选错了。</strong> 换一套做法，用 10 倍少的训练 token，步数更少，就能超越 MDLM、Duo 这类离散 DLM。</p> <p>今天想和大家聊聊 ELF 到底解决了什么真问题。因为涉及 Diffusion 和 Flow Matching 的基础，我会先从头讲清楚这些概念——不是泛泛地说”加噪去噪”，而是把它讲到你能看懂 ELF 的设计为止。</p> <hr/> <h2 id="2-diffusion-和-flow-matching-到底在干什么">2. Diffusion 和 Flow Matching 到底在干什么？</h2> <h3 id="21-核心目标学会从噪声里还原数据">2.1 核心目标：学会从噪声里还原数据</h3> <p>不管是 DDPM 还是 Flow Matching，目标都是同一件事：</p> <blockquote> <p>训练一个网络，输入”被噪声污染的数据”，输出”去掉噪声后的干净数据”。</p> </blockquote> <p>一旦这个网络训好了，生成时就从一团纯随机噪声出发，反复调用网络去噪，最终得到一个逼真的生成结果（图片或文字）。</p> <p><strong>区别在于”怎么加噪、怎么去噪”</strong>——DDPM 的路径弯，需要 1000 步；Flow Matching 的路径直，32 步就够。为了说清楚这个差异，我们直接看 PyTorch 伪代码。</p> <h3 id="22-ddpm-训练给数据掺噪声学着把噪声去掉">2.2 DDPM 训练：给数据掺噪声，学着把噪声去掉</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># ===== DDPM 训练一步 =====
# x: 干净数据, shape [B, D]
#    - 图像生成时: x = 一张图片的像素值 (如 [B, 3, 256, 256])
#    - 文本生成时: x = 一段话的 embedding 向量 (如 [B, seq_len, 512])
</span>
<span class="n">noise</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn_like</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>              <span class="c1"># 随机高斯噪声，和 x 同形状
</span><span class="n">t</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randint</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">T</span><span class="p">,</span> <span class="p">(</span><span class="n">B</span><span class="p">,))</span>            <span class="c1"># 随机选一个时间步 t ∈ {0,...,999}
</span><span class="n">alpha_bar_t</span> <span class="o">=</span> <span class="n">alpha_bar_schedule</span><span class="p">[</span><span class="n">t</span><span class="p">]</span>       <span class="c1"># 查表得到"此刻噪声占比"
</span>
<span class="c1"># 加噪：把干净数据和噪声按比例混合
</span><span class="n">z_t</span> <span class="o">=</span> <span class="nf">sqrt</span><span class="p">(</span><span class="n">alpha_bar_t</span><span class="p">)</span> <span class="o">*</span> <span class="n">x</span> <span class="o">+</span> <span class="nf">sqrt</span><span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="n">alpha_bar_t</span><span class="p">)</span> <span class="o">*</span> <span class="n">noise</span>

<span class="c1"># 网络预测：输入被污染的 z_t 和时间步 t，试图还原出加入的噪声
</span><span class="n">noise_pred</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">z_t</span><span class="p">,</span> <span class="n">t</span><span class="p">)</span>               <span class="c1"># 输出和 noise 同形状
</span><span class="n">loss</span> <span class="o">=</span> <span class="nc">MSE</span><span class="p">(</span><span class="n">noise_pred</span><span class="p">,</span> <span class="n">noise</span><span class="p">)</span>            <span class="c1"># loss = 预测噪声 vs 真实噪声
</span><span class="n">loss</span><span class="p">.</span><span class="nf">backward</span><span class="p">()</span>
</code></pre></div></div> <p>直觉：$t$ 越大，<code class="language-plaintext highlighter-rouge">alpha_bar_t</code> 越小，<code class="language-plaintext highlighter-rouge">z_t</code> 里噪声占比越高，网络任务越难。$t=0$ 时 <code class="language-plaintext highlighter-rouge">z_t ≈ x</code>（几乎没加噪），$t=999$ 时 <code class="language-plaintext highlighter-rouge">z_t ≈ noise</code>（看不出原始数据了）。</p> <p><strong>DDPM 推理</strong>（从噪声反推数据）：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># ===== DDPM 推理（生成） =====
</span><span class="n">z</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="n">B</span><span class="p">,</span> <span class="n">D</span><span class="p">)</span>                     <span class="c1"># 从纯噪声出发（对应 t=T）
</span>
<span class="k">for</span> <span class="n">t</span> <span class="ow">in</span> <span class="nf">reversed</span><span class="p">(</span><span class="nf">range</span><span class="p">(</span><span class="n">T</span><span class="p">)):</span>               <span class="c1"># 从 t=999 倒退到 t=0
</span>    <span class="n">noise_pred</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">z</span><span class="p">,</span> <span class="n">t</span><span class="p">)</span>              <span class="c1"># 网络预测 z 里混了多少噪声
</span>    <span class="c1"># 用预测的噪声"减掉"一部分，让 z 稍微变干净一点
</span>    <span class="n">z</span> <span class="o">=</span> <span class="p">(</span><span class="n">z</span> <span class="o">-</span> <span class="n">beta</span><span class="p">[</span><span class="n">t</span><span class="p">]</span><span class="o">/</span><span class="nf">sqrt</span><span class="p">(</span><span class="mi">1</span><span class="o">-</span><span class="n">alpha_bar</span><span class="p">[</span><span class="n">t</span><span class="p">])</span> <span class="o">*</span> <span class="n">noise_pred</span><span class="p">)</span> <span class="o">/</span> <span class="nf">sqrt</span><span class="p">(</span><span class="mi">1</span><span class="o">-</span><span class="n">beta</span><span class="p">[</span><span class="n">t</span><span class="p">])</span>
    <span class="k">if</span> <span class="n">t</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">:</span>
        <span class="n">z</span> <span class="o">+=</span> <span class="nf">sqrt</span><span class="p">(</span><span class="n">beta</span><span class="p">[</span><span class="n">t</span><span class="p">])</span> <span class="o">*</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn_like</span><span class="p">(</span><span class="n">z</span><span class="p">)</span>  <span class="c1"># 加一点随机扰动（SDE）
</span>
<span class="c1"># 循环结束后 z ≈ x（干净数据）
</span></code></pre></div></div> <p>注意这里每步要做除法、查表、加随机噪声——公式复杂，而且必须走满 1000 步（或用 DDIM 加速到 50~200 步）。</p> <p><strong>DDPM 的根本问题</strong>：<code class="language-plaintext highlighter-rouge">alpha_bar_t</code> 的衰减曲线不是均匀的（它是一堆 $1-\beta_t$ 的连乘积），导致 $\boldsymbol{z}_t$ 从 $\boldsymbol{x}$ 到噪声的路径弯弯曲曲。推理时必须沿着同样弯的路径一小步一小步回退，需要 200~1000 步，非常慢。</p> <h3 id="23-flow-matching-训练一行代码搞定加噪">2.3 Flow Matching 训练：一行代码搞定加噪</h3> <p>Flow Matching 的关键改进就一句话：<strong>把弯曲路径换成直线</strong>。怎么做？加噪公式不用复杂的 <code class="language-plaintext highlighter-rouge">alpha_bar</code> 调度表，直接用最简单的线性插值：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># ===== Flow Matching 训练一步 =====
</span><span class="n">noise</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn_like</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
<span class="n">t</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">rand</span><span class="p">(</span><span class="n">B</span><span class="p">)</span>                         <span class="c1"># t ∈ [0, 1] 连续均匀采样
</span>
<span class="c1"># 加噪：直接线性插值！
</span><span class="n">z_t</span> <span class="o">=</span> <span class="n">t</span> <span class="o">*</span> <span class="n">x</span> <span class="o">+</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="n">t</span><span class="p">)</span> <span class="o">*</span> <span class="n">noise</span>             <span class="c1"># t=0 时纯噪声，t=1 时干净数据
</span>
<span class="c1"># 网络预测干净数据 x（x-prediction）
</span><span class="n">x_pred</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">z_t</span><span class="p">,</span> <span class="n">t</span><span class="p">)</span>
<span class="n">loss</span> <span class="o">=</span> <span class="nc">MSE</span><span class="p">(</span><span class="n">x_pred</span><span class="p">,</span> <span class="n">x</span><span class="p">)</span>
<span class="n">loss</span><span class="p">.</span><span class="nf">backward</span><span class="p">()</span>
</code></pre></div></div> <p>就这么简单。对比一下 DDPM：没有 <code class="language-plaintext highlighter-rouge">alpha_bar_schedule</code>，没有开根号，没有查表——加噪就是一次线性插值。</p> <h4 id="但是为什么网络能从-z_t-里还原出-x">但是，为什么网络能从 <code class="language-plaintext highlighter-rouge">z_t</code> 里还原出 <code class="language-plaintext highlighter-rouge">x</code>？</h4> <p>初看这个训练代码可能觉得奇怪：<code class="language-plaintext highlighter-rouge">z_t</code> 是 <code class="language-plaintext highlighter-rouge">x</code> 和 <code class="language-plaintext highlighter-rouge">noise</code> 的混合物，而 <code class="language-plaintext highlighter-rouge">noise</code> 是随机的——网络怎么能从这堆混合里把 <code class="language-plaintext highlighter-rouge">x</code> 猜出来？</p> <p>关键在于 <strong><code class="language-plaintext highlighter-rouge">t</code> 告诉了网络”信号占多少比例”</strong>。举一个具体例子：</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">t = 0.9</code></strong>：<code class="language-plaintext highlighter-rouge">z_t = 0.9 * x + 0.1 * noise</code>。此时 <code class="language-plaintext highlighter-rouge">z_t</code> 里 90% 是干净数据、10% 是噪声。网络看到的几乎就是原图稍微加了点毛刺，还原 <code class="language-plaintext highlighter-rouge">x</code> 很容易——就像看一张轻微模糊的照片，你一眼就能认出内容。</li> <li><strong><code class="language-plaintext highlighter-rouge">t = 0.5</code></strong>：<code class="language-plaintext highlighter-rouge">z_t = 0.5 * x + 0.5 * noise</code>。一半信号一半噪声，像隔着毛玻璃看。网络还能猜到大致轮廓，但细节得靠”经验”（从训练数据里学到的统计规律）。</li> <li><strong><code class="language-plaintext highlighter-rouge">t = 0.1</code></strong>：<code class="language-plaintext highlighter-rouge">z_t = 0.1 * x + 0.9 * noise</code>。几乎全是噪声，只有 10% 的微弱信号。网络基本靠”想象”——根据那一丝微弱线索和它学到的数据分布来猜。</li> </ul> <p>本质上，<strong>这就是一个”不同难度的去噪任务”</strong>：</p> <ul> <li>噪声少时（$t$ 大）→ 容易的去噪，网络学精修细节</li> <li>噪声多时（$t$ 小）→ 困难的去噪，网络学整体结构和分布规律</li> </ul> <p>同一个网络同时学所有难度级别，最终就掌握了”从任意噪声程度还原数据”的完整能力。这和 DDPM 的思路本质相同，只是混合比例的公式从弯曲的 <code class="language-plaintext highlighter-rouge">sqrt(alpha_bar)</code> 换成了简单的线性 <code class="language-plaintext highlighter-rouge">t</code>。</p> <p><strong>为什么这叫”直线”？</strong> 想象一个高维空间里有两个点：纯噪声 <code class="language-plaintext highlighter-rouge">noise</code>（起点）和干净数据 <code class="language-plaintext highlighter-rouge">x</code>（终点）。随着 $t$ 从 0 到 1，<code class="language-plaintext highlighter-rouge">z_t = t*x + (1-t)*noise</code> 在这两点之间<strong>匀速直线移动</strong>。画出来就是一条笔直的连线，不拐弯。DDPM 那个带 <code class="language-plaintext highlighter-rouge">sqrt(alpha_bar)</code> 的公式画出来则是一条弧线（因为 x 和 noise 前面的系数变化不均匀）。</p> <h3 id="24-flow-matching-推理为什么需要速度">2.4 Flow Matching 推理：为什么需要”速度”</h3> <p>训练时我们知道 <code class="language-plaintext highlighter-rouge">x</code>（干净数据就在手边），但<strong>推理时 <code class="language-plaintext highlighter-rouge">x</code> 是我们要生成的未知量</strong>——我们只有纯噪声 <code class="language-plaintext highlighter-rouge">z_0 = noise</code>，要一步步走到 <code class="language-plaintext highlighter-rouge">x</code>。问题来了：不知道终点在哪，怎么走？</p> <p>答案是靠网络告诉你<strong>每一步往哪个方向走、走多远</strong>。这个”方向+步长”在数学上就叫<strong>速度</strong>。</p> <p>来看推理代码就明白了：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># ===== Flow Matching 推理（生成） =====
</span><span class="n">z</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="n">B</span><span class="p">,</span> <span class="n">D</span><span class="p">)</span>                     <span class="c1"># 从纯噪声出发
</span><span class="n">steps</span> <span class="o">=</span> <span class="mi">32</span>
<span class="n">dt</span> <span class="o">=</span> <span class="mf">1.0</span> <span class="o">/</span> <span class="n">steps</span>                           <span class="c1"># 每步走 1/32 的时间
</span>
<span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">steps</span><span class="p">):</span>
    <span class="n">t</span> <span class="o">=</span> <span class="n">i</span> <span class="o">*</span> <span class="n">dt</span>                              <span class="c1"># 当前时间
</span>    <span class="n">x_pred</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">z</span><span class="p">,</span> <span class="n">t</span><span class="p">)</span>                   <span class="c1"># 网络预测"终点在哪"
</span>    <span class="n">velocity</span> <span class="o">=</span> <span class="n">x_pred</span> <span class="o">-</span> <span class="n">z</span>                  <span class="c1"># "终点 - 当前位置" = 要走的方向和距离
</span>    <span class="c1"># 注意：这里不是一步跳到 x_pred，而是只走一小段
</span>    <span class="n">z</span> <span class="o">=</span> <span class="n">z</span> <span class="o">+</span> <span class="n">dt</span> <span class="o">*</span> <span class="n">velocity</span>                  <span class="c1"># 往终点方向移动一小段
</span>    
<span class="c1"># 循环结束后 z ≈ x（干净数据）
</span></code></pre></div></div> <p><strong>为什么叫速度？</strong> 物理直觉：你站在 <code class="language-plaintext highlighter-rouge">z</code>（当前位置），想走到 <code class="language-plaintext highlighter-rouge">x_pred</code>（网络猜的终点），两者之差 <code class="language-plaintext highlighter-rouge">x_pred - z</code> 就是位移方向。除以时间就是速度，乘以 <code class="language-plaintext highlighter-rouge">dt</code>（一小段时间）就是这一步实际走的位移。跟初中物理的”路程 = 速度 × 时间”完全一样。</p> <p><strong>为什么不一步直接跳到 <code class="language-plaintext highlighter-rouge">x_pred</code>？</strong> 因为早期 $t$ 很小时，<code class="language-plaintext highlighter-rouge">z</code> 几乎是纯噪声，网络的预测 <code class="language-plaintext highlighter-rouge">x_pred</code> 不太准（噪声太大，信息太少）。所以只走一小步，到了新的 <code class="language-plaintext highlighter-rouge">z</code> 之后重新问网络，逐步修正。随着 $t$ 增大，<code class="language-plaintext highlighter-rouge">z</code> 越来越像干净数据，预测也越来越准，最后几步几乎可以一步到位。</p> <p><strong>为什么 Flow Matching 只要 32 步？</strong> 因为真实路径是直线。网络学的”速度”在路径上每个点都指向同一个方向（终点），所以哪怕步子大一点也不会偏离轨道太多。DDPM 的路径是弯的，步子大了就走偏，必须小步碎走。</p> <h3 id="25-网络也可以预测别的东西">2.5 网络也可以预测别的东西</h3> <p>上面的代码里网络预测的是 <code class="language-plaintext highlighter-rouge">x</code>（干净数据），这叫 <strong>x-prediction</strong>。其实网络也可以选择预测别的量：</p> <ul> <li><strong>x-prediction</strong>：网络输出 $\hat{x}$，然后用 <code class="language-plaintext highlighter-rouge">velocity = x_pred - z</code> 算速度。直觉：”直接猜终点在哪”</li> <li><strong>v-prediction</strong>：网络直接输出速度 $\hat{v}$，推理时直接用 <code class="language-plaintext highlighter-rouge">z = z + dt * v_pred</code>。直觉：”直接猜该往哪走”</li> <li><strong>ε-prediction</strong>：网络输出噪声 $\hat{\epsilon}$，用 $\hat{x} = (z_t - (1-t)\hat{\epsilon}) / t$ 反推 $x$。直觉：”猜这团东西里混了多少噪声”</li> </ul> <p>三种方式在数学上等价（知道一个就能算出另外两个），但在实际训练中稳定性不同。<strong>ELF 只能用 x-prediction</strong>——原因在第 4 节详细讲。</p> <hr/> <h2 id="3-文字--图像diffusion-lm-的困境">3. 文字 ≠ 图像：Diffusion LM 的困境</h2> <h3 id="31-根本矛盾离散-vs-连续">3.1 根本矛盾：离散 vs 连续</h3> <p>Diffusion 模型设计上就是为连续数据服务的——图像的像素值是实数，加减高斯噪声天然合理。</p> <p>文字是离散的，token 只是一个整数 ID。你无法直接在 token 上加高斯噪声——token 42 加上 0.3 的噪声还是 token 42，没有意义。</p> <p>历史上大家走了两条路：</p> <p><strong>路线一：连续 DLM。</strong> 先把 token 映射到连续的 embedding 向量（比如用神经网络的 word embedding），再在这个连续空间里做 diffusion。生成结束后，再把连续 embedding 转回 token。代表工作：Diffusion-LM、DiffuSeq、CDCD。</p> <p><strong>路线二：离散 DLM。</strong> 发明一种直接作用在 token 上的”噪声”。最直观的做法是用 [MASK] 替换 token（类似完形填空），逐步 mask 越来越多，然后学会逆向逐步 unmask。代表工作：MDLM（masked diffusion）、Duo（uniform diffusion）。</p> <p><strong>离散 DLM 目前效果更好，是主流。</strong> 连续 DLM 被一直追赶但追不上。</p> <h3 id="32-连续-dlm-为什么一直输">3.2 连续 DLM 为什么一直输？</h3> <p>连续 DLM 的核心难题是<strong>接口问题</strong>：中间过程在连续 embedding 空间，最终输出必须是离散 token，这个桥怎么搭？</p> <p>以往大家的解法普遍有两个问题：</p> <p><strong>问题一：每步去噪都往 token 对齐（per-step discretization）。</strong> 很多早期连续 DLM 在每个中间步都加一个 cross-entropy loss，强迫中间的 embedding 也对应到某个 token。结果是连续空间里的 trajectory 被 token 空间”拴住”了，丧失了在连续空间里自由流动的优势。</p> <p><strong>问题二：需要单独训练一个 decoder。</strong> Latent Diffusion 类方法（如 LD4LG）把连续 diffusion 在 encoder 的 latent 空间里做完后，再训一个 decoder 把 latent 解码回 token。两个模型，训练更复杂，推理要多跑一次 decoder。</p> <p>看下图（Table 2）可以更直观地感受——现有连续 DLM 在”Train per-step discr.”或”Sep. dec.”这两列几乎全部打了勾。而 <strong>ELF 是这张表里唯一一个两列都是空白的方法</strong>：</p> <p><img src="/assets/img/posts/20260526-flowtok/crop_table2_survey.png" alt="连续 DLM 综合对比表（Table 2）"/></p> <hr/> <h2 id="4-elf最后一步才-token">4. ELF：最后一步才 token</h2> <h3 id="41-一句话概括">4.1 一句话概括</h3> <p>ELF 的方案极简：<strong>全程在连续 embedding 空间里跑 Flow Matching，只在最后一步（$t=1$）把 embedding 解码成 token</strong>。</p> <p>如下图（Figure 2）所示，橙色点是 embedding 空间里的数据，紫色线是去噪轨迹——从 $t=0$（纯高斯噪声）一路走到 $t=1$（干净 embedding），最后一步才 argmax 到 token：</p> <p><img src="/assets/img/posts/20260526-flowtok/crop_fig2_concept.png" alt="ELF 概念图：denoising 全程在连续空间，最后一步才离散化"/></p> <p>这个设计为什么合理？<strong>因为 Flow Matching 在 $t=1$ 时本来就是”干净数据”，只差最后一步把它投影到最近的 token</strong>。这个操作不是额外加的，而是 Flow Matching 框架里最后一步的自然意义。</p> <h3 id="42-完整框架">4.2 完整框架</h3> <p><strong>训练时</strong>（如下图 Figure 3 左半部分）：</p> <ol> <li>把输入 token 序列 $\boldsymbol{s}$ 用<strong>预训练 T5 encoder</strong> 编码成干净 embedding $\boldsymbol{x}$</li> <li>按 Flow Matching 加噪：$\boldsymbol{z}_t = t\boldsymbol{x} + (1-t)\boldsymbol{\epsilon}$</li> <li>网络 $\text{net}_\theta$ 预测干净 embedding $\hat{\boldsymbol{x}}$（x-prediction），用 MSE loss：</li> </ol> \[\mathcal{L}_\text{MSE} = \mathbb{E}_{t,\boldsymbol{x},\boldsymbol{\epsilon}} \left[ \frac{1}{(1-t)^2} \| \hat{\boldsymbol{x}} - \boldsymbol{x} \|^2 \right]\] <ol> <li>对于最后一步 $t=1$：<strong>同一个网络</strong>切换到 “decode” 模式，输出经过 unembedding 层 $W$ 得到 logit，CE loss：</li> </ol> \[\mathcal{L}_\text{CE} = \mathbb{E}_{\tilde{\boldsymbol{z}}} \left[ \text{CrossEnt}(W \hat{\boldsymbol{x}}(\tilde{\boldsymbol{z}}),\ \boldsymbol{s}) \right]\] <p><strong>推理时</strong>（Figure 3 右半部分）：从 $\boldsymbol{z}_0 \sim \mathcal{N}(0, \boldsymbol{I})$ 出发，Euler 步迭代，最后一步 argmax 得到 token：</p> <p><img src="/assets/img/posts/20260526-flowtok/crop_fig3_overview.png" alt="ELF 训练（左）和推理（右）流程示意"/></p> <p>关键点：<strong>T5 encoder 只在训练时使用</strong>，用于提供高质量的 embedding 作为学习目标。推理时不需要 encoder，只需要网络本身跑 ODE。</p> <h3 id="43-共享权重-denoiser-decoder">4.3 共享权重 denoiser-decoder</h3> <p>前面说 denoiser 和 decoder 是”同一个网络”——这是 ELF 的精妙之处。网络通过一个二值 mode token 区分：</p> <ul> <li><code class="language-plaintext highlighter-rouge">mode = "denoise"</code>：中间步去噪，MSE loss</li> <li><code class="language-plaintext highlighter-rouge">mode = "decode"</code>：最后步解码，CE loss</li> </ul> <p>训练时 80% 的 batch 走 denoise 分支，20% 走 decode 分支，两部分的 loss 合并更新同一套权重。推理时只在 $t=1$ 切一次 decode，完全不需要额外的 decoder 网络。</p> <p>消融实验显示：shared-weight 方案与单独训练 decoder 的效果相近，但在低 perplexity 区间能推得更远，且省了一个训练阶段。</p> <h3 id="43a-看源码elf-的训练和推理实际长什么样">4.3a 看源码：ELF 的训练和推理实际长什么样</h3> <p>以下是从 <a href="https://github.com/lillian039/ELF/tree/pytorch_elf">ELF 官方 PyTorch 代码</a> 提炼的简化版核心逻辑，帮助理解输入输出的完整计算流。</p> <p><strong>训练一步</strong>（简化自 <code class="language-plaintext highlighter-rouge">src/train_step.py</code> + <code class="language-plaintext highlighter-rouge">src/utils/sampling_utils.py</code>）：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># ===== ELF 训练一步（简化版） =====
# 输入: input_ids [B, seq_len] — 一个 batch 的 token 序列
</span>
<span class="c1"># Step 1: Token → 连续 embedding（通过冻结的 T5 encoder）
</span><span class="n">x0</span> <span class="o">=</span> <span class="nf">t5_encoder</span><span class="p">(</span><span class="n">input_ids</span><span class="p">)</span>                <span class="c1"># [B, seq_len, 512]  干净 embedding
</span>
<span class="c1"># Step 2: 采样随机时间步 t（logit-normal 分布，让中等噪声水平被更多采样）
</span><span class="n">t</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">sigmoid</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="n">B</span><span class="p">)</span> <span class="o">*</span> <span class="mf">0.8</span> <span class="o">-</span> <span class="mf">0.8</span><span class="p">)</span>   <span class="c1"># [B]  ∈ (0, 1)
</span>
<span class="c1"># Step 3: Flow Matching 加噪
</span><span class="n">noise</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn_like</span><span class="p">(</span><span class="n">x0</span><span class="p">)</span>              <span class="c1"># [B, seq_len, 512]
</span><span class="n">z_t</span> <span class="o">=</span> <span class="n">t</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">]</span> <span class="o">*</span> <span class="n">x0</span> <span class="o">+</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="n">t</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">])</span> <span class="o">*</span> <span class="n">noise</span>  <span class="c1"># 线性插值
</span>
<span class="c1"># Step 4: 每个样本随机决定走哪个分支（伯努利采样，80% denoiser / 20% decoder）
</span><span class="n">is_decoder</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">bernoulli</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">full</span><span class="p">((</span><span class="n">B</span><span class="p">,),</span> <span class="mf">0.2</span><span class="p">))</span>  <span class="c1"># [B]
</span>
<span class="c1"># Step 5: Decoder 分支的输入 — t=1 但故意加了 per-token 噪声（模拟推理时的不完美）
</span><span class="n">lambda_t</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">sigmoid</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="n">B</span> <span class="o">*</span> <span class="n">seq_len</span><span class="p">)</span> <span class="o">*</span> <span class="mf">0.8</span> <span class="o">+</span> <span class="mf">0.8</span><span class="p">)</span>  <span class="c1"># per-token
</span><span class="n">decoder_z</span> <span class="o">=</span> <span class="n">lambda_t</span> <span class="o">*</span> <span class="n">x0</span> <span class="o">+</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="n">lambda_t</span><span class="p">)</span> <span class="o">*</span> <span class="n">noise_decoder</span>      <span class="c1"># 接近干净，但不完美
</span>
<span class="c1"># Step 6: 拼混合输入（decoder 分支用 decoder_z，denoiser 分支用 z_t）
</span><span class="n">z_mixed</span> <span class="o">=</span> <span class="n">is_decoder</span> <span class="o">*</span> <span class="n">decoder_z</span> <span class="o">+</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="n">is_decoder</span><span class="p">)</span> <span class="o">*</span> <span class="n">z_t</span>
<span class="n">t_mixed</span> <span class="o">=</span> <span class="n">is_decoder</span> <span class="o">*</span> <span class="mf">1.0</span> <span class="o">+</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="n">is_decoder</span><span class="p">)</span> <span class="o">*</span> <span class="n">t</span>

<span class="c1"># Step 7: 网络前向（同一个网络，一次 forward 同时出两个 head 的结果）
</span><span class="n">x_pred</span><span class="p">,</span> <span class="n">decoder_logits</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">z_mixed</span><span class="p">,</span> <span class="n">t_mixed</span><span class="p">,</span> <span class="n">decoder_step_active</span><span class="o">=</span><span class="n">is_decoder</span><span class="p">)</span>
<span class="c1">#   x_pred:         [B, seq_len, 512]  — denoiser 分支的 x-prediction
#   decoder_logits: [B, seq_len, vocab_size] — decoder 分支的 token 预测
</span>
<span class="c1"># Step 8: 算速度，然后 L2 loss（仅 denoiser 分支有效）
</span><span class="n">v_pred</span> <span class="o">=</span> <span class="p">(</span><span class="n">x_pred</span> <span class="o">-</span> <span class="n">z_t</span><span class="p">)</span> <span class="o">/</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="n">t</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">])</span>   <span class="c1"># 从 x-prediction 推导速度
</span><span class="n">v_target</span> <span class="o">=</span> <span class="p">(</span><span class="n">x0</span> <span class="o">-</span> <span class="n">z_t</span><span class="p">)</span> <span class="o">/</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="n">t</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">])</span>     <span class="c1"># ground truth 速度
</span><span class="n">l2_loss</span> <span class="o">=</span> <span class="p">((</span><span class="n">v_pred</span> <span class="o">-</span> <span class="n">v_target</span><span class="p">)</span> <span class="o">**</span> <span class="mi">2</span><span class="p">).</span><span class="nf">mean</span><span class="p">(</span><span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>   <span class="c1"># [B, seq_len]
</span>
<span class="c1"># Step 9: CE loss（仅 decoder 分支有效）
</span><span class="n">ce_loss</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">cross_entropy</span><span class="p">(</span><span class="n">decoder_logits</span><span class="p">,</span> <span class="n">input_ids</span><span class="p">,</span> <span class="n">reduction</span><span class="o">=</span><span class="sh">'</span><span class="s">none</span><span class="sh">'</span><span class="p">)</span>  <span class="c1"># [B, seq_len]
</span>
<span class="c1"># Step 10: 合并两个分支的 loss（用 mask 分开）
</span><span class="n">loss</span> <span class="o">=</span> <span class="p">(</span><span class="n">l2_loss</span> <span class="o">*</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="n">is_decoder</span><span class="p">)</span> <span class="o">+</span> <span class="n">ce_loss</span> <span class="o">*</span> <span class="n">is_decoder</span><span class="p">).</span><span class="nf">mean</span><span class="p">()</span>
<span class="n">loss</span><span class="p">.</span><span class="nf">backward</span><span class="p">()</span>
</code></pre></div></div> <p><strong>推理（生成）</strong>（简化自 <code class="language-plaintext highlighter-rouge">src/utils/generation_utils.py</code>）：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># ===== ELF 推理（生成）=====
# 无需 T5 encoder，直接从噪声出发
</span>
<span class="c1"># Step 1: 初始化纯噪声
</span><span class="n">z</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="n">B</span><span class="p">,</span> <span class="n">seq_len</span><span class="p">,</span> <span class="mi">512</span><span class="p">)</span>         <span class="c1"># 纯高斯噪声
</span><span class="n">x_pred_prev</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros_like</span><span class="p">(</span><span class="n">z</span><span class="p">)</span>         <span class="c1"># self-conditioning 初始为全零
</span>
<span class="c1"># Step 2: 生成时间步序列 [0, t1, t2, ..., t_N-1, 1.0]
</span><span class="n">t_steps</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">linspace</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">steps</span><span class="o">=</span><span class="mi">33</span><span class="p">)</span>  <span class="c1"># 32 步 + 终点
</span>
<span class="c1"># Step 3: ODE 循环 — 逐步从噪声走到干净 embedding
</span><span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">32</span><span class="p">):</span>
    <span class="n">t</span><span class="p">,</span> <span class="n">t_next</span> <span class="o">=</span> <span class="n">t_steps</span><span class="p">[</span><span class="n">i</span><span class="p">],</span> <span class="n">t_steps</span><span class="p">[</span><span class="n">i</span> <span class="o">+</span> <span class="mi">1</span><span class="p">]</span>

    <span class="c1"># 网络预测 x（self-conditioning: 把上一步的预测拼进输入）
</span>    <span class="n">z_input</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">([</span><span class="n">z</span><span class="p">,</span> <span class="n">x_pred_prev</span><span class="p">],</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>   <span class="c1"># [B, seq_len, 1024]
</span>    <span class="n">x_pred</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">z_input</span><span class="p">,</span> <span class="n">t</span><span class="p">)</span>                       <span class="c1"># [B, seq_len, 512]
</span>
    <span class="c1"># 从 x-prediction 推导速度
</span>    <span class="n">v_pred</span> <span class="o">=</span> <span class="p">(</span><span class="n">x_pred</span> <span class="o">-</span> <span class="n">z</span><span class="p">)</span> <span class="o">/</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="n">t</span><span class="p">)</span>

    <span class="c1"># Euler 步：沿速度方向走一小段
</span>    <span class="n">z</span> <span class="o">=</span> <span class="n">z</span> <span class="o">+</span> <span class="p">(</span><span class="n">t_next</span> <span class="o">-</span> <span class="n">t</span><span class="p">)</span> <span class="o">*</span> <span class="n">v_pred</span>

    <span class="c1"># 更新 self-conditioning 状态
</span>    <span class="n">x_pred_prev</span> <span class="o">=</span> <span class="n">x_pred</span>

<span class="c1"># Step 4: 最后一步 — 切换到 decode 模式，把连续 embedding 变成 token
</span><span class="n">_</span><span class="p">,</span> <span class="n">decoder_logits</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">z</span><span class="p">,</span> <span class="n">t</span><span class="o">=</span><span class="mf">1.0</span><span class="p">,</span> <span class="n">decoder_step_active</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
<span class="n">tokens</span> <span class="o">=</span> <span class="n">decoder_logits</span><span class="p">.</span><span class="nf">argmax</span><span class="p">(</span><span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>    <span class="c1"># [B, seq_len]  生成完毕！
</span></code></pre></div></div> <p>几个值得注意的点：</p> <ul> <li><strong>训练时用 v_target 做 L2 loss</strong>（不是直接 MSE(x_pred, x0)），这是因为 $v = (x - z)/(1-t)$ 做了 $1/(1-t)$ 的加权，让 $t$ 接近 1 时 loss 被放大——逼迫网络在”快到终点”时预测更精确</li> <li><strong>decoder 分支的输入不是纯净 x0</strong>，而是故意加了 logit-normal 噪声的 embedding，模拟推理时 ODE 最后一步输出的”不完美”结果</li> <li><strong>self-conditioning</strong>：把上一步的 x_pred 拼接到当前输入里，让网络能”参考之前的猜测”来修正预测，不额外增加 forward pass 次数</li> </ul> <h3 id="44-为什么必须-x-prediction">4.4 为什么必须 x-prediction？</h3> <p>还记得前面说的三种预测目标？ELF 选 x-prediction，有两个不可绕过的理由：</p> <p><strong>理由一：高维空间的稳定性。</strong> 语言 embedding 的维度（T5-small 是 512 维/token，T5-large 是 1024 维/token）远比图像 patch 高。在这么高的维度里，<strong>干净的语言数据实际上分布在一个低维流形上</strong>——也就是说，512 维空间里大部分方向是”没有真实数据”的。</p> <p>x-prediction 直接学习”怎么从噪声里恢复干净数据”，天然指向这个低维流形。而 v-prediction 学的是 $\boldsymbol{x} - \boldsymbol{\epsilon}$（干净数据减噪声），在高维里这个差值本身就很难学准，两个高维向量之差的误差会放大。ε-prediction 同理。</p> <p>实验证实（下图 Figure 10）：v-prediction 在 512 维还 OK，768 维以上就崩了（Gen. PPL 急剧升高）；ε-prediction 在所有维度都崩；只有 x-prediction 全程稳定：</p> <p><img src="/assets/img/posts/20260526-flowtok/crop_fig10_pred.png" alt="x/v/ε-prediction 在不同 embedding 维度下的表现（Figure 10）"/></p> <p><strong>理由二：权重共享的内在要求。</strong> Decoder 的任务是”给定（加了噪的）embedding，预测干净 token”，本质上是预测干净 embedding $\boldsymbol{x}$（然后通过 $W$ 得到 token）。如果 denoiser 用 v-prediction，它输出的是速度向量，这和”预测 $\boldsymbol{x}$”这件事在语义上不对齐，权重共享就会失效——实验中 v-prediction + shared weights 确实效果很差。</p> <hr/> <h2 id="5-cfg连续空间带来的免费能力">5. CFG：连续空间带来的”免费”能力</h2> <h3 id="51-什么是-cfg">5.1 什么是 CFG？</h3> <p>CFG（Classifier-Free Guidance，无分类器引导）是图像生成里的利器，简单说就是：</p> <p><strong>“让模型沿着条件方向走得更远一点，牺牲一点多样性，换来更高的生成质量。”</strong></p> <p>数学上，CFG 在 Flow Matching 里定义为：</p> \[\boldsymbol{v}_\text{cfg}(\boldsymbol{z}_t | \boldsymbol{c}) = \omega \cdot \boldsymbol{v}(\boldsymbol{z}_t | \boldsymbol{c}) + (1-\omega) \cdot \boldsymbol{v}(\boldsymbol{z}_t | \varnothing)\] <p>$\omega &gt; 1$ 时，有条件的预测被放大，无条件的被压缩——相当于”更使劲地朝条件方向去”。</p> <p>这个操作的本质是<strong>对连续向量的线性外推</strong>，在连续空间里完全自然。但在离散 DLM 里，token 分布上做线性外推没有清晰的含义，CFG 效果很差——这是离散 DLM 的一个固有短板。</p> <p><strong>ELF 在连续空间里跑，所以直接继承了图像 diffusion 的 CFG 工具箱</strong>。</p> <h3 id="52-用-self-conditioning-提供-guidance-信号">5.2 用 self-conditioning 提供 guidance 信号</h3> <p>标准 CFG 需要一个条件 $\boldsymbol{c}$（比如类别标签或文字 prompt）。ELF 做无条件语言生成，没有外部标签——怎么办？</p> <p>用 self-conditioning：网络每步预测出的 $\hat{\boldsymbol{x}}’$ 本身可以作为下一步的条件 $\boldsymbol{c}$。训练时，50% 的概率做一次额外的 forward pass 得到 $\hat{\boldsymbol{x}}’$，然后 concat 到 $\boldsymbol{z}_t$ 上再做一次预测：</p> \[\hat{\boldsymbol{x}} = \text{net}_\theta(\boldsymbol{z}_t \| \hat{\boldsymbol{x}}', t)\] <p>另外 50% 的概率，用全零向量 $\mathbf{0}$ 替代 $\hat{\boldsymbol{x}}’$（模拟无条件）。推理时，每步用上一步的预测当 $\hat{\boldsymbol{x}}’$，不增加任何额外 forward pass。</p> <h3 id="53-training-time-cfg推理只跑一次">5.3 Training-time CFG：推理只跑一次</h3> <p>标准 CFG 推理时需要两次 forward pass（一次有条件，一次无条件），overhead 翻倍。ELF 用 training-time CFG：训练时让网络直接输出”已经融合了 CFG 权重”的预测结果，推理时一次 forward 搞定。CFG scale $\omega$ 作为输入条件（4 个 control token）告诉网络，推理时改变这几个 token 就能调节质量-多样性 trade-off，不需要改模型架构。</p> <hr/> <h2 id="6-训练和推理细节appendix-精华">6. 训练和推理细节（Appendix 精华）</h2> <h3 id="61-完整训练-pipeline">6.1 完整训练 pipeline</h3> <p>下图（Figure 9）是 ELF 完整的训练流程：</p> <p><img src="/assets/img/posts/20260526-flowtok/crop_fig9_pipeline.png" alt="ELF 完整训练 pipeline（Figure 9）"/></p> <p>完整流程：Token 序列 → T5 encoder → 干净 embedding $\boldsymbol{x}$ → 加噪得到 $\boldsymbol{z}_t$ → self-conditioning → 拼接 control tokens（time、CFG scale、mode）→ ELF 网络 → 按 mode 计算 MSE 或 CE loss。</p> <h3 id="62-最后一步的特殊处理">6.2 最后一步的特殊处理</h3> <p>最后一步（$t=1$）理论上输入是干净 embedding，但这样模型在推理时没法泛化——因为推理时的最后一步输入是”ODE 跑出来的、不完美的 embedding”，不是真正干净的。</p> <p>解决方案：训练时故意对 decode 分支的输入做 per-token 随机损坏：</p> \[\tilde{\boldsymbol{z}} = p \cdot \boldsymbol{x} + (1-p) \cdot \boldsymbol{\epsilon}, \quad p \sim \text{LogitNormal}(0.8, 0.8)\] <p>每个 token 的损坏程度 $p$ 不同（而不是整个序列统一），逼迫 decoder 学会从周围上下文里恢复被损坏的 token，更贴近推理时的真实场景。</p> <h3 id="63-简洁的训练和推理伪代码algorithm-1--2">6.3 简洁的训练和推理伪代码（Algorithm 1 &amp; 2）</h3> <p>论文正文里有两个简洁的伪代码（Algorithm 1 训练、Algorithm 2 推理），清晰展示了 ELF 的两分支设计：</p> <p><img src="/assets/img/posts/20260526-flowtok/crop_alg12.png" alt="Algorithm 1（ELF 训练）和 Algorithm 2（ELF 推理）"/></p> <p>核心逻辑：训练时以一定概率走 denoise 分支（MSE loss）或 decode 分支（CE loss）；推理时用 Euler 步迭代 ODE，最后一步切 decode 模式 argmax。</p> <h3 id="64-带完整-guidance-的训练和推理算法appendix">6.4 带完整 guidance 的训练和推理算法（Appendix）</h3> <p>Appendix 里的 Algorithm 3 是加入了 self-conditioning CFG 的完整训练算法：</p> <p><img src="/assets/img/posts/20260526-flowtok/crop_alg3.png" alt="Algorithm 3：含 guidance 的完整训练算法"/></p> <p>Algorithm 5 是完整推理算法（含 guidance）：</p> <p><img src="/assets/img/posts/20260526-flowtok/crop_alg5.png" alt="Algorithm 5：含 guidance 的完整推理算法"/></p> <h3 id="65-ode-vs-sde-采样器algorithm-6">6.5 ODE vs SDE 采样器（Algorithm 6）</h3> <p>除了确定性 ODE 采样，ELF 还支持 SDE-inspired 随机采样器：</p> <p><img src="/assets/img/posts/20260526-flowtok/crop_alg6.png" alt="Algorithm 6：ODE 和 SDE 采样器对比"/></p> <p>SDE 变体的思路：每步 ODE update 之前，往 $\boldsymbol{z}_t$ 里重新注入一点高斯噪声（强度由 $\gamma$ 控制），时间 $t$ 也相应往 noise 方向偏移一点。效果是：错误的去噪轨迹有机会被”抖一抖”，不至于一错到底。</p> <p>$\gamma=0$ 时退化为 ODE。实验显示 <strong>SDE sampler 在 few-step 场景（8-32 步）显著优于 ODE</strong>，而 ODE 在步数足够多时追上来。</p> <hr/> <h2 id="7-实验结果">7. 实验结果</h2> <h3 id="71-主结果更少训练数据更少推理步数更好效果">7.1 主结果：更少训练数据，更少推理步数，更好效果</h3> <p><img src="/assets/img/posts/20260526-flowtok/crop_fig1_results.png" alt="主实验结果：ELF vs MDLM/Duo/FLM/LangFlow（Figure 1）"/></p> <p>上图是 ELF-B（105M 参数）与四个主流 baseline（约 170M 参数）的对比。纵轴是 Gen. PPL（生成 perplexity，越低越好），横轴是采样步数。</p> <p><strong>ELF-B 在 32 步就达到 Gen. PPL = 24</strong>，远好于 MDLM/Duo/FLM/LangFlow 即使用 1024 步的结果。</p> <p>数据量：ELF 用了 <strong>45B tokens</strong>（OWT 跑 5 个 epoch），对比方法用了 <strong>524B+ tokens</strong>，整整少了 10 倍多。</p> <h3 id="72-与蒸馏方法对比system-level-分析">7.2 与蒸馏方法对比、System-level 分析</h3> <p>详细 system-level 对比见下图（Figure 6 是 scaling，Figure 7 是系统对比）：</p> <p><img src="/assets/img/posts/20260526-flowtok/crop_fig6_scaling.png" alt="Figure 6（scaling）"/></p> <p><img src="/assets/img/posts/20260526-flowtok/crop_fig7_system.png" alt="Figure 7（system-level 对比）"/></p> <p>值得注意的是，ELF <strong>不使用任何蒸馏</strong>，但在 few-step 场景下仍然超越经过蒸馏的 MDLM+SDTT、Duo+DCD、FMLM。蒸馏方法需要额外训练轮次，ELF 更干净。</p> <p>模型 scaling 结果（Figure 6）：ELF-B (105M) → ELF-M (342M) → ELF-L (652M)，Gen. PPL 随规模持续下降，scaling 有效。</p> <h3 id="73-各项消融实验">7.3 各项消融实验</h3> <p>下图（Figure 4 和 Figure 5）是消融实验结果，每条曲线通过扫 CFG scale 得到（即 quality-diversity trade-off 的 Pareto frontier）：</p> <p><img src="/assets/img/posts/20260526-flowtok/crop_fig4_cfg.png" alt="Figure 4（CFG 消融）"/></p> <p><img src="/assets/img/posts/20260526-flowtok/crop_fig5_ablation.png" alt="Figure 5（embedding、decoding、sampler 消融）"/></p> <p>几个关键结论：</p> <ul> <li><strong>CFG scale</strong>（Figure 4）：增大 $\omega$ → Gen. PPL 下降（质量↑）但 entropy 下降（多样性↓），经典 trade-off</li> <li><strong>Embedding 选择</strong>（Figure 5a）：预训练 contextual embedding（T5）&gt; scratch encoder &gt; frozen Gaussian &gt; learnable embedding。learnable 最差，因为 embedding 和 denoiser 联合优化太难</li> <li><strong>Decoding 策略</strong>（Figure 5b）：shared-weight 和 separate decoder 效果接近，但 shared-weight 在低 PPL 区间更优</li> <li><strong>Sampler</strong>（Figure 5c）：SDE sampler 在 few-step 显著优于 ODE</li> </ul> <h3 id="74-条件生成翻译--摘要">7.4 条件生成（翻译 + 摘要）</h3> <p>ELF 在 WMT14 De-En 翻译（BLEU 26.4）和 XSum 摘要（R-1/R-2/R-L = 36.0/12.2/27.8）上全面超越 MDLM、Duo、E2D2、SeqDiffuSeq、CDCD：</p> <table> <thead> <tr> <th>方法</th> <th>Params</th> <th>BLEU (De-En)</th> <th>R-1</th> <th>R-2</th> <th>R-L</th> </tr> </thead> <tbody> <tr> <td>AR</td> <td>99M</td> <td>25.2</td> <td>30.5</td> <td>10.2</td> <td>24.4</td> </tr> <tr> <td>MDLM</td> <td>99M</td> <td>18.4</td> <td>33.4</td> <td>11.6</td> <td>25.8</td> </tr> <tr> <td>Duo</td> <td>170M</td> <td>21.3</td> <td>31.4</td> <td>10.1</td> <td>25.0</td> </tr> <tr> <td>E2D2</td> <td>99M</td> <td>24.8</td> <td>28.4</td> <td>8.3</td> <td>22.0</td> </tr> <tr> <td><strong>ELF (ours)</strong></td> <td><strong>105M</strong></td> <td><strong>26.4</strong></td> <td><strong>36.0</strong></td> <td><strong>12.2</strong></td> <td><strong>27.8</strong></td> </tr> </tbody> </table> <hr/> <h2 id="8-个人感想">8. 个人感想</h2> <p>ELF 的贡献用一句话总结：<strong>把图像 diffusion 的成熟工具箱（Flow Matching、x-prediction、training-time CFG、SDE sampler）几乎原封不动地搬到语言生成，同时用”最后一步才 token”这个极简设计解决了连续-离散接口问题。</strong></p> <p>这件事听起来简单，但它揭示了一个重要 insight：<strong>连续 DLM 之前差，不是因为”文字天生离散”这个无法绕开的本质矛盾，而是之前大家在接口设计上选了错误的路</strong>——每步都往 token 对齐、或者用两段式训练。这些设计限制了连续空间里的流动自由度，才导致效果输给离散方案。</p> <p>Kaiming He 在这篇里的角色也值得一提。他的”Back to basics”工作建立了 x-prediction 在高维数据上的理论基础，”Mean Flows”工作建立了 training-time CFG 的方法论，ELF 是这条技术路线在语言生成上的直接落地。</p> <p>当然，ELF 目前规模还小（最大 652M），和 7B/70B 级别的 LLM 差距还很大。Diffusion LM 在 in-context learning、few-shot prompting 上能不能追上 AR 模型，还是开放问题。但作为连续 DLM 方向的一个重要里程碑，这篇工作的技术路线是清晰且有说服力的。</p> <hr/> <p>代码开源：https://github.com/lillian039/ELF，感兴趣的可以跑跑看。有问题欢迎评论区交流。</p>]]></content><author><name></name></author><category term="LLM"/><category term="Diffusion"/><category term="Flow Matching"/><category term="论文解读"/><summary type="html"><![CDATA[arXiv’26 | ELF：Flow Matching 生成文字，用 10 倍少的数据全面超越主流 Diffusion LM]]></summary></entry><entry><title type="html">arXiv’26 | Frontier：LLM 推理仿真器，端到端误差从 51.7% 降到 2.6%</title><link href="https://marsggbo.github.io/blog/2026/20260526-frontier/" rel="alternate" type="text/html" title="arXiv’26 | Frontier：LLM 推理仿真器，端到端误差从 51.7% 降到 2.6%"/><published>2026-05-26T00:00:00+00:00</published><updated>2026-05-26T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260526-frontier</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260526-frontier/"><![CDATA[<h1 id="arxiv26--frontierllm-推理仿真器端到端误差从-517-降到-26">arXiv’26 | Frontier：LLM 推理仿真器，端到端误差从 51.7% 降到 2.6%</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2605.21312">Frontier: Towards Comprehensive and Accurate LLM Inference Simulation</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有想过，在决定用 PDD 还是 co-location 部署一个 200B 的 MoE 模型之前，能不能先”模拟跑一遍”？</p> <p>不是假设，是真实的问题。一个 200B 参数的 MoE 模型，合理的配置组合（parallelism 策略 + 批大小 + 架构选型）轻松超过 1000 种。在 64-GPU H100 集群上，扫 100 个配置就要花 12,800–25,600 GPU-hours，成本高达 <strong>$180,000</strong>。没有哪个团队能负担得起这个开销，大多数人的选择是”用个保守的默认配置凑合”，结果是<strong>白白损失了 3–5× 的吞吐量</strong>。</p> <p>所以 inference simulator 这个方向一直有人做。但做好很难。</p> <p>这篇来自香港中文大学 + StepFun 的工作——Frontier——正面硬刚了这个问题，给出了目前来看最系统的答案：端到端误差从对比方法的 <strong>51.7% 压到了 2.6%</strong>，还能模拟 1K+ GPU 的集群，跑在普通 CPU 上。</p> <hr/> <h2 id="2-现有仿真器差在哪">2. 现有仿真器差在哪？</h2> <p>先交代下背景：现有做 LLM inference simulation 的代表工作有四个——Vidur（微软）、AIConfigurator（NVIDIA）、LLMServingSim 2.0、APEX。</p> <p>他们的问题集中在两点。</p> <p><strong>问题一：架构不完整。</strong></p> <p>现有仿真器大多是围绕”一组同质化的单体 replica”来建模的——每个 replica 跑完整的模型路径，调度状态自包含，本地循环执行。这套抽象对传统 co-location serving 够用，但在 disaggregated serving（PDD/AFD）面前就崩了。</p> <p>PDD（Prefill-Decode Disaggregation）把 prefill 和 decode 拆到两组 GPU 上，KV cache 要跨集群传；AFD（Attention-FFN Disaggregation）进一步把 decode 阶段的 attention 和 FFN 分到不同硬件池。这种架构下，事件图本身就变了——请求要穿越多个 role-specific cluster，KV cache 传输是显式的因果边，MoE EP 引入了 routing 依赖的同步。</p> <p>你没法”加一个 latency term”来给单体仿真器打补丁，这是架构层面的不匹配。</p> <p><strong>问题二：仿真精度不够，会让你选错配置。</strong></p> <p>更离谱的是，现有仿真器的精度差到可以让你做出错误决策。</p> <p>如下图，现有方法对 CUDA Graph 的建模是缺失的。CUDA Graph 会把 decode 的 TPOT 降低 32%–47%（co-location）或 37%–60%（PDD），但同时会因为 batch size 对齐产生 padding overhead，增加 22%–58% 的无效 token 计算。这两个效果耦合在一起，<strong>任何标量 speedup factor 都无法同时捕捉</strong>，但 AIConfigurator 直接忽略了 CUDA Graph 建模。</p> <p><img src="/assets/img/posts/20260526-frontier/crop_fig1_cuda_graph.png" alt="CUDA Graph 对 TPOT 的影响（Frontier 实测）"/></p> <p>更严重的是保真度问题。以算子为例：Vidur 用 token count 作为 FlashAttention 的 runtime 预测特征，误差 p50/p95 达到 <strong>55.4%/376.1%</strong>。对于 MoE 的 GroupedGEMM，Vidur 直接没有建模。内存建模同样差：用”总显存减去模型权重”估算 KV cache budget 的方法（analytical 法），会高估 8.1%–27.2% 的可用 KV cache，进而虚报 23.6%–32.4% 的解码吞吐量。下图直接展示了这两个问题的量化结果：</p> <p><img src="/assets/img/posts/20260526-frontier/crop_fig4_fidelity_gaps.png" alt="简化建模导致的保真度缺口：attention 算子中位误差 22.3%，KV budget 被高估 8.1%–27.2%"/></p> <p>这两个误差不是独立的——KV cache 算多了，调度器能接的请求多了，实际 GPU 却装不下，于是发生 preemption，delay 被放大，TPOT 实测比仿真高出一大截。下图是最终结果：</p> <p><img src="/assets/img/posts/20260526-frontier/crop_fig5_sla_drift.png" alt="仿真器选了个看似最优的配置，部署到 vLLM 之后 SLA 直接炸了"/></p> <p>仿真器（APEX）选出了一个”最优配置”，但因为算子 runtime 和 KV cache budget 预测过于乐观，这个配置在实际 vLLM 上运行时超出了 SLA 边界。<strong>仿真本来是为了帮你避免踩坑，结果踩坑踩得更准。</strong></p> <p>这就是 Frontier 要解决的问题。</p> <hr/> <h2 id="3-frontier-怎么做的">3. Frontier 怎么做的</h2> <p>Frontier 的整体架构分四个模块：</p> <p><img src="/assets/img/posts/20260526-frontier/crop_fig6_architecture.png" alt="Frontier 系统架构（四平面设计）"/></p> <p>① <strong>Workload &amp; Config</strong>：描述仿真场景（模型、serving 架构、parallelism、workload 类型）；② <strong>Fidelity Plane</strong>：提供经过校准的算子 runtime、通信开销、KV cache budget 预测；③ <strong>Control Plane</strong>：把 serving 配置编译成可运行的仿真拓扑，处理 disaggregated 架构的跨集群依赖；④ <strong>Execution Plane</strong>：驱动每个 replica 内部的调度-批处理-执行循环，支持 runtime adapter 注入各种优化特性。</p> <p>核心设计有三点，分别对应两个根本问题。</p> <h3 id="31-control-planedisaggregated-抽象">3.1 Control Plane：disaggregated 抽象</h3> <p>这是解决”架构不完整”的关键。Frontier 放弃了单体 replica 抽象，引入 <strong>role-specific cluster worker</strong> 的概念。每种 serving 架构对应不同的 role 集合：</p> <ul> <li>Co-location：单一集群 C，同时处理 prefill 和 decode</li> <li>PDD：prefill 集群 P + decode 集群 D，二者通过 KV cache 传输同步</li> <li>AFD：prefill 集群 P + attention 集群 A + FFN 集群 F，decode 阶段 A→F→A 每层激活传一次</li> </ul> <p>跨集群的依赖（KV cache transfer、activation transfer、MoE EP 同步）都作为显式事件建模到 DES 事件图里。每次 prefill 完成，触发 KVCacheTransferStart 事件，decode cluster 在 Transfer End 后才能接管请求——排队延迟和同步都从事件图里自然涌现，不是靠”加个 latency 系数”糊的。</p> <p>对于 reasoning/agent/RL rollout 这类有状态的 workload，Frontier 引入了 <strong>stateful request 抽象</strong>：每个请求可以携带思考轮数、每轮的 tool call 延迟、每轮的 prefill/decode token 计划，在推理结束前持续在集群内重入调度。</p> <h3 id="32-execution-planeruntime-adapter-机制">3.2 Execution Plane：Runtime Adapter 机制</h3> <p>为了把 CUDA Graph、speculative decoding、prefix caching 这些优化建进来，Frontier 设计了 <strong>Runtime Adapter</strong> 抽象——每个 adapter 只改调度循环中一个明确划定的切片：</p> <ul> <li><strong>CUDA Graph adapter</strong>：decode batch 按 capture bins（{1,2,4,8,16,32,64}）向上对齐，把 padding 后的 batch 大小报给 Fidelity Plane，同时切换到 kernel-only（无 launch overhead）的计时模式。padding 引入的额外 token、graph 去掉的 launch overhead，两个效果同时被捕捉到。</li> <li><strong>Speculative decoding adapter</strong>：per-request 维护 planned/verified/accepted/committed token count，MTP 的 draft→verify→commit 循环跟普通 decode work 共享 batch，不同请求可以有不同的 speculative depth 和不同的 acceptance outcome——tail latency 的来源在这里。</li> </ul> <p>这种 adapter 机制的好处是：优化特性不需要硬编码进 simulation core，可以独立开发、独立测试。</p> <h3 id="33-fidelity-plane三类算子精准建模">3.3 Fidelity Plane：三类算子，精准建模</h3> <p>现有方法用 token count 作为所有算子的 proxy feature，这是保真度差的根本原因。Frontier 把算子分成三类，分别用不同的预测策略：</p> <ul> <li><strong>Token-count operators</strong>（GEMM、elementwise、norm）：runtime 主要取决于 TP slice 里的 token 数，线性回归或小 random forest 就够了。</li> <li><strong>Sequence-dependent operators</strong>（attention）：runtime 和 batch 内各请求的序列长度分布强相关，不只是总 token 数。Frontier 用 random forest，feature 包含 batch size、total tokens、prefill/decode 长度的 min/max/分位数。这就能捕捉到 chunked prefill 下的非线性行为。</li> <li><strong>Routing-dependent operators</strong>（MoE GroupedGEMM）：runtime 和 token-to-expert 路由分布有关。feature 加入了 load balance 统计（variance、max of expert token counts）、expert selection ratio 等。</li> </ul> <p>内存建模方面，Frontier 做了一次 dummy profile run（用假权重初始化模型，跑一次 forward，读 PyTorch allocator snapshot），得到三个分量：权重内存、torch peak 增量（activation scratch）、non-torch residency（NCCL workspace 等）。KV cache budget = 总显存预算 - 权重 - profiled non-KV overhead。这比”总显存减权重”精准得多。</p> <hr/> <h2 id="4-实验结果">4. 实验结果</h2> <h3 id="40-论文怎么证明-frontier-可信">4.0 论文怎么证明 Frontier 可信？</h3> <p>在看具体数字之前，先说清楚这篇论文的验证思路——这是判断”结论能不能信”的关键。</p> <p>Frontier 的验证分三层，从底层到顶层依次累积：</p> <p><strong>第一层：算子级 microbenchmark。</strong> 在真实 H800 GPU（BF16 + FP8）上，profiling 各类算子（GEMM、attention、GroupedGEMM、all-reduce）在不同 batch size、序列长度、并行配置下的实际 runtime，和 Frontier 的预测值对比。这一层的目的是隔离其他因素，单独验证 Fidelity Plane 的建模是否准确。</p> <p><strong>第二层：组件级验证。</strong> 单独测试各 runtime adapter 是否正确建模了对应的优化效果——CUDA Graph adapter 在不同 workload 下对 TPOT 的缩减比例是否和 vLLM 实测一致，prefix cache 的 block 命中率曲线是否跟踪准确，KV cache budget 和 profiling 实测值的偏差是多少。</p> <p><strong>第三层：端到端 testbed 对比。</strong> 这是最关键的一层。用 16 张 H800 搭真实 vLLM 集群，跑真实请求流，把 vLLM 的 TTFT/TPOT/throughput/E2E makespan 作为 ground truth，和 Frontier 仿真结果逐点对比。覆盖两种模型（dense + MoE）× 两种架构（co-location + PDD）× 四种 workload 模式，共 64 个 case。</p> <p>这个设计的合理之处在于：如果第一层准，第三层就应该准；如果第三层有系统性偏差，能回溯到第一层定位原因。这比”只报端到端误差”更有说服力，因为每一层的误差来源是可以分开解释的。</p> <h3 id="41-算子和内存精度">4.1 算子和内存精度</h3> <p>先看算子级精度。在 H800 BF16 下，Frontier 的 attention p50/p95 误差是 <strong>3.5%/14.2%</strong>，Vidur 同样数据下是 <strong>55.4%/376.1%</strong>。FP8 下 Frontier p95 进一步降到 8.8%，而 Vidur 根本不支持 FP8。</p> <p>KV cache budget 方面，Frontier 在所有 4 种并行配置下把初始 block 数量的误差控制在 <strong>1.89% 以内</strong>；analytical 方法（”总减权重”）高估 14.10%–39.73%。</p> <h3 id="42-端到端仿真精度">4.2 端到端仿真精度</h3> <p>这是最关键的数字：</p> <p><img src="/assets/img/posts/20260526-frontier/crop_fig11_e2e.png" alt="端到端精度对比（16 卡 H800 testbed）"/></p> <p>在 16 卡 H800 testbed 上，跨 dense（Llama3.1-8B）和 MoE（Qwen3-30B MoE）、co-location 和 PDD、四种 workload pattern（prefill-heavy、decode-heavy、balanced、SharedGPT）：</p> <ul> <li><strong>Co-location</strong>：Frontier 在 32 个 case 里跟 vLLM 误差 &lt; 9.37%；Vidur 最差到 45.5%；AIConfigurator 在 decode-heavy 下 E2E makespan 误差飙到 170.0%。</li> <li><strong>PDD</strong>：Frontier 32 个 case 全部在 10.99% 以内，29 个在 9% 以内；AIConfigurator 在 dense 和 MoE 的 decode-heavy 下 E2E 误差都达到 200%（architecture level 的崩塌，没有 KV cache transfer dependency 建模，任何 scalar 修正都救不了）；Vidur 和 APEX 直接不支持 PDD。</li> <li><strong>AFD</strong>：在 Step3-316B 上，Frontier 把 TPOT 控制在 6.4% 以内、throughput 在 7.0% 以内。没有其他仿真器能跑 AFD。</li> </ul> <p>和 SOTA 对比（AIConfigurator）：SharedGPT trace 下，E2E latency 误差从 <strong>44.9% → 6.4%</strong>（co-location），<strong>51.7% → 2.6%</strong>（disaggregation）。</p> <hr/> <h2 id="5-用来干什么">5. 用来干什么</h2> <p>这才是这篇工作真正有意思的地方——四个 use case，<strong>没有一个是之前的仿真器能复现的</strong>。</p> <h3 id="51-pareto-frontier-探索">5.1 Pareto frontier 探索</h3> <p>256 卡 H800，部署 Llama-3.3-70B，对比 co-location/PDD/AFD。合理的配置空间有 483,536 种，扣掉 OOM 的还剩 496 个满足 SLA 的。</p> <p>Frontier 扫完之后给出了三条 Pareto 曲线：</p> <p><img src="/assets/img/posts/20260526-frontier/crop_fig13_pareto.png" alt="三种架构的 throughput-generation speed Pareto 曲线"/></p> <p>结论：<strong>SLA 松（TTFT ≤ 3000ms）时，PDD 最优</strong>（137.4K toks/s）；<strong>SLA 紧（TTFT ≤ 500ms）时，AFD 反而更好</strong>（116.2K toks/s，PDD 在这个约束下只能做到 100.7K）。</p> <p>如果没有仿真器，就只能在”松 SLA 下 PDD 看起来更好”这个错误结论上停下来。</p> <h3 id="52-异构-gpu-分配">5.2 异构 GPU 分配</h3> <p>1024 卡集群（H800 + H20 混用，H800 $3.49/h，H20 $1.59/h），部署 Qwen3-235B-A22B。结论很实际：</p> <ul> <li><strong>便宜不代表合适</strong>：把 H20 放在 compute-bound 的 FFN role 上，性价比不升反降（CE &lt; 1.08，被 Gate 1 过滤掉了）；</li> <li><strong>对的 role 才能省钱</strong>：PDD 下把 D role 换成 H20（P:D=1:1），省了 37% 的成本，性价比提升 33%，SLA 也守住了；AFD 下把 attention cluster 换成 H20，省 16%，CE 提升 12%。</li> </ul> <h3 id="53-rl-rollout-动态-parallelism-切换">5.3 RL Rollout 动态 parallelism 切换</h3> <p>4000 条 trajectory 的 burst，5% 是 heavy tail。</p> <ul> <li><strong>Layout A</strong>（DP=32,PP=16,TP=2）：高 DP，适合 burst；</li> <li><strong>Layout B</strong>（DP=8,PP=16,TP=8）：宽 TP，适合 tail 阶段 per-trajectory decode；</li> <li>动态策略：burst 阶段用 A，active trajectory &lt; 10% 时切换到 B。</li> </ul> <p>结果：makespan 从 528.8s → 259.1s，decode throughput <strong>提升 2.04×</strong>。</p> <hr/> <h2 id="6-一些想法">6. 一些想法</h2> <p>先说我认为这篇工作好的地方。</p> <p>最本质的贡献是把”disaggregated serving 仿真”这件事做到了工程层面可用。PDD 在工业界（DistServe、Mooncake）已经是标配，AFD 也在 MegaScale-Infer 里被验证过，但现有仿真器在这块全是空白。Frontier 填的这个空，不是”做了个能跑的 prototype”，而是误差在 10% 以内的 decision-grade 仿真。</p> <p>三类算子的分类（token-count / sequence-dependent / routing-dependent）也挺扎实，是从真实的 kernel 行为出发做的归纳，不是拍脑袋分的。CUDA Graph 的双效果建模（padding + launch overhead 各自独立捕捉）这个细节我觉得做得对，这个坑确实不好踩。</p> <p>然后谈谈我对”这个仿真器到底可不可信”这个问题的看法——毕竟验证方法设计得再合理，也有它覆盖不到的地方。</p> <p><strong>大规模准确性没有 ground truth。</strong> 论文端到端验证用的是 16 卡 H800。但 use case 部分拿去用的是 256 卡（Pareto 探索）和 1024 卡（异构 GPU 分配）。两者之间差了 16–64×，这中间的 gap 完全没有实测验证。论文的隐含假设是算子级和端到端误差在更大规模下保持稳定，但大规模下集合通信的 congestion 模式、跨集群的 scheduling 交互、rack-level 的 incast 效应——这些在 16 卡上几乎观察不到，却可能在真实的 1K GPU 集群上出现。文中对此没有任何量化分析。</p> <p><strong>校准数据对硬件和框架版本敏感。</strong> Frontier 的算子预测模型是用 H800 BF16/FP8 profiling 数据训练的。换到 H100、A100、或者 AMD MI300x——calibration 数据直接作废，需要重新采集。更新 vLLM 版本同理：v0.10.2 之后 vLLM 持续迭代调度逻辑，CUDA Graph 的 capture bins 也可能改动。论文没有说清楚 recalibration 的成本和频率，这对长期使用是个实际问题。</p> <p><strong>MoE GroupedGEMM 的 p95 误差仍然非平凡。</strong> Frontier 的 GroupedGEMM 预测 p50/p95 是 4.4%/17.2%。对于 GroupedGEMM 占主导的大规模 MoE（比如 Qwen3-235B-A22B），p95 17% 的单算子误差叠加多层之后对尾延迟的影响有多大？论文没有单独分析这个误差传播链。</p> <p><strong>workload 分布是个盲点。</strong> 实验用的四种 workload 模式都是事先固定分布的。但生产流量是非平稳的：早高峰和夜间流量特征不同，用户行为随应用演进变化。Frontier 能处理给定 trace 的仿真，但对分布 shift 有多鲁棒——完全没有讨论。</p> <p>综合来看，Frontier 的验证在现有仿真器里是最扎实的，三层验证结构（算子→组件→端到端）也是正确的思路。但”16 卡 → 1K 卡”这个尺度 gap 是我目前最不放心的地方。如果有机会在更大规模的 testbed 上做一次 validation，说服力会强得多。</p> <p>对于做 LLM serving 研究、需要快速探索部署配置空间的团队来说，这工具是有用的：在 CPU 上跑仿真，误差 &lt; 10%，不用花 $180,000 扫集群。代码还没有开源（截稿时），等出来值得拉下来配合真实 profiling 数据用用。</p> <hr/> <blockquote> <p>如果有用，欢迎评论区交流，或者指出理解有偏差的地方。</p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="推理优化"/><category term="仿真器"/><category term="分布式系统"/><category term="论文解读"/><summary type="html"><![CDATA[arXiv’26 | Frontier：LLM 推理仿真器，端到端误差从 51.7% 降到 2.6%]]></summary></entry><entry><title type="html">LLM Agent Memory 全景拆解：从 RAG 到 KV Cache 到参数写入，100+ 篇工作的方法演进与真实取舍</title><link href="https://marsggbo.github.io/blog/2026/llm-agent-memory/" rel="alternate" type="text/html" title="LLM Agent Memory 全景拆解：从 RAG 到 KV Cache 到参数写入，100+ 篇工作的方法演进与真实取舍"/><published>2026-05-19T00:00:00+00:00</published><updated>2026-05-19T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/llm-agent-memory</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/llm-agent-memory/"><![CDATA[<h1 id="llm-agent-memory-全景拆解从-rag-到-kv-cache-到参数写入100-篇工作的方法演进与真实取舍">LLM Agent Memory 全景拆解：从 RAG 到 KV Cache 到参数写入，100+ 篇工作的方法演进与真实取舍</h1> <blockquote> <p>原文：<a href="https://www.preprints.org/frontend/manuscript/cb9b2d755639339002ad1d6da7a9e230/download_pub">LLM Agent Memory: A Survey from a Unified Representation–Management Perspective</a> 作者：Zhenheng Tang, Xin He, Tiancheng Zhao 等（A*STAR CFAR / HKUST）</p> </blockquote> <hr/> <h2 id="1-先交代下背景为什么我们觉得这篇综述有写的必要">1. 先交代下背景：为什么我们觉得这篇综述有写的必要</h2> <p>你有没有发现一个现象：2024-2025 年几乎所有做 LLM agent 的团队都说自己”加了 memory”，但你去看它们的实现，有人在做向量库检索，有人在做 KV cache eviction，有人在做 LoRA 持续学习——<strong>表面同名，底层完全不是一个问题</strong>。</p> <p>这就导致了一个很尴尬的局面：reviewer 问你 “和 XXX memory system 比怎样”，你根本没法比，因为你们根本不在同一层解决问题。</p> <p>所以这篇综述不是又列一遍论文清单。它想做的事情是：<strong>把 memory 这个被滥用的词还原成一个可拆分、可比较、可设计的系统问题。</strong></p> <p>核心切法很清晰——任何 LLM memory 机制都可以拆成两个正交维度：</p> <ol> <li><strong>表示层 (Representation)</strong>：信息以什么形式存在——文本 token、KV cache 中间状态、还是模型参数？</li> <li><strong>管理层 (Management)</strong>：信息怎么建、怎么改、怎么取——construction、update、query 三件事哪个是瓶颈？</li> </ol> <p>下图是整个框架的总览：</p> <p><img src="/assets/img/posts/llm-agent-memory/fig1_framework.png" alt="LLM memory 统一框架"/></p> <p>上层是应用场景（多轮对话、文档问答），中间层是三种 memory 表示，下层是统一的管理接口抽象。这张图最有价值的地方不是分类本身，而是它揭示的一个事实：<strong>不同 memory 方向的研究者其实在用同一套管理语义（构建、更新、查询）去操作不同的存储后端。</strong></p> <p>一旦你接受了这个统一视角，很多看似不相关的工作就突然可以互相对话了。</p> <hr/> <h2 id="2-token-level-memory检索的演进远比你以为的深">2. Token-level Memory：检索的演进远比你以为的深</h2> <h3 id="21-rag-的真实演进路线从-naive-retrieval-到可控-pipeline">2.1 RAG 的真实演进路线：从 naive retrieval 到可控 pipeline</h3> <p>大部分人对 RAG 的印象还停留在”向量检索 + 拼 prompt”，但如果你把最近两年的方法按技术路线排开，会发现它其实已经经历了三代演进：</p> <p><strong>第一代：静态检索。</strong> BM25 或 dense embedding 做一次检索，把 top-k 结果直接拼到 prompt 里。问题很明显——query 和检索 granularity 不匹配的时候，结果很差。</p> <p><strong>第二代：Query 重构与增强。</strong> 这里出现了一批很重要的工作：</p> <ul> <li><strong>CoVe</strong> (Dhuliawala et al., 2023)：先让模型对自己的答案做 verification，再检索，减少 hallucination</li> <li><strong>Step-back prompting</strong> (Zheng et al., 2024)：把具体 query 抽象成更高层的问题再去检索，拿到互补证据</li> <li><strong>HyDE</strong> (Gao et al., 2022)：先让模型生成一个假设性答案，再用这个假设答案去检索真正的文档</li> </ul> <p>这一代的共同 insight 是：<strong>检索质量的瓶颈往往不在索引端，而在 query 端</strong>。一个精心重构的 query 比更好的 embedding 模型带来的提升大得多。</p> <p><strong>第三代：动态可控检索。</strong> 这才是真正让 RAG 从”管道”变成”系统”的一步：</p> <ul> <li><strong>DSP</strong> (Khattab et al., 2022)：把检索和生成拆成可组合的模块化 pipeline</li> <li><strong>FLARE</strong> (Jiang et al., 2023)：在生成过程中动态判断何时需要检索——只在 confidence 低的时候触发</li> <li><strong>Self-RAG</strong> (Asai et al., 2023)：训练模型本身输出 reflection token，让它自己决定是否检索、检索的结果是否有用、自己的生成是否 faithful</li> </ul> <p>Self-RAG 之所以重要，不只是因为它好用，而是它代表了一个范式转移：<strong>检索不再是外部系统强加的，而是模型自身 reasoning 流程的一部分</strong>。这就把 memory query 从 “engineering heuristic” 提升到了 “learnable decision”。</p> <h3 id="22-agentic-memory从-log-到-evolving-knowledge">2.2 Agentic Memory：从 log 到 evolving knowledge</h3> <p>RAG 处理的是相对静态的外部语料。Agentic memory 要解决的是一个更难的问题：<strong>agent 自己的交互历史怎么变成可复用的知识</strong>。</p> <p>关键区别在于：agent memory 不只是”存下来再查”，而是需要持续演化。</p> <p>看一下这条技术线的演进：</p> <ul> <li><strong>MemoryBank / RET-LLM</strong> (Zhong et al., 2024; Modarressi et al., 2023)：最早的做法，对对话历史做摘要存下来</li> <li><strong>HippoRAG</strong> (Gutiérrez et al., 2024)：引入 knowledge graph 结构，让记忆之间有 relation</li> <li><strong>A-MEM</strong> (Xu et al., 2025)：不只是存储，而是让 memory 自己去合并、裂变、重组</li> <li><strong>Synapse</strong> (Zheng et al., 2024) / <strong>SCM</strong> (Wang et al., 2024)：引入 self-reflection 和 memory blending</li> </ul> <p>这里最值得注意的 insight 是：<strong>区分一个 agentic memory 系统是不是真正有价值的，关键不在于它怎么存，而在于它怎么”忘”和怎么”变”。</strong></p> <p>单纯存得多没有用，甚至有害。一个 agent 跑 1000 轮之后，如果不做 memory evolution（选择性遗忘、冲突消解、抽象化），它的 memory 就会退化成一个高噪声的 log——这几乎必然拖垮 query 质量。</p> <p>Reflexion (Shinn et al., 2024)、BoT (Yang et al., 2024) 和 ReAct (Yao et al., 2022) 代表了另一条相关的线：<strong>让 agent 对自己的经验做 reasoning 和 self-reflection，把”经历”变成”教训”再存入 memory</strong>。这比 raw experience replay 有效得多。</p> <hr/> <h2 id="3-kv-cache-memory一个被低估的系统问题">3. KV Cache Memory：一个被低估的系统问题</h2> <h3 id="31-四条技术路线与它们的-os-对应物">3.1 四条技术路线与它们的 OS 对应物</h3> <p>这是我个人觉得这篇综述整理得最有价值的一节。KV cache 的工作数量爆炸式增长，但如果你站远一步看，几乎所有方法都可以映射到操作系统或数据库里的经典问题：</p> <table> <thead> <tr> <th style="text-align: left">KV Cache 方法类别</th> <th style="text-align: left">OS/DB 对应概念</th> <th style="text-align: left">代表工作</th> </tr> </thead> <tbody> <tr> <td style="text-align: left">Eviction &amp; dropping</td> <td style="text-align: left">Page replacement (LRU/LFU)</td> <td style="text-align: left">H₂O, StreamingLLM, FastGen, NACL</td> </tr> <tr> <td style="text-align: left">Merging &amp; compression</td> <td style="text-align: left">Data deduplication / compaction</td> <td style="text-align: left">CaM, D2O, similarity-based merge</td> </tr> <tr> <td style="text-align: left">Quantization &amp; low-rank</td> <td style="text-align: left">Lossy compression / tiered storage</td> <td style="text-align: left">KIVI, KVQuant, Gear</td> </tr> <tr> <td style="text-align: left">System-aware allocation</td> <td style="text-align: left">NUMA-aware / disaggregated memory</td> <td style="text-align: left">vLLM PagedAttention, Mooncake</td> </tr> </tbody> </table> <p><strong>H₂O</strong> (Zhang et al., 2023) 本质上做的事情是：统计每个 token 在过去所有 attention 计算中被关注的累积分数，优先保留”重度命中”的 token。这几乎就是 <strong>LFU (Least Frequently Used)</strong> 的翻版。</p> <p><strong>StreamingLLM</strong> (Xiao et al., 2024) 更有意思：它发现”第一个 token 的 attention sink 现象”——模型总会把一些 attention 分配给第一个 token，不管内容是什么。于是它永久保留前几个 token + 最近几个 token 的 KV，中间全部丢掉。这本质上就是 <strong>pinned pages + sliding window</strong>。</p> <p><strong>QUEST</strong> (Tang et al., 2024) 和 <strong>TokenSelect</strong> (Wu et al., 2025) 走的是 query-dependent selection：不是对所有 query 用同一套 cache，而是每次 attention 计算只选择和当前 query 最相关的 KV subset。这非常接近 <strong>demand paging</strong>——只有被”访问”到的 page 才加载进来。</p> <h3 id="32-为什么我说-kv-cache-不只是推理优化">3.2 为什么我说 KV cache 不只是”推理优化”</h3> <p>很多人会把 KV cache 压缩归类到”推理加速”里。但从这篇综述的视角看，它其实在回答一个更本质的问题：</p> <p><strong>当 context 越来越长，模型内部的 working memory 该怎么管理？</strong></p> <p>考虑一个实际场景：一个 agent 在执行 100 步的长任务。每步生成都需要 attend to 前面所有步骤的 KV cache。到第 80 步时，可能 step 3-20 的信息完全不会再被用到了，但 step 45 的关键决策需要一直被记住。</p> <p>这时候 KV cache management 面对的就不是”压缩”问题，而是一个真正的<strong>在线内存管理</strong>问题——你需要在 latency constraint 下做出 evict/keep/merge 的实时决策，而且这个决策的质量直接决定了 agent 后续推理的正确性。</p> <p>Table 1 给出的判断非常精准：<strong>Intermediate latent memory 的核心瓶颈在 update（什么时候丢什么），而不是 construction 或 query。</strong></p> <p><img src="/assets/img/posts/llm-agent-memory/table1_clean.png" alt="Decision matrix"/></p> <h3 id="33-steering-vectors一种被低估的行为记忆">3.3 Steering Vectors：一种被低估的”行为记忆”</h3> <p>这篇综述还把 steering vectors 归入了 intermediate latent memory，我觉得这个归类相当有洞察。</p> <p>Steering vectors 是什么？简单说就是一组方向向量，注入到模型中间层的 activation 里，可以持续改变模型的行为倾向（比如让它更 truthful、less toxic、或者更像某个 persona）。</p> <p>演进路线：</p> <ul> <li><strong>PPLM</strong> (Dathathri et al.)：最早的尝试，用梯度引导 hidden states</li> <li><strong>Turner et al., 2023</strong>：contrastive 方法——用”好行为”和”坏行为”的 activation 差作为 steering direction</li> <li><strong>Arditi et al., 2024</strong>：把 refusal behavior 定位到具体的 steering direction 并移除</li> <li><strong>Hernandez et al., 2023</strong>：optimization-based，用单个样本就能学出 steering vector</li> </ul> <p>为什么说它是”行为记忆”？因为 steering vector 本质上编码的是<strong>持久的行为偏好</strong>（不是事实知识），以一种不需要在每次推理时重新指定的方式存在于模型里。它和 KV cache 的区别是：KV cache 存的是”当前会话的上下文状态”，steering vector 存的是”跨会话的行为倾向”。</p> <p>但 steering vector 目前的局限也很明显：contrastive method 依赖精心构造的对比数据，很容易捕获到 spurious correlation 而非 causal direction。这也是为什么 probe-based 和 low-shot 方法（Li et al., 2024; Cao et al., 2024）在尝试解决鲁棒性问题。</p> <hr/> <h2 id="4-parameter-level-memory写入成本决定了研究格局">4. Parameter-level Memory：写入成本决定了研究格局</h2> <h3 id="41-为什么这条线的工作最碎">4.1 为什么这条线的工作最”碎”</h3> <p>如果你看参数层面的 memory 工作，会发现一个有趣现象：<strong>研究方向极其分散</strong>——continual learning、knowledge editing、PEFT、model merging、task arithmetic 都算，但大家之间几乎不互引。</p> <p>原因在于：参数写入的成本太高了，没有任何一个方法能做到”对所有类型的知识都通用”，于是每种方法都只能在自己的 niche 里深耕：</p> <ul> <li><strong>EWC / TaSL / POCL</strong>：防灾难性遗忘——先算哪些参数对旧知识重要，再限制这些参数的更新幅度</li> <li><strong>Knowledge editing</strong> (ROME, MEMIT)：精准改一个事实，但只能处理”单跳事实”，multi-hop 效果就崩</li> <li><strong>LoRA / PEFT</strong>：轻量级适配，但本质上是在编码 task-specific bias，不是 factual memory</li> <li><strong>Model merging</strong> (TIES, DARE, Model Breadcrumbs)：把多个 fine-tuned model 合成一个，用参数空间的向量运算解决</li> </ul> <p>这些方法看似差异巨大，但如果用这篇综述的框架来看，它们都在回答同一个问题：</p> <p><strong>如何在不破坏已有能力的前提下，让参数空间对新信息产生可控的、局部的响应。</strong></p> <h3 id="42-task-arithmetic-与-model-merging参数空间的向量记忆">4.2 Task Arithmetic 与 Model Merging：参数空间的”向量记忆”</h3> <p>这里面我觉得最有启发性的方向是 <strong>task arithmetic</strong>（Ilharco et al., 2023）。</p> <p>它的核心思想很简单但很漂亮：把 fine-tuning 后的模型减去 base model，得到一个”任务向量”（task vector）。这个向量可以被加减组合——<strong>加上它就获得某种能力，减去它就移除某种能力</strong>。</p> <p>这意味着参数 memory 在某种意义上可以被”可编辑化”了。你不需要重新训练，只需要做向量运算就能组合多种知识。</p> <p>当然，现实没这么美好。Task vector 之间会有 interference（两个任务向量加在一起可能互相抵消），这也催生了 TIES（修剪冲突参数）、DARE（随机 drop 低幅度参数）等后续工作。</p> <p>但方向是对的：<strong>让参数层面的写入变得更 composable、更 reversible，是参数 memory 最有前景的演进方向之一。</strong></p> <hr/> <h2 id="5-三层之间的真正-insight瓶颈转移定律">5. 三层之间的真正 insight：瓶颈转移定律</h2> <p>如果你把上面三节的分析放在一起看，会发现一个非常有意思的规律：</p> <p><strong>Memory 的写入成本和查询成本成反比。写入越便宜的 memory，查询越贵；写入越贵的 memory，查询越廉价。</strong></p> <p>具体来说：</p> <ul> <li><strong>Token memory</strong>：写入几乎无成本（直接存文本），但查询极贵（要在海量文档中精确检索）</li> <li><strong>KV cache</strong>：写入成本中等（forward pass 自然产生），但维护成本高（要不断 evict/merge/compress）</li> <li><strong>Parameter memory</strong>：写入极贵（需要 fine-tune 或 editing），但查询几乎为零（推理时自动激活，不需要额外检索步骤）</li> </ul> <p>这不是巧合。这和计算机体系结构里的 <strong>存储层次 (memory hierarchy)</strong> 规律完全一致：寄存器快但小且贵，DRAM 大但慢，磁盘最大最便宜但最慢。</p> <p>所以论文给出的 practical guideline 非常实用：</p> <p><img src="/assets/img/posts/llm-agent-memory/conclusion_guidelines.png" alt="设计指南"/></p> <p>翻译成工程语言就是：</p> <ul> <li><strong>频繁变动的信息</strong> → token memory（向量库、RAG）</li> <li><strong>会话内的推理状态</strong> → KV cache（intermediate latent）</li> <li><strong>稳定的长期知识</strong> → 参数（fine-tune、model merge）</li> </ul> <p><strong>一个健壮的 LLM agent 系统，最终一定是多层 memory 的组合</strong>，就像操作系统同时有 L1/L2 cache、RAM、SSD 一样。问题只是：谁来做跨层的 routing 和 scheduling。</p> <hr/> <h2 id="6-未来方向不是更-fancy-的方法而是更成熟的系统思维">6. 未来方向：不是更 fancy 的方法，而是更成熟的系统思维</h2> <h3 id="61-跨层-memory-routing-与联合调度">6.1 跨层 Memory Routing 与联合调度</h3> <p>目前几乎所有系统都是各层独立工作：RAG 是一套检索逻辑，KV cache 有自己的 eviction policy，参数更新走单独的 fine-tuning pipeline。三者都在试图让模型”记住对的信息”，但<strong>没有人在做统一的调度</strong>。</p> <p>想象一下：如果一个 agent 发现某条信息被 RAG 检索了 50 次，是不是应该考虑把它”下沉”到 KV cache（prefix caching）甚至直接 consolidate 到参数里？反过来，如果某个参数化的知识过时了，是不是应该”浮上来”到 token memory 层用新数据覆盖？</p> <p>这就是 <strong>memory tiering</strong>——数据库和存储系统里早就成熟的东西，但在 LLM 领域几乎没有工作在认真做。</p> <h3 id="62-从-os-借鉴的不只是-eviction-policy">6.2 从 OS 借鉴的不只是 eviction policy</h3> <p>这篇综述在 Appendix C 明确指出了三个跨学科迁移方向：</p> <ol> <li><strong>OS page management → KV cache</strong>：不只是 LRU/LFU，还有预取（prefetch）、copy-on-write、huge pages 等概念都有对应</li> <li><strong>数据库 query optimization → RAG query</strong>：Cost-based optimizer、query plan selection、materialized view 都可以借鉴</li> <li><strong>分布式系统 → 多节点 memory 协同</strong>：Mooncake、DistServe 这类工作已经开始把 KV cache 做成 disaggregated service</li> </ol> <p>最有潜力的方向可能是 <strong>“LLM memory 也该有 garbage collector”</strong>。目前 agent memory 几乎都是 append-only 的——信息只增不减（或者简单地按时间丢弃）。但一个真正长期运行的 agent 需要类似 GC 的机制：定期检测 unreachable 或 contradicted 的记忆碎片并回收。</p> <h3 id="63-unified-traininginference模糊训练和推理的边界">6.3 Unified Training–Inference：模糊训练和推理的边界</h3> <p>这是最激进但也最有前景的方向。</p> <p>目前 LLM 的训练和推理是严格分离的：训练时学知识，推理时只能读。但长期运行的 agent 必然需要在使用过程中不断学习新东西。</p> <ul> <li><strong>Continual learning</strong>（EWC, TaSL）已经在做这件事，但成本太高</li> <li><strong>PEFT + online fine-tuning</strong> 能做轻量级适配，但精度有限</li> <li><strong>Memory consolidation</strong>——像人类睡眠时做的那样，把 episodic memory 定期整合进 semantic memory（参数）</li> </ul> <p>未来可能出现的架构：agent 在推理时用 token memory + KV cache 做”快思考”，定期（比如空闲时）把高频使用的 memory pattern consolidate 成参数更新，实现真正的”学习-使用”闭环。</p> <hr/> <h2 id="7-看完之后的一些个人-take">7. 看完之后的一些个人 take</h2> <ol> <li> <p><strong>Memory research 的下一个爆发点不在任何单层，而在跨层协同</strong>。谁先把 memory routing 做好，谁就定义了下一代 agent 基础设施。</p> </li> <li> <p><strong>KV cache 方向已经进入”方法论内卷期”</strong>。eviction + merging + quantization 排列组合可以无限水文章，但真正有影响力的下一步应该是 <strong>system-level memory management</strong>（像 vLLM PagedAttention 那样改变整个范式的东西）。</p> </li> <li> <p><strong>Agentic memory 的关键不在”记住”而在”忘记”和”重组”</strong>。A-MEM、Synapse 这类做 memory evolution 的工作比单纯做 memory bank 有前途得多。</p> </li> <li> <p><strong>Steering vectors 作为”行为记忆”被严重低估了</strong>。如果未来每个 user 的 persona preference 都能编码成一个 steering vector 并在推理时注入，这就是真正 zero-cost 的个性化——不需要 per-user fine-tuning。</p> </li> <li> <p><strong>系统领域的同学应该认真看 LLM memory</strong>。这里面大量问题（cache replacement、tiered storage、query optimization、garbage collection）都是你们已经解决过的——只是换了个 domain。</p> </li> </ol> <hr/> <p>写到这里差不多了。如果让我用一句话总结这篇综述给我最大的启发：</p> <p><strong>LLM memory 不是一个功能模块的设计问题，而是一个完整的存储系统设计问题——带着所有存储系统该有的层次、调度、一致性和生命周期管理。</strong></p> <p>谁先用系统的视角去设计 memory，谁就能做出真正能长期运行的 agent。</p>]]></content><author><name></name></author><category term="LLM"/><category term="Agent"/><category term="Memory"/><category term="KV Cache"/><category term="RAG"/><category term="论文解读"/><summary type="html"><![CDATA[LLM Agent Memory 全景拆解：从 RAG 到 KV Cache 到参数写入，100+ 篇工作的方法演进与真实取舍]]></summary></entry><entry><title type="html">LLMRouterBench：当所有 routing 方法被拉到同一起跑线，结果有些尴尬</title><link href="https://marsggbo.github.io/blog/2026/llmrouterbench/" rel="alternate" type="text/html" title="LLMRouterBench：当所有 routing 方法被拉到同一起跑线，结果有些尴尬"/><published>2026-05-19T00:00:00+00:00</published><updated>2026-05-19T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/llmrouterbench</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/llmrouterbench/"><![CDATA[<h1 id="llmrouterbench当所有-routing-方法被拉到同一起跑线结果有些尴尬">LLMRouterBench：当所有 routing 方法被拉到同一起跑线，结果有些尴尬</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2601.07206">LLMRouterBench: A Massive Benchmark and Unified Framework for LLM Routing</a> 作者：Hao Li, Yiqun Zhang 等（上海 AI Lab）</p> </blockquote> <hr/> <h2 id="1-前言llm-routing-到底在解决什么问题">1. 前言：LLM Routing 到底在解决什么问题</h2> <p>你有没有想过一个很现实的问题：</p> <p>现在模型这么多——GPT-5、DeepSeek-R1、Qwen3-235B、Claude 4、Gemini 2.5 Pro——每个模型都有自己擅长和拉胯的领域。那是不是可以搞一个 router，每来一个 query 就智能选一个最合适的模型？</p> <p>这就是 <strong>LLM routing</strong> 的核心想法。听起来太对了。Model complementarity 是显而易见的——没有任何一个模型在所有任务上都是最好的（这个事实在本文的实验里也再次被确认了）。</p> <p>但问题在于：<strong>这个方向的研究现状比较混乱。</strong> 大家各用各的模型池、各用各的评测数据集、各用各的指标，你说你比别人好 2%，但你们用的模型都不一样，怎么比？</p> <p>所以这篇 LLMRouterBench 做的事情很直接：<strong>把所有主流 routing 方法拉到同一个模型池、同一个数据集、同一套评价标准下，重新跑一遍。</strong></p> <p>结果嘛……有些发现挺让人清醒的。</p> <hr/> <h2 id="2-benchmark-长什么样">2. Benchmark 长什么样</h2> <p>先快速过一下 LLMRouterBench 的规模：</p> <ul> <li><strong>400K+ instances</strong>，来自 21 个数据集（涵盖数学、代码、逻辑、知识、情感、指令遵循、工具使用）</li> <li><strong>33 个模型</strong>：20 个 ~7B 轻量模型（performance-oriented setting）+ 13 个 flagship 模型（performance-cost setting）</li> <li><strong>10 个 routing baseline</strong>：RouterDC、EmbedLLM、MODEL-SAT、GraphRouter、Avengers、HybridLLM、FrugalGPT、RouteLLM、Avengers-Pro、OpenRouter</li> <li>花了 ~1000 GPU hours + $2,771 API cost 收集数据</li> </ul> <p><img src="/assets/img/posts/llmrouterbench/001_Figure_1.png" alt="LLMRouterBench 总览"/></p> <p>它支持两种 routing 范式：</p> <ol> <li><strong>Performance-oriented routing</strong>：只看准确率，不管成本——从一堆相近大小的模型里挑最好的</li> <li><strong>Performance-cost tradeoff routing</strong>：同时考虑准确率和推理成本——在更好和更便宜之间找平衡</li> </ol> <p>这个设定比之前的 benchmark 合理得多。之前的 RouterBench 只有 8 个数据集 + GPT-4/Claude v1 这种老模型，RouterArena 甚至各 router 用的模型池都不一样。LLMRouterBench 终于把基本的公平性给补齐了。</p> <hr/> <h2 id="3-实验结果几个让人清醒的发现">3. 实验结果：几个让人清醒的发现</h2> <h3 id="31-model-complementarity-是真实存在的routing-的前提成立">3.1 Model complementarity 是真实存在的——routing 的前提成立</h3> <p>先确认好消息：模型确实互补。</p> <p><img src="/assets/img/posts/llmrouterbench/003_Figure_3.png" alt="模型在不同领域的表现"/></p> <ul> <li>数学：Intern-S1-mini 和 Qwen3-8B 领先</li> <li>代码：Fin-R1 和 Qwen-Coder 最强</li> <li>逻辑：DS-Qwen3 和 MiniCPM 占优</li> <li>情感：Gemma-2-it 最好</li> </ul> <p>没有一个模型通吃所有 domain。这说明 routing 的基本前提——”不同问题交给不同专家”——是成立的。</p> <h3 id="32-但所有-routing-方法的表现几乎一样">3.2 但所有 routing 方法的表现……几乎一样</h3> <p>这才是本文最关键的发现。当你把 5 种 performance-oriented routing 方法拉到同一起跑线上看：</p> <p><img src="/assets/img/posts/llmrouterbench/004_Figure_4.png" alt="各方法性能对比"/></p> <p>EmbedLLM (71.24)、GraphRouter (70.29)、MODEL-SAT (71.88)、Avengers (71.94)——<strong>差距只在 1-2 个点之内。</strong></p> <p>更值得玩味的是：<strong>Avengers 用的只是 k-means clustering + 简单的聚类分配，完全不需要训练神经网络</strong>。它的效果和需要 7B/14B 参数训练的 RouterDC、MODEL-SAT 几乎一样好。</p> <p>这说明什么？论文给的解释我觉得很到位：</p> <p><strong>目前 routing 方法能学到的，主要是粗粒度的 domain structure</strong>（比如”这是数学题应该给 Qwen3”、”这是代码题应该给 Fin-R1”），而不是 fine-grained 的 instance-level 判断。证据就是这些方法的表现都非常接近 Dataset Oracle（按数据集级别选最优模型）。</p> <p>说白了：<strong>现有的 router 本质上只是在做 task classification，不是真正的 per-query routing。</strong></p> <h3 id="33-距离-oracle-还有巨大-gap问题出在-model-recall-failure">3.3 距离 Oracle 还有巨大 gap——问题出在 model-recall failure</h3> <p>虽然各方法之间差不多，但它们和 Oracle（per-instance 选最佳模型）之间的差距还是很大的：Gap@Oracle 在 20-33% 之间。</p> <p>为什么？论文分析了一个很关键的现象——<strong>model-recall failure</strong>：</p> <p><img src="/assets/img/posts/llmrouterbench/005_Figure_5.png" alt="Query 难度分布与路由准确率"/></p> <p>当一个 query 只有 1-3 个模型答对时（占测试集约 11.9%），现有 router 的准确率只有 ~24%。也就是说：<strong>越是需要 routing 发挥作用的”难题”，router 反而越不行。</strong></p> <p>这其实挺反直觉的：routing 的全部价值就应该体现在”找到那个唯一答对的模型”上。但目前的方法在这种最关键的场景下，做得最差。</p> <h3 id="34-embedding-模型几乎不影响结果">3.4 Embedding 模型几乎不影响结果</h3> <p>很多 routing 方法（GraphRouter、EmbedLLM、Avengers）都依赖 embedding 模型来表征 query。你可能会觉得，换个更好的 embedding 应该能提升 routing 质量吧？</p> <p>实验结论：<strong>几乎没差别。</strong></p> <p>把 7B 的 gte-qwen2-7B-instruct 换成 22.7M 参数的 all-MiniLM-L6-v2（小了 300 倍），routing 性能差异不到 2 个点。</p> <p>这进一步说明：<strong>当前 routing 的瓶颈不在 query representation，而在于 routing decision mechanism 本身。</strong> 你再好的 embedding 也救不了一个只能做 domain-level classification 的 router。</p> <h3 id="35-模型池越大不一定越好精选子集更有效">3.5 模型池越大不一定越好——精选子集更有效</h3> <p>这个发现对工程实践很有价值：</p> <p><img src="/assets/img/posts/llmrouterbench/006_Figure_6.png" alt="模型池大小 vs Oracle 性能"/></p> <ul> <li>随机加模型到池子里，收益递减非常明显——从 2 个到 6 个模型时 Oracle 提升很大，之后几乎就平了</li> <li><strong>精心挑选的 top-4（Qwen3-8B, NVIDIA-Nemo, DS-Qwen3, GLM-Z1）就能达到接近 20 个模型随机组合的效果</strong></li> </ul> <p>启示：与其维护一个巨大的模型池，不如花精力做好 <strong>model curation</strong>——选一组互补性强的模型子集。</p> <hr/> <h2 id="4-performance-cost-场景商业-router-翻车了">4. Performance-Cost 场景：商业 router 翻车了</h2> <p>在 performance-cost 场景下，结果更有意思：</p> <p><img src="/assets/img/posts/llmrouterbench/007_Figure_7.png" alt="各方法性能增益与成本节省"/></p> <ul> <li><strong>Avengers-Pro</strong>：+4% 准确率、节省 31.7% 成本，是唯一一个同时做到”更好”且”更便宜”的方法</li> <li><strong>RouteLLM</strong>：+2.6% 准确率、节省 11.4% 成本</li> <li><strong>OpenRouter（商业 router）</strong>：准确率反而下降 24.7%，成本还没省下来（N/A）</li> </ul> <p>没错，<strong>花钱用的商业 routing 服务，效果还不如直接用 Best Single 模型（GPT-5）。</strong></p> <p>Pareto frontier 分析更直观：</p> <p><img src="/assets/img/posts/llmrouterbench/008_Figure_8.png" alt="Pareto frontier"/></p> <p>Avengers-Pro 几乎霸占了整条 Pareto frontier（ParetoDist = 0.001），而 OpenRouter 离 frontier 最远（0.394）。也就是说：<strong>一个基于 k-means clustering 的免训练方法，在性能-成本 tradeoff 上全面碾压了商业 router。</strong></p> <p>这个结论对行业的冲击挺大的。</p> <hr/> <h2 id="5-为什么-llm-routing-这个方向依然值得做">5. 为什么 LLM Routing 这个方向依然值得做</h2> <p>看完上面的结果你可能会想：routing 是不是不行了？</p> <p>不是。恰恰相反——<strong>Oracle 和现有方法之间巨大的 gap 说明这个方向远没有到头</strong>。问题只是目前的方法还太”粗”了。</p> <p>几个我觉得值得继续挖的方向：</p> <p><strong>1. 从 domain-level routing 到 instance-level routing</strong></p> <p>当前 router 基本只学会了”数学题给数学模型”这种粗粒度分配。但 Oracle 告诉我们：即使是同一个 domain 内，不同 instance 的最优模型也差异巨大。如何做到 per-query 的精准 routing——尤其是那些只有 1-2 个模型能答对的 hard cases——这是核心挑战。</p> <p><strong>2. Model curation 和 routing 应该联合优化</strong></p> <p>论文已经证明了：精选子集 &gt; 简单堆模型。但目前没人在认真做”routing-aware model selection”——先选一组互补性最强的模型子集，再针对这个子集训练 router。这两件事应该是 joint 的。</p> <p><strong>3. 超越 accuracy/cost：latency 是第三个维度</strong></p> <p>论文还展示了一个 latency 分析：Qwen3-Thinking 和 GLM-4.6 在准确率和成本上差不多，但 latency 差了 8 倍（262s vs 32s）。用户体验角度看，这完全是两个东西。Performance-cost-latency 三目标优化几乎没人做。</p> <p><strong>4. Routing 需要 uncertainty estimation</strong></p> <p>Model-recall failure 的本质是：router 不知道自己不确定。如果 router 能估计自己的 confidence，在低 confidence 时尝试多个模型或走 ensemble，那 recall rate 会好很多。</p> <p><strong>5. 训练数据效率问题</strong></p> <p>RouterDC 需要 7B 参数训练，MODEL-SAT 需要 14B，但效果和不需要训练的 Avengers 一样。这说明当前的训练方式效率极低——大部分参数都在学 domain boundary 而不是 instance-level signal。</p> <hr/> <h2 id="6-对工程实践的建议">6. 对工程实践的建议</h2> <p>如果你现在要在产品里加 LLM routing，这篇论文的结论其实给了非常实操的指导：</p> <ol> <li> <p><strong>别急着上复杂 router</strong>。一个简单的 k-means clustering（Avengers/Avengers-Pro 方案）就能达到 SOTA 水平，而且免训练、轻量、好维护。</p> </li> <li> <p><strong>模型池不要贪多</strong>。4-6 个精选的互补模型 &gt; 20 个随机堆的模型。把精力放在选对模型上，而不是 router 算法上。</p> </li> <li> <p><strong>别信商业 router 的宣传</strong>。至少 OpenRouter 在这个 benchmark 下的表现是显著低于 Best Single 的。在你用之前，先验证它在你的 use case 上是否真的比直接调 GPT-5 好。</p> </li> <li> <p><strong>Embedding 模型不值得纠结</strong>。一个 22M 参数的小 embedding 模型就够用了，不需要上 7B 的 embedding backbone。</p> </li> </ol> <hr/> <h2 id="7-结尾">7. 结尾</h2> <p>回到最开始的问题：LLM routing 这个方向有前景吗？</p> <p>我的判断是：<strong>前景巨大，但当前方法还太初级。</strong></p> <p>Oracle 告诉我们上限在 91.64%（performance-oriented）和 85.66%（performance-cost），而现有最好的方法只到 71-68%。这个 20+ 个点的 gap 全是研究机会。</p> <p>但这篇 benchmark 也给出了一个很清醒的 warning：<strong>不要假装自己在做 fine-grained routing，实际上只是在做 domain classification。</strong> 如果你的方法不能在 “只有 1 个模型答对” 的 hard cases 上显著超过 random，那你本质上没有解决 routing 的核心问题。</p> <p>这也是整个行业需要直面的核心矛盾：<strong>model complementarity 是真实的、巨大的，但当前的 routing mechanism 远没有 good enough 去 exploit 它。</strong></p> <p>谁能解决这个 gap，谁就定义了下一代 LLM serving 的基础设施。</p>]]></content><author><name></name></author><category term="LLM"/><category term="Routing"/><category term="Benchmark"/><category term="论文解读"/><summary type="html"><![CDATA[LLMRouterBench：当所有 routing 方法被拉到同一起跑线，结果有些尴尬]]></summary></entry><entry><title type="html">EuroSys’26 | TokenFlow：让 LLM 流式输出真正「流」起来</title><link href="https://marsggbo.github.io/blog/2026/tokenflow/" rel="alternate" type="text/html" title="EuroSys’26 | TokenFlow：让 LLM 流式输出真正「流」起来"/><published>2026-05-17T01:00:00+00:00</published><updated>2026-05-17T01:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/tokenflow</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/tokenflow/"><![CDATA[<h1 id="eurosys26--tokenflow让-llm-流式输出真正流起来">EuroSys’26 | TokenFlow：让 LLM 流式输出真正「流」起来</h1> <blockquote> <p>原文：<a href="https://dl.acm.org/doi/10.1145/3689031.3696083">TokenFlow: Responsive LLM Text Streaming Serving under Request Burst via Preemptive Scheduling</a></p> </blockquote> <hr/> <p>用过 ChatGPT 或者任何一个 AI 对话产品的人都有同样的体验：你发出一条消息，页面上的文字一个一个「流」出来，就像打字机一样。这种「流式输出」（text streaming）是现代 LLM 交互的标配，给人一种 AI 正在「思考」的感觉，体验比等待一整段文字突然出现好得多。</p> <p>但如果你仔细观察，有时候这个「流」并不稳定——刚开始等了很久才出现第一个字（TTFT 高），或者文字时快时慢、突然卡顿（TBT 不稳）。这篇来自 EuroSys 2026 的论文 TokenFlow 就是要彻底解决这个问题。</p> <h2 id="1-流式输出的两难困境">1. 流式输出的两难困境</h2> <p>LLM 流式服务的核心矛盾，可以用两个指标来描述：</p> <ul> <li><strong>TTFT（Time-to-First-Token）</strong>：用户发出请求到看到第一个 token 的延迟。越低越好，这决定了用户感知到的「响应速度」。</li> <li><strong>TBT（Time-Between-Tokens）</strong>：相邻两个 token 之间的间隔。需要足够稳定，避免出现「卡一下、蹦一下」的感觉。</li> </ul> <p>问题在于，<strong>这两个目标天生矛盾</strong>。</p> <p>当大量请求同时涌入（request burst），系统资源有限，服务器必须做取舍。SGLang 等主流框架采用 FCFS（先来先服务）策略：正在处理的请求继续跑，新来的请求乖乖排队。结果是——</p> <ul> <li>正在跑的请求：生成速度远超用户阅读速度（几十 tokens/秒 vs 用户 5 tokens/秒），白白浪费算力</li> <li>排队等待的请求：TTFT 飙升到 10 秒以上，用户以为系统挂了</li> </ul> <p>如下图所示，SGLang 在 burst 负载下，P99 TTFT 轻松突破 30 秒，而生成速度却远超用户阅读需求——两头都没做好。</p> <p><img src="/assets/img/posts/tokenflow/fig02.png" alt="SGLang burst 问题"/></p> <h2 id="2-被忽视的优化机会token-buffer">2. 被忽视的优化机会：Token Buffer</h2> <p>这里有一个关键观察，也是 TokenFlow 的核心洞察：</p> <p><strong>LLM 的生成速度远远快于用户的阅读速度。</strong></p> <p>实测数据（Figure 1）显示，用户平均阅读速度约 3-5 tokens/秒，而模型生成速度可以达到 30-100 tokens/秒，相差一个数量级。</p> <p><img src="/assets/img/posts/tokenflow/fig01.png" alt="token 消费速度分布"/></p> <p>这意味着，服务端可以提前生成一批 token 存在 <strong>output buffer</strong> 里，用户慢慢消费，系统不用死等用户。只要 buffer 不空，用户体验就是流畅的。</p> <p>这个 buffer 的存在，给了系统一个「喘息空间」——<strong>可以暂停一个正在跑的请求，先处理一个新来的请求的 prefill（生成第一个 token），只要 buffer 里还有剩余 token，暂停的那个用户不会察觉任何异常。</strong></p> <p>这就是 preemptive scheduling（抢占式调度）的机会所在。</p> <h2 id="3-tokenflow-系统设计">3. TokenFlow 系统设计</h2> <p>TokenFlow 的整体架构如下图所示，核心由两部分组成：<strong>Buffer-Aware 调度器</strong> + <strong>Proactive KV Cache 管理器</strong>。</p> <p><img src="/assets/img/posts/tokenflow/fig03.png" alt="TokenFlow 系统架构"/></p> <h3 id="31-buffer-aware-抢占式调度">3.1 Buffer-Aware 抢占式调度</h3> <p>传统抢占式调度（如 Andes）的思路是：新请求来了就强制暂停旧请求。问题是这会导致频繁的 context switch，干扰正在运行的请求。</p> <p>TokenFlow 更聪明——它引入了 <strong>Virtual Buffer Counter</strong>，实时追踪每个请求的：</p> <ul> <li>当前 buffer 中还剩多少 token（buffer occupancy）</li> <li>用户的 token 消费速率（通过请求头或估计得到）</li> </ul> <p>基于这两个信息，调度器动态决定：<strong>什么时候该暂停哪个请求</strong>。</p> <p>具体来说，调度策略分两步（如 Figure 7 所示）：</p> <ol> <li><strong>Soft Preemption</strong>：当某请求的 buffer 充足（用户短期内不会体验到卡顿），就把它暂时移出运行批次，腾出 GPU 资源给队列里等待的请求做 prefill</li> <li><strong>Hard Preemption</strong>：当 GPU 内存即将耗尽，才触发强制抢占 + KV cache 卸载到 CPU</li> </ol> <p><img src="/assets/img/posts/tokenflow/fig06.png" alt="Buffer-Aware 调度示意图"/></p> <p>这样的设计，让系统在”稳住老用户”和”快速响应新用户”之间找到最优平衡点。</p> <h3 id="32-proactive-kv-cache-管理">3.2 Proactive KV Cache 管理</h3> <p>这是 TokenFlow 另一个关键创新。</p> <p>传统系统（包括 Andes）的 KV cache 管理是<strong>被动的（reactive）</strong>：只有当 GPU 内存快满时，才开始把 KV cache 卸载（evict）到 CPU；需要恢复请求时，再从 CPU 加载（load）回 GPU。这个过程 I/O 开销极大，会导致系统从 compute-bound 变成 I/O-bound。</p> <p>TokenFlow 的做法是<strong>主动的（proactive）</strong>：</p> <p><strong>Write-Through 策略</strong>：每次生成新 token，就立刻把对应的 KV cache 同步写到 CPU 内存（类似 CPU cache 的 write-through 策略）。这样，当真正需要抢占请求时，KV cache 已经在 CPU 里了，evict 操作几乎是零开销。</p> <p><img src="/assets/img/posts/tokenflow/fig08.png" alt="Write-Through vs Write-Back 对比"/></p> <p><strong>Chunked + Overlap 策略</strong>：KV cache 的 load/evict 操作被拆成小块（chunks），与 GPU 计算并行执行，利用 PCIe 带宽不影响 CUDA 核心的特点，做到 I/O 和计算的充分重叠。</p> <p><img src="/assets/img/posts/tokenflow/fig10.png" alt="Load-Evict Overlap 技术"/></p> <p>具体实现上，TokenFlow 基于 SGLang 构建，用 CUDA streams + Python 多线程实现了完全重叠的计算与内存操作，代码约 4000 行 Python。</p> <h2 id="4-新的评估指标effective-throughput--qos">4. 新的评估指标：Effective Throughput + QoS</h2> <p>这篇论文还提出了一个重要观点：<strong>传统的 tokens/second 吞吐量指标不足以评估流式服务。</strong></p> <p>原因是：如果服务器狂生 token 但用户 buffer 已经满了，那些多余的 token 对用户没有任何价值，反而浪费了 GPU 算力。</p> <p>TokenFlow 定义了 <strong>Effective Throughput</strong>：只统计用户实际消费到的 token，而不是系统生成的所有 token。这个指标综合了 TTFT、TBT 稳定性和 buffer 利用率，更贴近真实的用户体验。</p> <p>同时，论文定义了 <strong>QoS（Quality of Service）</strong> 指标，综合考虑：</p> <ul> <li>首 token 等待惩罚（TTFT 相关）</li> <li>Playback stall 惩罚（TBT 过长时的卡顿）</li> <li>Token utility（实际被消费的比例）</li> </ul> <p><img src="/assets/img/posts/tokenflow/fig05.png" alt="QoS 指标体系"/></p> <h2 id="5-实验效果">5. 实验效果</h2> <p>实验在多种硬件（RTX 4090、A6000、H200）和模型（Llama3-8B、Qwen2.5-8B/32B）上进行，基线包括 SGLang 和 Andes。</p> <p><strong>使用真实 workload（BurstGPT trace + 内部运营商 trace）的结果：</strong></p> <ul> <li>Effective Throughput：<strong>平均提升 45.1%（A6000），37.1%（H200）</strong>，最高达 82.5%</li> <li>Mean TTFT：<strong>平均降低 52.6%</strong>，P50 最高降低 88.7%</li> </ul> <p><img src="/assets/img/posts/tokenflow/fig12.png" alt="H200 端到端性能对比"/></p> <p><strong>Burst 场景下（合成 workload）：</strong></p> <p>TokenFlow 在 burst 到来时明显减少了排队请求数量，同时保持更高的并发运行请求数——说明系统在压力下能更高效地利用 GPU。</p> <p><img src="/assets/img/posts/tokenflow/fig14.png" alt="Burst 场景排队请求变化"/></p> <p><strong>消融实验（Table 2）</strong>验证了两个核心模块的贡献：单独使用 buffer-aware 调度器或 proactive KV cache 管理都有提升，但两者协同才能达到最优效果——co-design 的价值不言而喻。</p> <p><img src="/assets/img/posts/tokenflow/table2.png" alt="消融实验"/></p> <h2 id="6-一点感想">6. 一点感想</h2> <p>TokenFlow 这篇论文最打动我的地方，是它对”token buffer”这个平时被忽视的结构的洞察。大家都知道 LLM 生成比用户读快，但把这个速度差转化成调度的优化空间，需要一个跨越层次的系统思维。</p> <p><strong>Buffer-aware 调度 + Proactive KV cache 管理</strong>，这两个设计形成了一个自洽的闭环：调度器知道什么时候可以安全抢占（因为 buffer 里还有余量），KV cache 管理器提前把数据备好让抢占近乎无代价，两者协作，最终让”流式输出”真正流起来。</p> <p>这个思路对做 LLM serving 的朋友很有参考价值——不只是优化单个请求的延迟，而是从整个 request burst 的角度来看资源分配和调度策略。下一步自然的延伸问题是：如果用户的阅读速率是动态变化的（比如暂停了 App），或者有不同 SLA 的混合请求，buffer-aware 策略该如何自适应调整？这应该是后续工作有意思的方向。</p>]]></content><author><name></name></author><category term="LLM Inference"/><category term="System"/><category term="LLM Serving"/><category term="Preemptive Scheduling"/><category term="KV Cache"/><category term="Streaming"/><category term="EuroSys"/><summary type="html"><![CDATA[EuroSys’26 | TokenFlow：让 LLM 流式输出真正「流」起来]]></summary></entry><entry><title type="html">KBS’21 | 我写的 AutoML 综述被引 2700+ 次，今天来聊聊这篇文章的来龙去脉</title><link href="https://marsggbo.github.io/blog/2026/automl-survey/" rel="alternate" type="text/html" title="KBS’21 | 我写的 AutoML 综述被引 2700+ 次，今天来聊聊这篇文章的来龙去脉"/><published>2026-05-17T00:00:00+00:00</published><updated>2026-05-17T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/automl-survey</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/automl-survey/"><![CDATA[<h1 id="kbs21--我写的-automl-综述被引-2700-次今天来聊聊这篇文章的来龙去脉">KBS’21 | 我写的 AutoML 综述被引 2700+ 次，今天来聊聊这篇文章的来龙去脉</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/1908.00709">AutoML: A Survey of the State-of-the-Art</a></p> </blockquote> <hr/> <h2 id="1-一个不太谦虚的开场">1. 一个不太谦虚的开场</h2> <p>这篇综述是我读博早期（2019年）写的，发表在 Knowledge-Based Systems（KBS），现在谷歌学术上显示引用量已经超过 <strong>2700 次</strong>。每次看到这个数字都有点不真实的感觉——一篇综述能有这种引用量，说明当时确实踩在了一个节点上：<strong>整个 AutoML/NAS 领域刚刚爆发，大家都急需一个系统性的梳理</strong>。</p> <p>今天想和大家聊聊这篇文章在写什么、当时的研究背景是什么，以及五年多过去了，这个领域发生了什么变化。</p> <p>（不排除穿插一些个人感受和私货，哈哈）</p> <hr/> <h2 id="2-为什么要写-automl-综述">2. 为什么要写 AutoML 综述？</h2> <p>2019 年前后，深度学习已经在图像分类、目标检测、语言模型等方向都取得了突破性进展。但有一个问题一直存在：</p> <p><strong>搭一个好的深度学习系统，高度依赖人类专家。</strong></p> <p>从数据预处理到特征工程，从超参数调整到网络结构设计，每一步都需要领域知识和大量试错。这是深度学习大规模落地的最大障碍之一。</p> <p>而”自动化机器学习”（AutoML）就是为了解决这个问题的一系列方法的总称。问题是，当时 AutoML 领域的综述要么只覆盖 NAS，要么只覆盖 HPO（超参数优化），没有一个完整覆盖整个 ML pipeline 的综述。</p> <p>如下图，我们的综述覆盖了完整的 AutoML 流程：</p> <p><img src="/assets/img/posts/automl-survey/fig01_pipeline.png" alt="AutoML完整pipeline：数据准备→特征工程→模型生成→模型评估"/></p> <p>对比当时已有的综述：</p> <p><img src="/assets/img/posts/automl-survey/table1_comparison.png" alt="不同AutoML综述的覆盖范围对比"/></p> <p>我们是第一个同时覆盖 Data Preparation、Feature Engineering、HPO 和 NAS 的综述。</p> <hr/> <h2 id="3-综述内容速览">3. 综述内容速览</h2> <h3 id="31-数据准备">3.1 数据准备</h3> <p>数据是 ML 的基础，AutoML 在数据层面的自动化主要包括三块：</p> <p><strong>数据收集</strong>：如何自动爬取、众包标注、主动学习标注等。</p> <p><strong>数据清洗</strong>：从简单的缺失值填充到 AlphaClean（把清洗操作建模成超参数优化问题）。</p> <p><strong>数据增强</strong>：这是 2019 年的热点话题，AutoAugment（Google）用 RL 搜索最优增强策略轰动一时。如下图，我们系统整理了图像、音频、NLP 三个模态的增强方法：</p> <p><img src="/assets/img/posts/automl-survey/fig03_da_taxonomy.png" alt="数据增强方法分类：图像/音频/NLP三个模态"/></p> <h3 id="32-特征工程">3.2 特征工程</h3> <p>特征选择和特征构造的自动化。这一块在 NAS 出现后关注度有所下降，但在表格数据（非深度学习场景）里仍然很重要，AutoFeat、Featuretools 等工具就在做这件事。</p> <h3 id="33-超参数优化hpo">3.3 超参数优化（HPO）</h3> <p>HPO 是 AutoML 里最”经典”的部分，方法分几类：</p> <ul> <li><strong>Grid Search / Random Search</strong>：简单粗暴，但高维效率极差</li> <li><strong>贝叶斯优化</strong>（BO）：用代理模型（Gaussian Process、Tree Parzen Estimator）建模 f(超参)→性能 的关系，再用 acquisition function 决定下一个采样点。Hyperopt、SMAC3 都是这个方向的代表。</li> <li><strong>进化算法</strong>：把每组超参数当成一个”个体”，交叉变异选优。</li> <li><strong>Hyperband / BOHB</strong>：在资源分配上做了创新，用 early stopping 快速淘汰差的配置。</li> </ul> <h3 id="34-神经架构搜索nas重头戏">3.4 神经架构搜索（NAS）——重头戏</h3> <p>这也是综述最重头的部分（40%+ 的篇幅都在讲 NAS），因为 2019 年 NAS 正在爆炸。</p> <h4 id="搜索空间">搜索空间</h4> <p>NAS 的搜索空间定义了”可以搜索哪些架构”，主要分三种：</p> <p><strong>Entire-structured</strong>：整个网络从头搜——最灵活但搜索代价极大。如下图，节点是 layer，边是操作（conv 3×3、max pool 等）：</p> <p><img src="/assets/img/posts/automl-survey/fig06_search_space.png" alt="全局搜索空间：每个节点是一层，边是操作"/></p> <p><strong>Cell-based</strong>：把网络分成可重复的 cell（block），只搜 cell 的结构再堆叠——大幅降低搜索空间，且天然可迁移。NASNet、DARTS、AmoebaNet 都在这个范式下。</p> <p><strong>Hierarchical</strong>：多级 cell 嵌套——cell 里面还有 cell。</p> <h4 id="架构优化方法">架构优化方法</h4> <ul> <li><strong>强化学习（RL）</strong>：用 controller RNN 生成架构，用验证集精度作为 reward。NASNet 是代表，但当时需要 450 块 GPU 跑 4 天，离谱的消耗。</li> <li><strong>进化算法（EA）</strong>：把架构当成基因，交叉变异。AmoebaNet 系列走这条路。</li> <li><strong>梯度下降（Gradient-based）</strong>：最划时代的突破来自 DARTS（2018）——把离散的架构搜索松弛成连续的，用梯度优化，一块 GPU 跑几小时就搞定。如下图，DARTS 和 P-DARTS 的搜索阶段对比：</li> </ul> <p><img src="/assets/img/posts/automl-survey/fig08_darts.png" alt="DARTS vs P-DARTS：搜索阶段cell数量和操作数量的变化"/></p> <p>P-DARTS 是对 DARTS 的改进——把搜索分成多阶段，逐步增加 cell 数、减少候选操作数，桥接了搜索和评估阶段的 gap。</p> <ul> <li><strong>代理模型（Surrogate）/ 权重共享（One-shot NAS）</strong>：ENAS 用参数共享让所有子网络共享权重，搜索效率又上了一个台阶。</li> </ul> <hr/> <h2 id="4-写这篇综述的感受">4. 写这篇综述的感受</h2> <p>说实话，当时写这篇综述的时候，我刚开始读博不久，主要动机是<strong>搞清楚这个领域到底在干嘛</strong>。</p> <p>那时候 NAS 论文每周都在刷 SOTA，看不过来。写综述迫使我把几十篇论文系统读了一遍，整理出一个有逻辑的 taxonomy。这个过程对自己后续做 NAS 研究（比如我们后来的 EAGAN、NAS-LID 等工作）有很大帮助。</p> <p>引用量高的原因大概是：</p> <ol> <li><strong>时机好</strong>——NAS 正在从小众变大众，需要入门材料</li> <li><strong>覆盖面广</strong>——唯一覆盖完整 pipeline 的综述</li> <li><strong>持续更新</strong>——从 2019 发到 2021 年更新了 6 个版本，保持了一定的时效性</li> </ol> <hr/> <h2 id="5-现在来看哪些判断对了哪些没预测到">5. 现在来看，哪些判断对了，哪些没预测到？</h2> <p><strong>说对了的：</strong></p> <ul> <li>One-shot NAS（权重共享）会成为主流 ✓ —— NASBench、DARTS-PT、β-DARTS、BossNAS 等大量工作涌现</li> <li>NAS 会往更实用方向走（移动端、医疗等垂直场景）✓</li> <li>数据增强自动化（AutoAugment 方向）会持续发展 ✓</li> </ul> <p><strong>没预测到的：</strong></p> <ul> <li>LLM 的爆发让 NAS 在 NLP 侧几乎”消失”了——BERT/GPT 的出现让 Transformer 一统江湖，NLP 里的架构设计问题不再是搜索问题</li> <li>Foundation model 时代，”训一个大模型+微调”的范式让 AutoML 的部分工作（数据增强、架构搜索）在 NLP 里变得不再那么重要</li> <li>但 MoE 架构兴起之后，路由机制的设计又带来了新的 NAS-like 问题——这也是我现在做 ExpertFlow 这类工作的动机之一</li> </ul> <p><strong>一个感悟：</strong> AutoML 的核心精神（用自动化方法替代人工试错）没有过时，只是形态变了——现在更多是 prompt optimization、agent workflow 自动化、MoE 路由优化。</p> <hr/> <h2 id="6-写在最后">6. 写在最后</h2> <p>如果你现在才入门 ML/深度学习，这篇 2021 年的综述作为了解 AutoML 历史背景的读物还是有用的，但 NAS 那部分的很多具体工作现在已经是”历史”了（在 LLM 时代）。</p> <p>如果你对 NAS / AutoML 这个方向有兴趣，欢迎私信聊聊，或者看我们后续更新的工作。</p> <blockquote> <p>论文地址：<a href="https://arxiv.org/abs/1908.00709">https://arxiv.org/abs/1908.00709</a> 代码/工具：<a href="https://github.com/marsggbo/hyperbox">hyperbox</a>（我们组开发的 AutoML 框架）</p> </blockquote>]]></content><author><name></name></author><category term="AutoML"/><category term="NAS"/><category term="超参数优化"/><category term="综述"/><category term="论文解读"/><summary type="html"><![CDATA[KBS’21 | 我写的 AutoML 综述被引 2700+ 次，今天来聊聊这篇文章的来龙去脉]]></summary></entry><entry><title type="html">KDD’25 | BurstGPT：我们收集了 1031 万条 Azure OpenAI 真实 trace，LLM 推理系统没你想的那么稳</title><link href="https://marsggbo.github.io/blog/2026/burstgpt/" rel="alternate" type="text/html" title="KDD’25 | BurstGPT：我们收集了 1031 万条 Azure OpenAI 真实 trace，LLM 推理系统没你想的那么稳"/><published>2026-05-17T00:00:00+00:00</published><updated>2026-05-17T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/burstgpt</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/burstgpt/"><![CDATA[<h1 id="kdd25--burstgpt我们收集了-1031-万条-azure-openai-真实-tracellm-推理系统没你想的那么稳">KDD’25 | BurstGPT：我们收集了 1031 万条 Azure OpenAI 真实 trace，LLM 推理系统没你想的那么稳</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2401.17644">BurstGPT: A Real-world Workload Dataset to Optimize LLM Serving Systems</a> 数据集：<a href="https://github.com/HPMLL/BurstGPT">github.com/HPMLL/BurstGPT</a></p> </blockquote> <hr/> <h2 id="1-一个让人不舒服的真相">1. 一个让人不舒服的真相</h2> <p>做 LLM 推理系统优化的人都知道，系统好不好，要跑 benchmark 才算数。</p> <p>但问题是：<strong>大多数 benchmark 用的 workload 是假的</strong>。</p> <p>要么是用微软 Azure Functions（MAF）这类非 LLM 的 serverless workload，要么是靠参数化的合成数据（比如 Zipf 分布的 request length）自己捏一个。这些 workload 跟 LLM 的真实使用场景差距很大：</p> <ul> <li>请求频率：MAF 平均 1.64 RPS，真实 ChatGPT 对话服务只有 <strong>0.019 RPS</strong>（差了近 100 倍）</li> <li>并发模式：合成数据基本是稳定的，而真实 LLM 服务<strong>充满突发和不规律</strong></li> <li>响应长度：LLM 的输出长度是 auto-regressive 生成的，不确定性极高</li> </ul> <p>我们在香港科技大学（广州）和华为合作，采集了一家区域性 Azure OpenAI GPT 服务提供商（3000+ 用户）的数据，拿到了 <strong>213 天、1031 万条真实 trace</strong>，做成了 BurstGPT 数据集。</p> <hr/> <h2 id="2-burstgpt-是什么">2. BurstGPT 是什么？</h2> <p><img src="/assets/img/posts/burstgpt/001_Figure_1.png" alt="BurstGPT数据收集与使用方法：从Azure OpenAI采集→采样缩放→系统优化"/></p> <p>每条 trace 包含：</p> <table> <thead> <tr> <th>字段</th> <th>说明</th> </tr> </thead> <tbody> <tr> <td>Service Type</td> <td>API 或 Conversation（对话）</td> </tr> <tr> <td>LLM Type</td> <td>GPT-3.5 / GPT-4 / GPT-4v</td> </tr> <tr> <td>Request Start Time</td> <td>请求开始时间</td> </tr> <tr> <td>Request Length</td> <td>输入 token 数</td> </tr> <tr> <td>Response Length</td> <td>输出 token 数（0 = 失败）</td> </tr> </tbody> </table> <p>数据构成：</p> <ul> <li>ChatGPT API：<strong>869 万条</strong></li> <li>GPT-4 API：<strong>95 万条</strong></li> <li>ChatGPT 对话：<strong>30 万条</strong></li> <li>GPT-4 对话：<strong>16 万条</strong></li> </ul> <p>两种服务模式（Conversation vs API）的使用模式完全不同，这一点很关键。</p> <hr/> <h2 id="3-关键发现真实-workload-长什么样">3. 关键发现：真实 workload 长什么样</h2> <h3 id="31-并发模式对话服务周期性api-服务随机突发">3.1 并发模式：对话服务周期性，API 服务随机突发</h3> <p><strong>对话服务</strong>（用户主动使用）：</p> <ul> <li>长期：工作日高、周末低，明显周期性</li> <li>短期：工作时间高（9-18 点），夜间低</li> </ul> <p><strong>API 服务</strong>（程序调用）：</p> <ul> <li>长期：完全不规律，无明显周期</li> <li>短期：随机突发，burst 强度变化剧烈</li> </ul> <p>用 Gamma 分布建模并发度，参数 $\alpha$（形状，$\alpha$ 越小 burst 越强）和 $\beta$（速率）每 20 分钟都在变：</p> <p><img src="/assets/img/posts/burstgpt/006_Figure_6.png" alt="Gamma分布参数随时间变化：对话服务 vs API服务"/></p> <p>对话服务的 $\alpha$ 在上班高峰时剧烈变化，API 服务的 $\alpha$ 和 $\beta$ 整天都在高频波动。这说明：<strong>用固定的合成 workload 评估 LLM 系统，看到的稳定性是假象</strong>。</p> <h3 id="32-token-分布输入短但输出差异大">3.2 Token 分布：输入短但输出差异大</h3> <p><img src="/assets/img/posts/burstgpt/007_Figure_7.png" alt="Request/Response token分布：ChatGPT和Llama-2-13b-chat"/></p> <p>ChatGPT 对话服务：</p> <ul> <li>Request token：Zipf 分布，短请求居多</li> <li>Response token：<strong>双峰分布</strong>——一堆很短的（直接回答），一堆很长的（长文本生成）</li> </ul> <p>Llama-2-13b-chat：</p> <ul> <li>Response token：大量请求直接打到 2048 token 上限</li> </ul> <p>这个双峰现象对 KV cache 管理影响很大——你无法预测一个请求到底会用多少内存。</p> <h3 id="33-失败率chatgpt-对话失败率超过-5">3.3 失败率：ChatGPT 对话失败率超过 5%</h3> <p>这个数据出乎意料。</p> <p>一般云服务的失败率在 0.1% 以内，但我们测到的 ChatGPT 对话服务平均失败率 <strong>&gt;5%</strong>，显著高于 API 服务。</p> <p>原因分析：突发请求导致 KV cache 内存耗尽，系统来不及驱逐就 OOM，直接失败。这是 KV cache 管理不够精细的直接体现。</p> <hr/> <h2 id="4-burstgpt-perf配套-benchmark-工具">4. BurstGPT-Perf：配套 benchmark 工具</h2> <p>数据集有了，但怎么用？我们同时开源了 BurstGPT-Perf：</p> <ul> <li><strong>Workload Generator</strong>：把 BurstGPT trace 缩放到目标系统规模（RPS 缩放 or 参数化缩放）</li> <li><strong>Prompt Pool</strong>：按 Zipf 分布采样 prompt 长度，对齐真实 input 分布</li> <li><strong>Metrics</strong>：涵盖效率（latency、throughput）、稳定性（latency jitter）和可靠性（failure rate）</li> </ul> <p>两种缩放方式：</p> <ol> <li><strong>RPS Scaling</strong>：直接用参数 $c$ 乘时间戳，让平均 RPS 对齐目标系统</li> <li><strong>Modeled Scaling</strong>：调整 Gamma 分布参数，保留 burst 特性同时适配系统规模</li> </ol> <hr/> <h2 id="5-burstgpt-告诉了我们什么">5. BurstGPT 告诉了我们什么？</h2> <h3 id="51-burstgpt-vs-maf2同-rps-下burstgpt-更难伺候">5.1 BurstGPT vs MAF2：同 RPS 下，BurstGPT 更难伺候</h3> <p><img src="/assets/img/posts/burstgpt/014_Figure_14.png" alt="vLLM在BurstGPT和MAF2 workload下的平均延迟和吞吐量"/></p> <p>在同样 RPS（0.326）下，BurstGPT workload 下 vLLM 的平均 token latency（0.0651s）比 MAF2（0.0499s）高 30%，吞吐量也更高（因为实际处理了更多并发）。</p> <p><strong>结论</strong>：用 MAF2 测出来系统很稳，一换真实 workload 就原形毕露。合成数据评测的结论没法信。</p> <h3 id="52-同一优化在对话服务和-api-服务上效果可能相反">5.2 同一优化，在对话服务和 API 服务上效果可能相反</h3> <p>我们用 BurstGPT 的对话 trace 和 API trace 分别评估三种调度策略（FCFS、SRF、LRF）：</p> <ul> <li><strong>对话服务</strong>：LRF（Long Request First）在延迟上优于 FCFS</li> <li><strong>API 服务</strong>：上面的结论完全不成立，LRF 变成了劣势</li> </ul> <p>这说明：<strong>一个 workload 上调优的系统，未必能泛化到另一个 workload</strong>。现在很多论文只报一种 workload 的结果，结论可信度存疑。</p> <h3 id="53-huawei-pd-解耦系统的实际应用">5.3 Huawei PD 解耦系统的实际应用</h3> <p>BurstGPT 还被用于华为 Prefill-Decode（PD）解耦推理系统的原型开发：</p> <ul> <li>用 BurstGPT 的 trace 在仿真平台模拟不同 PD ratio 下的 goodput</li> <li>在工作日 6:00 时刻，随着 workload 从大量短请求（解码密集）切换到长请求（预填充密集），最优 PD ratio 从 2:6 切换到 6:2</li> <li>动态 PD ratio + beam search 进一步提升 goodput</li> </ul> <p>这个 case 说明 BurstGPT 不只是学术玩具，在工业系统设计中也有实际价值。</p> <hr/> <h2 id="6-为什么要做这个工作">6. 为什么要做这个工作？</h2> <p>这是一篇偏向系统的数据论文。我们在 2024 年初就把数据集挂到 arXiv 上了，然后持续更新了一年多（5 个版本），最终发表在 KDD 2025。</p> <p>这个工作的出发点其实很朴素：做 LLM serving 系统研究，需要真实 workload。但当时根本没有公开的 LLM trace 数据集。用合成数据做出来的结论，面对真实场景时经常失灵。</p> <p>我们有机会从一个运行 ChatGPT 区域服务的合作伙伴那里采集到这些数据（当然做了隐私处理，只保留了统计特征），就把它开源了。</p> <p>现在这个数据集在工业界用得比较多，vLLM、SGLang、Mooncake 等框架的测试中都看到过 BurstGPT 的影子。</p> <hr/> <h2 id="7-总结">7. 总结</h2> <table> <thead> <tr> <th>特征</th> <th>BurstGPT</th> </tr> </thead> <tbody> <tr> <td>数据量</td> <td>1031 万条 trace</td> </tr> <tr> <td>时间跨度</td> <td>213 天</td> </tr> <tr> <td>来源</td> <td>Azure OpenAI GPT 区域服务</td> </tr> <tr> <td>服务类型</td> <td>Conversation + API</td> </tr> <tr> <td>模型类型</td> <td>GPT-3.5、GPT-4、GPT-4v</td> </tr> <tr> <td>覆盖维度</td> <td>并发模式、对话模式、响应长度、失败率</td> </tr> </tbody> </table> <p><strong>核心结论：用合成 workload 优化出来的 LLM 推理系统，在真实场景里大概率会让你踩坑。</strong></p> <blockquote> <p>数据集：<a href="https://github.com/HPMLL/BurstGPT">github.com/HPMLL/BurstGPT</a> 论文：<a href="https://arxiv.org/abs/2401.17644">arxiv.org/abs/2401.17644</a></p> </blockquote>]]></content><author><name></name></author><category term="LLM推理"/><category term="Workload分析"/><category term="系统优化"/><category term="数据集"/><category term="论文解读"/><summary type="html"><![CDATA[KDD’25 | BurstGPT：我们收集了 1031 万条 Azure OpenAI 真实 trace，LLM 推理系统没你想的那么稳]]></summary></entry></feed>