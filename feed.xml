<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://marsggbo.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://marsggbo.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-09-22T07:10:57+00:00</updated><id>https://marsggbo.github.io/feed.xml</id><title type="html">HE Xin</title><subtitle>&quot;SSS: Study, Sleep, Slim&quot; </subtitle><entry><title type="html">Jev 拆解：不生成文本的 AI，究竟怎么直接做决策？</title><link href="https://marsggbo.github.io/blog/2026/20260921-jev/" rel="alternate" type="text/html" title="Jev 拆解：不生成文本的 AI，究竟怎么直接做决策？"/><published>2026-09-21T00:00:00+00:00</published><updated>2026-09-21T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260921-jev</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260921-jev/"><![CDATA[<h1 id="jev-拆解不生成文本的-ai究竟怎么直接做决策">Jev 拆解：不生成文本的 AI，究竟怎么直接做决策？</h1> <blockquote> <p>主要资料：<a href="https://typesafe.ai/blog/introducing-system-one-models-and-jev">Jev 官方发布博客</a> · <a href="https://docs.typesafe.ai/">Jev 官方文档</a> · <a href="https://arxiv.org/abs/2508.18847">ConfTuner（NeurIPS 2025）</a>。各实现的源码、模型卡与完整 URL 统一列在文末。</p> </blockquote> <hr/> <h2 id="1-一个只需要回答-abc-的任务为什么还要生成一段文字">1. 一个只需要回答 A/B/C 的任务，为什么还要生成一段文字</h2> <p>一个客服 Agent 收到这样的请求：</p> <blockquote> <p>信用卡被重复扣款了，希望今天退款，否则我就取消订阅。</p> </blockquote> <p>系统接下来真正需要做的，其实是几个很具体的判断：该转给 billing 还是 account security？用户有没有明确要求退款？流失风险高不高？要不要在一小时内回复？</p> <p>今天最常见的做法，是把工单、问题和候选答案一起塞给 LLM，让它返回 <code class="language-plaintext highlighter-rouge">A/B/C</code> 或一段 JSON。这个方案当然能用，但答案明明只有几个选项，模型还是要一个 token 一个 token 地生成；生成完还要解析 JSON、检查字段、处理越界选项，偶尔遇到 markdown code fence、解释性废话或者空字符串，还得重试。</p> <p><img src="/assets/img/posts/20260921-jev/diagram_workflow.png" alt="生成式 LLM 与类型化决策的执行链"/></p> <p>另一头是传统分类器。BERT 接一个 <code class="language-plaintext highlighter-rouge">Linear(H, K)</code> 分类头，一次 forward 就能返回 K 类概率，便宜又直接；问题是 K 个类别通常在训练时已经写死。业务一旦从“billing / technical / sales”换成另一套 taxonomy，往往就要改 head、准备数据、重新训练。</p> <p>Jev 想填的是两者之间这块空白：</p> <blockquote> <p><strong>任务和候选由自然语言在请求时定义，输出却像专用分类器一样，直接是程序可以消费的概率。</strong></p> </blockquote> <p>这类模型现在常被叫作 typed decision model，也就是“类型化决策模型”。这不是某一个具体模型的名字，而是一类输出合同：模型不负责自由写作，只在调用者给出的有限答案空间里做 Choice、Score 或 Boolean 判断。Jev 是闭源产品，随后出现的 OpenJev、Nimble、this-that-model、Laya 则用了完全不同的方式去实现相似能力。甚至 NeurIPS 2025 的 ConfTuner，虽然仍然保留文本生成，也在解决同一个核心问题：<strong>怎么把模型的判断变成可信、可执行的概率。</strong></p> <h2 id="2-jev-把自然语言理解包装成了一个概率接口">2. Jev 把自然语言理解包装成了一个概率接口</h2> <p>TypeSafe 在 2026 年 9 月发布 <a href="https://typesafe.ai/blog/introducing-system-one-models-and-jev">Jev</a>，把它称为第一个 “System One model”。这个名字借用了卡尼曼的系统一/系统二：系统二慢慢推理、组织语言，系统一快速做直觉判断。这个比喻是否严谨可以另说，产品定位倒是很清楚——<strong>Decisions, not strings</strong>。</p> <p>一次 Jev 请求由一份 <code class="language-plaintext highlighter-rouge">state</code> 和一组 <code class="language-plaintext highlighter-rouge">questions</code> 组成。<code class="language-plaintext highlighter-rouge">state</code> 是所有判断共享的当前状态，可以是字符串、JSON 或数组；每个 question 则描述“要判断什么”和“合法答案有哪些”。例如：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">state</span> <span class="o">=</span> <span class="p">{</span>
    <span class="sh">"</span><span class="s">message</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">信用卡被重复扣款，希望今天退款，否则取消订阅</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">plan</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">Pro</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">duplicate_charge</span><span class="sh">"</span><span class="p">:</span> <span class="bp">True</span><span class="p">,</span>
<span class="p">}</span>

<span class="n">questions</span> <span class="o">=</span> <span class="p">{</span>
    <span class="sh">"</span><span class="s">department</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">type</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">choice</span><span class="sh">"</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">instructions</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">哪个团队应该处理这张工单？</span><span class="sh">"</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">criteria</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span>
            <span class="sh">"</span><span class="s">billing</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">账单、扣款和退款</span><span class="sh">"</span><span class="p">,</span>
            <span class="sh">"</span><span class="s">security</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">盗刷与账户安全</span><span class="sh">"</span><span class="p">,</span>
            <span class="sh">"</span><span class="s">technical</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">产品故障</span><span class="sh">"</span><span class="p">,</span>
        <span class="p">},</span>
    <span class="p">},</span>
    <span class="sh">"</span><span class="s">churn_risk</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">type</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">score</span><span class="sh">"</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">instructions</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">评估用户流失风险</span><span class="sh">"</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">criteria</span><span class="sh">"</span><span class="p">:</span> <span class="p">[</span><span class="sh">"</span><span class="s">低</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">中</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">高</span><span class="sh">"</span><span class="p">],</span>
    <span class="p">},</span>
    <span class="sh">"</span><span class="s">refund_requested</span><span class="sh">"</span><span class="p">:</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">type</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">noul</span><span class="sh">"</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">instructions</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">用户是否明确要求退款？</span><span class="sh">"</span><span class="p">,</span>
    <span class="p">},</span>
<span class="p">}</span>
</code></pre></div></div> <p>这三个 question 都能读到同一份 state，但彼此独立，不能读取其他问题的答案。问题 ID（上面的 <code class="language-plaintext highlighter-rouge">department</code>）只负责让返回结果对上字段，模型实际判断依赖的是 <code class="language-plaintext highlighter-rouge">instructions</code> 和 <code class="language-plaintext highlighter-rouge">criteria</code>，所以不能只取一个聪明的字段名，却把指令写得含糊不清。</p> <h3 id="21-choicescore-和-noul-是-jev-唯一能说的三种话">2.1 Choice、Score 和 Noul 是 Jev 唯一能“说”的三种话</h3> <p><img src="/assets/img/posts/20260921-jev/jev_primitives.png" alt="Jev 三种类型化问题的输入输出"/></p> <p><strong>Choice</strong> 接受 2～255 个候选，返回每个候选的完整概率分布、top-1 选项和一个 confidence。候选来自本次请求，不是训练时写死的类别：今天可以判断客服队列，下一次也可以判断 Agent 该调用哪个 tool。</p> <p><strong>Score</strong> 接受 2～10 个有序等级，返回各等级的概率，再计算期望分：</p> \[\text{score}=\sum_{i=0}^{K-1} i\,p_i\] <p>假设“低、中、高”三个等级的概率分别是 <code class="language-plaintext highlighter-rouge">[0.1, 0.6, 0.3]</code>，期望分就是 $0\times0.1+1\times0.6+2\times0.3=1.2$。它不是粗暴地只返回“中”，而是保留了向“高”偏移的不确定性。</p> <p><strong>Noul</strong> 是 Jev 给 yes/no 判断起的名字，SDK 里也常写成 Boolean。它只需要返回一个 $P(\text{true})$，因为 $P(\text{false})=1-P(\text{true})$。</p> <p>完整分布比单独返回 top-1 更有价值：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>A: 0.91, B: 0.06, C: 0.03
A: 0.43, B: 0.41, C: 0.16
</code></pre></div></div> <p>两次的答案都是 A，但后者几乎是掷硬币。前者可以自动执行，后者更适合追问用户、升级到更强模型或者转人工。<strong>Jev 真正卖的不是“会分类”，而是把自然语言理解变成 workflow 的控制信号。</strong></p> <h3 id="22-255-个候选不够时不是把-head-硬扩到几万个类别">2.2 255 个候选不够时，不是把 head 硬扩到几万个类别</h3> <p>官方文档给 Choice 设的上限是 255 个选项。这个数字究竟来自模型结构还是产品工程约束，官方没有解释，所以不能看到 255 就脑补成 <code class="language-plaintext highlighter-rouge">Linear(H, 255)</code>，更不能猜它和一个字节有关。</p> <p>对于上千甚至上万个候选，官方建议把 taxonomy 做成树：先判断大类，再判断子类，并用 beam search 在每一级保留概率最高的 K 条路径。beam search 可以理解成“不只押一条路”，它会留下几个最有希望的分支继续往下搜索，避免第一层一次误判就彻底出局。</p> <p><img src="/assets/img/posts/20260921-jev/jev_choice_tree.png" alt="大规模候选通过层级 Choice 逐步收窄"/></p> <p>这条路线适合产品目录、工单 taxonomy 等天然有层级的任务，但它也意味着“大规模选择”不再是一次 forward：树越深，请求链越长，上一级的错误也会传给下一级。</p> <h3 id="23-没有输出-token不等于没有计算">2.3 “没有输出 token”不等于“没有计算”</h3> <p>Jev 当前公开的信息可以支持下面几个判断：</p> <ul> <li>它不向用户生成自由文本，而是返回 Choice、Score、Noul 的数值结果；</li> <li>多个独立 question 可以在一次请求中处理；</li> <li>state 与全部 questions 合计最多 64k token，state 加最长单个 question 不超过 32k；</li> <li>官方定价是每百万输入 token 0.042 美元，输出免费，因为没有输出 token；</li> <li>官方宣称在自己的 workflow 中比生成式模型快 20～200 倍、便宜 40～400 倍。</li> </ul> <p>最后一组数字高度依赖模型、输入长度、网络、batch 和生成长度，适合说明方向，不能当成通用 benchmark。</p> <p>更重要的是，官方<strong>没有</strong>公开 backbone、参数量、attention mask、decision head 和 forward 过程。因此下面这些说法都不能当成 Jev 事实：</p> <ul> <li>“Jev 就是让 LLM 只生成一个 A token”；</li> <li>“它一定复用了 state 的 KV Cache”；</li> <li>“它内部是 pointer head”；</li> <li>“255 就是固定输出 head 的宽度”。</li> </ul> <p>不生成自由文本，省掉的是自回归 decoding loop、输出传输和 parser，不是把 prefill、question encoding 和候选打分全部省掉。后面会看到，不同开源项目都对外返回相似的 JSON，底下的计算图却完全不是一回事。</p> <h3 id="24-rlcd-想让概率对得上现实但-recipe-还在黑盒里">2.4 RLCD 想让概率对得上现实，但 recipe 还在黑盒里</h3> <p>TypeSafe 把 Jev 的训练方法叫作 RLCD（Reinforcement Learning for Calibrated Decisions，面向校准决策的强化学习）。所谓 calibration，不是“模型每次都很自信”，而是它给出 0.8 的那批判断，长期看应该大约有 80% 真正答对。</p> <p>这和 accuracy 是两个维度。一个模型可以 top-1 accuracy 很高，却总把 60% 的把握报成 99%；也可以 accuracy 一般，但很诚实地知道自己什么时候不会。业务要根据阈值自动退款、执行 shell command 或拦截用户时，后者往往同样重要。</p> <p>遗憾的是，RLCD 目前公开的是目标，不是完整 recipe。reward 怎么算、是否真的使用 policy gradient、训练数据从哪里来、规模多大、backbone 更新哪些参数，都没有披露。Jev 可以作为闭源基线，但不能把后面任何一个开源实现的结构倒推成它的内部结构。</p> <h2 id="3-从会报置信度到完全不生成差别不只是换一个输出头">3. 从“会报置信度”到“完全不生成”，差别不只是换一个输出头</h2> <p>Jev 把目标定义得很清楚：输入一份 state 和若干问题，直接返回有限选项上的概率。但官方没有公开模型结构，于是社区实际上沿着几条不同的路在回答同一个问题：<strong>怎样把一个擅长语言理解的模型，变成便宜、稳定、可以直接接进程序分支的决策器？</strong></p> <p>下面这张图先给出全貌。阅读时从上往下看，改造逐渐深入：最上面仍然让 LLM 写答案；第二层不再生成，只读取现成模型对 A/B/C 的分数；第三层开始用决策数据训练；第四层继续解决多个问题重复计算 state 的问题；最下面则连生成式 backbone 都换掉。右侧不是效果排名，而是每条路线最鲜明的取舍。</p> <p><img src="/assets/img/posts/20260921-jev/diagram_routes.png" alt="类型化决策的五条实现路线"/></p> <p>一句话概括各自特点：</p> <ol> <li><strong><a href="https://arxiv.org/abs/2508.18847">ConfTuner</a></strong>：不改生成范式，重点解决“模型说 80% 时，能不能真的对 80%”。</li> <li><strong><a href="https://github.com/TheoLeeCJ/SemIf">SemIf</a> / <a href="https://github.com/ekzhang/openjev-sglang">openjev-sglang</a></strong>：不训练新模型，直接读取现成 LLM 对合法选项的分数，是门槛最低的 baseline。</li> <li><strong><a href="https://github.com/bespokelabsai/nimble">Nimble</a> / <a href="https://github.com/TianyuCodings/NanoJev">NanoJev</a></strong>：给现成 LLM 加 LoRA 或打分头，让候选分数真正学会跟随关键证据变化。</li> <li><strong><a href="https://github.com/FLock-io/this-that-model">this-that-model</a> / <a href="https://github.com/jaredpalmer/kev">kev</a></strong>：不只改怎么打分，还改一次 forward 里的信息流，尽量让多个问题共享 state。</li> <li><strong><a href="https://github.com/NandhaKishorM/laya">Laya</a> / <a href="https://github.com/vinnylarouge/jevlike">Jevlike</a></strong>：放弃自回归生成骨干，改用双向 encoder 和专用 decision head，模型更小，但能力边界也更依赖训练数据。</li> </ol> <p>还有一条不能硬塞进这条梯子里的旁路：<a href="https://github.com/razorback16/openjev">基于 DiffusionGemma 的 OpenJev</a> 把多个答案空位放在同一块 masked canvas 上，用离散扩散同时去噪。它不是普通 causal LLM 的 next-token readout，后面单独讲。</p> <p>后文会反复用到两个词。<strong>Readout</strong> 指“模型算完 hidden states 后，究竟从哪里把最终答案读出来”；<strong>answer slot</strong> 则是输入中专门留给某个问题的“答案位置”，模型不一定真的在这里生成 token，也可以只读取这个位置的 hidden state。先把这两个概念放稳，后面的结构差异就不会像模块名堆砌。</p> <h2 id="4-neurips25-conftuner不省生成成本先解决80-到底是不是真的-80">4. NeurIPS’25 ConfTuner：不省生成成本，先解决“80% 到底是不是真的 80%”</h2> <p><a href="https://arxiv.org/abs/2508.18847">ConfTuner 论文</a>不是 Jev-compatible server，也没有取消自回归生成。它代表这条路线的起点：模型照常生成答案，再用自然语言写出 <code class="language-plaintext highlighter-rouge">Confidence: 80%</code>。论文把这种说出来的置信度称为 verbalized confidence，也就是“语言化置信度”。</p> <h3 id="41-多问十次得到的一致率不等于模型答对的概率">4.1 多问十次得到的一致率，不等于模型答对的概率</h3> <p>过去常见的办法，是让模型回答同一道题很多次：十次有九次都答 A，就把 confidence 记成 90%。问题是模型完全可能稳定地答错。另一些方法让 teacher model 给答案打分，也只是把 teacher 的偏差搬过来；多次采样还会同步放大数据构造和推理成本。</p> <p>ConfTuner 的关键特点可以压成一句话：<strong>不用“模型自己是否前后一致”当标签，而是直接用答案真实的对错，校准它说出口的 confidence。</strong></p> <h3 id="42-公式看起来绕本质是让答对报高答错报低可以反向传播">4.2 公式看起来绕，本质是让“答对报高、答错报低”可以反向传播</h3> <p>模型生成到 <code class="language-plaintext highlighter-rouge">Confidence:</code> 后，ConfTuner 先不急着取最终数字，而是读出 0～100 这 101 个数字 token 的 logits。logit 是 softmax 之前的原始分数；对这 101 个分数做 softmax，得到 $q_0,\ldots,q_{100}$，表示模型分别有多倾向于报告 0%、1%……100%。</p> <p>下图从左到右分两步。上半部分先把模型原本想说的 99% 展开成 101 个数字上的分布；下半部分再根据答案其实是错的，把目标放到 0%，用损失把概率质量从 99 一侧推回 0 一侧。图中的重点不是“最后从 99 改成 5”这个个例，而是训练时使用了<strong>完整的 101 维分布</strong>，不是把最终输出的一个数字当普通字符串处理。</p> <p><img src="/assets/img/posts/20260921-jev/conftuner_method_html.png" alt="ConfTuner 两阶段校准流程"/></p> <p>论文使用 tokenized Brier score：</p> \[\mathcal L=\sum_{i=0}^{100}\left(q_i-\mathbb 1[i=100y]\right)^2,\qquad y\in\{0,1\}.\] <p>把公式翻译成人话：$y=1$ 表示答案正确，目标就在 100% 那一格；$y=0$ 表示答案错误，目标就在 0% 那一格。模型在 101 个格子上预测的概率与目标相差多少，逐格平方再求和。Brier score 越低越好，而且它是 proper scoring rule——长期来看，模型如实报告自己相信的正确概率，期望损失最低；永远报 99% 或者永远躲在 50%，都占不到便宜。</p> <p>训练只用了 HotpotQA 的 2000 条样本和 LoRA。LoRA（Low-Rank Adaptation）是在部分线性层旁插入小型低秩矩阵，只训练这部分增量参数；论文设置是 rank 8、alpha 32，作用在 attention 的 Q/V projection 上。</p> <p>下表最值得看的是最后两列：ConfTuner 每条输入只采样一次，因此 2000 条训练问题就对应 2000 条响应；SaySelf 每条采样 100 次，最终滚到 900 万条响应。训练时间也从 120 分钟降到 4 分钟。这里节省的不是 inference decoding，而是训练阶段不再为 proxy confidence 反复采样。</p> <p><img src="/assets/img/posts/20260921-jev/table6_p9.png" alt="ConfTuner 的训练成本对比"/></p> <p>从表里横向看，LACIE、SaySelf 和 Ensemble 都要为同一道输入采样多次；ConfTuner 的 <code class="language-plaintext highlighter-rouge">Sample times=1</code>，所以总训练响应数没有被重复采样放大。这里的 4 分钟和 120 分钟对应论文环境，只用于比较这几种数据构造方式，不能外推成所有模型上的固定训练时间。</p> <h3 id="43-html-原图说明了两件事同分布很强跨分布仍然打折">4.3 HTML 原图说明了两件事：同分布很强，跨分布仍然打折</h3> <p>论文用 ECE（Expected Calibration Error，期望校准误差）衡量置信度是否诚实。它把样本按 confidence 分桶，再比较每个桶的平均 confidence 和真实正确率，越低越好。</p> <p>下面这张表直接从论文 HTML 页面渲染。每个 backbone 有五行方法；左侧 HotpotQA 是训练分布，右侧四个数据集没有参与这次训练。读表时先看每组最后一行 ConfTuner：LLaMA 在 HotpotQA 上从 0.4803 降到 0.0405，Ministral 从 0.6767 降到 0.1027；再看最右侧 OOD 平均值，仍有提升，但没有同分布上那么夸张。Qwen 在 GSM8K、StrategyQA 上甚至不是该列最优。</p> <p><img src="/assets/img/posts/20260921-jev/conftuner_ece_html.png" alt="ConfTuner 的 ECE 结果"/></p> <p>这张表还有个容易漏掉的细节：ConfTuner 并非每个 OOD 单项都拿第一，但三种 backbone 的 OOD 平均 ECE 都是本组最低。更准确的结论是“整体迁移后仍有收益”，而不是“在任何新任务上都最优”。</p> <p>Reliability diagram 更直观：横轴是模型报告的 confidence，纵轴是该桶真实答对的比例；红色虚线代表理想状态，蓝柱与虚线之间的红色缺口越小越好。下图上排是 HotpotQA，下排是分布外的 TriviaQA，五列从 Base 到 ConfTuner。最右一列整体更贴近虚线，但 TriviaQA 仍有可见偏差——<strong>校准能力不是训练一次就能无条件迁移到所有任务。</strong></p> <p><img src="/assets/img/posts/20260921-jev/conftuner_reliability_html.png" alt="ConfTuner 的置信度可靠性图"/></p> <p>论文还让模型看到自己的答案和 confidence，再决定要不要修改。下图左半边比较 self-correction 前后的正确率变化，右半边比较在相同修改预算下的准确率。ConfTuner 的意义不是让每道题都再想一次，而是让低 confidence 更像一个有用的升级信号。</p> <p><img src="/assets/img/posts/20260921-jev/figure4_p9.png" alt="置信度用于自我修正"/></p> <p>ConfTuner 把“概率可信”这件事讲透了，但它没有解决 Jev 最显眼的系统开销：答案和 confidence 仍然要逐 token 生成。于是下一条路线干脆先不训练——<strong>既然最终只认 A/B/C，能不能直接读取模型对这几个选项的分数？</strong></p> <h2 id="5-semif-与-openjev-sglang零训练拿掉-decoding先得到最便宜的-jev-like-baseline">5. SemIf 与 openjev-sglang：零训练拿掉 decoding，先得到最便宜的 Jev-like baseline</h2> <p>这里必须先把几个容易混淆的网站拆开：</p> <ul> <li><a href="https://openjev.sh/">openjev.sh</a> 与 <a href="https://openjev.tech/">openjev.tech</a> 当前是同一套访问平台。它们宣称用 <code class="language-plaintext highlighter-rouge">$JEV</code> 交易费用补贴调用，底层提供的是 TypeSafe Jev 的公共入口，<strong>不是一个开源模型实现</strong>。</li> <li><a href="https://openjev.com/">openjev.com</a> 是原名 OpenJev、后来改名为 <strong>SemIf</strong> 的浏览器实验；其代码仓库是 <a href="https://github.com/TheoLeeCJ/SemIf">TheoLeeCJ/SemIf</a>。</li> <li><a href="https://github.com/ekzhang/openjev-sglang">openjev-sglang</a> 是 Qwen + SGLang 的 Jev-compatible server。</li> <li><a href="https://github.com/razorback16/openjev">razorback16/openjev</a> 是 DiffusionGemma structured read，计算方式不同，放到第 9 节。</li> </ul> <p><a href="https://github.com/TheoLeeCJ/SemIf">SemIf</a> 和 <a href="https://github.com/ekzhang/openjev-sglang">openjev-sglang</a> 的共同点是：<strong>不先训练一个 decision model，而是把现成 instruction model 的 next-token logits 当作选项分数。</strong></p> <p>下图按从左到右的执行顺序读。prompt 先告诉模型 state、问题和候选；Transformer 做完 prefill，在最后位置产生 hidden state；原本的 LM head 会给整个词表打分；程序只留下 A、B、C 三个合法 label 的分数再 softmax。这里 A/B/C 只是候选的单 token 编号，比如 A 映射到 billing、B 映射到 security。模型不需要真的把字符 B 写出来。</p> <p><img src="/assets/img/posts/20260921-jev/diagram_restricted_logits.png" alt="零训练候选读分流程"/></p> <p>这种做法最大的优点是门槛低：没有新权重、没有训练数据，任何能返回 logits 的 LLM 都能先跑起来。SemIf 还测了 state 复用：在 37 个 state、每个 21 个判断的 workload 上，fresh scoring 是 2.33 decisions/s，serial prefix reuse 是 10.75，parallel suffixes 是 20.03。不过 BF16 的快速复用路径相对 fresh scoring 出现了 5～6/777 次 argmax 变化，说明“数学上相同的前缀复用”到了有限精度和不同 kernel 下，也要检查数值漂移。</p> <p>它的短板同样直接：base model 从未被专门训练成“让 A/B/C logits 表达可靠的业务概率”。候选顺序、prompt 模板和 label token 的先验都可能影响结果。SemIf 对 Qwen3.5-4B 做的 per-workload temperature scaling，把 WANLI 上的 ECE 从 0.208 降到 0.069，也再次说明 raw softmax 不是开箱即用的 calibrated probability。</p> <p><code class="language-plaintext highlighter-rouge">openjev-sglang</code> 的重点更偏 serving：Qwen3.6-35B-A3B 提供能力，SGLang 的 radix cache 用前缀树复用相同 prompt prefix，再配合异步 batching 和 CUDA graph 降低 prefill overhead。它优化的是<strong>怎么更快地读现成模型</strong>，不是怎么用新数据教模型做 decision。</p> <p>零训练 baseline 能验证“取消 decoding 到底省多少”，但不能保证选项分数会对关键证据敏感。下一步自然是保留这种窄 readout，同时让模型专门学一次。</p> <h2 id="6-nimble-与-nanojev用决策数据训练-readout让概率跟着关键证据变化">6. Nimble 与 NanoJev：用决策数据训练 readout，让概率跟着关键证据变化</h2> <h3 id="61-nimble-的特点不是读取一个-token而是用反事实样本训练这个-token">6.1 Nimble 的特点不是“读取一个 token”，而是用反事实样本训练这个 token</h3> <p><a href="https://github.com/bespokelabsai/nimble">Bespoke Nimble</a> 保留 Qwen3.5-9B 的语言能力，用 LoRA 把它训练成 decision scorer。所谓 <strong>answer code</strong>，只是给长候选取的单 token 代号：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>A → 拒绝退款，因为超过 14 天窗口
B → 同意退款
C → 转人工复核
</code></pre></div></div> <p>模型不生成右侧长文本，只读取 A/B/C 的 logits，再由程序映射回原始候选。answer code 本身不神秘，真正决定效果的是它前面的 hidden state 是否已经理解 state、policy 和候选之间的关系。</p> <p>下图分四块：左上是一次 prefill 后读候选分数；右上是数据构造，只改一条关键事实并让 label 翻转；左下是 Qwen3.5-9B 上做 LoRA；右下是 held-out 结果。读图时最值得关注右上到左下这条因果链——模型不是模仿 Jev 概率，而是从成对的反事实样本中学习“哪条证据应该改变答案”。</p> <p><img src="/assets/img/posts/20260921-jev/nimble_infographic.png" alt="Nimble 的数据、训练与推理流程"/></p> <p>例如规则写着“只有 Mira 可以批准 account 42 的退款”。原始记录由 Mira 签署，答案是 true；反事实只把 Mira 改成 Noah，答案就变成 false。问题、规则和其余上下文保持不变。这种 contrastive data curation 能减少“看到 refund 就答 true”这类表面捷径。</p> <p>训练集有 2676 条、覆盖 10 类任务，held-out 集 324 条；最终 recipe 是 LoRA rank 16、学习率 $5\times10^{-5}$、effective batch size 8、一个 epoch，在合法 answer-code logits 上优化 cross-entropy。标签由模型审核后再通过规则程序生成，没有人工逐条复核，也没有用 Jev 概率蒸馏。</p> <p>下图比较同一批 324 条 held-out 样本上的 reference-label agreement。柱高只表示最终选项与 synthetic reference 是否一致：Nimble 为 90.12%，Jev 1.13.0 为 93.21%，未微调 Qwen3.5-9B 为 66.36%。它不衡量 probability calibration，所以不能从这张图推出 Nimble 的 0.9 和 Jev 的 0.9 同样可信。</p> <p><img src="/assets/img/posts/20260921-jev/nimble_evidence_comparison.png" alt="Nimble 的 held-out agreement"/></p> <p>Nimble 自己也明确写了 temperature 固定为 1.0，尚未校准；CUDA scorer 当前还会为每个 field 重跑完整 prompt，MLX <code class="language-plaintext highlighter-rouge">ParallelScorer</code> 才复用 context。<strong>训练解决了决策能力，不等于顺手解决了概率校准和共享计算。</strong></p> <h3 id="62-nanojev-把每候选一条路径训练得更完整但重复-state-的成本也更明显">6.2 NanoJev 把“每候选一条路径”训练得更完整，但重复 state 的成本也更明显</h3> <p><a href="https://github.com/TianyuCodings/NanoJev">NanoJev</a> 用 Qwen3-0.6B、共享 scalar head 和 Choice set-attention，公开了模型、数据、训练与游戏评测。它会为每个候选构造一条 <code class="language-plaintext highlighter-rouge">state + question + candidate</code> 路径，由同一个 <code class="language-plaintext highlighter-rouge">Linear(H,1)</code> 打成 scalar logit，再对 K 个候选 softmax；Choice 后面的 set-attention 让候选之间可以相互比较。</p> <p>好处是候选文本直接参与语义编码，训练链路也完整；代价是 20 个候选通常意味着 20 份 state 路径，只是被合并进 batch，而不是 state 真正只编码一次。这恰好引出下一层问题：<strong>决策学会了以后，怎样组织一次 forward，才能不为每个问题或候选反复算公共上下文？</strong></p> <h2 id="7-this-that-model-与-kev重点从怎么打分转向state-怎样只算一次">7. this-that-model 与 kev：重点从“怎么打分”转向“state 怎样只算一次”</h2> <h3 id="71-this-that-model-用-answer-slot-复用-lm-head">7.1 this-that-model 用 answer slot 复用 LM head</h3> <p><a href="https://github.com/FLock-io/this-that-model">FLock-io/this-that-model</a> 在每个问题后留一个 answer slot。这里的 slot 不是已经填入 A/B/C 的 token，而是 <code class="language-plaintext highlighter-rouge">Answer: (</code> 末尾那个位置：backbone 跑完后，该位置自然有一个 hidden vector，程序直接拿它打分，不把答案写回输入。</p> <p>下面的图按箭头读：两个问题先各自提供一个答案位置；一次 backbone forward 取出 $h_1,h_2$；再从原模型 LM head 里抽出 A/B/C 三个 label 对应的权重行；hidden vector 与每一行做点积，得到每题三个 logits，最后 softmax。图中的 $H$ 是 hidden size，$N$ 是问题数，$K$ 是本题候选数。</p> <p><img src="/assets/img/posts/20260921-jev/diagram_answer_slot.png" alt="this-that-model 的 answer slot readout"/></p> <p>公式是：</p> \[p_k(j\mid x)=\operatorname{softmax}_j\left(\frac{\langle w_j,h_k\rangle}{\tau}\right).\] <p>通俗地说，$h_k$ 是“模型读完第 k 个问题后，对答案的内部表示”，$w_j$ 是原 LM head 里“把这个表示解释成标签 j”的方向；点积越大，表示越支持该标签。$\tau$ 是 temperature，用来控制分布尖锐程度。只在合法的 K 行上 softmax 后，非法字符串根本不在答案空间里。</p> <p>它的 <code class="language-plaintext highlighter-rouge">state_first</code> 把多个 question/slot 放在一条序列里，一次 backbone forward 读出多个结果；<code class="language-plaintext highlighter-rouge">schema_first</code> 则把固定问题模板放到前缀，处理许多不同 state 时可以复用 schema cache。不过 causal attention 下，后面的 slot 能看到前面问题文本，只是看不到前一个答案——所以“答案不相互条件依赖”不等于“问题分支完全隔离”。</p> <p>仓库在 68 个第三方记录问题上报告 accuracy 0.941、Brier 0.042，Jev 是 0.765/0.133；但 68 条太小，多个 frontier model 已经全对。下图横轴还把本地电费和商业 API 售价放在一起，作者自己提醒应读成约一个数量级，而不是视觉上的五个数量级。</p> <p><img src="/assets/img/posts/20260921-jev/thisthat_accuracy_cost.png" alt="this-that-model 的准确率与成本"/></p> <p>更大的 spatial benchmark 共同子集有 2250 题，this-that-model 为 0.844、Jev 为 0.803、Laya 为 0.345。读这张表必须同时看实验条件：this-that-model 训练见过全部 15 种 question shape，其他 hosted model 是 zero-shot，所以它证明的是“稳定任务形状可以训练出便宜专用模型”，不是 1.88B 全面超过 frontier model。</p> <p><img src="/assets/img/posts/20260921-jev/table3_p12.png" alt="空间决策 benchmark"/></p> <p>表里 <code class="language-plaintext highlighter-rouge">tokens/question=0</code> 只表示这些系统没有生成回答 token，不表示 forward 没有计算；<code class="language-plaintext highlighter-rouge">seen/unseen</code> 则按 this-that-model 自己的训练任务划分。把这两列连起来看，最有价值的信息不是谁排第三或第四，而是同一个 decision model 在训练覆盖与未覆盖任务上的差距。</p> <h3 id="72-kev-用-block-causal-mask-把共享-state-和隔离问题同时写进-attention">7.2 kev 用 block-causal mask 把共享 state 和隔离问题同时写进 attention</h3> <p><a href="https://github.com/jaredpalmer/kev">kev</a> 对“共享”处理得更严格：state prefix 只出现一次，后面接多个 question branch；block-causal attention mask 规定每个 branch 可以看 state 和自己，却看不到兄弟 branch。每个 option 的边界位置产生 key，<code class="language-plaintext highlighter-rouge">&lt;decide&gt;</code> 位置产生 query，再用 pointer head 做匹配。</p> <p>Pointer head 可以理解成“不是固定输出 255 个类别，而是拿一个问题向量逐个查询本次请求里的 K 个候选向量”。因此候选数变化时，模型参数不变；同时 <code class="language-plaintext highlighter-rouge">&lt;decide&gt;</code> 位于整组 options 之后，可以先看完整候选集合再做 listwise 判断。</p> <p>Kev 基于 Qwen3.5 提供 0.8B、4B、9B 权重，训练数据包含 Banking77、BoolQ、AG News、MNLI、SST-5、Yelp 等公开任务及合成 policy 数据，用 cross-entropy 训练 LoRA 和 pointer head，再在独立 calibration partition 上拟合 temperature。它比 this-that-model 多做了 branch isolation，代价是自定义 attention mask、训练和 serving 都更复杂。</p> <p>这一路已经把 causal LLM 改造成很像专用 decision network 了。再往前一步，自然会问：既然不需要左到右生成，为什么还背着 causal decoder？</p> <h2 id="8-laya-与-jevlike换成双向-encoder速度更直接泛化更依赖训练覆盖">8. Laya 与 Jevlike：换成双向 encoder，速度更直接，泛化更依赖训练覆盖</h2> <h3 id="81-laya-用每个-mask-位置代表一个候选">8.1 Laya 用每个 <code class="language-plaintext highlighter-rouge">[MASK]</code> 位置代表一个候选</h3> <p><a href="https://github.com/NandhaKishorM/laya">Laya</a> 使用 421M 参数的 ModernBERT-large，多语言版使用 322M 的 mmBERT-base。ModernBERT 是 bidirectional encoder：每个 token 一次 forward 就能看左右上下文，不需要遵守“只能看前文”的 causal mask，更适合判别任务。</p> <p>它把每个问题组织为：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[CLS] &lt;type&gt; instructions [SEP]
[MASK] option 0 [MASK] option 1 ... [SEP]
state [SEP]
</code></pre></div></div> <p>每个 <code class="language-plaintext highlighter-rouge">[MASK]</code> 是该候选的 marker。encoder 输出 <code class="language-plaintext highlighter-rouge">[B,L,H]</code> 后，程序抽出 K 个 marker vectors，再经过共享 MLP：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>LayerNorm(H) → Linear(H,H) → GELU → Linear(H,1)
</code></pre></div></div> <p>每个候选得到一个 scalar logit，K 个 logits softmax 成分布。README 报告 T4 上单问题约 33ms、batch 后约 7.2ms/question；context limit 为 512 或 1024 token。</p> <p>但 “many questions in one forward pass” 在当前源码中是<strong>把 N 条都含完整 state 的序列组成 batch</strong>，并不是 state hidden states 只算一次。Laya 还公开了 proper-scoring-rule reward，不过代码会把某些过度锐化的 checkpoint temperature 限制回 <code class="language-plaintext highlighter-rouge">[0.5,5.0]</code>，并明确提示相关 confidence 应视为未校准。</p> <h3 id="82-jevlike-让候选主动查询上下文结构比固定分类头更适合动态-options">8.2 Jevlike 让“候选主动查询上下文”，结构比固定分类头更适合动态 options</h3> <p><a href="https://github.com/vinnylarouge/jevlike">Jevlike</a> 让每个 option 变成 query，对 context tokens 做 attention，得到该 option 自己的 context vector，再由共享 scorer 产生一个分数。它的特点可以压成一句话：<strong>不是先把整段输入压成一个向量再固定分类，而是让每个动态候选主动去上下文里找支持自己的证据。</strong></p> <p>默认版本从 byte embedding 训练小 encoder，也支持冻结 Hugging Face encoder；数据格式就是 <code class="language-plaintext highlighter-rouge">context + 可变 options + label</code>。它还把相同 option-attention head 用到 Doom 按键和棋盘操作，说明这种 readout 可以扩展到视觉 controller。不过从头训练的小模型能否跨领域，仍然完全取决于数据覆盖。</p> <p>Encoder 路线省掉了生成能力和大词表 LM head，速度优势最直接；代价也同样直接：长上下文、世界知识、复杂规则和 OOD 泛化，不会因为模型被叫作 decision model 就凭空出现。</p> <h2 id="9-diffusion-openjev答案位置同时去噪不是普通-llm-的-next-token-logits">9. Diffusion OpenJev：答案位置同时去噪，不是普通 LLM 的 next-token logits</h2> <p><a href="https://github.com/razorback16/openjev">razorback16/openjev</a> 使用 <a href="https://huggingface.co/nvidia/diffusiongemma-26B-A4B-it-NVFP4">DiffusionGemma 26B-A4B NVFP4</a>，与第 5 节的 SemIf/openjev-sglang 不是同一种 readout。</p> <p>DiffusionGemma 是离散扩散语言模型：不是从左到右一次追加一个 token，而是在一块 token canvas 上反复把 masked/noisy 位置去噪。其基础结构来自 Gemma 4 26B A4B MoE，总参数 25.2B、每次激活约 3.8B，使用 encoder-decoder 与 bidirectional attention，并可按 256-token block 并行生成。MoE（Mixture of Experts）表示总参数分成多个专家，每个 token 只激活其中一部分，因此“25.2B 总参数”和“3.8B active”并不矛盾。</p> <p>OpenJev 利用这个结构预先建好一块 canvas，state 和问题文本固定，只有每个问题的答案位置保留为 mask。下图从左往右读：第一块 canvas 有三个 <code class="language-plaintext highlighter-rouge">[?]</code>；一次 diffusion read 同时计算三个位置；每个位置只保留自己的合法标签，如 yes/no、A/B/C、0/1/2；最后直接返回 typed answers。其他 token 被 pinned，不会在去噪过程中把 prompt 改坏。</p> <p><img src="/assets/img/posts/20260921-jev/diagram_diffusion_openjev.png" alt="Diffusion OpenJev 的 masked-canvas readout"/></p> <p>如果某个 slot 的 entropy 大于 0.1，默认实现会换新噪声最多重读四次并平均；也可以增加 1～8 个 denoise steps。多一步会增加 GPU 时间，但复用同一份 prompt prefill。其 vLLM 实现依赖 structured-read 扩展，MLX 版本则在进程内直接调用 diffusion decoder。</p> <h3 id="91-它没有-jev-专用训练集所谓泛化主要来自-diffusiongemma-本身">9.1 它没有 Jev 专用训练集，所谓泛化主要来自 DiffusionGemma 本身</h3> <p>这一点必须讲清楚：OpenJev 仓库没有再用一份 typed-decision dataset 微调 DiffusionGemma。它做的是<strong>推理结构改造</strong>，不是 decision post-training。NVIDIA 的 NVFP4 checkpoint 只是用 CNN/DailyMail 和 Nemotron Post-Training Dataset v2 做量化 calibration；底层 DiffusionGemma 的通用训练数据规模未披露，覆盖 web、code、math、图像等大规模语料。这些不是 OpenJev 为 Choice/Score/Noul 收集的训练数据。</p> <p>因此它的泛化上限来自基础模型已经学到的语言、视觉和知识能力，无法通过 OpenJev 代码本身“保证”。仓库 README 也明确要求在自己的 workload 上评估答案质量。它的 entropy confidence 仍只衡量分布是否尖锐，不证明 0.9 真的是 90%。重复带噪 read 可以降低采样方差，也不能修复基础模型系统性理解错误。</p> <p><a href="https://github.com/githubnext/localjev">LocalJev</a> 名字相似，但又是另一条路：普通 oMLX API 暴露不了 structured read，于是它让 DiffusionGemma<strong>生成 JSON 概率</strong>，再校验和重试。它 wire-compatible，却重新引入了文本生成、自报概率和 parser，不能与这里的 masked-slot readout 混为一谈。</p> <h2 id="10-总结真正的分水岭是训练信号共享计算和概率校准">10. 总结：真正的分水岭是训练信号、共享计算和概率校准</h2> <table> <thead> <tr> <th>路线</th> <th>代表实现</th> <th>一句话特点</th> <th>是否专门训练</th> <th>state 是否天然共享</th> </tr> </thead> <tbody> <tr> <td>生成式置信度</td> <td>ConfTuner</td> <td>保留回答能力，专门校准说出口的数字</td> <td>LoRA + Brier</td> <td>不适用</td> </tr> <tr> <td>零训练读 logits</td> <td>SemIf、openjev-sglang</td> <td>不改权重，直接把 A/B/C 原始分数变成概率</td> <td>否</td> <td>依赖 prefix cache / runtime</td> </tr> <tr> <td>训练候选分数</td> <td>Nimble、NanoJev</td> <td>用反事实数据、LoRA 或 scalar head 学会看证据</td> <td>是</td> <td>实现不同，常有重复 state</td> </tr> <tr> <td>共享 state 的专用 readout</td> <td>this-that-model、kev</td> <td>用 answer slot 或隔离分支一次读多个问题</td> <td>是</td> <td>是，但隔离语义不同</td> </tr> <tr> <td>Encoder + decision head</td> <td>Laya、Jevlike</td> <td>完全放弃生成，直接为动态候选打分</td> <td>是</td> <td>Laya 当前按问题复制 state</td> </tr> <tr> <td>Diffusion masked canvas</td> <td>Diffusion OpenJev</td> <td>多个答案空位在同一 canvas 上并行去噪</td> <td>否</td> <td>同一 read 内共享</td> </tr> </tbody> </table> <p>这几条路线没有简单的“谁最高级”。零训练读分最容易部署，却最依赖 prompt；Nimble/NanoJev 用数据换能力，但数据覆盖决定上限；this-that/kev 把重点推进到计算组织；Laya/Jevlike 用更小模型换速度；Diffusion OpenJev 则利用基础模型原本就能并行填 mask 的结构。</p> <p>真正共同的难点有三个：</p> <ol> <li><strong>训练信号</strong>：模型到底学过哪些 decision shape，换规则、换领域后是否还成立？</li> <li><strong>共享计算</strong>：所谓“一次请求”究竟是 state 只编码一次，还是 N 条序列被塞进同一个 batch？</li> <li><strong>概率校准</strong>：softmax、entropy confidence 和“长期真的对 90%”是三件不同的事。</li> </ol> <p>至少要分别看 accuracy、NLL、Brier score 和 ECE。Accuracy 看 top-1 选对多少；NLL 对“真实答案只拿到很低概率”惩罚很重；Brier 看完整预测分布与真实结果的平方距离；ECE 看每个 confidence 区间的预测把握与实际正确率差多少。一个 99% 的错误答案可能 entropy confidence 很高，却会在后三个指标上暴露问题。</p> <p>Jev 真正打开的不是一种唯一模型架构，而是一种模型分工：如果 workflow 节点最终只需要做决定，就没必要默认让 LLM 先写一段话。但程序准备根据 0.9 自动退款、执行命令或者拦截用户时，<strong>这个 0.9 是否真的意味着 90%，比省掉几个输出 token 更重要。</strong></p> <h2 id="11-参考资料完整链接">11. 参考资料（完整链接）</h2> <p>部分平台不会保留 Markdown 超链接，下面把正文用到的主要资料以完整 URL 再列一遍：</p> <ul> <li>Jev 官方发布博客：<a href="https://typesafe.ai/blog/introducing-system-one-models-and-jev">https://typesafe.ai/blog/introducing-system-one-models-and-jev</a></li> <li>Jev 官方文档：<a href="https://docs.typesafe.ai/">https://docs.typesafe.ai/</a></li> <li>ConfTuner（NeurIPS 2025）：<a href="https://arxiv.org/abs/2508.18847">https://arxiv.org/abs/2508.18847</a></li> <li>OpenJEV 公共访问平台：<a href="https://openjev.sh/">https://openjev.sh/</a>（镜像：<a href="https://openjev.tech/">https://openjev.tech/</a>）</li> <li>SemIf（原 OpenJev）网页：<a href="https://openjev.com/">https://openjev.com/</a></li> <li>SemIf 源码：<a href="https://github.com/TheoLeeCJ/SemIf">https://github.com/TheoLeeCJ/SemIf</a></li> <li>openjev-sglang：<a href="https://github.com/ekzhang/openjev-sglang">https://github.com/ekzhang/openjev-sglang</a></li> <li>Bespoke Nimble：<a href="https://github.com/bespokelabsai/nimble">https://github.com/bespokelabsai/nimble</a></li> <li>NanoJev：<a href="https://github.com/TianyuCodings/NanoJev">https://github.com/TianyuCodings/NanoJev</a></li> <li>this-that-model：<a href="https://github.com/FLock-io/this-that-model">https://github.com/FLock-io/this-that-model</a></li> <li>kev：<a href="https://github.com/jaredpalmer/kev">https://github.com/jaredpalmer/kev</a></li> <li>Laya：<a href="https://github.com/NandhaKishorM/laya">https://github.com/NandhaKishorM/laya</a></li> <li>Jevlike：<a href="https://github.com/vinnylarouge/jevlike">https://github.com/vinnylarouge/jevlike</a></li> <li>DiffusionGemma 版 OpenJev：<a href="https://github.com/razorback16/openjev">https://github.com/razorback16/openjev</a></li> <li>DiffusionGemma NVFP4 模型卡：<a href="https://huggingface.co/nvidia/diffusiongemma-26B-A4B-it-NVFP4">https://huggingface.co/nvidia/diffusiongemma-26B-A4B-it-NVFP4</a></li> <li>LocalJev：<a href="https://github.com/githubnext/localjev">https://github.com/githubnext/localjev</a></li> <li>Jev 黑盒分析：<a href="https://archerhume.com/posts/jevs-architecture-unmasked">https://archerhume.com/posts/jevs-architecture-unmasked</a></li> </ul> <hr/> <p>顺带扯一句题外话。这篇里的几条路线，本质上都在做“预训练模型的任务专用化”：有人改 loss，有人加 LoRA，有人换 decision head，也有人直接换掉生成式 backbone。《动手学 AutoML：从 NAS 到大语言模型优化实战》里没有专门讲 Jev，但第 8、9 章讨论了 LLM 压缩、模型融合以及 LLM 驱动的 AutoML，关注的是怎么系统地寻找这类改造方案。角度不同，目标很接近：不是所有任务都值得让完整大模型用同一种方式做到底。</p> <blockquote> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="类型化决策"/><category term="概率校准"/><category term="模型架构"/><summary type="html"><![CDATA[Jev 拆解：不生成文本的 AI，究竟怎么直接做决策？]]></summary></entry><entry><title type="html">Jev 拆解 | 一个不生成 token 的模型，怎么做决策？</title><link href="https://marsggbo.github.io/blog/2026/20260919-jev/" rel="alternate" type="text/html" title="Jev 拆解 | 一个不生成 token 的模型，怎么做决策？"/><published>2026-09-19T00:00:00+00:00</published><updated>2026-09-19T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260919-jev</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260919-jev/"><![CDATA[<h1 id="jev-拆解--一个不生成-token-的模型怎么做决策">Jev 拆解 | 一个不生成 token 的模型，怎么做决策？</h1> <blockquote> <p>参考资料：<a href="https://typesafe.ai/blog/introducing-system-one-models-and-jev">TypeSafe 官方发布博客</a> · <a href="https://docs.typesafe.ai/">TypeSafe 官方文档</a> · <a href="https://archerhume.com/posts/jevs-architecture-unmasked">Jev 黑盒分析</a> · <a href="https://github.com/TheoLeeCJ/SemIf">SemIf</a> · <a href="https://github.com/TianyuCodings/NanoJev">NanoJev</a> · <a href="https://github.com/jaredpalmer/kev">kev</a></p> </blockquote> <hr/> <h2 id="1-为什么一个只会判断的模型反而有这么多地方能用">1. 为什么一个“只会判断”的模型，反而有这么多地方能用</h2> <p>先看几个场景。</p> <p><strong>客服系统</strong>收到一句“信用卡被重复扣款了”，真正要做的不是写一篇分析，而是判断它该去账单、退款还是账户安全队列；<strong>搜索和推荐</strong>拿到几百个候选结果，也不是再生成一份网页，而是判断哪一个应该排在前面；<strong>Agent</strong>准备调用工具时，需要判断的是“这一步是否安全”“该调用哪个 API”“要不要先问用户确认”；放到游戏里，则变成判断下一步往东、西、南、北哪个方向走。</p> <p>这些任务表面上差很多，底层其实是同一件事：</p> <blockquote> <p><strong>给定当前状态和一组候选，判断每个候选有多大可能是对的。</strong></p> </blockquote> <p>分类并不新，为什么这件事现在又变得重要了？因为 LLM 进入 Agent 和 workflow 之后，很多任务已经不是“帮我写一段话”，而是“下一步到底做什么”。这类判断会出现在一条 workflow 的每个节点上：先判断请求属于哪一类，再判断该调哪个工具，执行后还要判断结果是否可信。单次分类看起来不起眼，但一天跑几百万次以后，生成式 LLM 的解码延迟、输出成本和格式错误就都放大了。</p> <p>传统做法通常是把候选写进 prompt，让 LLM 最后回答 <code class="language-plaintext highlighter-rouge">A/B/C</code> 或一段 JSON。问题是答案明明只有一个选项，模型仍然要一个 token 一个 token 地生成；生成后还要检查 JSON 是否合法、选项是否越界，而且 next-token 概率也不天然等于“这个判断有多可信”。</p> <p>TypeSafe AI 今年 9 月发布的 <strong>Jev</strong>，就是冲着这个问题来的。它不生成文本，只接收一段 <code class="language-plaintext highlighter-rouge">state</code>（当前状态）和若干个 <code class="language-plaintext highlighter-rouge">question</code>（要做的判断），直接返回完整概率分布。例如：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>state: 用户说“信用卡被重复扣款了”
question: 应该路由到哪个队列？

billing       0.91
refund        0.06
account       0.02
security      0.01
</code></pre></div></div> <p>概率分布比单独返回一个 <code class="language-plaintext highlighter-rouge">billing</code> 更有用：0.91 时可以直接执行；如果前两项分别是 0.43 和 0.41，系统就知道这次判断并不稳，可以追问用户或转人工。<strong>Jev 真正卖的不是“会分类”，而是把自然语言理解变成一个能直接驱动下一步动作的概率接口。</strong></p> <p>官方展示的应用基本都沿着这条线展开：</p> <ul> <li><strong>智能家居</strong>：一次判断请求类型、房间、设备和动作，直接控制后续执行；</li> <li><strong>Wikiracing / 检索重排</strong>：面对一个页面里的几百个链接，判断下一跳选哪个；</li> <li><strong>实时游戏 AI</strong>：根据当前局面判断移动方向或是否开枪；</li> <li><strong>Agent trace 和安全事件判断</strong>：决定一步操作是否异常、是否应该拦截；</li> <li><strong>发票抽取、客服路由</strong>：把自由文本映射成有限字段或业务队列。</li> </ul> <p>官方博文给出的数字是自家工作流里 <strong>20–200× 更快、40–400× 更便宜</strong>。这类数字当然要结合输入长度、网络位置和对比方法看，不能直接当通用 benchmark；但方向很好理解：<strong>如果任务最终只需要做决定，就没必要为“生成一段答案”付完整的解码成本。</strong></p> <p>下图是 NanoJev 仓库里的对比演示：Jev、NanoJev 和未经微调的 Qwen3-0.6B 同时走迷宫。这里模型每一步做的事情都很简单——给几个可行方向分配概率——但这些连续的小判断最终决定了能不能走到出口。</p> <p><img src="/assets/img/posts/20260919-jev/000_side_by_side_maze.png" alt="Jev vs NanoJev vs 未微调 Qwen 迷宫探索对比"/></p> <p>理解了“分类其实是 workflow 的控制面”之后，再看它为什么要设计 Choice、Score 和 Boolean 三种接口，以及一个模型怎么应付每次都不同的候选集合，就顺了。</p> <h2 id="2-jev-把判断拆成了三种接口">2. Jev 把判断拆成了三种接口</h2> <p>一次请求由两部分组成：<code class="language-plaintext highlighter-rouge">state</code>（字符串/JSON/数组，多个问题可共用）和 <code class="language-plaintext highlighter-rouge">questions</code>（每个问题有自己的 <code class="language-plaintext highlighter-rouge">type</code>、<code class="language-plaintext highlighter-rouge">instructions</code>、<code class="language-plaintext highlighter-rouge">criteria</code>）。<strong>问题之间互相独立，不能读取彼此答案</strong>，这是接口层面的硬约束。</p> <p>三种类型（primitives）：</p> <table> <thead> <tr> <th>类型</th> <th>输入 criteria</th> <th>输出</th> </tr> </thead> <tbody> <tr> <td><strong>Choice</strong></td> <td>2–255 个候选，每个是 <code class="language-plaintext highlighter-rouge">候选名: 描述</code></td> <td>每个候选的完整概率分布 + <code class="language-plaintext highlighter-rouge">confidence</code></td> </tr> <tr> <td><strong>Score</strong></td> <td>2–10 个<strong>有序</strong>等级描述</td> <td>每级概率分布 + 期望分 <code class="language-plaintext highlighter-rouge">Σ i·p_i</code> + legend</td> </tr> <tr> <td><strong>Noul</strong>（SDK 里叫 Boolean）</td> <td>可选 true/false 说明</td> <td>单个概率值 <code class="language-plaintext highlighter-rouge">P(是)</code>，没有独立 confidence</td> </tr> </tbody> </table> <p><img src="/assets/img/posts/20260919-jev/002_io.png" alt="Jev 三种 primitive 的输入输出示意"/></p> <p>这套接口里有几个设计挺容易被忽略。</p> <p>首先，<strong>Choice 的问题 ID 不会进入模型</strong>。比如你把一个问题命名成 <code class="language-plaintext highlighter-rouge">route</code> 或 <code class="language-plaintext highlighter-rouge">urgency</code>，这个名字只负责在返回结果里对上答案，模型真正看到的是 <code class="language-plaintext highlighter-rouge">instructions</code> 和候选描述。所以问题不能只写一句“请判断”，指望模型从 <code class="language-plaintext highlighter-rouge">route</code> 这个 key 猜出你想问什么。</p> <p>其次，<strong>Score 不会把等级数字喂给模型，也不会让某一级看到相邻等级的描述</strong>。假设四档严重程度分别叫 0、1、2、3，模型判断“核心功能受阻”时，并不知道它前面是“次要功能受阻”、后面是“服务完全中断”。每一档先独立得到分数，最后才把概率按顺序加权成期望值。这样做是为了避免模型只靠“第 3 档听起来比第 2 档严重”这种位置线索来猜。</p> <p>最后，<strong>Noul 没有单独的 confidence 字段</strong>，只返回 <code class="language-plaintext highlighter-rouge">P(true)</code>。Choice 和 Score 的 confidence 也不是模型额外预测出来的另一个数，而是根据最终概率分布算出来的。</p> <p>上下文预算：state + 全部 questions 合计 64k token，state + 最长单个 question 32k token；定价 <strong>输入 $0.042/百万 token，输出免费</strong>（本来就不产生输出 token）。</p> <p>confidence 公式（来自 MIT 协议的 LLM adapter，是 adapter 实现不是服务端证明，但可合理参考）：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>choice_confidence = (max(p) - 1/K) / (1 - 1/K)      # K 是候选数

m = argmax(p); d = Σ p_i * |i - m|; d_uniform = (1/K) * Σ |i - (K-1)/2|
score_confidence = max(0, 1 - d / d_uniform)
</code></pre></div></div> <p>所以这里的 <strong>confidence 衡量的是“这组概率有多集中”，不是“这次答案有多大概率正确”</strong>。分布越接近 one-hot，confidence 越接近 1；越接近均匀分布，就越接近 0。这两个概念不能混在一起：一个模型完全可能非常自信地选错。</p> <p>接口讲得很清楚，但模型内部怎么实现，官方基本没说。到目前为止，下面这些信息都没有公开：</p> <ul> <li>网络具体结构、参数量（官方只说”不是小模型，也不是传统 LLM”，没给数字）</li> <li><strong>RLCD</strong> 的 reward 设计、loss、采样算法、<strong>训练数据规模和来源</strong></li> <li>训练代码本身</li> </ul> <p>官方倒是讲了 workflow eval 怎么做：ground truth 来自 GPT-6 Astra 和 Claude Fable 5.1 高推理模式的共识标注，Jev 在这套 eval 上的分数约为 67.8%，而且这些评测集不进入训练集。但这只能回答“怎么评”，回答不了“怎么训”。模型结构和训练流程这块，只能把黑盒实验与不同开源实现放在一起看，拼出几种可能的答案。</p> <h2 id="3-jev-到底是什么模型先把官方事实和社区复现分开">3. Jev 到底是什么模型？先把官方事实和社区复现分开</h2> <p>看到这里，一个最容易产生的误解是：Jev 是不是一个换了分类头的 LLM？</p> <p><strong>现在还不能这么下结论。</strong> TypeSafe 没有公开权重、网络结构、参数量、基座模型和 RLCD 的具体训练算法。官方确认的只有下面几件事：</p> <ol> <li>Jev 是从 pretrained language model 继续 post-training 得到的 System One model；</li> <li>它不生成回答文本，而是直接返回 Choice、Score、Noul 的数值结果；</li> <li>同一份 state 只输入一次，多个 question 并行、彼此隔离地读取它；</li> <li>训练目标叫 RLCD（Reinforcement Learning for Calibrated Decisions），目标是让高概率对应更高的真实命中率；</li> <li>当前 <code class="language-plaintext highlighter-rouge">jev-1.13.0</code> 只支持文本，整次请求 64k token，state 加最长单个 question 不超过 32k。</li> </ol> <p>所以更准确的说法是：<strong>Jev 不是某个已经公开结构的模型名字，而是一款闭源的 decision model 产品。</strong> 它保留语言模型理解自然语言的能力，但把输出合同从“继续写 token”改成了“对有限答案空间给概率”。至于内部用的是 decoder、encoder、MoE，还是专门设计的 readout，官方都没有确认。</p> <p>这也是为什么网上的“Jev 复现”要分两类看：一类只是做出了相同 API；另一类才真的尝试复现“共享 state、并行 question、直接读概率”这套计算方式。两者都不能证明自己还原了 Jev。</p> <h3 id="31-现在关注度较高的开源实现有哪些">3.1 现在关注度较高的开源实现有哪些</h3> <p>截至 2026 年 9 月 20 日，GitHub 上关注度较高、而且确实提供本地模型或推理代码的项目主要有这些。Star 变化很快，表里的数字只对应这次检查时间。</p> <table> <thead> <tr> <th>项目</th> <th style="text-align: right">Star</th> <th>它实际做了什么</th> <th>适合拿来做什么</th> </tr> </thead> <tbody> <tr> <td><a href="https://github.com/TheoLeeCJ/SemIf">SemIf</a></td> <td style="text-align: right">2.2k</td> <td>直接读取 Qwen 等开源模型的选项 token logits，不训练新的 Jev 模型</td> <td>最成熟的 zero-shot baseline，benchmark 和复现材料比较全</td> </tr> <tr> <td><a href="https://github.com/TianyuCodings/NanoJev">NanoJev</a></td> <td style="text-align: right">1.2k</td> <td>Qwen3-0.6B 加 scalar head 和 Choice set-attention，带数据、训练、评测和 serving</td> <td>看完整训练 pipeline，以及动态候选怎么实现</td> </tr> <tr> <td><a href="https://github.com/vinnylarouge/jevlike">Jevlike</a></td> <td style="text-align: right">1.0k</td> <td>每个 option 查询 context，再用共享打分器输出概率；还有 Doom/Chess 视觉例子</td> <td>看最小模型和多模态 option scoring</td> </tr> <tr> <td><a href="https://github.com/bespokelabsai/nimble">Bespoke Nimble</a></td> <td style="text-align: right">848</td> <td>Qwen3.5-9B + LoRA，读取单 token answer code；公开权重和对比式数据构造</td> <td>看数据 recipe 和相对强的开源效果</td> </tr> <tr> <td><a href="https://github.com/jaredpalmer/kev">kev</a></td> <td style="text-align: right">752</td> <td>state 只编码一次，question 用 block-causal mask 隔离，pointer head 对动态 options 打分</td> <td>目前最值得参考的“架构型复现”</td> </tr> <tr> <td><a href="https://github.com/githubnext/localjev">LocalJev</a></td> <td style="text-align: right">553</td> <td>把 typed questions 改写成 prompt，让 DiffusionGemma 生成 JSON 概率</td> <td>只适合 API 兼容，不是 generation-free 复现</td> </tr> </tbody> </table> <p>这些仓库基本都是 Jev 发布后几天内出现的，Star 涨得快不等于技术已经被充分验证。这里把 Star 当成“社区在关注什么”，不把它当成正确性排名。真正需要看的还是：有没有公开权重和数据，state 是否只计算一次，question 是否真的隔离，概率是不是直接 readout，以及 benchmark 能不能复现。</p> <p>这里不能简单按 Star 选一个“最正确”的。<strong>SemIf 关注度最高，但它本质上是无需训练的 option-logit baseline；NanoJev 的训练链路最完整；kev 对“state 只算一次、question 相互隔离、动态 option readout”这几个 Jev 特征还原得更到位。</strong></p> <p>如果目的是跑一个开源 baseline，优先用 SemIf；如果要研究训练方法，NanoJev 和 Nimble 更合适；如果要理解 Jev 最可能怎样组织一次 forward，kev 是目前更好的参考。</p> <h3 id="32-nanojev-是正确实现吗">3.2 NanoJev 是正确实现吗</h3> <p>从代码、测试和仓库保存的实验记录看，NanoJev 这套实现内部是自洽的，它确实能完成下面这条链路：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>state + question + candidate
  → Qwen3-0.6B hidden state
  → LayerNorm + Linear(d, 1)
  → 每个 candidate 一个 logit
  → softmax 得到完整分布
</code></pre></div></div> <p>它也真的支持动态候选、三种 primitive、训练、评测和服务，不是只套了一层接口的 demo。问题在于：<strong>“能实现 Jev 的输入输出”不等于“实现方式就是 Jev”。</strong></p> <p>NanoJev 会给每个候选复制一遍完整的 <code class="language-plaintext highlighter-rouge">state + question</code>，然后把所有候选路径塞进同一个 batch。比如 20 个候选，本质上还是 20 条完整序列，只是合并成一次 GPU forward。它没有做到 state 在模型内部只编码一次，所以候选越多、state 越长，重复计算越明显。</p> <p>Choice 的候选最开始也是独立打分的。为了让候选之间互相影响，它后面又补了一个 set-attention head。这能解决一部分 listwise interaction，但属于 NanoJev 自己的设计，不是官方披露的 Jev 结构。</p> <p>但这不等于它已经经过了独立、完整的第三方复现实验。对 NanoJev 更合适的评价是：</p> <blockquote> <p><strong>这是一个实现链路完整、可以运行的 Jev-inspired 方案，但没有证据说明它还原了 Jev，也不能仅凭仓库自己的结果证明所有质量与性能结论。</strong></p> </blockquote> <p>原文如果把 NanoJev 写成“目前唯一能看到 Jev 执行细节的参考”，现在已经不准确了。它仍然值得讲，但应该放在多个实现中对照，而不是把它的 scalar head 当成 Jev 本身。</p> <h3 id="33-更接近-jev-行为的实现state-只算一次question-各走一条分支">3.3 更接近 Jev 行为的实现：state 只算一次，question 各走一条分支</h3> <p>2026 年 9 月 17 日，有人用约 10,000 次 API probe 做了一份黑盒分析：<a href="https://archerhume.com/posts/jevs-architecture-unmasked">Jev’s Architecture Unmasked</a>。这些实验不是官方披露，但观察到了几个很有意思的信号：</p> <ul> <li>state 很长时，增加 question 的成本远低于把 state 重复请求多次；</li> <li>question 调换顺序后彼此不会读到对方，符合“共享 state、隔离分支”；</li> <li>添加一个新 option 后，旧 options 之间的相对赔率也可能改变，说明 Choice 不像简单的独立打分再 softmax；</li> <li>200 个 option 的返回时间和 2 个 option 接近，API 里的 <code class="language-plaintext highlighter-rouge">output_tokens</code> 更像计费/序列化数字，不能拿它当生成速度；</li> <li>32k 单分支限制和 64k 整体限制，很像一份 state 加多条 question branch 被打包在同一请求中。</li> </ul> <p>基于这些现象，一个更像样的推断结构是：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>                    ┌→ question 1 + options → decide → distribution 1
state → shared KV ──┼→ question 2 + options → decide → distribution 2
                    └→ question 3 + options → decide → distribution 3
</code></pre></div></div> <p>state 只计算一次。每个 question 都能看见 state，却看不见其他 question。实现这种信息流可以用 block-causal attention mask：state 区域正常做 causal attention；question 只能读取 state 和自己分支里的 token；兄弟分支之间直接 mask 掉。</p> <p>kev 就沿着这条思路实现。它把整个请求打包成：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;state&gt; ...
&lt;q&gt; instructions &lt;opt&gt; A &lt;/opt&gt; &lt;opt&gt; B &lt;/opt&gt; ... &lt;decide&gt;
&lt;q&gt; instructions &lt;opt&gt; A &lt;/opt&gt; &lt;opt&gt; B &lt;/opt&gt; ... &lt;decide&gt;
</code></pre></div></div> <p>每个 option 结束位置有一个 hidden state，<code class="language-plaintext highlighter-rouge">&lt;decide&gt;</code> 位置也有一个 hidden state。pointer head 用 <code class="language-plaintext highlighter-rouge">&lt;decide&gt;</code> 去查询所有 option：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>q = Wq · h_decide
k_i = Wk · h_option_i
z_i = qᵀk_i / √d
p = softmax([z_1, z_2, ..., z_K])
</code></pre></div></div> <p>这和 NanoJev 的最大区别是：<strong><code class="language-plaintext highlighter-rouge">&lt;decide&gt;</code> 在所有 options 后面，因此它做判断时已经看过完整候选集合。</strong> 候选 A 的分数不再只取决于 A 自己，也可以受 B、C 和“以上都不对”影响，更符合 Choice 的 listwise 语义。</p> <h3 id="34-2-到-255-个选项输出维度为什么能跟着变">3.4 2 到 255 个选项，输出维度为什么能跟着变</h3> <p>pointer head 也解释了动态候选问题。它不是一个固定的 <code class="language-plaintext highlighter-rouge">Linear(d, 255)</code>，而是对这次请求里的每个 option hidden state 都算一次相似度。</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>2 个 options   → [z1, z2]               → 2 个概率
5 个 options   → [z1, z2, z3, z4, z5]  → 5 个概率
255 个 options → [z1, ..., z255]         → 255 个概率
</code></pre></div></div> <p>模型参数 <code class="language-plaintext highlighter-rouge">Wq</code> 和 <code class="language-plaintext highlighter-rouge">Wk</code> 没有变化，变化的只是 option hidden state 的数量。来 K 个 option，就得到 K 个 key，最后 softmax 的输入自然也是 K 维。</p> <p>NanoJev 用“同一个 scalar head 给 K 条候选路径分别打分”，kev 用“一个 decide query 与 K 个 option key 做 pointer score”。两种方法都能动态输出 K 个概率，但后者能先看完整 option set，而且更容易共享 state 计算。</p> <p>当然，官方也可能使用固定的 256-slot head：最多 255 个 options，再留一个特殊 slot。黑盒行为无法排除这种方案。<strong>现在能够确认的是动态输出和 listwise interaction，不能确认内部究竟是 pointer head、固定 slot，还是另一种 readout。</strong></p> <h3 id="35-rlcd-到底训练了什么">3.5 RLCD 到底训练了什么</h3> <p>官方对 RLCD 的描述只有目标，没有 recipe。它想让预测满足：模型给 0.8 的事件，在大量样本中应该约有 80% 真正发生。这叫 calibration，关注的是概率和真实频率是否一致，而不只是 top-1 选没选对。</p> <p>但下面这些关键细节都没公开：</p> <ul> <li>reward 具体怎么算；</li> <li>用的是 policy gradient、交叉熵、Brier loss，还是几种目标的组合；</li> <li>label 来自人工、规则、真实 outcome，还是 teacher model；</li> <li>backbone 哪些参数更新；</li> <li>训练数据规模和领域组成。</li> </ul> <p>开源实现走了不同路线。NanoJev 同时尝试 hard label、teacher distribution 和一套 RLCD-inspired proper reward；Nimble 用对比式数据构造，让一条关键事实变化时答案也随之翻转；kev 用公开分类/NLI/政策规则数据做交叉熵训练；SemIf 干脆不训练，只读取 base model 的 option logits。</p> <p>这几条路线都能输出概率，但“softmax 出来一个数”不代表概率已经 calibrated。要验证它，必须在独立数据上看 Brier score、NLL 和 reliability curve，而不是只看 accuracy。Brier score 衡量预测概率和真实结果之间的平方误差，越低越好；NLL 看模型给真实答案分配了多少概率，同样越低越好。</p> <p>从现有公开结果看，Jev 仍然领先。kev 在 764 条 out-of-domain 记录上的 accuracy 是 0.79–0.80，Jev 是 0.857；Bespoke Nimble 在 324 条 held-out 样本上达到 90.1% reference-label agreement，Jev 是 93.2%。这些数字来自各项目自己的实验，数据和协议不同，不能横向排成统一榜单，但至少说明：<strong>开源实现已经能接近这套接口，离证明“复现了 Jev”还有一段距离。</strong></p> <h2 id="4-如果把这套判断能力接到音频上会发生什么">4. 如果把这套判断能力接到音频上，会发生什么</h2> <p>Jev 目前接收的 <code class="language-plaintext highlighter-rouge">state</code> 本质上还是文本或 JSON。那能不能把它扩展到多模态？拿 ASR 来说，这件事确实有价值，但不是所有环节都需要音频。</p> <p>比如一段语音被识别成：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>我们把 Kubernetes 部属到两张 A100 上。
</code></pre></div></div> <p>只看文本，模型很容易根据上下文猜到“部属”应该改成“部署”。这种错误用现有 Jev 就能做：把 ASR 的多个候选放进 Choice，让它根据上下文重新排序。</p> <p>但换一个例子就不行了：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>A. 这批卡已经到齐了
B. 这批卡已经到期了
</code></pre></div></div> <p>两句话在语言上都成立，真正的证据藏在音频里。text-only 模型再聪明，也只能猜哪句话更常见，无法判断说话人发的是“齐”还是“期”。<strong>当错误来自声学歧义，而不是语言不通顺时，多模态就不是锦上添花，而是缺失的证据。</strong></p> <h3 id="41-不要一上来就改模型先看纯文本判断能做到哪一步">4.1 不要一上来就改模型，先看纯文本判断能做到哪一步</h3> <p>第一步甚至不用训练 audio Jev。让 ASR 输出 N-best，也就是同一段音频下概率最高的若干条转写，再把这些候选、前后文、热词和 ASR score 塞进 <code class="language-plaintext highlighter-rouge">state</code>：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>state:
  context: 技术周会，正在讨论服务器部署
  hotwords: [Kubernetes, A100]
  candidates:
    A: 我们把 Kubernetes 部署到两张 A100 上
    B: 我们把 Kubernetes 部属到两张 A100 上

question: 哪条转写最符合当前上下文？
</code></pre></div></div> <p>这其实是一个 context-aware reranker：ASR 负责“听”，Jev 负责利用语言和业务上下文重排。专业术语、中英混说、人名和产品名这类错误，往往仅靠热词与上下文就能修掉一部分，而且不需要碰音频模型。</p> <p>只有当 text-only reranker 已经到顶、剩下的 bad case 主要是“两个候选都说得通，但发音不同”时，再把音频接进来。否则一开始就做多模态，训练成本上去了，却说不清收益究竟来自音频，还是来自更强的文本模型。</p> <h3 id="42-真要接音频可以复用-asr-的-audio-encoder">4.2 真要接音频，可以复用 ASR 的 audio encoder</h3> <p>一个直接的实现，是把 NanoJev 的文本 backbone 换成 Qwen3-ASR 这类已经能处理音频的 backbone：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>音频 ─→ audio encoder ─→ audio embedding ─┐
                                          ├→ backbone → scalar head → logit
候选转写 + 问题 + 上下文 ─────────────────┘
</code></pre></div></div> <p>音频只编码一次，候选 A、B、C 分别接在同一份 audio embedding 后面。每条候选仍然经过同一个 <code class="language-plaintext highlighter-rouge">Linear(d_model, 1)</code> 得到一个 logit，最后对当次的 K 个 logit 做 softmax。这样前面讲的动态候选机制不需要改：有 4 条 ASR 候选就返回 4 个概率，有 10 条就返回 10 个。</p> <p>真正需要优化的是重复计算。音频通常比文本长，如果 10 个候选都把音频从头编码一遍，成本很快就上去了。更合理的做法是共享 audio embedding，进一步共享公共前缀的 KV cache，只让不同的候选文本走各自的分支。前面提到的 tree attention，放到这里反而比纯文本场景更有价值，因为最贵的音频前缀只需要算一次。</p> <p>训练也不需要从头做。先冻结 audio encoder，只训练 scalar head 和少量 backbone 参数，让模型学会“这段音频更支持哪条文字”；如果效果不够，再逐步解冻。训练数据可以直接从现有 ASR 数据构造：标准转写是正候选，beam search、不同 ASR 模型和同音词替换产生困难负候选。</p> <h3 id="43-在-asr-pipeline-里它最适合做三类决定">4.3 在 ASR pipeline 里，它最适合做三类决定</h3> <p><strong>第一类是 N-best 选择。</strong> ASR 不再只吐一个结果，而是给出几条候选；audio-text Jev 同时看音频和上下文，选出最匹配的一条。这是最直接的准确率增益。</p> <p><strong>第二类是局部纠错。</strong> 两条转写通常只有一两个位置不同，没必要把整句话重新判断。先对齐候选，找到“部署/部属”这种分歧片段，再截取对应音频，只判断这个位置应该保留还是替换。判断范围越小，候选越明确，训练数据也越容易构造。</p> <p><strong>第三类是决定要不要继续花算力。</strong> 如果第一名概率是 0.96，可以直接接受；如果前两名是 0.41 和 0.39，说明模型自己也拿不准，此时再带热词重跑、换更大的 ASR，或者交给人工。它带来的提升不一定全来自“选得更准”，也来自<strong>知道什么时候不该瞎选</strong>。</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>音频 → 小 ASR 生成 N-best → audio-text judge
                              ├─ 高置信：直接输出
                              ├─ 有明确替代项：局部纠错
                              └─ 低置信：热词重跑 / 大模型 / 人工
</code></pre></div></div> <p>所以多模态版本最值得先验证的，不是“给音频做一个万能分类器”，而是一个很具体的闭环：<strong>ASR 负责产生候选，audio-text judge 负责选候选和判断是否拒绝。</strong></p> <p>实验时先看两个上限。第一，正确转写有没有出现在 N-best 里；如果根本不在，judge 再强也救不回来。第二，text-only judge 已经能修掉多少；只有 audio-text 版本在同一批候选上继续提升，才能证明听音频确实有用。最终同时报告 1-best、N-best oracle、text-only rerank 和 audio-text rerank 的 CER/WER——中文按字、英文按词计算需要多少次增删改才能变成标准答案，都是越低越好。</p> <p>这套思路也能往图像、视频上迁移，但没必要为了“多模态”三个字强行扩展。判断所需的证据如果已经完整存在于文本里，就继续用文本；<strong>只有答案取决于声音、画面或时序信息时，才值得把对应模态接进 decision model。</strong></p> <h2 id="5-小结jev-和现有分类方法到底差在哪">5. 小结：Jev 和现有分类方法到底差在哪</h2> <p>读到这里再回头看，Jev 解决的并不是“模型能不能分类”——BERT 时代就能做，今天拿任意 LLM 回答 A/B/C 也能做。它真正想解决的是另一件事：</p> <blockquote> <p><strong>能不能让一个理解自然语言指令、候选集合每次都可以变化的模型，像专用分类器一样直接输出概率，而不是先生成文字再解析。</strong></p> </blockquote> <p>放在一起看，差别就清楚了：</p> <table> <thead> <tr> <th>方法</th> <th>候选从哪里来</th> <th style="text-align: right">候选数</th> <th>怎么得到答案</th> <th>主要问题</th> </tr> </thead> <tbody> <tr> <td>固定分类头</td> <td>训练时写死</td> <td style="text-align: right">固定</td> <td>一次输出 N 类概率</td> <td>换类别就要改 head、重新训练</td> </tr> <tr> <td>LLM 生成 A/B/JSON</td> <td>请求时给</td> <td style="text-align: right">可变</td> <td>自回归生成后解析</td> <td>有解码成本和格式失败</td> </tr> <tr> <td>受限解码</td> <td>请求时给</td> <td style="text-align: right">可变</td> <td>mask 非法 token 后生成</td> <td>长文本候选、多 token 候选处理麻烦</td> </tr> <tr> <td>Cross-encoder reranker</td> <td>请求时给</td> <td style="text-align: right">可变</td> <td>每个候选独立打分</td> <td>K 个候选通常要做 K 次独立判断</td> </tr> <tr> <td>Jev 式决策</td> <td>请求时给</td> <td style="text-align: right">最多 255</td> <td>直接返回 typed probability readout；具体内部结构未公开</td> <td>依赖专门训练，开源方案尚未追平官方结果</td> </tr> </tbody> </table> <p>动态候选在开源实现里至少有两条路：NanoJev 用共享 scalar head 分别处理 K 条候选路径，kev 用一个 decide query 对 K 个 option hidden states 做 pointer readout。两者都不需要固定 255 维分类头。真实 Jev 也可能采用固定 slot 或另一种 readout，黑盒实验还不足以把结构唯一确定下来。</p> <p>官方 Jev 和这些开源实现仍然要分开。TypeSafe 只公开了接口、上下文限制、价格和部分评测，网络结构、参数量、RLCD recipe 和训练数据都没有公开。NanoJev 的 scalar head、kev 的 pointer head、SemIf 的 option-token logits，都是实现同一类产品接口的不同路线，<strong>谁都不能据此宣称还原了真实 Jev。</strong></p> <p>性能上也不能把“没有自回归解码”理解成“候选和问题没有成本”。NanoJev 会重复计算每条候选的 state；kev 虽然共享 state，question branch 和 option readout 仍然要算；SemIf 的共享前缀路径也有数值漂移记录。省掉的是逐 token 生成的依赖链，不是把所有计算都省掉。</p> <p>所以这类模型更准确的定位，不是一个神秘的新分类器，而是 <strong>workflow 的决策层</strong>：上游把当前状态和合法选项交给它，它返回一个可以直接用于执行、拒绝或升级处理的概率分布。智能家居、Agent 工具选择、检索重排和游戏控制看起来完全不同，最后都能归到这个接口上。这才是一个简单分类任务突然变得有价值的原因。</p> <p>顺带扯一句题外话:这类”用 decision head 替代生成”的思路,是在<strong>判断这个任务压根不需要生成</strong>,而不是优化生成过程本身。这两年一个挺明显的趋势就是越来越多团队开始琢磨”这个子任务到底要不要走完整自回归”。我们把 LLM 架构自动化、模型压缩这些相关积累整理成了一本书,第 9 章正好讲 LLM 驱动的 AutoML 和 LLM Agent 结合的方向,跟这篇聊的”任务专用化改造”角度不同但目标相近。</p> <blockquote> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote> <p>欢迎评论区交流,如果发现文章里哪些细节和你了解的官方情况不一致,也欢迎指出来。</p>]]></content><author><name></name></author><category term="LLM"/><category term="模型架构"/><category term="推理效率"/><category term="源码解析"/><summary type="html"><![CDATA[Jev 拆解 | 一个不生成 token 的模型，怎么做决策？]]></summary></entry><entry><title type="html">StepAudio 3 Gen 把通用音频生成压成纯离散 token 预测，TTS 人味评测赢遍商业模型”</title><link href="https://marsggbo.github.io/blog/2026/20260916-stepaudio3gen/" rel="alternate" type="text/html" title="StepAudio 3 Gen 把通用音频生成压成纯离散 token 预测，TTS 人味评测赢遍商业模型”"/><published>2026-09-16T00:00:00+00:00</published><updated>2026-09-16T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260916-stepaudio3gen</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260916-stepaudio3gen/"><![CDATA[<h1 id="stepaudio-3-gen-把通用音频生成压成纯离散-token-预测tts-人味评测赢遍商业模型">StepAudio 3 Gen 把通用音频生成压成纯离散 token 预测，TTS 人味评测赢遍商业模型</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2609.12945">StepAudio 3 Gen Technical Report</a>（arXiv 2609.12945，StepFun-Audio Team）</p> <p>试听 demo：<a href="https://stepaudiollm.github.io/step-audio-3-gen/">stepaudiollm.github.io/step-audio-3-gen</a></p> </blockquote> <hr/> <h2 id="1-前言一段三种声音混在一起的音频暴露了整条流水线的裂缝">1. 前言：一段”三种声音混在一起”的音频，暴露了整条流水线的裂缝</h2> <p>设想一个很常见的需求：给 60 秒的短视频配一条音频，里面有两个人对话、有开门和脚步声、背景还要压一段不抢戏的钢琴。</p> <p>今天你要做这件事，基本得拼三条流水线：对话走 TTS（text-to-speech，文字转语音），音效走 text-to-audio 模型，背景音乐走 text-to-music 模型。三条流水线各有各的<strong>输入表征</strong>（一条吃文本 + 说话人参考，一条吃 caption，一条吃风格描述）、各有各的<strong>生成方式</strong>（有的跑 diffusion，有的跑 flow matching）、各有各的<strong>时间控制接口</strong>（谁负责把这三个音轨对齐到时间轴上？最后往往是人手在 Audition 里拖）。</p> <p>问题不在于”哪个模型不够好”。单看每一块，今天的专用模型都很能打。问题在于<strong>它们之间没有共同语言</strong>——所以你也拼不出一个”同一个模型、一句话进去、三种声音混着出来、时间关系都对”的东西。</p> <p>这正是 StepAudio 3 Gen 想去的地方。它给出的答案很激进：</p> <p><strong>把通用音频全部表示成同一套离散 token，然后交给一个 LLM 自回归地预测——不做 diffusion，不做 flow matching，连 acoustic renderer 都不要。</strong></p> <p>这里的”通用音频”包括语音、歌声、音乐、音效，也包括它们混在一起的复杂场景；论文里还专门列了一类 <strong>vibe speech</strong>，指带环境氛围和背景声的语音——不是一条干巴巴的朗读，而是”咖啡馆里边喝东西边聊天气”那种有空间感的东西。</p> <p>结果上，这份技术报告里最硬的数字是 TTS：在中文”像不像真人在说话”的 Arena 式主观评测（两两对打，让人盲选更像真人的那条）里拿到 Elo 1755.33，比第二名 Qwen-Audio-3.0-TTS-Plus（1544.3）高出 211 分，对五个商业 TTS 系统的直接对比胜率是 82.0%，单场胜率区间 73.0%–90.0%。Voice design（用自然语言描述音色和风格，让模型”捏”一个声音出来）同样是第一，Elo 1668.5，胜率 75.5%。</p> <p>这篇文章按三层来拆：<strong>结构上它做了什么、数据上它怎么凑的、训练上它做了哪些事才换来这个结果</strong>。</p> <h2 id="2-先搞清楚为什么音频生成一直没长成一个llm-任务">2. 先搞清楚：为什么音频生成一直没长成一个”LLM 任务”</h2> <p>统一音频生成不是新目标，最近一两年不少工作在做。路线大致两条，各自都有说不出的苦。</p> <p>先插一句名词：后面反复出现的 <strong>codec</strong>，指的是<strong>音频编解码器</strong>——一个专门把音频压缩成紧凑表示、又能从这个表示还原回音频的模型（encoder 负责压、decoder 负责还原）。手机里录音存成的压缩格式是传统 codec，这里说的是神经网络版的 neural codec，思路一样：先压再还原。</p> <p>第一条路线是<strong>连续 latent + diffusion / flow matching</strong>。用一个大 VAE 或者 codec 的 encoder 把音频压成连续的 latent（一串连续的浮点向量），然后在 latent 上跑 diffusion 来生成。好处是声学细节渲染得漂亮，多家通用音频模型（speech、sound、music 一起做的那种）走的是这条路。坏处是它和 LLM 天然不兼容：LLM 的词表是离散的 softmax，它的训练目标是 next-token prediction，它的上下文是文本和音频随意交错的序列。你没法把一个连续 latent 塞进 softmax 里。</p> <p>第二条是<strong>离散 unit + LM 式序列建模</strong>，也就是先把音频变成 token（”音频 tokenizer”这个说法就是指做这件事的模块，它同时也要负责把 token 变回波形），再像语言模型那样预测。这条路天生和 LLM 兼容，但要理解它，得先把”音频怎么变成 token”这件事说清楚——因为<strong>所有后续设计的坑，都是从这里长出来的</strong>。</p> <h3 id="21-先搞懂一段音频怎么变成-16-个整数">2.1 先搞懂：一段音频怎么变成 16 个整数</h3> <p>这一节把”音频 → token”整个走一遍。很多细节论文默认你懂，这里补齐。</p> <p><strong>第一步：音频在电脑里本来是什么样。</strong> 一段 24 kHz 的音频，在硬盘里确实是一长串离散的数——每秒 24000 个采样点，每个点是一个振幅值。但这串数<strong>又长又不好用</strong>：1 秒就是 24000 个数，而且相邻采样点之间高度冗余，直接拿去做 token 毫无意义。所以第一步不是量化，而是<strong>先用一个编码器把它压成”特征向量”</strong>。</p> <p><strong>第二步：编码器把波形压成一帧一个向量。</strong> 这就是 3.1 节要讲的那两个编码器干的活。它们把 24000 个采样点/秒的波形，压成 <strong>12.5 帧/秒</strong>，每一帧输出<strong>一个特征向量</strong>——这个向量有固定的维度（记作 $D$，比如几百到一千维，是模型结构定死的）。注意这里的关键转变：<strong>采样点是”声音的振幅”，特征向量是”这一帧的抽象表示”</strong>（里面编码了音高、音色、这一帧在说什么音等信息）。到这一步，1 秒音频 = 12.5 个 $D$ 维向量，还都是连续的浮点数。</p> <p><strong>第三步：codebook 把每个向量换成整数。</strong> 现在才轮到量化。codebook（码本）是一张表，有 2048 行，<strong>每一行也是一个 $D$ 维向量</strong>——和上一步的特征向量维度一模一样，这是它们能比较的前提。这张表不是人手写的，是<strong>训练时学出来的</strong>：模型自己去调这 2048 行的数值，目标是”让任意一个特征向量都能在表里找到一个足够接近的行”。训练完，这 2048 行就固定下来了。</p> <p>编码时，拿一帧的特征向量，逐行去和 codebook 的 2048 行比<strong>相似度</strong>（论文用的是<strong>余弦相似度</strong>——看两个向量方向有多接近，越接近越像），找出最像的那一行，把它的<strong>行号</strong>（0~2047 之间的一个整数）记下来。所以：</p> <blockquote> <p><strong>一个 $D$ 维浮点向量 → 一个 0~2047 的整数。</strong> 这就是量化，本质是”从 2048 个预设向量里挑一个最接近的来代替你”。</p> </blockquote> <p><img src="/assets/img/posts/20260916-stepaudio3gen/diagram_quant.png" alt="codebook 量化和 RVQ 残差补偿"/></p> <p><strong>第四步：一层不够，用 16 层补。</strong> 一个 2048 行的表，最多只能表达 2048 种不同的向量，精度远远不够——用最接近的那一行去顶替，和原始向量总有差距（这个差距叫<strong>残差</strong>）。于是有了 <strong>RVQ</strong>（residual vector quantization，残差向量量化）：第一层量化完，把”原始向量 − 第一行原型”这个残差<strong>再送进第二个 codebook</strong> 量化一次，第二层的残差再送进第三个……一共 <strong>16 层</strong>，每层都有自己独立的 2048 行表。每多一层，就多记一个整数、重建就更准一点。</p> <p>于是最终答案是：<strong>一帧音频 → 16 个 0~2047 的整数</strong>（记作 $c_0$ 到 $c_{15}$）。第 0 层（$c_0$）最粗、编码的是这一帧最主要的信息；越往后的层补的越是细节。一段 1 秒的音频，就变成 12.5 帧 × 16 = 200 个整数。</p> <p>（这里对维度做了简化，好抓住主干。真实实现里 codebook 查找用的是 factorized 形式——先把特征向量投影到一个更低维的空间再和码本比，省计算；不影响上面的理解。）</p> <h3 id="22-由此而来的长度问题16-个整数怎么排成序列">2.2 由此而来的长度问题：16 个整数怎么排成序列</h3> <p>每帧 16 个整数，LLM 是”一个接一个读 token”的，所以得先决定：这些整数按什么顺序排成一条序列喂给它。<strong>下图和下文里，$t$ 表示第几帧（时间方向），$k$ 表示第几层码本（深度方向，$k_0$ 到 $k_{15}$）。</strong> 为了画得下，图里用 3 帧 × 4 层做示意。</p> <p><img src="/assets/img/posts/20260916-stepaudio3gen/diagram_layout.png" alt="三种序列排法对比"/></p> <p>有三种排法：</p> <ul> <li> <p><strong>① 直接展平</strong>：把所有整数首尾接成<strong>一条</strong>序列——第 1 帧的 $k_0 k_1 \ldots k_{15}$，接第 2 帧的 $k_0 k_1 \ldots k_{15}$，一直排下去。最直观，但长度爆炸：1 秒 = 200 个 token，1 分钟就是 12000 个位置，训不动。</p> </li> <li> <p><strong>② delayed pattern（延迟模式，MusicGen 用的）</strong>：不再串成一条，而是开 <strong>16 条并行的流</strong>（每层码本一条），模型每个时间步同时吐出 16 个 token（每条流一个）。诀窍在于<strong>把第 $k$ 条流整体往后错 $k$ 步</strong>：第 0 层从第 1 步开始出，第 1 层从第 2 步才开始出……于是<strong>同一帧的 16 个码，不在同一步里，而是排在一条斜线上</strong>（见图中高亮的对角线）。这样总步数只有”帧数 + 16 − 1”，几乎不膨胀。代价是模型必须<strong>同时具备 16 个输出头、并一次预测出全部 16 层</strong>。</p> </li> <li> <p><strong>③ time-depth（时间-深度分离，本文选的）</strong>：把两个方向彻底拆开。<strong>时间方向的主干序列里，每帧只放 $c_0$ 一个 token</strong>；剩下的 $c_1 \ldots c_{15}$ 不进主干，交给一个小模块沿<strong>深度方向</strong>（同一帧内部逐层）单独生成。主干因此短得和”每帧一个 token”一样。</p> </li> </ul> <p>论文选 ③，理由说得很直白：② 的延迟模式要求<strong>主干模型自己长出 16 个输出头、直接预测全部 16 层、并承接所有层的声学损失</strong>——这对一个想直接拿来复用的预训练文本 LLM 来说，是伤筋动骨的改造。而 ③ 只让主干管 $c_0$（语义最集中的那层），把 15 层残差和它们的梯度关进一个小模块里，主干几乎不用改。</p> <p>这个”只让主干管 $c_0$”的选择，是理解全文的第一个支点。</p> <h2 id="3-结构一根时间轴管语义一根深度轴管声学">3. 结构：一根时间轴管语义，一根深度轴管声学</h2> <p>先看论文自己的架构图，建立整体印象：</p> <p><img src="/assets/img/posts/20260916-stepaudio3gen/figure1_full.png" alt="论文原图：完整架构图"/></p> <p>下面这张是按论文重新组织的输入/输出侧拆解。这张信息比较密，<strong>只抓两条主线就够了</strong>：上半部分是音频怎么进 LLM（紫色的 RVQ Adaptor），下半部分是 LLM 怎么把音频吐回来（绿色的时间轴 + 紫色的深度轴）：</p> <p><img src="/assets/img/posts/20260916-stepaudio3gen/diagram_arch.png" alt="输入/输出侧结构拆解"/></p> <h3 id="31-表征125-hz16-个码本语义和声学不分家">3.1 表征：12.5 Hz、16 个码本，语义和声学不分家</h3> <p>StepAudio Tokenizer 是整套东西的地基，规格是：<strong>12.5 Hz</strong>（每帧 80 毫秒，一秒钟音频 12.5 个时间步）、每帧 <strong>16 个 codebook，每个 2048 项</strong>、重建 <strong>24 kHz</strong> 波形。</p> <p>但光看规格看不出门道。真正该先问的是：<strong>这套 token 是给谁用的？</strong> 答案是给 LLM 读写的——它既要当 LLM 的输入（音频理解），又要当 LLM 的输出（音频生成）。这个定位决定了表征必须同时满足两个互相拉扯的要求，而这一节剩下的每一个设计，都是在为同时满足这两件事服务。</p> <p><strong>要求一：LLM 得”读得懂”它，所以必须带语义。</strong> LLM 的全部本领就是 next-token prediction：给定前面的 token，预测下一个。如果音频 token 的分布和语言没关系，LLM 就学不到”这句话说完，下一句该说什么”这种语言层面的规律，audio token 也没法跟 text token 放进同一个词表；往小了说，后训练里那堆理解类任务——ASR、语音翻译、音频描述——一个都做不了。所以语义信息必须在。</p> <p><strong>要求二：波形得还原得回去，所以必须带声学。</strong> 这套 token 还得负责出声。只靠语义做不到：同一个 c0 对应着无数种音色、无数种录音环境——”这人说’你好’“和”那人说’你好’“在语义上没差别，波形却完全不同。这是典型的一对多问题，光有语义层，还原时只能瞎挑一个，说话人信息全丢。</p> <p>麻烦在于，<strong>单独任何一路都满足不了这两个要求</strong>：</p> <ul> <li>只用语义特征（早期 semantic token + 外挂 vocoder 的做法）：语义强，但丢音色，一对多还原不回去；</li> <li>只用声学特征（SoundStream / EnCodec 这类纯重建导向的 codec）：重建好，但 token 分布跟语言没关系，LLM 学不到东西。</li> </ul> <p>StepAudio 的 tokenizer 沿用了一条叫 <strong>X-Codec</strong> 的现成路线（一系列专门解决”codec 缺语义”问题的音频 codec 工作，见文末参考），核心主张就是：<strong>语义和声学两个都要，并且在量化之前就把它们合在一起</strong>。具体用两路编码器：</p> <ul> <li><strong>SSL 编码器</strong>（self-supervised learning，自监督学习）。指 HuBERT、wav2vec 2.0、WavLM 这一族模型：拿海量无标注音频，把一段帧遮掉、让模型去预测被遮位置对应的离散单元，监督信号从数据本身来，所以叫”自监督”。这类模型训完有个鲜明性格——<strong>对”说了什么”极其敏感，对”谁说的、在哪儿说的”相当不敏感</strong>。对识别任务是优点，对生成是缺点，因为它根本不记音色。</li> <li><strong>声学编码器</strong>。一个卷积编码器（配 SnakeBeta 激活，这个激活函数来自 BigVGAN），直接从原始波形、在 50 Hz 速率上提特征。它<strong>没有预训练、没有语义目标</strong>，是跟着 tokenizer 的重建损失和 GAN 损失端到端训出来的，学到的正是”要把波形还原出来还缺什么”——音色、基频、能量包络、混响。</li> </ul> <p>至于 SSL 为什么是冻结的，论文没展开，但理由不难猜：一是省事，这个模型本来就是现成的；二是它还兼任语义蒸馏的 teacher（下面会讲），teacher 必须不动，否则蒸馏就失去了稳定的参考系。</p> <p>两路沿通道维拼起来（不是二选一），再用一个 strided convolution 在时间上压到 12.5 Hz，交给<strong>同一个</strong>共享量化器。结果是<strong>每一层 codebook 里都同时有语义和声学信息</strong>，而不是前几层管语义、后几层管声音的硬分工。</p> <p>但”信息都在”和”信息在前面”是两回事。3.2 节的 time-depth 分工要求主干只预测 c0，前提是 <strong>c0 足够”语义”</strong>——否则 LLM 拿一个混着音色噪声的 token 去做时间规划，是没意义的。为此训练时上了两个机关，把语义往前面几层挤：</p> <ul> <li><strong>量化器 dropout 取 0.5</strong>。指的是训练时随机跳过若干层残差量化，逼前面的层自己去扛更多信息。0.5 这个比例是刻意选的——论文提到再大一点，前几层确实更强，但满深度重建质量会掉；</li> <li><strong>语义蒸馏分支</strong>，用一个回归目标把冻结 SSL 编码器的特征从量化后的 latent 里还原出来。注意 SSL 编码器在这里是<strong>两重身份</strong>：既是被融合进输入的那一路，又是蒸馏的 teacher——这也是它必须冻结的另一个原因。</li> </ul> <p>两个机关叠在一起，语义信息被集中到了最前面的 codebook 上。<strong>这不是一个可选的小优化，而是后面整套 time-depth 分工能成立的前提。</strong></p> <p>另外两个工程细节值得记一笔。第一，量化用的是 16×2048 的 RVQ，codebook 查找是 factorized + 余弦相似度的形式，不是常见的欧氏距离 L2 最近邻。第二，<strong>解码器是全因果的</strong>：Vocos 风格的 Transformer 骨架，配 RoPE 位置编码和 25 帧滑窗注意力，最后用一个 iSTFT head 出 24 kHz 波形——这么设计的目的很直接：全因果 + 无 look-ahead 意味着能边出 token 边出声，这是上实时在线服务的前提，也是很多走 diffusion 的通用音频模型绕不过去的坎。</p> <h3 id="32-生成c0-交给-llmc1c15-交给一个小-transformer">3.2 生成：c0 交给 LLM，c1~c15 交给一个小 Transformer</h3> <p>有了统一表征，生成就被拆成了两根轴：</p> <p><strong>时间轴</strong>上，16 个 codebook 里的第 0 个（记作 c0）被<strong>提升进主语言模型的词表</strong>——2048 个连续的 audio token 直接接在文本词表后面。于是主 LM head 一个 softmax 就同时管了自然语言 token 和音频的顶层 token，两种模态可以自由交错。</p> <p><strong>深度轴</strong>上，剩下 15 层残差交给一个 4 层因果 Transformer，论文里叫 <strong>RVQ Code Predictor</strong>。它每一步收到两个条件：LLM 在这一帧的 hidden state $z_t$（线性投影到 predictor 的宽度），和主 LM 刚刚选出来的 c0。然后它自回归地吐出 c1、c2……直到 c15，每一层的输出再喂回去当下一步的输入。</p> <p>写成分解式就是：</p> \[p(c_{t,0},\ldots,c_{t,15}\mid \mathbf{z}_{&lt;t},\mathbf{c}_{&lt;t}) = \underbrace{p(c_{t,0}\mid \mathbf{z}_{&lt;t},\mathbf{c}_{&lt;t})}_{\text{主 LM head}} \;\prod_{k=1}^{15}\;\underbrace{p(c_{t,k}\mid \mathbf{z}_{t},c_{t,0},\ldots,c_{t,k-1})}_{\text{RVQ Code Predictor}}\] <p>论文特别强调这个分解是<strong>精确的而不是近似的</strong>，理由有点绕但值得品：因为一个音频帧的 16 个向量是<strong>先相加成一个帧向量</strong>再进 LLM 的，所以 $z_t$ 里已经通过 backbone 编码了历史帧的全部 16 层（$\mathbf{c}_{&lt;t}$），不存在”past 帧的残差没被 LLM 看到”的信息缺口。</p> <p>它放弃的只是<strong>反方向</strong>：predictor 的输出不回灌给 backbone。而这恰恰是后面训练策略里那个 <code class="language-plaintext highlighter-rouge">detach</code> 操作的由来——因为两边既然不是必须联合优化，就可以<strong>分开、分阶段</strong>优化。</p> <h3 id="33-输入侧rvq-adaptor-这道调压阀">3.3 输入侧：RVQ Adaptor 这道”调压阀”</h3> <p>输出侧的设计已经够简洁了，输入侧还埋了一个更关键的模块。</p> <p>一个音频帧不是一个向量，是 16 个向量。<strong>这 16 个 codebook 各自有一张 embedding 表，查出来的 16 个向量直接相加</strong>，得到这一帧的帧向量。然后这个帧向量过一道 <strong>RVQ Adaptor</strong>——一摞逐 token 作用的残差块（pre-norm + SwiGLU），输出<strong>只加在音频位置上</strong>，和 c0 那个 audio token 的 embedding 逐元素相加。</p> <p>注意 c0 在这里被表示<strong>两次</strong>：一次在主词表里（作为那个 audio token），一次通过它自己的那张表混在帧向量里。</p> <p>这道 adaptor 有两个细节决定了它到底管不管用：</p> <ul> <li><strong>位置门控</strong>：文本位置没有任何音频码，整条通路不参与，只加在音频位置；</li> <li><strong>下投影零初始化</strong>：这意味着训练刚开始时，adaptor 是一个<strong>恒等映射</strong>——音频通路对文本表征的扰动，在初始化那一瞬间严格是 0。</li> </ul> <p>第二条是整套防干扰设计的源头。后面讲训练时会看到，正是靠它，第一阶段才敢只训 adaptor 而完全不做文本回放。</p> <p>最后是损失函数：</p> \[\mathcal{L} = \mathcal{L}_{\mathrm{tok}} + \lambda\,\mathcal{L}_{\mathrm{sp}}, \qquad \mathcal{L}_{\mathrm{tok}} = -\sum \log p(c_{t,0}, x_t), \qquad \mathcal{L}_{\mathrm{sp}} = -\sum_t \sum_{k=1}^{15} \log p(c_{t,k}\mid\cdot)\] <p>一个小到容易看漏、但后面会变成关键的设计：<strong>$\mathcal{L}_{\mathrm{sp}}$ 是对 15 层 codebook 求和（sum），而不是取平均</strong>。也就是说，声学损失的量级天然比 token 级损失大一个数量级。这个”债务”迟早要还——还的方式就是第 5 节里那个 $\lambda$。</p> <h2 id="4-数据预训练铺覆盖后训练全靠自己造">4. 数据：预训练铺覆盖，后训练全靠自己造</h2> <p><img src="/assets/img/posts/20260916-stepaudio3gen/diagram_data.png" alt="数据的两段式组织"/></p> <p>这张图左右两栏对应两段，<strong>先看两栏的标题，再看里面的粗体小标题，最后才是正文细节</strong>——如果只想抓住主线，看完左右两栏的标题就可以往下走。</p> <p>整份报告的数据组织分成两段：<strong>预训练铺覆盖面</strong>（目标是建立一套统一的音频理解 + 生成能力），<strong>后训练按任务逐个打磨</strong>（目标是质量、自然度和可控性）。四个阶段加起来，LLM 侧一共吃了约 <strong>2.7T tokens</strong>。</p> <h3 id="41-预训练一份生成--理解--文本的混合料">4.1 预训练：一份”生成 + 理解 + 文本”的混合料</h3> <p>预训练的音频来源横跨语音、歌声、音乐和环境音，任务上分两类：</p> <ul> <li><strong>生成类</strong>：TTS、歌声合成、歌词条件音乐、caption 条件音效、语音到语音翻译；</li> <li><strong>理解类</strong>：ASR（自动语音识别）、语音到文本翻译、音频描述、副语言问答、口音 / 方言识别、交错的多轮语音对话。</li> </ul> <p>理解类这一半很容易被忽略，但它其实是<strong>把 RVQ 表征锚在语言上</strong>的那根钉子。论文的原话是，这些 understanding 任务负责”暴露音色、情绪、口音这些可控属性”——模型要先能听懂”这是什么音色 / 什么情绪”，后面才谈得上按指令生成。</p> <p>音频统一按 12.5 Hz × 16 codebook 编码，生成样本的排布方式是 <strong>1 个文本 token : 2 个音频帧</strong>交错。</p> <p>采样配比有个很硬的比例，从第 2 阶段起生效：</p> <ul> <li>从 Stage 2 开始，<strong>文本语料占每个 optimizer step 的一半</strong>；</li> <li>生成阶段的配比固定为 <code class="language-plaintext highlighter-rouge">text : TTS : 交错对话 = 3 : 1 : 2</code>——文本份额恰好 50%，TTS 占每个 micro-batch 的 1/6，也就是音频生成流里的三分之一。</li> </ul> <h3 id="42-tokenizer-自己的训练700k-小时--两阶段">4.2 Tokenizer 自己的训练：700k 小时 + 两阶段</h3> <p>Tokenizer 不是白来的，它单独训了 <strong>约 70 万小时</strong>的语音 / 音乐 / 音效，用模态均衡的采样器保证不会偏向某一种。整个训练走 GAN 框架（多周期波形判别器 + 多分辨率频谱判别器），再配 feature matching、多尺度 mel 谱和 RVQ commitment 损失。</p> <p>第二阶段的设计有个巧思：<strong>冻结编码器、量化器和语义分支，只练声学解码器和判别器</strong>。好处是重建质量涨了、但 <strong>token 空间一个字没动</strong>——所有已经在这些 token 上训过的模型依然兼容。说白了就是把”表示”和”渲染”彻底解耦，表示定稿之后，渲染可以慢慢调，不会连累下游。</p> <h3 id="43-后训练约-5000-小时几乎每一块都是造出来的">4.3 后训练：约 5,000 小时，几乎每一块都是造出来的</h3> <p>预训练之后是 SFT（supervised fine-tuning，监督微调），语料约 <strong>5,000 小时</strong>，横跨音乐、音效、voice design、歌声和语音。这部分的构造细节比模型结构更能说明现在做通用音频模型的真实门槛——<strong>每一类数据背后都有一条用别的模型搭起来的流水线</strong>。挑几条看：</p> <ul> <li><strong>TTS</strong>：为了”像人在说话”，单独精挑了 <strong>1,500 小时</strong>自然口语。筛选方式是用自家的 StepAudio R1.5 做面向 TTS 的音频 captioning，描述录音场景、底噪水平和说话人特征；再配一个专门优化过副语言现象的 ASR 做转写和校验，把语气、犹豫这类表达线索保住。</li> <li><strong>speech → vocal（语音转歌声）</strong>：语音-歌声成对数据极其稀缺，所以自己造伪配对。两条互补路线——TTS 音色克隆（拿一段歌声当条件，让 TTS 用它的音色说话），和歌声转换 SVC（把歌声换成目标说话人的音色，再和这个说话人的正常语音配对）。造完用三重指标筛：Audiobox Aesthetics 的 <strong>PQ / CE</strong> 两个分数（听感质量和内容愉悦度）、<strong>SingMOS</strong> 的歌声 MOS 预测（MOS 就是主观打分，1–5 分越高越好），加上合成音频与目标音频的音色相似度。<strong>音频质量和音色相似度两个门槛都得过才留</strong>。</li> <li><strong>音乐</strong>：干脆用自家的 StepAudio Music Model 按提示生成器乐素材，再给每条素材配三种粒度的中英条件——简短描述、细节描述（配器、节奏、制作风格）、口语化请求（用户偏好和使用场景）。三种粒度意味着同一段音乐在不同描述层级下都被监督到。</li> <li><strong>音效</strong>：真实音效库 + 合成混音两条腿。真实部分按”事件 / 声源 / 声学属性 / 场景 / 时间结构”做多层标注；混合场景则是先抽一个场景骨架（谁在说话、哪几段语音、哪些音效、什么环境声），让文本 LLM 去补台词、说话风格、音效描述、时长和时间关系，然后<strong>分 stem 合成</strong>（语音和音效各自独立生成），最后按声源显著度和距离调增益再混音——这一步是为了保住前景 / 背景的层次关系。</li> <li><strong>Voice design</strong>：录制 + 合成两条来源，覆盖日常对话、角色对白、专业配音，以及各种录音环境，再把欠代表的”音色 × 说话风格 × 声学场景”组合用合成数据补上。</li> </ul> <p>RL 阶段复用同一套任务分类和条件格式，只把<strong>指令模糊、难以客观核验</strong>的样本剔掉，减少奖励噪声。</p> <h2 id="5-训练四阶段课程和它要挡的三件事">5. 训练：四阶段课程，和它要挡的三件事</h2> <p><img src="/assets/img/posts/20260916-stepaudio3gen/diagram_four_stage.png" alt="四阶段预训练课程与三道防干扰闸门"/></p> <p>这张图分两层：<strong>上面四张卡片是四个阶段，看每张卡片左上角的 Stage 名字和右上角那行参数就够了</strong>；下面三张小卡片是四个阶段合起来要挡的三条伤害路径，对应 5.1 节。</p> <p>这是整份报告里最”值钱”的部分，也是最能解释”为什么结果这么好”的部分。</p> <h3 id="51-先看清敌人给文本-llm-加音频有两条伤害路径">5.1 先看清敌人：给文本 LLM 加音频，有两条伤害路径</h3> <p>论文把这件事说得非常直接：<strong>往一个文本 LLM 里加音频 token，不是中性的操作。</strong></p> <p>伤害路径有两条，方向完全不同：</p> <ol> <li><strong>表征层面</strong>：一堆新初始化的 RVQ embedding 加起来，它的统计量跟预训练文本 embedding 的统计量<strong>本来就对不上</strong>。LLM 被迫去适应一个分布外的新输入；</li> <li><strong>目标层面</strong>：每个时间步要预测 16 个 code，而残差那 15 层的损失因为是<strong>求和</strong>的，量级远大于 token 级交叉熵。如果一上来就联合优化，backbone 会优先去吸收这份巨大的声学梯度——<strong>在音频模块还没变有用之前，先把继承来的语言能力花掉了</strong>。</li> </ol> <p>这两条路各自对应了具体的对策，而”四阶段课程”就是这些对策在时间上的排布。</p> <h3 id="52-四个阶段各自在干什么">5.2 四个阶段各自在干什么</h3> <p><img src="/assets/img/posts/20260916-stepaudio3gen/table1_p8.png" alt="四阶段配方表"/></p> <p><strong>Stage 1 · 模态对齐。</strong> 数据是 ASR + 语音翻译，<strong>只监督文本输出</strong>，一个字音频都不生成。backbone、LM head、code predictor 全部零学习率，<strong>只训输入侧的 audio embedding 和 RVQ Adaptor</strong>。学习率 $2\times10^{-4} \to 2\times10^{-5}$ 走 cosine，batch 4.19M。</p> <p>这一阶段有个很漂亮的论证：因为 adaptor 的下投影零初始化（恒等映射）+ backbone 冻结，<strong>文本能力的漂移严格等于 0</strong>，所以这一阶段根本不需要文本回放。另外 token embedding 给了 0.1× 的学习率——不是整块冻结，而是让新加的那 2048 行 audio-code 慢慢适应已有空间。</p> <p><strong>Stage 2 · 音频理解。</strong> 全部参数解冻，训理解类任务，<strong>仍然不监督任何音频生成目标</strong>。同时从这一阶段开始，原始文本语料<strong>占每个 step 的一半</strong>。学习率降到 $2\times10^{-5}$ 之后就走 constant——注意这个细节，后面几个阶段直接从这个平台接着跑，不再重新 warmup，也就不会再有”学习率从高往低冲一遍”带来的表征抖动。新加的模块学习率 ×10，并且排除在 weight decay 之外。</p> <p><strong>Stage 3 · 生成（detached）。</strong> 主预训练，生成能力从纯语音起步，配比 <code class="language-plaintext highlighter-rouge">text : TTS : 对话 = 3 : 1 : 2</code>。这一阶段的核心操作是：<strong>code predictor 的条件输入（backbone hidden state）被 detach</strong>，那 15 层残差的损失梯度不回流 backbone。</p> <p>论文给的动机很实在：$\mathcal{L}_{\mathrm{sp}}$ 是对 15 个 codebook <strong>求和</strong>而非平均，量级比 token 级损失高一个数量级；而 predictor 此刻还是<strong>随机初始化</strong>的。这两个条件凑在一起，那份梯度一上来就会把前两阶段辛苦建立的表征重新捏一遍。detach 之后，残差声学那部分只训 predictor，backbone 继续由 token 级目标驱动——<strong>把”学声学细节”和”学语义韵律规划”彻底拆开</strong>，这是保护文本能力的第二道保险。这一阶段 $\lambda = 1.0$。</p> <p><strong>Stage 4 · 长上下文联合 cool-down。</strong> detach 解除，声学损失开始回流 backbone——于是 $z_t$ 除了要当好”语义条件”，还得被塑造成<strong>好的声学条件</strong>。同时 $\lambda$ 从 1.0 压到 0.1：既然 depth 损失现在能打到 backbone 了，就得把两项拉回同一量级，不能再让它高一个数量级。序列长度从 16,384 扩到 32,768（配 context parallelism），按 12.5 Hz 算，<strong>32,768 个时间步 ≈ 44 分钟音频</strong>，够覆盖长音频合成和多轮对话。batch 也翻倍到 25.17M。</p> <p>为什么放到最后？论文两个理由都很实：一是这时 predictor 已经收敛，回流的是”有意义的声学梯度”而不再是随机噪声；二是 cool-down 阶段的低学习率本身就限制了它改动 backbone 表征的幅度。</p> <p>把三道闸门并列看，会更清楚：</p> <ol> <li><strong>表征层面 · RVQ Adaptor</strong>——零初始化让它开局是恒等映射，音频通路先学会怎么进 LLM 的输入空间，再逐步放开扰动；</li> <li><strong>数据层面 · 50% text replay</strong>——文本能力不是”尽量少掉”，是每一批梯度里都还在被主动训练；</li> <li><strong>目标层面 · detach + λ</strong>——先挡住那份量级失衡的声学梯度，等 predictor 收敛、再放开并把 λ 从 1.0 压到 0.1。</li> </ol> <h3 id="53-后训练sft-全参微调--grpo">5.3 后训练：SFT 全参微调 + GRPO</h3> <p>SFT 用的是<strong>全参数微调</strong>，把预训练末尾的联合配置直接继承下来（detach 保持关闭，$\lambda=0.1$ 的语音损失继续穿过 code predictor 打到 backbone）。序列长 16,384，batch 64，学习率从 $1.0\times10^{-5}$ 走 cosine 降到 $1.0\times10^{-6}$。audio embedding、adaptor、code predictor 这三个新模块保持 10× 学习率加成。</p> <p>SFT 之后是 <strong>GRPO</strong>（Group Relative Policy Optimization）——这是 PPO 的一个变体，核心差别是<strong>不训 value model</strong>，而是对同一个指令采样一组输出，用组内相对好坏当优势值来更新策略。这里的几个设计值得单独看：</p> <p><strong>奖励是乘性的，不是加性的。</strong> 对每条生成的音频 $y_i$，先用一个音频理解模型写出 caption $c_i$，再让一个文本 LLM 比较 $c_i$ 和原始指令 $x$，给一个 0–100 的指令一致性分数。为了压评估方差，<strong>每个”指令-caption”对独立打 4 次分取均值</strong> $\bar{s}_i$。对有目标文本的语音样本，再用 ASR 转出 $\hat{t}_i$，算错误率 $e_i$（中文按字算就是 CER，英文按词算就是 WER，都是”改多少个字才能变成标准答案”，越低越好）：</p> \[R_i = \frac{\bar{s}_i}{100}\begin{cases}\exp(-\tau e_i), &amp; e_i \leq 0.5,\\ 0, &amp; e_i &gt; 0.5,\end{cases} \qquad \tau = 3\] <p>乘法的意义很直白：<strong>一段内容完全念错的音频，不能因为”风格很像指令描述的那样”就拿到高分</strong>。没有目标文本的任务（纯音效、纯音乐）就把 ASR 那一项设为 1。</p> <p><strong>动态采样滤掉”没信号”的组。</strong> 每条指令采 $G=16$ 个候选，优势 $\hat{A}_i=(R_i-\mu_R)/(\sigma_R+\eta)$。借鉴 DAPO 的做法，<strong>组内奖励标准差 $\sigma_R &lt; \delta$（$\delta=0.02$）的组直接丢掉</strong>——一组 16 个输出分数都差不多，说明这条指令对当前策略没区分度，留着重只会加噪声。</p> <table> <tbody> <tr> <td><strong>重要性比率是按 codebook 算的。</strong> 损失函数里对时间位置 $t$ 和 codebook 下标 $\ell \in {0,\ldots,15}$ 都算了 token 级的重要性比率 $\rho_{i,t,\ell}$，再除以 $16</td> <td>y_i</td> <td>$ 做归一化，配一个很小的 KL 系数 $\beta$（主要靠 clip 约束更新），学习率 $10^{-6} \to 10^{-7}$。</td> </tr> </tbody> </table> <p>这一步其实是个不太显眼但很实在的工程选择：GRPO 本来是给文本 token 设计的，这里把它<strong>直接铺到 16 层 codebook 上</strong>，让残差层也接受偏好信号的调教——而不是只训 c0、让 predictor 自生自灭。</p> <h2 id="6-结果每一块设计值多少分">6. 结果：每一块设计值多少分</h2> <p>报告里的实验组织得很干净，正好是三个问题：RVQ Adaptor 有没有用、干扰控制有没有用、最终生成质量如何。</p> <h3 id="61-rvq-adaptor不加它音频根本没进到-llm-的语义空间">6.1 RVQ Adaptor：不加它，音频根本没进到 LLM 的语义空间</h3> <p><img src="/assets/img/posts/20260916-stepaudio3gen/table2_p11.png" alt="加 / 不加 RVQ Adaptor 的对比"/></p> <p>先回答一个容易犯嘀咕的问题：<strong>这个模型是拿来做音频生成的，为什么能报 ASR（语音转文字）成绩？</strong> 因为它并不是一个纯生成模型——2.1 节说过，它的 token 既能当输入也能当输出，而 5.2 节的 Stage 2 专门用 ASR、语音翻译、音频描述这些<strong>理解类任务</strong>训过它。也就是说，同一个模型既能”听懂”（把音频转成文字、做翻译），也能”开口”（生成音频）。这一节考的正是它”听”的能力。</p> <p>几个指标先解释一下：<strong>CER / WER</strong> 是字/词错误率（识别结果要改多少字/词才能变成标准答案），越低越好；<strong>MMAU</strong> 是音频理解与推理基准，报准确率；<strong>CoVoST</strong> 是语音到文本翻译基准，报 BLEU（衡量生成译文和参考译文的 n-gram 重合度，越高越好）；<strong>SpeechMMLU</strong> 是语音形式的知识问答，准确率越高越好。</p> <p>对比结果：</p> <ul> <li>ASR：AISHELL-1 的 CER 从 5.25 → <strong>3.00</strong>，LibriSpeech 的 WER 从 6.00 → <strong>3.41</strong>；</li> <li>MMAU 准确率 40.70 → <strong>51.70</strong>；</li> <li>CoVoST BLEU：En→Zh 12.05 → <strong>30.56</strong>，Zh→En 5.99 → <strong>18.59</strong>；</li> <li>SpeechMMLU (T2S) 准确率 12.13 → <strong>58.76</strong>。</li> </ul> <p>最后那一行是重点。SpeechMMLU 的 12.13% 是什么概念——基本等于在瞎猜。这说明<strong>不加 adaptor 的时候，多 codebook 音频表征根本没有落进 LLM 的语义空间</strong>，模型只是勉强在做模式匹配；加了之后才跳到 58.76%。这不是”涨了几个点”，是两个状态。</p> <h3 id="62-干扰控制文本能力不是少掉一点而是全面反超">6.2 干扰控制：文本能力不是”少掉一点”，而是全面反超</h3> <p><img src="/assets/img/posts/20260916-stepaudio3gen/table3_full.png" alt="三阶段 baseline 与干扰感知预训练的文本基准对比"/></p> <p>对照组是<strong>不用 RVQ Adaptor 的三阶段 baseline</strong>（ASR 训练 → 音频理解 + 生成训练 → cool-down），在 8 个文本基准上比：</p> <ul> <li>FinEval 65.86 → <strong>71.68</strong>，C-Eval 66.34 → <strong>71.92</strong>，MMLU 64.99 → <strong>69.20</strong>，CMMLU 66.69 → <strong>71.73</strong></li> <li>MATH 36.62 → <strong>45.07</strong>，GSM8K 67.94 → <strong>74.05</strong>，BBH 59.61 → <strong>67.03</strong>，HumanEval 47.56 → <strong>54.27</strong></li> </ul> <p>八个基准<strong>全部反超，而且幅度不小</strong>。这比”文本能力只掉了 X%”有说服力得多——它说明这套课程不只是”少伤害文本能力”，而是在引入音频的同时<strong>顺带把文本能力也练上去了</strong>（毕竟 Stage 2 起一半的梯度就是纯文本）。</p> <h3 id="63-tts放弃-cer-和-ss改问像不像真人">6.3 TTS：放弃 CER 和 SS，改问”像不像真人”</h3> <p>TTS 这块，报告的评测取向是全文最有意思的一处”反潮流”。</p> <p>它明确说<strong>不用 CER 和 SS（speaker similarity，说话人相似度）这类客观指标</strong>，理由也站得住：一段<strong>语调平得像播报</strong>的音频，只要内容念对了，CER 照样很低；而 SS 反映的是说话人身份和音色，反映不了表达力。这两个指标恰恰测不出他们想要的”人味”。</p> <p>所以他们搭了一个 Arena 式的成对主观评测：评者听两条音频，选”更像真人在自然说话”的那条，允许判平局。评测集是从<strong>真实人类语音</strong>里抽文本构造的中文 set，六个系统两两对比，一共 <strong>1,500 次比较</strong>，最后用 Elo 汇总（Elo 就是国际象棋那套评分，赢了涨分输了掉分，分数越高代表在这个池子里越被偏好）。</p> <p><img src="/assets/img/posts/20260916-stepaudio3gen/fig_tts_elo.png" alt="中文人味评测的 Elo 排名"/></p> <p><img src="/assets/img/posts/20260916-stepaudio3gen/fig_tts_h2h.png" alt="与五个商业 TTS 系统的正面对比"/></p> <ul> <li>StepAudio 3 Gen <strong>1,755.3</strong>，比第二名 Qwen-Audio-3.0-TTS-Plus（1,544.3）高 211.1 分；</li> <li>对五个商业系统的直接对比，<strong>总胜率 82.0%</strong>，单场从 73.0%（对 Qwen-Audio-3.0-TTS-Plus）到 90.0%（对 Inworld-TTS-2 和 Minimax-Speech-2.8-HD），每一场都是明显多数。</li> </ul> <h3 id="64-voice-design指令遵循--盲测偏好双第一">6.4 Voice design：指令遵循 + 盲测偏好双第一</h3> <p>Voice design 用了两个评测。</p> <p>第一个是 <strong>InstructTTSEval</strong> 客观基准：2,000 条文本（中英各 1,000），每条在三种指令条件下各生成一遍——<strong>APS</strong>（声学参数指定，比如直接给音高语速）、<strong>DSD</strong>（描述性风格指令）、<strong>RP</strong>（角色扮演），一共 6,000 条音频，由 Gemini 3.1 Pro 判定风格一致性。</p> <p><img src="/assets/img/posts/20260916-stepaudio3gen/table4_p13.png" alt="InstructTTSEval 风格一致性"/></p> <p>中文 AVG <strong>85.2</strong>（APS 88.5 / DSD 91.5 / RP 75.5），英文 AVG <strong>77.7</strong>（82.4 / 85.5 / 65.2），中英两边都排第一。三项里 RP 一致最低——角色扮演这类条件本身就最模糊，其它系统更低（Qwen3-TTS 的 RP 中文只有 56.2）。</p> <p>第二个是盲测人类偏好，490 次中英对比，5 个模型：</p> <p><img src="/assets/img/posts/20260916-stepaudio3gen/fig_vd_elo.png" alt="voice design 的 Elo 排名"/></p> <p><img src="/assets/img/posts/20260916-stepaudio3gen/fig_vd_h2h.png" alt="voice design 正面对比"/></p> <p>Elo <strong>1,668.5</strong> 排第一，领先 Qwen3-TTS-12Hz-1.7B-VoiceDesign（1,571.5）97 分；总胜率 <strong>75.5%</strong>，单场 65.3%–85.7%。</p> <h2 id="7-几点评述">7. 几点评述</h2> <p><strong>第一，这篇真正的贡献是那套”隔离”思路，不是某个模块。</strong></p> <p>RVQ Adaptor 和 4 层 predictor 都不算新东西——time-depth 结构在音频 LLM 里已经用了好几年。真正少见的是把”加一个新模态会怎么伤害已有能力”这件事<strong>拆成两条具体路径、给每条配一个机制、再排成时间课程</strong>。零初始化 adaptor 管表征、50% text replay 管分布、detach + λ 管梯度量级——三个动作各自对应一个可诊断的问题，而不是笼统地”加个 LoRA 保一下”。</p> <p>而且这套设计<strong>前后是自洽的</strong>：$\mathcal{L}_{\mathrm{sp}}$ 求和而非平均这件事，在 Stage 3 是必须 detach 的理由，在 Stage 4 是必须降 λ 的理由。全文的两处关键决策，都是从”损失量级天然失衡”这一个小小的事实推出来的。这种”从一个实现细节贯穿到底”的一致性，是读技术报告时最值得学的地方。</p> <p><strong>第二，TTS 评测从客观指标转向主观 Arena，这个方向是对的。</strong></p> <p>CER 和 SS 之所以流行，是因为它们便宜、可复现、能自动跑。但它们测的是”内容对不对”和”音色像不像”，而不是”听起来像不像人”。而通用音频模型发展到现在，<strong>剩下的差距恰恰主要在 CER 量不到的地方</strong>——韵律、节奏、呼吸、犹豫、什么时候该停顿。这篇选择用 Elo 和胜率来汇报，虽然主观、难复核，但至少测的是他们真正想优化的东西。它敢这么报，也是因为它赢得很明显（82% 胜率不是靠运气）。</p> <p><strong>第三，通用音频模型的门槛，已经从”模型架构”转移到了”数据构造流水线”。</strong></p> <p>把第 4 节的数据流水线再回顾一遍：TTS 数据要先用自家的 captioning 模型筛、再用自家的副语言 ASR 校验；speech→vocal 要用自家的 TTS 和 SVC 造伪配对，再用 Audiobox Aesthetics 和 SingMOS 三重筛；音乐数据直接用自家的音乐生成模型造；音效混合场景要靠文本 LLM 去补场景脚本。<strong>每一类数据的产出，都依赖另外几个已经训练好的模型。</strong></p> <p>这不是 StepFun 一家的特例，而是这个方向现在的普遍形态。它的含义是：一个团队想做通用音频生成，光有算力和架构不够，你还得先有一整套能互相当”数据生成器”的模型资产。这也直接解释了为什么这份报告里”数据”那一节的篇幅几乎和”训练”一样长。</p> <p><strong>第四，时间轴和深度轴的”权责划分”值得琢磨。</strong></p> <p>让 LLM 只管 c0，看起来是”降低了 LLM 的职责”，但换回来的是三件事：序列长度不膨胀、预训练的语言能力不被 15 层残差损失冲垮、以及 predictor 可以独立训练和独立 detach。<strong>在一个已经很重的 backbone 旁边加一个轻量、可隔离、可单独调度的模块</strong>，这个模式在别的模态扩展场景里大概率也能复用。</p> <h2 id="8-局限">8. 局限</h2> <p>说几个报告里没解决、或者没说的：</p> <ul> <li><strong>backbone 的规模完全没披露。</strong> 全文只给了 batch size、学习率、序列长度和 λ，没有一个字提参数量、层数、hidden 维度。作为一份技术报告，这个信息缺口比较大——你没法从这篇里学到”这套配方在不同规模的 LM 上是否同样有效”，而这恰恰是做多模态扩展时最关心的 scaling 问题。</li> <li><strong>数据构造的可复现性接近零。</strong> 前面说了，TTS captioning 用 StepAudio R1.5、副语言转写用自家 ASR、音乐数据用 StepAudio Music Model、音色筛选用 Audiobox 和 SingMOS。这套链条里除了 Audiobox 和 SingMOS，其余都是内部模型。外部团队能复现的只有”设计思路”，复现不了”数据”。</li> <li><strong>主观评测是自家的池子和流程。</strong> Arena 式评测的评测集、评者构成、判定规则都会影响结果。1,500 次比较听起来不少，但对比的五个商业系统每家只用了<strong>一个音色</strong>——对 TTS 这种强依赖音色的任务，单音色对比的结论外推时要谨慎。</li> <li><strong>客观指标基本空缺。</strong> 只报主观 Elo 和胜率，一个 CER / SS / 相似度数字都没有。这在”强调人味”的框架下可以理解，但读者没法判断内容准确性上有没有代价——比如为了更自然的韵律，有没有牺牲字准。</li> <li><strong>“通用”的覆盖面报得不均匀。</strong> 音乐只说了”能生成超过 60 秒、保持结构和风格一致”，没有和音乐专用模型做任何对比；音效、歌声、vibe speech 也都只有能力描述没有量化评测。相比之下 TTS 和 voice design 的评测完整得多。所以”general audio”这个说法目前更像是一个<strong>统一框架的存在性证明</strong>，而不是”在每个子领域都能打”的证明。</li> </ul> <h2 id="9-总结">9. 总结</h2> <p>StepAudio 3 Gen 的核心思路可以压缩成一句话：<strong>用一套 12.5 Hz、16 层 codebook 的共享 RVQ 表征把语音、歌声、音乐、音效统一起来，然后让 LLM 管时间轴上的语义决策、让一个 4 层小 Transformer 管帧内的声学残差，整套生成过程完全在离散 token 空间里完成，不碰 diffusion。</strong></p> <p>它之所以能把结果做到这个水平，靠的不是某一个模块的新意，而是两件事的配合：</p> <p><strong>结构上</strong>，把”语义密集的 c0”和”声学密集的 c1–c15”分到两根轴、两个模块上，让 LLM 可以既参与音频生成、又不必承受全部声学损失；输入侧用零初始化的 RVQ Adaptor 把多 codebook 表征平滑地接进预训练 embedding 空间。</p> <p><strong>训练上</strong>，把”加新模态会伤害已有能力”拆成表征和目标两条路径，配三个机制（adaptor / 50% text replay / detach + λ），再排成四阶段课程。效果上，文本基准不是”少掉”，而是 MMLU +4.2、GSM8K +6.1、HumanEval +6.7 的全面反超。</p> <p>要挑刺的话，信息透明度是这份报告最大的短板——模型规模不披露、数据链条不可复现、客观指标空缺。但就”离散自回归能不能撑起通用音频生成”这个问题而言，它给了一个相当有力的肯定答案：<strong>在 TTS 和 voice design 上，纯离散 token 的路线已经能在盲测里赢过一堆走连续 latent + diffusion 的商业系统。</strong></p> <hr/> <h2 id="参考资料">参考资料</h2> <ul> <li>StepAudio 3 Gen Technical Report（StepFun-Audio Team）— <a href="https://arxiv.org/abs/2609.12945">arXiv:2609.12945</a></li> <li>试听样例 — <a href="https://stepaudiollm.github.io/step-audio-3-gen/">stepaudiollm.github.io/step-audio-3-gen</a></li> <li>X-Codec: 语义 / 声学联合量化的音频 codec 系列工作（StepAudio Tokenizer 的路线参考）</li> <li>MusicGen（Copet et al., 2023）— delayed pattern 多流码本建模的代表</li> <li>DAPO: 动态采样思路的来源，本文用它滤掉低区分度的 GRPO 组</li> <li>Audiobox Aesthetics — PQ（Production Quality）/ CE（Content Enjoyment）打分模型</li> </ul> <hr/> <p>顺带扯一句题外话。这篇里有个初读时容易愣一下的小设计：RVQ 是”第一层粗量化、后面每层补上一层没还原出来的残差”，而书里讲 LLM 压缩时聊到的<strong>量化</strong>（把权重从 FP16 压到 INT8 / INT4），数学动作其实是同一族的——都是”用有限的离散候选去逼近一个连续量，然后逐层 / 逐通道补偿误差”。方向完全不同（一个是输入的表征、一个是权重的存储），但看 RVQ 的残差补偿那几行时，如果刚翻过量化那部分，会觉得挺熟。</p> <p>另外第 5 节那套”先冻结对齐、再逐步放开、最后联合 cool-down”的课程，和书里 NAS 搜索策略那部分讲的 warm start、渐进式搜索空间放宽，也是同一种工程直觉在起作用：<strong>一次只放开一个自由度</strong>。我们把手头关于 AutoML / NAS 和大模型压缩的积累整理成了《动手学 AutoML：从 NAS 到大语言模型优化实战》，上面这两块对应的章节里都有，感兴趣可以翻翻。</p> <blockquote> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="Audio LLM"/><category term="TTS"/><category term="RVQ"/><category term="论文解读"/><summary type="html"><![CDATA[StepAudio 3 Gen 把通用音频生成压成纯离散 token 预测，TTS 人味评测赢遍商业模型]]></summary></entry><entry><title type="html">ICLR’26 | FastMix 把预训练数据配比搜索从 720 GPU 小时干到 1.3 小时</title><link href="https://marsggbo.github.io/blog/2026/20260915-fastmix/" rel="alternate" type="text/html" title="ICLR’26 | FastMix 把预训练数据配比搜索从 720 GPU 小时干到 1.3 小时"/><published>2026-09-15T00:00:00+00:00</published><updated>2026-09-15T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260915-fastmix</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260915-fastmix/"><![CDATA[<h1 id="iclr26--fastmix-把预训练数据配比搜索从-720-gpu-小时干到-13-小时">ICLR’26 | FastMix 把预训练数据配比搜索从 720 GPU 小时干到 1.3 小时</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2606.14971">FastMix: Fast Data Mixture Optimization via Gradient Descent</a>（ICLR 2026，HKU × 腾讯混元 × 港中文） 代码：<a href="https://github.com/hrtan/fastmix">github.com/hrtan/fastmix</a></p> </blockquote> <hr/> <h2 id="1-前言预训练剧本里唯一靠拍脑袋写的那一行">1. 前言：预训练剧本里，唯一靠拍脑袋写的那一行</h2> <p>训练一个大语言模型，配置表里大部分内容都是有章法的：模型架构抄主流、学习率照着 scaling law 推、batch size 看显存。只有一样东西基本靠经验——<strong>数据配比</strong>。</p> <p>想象一下你手里有 17 个数据源（The Pile 就是 17 个无版权子集，代码、数学、书籍、百科、论坛什么都有），每个都可能有用，但也显然不能等权。于是你在配置里写下：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>OpenWebText2: 0.30
Books3:       0.08
ArXiv:        0.12
PtB:          0.03
...
</code></pre></div></div> <p>这些数字从哪来？绝大多数时候是「上一轮实验调出来的 + 看别人论文里抄的 + 感觉」。问题是：<strong>调一次配比 = 完整预训练一遍</strong>。当一次预训练要几万 GPU 小时的时候，试错成本高到离谱。而这个数字恰恰又是「决定模型最终水平」的关键变量之一——同样的算力，配比选好了，下游分数肉眼可见地涨。</p> <p>所以这个问题很本质：</p> <p><strong>能不能用很小的代价，自动找到（接近）最优的数据配比？</strong></p> <p>这正是 FastMix 这篇 ICLR 2026 论文解决的问题。它把配比搜索的时间成本从 RegMix 的 720 GPU 小时压到 1.3 GPU 小时（约 550 倍），同时下游平均分还更高。这篇文章除了拆 FastMix 本身，我更想先带大家把「现在都有哪些做法」梳理一遍——因为不站在这个脉络上，你很难理解 FastMix 的巧妙之处。</p> <h2 id="2-先把问题写清楚一个双层优化">2. 先把问题写清楚：一个双层优化</h2> <p>设数据源集合 $D_1, \dots, D_k$，配比 $\alpha$ 位于概率单纯形上（各分量非负且和为 1）。数据配比问题可以形式化为：</p> \[\min_{\alpha}\; \mathcal{L}_{\text{target}}\big(w^*(\alpha)\big) \quad \text{s.t.} \quad w^*(\alpha) = \arg\min_{w}\; \sum_{i=1}^{k} \alpha_i\, \mathcal{L}_{\text{train}}(D_i, w)\] <p>内层：给定配比，把模型训到最优；外层：调整配比，让验证集或下游任务目标最小。这是个标准的<strong>双层优化（bilevel optimization）</strong>结构。</p> <p>所有方法其实都在这张图上做文章，区别只在两点：</p> <ol> <li><strong>内层的「$w^*$」怎么逼近</strong>——训一个小代理模型？还是训练过程中在线估计？</li> <li><strong>外层的「$\min_\alpha$」怎么搜</strong>——网格？回归？贝叶斯？梯度？</li> </ol> <p>而<strong>卡住「直接用梯度优化」的，正是 $\alpha$ 本身：它是采样概率，采样是离散算子，不可微</strong>。理解这一点是读懂全部工作的钥匙——FastMix 的全部魔法，就是从「让 $\alpha$ 可微」开始的。</p> <h2 id="3-现有方法地图两条主线四代人">3. 现有方法地图：两条主线、四代人</h2> <h3 id="31-派系划分静态-vs-动态">3.1 派系划分：静态 vs 动态</h3> <p>先给一张地图。数据配比方法大概分两派：</p> <ul> <li><strong>静态派（先搜索、后训练）</strong>：正式训练前，先用小模型（代理模型）或拟合函数把配比定下来，再一次性按这个配比训练大模型。DoReMi、RegMix、CLIMB、配比定律（Data Mixing Laws）都属于这一派。</li> <li><strong>动态派（训练中调整）</strong>：不预搜索，训练过程中根据当前模型状态在线更新配比，代表是 ODM 和 IDEAL。优点是不需要代理模型、自适应；缺点是容易不稳定，论文实验里动态派通常打不过静态派。</li> </ul> <h3 id="32-静态派的进化从-0-个代理到-512-个代理再回到-1-个代理">3.2 静态派的进化：从 0 个代理到 512 个代理，再回到 1 个代理</h3> <p><strong>代 0：人工启发式（Human Heuristic）。</strong> 就是开头那行配置。零成本，但换数据集就重来，而且没人知道离最优有多远。</p> <p><strong>代 1：单代理模型 + 相对损失（DoReMi, 2024）。</strong> 思路：先拿一个「参考模型」，再看代理模型在各个数据源上的损失相对参考模型有多大——比值大说明这个域「还没学好、还有得学」，应该多采样。DoReMi 用这个比值重加权得到配比，再去训练大模型。它的哲学很朴素：<strong>用「相对损失」当「该域还有多少可学」的代理信号</strong>。在 FastMix 的预训练设置下搜索成本 7.4 GPU 小时。优点：只要 1 个代理模型，便宜；缺点：信号间接——「还没学好」不等于「学好了对下游有用」，容易把噪声大的域拉高。</p> <p><strong>代 2：多代理模型 + 回归外推（RegMix, 2024）。</strong> RegMix 的做法更「暴力」：训练 512 个不同配比下的代理模型，得到 512 个「配比 → 最终损失」的数据点，拟合一个回归器，再在回归器上做优化，外推出最优配比。它把配比问题整个当成黑盒函数优化来做。缺点很明显：512 个代理模型 = 720.5 GPU 小时；而且「拟合曲面再采点」这个范式，拟合不准就全盘皆输。</p> <p><strong>代 2.5：聚类迭代缩搜（CLIMB, 2025）。</strong> CLIMB 是对 RegMix 的工程化改进：不一次性均匀撒 512 个点，而是先粗搜 → 聚类 → 缩小搜索区域 → 迭代细化。代理模型数量降到 64 个，搜索成本降到 71.9 GPU 小时。成本降了一个数量级，但「要多训不少代理模型」的本质没变，聚类迭代本身也引入了自己的超参。</p> <p>（这里还有一条并行的支线：<strong>配比定律（Data Mixing Laws）</strong>。DMLaw、Ge et al. 等人的工作尝试拟合「配比 → 损失」的解析函数，并利用小模型上拟合出的参数外推到大模型。它和 RegMix 共享「拟合」哲学，但目的更偏向「预测/外推」而非搜索。这条线为代理模型方法提供了理论基础：<strong>小模型上找到的最优配比，可以迁移到大模型</strong>——否则「用 1M 参数的代理模型替 70B 模型做决定」这件事本身就站不住脚。）</p> <p><strong>代 3：梯度直通（FastMix, ICLR 2026）。</strong> 到这里思路变了：不再「外面套一层搜索」，而是把配比本身变成可学习参数，让梯度一路从验证目标传到配比上。只需要 1 个代理模型，1.3 GPU 小时。这就是本文主角，下一节细说。</p> <h3 id="33-动态派odm-与-ideal">3.3 动态派：ODM 与 IDEAL</h3> <ul> <li><strong>ODM（Albalak et al., 2023）</strong>：训练过程中在线调整数据混合比例，思路更接近课程学习。</li> <li><strong>IDEAL（Ming et al., 2025）</strong>：用影响函数（influence function）估计每个数据源对下游性能的贡献，动态重平衡训练数据。</li> </ul> <p>影响函数本身是漂亮的想法——用二阶信息估计「把某个样本的权重微调 $\varepsilon$，验证损失会变多少」——但工程上贵且数值不稳定。记住这一点，后面 FastMix 的闭式梯度会和它有个有意思的对照。</p> <h3 id="34-一张表--一张图">3.4 一张表 + 一张图</h3> <table> <thead> <tr> <th>方法</th> <th>核心思路</th> <th>代理模型数</th> <th>搜索成本（预训练）</th> <th>主要短板</th> </tr> </thead> <tbody> <tr> <td>人工启发式</td> <td>手工设定</td> <td>0</td> <td>0</td> <td>不可迁移，无最优性保证</td> </tr> <tr> <td>DoReMi</td> <td>相对损失重加权</td> <td>1</td> <td>7.4h</td> <td>信号间接，易放大噪声域</td> </tr> <tr> <td>RegMix</td> <td>拟合「配比→损失」曲面再外推</td> <td>512</td> <td>720.5h</td> <td>代理模型太多，拟合误差</td> </tr> <tr> <td>CLIMB</td> <td>聚类迭代缩小搜索空间</td> <td>64</td> <td>71.9h</td> <td>代价仍大，超参多</td> </tr> <tr> <td>ODM / IDEAL</td> <td>训练中在线调整</td> <td>0</td> <td>—</td> <td>不稳定，精度通常弱于静态派</td> </tr> <tr> <td><strong>FastMix</strong></td> <td><strong>重参数化 + 双层优化，梯度直通</strong></td> <td><strong>1</strong></td> <td><strong>1.3h</strong></td> <td>$n_2=1$ 偏贪婪，对噪声敏感</td> </tr> </tbody> </table> <p>（成本数字对应 FastMix 论文的预训练设置：Pile 数据集、1M 参数代理模型、最多 1B token 搜索预算。）</p> <p><img src="/assets/img/posts/20260915-fastmix/fig_evolution.png" alt="数据配比方法演进与搜索成本"/></p> <h3 id="35-脉络总结">3.5 脉络总结</h3> <p>把上面串起来，能看到两条清晰的线：</p> <p><strong>第一条线：怎么用更小的代价「试」配比？</strong> 答案是代理模型——用 1M 参数的小模型替大模型试错。这条线的尽头是 RegMix/CLIMB 的「多代理 + 回归」：把配比搜索当黑盒，用采样换信息。</p> <p><strong>第二条线：怎么让配比「自己学习」？</strong> 这是更本质的追求：把 $\alpha$ 从「要被搜索的离散量」变成「可以被梯度优化的连续量」。FastMix 的贡献就是打通了这条线。</p> <p>有趣的是，第二条线的成功恰恰缓解了第一条线的焦虑：当配比可以用 1 个代理模型 + 梯度直接优化时，「多训几个代理模型来拟合曲面」就变得没有必要了。</p> <h2 id="4-fastmix-的原理">4. FastMix 的原理</h2> <h3 id="41-核心一招重参数化把采样概率变成损失权重">4.1 核心一招：重参数化，把「采样概率」变成「损失权重」</h3> <p>FastMix 全部工作的起点是一个看似平淡的数学观察：<strong>混合采样下，期望训练损失 = 各数据源损失的凸组合</strong>，即</p> \[\mathbb{E}\big[\mathcal{L}_{\text{train}}(D, w \mid \alpha)\big] = \sum_{i=1}^{k} \alpha_i\, \mathcal{L}_{\text{train}}(D_i, w)\] <p>这个恒等式意味着什么？意味着我们<strong>不需要真的「按 $\alpha$ 采样」来训练</strong>——可以在每个 batch 里均匀采样各源数据，然后按 $\alpha$ 对损失加权。而一旦切换到这个视角，$\alpha$ 的语义也变了：它不再是采样概率，而是<strong>损失权重</strong>。损失加权是可微操作，于是 $\alpha$ 从「不可微的采样算子参数」变成了「完全可微的连续权重」，可以名正言顺地放进任何梯度优化器里。<strong>这就是全文最值钱的一步：重参数化（reparameterization）。</strong></p> <p>这个技巧在架构搜索领域特别眼熟——DARTS 就是把「选哪一个候选操作」的离散 argmax 松弛成 softmax 权重。FastMix 相当于在数据维度上做了一次同样的松弛：把「选哪个数据源」变成「给每个数据源一个连续权重」。</p> <p><img src="/assets/img/posts/20260915-fastmix/fig_mechanism.png" alt="FastMix 重参数化与双层优化机制"/></p> <h3 id="42-双层优化模型-w-与配比-alpha-交替更新">4.2 双层优化：模型 $w$ 与配比 $\alpha$ 交替更新</h3> <p>有了可微的 $\alpha$，FastMix 的优化就顺理成章了，完整流程见算法 1：</p> <ul> <li><strong>内层循环（跑 $n_1$ 步）</strong>：固定当前 $\alpha$，用加权损失 $\sum_i \alpha_i\, \mathcal{L}_{\text{train}}(D_i, w)$ 更新模型参数 $w$（SGD）。注意：代理模型并不是单独预训练好的，而是和配比搜索同时训练——它本身就是搜索过程的一部分。</li> <li><strong>外层循环（只走 $n_2 = 1$ 步）</strong>：固定 $w$，用验证集反馈更新 $\alpha$：</li> </ul> \[\alpha \leftarrow \alpha - \eta_\alpha \cdot \frac{\partial \mathcal{L}_{\text{target}}}{\partial \alpha}\] <p>其中搜索目标 $\mathcal{L}_{\text{target}}$ 是三项之和：</p> \[\mathcal{L}_{\text{target}} = \mathcal{L}_{\text{val}}(w) + \beta\, \mathcal{L}_{\text{train}}(w) + \lambda \sum_{i} \alpha_i \log \alpha_i\] <ul> <li>第一项是<strong>验证损失</strong>（或下游任务损失），是主要优化信号；</li> <li>第二项是<strong>训练损失</strong>，作为辅助目标，减少对有限验证集的依赖（论文里 $\beta \approx 0.1$ 时效果最好）；</li> <li>第三项是<strong>熵正则化</strong>（$\lambda \approx 10^{-5}$），防止配比坍缩到少数几个数据源上——只盯着一个源猛采很容易过拟合验证信号。</li> </ul> <p><img src="/assets/img/posts/20260915-fastmix/algorithm1_full.png" alt="FastMix 优化算法"/></p> <h3 id="43-最漂亮的一步n_21-时的闭式梯度">4.3 最漂亮的一步：$n_2=1$ 时的闭式梯度</h3> <p>如果外层只走一步（$n_2 = 1$），$\alpha$ 的梯度有一个非常干净的闭式解：</p> \[\frac{\partial \mathcal{L}_{\text{target}}}{\partial \alpha_i} = -\eta_w \cdot \nabla_w \mathcal{L}_{\text{val}}(V, w^{t+1}) \cdot \nabla_w \mathcal{L}_{\text{train}}(D_i, w^t)\] <p>翻译成人话：<strong>$\alpha_i$ 的更新方向正比于「验证集梯度」和「第 $i$ 个数据源训练梯度」的点积</strong>。这个点积有非常直观的几何含义：</p> <p><img src="/assets/img/posts/20260915-fastmix/fig_alignment.png" alt="梯度对齐决定配比调整方向"/></p> <ul> <li><strong>同向（点积 $&gt; 0$）</strong>：这个数据源把模型往验证目标想要的方向推 → 加大它的配比；</li> <li><strong>反向（点积 $&lt; 0$）</strong>：它在拖后腿 → 降低配比；</li> <li><strong>正交（点积 $\approx 0$）</strong>：与当前目标无关 → 基本不动。</li> </ul> <p>这个解释优雅得不像工程方法（笑）。而且它顺带解释了 FastMix 为什么「知道」每个源当下该不该被重视——它不是在全局意义上给每个源定一个静态权重，而是在<strong>训练过程的每一刻</strong>都能给出「此刻谁在帮倒忙」的信号。</p> <h3 id="44-为什么-n_2-必须等于-1">4.4 为什么 $n_2$ 必须等于 1</h3> <p>论文里专门有一节「No Free Lunch」讨论工业实践中的坑，其中很关键的一条就是：<strong>长外循环（$n_2 &gt; 1$）不可行</strong>。为什么？$n_2 &gt; 1$ 意味着 $\alpha$ 的更新要穿过 $n_2$ 步模型更新做反向传播——要么用 BPTT（显存灾难），要么用有限差分（慢且噪声大，尤其当目标还是非可微指标时）。作者实际试了 SPSA、有限差分等黑盒梯度估计器，在大规模工业数据上几乎不收敛。所以 FastMix 坚定地选择 $n_2 = 1$：<strong>宁可有点贪婪，也要梯度干净</strong>。</p> <h2 id="5-实现思路怎么把论文变成代码">5. 实现思路：怎么把论文变成代码</h2> <p>官方实现在 <a href="https://github.com/hrtan/fastmix">github.com/hrtan/fastmix</a>，读完代码有几个实现细节值得记一下：</p> <ol> <li> <p><strong>配比就是一组可学习 logits。</strong> 混合权重对应一个 <code class="language-plaintext highlighter-rouge">dataset_probs</code> 参数，softmax 之后得到采样分布。搜索过程中每隔若干步把当前 logits 存下来（<code class="language-plaintext highlighter-rouge">FastMixtureOut/probs_module_step*.pt</code>），所以<strong>配比随训练的演化轨迹是直接可观测的</strong>——这相当于免费送你一份「数据课程」曲线。</p> </li> <li><strong>两种搜索目标（两个入口）：</strong> <ul> <li><code class="language-plaintext highlighter-rouge">train_fastmix_val.py</code>：目标 = 留出验证集上的语言建模损失（纯 LM 目标，适合预训练场景）；</li> <li><code class="language-plaintext highlighter-rouge">train_fastmix_sft.py</code>：目标 = 下游 SFT 数据。每条样本是 <code class="language-plaintext highlighter-rouge">{"question","answer"}</code> 对，实现里会 <strong>mask 掉 question 部分，只在 answer token 上算 loss 和梯度</strong>——论文的后训练实验就是它跑的。</li> </ul> <p>注意「可微目标」在实现层面的体现：验证指标是离散的（比如准确率）时没法直接求导，所以论文建议用 SFT loss 当可微代理目标——这是「No Free Lunch」一节反复强调的实践要点。</p> </li> <li> <p><strong>效率靠工程。</strong> 代理模型本身不大（后训练实验里约 1B：Qwen2.5-1.5B-Instruct），但整个 pipeline 用了 flash-attention 的融合 cross-entropy / RMSNorm 核；数据侧走 RegMix 的管线（gpt-neox 分词 → 打包成二进制分片）；验证集用更小的 chunk size（$131136 = 2049 \times 64$）适配小数据量。</p> </li> <li><strong>会踩的坑（论文 + 代码共同提示）：</strong> <ul> <li><strong>代理模型小于 0.5B 时不可靠</strong>：收敛不稳定、配比噪声大、对不同数据源有容量/架构偏差——作者把 RegMix 当 oracle 也观察到同样现象，说明这不是 FastMix 独有的问题，而是代理模型范式的通病；</li> <li><strong>序列长度不匹配</strong>：预训练序列长、SFT 序列短，梯度方向差异巨大，FastMix 会直接失败。解法是把多个 SFT 序列拼接起来，对齐到预训练长度；</li> <li><strong>正则化选型</strong>：学术界小数据集上熵正则防坍缩够用，但工业数据上，最稳的是「过采样不超过原始数据 3 倍」这种硬约束；</li> <li>一个直接可复用的命令长这样（照 README 改路径即可）：</li> </ul> </li> </ol> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python pretrain/train_fastmix_val.py <span class="se">\</span>
  <span class="nt">--train_data_dir</span> /path/to/welldata <span class="se">\</span>
  <span class="nt">--val_data_dir</span> /path/to/welldata <span class="se">\</span>
  <span class="nt">--data_yaml_file</span> configs/pile.yaml <span class="se">\</span>
  <span class="nt">--out_name</span> my_run <span class="nt">--resume</span> False <span class="se">\</span>
  <span class="nt">--learning_rate_dataset</span> 0.01
</code></pre></div></div> <h2 id="6-实验结果">6. 实验结果</h2> <h3 id="61-预训练482-分914-项最佳13-gpu-小时">6.1 预训练：48.2 分、9/14 项最佳、1.3 GPU 小时</h3> <p>设置：Pile 的 17 个无版权子集；代理模型约 1M 参数、最多训 1B token；搜索目标选 Pile-cc 子集上的损失；搜索完成后用得到的配比训练 1B 参数模型（25B token），在 14 个下游任务上评测。</p> <table> <thead> <tr> <th>方法</th> <th>14 项平均分</th> <th>搜索成本</th> </tr> </thead> <tbody> <tr> <td><strong>FastMix</strong></td> <td><strong>48.2</strong>（14 项中 9 项最佳）</td> <td><strong>1.3 GPU 小时</strong></td> </tr> <tr> <td>CLIMB</td> <td>47.5</td> <td>71.9 GPU 小时</td> </tr> <tr> <td>RegMix</td> <td>47.2</td> <td>720.5 GPU 小时</td> </tr> </tbody> </table> <p><img src="/assets/img/posts/20260915-fastmix/figure2_p7.png" alt="预训练 14 个下游任务表现与搜索成本"/></p> <p>FastMix 比 CLIMB 快约 55 倍、比 RegMix 快约 550 倍，而且平均分最高、最佳任务数最多（9/14）——「更快」和「更好」同时成立，这在搜索类方法里挺少见。</p> <p><img src="/assets/img/posts/20260915-fastmix/figure1_p1.png" alt="性能与搜索成本权衡（预训练与后训练）"/></p> <h3 id="62-后训练sft55-分52-倍加速">6.2 后训练（SFT）：+5.5 分、52 倍加速</h3> <p>设置：Qwen2.5-Math-Instruct-7B 上做指令微调；8 个领域 SFT 数据（数学 OpenR1-Math-220k、代码 OpenThoughts-114K、对话 ShareGPT、STEM Platypus 等）；优化目标 = GSM8K 与 gaokao2023en 的 1:1 加权评分；代理模型约 1B 参数（Qwen2.5-1.5B-Instruct）。为公平，RegMix/CLIMB 的代理模型数被压到 64 个。</p> <table> <thead> <tr> <th>方法</th> <th>平均分</th> <th>搜索成本</th> </tr> </thead> <tbody> <tr> <td><strong>FastMix</strong></td> <td><strong>65.4</strong></td> <td><strong>2.2 GPU 小时</strong></td> </tr> <tr> <td>CLIMB</td> <td>59.9</td> <td>117.4 GPU 小时</td> </tr> <tr> <td>RegMix</td> <td>—</td> <td>115.9 GPU 小时</td> </tr> </tbody> </table> <p><img src="/assets/img/posts/20260915-fastmix/figure3_p8.png" alt="后训练（SFT）下游表现与搜索成本"/></p> <p>最值得注意的其实不是分数本身，而是<strong>泛化性</strong>：搜索信号只有数学（GSM8K + 高考题），但 FastMix 得到的配比在 LiveCodeBench（代码）和 GPQA-Diamond（STEM 问答）上也全面领先——说明它没有过拟合到优化信号上，找到的是一个「结构性更好」的配比，而不是「对数学卷子特攻」的配比。</p> <h2 id="7-takeaways">7. Takeaways</h2> <p>最后说点私货。</p> <ol> <li> <p><strong>重参数化是全文的灵魂。</strong> 从 AutoML 的视角看（我自己做 NAS 出身），FastMix 和 DARTS 走的是同一条路线：把离散的选择问题松弛成连续的权重问题，梯度就有了立足之地。差别在于 DARTS 松弛的是架构，FastMix 松弛的是数据采样——而数据配比恰恰是 LLM 时代最贵、最值得被「可微化」的变量之一。</p> </li> <li> <p><strong>黑盒与白盒的差距是数量级的。</strong> RegMix 用 512 个模型去「采样」配比-性能曲面，每个模型只贡献一个标量损失；FastMix 的 1 个模型每步都在产出全梯度信息。512 个标量 vs 每一步一个向量——信息量完全不在一个量级。这解释了为什么「回归拟合」路线在配比这种高维、非凸、噪声大的问题上总是吃力不讨好。</p> </li> <li> <p><strong>梯度对齐是一种一阶的影响函数。</strong> $\alpha$ 的更新方向是「验证梯度 · 各源梯度」，本质是一种数据归因：每个源对验证目标的贡献。IDEAL 用影响函数做同样的事，但需要二阶信息和 Hessian 近似，数值上很娇气；FastMix 用一阶梯度就拿到了大部分直觉，代价是短视（$n_2 = 1$ 的贪婪）。这两种思路的取舍值得记一笔。</p> </li> <li> <p><strong>配比是时间的函数，不是常数。</strong> 论文观察到不同数据源的重要性随训练动态演化：有的源前期关键，有的源训练很久之后才主导。这给了课程学习（curriculum learning）一个很自然的视角——FastMix 产出的 $\alpha$ 轨迹序列，本身就是一份可复用的数据课程。如果后续工作把 $\alpha$ 显式参数化成时间函数（而不是逐轮外循环更新），可能会更有意思。</p> </li> <li> <p><strong>工程细节决定成败。</strong> 这一篇最实在的地方就是「No Free Lunch」那一节：SPSA/有限差分在工业数据上不收敛、小于 0.5B 的代理模型不可靠、序列长度不匹配直接失败、熵正则不如过采样硬约束……这些「论文正文不会告诉你」的教训，恰恰是复现和落地时最值钱的。</p> </li> <li> <p><strong>局限。</strong> $n_2 = 1$ 的贪婪性让它对数据噪声敏感；当前流程还是「先搜后训」的一次性流程，离「边训边调」的在线范式还有距离；对验证集/搜索目标的选择依然敏感（选 Pile-cc 当目标 vs 选下游任务，结果会有差异）。</p> </li> </ol> <hr/> <h2 id="参考资料">参考资料</h2> <ul> <li>FastMix: Fast Data Mixture Optimization via Gradient Descent（ICLR 2026）— <a href="https://arxiv.org/abs/2606.14971">arXiv:2606.14971</a></li> <li>官方实现 — <a href="https://github.com/hrtan/fastmix">github.com/hrtan/fastmix</a></li> <li>DoReMi: Optimizing Data Mixtures Speeds Up Language Model Pretraining（Xie et al., 2024）</li> <li>RegMix: Data Mixture as Language Model Pretraining Strategy（Liu et al., 2024）</li> <li>CLIMB: Clustering-based Iterative Data Mixture Bootstrapping for Language Model Pretraining（Diao et al., 2025）</li> </ul> <hr/> <p>顺带说一句：配比搜索本质上就是一次「超参搜索」，只是这个超参维度高、评估又贵。更有意思的是，FastMix 这套「内层训练模型、外层更新权重」的双层优化流程，和 DARTS 搜架构其实是同一个套路——DARTS 用 bi-level 优化搜索的是架构权重，FastMix 只是把搜索对象换成了数据集权重，换汤不换药。我们把手头关于 AutoML / NAS 的积累整理成了《动手学 AutoML：从 NAS 到大语言模型优化实战》，书里在搜索策略一章和「DARTS 可微分搜索」实战章节，把这个双层优化从数学推导讲到代码实现；理解了这一层，再回头看 FastMix 的闭式梯度，你会觉得它「原来如此」，而不是「不明觉厉」。感兴趣可以翻翻。</p> <blockquote> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="数据配比"/><category term="论文解读"/><category term="预训练"/><summary type="html"><![CDATA[ICLR’26 | FastMix 把预训练数据配比搜索从 720 GPU 小时干到 1.3 小时]]></summary></entry><entry><title type="html">arXiv’26 | 小米 CocktailASR-1 给 ASR 装上“选择性听觉”：参考语音当声纹 prompt，一个模型只录你一个人</title><link href="https://marsggbo.github.io/blog/2026/20260912-cocktailasr/" rel="alternate" type="text/html" title="arXiv’26 | 小米 CocktailASR-1 给 ASR 装上“选择性听觉”：参考语音当声纹 prompt，一个模型只录你一个人"/><published>2026-09-12T00:00:00+00:00</published><updated>2026-09-12T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260912-cocktailasr</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260912-cocktailasr/"><![CDATA[<h1 id="arxiv26--小米-cocktailasr-1-给-asr-装上选择性听觉参考语音当声纹-prompt一个模型只录你一个人">arXiv’26 | 小米 CocktailASR-1 给 ASR 装上“选择性听觉”：参考语音当声纹 prompt，一个模型只录你一个人</h1> <blockquote> <p>原文：<a href="https://arxiv.org/html/2609.11274v1">Xiaomi-CocktailASR-1 Technical Report</a> 代码：<a href="https://github.com/xiaomi-research/xiaomi-cocktailasr-1">xiaomi-research/xiaomi-cocktailasr-1</a>（模型权重与推理代码在 <a href="https://huggingface.co/Ease3/Xiaomi-CocktailASR-1">HuggingFace: Ease3/Xiaomi-CocktailASR-1</a>）</p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>大模型把 ASR 带入了一个新阶段：把音频编码器接到 LLM 前面、音频编码成 embedding 当 token 喂进去，由 LLM 自回归地把文字解码出来。这类模型一般叫 SpeechLLM，它不是一个具体模型的名字，而是一类架构的统称——Qwen3-ASR、StepAudio2、Seed-ASR 都属于这一类。它们把多语言、方言、时间戳、上下文理解都收进了一个统一的端到端模型，在干净的单说话人语音上准确率已经高到没什么可优化的。</p> <p>但这类模型有一个共同的软肋：<strong>说话人一多就崩</strong>。一段录音里两三个人同时开口，让模型转录其中某一位——这个困扰了语音领域几十年的”鸡尾酒会问题”（cocktail party problem），到现在仍然是 ASR 走向真实场景最硬的那道坎。上面那几个模型在强重叠的多人测试集上，错误率能飙到 60% 甚至 160%，基本等于不可用。</p> <p>小米这次放出的 <strong>Xiaomi-CocktailASR-1</strong> 就是冲着这个场景去的，而且它想同时解决四件事：在多人混音里准确转录指定说话人、在只有一个人说话时不掉点、在参考说话人根本不在录音里时老老实实输出空、以及可选地把”为什么选这个人”的推理过程写出来。</p> <p>这四件事的难点不在于单独做会做——前两件过去十年都有人做，难的是<strong>它们的最优解互相打架</strong>，而同一个产品里这四种情况是随机出现的。这篇技术报告的价值，就是给出了一个”统一架构 + 分阶段训练”的方案，把四种能力放进同一个模型里还能各自不掉点。下面我们按 需求 → 老方案为什么不行 → 新设计 → 怎么训 → 数据怎么造 → 效果 的顺序完整过一遍。</p> <p><img src="/assets/img/posts/20260912-cocktailasr/figure1.png" alt="CocktailASR-1 的四个核心能力"/></p> <h2 id="2-目标说话人识别的两条老路一个在传递误差一个在传递噪声">2. 目标说话人识别的两条老路：一个在传递误差，一个在传递噪声</h2> <p>要搞清楚 CocktailASR-1 的设计动机，得先看这个任务过去是怎么做的。</p> <p>这个任务在学术上叫 <strong>TS-ASR</strong>（Target-Speaker ASR，目标说话人语音识别）：输入是一段混着好几个人的录音，外加一小段”指定是谁”的参考语音，输出只包含那个人的话。注意它和 MT-ASR（Multi-Talker ASR，多说话人识别）不是一回事——MT-ASR 是”把所有人依次转出来”，输出用特殊 token 分隔，但<strong>它不告诉你每句话是谁说的</strong>，在你只关心某一个特定说话人的场景里就没法用。</p> <p>TS-ASR 此前主要有两条技术路线：</p> <p><strong>第一条是级联式：先分离，再识别。</strong> 前面挂一个 TSE（Target Speech Extraction，目标语音提取）模型，把目标说话人的声音从混音里剥离出来，后面接一个普通 ASR 模型做转录。这条路的工程直觉很顺，但有两个绕不过去的问题：一是分离模型输出的音频天然带失真和伪影，这些误差会<strong>原样传给下游 ASR</strong>，一步错步步错；二是两个模块独立训练、独立优化，中间没有任何机制能补偿对方的缺陷。</p> <p><strong>第二条是端到端注入声纹向量。</strong> 不做显式分离，而是用一个独立训练好的说话人验证模型（d-vector / x-vector / ECAPA-TDNN 这一类）把参考语音压成一个<strong>定长向量</strong>，再把这个向量作为条件信号注入到 ASR 模型的某一层。这条路省掉了分离环节，但它把问题换了个形式：参考语音被压成了一个几百维的向量，说话人音色之外的细节全丢了；更麻烦的是，这个声纹编码器的训练目标是”区分说话人身份”，不是”服务于识别内容”，两边的优化目标从一开始就不一致。后果是，<strong>哪怕一段音频里只有一个人在说话、压根不需要”认人”，这个硬塞进来的条件信号依然会干扰模型的正常识别</strong>——具体表现就是模型对着单说话人语音也会过度抑制，把本该转出来的内容当成”非目标”给删掉，这种删除错误在错误率上的表现比替换错误更难看。</p> <p>这两条路还共享一个更根本的缺陷：<strong>它们都没有任何环节负责判断”目标说话人到底在不在这一段里”</strong>。分离模型总会输出点东西，声纹条件信号总会被注入，下游 ASR 自然也总会给出一段转录——即便参考说话人压根没出现在混音里，模型也会一本正经地”编”一段不存在的话出来，而不是老实说”没有这个人”。在智能音箱、耳机这类实时交互设备上，这种误触发比识别错词更伤用户体验。</p> <p>到这里，CocktailASR-1 要解决的核心矛盾就清楚了：<strong>为多人场景做的优化会拖累单说话人的保真度，而且不管怎么优化，拒识能力都不是靠调参能调出来的</strong>。</p> <h2 id="3-cocktailasr-1-的解法参考语音不做压缩不走独立模块直接和目标语音进同一套表征">3. CocktailASR-1 的解法：参考语音不做压缩、不走独立模块，直接和目标语音进同一套表征</h2> <p>既然老方案的问题出在”中间要过一道独立的声纹模块”，CocktailASR-1 的做法就是把这道中间环节整个拿掉——<strong>没有独立的声纹验证模型，也没有独立的语音分离模块</strong>。</p> <p>它的信息通路是这样设计的：参考语音和待识别的混合语音被拼接成一整段波形，一起送进同一个 <strong>Data2Vec2（D2V2）音频编码器</strong>，编码结果直接喂给 <strong>Qwen3-8B</strong>。于是”这段混音里哪一段是目标说话人”这件事，不再由某个专门模块提前算好再交接给 ASR，而是变成了 LLM 在自己 self-attention 里做的事：参考语音那一段的帧级表征，和混合语音里每个候选说话人对应时间段的帧级表征，被平等地摆进同一个序列，模型跨位置比较相似度、锁定该转录哪一段——这本来就是 Transformer 最擅长干的活。</p> <p>这个设计里有三处值得单独说清楚的判断：</p> <p><strong>第一，为什么用 D2V2 这种自监督学习（SSL）编码器，能顺便省掉声纹模块。</strong> D2V2 的预训练目标是掩码预测（mask prediction）：把输入的一段语音特征遮住，让模型根据上下文把被遮的部分重建出来。要做这件事，模型必须同时理解”说了什么内容”和”这段声音像谁”——前者给语义，后者给音色。所以 SSL 模型天然把语义和说话人信息融合在了同一套表征里，不需要再外挂一个只认身份的编码器。论文在架构一节把这点讲得很直白：正是这个机制让模型在提取目标说话人语音特征时能自适应地增强目标、抑制干扰。</p> <p><strong>第二，为什么保留帧级表征比压缩成声纹向量更划算。</strong> 前面说的级联方案和声纹向量方案，本质上都在做同一件事：把参考语音<strong>压缩</strong>成一个中间表示（一段分离出来的音频，或者一个定长向量）。任何压缩都会丢信息，而这里丢掉的恰好是判断”像不像”最需要的细粒度线索。CocktailASR-1 选择不压缩——参考语音保留完整的帧级序列，和目标语音的表征摆在同一个空间里直接比对。</p> <p><strong>第三，为什么这套设计能给单说话人场景留退路。</strong> 因为编码通路是共享的：只有一个人说话时，模型走的是一条和普通 ASR 几乎一样的路径，不存在”额外的条件信号来扰乱正常识别”这回事。这也是为什么后面第 5 节的第一个训练阶段，本质上就是在训一个普通 ASR 模型。</p> <p>至于拒识，这套设计给出的答案是：参考音、目标音、输出文本全部处于同一个自回归序列里，只要训练数据里放了”参考说话人缺席”的负样本，模型自然有地方去学会<strong>把输出收敛成空</strong>，不需要推理时再加阈值判断逻辑。</p> <h2 id="4-一段拼接波形怎么变成一段文本编码贴-token生成的完整链路">4. 一段拼接波形怎么变成一段文本：编码、贴 token、生成的完整链路</h2> <p>上面说的是设计思路，具体到实现，结合 HuggingFace 上开源的 <code class="language-plaintext highlighter-rouge">modeling_mic_asr.py</code> 和 <code class="language-plaintext highlighter-rouge">feature_extraction_mic_asr.py</code>（通过 <code class="language-plaintext highlighter-rouge">trust_remote_code</code> 加载）能看得很清楚。</p> <p><img src="/assets/img/posts/20260912-cocktailasr/figure2.png" alt="CocktailASR-1 框架总览"/></p> <p>下面这套流程涉及不少维度变化和模块先后顺序，光看文字容易绕，先给一张完整链路图，再逐步展开说细节：</p> <p><img src="/assets/img/posts/20260912-cocktailasr/pipeline_encode.png" alt="输入拼接→编码→贴入LLM的完整流程"/></p> <p><strong>第一步，输入怎么拼。</strong> <code class="language-plaintext highlighter-rouge">MicAsrFeatureExtractor.prepare_audio</code> 先把参考音频和目标音频各自做峰值归一化（统一到 0.99），然后处理参考音频的长度：短于 1 秒就补零到 1~4 秒中的一个随机长度；长于 4 秒就在 1~4 秒区间里随机采样一个目标长度，再从原音频里<strong>随机截取</strong>一段（不是固定截头或截尾）。处理好的参考片段记作 <code class="language-plaintext highlighter-rouge">ref_cut</code>，目标音频前面拼上 1 秒静音，最终喂给模型的是一整条 1D 单声道波形：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>waveform = ref_cut ⊕ [1秒静音] ⊕ target
</code></pre></div></div> <p>顺序是<strong>参考在前、目标在后</strong>，中间的 1 秒静音是刻意插进去的——它的作用是明确切开”参考”和”待识别”这两段信号，给声纹特征一个锚点，否则模型很难判断参考片段在哪里结束。</p> <p><strong>第二步，怎么编码。</strong> 这整条波形送进 <code class="language-plaintext highlighter-rouge">MicAsrAudioEncoder</code>。里面的 D2V2 编码器（0.6B 参数），先把波形转成 FBank 声学特征（25ms 窗、10ms 步长、80 维），再过两层 stride=2 的卷积做时间维下采样（合计 4 倍），于是帧率从 10ms 降到 <strong>40ms 一帧</strong>——一段 6 秒的拼接音频，大概会变成 150 个左右的帧级表征。这里有个论文提到的工程改动：原始 D2V2 是吃裸波形的，小米在编码器前端加了一个 FBank 处理模块来替换波形输入，训练和推理速度都能明显加快，而性能不掉。</p> <p>这些表征经过两层线性映射：<code class="language-plaintext highlighter-rouge">in_proj</code> 是 <code class="language-plaintext highlighter-rouge">Linear(1280, 1280)</code>，<code class="language-plaintext highlighter-rouge">out_proj</code> 是 <code class="language-plaintext highlighter-rouge">Linear(1280, 4096)</code>，<code class="language-plaintext highlighter-rouge">4096</code> 正好是 Qwen3-8B 的隐藏维度。这两层就是论文说的 Adapter，做的事情很直接：把音频编码器的输出维度对齐到 LLM 能吃的维度，完成跨模态对齐。</p> <p><strong>第三步，怎么”贴”进 LLM。</strong> 拿到形状为 <code class="language-plaintext highlighter-rouge">[1, seq_len, 4096]</code> 的音频向量后，代码构造了一条输入 token 序列：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>input_ids = [audio_token_index] * seq_len + prompt_ids
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">audio_token_index</code> 是词表里预留的一个占位 id（值 151669），先占住音频对应的每一个位置，后面接文本 prompt 编码出的 token id。真正的音频向量不是拼进 embedding 序列的，而是通过在 Qwen3 的 <code class="language-plaintext highlighter-rouge">embed_tokens</code> 模块上挂一个 forward hook 塞进去的：hook 在词嵌入查表完成之后，把 <code class="language-plaintext highlighter-rouge">input_ids</code> 里等于 <code class="language-plaintext highlighter-rouge">audio_token_index</code> 的那些位置，用 <code class="language-plaintext highlighter-rouge">masked_scatter</code> 直接替换成前面算出来的音频向量。这样就把连续的音频表征”贴”进了离散 token 序列里，<strong>不需要改动 Qwen3 本身的 embedding 层</strong>。替换后的整条序列送进 Qwen3 做标准自回归生成，从输入长度之后的部分开始解码，就是最终文本。</p> <p>有一点容易看漏：实际拼进模型的是 <code class="language-plaintext highlighter-rouge">prompt_prefix + prompt</code>，<code class="language-plaintext highlighter-rouge">prompt_prefix</code> 是一个 <code class="language-plaintext highlighter-rouge">&lt;|im_end|&gt;</code> 特殊 token。标准模式和 CoT 模式用的是两套 prompt，除此之外前向计算逻辑完全一样，生成配置也共用：贪心解码（<code class="language-plaintext highlighter-rouge">do_sample=False</code>）、重复惩罚 1.1、标准模式 <code class="language-plaintext highlighter-rouge">max_new_tokens=256</code>、CoT 模式 512。</p> <p><strong>第四步，不同模式下输入输出分别长什么样。</strong> 这张表把三种情况说清楚：</p> <table> <thead> <tr> <th>场景</th> <th>输入（音频）</th> <th>输入（prompt）</th> <th>输出</th> </tr> </thead> <tbody> <tr> <td>标准模式 · 单人</td> <td><code class="language-plaintext highlighter-rouge">ref_cut ⊕ 静音 ⊕ target</code></td> <td><code class="language-plaintext highlighter-rouge">prompt_standard</code></td> <td>目标说话人的转录文本</td> </tr> <tr> <td>标准模式 · 多人</td> <td>同上</td> <td><code class="language-plaintext highlighter-rouge">prompt_standard</code></td> <td>目标说话人的转录文本</td> </tr> <tr> <td>标准模式 · 拒识</td> <td>同上（但参考说话人<strong>不在</strong> target 里）</td> <td><code class="language-plaintext highlighter-rouge">prompt_standard</code></td> <td>空字符串</td> </tr> <tr> <td>CoT 模式</td> <td>同上三种</td> <td><code class="language-plaintext highlighter-rouge">prompt_cot</code>：额外要求分步推理，结果放进 <code class="language-plaintext highlighter-rouge">&lt;think&gt;</code> / <code class="language-plaintext highlighter-rouge">&lt;answer&gt;</code> 标签</td> <td><code class="language-plaintext highlighter-rouge">&lt;think&gt;</code> 内是推理过程，<code class="language-plaintext highlighter-rouge">&lt;answer&gt;</code> 内是最终转录（拒识时 <code class="language-plaintext highlighter-rouge">&lt;answer&gt;</code> 内为空）</td> </tr> </tbody> </table> <p>这里最值得强调的是”拒识”那一行：<strong>代码里没有任何显式的相似度阈值判断去触发拒识</strong>，走的和标准模式完全一样的代码路径，模型只是在训练阶段学会了在”参考说话人缺席”这种输入模式下，把生成结果收敛成空。换句话说，拒识这件事在 CocktailASR-1 里是一个<strong>训出来的行为</strong>，不是一段推理时的 if-else——这也解释了为什么它在其他方案里天然缺失：不是加个阈值就能补上的，得是训练目标里就有这一项。</p> <h2 id="5-四个能力为什么要放在一个模型里它们的最优解在互相打架">5. 四个能力为什么要放在一个模型里：它们的最优解在互相打架</h2> <p>到这里有必要停下来，把”这个模型要同时做四件事”讲透，因为这是整篇报告的问题起点。</p> <p>论文在训练一节开头就把这四件事列了出来，按重要性分别是：</p> <p><strong>多人识别</strong>——这是 TS-ASR 的本职工作：在重叠语音里准确转录目标说话人，同时压住其他说话人的干扰。智能音箱、随身耳机、会议系统这类场景里，用户开口时旁边有人在说话是常态。</p> <p><strong>单说话人识别</strong>——论文给了一个很实际的数字：真实会议数据集 AliMeeting 的重叠率只有 30%~40%，也就是说<strong>大多数时候现场只有一个人在说话</strong>，而这个人可能正在使用一个专门为”抗多人干扰”设计的系统。更关键的是，系统事先并不知道这一段音频里有几个说话人，所以一个只为多人场景优化的 TS-ASR 模型，必然在单人场景下吃亏。</p> <p><strong>负样本拒识</strong>——用户可能只是路过，或者暂时没开口，此时录音里只有别人的声音。一个没有拒识能力的模型会把这些无关内容强行转录出来，造成严重的误触发。</p> <p><strong>CoT 推理</strong>——输出显式的中间推理步骤（有几个说话人、每个候选人的性别和声纹相似度），提升可解释性，同时给下游任务提供结构化信息。这一项论文自己也在强调它的定位是”可解释性和扩展性”，而不是单纯刷指标。</p> <p>四件事放在一起，真正的难点就浮现了：<strong>它们的目标函数互相拉扯</strong>。</p> <ul> <li>拒识能力的训练，本质是在教模型”输出可以为空”。这个信号一旦给多了，模型在正常识别时也可能动不动就输出空——<strong>拒识和保真度打架</strong>。</li> <li>多人抗干扰的训练，本质是在教模型”抑制一部分语音内容”。这个能力泛化到单说话人场景，就会变成前面说的过度抑制，把正常内容删掉——<strong>多人能力和单人能力打架</strong>。</li> </ul> <p>这就是第 2 节结尾那个”核心矛盾”在训练侧的体现。CocktailASR-1 给出的解法不是设计一个更复杂的损失函数去平衡它们，而是<strong>用四个训练阶段把冲突拆开，一次只加一种约束</strong>。</p> <p><img src="/assets/img/posts/20260912-cocktailasr/figure4.png" alt="四阶段训练流程"/></p> <p><strong>Stage 1：先训一个普通 ASR 模型（ASR Base）。</strong> 这一阶段完全不带参考语音，只用标准的 ASR prompt 和普通单说话人数据，把预训练音频编码器和 Qwen3 base 通过 Adapter 对齐起来，让一个本来只会读文本的 LLM 学会听写。这一步得到的其实就是一个标准 ASR 模型——它的存在是为了让”单说话人识别”这条路径有一个干净的起点，后面的训练都是在这个基础上叠加能力，而不是从头学习怎么听清内容。</p> <p><strong>Stage 2：正式引入参考语音（TS-ASR Base）。</strong> 数据换成”参考 + 静音 + 目标”的拼接形式，配 TS-ASR 专属 prompt，让模型学会提取声纹特征、定位目标说话人。这一阶段做了两件关键的加料：一是以 <strong>50% 的概率</strong>对训练数据施加噪声混合策略，把一半的干净数据直接变成多人重叠语音，同时注入环境噪声和人声干扰，保证重叠的随机性和多样性；二是掺入<strong>少量负样本</strong>，让模型第一次接触”拒识”这个信号，但比例控制得很低——原文的措辞是”在不损害核心识别性能的前提下”获得初步拒识能力。</p> <p><strong>Stage 3：加入推理能力（TS-ASR + CoT）。</strong> CoT 数据和原来的 TS-ASR 数据<strong>等比例混合</strong>做联合训练。这里的重点是联合训练而不是替换训练：模型要学会看 CoT prompt 就在 <code class="language-plaintext highlighter-rouge">&lt;think&gt;</code> 标签里写推理过程，看标准 prompt 就直接输出转录文本。这样标准模式的能力不会被 CoT 训练冲掉，用户可以按需开关 CoT。</p> <p><strong>Stage 4：精调收尾（Fine-tuning）。</strong> 在精心挑选的高质量数据上做 SFT，再叠加强化学习（RL，论文引的是 DeepSeekMath 那篇的做法）做进一步优化。这一阶段的核心动作是<strong>控制各类训练数据的混合配比</strong>，让多人识别、单人泛化、拒识三方面能力达到平衡。</p> <p>这个顺序的用意，说白了就是把”听清楚 → 认人 → 说理由 → 精调”拆成四步走，每一步都在前一步已经稳定的能力上叠加新目标，而不是一上来就让模型同时应付所有相互冲突的约束。</p> <h2 id="6-数据从哪来三类数据解决三个问题配比随训练阶段动态变">6. 数据从哪来：三类数据解决三个问题，配比随训练阶段动态变</h2> <p>四阶段能成立，前提是每一阶段都有对应的数据。CocktailASR-1 训练数据一共三类，每一类对应一个要解决的问题：</p> <p><strong>多说话人 TS-ASR 数据（约 40 万小时）</strong>，用来训核心能力。来源有三部分：开源多人数据，包括真实重叠录音（AMI、AliMeeting）和合成混音（LibriMix）；用自有的单说话人 ASR 数据合成出来的模拟重叠数据；以及自采的日常对话录音。合成方式是按概率往干净语音里注入环境噪声和人声干扰，制造随机重叠的多说话人场景——这个噪声混合策略就是 Stage 2 里以 50% 概率施加的那个。</p> <p><strong>单说话人 TS-ASR 数据（约 60 万小时）</strong>，用来防止过抑制。构造方式和多人数据正好相反：参考语音和待识别语音<strong>取自同一个说话人的不同录音片段</strong>。这个设计的意思很直白——让模型在”参考和目标其实是同一个人”的大量样本上，学会识别而不是拒绝，从而把多人训练带来的过度抑制倾向压下去。论文特意点出，单人和多人数据的采样比例是需要调优的，模型就是靠这个比例在”压住多人干扰”和”保住单人内容完整性”之间找到平衡点。</p> <p><strong>负样本数据（约 1 万小时）</strong>，用来训拒识。构造方式是把参考语音换成一段和目标音频完全无关的说话人录音，制造”参考说话人缺席”的场景。规模比前两类小两个数量级，但论文强调它的<strong>采样比例在不同训练阶段是逐步提高的</strong>——这正是 Stage 2 到 Stage 4 逐步加码的机制。如果一上来就给大量负样本，模型会过度倾向输出空，把正常识别也带崩。</p> <p><strong>CoT 数据</strong>不需要新录音，而是在上述三类样本上套模板生成的，按单说话人正样本、多说话人正样本、负样本三种格式分别构造。以开源代码里给的样例为例，一段 CoT 标注长这样：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;think&gt; Audio information: 0-2.15s is enrollment speech; 2.15-3.15s is silence;
3.15-18.1s is 2-speaker mixture audio; total duration 18.1s.
Enrollment speech: male.
Speaker1 information: male; similarity to the enrollment speech is 4.
Speaker2 information: female; similarity to the enrollment speech is 2.
Target speaker: Speaker1 and the enrollment speech are both male; 4(Speaker1) &gt; 2(Speaker2);
Speaker1 has the highest similarity score ... since 4 &gt;= 3, it is identified as the target speaker.
Final output:&lt;/think&gt; &lt;answer&gt;THIRTY OR FORTY MILES EASTWARD FROM SEATTLE&lt;/answer&gt;
</code></pre></div></div> <p>推理链条是固定的四段：先给出拼接音频的分段信息（参考语音段、静音段、待识别语音段各自的时间戳）、参考说话人的性别，然后逐个列出混合音频里每个候选说话人的性别和相似度打分，最后比较分数、给出结论。</p> <p>其中<strong>相似度打分是这套 CoT 数据生成的关键</strong>：用 CAM++ 声纹模型（一个说话人验证模型）分别提取各说话人和参考语音的 embedding，算余弦相似度，得到 0~1 之间的连续分数，再<strong>按均匀量化映射到 1~5 五个离散等级</strong>写进标注。论文解释了为什么要做这个离散化：连续分数里那些细微的数值差异对模型是噪声，离散成五个等级反而让训练更稳、收敛更快。判定规则是取相似度最高的候选说话人作为目标，若最高分低于等级 3，就标注为”目标说话人缺席”。</p> <p>这里要接上第 4 节那句话：这个等级 3 的阈值<strong>只出现在训练数据的标注阶段</strong>，是用来生成 CoT 监督信号的规则，不是模型推理时跑的代码逻辑。真正让模型学会拒识的，是这些带阈值标注的样本本身。</p> <p>三类数据的分工可以用一句话概括：<strong>多人数据教它”挑人”，单人数据教它”别乱删”，负样本教它”该闭嘴时闭嘴”，CoT 模板则把这些判断过程写成可监督的文本。</strong></p> <h2 id="7-效果多人场景拿到-sota同时单人和拒识两项都没塌">7. 效果：多人场景拿到 SOTA，同时单人和拒识两项都没塌</h2> <p>论文的评测分了四类测试集：合成的多人混音、真实的多人会议录音、单说话人测试集，以及专门构造的负样本测试集。指标这里先交代清楚，后面看表会顺畅很多：</p> <ul> <li><strong>WER / CER</strong>（Word / Character Error Rate，词 / 字错误率）：把识别结果改成参考答案需要改动的词数（或字数）占参考答案长度的比例，替换、删除、插入三类错误都计入，<strong>越低越好</strong>。中文按字算所以用 CER，英文按词算用 WER。这里要提醒一句：因为插入错误会膨胀分子，<strong>WER 是可以超过 100% 的</strong>——所以下面表里那些 110%、160% 的数字，意思是模型输出的内容比参考答案长得多、且大部分是错的，基本等于完全不可用。</li> <li><strong>RR</strong>（Rejection Rate，拒识率）：负样本里被正确拒识（输出空）的比例，<strong>越高越好</strong>。</li> <li><strong>FRR</strong>（False Rejection Rate，误拒率）：本该有内容的正样本被错误拒识的比例，<strong>越低越好</strong>——这个指标直接决定了模型在日常使用中会不会动不动就”装聋”。</li> <li><strong>Non-empty WER</strong>：把输出为空的样本剔除之后再算的 WER，避免拒识行为干扰对转录准确率的判断。</li> </ul> <p>先看合成多人数据集（LibriMix 和 LibriSpeechMix，都是一段音频里叠 2 人或 3 人的混音）：</p> <table> <thead> <tr> <th>Model</th> <th>LibriMix 2mix</th> <th>LibriMix 3mix</th> <th>LibriSpeechMix 2mix</th> <th>LibriSpeechMix 3mix</th> </tr> </thead> <tbody> <tr> <td><strong>Xiaomi-CocktailASR-1</strong></td> <td><strong>4.11</strong></td> <td>12.29</td> <td><strong>2.90</strong></td> <td><strong>4.91</strong></td> </tr> <tr> <td>Qwen3-ASR-1.7b</td> <td>68.75</td> <td>106.04</td> <td>92.17</td> <td>160.82</td> </tr> <tr> <td>StepAudio2</td> <td>71.23</td> <td>121.08</td> <td>92.72</td> <td>164.66</td> </tr> <tr> <td>Gemini-2.5-pro</td> <td>48.41</td> <td>76.10</td> <td>30.69</td> <td>52.34</td> </tr> <tr> <td>TCP</td> <td>4.84</td> <td>12.23</td> <td>-</td> <td>-</td> </tr> <tr> <td>CONF-TSASR</td> <td>-</td> <td>-</td> <td>5.40</td> <td>7.60</td> </tr> <tr> <td>MT-LLM</td> <td>6.70</td> <td>16.2</td> <td>-</td> <td>-</td> </tr> <tr> <td>TS-VAD(460h)</td> <td>6.61</td> <td>14.81</td> <td>7.92</td> <td>15.97</td> </tr> <tr> <td>Whisper-SS-TTI</td> <td>7.97</td> <td>21.97</td> <td>-</td> <td>-</td> </tr> <tr> <td>Transformer-SA-ASR</td> <td>-</td> <td>-</td> <td>6.40</td> <td>8.50</td> </tr> </tbody> </table> <p>这张表里最有信息量的不是第一名，而是两类失败的对比。Qwen3-ASR 和 StepAudio2 这些通用语音大模型在强重叠数据上直接崩到 60%~160% 的区间，<strong>这其实不是它们”弱”，而是它们从来没有被训练去做”在多人里挑一个人”这件事</strong>，输入一段重叠语音，它们会试图把所有内容都转出来，于是输出长度爆炸、错误率爆表。Gemini-2.5-pro 稍好一些——它能通过 prompt 理解”只转某个人”这个要求，所以在合成集上做到了 30%~80%，说明通用多模态模型确实有这个潜力，但缺乏针对性训练还是差得远。</p> <p>另一类对比是过去的 TS-ASR 专用 SOTA 模型，比如 TCP 和 CONF-TSASR：<strong>它们各自在自己擅长的数据分布上很强，但一个模型很难同时覆盖所有数据集</strong>。TCP 在 LibriMix 2mix 上做到 4.84%，CONF-TSASR 在 LibriSpeechMix 2mix 上是 5.40%，但表中它们的其他格子大多是空的。CocktailASR-1 的价值在于<strong>用一个模型把整张表都填上了</strong>：LibriMix 2mix 从 4.84% 降到 4.11%（相对下降 15.1%），LibriSpeechMix 2mix 从 5.40% 降到 2.90%（相对下降 46%）。3mix 那一列例外——LibriMix 3mix 上 12.29% 略高于 TCP 的 12.23%，基本可以看作打平或微弱落后。</p> <p>再看真实会议录音（AMI-SDM 是英文远场单麦克风会议，AliMeeting-Far 是中文远场 8 麦阵列会议，都带严重的混响和自发重叠）：</p> <table> <thead> <tr> <th>Model</th> <th>AMI SDM</th> <th>AliMeeting Far</th> </tr> </thead> <tbody> <tr> <td><strong>Xiaomi-CocktailASR-1</strong></td> <td><strong>21.81</strong></td> <td><strong>20.63</strong></td> </tr> <tr> <td>Qwen3-ASR-1.7b</td> <td>38.18</td> <td>39.64</td> </tr> <tr> <td>StepAudio2</td> <td>110.50</td> <td>76.82</td> </tr> <tr> <td>Whisper Large-v2</td> <td>36.40</td> <td>-</td> </tr> <tr> <td>Gemini-2.5-pro</td> <td>52.95</td> <td>56.75</td> </tr> <tr> <td>SQ-Whisper</td> <td>22.0</td> <td>-</td> </tr> <tr> <td>MC-TS-ASR</td> <td>-</td> <td>27.50</td> </tr> </tbody> </table> <p>真实数据上的优势更能说明问题，因为这里的语音是自发的、重叠是不规则的、还叠着远场混响。Qwen3-ASR 这类模型的错误率在真实集上反而比合成集更低一些（因为真实录音里有大量单人片段，缓解了它们的崩溃），但依然在 30% 以上，离可用还有距离。对比专用 TS-ASR 模型，AMI-SDM 上前一版最好的 SQ-Whisper 是 22.0%，CocktailASR-1 做到 21.81%——<strong>属于小幅超越</strong>；AliMeeting-Far 上前一版最好的 MC-TS-ASR 是 27.50%，CocktailASR-1 把错误率砍到 20.63%，这个提升就很实在了。这两个数字也说明它跨数据集泛化得比较均匀，不是只在某个数据分布上过拟合出来的结果。</p> <p>接下来是单说话人场景——这部分才是检验那个”设计取舍”的地方：</p> <table> <thead> <tr> <th>Model</th> <th>LibriSpeech<br/>FRR / WER</th> <th>AliMeeting-near<br/>FRR / WER</th> <th>AMI-ihm<br/>FRR / WER</th> <th>WenetSpeech(meeting)<br/>FRR / WER</th> <th>CommonVoice(zh)<br/>FRR / WER</th> </tr> </thead> <tbody> <tr> <td><strong>Xiaomi-CocktailASR-1</strong></td> <td>0.36 / 1.73</td> <td>0.38 / 6.57</td> <td>0.01 / 8.89</td> <td>0 / 5.81</td> <td>0.73 / 4.95</td> </tr> <tr> <td>Qwen3-ASR-1.7b</td> <td>0 / 1.87</td> <td>0 / 6.39</td> <td>0 / 10.56</td> <td>0 / 5.84</td> <td>0 / 5.39</td> </tr> <tr> <td>StepAudio2</td> <td>0 / 1.58</td> <td>0 / 6.82</td> <td>0 / 37.54</td> <td>0 / 5.46</td> <td>0 / 5.07</td> </tr> <tr> <td>Whisper Large-v2</td> <td>0 / 2.70</td> <td>-</td> <td>0 / 16.90</td> <td>-</td> <td>0 / 26.8</td> </tr> <tr> <td>Gemini-2.5-pro</td> <td>21.31 / 6.77</td> <td>14.95 / 16.10</td> <td>24.30 / 21.02</td> <td>0 / 27.58</td> <td>0.003 / 14.01</td> </tr> </tbody> </table> <p>结论很清楚：CocktailASR-1 在单说话人场景下的 WER 和专门做单人 ASR 的 Qwen3-ASR、StepAudio2 基本处于同一水平线（LibriSpeech 上 1.73% vs 1.87% / 1.58%），在 AMI-ihm 这种远场会议场景上还大幅领先（8.89% vs StepAudio2 的 37.54%），同时 FRR 控制在 0.36%~0.73% 这个量级。<strong>“给模型加拒识能力”这件事，没有以牺牲正常识别为代价。</strong></p> <p>作为对照，Gemini-2.5-pro 展示了另一种失衡：它在单说话人场景下的 FRR 高达 21.31%，说明它倾向于”宁可拒识也不乱转”，代价是把大量正常语音也拒掉了，WER 直接崩掉。这就是论文反复强调的”拒识能力和保真度要平衡”的现实意义。</p> <p>最后是负样本上的拒识表现：</p> <table> <thead> <tr> <th>Model</th> <th>LibriSpeech Neg</th> <th>Aishell Neg</th> <th>Chinese in-house Neg</th> </tr> </thead> <tbody> <tr> <td><strong>Xiaomi-CocktailASR-1</strong></td> <td>79.59</td> <td><strong>75.35</strong></td> <td><strong>68.54</strong></td> </tr> <tr> <td>Qwen3-ASR-1.7b</td> <td>0</td> <td>0</td> <td>0</td> </tr> <tr> <td>Gemini-2.5-pro</td> <td><strong>81.79</strong></td> <td>64.20</td> <td>54.7</td> </tr> <tr> <td>StepAudio2</td> <td>0</td> <td>0</td> <td>0</td> </tr> </tbody> </table> <p>没有专门设计过拒识机制的模型，拒识率清一色是 0——Qwen3-ASR 和 StepAudio2 遇到参考说话人缺席的情况，会一本正经地把不存在的人说的话”编”出来。Gemini 在英文负样本上做到 81.79%，比 CocktailASR-1 的 79.59% 还高一点，但结合上一张表看，它是靠”普遍倾向于拒识”换来的，误拒率高达 21.31%；CocktailASR-1 则是在 0.36% 误拒率的前提下做到 79.59% 的拒识率，中文场景（Aishell Neg 75.35%、自采中文数据 68.54%）也稳定领先 Gemini。<strong>两个指标要放在一起看才说明问题</strong>：单看拒识率，Gemini 在英文上更好；单看误拒率，所有基线都”完美”（因为它们压根不拒识）；只有同时看，才知道谁真的把这件事做成了。</p> <h2 id="8-cot-的真实价值不是把-wer-压下去是把选人过程变成看得见能复用的中间结果">8. CoT 的真实价值：不是把 WER 压下去，是把”选人过程”变成看得见、能复用的中间结果</h2> <p>前面把 CoT 放在四能力里讲了它在训练上怎么和其他能力共存，这里单独看看它到底值多少。先看数据——报告在合成多人集上对比了标准模式和 CoT 模式：</p> <table> <thead> <tr> <th>Test Set</th> <th>Standard mode</th> <th>CoT mode</th> </tr> </thead> <tbody> <tr> <td>LibriMix 2mix</td> <td>4.11</td> <td>3.87</td> </tr> <tr> <td>LibriMix 3mix</td> <td>12.287</td> <td>12.285</td> </tr> <tr> <td>LibriSpeechMix 2mix</td> <td>2.90</td> <td>2.88</td> </tr> <tr> <td>LibriSpeechMix 3mix</td> <td>4.91</td> <td>4.81</td> </tr> </tbody> </table> <p>坦白说，<strong>这个提升很小</strong>：LibriMix 2mix 上从 4.11% 降到 3.87%，是目前看到的比较明显的一处；LibriMix 3mix 上 12.287% 到 12.285%，等于没有变化；剩下两个测试集都是 0.1 个点以内的波动。如果只看这张表，CoT 的性价比确实不高——而且代价是真实的：CoT 模式下生成长度上限翻倍到 512 个 token，推理开销和延迟都要相应增加。</p> <p>那论文为什么还把 CoT 列为四大能力之一？报告自己给的定位是两件事：</p> <p>一是<strong>结构化推理过程会引导模型显式关注声纹这类关键特征</strong>，在复杂场景下减少一部分识别错误——这对应上表里那点有限的提升。</p> <p>二是推理过程本身产出的中间结果——说话人数量、每个人的性别、各自的声纹相似度水平——<strong>对需要更深层语音理解的下游任务是可以直接复用的辅助信息</strong>（会议摘要、说话人日志这类任务本来就要先算这些东西）。也就是说，用 CoT 模式跑一遍，等于顺手把 diarization 的部分产物也拿到了。</p> <p>所以对 CoT 这块，比较诚实的评价是：<strong>它的价值主要体现在把”为什么选中这个人”的判断过程变成一份可读、可复用的结构化输出</strong>，而不是换取更低的 WER。它的真实代价（延迟、token 消耗）和真实收益（可解释性、下游可用性）都需要一并放进决策里——好在训练时 CoT 和标准模式是联合训练的，用户可以在推理时按需开关，不必为不需要的场景付这份开销。</p> <h2 id="9-总结">9. 总结</h2> <p>CocktailASR-1 这份技术报告的思路，一句话概括：<strong>把”认人”这件事从独立模块里拿出来，交给 LLM 自己在一套统一表征里完成</strong>。参考语音不做压缩、不走声纹编码器，直接和目标语音拼在一起进同一个 D2V2 编码器，让 Qwen3-8B 用自己的 attention 去比对、去定位、去决定转录哪一段，以及要不要转录。</p> <p>这套设计之所以能成立，靠的不只是架构上的简化，而是两件事的配合：</p> <ul> <li><strong>架构上</strong>，D2V2 这种 SSL 编码器本身就融合了语义和声纹信息，所以不需要外挂声纹模块；参考语音保留帧级表征而不是压缩成定长向量，信息损失小；共享编码通路又让单说话人场景不会因为”多加了条件信号”而退化。</li> <li><strong>训练和数据上</strong>，四个互相拉扯的能力被拆成了四个阶段、三类数据：多人数据（40 万小时）教它挑人，单人数据（60 万小时）教它别乱删，负样本（1 万小时）教它该闭嘴时闭嘴，负样本比例在阶段间逐步提高以平衡拒识率和误拒率，CoT 数据则用 CAM++ 打分加量化模板把”选人”的判断过程变成可监督的文本。</li> </ul> <p>效果上，它在合成和真实的多说话人基准上拿到 SOTA（LibriSpeechMix 2mix 相对下降 46%，AliMeeting-Far 从 27.5% 降到 20.63%），同时单说话人场景和专用 ASR 模型基本持平、拒识率 79.59% 而误拒率只有 0.36%。要挑刺的话，CoT 对 WER 的贡献确实有限（LibriMix 3mix 上基本为零），40 万 / 60 万小时这个数据规模也不是普通团队能复刻的——它更像是小米把自家数据资产和工程能力一次性兑现出来的产物。</p> <p>不过好消息是权重和推理代码都放出来了：<code class="language-plaintext highlighter-rouge">Ease3/Xiaomi-CocktailASR-1</code> 上 <code class="language-plaintext highlighter-rouge">trust_remote_code=True</code> 直接能跑，仓库里 <code class="language-plaintext highlighter-rouge">tools/test_batch_scp.py</code> 也给了现成的批量测试脚手架。如果你手头有多说话人的会议或对话数据，想在真实分布上试一把”只转录某个人”，复现成本并不高。</p> <hr/> <blockquote> <p>如果这篇文章涉及的语音多模态架构设计、SSL 编码器与 LLM 的 adapter 对齐方式想系统深入，可以看看之前出版的<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，书里 LLM 架构自动化和参数高效微调那部分内容，和本文讲的 encoder-adapter-LLM 三段式架构、以及用 forward hook 做模态拼接的思路有一定呼应——角度不同，但底层都是”怎么把一个模态的表征对接到 LLM 的可消费空间里”这个问题。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="ASR"/><category term="语音识别"/><category term="说话人识别"/><category term="论文解读"/><summary type="html"><![CDATA[arXiv’26 | 小米 CocktailASR-1 给 ASR 装上“选择性听觉”：参考语音当声纹 prompt，一个模型只录你一个人]]></summary></entry><entry><title type="html">DeepSeek V4.1 Flash | 后训练没有新算法，Agent 能力从哪来？</title><link href="https://marsggbo.github.io/blog/2026/20260910-deepseek-v41-flash/" rel="alternate" type="text/html" title="DeepSeek V4.1 Flash | 后训练没有新算法，Agent 能力从哪来？"/><published>2026-09-10T00:00:00+00:00</published><updated>2026-09-10T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260910-deepseek-v41-flash</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260910-deepseek-v41-flash/"><![CDATA[<h1 id="deepseek-v41-flash--后训练没有新算法agent-能力从哪来">DeepSeek V4.1 Flash | 后训练没有新算法，Agent 能力从哪来？</h1> <blockquote> <p>“… our post-training introduces no algorithmic innovation. All substantive changes lie instead in the data pipeline.”</p> <p>—— DeepSeek-V4.1-Flash 技术报告 《DeepSeek-V4.1-Flash: Pushing the Limits of KV Cache Compression》<br/> https://huggingface.co/deepseek-ai/DeepSeek-V4.1-Flash/blob/main/DeepSeek_V41_Tech_Report.pdf</p> </blockquote> <p>这句话放在一份以 KV Cache Compression 为标题的报告里，多少有点反直觉。报告前半花了大量篇幅重写模型处理长上下文的方式：哪些历史信息要算、要存、要在缓存失效后重建；到了后训练，作者却刻意不再改优化算法，把注意力放到数据、任务和环境是如何被造出来的。</p> <p>模型一旦开始调用工具、处理代码仓库和网页、跑很多轮任务，先得付得起上下文不断增长的成本。模型会把已经读过的 token 留成一份可复用的注意力记忆，这份记忆通常叫 KV Cache：</p> <ol> <li>Agent 的上下文不断增长，未命中缓存时，重新读入整段历史的计算很贵；</li> <li>命中缓存后，缓存本身仍要占 GPU 的高带宽显存（HBM）、主存或 SSD，还要在机器之间传输；</li> <li>稀疏注意力已经显著降低了“看全上下文”的计算量，于是 KV 的存储和搬运反而越来越突出；</li> <li>因此，V4.1 Flash 同时改了 prefill 的计算路径、全局 KV 的跨层复用，以及持久化缓存的恢复方式。</li> </ol> <p>报告给出的模型规模是 552B backbone parameters，支持 1M context；但更应该关注的是每 token 激活参数：prefill 为 8B，decode 为 16B。这不是两套模型，也不是简单少用一半专家，而是“读入整段历史”和“生成一个新 token”走了不同深度的计算路径。这里的 “Flash” 并不意味着总参数最少，而是它试图把长上下文 Agent 的主要系统成本压下来。</p> <p>成本能压下来，只是让更长的 rollout 变得可行。模型在 rollout 里究竟看见什么任务、进入什么环境、怎样知道一次尝试是真的完成而不是碰巧骗过评分器，决定了这些算力最后会变成什么能力。报告后半的重点正落在这里。</p> <h2 id="1-先把问题说清楚长程-agent-到底贵在哪里">1. 先把问题说清楚：长程 Agent 到底贵在哪里</h2> <h3 id="11-一轮-agent-任务里其实有三种不同的成本">1.1 一轮 Agent 任务里，其实有三种不同的成本</h3> <p>对普通聊天来说，几十轮以内的上下文通常还不至于让 KV Cache 成为第一问题。但对代码 Agent、浏览器 Agent 或企业流程 Agent 来说，模型会反复经历“读历史—调用工具—把工具结果塞回上下文—继续读”的循环。</p> <p>这时可以把成本拆成三部分：</p> <ul> <li><strong>Prefill</strong>：模型第一次读入一段新上下文，并为它计算 KV Cache。工具返回很长、缓存失效、或者切换机器后要恢复任务时，这部分会重新出现。</li> <li><strong>Decode</strong>：模型基于已有上下文逐 token 生成答案、命令或下一步工具调用。</li> <li><strong>KV Cache 的存储与迁移</strong>：长任务不能只把缓存放在单张 GPU 的 HBM 里；缓存需要持久化到主机内存或 SSD，也可能跨节点搬运。此时容量和带宽都会变成吞吐限制。</li> </ul> <p>这里的 KV Cache 可以粗略理解为：模型读过历史 token 后留下的一份“可直接复用的注意力记忆”。下次生成时不必从头重新计算每个历史 token 的 Key 和 Value，但代价是这份记忆会随上下文长度增长。</p> <p><img src="/assets/img/posts/20260910-deepseek-v41-flash/figure1_p1.png" alt="图 1：Agent 基准与每 token 全局 KV Cache 对比"/></p> <p><em>图 1。先看右图：报告把 V4.1 Flash 的全局 KV Cache 标成 890 bytes/token，约为 V4-Flash 的四分之一。左图则放了 Agent 基准表现。两张图并列的意思不是“缓存更小必然分数更高”，而是作者要说明：压缩缓存之后，模型并没有只换来成本下降，也维持了较强的 Agent 能力。</em></p> <p>一个容易被忽略的点是：<strong>运行时 KV</strong> 和 <strong>持久化 KV</strong> 不是同一回事。</p> <p>运行时 KV 常驻在 HBM 中，直接影响单机能同时服务多少长上下文请求；持久化 KV 则被放到主存或 SSD，供后续回合或迁移恢复使用。前者主要受显存容量限制，后者还会受到 I/O 和网络带宽影响。报告后面“约四分之一”和“约八分之一”这两个数字，分别对应这两层问题，不能混在一起看。</p> <h3 id="12-稀疏注意力解决了一半剩下的一半变得更显眼">1.2 稀疏注意力解决了一半，剩下的一半变得更显眼</h3> <p>DeepSeek V4 的路线是把注意力分成两支：局部的 Sliding-Window Attention（SWA）只看最近窗口；全局分支用稀疏方式，从很长的历史里挑少量位置读取。</p> <p>SWA 的缓存上限由窗口大小决定，窗口固定时不会随上下文无限增长。真正会随序列增长的，主要是全局分支的 KV。因此，当稀疏注意力已经让全局检索的算力下降后，长期保存这部分 KV 的空间和搬运开销就会更刺眼。</p> <p><img src="/assets/img/posts/20260910-deepseek-v41-flash/figure2_p5.png" alt="图 2：不同 DeepSeek 模型的单 token decode FLOPs 随上下文长度变化"/></p> <p><em>图 2。横轴是 context length，纵轴是生成一个 token 的加权 FLOPs。要看的不是某个点绝对值，而是曲线斜率：V4.1 Flash 从 4K 扩到 1M context 时，decode FLOPs 只小幅增长；V4-Flash 的曲线则更明显上扬。报告将不同精度的操作按 BF16=1、FP8=0.5、FP4=0.25 加权，因此这不是通用硬件上的真实延迟图，而是计算量口径下的架构比较。</em></p> <p>到这里，问题才完整：既要少算新上下文，也要少存、少搬旧上下文。V4.1 Flash 的三个核心设计正好分别对应这三个动作。</p> <h2 id="2-第一步把读完整段历史和生成下一个-token拆开算">2. 第一步：把“读完整段历史”和“生成下一个 token”拆开算</h2> <h3 id="21-8b16b-不是规格差异而是两条计算路径">2.1 8B/16B 不是规格差异，而是两条计算路径</h3> <p>报告把这套设计命名为 Causal Encoder-Decoder（CED）。它将 40 层 Transformer 分成 20 层 causal encoder 和 20 层 decoder：前者负责把已经出现的历史 token 编成一份长期记忆，后者负责在生成时读取这份记忆并完成更深的变换。这里的 causal 仍表示只能看当前位置之前的 token，并不是把输入和输出完全分开的传统机器翻译式 encoder-decoder。</p> <p>先说清 “activated parameters” 的口径。V4.1 使用的是混合专家模型（Mixture-of-Experts，MoE）：总参数被分在很多专家网络中，每个 token 只会路由到其中少量专家。这里的 8B 或 16B，是一个 token 在对应阶段真正经过的参数规模。V4.1 Flash 的差异来自层数，而不是 prefill 阶段换成了另一种专家选择策略：</p> <table> <thead> <tr> <th>阶段</th> <th>全部 $N$ 个 prompt token 都要经过的路径</th> <th>decoder 额外做什么</th> <th style="text-align: right">报告给出的每 token 激活参数</th> </tr> </thead> <tbody> <tr> <td>Prefill</td> <td>前 20 层 causal encoder</td> <td>后 20 层的<strong>全局 KV</strong>由 encoder 输出直接投影；只有最后 $n_{\text{win}}$ 个 token 需要进入 decoder 重建局部 SWA 状态</td> <td style="text-align: right">8B</td> </tr> <tr> <td>Decode</td> <td>新生成的 token 依次经过 20 层 encoder 和 20 层 decoder</td> <td>使用已建好的全局 KV 与局部 SWA KV，形成最终 logits</td> <td style="text-align: right">16B</td> </tr> </tbody> </table> <p>因此，长 prompt 的主路径相当于只跑半个模型：20 层 encoder 对全部历史 token 做完整计算，得到中间表示；decoder 不必把每个历史 token 再完整过 20 层，只需从这个中间表示“写出”自己要用的全局记忆。设模型一共 $L$ 层，输入长度为 $N$，对于后 $L/2$ 层，全局 KV 不再从该层自己的隐藏状态生成，而是直接从第 $L/2$ 层的输出投影出来：</p> \[C_l = H_{L/2}W^{KV}_l,\qquad Z_l = H_{L/2}W^{Z}_l,\qquad l&gt;\frac{L}{2}.\] <p>这里 $H_{L/2}$ 是 encoder 最后一层对整段输入的表示；$C_l$ 是第 $l$ 个 decoder 层使用的压缩全局 KV 表示，$Z_l$ 是对应的压缩权重；$W_l^{KV}$ 与 $W_l^Z$ 则是该层各自的投影参数。关键在于：所有 decoder 层共享同一个来源 $H_{L/2}$，但每一层仍有自己的投影矩阵，因此它们拿到的并不是一份完全相同的 KV。</p> <p>这件事之所以有机会成立，是因为 decoder 的全局注意力需要的是一份可按位置读取的长期记忆，并不一定要求每个历史 token 都先经过该 decoder 层，才能写出这份记忆。前 20 层已经把整段历史编码成 $H_{L/2}$；上半层再用自己的投影把它转换成各自适合检索的 KV。真正更依赖层级演化的局部模式没有被一刀切丢掉：SWA 仍按层保留，只是对历史前缀采用有限回放来恢复。换来的代价是，decoder 的全局记忆不再等价于“完整 40 层前向后该层本应得到的隐藏状态”，所以 CED 必须在训练和评测中验证这种替代是否可靠。</p> <p><img src="/assets/img/posts/20260910-deepseek-v41-flash/figure3_p7.png" alt="图 3：V4.1 Flash 的整体架构"/></p> <p><em>图 3。阅读顺序可以从下往上看。40 个 Transformer block 被分为 20 层 causal encoder 和 20 层 decoder；encoder 产生的表示向上投影为 decoder 的全局 KV。图中 decoder 仍保留局部 SWA 路径，因此“只算半个模型”只描述长 prompt 的主 prefill 路径：后半层没有为全部历史 token 重新做完整计算，但会为最后一个局部窗口补算 SWA 状态；真正 decode 新 token 时，40 层仍都要执行。</em></p> <p>如果只看长 prompt 中“平均每个输入 token”经过的激活参数，直觉上可以写成：</p> \[P_{\text{prefill}}(N) \approx 8\text{B}+8\text{B}\cdot\frac{n_{\text{win}}}{N}, \qquad P_{\text{decode}}\approx 16\text{B}.\] <p>第一项是所有 token 都要跑的 encoder；第二项是最后 $n_{\text{win}}$ 个 token 为了局部注意力额外过 decoder 的摊销成本。它不是报告的精确计费公式，而是解释为什么表里的主规格会写成 prefill 8B、decode 16B：当 $N\gg n_{\text{win}}$ 时，第二项几乎可以忽略；短 prompt 或频繁短回合时，这个额外项就不能忽略。</p> <p>这也回答了“为什么 decode 不能也只用 8B”：缓存里的全局 KV 只解决了历史信息从哪里读，新 token 仍要经过 decoder 的层级变换、自己的局部注意力和 FFN，才能形成最终 logits。CED 省掉的是<strong>为历史 token 重复生成 decoder 全局 KV 的计算</strong>，并没有把 decoder 从生成路径里删除。</p> <p>对于 $N\gg n_{\text{win}}$ 的长输入，报告给出的 prefill 复杂度近似从</p> \[O(NL)\] <p>变成</p> \[O\left(N\frac{L}{2}+n_{\text{win}}\frac{L}{2}\right) \approx O\left(N\frac{L}{2}\right).\] <p>其中 $n_{\text{win}}$ 是 SWA 的局部窗口大小。这个近似成立的前提很明确：输入足够长，窗口项相对于 $N$ 可以忽略。因此 CED 特别适合“工具结果很长、每轮新输入很多”的 Agent，而不是声称对任何短 prompt 都会获得同样接近一半的收益。</p> <h3 id="22-局部注意力仍需要状态有界回放是在省存储不是在凭空消失">2.2 局部注意力仍需要状态：有界回放是在省存储，不是在凭空消失</h3> <p>CED 没有把所有计算都砍掉。为了保持局部建模能力，SWA 的 KV 仍然是逐层形成的。问题在于：如果要精确恢复 decoder 各层的 SWA 状态，理论上需要重放最近 $L\times n_{\text{win}}$ 个 token。</p> <p>DeepSeek V4.1 Flash 采用了 <strong>SWA Bounded Replay</strong>：恢复时只重放最近 $n_{\text{win}}$ 个 token，而不是每层都沿着窗口继续展开。它用一小段重新 prefill 来近似重建需要的局部状态，于是不必把所有 SWA KV 持久化到 SSD。</p> <p>这也是报告把持久化 KV 降到 V4-Flash 约八分之一的关键原因：一部分来自全局 KV 更小，另一部分来自不再长期保存 SWA KV。代价也不能略过——这是近似恢复。报告只在已评估设置中观察到很小的性能影响；在极端长程依赖、缓存断点附近或未覆盖的输入分布上，状态误差仍可能累积，这也是报告在局限性中明确提到的边界。</p> <h2 id="3-第二步别让每一层都重复建缓存重复找历史">3. 第二步：别让每一层都重复建缓存、重复找历史</h2> <h3 id="31-先理解全局稀疏注意力里重复了什么">3.1 先理解全局稀疏注意力里重复了什么</h3> <p>长上下文的全局注意力通常不会对每个 query 和全部历史 token 做完整计算，而是先用一个轻量检索器从历史中选出 Top-K 个候选位置，再让主注意力读取这些候选 KV。</p> <p>如果每一层都独立完成这件事，至少会有三类重复：</p> <ul> <li>每层都保存一份全局 main KV；</li> <li>每层都保存或计算用于检索的 indexer K；</li> <li>每层都对长历史重新打分，得到自己的 Top-K 索引。</li> </ul> <p>报告把压缩视为三个相乘的维度：减少单个 KV entry 的字节数、减少序列维度上的 entry 数、以及减少层维度上的重复。前两种做法已经比较常见；它把第三种跨层复用的设计称为 Compressed Sparse Attention 2（CSA2）。</p> <h3 id="32-fullreindexreuse把建库检索读取拆开复用">3.2 Full、Reindex、Reuse：把“建库、检索、读取”拆开复用</h3> <p>CSA2 为每个注意力层静态指定三种模式。它们都保留当前层自己的 main Q 和局部 SWA KV；区别只在全局 KV、检索 key 与 Top-K 索引从哪里来。</p> <p><img src="/assets/img/posts/20260910-deepseek-v41-flash/figure4_p10.png" alt="图 4：CSA2 的 Full、Reindex、Reuse 三种模式"/></p> <p><em>图 4。按从上到下读即可。绿色是当前层新计算的量，黄色是从最近 Full 层复用的 main KV 与 indexer K，红色是从最近一次 Full 或 Reindex 复用的 Top-K 索引。关键是三种模式不是“有无注意力”的区别，而是对建库、重新排序、直接读取这三项工作做不同粒度的复用。</em></p> <ul> <li><strong>Full Mode</strong>：当前层完整生成 main KV，投影 indexer K，用自己的 indexer Q 扫描并得到新的 Top-K。它相当于建立一份新的全局记忆基准。</li> <li><strong>Reindex Mode</strong>：不再生成新的 main KV 和 indexer K，而是复用最近 Full 层的那一份；但它用自己的 indexer Q 重新打分，得到自己的 Top-K。这样“库”可以共用，层的检索偏好仍可不同。</li> <li><strong>Reuse Mode</strong>：连 Top-K 也直接沿用最近一次产生索引的层；它只计算自己的 main Q、局部 SWA KV，并对同一批全局位置做注意力。这样最省索引计算，但也最依赖此前的选择没有漏掉关键信息。</li> </ul> <p>这三种模式把一个很大的取舍拆得更细：完全逐层独立，效果上最自由但最贵；全层共享同一套 Top-K，最便宜但可能太死；CSA2 则在中间插入 Reindex，让一部分层能换检索结果，而不需要重新建全局 KV。</p> <p>报告同时把全局 KV 用 FP4 存储。精度压缩和跨层复用是叠加的：前者让一份缓存更小，后者让需要存的“份数”更少。最终，报告给出的运行时全局 KV 体积是 890 bytes/token，约为 V4-Flash 的四分之一。</p> <h3 id="33-分层索引只有第一层全量扫后续层在候选池里找">3.3 分层索引：只有第一层全量扫，后续层在候选池里找</h3> <p>即使已经有 Reindex，重新检索的层如果仍要对 1M 个位置逐个打分，成本还是会随 context 增长。于是报告又加了 Hierarchical Sparse Indexer。</p> <p><img src="/assets/img/posts/20260910-deepseek-v41-flash/figure5_p11.png" alt="图 5：Hierarchical Sparse Indexer 的候选池构造"/></p> <p><em>图 5。最上面的 Full Mode 先看完整历史：绿色方块是它选出的 Top-512，蓝色框表示这些高分位置所在的 block。中间和下面的 Reindex Mode 不再遍历全部位置，而只在蓝框覆盖的共享候选池中选自己的 Top-512。要注意：后续层共享的是“搜索范围”，不是强制共享最终 Top-K。</em></p> <p>具体过程是：</p> <ol> <li>decoder 中第一个 Full Mode 层对所有可见的全局 KV 位置打分；</li> <li>它一边选出自己要读的 Top-K，一边按 block 聚合分数：一个 block 的分数取内部位置的最大值；</li> <li>选出高分 block，拼成比最终 Top-K 更大的候选池。报告举例：选 2,048 个 block、每个 block 8 个位置，会得到 16,384 个候选位置；</li> <li>后续 Reindex 层只在这 16,384 个位置里用自己的 query 重新选 Top-K；Reuse 层则继续复用已有索引。</li> </ol> <p>这一步为什么能让 decode 对上下文长度不敏感？因为第一层仍要全局扫描，但后续 indexer 的评分数量被候选池上限固定住了。随着 context 从 4K 变成 1M，后续层不会再按 256 倍的历史长度重复扩大检索工作。</p> <p>代价也同样明确：如果第一层建候选池时漏掉了某段真正重要的历史，后续 Reindex 层就没有机会把它找回来。报告称训练和推理都使用同样的候选限制，目的是让后续层适应这个搜索空间；但这并不能替代对超长稀疏检索失败案例的压力测试。</p> <h2 id="4-第三步缓存小了之后还要让模型与系统真的跑得动">4. 第三步：缓存小了之后，还要让模型与系统真的跑得动</h2> <h3 id="41-把残差混合改成一次读取single-pass-mhc">4.1 把残差混合改成一次读取：Single-Pass mHC</h3> <p>CED 主要削减 prefill，CSA2 主要削减全局 KV 与索引；但 decode 阶段还有另一类常被 FLOPs 掩盖的成本：<strong>activation memory traffic</strong>，也就是中间激活在 HBM 与计算单元之间被反复读写的流量。batch 很小、逐 token 生成时，算术单元未必是第一瓶颈，等内存数据到位反而可能更慢。</p> <p>mHC 是 V4 已有的 residual-stream mixing 设计。普通 Transformer 可以粗略看成维护一条 residual stream；mHC 同时维护 $n$ 条，并为每个 token 预测三组混合系数 $A_l,B_l,C_l$，决定本层输入从哪些 stream 混合而来、旧 stream 如何更新。原始形式可以写成：</p> \[X_{l+1}=B_lX_l+C_lF_l(A_lX_l),\qquad (A_l,B_l,C_l)=H(X_l).\] <p>$X_l$ 是第 $l$ 层的 $n$ 条 residual stream，$F_l$ 是这一层的 Transformer block，$H$ 则根据当前 $X_l$ 预测混合系数。问题不是这个式子本身，而是执行顺序被卡死：先更新 $X_l$，才能算 $H(X_l)$；算完 $H(X_l)$，才能用新得到的 $A_l$ 做输入混合。旧实现因此至少要按“残差更新 → 系数预测 → 输入混合”分多次读同一份激活，kernel 很难合并。</p> <p>Single-Pass mHC 把一个很小但关键的时间依赖向后挪一层：第 $l$ 层不再等 $A_l$，而使用前一层已经算好的 $A_{l-1}$：</p> \[X_{l+1}=B_lX_l+C_lF_l(A_{l-1}X_l).\] <p>这样读到 $X_l$ 的同一时刻，就能同时完成残差更新、输入混合，并累积下一层 $A_l,B_l,C_l$ 的预测所需统计量。报告的 Mega-mHC kernel 正是利用这个顺序把三个阶段融合为一次 traversal；按作者的读写量计算，activation traffic 从原实现的 $(4n+4)d$ 降到 $(2n+2)d$，约减半。这里并不是“少了一次 Transformer 计算”，而是减少了中间向量在显存里来回搬运。</p> <p>这个设计的取舍也很具体：当前 block 用到的混合系数比原来滞后一层。报告称实测性能损失可以忽略，但训练阶段仍沿用多 kernel 的实现；真正把 Single-Pass 用到极致的是部署阶段。这说明它更像为推理 kernel 解依赖的系统改写，而不是为了训练提出的新表达能力。</p> <h3 id="42-两条旁路条件记忆-engram-与草稿生成-dspark">4.2 两条旁路：条件记忆 Engram 与草稿生成 DSpark</h3> <p><strong>Engram 不是 KV Cache</strong>。KV Cache 保存的是当前这条上下文已经算出的注意力状态；Engram 是额外的条件记忆模块，试图把“见过的局部模式”从每个 token 都要经过的 MoE 主干中分出来。报告的实现会先压缩 token，再对 $2$、$3$、$4$-gram 通过多头哈希寻址，从 embedding table 取回若干候选记忆；随后由上下文相关的 gate 决定拿多少、怎样同主干表示融合。</p> <p>为什么这会影响推理系统？报告将 196B Engram 参数分在两个模块中，放在第 1 和第 14 层；但这些表不是每个 token 都全量读取。哈希地址是确定的，因此系统可以在 host memory 中预取对应 embedding，并用后台 RDMA 与前面 Transformer block 的计算重叠。换句话说，它把一部分“记住 n-gram 局部规律”的工作变成稀疏查表和预取，目标是让主干参数更多服务于组合、推理和上下文建模。</p> <p>这条路径也有边界：哈希寻址与外部表带来碰撞、预取命中和主机内存带宽等新依赖，报告没有给出单独的 Engram 消融来量化这些条件分别贡献多少。因此更稳妥的理解是，Engram 为参数规模和激活规模之间增加了一层可稀疏访问的记忆，而不是证明“外部记忆一定比主干权重更有效”。</p> <p><strong>DSpark</strong> 则解决输出端的自回归串行性。常规 speculative decoding 的思路是：小 draft model 先猜多个 token，大模型一次验证，猜对的部分就跳过逐 token 生成。V4.1 的 drafter 只有 3 个 Transformer block，局部 attention window 为 128；一次前向会并行给出未来 5 个 draft position 的基础 logits，再用轻量 Markov head 补充这些 draft token 之间的依赖。</p> <p>真正有意思的是它没有固定“每次验证 5 个”。DSpark 还训练了 confidence head，预测每个候选位置在此前 token 被接受的条件下、自己会被接受的概率；把这些概率连乘可以得到不同 draft 前缀的存活概率。scheduler 再结合离线测得的引擎吞吐曲线，为每个请求动态选验证长度，目标是当前负载下的<strong>系统总 token throughput</strong>，而不只是让单个请求尽量激进地多猜。</p> <p>训练顺序也说明了它是一个服务模块：backbone pre-training 结束后，先冻结 backbone 单独训练 DSpark；post-training 时两者一起更新，但 DSpark 的目标不向 backbone 反传梯度。这样 DSpark 能跟上 RL 后不断变化的策略，同时不让“更容易被验证”这件事改变主模型的能力目标。draft 猜错仍会带来验证浪费，因此收益依赖接受率、验证 kernel 和在线负载；这正是 scheduler 需要存在的原因。</p> <h3 id="43-再把缓存缩小fp4-与三段式部署">4.3 再把缓存缩小：FP4 与三段式部署</h3> <p>前面说 CSA2 减少了要保存多少份全局 KV，FP4 Main KV Cache 则继续减少每一份占多少字节。FP4 可以先把它理解为四比特浮点存储：它不是为了让注意力矩阵乘更快，而是把写进缓存的 main KV 压得更小。报告把量化感知训练从 indexer 的 Q/K 扩展到 main KV：main KV 用 OCP MXFP4 格式的 E2M1 表示，每 16 个 channel 共用一个 E4M3 scale；真正做 attention 前再反量化。这样不要求硬件原生支持该 FP4 格式的矩阵乘，主要收益是存储布局更小。</p> <p>这里有个不能混淆的细节：<strong>只有 main KV 被压到 FP4，SWA KV 仍保留 FP8</strong>。作者认为局部窗口状态对量化更敏感；相对 V4 的 FP8 main KV，FP4 大约再减半 main KV 的 HBM 与 SSD 占用。报告还给出数值范围的理由：训练中观察到的 main KV 幅值约为 10，而该格式在所用 scale 下的范围仍有余量。不过这依然是 QAT 和该模型分布下的结论，不能外推成“所有 KV 都可以直接用四比特”。</p> <p>最后是部署切分。报告采用 Encoder–Prefill–Decode（EPD）disaggregation：视觉编码、读入 prompt、逐 token 生成可以由独立资源池扩缩容并重叠执行。持久化缓存只长期保留全局 KV，报告称至少保留 72 小时；SWA KV 转到每机约 10% host DRAM 的短 TTL 分布式池中，仅服务活跃会话。短期状态丢了就触发 Bounded Replay，而不是从 SSD 恢复完整 SWA。到这一层，CED、CSA2、FP4 和 replay 才真正接上同一条链：少算、少存、少搬，并把不可避免的 miss 降成一次有限范围的重算。</p> <p>报告提到 CSA2 的 Reuse Mode 层在 prefill 使用 15 个 kernel、decode 使用 11 个 kernel；这是部署实现层面的指标，不该直接等同于任何硬件、任何 batch size 下的端到端延迟。</p> <h3 id="44-flash不等于所有阶段都比前代少算">4.4 “Flash”不等于所有阶段都比前代少算</h3> <p>报告的 base model 总表很长，直接贴出来会让读者在一张小字截图里找重点。下面只摘出和本文判断直接相关的参数与文本/代码/长上下文项目；分数来自报告同一套内部评测，表头说明分差不超过 0.3 时视为同一水平。</p> <table> <thead> <tr> <th>Base model</th> <th style="text-align: right">Backbone 参数</th> <th style="text-align: right">每 token 激活参数</th> <th style="text-align: right">MMLU-Pro</th> <th style="text-align: right">BigCodeBench</th> <th style="text-align: right">HumanEval</th> <th style="text-align: right">LongBench-V2</th> </tr> </thead> <tbody> <tr> <td>V4-Flash</td> <td style="text-align: right">284B</td> <td style="text-align: right">13B</td> <td style="text-align: right">68.3</td> <td style="text-align: right">56.8</td> <td style="text-align: right">69.5</td> <td style="text-align: right">44.7</td> </tr> <tr> <td>V4-Pro</td> <td style="text-align: right">1.6T</td> <td style="text-align: right">49B</td> <td style="text-align: right">73.5</td> <td style="text-align: right">59.2</td> <td style="text-align: right">76.8</td> <td style="text-align: right">51.5</td> </tr> <tr> <td>V4.1 Flash</td> <td style="text-align: right">552B</td> <td style="text-align: right">prefill 8B / decode 16B</td> <td style="text-align: right">74.1</td> <td style="text-align: right">60.6</td> <td style="text-align: right">79.4</td> <td style="text-align: right">45.2</td> </tr> </tbody> </table> <p>V4.1 Flash 的 backbone 有 552B 参数，明显大于 V4-Flash Base 的 284B；decode 激活参数是 16B，也高于 V4-Flash 的 13B。它所换来的，是 prefill 时只激活 8B，以及更小的 KV footprint。表中最能说明取舍的是 LongBench-V2：V4.1 的 45.2 高于 V4-Flash 的 44.7，却仍低于 V4-Pro 的 51.5。结论不应被简化成“参数更少、能力更强”，而应是它针对长程服务重新分配了参数、计算与缓存预算。</p> <p>报告称，该模型在 45T 多模态 tokens 上预训练，并从 64K sequence length 起就训练稀疏注意力，没有先用 dense attention warmup。作者报告 V4.1 Flash Base 在其 held-out evaluations 上相对 V4-Pro Base 有 5%–10% 改善，同时使用约三分之一总参数和四分之一激活参数。由于这些 held-out 集的构成没有完全公开，这个结论更适合被理解为作者的训练内证据，而不是跨模型的独立排名。</p> <h2 id="5-训练数据不再只是-prompt">5. 训练数据不再只是 prompt</h2> <h3 id="51-一道-agent-任务要先变成一个能运行能判分的世界">5.1 一道 Agent 任务，要先变成一个能运行、能判分的世界</h3> <p>报告所说的“没有新算法”只针对后训练。前面介绍的缓存和计算路径仍是实打实的架构与系统改动；但进入后训练后，整体配方沿用了已有做法：先用示范数据做监督微调（SFT），再用任务奖励做强化学习（RL），最后进行 on-policy distillation（OPD）。变化发生在模型面对的材料上。</p> <p>拿代码 Agent 来说，一条“修复这个 bug”的指令远远不够。模型需要看到一个能启动的仓库、正确版本的依赖、任务开始时的工作区状态，以及不会把答案泄漏到上下文里的测试。做完之后，还需要一个验证器判断它究竟修好了问题，还是只改了恰好能让表面测试通过的地方。报告把一个训练任务写成 problem、environment 和 verification system 的组合，三者缺一个，RL 都很容易学偏。</p> <p>因此，通用 Agent 的训练材料会从真实工作流的工具接口、失败反馈和多轮交互中重建 mock environment；代码 Agent 则从复杂会话和公开仓库中筛出任务，由不同 agent 负责容器搭建、求解尝试、质量检查和修复。任务被使用后留下的轨迹并不会立刻作废：新的失败方式会回到质检环节，暴露出难度不够、事实不对、验证点不严或可被钻空子的地方，再进入下一轮构造。</p> <p>在这种定义下，数据管线包含的不是更多 prompt，而是更多可验证的任务、更多不同的交互环境，以及一套能持续过滤、去重和调节难度的机制。报告在第 25 页的判断也因此比“数据很重要”更强：在一个固定且并不特殊的优化流程下，合成数据和环境的<strong>规模、多样性与可验证性</strong>几乎解释了观察到的全部能力增益。这当然仍是作者对自身训练系统的归因，不是普适的因果定律；但它把 Agent 后训练的重心，从设计一个更花哨的 reward，推到了如何长期稳定地生产好任务上。</p> <p><img src="/assets/img/posts/20260910-deepseek-v41-flash/figure7_p27.png" alt="图 7：随着 RL steps 增长，代码 Agent 基准继续提升"/></p> <p><em>图 7。四个子图分别是 DeepSWE v1.1、SWE-Bench Pro、Terminal-Bench v2.1 和 v3.0。实线看 Pass@1，虚线看输出 token 数。图想说明两件事：更多 RL steps 仍带来提升；在最右侧 Terminal-Bench v3.0 上，最大 context 从 512K 增至 1M 也有额外收益。虚线同时提醒我们，这些提升伴随着更长轨迹，不能只看准确率。</em></p> <h3 id="52-换一套-agent-外壳能力还是否稳定">5.2 换一套 Agent 外壳，能力还是否稳定</h3> <p>一个 Agent 的外壳通常被称为 scaffold：它规定系统提示词、工具 schema、上下文压缩方式和轮次控制。能力很容易被某个固定 scaffold 放大或限制；如果训练和评测只在一种框架里完成，模型可能学到的是某套交互惯例，而不是真正稳定的工具使用能力。</p> <p><img src="/assets/img/posts/20260910-deepseek-v41-flash/figure8_p28.png" alt="图 8：多版本、多 scaffold 联合 RL 的扩展曲线"/></p> <p><em>图 8。左图把多个 Claude Code 版本一起训练，右图混合 OpenCode、Pi 和不同 DeepSeek Harness 设置。深色线是联合训练后的总体表现，浅色线是单一 scaffold 或版本。作者据此主张：把交互协议做得更多样，RL 的收益仍可继续累积。这个图并不能单独证明因果关系，但至少说明报告没有只用单一 harness 来展示训练曲线。</em></p> <p>这种训练方式也让“性能属于模型还是属于 harness”变成一个应当正面回答的问题。报告后面给了跨 scaffold 的评测，算是对这点做了必要补充。</p> <h2 id="6-如何读结果能力有提升但不要把所有-benchmark-混成一个结论">6. 如何读结果：能力有提升，但不要把所有 benchmark 混成一个结论</h2> <h3 id="61-相对-v4-flash-的提升最明确对顶级闭源模型仍是分任务看">6.1 相对 V4-Flash 的提升最明确；对顶级闭源模型仍是分任务看</h3> <p>报告的主结果表有 20 多项 benchmark。下面仍只摘出足以看出代际变化和能力边界的项目；其中 Pass@1 表示一次采样就完成任务的比例，DeepSWE 的 Resolved 表示被判定解决的问题比例，越高越好。所有数值仍是报告自己的 max-effort 设置，跨公司、跨 harness 的横向比较只能作为参考。</p> <table> <thead> <tr> <th>任务</th> <th style="text-align: right">V4-Flash</th> <th style="text-align: right">V4.1 Flash</th> <th style="text-align: right">V4-Pro</th> <th style="text-align: right">GPT-5.6 Sol</th> <th style="text-align: right">Opus-5</th> </tr> </thead> <tbody> <tr> <td>Codeforces（rating）</td> <td style="text-align: right">3289</td> <td style="text-align: right"><strong>3471</strong></td> <td style="text-align: right">3348</td> <td style="text-align: right">—</td> <td style="text-align: right">—</td> </tr> <tr> <td>Terminal-Bench 2.1（Pass@1）</td> <td style="text-align: right">82.7</td> <td style="text-align: right"><strong>90.6</strong></td> <td style="text-align: right">87.9</td> <td style="text-align: right">88.8</td> <td style="text-align: right">89.1</td> </tr> <tr> <td>DeepSWE v1.1（Resolved）</td> <td style="text-align: right">54.4</td> <td style="text-align: right"><strong>74.2</strong></td> <td style="text-align: right">62.7</td> <td style="text-align: right">73.0</td> <td style="text-align: right">74.0</td> </tr> <tr> <td>Terminal-Bench 3.0（Pass@1）</td> <td style="text-align: right">7.6</td> <td style="text-align: right">30.0</td> <td style="text-align: right">11.8</td> <td style="text-align: right">34.4</td> <td style="text-align: right"><strong>43.3</strong></td> </tr> <tr> <td>Terminal-Bench 4.0（Pass@1）</td> <td style="text-align: right">7.0</td> <td style="text-align: right">31.2</td> <td style="text-align: right">12.4</td> <td style="text-align: right">39.9</td> <td style="text-align: right"><strong>51.8</strong></td> </tr> <tr> <td>Automation-Bench（Pass@1）</td> <td style="text-align: right">37.7</td> <td style="text-align: right"><strong>54.8</strong></td> <td style="text-align: right">43.2</td> <td style="text-align: right">45.8</td> <td style="text-align: right">50.3</td> </tr> </tbody> </table> <p>先纵向看 V4-Flash 与 V4.1 Flash 两列：DeepSWE v1.1 从 54.4 到 74.2，Terminal-Bench 2.1 从 82.7 到 90.6，Codeforces rating 从 3289 到 3471，这是最直接的代际改进。再横向看其他模型：V4.1 在 Terminal-Bench 2.1、DeepSWE、Automation-Bench 等任务很强，但 Terminal-Bench 3.0/4.0 仍落后于表内顶级闭源模型。不同 benchmark 的 harness、预算和评价设置并不完全相同，因此不宜把其中某一个最高分外推成笼统的“全面超越”。</p> <p>几个具体结论可以相对稳妥地读：</p> <ul> <li>对前代 V4-Flash，报告中的 Agent 任务增幅很大，尤其 DeepSWE v1.1 为 74.2 对 54.4，Terminal-Bench 2.1 为 90.6 对 82.7。</li> <li>对 DeepSeek V4-Pro，V4.1 在 Codeforces（3471 对 3348）和部分 Agent 基准上更高，但并不在每个项目都占优。</li> <li>对表内闭源模型，V4.1 在 Terminal-Bench 2.1、DeepSWE、AutomationBench 等单项上有竞争力；而在 Terminal-Bench 4.0 这类更偏科学与专家知识的任务上，仍显著落后于 Opus-5 和 GPT-5.6 Sol。</li> </ul> <p>报告自己也承认这条边界：日常 coding 和白领工作流已可覆盖很多场景，但在需要专家级领域知识的科学型 Agent 任务上还有差距。这个说法比“能完成 95% 真实任务”更值得参考，因为后者缺少可复核的任务分布与统计口径。</p> <h3 id="62-reasoning-effort-是一个可用的成本旋钮而非免费提升">6.2 reasoning effort 是一个可用的成本旋钮，而非免费提升</h3> <p>报告在 RL 时向 system prompt 加入 $b\in[1,100]$ 的 scalar effort。较高 $b$ 会降低长度惩罚，让模型有空间输出更长的推理与探索轨迹；不同 effort 下的样本在各自子组内计算相对优势，而不是直接把低 effort 和高 effort 的回答拿来比较。</p> <p><img src="/assets/img/posts/20260910-deepseek-v41-flash/figure9_p35.png" alt="图 9：reasoning effort 与性能、输出长度的关系"/></p> <p><em>图 9。每个子图的实线对应 Pass@1，虚线对应平均输出 token 数；横轴从 effort 25 增至 100。三个任务上分数都上升，但 token 数也明显增长。报告给出的汇总是：八个推理基准的平均 Pass@1 从 67.1% 到 76.3%，DeepSWE 从 66.0% 到 74.2%，Terminal-Bench 2.1 从 82.4% 到 90.6%，代价约为 2.5 倍输出 tokens。</em></p> <p>对实际使用者更有价值的信息在边际收益上：报告认为 effort 60–80 已拿到大部分最大 effort 的准确率，而到 100 时，Agent 轨迹会再长 1.6–1.8 倍，提升则变得有限。因此 API 中 low/high/max 映射为 $b=50/75/100$，应该被看成服务质量与 token 预算的操作点，而不是同一个模型的“免费档位”。</p> <p><img src="/assets/img/posts/20260910-deepseek-v41-flash/table4_p35.png" alt="表 4：同一模型在不同 Agent scaffold 下的表现"/></p> <p><em>表 4。保持 checkpoint、解码参数和任务集不变，只替换 Claude Code、Codex、OpenCode、Pi、mini-SWE 与 DeepSeek Harness 等外围框架。DeepSWE 的区间为 65.5–74.2，Terminal-Bench 2.1 为 84.1–90.6。它说明模型并未只在某一种框架下工作；但 8 个配置之间仍有明显差距，也说明部署时 scaffold 不是可忽略的“外壳”。</em></p> <h3 id="63-多-agent-曲线值得看但目前仍是初步结果">6.3 多 Agent 曲线值得看，但目前仍是初步结果</h3> <p><img src="/assets/img/posts/20260910-deepseek-v41-flash/figure10_p36.png" alt="图 10：单 Agent 与多 Agent 的 test-time compute scaling"/></p> <p><em>图 10。横轴是每次 rollout 的墙钟时间上限，纵轴分别是 ProgramBench 的 Almost@1 与 FrontierSWE v2 的 Mean@5。两项任务上，多 Agent 曲线都高于单 Agent：ProgramBench 在 8 小时处为 30.04% 对 20.39%，FrontierSWE v2 在 20 小时处为 32.90% 对 28.20%。但这是作者挑选的最强配置之间的 preliminary comparison，不应直接读作“多 Agent 必然更好”。</em></p> <p>这部分实验真正传达的是：当任务预算从一次模型回复变成数小时的工具执行，多 Agent 的价值可能来自并行探索和分工，而不是单个模型突然更聪明。但相应地，协调、共享工作区、消息同步和截止时间都会成为算法的一部分。</p> <h2 id="7-这份报告真正给出的启发与仍未回答的问题">7. 这份报告真正给出的启发，与仍未回答的问题</h2> <h3 id="71-最值得带走的是一套成本建模方式">7.1 最值得带走的是一套成本建模方式</h3> <p>V4.1 Flash 最有价值的地方，不是某个单独模块，而是它把长上下文 Agent 的成本拆成了可分别优化的对象：</p> <table> <thead> <tr> <th>约束</th> <th>对应设计</th> <th>解决的不是</th> </tr> </thead> <tbody> <tr> <td>新上下文读入太贵</td> <td>CED + decoder SWA bounded replay</td> <td>单纯减少输出 token</td> </tr> <tr> <td>全局 KV 太大</td> <td>CSA2 跨层复用 + FP4 KV</td> <td>只降低注意力算力</td> </tr> <tr> <td>长上下文检索反复扫描</td> <td>Hierarchical Sparse Indexer</td> <td>让所有层共享同一检索结果</td> </tr> <tr> <td>缓存要跨回合保留</td> <td>不持久化 SWA KV、恢复时有限重放</td> <td>永久不再计算</td> </tr> <tr> <td>模型在单一工具框架里过拟合</td> <td>多环境、多 scaffold 的 RL</td> <td>不需要评测 harness</td> </tr> </tbody> </table> <p>这张表也解释了为何“更长 context”本身不是完整答案。上下文窗口做到了 1M，如果缓存必须按层完整保存、每层都重新索引、缓存恢复还要精确重放所有局部状态，服务系统依然会被显存、SSD 与网络拖住。</p> <h3 id="72-它的风险也恰好藏在最省钱的地方">7.2 它的风险也恰好藏在最省钱的地方</h3> <p>报告的节省都有对应的假设：</p> <ul> <li>CED 假设 encoder 中间层的表示足以为 decoder 生成有效的全局 KV；</li> <li>Reuse Mode 假设相邻层可以共用一批被检索到的位置；</li> <li>Hierarchical Sparse Indexer 假设第一层构造的候选池不会漏掉后续层真正需要的信息；</li> <li>Bounded Replay 假设只重放一个局部窗口就能近似恢复所需的 SWA 状态；</li> <li>FP4 KV 假设极低精度存储不会在关键长程任务上造成不可接受的误差。</li> </ul> <p>这些不是理论上无代价的压缩。报告已把 CSA2 的 selection error 和 Bounded Replay 的近似状态恢复列为尚需继续做压力测试的边界。更完整的后续验证，应该包括：对针尖式长程信息检索的对抗样本、跨缓存恢复点的一致性、不同 I/O 条件下的端到端吞吐，以及真实长任务的成本曲线，而不只是单 token FLOPs 或单一 benchmark。</p> <h2 id="结语">结语</h2> <p>如果把这份报告只概括成“DeepSeek 又发布了一个更强的 Flash 模型”，会漏掉最重要的部分。它真正处理的是：当 Agent 工作流越来越长时，模型能力之外的 KV Cache 已经变成产品可用性的一部分。</p> <p>CED 让输入阶段少跑一半左右的全局计算；CSA2 让全局缓存和检索决策在层间以不同粒度复用；Bounded Replay 则把持久化缓存从“完整保存状态”改成“保存更少、需要时小范围重算”。三者拼在一起，才得到报告中运行时全局 KV 约为前代四分之一、持久化 KV 约为前代八分之一的结论。</p> <p>对做模型服务的人来说，这比一个漂亮的参数量或单项分数更接近实际问题：未来的竞争不只是模型能不能完成任务，也是谁能以可恢复、可迁移、可承受的缓存成本，让它把任务做得足够久。</p> <hr/> <p>如果你对 MoE、KV Cache、长上下文推理这些问题感兴趣，可以看看我的新书《大模型基础与前沿：从 Transformer 到 Agent》。书里尝试从 Transformer 的计算图出发，把注意力、推理、训练与 Agent 系统里的概念放在同一套框架下解释；和本文一样，更关心“一个设计究竟在解决哪一类约束”。</p> <p><img src="https://github.com/marsggbo/blog-assets/raw/main/book.png" alt=""/></p>]]></content><author><name></name></author><category term="DeepSeek"/><category term="MoE"/><category term="KV Cache"/><category term="长上下文"/><category term="Agent"/><category term="论文解读"/><summary type="html"><![CDATA[DeepSeek V4.1 Flash | 后训练没有新算法，Agent 能力从哪来？]]></summary></entry><entry><title type="html">ICML’25 | ASR 微调的数据配比，能不能不再靠人工网格搜索</title><link href="https://marsggbo.github.io/blog/2026/asr-data-mixing/" rel="alternate" type="text/html" title="ICML’25 | ASR 微调的数据配比，能不能不再靠人工网格搜索"/><published>2026-09-09T00:00:00+00:00</published><updated>2026-09-09T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/asr-data-mixing</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/asr-data-mixing/"><![CDATA[<h1 id="icml25--asr-微调的数据配比能不能不再靠人工网格搜索">ICML’25 | ASR 微调的数据配比，能不能不再靠人工网格搜索</h1> <blockquote> <p>涉及三篇工作： <a href="https://www.isca-archive.org/interspeech_2020/zhang20r_interspeech.pdf">Monolingual Data Selection Analysis for English-Mandarin Hybrid Code-Switching Speech Recognition</a>（Interspeech 2020） <a href="https://arxiv.org/abs/2508.11953">Data Mixing Optimization for Supervised Fine-Tuning of Large Language Models</a>（ICML 2025） <a href="https://arxiv.org/pdf/2603.26164">DataFlex: A Unified Framework for Data-Centric Dynamic Training of Large Language Models</a>（arXiv 2026）</p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>现在做 ASR 微调，手上的数据基本不会是单一来源：真实录音、TTS 合成、不同口音、不同领域（客服、医疗、会议）、甚至夹杂着 code-switching（中英混说）——这些数据源混在一起怎么配比，直接决定微调出来的模型在哪些场景好用、在哪些场景拉胯。多试几个配比对比一下效果，听起来是常规操作，但配比的自由度一旦超过两三个数据源，”多试几个”很快就变成没法穷举的组合爆炸，而且这一次训练调好的最优配比，换个数据规模、换个模型大小，很可能又不成立了。</p> <p>这不是一个新问题。翻到 2020 年一篇 Interspeech 论文才发现，做中英 code-switching 语音识别的团队早就撞上了同一个坑，只是那时候的模型还是 HMM-DNN，不是现在的 SpeechLLM。有意思的是，那篇论文解决这个问题的办法，就是把每个候选配比都真的跑一遍、画曲线看拐点——这基本等于人工做了一遍网格搜索。六年后的今天，这件事已经有了更系统的解法：ICML’25 有一篇论文专门把”数据配比”定义成一个可以求解的优化问题，不用穷举网格就能拿到接近最优的配比；同时 LLaMA-Factory 团队今年也放出了一套叫 DataFlex 的训练框架，把”动态选数据、动态调配比、动态调样本权重”这三件事统一成了可插拔的组件。</p> <p>这篇想把这三样东西串起来看：先看那个六年前的坑到底长什么样，再看现在的方法论怎么把”跑网格”变成”解方程”，最后看看这套思路要落到 ASR 微调上，还缺什么、要注意什么。</p> <h2 id="2-六年前的一篇-interspeech-论文把这个坑踩了个底">2. 六年前的一篇 Interspeech 论文，把这个坑踩了个底</h2> <p><a href="https://www.isca-archive.org/interspeech_2020/zhang20r_interspeech.pdf">这篇 2020 年的工作</a>是给一场中英 code-switching 语音识别比赛做的系统。比赛方给了三份数据：200 小时中英混说数据（记作 <code class="language-plaintext highlighter-rouge">CS200</code>）、500 小时纯中文数据（<code class="language-plaintext highlighter-rouge">Man500</code>，其中约 15 小时其实也带 code-switching，记作 <code class="language-plaintext highlighter-rouge">CS15</code>）、960 小时英文 LibriSpeech（<code class="language-plaintext highlighter-rouge">Libri</code>）。选手可以自由组合这三份数据去训练一个 HMM-DNN 混合系统。</p> <p>直觉上，数据越多越好，那就把三份数据全部堆进去。作者试了才发现不对：</p> <p><img src="/assets/img/posts/20260909-asr-data-mixing/zhang20r_table3_mer.png" alt="MER结果对比表"/></p> <p><code class="language-plaintext highlighter-rouge">MER</code>（Meta-data Error Rate）是这篇论文自定义的一个混合错误率——中文部分按字错率算、英文部分按词错率算，混在一起报一个数，数值越低越好。表里四行都是在 <code class="language-plaintext highlighter-rouge">CS200</code> 基础上分别再加一种数据、各自单独训一个模型对比：加上同分布的 <code class="language-plaintext highlighter-rouge">CS15</code> 之后几乎全面变好（eval 7.08→6.87）；换成加 <code class="language-plaintext highlighter-rouge">Man500</code>（纯中文）效果更好，eval 降到 6.63；但换成加 <code class="language-plaintext highlighter-rouge">Libri</code>（纯英文），结果反而比什么都不加的基线还差，eval 涨到 7.50。同样是”往 <code class="language-plaintext highlighter-rouge">CS200</code> 里加数据”，中文数据加了更好，英文数据加了更差——光看这一张表，已经能确认”数据越多越好”在这里是不成立的。</p> <p>问题就变成了：中文数据和英文数据分别应该配多少。作者的做法是固定 <code class="language-plaintext highlighter-rouge">CS200</code>，把 <code class="language-plaintext highlighter-rouge">Man500</code> 从 10 小时开始每次多加一点，一路加到 500 小时，每加一次都重新训一遍模型、测一遍三个测试集：</p> <p><img src="/assets/img/posts/20260909-asr-data-mixing/zhang20r_mandarin_balance.png" alt="中文数据配比曲线"/></p> <p>这四张子图分别是整体 MER、中文部分 CER、英文部分 WER、以及 eval 集上和不加中文数据相比的对照。中文数据加得越多，中文部分的识别效果越好（子图 b 一路走低），英文部分基本没受影响（子图 c 几乎是平的）——但整体效果（子图 d）在大约 400 小时处见底，过了这个点继续加数据，eval MER 反而开始回升。换句话说，”中文数据”这个维度上存在一个具体的最优点，不是越多越好，多出来的那部分反而在拖后腿。同一套流程换到英文数据上，作者发现情况更糟：无论 LibriSpeech 加多少，各个词数分组的识别效果都在变差，不存在一个”加到某个点就够了”的甜蜜区，而是持续负收益——</p> <p><img src="/assets/img/posts/20260909-asr-data-mixing/zhang20r_english_mismatch.png" alt="英文数据配比曲线"/></p> <p>这四张子图是按句子里含 1、2、3、4+ 个英文词分组之后，各自随英文数据量变化的 MER 曲线，可以看到不管哪一组，曲线基本都是往上走的。作者把这归因于数据不匹配：LibriSpeech 是美式英语朗读语料，跟中国大陆说话人夹在中文里说的英文单词，口音和说话方式完全是两回事，”英文数据”这四个字底下其实盖着一个分布差异很大的子问题，加了不但没用，反而把模型往错误的方向拉。</p> <p>这篇论文自己给出的结论是三句话：数据匹配比数据量更重要；存在一个配比的平衡点，过了会伤性能；不同的 code-switching 模式（句子里到底混了几个外语词）对配比的敏感度还不一样。这三句话放到 2026 年的 ASR 微调场景里，一个字都不用改。差别只在于，六年前找这个平衡点的办法是老老实实地跑一条配比扫描曲线——每加一档数据就重训一次、测一次——这在算力紧张、模型只有 HMM-DNN 规模的年代还能忍，换到今天动辄要微调一个数十亿参数的 SpeechLLM，同样的”扫一遍再看拐点”就顶不住了。</p> <h2 id="3-换个思路把数据配比当成一个可以求解的优化问题">3. 换个思路：把数据配比当成一个可以求解的优化问题</h2> <p><a href="https://arxiv.org/abs/2508.11953">ICML 2025 的这篇工作</a>瞄准的正是这件事，只不过场景是文本 LLM 的 SFT（supervised fine-tuning，即指令微调阶段），不是 ASR。它的出发点很直接：给定 $K$ 个数据域（论文里是 IF 通用指令、数学、代码三个域）和一个总的数据预算 $N_0$，要找一个配比向量 $w=(w_1,\dots,w_K)$（每个 $w_i\ge0$，加起来等于 1），使得训完之后模型在各个验证集上的总损失最小。这本身没有闭式解，直接按网格枚举 $w$ 再逐个训练验证，就是上一节那种”扫一遍看拐点”的做法——只是维度从 1 维（中文数据量）变成了 $K-1$ 维单纯形，枚举成本指数级增长。</p> <p>论文绕开枚举的办法，是先给每个域的验证损失建一个可以外推的函数，再对这个函数求最优解，而不是对真实训练过程求最优解。具体分两步走：</p> <table> <tbody> <tr> <td>第一步借用了”有效迁移数据量”（effective data from transfer）的概念：域 $i$ 的表现，不只取决于域 $i$ 自己有多少数据 $N_i$，其他域的数据 $</td> <td>D_{\setminus i}</td> <td>$ 也会通过跨域迁移间接帮到域 $i$，这部分贡献换算成”相当于多少域 $i$ 自己的数据”，记作 $</td> <td>D_i^T</td> <td>= k_i \cdot</td> <td>D_{\setminus i}</td> <td>^{\alpha_i}$。第二步套用了微调阶段的 scaling law：损失随数据量近似服从幂律 $L(D) \approx C \cdot</td> <td>D</td> <td>^{-\beta} + E$，$E$ 是不可约损失。把两者拼起来，域 $i$ 的验证损失就被参数化成：</td> </tr> </tbody> </table> \[\tilde{L}_i \approx C_i \cdot \left(N_i + k_i \cdot |D_{\setminus i}|^{\alpha_i}\right)^{-\beta_i} + E_i\] <p>每个域有 5 个待拟合参数 ${C_i, k_i, \alpha_i, \beta_i, E_i}$。拟合这 5 个参数不需要跑真正预算规模的训练——论文的做法是每个域只用一个很小的样本量 $n$（实验里是 66 万 token），固定其他域数据不变，把当前域的数据量按 4 个扰动比例（1/3、1/2、2、3 倍）分别训一个小模型、测一次损失，5 个点（含原始比例）拟合 5 个参数，用 Trust Region 方法配 Huber loss 求解。完整流程写成了 Algorithm 1：</p> <p><img src="/assets/img/posts/20260909-asr-data-mixing/li25bh_algorithm1.png" alt="数据配比优化算法"/></p> <p>拟合完参数之后，第二步才是真正的优化：把 $\tilde{L}_i$ 代入目标函数，对真实的数据预算 $N_0$ 求解</p> \[\min_{w}\ \sum_{i=1}^K \Big[C_i\big(w_iN_0+k_i(N_0-w_iN_0)^{\alpha_i}\big)^{-\beta_i}+E_i\Big]\quad \text{s.t. } 0\le w_i\le1,\ \sum_i w_i=1\] <p>这是一个可以证明是凸函数、但没有闭式解的约束优化问题，论文用 SLSQP（一种支持等式和不等式约束的梯度法）求解，一次求解只需要几秒钟，不需要额外训练任何模型。也就是说，真正花训练成本的只有”拟合参数”这一步——3 个域、4 个扰动比例，一共只需要 12 次小规模训练加上 1 次初始训练，合计 13 次；求最优配比这一步是纯数值优化，而且这一套参数可以直接套用到任意目标预算 $N_0$ 上，不需要为每个预算重新拟合。论文图里画的就是同一套参数在 $N_0$ 从几百万到 5 亿 token 之间求出的最优权重曲线：</p> <p><img src="/assets/img/posts/20260909-asr-data-mixing/li25bh_figure2_optimal_weights.png" alt="不同数据预算下的最优权重"/></p> <p>三条线分别是指令、数学、代码三个域的权重随预算变化的走势——注意权重并不是持续单调地涨或跌，而是很快趋于一个平台。论文管这个现象叫”没有域被落下”（no domains left behind）：因为幂律本身自带边际收益递减，一个域数据加得越多，它自己的偏导数越小，自然就不会无限制地挤占其他域的份额，这也是这套方法在数学上被证明”不会让某个域彻底吃亏”的原因。</p> <p>效果怎么样，论文拿网格搜索的结果直接对比。在 21 组网格搜索配比（网格取 ${0.125,0.25,\dots,0.75}^3$ 里加和等于 1 的组合）里挑出效果最好的那组，跟自己方法求出的配比训出来的模型比困惑度（PPL，语言模型下一词预测的不确定度，越低越好）：</p> <p><img src="/assets/img/posts/20260909-asr-data-mixing/li25bh_figure4_vs_gridsearch.png" alt="和网格搜索最优解的对比"/></p> <p>四个不同规模的模型（Qwen2.5-0.5B/1.5B、Llama3.2-3B、Llama3.1-8B），黄色柱子（本方法）和深蓝柱子（网格搜索最优）几乎贴在一起，平均只高 0.66%。换句话说，13 次小规模训练 + 一次数值求解，换来了跟 21 组网格搜索几乎打平的效果——网格搜索本身还只是三个域、四种模型规模里的一个切片，真要覆盖更多域或者更细的网格，枚举成本会继续爬。这正是上一节里 code-switching 论文没有的东西：不用真的把每个候选配比都训一遍，就能定位到平衡点附近。</p> <h2 id="4-光有公式不够还得有地方能插进去跑">4. 光有公式不够，还得有地方能插进去跑</h2> <p>上面这套方法要落地，还差一层工程基础设施：怎么把”拟合参数、求解权重、按权重重新混数据”这套流程，接进一次真实的训练里，而不是每次都手写脚本现改现跑。<a href="https://arxiv.org/pdf/2603.26164">DataFlex</a> 这份技术报告解决的正是这一层。</p> <p>它的定位是 LLaMA-Factory（目前最常用的开源 LLM 微调框架之一）的一个训练层替换插件，把”数据”从一份训练开始前就定好的静态文件，变成训练过程中可以被观察、被调整的一等变量。它把数据驱动的动态训练归纳成三种范式：动态选样本（哪些样本该被训练，比如按梯度相似度挑高价值样本）、动态调配比（不同数据源该占多少比例，比如上一节的场景）、动态调权重（每个样本对梯度更新的贡献强度该乘多大的系数）。对应到系统设计上，就是三种 Trainer 抽象：</p> <p><img src="/assets/img/posts/20260909-asr-data-mixing/dataflex_figure1_architecture.png" alt="DataFlex 三层架构"/></p> <p>最底下的 Base Layer 直接继承自 LLaMA-Factory，负责模型管理、数据处理、优化器这些通用能力，不重新发明轮子；中间的 Trainer Layer 是三个可以互相替换 LLaMA-Factory 原生 Trainer 的抽象——Select Trainer、Mix Trainer、Weight Trainer；最上面的 Component Layer 才是具体算法，每种 Trainer 配一组可插拔的组件：Select Trainer 配 Selector（比如 LESS、NICE、Loss-based 方法），Mix Trainer 配 Mixer（比如 DoReMi、ODM），Weight Trainer 配 Weighter。新算法只要按接口实现成一个组件、用装饰器注册进去，就能被对应的 Trainer 在训练时自动调用，不用改训练主循环的代码。</p> <p>目前已经接进去的方法，论文列了一张表：</p> <p><img src="/assets/img/posts/20260909-asr-data-mixing/dataflex_table1_methods.png" alt="已集成的数据方法"/></p> <p>数据选择这一栏有 7 种方法，按打分方式分成基于梯度（LESS、NICE）、基于损失（Loss、Delta Loss）、基于分布（NEAR、TSDS）三类；数据配比这一栏是 DoReMi（离线：先用小模型算出每个域相对参考模型的”超额损失”，再据此更新配比，最后把配比定死去训真正的目标模型）和 ODM（在线：训练过程中用多臂老虎机算法根据实时 batch loss 动态调整配比，不需要额外的参考模型）；数据重加权这一栏是基于损失的样本级加权。切换到某种动态训练范式，配置文件里只需要多加几行：</p> <div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">### dataflex</span>
<span class="na">train_type</span><span class="pi">:</span> <span class="s">dynamic_mix</span>
<span class="na">component_name</span><span class="pi">:</span> <span class="s">doremi</span>
<span class="na">init_mixture_proportions</span><span class="pi">:</span> <span class="pi">[</span><span class="nv">0.5</span><span class="pi">,</span> <span class="nv">0.5</span><span class="pi">]</span>
<span class="na">warmup_step</span><span class="pi">:</span> <span class="m">100</span>
<span class="na">update_step</span><span class="pi">:</span> <span class="m">200</span>
<span class="na">update_times</span><span class="pi">:</span> <span class="m">3</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">train_type</code> 选范式，<code class="language-plaintext highlighter-rouge">component_name</code> 选具体算法，剩下几个字段控制什么时候开始更新、每隔多少步更新一次、总共更新几次——除了这一小段，模型、数据集、学习率这些字段和原生 LLaMA-Factory 的配置文件完全一样。DataFlex 自己在 SlimPajama（英文预训练语料）上验证了 DoReMi 和 ODM：6B token 规模下，两种动态配比方法的 MMLU 准确率都超过了默认配比的静态基线（ODM 26.04%、DoReMi 25.84%，基线 25.27%），整体困惑度上 DoReMi 最低（4.134）。但拆到七个子域去看，没有一种方法通吃：DoReMi 在数据量最大的两个域 CommonCrawl、C4 上最低，ODM 在 StackExchange、ArXiv 上最低，反而是什么都没调的默认配比在 Wikipedia、GitHub 这两个相对小的域上意外地更低——这和第 2 节里”配比效果在不同子群体上并不一致”是同一个现象，只是这里的”子群体”换成了文本域，而且 DataFlex 跑的是真实的预训练规模训练，不是第 3 节那种小规模拟合。</p> <p>DoReMi、ODM 都是给预训练场景设计的方法，论文里也提到 SFT 阶段的数据配比”remains under-explored”（还没被充分研究）——这恰好是第 3 节那篇 ICML 论文补的空。DataFlex 的 Mixer 接口本身是通用的：只要按同样的接口实现一个新组件，理论上可以把第 3 节这套”小规模扰动实验拟合参数、SLSQP 求解权重”的流程也注册成一个 Mixer，接进 Mix Trainer 里跑——但目前论文列出来的两种方法都不是这个思路，这是一个现成但还没被填的位置。</p> <h2 id="5-落到-asr-微调上这套思路要怎么搬">5. 落到 ASR 微调上，这套思路要怎么搬</h2> <p>把前面三节拼起来看，会发现一件挺清楚的事：第 2 节的问题在 ASR 领域一直存在，第 3 节的方法论是文本 LLM 那边刚补上的解法，第 4 节的基础设施也刚刚成型——三块拼图基本都齐了，但没有一块是直接为 ASR 微调准备的。要真的搬过来用，至少有三个地方需要落地：</p> <p><strong>配比的”域”要重新定义。</strong> li25bh 里的域是 IF/数学/代码，DataFlex 的 Mixer 也是照着文本预训练的域设计的；ASR 微调场景下，第 2 节那篇论文已经给出了具体的坐标：语言/方言、口音、领域（客服/医疗/会议）、真实录音 vs TTS 合成、甚至像 code-switching 这种”句子内部混合模式”本身都可以是一个独立的轴——第 2 节里”英文词数越多，加英文数据的效果越不一样”这个发现，说明域的划分粒度可能要比”哪个数据集来的”更细，切到”某种说话模式”这一层。</p> <p><strong>验证损失要换成能反映子群体差异的指标。</strong> li25bh 用的是 token 级困惑度，一个数就能代表一个域；ASR 场景下更自然的是 WER/CER，但第 2 节那组按句子里外语词数分组的曲线已经说明，同一个数据源在不同子群体上的收益可能完全不同方向——如果只看整体 WER 去拟合 scaling law，很可能把”对某个子群体是负收益”这个信号平均掉。这意味着搬这套方法过来，第一步得先老老实实按第 2 节的方式，切分出几个关心的子群体分别算验证指标，再决定要不要把子群体也当成域来分别建模。</p> <p><strong>扰动实验的成本结构不一样，可能反而更划算。</strong> li25bh 需要真的训一个模型（哪怕是小规模）才能拿到一个验证损失点，这也是它要控制在 13 次小规模训练的原因；ASR 微调如果用的是在预训练 SpeechLLM 上做 LoRA 这类轻量微调，一次”扰动实验”的成本本来就远低于从零训练，13 次量级的小规模微调加验证，在预算上可能比文本 LLM 场景更容易接受，不需要像 DoReMi 那样专门引入一个更小的代理模型来降成本。</p> <p><strong>先验证 scaling law 是否成立，再信配比公式。</strong> 第 3 节整套优化建立在”损失（或者错误率）随数据量呈幂律衰减”这个假设上，这个假设在文本 PPL 上有实证支持，但 WER/CER 是否也服从同样的幂律形状——尤其是像第 2 节里”过了平衡点反而变差”这种非单调现象——目前没有证据说明能被这套单调递减的幂律公式直接套用。比较扎实的做法是先把 Algorithm 1 里的扰动实验单独跑一遍，把拟合出来的曲线和真实测出来的点画在一起看看像不像幂律，而不是直接假设它成立就去解优化问题。</p> <p>综合起来，一个可以落地的路径大概是：借 DataFlex 的 Mix Trainer 抽象接口，把”小规模扰动微调 → 按子群体拟合每个域的 scaling law 参数 → SLSQP 求解配比”这套流程实现成一个新的 Mixer 组件，先在几个候选域和几个关心的子群体（比如按语言、按口音、按是否 code-switching）上跑一遍验证曲线，确认幂律假设站得住，再把这个 Mixer 接进真正的微调流程——这条路径目前看下来是缺人去做，不是缺积木。</p> <h2 id="6-结论">6. 结论</h2> <p>数据配比不是一个新问题，2020 年做 HMM-DNN 的团队已经用最笨的方式——把每个候选配比都跑一遍、画曲线找拐点——证明了”数据不是越多越好”这件事，也顺手发现了配比效果在不同子群体上并不一致。六年后，文本 LLM 那边把这件事系统化成了一个可以用小规模扰动实验加数值优化求解的问题，效果接近网格搜索、成本却低了一个量级；同时期的训练框架也把”动态选数据、动态调配比、动态调权重”统一成了可插拔组件。这三块东西现在都摆在桌面上了，缺的是把它们按 ASR 微调的实际情况——重新定义域、按子群体验证、先验证幂律假设再信公式——拼到一起跑一遍。这也是接下来我想动手试的方向。</p> <hr/> <p>顺带说一句，这篇聊的数据配比优化，本质上和 AutoML 里”用搜索/优化代替人工调参”是同一套方法论，只是这次的搜索空间是数据配比而不是网络结构或超参数。《动手学 AutoML：从 NAS 到大语言模型优化实战》里专门有一章讲数据增强和代理数据集搜索，思路上和这篇的”小规模代理实验 + 外推到真实预算”是相通的，感兴趣可以翻一下。</p> <blockquote> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="ASR"/><category term="数据配比"/><category term="LLM"/><category term="论文解读"/><summary type="html"><![CDATA[ICML’25 | ASR 微调的数据配比，能不能不再靠人工网格搜索]]></summary></entry><entry><title type="html">arXiv’26 | VibeVoice-ASR-Streaming：LLM 如何实时转写谁说了什么</title><link href="https://marsggbo.github.io/blog/2026/20260907-vibevoice/" rel="alternate" type="text/html" title="arXiv’26 | VibeVoice-ASR-Streaming：LLM 如何实时转写谁说了什么"/><published>2026-09-07T00:00:00+00:00</published><updated>2026-09-07T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260907-vibevoice</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260907-vibevoice/"><![CDATA[<h1 id="arxiv26--vibevoice-asr-streamingllm-如何实时转写谁说了什么">arXiv’26 | VibeVoice-ASR-Streaming：LLM 如何实时转写谁说了什么</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2609.02812">VibeVoice-ASR-Streaming Technical Report</a> 代码：<a href="https://github.com/microsoft/VibeVoice">microsoft/VibeVoice</a>（本文结合仓库 <code class="language-plaintext highlighter-rouge">1541f59</code> 阅读，模型结构数字取自 <code class="language-plaintext highlighter-rouge">microsoft/VibeVoice-ASR-Streaming-7B</code> 在 HuggingFace 上公开的 <code class="language-plaintext highlighter-rouge">config.json</code>）</p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>会议字幕这几年已经被打磨得很成熟，单人说话“听到什么写什么”基本够用。真正麻烦的是紧跟在文字后面那半句：这句话是谁说的。只要房间里有两个人以上，Speaker 0 讲完一句，Speaker 1 接上，几分钟后 Speaker 0 又开口——系统必须一直用同一个编号认出他，而且不能等录音结束才回头补标签，因为下游的语音 agent 早就根据这句话做出反应了。这类任务有个专门的名字，叫 <strong>streaming speaker-attributed ASR</strong>：ASR 负责把语音变成文字，speaker attribution 负责把文字挂到会话里已经出现过的某个说话人身上。这里的“身份”不是真实姓名，而是按出场顺序分配的 Speaker 0、Speaker 1，之后这个人每次再开口都要沿用同一个编号。</p> <p>最直观的做法是把音频切成 2 秒一段，各自独立跑一次识别。接口简单、吞吐好堆，但两类信息会同时丢掉：语言上下文断在切片边界上，跨块的指代、断句、半个词全靠猜；说话人历史也断了——第 0 段第一次冒出来的声音被叫 Speaker 0，第 15 段这个人回来了，独立请求没有任何依据继续叫他 Speaker 0。传统系统通常在 ASR 外面另挂一个 diarization（说话人分割聚类）模块去补这个缺口：抽 speaker embedding、维护一个 speaker cache、拿新片段去匹配。这确实能工作，但代价是转写和说话人归属变成两套独立状态，标签随时可能被后续证据重新聚类、回写——也就是说，你当下看到的输出，并不是真正意义上的“最终结果”，后面还有一套修正逻辑在悄悄运行。</p> <p>VibeVoice-ASR-Streaming 想做的，是把“要不要保留会话历史”和“能不能不做事后修正”这两件事，用一个模型同时解决，而不是维持“ASR + 一个外部说话人组件”的老架构。它的做法可以先用一句话概括：<strong>把新音频块和模型已经生成的带说话人文本交错写进同一个 LLM 上下文</strong>，让说话人编号变成模型自己要维护的上下文状态，而不是外部聚类的产物。</p> <p>这篇不打算停在“有这么个设计”的层面。下面每一节都会尽量落到具体数字和可以跑起来的代码上：两个语音 encoder 到底输出多少维、怎么和 LLM 的 3584 维 embedding 对上；一个 chunk 的窗口边界具体切在哪一秒；训练数据是怎么按词级时间切成逐块目标的；K/V 缓存是怎么一格一格长起来、又为什么没有回头改的余地。手上没有下载 7B 的权重，所以模型本身的文字预测能力没法在这篇文章里复现，但架构里所有”纯计算”的部分——窗口怎么滑、维度怎么变、缓存怎么长——都可以用真实的维度和真实的切分规则跑一遍给你看。</p> <h2 id="2-核心机制交错序列把历史变成模型自己的状态">2. 核心机制：交错序列，把历史变成模型自己的状态</h2> <p>第 $k$ 个音频块记为 $X_k$，对应输出记为 $Y_k$，模型看到的是：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>提示词, X0, Y0, X1, Y1, X2, Y2, ...
</code></pre></div></div> <p>生成当前 $Y_k$ 时，条件写成：</p> \[p(Y_k \mid X_{&lt;k}, \widetilde{X}_k, Y_{&lt;k})\] <p>其中 $X_{&lt;k}$ 是此前已经听过的音频，$Y_{&lt;k}$ 是此前已经写出的文字和 Speaker 编号，$\widetilde{X}_k$ 是当前负责的音频块再加上一小段 look-ahead（第 5 节展开）。这条式子表达的是一个具体分工：<strong>历史用于延续会话状态，look-ahead 用于降低当前边界的不确定性。</strong> 生成的 $Y_k$ 不是旁路结果，而是直接成为下一轮的上下文——具体是怎么“成为上下文”的，本质上就是一次 K/V 缓存的追加，第 6 节会用代码把这个追加过程画出来。</p> <p>下面的架构图中，蓝色连续 latent 和文本 token 交替进入同一个自回归模型；不是“一个声学模型先出结果，再让语言模型另行润色”。</p> <p><img src="/assets/img/posts/20260907-vibevoice/figure2_p4.png" alt="交错的语音与文本上下文"/></p> <p>按图从下往上读会比较清楚。最底部是原始波形，第一段括号标出本轮新增的 2.9 秒，右侧斜线区域是额外读取的 0.5 秒前瞻。波形先分别经过两个 encoder（第 3 节会给出它们各自输出多少维、怎么进 LLM），两个小圆圈表示同一时刻的两路特征合并成一个 latent frame。中间的 <code class="language-plaintext highlighter-rouge">BOS</code>、连续蓝色 latent、<code class="language-plaintext highlighter-rouge">EOC</code> 组成当前音频输入，<code class="language-plaintext highlighter-rouge">EOC</code>（end of chunk）告诉 LLM：这一段声音已经结束，可以开始生成文字。最上方的文本 token 是模型自回归生成的 <code class="language-plaintext highlighter-rouge">Speaker 0: ...</code>、<code class="language-plaintext highlighter-rouge">Speaker 1: ...</code>；生成完一个块后，下一块音频接在同一条序列后面。图左侧的 optional context 是名字、术语、缩写等提示词，它在开头加入，之后和音频、文本历史一起保留。</p> <p>保留历史这件事，论文强调是任务本身的要求，不是一个可选的优化：一个丢弃历史的系统，必须在别处把历史找回来——外部 embedding 库、speaker cache，或者一次离线聚类，这恰好是这个方案想去掉的独立阶段。代价也很直接：上下文随录音时长线性增长，因此发布的 checkpoint 目前只支持约 8 分钟的会话，第 11 节还会再谈这个限制。</p> <h2 id="3-语音怎么变成模型能读的向量真实维度以及一个和论文描述不完全一致的细节">3. 语音怎么变成模型能读的向量：真实维度，以及一个和论文描述不完全一致的细节</h2> <p>先回答一个容易被跳过的问题：图里那两路进入 latent frame 的分支，是模型，还是只是某种固定的分词规则？答案是——它们是两个独立训练好的神经网络，训练这件事根本不发生在这篇论文里。</p> <p>这两个 encoder 叫 Acoustic tokenizer 和 Semantic tokenizer，来自另一篇论文，也就是 VibeVoice 语音合成技术报告（[PYW+25]）。在那篇报告里，它们都是“encoder + decoder”成对出现的：encoder 把波形压成连续 latent，decoder 再把 latent 还原成波形，服务于文本到语音合成。VibeVoice-ASR-Streaming 只取用其中的 encoder 半部分，而且在本文的全部训练过程中始终冻结、不参与梯度更新——真正被训练的只有 Qwen2.5 这个 LLM backbone。</p> <p>真实的维度可以直接从 HuggingFace 上公开的 <code class="language-plaintext highlighter-rouge">config.json</code> 里读出来，不用猜：</p> <table> <thead> <tr> <th>模块</th> <th>关键字段</th> <th>数值（7B 配置）</th> </tr> </thead> <tbody> <tr> <td>Acoustic tokenizer</td> <td><code class="language-plaintext highlighter-rouge">vae_dim</code></td> <td><strong>64</strong></td> </tr> <tr> <td>Semantic tokenizer</td> <td><code class="language-plaintext highlighter-rouge">vae_dim</code></td> <td><strong>128</strong></td> </tr> <tr> <td>两个 tokenizer 共用</td> <td><code class="language-plaintext highlighter-rouge">encoder_ratios</code></td> <td><code class="language-plaintext highlighter-rouge">[8, 5, 5, 4, 2, 2]</code>，连乘 = 3200，对应 3200× 下采样</td> </tr> <tr> <td>两个 tokenizer 共用</td> <td><code class="language-plaintext highlighter-rouge">encoder_depths</code></td> <td><code class="language-plaintext highlighter-rouge">3-3-3-3-3-3-8</code>，7 个阶段，每阶段是若干层 depthwise convolution 组成的残差块，用 RMSNorm 归一化</td> </tr> <tr> <td>Qwen2.5 backbone（7B）</td> <td><code class="language-plaintext highlighter-rouge">hidden_size</code> / <code class="language-plaintext highlighter-rouge">num_hidden_layers</code></td> <td><strong>3584</strong> / 28 层，28 个 attention head、4 个 KV head（GQA），<code class="language-plaintext highlighter-rouge">intermediate_size</code> 18944</td> </tr> <tr> <td>Qwen2.5 backbone（1.5B）</td> <td><code class="language-plaintext highlighter-rouge">hidden_size</code> / <code class="language-plaintext highlighter-rouge">num_hidden_layers</code></td> <td><strong>1536</strong> / 28 层，12 个 attention head、2 个 KV head</td> </tr> </tbody> </table> <p>Acoustic tokenizer 输出 64 维、Semantic tokenizer 输出 128 维，这两个数字不相等，本身就说明它们不是同一种特征的两份拷贝：Acoustic 是 σ-VAE（变分自编码器，来自 [SBW+24]）设计，输出一个连续隐变量分布，用来保留频谱、音色、韵律等声学细节；Semantic 输出的是确定性特征（不做 VAE 式随机采样），天然更贴近文字内容。两者的下采样率完全一致——<code class="language-plaintext highlighter-rouge">encoder_ratios</code> 连乘 8×5×5×4×2×2=3200，对应到 24 kHz 波形就是每 133.3 ms（3200/24000 秒）产生一帧，7.5 帧/秒。</p> <p>真正有意思的是这两路 64 维和 128 维特征怎么变成 LLM 认识的向量。论文原文的说法是“两路表示沿特征维拼接，再投影进 Qwen2.5 的 embedding 空间”——听起来像是 <code class="language-plaintext highlighter-rouge">concat(64, 128) → Linear(192, 3584)</code> 这种一次性投影。但仓库里 <code class="language-plaintext highlighter-rouge">SpeechConnector</code> 的实现是这样的：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># vibevoice/modular/modeling_vibevoice_streaming.py
# class SpeechConnector
</span><span class="k">class</span> <span class="nc">SpeechConnector</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">input_dim</span><span class="p">,</span> <span class="n">output_dim</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">fc1</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">input_dim</span><span class="p">,</span> <span class="n">output_dim</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">norm</span> <span class="o">=</span> <span class="nc">LlamaRMSNorm</span><span class="p">(</span><span class="n">output_dim</span><span class="p">,</span> <span class="n">eps</span><span class="o">=</span><span class="mf">1e-6</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">fc2</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">output_dim</span><span class="p">,</span> <span class="n">output_dim</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">features</span><span class="p">,</span> <span class="o">**</span><span class="n">kwargs</span><span class="p">):</span>
        <span class="n">x</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">fc1</span><span class="p">(</span><span class="n">features</span><span class="p">)</span>
        <span class="n">x</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">norm</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
        <span class="n">x</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">fc2</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">x</span>
</code></pre></div></div> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># vibevoice/modular/modeling_vibevoice_asr.py
# class VibeVoiceASRModel（构造函数），约第 84 行
</span><span class="n">self</span><span class="p">.</span><span class="n">acoustic_connector</span> <span class="o">=</span> <span class="nc">SpeechConnector</span><span class="p">(</span><span class="n">config</span><span class="p">.</span><span class="n">acoustic_vae_dim</span><span class="p">,</span> <span class="n">lm_config</span><span class="p">.</span><span class="n">hidden_size</span><span class="p">)</span>
<span class="n">self</span><span class="p">.</span><span class="n">semantic_connector</span> <span class="o">=</span> <span class="nc">SpeechConnector</span><span class="p">(</span><span class="n">config</span><span class="p">.</span><span class="n">semantic_vae_dim</span><span class="p">,</span> <span class="n">lm_config</span><span class="p">.</span><span class="n">hidden_size</span><span class="p">)</span>
</code></pre></div></div> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># vibevoice/modular/modeling_vibevoice_asr.py
# encode_speech(...) 末尾，约第 333 行
</span><span class="n">combined_features</span> <span class="o">=</span> <span class="n">acoustic_features</span><span class="p">[</span><span class="n">speech_masks</span><span class="p">]</span> <span class="o">+</span> <span class="n">semantic_features</span><span class="p">[</span><span class="n">speech_masks</span><span class="p">]</span>
</code></pre></div></div> <p>也就是说，Acoustic 的 64 维和 Semantic 的 128 维<strong>各自</strong>先经过一个“Linear → RMSNorm → Linear”的两层 connector，独立投影到 3584 维（7B 配置），投影完之后是<strong>逐元素相加</strong>，不是先拼接再投影。这两种做法不是等价的：先拼接再过一个线性层，等价于两路特征各自乘一个权重矩阵再相加，是一个线性组合；而这里每一路先各自过完整的两层 MLP（中间还夹了一次归一化）才相加，两路特征在被“融合”之前已经各自经过一次非线性变换。论文里“concatenate”是对整体设计意图的概括描述，实际实现是两个独立 connector 分别升维再相加——这个细节只有读代码才能看到。</p> <p>用真实维度把这一步在本地跑一遍（不需要下载 7B 权重，两个 connector 用随机初始化代替，只是为了验证维度和计算顺序）：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">torch</span><span class="p">,</span> <span class="n">torch</span><span class="p">.</span><span class="n">nn</span> <span class="k">as</span> <span class="n">nn</span>

<span class="n">ACOUSTIC_VAE_DIM</span><span class="p">,</span> <span class="n">SEMANTIC_VAE_DIM</span><span class="p">,</span> <span class="n">HIDDEN_SIZE</span> <span class="o">=</span> <span class="mi">64</span><span class="p">,</span> <span class="mi">128</span><span class="p">,</span> <span class="mi">3584</span>

<span class="k">class</span> <span class="nc">SpeechConnector</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">input_dim</span><span class="p">,</span> <span class="n">output_dim</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">fc1</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">input_dim</span><span class="p">,</span> <span class="n">output_dim</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">norm</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">RMSNorm</span><span class="p">(</span><span class="n">output_dim</span><span class="p">,</span> <span class="n">eps</span><span class="o">=</span><span class="mf">1e-6</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">fc2</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">output_dim</span><span class="p">,</span> <span class="n">output_dim</span><span class="p">)</span>
    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">):</span>
        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">fc2</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">norm</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">fc1</span><span class="p">(</span><span class="n">x</span><span class="p">)))</span>

<span class="n">acoustic_connector</span> <span class="o">=</span> <span class="nc">SpeechConnector</span><span class="p">(</span><span class="n">ACOUSTIC_VAE_DIM</span><span class="p">,</span> <span class="n">HIDDEN_SIZE</span><span class="p">)</span>
<span class="n">semantic_connector</span> <span class="o">=</span> <span class="nc">SpeechConnector</span><span class="p">(</span><span class="n">SEMANTIC_VAE_DIM</span><span class="p">,</span> <span class="n">HIDDEN_SIZE</span><span class="p">)</span>

<span class="n">WINDOW_FRAMES</span> <span class="o">=</span> <span class="mi">26</span>  <span class="c1"># 22 帧 chunk + 4 帧 lookahead，第 5 节会解释这两个数字
</span><span class="n">acoustic_latent</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="n">WINDOW_FRAMES</span><span class="p">,</span> <span class="n">ACOUSTIC_VAE_DIM</span><span class="p">)</span>
<span class="n">semantic_latent</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="n">WINDOW_FRAMES</span><span class="p">,</span> <span class="n">SEMANTIC_VAE_DIM</span><span class="p">)</span>

<span class="n">acoustic_features</span> <span class="o">=</span> <span class="nf">acoustic_connector</span><span class="p">(</span><span class="n">acoustic_latent</span><span class="p">)</span>
<span class="n">semantic_features</span> <span class="o">=</span> <span class="nf">semantic_connector</span><span class="p">(</span><span class="n">semantic_latent</span><span class="p">)</span>
<span class="n">combined_features</span> <span class="o">=</span> <span class="n">acoustic_features</span> <span class="o">+</span> <span class="n">semantic_features</span>   <span class="c1"># 逐元素相加，不是 concat
</span><span class="nf">print</span><span class="p">(</span><span class="n">combined_features</span><span class="p">.</span><span class="n">shape</span><span class="p">)</span>   <span class="c1"># torch.Size([1, 26, 3584])
</span></code></pre></div></div> <p>实际打印出来是：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>acoustic_latent : torch.Size([1, 26, 64])
semantic_latent : torch.Size([1, 26, 128])
acoustic_features (fc1-&gt;norm-&gt;fc2 之后): torch.Size([1, 26, 3584])
semantic_features (fc1-&gt;norm-&gt;fc2 之后): torch.Size([1, 26, 3584])
combined_features (逐元素相加，不是 concat): torch.Size([1, 26, 3584])
audio_embeds (BOS + 26 帧 latent + EOC): torch.Size([1, 28, 3584])
</code></pre></div></div> <p>最后一行的 <code class="language-plaintext highlighter-rouge">audio_embeds</code> 是把这 26 帧再前后各拼一个边界 token 的 embedding：起始边界复用的是 Qwen2.5-VL 词表里原本给视觉用的 <code class="language-plaintext highlighter-rouge">&lt;|object_ref_start|&gt;</code>（id 151646），结束边界是 <code class="language-plaintext highlighter-rouge">&lt;|object_ref_end|&gt;</code>（id 151647）——图里的 <code class="language-plaintext highlighter-rouge">BOS</code>/<code class="language-plaintext highlighter-rouge">EOC</code> 具体就是这两个 token，只是被 VibeVoice 拿来标记“一段语音的开始/结束”而不是图像区域。这一整条 28 长的向量序列，就是一次 forward 真正喂给 Qwen2.5 的“音频输入”。</p> <p>所以“15 帧”“22 帧”“4 帧”不是抽象的模型 token 数，而是明确的时间预算：每帧固定对应 133.3 ms 音频。仓库的 <code class="language-plaintext highlighter-rouge">ChunkGeometry.from_pretrained()</code> 会从 checkpoint 的 <code class="language-plaintext highlighter-rouge">preprocessor_config.json</code> 读取 <code class="language-plaintext highlighter-rouge">chunk_frames</code> 和 <code class="language-plaintext highlighter-rouge">lookahead_frames</code>（7B、22-frame 配置读到的正是 <code class="language-plaintext highlighter-rouge">22</code> 和 <code class="language-plaintext highlighter-rouge">4</code>），这不是配置洁癖——训练分布已经绑定了窗口长度和边界语义，拿 15-frame 模型按 22-frame 的节奏运行，即使 tensor shape 不报错，也已经改变了模型学到的输入输出契约。</p> <h2 id="4-说话人怎么区分先看训练目标怎么构建再看模型怎么用它">4. 说话人怎么区分：先看训练目标怎么构建，再看模型怎么用它</h2> <p>这一节要拆两个问题：一个 chunk 里出现两个甚至更多说话人，文本是怎么组织的；模型又是凭什么把新出现的声音和几分钟前的某个编号对上。前一半是可以精确复现的机械规则，后一半是训练出来的能力，没有 7B 权重没法真的跑一遍“模型认出了谁”，但可以把它学习时看到的监督信号原原本本摆出来。</p> <p><strong>先看规则。</strong> 论文 3.2 节给的词级切分规则是：先用 Qwen3-ForcedAligner-0.6B（一个独立的强制对齐小模型，只在构造训练目标时用，推理时不需要）跑出每个词的结束时间，再判断——“一个词属于 chunk k，如果它的结束时间落在 chunk k 的边界之前，允许 0.1 秒容差”。这个规则可以直接用代码复现，不需要真实模型：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">CHUNK_DUR</span><span class="p">,</span> <span class="n">TOLERANCE</span> <span class="o">=</span> <span class="mf">2.9333</span><span class="p">,</span> <span class="mf">0.1</span>   <span class="c1"># 22 帧 chunk，7B/22-frame 配置的真实时长
</span>
<span class="k">def</span> <span class="nf">assign_chunk</span><span class="p">(</span><span class="n">word_end_time</span><span class="p">,</span> <span class="n">chunk_dur</span><span class="o">=</span><span class="n">CHUNK_DUR</span><span class="p">,</span> <span class="n">tol</span><span class="o">=</span><span class="n">TOLERANCE</span><span class="p">):</span>
    <span class="n">k</span> <span class="o">=</span> <span class="mi">0</span>
    <span class="k">while</span> <span class="n">word_end_time</span> <span class="o">&gt;</span> <span class="p">(</span><span class="n">k</span> <span class="o">+</span> <span class="mi">1</span><span class="p">)</span> <span class="o">*</span> <span class="n">chunk_dur</span> <span class="o">+</span> <span class="n">tol</span><span class="p">:</span>
        <span class="n">k</span> <span class="o">+=</span> <span class="mi">1</span>
    <span class="k">return</span> <span class="n">k</span>
</code></pre></div></div> <p>拿一段自己编的两人对话（speaker, word, 词结束时间）喂给它，再把落在同一个 chunk、同一个说话人的连续词拼成一段、说话人切换就换一行 <code class="language-plaintext highlighter-rouge">\n Speaker k:</code>——这正是附录 A 里那份解码轨迹的构造方式：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">words</span> <span class="o">=</span> <span class="p">[</span>
    <span class="p">(</span><span class="sh">"</span><span class="s">S0</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">Okay,</span><span class="sh">"</span><span class="p">,</span> <span class="mf">0.42</span><span class="p">),</span> <span class="p">(</span><span class="sh">"</span><span class="s">S0</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">let</span><span class="sh">'</span><span class="s">s</span><span class="sh">"</span><span class="p">,</span> <span class="mf">0.71</span><span class="p">),</span> <span class="p">(</span><span class="sh">"</span><span class="s">S0</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">start</span><span class="sh">"</span><span class="p">,</span> <span class="mf">1.05</span><span class="p">),</span>
    <span class="p">(</span><span class="sh">"</span><span class="s">S0</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">with</span><span class="sh">"</span><span class="p">,</span> <span class="mf">1.24</span><span class="p">),</span> <span class="p">(</span><span class="sh">"</span><span class="s">S0</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">the</span><span class="sh">"</span><span class="p">,</span> <span class="mf">1.38</span><span class="p">),</span> <span class="p">(</span><span class="sh">"</span><span class="s">S0</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">deployment</span><span class="sh">"</span><span class="p">,</span> <span class="mf">1.94</span><span class="p">),</span> <span class="p">(</span><span class="sh">"</span><span class="s">S0</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">plan.</span><span class="sh">"</span><span class="p">,</span> <span class="mf">2.31</span><span class="p">),</span>
    <span class="p">(</span><span class="sh">"</span><span class="s">S1</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">Sure.</span><span class="sh">"</span><span class="p">,</span> <span class="mf">3.62</span><span class="p">),</span> <span class="p">(</span><span class="sh">"</span><span class="s">S1</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">I</span><span class="sh">"</span><span class="p">,</span> <span class="mf">3.81</span><span class="p">),</span> <span class="p">(</span><span class="sh">"</span><span class="s">S1</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">think</span><span class="sh">"</span><span class="p">,</span> <span class="mf">4.05</span><span class="p">),</span> <span class="p">(</span><span class="sh">"</span><span class="s">S1</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">we</span><span class="sh">"</span><span class="p">,</span> <span class="mf">4.20</span><span class="p">),</span>
    <span class="p">(</span><span class="sh">"</span><span class="s">S1</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">should</span><span class="sh">"</span><span class="p">,</span> <span class="mf">4.48</span><span class="p">),</span> <span class="p">(</span><span class="sh">"</span><span class="s">S1</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">first</span><span class="sh">"</span><span class="p">,</span> <span class="mf">4.79</span><span class="p">),</span> <span class="p">(</span><span class="sh">"</span><span class="s">S1</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">verify</span><span class="sh">"</span><span class="p">,</span> <span class="mf">5.21</span><span class="p">),</span> <span class="p">(</span><span class="sh">"</span><span class="s">S1</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">the</span><span class="sh">"</span><span class="p">,</span> <span class="mf">5.38</span><span class="p">),</span>
    <span class="p">(</span><span class="sh">"</span><span class="s">S1</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">latency</span><span class="sh">"</span><span class="p">,</span> <span class="mf">5.86</span><span class="p">),</span> <span class="p">(</span><span class="sh">"</span><span class="s">S1</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">on</span><span class="sh">"</span><span class="p">,</span> <span class="mf">6.62</span><span class="p">),</span> <span class="p">(</span><span class="sh">"</span><span class="s">S1</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">the</span><span class="sh">"</span><span class="p">,</span> <span class="mf">6.75</span><span class="p">),</span> <span class="p">(</span><span class="sh">"</span><span class="s">S1</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">meeting</span><span class="sh">"</span><span class="p">,</span> <span class="mf">7.10</span><span class="p">),</span> <span class="p">(</span><span class="sh">"</span><span class="s">S1</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">set.</span><span class="sh">"</span><span class="p">,</span> <span class="mf">7.43</span><span class="p">),</span>
    <span class="p">(</span><span class="sh">"</span><span class="s">S0</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">Agreed.</span><span class="sh">"</span><span class="p">,</span> <span class="mf">8.05</span><span class="p">),</span> <span class="p">(</span><span class="sh">"</span><span class="s">S0</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">We</span><span class="sh">"</span><span class="p">,</span> <span class="mf">8.21</span><span class="p">),</span> <span class="p">(</span><span class="sh">"</span><span class="s">S0</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">can</span><span class="sh">"</span><span class="p">,</span> <span class="mf">8.39</span><span class="p">),</span> <span class="p">(</span><span class="sh">"</span><span class="s">S0</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">run</span><span class="sh">"</span><span class="p">,</span> <span class="mf">8.58</span><span class="p">),</span>
    <span class="p">(</span><span class="sh">"</span><span class="s">S0</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">that</span><span class="sh">"</span><span class="p">,</span> <span class="mf">8.79</span><span class="p">),</span> <span class="p">(</span><span class="sh">"</span><span class="s">S0</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">today.</span><span class="sh">"</span><span class="p">,</span> <span class="mf">9.24</span><span class="p">),</span>
<span class="p">]</span>
<span class="n">chunks</span> <span class="o">=</span> <span class="p">{}</span>
<span class="k">for</span> <span class="n">spk</span><span class="p">,</span> <span class="n">w</span><span class="p">,</span> <span class="n">end_t</span> <span class="ow">in</span> <span class="n">words</span><span class="p">:</span>
    <span class="n">chunks</span><span class="p">.</span><span class="nf">setdefault</span><span class="p">(</span><span class="nf">assign_chunk</span><span class="p">(</span><span class="n">end_t</span><span class="p">),</span> <span class="p">[]).</span><span class="nf">append</span><span class="p">((</span><span class="n">spk</span><span class="p">,</span> <span class="n">w</span><span class="p">))</span>
</code></pre></div></div> <p>跑出来的结果是：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>chunk  0: " \n Speaker 0:Okay, let's start with the deployment plan."
chunk  1: " \n Speaker 1:Sure. I think we should first verify the latency"
chunk  2: " \n Speaker 1:on the meeting set. \n Speaker 0:Agreed. We can run that"
chunk  3: " \n Speaker 0:today."
</code></pre></div></div> <p>chunk 2 正好落在 Speaker 1 说完“set.”、Speaker 0 又接上“Agreed.”的位置，于是一个 chunk 里出现了两段、换了一次标签——这就是“一个 chunk 里有多个说话人”在训练数据层面的样子：不是靠额外规则判断，纯粹是词的结束时间落到了哪个 2.9333 秒的区间里。如果某个 chunk 区间里恰好没有任何词落进去（比如长时间沉默），这条规则会自然产出一个空字符串，对应论文附录 A 里 <code class="language-plaintext highlighter-rouge">chunk 4 : ""</code> 那一行；训练时 <code class="language-plaintext highlighter-rouge">&lt;|text_chunk_end|&gt;</code> 在这种空 chunk 上照样被监督，模型因此学到的是“每个 chunk 都要给一个判断，哪怕是空的”，而不是“遇到没内容的块就跳过”。</p> <p><strong>再看模型怎么用它。</strong> 编号分配的规则很直接：按第一次出现的顺序编号（Speaker 0、Speaker 1……），后面同一个人再开口，继续用这个编号，不重新分配，也没有真实姓名。支撑这件事的完全是“历史文字仍留在上下文里”：LLM 在预测第 12 个 chunk 该写 Speaker 几时，能看到前面 11 个 chunk 里每一句话对应的音频和文字（第 2 节的交错序列），可以综合声学线索（音色、语调，来自 Acoustic tokenizer 的特征）、语言线索（措辞、话题连续性）和对话结构（轮次切换的位置）一起判断——本质上是一次 next-token 预测，而不是提取一个声纹向量去缓存里找最近邻。这个判断能力是训练出来的：模型在约 42 万小时（Stage 2）加约 1.3 万小时（Stage 3）的多说话人语音上，被反复要求在千变万化的声音、话题、轮次组合里做出这个判断，没有一个显式的“相似度阈值”或“聚类算法”可以写成伪代码，这也是为什么这一段没法像上面的切分规则一样简单复现——它是 28 层 Transformer 权重里的东西，不是一段可以抽出来单独运行的逻辑。</p> <p>那两个人真的同时开口呢？论文没有回避，明确列为限制：输出终究是一条文本流，解码器必须把重叠语音串行化成前后相继的片段，不能并行输出两条流。Limitations 一节原话是“长时间重叠会让性能下降，因为解码器必须把重叠语音串行化进单一输出流”，并把 separation-aware 表示或 multi-stream 解码列为未来工作。训练数据里合成的短暂多人重叠（抢话、附和，第 7 节会提到怎么合成）能让模型学到一个说得过去的顺序，但长时间、多人持续同时说话，不是这篇论文已经解决的问题。</p> <h2 id="5-look-ahead-和更新粒度一次-forward-到底吃进多少音频多久才提交一次">5. Look-ahead 和更新粒度：一次 forward 到底吃进多少音频，多久才提交一次</h2> <p>假设当前输出覆盖 0.0–2.93 秒。若模型只看到这段音频，2.93 秒处的“我觉得这个方案……”可能有三种后续：下一句是“可以”、是“不行”，或者说话人已经换了。没有边界右侧的证据，系统必须在最不确定的位置提前做决定。VibeVoice 的做法是：在生成当前块的文字前，再读取未来 $L=4$ 个 latent frame，对应约 0.5 秒：</p> \[T_{\text{lookahead}}=4\times\frac{3200}{24000}\approx0.5\text{ s}\] <p>这半秒<strong>只用于决定上一块如何结束、以及边界附近的说话人归属</strong>，不会把未来半秒的文字提前输出。真实的窗口滑动关系用第 3 节确认过的 <code class="language-plaintext highlighter-rouge">chunk_frames=22</code>、<code class="language-plaintext highlighter-rouge">lookahead_frames=4</code>、<code class="language-plaintext highlighter-rouge">frame_samples=3200</code>、<code class="language-plaintext highlighter-rouge">sample_rate=24000</code> 这几个数字，直接可以算出来，不用猜：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">FRAME_DUR</span> <span class="o">=</span> <span class="mi">3200</span> <span class="o">/</span> <span class="mi">24000</span>                 <span class="c1"># 0.1333 s / 帧
</span><span class="n">CHUNK_DUR</span> <span class="o">=</span> <span class="mi">22</span> <span class="o">*</span> <span class="n">FRAME_DUR</span>               <span class="c1"># 2.9333 s，本轮新增音频
</span><span class="n">LOOKAHEAD_DUR</span> <span class="o">=</span> <span class="mi">4</span> <span class="o">*</span> <span class="n">FRAME_DUR</span>            <span class="c1"># 0.5333 s，前瞻
</span><span class="n">WINDOW_DUR</span> <span class="o">=</span> <span class="n">CHUNK_DUR</span> <span class="o">+</span> <span class="n">LOOKAHEAD_DUR</span>    <span class="c1"># 3.4667 s，单次 forward 真正吃进的音频
</span>
<span class="n">t</span> <span class="o">=</span> <span class="mf">0.0</span>
<span class="k">for</span> <span class="n">chunk_idx</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">4</span><span class="p">):</span>
    <span class="n">window</span> <span class="o">=</span> <span class="p">(</span><span class="n">t</span><span class="p">,</span> <span class="nf">min</span><span class="p">(</span><span class="n">t</span> <span class="o">+</span> <span class="n">WINDOW_DUR</span><span class="p">,</span> <span class="mf">10.0</span><span class="p">))</span>
    <span class="n">commit</span> <span class="o">=</span> <span class="p">(</span><span class="n">t</span><span class="p">,</span> <span class="nf">min</span><span class="p">(</span><span class="n">t</span> <span class="o">+</span> <span class="n">CHUNK_DUR</span><span class="p">,</span> <span class="mf">10.0</span><span class="p">))</span>
    <span class="nf">print</span><span class="p">(</span><span class="n">chunk_idx</span><span class="p">,</span> <span class="sh">"</span><span class="s">窗口</span><span class="sh">"</span><span class="p">,</span> <span class="n">window</span><span class="p">,</span> <span class="sh">"</span><span class="s">提交</span><span class="sh">"</span><span class="p">,</span> <span class="n">commit</span><span class="p">)</span>
    <span class="n">t</span> <span class="o">+=</span> <span class="n">CHUNK_DUR</span>   <span class="c1"># 注意：往前推进的是 CHUNK_DUR，不是 WINDOW_DUR
</span></code></pre></div></div> <p>对一段 10 秒音频跑出来是：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>chunk |  窗口起点  |  窗口终点  |     本轮真正提交的时间段
    0 |    0.000  |    3.467  | [0.000, 2.933)
    1 |    2.933  |    6.400  | [2.933, 5.867)
    2 |    5.867  |    9.333  | [5.867, 8.800)
    3 |    8.800  |   10.000  | [8.800, 10.000)
</code></pre></div></div> <p>这张表把“窗口”和“时间推进”的区别摆得很清楚：chunk 0 的窗口一直读到 3.467 秒，但它只对 [0.000, 2.933) 这段负责；chunk 1 的窗口从 2.933 秒开始——正是 chunk 0 窗口的末尾——往后再读到 6.400 秒，其中 [2.933, 3.467) 这 0.533 秒的音频，chunk 0 已经拿它当过一次“前瞻”，chunk 1 会把它当作正式内容重新处理一遍。窗口有重叠，时间边界不重叠，模型因此能看到边界右侧的证据，又不会把同一段声音的文字交付两次。</p> <p>前瞻不是越大越好，也不是免费的。固定 22-frame、7B 模型测试 $L=0,2,4$ 时，每增加 2 帧要多付出 0.267 秒期望延迟；五套评测的平均 WER/CER（不考虑说话人的词/字错误率，越低越好）从 27.18 降到 25.91、再到 24.66，cpWER/cpCER（在此基础上还考察话有没有挂到正确说话人名下，同样越低越好）从 35.88 降到 34.09、再到 31.55。</p> <p><img src="/assets/img/posts/20260907-vibevoice/table7_p10.png" alt="前瞻长度的消融结果"/></p> <p>读这张表时先看表头：横向三组分别是 $L=0$、$L=2$、$L=4$，括号里的 1.47 s、1.73 s、2.00 s 是对应的期望延迟；每组里面左列是普通 WER，右列是带说话人归属的 cpWER。底部的 <code class="language-plaintext highlighter-rouge">Δ vs. previous column</code> 直接给出每增加两帧带来多少改善：第一步（0→2 帧）WER/CER 改善 1.27、cpWER/cpCER 改善 1.80；第二步（2→4 帧）分别是 1.24 和 2.54。最后两帧对 cp 指标的改善比对普通识别的改善更大，而且是持续上升而非收敛，这支持一个具体判断：<strong>边界右侧的信息对“谁在说”尤其重要，而且在 $L=4$ 处仍未见顶</strong>，这也是发布配置选择读 4 帧而不是更少的原因。</p> <p>再说更新粒度：这个粒度不在模型内部——LLM 内部确实是逐 token 自回归生成的——而在下游每次能拿到一批新结果的边界在哪，这个边界就是上面表里“提交”那一列的宽度，2.9333 秒一格，不会更细。在一个 chunk 的窗口还没读完之前，模型不会开始生成这个 chunk 对应的文字，生成过程中也不会把这段文字拆成“先给一半、再补一半”的中间结果——解码到 <code class="language-plaintext highlighter-rouge">&lt;|text_chunk_end|&gt;</code> 才算这一块完成。首包（第一次看到任何输出）要等满整个窗口，也就是表里 chunk 0 的 3.467 秒，论文按帧数取整报告为 3.5 秒；后续每一轮的刷新间隔固定是 2.9333 秒，但论文报的 2.00 秒“期望算法延迟”不是刷新间隔，而是一个词从被说出到它的说话人标签稳定下来、平均要经过的时间——一个词可能落在 chunk 中间任意位置，平均要等半个 chunk（$C/2$）才能被这一块的生成覆盖到，再加上 0.5 秒前瞻，所以是 $C/2+T_{\text{lookahead}}$，不是 $C$ 本身。</p> <p>这个刷新粒度是训练和推理都固定死的 chunk contract，不是可以在推理时随意调小的旁路参数：15-frame 和 22-frame 是两个独立训练出来的 checkpoint，各自的 <code class="language-plaintext highlighter-rouge">preprocessor_config.json</code> 记着不同的 <code class="language-plaintext highlighter-rouge">chunk_frames</code>，粒度变了，模型学到的边界语义也变了。服务端的解码速度也不是瓶颈：7B 模型 15-frame 配置下，单块 decode 只需 146–208 ms，远小于 2000 ms 的 chunk 时长，real-time factor（处理耗时除以音频时长，低于 1 才能跟上实时输入）在 0.073–0.104 之间。真正决定“多久能看到新结果”的是 chunk + look-ahead 这个协议本身，不是算力。</p> <h2 id="6-一次提交不再修改kv-缓存怎么长以及和-google-stt-的-d0dfin-对比">6. 一次提交、不再修改：K/V 缓存怎么长，以及和 Google STT 的 D0/Dfin 对比</h2> <p>论文里出现过“revise”这个词，但主语不是 VibeVoice-ASR-Streaming，而是作为对比基线的 Google Cloud Speech-to-Text，这一点值得单独说清楚，因为容易被读成“这篇论文的模型会先给一个粗糙结果，再自己回头纠错”——实际情况正好相反，而且“不纠错”不是一句设计口号，是可以在 K/V 缓存的增长方式上直接看到的机械事实。</p> <p>先看这个机械事实。仓库里 <code class="language-plaintext highlighter-rouge">streaming_generate</code> 的每一轮都在做同一件事：把这一轮的 <code class="language-plaintext highlighter-rouge">audio_embeds</code> 追加到 <code class="language-plaintext highlighter-rouge">past_key_values</code> 后面，逐 token 生成直到 <code class="language-plaintext highlighter-rouge">&lt;|text_chunk_end|&gt;</code>，再把这个结束符也追加进去——全程只有追加，没有任何一步会回头修改前面已经写入缓存的位置。用一个 2 层的玩具 transformer（参数随机初始化，没有下载 7B 权重，所以解码出来的 token 没有语言意义，这里只演示缓存怎么增长）可以把这件事跑出来看：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">torch</span><span class="p">,</span> <span class="n">torch</span><span class="p">.</span><span class="n">nn</span> <span class="k">as</span> <span class="n">nn</span><span class="p">,</span> <span class="n">math</span>

<span class="n">HIDDEN</span><span class="p">,</span> <span class="n">N_HEADS</span><span class="p">,</span> <span class="n">VOCAB</span> <span class="o">=</span> <span class="mi">64</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="mi">200</span>

<span class="k">class</span> <span class="nc">TinyCausalBlock</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">head_dim</span> <span class="o">=</span> <span class="n">HIDDEN</span> <span class="o">//</span> <span class="n">N_HEADS</span>
        <span class="n">self</span><span class="p">.</span><span class="n">qkv</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">HIDDEN</span><span class="p">,</span> <span class="n">HIDDEN</span> <span class="o">*</span> <span class="mi">3</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">out</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">HIDDEN</span><span class="p">,</span> <span class="n">HIDDEN</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">norm</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">RMSNorm</span><span class="p">(</span><span class="n">HIDDEN</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">,</span> <span class="n">past_kv</span><span class="o">=</span><span class="bp">None</span><span class="p">):</span>
        <span class="n">B</span><span class="p">,</span> <span class="n">T</span><span class="p">,</span> <span class="n">_</span> <span class="o">=</span> <span class="n">x</span><span class="p">.</span><span class="n">shape</span>
        <span class="n">qkv</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">qkv</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">norm</span><span class="p">(</span><span class="n">x</span><span class="p">)).</span><span class="nf">view</span><span class="p">(</span><span class="n">B</span><span class="p">,</span> <span class="n">T</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="n">N_HEADS</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">head_dim</span><span class="p">)</span>
        <span class="n">q</span><span class="p">,</span> <span class="n">k</span><span class="p">,</span> <span class="n">v</span> <span class="o">=</span> <span class="n">qkv</span><span class="p">[:,</span> <span class="p">:,</span> <span class="mi">0</span><span class="p">],</span> <span class="n">qkv</span><span class="p">[:,</span> <span class="p">:,</span> <span class="mi">1</span><span class="p">],</span> <span class="n">qkv</span><span class="p">[:,</span> <span class="p">:,</span> <span class="mi">2</span><span class="p">]</span>
        <span class="k">if</span> <span class="n">past_kv</span> <span class="ow">is</span> <span class="ow">not</span> <span class="bp">None</span><span class="p">:</span>
            <span class="n">k</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">([</span><span class="n">past_kv</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span> <span class="n">k</span><span class="p">],</span> <span class="n">dim</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>
            <span class="n">v</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">([</span><span class="n">past_kv</span><span class="p">[</span><span class="mi">1</span><span class="p">],</span> <span class="n">v</span><span class="p">],</span> <span class="n">dim</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>
        <span class="n">attn</span> <span class="o">=</span> <span class="p">(</span><span class="n">q</span><span class="p">.</span><span class="nf">transpose</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">)</span> <span class="o">@</span> <span class="n">k</span><span class="p">.</span><span class="nf">transpose</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">).</span><span class="nf">transpose</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="o">-</span><span class="mi">2</span><span class="p">))</span> <span class="o">/</span> <span class="n">math</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">head_dim</span><span class="p">)</span>
        <span class="n">o</span> <span class="o">=</span> <span class="p">(</span><span class="n">attn</span><span class="p">.</span><span class="nf">softmax</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span> <span class="o">@</span> <span class="n">v</span><span class="p">.</span><span class="nf">transpose</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">)).</span><span class="nf">transpose</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">).</span><span class="nf">reshape</span><span class="p">(</span><span class="n">B</span><span class="p">,</span> <span class="n">T</span><span class="p">,</span> <span class="n">HIDDEN</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">x</span> <span class="o">+</span> <span class="n">self</span><span class="p">.</span><span class="nf">out</span><span class="p">(</span><span class="n">o</span><span class="p">),</span> <span class="p">(</span><span class="n">k</span><span class="p">,</span> <span class="n">v</span><span class="p">)</span>

<span class="k">class</span> <span class="nc">TinyStreamingLM</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">n_layers</span><span class="o">=</span><span class="mi">2</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">embed</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Embedding</span><span class="p">(</span><span class="n">VOCAB</span><span class="p">,</span> <span class="n">HIDDEN</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">blocks</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">ModuleList</span><span class="p">([</span><span class="nc">TinyCausalBlock</span><span class="p">()</span> <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">n_layers</span><span class="p">)])</span>
        <span class="n">self</span><span class="p">.</span><span class="n">lm_head</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">HIDDEN</span><span class="p">,</span> <span class="n">VOCAB</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">inputs_embeds</span><span class="p">,</span> <span class="n">past_key_values</span><span class="o">=</span><span class="bp">None</span><span class="p">):</span>
        <span class="n">x</span><span class="p">,</span> <span class="n">new_pkv</span> <span class="o">=</span> <span class="n">inputs_embeds</span><span class="p">,</span> <span class="p">[]</span>
        <span class="k">for</span> <span class="n">i</span><span class="p">,</span> <span class="n">block</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">blocks</span><span class="p">):</span>
            <span class="n">x</span><span class="p">,</span> <span class="n">kv</span> <span class="o">=</span> <span class="nf">block</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">past_key_values</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="k">if</span> <span class="n">past_key_values</span> <span class="k">else</span> <span class="bp">None</span><span class="p">)</span>
            <span class="n">new_pkv</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">kv</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">lm_head</span><span class="p">(</span><span class="n">x</span><span class="p">),</span> <span class="n">new_pkv</span>

<span class="n">model</span> <span class="o">=</span> <span class="nc">TinyStreamingLM</span><span class="p">()</span>
<span class="n">TEXT_CHUNK_END_ID</span><span class="p">,</span> <span class="n">AUDIO_EMBEDS_PER_CHUNK</span> <span class="o">=</span> <span class="mi">199</span><span class="p">,</span> <span class="mi">28</span>   <span class="c1"># 28 = 第 3 节算出的 BOS+26帧+EOC
</span>
<span class="n">_</span><span class="p">,</span> <span class="n">past_kv</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">model</span><span class="p">.</span><span class="nf">embed</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">randint</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">VOCAB</span><span class="p">,</span> <span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">12</span><span class="p">))))</span>   <span class="c1"># 玩具 prompt
</span><span class="n">cache_len</span> <span class="o">=</span> <span class="mi">12</span>
<span class="k">for</span> <span class="n">chunk_idx</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">4</span><span class="p">):</span>
    <span class="n">logits</span><span class="p">,</span> <span class="n">past_kv</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="n">AUDIO_EMBEDS_PER_CHUNK</span><span class="p">,</span> <span class="n">HIDDEN</span><span class="p">),</span> <span class="n">past_kv</span><span class="p">)</span>
    <span class="n">cache_len</span> <span class="o">+=</span> <span class="n">AUDIO_EMBEDS_PER_CHUNK</span>
    <span class="n">frozen</span> <span class="o">=</span> <span class="n">cache_len</span>   <span class="c1"># 追加 audio_embeds 之后，这些位置的 K/V 立刻冻结
</span>    <span class="n">n_new</span> <span class="o">=</span> <span class="mi">0</span>
    <span class="n">next_logits</span> <span class="o">=</span> <span class="n">logits</span><span class="p">[:,</span> <span class="o">-</span><span class="mi">1</span><span class="p">:,</span> <span class="p">:]</span>
    <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">6</span><span class="p">):</span>
        <span class="n">next_id</span> <span class="o">=</span> <span class="nf">int</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">argmax</span><span class="p">(</span><span class="n">next_logits</span><span class="p">[</span><span class="mi">0</span><span class="p">,</span> <span class="o">-</span><span class="mi">1</span><span class="p">]))</span>
        <span class="k">if</span> <span class="n">next_id</span> <span class="o">==</span> <span class="n">TEXT_CHUNK_END_ID</span><span class="p">:</span>
            <span class="k">break</span>
        <span class="n">next_logits</span><span class="p">,</span> <span class="n">past_kv</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">model</span><span class="p">.</span><span class="nf">embed</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">tensor</span><span class="p">([[</span><span class="n">next_id</span><span class="p">]])),</span> <span class="n">past_kv</span><span class="p">)</span>
        <span class="n">cache_len</span> <span class="o">+=</span> <span class="mi">1</span>
        <span class="n">n_new</span> <span class="o">+=</span> <span class="mi">1</span>
    <span class="n">_</span><span class="p">,</span> <span class="n">past_kv</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">model</span><span class="p">.</span><span class="nf">embed</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">tensor</span><span class="p">([[</span><span class="n">TEXT_CHUNK_END_ID</span><span class="p">]])),</span> <span class="n">past_kv</span><span class="p">)</span>
    <span class="n">cache_len</span> <span class="o">+=</span> <span class="mi">1</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">chunk </span><span class="si">{</span><span class="n">chunk_idx</span><span class="si">}</span><span class="s">: cache 长度 -&gt; </span><span class="si">{</span><span class="n">cache_len</span><span class="si">}</span><span class="s">，</span><span class="sh">"</span>
          <span class="sa">f</span><span class="sh">"</span><span class="s">其中前 </span><span class="si">{</span><span class="n">frozen</span> <span class="o">-</span> <span class="n">AUDIO_EMBEDS_PER_CHUNK</span><span class="si">}</span><span class="s"> 个位置在这一轮里完全没被重新计算</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <p>跑出来是：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>prompt 之后：past_key_values 长度 = 12
chunk 0: 喂入 audio_embeds 后 cache 长度 = 40  (新增 28)
        生成 6 个文本 token + 1 个 &lt;|text_chunk_end|&gt;，cache 长度变为 47
        —— 前面 12 个位置的 K/V 完全没有被重新计算，chunk 0 一旦写完就冻结了
chunk 1: 喂入 audio_embeds 后 cache 长度 = 75  (新增 28)
        生成 6 个文本 token + 1 个 &lt;|text_chunk_end|&gt;，cache 长度变为 82
        —— 前面 47 个位置的 K/V 完全没有被重新计算，chunk 1 一旦写完就冻结了
chunk 2: 喂入 audio_embeds 后 cache 长度 = 110  (新增 28)
        生成 6 个文本 token + 1 个 &lt;|text_chunk_end|&gt;，cache 长度变为 117
        —— 前面 82 个位置的 K/V 完全没有被重新计算，chunk 2 一旦写完就冻结了
chunk 3: 喂入 audio_embeds 后 cache 长度 = 145  (新增 28)
        生成 6 个文本 token + 1 个 &lt;|text_chunk_end|&gt;，cache 长度变为 152
        —— 前面 117 个位置的 K/V 完全没有被重新计算，chunk 3 一旦写完就冻结了
</code></pre></div></div> <p>这就是“一次提交、不再修改”的字面意思：<code class="language-plaintext highlighter-rouge">Speaker k:</code> 这个标签一旦作为某个 token 的 embedding 被写进 <code class="language-plaintext highlighter-rouge">past_key_values</code>，它对应的 K/V 向量在后续所有轮次里只会被新 token 拿来做 attention 的“被查询对象”，永远不会被重新计算或覆盖。想要修改一个已经提交的标签，唯一的办法是把那部分缓存整个扔掉、从那个位置重新跑一遍前向——而这正是 VibeVoice-ASR-Streaming 的推理循环里没有写的一条分支。</p> <p>对比一下 Google Cloud Speech-to-Text 的做法，就能看出“不写这条分支”省掉的是什么。Google STT 在实时转写时会先给一个说话人标签，记作 <strong>D0</strong>（首次输出时的标签）；等整段录音处理完，再基于全局信息重新聚类、修正标签，记作 <strong>Dfin</strong>（最终修订标签）——这本质上就是上面那个“把缓存扔掉重算”的操作，只是发生在一个显式的、独立的后处理阶段。论文测出来这个“回头改”的代价相当大：文字大约 1.5 秒就基本稳定，但说话人标签的中位数还要再改 <strong>16.5 到 31.3 秒</strong>才收敛；<strong>42.6%–67.9%</strong> 的词，标签在首次输出后至少被改过一次；哪怕给 30 秒的“修正预算”，也只有 66.4%（AMI-IHM）、66.8%（AMI-SDM）、78.4%（MLC 英语部分）的标签能追上 Dfin 的最终结果。论文汇总表里给出的整体平均延迟是：Azure Conversation Transcriber 8.21 秒，Google STT 的 D0 为 9.12 秒、Dfin 为 <strong>51.06 秒</strong>，VibeVoice 为 2.00 秒——这 51 秒不是录音本身变长，而是标签修正过程本身消耗的时间。修正带来的准确率差距也很直接：用 D0 算英语 cpWER 是 60.57，等到 Dfin 收敛之后，同样的文字配上修正过的标签，cpWER 降到 28.40——差距接近一半。</p> <p><img src="/assets/img/posts/20260907-vibevoice/table2_full.png" alt="完整的实时说话人结果"/></p> <p>这张表需要先看三组系统列：Azure CT 只有一组 WER/CER 和 cpWER/cpCER；Google STT 中间是 D0 对应的 WER/cpWER，右侧多给出 Dfin 对应的 cpWER；VibeVoice 在最右侧同时报告普通识别和说话人归属。最上面的 <code class="language-plaintext highlighter-rouge">Avg. latency</code> 一行就是刚才那几个数字。VibeVoice 在能对比的设置上（比如 AMI-IHM cpWER 27.48、AMI-SDM cpWER 39.01）仍优于或接近 Google STT 的 Dfin，同时把整体延迟从 9–51 秒压到 2 秒左右——不是“因为不纠错所以更快但更不准”，而是在能对比的设置里数字反而更好，这正是论文强调 13 项设置里 12 项 best-or-tied-best 的来源之一。</p> <p>这也解释了论文另一组实验：把 speaker label 放在片段文字前面（Head，发布配置）和放在末尾（Tail）对比。如果标签完全依赖“攒够声音再决定”的声纹聚类逻辑，等整段说完再输出似乎应该更有利；但结果是五套数据平均 cpWER/cpCER 几乎一样，31.55 对 31.56，Tail 的普通 WER/CER 反而高 0.98 个点。</p> <p><img src="/assets/img/posts/20260907-vibevoice/table6_p9.png" alt="说话人标签位置的对比"/></p> <p>表头的 <code class="language-plaintext highlighter-rouge">Head</code> 表示先输出 <code class="language-plaintext highlighter-rouge">Speaker k:</code> 再输出这段话，<code class="language-plaintext highlighter-rouge">Tail</code> 表示先输出文字、等片段结束后再补标签；最右侧的 $\Delta$ 是 Tail 减 Head，正数代表尾置更差。逐行看方向并不完全一致（AMI-SDM 的 cpWER 尾置反而低 2.49），最终五集平均几乎抵消。这和上面 K/V 缓存的事实是同一回事：模型的说话人判断从一开始就同时依赖声学证据和文字/对话上下文，不是一个先攒够声音再决定的纯声纹聚类过程，所以没有必要等，也没有“等了会更准”的空间。</p> <h2 id="7-训练怎么让模型学会这套协议">7. 训练怎么让模型学会这套协议</h2> <p>如果只在推理时切音频，模型并不会自动学会“每个块输出到这里就停”，也不会自动学会“看到新块也要保持标签一致”。因此作者不只是改变推理脚本，而是分三阶段组织训练样本：</p> <ol> <li><strong>Stage 1（非流式训练）</strong>：模型先在离线 speaker-attributed 设定下训练——完整录音在生成前整段可见。这一步建立基本的多说话人识别和归属能力，不带任何流式约束，对应的正是先前发布的 VibeVoice-ASR（[PYC+26]）：论文里 Table 4 拿来对比的 non-streaming VibeVoice-ASR checkpoint，就是这一阶段（同规模）的产物，后续两阶段都从它初始化，不需要从零训练。</li> <li><strong>Stage 2（streaming pre-training）</strong>：从 Stage 1 checkpoint 初始化，把样本组织成 $X_0,Y_0,X_1,Y_1,\dots$，每个音频块带固定前瞻；架构、可训练模块和训练目标都和 Stage 1 相同，只是输入组织方式变了——也就是第 4 节那段切分代码产出的格式。使用约 42 万小时中英文语音（取自 Stage 1 语料的子集），让这种交错格式成为模型的默认工作模式。</li> <li><strong>Stage 3（streaming fine-tuning）</strong>：用约 1.3 万小时精选、合成和公开数据，收敛转写规范、Speaker 编号一致性和热词遵循等用户可见行为。</li> </ol> <p>训练目标中的关键 token 是 <code class="language-plaintext highlighter-rouge">&lt;|text_chunk_end|&gt;</code>（真实 id 是 151665，来自released tokenizer 的 <code class="language-plaintext highlighter-rouge">added_tokens.json</code>）。每个 $Y_k$ 最后都监督它，包括第 4 节提到的静音、目标文本为空的 chunk。生成到这个 token，模型就向音频流发出“本块完成”的信号，系统才追加下一块音频；输出长度没有预先给定，何时停止本身是模型要学习的预测。</p> <p>第 4 节的合成数据部分（50,884 段录音、共 4,519.6 小时增强多说话人语音）额外加入专业术语、口语读法与书面规范的差异、多人 overlap 和 room impulse response（模拟房间混响与麦克风响应），确保“重叠语音怎么排序”这件事也有真实的监督来源，第 4 节说的短暂重叠处理能力正是从这里来的。</p> <h2 id="8-源码里的一个-turn真正的-7b-模型上跑的是同一套逻辑">8. 源码里的一个 turn：真正的 7B 模型上跑的是同一套逻辑</h2> <p>上一节的玩具模型演示的是缓存怎么增长；仓库里 <code class="language-plaintext highlighter-rouge">streaming_generate</code> 跑在真正的 Qwen2.5 backbone 上，逻辑和玩具版一一对应，只是 <code class="language-plaintext highlighter-rouge">past_key_values</code> 里装的是第 3 节算出来的 3584 维真实向量：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># vibevoice/modular/modeling_vibevoice_asr.py
# class VibeVoiceASRForConditionalGeneration
# def streaming_generate(...)，约第 529 行
</span><span class="k">for</span> <span class="n">chunk_idx</span><span class="p">,</span> <span class="n">feat_chunk</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">feature_chunks</span><span class="p">):</span>
    <span class="n">audio_embeds</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">([</span><span class="n">sp_start_embed</span><span class="p">,</span> <span class="n">feat_chunk</span><span class="p">,</span> <span class="n">sp_end_embed</span><span class="p">],</span> <span class="n">dim</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>
    <span class="n">outputs</span> <span class="o">=</span> <span class="nf">self</span><span class="p">(</span><span class="n">inputs_embeds</span><span class="o">=</span><span class="n">audio_embeds</span><span class="p">,</span>
                   <span class="n">past_key_values</span><span class="o">=</span><span class="n">past_key_values</span><span class="p">,</span> <span class="n">use_cache</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
                   <span class="n">return_dict</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
    <span class="n">past_key_values</span> <span class="o">=</span> <span class="n">outputs</span><span class="p">.</span><span class="n">past_key_values</span>
    <span class="n">next_logits</span> <span class="o">=</span> <span class="n">outputs</span><span class="p">.</span><span class="n">logits</span><span class="p">[:,</span> <span class="o">-</span><span class="mi">1</span><span class="p">:,</span> <span class="p">:]</span>

    <span class="n">chunk_tokens</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">max_new_tokens_per_chunk</span><span class="p">):</span>
        <span class="n">next_token_id</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">argmax</span><span class="p">(</span><span class="n">next_logits</span><span class="p">[:,</span> <span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="p">:],</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">).</span><span class="nf">item</span><span class="p">()</span>
        <span class="k">if</span> <span class="n">next_token_id</span> <span class="o">==</span> <span class="n">text_chunk_end_id</span> <span class="ow">or</span> <span class="n">next_token_id</span> <span class="o">==</span> <span class="n">eos_id</span><span class="p">:</span>
            <span class="k">break</span>
        <span class="n">chunk_tokens</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">next_token_id</span><span class="p">)</span>
        <span class="n">next_embed</span> <span class="o">=</span> <span class="nf">embed_tokens</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">tensor</span><span class="p">([[</span><span class="n">next_token_id</span><span class="p">]],</span> <span class="n">device</span><span class="o">=</span><span class="n">device</span><span class="p">))</span>
        <span class="n">outputs</span> <span class="o">=</span> <span class="nf">self</span><span class="p">(</span><span class="n">inputs_embeds</span><span class="o">=</span><span class="n">next_embed</span><span class="p">,</span>
                       <span class="n">past_key_values</span><span class="o">=</span><span class="n">past_key_values</span><span class="p">,</span> <span class="n">use_cache</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
                       <span class="n">return_dict</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
        <span class="n">past_key_values</span> <span class="o">=</span> <span class="n">outputs</span><span class="p">.</span><span class="n">past_key_values</span>
        <span class="n">next_logits</span> <span class="o">=</span> <span class="n">outputs</span><span class="p">.</span><span class="n">logits</span>

    <span class="n">tce_embed</span> <span class="o">=</span> <span class="nf">embed_tokens</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">tensor</span><span class="p">([[</span><span class="n">text_chunk_end_id</span><span class="p">]],</span> <span class="n">device</span><span class="o">=</span><span class="n">device</span><span class="p">))</span>
    <span class="n">outputs</span> <span class="o">=</span> <span class="nf">self</span><span class="p">(</span><span class="n">inputs_embeds</span><span class="o">=</span><span class="n">tce_embed</span><span class="p">,</span>
                   <span class="n">past_key_values</span><span class="o">=</span><span class="n">past_key_values</span><span class="p">,</span> <span class="n">use_cache</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
                   <span class="n">return_dict</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
    <span class="n">past_key_values</span> <span class="o">=</span> <span class="n">outputs</span><span class="p">.</span><span class="n">past_key_values</span>
    <span class="k">yield</span> <span class="n">chunk_idx</span><span class="p">,</span> <span class="n">total_chunks</span><span class="p">,</span> <span class="n">tokenizer</span><span class="p">.</span><span class="nf">decode</span><span class="p">(</span><span class="n">chunk_tokens</span><span class="p">,</span> <span class="n">skip_special_tokens</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">feat_chunk</code> 就是第 3 节 <code class="language-plaintext highlighter-rouge">encode_speech</code> 算出来的 <code class="language-plaintext highlighter-rouge">combined_features</code>，形状是 <code class="language-plaintext highlighter-rouge">[batch, window_frames, 3584]</code>；<code class="language-plaintext highlighter-rouge">sp_start_embed</code>、<code class="language-plaintext highlighter-rouge">sp_end_embed</code> 是 <code class="language-plaintext highlighter-rouge">&lt;|object_ref_start|&gt;</code>（151646）和 <code class="language-plaintext highlighter-rouge">&lt;|object_ref_end|&gt;</code>（151647）这两个复用自 Qwen2.5-VL 词表的边界 token 各自查表得到的 embedding。<code class="language-plaintext highlighter-rouge">past_key_values</code> 里已有提示词和此前所有轮次；输出是 generator 每轮 yield 的一段 chunk 文本，不是整篇最终稿。最后那次 <code class="language-plaintext highlighter-rouge">tce_embed</code> 对应的 forward 看起来像多余的 bookkeeping，实际上很关键：<code class="language-plaintext highlighter-rouge">&lt;|text_chunk_end|&gt;</code> 必须进入下一轮上下文，否则序列会和训练格式错位，第 6 节的玩具版里同样保留了这一步。</p> <h2 id="9-vllm-服务怎样避免保留历史变成重复计算">9. vLLM 服务怎样避免“保留历史”变成重复计算</h2> <p>参考实现手里有显式 K/V 缓存；vLLM 的服务接口则通常是一轮一个 request，没有直接暴露“把我的 K/V 缓存拿回来继续 append”的 API。仓库因此采用 automatic prefix caching：每轮重建到目前为止的 token prompt，依靠完全相同的前缀命中缓存。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># vllm_plugin/asr_streaming_server.py
# class StreamingSession
# async def push(self, window)，约第 275 行
</span><span class="n">self</span><span class="p">.</span><span class="n">_windows</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">window</span><span class="p">)</span>
<span class="n">tokens</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">_tokens</span> <span class="o">+</span> <span class="p">[</span><span class="n">self</span><span class="p">.</span><span class="n">audio_token_id</span><span class="p">]</span>
<span class="n">k</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">_windows</span><span class="p">)</span> <span class="o">-</span> <span class="mi">1</span>

<span class="c1"># 已处理窗口不重复传波形；uuid 必须保持不变。
</span><span class="n">uuids</span> <span class="o">=</span> <span class="p">[</span><span class="sa">f</span><span class="sh">"</span><span class="si">{</span><span class="n">self</span><span class="p">.</span><span class="n">session_id</span><span class="si">}</span><span class="s">-</span><span class="si">{</span><span class="n">i</span><span class="si">}</span><span class="sh">"</span> <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">k</span> <span class="o">+</span> <span class="mi">1</span><span class="p">)]</span>
<span class="n">audio</span> <span class="o">=</span> <span class="p">[</span><span class="bp">None</span><span class="p">]</span> <span class="o">*</span> <span class="n">k</span> <span class="o">+</span> <span class="p">[</span><span class="n">window</span><span class="p">]</span>
<span class="n">out</span> <span class="o">=</span> <span class="k">await</span> <span class="n">self</span><span class="p">.</span><span class="n">engine</span><span class="p">.</span><span class="nf">generate</span><span class="p">(</span><span class="n">tokens</span><span class="p">,</span> <span class="n">audio</span><span class="p">,</span> <span class="n">uuids</span><span class="p">,</span>
                                 <span class="n">self</span><span class="p">.</span><span class="n">sampling_params</span><span class="p">,</span> <span class="n">req_id</span><span class="p">)</span>

<span class="n">gen</span> <span class="o">=</span> <span class="nf">list</span><span class="p">(</span><span class="n">out</span><span class="p">.</span><span class="n">outputs</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">token_ids</span><span class="p">)</span>
<span class="k">while</span> <span class="n">gen</span> <span class="ow">and</span> <span class="n">gen</span><span class="p">[</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span> <span class="o">==</span> <span class="n">self</span><span class="p">.</span><span class="n">text_chunk_end_id</span><span class="p">:</span>
    <span class="n">gen</span><span class="p">.</span><span class="nf">pop</span><span class="p">()</span>

<span class="c1"># stop token 不回显，但要手动补到下一轮 prompt。
</span><span class="n">self</span><span class="p">.</span><span class="n">_tokens</span> <span class="o">=</span> <span class="n">tokens</span> <span class="o">+</span> <span class="n">gen</span> <span class="o">+</span> <span class="p">[</span><span class="n">self</span><span class="p">.</span><span class="n">text_chunk_end_id</span><span class="p">]</span>
<span class="k">return</span> <span class="nf">_strip_specials</span><span class="p">(</span><span class="n">out</span><span class="p">.</span><span class="n">outputs</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">text</span><span class="p">)</span>
</code></pre></div></div> <p>这个函数的输入 <code class="language-plaintext highlighter-rouge">window</code> 是当前新到的一段 PCM 音频，返回值是当前 chunk 的可显示文本；<code class="language-plaintext highlighter-rouge">self._tokens</code> 和 <code class="language-plaintext highlighter-rouge">self._windows</code> 则留给下一轮。实现里有三个容易被忽略的约束：</p> <ol> <li><code class="language-plaintext highlighter-rouge">audio = [None] * k + [window]</code> 让旧窗口不必随每轮请求重复上传；UUID 仍要字节级稳定，vLLM 才能把它们识别成相同的多模态前缀。</li> <li>文本历史仍然完整保留。省掉的是重新计算，不是语义信息；第 4 节说的 Speaker 编号一致性依然依赖这份历史。</li> <li>如果多模态处理缓存被驱逐，代码会降级为重发所有窗口。这比静默丢掉历史可靠，但意味着部署时要给 <code class="language-plaintext highlighter-rouge">mm_processor_cache_gb</code> 留足空间。</li> </ol> <p>WebSocket 入口和这个状态模型是一致的：缓冲区达到 <code class="language-plaintext highlighter-rouge">window_samples</code> 后送出窗口，然后执行 <code class="language-plaintext highlighter-rouge">buf = buf[chunk_samples:]</code>，只消费新增 chunk，保留 look-ahead 重叠。尾部不足一个完整窗口时补零，避免最后一次编码出现训练时没见过的“短窗口但没有前瞻”。</p> <h2 id="10-实验结果说明了什么收益存在但流式化确实有代价">10. 实验结果说明了什么：收益存在，但流式化确实有代价</h2> <p>论文在英语上用 WER、中文/日文/韩文上用 CER，都是“把预测文本改成参考文本需要多少编辑”的错误率，越低越好。7B、22-frame 配置在四个会议集和 MLC-Challenge 的五集平均普通识别误差为 24.66；论文对比的 Gemini 3.5 Transcribe Live 为 25.23。speaker-attributed 评测中，VibeVoice 在 13 个可比较设置里有 12 个达到最好或并列最好，第 6 节已经详细拆解了它和 Google STT D0/Dfin 的对照，这里不重复。</p> <p>比 D0/Dfin 更直接的一组代价，是同源模型“流式化前后”的差距：streaming 和非 streaming 的同源 checkpoint 对比显示，流式化后普通 WER/CER 增加 0.75–3.53 个点，cpWER/cpCER 增加 5.13–6.67 个点。后者下降更多，和前面“有限未来上下文对说话人归属更敏感”的推断一致。</p> <p><img src="/assets/img/posts/20260907-vibevoice/table4_p8_08.png" alt="流式化带来的准确率代价"/></p> <p>这张表是同一个 7B 模型的配对比较：左边是初始化来源的 non-streaming checkpoint（即第 7 节 Stage 1 的产物），中间是加入 22-frame streaming 约束后的结果，最右侧 $\Delta$ 是“流式减离线”，所有正数都是流式化带来的误差增加。逐行看，普通 WER/CER 增加 0.75–3.53 个点，而 cpWER/cpCER 增加 5.13–6.67 个点；后者在每个数据集都更大，说明额外损失主要不只是词识别变差，还来自说话人归属在有限上下文下更难。</p> <p>模型规模和 chunk 长度也主要影响说话人归属：22-frame 比 15-frame 在 7B 五集平均改善 1.46 个 WER/CER 点、4.06 个 cpWER/cpCER 点；1.5B 升到 7B 时，22-frame 的 cp 指标改善 12.76 点，而普通指标改善 4.69 点。这个现象更像是更长窗口和更大模型（对应第 3 节表里 hidden_size 从 1536 涨到 3584）提供了更丰富的 speaker evidence，而不是单纯提升了词汇建模能力。</p> <p>服务成本方面，论文只报告了一个明确配置：7B、15-frame、batch size 1、A100 80GB PCIe、bfloat16 和 vLLM。在 30–480 秒音频上，每个 2 秒 chunk 的 decode 为 146–208 ms，RTF 为 0.073–0.104，和第 5 节的结论一致：粒度由协议决定，不由算力决定。</p> <h2 id="11-适用范围和我的判断">11. 适用范围和我的判断</h2> <p>这篇工作最清楚的贡献，不是宣称 streaming ASR 已经没有问题，而是把一个难以拆开的状态问题拆成了几个可验证、甚至可以在没有真实权重的情况下用代码复现出计算过程的机制：两个独立冻结的语音 encoder，各自升维到 3584 后逐元素相加，而不是先拼接再投影；训练目标按词级时间以 0.1 秒容差切进固定长度的 chunk；K/V 缓存只追加不回改，把“不做事后修正”变成推理循环里压根没写的一条分支；vLLM 用前缀缓存把这套“只追加”的语义落实成可接受的服务成本。</p> <p>相应的限制也很明确：训练语言受 Qwen3-ForcedAligner-0.6B 的词级对齐覆盖限制，目前是十种语言；长时间多人重叠会因为单一文本流的串行化而下降（第 4 节已详细讨论）；发布 checkpoint 支持的录音长度约为 8 分钟，主要受未压缩上下文的计算成本限制；首包延迟仍是完整 chunk 加前瞻（3.5 s / 2.5 s），而不是报告里 2.00 s / 1.53 s 的稳态数字（第 5 节）。</p> <p>因此比较稳妥的结论是：VibeVoice-ASR-Streaming 给出了一条把“实时转写”和“会话内说话人一致性”放进同一个 LLM 状态里的可运行路径，实验也显示它在本文设置下有竞争力；但它不是把 diarization 的所有困难凭空消除了，而是把这些困难转成了上下文长度、前瞻预算、训练对齐和服务缓存的工程取舍。</p> <hr/> <p>顺带一提，ASR streaming 和《动手学 AutoML：从 NAS 到大语言模型优化实战》并不是同一个方向；但两者都说明，模型效果不能脱离输入格式、上下文长度和部署资源单独讨论。书里把 LLM 架构自动化、压缩和后训练剪枝整理成了可复现的实现路径，感兴趣可以看下。</p> <blockquote> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="ASR"/><category term="LLM"/><category term="Streaming"/><category term="论文解读"/><summary type="html"><![CDATA[arXiv’26 | VibeVoice-ASR-Streaming：LLM 如何实时转写谁说了什么]]></summary></entry><entry><title type="html">Kimi K3技术详解系列（二）：AttnRes 与 Stable LatentMoE</title><link href="https://marsggbo.github.io/blog/2026/20260902-kimi-k3-attnres-moe/" rel="alternate" type="text/html" title="Kimi K3技术详解系列（二）：AttnRes 与 Stable LatentMoE"/><published>2026-09-02T00:00:00+00:00</published><updated>2026-09-02T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260902-kimi-k3-attnres-moe</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260902-kimi-k3-attnres-moe/"><![CDATA[<h1 id="kimi-k3技术详解系列二attnres-与-stable-latentmoe">Kimi K3技术详解系列（二）：AttnRes 与 Stable LatentMoE</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2607.24653">Kimi K3: Open Frontier Intelligence</a></p> </blockquote> <p>一个 93 层的模型，第 80 层还能不能”看到”第 10 层提取出来的特征？或者说，如果第 80 层就是想要一点早期的局部语法信息，它有没有办法”点名”要，而不是被动接受一层层加工后的混合液？</p> <p>这一篇要回答的就是这种问题。上一篇解决的是<strong>时间轴</strong>上的事：很长的历史怎么压缩、怎么遗忘。这一篇换两个方向。<strong>深度方向</strong>问层与层之间：信息从浅层流到深层时，能不能让某一层自己挑”我要来自哪几层的输入”；<strong>宽度方向</strong>问专家与专家之间：专家数量涨到 896 个之后，怎么让每个 token 的花费还跟只有十几个专家时差不多。两个方向各自有一个主角——深度方向是 AttnRes（Attention Residuals，注意力残差），宽度方向是 Stable LatentMoE（稳定的潜在空间专家混合）。</p> <p>动笔前先把两个背景概念说清楚，后面全程要反复用。Transformer 的一层由两块拼成：<strong>attention</strong>（注意力）负责在不同 token 之间交换信息，回答”当前这个词该看前文哪些词”；<strong>FFN</strong>（Feed-Forward Network，前馈网络）负责在单个 token 内部做一次非线性变换，不和其他位置发生关系。MoE（Mixture of Experts，专家混合）要改造的正是 FFN：把一整块 FFN 拆成多位”专家”（各自带独立参数的子网络），由一个 router（路由器，一个为每个 token 给所有专家打分的轻量网络）为每个 token 挑出少数几位来算。所有 token 都会经过的分支叫 <strong>shared experts</strong>（共享专家），由 router 按分数挑选的分支叫 <strong>routed experts</strong>（路由专家）。</p> <h2 id="系列目录">系列目录</h2> <ol> <li><a href="https://marsggbo.github.io/blog/2026/20260902-kimi-k3-kda/">Kimi K3技术详解系列（一）：KDA 与 Hybrid Attention</a></li> <li><a href="https://marsggbo.github.io/blog/2026/20260902-kimi-k3-attnres-moe/">Kimi K3技术详解系列（二）：AttnRes 与 Stable LatentMoE</a></li> <li><a href="https://marsggbo.github.io/blog/2026/20260902-kimi-k3-infra/">Kimi K3技术详解系列（三）：训练与 Serving Infra</a></li> </ol> <p>下面这张图先给个全貌：上半是宽度方向——full-width 的 shared experts 留住通用能力，routed experts 在较窄的 latent space 里做特化；下半是深度方向——当前层从 embedding、已完成 block 的摘要和当前 block 的已有结果里选来源。这一篇所有的公式和模块，都是围着这两条信息流转的。</p> <p><img src="/assets/img/posts/20260825-kimi-k3-02-attnres-moe/depth_width_flow.png" alt="K3 的深度与宽度信息流"/></p> <h2 id="1-深度方向的默认残差有什么限制">1. 深度方向的默认残差有什么限制</h2> <p>标准 Transformer 每层通常做这样的事情：</p> \[h_l=h_{l-1}+F_l(h_{l-1})\] <p>$h_{l-1}$ 是上一层的 hidden state（隐状态，即某一层输出的那条向量），$F_l$ 是第 $l$ 层的 attention 或 FFN 变换。这个”输入直接加到输出上”的结构叫<strong>残差连接</strong>（residual connection）：它相当于给网络一个保底选项——就算这一层学歪了，输出也至少还是原来的输入，于是每一层只需要学”增量”。没有它，几十上百层叠起来，梯度和数值很容易在传递中消失或爆炸，这也是当年深层网络训不动的核心原因之一。</p> <p>那么限制在哪？残差连接并没有”丢掉”更早层的信息，它会一路把它们带下来；问题在于，走到第 $l$ 层时，这些信息已经被压成了一份统一的 $h_{l-1}$。第 $l$ 层不能显式决定”这次多取一点第 10 层的特征、少取一点第 60 层的特征”，只能接受前面各层一路加工后的混合结果。</p> <p>举个具体例子。假设一个 90 层模型的第 80 层需要某个早期层提取的局部语法特征。这个特征只能混在第 10 层到第 79 层的连续变换里一层层传过来，中间每一层都可能改变它。模型当然可以学着把信息藏在残差里偷偷传，但这条通路不是显式的”回到第 10 层查一下”——<strong>残差保证信息不丢，却保证不了信息可检索</strong>。</p> <p>这听起来是不是很像第一篇里的 recurrent state？没错：信息没有消失，只是被压进一个不断更新的状态，你没法从里面”挑”出想要的那一份。Transformer 在时间方向用 attention 解决了”从历史 token 中选择”的问题；K3 的 AttnRes 把同一件事搬到深度方向，把”只接上一层”改成”从前面多个层级中检索”。<strong>深度方向上，也可以有检索。</strong></p> <h2 id="2-attnres把残差连接改成深度上的-attention">2. AttnRes：把残差连接改成深度上的 attention</h2> <p>先只看一个 token 位置。假设当前是第 $l$ 层，这个位置在前面已经产生了 $l$ 个表示：</p> \[H_{&lt;l}=[h_0,h_1,\ldots,h_{l-1}]\in\mathbb R^{l\times d}\] <p>其中 $h_0$ 是 token embedding（词嵌入，token 进入网络第一层时的那条向量），$d$ 是 hidden dimension。AttnRes 为当前层准备一个<strong>可学习的 pseudo query</strong>（伪查询向量）$\bm w_l\in\mathbb R^d$，把前面所有模块的输出当作 key/value 来查。这里 attention 的”查询”不再是某个 token 的内容，而是一个<strong>固定的、为第 $l$ 层专属训练的向量</strong>——它回答的问题是”我这一层想要什么深度的信息”。因为 $\bm w_l$ 对第 $l$ 层是固定的，但每个 token 位置的 $H_{&lt;l}$ 不同，所以最后的深度权重仍然会随 token 内容变化：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># H_prev: [l, d]，前面 l 个层级的表示
# w_l: [d]，第 l 层自己的可学习 query
</span><span class="n">keys</span> <span class="o">=</span> <span class="nf">rmsnorm</span><span class="p">(</span><span class="n">H_prev</span><span class="p">)</span>                 <span class="c1"># [l, d]
</span><span class="n">weights</span> <span class="o">=</span> <span class="nf">softmax</span><span class="p">(</span><span class="n">w_l</span> <span class="o">@</span> <span class="n">keys</span><span class="p">.</span><span class="n">T</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span> <span class="c1"># [l]
</span><span class="n">residual</span> <span class="o">=</span> <span class="n">weights</span> <span class="o">@</span> <span class="n">H_prev</span>             <span class="c1"># [d]
</span></code></pre></div></div> <p>公式写出来是：</p> \[\bm a_l=\operatorname{softmax}\left(\bm w_l\,\operatorname{RMSNorm}(H_{&lt;l})^\top\right), \qquad r_l=\bm a_lH_{&lt;l}\] <p>这里的 attention 没有发生在 token 之间，而是发生在<strong>层的深度轴</strong>上。它回答的不是”这个词该看前文哪个词”，而是”当前 token 送入第 $l$ 个模块时，哪一个处理深度的表示更适合作为残差信息”。注意这里 softmax 的作用：把任意一串分数变成一组非负、和为 1 的权重——<strong>分数高的层权重高，但谁也别想独吞</strong>。</p> <p>为什么必须先做 RMSNorm（均方根归一化，把向量按自己的均方根长度缩放到单位尺度）？不同层的输出幅值可能差别很大。如果直接点积，某一层仅仅因为数值范数大，就可能在 softmax 中占据绝大多数权重，其他层辛辛苦苦算出来的内容全被淹没；RMSNorm 把比较重点放回<strong>方向和内容</strong>，而不是谁的向量更长。这跟第一篇里 KDA 每次更新前要给状态做归一化，是同一个道理：<strong>先统一个计量口径，再谈取舍。</strong></p> <h3 id="21-用-4-层的例子走一遍">2.1 用 4 层的例子走一遍</h3> <p>假设前面有 embedding、layer 1、layer 2、layer 3 四个表示，当前 layer 4 的 pseudo query 计算出：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>attention over depth = [0.05, 0.10, 0.70, 0.15]
</code></pre></div></div> <p>那么 layer 4 使用的 residual 不是 <code class="language-plaintext highlighter-rouge">h_3</code>，而是：</p> \[r_4=0.05h_0+0.10h_1+0.70h_2+0.15h_3\] <p><img src="/assets/img/posts/20260825-kimi-k3-02-attnres-moe/attnres_flow.png" alt="AttnRes 四层例子的深度加权"/></p> <p>这意味着一件很关键的事：<strong>第 4 层不必把所有深度信息都塞进上一层状态里再硬传下来，它能直接复用第 2 层的表征</strong>——而第 2 层恰好在这次查询里最受青睐（权重 0.70）。换一个 token，前面各层的输出变了，即便 pseudo query 不变，权重也可能移向 $h_3$。和沿深度无差别累积相比，这多出来的是一种<strong>选择能力</strong>：网络学会了在什么情况下该向哪个深度借力。</p> <h2 id="3-为什么-k3-使用-block-attnres">3. 为什么 K3 使用 Block AttnRes</h2> <p>Full AttnRes（每一层都把前面所有层的输出完整存起来供检索）的算术量其实不算可怕——模型深度不到 100，多算几次点积而已。真正贵的是<strong>内存</strong>：为了让后面任意一层都能随时检索，每一层的输出都不能立刻释放。若有 $L$ 层、hidden dimension 为 $d$，持久保存和跨 pipeline stage（流水线阶段，即模型被切成几段分给不同 GPU 时，每段是一个 stage）传输的规模是 $O(Ld)$。这正好把”给深度更多选择”变成了一笔显存和通信账单。</p> <p>K3 的解法不是随手每 12 层取一个点，而是<strong>把层分成 block</strong>：已完成的 block 用块内输出的和压成一个 block representation（块表示），相当于给这个 block 写了一份”摘要”；在当前 block 内部，还保留”到上一层为止”的 partial sum（部分和）。于是当前层既能看 embedding、以前各 block 的摘要，又因为当前 block 还没算完、看不到同 block 后面层的输出——这天然符合因果性。以 93 层为例，8 个约 12 层的 block 加 embedding，共约 <strong>9 个长期保存的来源</strong>，而不是 93 个。</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>embedding  ───────────────┐
layers 1-12  -&gt; block 1 ──┤
layers 13-24 -&gt; block 2 ──┤──&gt; 当前层的 depth attention
...                       │
当前 block 已过的层 -&gt; partial sum ─┘
</code></pre></div></div> <p>这个近似的直觉是：同一个 block 内相邻层的表示往往比较接近，没必要每一层都单独留一个长期入口——就像一本书里同一小节的内容，不必每段都插书签；跨 block 的语义变化更大，才值得保留独立入口。论文的消融显示，约 8 个 block 就能拿到 Full 版本的大部分收益。</p> <p>所以 <strong>Block AttnRes 的本质，是把”保留多个深度层级的可检索性”和”持久状态只保留约 9 份”放进同一个折中里</strong>：多层的可检索性接近满血，内存开销却降了一个数量级。</p> <p><img src="/assets/img/posts/20260825-kimi-k3-02-attnres-moe/figure2_p3.png" alt="Kimi K3 的三维架构"/></p> <p>上图把 K3 的三个方向放在一起：token mixing（token 之间的混合）对应 KDA/MLA，layer mixing（层之间的混合）对应 AttnRes，channel mixing（通道之间的混合）对应 MoE。接下来进入宽度方向。</p> <h2 id="4-896-个专家为什么要先进入-latent-space">4. 896 个专家为什么要先进入 latent space</h2> <p>MoE 的目标是扩大”可选的计算分支数”：<strong>总参数可以很大，但单个 token 只激活少数专家</strong>——好比一个巨型医院，全科医生都配齐了，但每位病人只看对应的几个科室。</p> <p>K3 使用 896 个 routed expert，每个 token 选择 16 个（即从 router 给出的 896 个分数里取最高的 16 个，行话叫 Top-16），另有 2 个所有 token 都经过的 shared expert。问题在于，选择 16 个专家不代表只多算 16 次小函数：<strong>若每个 routed expert 都在 7168 维 hidden space 里做完整 FFN，权重容量、token dispatch（把 token 的表示分发到它选中的专家那里）的通信量和专家侧矩阵乘都会按全宽放大</strong>。896 位专家各存一份 7168 维的权重，光参数规模就吃不消。</p> <p>K3 让 routed 分支先把输入从 7168 维压到 3584 维 latent space（潜在空间，一个更窄的中间表示空间），在较窄的空间里完成专家计算，再投影回 7168 维：</p> <p><img src="/assets/img/posts/20260825-kimi-k3-02-attnres-moe/latent_moe_path.png" alt="routed 分支先降维进 latent space 的路径"/></p> <p>如果所有路径都必须先压到 3584 维，就会出现一个新问题：所有通用计算都要经过一个低维瓶颈，表达能力被卡住。所以 <strong>shared experts 保留 full-width 的公共通路，routed experts 只在 latent space 提供稀疏特化</strong>——通用的事走大路，专精的事钻小路。于是 896 个专家的成本落在窄空间里，通用信息仍有不经压缩的路径。</p> <h2 id="5-激活为什么会爆先看-routed-分支的乘法链">5. 激活为什么会爆：先看 routed 分支的乘法链</h2> <p>一条 routed 分支要依次经过 down projection（降维投影）、专家内部的 GLU、16 个专家的加权聚合和 up projection（升维投影），再与 shared 分支相加。这里先解释一下 GLU（Gated Linear Unit，门控线性单元）：它是一种经典激活结构，用一个分支的输出当”门”去乘另一个分支，让网络自己决定每个维度放行多少信息；SwiGLU 则是用 Swish 激活函数做门的那一版，也是今天大模型的默认选择。</p> <p>回到正题。这条链路不是普通 FFN 里一次矩阵乘之后的小波动：<strong>前一段的异常幅值会成为后一段矩阵乘的输入，16 个分支的尺度差异又会在聚合时叠加</strong>（有的专家输出大、有的小，加在一起就像把高低不齐的砖头摞成一堵墙）。在 2.8T 参数规模和低精度训练下，这条乘法链比”某一层输出略大”危险得多——它的危险在于每一次放大都叠在别人的放大之上。</p> <p>K3 的第一道保险，是在 routed experts 聚合之后、up projection 之前加一层 RMSNorm：</p> \[u=\sum_{i\in\mathrm{TopK}}p_iE_i(W_{down}x), \qquad y=W_{up}\operatorname{RMSNorm}(u)+y_{shared}\] <p>它不改变 token 选择哪个专家，也不把专家输出变成同一个方向（归一化只调幅值、不动方向）；它只在进入最容易放大幅值的 $W_{up}$ 之前，把 16 个专家聚合后的 scale 拉回可控范围。<strong>这里的 RMSNorm 是为 latent 路径带来的尺度波动配的一道闸门。</strong></p> <p>第二道保险是把 SwiGLU 换成 SiTU-GLU。SwiGLU 的两个分支在大输入上没有硬上限，低精度训练时更容易产生极端值。SiTU-GLU 给两个分支加 soft cap（软上限，即让输出平滑地逼近一个上界，而不是被硬生生截断）：</p> \[f(x)=\left[\beta_1\tanh(W_gx/\beta_1)\odot\sigma(W_gx)\right] \odot\left[\beta_2\tanh(W_ux/\beta_2)\right]\] <p>K3 取 $\beta_1=4$、$\beta_2=25$。为什么这样设？在输入接近 0 时，tanh 近似线性，函数仍像 SwiGLU 一样工作，<strong>正常区域的表达能力几乎没有被改写</strong>；输入很大时，每个分支会平滑封顶，输出幅值上界约为 $\beta_1\beta_2=100$——极端激活最多被压到 100，而不是跟着指数一起冲上天。</p> <p><img src="/assets/img/posts/20260825-kimi-k3-02-attnres-moe/figure4_p7.png" alt="SiTU-GLU 的幅值控制"/></p> <p>看图时重点看两段：原点附近红色 SiTU-GLU 跟 SwiGLU 几乎重合，说明普通输入区域的行为没被破坏；大输入区域红线逐渐压平，说明低精度路径不会任由异常激活继续放大。<strong>一道闸门管聚合后的尺度，一个软上限管极端输入，两道保险合起来，才敢说这 896 个专家在低精度下是”稳定”的</strong>——这也是 Stable LatentMoE 里 Stable 这个词的来历。</p> <h2 id="6-896-个专家为什么会负载不均">6. 896 个专家为什么会负载不均</h2> <p>即使激活稳定了，router 还有另一个独立问题：它可能反复把 token 送到少数热门专家。假设一个 batch 有 8 个 token、4 个专家，每个 token 只选 1 个，朴素 Top-1 可能得到负载：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>expert 1: 4 tokens
expert 2: 3 tokens
expert 3: 1 token
expert 4: 0 token
</code></pre></div></div> <p>在专家并行（Expert Parallelism，EP，即把不同专家放到不同 GPU 上，token 通过通信送到对应专家的组织方式）中，专家 1 所在的 GPU 要处理 4 个 token，专家 4 所在的 GPU 没活干；整个 batch 的时间由最忙的 GPU 决定。专家数量越多，少量偏差更容易把某些专家推成”过热”（被抢破头）或”濒死”（长期接不到活，越训越废）——<strong>315 号窗口排起了长队，316 号窗口却一个病人都没有。</strong></p> <p>过去常见的做法是加 auxiliary loss（辅助损失，在训练目标之外额外加一项，专门惩罚路由不均衡）；后来流行的 <strong>auxiliary-loss-free 路由</strong>（无辅助损失路由）则走另一条路：为每个专家维护一个 bias（偏置，一个可训练的标量），热门专家减一点、冷门专家加一点，让分数往均衡的方向挪。后者虽然避免了额外损失项，却要在”每步太小，调不过来”和”每步太大，来回震荡”之间反复调步长。</p> <p>K3 注意到一件事：<strong>一次 router 前向其实已经给出了更直接的信息——每个 token 要让某个专家进入 Top-k，究竟还差多少分。</strong> 与其猜步长，不如直接算账。这就是下一节的 Quantile Balancing。</p> <h2 id="7-quantile-balancing从门槛直接反推-bias">7. Quantile Balancing：从门槛直接反推 bias</h2> <p>令 $s_{i,j}$ 是 token $i$ 对专家 $j$ 的原始 router 分数。K3 用 bias 只调 Top-k 的<strong>选择</strong>，而不把 bias 混入最终专家权重——也就是说，bias 只决定”谁能进决赛”，决赛里的权重混合比例还是由原始分数决定，<strong>bias 是调度旋钮，不会直接篡改已选专家之间的混合比例</strong>。</p> <p>对 token $i$，在当前带 bias 的分数中取第 $k+1$ 大值 $\alpha_i$。它就是”进入这个 token 的 Top-k 必须跨过的门槛”（cutoff）。下一步若给专家 $j$ 候选 bias $b_j$，它能进入 Top-k，当且仅当：</p> \[s_{i,j}+b_j&gt;\alpha_i\] <p>移项以后：</p> \[b_j&gt;\alpha_i-s_{i,j}\] <p>换句话说，$s_{i,j}-\alpha_i$ 表示专家 $j$ 相对 token $i$ 门槛的<strong>余量</strong>（headroom）：正数说明已经够格，负数说明还差一点。若一个 batch 有 $m$ 个 token、$n$ 个专家、每个 token 选 $k$ 个，理想平均负载是 $q=mk/n$。我们不需要猜一个小步长，只需把 $b_j$ 设到<strong>恰好让 $q$ 个 token 跨过门槛</strong>的位置，也就是余量分布的相应分位点（quantile，把余量从小到大排队后，取第 $q/m$ 百分位处的那个值）：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># s: [m, n]，m 个 token 对 n 个专家的原始 router 分数
# alpha: [m]，当前带 bias 的 Top-(k+1) cutoff
</span><span class="n">headroom</span> <span class="o">=</span> <span class="n">s</span> <span class="o">-</span> <span class="n">alpha</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">]</span>
<span class="n">b_hat</span> <span class="o">=</span> <span class="o">-</span><span class="n">torch</span><span class="p">.</span><span class="nf">quantile</span><span class="p">(</span><span class="n">headroom</span><span class="p">,</span> <span class="n">q</span><span class="o">=</span><span class="mi">1</span> <span class="o">-</span> <span class="n">k</span> <span class="o">/</span> <span class="n">n</span><span class="p">,</span> <span class="n">dim</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>
<span class="n">b</span> <span class="o">=</span> <span class="n">b_hat</span> <span class="o">-</span> <span class="n">b_hat</span><span class="p">.</span><span class="nf">mean</span><span class="p">()</span>
</code></pre></div></div> <p>为什么最后要减均值？给所有专家 bias 同时加上同一个常数，不会改变任何 token 的 Top-k 排序；减掉均值只是去掉这个无意义的公共偏移，让 bias 保持在 0 附近。</p> <p><img src="/assets/img/posts/20260825-kimi-k3-02-attnres-moe/quantile_balancing.png" alt="Quantile Balancing 从余量分布反推 bias"/></p> <p>以 8 token、4 expert、$k=1$ 为例，目标负载是 $q=8\times1/4=2$。Quantile Balancing 不是让 bias 每轮只移动一点，而是<strong>根据当前余量分布直接估计一个能让每个专家接近 2 个 token 的 bias</strong>；为避免同一 batch 用自己算出的 bias 重新路由（自己给自己出难题），它在<strong>下一步才生效</strong>。</p> <p>实际训练中，K3 不会把百万级余量全部 gather 到一张卡上——那样通信量就白省了——而是每张卡先统计每个专家的 1000-bin 直方图（bin 即直方图的柱），再 all-reduce（把所有卡上的整数计数汇总相加）合并，最后从合并直方图近似读取分位点。<strong>通信量从”所有浮点余量”降到”每个专家固定数量的 bin”，误差约为 bin 宽度级别</strong>——用一点点近似，换掉一整轮全量通信。</p> <h2 id="8-为什么-k3-的宽度设计要和稳定性绑在一起">8. 为什么 K3 的宽度设计要和稳定性绑在一起</h2> <p>把 routed experts 放进 0.5× latent space，并不是单纯为了少算一半矩阵。它同时降低了：</p> <ul> <li>896 个专家各自保存 FFN 权重的成本；</li> <li>token dispatch 到专家后的矩阵乘规模；</li> <li>多专家聚合后传回全宽 hidden space 的压力。</li> </ul> <p>但压缩宽度以后，shared expert、RMSNorm 和 SiTU-GLU 就变成了<strong>必要的稳定性配套</strong>：没有它们，窄空间里聚合出来的异常尺度会一路放大到输出；专家数量变多以后，Quantile Balancing 又承担了把计算量分回各个 rank 的职责。<strong>latent space、激活 soft cap、路由 bias 不是三个互不相关的小技巧，而是同一个”超大稀疏 FFN 如何可训练、可并行”的问题的不同答案。</strong> 这大概也是本篇把 AttnRes 和 Stable LatentMoE 放在一起的原因——深度可检索、宽度可训练，两条腿缺一不可。</p> <p>论文的 scaling law（缩放定律，指模型性能随计算量、参数、数据规模变化呈现的规律性关系）图报告 K3 相比 K2 约 2.5× 的 scaling efficiency（缩放效率）提升。这里要强调一句：<strong>scaling efficiency 是在相同训练损失或能力目标下所需计算量的拟合比较，不等同于单个 kernel 的 2.5× 加速</strong>；它包含架构、训练配方和系统效率的共同影响。</p> <p><img src="/assets/img/posts/20260825-kimi-k3-02-attnres-moe/figure7_p11.png" alt="K2 与 K3 的 scaling law"/></p> <h2 id="9-小结">9. 小结</h2> <p>这一篇可以这样记：</p> <ol> <li>标准残差只把上一层送给下一层，AttnRes 把”从前面哪些层取信息”变成了可学习的 depth attention（深度注意力）；</li> <li>Full AttnRes 要保存所有层的输出做检索，Block AttnRes 用约 8 个 block 摘要就保留了大部分效果；</li> <li>896 个 routed experts 如果都在全宽空间运行，参数和通信都太贵，因此先进入 0.5× latent space；</li> <li>进了窄空间就要配稳定性：shared expert 保住全宽通路，RMSNorm 管聚合后的尺度，SiTU-GLU 用软上限管极端激活；</li> <li>Quantile Balancing 从第 $k+1$ 个分数提供的门槛反推 bias，让负载均衡从”小步试错”变成”按余量分布直接估计”，而且只花直方图那么点通信量。</li> </ol> <p>下一篇进入系统部分：MoonEP 如何让专家并行的 token 数固定，KDA 的状态如何跨卡传递，1M context 的 Agentic RL 和 prefix cache 又为什么需要重新设计。</p> <hr/> <blockquote> <p>扯一句题外话：打个广告，《动手学 AutoML：从 NAS 到大语言模型优化实战》系统地介绍了如何自动化地设计、搜索和压缩模型架构，结合最新的 attention 设计范式，可以让 AI 自动搜出更高效的结构，感兴趣的同学可以看看我们的这本书。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="MoE"/><category term="Attention Residuals"/><category term="模型稳定性"/><category term="论文解读"/><summary type="html"><![CDATA[Kimi K3技术详解系列（二）：AttnRes 与 Stable LatentMoE]]></summary></entry><entry><title type="html">Kimi K3技术详解系列（三）：训练与 Serving Infra</title><link href="https://marsggbo.github.io/blog/2026/20260902-kimi-k3-infra/" rel="alternate" type="text/html" title="Kimi K3技术详解系列（三）：训练与 Serving Infra"/><published>2026-09-02T00:00:00+00:00</published><updated>2026-09-02T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260902-kimi-k3-infra</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260902-kimi-k3-infra/"><![CDATA[<h1 id="kimi-k3技术详解系列三训练与-serving-infra">Kimi K3技术详解系列（三）：训练与 Serving Infra</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2607.24653">Kimi K3: Open Frontier Intelligence</a></p> </blockquote> <p>一个 2.8T 参数的模型，训练时卡怎么分、状态怎么存、一条 1M token 的请求来了怎么恢复，这些问题不会因为”模型效果好”就自动消失——恰恰相反，<strong>架构每改一处，系统就得跟着还一笔债</strong>。</p> <p>这是 Kimi K3 系列的第三篇。前两篇分别讲了 KDA/Hybrid Attention，以及 AttnRes/Stable LatentMoE。本篇不把它们当作几个可以独立替换的层，而是顺着一个问题看：<strong>结构变了以后，训练和 serving（在线服务，即模型部署后对外提供推理请求的环节）原先默认的”数据怎么分、状态怎么存、请求怎么恢复”，为什么都要改。</strong></p> <h2 id="系列目录">系列目录</h2> <ol> <li><a href="https://marsggbo.github.io/blog/2026/20260902-kimi-k3-kda/">Kimi K3技术详解系列（一）：KDA 与 Hybrid Attention</a></li> <li><a href="https://marsggbo.github.io/blog/2026/20260902-kimi-k3-attnres-moe/">Kimi K3技术详解系列（二）：AttnRes 与 Stable LatentMoE</a></li> <li><a href="https://marsggbo.github.io/blog/2026/20260902-kimi-k3-infra/">Kimi K3技术详解系列（三）：训练与 Serving Infra</a></li> </ol> <h2 id="1-先分清每种并行究竟在切什么">1. 先分清：每种并行究竟在切什么</h2> <p>训练 2.8T 参数模型时，单卡肯定放不下，因此必须把不同维度的工作切到多张 GPU。这里的几个缩写容易一起出现、互相打架，但它们各自切的是<strong>不同对象</strong>，先分清再谈别的：</p> <ul> <li><strong>TP（Tensor Parallelism，张量并行）</strong>：把一个矩阵切到多张卡，每张卡算一部分列或行——单个矩阵太大，一张卡算不完；</li> <li><strong>EP（Expert Parallelism，专家并行）</strong>：不同 GPU 保存不同的 MoE experts，token 通过通信送到对应专家——专家太多，一张卡存不下；</li> <li><strong>PP（Pipeline Parallelism，流水线并行）</strong>：把连续的模型层切成多个 stage，数据像流水线一样经过这些 stage——层数太深，一张卡塞不下；</li> <li><strong>DP（Data Parallelism，数据并行）</strong>：每张卡或每组卡保存模型副本，处理不同 batch，再同步梯度——模型不大但数据多时，用多份副本并行吃数据；</li> <li><strong>CP（Context Parallelism，上下文并行）</strong>：把一条很长的序列切给不同 GPU，各卡负责不同的上下文片段——单条序列太长，一张卡装不下。</li> </ul> <p>K3 把这些方式组合起来，再加上 <strong>ZeRO</strong>（Zero Redundancy Optimizer，把 optimizer state、梯度等按维度分片保存，消除冗余）和 <strong>offload</strong>（把暂时不用的状态搬到 GPU 外，如 CPU 内存或 NVMe 硬盘）。这里要强调一句：<strong>这不是”并行方式越多越先进”</strong>——TP 解决单个矩阵太大，EP 解决专家太多，PP 解决层数太深，CP 解决序列太长，每一种并行都是针对一个具体瓶颈的”补丁”。下面的 MoonEP、KCP 和 cache 机制，分别是在 EP、CP 和请求生命周期上处理这些切分留下的后果。</p> <h2 id="2-moonep为什么专家并行最怕动态负载">2. MoonEP：为什么专家并行最怕动态负载</h2> <h3 id="21-一个-batch-就能制造负载倾斜">2.1 一个 batch 就能制造负载倾斜</h3> <p>假设有 4 个 experts，8 个 token，每个 token 只选 1 个。一次 router 可能给出：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>expert 1: 4 tokens
expert 2: 3 tokens
expert 3: 1 token
expert 4: 0 token
</code></pre></div></div> <p>如果 4 个 expert 分别放在 4 张 GPU 上，GPU 1 要处理 4 个 token，GPU 4 没有工作；即使 GPU 2、3 算完了，整个 batch 也要等 GPU 1。<strong>专家并行的吞吐由最忙的 rank（参与训练的 GPU 进程编号）决定</strong>——就像排队结账，收银台再多，队伍最长的那条决定了你的等待时间。</p> <p>通常 token 会先通过 <strong>all-to-all</strong>（一种集合通信：每张卡把发给其他卡的数据直接送到对方，同时接收别人发给自己的数据）发送到目标专家所在的 GPU，再做 <strong>grouped GEMM</strong>（把不同专家的 token 合并成一批矩阵乘法，按专家分组计算）。如果每步的 token 数不稳定，通信 buffer 和矩阵乘法的形状也跟着变，host-device 同步（GPU 计算完成后的显式同步点）和动态 kernel launch（运行时才确定形状、临时启动计算内核）会进一步拖慢训练。</p> <h3 id="22-moonep-的思路先规划再迁移专家">2.2 MoonEP 的思路：先规划，再迁移专家</h3> <p>MoonEP 是 K3 报告中的专家并行方案。router 的选择不变，但系统可以把<strong>热门 expert 的冗余副本</strong>临时放到其他 rank，让原本都要找同一位专家的 token 分流处理。目标不是”平均差不多”，而是让每个 EP rank 在一层<strong>恰好接收</strong> $S\times K$ 个 token，其中 $S$ 是该 rank 的序列 token 数，$K$ 是每个 token 选择的专家数。</p> <p>这个固定数量带来两个系统收益：</p> <ol> <li>每个 rank 的矩阵乘形状在编译或计划阶段就知道，不需要等 GPU 统计完 token 数再决定下一步；</li> <li>通信 buffer 可以按 $S\times K$ 预分配，而不用按最坏情况扩成 $S\times K\times R$，其中 $R$ 是 EP rank 数——<strong>buffer 从”最坏情况”降为”精确大小”，省下的不只是内存，还有反复分配释放的开销</strong>。</li> </ol> <p>一次执行可以按四步理解，下图是这四步的手绘版：</p> <p><img src="/assets/img/posts/20260825-kimi-k3-03-infra/moonep_flow.png" alt="MoonEP 将动态路由变成固定 shape 执行"/></p> <p>router 先给出 token-expert 配对；planning kernel（规划内核）决定哪些热门 expert 要有冗余副本、每个配对该去哪个 rank；all-to-all 把 token 直接写进远端已经按专家分组的位置；随后 grouped GEMM 直接把这个 buffer 当输入。<strong>最后一步不再重排一次，这就是 zero-copy（零拷贝）所说的”通信 buffer 直接作为计算 buffer”</strong>——数据搬到位就开算，中间不折腾第二遍。</p> <p>论文还给出一个冗余槽位的存在性结论：只要每个 rank 预留约 $E/R$ 个冗余 expert 槽位（$E$ 为 expert 数，$R$ 为 rank 数），就能找到满足均衡的专家迁移方案。这个 bound 说明”<strong>总能找到方案</strong>“是有保证的，但真实收益仍取决于迁移成本、expert locality（专家在物理上的局部性）和通信拓扑。</p> <h3 id="23-为什么固定-shape-对大模型很重要">2.3 为什么固定 shape 对大模型很重要</h3> <p>在小模型里，多一次同步可能只是一点微秒；在 93 层、数万步的训练里，微秒会乘上层数、pipeline stage 和训练步数。传统 EP 里，每个专家实际收到多少 token 要等 router 在 GPU 上跑完才知道，host 因而要等 shape 再发起后续计算，中间那段空等会随着训练步数被无限放大。MoonEP 把每个 rank 的总 token 数固定后，通信 buffer、矩阵乘形状和内核调度都可以提前确定，CUDA Graph（把一串内核启动录制下来、以后一次回放的加速技术）也更容易复用。</p> <p>所以 <strong>MoonEP 的价值不只在负载均衡公式，而在于把”动态路由问题”变成”固定 shape 的执行问题”</strong>：专家是否热门仍然会变化，但每个 rank 最终接收多少 token 被系统约束住了——变化的是”谁在忙”，不变的”每个人分到多少活”。</p> <h2 id="3-moonep-解决时间显存策略解决容量">3. MoonEP 解决时间，显存策略解决容量</h2> <p>MoonEP 让卡不再互相等，但不会让模型本身少占一字节。训练时显存通常被四类东西占满：<strong>模型参数、激活（每一层计算产生的中间结果）、梯度、optimizer state</strong>（优化器状态，如 Adam 的一二阶动量）。K3 的做法不是给所有 tensor 规定同一种存储方式，而是按三个问题分别决定：<strong>反向时能否便宜重算？是否马上还会用？搬运多少字节？</strong> 答案组合起来，就是下面四种策略的取舍：</p> <p><img src="/assets/img/posts/20260825-kimi-k3-03-infra/memory_strategy.png" alt="训练显存的四类占用与三种对策"/></p> <h3 id="31-激活用重计算换空间">3.1 激活：用重计算换空间</h3> <p>如果保存每层所有中间激活，反向传播时可以直接读取，但显存随层数和序列长度增长。<strong>重计算</strong>（recomputation，也叫 checkpointing，只保存关键检查点，反向时重新跑一小段前向）则用额外计算换掉激活存储——<strong>多花一次前向的时间，省下一整层的显存</strong>。</p> <p>K3 对大块激活使用 <strong>block-wise FP8 量化</strong>（把激活按块转成 8 位浮点，块越小误差越小，块越大越省）再 offload，对逐元素算子更多采用重计算。量化减少传输字节数，重计算减少需要传输的 tensor 数量，两者针对的是不同的成本：<strong>一个省”搬运的斤两”，一个省”要搬的件数”。</strong></p> <h3 id="32-moe-反向不要保存完整-dispatch-中间量">3.2 MoE 反向：不要保存完整 dispatch 中间量</h3> <p>MoE 前向要把 token 按 expert 重新排列，得到 dispatch buffer；反向如果把所有排列后的中间激活完整保存，显存和通信都很可观。K3 利用梯度表达式，只保存必要的 dispatch 输入，反向时重算排列和 GEMM，并与通信流水线重叠——<strong>反向的路，前向重走一遍，但只走必要的几段。</strong></p> <h3 id="33-attnres-并不一定让显存线性增加">3.3 AttnRes 并不一定让显存线性增加</h3> <p>Full AttnRes 确实需要保留多层表示，但 K3 使用 Block AttnRes（见系列二）。一个 block 内的输出先汇总，后续层只访问少量 block 表示；把这段计算纳入 checkpointing 后，每层保存的激活量可以接近标准残差架构，而不是保存 93 份独立层输出。<strong>深度方向的可检索性，并没有让显存账单翻 93 倍——这就是 Block 化的意义。</strong></p> <h2 id="4-kda-的状态怎样跨卡难点是顺序不是状态大小">4. KDA 的状态怎样跨卡：难点是顺序，不是状态大小</h2> <p>KDA 的优势是状态 $\mathbf S$ 不随序列长度增长，正因如此跨卡传它本身并不大；难点是<strong>状态更新具有顺序依赖</strong>。假设把一条长序列切成两段：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GPU 0: token 0 ... token 49999
GPU 1: token 50000 ... token 99999
</code></pre></div></div> <p>GPU 1 不能从空状态开始算，因为 token 50000 的结果依赖前 50000 个 token 的状态。最朴素的做法是 GPU 0 先完整算完、传一次固定大小的 $S$ 给 GPU 1；通信并不贵，但 GPU 1 全程闲着——<strong>并行了个寂寞。</strong> KCP 要解决的是：<strong>两段能否先并行做自己的工作，最后再精确接回同一条状态链。</strong></p> <h3 id="41-普通线性-attention-为什么可以简单相加">4.1 普通线性 attention 为什么可以简单相加</h3> <p>最简单的线性 attention 更新是：</p> \[\mathbf S_t=\mathbf S_{t-1}+\bm k_t\bm v_t^\top\] <p>每段从零开始算出本地贡献，最后把此前各段贡献相加即可。这是因为后面 token 只是在旧状态上加一项，没有改变旧状态本身——<strong>加法满足交换律，先加后加无所谓。</strong></p> <p>KDA 不满足这个条件。当前 token 不仅写入一项，还会遗忘和擦除旧状态，因此它对入口状态施加一个由 token 决定的转移：</p> \[\mathbf S_t=\mathbf M_t\mathbf S_{t-1}+\mathbf b_t\] <p>其中 $\mathbf M_t=(\mathbf I-\beta_t\bm k_t\bm k_t^\top)\operatorname{Diag}(\bm\alpha_t)$，$\mathbf b_t=\beta_t\bm k_t\bm v_t^\top$。所以后半段即使从零算出了自己的 $\tilde{\mathbf S}$，它仍必须先把前半段传来的状态乘上本段累计 $\mathbf M$；<strong>两段本地状态不能简单相加，因为后半段会”改写”不是”追加”。</strong></p> <h3 id="42-kcp传固定大小的转移信息">4.2 KCP：传固定大小的转移信息</h3> <p>Kimi 的 KDA Context Parallelism（KCP）把每段的作用总结成一句<strong>仿射变换</strong>：给我任意入口状态 $S_{in}$，我都会输出 $\mathbf M_{seg}S_{in}+\tilde{\mathbf S}_{seg}$，即”先乘一个矩阵、再加一个偏移”。其中两项都能只根据本段 token 预先算出：</p> <ol> <li>这一段的<strong>累积转移矩阵</strong> $\mathbf M$；</li> <li>假设输入状态为零时，这一段自身产生的状态 $\tilde{\mathbf S}$。</li> </ol> <p>例如 GPU 0 的 fragment（片段，这里指一段 token 及其对应的 $\mathbf M,\tilde{\mathbf S}$ 摘要）是 $(\mathbf M^{(0)},\tilde{\mathbf S}^{(0)})$，GPU 1 的 fragment 是 $(\mathbf M^{(1)},\tilde{\mathbf S}^{(1)})$。从初始零状态出发，GPU 1 的真实入口就是 GPU 0 的输出：</p> \[\mathbf S_{in}^{(1)}=\mathbf M^{(0)}\mathbf S_{in}^{(0)}+\tilde{\mathbf S}^{(0)}.\] <p>GPU 1 随后把这个入口代入自己的 fragment，得到</p> \[\mathbf S_{out}^{(1)}=\mathbf M^{(1)}\mathbf S_{in}^{(1)}+\tilde{\mathbf S}^{(1)}.\] <p>两段的组合仍是同一种”先乘 $\mathbf M$、再加 $\tilde{\mathbf S}$”的形式，因此可以用 <strong>prefix scan</strong>（前缀扫描，把所有 fragment 按顺序两两复合一遍，最终每个位置都得到”从开头到这里的完整变换”）连续合并更多 rank：</p> <p><img src="/assets/img/posts/20260825-kimi-k3-03-infra/kcp_flow.png" alt="KCP 先并行总结，再恢复状态顺序"/></p> <p>各 GPU 先并行计算自己的 fragment，再交换这些固定大小对象，按 token 段顺序做一次 prefix scan，就能恢复每段真实的入口状态，继而得到输出。<strong>关键是 fragment 的复合仍保持顺序，却不需要等待上一段完成 token 级递推；通信量与状态矩阵大小相关，而不随整条序列线性增长。</strong> 这和”把序列直接切两半、各算各的”不同：KCP 传的是<strong>每段如何变换状态的信息</strong>，最后仍然遵守原始 token 顺序——并行的是计算，不是语义。</p> <h2 id="5-1m-context-的-agentic-rl状态要能暂停和恢复">5. 1M context 的 Agentic RL：状态要能暂停和恢复</h2> <p>到这里讨论的还是一次前向或一次训练 step。Agentic RL（智能体强化学习，让模型在环境里自主调用工具、完成多步任务再训练的范式）则把任务拉成很长的生命周期：模型调用工具、等待工具结果、继续读写文件和执行下一步，再根据最终结果训练。一个 1M token rollout（一次完整的多轮交互轨迹）可能有几百次这样的往返，因此系统要同时管理两类状态：<strong>模型已经读过前缀的 cache</strong>，以及<strong>工具环境里的文件、进程和磁盘状态</strong>。</p> <h3 id="51-cache-池只为离开-gpu-的前缀付费">5.1 Cache 池：只为离开 GPU 的前缀付费</h3> <p>正在 decode（逐 token 生成）的请求必须把 cache 留在 GPU；正在等待工具结果的请求暂时不用算，却也不该丢失已经 prefill（预填充，把提示词一次性算完、得到每层 KV）好的长前缀。若所有这类闲置前缀都常驻 GPU，cache 很快爆掉；若每次都从头 prefill，1M token 的等待又会变得很贵。K3 用 <strong>write-back</strong>（写回缓存：只在数据被驱逐时才写回下一级存储）：只在前缀被 GPU 驱逐时才写回 CPU DRAM，下次重新调度前再预取。它和 write-through（写穿缓存：每次更新都同步写一份到下一级）的区别是，<strong>仍在 GPU 活跃的 cache 不额外复制一份到 CPU</strong>——不搬家就不付搬家的钱。</p> <p>KDA 状态与对应 MLA KV block 一起 offload，因为它们必须在相同的 token 边界恢复。模型权重和 optimizer state 还会继续 offload 到 NVMe，把 DRAM 主要留给 cache 池——<strong>冷数据住硬盘，热数据住内存，正在用的住显存，三级存储各司其职。</strong></p> <h3 id="52-为什么沙箱要用-microvm">5.2 为什么沙箱要用 microVM</h3> <p>cache 恢复了模型的上下文，工具沙箱还要恢复”任务做到哪里”。Agent 可能会创建文件、运行代码、启动容器，甚至触发内核级操作；普通容器隔离不够强时，一次异常就可能影响整台训练机器。K3 使用 <strong>Firecracker microVM</strong>（一种轻量虚拟机，启动快、隔离强，把每个 AgentENV 当作可暂停、可复制的独立小虚拟机）。</p> <p>它支持<strong>增量 checkpoint</strong>，只保存脏页（自上次检查点以来被修改过的内存页）；报告中的 checkpoint 约 133ms、resume 约 49ms。Agent 等待工具结果时，沙箱可以暂停，不继续占用 CPU 和内存；需要比较不同动作的 reward 时，还可以 fork 出副本，在副本里试错而不影响原环境——<strong>试错成本从”重开一局”变成”复制一份”。</strong></p> <p>这类机制对 1M context 很关键：<strong>长任务的瓶颈不只是 token 数，还包括”一个任务的外部世界状态要活多久”。</strong></p> <h2 id="6-prefix-cachekda-让已经成熟的-cache-又变复杂">6. Prefix Cache：KDA 让已经成熟的 cache 又变复杂</h2> <p>传统 KV cache 是”每个 token 一份 K/V”，按页保存后只要前缀 hash 命中，就能在相应边界继续。KDA 的 cache 不是 token 列表，而是该前缀累积得到的一整块状态；要从某个边界恢复，必须<strong>正好</strong>有该边界的 state checkpoint。于是一个请求可能出现：MLA 的前缀已经命中，但同一边界没有 KDA checkpoint，仍不能安全继续——<strong>就像护照页都在，但签证页缺了，还是过不了关。</strong></p> <p>K3 的解决思路是把”匹配有多细”和”物理块有多大”解耦：</p> <ul> <li><strong>逻辑匹配</strong>在 512-token hash block 上进行，保证前缀可以细粒度比较；</li> <li><strong>物理内存</strong>用较大的 block 分配，例如一个 6144-token physical block 里包含 12 个 hash block；</li> <li><strong>KDA checkpoint</strong> 只在稀疏边界保存，通常优先选择对话轮次边界——反正 KDA 恢复成本低，不必每个 token 都存；</li> <li>命中后恢复最近 checkpoint，中间未覆盖的 token 用 <strong>copy-on-write</strong>（写时复制，复制一份私有副本再修改，不动共享原页）继续 prefill。</li> </ul> <p>下图先把一次请求的决策走一遍：</p> <p><img src="/assets/img/posts/20260825-kimi-k3-03-infra/serving_cache_flow.png" alt="Prefix cache 的匹配、恢复与 write-back 流程"/></p> <p>真正可用的命中必须<strong>同时</strong>满足 MLA 前缀 hash 与各个 KDA 组的 checkpoint；命中后复制出私有的可写部分，再 prefill 新增加的 token，不能直接修改共享 cache。</p> <p><img src="/assets/img/posts/20260825-kimi-k3-03-infra/figure12_p23.png" alt="KDA 感知的 prefix cache"/></p> <p>图中蓝色部分是已缓存的 MLA hash block，下面的点表示 KDA checkpoint。比如请求前 2800 个 token 都相同，最长可用边界也许是 2560：此处既有 5 个 512-token hash block，又有 KDA checkpoint。系统复制共享 MLA 的未满物理页，恢复 2560 处的 KDA 状态，然后只 prefill 后面的 240 个 token；<strong>它不会为了复用 MLA 而从一个没有 KDA 状态的 2800 边界冒险继续</strong>——省 240 个 token 的 prefill，不值得赌上整条状态链的正确性。</p> <p>这里的关键不是”缓存粒度越细越好”。Checkpoint 本身比单个 token 的 KV 大得多，存得太密会抵消 KDA 的空间优势；存得太稀又会增加恢复时需要重算的 token。<strong>K3 的设计是在匹配粒度和状态保存成本之间折中</strong>：粒度管”能不能精确接上”，成本管”接上要花多少”。</p> <h2 id="7-speculative-decodingkda-状态不能随便回滚">7. Speculative Decoding：KDA 状态不能随便回滚</h2> <p>Speculative decoding（推测解码）让一个较小的 draft model（草稿模型，先快速猜出候选 token 的模型）一次猜多个 token，再让大模型验证。接受的 token 被写入正式状态，被拒绝的 token 则需要丢弃；它的收益来自<strong>一次验证多步候选</strong>，而不是让大模型少做验证——大模型该算的还是一步不落。</p> <p>在普通 KV cache 里，回滚通常是释放被拒绝 token 对应的 cache block，改个长度指针就完事；KDA 每生成一个 token 都会更新同一个状态矩阵，如果 draft 连续走了 7 步，再拒绝第 3 步，状态已经多更新了几次，<strong>不能只改一个长度指针</strong>——你不是删掉了几行字，而是把整个笔记本的墨水都改了。</p> <p>K3 的思路是：状态并不需要存下来，只要保存<strong>每个 draft token 驱动状态更新所需的 q/k/v 投影输入</strong>。验证 kernel 确认接受到第几步后，在片上从最后一个正式状态<strong>重放</strong> accepted token 的更新，最后只写回合法状态。投影输入远小于为每个候选都复制一份完整 $\mathbf S$，这就是”重放”比”逐步快照”划算的原因——<strong>存”怎么算”比存”算到哪”便宜。</strong></p> <h2 id="8-量化和-vllm训练时就面对部署格式">8. 量化和 vLLM：训练时就面对部署格式</h2> <p>K3 的 post-training 使用 <strong>MXFP4 量化感知训练</strong>：MoE expert 权重以 MXFP4 保存和计算，激活使用 MXFP8，attention projection 和 router 保持更高精度。MXFP4/MXFP8 是<strong>带微缩放因子的低精度浮点格式</strong>（先按块统计一个缩放因子，再把块内数值缩放到 4 位或 8 位表示，兼顾范围和精度）；量化感知训练（QAT）的意思是，<strong>训练过程中就模拟部署时的低精度</strong>，而不是训练完成后突然把 BF16 权重转换成 MXFP4。</p> <p>这样做是为了减少 <strong>train-inference mismatch</strong>（训练与推理的数值格式不一致）：训练时模型已经见过量化误差，rollout 和训练使用同一套数值格式，部署后不容易出现突然掉点——<strong>考试用什么样的卷子，平时就练什么样的卷子。</strong></p> <p>开源 serving 侧，vLLM 已提供 K3 的 Day-0 支持。一个最简启动方式是：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>vllm serve moonshotai/Kimi-K3 <span class="se">\</span>
  <span class="nt">--tensor-parallel-size</span> 8 <span class="se">\</span>
  <span class="nt">--trust-remote-code</span> <span class="se">\</span>
  <span class="nt">--load-format</span> fastsafetensors <span class="se">\</span>
  <span class="nt">--enable-prefix-caching</span> <span class="se">\</span>
  <span class="nt">--enable-auto-tool-choice</span> <span class="se">\</span>
  <span class="nt">--tool-call-parser</span> kimi_k3 <span class="se">\</span>
  <span class="nt">--reasoning-parser</span> kimi_k3
</code></pre></div></div> <p>这里的 <code class="language-plaintext highlighter-rouge">tensor-parallel-size 8</code> 表示用 8 张 GPU 切分张量；<code class="language-plaintext highlighter-rouge">enable-prefix-caching</code> 打开前面讲的混合 cache；tool-call 和 reasoning parser 则负责把模型输出解析成工具调用和思考/答案结构——<strong>模型只管吐 token，怎么解释 token 是部署侧的事。</strong></p> <p>vLLM 的 scheduler 需要同时管理两种 cache：MLA 的分页 KV block 和 KDA 的固定状态 block。prefill 使用 FlashKDA，decode 使用融合 CUDA kernel。高吞吐部署还可以把 prefill 和 decode 分到不同机器：prefill 侧用 <strong>TEP</strong>（Tensor-Expert Parallelism，把 attention 做 TP、MoE 做 EP 的并行组合）快速处理长 prompt，decode 侧用 <strong>DEP</strong>（Decode-Expert Parallelism，以专家并行为主的解码并行）提高逐 token 吞吐，中间通过 <strong>NIXL</strong>（Non-blocking Inter-node eXchange Layer，跨节点数据传输层）发送 MLA KV、KDA 状态和 block table。</p> <p><img src="/assets/img/posts/20260825-kimi-k3-03-infra/figure11_p19.png" alt="K3 serving 的计算与通信重叠"/></p> <p>这张图反映的是另一个系统事实：当计算、通信和 offload 能够重叠时，<strong>单独优化某一个 kernel 未必决定最终性能；调度顺序和 buffer 生命周期同样重要</strong>。</p> <h2 id="9-系列小结新架构把哪些老问题重新打开了">9. 系列小结：新架构把哪些老问题重新打开了</h2> <p>回头看整个系列：KDA 减少了随 context 增长的状态，但引入了状态传递、checkpoint 和回滚问题；AttnRes 提供了深度方向的检索，但要求系统管理跨层表示；LatentMoE 压低了专家计算成本，却要求更严格的负载均衡和激活稳定性。</p> <p>对应到 infra，就是一条连锁反应：</p> <ol> <li><strong>MoonEP</strong> 把动态专家路由约束成固定 shape，减少 EP 的尾部等待；</li> <li><strong>KCP</strong> 用固定大小 fragment 做 KDA 的 context parallelism——传”变换规则”而不是”完整状态”；</li> <li><strong>混合 prefix cache</strong> 同时管理分页 KV 和稀疏 checkpoint，匹配粒度与存储成本折中；</li> <li><strong>Speculative decoding</strong> 通过重放状态避免保存大量快照；</li> <li><strong>量化感知训练</strong>让训练、rollout 和 serving 使用接近的数值格式；</li> <li><strong>vLLM</strong> 把这些结构暴露成 scheduler、kernel 和部署参数，而不是只停留在论文里的模块图。</li> </ol> <p>Kimi K3 这份报告的系统价值，主要就在这里：<strong>它没有把”新 attention”“超大 MoE”“1M context”当成互不相干的卖点，而是展示了每个结构变化会把哪些系统假设推翻。</strong> 真正部署时，决定成败的往往不是某个公式单独快多少，而是状态、通信、内存和调度能不能在同一条 pipeline 里对上。</p> <hr/> <blockquote> <p>扯一句题外话：打个广告，《动手学 AutoML：从 NAS 到大语言模型优化实战》系统地介绍了如何自动化地设计、搜索和压缩模型架构，结合最新的 attention 设计范式，可以让 AI 自动搜出更高效的结构，感兴趣的同学可以看看我们的这本书。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="MoE"/><category term="分布式训练"/><category term="Serving"/><category term="KV Cache"/><category term="论文解读"/><summary type="html"><![CDATA[Kimi K3技术详解系列（三）：训练与 Serving Infra]]></summary></entry></feed>