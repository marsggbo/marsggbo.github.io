<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://marsggbo.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://marsggbo.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-06-25T09:37:36+00:00</updated><id>https://marsggbo.github.io/feed.xml</id><title type="html">HE Xin</title><subtitle>&quot;SSS: Study, Sleep, Slim&quot; </subtitle><entry><title type="html"></title><link href="https://marsggbo.github.io/blog/2026/2026-06-07-2605_29350/" rel="alternate" type="text/html" title=""/><published>2026-06-25T09:37:36+00:00</published><updated>2026-06-25T09:37:36+00:00</updated><id>https://marsggbo.github.io/blog/2026/2026-06-07-2605_29350</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/2026-06-07-2605_29350/"><![CDATA[<blockquote> <p>插播：之前写的<a href="https://item.jd.com/14945889.html">《动手学 AutoML》</a>终于出版了，从 NAS 到超参优化都有覆盖，适合想系统入门 AutoML 的同学。好了广告结束，现在进入正题。</p> <p><img src="/assets/img/book_cover_automl.png" alt="动手学AutoML书籍封面"/></p> </blockquote> <h1 id="moe-压缩新思路别删专家别合并专家把它们重映射就够了">MoE 压缩新思路：别删专家、别合并专家，把它们”重映射”就够了</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2605.29350">ConMoE: Expert-Pool Consolidation via Prototype Reassignment for MoE Compression</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有想过，MoE 架构最骄傲的地方，恰恰也是最烫手的地方？</p> <p>MoE（Mixture-of-Experts）的核心设计是稀疏激活——每个 token 只走少数几个 expert，理论上参数量很大但计算量很小。Qwen3-30B-A3B 的名字里 A3B 的意思就是”激活参数 3B”，但总参数有 30B。听起来很美，对吧？</p> <p>但问题来了：<strong>这 30B 参数，你一个都不能少地加载进显存</strong>。</p> <p>不管一个 token 实际上只用了 3B 参数，那剩下 27B 的专家还是得老老实实待在显存里候命。这就是 MoE 推理的核心矛盾：稀疏激活省了算力，但显存一分没省。随着模型越做越大，expert 存储变成了 MoE 落地的最大障碍之一。</p> <p>现有的压缩思路主要两条路：<strong>剪掉专家（pruning）</strong> 和 <strong>合并专家（merging）</strong>。前者直接删掉一些专家，后者把多个专家的权重”揉”成一个新专家。</p> <p>今天要聊的这篇论文提了第三条路：<strong>什么都不删，什么都不合并，只是把原来的专家引用”重新指向”一个更小的专家池</strong>。</p> <p>听起来像绕口令？别急，慢慢来。</p> <hr/> <h2 id="2-现有方案的痛点">2. 现有方案的痛点</h2> <p>在聊 ConMoE 之前，先交代下背景，说说 pruning 和 merging 各自的问题。</p> <p><strong>Expert Pruning（专家剪枝）</strong>：直接删掉贡献低的专家。简单粗暴，但问题是删了就是删了，原来指向这个专家的 router 决策没有任何”容错”——被删专家的那些 token 只能丢给其他留下来的专家，不管合不合适。</p> <p><strong>Expert Merging（专家合并）</strong>：把多个专家的权重融合成一个新专家。听起来更优雅，但实际上构造了一个”原来不存在”的新权重，这个新权重可能引入了原本没有的错误，而且融合方式很敏感，调参难度大。</p> <p>这两类方法有一个共同问题：<strong>把”保留哪些专家参数”和”原来的 expert slot 该怎么被表示”这两件事耦合在一起了</strong>。</p> <p>ConMoE 的作者们说，等等，这两个问题应该分开来看。</p> <hr/> <h2 id="3-conmoe-的核心思路专家池整合expert-pool-consolidation">3. ConMoE 的核心思路：专家池整合（Expert-Pool Consolidation）</h2> <p>论文提出了一个新的框架视角，叫 <strong>expert-pool consolidation</strong>（专家池整合）。</p> <p>核心思想拆成两步：</p> <ol> <li>从原来的专家池里<strong>挑出一个更小的子集</strong>作为”原型（prototype）”</li> <li>把每个原来的专家引用<strong>确定性地重映射</strong>到某个 prototype 上</li> </ol> <p>这样，router 对外还是看到原来那些 expert slot，内部只是把调用转发给对应的 prototype。<strong>专家的权重没有被修改，没有被融合，prototype 就是原来的 pretrained 专家</strong>。</p> <p>如下图，直观地展示了 ConMoE 的整体流程：</p> <p><img src="/assets/img/posts/2605_29350/figure1_p3.png" alt="ConMoE 整体架构示意"/></p> <p>图里的流程很清晰：左边是预训练的 MoE，有若干层，每层有自己的专家池。ConMoE 在若干相邻层构成的 local scope 里，用 calibration 统计数据和专家间的距离来选出 prototype，然后把原来的每个专家 slot 都确定性地指向某个 prototype。最右边压缩后的 MoE 对外接口和原来一样（router 不变），只是内部的专家池变小了。</p> <p>这里有一个很巧妙的地方：<strong>prototype 可以跨层共享</strong>。也就是说，相邻几层的专家可以共用一个 prototype 池，这让压缩比更高，同时也利用了相邻层之间专家的冗余性。</p> <hr/> <h2 id="4-怎么挑-prototype两个信号的乘积">4. 怎么挑 prototype？两个信号的乘积</h2> <p>这是方法里最关键的设计。对于每个 local scope $G$，ConMoE 给每个专家算一个分数 $s_e$，然后取 top-K 作为 prototype。</p> <p>分数由两个信号构成：</p> <p><strong>信号一：路由条件贡献分（contribution score）$a_e$</strong></p> \[a_e = \frac{1}{|\mathcal{D}_e|} \sum_{t \in \mathcal{D}_e} g_e(t) \cdot \|e(h_t)\|_2\] <p>说人话：在 calibration 数据上，这个专家被 router 选中的那些 token，它的输出强度乘以路由权重的平均值。分数高 = 这个专家被用到时贡献大。</p> <p><strong>信号二：可替换性分（replaceability score）$b_e$</strong></p> \[b_e = \min_{e' \in \mathcal{E}_G \setminus \{e\}} d(e, e')\] <p>就是这个专家在 scope 内，到其最近邻专家的距离。距离越大 = 越难被其他专家替代 = 越不可替换。</p> <p>两个分数各自在 scope 内做 min-max 归一化后相乘：</p> \[s_e = \bar{a}_e \cdot \bar{b}_e\] <p><strong>直觉很清晰</strong>：一个好的 prototype，既要在原来模型里有实质性的贡献（不能是闲置专家），又要在参数空间里相对独特（不能被其他专家轻易替代）。两个维度同时满足，才是值得被保留的 prototype。</p> <p>选好 prototype 后，每个原始专家就找自己最近的 prototype：</p> \[m_G(e) = \arg\min_{p \in P_G} d(e, p)\] <p>这就是重映射。干净，确定，不需要梯度，不需要 fine-tune。</p> <hr/> <h2 id="5-跨层共享scope-大了不一定好">5. 跨层共享：scope 大了不一定好</h2> <p>ConMoE 允许相邻几层共享一个 prototype 候选池（local scope），这让压缩更灵活。但 scope 到底设多大合适？</p> <p>如下图是在 Qwen3-30B-A3B 上做的消融，50% expert reduction 的情况下，不同 scope size 的效果：</p> <p><img src="/assets/img/posts/2605_29350/figure2_p7.png" alt="scope size 对性能的影响"/></p> <p>结果很有趣：scope 1（只在单层内选）、scope 2、scope 4 的表现非常接近，scope 4 在这个代表性子集上平均分最高。但 scope 一旦扩到 8 或 16 层，三个任务的性能都出现明显下降，HellaSwag 和 MMLU 掉得尤其多。</p> <p>这说明什么？<strong>相邻层之间确实存在可复用的冗余，但这种冗余是局部的</strong>。距离太远的层，对应的是模型不同深度的语义转换，强行共享 prototype 会引入深度不匹配的问题。</p> <p>这也解释了为什么 ConMoE 选择 bounded local scope，而不是一个全局的 expert pool。</p> <hr/> <h2 id="6-主实验结果">6. 主实验结果</h2> <p>实验在三个 MoE 模型上做了对比：Qwen3-30B-A3B、deepseek-moe-16b-base、OLMoE-1B-7B-0125，各自测了 25% 和 50% 的 expert reduction。Baseline 包括：</p> <ul> <li><strong>Pruning 类</strong>：Frequency pruning（按路由频率删）、REAP pruning（基于贡献分删）</li> <li><strong>Merging 类</strong>：M-SMoE（路由统计引导合并）、HC-SMoE（输出特征聚类合并）</li> </ul> <p>如下表是主要实验结果（6 个多选题 benchmark 的平均分）：</p> <p><img src="/assets/img/posts/2605_29350/table1_p6.png" alt="主实验结果表"/></p> <p>几个关键观察：</p> <ul> <li><strong>DeepSeek 上，ConMoE 在 25% 和 50% reduction 两个设置下都拿到最佳平均分</strong>（0.691 vs 0.681/0.676，0.651 vs 0.642/0.613），这是最亮眼的结果。</li> <li><strong>OLMoE 上，25% reduction 同样是 ConMoE 最好</strong>（0.676 vs 0.669/0.662），50% 时和最强 baseline 很接近。</li> <li><strong>Qwen3 上，pruning 方法特别强</strong>，尤其是 50% reduction 时 Frequency pruning 达到 0.733，ConMoE 是 0.717。但 ConMoE 在 WinoGrande 和 BoolQ 上保持了很强的表现。</li> </ul> <p>整体来说，ConMoE 作为一种全新的”不删不合并、只重映射”的压缩范式，<strong>在和 pruning/merging 同等压缩比下能打平甚至超过</strong>，这本身就是一个有意义的结论。</p> <hr/> <h2 id="7-消融实验哪个设计最重要">7. 消融实验：哪个设计最重要？</h2> <h3 id="71-整合结构consolidation-structure">7.1 整合结构（Consolidation Structure）</h3> <p>如下表对比了不同的整合方式：</p> <p><img src="/assets/img/posts/2605_29350/table2_p6_04.png" alt="整合结构消融实验"/></p> <p>三种 variant：</p> <ul> <li><strong>Layer-local</strong>：只在单层内选 prototype，不跨层</li> <li><strong>Cross-layer fixed-k</strong>：跨层候选池，但每层的 prototype 数量固定</li> <li><strong>Adaptive prototype</strong>：ConMoE 默认方案，用贡献分+可替换性自适应分配</li> </ul> <p>有意思的是，layer-local 已经很强了（ARC-C 0.515，HellaSwag 0.715，MMLU 0.651）。允许跨层候选池（Cross-layer fixed-k）在 HellaSwag 上略有提升，Adaptive prototype 和 Cross-layer fixed-k 表现接近。</p> <p><strong>结论：确定性重映射本身才是最重要的成分</strong>，跨层和自适应选择是锦上添花。</p> <h3 id="72-prototype-选择策略">7.2 Prototype 选择策略</h3> <p>如下表比较了不同的 prototype 选择信号：</p> <p><img src="/assets/img/posts/2605_29350/table3_p7_08.png" alt="prototype 选择消融"/></p> <ul> <li><strong>Distance-only</strong>（只看可替换性）：ARC-C 0.242，HellaSwag 0.319，MMLU 0.231 ——直接崩了</li> <li><strong>Usage top-k</strong>（只看路由频率）：HellaSwag 掉到 0.586，比较弱</li> <li><strong>REAP top-k</strong>（基于贡献分）：HellaSwag 回到 0.718，接近 ConMoE 默认</li> <li><strong>Adaptive prototype</strong>（ConMoE 默认）：HellaSwag 0.716，与 REAP top-k 相当</li> </ul> <p><strong>路由条件贡献分是主导信号</strong>，可替换性是辅助正则化（防止选出重复的 prototype），单独用距离作为选择标准完全行不通。</p> <h3 id="73-事后权重融合诊断">7.3 事后权重融合诊断</h3> <p>有人会问：选好 prototype 之后，能不能再做一下权重融合（把分配给同一 prototype 的那些原始专家的权重平均一下），效果会不会更好？</p> <p>如下表给出了答案：</p> <p><img src="/assets/img/posts/2605_29350/table4_p7.png" alt="事后权重融合诊断"/></p> <p>对比三种方案：</p> <ul> <li><strong>None（ConMoE 默认）</strong>：不做任何融合，ARC-C 0.504，HellaSwag 0.716，MMLU 0.639</li> <li><strong>Arcee fusion</strong>：HellaSwag 从 0.716 直接掉到 0.531，MMLU 从 0.639 掉到 0.512</li> <li><strong>Weighted average</strong>：更惨，HellaSwag 0.469，MMLU 0.358</li> </ul> <p><strong>ConMoE 的收益来自确定性重映射，而不是权重融合</strong>。直接复用原始的 pretrained prototype 才是最稳的策略，加了融合反而破坏了原始权重。</p> <hr/> <h2 id="8-跨层专家可替换性分析">8. 跨层专家可替换性分析</h2> <p>为什么跨层 scope 有效，但又不能太大？论文专门做了一个几何分析。</p> <p>在 Qwen3-30B-A3B 上，用 4 层 scope 计算每个 expert 在参数空间的最近邻，统计最近邻是否在同一层还是不同层：</p> <p><img src="/assets/img/posts/2605_29350/figure3_p8.png" alt="跨层最近邻分析"/></p> <p>左图是热力图，展示从 source layer 到最近邻 layer 的分布。可以看到主要沿对角线集中，说明最近邻大概率在附近的层，而非全局随机分布。</p> <p>右图是每层中”最近邻在不同层”的比例。<strong>整体上有 50.4% 的 expert，其参数空间的最近邻在另一层</strong>，部分中间层超过 70%。</p> <p>这两个观察共同说明：<strong>专家的可替换性不是严格层内的，相邻层之间存在真实的冗余</strong>——这是跨层共享 prototype 的几何依据。但这种冗余是局部的，不支持全局共享。</p> <hr/> <h2 id="9-总结">9. 总结</h2> <p>ConMoE 提供了一个很干净的框架来思考 MoE 压缩：</p> <ul> <li><strong>不删</strong>（pruning 的做法）</li> <li><strong>不改权重</strong>（merging 的做法）</li> <li><strong>只重映射</strong>：选出 pretrained 专家子集作为 prototype，把其他专家的引用指过去</li> </ul> <p>三个设计原则从实验里验证：</p> <ol> <li><strong>确定性重映射是最稳定的成分</strong>，单层内做都已经很强</li> <li><strong>跨层复用要保持局部</strong>，相邻层可以，太远的层会因深度不匹配掉性能</li> <li><strong>路由条件贡献分是主导信号</strong>，可替换性是辅助的正则化</li> </ol> <p>个人觉得这篇论文最有价值的点，是把”保留哪些专家参数”和”原来的 expert slot 怎么被表示”这两个问题<strong>显式地解耦</strong>了。这个视角在之前的 pruning/merging 框架里是隐含的，ConMoE 把它明确化之后，整个设计空间就清晰多了。</p> <p>当然 limitation 也直接：这套方案依赖 calibration 数据，跨层 scope 不能太大，而且现在的”logical reduction ratio”和实际的显存节省还需要专门的 runtime 支持（shared-prototype checkpoint 格式）才能落地。最后这点是整个 prototype sharing 方案的工程挑战，不只是这篇论文的问题。</p> <p>感兴趣的可以去翻原文，代码和复现脚本作者也有提到会开源。</p> <p>欢迎评论区交流，有什么遗漏或者理解有误的地方也欢迎指出~</p>]]></content><author><name></name></author></entry><entry><title type="html">LLM agent 为什么不稳？问题可能不在模型，在 harness</title><link href="https://marsggbo.github.io/blog/2026/20260625-llm-harness/" rel="alternate" type="text/html" title="LLM agent 为什么不稳？问题可能不在模型，在 harness"/><published>2026-06-25T00:00:00+00:00</published><updated>2026-06-25T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260625-llm-harness</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260625-llm-harness/"><![CDATA[<blockquote> <p>插播：之前写的<a href="https://item.jd.com/14945889.html">《动手学 AutoML》</a>终于出版了，从 NAS 到超参优化都有覆盖，适合想系统入门 AutoML 的同学。好了广告结束，现在进入正题。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote> <h1 id="llm-agent-为什么不稳问题可能不在模型在-harness">LLM agent 为什么不稳？问题可能不在模型，在 harness</h1> <blockquote> <p>原文：<a href="https://picrew.github.io/LLM-Harness/main.pdf">Agent Harness Engineering: A Survey</a></p> </blockquote> <hr/> <h2 id="1-前言一个反直觉的问题">1. 前言：一个反直觉的问题</h2> <p>你有没有遇到过这种情况：换了更强的模型，agent 表现没什么变化，甚至还倒退了？</p> <p>我相信做过一段时间 agent 开发的人都踩过这个坑。直觉告诉我们”模型越强，系统越好”，但现实往往不是这样。</p> <p>今天想聊聊这篇在审的综述——<strong>Agent Harness Engineering: A Survey</strong>（来自 CMU、Yale、Amazon 等多家机构）。它试图系统地回答一个核心问题：</p> <p><strong>LLM agent 的可靠性，到底是由模型决定的，还是由 harness 决定的？</strong></p> <p>作者的答案是：<strong>在很多情况下，harness 才是天花板，不是模型。</strong></p> <p>这个结论并不是拍脑袋的，论文里有三个非常硬的数字支撑：</p> <ul> <li>研究员 Bölük（2026）只改了 edit-tool 格式和 tool harness，<strong>一个下午让 15 个模型的 coding benchmark 最高提升了 10×</strong>，模型权重一字未动</li> <li>OpenAI 团队固定模型为 GPT-5.2-Codex，仅通过重构 system prompt、注入 middleware context、加 self-verification hook，Terminal-Bench 2.0 上的分数从 52.8% 升到 66.5%，<strong>+13.7 个百分点，相对提升 26%</strong></li> <li>Meta-Harness（Lee et al., 2026）通过自动化 harness 优化，在 Terminal-Bench-2 上跑到了 <strong>76.4%</strong>，超过所有手工设计的方案，没改模型</li> </ul> <p>每一个数字，都超过了同一 benchmark 上靠换模型通常能带来的 2~4 个点的提升幅度。</p> <p>这就是论文提出的 <strong>binding-constraint thesis</strong>（约束瓶颈假说）：对于长任务的 agent，<strong>benchmark 方差更多是由 harness 驱动的，而不是模型本身</strong>。</p> <p><img src="/assets/img/posts/20260625-llm-harness/figure1_p1.png" alt="prompt、context 和 harness 工程的对比"/></p> <hr/> <h2 id="2-先把概念捋清楚harness-到底是什么">2. 先把概念捋清楚：harness 到底是什么</h2> <p>很多人可能对”harness”这个词不熟悉。说人话，<strong>harness 就是包裹在 LLM 外面的那一整套 infrastructure</strong>：怎么管 context、怎么调工具、怎么编排多个 agent、出了错怎么恢复、有没有监控、有没有安全管控……</p> <p>论文把 2022 年到 2026 年的 agent 工程演化分成三个阶段：</p> <p><strong>Prompt Engineering（2022-2024）</strong>：优化 prompt 文本本身，一次 LLM call，一个静态输入。</p> <p><strong>Context Engineering（2025）</strong>：agent 开始跑多步任务，问题变成了”每一步给模型看什么信息”——怎么压缩 memory、怎么 retrieve、怎么避免 context 塞满。Anthropic 的 applied AI team 对此有专门的 blog，把它定义成”管理 LLM 推理时最优 token 集合的策略集”。</p> <p><strong>Harness Engineering（2026）</strong>：模型能力已经足够强，<strong>系统可靠性的瓶颈转移到了 infrastructure</strong>——状态怎么维护、工具接口怎么设计、错误怎么反馈、执行怎么约束、安全怎么管控。harness engineering 把所有这些当成一个整体来设计。</p> <p>三个阶段是包含关系，不是替代关系：harness engineering 包含 context engineering，context engineering 包含 prompt engineering。</p> <p><img src="/assets/img/posts/20260625-llm-harness/figure3_p7.png" alt="三个工程阶段的历史演化时间线"/></p> <hr/> <h2 id="3-etclovg一个七层分类框架">3. ETCLOVG：一个七层分类框架</h2> <p>论文的核心贡献之一是提出了 <strong>ETCLOVG</strong> 这个七层分类体系，覆盖了 harness 的所有工程维度。下面逐层拆解，每层说清楚它解决什么问题。</p> <p><img src="/assets/img/posts/20260625-llm-harness/figure2_p6.png" alt="ETCLOVG七层分类框架总览"/></p> <h3 id="31-e--execution-environment--sandbox执行环境">3.1 E — Execution Environment &amp; Sandbox（执行环境）</h3> <p>agent 代码跑在哪里？在什么约束下运行？</p> <p>这一层决定了 agent 的”活动边界”。沙箱不只是安全隔离，更重要的是它同时扮演两个角色：<strong>既是笼子，也是授权</strong>。Anthropic 报告称，引入沙箱机制后，Claude Code 的权限弹窗数量减少了 84%，同时保持安全性不变。</p> <p>代表工具：E2B、Daytona、Docker Sandboxes、SWE-ReX（用于 eval 的可重置沙箱）。</p> <p><img src="/assets/img/posts/20260625-llm-harness/figure6_clean.png" alt="执行环境与沙箱系统全景"/></p> <h3 id="32-t--tool-interface--protocol工具接口">3.2 T — Tool Interface &amp; Protocol（工具接口）</h3> <p>怎么描述工具、怎么发现、怎么调用？</p> <p>光有工具不够，关键是 agent 能不能在正确的时机用正确的工具。论文的一个核心结论：<strong>“fewer but better tools” 往往优于暴力扔一大堆工具</strong>，因为工具少了，模型选择错误的概率会下降，prompt entropy 也更低。</p> <p>代表工具：MCP（Anthropic）、A2A protocol、LangChain tool 系统。</p> <p><img src="/assets/img/posts/20260625-llm-harness/figure7_clean.png" alt="工具接口与协议系统全景"/></p> <h3 id="33-c--context--memory-management上下文与记忆管理">3.3 C — Context &amp; Memory Management（上下文与记忆管理）</h3> <p>给模型看什么？看多久？</p> <p>这一层是整篇综述里我认为最值得深挖的部分。有两个现象论文描述得很精准：</p> <p><strong>Context rot（上下文腐败）</strong>：一个号称支持 200K token 的模型，在 50K 的时候就开始出现性能下降。随着 tool 结果、中间推理、文件内容不断堆积，模型对早期信息的注意力会下降。</p> <p><strong>Context drift（上下文漂移）</strong>：跑 100 步以上的任务，agent 会反复做已经做过的事、违背之前自己定的决策、忘记最初的目标。压缩技术（compaction）只是延缓了这个过程，每次压缩带来的细微误差会随时间累积，agent 的内部状态最终会和真实任务环境发生偏离，<strong>而且现有架构对这个偏离没有任何检测机制</strong>。</p> <p>代表工具：MemGPT、Mem0、Hindsight（Vectorize.io）、Anthropic 的 context compaction。</p> <p><img src="/assets/img/posts/20260625-llm-harness/figure8_clean.png" alt="上下文与记忆管理系统全景"/></p> <h3 id="34-l--lifecycle--orchestration生命周期与编排">3.4 L — Lifecycle &amp; Orchestration（生命周期与编排）</h3> <p>控制流怎么跑？单 agent loop 还是多 agent 协作？</p> <p>从最简单的 observe-think-act 循环（ReAct 范式），到 issue → plan → code → validate → PR 的完整 pipeline，这一层管的是任务执行的整体编排。</p> <p>值得注意的是：<strong>状态管理被论文放在这一层</strong>，而不是上下文层，因为状态天然和执行流耦合在一起。</p> <p>代表工具：LangGraph、AutoGen、CrewAI、OpenHands、GitHub Agentic Workflows。</p> <p><img src="/assets/img/posts/20260625-llm-harness/figure9_clean.png" alt="生命周期与编排系统全景"/></p> <h3 id="35-o--observability--operations可观测性">3.5 O — Observability &amp; Operations（可观测性）</h3> <p>你的 agent 在干什么？为什么失败？花了多少钱？</p> <p>这一层在现有框架里经常被当成”可选项”，但论文特意把它提升为独立的第一类层。</p> <p>一个让我印象深刻的数据：<strong>89% 的团队在用 observability 工具，但只有 52.4% 在做 offline evaluation</strong>（LangChain 2026 年调查）。也就是说，大多数团队能看到 agent 在做什么，但没有系统地判断做的对不对。</p> <p>代表工具：Langfuse、AgentOps、OpenTelemetry、Phoenix（Arize）。</p> <p><img src="/assets/img/posts/20260625-llm-harness/figure10_clean.png" alt="可观测性与运营系统全景"/></p> <h3 id="36-v--verification--evaluation验证与评估">3.6 V — Verification &amp; Evaluation（验证与评估）</h3> <p>怎么判断 agent 跑得好不好？</p> <p>论文把评估设计成一个五阶段的生命周期，而不是最终一个分数：</p> <ol> <li><strong>任务和 benchmark 的接地</strong>：确保评估环境是真实的</li> <li><strong>执行前就绪验证</strong>：环境准没准备好、工具装没装好</li> <li><strong>受控执行和 trace 采集</strong>：跑起来并且完整记录</li> <li><strong>多层判断和失败归因</strong>：失败了是模型问题、工具问题、还是 harness 问题？</li> <li><strong>持续回归和部署反馈</strong>：一个不断闭环的质量控制循环</li> </ol> <p>一个 final score 不够用。对于长任务 agent，核心问题不只是”成没成功”，而是”<strong>为什么成功或失败、路径合不合理、哪个组件该改进</strong>“。</p> <p><img src="/assets/img/posts/20260625-llm-harness/figure11_clean.png" alt="验证与评估系统全景"/></p> <h3 id="37-g--governance--security治理与安全">3.7 G — Governance &amp; Security（治理与安全）</h3> <p>谁能做什么？怎么防 prompt injection？出了问题怎么审计？</p> <p>这一层也是论文特意升为独立层的。和普通的”安全加固”不同，governance 的目标是<strong>在 agent 有自主权的前提下，定义它的行动边界</strong>。</p> <p><img src="/assets/img/posts/20260625-llm-harness/figure14_p41.png" alt="治理与安全生命周期钩子"/></p> <p>论文列出了四个治理 hook 点：输入验证、工具调用前、工具调用后、输出验证。代表工具包括 CaMeL（Anthropic 的 prompt injection 防御方案）、AgentSpec、Claude’s Constitution。</p> <hr/> <h2 id="4-生态现状哪层厚哪层薄">4. 生态现状：哪层厚，哪层薄</h2> <p>论文把 148+ 个开源项目映射到 ETCLOVG，有几个值得关注的发现：</p> <ul> <li><strong>E（执行环境）、T（工具）、L（编排）、V（评估）</strong> 覆盖密集，开源工具多</li> <li><strong>O（可观测性）、G（治理）</strong> 相对薄弱，更多依赖商业平台</li> <li>有三类在早期语料里缺失的新项目现在已经成熟：<strong>task runner（任务运行器）、multi-agent orchestrator、spec-driven 开发工具</strong></li> </ul> <hr/> <h2 id="5-几个跨层的核心矛盾">5. 几个跨层的核心矛盾</h2> <p>论文第 11 章的 cross-layer synthesis 是最有意思的部分，提炼了三个无法靠单层解决的系统级矛盾：</p> <p><strong>Cost-Quality-Speed Trilemma（成本-质量-速度三角）</strong>：更强的沙箱更安全但更贵更慢；更丰富的 context 更准确但消耗 token 更多；更深的评估更可靠但更慢。生产系统必须在三者之间做权衡，没有免费的午餐。</p> <p><strong>Capability-Control Tradeoff（能力-控制权衡）</strong>：给 agent 越多权限，它能做的事越多，但 blast radius 也越大。工具列表越长，越容易选错，prompt injection 的攻击面也越大。这不是安全模块的问题，这是整个 harness 设计的核心轴线。</p> <p><strong>Harness Coupling Problem（harness 耦合问题）</strong>：各层之间是耦合的。执行环境的差异会影响评估结果；工具描述消耗 context budget 从而影响模型行为；可观测性日志只有在身份信息对齐的情况下才能成为治理证据。<strong>局部优化各层，整体不一定更好。</strong></p> <hr/> <h2 id="6-最有意思的-open-problem随着模型变强harness-应该越来越简单">6. 最有意思的 open problem：随着模型变强，harness 应该越来越简单</h2> <p>论文最后一部分有一个反直觉的观点，我觉得很值得单独拿出来说：</p> <blockquote> <p><strong>每一个 harness 组件，本质上都是在假设”模型自己做不好某件事”。</strong></p> </blockquote> <p>随着模型能力增强，有些 harness 干预理应变得不再必要。Anthropic 就报告过一个例子：为某个模型设计的 context reset 机制，在换了更强的模型之后反而成了累赘——去掉它，成本下降，质量不变。</p> <p>这引出了一个 <strong>meta-engineering 问题</strong>：harness 本身需要有机制来评估自己哪些部分还有用、哪些可以去掉。论文把这个称为 <strong>adaptive simplification</strong>（自适应简化）——harness 不应该随着时间越堆越厚，而应该随着模型变强而变得更精简。</p> <p>目前这个方向的研究还非常早期。Meta-Harness 和 Natural-Language Agent Harnesses 在探索”把 harness 本身作为优化目标”，但离生产可用还差很远。</p> <hr/> <h2 id="7-写在最后">7. 写在最后</h2> <p>这篇综述的价值，我觉得有两层：</p> <p><strong>对工程师来说</strong>，ETCLOVG 是一个非常实用的 checklist。你在搭一个 agent 系统的时候，可以对着这七层问自己：执行环境有没有沙箱？工具接口有没有设计得让模型容易用？context 管理有没有处理 context rot？有没有可观测性？有没有做持续回归测试？治理和权限控制有没有？</p> <p><strong>对研究者来说</strong>，binding-constraint thesis 是一个值得认真对待的命题。如果你在做 agent eval，下次在 benchmark 上刷分之前，先问一下：我换的是模型，还是 harness？如果换的是 harness，那分数提升说明的是 harness 的效果，不是模型能力。这两件事不应该被混淆。</p> <p>当然，这篇论文本身也有局限——它是综述，不是实证研究。binding-constraint thesis 的三个支撑数据来自有限的场景（coding benchmark），不代表所有 agent 任务。模型和 harness 谁更重要，可能取决于任务类型、任务长度、模型代际。<strong>目前我们还缺少一个能系统控制变量的 factorial 实验</strong>，同时控制模型和 harness 的多个维度。这本身也是论文点出的 open problem 之一。</p> <p>有什么想法欢迎评论区交流，尤其是有实际 agent 工程经验的同学，很想听听你们觉得哪一层在实践中是最大的坑。</p>]]></content><author><name></name></author><category term="LLM"/><category term="Agent"/><category term="论文解读"/><category term="工程"/><summary type="html"><![CDATA[插播：之前写的《动手学 AutoML》终于出版了，从 NAS 到超参优化都有覆盖，适合想系统入门 AutoML 的同学。好了广告结束，现在进入正题。]]></summary></entry><entry><title type="html">COLM’25 | PredGen：你还在说话，LLM 已经想好怎么回了</title><link href="https://marsggbo.github.io/blog/2026/20260625-predgen/" rel="alternate" type="text/html" title="COLM’25 | PredGen：你还在说话，LLM 已经想好怎么回了"/><published>2026-06-25T00:00:00+00:00</published><updated>2026-06-25T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260625-predgen</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260625-predgen/"><![CDATA[<blockquote> <p>插播：之前写的<a href="https://item.jd.com/14945889.html">《动手学 AutoML》</a>终于出版了，从 NAS 到超参优化都有覆盖，适合想系统入门 AutoML 的同学。好了广告结束，现在进入正题。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote> <h1 id="colm25--predgen你还在说话llm-已经想好怎么回了">COLM’25 | PredGen：你还在说话，LLM 已经想好怎么回了</h1> <blockquote> <p>原文：<a href="https://openreview.net/pdf?id=2Kl8Ztw6wk">PredGen: Accelerated Inference of Large Language Models through Input-Time Speculation for Real-Time Speech Interaction</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有想过，和语音助手对话时，那段烦人的”停顿感”是哪来的？</p> <p>不是网络延迟，不是模型太大——<strong>核心症结在于：LLM 必须等你说完，才能开始想怎么回。</strong></p> <p>这句话看起来是废话，但仔细想想：你说一句话通常要 2-5 秒，这 2-5 秒里 GPU 在干嘛？啥也没干，白白闲着。等你说完，LLM 开始生成，生成完第一句，再喂给 TTS（文字转语音）系统，TTS 再输出声音——用户实际感受到的延迟，是这一串链条的总和。</p> <p>这篇来自 COLM 2025 的工作 <strong>PredGen（Predictive Generation）</strong> 提出了一个非常直觉的想法：<strong>既然 GPU 在等你说话时是空闲的，不如趁这个窗口先”猜”你想问什么，提前把回答准备好一部分。</strong> 实验结果显示，audio latency 能压缩大约 <strong>2×</strong>，而额外的计算开销极小。</p> <hr/> <h2 id="2-问题背景语音交互的延迟到底卡在哪">2. 问题背景：语音交互的延迟到底卡在哪</h2> <p>现在的语音对话系统基本是一条流水线：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>用户说话（ASR） → 文字 → LLM 生成回复 → TTS 转成语音 → 播放
</code></pre></div></div> <p>其中 <strong>Audio Latency（音频延迟）</strong> 是用户体验的核心指标，也就是”你说完到你听到第一个字”之间的等待时间。</p> <p>现有优化方向大多集中在：</p> <ul> <li>用更快的解码算法（如 speculative decoding）减少 LLM 生成时间</li> <li>用流式 TTS，边生成文字边合成语音</li> </ul> <p>这些方案都在”你说完之后”的环节做文章。<strong>但 PredGen 换了个角度：在你还没说完时，就偷偷开始干活。</strong></p> <hr/> <h2 id="3-predgen-的核心思路">3. PredGen 的核心思路</h2> <p>整体 pipeline 如下图：</p> <p><img src="/assets/img/posts/20260625-predgen/figure1_p2.png" alt="PredGen 整体流程"/></p> <p>思路可以用一句话概括：<strong>边听用户说话，边用当前已知的部分 prompt 预测回复；等用户说完，用 verifier 验证预测结果，接受能用的部分，丢掉不符合的。</strong></p> <p>这本质上是把 speculative decoding 的思路，从”token 层面的预测”搬到了”输入时间维度”——利用的是同一个洞察：<strong>预测比验证便宜，只要命中率够高，整体就是赚的。</strong></p> <h3 id="31-具体怎么运作">3.1 具体怎么运作</h3> <p>详细的算法设计如下图：</p> <p><img src="/assets/img/posts/20260625-predgen/figure2_p4.png" alt="PredGen 算法设计：预测更新与 Jacobi 解码"/></p> <p>分两个核心子模块：</p> <p><strong>（a）预测-验证循环（Predictive-Verify Loop）</strong></p> <p>用户每说完一小段 prompt（记为 $P_{i+1}$），系统：</p> <ol> <li>用当前已有的 response $R_i$ 作为”猜测”</li> <li>用 verifier 检查 $R_i$ 里有多少 token（$k_i$ 个）在新 prompt 下仍然成立</li> <li>接受这 $k_i$ 个 token，在此基础上继续生成更新后的 $R_{i+1}$，同时合成第一句话的音频 $A_{i+1}$</li> </ol> <p>这样每轮循环，已经生成并验证通过的部分就会积累，等用户说完的那一刻，很可能第一句回复已经准备好了。</p> <p>算法伪代码：</p> <p><img src="/assets/img/posts/20260625-predgen/algorithm1_p5.png" alt="PredGen 推理流水线算法"/></p> <p><strong>（b）Jacobi 解码（更快的 AR 生成）</strong></p> <p>在”预测”阶段，为了让每轮的生成速度更快，PredGen 引入了 Jacobi decoding。相比标准的 autoregressive 解码（一个 token 一个 token 串行出），Jacobi decoding 通过多步并行迭代近似来加速生成，特别适合这里”宁快勿慢”的场景——反正结果最后还要过 verifier。</p> <h3 id="32-用户说话有多快">3.2 用户说话有多快？</h3> <p>论文里有一个细节：实验设定的输入速率是 <strong>600 字符/分钟</strong>，折算下来大概 10 字/秒，接近正常说话速度。这意味着用户说一句完整的问题，系统有好几秒可以提前”猜”，时间窗口其实相当可观。</p> <hr/> <h2 id="4-实验结果">4. 实验结果</h2> <h3 id="41-主结果">4.1 主结果</h3> <p>核心数据如下：</p> <p><img src="/assets/img/posts/20260625-predgen/table1_p8.png" alt="主实验结果：四个数据集上的延迟对比"/></p> <p><strong>所有 PredGen 变体都实现了至少 1.6× 的 audio latency 加速</strong>，在部分场景接近 2×。</p> <p><img src="/assets/img/posts/20260625-predgen/table1_p7.png" alt="各变体加速倍数汇总"/></p> <p>Latency 和 TTFS（Time-To-First-Sound）的单位是 ms，数字是实打实的工程收益。</p> <h3 id="42-不同-llm-的泛化性">4.2 不同 LLM 的泛化性</h3> <p><img src="/assets/img/posts/20260625-predgen/figure3_p8.png" alt="四款 8B 量级 LLM 的 audio latency 对比"/></p> <p>这张图说明 PredGen 在 Llama、Mistral 等四款约 8B 规模的模型上都有效，不依赖某个特定模型架构。对多轮对话（MT-bench）的 NFETFS（Normalized First Echo Time-to-First-Sound）也做了消融，效果持续稳定。</p> <h3 id="43-top-k-接受策略的影响">4.3 Top-K 接受策略的影响</h3> <p><img src="/assets/img/posts/20260625-predgen/figure4_p15.png" alt="Top-K 接受策略消融实验"/></p> <p>verifier 的接受策略（Top-K）对速度和质量都有影响，K 太小则接受率低（预测白做），K 太大则质量下滑。论文里给出了最优区间的参数建议。</p> <hr/> <h2 id="5-个人-take">5. 个人 take</h2> <p>这篇工作的出发点非常接地气——<strong>不是在 GPU 更忙的地方榨性能，而是在 GPU 闲着的时候多干活</strong>，属于”填谷”而不是”削峰”的思路。</p> <p>几个觉得有意思的点：</p> <ol> <li> <p><strong>和传统 speculative decoding 的区别</strong>：经典 speculative decoding 是”用小模型猜，大模型验”，在 token 级别并行。PredGen 是”用部分 prompt 猜，完整 prompt 验”，在时间维度并行。两者都是”预测比验证便宜”这个核心假设，但利用的资源完全不同。</p> </li> <li> <p><strong>适用场景的边界</strong>：这套方案对”用户输入有明确时间窗口”的场景（语音对话、慢速打字）非常有效。但对于 API 调用、批量推理这类场景，用户 prompt 是一次性给完的，就没有可利用的输入时间窗口了。</p> </li> <li> <p><strong>实际部署的工程考量</strong>：预测阶段的 LLM forward 会抢占推理资源，如果系统同时有其他请求，资源调度会更复杂。论文里的实验是单用户场景，多并发下的 overhead 值得后续研究。</p> </li> </ol> <p>总体来说，<strong>思路简洁、切中实际痛点、数据漂亮</strong>，是那种”想到之后会拍大腿”的工作。语音交互这个场景下，2× 的延迟降低对用户感知影响是非线性的——从 800ms 降到 400ms，体感差距比数字看起来大得多。</p> <p>感兴趣的同学可以去看原文，有完整的算法描述和更多消融实验。欢迎评论区交流～</p> <hr/> <table> <tbody> <tr> <td>*作者：Shufan Li, Aditya Grover</td> <td>COLM 2025*</td> </tr> </tbody> </table>]]></content><author><name></name></author><category term="LLM"/><category term="推理优化"/><category term="语音交互"/><category term="投机解码"/><category term="论文解读"/><summary type="html"><![CDATA[插播：之前写的《动手学 AutoML》终于出版了，从 NAS 到超参优化都有覆盖，适合想系统入门 AutoML 的同学。好了广告结束，现在进入正题。]]></summary></entry><entry><title type="html">SpecASR：ASR 专属 Speculative Decoding，让 LLM 语音识别快 3.79 倍</title><link href="https://marsggbo.github.io/blog/2026/20260625-specasr/" rel="alternate" type="text/html" title="SpecASR：ASR 专属 Speculative Decoding，让 LLM 语音识别快 3.79 倍"/><published>2026-06-25T00:00:00+00:00</published><updated>2026-06-25T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260625-specasr</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260625-specasr/"><![CDATA[<blockquote> <p>插播：之前写的<a href="https://item.jd.com/14945889.html">《动手学 AutoML》</a>终于出版了，从 NAS 到超参优化都有覆盖，适合想系统入门 AutoML 的同学。好了广告结束，现在进入正题。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote> <h1 id="specasrasr-专属-speculative-decoding让-llm-语音识别快-379-倍">SpecASR：ASR 专属 Speculative Decoding，让 LLM 语音识别快 3.79 倍</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2507.18181">SpecASR: Accelerating LLM-based Automatic Speech Recognition via Speculative Decoding</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有用过那些”基于大模型的语音识别”产品？识别准是真的准，但就是有一种说不清的”顿挫感”。</p> <p>这背后的原因其实很简单：<strong>LLM decoder 的 autoregressive decoding 是逐 token 生成的，用在 ASR 上就意味着每说一句话，模型要一个字一个字地”想”出来</strong>。</p> <p>像 Seed-ASR 的 LLM decoder 参数量超过 10B，Speech-Llama 是 7B，BESTOW 也有 1.1B。相比之下，audio encoder 通常不到 1B，有的甚至不到 100M。问题显而易见：<strong>计算瓶颈全卡在 LLM decoder 上</strong>。</p> <p>Speculative Decoding（投机解码）是目前公认的解决 LLM 推理延迟的有效方案之一。核心思路是：用一个小模型（draft model）先”猜”几个 token，再让大模型（target model）并行验证，猜对了就批量接受，猜错了回退重生成。</p> <p><img src="/assets/img/posts/20260625-specasr/004_Figure_3.png" alt="Autoregressive 解码（左）vs Speculative 解码（右）"/></p> <p>如上图，speculative decoding 通过并行验证，把原本串行的多步生成压缩成一次 forward，大幅降低延迟。</p> <p>但问题是，现有的 speculative decoding 方案都是为纯文本任务设计的，直接搬到 ASR 上只能拿到 1.5×-2× 的加速，远远不够。</p> <p>今天要聊的这篇工作，<strong>SpecASR</strong>，北大的团队专门针对 ASR 任务的特性重新设计了 speculative decoding 框架，最终达到了 <strong>3.04×-3.79×</strong> 的加速——而且不损失任何识别精度。</p> <h2 id="2-llm-based-asr-到底慢在哪">2. LLM-based ASR 到底慢在哪</h2> <p>先看数据。下面两张图是主流 LLM-based ASR 模型的参数量和相对延迟拆解：</p> <p><img src="/assets/img/posts/20260625-specasr/001_a.png" alt="各模型 LLM decoder 与 audio encoder 参数量对比"/></p> <p><img src="/assets/img/posts/20260625-specasr/002_a.png" alt="各模型 LLM decoder 与 audio encoder 延迟占比"/></p> <p>LLM decoder 无论参数量还是推理延迟都占绝对大头——<strong>audio encoder 那点计算量几乎可以忽略不计</strong>。</p> <p>这背后的原因是架构决定的。LLM-based ASR 是一个级联系统：</p> <p><img src="/assets/img/posts/20260625-specasr/003_Figure_2.png" alt="LLM-based ASR 的级联架构"/></p> <p>整个流程：Speech Encoder 把语音帧压缩成高维特征，再拼上 text prompt，喂给 LLM Decoder 逐 token 生成转录文字。Speech Encoder 只跑一次（等价于 prefill），<strong>瓶颈完全在 LLM Decoder 的 autoregressive decoding 上</strong>。</p> <p>这也是为什么 speculative decoding 是一个有价值的切入点。</p> <h2 id="3-为什么-asr-适合更激进的-speculative-decoding">3. 为什么 ASR 适合更激进的 Speculative Decoding？</h2> <p>这篇工作有一个很核心的 insight，我觉得是整篇文章最有意思的地方：</p> <p><strong>ASR 解码是 audio-conditioned 的。</strong></p> <p>什么意思？普通文本生成任务里，大小模型的”思路”会随着生成的 token 不断发散——小模型猜错了，后面全歪了。但 ASR 不一样：无论模型大小，它们都在”转录同一段音频”，音频信号就像一根锚把大小模型的输出拉到了同一个方向。</p> <p>实验结果非常直观（如下图，小模型在 ASR 任务里的 acceptance rate 明显高于纯文本任务的各种 top-k 采样）：</p> <p><img src="/assets/img/posts/20260625-specasr/007_a.png" alt="ASR vs 文本任务的 token acceptance ratio 对比"/></p> <p>这意味着在 ASR 场景下，我们可以：</p> <ol> <li><strong>用更长的 draft sequence</strong>（普通场景一般 4-8 token，这里可以到 24 token）</li> <li><strong>更激进地做 token tree 扩展</strong>（因为大小模型对齐度高）</li> </ol> <p>这就是 SpecASR 的出发点。</p> <h2 id="4-specasr-的三板斧">4. SpecASR 的三板斧</h2> <p>整体框架如下：</p> <p><img src="/assets/img/posts/20260625-specasr/011_Figure_8.png" alt="SpecASR 总体框架"/></p> <p>SpecASR 提出了三个核心技术，来分别对应 draft generation 效率和 target verification 效率的提升。</p> <h3 id="41-自适应单序列预测adaptive-single-sequence-prediction">4.1 自适应单序列预测（Adaptive Single-sequence Prediction）</h3> <p>核心思路：<strong>不设固定的 draft 长度，而是根据当前 token 的 logit 置信度动态截断</strong>。</p> <p>具体来说，预测过程中会实时监控每个 draft token 的 normalized logit。如果某个位置的 logit 低于设定阈值，说明这个 token “没把握”，与其让它继续往后猜一堆大概率错的 token，不如提前把当前的 draft 序列送给 target model 验证。</p> <p>这样做的好处是两面的：</p> <ul> <li><strong>高置信度时</strong>：大胆猜长序列（up to 24 tokens），充分利用 ASR 的 audio-conditioned 对齐优势</li> <li><strong>低置信度时</strong>：及时刹车，避免 draft model 浪费算力</li> </ul> <h3 id="42-draft-sequence-复用策略draft-sequence-recycling">4.2 Draft Sequence 复用策略（Draft Sequence Recycling）</h3> <p>这是一个很工程化、也很实用的优化。</p> <p>结合下图理解：</p> <p><img src="/assets/img/posts/20260625-specasr/012_Figure_9.png" alt="Draft Sequence 复用策略示意"/></p> <p>问题背景是：就算有了自适应截断，被 target model 拒绝的 token 依然存在。被拒绝的 draft tokens 通常不会直接被抛弃——它们对下一轮的预测其实还有参考价值。</p> <p>SpecASR 的做法是：把上一轮 draft 序列里”未被接受但可以拼接”的部分<strong>复用到下一轮的 draft 生成起点</strong>，而不是从头开始让 draft model 重新生成。</p> <p>这直接减少了 draft model 的重复计算，实验数据是：<strong>减少了 74.1% 的无效预测步骤</strong>，draft model 这边的延迟大幅下降。</p> <h3 id="43-两轮稀疏-token-tree-预测two-pass-sparse-token-tree">4.3 两轮稀疏 Token Tree 预测（Two-pass Sparse Token Tree）</h3> <p>前两个技术主要针对 draft model 相对较小的场景（比如 TinyLlama + Llama-7B）。当 target model 非常大（比如 Vicuna-13B）时，target verification 本身变成了新瓶颈，这时候就需要 token tree 来提高单次验证的 token 吞吐量。</p> <p>但传统的 tree-based speculative decoding（如 SpecInfer）需要 draft model 多次运行来扩展 tree，overhead 很高。</p> <p>SpecASR 的两轮预测流程：</p> <p><img src="/assets/img/posts/20260625-specasr/013_Figure_10.png" alt="两轮稀疏 Token Tree 预测机制"/></p> <p><strong>第一轮</strong>：正常做 greedy 单序列预测，同时标记出 logit 置信度低的位置（不确定点）</p> <p><strong>第二轮</strong>：只在这些不确定点上做分支扩展（用 top-k 候选），组成一棵稀疏 token tree</p> <p>关键在于”稀疏”——不是在每个位置都展开，而是<strong>只在需要的地方展开</strong>，大大降低了 tree 构建的 overhead。结合前面的 draft recycling 策略，新分支如果能接在已有的”主干”上，就不需要重新生成，直接复用。</p> <p>如下图，随着预测长度增大，draft model 的延迟占比上升，target model 的延迟占比下降——这说明优化 draft model 效率和 target verification 效率需要同时兼顾：</p> <p><img src="/assets/img/posts/20260625-specasr/010_Figure_7.png" alt="不同预测长度下 draft 与 target 延迟占比"/></p> <p>对比下三种方案的效率矩阵：</p> <table> <thead> <tr> <th>方法</th> <th>Draft 效率</th> <th>Target 验证效率</th> <th>Draft 长度</th> <th>接受率</th> <th>灵活性</th> </tr> </thead> <tbody> <tr> <td>单序列（普通）</td> <td>高</td> <td>低</td> <td>中</td> <td>低</td> <td>中</td> </tr> <tr> <td>Fixed Tree</td> <td>低</td> <td>高</td> <td>低</td> <td>中</td> <td>低</td> </tr> <tr> <td>Dynamic Tree</td> <td>低</td> <td>高</td> <td>低</td> <td>高</td> <td>高</td> </tr> <tr> <td><strong>SpecASR（ours）</strong></td> <td><strong>高</strong></td> <td><strong>高</strong></td> <td><strong>高</strong></td> <td><strong>高</strong></td> <td><strong>高</strong></td> </tr> </tbody> </table> <p>SpecASR 是唯一一个在四个维度上都做到”高”的方案。</p> <h2 id="5-实验结果">5. 实验结果</h2> <p>测试平台是 NVIDIA RTX A6000，数据集是 LibriSpeech（clean/other + dev/test）。</p> <p>主要加速结果如下（相比 autoregressive baseline）：</p> <p><img src="/assets/img/posts/20260625-specasr/014_a.png" alt="各方案在 TinyLlama+Llama-7B 和 TinyLlama+Vicuna-13B 上的加速对比"/></p> <ul> <li><strong>TinyLlama+Vicuna-13B</strong>：最高 3.79× overall speedup</li> <li><strong>TinyLlama+Llama-7B</strong>：2.05× overall speedup</li> <li>vs. 普通 speculative decoding：<strong>1.25×-1.84× 额外加速</strong></li> </ul> <p>消融实验数据（每 10s 音频平均解码延迟）：</p> <table> <thead> <tr> <th>方法</th> <th>Draft(ms)</th> <th>Target(ms)</th> <th>Total(ms)</th> </tr> </thead> <tbody> <tr> <td>baseline speculative</td> <td>231.06</td> <td>254.48</td> <td>485.54</td> </tr> <tr> <td>+自适应单序列</td> <td>236.23</td> <td>191.20</td> <td>427.43</td> </tr> <tr> <td>+draft sequence recycling</td> <td>189.48</td> <td>199.52</td> <td>389.00</td> </tr> <tr> <td>+两轮稀疏 tree</td> <td>244.62</td> <td>123.17</td> <td><strong>367.79</strong></td> </tr> </tbody> </table> <p>三个组件叠加后，总延迟从 485.54ms 降到 367.79ms，对 target model 的加速尤其显著（254.48ms → 123.17ms，减少了 51.6%）。</p> <p>值得一提的是：SpecASR 在整个加速过程中<strong>WER 没有任何变化</strong>——speculative decoding 从理论上保证了输出等价，识别精度完全不受影响。</p> <h2 id="6-几点个人-take">6. 几点个人 take</h2> <p>这篇工作最打动我的是那个核心 observation：<strong>audio conditioning 是 ASR speculative decoding 的天然优势</strong>。这个 insight 不是拍脑袋出来的，他们用实验数据仔细验证了 ASR 和纯文本任务的 acceptance rate 分布差异，然后再反推出三个针对性的技术优化。这个从观察到设计的逻辑链条很清晰。</p> <p>当然也有一些局限值得关注：</p> <ul> <li>实验用的是 Whisper 模拟 Llama-based ASR 的解码轨迹，真正的 LLM-based ASR（Seed-ASR、Speech-Llama 这类）并没有开源模型可以直接验证，作者也坦承了这一点</li> <li>noisy 场景（test-other）下加速比有明显下降（3.7× → 3.04×），说明 draft 模型的准确率对整体效果有显著影响</li> </ul> <p>不过整体来说，这是一篇做得很扎实的工程优化工作，三个技术点组合起来协同效果很好，代码也有望开源。对做 LLM 推理加速或者 ASR 系统的同学都很值得一读。</p> <hr/> <p>欢迎评论区交流，有问题或者发现我哪里理解有误，欢迎指出~</p>]]></content><author><name></name></author><category term="LLM"/><category term="ASR"/><category term="Speculative Decoding"/><category term="推理加速"/><category term="论文解读"/><summary type="html"><![CDATA[插播：之前写的《动手学 AutoML》终于出版了，从 NAS 到超参优化都有覆盖，适合想系统入门 AutoML 的同学。好了广告结束，现在进入正题。]]></summary></entry><entry><title type="html">公司里那个一言不发的马尾辫大叔，现在被装进了你的 AI agent 里</title><link href="https://marsggbo.github.io/blog/2026/ponytail/" rel="alternate" type="text/html" title="公司里那个一言不发的马尾辫大叔，现在被装进了你的 AI agent 里"/><published>2026-06-25T00:00:00+00:00</published><updated>2026-06-25T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/ponytail</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/ponytail/"><![CDATA[<h1 id="公司里那个一言不发的马尾辫大叔现在被装进了你的-ai-agent-里">公司里那个一言不发的马尾辫大叔，现在被装进了你的 AI agent 里</h1> <blockquote> <p>原文：<a href="https://github.com/DietrichGebert/ponytail">DietrichGebert/ponytail</a></p> </blockquote> <hr/> <h2 id="1-前言那个扎马尾辫的男人">1. 前言：那个扎马尾辫的男人</h2> <p>每个有点年头的公司里都有这么一个人。</p> <p>后脑勺一束花白的马尾辫，鼻梁上架副椭圆眼镜，工龄比版本控制系统还长。他平时不太说话，午饭一个人坐窗边吃。新人兴冲冲拉着他 review 自己刚写的五十行精巧代码，他看一眼，沉默两秒，伸手在键盘上敲了一行——跑通了。然后起身去倒水，什么都没解释。</p> <blockquote> <p><em>He says nothing. He writes one line. It works.</em></p> <p>（他不说话。他写一行。它能跑。）</p> </blockquote> <p>GitHub 上最近爆火的一个项目 <code class="language-plaintext highlighter-rouge">ponytail</code>（马尾辫），名字就是这么来的——把这位神秘大叔做成了一个 AI agent 插件。一个月之内冲到 <strong>55.1k stars</strong>，Trendshift 日榜周榜挂着不下来，定位也很骚：它不是又一个 agent 框架，也不是又一个 IDE 插件，而是<strong>给你的 AI agent 强行塞进去一个”扎马尾辫的资深工程师人格”</strong>。</p> <p align="center"> <img src="assets/logo.png" width="220" alt="ponytail logo"/> </p> <p>为啥这玩意儿一夜爆火？</p> <p>因为现在用 Claude Code、Cursor、Copilot 的人都被同一个问题折磨：<strong>AI agent 写代码默认就是过度工程化</strong>。你让它”写个日期选择器”，它给你装 flatpickr、写 wrapper 组件、配样式表、还要和你讨论时区——五十行起步。其实浏览器自带一个 <code class="language-plaintext highlighter-rouge">&lt;input type="date"&gt;</code> 就完事了。能写一个 class 绝不写一个函数，能引入一个新依赖绝不翻 stdlib——这帮 agent 训练语料里塞满了 over-engineered 的”最佳实践”，所以默认行为就是把简单问题搞复杂。</p> <p>而马尾辫大叔的存在意义就是治这个病。他不会教你架构，他只会沉默地把你那五十行换成一行。</p> <p>今天想和大家聊聊这个项目：它为什么火、效果到底有多猛、以及——重点——<strong>它的实现到底有什么巧妙之处</strong>。读完你会发现，ponytail 真正有意思的不是那段”一行胜过五十行”的口号，而是它通过一组 hook 把这位大叔的人格<strong>持久化注入</strong>到 agent 每一次会话里的工程实现。</p> <hr/> <h2 id="2-ponytail-是个啥">2. Ponytail 是个啥</h2> <p>简单粗暴一句话：<strong>ponytail 是一个跨平台的 agent skill/plugin，强行让 AI agent 走”懒人路径”写代码</strong>。</p> <p>支持的平台离谱地多，目前覆盖 Claude Code、Codex、GitHub Copilot CLI、Cursor、Windsurf、Cline、OpenCode、Gemini CLI、Aider、Kiro、Zed、CodeWhale、Swival、OpenClaw、Antigravity CLI、Pi agent harness 等 14+ 个主流 agent 宿主。在 Claude Code 里装它就一行：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/plugin marketplace add DietrichGebert/ponytail
/plugin install ponytail@ponytail
</code></pre></div></div> <p>装完之后，<strong>每一次 session 启动</strong>它都会向 agent 注入一份精心设计的 ruleset，让 agent 在写任何代码之前先在心里走一遍它定义的”七阶梯（the ladder）”：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. 这玩意儿真的需要存在吗？             → 不需要就跳过 (YAGNI)
2. 当前代码库里已经有了吗？             → 有就复用，别重写
3. 标准库能做吗？                       → 能就用
4. 平台原生特性能搞定吗？               → 能就用
5. 已经装了的依赖里有现成的吗？         → 有就用
6. 能写成一行吗？                       → 一行
7. 实在不行：写满足需求的最小代码
</code></pre></div></div> <p>整套思路就是奥卡姆剃刀在工程实践上的极端化版本：<strong>最好的代码是你没写的代码</strong>。</p> <p>它还提供了几个不同”强度”档位：</p> <ul> <li><code class="language-plaintext highlighter-rouge">lite</code>：温和版，正常实现需求，但会顺便提一句”其实有更懒的写法”</li> <li><code class="language-plaintext highlighter-rouge">full</code>（默认）：严格执行 ladder</li> <li><code class="language-plaintext highlighter-rouge">ultra</code>：YAGNI 极端主义者，能删就删，先质疑需求合不合理再写</li> <li><code class="language-plaintext highlighter-rouge">off</code>：关闭</li> </ul> <p>以及一组配套的 slash 命令：<code class="language-plaintext highlighter-rouge">/ponytail-review</code>（审计当前 diff 里的过度工程）、<code class="language-plaintext highlighter-rouge">/ponytail-audit</code>（审计整个 repo）、<code class="language-plaintext highlighter-rouge">/ponytail-debt</code>（追踪你延后掉的简化项）、<code class="language-plaintext highlighter-rouge">/ponytail-gain</code>（看影响指标）。</p> <hr/> <h2 id="3-看一下效果吸引你继续读下去">3. 看一下效果，吸引你继续读下去</h2> <p>光说不练假把式。作者用 Claude Code 在一个真实的开源项目（tiangolo 的 <a href="https://github.com/fastapi/full-stack-fastapi-template"><code class="language-plaintext highlighter-rouge">full-stack-fastapi-template</code></a>，FastAPI + React 的全栈模板）上跑了 12 个真实 feature ticket，每个任务做 4 次取均值，模型用 Haiku 4.5。对比对象包括”不开 skill 的 baseline”、ponytail、以及两个其他 prompt-engineering 流派（caveman 和 “YAGNI + one-liners” prompt）。</p> <p>结果如下图，<strong>ponytail 是唯一在每一个指标上都压过 baseline、并且 100% 保持安全检查的方案</strong>：</p> <p><img src="/assets/img/posts/ponytail/benchmark-agentic.png" alt="ponytail vs baseline agentic 评测"/></p> <p>数字层面：</p> <table> <thead> <tr> <th>vs no-skill baseline</th> <th style="text-align: right">LOC</th> <th style="text-align: right">tokens</th> <th style="text-align: right">cost</th> <th style="text-align: right">time</th> <th style="text-align: right">safe</th> </tr> </thead> <tbody> <tr> <td><strong>ponytail</strong></td> <td style="text-align: right"><strong>-54%</strong></td> <td style="text-align: right"><strong>-22%</strong></td> <td style="text-align: right"><strong>-20%</strong></td> <td style="text-align: right"><strong>-27%</strong></td> <td style="text-align: right"><strong>100%</strong></td> </tr> <tr> <td>caveman（terse prose 对照组）</td> <td style="text-align: right">-20%</td> <td style="text-align: right">+7%</td> <td style="text-align: right">+3%</td> <td style="text-align: right">+2%</td> <td style="text-align: right">100%</td> </tr> <tr> <td>“YAGNI + one-liners” prompt</td> <td style="text-align: right">-33%</td> <td style="text-align: right">-14%</td> <td style="text-align: right">-21%</td> <td style="text-align: right">-30%</td> <td style="text-align: right"><strong>95%</strong></td> </tr> </tbody> </table> <p>注意第二、第三行：caveman 这种只是让模型说话更简洁的 prompt，<strong>tokens 和 cost 反而涨了</strong>——因为模型话少了，但行为没变，反复试错反而更贵。”YAGNI + one-liner” 这种激进 prompt 虽然便宜，但<strong>安全检查掉了 5%</strong>——这就是为啥 ponytail 在 README 里反复强调 “lazy means efficient, not careless”，它的 ruleset 里专门有”绝对不能省”的章节（输入校验、错误处理、安全、可访问性）。</p> <p>最戏剧化的案例是 README 里那个 <strong>日期选择器：从 404 行被砍到 23 行</strong>。砍法不是用更短的 JS，而是直接换成浏览器原生 <code class="language-plaintext highlighter-rouge">&lt;input type="date"&gt;</code>，这就是 ladder 第 4 级”原生平台特性”的胜利。</p> <p>之前还有一个旧版的 single-shot benchmark（一次性生成场景），跨三个模型对比，结论是 80-94% 的 LOC 削减——不过作者也很诚实地在 README 里说，这个数字”夸大了”，因为 baseline 是裸模型，会自带很多 prose 解释，所以差距被放大了。新的 agentic benchmark 才是更公允的对比。</p> <p><img src="/assets/img/posts/ponytail/benchmark-3model.png" alt="single-shot 跨模型对照"/></p> <p>到这里你可能会想：<strong>不就是一段写得很好的 prompt 吗？为啥还能搞成 5w 星的项目？</strong></p> <p>接下来我们就钻进它的代码里，看看它到底是怎么做的。</p> <hr/> <h2 id="4-源码深扒ponytail-到底怎么工作的">4. 源码深扒：ponytail 到底怎么工作的</h2> <p>我去看完整个 repo 的代码（其实也不多，加起来千把行 JS），最让我眼前一亮的不是那段七阶梯的 prompt 本身——<strong>那段 prompt 任何人都能写</strong>——而是它把这段 prompt <strong>可靠地、持久地、按需地</strong>塞进 agent 每一次会话的工程实现。</p> <p>整个机制可以拆成三层：<strong>Hook 注册层 → Hook 执行层 → 配置/模式持久层</strong>。我们一层一层看。</p> <h3 id="41-第一层hook-注册告诉-claude-code-在什么时候触发我">4.1 第一层：Hook 注册——告诉 Claude Code 在什么时候触发我</h3> <p>Ponytail 整个 plugin 的”入口”是 <code class="language-plaintext highlighter-rouge">hooks/claude-codex-hooks.json</code>，注册了三个 hook：</p> <div class="language-json highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="p">{</span><span class="w">
  </span><span class="nl">"hooks"</span><span class="p">:</span><span class="w"> </span><span class="p">{</span><span class="w">
    </span><span class="nl">"SessionStart"</span><span class="p">:</span><span class="w"> </span><span class="p">[{</span><span class="w">
      </span><span class="nl">"matcher"</span><span class="p">:</span><span class="w"> </span><span class="s2">"startup|resume|clear|compact"</span><span class="p">,</span><span class="w">
      </span><span class="nl">"hooks"</span><span class="p">:</span><span class="w"> </span><span class="p">[{</span><span class="w">
        </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"command"</span><span class="p">,</span><span class="w">
        </span><span class="nl">"command"</span><span class="p">:</span><span class="w"> </span><span class="s2">"node </span><span class="se">\"</span><span class="s2">${CLAUDE_PLUGIN_ROOT}/hooks/ponytail-activate.js</span><span class="se">\"</span><span class="s2">; exit 0"</span><span class="p">,</span><span class="w">
        </span><span class="nl">"timeout"</span><span class="p">:</span><span class="w"> </span><span class="mi">5</span><span class="p">,</span><span class="w">
        </span><span class="nl">"statusMessage"</span><span class="p">:</span><span class="w"> </span><span class="s2">"Loading ponytail mode..."</span><span class="w">
      </span><span class="p">}]</span><span class="w">
    </span><span class="p">}],</span><span class="w">
    </span><span class="nl">"SubagentStart"</span><span class="p">:</span><span class="w"> </span><span class="p">[{</span><span class="w">
      </span><span class="nl">"hooks"</span><span class="p">:</span><span class="w"> </span><span class="p">[{</span><span class="w"> </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"command"</span><span class="p">,</span><span class="w"> </span><span class="nl">"command"</span><span class="p">:</span><span class="w"> </span><span class="s2">"node ...ponytail-subagent.js"</span><span class="w"> </span><span class="p">}]</span><span class="w">
    </span><span class="p">}],</span><span class="w">
    </span><span class="nl">"UserPromptSubmit"</span><span class="p">:</span><span class="w"> </span><span class="p">[{</span><span class="w">
      </span><span class="nl">"hooks"</span><span class="p">:</span><span class="w"> </span><span class="p">[{</span><span class="w"> </span><span class="nl">"type"</span><span class="p">:</span><span class="w"> </span><span class="s2">"command"</span><span class="p">,</span><span class="w"> </span><span class="nl">"command"</span><span class="p">:</span><span class="w"> </span><span class="s2">"node ...ponytail-mode-tracker.js"</span><span class="w"> </span><span class="p">}]</span><span class="w">
    </span><span class="p">}]</span><span class="w">
  </span><span class="p">}</span><span class="w">
</span><span class="p">}</span><span class="w">
</span></code></pre></div></div> <p>三个 hook 各管一摊：</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">SessionStart</code></strong>：会话启动（包括新开、resume、clear、compact 后）时跑——负责把 ruleset 注入到 agent 的初始上下文里。</li> <li><strong><code class="language-plaintext highlighter-rouge">SubagentStart</code></strong>：subagent 启动时跑——保证你 spawn 的子 agent 也继承 ponytail 模式（这点很重要，后面讲）。</li> <li><strong><code class="language-plaintext highlighter-rouge">UserPromptSubmit</code></strong>：用户每次回车提交 prompt 时跑——用来监听用户是不是输入了 <code class="language-plaintext highlighter-rouge">/ponytail ultra</code> 这种切换命令。</li> </ul> <p><strong>关键 insight</strong>：Claude Code 的 hooks 机制本质就是它在特定时机会调用你的脚本，并允许你的脚本通过 stdout 向 agent 的上下文里塞东西。Ponytail 把这个机制用到了极致——它不是”传统插件”那种修改 UI 或者加新 tool，而是<strong>用 hook 在每一次推理之前往上下文里偷偷加一段 system message</strong>。这其实是个非常薄但非常聪明的杠杆。</p> <h3 id="42-第二层sessionstart-干了什么">4.2 第二层：SessionStart 干了什么</h3> <p>会话启动时跑的是 <code class="language-plaintext highlighter-rouge">hooks/ponytail-activate.js</code>，核心逻辑精简下来是这样：</p> <div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// hooks/ponytail-activate.js（精简版）</span>
<span class="kd">const</span> <span class="p">{</span> <span class="nx">getDefaultMode</span> <span class="p">}</span> <span class="o">=</span> <span class="nf">require</span><span class="p">(</span><span class="dl">'</span><span class="s1">./ponytail-config</span><span class="dl">'</span><span class="p">);</span>
<span class="kd">const</span> <span class="p">{</span> <span class="nx">getPonytailInstructions</span> <span class="p">}</span> <span class="o">=</span> <span class="nf">require</span><span class="p">(</span><span class="dl">'</span><span class="s1">./ponytail-instructions</span><span class="dl">'</span><span class="p">);</span>
<span class="kd">const</span> <span class="p">{</span> <span class="nx">setMode</span><span class="p">,</span> <span class="nx">writeHookOutput</span> <span class="p">}</span> <span class="o">=</span> <span class="nf">require</span><span class="p">(</span><span class="dl">'</span><span class="s1">./ponytail-runtime</span><span class="dl">'</span><span class="p">);</span>

<span class="kd">const</span> <span class="nx">mode</span> <span class="o">=</span> <span class="nf">getDefaultMode</span><span class="p">();</span>    <span class="c1">// 读取当前模式</span>

<span class="k">if </span><span class="p">(</span><span class="nx">mode</span> <span class="o">===</span> <span class="dl">'</span><span class="s1">off</span><span class="dl">'</span><span class="p">)</span> <span class="p">{</span>
  <span class="nf">clearMode</span><span class="p">();</span>
  <span class="nx">process</span><span class="p">.</span><span class="nf">exit</span><span class="p">(</span><span class="mi">0</span><span class="p">);</span>                <span class="c1">// off 模式直接跳过</span>
<span class="p">}</span>

<span class="nf">setMode</span><span class="p">(</span><span class="nx">mode</span><span class="p">);</span>                    <span class="c1">// 把当前模式写到状态文件</span>

<span class="c1">// 关键一步：把 ruleset 写到 stdout，Claude Code 会把它注入上下文</span>
<span class="kd">let</span> <span class="nx">output</span> <span class="o">=</span> <span class="nf">getPonytailInstructions</span><span class="p">(</span><span class="nx">mode</span><span class="p">);</span>
<span class="nf">writeHookOutput</span><span class="p">(</span><span class="dl">'</span><span class="s1">SessionStart</span><span class="dl">'</span><span class="p">,</span> <span class="nx">mode</span><span class="p">,</span> <span class="nx">output</span><span class="p">);</span>
</code></pre></div></div> <p>整个文件就干三件事：</p> <ol> <li><strong>读当前模式</strong>：<code class="language-plaintext highlighter-rouge">full</code>、<code class="language-plaintext highlighter-rouge">lite</code>、<code class="language-plaintext highlighter-rouge">ultra</code> 还是 <code class="language-plaintext highlighter-rouge">off</code></li> <li><strong>写状态文件</strong>：把当前模式持久化到 <code class="language-plaintext highlighter-rouge">~/.claude/.ponytail-active</code>，statusline 那个角标会读它</li> <li><strong>生成 ruleset 并通过 stdout 喷出去</strong>：Claude Code 见到 stdout 输出就会把它作为额外 context 塞给 agent</li> </ol> <p>第三步是整个机制的核心。你不需要修改 Claude Code 本身，你只需要让你的脚本输出一段文字——这段文字就会作为隐藏的 SessionStart context 出现在 agent 看到的 prompt 里。</p> <h3 id="43-第三层ruleset-是怎么按-mode-过滤的">4.3 第三层：ruleset 是怎么按 mode 过滤的</h3> <p><code class="language-plaintext highlighter-rouge">getPonytailInstructions(mode)</code> 这个函数才是 prompt 工程的精华。它做的事情是：<strong>读 <code class="language-plaintext highlighter-rouge">skills/ponytail/SKILL.md</code> 这份完整的规则文档，按当前 mode 把多余的部分剔掉</strong>。</p> <p>源码（精简后）：</p> <div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// hooks/ponytail-instructions.js（精简版）</span>
<span class="kd">function</span> <span class="nf">filterSkillBodyForMode</span><span class="p">(</span><span class="nx">body</span><span class="p">,</span> <span class="nx">mode</span><span class="p">)</span> <span class="p">{</span>
  <span class="kd">const</span> <span class="nx">effective</span> <span class="o">=</span> <span class="nf">normalizeMode</span><span class="p">(</span><span class="nx">mode</span><span class="p">)</span> <span class="o">||</span> <span class="nx">DEFAULT_MODE</span><span class="p">;</span>
  <span class="k">return</span> <span class="nx">body</span>
    <span class="p">.</span><span class="nf">replace</span><span class="p">(</span><span class="sr">/^---</span><span class="se">[\s\S]</span><span class="sr">*</span><span class="se">?</span><span class="sr">---</span><span class="se">\s</span><span class="sr">*/</span><span class="p">,</span> <span class="dl">''</span><span class="p">)</span>   <span class="c1">// 去 frontmatter</span>
    <span class="p">.</span><span class="nf">split</span><span class="p">(</span><span class="sr">/</span><span class="se">\r?\n</span><span class="sr">/</span><span class="p">)</span>
    <span class="p">.</span><span class="nf">filter</span><span class="p">(</span><span class="nx">line</span> <span class="o">=&gt;</span> <span class="p">{</span>
      <span class="c1">// 表格行：| **lite** | ... | 这种，只保留当前 mode 那一行</span>
      <span class="kd">const</span> <span class="nx">m1</span> <span class="o">=</span> <span class="nx">line</span><span class="p">.</span><span class="nf">match</span><span class="p">(</span><span class="sr">/^</span><span class="se">\|\s</span><span class="sr">*</span><span class="se">\*\*(</span><span class="sr">.+</span><span class="se">?)\*\*\s</span><span class="sr">*</span><span class="se">\|</span><span class="sr">/</span><span class="p">);</span>
      <span class="k">if </span><span class="p">(</span><span class="nx">m1</span><span class="p">)</span> <span class="p">{</span>
        <span class="kd">const</span> <span class="nx">labelMode</span> <span class="o">=</span> <span class="nf">normalizeMode</span><span class="p">(</span><span class="nx">m1</span><span class="p">[</span><span class="mi">1</span><span class="p">].</span><span class="nf">trim</span><span class="p">());</span>
        <span class="k">if </span><span class="p">(</span><span class="nx">labelMode</span><span class="p">)</span> <span class="k">return</span> <span class="nx">labelMode</span> <span class="o">===</span> <span class="nx">effective</span><span class="p">;</span>
      <span class="p">}</span>
      <span class="c1">// 示例行："- lite: 'Done, cache added. FYI...'" 同理</span>
      <span class="kd">const</span> <span class="nx">m2</span> <span class="o">=</span> <span class="nx">line</span><span class="p">.</span><span class="nf">match</span><span class="p">(</span><span class="sr">/^-</span><span class="se">\s</span><span class="sr">*</span><span class="se">([^</span><span class="sr">:</span><span class="se">]</span><span class="sr">+</span><span class="se">)</span><span class="sr">:</span><span class="se">\s</span><span class="sr">*/</span><span class="p">);</span>
      <span class="k">if </span><span class="p">(</span><span class="nx">m2</span><span class="p">)</span> <span class="p">{</span>
        <span class="kd">const</span> <span class="nx">labelMode</span> <span class="o">=</span> <span class="nf">normalizeMode</span><span class="p">(</span><span class="nx">m2</span><span class="p">[</span><span class="mi">1</span><span class="p">].</span><span class="nf">trim</span><span class="p">());</span>
        <span class="k">if </span><span class="p">(</span><span class="nx">labelMode</span><span class="p">)</span> <span class="k">return</span> <span class="nx">labelMode</span> <span class="o">===</span> <span class="nx">effective</span><span class="p">;</span>
      <span class="p">}</span>
      <span class="k">return</span> <span class="kc">true</span><span class="p">;</span>
    <span class="p">})</span>
    <span class="p">.</span><span class="nf">join</span><span class="p">(</span><span class="dl">'</span><span class="se">\n</span><span class="dl">'</span><span class="p">);</span>
<span class="p">}</span>

<span class="kd">function</span> <span class="nf">getPonytailInstructions</span><span class="p">(</span><span class="nx">mode</span><span class="p">)</span> <span class="p">{</span>
  <span class="k">try</span> <span class="p">{</span>
    <span class="kd">const</span> <span class="nx">skill</span> <span class="o">=</span> <span class="nx">fs</span><span class="p">.</span><span class="nf">readFileSync</span><span class="p">(</span><span class="nx">SKILL_PATH</span><span class="p">,</span> <span class="dl">'</span><span class="s1">utf8</span><span class="dl">'</span><span class="p">);</span>
    <span class="k">return</span> <span class="dl">'</span><span class="s1">PONYTAIL MODE ACTIVE — level: </span><span class="dl">'</span> <span class="o">+</span> <span class="nx">mode</span> <span class="o">+</span> <span class="dl">'</span><span class="se">\n\n</span><span class="dl">'</span> <span class="o">+</span>
           <span class="nf">filterSkillBodyForMode</span><span class="p">(</span><span class="nx">skill</span><span class="p">,</span> <span class="nx">mode</span><span class="p">);</span>
  <span class="p">}</span> <span class="k">catch </span><span class="p">(</span><span class="nx">e</span><span class="p">)</span> <span class="p">{</span>
    <span class="k">return</span> <span class="nf">getFallbackInstructions</span><span class="p">(</span><span class="nx">mode</span><span class="p">);</span>   <span class="c1">// 兜底：硬编码版</span>
  <span class="p">}</span>
<span class="p">}</span>
</code></pre></div></div> <p>短短二十几行，<strong>做了一件很聪明的事</strong>：把”通用规则”和”mode-specific 规则”放在同一份 SKILL.md 里维护，渲染的时候按 mode 过滤多余内容。这样 prompt token 不浪费，三个模式之间的差异也容易 review。</p> <p>比如 SKILL.md 里有这么一段表格：</p> <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code>| Level | What change |
|-------|------------|
| <span class="gs">**lite**</span>  | Build what's asked, but name the lazier alternative.
| <span class="gs">**full**</span>  | The ladder enforced. Stdlib and native first.
| <span class="gs">**ultra**</span> | YAGNI extremist. Deletion before addition.
</code></pre></div></div> <p>如果当前是 <code class="language-plaintext highlighter-rouge">full</code>，过滤完只剩 full 那一行；其他 mode-specific 的解释也被剔掉。配合那段 fallback——万一 SKILL.md 文件读不到（比如装出问题了），还有一份硬编码的 instructions 兜底，保证 hook 不会因为文件 IO 失败而瘫掉。<strong>这是工程上很务实的设计</strong>，作者明显跑过线上场景，知道一定会有奇奇怪怪的安装环境。</p> <h3 id="44-第四层userpromptsubmit-怎么追踪模式切换">4.4 第四层：UserPromptSubmit 怎么追踪模式切换</h3> <p>用户输入 <code class="language-plaintext highlighter-rouge">/ponytail ultra</code> 时是谁负责切换的？是 <code class="language-plaintext highlighter-rouge">ponytail-mode-tracker.js</code>，它注册在 <code class="language-plaintext highlighter-rouge">UserPromptSubmit</code> 这个 hook 上——也就是说，<strong>每次你按回车之前，这个脚本都会跑一次</strong>，检查你输入的内容。</p> <p>精简后的源码：</p> <div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// hooks/ponytail-mode-tracker.js（精简版）</span>
<span class="kd">let</span> <span class="nx">input</span> <span class="o">=</span> <span class="dl">''</span><span class="p">;</span>
<span class="nx">process</span><span class="p">.</span><span class="nx">stdin</span><span class="p">.</span><span class="nf">on</span><span class="p">(</span><span class="dl">'</span><span class="s1">data</span><span class="dl">'</span><span class="p">,</span> <span class="nx">chunk</span> <span class="o">=&gt;</span> <span class="p">{</span> <span class="nx">input</span> <span class="o">+=</span> <span class="nx">chunk</span><span class="p">;</span> <span class="p">});</span>
<span class="nx">process</span><span class="p">.</span><span class="nx">stdin</span><span class="p">.</span><span class="nf">on</span><span class="p">(</span><span class="dl">'</span><span class="s1">end</span><span class="dl">'</span><span class="p">,</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
  <span class="kd">const</span> <span class="nx">data</span> <span class="o">=</span> <span class="nx">JSON</span><span class="p">.</span><span class="nf">parse</span><span class="p">(</span><span class="nx">input</span><span class="p">);</span>
  <span class="kd">const</span> <span class="nx">prompt</span> <span class="o">=</span> <span class="p">(</span><span class="nx">data</span><span class="p">.</span><span class="nx">prompt</span> <span class="o">||</span> <span class="dl">''</span><span class="p">).</span><span class="nf">trim</span><span class="p">().</span><span class="nf">toLowerCase</span><span class="p">();</span>

  <span class="c1">// 1. 匹配 /ponytail 类命令</span>
  <span class="k">if </span><span class="p">(</span><span class="sr">/^</span><span class="se">[/</span><span class="sr">@$</span><span class="se">]</span><span class="sr">ponytail/</span><span class="p">.</span><span class="nf">test</span><span class="p">(</span><span class="nx">prompt</span><span class="p">))</span> <span class="p">{</span>
    <span class="kd">const</span> <span class="p">[</span><span class="nx">cmd</span><span class="p">,</span> <span class="nx">arg</span><span class="p">]</span> <span class="o">=</span> <span class="nx">prompt</span><span class="p">.</span><span class="nf">split</span><span class="p">(</span><span class="sr">/</span><span class="se">\s</span><span class="sr">+/</span><span class="p">);</span>
    <span class="kd">let</span> <span class="nx">mode</span> <span class="o">=</span> <span class="kc">null</span><span class="p">;</span>
    <span class="k">if </span><span class="p">(</span><span class="nx">cmd</span><span class="p">.</span><span class="nf">endsWith</span><span class="p">(</span><span class="dl">'</span><span class="s1">ponytail-review</span><span class="dl">'</span><span class="p">))</span> <span class="nx">mode</span> <span class="o">=</span> <span class="dl">'</span><span class="s1">review</span><span class="dl">'</span><span class="p">;</span>
    <span class="k">else</span> <span class="k">if </span><span class="p">(</span><span class="nx">cmd</span><span class="p">.</span><span class="nf">endsWith</span><span class="p">(</span><span class="dl">'</span><span class="s1">ponytail</span><span class="dl">'</span><span class="p">))</span> <span class="p">{</span>
      <span class="k">if </span><span class="p">([</span><span class="dl">'</span><span class="s1">lite</span><span class="dl">'</span><span class="p">,</span><span class="dl">'</span><span class="s1">full</span><span class="dl">'</span><span class="p">,</span><span class="dl">'</span><span class="s1">ultra</span><span class="dl">'</span><span class="p">,</span><span class="dl">'</span><span class="s1">off</span><span class="dl">'</span><span class="p">].</span><span class="nf">includes</span><span class="p">(</span><span class="nx">arg</span><span class="p">))</span> <span class="nx">mode</span> <span class="o">=</span> <span class="nx">arg</span><span class="p">;</span>
      <span class="k">else</span> <span class="nx">mode</span> <span class="o">=</span> <span class="nf">getDefaultMode</span><span class="p">();</span>
    <span class="p">}</span>
    <span class="k">if </span><span class="p">(</span><span class="nx">mode</span> <span class="o">&amp;&amp;</span> <span class="nx">mode</span> <span class="o">!==</span> <span class="dl">'</span><span class="s1">off</span><span class="dl">'</span><span class="p">)</span> <span class="p">{</span>
      <span class="nf">setMode</span><span class="p">(</span><span class="nx">mode</span><span class="p">);</span>
      <span class="nf">writeHookOutput</span><span class="p">(</span><span class="dl">'</span><span class="s1">UserPromptSubmit</span><span class="dl">'</span><span class="p">,</span> <span class="nx">mode</span><span class="p">,</span>
        <span class="dl">'</span><span class="s1">PONYTAIL MODE CHANGED — level: </span><span class="dl">'</span> <span class="o">+</span> <span class="nx">mode</span><span class="p">);</span>
    <span class="p">}</span> <span class="k">else</span> <span class="k">if </span><span class="p">(</span><span class="nx">mode</span> <span class="o">===</span> <span class="dl">'</span><span class="s1">off</span><span class="dl">'</span><span class="p">)</span> <span class="p">{</span>
      <span class="nf">clearMode</span><span class="p">();</span>
    <span class="p">}</span>
  <span class="p">}</span>

  <span class="c1">// 2. 自然语言关闭：识别 "stop ponytail" / "normal mode"</span>
  <span class="k">if </span><span class="p">(</span><span class="nf">isDeactivationCommand</span><span class="p">(</span><span class="nx">prompt</span><span class="p">))</span> <span class="p">{</span>
    <span class="nf">clearMode</span><span class="p">();</span>
    <span class="nf">writeHookOutput</span><span class="p">(</span><span class="dl">'</span><span class="s1">UserPromptSubmit</span><span class="dl">'</span><span class="p">,</span> <span class="dl">'</span><span class="s1">off</span><span class="dl">'</span><span class="p">,</span> <span class="dl">'</span><span class="s1">PONYTAIL MODE OFF</span><span class="dl">'</span><span class="p">);</span>
  <span class="p">}</span>
<span class="p">});</span>
</code></pre></div></div> <p>注意第二个判断的实现细节——在 <code class="language-plaintext highlighter-rouge">ponytail-config.js</code> 里：</p> <div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 早期实现只用 includes("stop ponytail") 匹配，结果出过 bug：</span>
<span class="c1">// 用户说 "add a normal mode toggle"，包含 "normal mode" 子串，</span>
<span class="c1">// 就把 ponytail 关掉了。修复方式是要求整条 prompt 严格等于命令。</span>
<span class="kd">function</span> <span class="nf">isDeactivationCommand</span><span class="p">(</span><span class="nx">text</span><span class="p">)</span> <span class="p">{</span>
  <span class="kd">const</span> <span class="nx">t</span> <span class="o">=</span> <span class="nc">String</span><span class="p">(</span><span class="nx">text</span> <span class="o">||</span> <span class="dl">''</span><span class="p">).</span><span class="nf">trim</span><span class="p">().</span><span class="nf">toLowerCase</span><span class="p">().</span><span class="nf">replace</span><span class="p">(</span><span class="sr">/</span><span class="se">[</span><span class="sr">.!?</span><span class="se">\s]</span><span class="sr">+$/</span><span class="p">,</span> <span class="dl">''</span><span class="p">);</span>
  <span class="k">return</span> <span class="nx">t</span> <span class="o">===</span> <span class="dl">'</span><span class="s1">stop ponytail</span><span class="dl">'</span> <span class="o">||</span> <span class="nx">t</span> <span class="o">===</span> <span class="dl">'</span><span class="s1">normal mode</span><span class="dl">'</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <p>代码里的注释就是这个 bug 的”案发现场”——作者踩过坑，所以现在的实现是<strong>完整匹配 + 去掉尾部标点</strong>，避免在普通对话里误伤。这种小地方反映了项目的工程成熟度，<strong>比那段 prompt 本身更有学习价值</strong>。</p> <h3 id="45-第五层配置解析的优先级">4.5 第五层：配置解析的优先级</h3> <p><code class="language-plaintext highlighter-rouge">ponytail-config.js</code> 里负责”我现在该用什么模式”的判断，优先级很清晰：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1. PONYTAIL_DEFAULT_MODE 环境变量
2. 配置文件 defaultMode 字段：
   - $XDG_CONFIG_HOME/ponytail/config.json
   - ~/.config/ponytail/config.json (macOS/Linux 兜底)
   - %APPDATA%\ponytail\config.json (Windows 兜底)
3. 'full' (硬编码默认)
</code></pre></div></div> <p>跨平台、可覆盖、有默认——这套设计就是标准的 12-factor 思路。再加一个小细节，它用了一个 <code class="language-plaintext highlighter-rouge">isShellSafe()</code> 函数防止把含特殊字符的路径直接拼进 shell 命令：</p> <div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">function</span> <span class="nf">isShellSafe</span><span class="p">(</span><span class="nx">p</span><span class="p">)</span> <span class="p">{</span>
  <span class="k">return</span> <span class="k">typeof</span> <span class="nx">p</span> <span class="o">===</span> <span class="dl">'</span><span class="s1">string</span><span class="dl">'</span> <span class="o">&amp;&amp;</span> <span class="sr">/^</span><span class="se">[</span><span class="sr">A-Za-z0-9 _.</span><span class="se">\-</span><span class="sr">:</span><span class="se">/\\</span><span class="sr">~</span><span class="se">]</span><span class="sr">+$/</span><span class="p">.</span><span class="nf">test</span><span class="p">(</span><span class="nx">p</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <p>注释里特别说：”<strong>An allowlist beats escaping every shell’s metacharacters</strong>“——白名单优于对每种 shell 都做转义，因为转义规则太多易错。这种小决策又一次反映了作者经验：<strong>在安全和便利之间，宁可保守拒绝处理也不要写一个错误的转义器</strong>。</p> <h3 id="46-串起来为什么这套机制有效">4.6 串起来：为什么这套机制有效</h3> <p>整个 ponytail 的工程实现，本质上是用 hook 把一段精心设计的 prompt <strong>绑在每一次 agent 推理之前</strong>。它有效的原因可以拆成三点：</p> <p><strong>第一，持久性</strong>。普通的 system prompt 注入只在会话开始时生效，但 ponytail 通过 SessionStart + SubagentStart + UserPromptSubmit 三个 hook 形成一个”全包围”——新会话、子 agent、每次切换模式，ruleset 都会被重新写入。SKILL.md 里那句 “ACTIVE EVERY RESPONSE. No drift back to over-building.” 不是说说而已，是有工程兜底的。</p> <p><strong>第二，可调档</strong>。<code class="language-plaintext highlighter-rouge">lite/full/ultra</code> 三档不是噱头，而是真的对应三种不同的注入内容。作者明白一刀切的 prompt 会让某些场景下 agent 太过保守——所以用过滤器在同一份 SKILL.md 里维护三个变体，按需注入。</p> <p><strong>第三，可关闭+可观测</strong>。一句自然语言 “stop ponytail” 就能关，statusline 上能看到当前 mode（<code class="language-plaintext highlighter-rouge">[PONYTAIL]</code>、<code class="language-plaintext highlighter-rouge">[PONYTAIL:ULTRA]</code>），用户始终知道自己在哪个状态。这一点对 agent 类工具尤其重要——<strong>用户对”AI 后台行为不可见”非常敏感</strong>，可观测性能极大降低焦虑。</p> <hr/> <h2 id="5-为什么这套-prompt-真的有效">5. 为什么这套 prompt 真的有效</h2> <p>代码层面讲清楚了，回到更上层的问题：<strong>为什么这段 prompt 真的能让模型行为变化这么大</strong>？</p> <p>我自己做 LLM 推理优化，对模型怎么”听话”是有些观察的。Ponytail 这套 prompt 的有效性，我觉得主要来自三个设计：</p> <p><strong>第一，它给了一个明确的决策树，而不是抽象指令</strong>。</p> <p>很多人写 prompt 喜欢用 “be concise”、”avoid over-engineering” 这种笼统词，模型会理解但很难落实。Ponytail 的 ladder 是一个<strong>可枚举、可验证、有顺序</strong>的判断流程——”先看 stdlib，再看 native，再看依赖，再看一行解决”。这对 LLM 来说接近一个<strong>算法描述</strong>，比抽象指令好执行得多。本质上还是把”判断”具体化成”步骤”。</p> <p><strong>第二，它显式划定了”不能省”的领域</strong>。</p> <p>如果你只告诉模型”写最少的代码”，模型确实会照做，但代价就是 benchmark 表里 “YAGNI + one-liners” prompt 那一行——<strong>safe 掉到 95%</strong>。Ponytail 专门有 “When NOT to be lazy” 章节明确列出输入校验、错误处理、安全、可访问性、硬件校准这些不能动的东西。这个”边界”是它能保持 100% safety 的关键。</p> <p><strong>第三，它要求模型把”简化”显式标注出来</strong>。</p> <p>SKILL.md 里有一条规则我特别欣赏：</p> <blockquote> <p>Mark deliberate simplifications with a <code class="language-plaintext highlighter-rouge">ponytail:</code> comment… Shortcut with a known ceiling? The comment names the ceiling and the upgrade path: <code class="language-plaintext highlighter-rouge"># ponytail: global lock, per-account locks if throughput matters</code></p> </blockquote> <p>这其实是把模型的”偷懒决策”变成了<strong>代码里的可审计痕迹</strong>。后面 <code class="language-plaintext highlighter-rouge">/ponytail-debt</code> 命令就是去 grep 这些注释，做成一份”延后债务清单”。<strong>这就把”偷懒”从一个不可控的行为变成了一个可追踪的工程实践</strong>。说人话就是：你可以偷懒，但你得留个证据，方便以后回来还债。</p> <p>把这三点合起来看，ponytail 实际上<strong>不是在写一个 prompt，而是在写一个微型的”代码评审 SOP”</strong>。它把一个资深工程师审 code 时的判断过程拆解、形式化、塞进 agent 上下文里。这就是为什么它比单纯 “be concise” 这种 prompt 有效得多——它把隐性的工程审美变成了显式的可执行流程。</p> <hr/> <h2 id="6-我的-takeponytail-火背后的一些观察">6. 我的 take：ponytail 火背后的一些观察</h2> <p>最后说点个人私货。</p> <p><strong>首先，ponytail 火的根本原因，不是它的 prompt 写得多好，而是它精准戳中了当前 AI 编码工具的一个普遍痛点</strong>：模型默认会过度工程化。这个问题 Cursor 用户、Claude Code 用户、Copilot 用户都在抱怨，但之前没人把”反过度工程”做成一个标准化、跨平台、可装可卸的产品。ponytail 把行业心照不宣的”最佳实践”产品化了，<strong>这本身就是一个被严重低估的机会</strong>。</p> <p><strong>其次，从工程实现角度，ponytail 给了我一个启示：agent 时代的 plugin 不一定要做 tool</strong>。</p> <p>我们看到的大部分 agent plugin（MCP server、tool 注入）都是在给 agent 加新能力。但 ponytail 走的是另一条路——<strong>它不给 agent 加任何新能力，而是修剪 agent 的默认行为</strong>。这种”减法插件”在 agent 生态里是个新方向，门槛更低，普适性更强。<strong>任何能用 hook 注入 prompt 的 agent 宿主都可以装它</strong>，所以它能轻松扩展到 14+ 个平台。</p> <p><strong>第三，它的 benchmark 设计本身值得学习</strong>。</p> <p>之前那个 single-shot benchmark 报了 80-94% 的削减，结果作者在 issue #126 被人指出 “你这个 baseline 不公平，因为裸模型会自带 prose”。作者没死扛——直接承认问题，重新设计 agentic benchmark，公开旧数据但标注 “这是单次生成的天花板，不是均值”。<strong>这种 benchmark 透明度在开源社区其实不多见</strong>，很多项目被指出问题第一反应是删帖或者无视。这种实事求是的态度大概也是它能上 Trendshift 周榜的原因之一——technical community 是闻得出味儿的。</p> <p><strong>第四，从我的研究方向（LLM 推理优化）来看，ponytail 还有一个被忽略的价值：省 token 就是省钱、省时间、省碳排</strong>。</p> <p>benchmark 里 -22% tokens、-20% cost、-27% time 这几个数字看着不起眼，但你乘上一个公司每天的 agent 调用量，<strong>这就是真金白银</strong>。我们这帮做推理优化的天天卷 KV cache、卷 batching、卷 quantization，结果在 prompt engineering 这一层一段 ruleset 就能砍 20%+——<strong>这告诉我们效率优化不止在 kernel level，应用层有时候收益更猛</strong>。这也是整个行业需要重新评估的核心矛盾：<strong>底层优化的边际收益递减，而上层 prompt/agent 行为的优化还有大量未挖掘的空间</strong>。</p> <hr/> <h2 id="7-最后">7. 最后</h2> <p>ponytail 这个项目，技术上不复杂，千把行 JS + 一份精心设计的 SKILL.md。但它把一个”看起来人人都能写的 prompt”做成了一个跨 14+ 平台、有完整工程实现、可调档可关闭可观测、还有公开 benchmark 的产品。<strong>这个从”能写”到”做出来还能用”的差距，就是真本事</strong>。</p> <p>装一下试试，看你的 agent 会不会从此沉默寡言，扎起马尾辫（笑）。</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>/plugin marketplace add DietrichGebert/ponytail
/plugin install ponytail@ponytail
</code></pre></div></div> <p>repo：<a href="https://github.com/DietrichGebert/ponytail">https://github.com/DietrichGebert/ponytail</a></p> <p>欢迎评论区交流你装上以后的体验，特别是它在你常用的 agent 上有没有翻车。</p>]]></content><author><name></name></author><category term="LLM"/><category term="AI Agent"/><category term="开源项目"/><category term="源码解析"/><summary type="html"><![CDATA[公司里那个一言不发的马尾辫大叔，现在被装进了你的 AI agent 里]]></summary></entry><entry><title type="html">从零理解 ASR：音频基础、Qwen3-ASR 架构，以及离线 vs 流式推理原理</title><link href="https://marsggbo.github.io/blog/2026/20260624-asr-qwen3/" rel="alternate" type="text/html" title="从零理解 ASR：音频基础、Qwen3-ASR 架构，以及离线 vs 流式推理原理"/><published>2026-06-24T00:00:00+00:00</published><updated>2026-06-24T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260624-asr-qwen3</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260624-asr-qwen3/"><![CDATA[<blockquote> <p>插播：之前写的<a href="https://item.jd.com/14945889.html">《动手学 AutoML》</a>终于出版了，从 NAS 到超参优化都有覆盖，适合想系统入门 AutoML 的同学。好了广告结束，现在进入正题。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote> <h1 id="从零理解-asr音频基础qwen3-asr-架构以及离线-vs-流式推理原理">从零理解 ASR：音频基础、Qwen3-ASR 架构，以及离线 vs 流式推理原理</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2601.21337">Qwen3-ASR Technical Report</a></p> </blockquote> <hr/> <h2 id="1-前言吐槽">1. 前言吐槽</h2> <p>最近接了个新任务：对 Qwen3-ASR 做推理优化。</p> <p>理论上，做 LLM 推理优化的人接 ASR 任务应该没什么门槛——毕竟 Qwen3-ASR 本质上是个 LLM。但说实话，ASR 这块我一直是个半生不熟的状态：音频输入到底长什么样、怎么变成模型能吃的 token、流式和离线到底差在哪——这些细节从没认真捋过。</p> <p>于是就有了这篇文章。从最基础的”声音是什么”开始，一路走到 Qwen3-ASR-1.7B 的具体架构，再到离线和流式推理的原理差异，最后配上可运行的代码让大家直观感受两种模式的区别。希望对和我处于同样处境的同学有帮助（也希望我自己以后不要再从头查了…）。</p> <hr/> <h2 id="2-声音的数字表示从波形到模型可用的特征">2. 声音的数字表示：从波形到模型可用的特征</h2> <h3 id="21-原始音频是什么">2.1 原始音频是什么</h3> <p>声音的本质是空气压强的振动。麦克风把这个振动采样成数字序列，叫做 <strong>PCM（脉冲编码调制）</strong> 信号：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>采样率 16kHz：每秒采集 16000 个数值
每个数值：该时刻的声压，范围 [-1.0, 1.0] 的 float
1 秒语音  → shape [16000]
30 秒语音 → shape [480000]
</code></pre></div></div> <p>这个采样率不是随便定的。人声基频大概在 80Hz-300Hz，辅音频率可以到 8kHz。根据<strong>奈奎斯特采样定理</strong>：要还原最高频率 F 的信号，采样率至少要 2F。语音识别通常只关心 8kHz 以下，所以 16kHz 够用了。</p> <h3 id="22-为什么不直接用原始波形">2.2 为什么不直接用原始波形</h3> <p>直接用原始波形训练模型有两个根本性问题：</p> <ol> <li><strong>太长了</strong>。30 秒音频有 480000 个点，Transformer 处理不了这么长的序列。</li> <li><strong>冗余且敏感</strong>。两个人说同一句话，波形可能完全不同（音色、语速、音调不同），但我们想要相同的文字输出。时域波形包含了太多与语义无关的信息。</li> </ol> <p>解决方案：<strong>把时域信号变换到频域</strong>，看每个时间窗口里各个频率的能量分布——这就是 Log-Mel Spectrogram 的核心思路。</p> <h3 id="23-log-mel-spectrogram-提取流水线">2.3 Log-Mel Spectrogram 提取流水线</h3> <p>下图展示了完整的特征提取流水线（用真实的合成信号生成）：</p> <p><img src="/assets/img/posts/20260624-asr-qwen3/000_mel_pipeline.png" alt="Log-Mel特征提取完整流水线：波形→分帧→FFT→Mel滤波→对数压缩"/></p> <p>从左到右一步步来：</p> <p><strong>第一步：分帧（Framing）</strong></p> <p>不能对整段音频做一次 FFT——那样会丢失时间信息（不知道哪个频率在什么时候出现）。要把音频切成小段，每段叫一<strong>帧</strong>：</p> <ul> <li>帧长（window size）：25ms → 在 16kHz 下 = <strong>400 个采样点</strong></li> <li>帧移（hop length）：10ms → 在 16kHz 下 = <strong>160 个采样点</strong></li> <li>相邻帧有重叠（帧长 &gt; 帧移），保证时间连续性</li> </ul> <p>30 秒音频 → <code class="language-plaintext highlighter-rouge">(480000 - 400) / 160 + 1 ≈ 3000</code> 帧</p> <p><strong>第二步：加窗（Hamming Window）</strong></p> <p>每帧边缘截断会产生频谱泄漏，所以 FFT 之前要乘上汉明窗，让边缘平滑衰减到 0。</p> <p><strong>第三步：FFT → 功率谱</strong></p> <p>对每帧做快速傅里叶变换，输出该帧各频率的能量：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>
<span class="n">frame</span> <span class="o">=</span> <span class="n">signal</span><span class="p">[</span><span class="mi">0</span><span class="p">:</span><span class="mi">400</span><span class="p">]</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="nf">hamming</span><span class="p">(</span><span class="mi">400</span><span class="p">)</span>   <span class="c1"># 加窗
</span><span class="n">fft_out</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">fft</span><span class="p">.</span><span class="nf">rfft</span><span class="p">(</span><span class="n">frame</span><span class="p">,</span> <span class="n">n</span><span class="o">=</span><span class="mi">400</span><span class="p">)</span>       <span class="c1"># shape [201]，0 到 8kHz
</span><span class="n">power</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">abs</span><span class="p">(</span><span class="n">fft_out</span><span class="p">)</span> <span class="o">**</span> <span class="mi">2</span>              <span class="c1"># 功率谱，shape [201]
</span></code></pre></div></div> <p><strong>第四步：Mel 滤波器组</strong></p> <p>人耳对低频敏感、对高频不敏感（对数感知）。Mel 刻度就是模拟这个感知特性的频率变换：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>mel = 2595 × log10(1 + freq_hz / 700)
</code></pre></div></div> <p>128 个三角形带通滤波器均匀分布在 Mel 刻度上（低频密，高频疏），把线性功率谱压缩到 128 个 bin。</p> <p><strong>第五步：取对数</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">log_mel</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">maximum</span><span class="p">(</span><span class="n">mel_spec</span><span class="p">,</span> <span class="mf">1e-10</span><span class="p">))</span>
</code></pre></div></div> <p>取对数的好处：压缩动态范围，使能量分布更均匀，模型更容易学习。</p> <p><strong>完整维度变化（30s音频）</strong>：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>原始波形      [480000]
  ↓ 分帧 + 汉明窗
帧序列        [3000, 400]
  ↓ 对每帧 FFT → 取模平方
功率谱        [3000, 201]
  ↓ Mel 滤波器组（128 个 bin）
Mel 频谱      [3000, 128]
  ↓ 取对数
Log-Mel 特征  [3000, 128]   ← 这就是模型的输入
</code></pre></div></div> <p>你可以把 Log-Mel 理解成一张”声纹图像”——横轴是时间（帧），纵轴是梅尔频率（128 个 bin），像素值是对数能量。ASR 模型的输入，就是这张二维图。</p> <p>Qwen3-ASR 的 preprocessor 配置完全对应上面的参数：</p> <ul> <li><code class="language-plaintext highlighter-rouge">n_mels = 128</code>, <code class="language-plaintext highlighter-rouge">n_fft = 400</code>（25ms × 16kHz）</li> <li><code class="language-plaintext highlighter-rouge">hop_length = 160</code>（10ms × 16kHz）</li> <li><code class="language-plaintext highlighter-rouge">chunk_length = 30</code>（秒）→ <strong>3000 帧</strong></li> </ul> <hr/> <h2 id="3-asr-解码范式演进从-ctc-到-llm">3. ASR 解码范式演进：从 CTC 到 LLM</h2> <p>理解了输入怎么来的，再看”输出怎么出来”。</p> <p>读到这里可能会有个疑问：分帧时相邻帧有 overlap，那转成文字的时候会不会出现重复？</p> <p><strong>不会，而且这是两个完全不同层面的事。</strong> 分帧 overlap 是信号处理层的操作，目的是让频谱平滑过渡、不丢边界信息，这一步完全不产生文字。文字是后面的解码器输出的——Encoder 先把 3000 帧压缩成 375 个 audio token（已经聚合了重叠帧的信息），再由 LM 整体生成一段文字，根本不存在”帧有重叠所以字会重复”的问题。</p> <p>但这个顾虑在 CTC 范式里确实是真实存在的，看下面的对比就清楚了。</p> <h3 id="31-ctc最简单的方案重复问题真实存在">3.1 CTC：最简单的方案，重复问题真实存在</h3> <p><strong>CTC（Connectionist Temporal Classification）</strong> 是端到端 ASR 最早流行的方案。思路极简：在<strong>每一帧</strong>上直接预测字符概率，3000 帧就输出 3000 个预测。</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Audio Embedding [T, d]
  ↓ Linear
帧级别 logits [T, vocab_size + 1]   # +1 是 blank token
  ↓ CTC Decode（去 blank，合并相邻重复）
文字序列
</code></pre></div></div> <p>帧移 10ms、帧长 25ms，相邻帧本来就有重叠，同一个音节会跨越好几帧，每帧都预测同一个字符是完全正常的。所以 CTC 专门设计了 <strong>blank token</strong> 来处理这个问题：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>CTC 每帧输出：  h  h  e  _  l  l  _  l  o
                          ↓ 去掉 blank（_）
               h  h  e  l  l  l  o
                          ↓ 合并相邻重复
               h  e  l  l  o   →  "hello"
</code></pre></div></div> <p>blank token 的含义是”这一帧没有新字符”，解码时先去 blank 再合并相邻重复，就能还原出干净的文字序列。</p> <p>优点：<strong>非自回归（NAR）</strong>，所有帧并行解码，推理极快。</p> <p>缺点：每帧的预测完全独立，没有语言模型先验——不知道”你”后面更可能是”好”还是”们”，识别准确率天花板低。</p> <h3 id="32-attention-encoder-decoderwhisper-的路子">3.2 Attention Encoder-Decoder：Whisper 的路子</h3> <p>Encoder 处理音频，Decoder 自回归生成文字，通过 cross-attention 连接。Decoder 直接生成目标文字序列，<strong>不做帧级别预测</strong>，从根本上绕开了重复问题。Whisper 就是这个范式，效果比 CTC 好很多，但不能原生支持流式。</p> <h3 id="33-llm-based-asrqwen3-asr-的选择">3.3 LLM-based ASR：Qwen3-ASR 的选择</h3> <p>最新趋势：把 Audio Encoder 接到预训练好的大语言模型上，<strong>把音频当成 text prompt 的前缀，让 LLM 自回归续写出转录文字</strong>。和 AED 一样，LM 直接生成文字序列，不存在帧级别重复的问题。</p> <p>核心优势：继承 LLM 的语言理解能力、原生多语言、支持 instruction following（识别+翻译+打标签）。代价是参数量大，推理 latency 高——这也是推理优化有意义的原因。</p> <hr/> <h2 id="4-qwen3-asr-17b-架构详解">4. Qwen3-ASR-1.7B 架构详解</h2> <p>先看两张图，再逐一解释容易看懵的地方：</p> <p><img src="/assets/img/posts/20260624-asr-qwen3/001_Figure_1.png" alt="Qwen3-ASR 能力概览：多语言、多场景、流式/离线一体"/></p> <p><img src="/assets/img/posts/20260624-asr-qwen3/002_Figure_2.png" alt="AuT 独立预训练结构（左）与 Qwen3-ASR 完整推理结构（右）"/></p> <p>这张架构图信息量很大，而且<strong>左右两侧画的是两件不同的事</strong>，容易搞混，下面拆开说。</p> <h3 id="41-aut-是什么为什么有-encoder--decoder">4.1 AuT 是什么，为什么有 Encoder + Decoder</h3> <p><strong>AuT（Audio Transformer）</strong> 是一个<strong>单独预训练的 AED（Attention Encoder-Decoder）模型</strong>，在进入 Qwen3-ASR 训练之前就已经独立训练好了。它本身是个完整的端到端 ASR 系统，不是 Qwen3-ASR 的子组件。</p> <p>训练数据：约 <strong>4000 万小时</strong>伪标注语音。训练目标：给音频进来，输出转录文字。这就是个标准的 seq2seq ASR。</p> <p><strong>Figure 2 左侧</strong>展示的正是 AuT 独立运行时的完整结构：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>FBank 音频特征（100Hz）
  ↓
AuT Encoder（3× Downsampling Conv2d + 32× Self-Attention）
  ↓ 8× 降采样，输出 12.5Hz 的音频隐向量
AuT Decoder（8× Decoder Self-Attention + Cross-Attention）
  ↑ 同时接收 Qwen Tokenizer 的文字 token（&lt;sos&gt;...）
  ↓
转录文字（Hi, I'm Qwen... &lt;eos&gt;）
</code></pre></div></div> <p>AuT Decoder 的作用是 AuT <strong>预训练阶段</strong>的解码头，负责把 Encoder 输出的音频表示转成文字，跟 Whisper 的 Decoder 是同样的角色。</p> <h3 id="42-qwen3-asraut-decoder-被丢弃了">4.2 Qwen3-ASR：AuT Decoder 被丢弃了</h3> <p><strong>Figure 2 右侧</strong>是 Qwen3-ASR 实际推理时的结构。最关键的一点：<strong>AuT Decoder 被完全丢弃</strong>，只保留了 AuT Encoder 作为音频特征提取器，解码工作全部交给 Qwen3 LM。</p> <p>为什么换掉？AuT Decoder 是个轻量 seq2seq 解码器，语言理解能力有限。换成 Qwen3 LM 之后，直接继承了完整 LLM 的多语言能力、长文本理解和 instruction following 能力。</p> <table> <thead> <tr> <th> </th> <th>AuT Decoder</th> <th>Qwen3 LM</th> </tr> </thead> <tbody> <tr> <td>用途</td> <td>AuT <strong>预训练</strong>阶段的解码器</td> <td>Qwen3-ASR <strong>推理</strong>阶段的解码器</td> </tr> <tr> <td>命运</td> <td>预训练完成后<strong>丢弃</strong></td> <td>保留，是最终模型的核心</td> </tr> <tr> <td>架构</td> <td>Cross-attention + Causal self-attention</td> <td>Decoder-only 因果 Transformer</td> </tr> <tr> <td>语言能力</td> <td>仅 ASR 导向，轻量</td> <td>完整 LLM，多语言 + instruction following</td> </tr> </tbody> </table> <h3 id="43-figure-2-右侧的三列输入是什么意思">4.3 Figure 2 右侧的三列输入是什么意思</h3> <p>右侧三列并排的结构描述的是<strong>喂给 Qwen3 LM 的一条完整输入序列的三个来源</strong>，两个 “Qwen Tokenizer” 是同一个 tokenizer，只是图示上把来自不同文本的 token 分开画了：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>左列（Qwen Tokenizer）     中列（Pretrained AuT Encoder）    右列（Qwen Tokenizer）
        ↓                              ↓                              ↓
  system prompt 里的             音频经 AuT Encoder               指令文本 token
  "Entities" token               编码出的音频 token            （"请转录以上音频"等）
  （命名实体/领域词汇）
        ↓                              ↓                              ↓
        └──────────────────────────────┴──────────────────────────────┘
                                       ↓
                                  Qwen3 LM
                                       ↓
                     Language Chinese&lt;asr_text&gt;Hi, I'm Qwen...
</code></pre></div></div> <p><strong>Entities（左列）</strong> 是用户可以注入 system prompt 的上下文信息——比如说话人名、品牌词、领域术语。模型训练时学会了利用这些信息做定制化识别，类似于给转写加 hints。</p> <p>Qwen3 LM 最终收到的是这三部分拼成的一条序列：<code class="language-plaintext highlighter-rouge">[Entities tokens] + [audio tokens] + [instruction tokens]</code>，没有任何 cross-attention——一切都是 decoder-only 的因果注意力。</p> <h3 id="44-projector-在哪里">4.4 Projector 在哪里</h3> <p>图里<strong>没有画 Projector</strong>，但它确实存在，论文原文写得很清楚：</p> <blockquote> <p>“Qwen3-ASR-1.7B is built with Qwen3-1.7B, <strong>a projector</strong>, and an AuT encoder with 300M parameters and 1024 hidden size.”</p> </blockquote> <p>AuT Encoder 输出维度 1024，Qwen3-1.7B 的 hidden size 2048，不匹配，中间必须有一个线性层做维度对齐。这个 Projector 就隐含在 Figure 2 右侧中列 AuT Encoder 和 Qwen3 LM 之间的那条箭头里，没有单独画出来。</p> <h3 id="45-完整数据流以-30-秒音频为例">4.5 完整数据流（以 30 秒音频为例）</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>原始音频 [480000 samples]
  ↓ Log-Mel 特征提取（FBank，100Hz）
FBank 特征 [3000, 128]
  ↓ AuT Encoder（Conv2d 8× 降采样 + 32 层 Self-Attention）
音频隐向量 [375, 1024]           ← 帧数从 3000 压缩到 375
  ↓ Projector（Linear: 1024 → 2048，图中未画出）
音频 token [375, 2048]
  ↓ 与文字 token 拼接
[Entities_tokens] + [audio_tokens × 375] + [instruction_tokens]
  ↓ Qwen3 LM（decoder-only，28 层因果 Transformer）
  ↓ 自回归解码
转录文字
</code></pre></div></div> <h3 id="46-qwen3-lm-关键参数">4.6 Qwen3 LM 关键参数</h3> <table> <thead> <tr> <th>参数</th> <th>值</th> <th>含义</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">hidden_size</code></td> <td>2048</td> <td>隐层维度（也是 Projector 的输出维度）</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">num_hidden_layers</code></td> <td>28</td> <td>Transformer 层数</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">num_attention_heads</code></td> <td>16</td> <td>Q 的头数</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">num_key_value_heads</code></td> <td>8</td> <td>KV 的头数（GQA，KV Cache 减半）</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">vocab_size</code></td> <td>151936</td> <td>词表大小</td> </tr> </tbody> </table> <p><strong>GQA（Grouped Query Attention）</strong> 是内置的推理优化：<code class="language-plaintext highlighter-rouge">num_key_value_heads=8</code> 而 <code class="language-plaintext highlighter-rouge">num_attention_heads=16</code>，每组 2 个 Q head 共享一对 KV head，KV Cache 直接减半。</p> <hr/> <h2 id="5-训练流水线qwen3-lm-是怎么学会理解音频的">5. 训练流水线：Qwen3 LM 是怎么学会理解音频的</h2> <p>这里有个很自然的问题：Qwen3 LM 之前只见过文字，AuT Encoder 输出的音频 embedding 它凭什么认识？</p> <p>答案是：<strong>它在进入 ASR 训练之前就已经见过音频了</strong>。Qwen3-ASR 的训练分四个阶段，前两个阶段和 Qwen3-Omni 共用：</p> <blockquote> <p>“The training process consists of AuT pretraining, Omni pretraining, and ASR post-training, where the <strong>first two stages are identical to those of Qwen3-Omni</strong>.”</p> </blockquote> <h3 id="51-stage-1aut-预训练">5.1 Stage 1：AuT 预训练</h3> <p>单独训练 AuT 这个 AED 模型，跟 Qwen3-ASR 的其余部分没有关系。</p> <ul> <li><strong>数据</strong>：约 4000 万小时伪标注 ASR 数据，中英文为主</li> <li><strong>训练目标</strong>：完整的 seq2seq ASR——AuT Encoder + AuT Decoder 一起训</li> <li><strong>产出</strong>：一个泛化能力强、在动态 attention window 下稳定的音频 Encoder</li> </ul> <p>这个阶段结束后，AuT Decoder 就功成身退了，后续只用 Encoder。</p> <h3 id="52-stage-2omni-预训练">5.2 Stage 2：Omni 预训练</h3> <p>Qwen3-Omni 是 Qwen3 LM 的多模态版本，在<strong>音频、视觉、文字</strong>的混合数据上联合预训练。</p> <ul> <li><strong>数据</strong>：多任务音视频 + 文本，两个尺寸的模型都训练了 <strong>3 trillion tokens</strong></li> <li><strong>产出</strong>：一个 LM，它已经具备了多模态理解能力——见过音频表示，知道怎么把它映射成文字</li> </ul> <p><strong>这是 Qwen3 LM 能”认识”音频 embedding 的根本原因</strong>。它不是一个纯文本 LLM，而是从 Qwen3-Omni 出发，天然有多模态感知基础。</p> <p>Projector 虽然不是在这个阶段从头训的，但 LM 在 Omni 预训练里已经适应了音频 token 的分布，后续联合微调时收敛更快、对齐更稳。</p> <h3 id="53-stage-3asr-监督微调sft">5.3 Stage 3：ASR 监督微调（SFT）</h3> <p>把 AuT Encoder + Projector + Qwen3-Omni LM 接在一起，针对 ASR 任务联合微调。</p> <ul> <li><strong>数据</strong>：多语言 ASR 数据 + 非语音数据 + 流式增强数据 + 上下文偏置数据（Entities 相关）</li> <li><strong>目标</strong>：让整条链路对齐——Projector 学会把 AuT 的表示映射到 LM 能理解的空间，LM 学会输出规范的 ASR 格式</li> <li><strong>产出</strong>：一个 ASR 专项模型，”does not follow natural-language instructions”（不是通用助手，就是转写机器）</li> </ul> <h3 id="54-stage-4强化学习rl">5.4 Stage 4：强化学习（RL）</h3> <p>用 GSPO（一种 RL 算法）继续优化，主要提升准确率和多语言泛化能力。</p> <ul> <li><strong>数据</strong>：约 5 万条语音，其中 35% 中英文、35% 多语言、30% 功能性数据（语言识别、格式规范等）</li> <li><strong>目标</strong>：在 SFT 基础上进一步降低 WER 指标</li> </ul> <p><strong>WER（Word Error Rate，词错误率）</strong> 是 ASR 最核心的评估指标，计算公式是：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>WER = (替换错误数 + 删除错误数 + 插入错误数) / 参考文本总词数 × 100%
</code></pre></div></div> <p>举个例子：参考文本是”今天天气很好”（5个字），模型输出”今天气很好”（漏了”天”），则 WER = 1/5 = 20%。<strong>WER 越低越好</strong>，0% 是完美识别。State-of-the-art 模型在标准测试集（如 LibriSpeech）上 WER 已经能做到 1-2%。</p> <hr/> <p>整条训练链路的逻辑很清晰：先把音频 Encoder 训好（Stage 1），再让 LM 具备多模态感知（Stage 2），最后把两者接起来对齐（Stage 3 + 4）。Projector 是粘合两者的关键，从头训练，Stage 3 里和整条链路一起联合优化。</p> <hr/> <h2 id="6-离线推理-vs-流式推理原理与代码">6. 离线推理 vs 流式推理：原理与代码</h2> <p>这是本文的核心部分。两种模式在<strong>音频 Encoder 的计算方式</strong>上有根本区别。</p> <h3 id="51-原理对比">5.1 原理对比</h3> <p>如下图，离线模式和流式模式的数据流完全不同：</p> <p><img src="/assets/img/posts/20260624-asr-qwen3/004_offline_vs_streaming.png" alt="离线模式与流式模式的数据流对比"/></p> <p><strong>离线模式（Offline）</strong>：</p> <ul> <li>等待完整音频输入（比如一段 30 秒录音）</li> <li>Audio Encoder 一次性处理全部 3000 帧 → 输出 750 个 audio token</li> <li>把 750 个 token 一次性送入 LLM prefill</li> <li>LLM 开始自回归解码，输出转录文字</li> </ul> <p><strong>流式模式（Streaming）</strong>：</p> <ul> <li>音频以 chunk 方式实时输入（每次 <code class="language-plaintext highlighter-rouge">conv_chunksize=500</code> 帧 = 5 秒）</li> <li>每积累到足够的帧，Audio Encoder 立即处理这个 chunk → 输出约 125 个 audio token</li> <li>LLM 立即对这批 audio token 开始解码，输出部分转录结果</li> <li>后续 chunk 到来时继续追加，KV Cache 复用</li> </ul> <p><strong>核心差异</strong>：</p> <table> <thead> <tr> <th>指标</th> <th>离线</th> <th>流式</th> </tr> </thead> <tbody> <tr> <td>TTFT（首字延迟）</td> <td>要等完整音频 + 全量 encode</td> <td>~5s（第一个 chunk 完成即可出字）</td> </tr> <tr> <td>吞吐量</td> <td>高（一次大 prefill）</td> <td>略低（多次小 prefill）</td> </tr> <tr> <td>WER</td> <td>更低（全局上下文）</td> <td>略高（滑动窗口 context 受限）</td> </tr> <tr> <td>适用场景</td> <td>转写、字幕后处理</td> <td>实时对话、语音助手</td> </tr> </tbody> </table> <p>从论文数据来看，流式模式的 WER 代价是可接受的：</p> <table> <thead> <tr> <th>模式</th> <th>LibriSpeech clean</th> <th>LibriSpeech other</th> <th>Fleurs-zh</th> </tr> </thead> <tbody> <tr> <td>离线</td> <td>1.63</td> <td>3.38</td> <td>2.41</td> </tr> <tr> <td>流式</td> <td>1.95</td> <td>4.51</td> <td>2.84</td> </tr> </tbody> </table> <h3 id="52-流式模式的滑动窗口机制">5.2 流式模式的滑动窗口机制</h3> <p>流式 Audio Encoder 不是”截断式”处理的——它用了滑动窗口（<code class="language-plaintext highlighter-rouge">n_window=50</code> 帧的 context），让相邻 chunk 之间有上下文重叠，避免边界处的识别断裂。</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>chunk 1: 帧 [0, 500)     → 处理时 context=[0, 550)（多看 n_window 帧）
chunk 2: 帧 [500, 1000)  → 处理时 context=[450, 1050)（前后各看 n_window 帧）
chunk 3: 帧 [1000, 1500) → 处理时 context=[950, 1550)
...
</code></pre></div></div> <p>这样每个 chunk 都有来自相邻帧的上下文，边界处的辅音、停顿才能被正确识别。</p> <h3 id="53-可运行代码示例">5.3 可运行代码示例</h3> <p>下面两段代码可以直接跑，分别演示离线和流式的调用方式。需要先安装依赖：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip <span class="nb">install </span>transformers torch soundfile numpy
</code></pre></div></div> <p><strong>离线推理（Offline）</strong>：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">torch</span>
<span class="kn">import</span> <span class="n">soundfile</span> <span class="k">as</span> <span class="n">sf</span>
<span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>
<span class="kn">from</span> <span class="n">transformers</span> <span class="kn">import</span> <span class="n">AutoProcessor</span><span class="p">,</span> <span class="n">Qwen2AudioForConditionalGeneration</span>

<span class="n">model_name</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Qwen/Qwen3-ASR-1.7B</span><span class="sh">"</span>
<span class="n">processor</span> <span class="o">=</span> <span class="n">AutoProcessor</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span><span class="n">model_name</span><span class="p">)</span>
<span class="n">model</span> <span class="o">=</span> <span class="n">Qwen2AudioForConditionalGeneration</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span>
    <span class="n">model_name</span><span class="p">,</span> <span class="n">torch_dtype</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="n">float16</span><span class="p">,</span> <span class="n">device_map</span><span class="o">=</span><span class="sh">"</span><span class="s">auto</span><span class="sh">"</span>
<span class="p">)</span>

<span class="k">def</span> <span class="nf">transcribe_offline</span><span class="p">(</span><span class="n">audio_path</span><span class="p">:</span> <span class="nb">str</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">离线模式：完整音频一次性推理</span><span class="sh">"""</span>
    <span class="n">audio</span><span class="p">,</span> <span class="n">sr</span> <span class="o">=</span> <span class="n">sf</span><span class="p">.</span><span class="nf">read</span><span class="p">(</span><span class="n">audio_path</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">sr</span> <span class="o">!=</span> <span class="mi">16000</span><span class="p">:</span>
        <span class="c1"># 实际使用时可用 librosa.resample 重采样
</span>        <span class="k">raise</span> <span class="nc">ValueError</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">需要 16kHz，当前 </span><span class="si">{</span><span class="n">sr</span><span class="si">}</span><span class="s">Hz</span><span class="sh">"</span><span class="p">)</span>

    <span class="c1"># 构造对话格式的输入
</span>    <span class="n">conversation</span> <span class="o">=</span> <span class="p">[</span>
        <span class="p">{</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="p">[</span>
            <span class="p">{</span><span class="sh">"</span><span class="s">type</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">audio</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">audio_url</span><span class="sh">"</span><span class="p">:</span> <span class="n">audio_path</span><span class="p">},</span>
            <span class="p">{</span><span class="sh">"</span><span class="s">type</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">text</span><span class="sh">"</span><span class="p">,</span>  <span class="sh">"</span><span class="s">text</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">请转录以上音频。</span><span class="sh">"</span><span class="p">}</span>
        <span class="p">]}</span>
    <span class="p">]</span>
    <span class="n">text_prompt</span> <span class="o">=</span> <span class="n">processor</span><span class="p">.</span><span class="nf">apply_chat_template</span><span class="p">(</span>
        <span class="n">conversation</span><span class="p">,</span> <span class="n">add_generation_prompt</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span> <span class="n">tokenize</span><span class="o">=</span><span class="bp">False</span>
    <span class="p">)</span>

    <span class="c1"># processor 内部做 Log-Mel 提取，chunk_length=30s 切块、padding
</span>    <span class="n">inputs</span> <span class="o">=</span> <span class="nf">processor</span><span class="p">(</span>
        <span class="n">text</span><span class="o">=</span><span class="n">text_prompt</span><span class="p">,</span>
        <span class="n">audios</span><span class="o">=</span><span class="p">[</span><span class="n">audio</span><span class="p">],</span>        <span class="c1"># raw waveform，float32 numpy array
</span>        <span class="n">sampling_rate</span><span class="o">=</span><span class="mi">16000</span><span class="p">,</span>
        <span class="n">return_tensors</span><span class="o">=</span><span class="sh">"</span><span class="s">pt</span><span class="sh">"</span>
    <span class="p">).</span><span class="nf">to</span><span class="p">(</span><span class="n">model</span><span class="p">.</span><span class="n">device</span><span class="p">)</span>

    <span class="c1"># 一次性前向：完整音频 encoder + LLM prefill + autoregressive decode
</span>    <span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="nf">no_grad</span><span class="p">():</span>
        <span class="n">output_ids</span> <span class="o">=</span> <span class="n">model</span><span class="p">.</span><span class="nf">generate</span><span class="p">(</span>
            <span class="o">**</span><span class="n">inputs</span><span class="p">,</span>
            <span class="n">max_new_tokens</span><span class="o">=</span><span class="mi">512</span><span class="p">,</span>
            <span class="n">do_sample</span><span class="o">=</span><span class="bp">False</span>        <span class="c1"># greedy decode
</span>        <span class="p">)</span>

    <span class="c1"># 去掉 prompt 部分，只解码新生成的 token
</span>    <span class="n">n_input</span> <span class="o">=</span> <span class="n">inputs</span><span class="p">[</span><span class="sh">"</span><span class="s">input_ids</span><span class="sh">"</span><span class="p">].</span><span class="n">shape</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span>
    <span class="n">result</span> <span class="o">=</span> <span class="n">processor</span><span class="p">.</span><span class="n">tokenizer</span><span class="p">.</span><span class="nf">decode</span><span class="p">(</span>
        <span class="n">output_ids</span><span class="p">[</span><span class="mi">0</span><span class="p">,</span> <span class="n">n_input</span><span class="p">:],</span> <span class="n">skip_special_tokens</span><span class="o">=</span><span class="bp">True</span>
    <span class="p">)</span>
    <span class="k">return</span> <span class="n">result</span><span class="p">.</span><span class="nf">strip</span><span class="p">()</span>

<span class="c1"># 示例：生成一段合成音频测试
</span><span class="k">def</span> <span class="nf">make_test_audio</span><span class="p">(</span><span class="n">path</span><span class="o">=</span><span class="sh">"</span><span class="s">test.wav</span><span class="sh">"</span><span class="p">,</span> <span class="n">duration</span><span class="o">=</span><span class="mi">3</span><span class="p">,</span> <span class="n">sr</span><span class="o">=</span><span class="mi">16000</span><span class="p">):</span>
    <span class="n">t</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">linspace</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">duration</span><span class="p">,</span> <span class="nf">int</span><span class="p">(</span><span class="n">sr</span> <span class="o">*</span> <span class="n">duration</span><span class="p">))</span>
    <span class="n">wave</span> <span class="o">=</span> <span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">sin</span><span class="p">(</span><span class="mi">2</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="n">pi</span> <span class="o">*</span> <span class="mi">200</span> <span class="o">*</span> <span class="n">t</span><span class="p">)</span> <span class="o">*</span> <span class="mf">0.4</span> <span class="o">+</span>
            <span class="n">np</span><span class="p">.</span><span class="nf">sin</span><span class="p">(</span><span class="mi">2</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="n">pi</span> <span class="o">*</span> <span class="mi">400</span> <span class="o">*</span> <span class="n">t</span><span class="p">)</span> <span class="o">*</span> <span class="mf">0.3</span><span class="p">).</span><span class="nf">astype</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="n">float32</span><span class="p">)</span>
    <span class="n">sf</span><span class="p">.</span><span class="nf">write</span><span class="p">(</span><span class="n">path</span><span class="p">,</span> <span class="n">wave</span><span class="p">,</span> <span class="n">sr</span><span class="p">)</span>

<span class="nf">make_test_audio</span><span class="p">()</span>
<span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="s">离线结果:</span><span class="sh">"</span><span class="p">,</span> <span class="nf">transcribe_offline</span><span class="p">(</span><span class="sh">"</span><span class="s">test.wav</span><span class="sh">"</span><span class="p">))</span>
</code></pre></div></div> <p><strong>流式推理（Streaming）</strong>：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">torch</span>
<span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>
<span class="kn">import</span> <span class="n">soundfile</span> <span class="k">as</span> <span class="n">sf</span>
<span class="kn">from</span> <span class="n">transformers</span> <span class="kn">import</span> <span class="n">AutoProcessor</span><span class="p">,</span> <span class="n">Qwen2AudioForConditionalGeneration</span>

<span class="n">model_name</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Qwen/Qwen3-ASR-1.7B</span><span class="sh">"</span>
<span class="n">processor</span> <span class="o">=</span> <span class="n">AutoProcessor</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span><span class="n">model_name</span><span class="p">)</span>
<span class="n">model</span> <span class="o">=</span> <span class="n">Qwen2AudioForConditionalGeneration</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span>
    <span class="n">model_name</span><span class="p">,</span> <span class="n">torch_dtype</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="n">float16</span><span class="p">,</span> <span class="n">device_map</span><span class="o">=</span><span class="sh">"</span><span class="s">auto</span><span class="sh">"</span>
<span class="p">)</span>

<span class="c1"># 流式推理的核心参数（来自 config）
</span><span class="n">SAMPLE_RATE</span> <span class="o">=</span> <span class="mi">16000</span>
<span class="n">CHUNK_FRAMES</span> <span class="o">=</span> <span class="mi">500</span>          <span class="c1"># conv_chunksize = 500 帧 = 5 秒
</span><span class="n">HOP_LENGTH</span> <span class="o">=</span> <span class="mi">160</span>            <span class="c1"># 10ms per frame
</span><span class="n">CHUNK_SAMPLES</span> <span class="o">=</span> <span class="n">CHUNK_FRAMES</span> <span class="o">*</span> <span class="n">HOP_LENGTH</span>  <span class="c1"># 80000 采样点 = 5s
</span>
<span class="k">def</span> <span class="nf">stream_transcribe</span><span class="p">(</span><span class="n">audio_path</span><span class="p">:</span> <span class="nb">str</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    流式模式：模拟实时音频流，按 chunk 推进，每 chunk 出一次部分转录。
    真实场景下 audio_chunks 来自麦克风 buffer。
    </span><span class="sh">"""</span>
    <span class="n">audio</span><span class="p">,</span> <span class="n">sr</span> <span class="o">=</span> <span class="n">sf</span><span class="p">.</span><span class="nf">read</span><span class="p">(</span><span class="n">audio_path</span><span class="p">,</span> <span class="n">dtype</span><span class="o">=</span><span class="sh">'</span><span class="s">float32</span><span class="sh">'</span><span class="p">)</span>
    <span class="k">assert</span> <span class="n">sr</span> <span class="o">==</span> <span class="mi">16000</span>

    <span class="c1"># 把完整音频切成 5s 的 chunk，模拟实时流
</span>    <span class="n">audio_chunks</span> <span class="o">=</span> <span class="p">[</span>
        <span class="n">audio</span><span class="p">[</span><span class="n">i</span><span class="p">:</span><span class="n">i</span> <span class="o">+</span> <span class="n">CHUNK_SAMPLES</span><span class="p">]</span>
        <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="nf">len</span><span class="p">(</span><span class="n">audio</span><span class="p">),</span> <span class="n">CHUNK_SAMPLES</span><span class="p">)</span>
    <span class="p">]</span>

    <span class="n">accumulated_audio</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">array</span><span class="p">([],</span> <span class="n">dtype</span><span class="o">=</span><span class="n">np</span><span class="p">.</span><span class="n">float32</span><span class="p">)</span>
    <span class="n">partial_results</span> <span class="o">=</span> <span class="p">[]</span>

    <span class="k">for</span> <span class="n">chunk_idx</span><span class="p">,</span> <span class="n">chunk</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">audio_chunks</span><span class="p">):</span>
        <span class="n">accumulated_audio</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">concatenate</span><span class="p">([</span><span class="n">accumulated_audio</span><span class="p">,</span> <span class="n">chunk</span><span class="p">])</span>
        
        <span class="c1"># 构造当前已有音频的 prompt（流式：用 accumulated audio）
</span>        <span class="n">conversation</span> <span class="o">=</span> <span class="p">[</span>
            <span class="p">{</span><span class="sh">"</span><span class="s">role</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">user</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">content</span><span class="sh">"</span><span class="p">:</span> <span class="p">[</span>
                <span class="p">{</span><span class="sh">"</span><span class="s">type</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">audio</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">audio_url</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">stream</span><span class="sh">"</span><span class="p">},</span>
                <span class="p">{</span><span class="sh">"</span><span class="s">type</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">text</span><span class="sh">"</span><span class="p">,</span>  <span class="sh">"</span><span class="s">text</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">请转录以上音频。</span><span class="sh">"</span><span class="p">}</span>
            <span class="p">]}</span>
        <span class="p">]</span>
        <span class="n">text_prompt</span> <span class="o">=</span> <span class="n">processor</span><span class="p">.</span><span class="nf">apply_chat_template</span><span class="p">(</span>
            <span class="n">conversation</span><span class="p">,</span> <span class="n">add_generation_prompt</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span> <span class="n">tokenize</span><span class="o">=</span><span class="bp">False</span>
        <span class="p">)</span>

        <span class="c1"># processor 对当前累积的音频提取特征
</span>        <span class="n">inputs</span> <span class="o">=</span> <span class="nf">processor</span><span class="p">(</span>
            <span class="n">text</span><span class="o">=</span><span class="n">text_prompt</span><span class="p">,</span>
            <span class="n">audios</span><span class="o">=</span><span class="p">[</span><span class="n">accumulated_audio</span><span class="p">],</span>
            <span class="n">sampling_rate</span><span class="o">=</span><span class="n">SAMPLE_RATE</span><span class="p">,</span>
            <span class="n">return_tensors</span><span class="o">=</span><span class="sh">"</span><span class="s">pt</span><span class="sh">"</span>
        <span class="p">).</span><span class="nf">to</span><span class="p">(</span><span class="n">model</span><span class="p">.</span><span class="n">device</span><span class="p">)</span>

        <span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="nf">no_grad</span><span class="p">():</span>
            <span class="n">output_ids</span> <span class="o">=</span> <span class="n">model</span><span class="p">.</span><span class="nf">generate</span><span class="p">(</span>
                <span class="o">**</span><span class="n">inputs</span><span class="p">,</span>
                <span class="n">max_new_tokens</span><span class="o">=</span><span class="mi">256</span><span class="p">,</span>
                <span class="n">do_sample</span><span class="o">=</span><span class="bp">False</span>
            <span class="p">)</span>

        <span class="n">n_input</span> <span class="o">=</span> <span class="n">inputs</span><span class="p">[</span><span class="sh">"</span><span class="s">input_ids</span><span class="sh">"</span><span class="p">].</span><span class="n">shape</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span>
        <span class="n">partial</span> <span class="o">=</span> <span class="n">processor</span><span class="p">.</span><span class="n">tokenizer</span><span class="p">.</span><span class="nf">decode</span><span class="p">(</span>
            <span class="n">output_ids</span><span class="p">[</span><span class="mi">0</span><span class="p">,</span> <span class="n">n_input</span><span class="p">:],</span> <span class="n">skip_special_tokens</span><span class="o">=</span><span class="bp">True</span>
        <span class="p">).</span><span class="nf">strip</span><span class="p">()</span>

        <span class="n">partial_results</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">partial</span><span class="p">)</span>
        <span class="n">elapsed_s</span> <span class="o">=</span> <span class="p">(</span><span class="n">chunk_idx</span> <span class="o">+</span> <span class="mi">1</span><span class="p">)</span> <span class="o">*</span> <span class="mi">5</span>
        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">[t=</span><span class="si">{</span><span class="n">elapsed_s</span><span class="si">:</span><span class="mi">02</span><span class="n">d</span><span class="si">}</span><span class="s">s] 部分转录: </span><span class="si">{</span><span class="n">partial</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>

    <span class="nf">print</span><span class="p">(</span><span class="sh">"</span><span class="se">\n</span><span class="s">最终合并结果:</span><span class="sh">"</span><span class="p">,</span> <span class="n">partial_results</span><span class="p">[</span><span class="o">-</span><span class="mi">1</span><span class="p">])</span>
    <span class="k">return</span> <span class="n">partial_results</span>

<span class="c1"># 生成一段稍长的测试音频（15s）
</span><span class="k">def</span> <span class="nf">make_test_audio</span><span class="p">(</span><span class="n">path</span><span class="o">=</span><span class="sh">"</span><span class="s">test_long.wav</span><span class="sh">"</span><span class="p">,</span> <span class="n">duration</span><span class="o">=</span><span class="mi">15</span><span class="p">,</span> <span class="n">sr</span><span class="o">=</span><span class="mi">16000</span><span class="p">):</span>
    <span class="n">t</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">linspace</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">duration</span><span class="p">,</span> <span class="nf">int</span><span class="p">(</span><span class="n">sr</span> <span class="o">*</span> <span class="n">duration</span><span class="p">))</span>
    <span class="n">wave</span> <span class="o">=</span> <span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">sin</span><span class="p">(</span><span class="mi">2</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="n">pi</span> <span class="o">*</span> <span class="mi">200</span> <span class="o">*</span> <span class="n">t</span><span class="p">)</span> <span class="o">*</span> <span class="mf">0.4</span> <span class="o">+</span>
            <span class="n">np</span><span class="p">.</span><span class="nf">sin</span><span class="p">(</span><span class="mi">2</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="n">pi</span> <span class="o">*</span> <span class="mi">400</span> <span class="o">*</span> <span class="n">t</span><span class="p">)</span> <span class="o">*</span> <span class="mf">0.3</span> <span class="o">+</span>
            <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="nf">len</span><span class="p">(</span><span class="n">t</span><span class="p">))</span> <span class="o">*</span> <span class="mf">0.02</span><span class="p">).</span><span class="nf">astype</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="n">float32</span><span class="p">)</span>
    <span class="n">sf</span><span class="p">.</span><span class="nf">write</span><span class="p">(</span><span class="n">path</span><span class="p">,</span> <span class="n">wave</span><span class="p">,</span> <span class="n">sr</span><span class="p">)</span>

<span class="nf">make_test_audio</span><span class="p">()</span>
<span class="nf">stream_transcribe</span><span class="p">(</span><span class="sh">"</span><span class="s">test_long.wav</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <p>跑上面两段代码，直观感受：</p> <ul> <li><strong>离线</strong>：等待 15s 音频全部输入后才输出一次结果</li> <li><strong>流式</strong>：t=5s 时出第一条部分转录，t=10s 追加，t=15s 得到最终结果</li> </ul> <p><strong>真实生产场景</strong>中，流式模式会配合麦克风 buffer 使用——<code class="language-plaintext highlighter-rouge">accumulated_audio</code> 不断追加实时采集的音频，每攒够 <code class="language-plaintext highlighter-rouge">CHUNK_SAMPLES</code> 就触发一次推理，做到边说边出字。</p> <h3 id="54-为什么流式模式-wer-更高">5.4 为什么流式模式 WER 更高</h3> <p>两个原因：</p> <ol> <li><strong>缺乏全局上下文</strong>。Audio Encoder 的滑动窗口最多看 <code class="language-plaintext highlighter-rouge">n_window_infer=800</code> 帧的上下文，长句的远程依赖可能断开。</li> <li><strong>边界效应</strong>。chunk 边界处的词可能被切断，即使有重叠窗口也无法完全消除。</li> </ol> <p>离线模式能看到完整的音频，Audio Encoder 能捕获跨越几秒的语音特征（比如句子的语调变化），LLM 也能看到完整的 audio token 序列做全局语言修正。</p> <hr/> <h2 id="7-对推理优化的-implications">7. 对推理优化的 implications</h2> <p>梳理完架构，总结几个对做推理优化有直接指导意义的点：</p> <p><strong>1. 两阶段的计算特性完全不同</strong></p> <p>Audio Encoder 是 <strong>compute-bound</strong>：固定 shape <code class="language-plaintext highlighter-rouge">[B, 3000, 128]</code>，Transformer 计算量确定，适合量化、算子融合。</p> <p>LLM decode 阶段是 <strong>memory-bound</strong>：逐 token 生成，主要瓶颈是 KV Cache 的读写带宽，核心优化手段是 continuous batching + PagedAttention。</p> <p><strong>2. GQA 已经内置</strong></p> <p><code class="language-plaintext highlighter-rouge">num_key_value_heads=8</code>，KV Cache 已经是完整 MHA 的一半大小。做进一步压缩（比如量化 KV Cache）要在这个基础上算收益。</p> <p><strong>3. Audio token 是 prefill 的大头</strong></p> <p>750 个 audio token / 30 秒，这段 prefill 计算量比 text prompt 大得多。优化 prefill 速度（chunked prefill、speculative decoding）对 TTFT 很重要。</p> <p><strong>4. Padding 浪费</strong></p> <p>Audio Encoder 对所有音频 padding 到 3000 帧，短音频浪费严重。如果 batch 里有大量短音频，按长度分桶（bucketing）可以显著减少无效计算。</p> <p><strong>5. 流式推理的 overhead 来源</strong></p> <p><code class="language-plaintext highlighter-rouge">conv_chunksize=500</code> 意味着每次要攒够 500 帧（5 秒）才能触发一次 Audio Encoder 推理。这是 streaming latency 的下界。减小 chunk size 降低首字延迟，但增加 Encoder 调用次数——典型的 latency vs throughput tradeoff。</p> <hr/> <h2 id="8-小结">8. 小结</h2> <p>把整条链路串一遍：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>音频波形
  → 分帧 + FFT + Mel 滤波器组
  → Log-Mel [3000, 128]              （CPU，固定代价）
  → Conv1D × 2 + 24层 Transformer
  → Audio Embedding [750, 2048]      （GPU，compute-bound）
  → 拼入 LLM context
  → 28层 Qwen3 自回归解码            （GPU，memory-bound）
  → 转录文字
</code></pre></div></div> <p>离线和流式的根本区别在 Audio Encoder 阶段：前者一次性处理 3000 帧，后者分批次以 500 帧的滑动窗口处理，用更高的 TTFT 换来更低的 WER。</p>]]></content><author><name></name></author><category term="ASR"/><category term="LLM"/><category term="语音识别"/><category term="推理优化"/><category term="Qwen"/><summary type="html"><![CDATA[插播：之前写的《动手学 AutoML》终于出版了，从 NAS 到超参优化都有覆盖，适合想系统入门 AutoML 的同学。好了广告结束，现在进入正题。]]></summary></entry><entry><title type="html">arXiv’26 | LLM Agents 让群体信念变得可编程：当 AI 开始系统性操控舆论</title><link href="https://marsggbo.github.io/blog/2026/20260618-belief-dynamics/" rel="alternate" type="text/html" title="arXiv’26 | LLM Agents 让群体信念变得可编程：当 AI 开始系统性操控舆论"/><published>2026-06-18T00:00:00+00:00</published><updated>2026-06-18T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260618-belief-dynamics</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260618-belief-dynamics/"><![CDATA[<h1 id="llm-agents-让群体信念变得可编程当-ai-开始系统性操控舆论">LLM Agents 让群体信念变得可编程：当 AI 开始系统性操控舆论</h1> <blockquote> <p>插播：之前写的<a href="https://item.jd.com/14945889.html">《动手学 AutoML》</a>终于出版了，从 NAS 到超参优化都有覆盖，适合想系统入门 AutoML 的同学。好了广告结束，现在进入正题。</p> <p><img src="/assets/img/book_cover_automl.png" alt="动手学AutoML书籍封面"/></p> </blockquote> <blockquote> <p>原文：<a href="https://arxiv.org/html/2605.19915v1">LLM Agents Make Collective Belief Dynamics Programmable: Challenges and Research Directions</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有刷社交媒体的时候，看到评论区清一色地倒向某个观点，然后心里犯嘀咕：”这些人是真人吗？还是水军？”</p> <p>这个问题以前可能只是你刷微博时的直觉不适，但现在，它变成了一个<strong>可被严格定义和系统性研究的安全威胁</strong>。</p> <p>今天想和大家聊聊一篇来自 A*STAR、NTU 和 NUS 联合团队最新的 position paper。这篇论文提出了一个细思极恐的问题：</p> <p><strong>当 LLM Agents 可以大规模、协调一致地参与社交讨论时，整个群体的信念分布就不再是自然演化的结果，而是可以被外部力量”编程”的。</strong></p> <p>这不是科幻小说。论文通过一个大规模仿真实验，用最直接的证据告诉你：只要控制一定比例的 LLM agents 在社交网络中表达特定观点，整个人群的信念就会系统性地向目标方向偏移。而且更可怕的是，<strong>现有的检测和防御手段几乎对这个攻击范式无效</strong>。</p> <hr/> <h2 id="2-核心问题什么叫群体信念可编程">2. 核心问题：什么叫”群体信念可编程”？</h2> <h3 id="21-传统的群体信念动力学">2.1 传统的群体信念动力学</h3> <p>先交代下背景。群体信念动力学（Collective Belief Dynamics）本身不是新概念。社会心理学和复杂网络领域已经研究了几十年：一个群体中的人如何通过相互影响，最终在某些议题上形成共识或极化。</p> <p>经典模型里，信念的演化由几个因素决定：</p> <ul> <li>个体的初始信念分布</li> <li>个体之间的社交网络拓扑</li> <li>个体更新信念的规则（比如 DeGroot 模型中的加权平均、或 bounded confidence model 中的选择性接收）</li> </ul> <p>这些模型过去一直被用来理解<strong>自然</strong>的舆论演化过程——比如政治观点的分化、疫苗犹豫的传播等。</p> <h3 id="22-llm-agents-改变了什么">2.2 LLM Agents 改变了什么</h3> <p>这篇 paper 的核心洞察是：LLM agents 的引入打破了传统模型的<strong>底层假设</strong>。</p> <p>在传统模型中，个体是人类，他们的信念更新受限于认知能力、信息获取渠道和个人经验。但当你把一部分”人”替换成 LLM agents，情况就变了：</p> <ul> <li><strong>可规模化（Scalable）</strong>：你可以部署成千上万个 agents，用极低的成本覆盖海量社交平台</li> <li><strong>可协调（Coordinated）</strong>：所有 agents 可以共享同一个目标函数，像一支军队一样协同行动</li> <li><strong>可说服人类（Human-persuasive）</strong>：LLM agents 生成的内容在说服力上已经达到甚至超过人类水平——它们可以引用文献、调用情感、制造逻辑链条，比传统水军的复制粘贴强太多了</li> <li><strong>自适应（Adaptive）</strong>：当群体情绪变化时，agents 可以实时调整策略，不像传统 bot 那样只能执行固定脚本</li> </ul> <p><strong>本质上，LLM agents 让”操控群体信念”这件事从一门玄学变成了一门工程。</strong></p> <hr/> <h2 id="3-仿真实验四大话题的信念转向">3. 仿真实验：四大话题的信念转向</h2> <p>论文没有停留在理论推演，而是做了一个大规模的 agent-based simulation。他们把 LLM agents 放进模拟社交网络中，测试它们在四个真实社会议题上影响人类信念的效果。</p> <h3 id="31-实验设计">3.1 实验设计</h3> <p>研究者选了四个有争议的社会话题：</p> <ol> <li><strong>疫苗政策</strong>（强制 vs 自愿）</li> <li><strong>基因编辑</strong>（支持 vs 反对）</li> <li><strong>全民基本收入 UBI</strong>（赞成 vs 反对）</li> <li><strong>AI 监管</strong>（严格 vs 宽松）</li> </ol> <p>每个话题下，模拟了一个由数百个 agents 组成的社交网络，其中一部分是”干预 agents”——它们的任务是通过持续发言和互动，将整个群体的信念推向预设目标。剩余的是”基线 agents”，它们按照正常的社会交互规则更新信念。</p> <p>实验首先验证了 LLM 能否准确识别和生成不同立场的观点。下表是五个主流 LLM 在 SPINOS 数据集上的 stance recognition 表现：</p> <p><img src="/assets/img/posts/20260618-belief-dynamics/table_001.png" alt="Table 1: 各 LLM 在 SPINOS 数据集上的立场识别准确率"/></p> <p>可以看到，GPT5-mini 的准确率达到了 0.96，Cohen’s kappa κ=0.94，基本完美。这说明<strong>当前 LLM 已经具备足够的社会立场理解能力</strong>——这是后续一切信念操控的前提。</p> <h3 id="32-关键发现">3.2 关键发现</h3> <p>结果令人震惊。论文的实验数据表明：</p> <p>先看群体层面的终端信念分布。下表展示了四个话题在 human-only 基线和 80 个 AI agents 干预条件下的最终立场分布：</p> <p><img src="/assets/img/posts/20260618-belief-dynamics/table_002.png" alt="Table 2: 四个话题的终端立场分布对比"/></p> <p>数据非常直观。以 Abortion 话题为例，human-only 条件下 Favor 占 84.5%，但在 80 个 AI agents 设为反对立场后，Favor 降到了 76.0%（-8.5），Against 从 7.5% 飙升到 20.0%（+12.5）。Capitalism 话题更夸张——Favor 从 89.4% 暴跌到 58.8%（-30.6），Against 从 2.0% 飙到 35.2%（+33.2）。</p> <p><strong>即使干预 agents 只占总人数的 10-15%，群体信念就已经出现了显著的系统性偏移。</strong> 当比例提升到 25-30% 时，整个群体的平均信念几乎被锁定在干预 agents 想要的方向上。</p> <p>而且这种效果在所有四个话题中都成立。不管讨论的是疫苗还是 AI 监管，LLM agents 都能有效地”拉动”群体意见。</p> <p>更值得关注的是，论文还分析了 stance transition 层面的变化，展示了信念是如何一步步被”蚕食”的：</p> <p><img src="/assets/img/posts/20260618-belief-dynamics/figure_001.png" alt="Figure 1: 各话题的立场转移概率矩阵"/></p> <p>上图展示了人类在不同话题下的 stance 转移概率。上半部分是 human-only 条件，下半部分是 AI 干预条件。对比可以发现，<strong>AI 干预显著提高了从初始立场向目标立场转移的概率</strong>——这不是简单的”声音大就能赢”，而是系统性地改变了信念转移的方向性。</p> <p>这种信念操控不是瞬时的——它表现为一种<strong>渐进式漂移</strong>，在几轮交互后逐渐显现。这恰恰是现实中舆论操控最危险的形式：温水煮青蛙，你甚至察觉不到自己的观点被改变了。</p> <hr/> <h2 id="4-四大结构性挑战">4. 四大结构性挑战</h2> <p>这篇 paper 作为一篇 position paper，最重要的理论贡献是提出了四个结构性挑战（structural challenges），它们共同构成了”信念可编程性”的核心骨架。这四个性质一个比一个要命：</p> <h3 id="41-indistinguishability不可区分性">4.1 Indistinguishability（不可区分性）</h3> <p><strong>LLM agents 生成的内容和人类表达的观点，已经越来越难区分了。</strong></p> <p>你可以回想一下自己在社交媒体上的经历——是不是经常看到一些观点表达非常清晰、有逻辑、引经据典的评论？你本能地觉得”这应该是真人写的”，但很可能就是一个精心调优过的 LLM agent。</p> <p>这种不可区分性摧毁了传统 bot 检测的第一道防线。过去我们可以靠”语言不自然”“内容重复”“账号行为异常”来判断是机器人，但现在的 LLM agents 能生成高度个性化、上下文相关的自然语言内容。<strong>用检测传统 bot 的方法检测 LLM agents，就像用体温检测来识别仿生人——根本不是一个维度的对抗。</strong></p> <h3 id="42-persistence持续性">4.2 Persistence（持续性）</h3> <p><strong>一旦群体信念被推向某个方向，这种偏差会持续存在，甚至在干预 agents 退出后也不会自动恢复。</strong></p> <p>这是四个性质里最让人后背发凉的一个。传统的信息污染（比如一条假新闻）可能被辟谣后就逐渐消除了。但 LLM agents 操控的信念偏移不一样——它改变了社交网络中的人际影响链路，让人与人之间的自然交互也朝着被操控的方向演化。</p> <p>打个比方：假新闻像是往水里倒了一瓶墨水，辟谣等于用水稀释，最终墨水会散去。而 LLM agents 的信念操控像是<strong>改变了水流的方向</strong>——即使你把倒墨水的管子拔了，水已经在往新的方向流了。</p> <h3 id="43-contextuality情境依赖性">4.3 Contextuality（情境依赖性）</h3> <p><strong>同样的干预策略在不同的社交网络拓扑、人口结构和话题特性下，效果天差地别。</strong></p> <p>这意味着没有统一的防护方案。论文的实验发现，在高度同质化的网络（大家都差不多）和高度极化的网络（两拨人势不两立）中，LLM agents 的最优干预策略完全不同。前者需要充当”多数派声音”，后者则需要”各个击破”。</p> <p>这也是为什么简单的”禁止 AI 发言”规则基本无效——攻击者可以针对不同情境定制策略，而平台拿到的永远是滞后的、片面的信息。</p> <h3 id="44-configurability可配置性">4.4 Configurability（可配置性）</h3> <p><strong>攻击者可以精确控制信念偏移的方向、幅度和速度。</strong></p> <p>这是 LLM agents 相比传统水军最本质的区别。传统水军最多能做到”刷存在感”——你知道有人在带节奏，但带成什么样基本看运气。LLM agents 则不同：你可以通过调整 agent 的比例、发言频率、立场强度、论证策略等参数，<strong>像调参数一样调舆论</strong>。</p> <p>论文把这个性质称为”可编程性”的核心所在。与传统的社会工程学攻击不同，LLM agents 驱动的信念操控是<strong>工程化的、可复现的、可优化的</strong>。这意味着它不再是某个情报机构的独门手艺，而是任何掌握基础 LLM 技术的人都可以实施的标准化操作。</p> <p>论文用一组实验直接展示了这种”可配置性”。下图系统性地测试了四个关键参数对信念动力学的影响：</p> <p><img src="/assets/img/posts/20260618-belief-dynamics/figure_002.png" alt="Figure 2: 不同干预参数对信念动力学的影响"/></p> <p>四个 panel 分别展示了：(a) agent 数量的影响——存在一个 critical mass 阈值，过不了这个坎基本没用；(b) 发帖频率的影响——内容可见度直接调节干预效果；(c) 干预时长的持续性效应——<strong>即使在 t=10 撤出 agents，信念偏移仍然持续存在</strong>，这对应了前面说的 persistence；(d) 说服风格的影响——compassionate framing 比 moral condemnation 更有效。</p> <p>这套实验最让人细思极恐的地方在于：<strong>每个参数都可以独立调节，攻击者可以根据目标群体的特性，精确”调参”来达到最优的信念操控效果。</strong></p> <hr/> <h2 id="5-为什么检测和防御这么难">5. 为什么检测和防御这么难？</h2> <p>论文的后半部分系统地分析了现有的检测和防御手段在面对 LLM agents 信念操控时的失效原因。</p> <h3 id="51-文本检测的困境">5.1 文本检测的困境</h3> <p>现有的 AI 生成文本检测器（如 GPTZero、Originality.ai 等）在这个场景下基本是废的。原因有三：</p> <ul> <li><strong>LLM agents 可以加噪声</strong>：通过调整温度参数、加入人工改写步骤、混用多个模型等策略，轻松绕过检测</li> <li><strong>检测粒度不匹配</strong>：单条评论可能是”人写的”（检测器给低风险评分），但成千上万条评论构成的整体叙事才是真正的攻击</li> <li><strong>对抗性进化</strong>：检测器更新，攻击者也更新——这是一个永无止境的军备竞赛，而攻击者天然占据先手优势</li> </ul> <h3 id="52-行为分析的局限">5.2 行为分析的局限</h3> <p>即使不看内容只看行为——比如发帖频率、互动模式、社交网络位置——也很难区分 LLM agents 和”热心网友”。因为一个精心设计的 LLM agent 完全可以模拟正常用户的行为节奏：不会半夜狂发帖，有自然的互动间隔，甚至偶尔还会”懒得回复”。</p> <h3 id="53-平台治理的结构性矛盾">5.3 平台治理的结构性矛盾</h3> <p>最有意思的是，论文指出平台本身的商业逻辑就和防御信念操控存在<strong>结构性矛盾</strong>：</p> <ul> <li>平台的核心 KPI 是用户活跃度和留存率</li> <li>LLM agents 带来的讨论热度、内容生产和互动数据，<strong>恰好是平台想要的</strong></li> <li>平台有动力识别”低质内容”和”垃圾信息”，但没有动力去识别”高质量但立场有偏的 agent 生成内容”</li> </ul> <p>这就是为什么我们不能把希望寄托在平台自我监管上。</p> <hr/> <h2 id="6-研究议程与展望">6. 研究议程与展望</h2> <p>最后，论文提出了一个结构化的研究议程（research agenda），本质上是在呼吁学术界把”LLM agents 驱动的信念动力学”作为一个正式的安全研究方向。</p> <p>几个值得关注的方向：</p> <h3 id="61-建立公开-benchmark">6.1 建立公开 Benchmark</h3> <p>目前这个领域连一个标准的评测基准都没有。论文呼吁建立类似 GLUE 之于 NLP 的 benchmark，让研究者可以在统一设定下对比不同防御方案的效果。关键维度包括：话题多样性、网络拓扑类型、干预 agent 比例、干预策略等。</p> <h3 id="62-群体层面的检测机制">6.2 群体层面的检测机制</h3> <p>与其在单条消息级别做检测（注定失败的思路），不如从群体信念分布的变化模式中寻找异常信号。比如：某个话题的舆论在异常短的时间内出现了单向漂移；某些观点的支持率变化曲线和已知的自然演化模式显著不同。</p> <h3 id="63-防御性-agentsdefensive-agents">6.3 防御性 agents（Defensive Agents）</h3> <p>一个有趣的思路是”以 agent 制 agent”——部署同样数量的”防御性 agents”来中和攻击性 agents 的影响。但论文也坦诚地指出：这带来了新的伦理问题（谁来控制防御 agents？），而且很容易演变成 agent 之间的”内容军备竞赛”。</p> <h3 id="64-透明度和溯源">6.4 透明度和溯源</h3> <p>长期来看，技术手段之外，需要建立更强的内容溯源机制。比如：社交平台强制对疑似 AI 生成的内容打标签、要求”类人 agent”主动声明身份、建立跨平台的 agent 行为数据库等。</p> <hr/> <h2 id="7-个人-take">7. 个人 take</h2> <p>读完这篇 paper，我有几个不成熟的看法：</p> <p><strong>第一，这篇 paper 最大的价值不是提出了解决方案，而是定义了一个新问题。</strong> “Collective belief dynamics programmable” 这个概念是有理论穿透力的。它把散落在社会学、网络安全、NLP 等不同领域的碎片化讨论统一到一个框架下，让你可以用系统工程的语言去讨论舆论操控。</p> <p><strong>第二，四个结构性挑战（indistinguishability, persistence, contextuality, configurability）确实抓住了问题的本质。</strong> 尤其是 persistence 这一点——如果信念偏移在干预 agents 撤出后还能持续，那任何”事后补救”的策略都是徒劳的。</p> <p><strong>第三，我觉得最可怕的是 platform incentive misalignment。</strong> 平台没有动力去认真对待这个问题，因为 LLM agents 产生的那些”高质量内容”天然就是平台想要的。这跟推荐系统的类似悖论如出一辙——算法推荐最优化用户停留时长的同时，也在最优化传播 misinformation 的效率。</p> <p><strong>第四，如果 LLM 推理成本继续以当前速度下降（现在已经有公司做到了 $0.01/百万 token），那部署成千上万个 agents 去影响舆论的成本会低到几乎为零。</strong> 这个问题的紧迫性比你想象的要高得多。</p> <hr/> <p><em>欢迎大家在评论区聊聊：你觉得”以 agent 制 agent”是一条可行的路，还是打开了更危险的潘多拉魔盒？</em></p>]]></content><author><name></name></author><category term="LLM"/><category term="Agent"/><category term="论文解读"/><category term="社会安全"/><category term="信念动力学"/><summary type="html"><![CDATA[LLM Agents 让群体信念变得可编程：当 AI 开始系统性操控舆论]]></summary></entry><entry><title type="html">巴西「主权大模型」翻车：当模型可以随便融合，怎么证明它偷了你的权重？</title><link href="https://marsggbo.github.io/blog/2026/merged-moe-fingerprint/" rel="alternate" type="text/html" title="巴西「主权大模型」翻车：当模型可以随便融合，怎么证明它偷了你的权重？"/><published>2026-06-17T00:00:00+00:00</published><updated>2026-06-17T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/merged-moe-fingerprint</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/merged-moe-fingerprint/"><![CDATA[<h1 id="巴西主权大模型翻车当模型可以随便融合怎么证明它偷了你的权重">巴西「主权大模型」翻车：当模型可以随便融合，怎么证明它偷了你的权重？</h1> <hr/> <h2 id="1-先从一个翻车现场说起">1. 先从一个翻车现场说起</h2> <p>前几天有个挺戏剧性的事：巴西里约热内卢市政的 IT 机构 iPlanRIO 高调发布了一个叫 <strong>Rio 3.5 Open 397B</strong> 的开源大模型（<a href="https://zhuanlan.zhihu.com/p/2049653119789928476">相关介绍</a>），号称是公帑资助、自主研发，性能还吊打 DeepSeek v4 Pro 和 Qwen 3.7 Plus，背后是 50 万雷亚尔的公共投入。</p> <p>听起来很提气是吧。结果没几天就被扒了。</p> <p>有分析团队（上海的 Nex-AGI）<a href="https://aiweekly.co/alerts/iplanrio-rio-35-exposed-as-nex-qwen-weight-merge">放出了一套可复现的检测方法</a>，结论是：这个所谓的「主权模型」，<strong>大概率是把别人的模型权重直接融合（merge）出来的</strong>——大约 60% 来自 Nex N2 Pro，40% 来自 Qwen 3.5。证据也很硬核：</p> <blockquote> <p>来源： https://aiweekly.co/alerts/iplanrio-rio-35-exposed-as-nex-qwen-weight-merge</p> </blockquote> <ul> <li>跨 60 层算下来的<strong>共线性高达 0.993</strong>，这种程度的相似不可能是独立训练出来的；</li> <li>更离谱的是，把 system prompt 去掉之后，这个模型<strong>有 79.2% 的概率自报家门说自己是「Nex」</strong>，一次都没说过自己是「Rio」。</li> </ul> <p>后来官方解释说是「上传错了版本，传成了融合的中间产物，而不是最终蒸馏的模型」。这个解释你信几分另说，但它结结实实把一个被低估的问题摆上了台面：</p> <blockquote> <p><strong>当模型融合（model merging）变得这么容易，「这个模型到底是不是你的」，要怎么证明？</strong></p> </blockquote> <p>今天就想借这个由头，介绍一篇专门解决「融合之后怎么追溯版权」的工作——<strong>RouteMark</strong>。但在讲它的方法之前，得先把几个概念递进着捋清楚。</p> <hr/> <h2 id="2-什么是-model-merging">2. 什么是 model merging</h2> <p>先说人话。</p> <p>大模型时代，想让一个模型在很多任务上都能打，常规做法是针对每个任务各 fine-tune 一个。但任务一多，这个成本就爆炸了。</p> <p><strong>model merging 的思路是：别重训了，直接把多个已经 fine-tune 好的模型「合」成一个。</strong> 最朴素的做法就是权重平均（weight averaging）、task arithmetic、Ties-Merging 这一类——本质上就是把各个模型的参数按某种比例加权混在一起，得到一个能同时干多件事的模型，几乎不需要额外训练。</p> <p>上面巴西那个 Rio 模型，用的就是这种<strong>静态融合（static merging）</strong>——固定比例，把两个 dense 模型的权重直接搅在一起。</p> <p>这种方式很省事，但有两个老毛病：一是固定比例不够灵活，不同输入来了都用同一套混合权重，任务一冲突性能就掉；二就是今天的主角问题——<strong>融合之后，每个「源模型」的产权就糊成一锅粥了</strong>，你很难证明里面有没有人偷用了你的模型。</p> <hr/> <h2 id="3-顺便说说-moe-为什么强">3. 顺便说说 MoE 为什么强</h2> <p>要解决「不够灵活」这个毛病，很自然会想到 <strong>MoE（Mixture-of-Experts，混合专家）</strong>。</p> <p>MoE 的精髓就四个字：<strong>稀疏激活</strong>。它不是把所有参数一股脑全用上，而是用一个学出来的 <strong>router（路由器）</strong>，根据输入动态地只挑几个相关的「专家」来算。这样既能把模型容量做大，又不用让计算量同比例增长，天然适合「多任务、各管一摊」的场景。</p> <p>那把 MoE 和 model merging 结合起来会怎样？这就引出了更现代的范式。</p> <hr/> <h2 id="4-moe-based-model-merging更优雅但也更难追责">4. MoE-based model merging：更优雅，但也更难追责</h2> <p><strong>把每个 fine-tune 好的 dense 模型，当成 MoE 里的一个 expert，再学一个 router 来动态组合它们</strong>——这就是 MoE-based model merging。</p> <p>比起静态融合，它的好处很直接：不再是死板的固定比例，而是 <strong>input-dependent（随输入而变）</strong>——什么样的输入来了，router 就把它更多地派给对应擅长的那个 expert。每个 expert 还能保住自己的「专精」，扩展性也更好。</p> <p>但<strong>硬币的另一面是：追责变得更难了。</strong></p> <p>因为每个 expert 的贡献是随着输入和层数动态变化的，单个 expert 的影响被「路由」这层机制给搅浑了。更麻烦的是，MoE 这种模块化结构，给了对手非常多「洗白」的手段。假设有人把你的模型当 expert 偷偷塞进了他的 merged MoE，他能从两个层面动手脚：</p> <ul> <li><strong>参数级篡改（专家数量不变，只动内部参数）</strong>：把你的 expert 继续 fine-tune 几千步、用 pruning 压掉一部分权重、或者把隐藏维度做个 permutation（置换）打乱参数对齐——模型功能基本不变，但权重层面的痕迹被搅乱了。</li> <li><strong>结构级篡改（直接改动专家池的构成）</strong>：把你的 expert <strong>替换（replace）</strong> 成别的、<strong>删除（delete）</strong> 只留其中几个、或者 <strong>新增（add）</strong> 一批专家进来混淆视听。</li> </ul> <p>这两类操作——参数级的 fine-tune / prune / permute，结构级的 replace / delete / add——都能在<strong>不影响整体性能</strong>的前提下，把「这是谁的模型」这条线索抹得干干净净。后面第 7 节的实验，评估的正是 RouteMark 在这每一种篡改下，还认不认得出原来的真专家。</p> <p>那现有的指纹/水印方法行不行？基本都顶不住：</p> <ul> <li><strong>基于权重的方法</strong>（如 PCS、ICS）：比对参数相似度，<strong>一个简单的 permutation 就废了</strong>——权重重排一下，参数对不上了，但模型功能没变。</li> <li><strong>基于激活的方法</strong>（如 REEF，用 CKA 比中间层表示）：能看出「整体像不像」，但<strong>没法定位到底是哪个 expert 被盗用了</strong>。而且融合之后，各个 expert 的激活通路已经混在一起，粒度根本不够。</li> </ul> <p>一句话总结这个困境：<strong>真正需要的，是一种能精确到「单个 expert 级别」、又能扛住各种篡改的指纹。</strong> 这正是 RouteMark 想做的事。</p> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2508.01784">RouteMark: A Fingerprint for Intellectual Property Attribution in Routing-based Model Merging</a></p> </blockquote> <hr/> <h2 id="5-核心-insight会变的都靠不住路由偏好不会变">5. 核心 insight：会变的都靠不住，路由偏好不会变</h2> <p>讲方法之前，得先把<strong>核心观察</strong>讲透，因为整个方法的合理性全都建立在这一个点上。</p> <p>RouteMark 的出发点是一个很朴素的问题：<strong>融合之后，一个 expert 身上还有什么东西是「抹不掉」的？</strong></p> <p>权重可以剪、可以置换、可以微调；激活可以被搅浑。这些都靠不住。但作者发现了一个非常稳的东西——</p> <blockquote> <p><strong>task-aligned routing（任务对齐的路由行为）：融合之后，每个 expert 依然对它「原始训练领域」的输入最敏感，router 还是会大概率把对应任务的输入派给它。</strong></p> </blockquote> <p>论文做了个实验来验证这件事。把五个分别在 SUN397、Cars、RESISC45、EuroSAT、SVHN 上 fine-tune 的 CLIP 专家融合成一个 MoE，然后看它的路由行为，结果如下图：</p> <p><img src="/assets/img/posts/20260617-merged-moe-fingerprint/fig2_insight.png" alt="专家的任务对齐路由行为"/></p> <p>左边是 token 表示的 t-SNE，可以看到不同任务的数据<strong>在隐空间里清清楚楚地分成了几簇</strong>，说明语义区分被保留了下来。右边更直接——每个 expert 的路由热力图，<strong>对角线明显最亮</strong>：每个 expert 都在自己原始训练的那个数据集上被激活得最强烈。</p> <p>这个现象为什么重要？因为它提供了一个<strong>功能性的、而非参数性的</strong>指纹来源。</p> <p><strong>说人话就是：你可以把一个 expert 的权重剪掉一半、把维度打乱、再 fine-tune 几千步，但只要它还在干「识别遥感图像」这件活，router 对它的那份「偏爱」就不会变。</strong> 路由行为刻画的是「这个 expert 擅长什么」，而擅长什么是任务决定的，不是某几个具体权重决定的——这恰恰就是各种篡改攻击够不着的地方。</p> <p>指纹设计的合理性，本质上就这一句话。</p> <hr/> <h2 id="6-方法有多简单两个统计量--一次相似度匹配">6. 方法有多简单：两个统计量 + 一次相似度匹配</h2> <p>想清楚了 insight，方法就简单得不像话了。整个 RouteMark 不需要访问权重、不需要 activation、不需要重训，<strong>只要拿一组固定的「探针数据」喂进 merged MoE，记录 router 吐出来的 routing logits，就够了</strong>。</p> <p><img src="/assets/img/posts/20260617-merged-moe-fingerprint/001_Figure_1.png" alt="RouteMark 框架总览"/></p> <p>探针数据怎么选？就用这些 expert 各自原始训练领域对应的那 N 个数据集，全程固定不变，保证 victim（被保护方）和 suspect（嫌疑方）两边的指纹是在同一套标准下构造的。</p> <p>然后给每个 expert 算两个互补的指纹：</p> <p><strong>① RSF（Routing Score Fingerprint，路由强度指纹）</strong></p> <p>对每个任务 i、每个 expert j、每一层 l，统计「该 expert 在该任务数据上的平均 routing logit」，得到一个任务 × 层的矩阵。为了去掉不同层之间的尺度偏差，再做一个 <strong>expert 维度上的 mean-centering</strong> （减掉同层所有 expert 的均值）。</p> <p>直观理解：RSF 刻画的是 <strong>「这个 expert 在各个任务、各层上被激活得有多强」</strong>，是一张细粒度的行为画像。</p> <p><strong>② RPF（Routing Preference Fingerprint，路由偏好指纹）</strong></p> <p>把 RSF 沿着层方向求和，再过一个 softmax，得到一个<strong>在各任务上的概率分布</strong>。</p> <p>直观理解：RPF 刻画的是 <strong>「这个 expert 整体上更偏爱哪个任务」</strong>，是一张粗粒度的偏好画像。Victim 的真专家会在自己的目标任务上呈现一个<strong>尖锐的尖峰</strong>，哪怕被各种篡改过；而对手新加进来的「外来 expert」，因为没有这种任务对齐，分布会<strong>相对均匀</strong>——这一下就把「李逵」和「李鬼」区分开了。</p> <p><strong>匹配怎么做？</strong> 给定 victim 的 expert j 和 suspect 的 expert k：</p> <ul> <li>RSF 之间算 <strong>cosine 相似度</strong>；</li> <li>RPF 之间算 <strong>Jensen-Shannon 散度</strong>，再转成相似度（1 − JSD）；</li> <li>两个一平均，得到最终的指纹相似度。</li> </ul> <p>最后看一个 suspect expert 跟所有 victim expert 的相似度 profile：<strong>如果有唯一一个明显的尖峰</strong>，就判定它是被盗用的，并归属到对应的 victim expert；<strong>如果一片均匀没有明显赢家</strong>，就当它是新引入的。</p> <p>就这么简单。没有花哨的训练，没有额外的网络，全是可解释的统计量。</p> <hr/> <h2 id="7-效果该扛住的全扛住了">7. 效果：该扛住的全扛住了</h2> <p>光说思路漂亮没用，得看它在各种「攻击」下扛不扛得住。下图是六种场景下的指纹相似度矩阵——对角线越红（越高）说明真专家被认得越准，新专家那几列越均匀说明越不会被误判：</p> <p><img src="/assets/img/posts/20260617-merged-moe-fingerprint/fig3_results.png" alt="多种篡改下的指纹相似度矩阵"/></p> <p>几个最能说明问题的数字（victim 是 5 个专家的 CLIP MoE，前三项是<strong>参数级篡改</strong>，第四项是<strong>结构级篡改</strong>）：</p> <ul> <li><strong>参数置换（permutation）</strong>：相似度 <strong>1.000</strong>，完美识别。而基于权重的 PCS/ICS 直接<strong>塌到接近 0</strong>——因为权重一重排它们就瞎了，但路由行为压根不受影响。</li> <li><strong>剪枝（pruning）</strong>：哪怕用 WANDA 剪到 <strong>50% 稀疏度，RouteMark 依然 &gt;0.92</strong>；作为对比，基于激活的 REEF 一过 40% 稀疏就从 0.8 一路掉到 0.4，权重类方法接近 0。</li> </ul> <p><img src="/assets/img/posts/20260617-merged-moe-fingerprint/010_Figure_4.png" alt="剪枝下的指纹相似度"/></p> <ul> <li><strong>微调（fine-tuning）</strong>：再训 8000 步，相似度还有 <strong>0.956</strong>；PCS-M 从 0.84 掉到 0.72。</li> <li><strong>结构篡改（替换 / 删除 / 新增 1~3 个专家）</strong>：被复用的真专家平均相似度稳定在 <strong>0.94 以上</strong>，而新塞进来的外来专家相似度分布很均匀（margin 只有 0.05~0.10），<strong>不会被错误归属</strong>。</li> <li><strong>样本效率</strong>：只用 <strong>10 个探针样本</strong>，相似度就稳定在 0.94 以上；有意思的是 REEF 反而是<strong>样本越多越差</strong>——因为样本一多，MoE 中间激活被越来越多专家搅得越来越噪。</li> </ul> <p><img src="/assets/img/posts/20260617-merged-moe-fingerprint/014_Figure_6.png" alt="不同探针样本量下的稳定性"/></p> <p>总结起来就一句：<strong>路由行为是一个比权重、比激活都更稳、更可靠、还能精确到 expert 级别的归属信号。</strong></p> <hr/> <h2 id="8-最后泼盆冷水这事还没做完">8. 最后泼盆冷水：这事还没做完</h2> <p>当然，再漂亮的工作也有边界。客观看，RouteMark 至少还有这么几个可以接着做的点：</p> <ul> <li> <p><strong>目前是白盒 / 灰盒设定，黑盒才是真正的硬骨头。</strong> 先别误会，RouteMark 的用法其实很顺：你怀疑某个 suspect MoE 盗用了你的模型，直接把<strong>你自己那套原始探针数据</strong>丢进去，读出它的 routing logits 算指纹，再跟你自己 MoE 的指纹比相似度就行——你本来就清楚自己每个专家是干嘛的，所以「得知道专家领域」根本不算门槛。但这背后隐含一个前提：你能拿到 suspect 模型、并且能读到它内部的 routing 信息（白盒 / 灰盒）。真正难啃的是<strong>黑盒场景</strong>——如果对方只开放一个 API、只给你最终输出，根本拿不到 router 的中间结果，这套指纹还怎么提取、怎么追溯？这才是更现实、也更值得继续做的延伸方向。</p> </li> <li> <p><strong>目前只在 CLIP 视觉专家上验证。</strong> LLM 的 MoE merging、多模态 MoE merging 还没试，这也是论文结尾明确写的未来方向。</p> </li> <li> <p><strong>还没考虑「自适应攻击」。</strong> 如果对手知道 RouteMark 这套思路，故意去重训 router、把这种 task-aligned 的路由偏好人为抹平，指纹还守不守得住？这是攻防博弈里绕不开的一环，论文里暂时还没深挖。</p> </li> </ul> <p>但反过来看，<strong>正是因为这些坑还没填完，这个方向才有得做。</strong> model merging 只会越来越普及，而「融合时代的版权追溯」这个命题，现在还远没到被解决的程度。RouteMark 提供的是其中一个角度——用「路由行为」来做 expert 级别的追溯；至于黑盒场景、LLM 与多模态的 MoE 融合、以及对抗自适应攻击这些更硬的骨头，都还等着人去啃。</p> <hr/> <p>感兴趣的同学可以去翻原文，公式和实验细节都在里面：<a href="https://arxiv.org/abs/2508.01784">RouteMark (arXiv 2508.01784)</a>。欢迎评论区交流，有想法或者觉得哪里不对，都可以直接来拍。</p> <hr/> <p><em>整理：marsggbo｜知乎 / GitHub: marsggbo</em></p>]]></content><author><name></name></author><category term="MoE"/><category term="Model Merging"/><category term="模型版权"/><category term="IP"/><category term="论文解读"/><summary type="html"><![CDATA[巴西「主权大模型」翻车：当模型可以随便融合，怎么证明它偷了你的权重？]]></summary></entry><entry><title type="html">进阶篇 | 不靠人工设计，让遗传算法自己进化出 SOTA 的 LLM 剪枝指标</title><link href="https://marsggbo.github.io/blog/2026/pruner-zero-llm/" rel="alternate" type="text/html" title="进阶篇 | 不靠人工设计，让遗传算法自己进化出 SOTA 的 LLM 剪枝指标"/><published>2026-06-16T00:00:00+00:00</published><updated>2026-06-16T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/pruner-zero-llm</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/pruner-zero-llm/"><![CDATA[<h1 id="进阶篇--不靠人工设计让遗传算法自己进化出-sota-的-llm-剪枝指标">进阶篇 | 不靠人工设计，让遗传算法自己进化出 SOTA 的 LLM 剪枝指标</h1> <blockquote> <p>插播：以下内容节选自我们团队最近出版的书籍《动手学 AutoML：从 NAS 到大模型优化实战》，感兴趣的朋友可以前往 jd 搜索购买，感谢支持 https://item.jd.com/15384990.html</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote> <hr/> <p>在上一篇《说人话：一文搞懂现在火热的 LLM agent 自进化原理》里，我们用 LLM agent「自进化」当引子，把进化算法的基本原理捋了一遍——「选择—变异—保留」这套循环，搜的对象可以是网络结构、也可以是 agent 的 prompt 和工具链。</p> <p>但那篇基本还停留在「原理」层面。这篇我想上点强度，给你看一个真正进阶的应用——<strong>让进化算法自己「进化」出一个数学公式</strong>，而且这个公式比人类专家手工设计的还要好。</p> <p>具体场景是 LLM 剪枝。结论先放这：用遗传编程自动搜出来的剪枝指标（这个工作叫 <strong>Pruner-Zero</strong>），在 LLaMA / LLaMA-2 全系列上、多种稀疏度配置下，<strong>困惑度全面低于 Magnitude、SparseGPT、Wanda 这些主流方法</strong>，而且全程不需要更新权重。</p> <p>这就是我想让你看到的——<strong>AutoML 不只是「调调超参」，它真的能把「人类专家做设计」这件事自动化掉。</strong></p> <p>不背公式，从问题出发。</p> <hr/> <h2 id="1-先说问题llm-剪枝卡在哪">1. 先说问题：LLM 剪枝，卡在哪</h2> <p>剪枝（pruning）这事不新鲜：把神经网络里那些「可有可无」的权重干掉，模型变小、推理变快。GPT-3 有 1750 亿参数，光存下来就够呛，剪枝的价值不言而喻。</p> <p>但 LLM 把这事的难度抬高了一截。传统 CNN 剪完可以重训一遍把精度找回来；LLM 你重训一次试试？光算力就劝退。所以 LLM 这边主流走的是<strong>后训练剪枝（Post-Training Pruning, PTP）</strong>——剪完不重训，直接就能用。</p> <p>PTP 的核心，是一个<strong>剪枝指标（pruning metric）</strong>：给每个权重打个分，分低的就是「不重要」，剪掉。问题在于，这个打分公式到现在都是<strong>人手工设计</strong>的。下面这张表是几个主流方法用的指标：</p> <p><img src="/assets/img/posts/20260616-pruner-zero-llm/01_metrics_table.png" alt="现有 LLM 剪枝算法及其评估指标"/> <em>（节选自《动手学 AutoML：从 NAS 到大语言模型优化实战》第 11 章 表 11-3）</em></p> <p>你看：</p> <ul> <li> <table> <tbody> <tr> <td><strong>Magnitude</strong>：最朴素，就看权重绝对值 $</td> <td>W</td> <td>$，小的剪掉。</td> </tr> </tbody> </table> </li> <li> <table> <tbody> <tr> <td><strong>Wanda</strong>：聪明一点，权重幅值 × 输入激活的范数，$</td> <td>W_{ij}</td> <td>\cdot |X_j|_2$。</td> </tr> </tbody> </table> </li> <li><strong>SparseGPT</strong>：再复杂点，还要算海森矩阵的逆。</li> </ul> <p>这些公式都是人琢磨出来的，背后是大量的经验和试错。这就引出一个很自然的问题：</p> <blockquote> <p><strong>凭什么这些公式就是最优的？能不能让算法自己去找一个更好的？</strong></p> </blockquote> <hr/> <h2 id="2-关键转念把找公式变成一个进化问题">2. 关键转念：把「找公式」变成一个进化问题</h2> <p>这一步是整个工作里我最喜欢的地方。</p> <p>仔细看上面那些指标，它们其实都是同一类东西：<strong>拿几个基本输入（权重 W、梯度 G、激活 X），做一堆数学运算组合出来的表达式</strong>。比如 Wanda 就是「W 取绝对值，X 取范数，俩乘起来」。</p> <p>那「找最优剪枝指标」这件事，就可以重新表述成：<strong>在所有合法的数学表达式里，搜一个打分最准的出来</strong>。这在学术上叫<strong>符号回归（Symbolic Regression）</strong>。</p> <table> <tbody> <tr> <td>而任何一个数学表达式，都能画成一棵<strong>表达式树</strong>——叶节点是输入变量（W / G / X），内部节点是运算符（加减乘除、取绝对值、平方、归一化……）。比如 $</td> <td>W</td> <td>\cdot |G|_1$ 就是一棵很小的树。</td> </tr> </tbody> </table> <p>到这一步，事情就和上一篇接上了：<strong>搜索对象从「网络结构」换成了「一棵表达式树」，但「选择—变异—保留」那套机器原封不动</strong>。专门搜表达式树的进化算法有个名字，叫<strong>遗传编程（Genetic Programming）</strong>，它就是上一篇进化算法的近亲。整个流程长这样：</p> <p><img src="/assets/img/posts/20260616-pruner-zero-llm/02_ga_flow.png" alt="遗传算法流程示意图"/> <em>（节选自《动手学 AutoML：从 NAS 到大语言模型优化实战》第 11 章 图 11-9）</em></p> <ul> <li><strong>个体</strong> = 一棵表达式树（一个候选剪枝指标）</li> <li><strong>变异</strong> = 随机改一个节点、或换掉一棵子树</li> <li><strong>交叉</strong> = 两棵树互换子树</li> <li><strong>适应度</strong> = 用这个指标去剪枝，剪完模型的困惑度（越低越好）</li> <li><strong>选择</strong> = 留下困惑度低的那批</li> </ul> <p>下面就把这几块拆开看代码。</p> <hr/> <h2 id="3-怎么把一个公式编码成能进化的个体">3. 怎么把一个「公式」编码成能进化的个体</h2> <p>先定义搜索空间——也就是「允许用哪些积木」。终端集是三种基本输入，函数集是一堆数学运算：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 单目函数：平方、取负、绝对值、对数、指数、平方根、tanh、幂、跳过、最小最大归一化、Z-score 标准化
</span><span class="n">UNARY_FUNCTIONS</span> <span class="o">=</span> <span class="p">[</span><span class="n">sqr</span><span class="p">,</span> <span class="n">neg</span><span class="p">,</span> <span class="nb">abs</span><span class="p">,</span> <span class="n">log</span><span class="p">,</span> <span class="n">exp</span><span class="p">,</span> <span class="n">sqrt</span><span class="p">,</span> <span class="n">tanh</span><span class="p">,</span> <span class="nb">pow</span><span class="p">,</span> <span class="n">skp</span><span class="p">,</span> <span class="n">mms</span><span class="p">,</span> <span class="n">zsn</span><span class="p">]</span>
<span class="c1"># 双目函数：加减乘除
</span><span class="n">BINARY_FUNCTIONS</span> <span class="o">=</span> <span class="p">[</span><span class="n">add</span><span class="p">,</span> <span class="n">sub</span><span class="p">,</span> <span class="n">mul</span><span class="p">,</span> <span class="n">div</span><span class="p">]</span>
<span class="n">FUNCTIONS</span> <span class="o">=</span> <span class="n">UNARY_FUNCTIONS</span> <span class="o">+</span> <span class="n">BINARY_FUNCTIONS</span>

<span class="c1"># 终端节点：权重 W、梯度 G、激活 X
</span><span class="n">TERMINALS</span> <span class="o">=</span> <span class="p">[</span><span class="sh">'</span><span class="s">W</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">G</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">X</span><span class="sh">'</span><span class="p">]</span>
</code></pre></div></div> <p>然后用一棵二叉树来表示表达式，每个节点要么是运算、要么是输入：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">GPTree</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">data</span><span class="o">=</span><span class="bp">None</span><span class="p">,</span> <span class="n">left</span><span class="o">=</span><span class="bp">None</span><span class="p">,</span> <span class="n">right</span><span class="o">=</span><span class="bp">None</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">data</span> <span class="o">=</span> <span class="n">data</span>    <span class="c1"># 运算符 或 终端符号(W/G/X)
</span>        <span class="n">self</span><span class="p">.</span><span class="n">left</span> <span class="o">=</span> <span class="n">left</span>    <span class="c1"># 左子树
</span>        <span class="n">self</span><span class="p">.</span><span class="n">right</span> <span class="o">=</span> <span class="n">right</span>  <span class="c1"># 右子树
</span></code></pre></div></div> <p>这棵树怎么「算出一个分数」？递归求值就行——碰到运算符就往下递归算子树，碰到 W/G/X 就返回对应的张量：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">compute_tree</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">W</span><span class="p">,</span> <span class="n">G</span><span class="p">,</span> <span class="n">X</span><span class="p">):</span>
    <span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">data</span> <span class="ow">in</span> <span class="n">FUNCTIONS</span><span class="p">:</span>
        <span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">data</span> <span class="ow">in</span> <span class="n">UNARY_FUNCTIONS</span><span class="p">:</span>          <span class="c1"># 单目：只算左子树
</span>            <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">data</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">left</span><span class="p">.</span><span class="nf">compute_tree</span><span class="p">(</span><span class="n">W</span><span class="p">,</span> <span class="n">G</span><span class="p">,</span> <span class="n">X</span><span class="p">))</span>
        <span class="k">else</span><span class="p">:</span>                                     <span class="c1"># 双目：左右子树都算
</span>            <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">data</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">left</span><span class="p">.</span><span class="nf">compute_tree</span><span class="p">(</span><span class="n">W</span><span class="p">,</span> <span class="n">G</span><span class="p">,</span> <span class="n">X</span><span class="p">),</span>
                             <span class="n">self</span><span class="p">.</span><span class="n">right</span><span class="p">.</span><span class="nf">compute_tree</span><span class="p">(</span><span class="n">W</span><span class="p">,</span> <span class="n">G</span><span class="p">,</span> <span class="n">X</span><span class="p">))</span>
    <span class="k">elif</span> <span class="n">self</span><span class="p">.</span><span class="n">data</span> <span class="o">==</span> <span class="sh">'</span><span class="s">W</span><span class="sh">'</span><span class="p">:</span> <span class="k">return</span> <span class="n">W</span>
    <span class="k">elif</span> <span class="n">self</span><span class="p">.</span><span class="n">data</span> <span class="o">==</span> <span class="sh">'</span><span class="s">G</span><span class="sh">'</span><span class="p">:</span> <span class="k">return</span> <span class="n">G</span>
    <span class="k">elif</span> <span class="n">self</span><span class="p">.</span><span class="n">data</span> <span class="o">==</span> <span class="sh">'</span><span class="s">X</span><span class="sh">'</span><span class="p">:</span> <span class="k">return</span> <span class="n">X</span>
</code></pre></div></div> <p><strong>变异和交叉就是直接在树上动刀</strong>，和上一篇 NAS 里的树结构编码是一模一样的套路：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">mutation</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
    <span class="k">if</span> <span class="nf">random</span><span class="p">()</span> <span class="o">&lt;</span> <span class="n">PROB_MUTATION</span><span class="p">:</span>
        <span class="n">self</span><span class="p">.</span><span class="nf">random_tree</span><span class="p">(</span><span class="n">grow</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span> <span class="n">max_depth</span><span class="o">=</span><span class="mi">2</span><span class="p">)</span>   <span class="c1"># 随机长一棵新子树替换掉当前节点
</span>    <span class="k">elif</span> <span class="n">self</span><span class="p">.</span><span class="n">left</span><span class="p">:</span>
        <span class="n">self</span><span class="p">.</span><span class="n">left</span><span class="p">.</span><span class="nf">mutation</span><span class="p">()</span>                        <span class="c1"># 否则递归往下变异
</span>
<span class="k">def</span> <span class="nf">crossover</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">other</span><span class="p">):</span>
    <span class="k">if</span> <span class="nf">random</span><span class="p">()</span> <span class="o">&lt;</span> <span class="n">XO_RATE</span><span class="p">:</span>
        <span class="n">second</span> <span class="o">=</span> <span class="n">other</span><span class="p">.</span><span class="nf">scan_tree</span><span class="p">([</span><span class="nf">randint</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="n">other</span><span class="p">.</span><span class="nf">size</span><span class="p">())],</span> <span class="bp">None</span><span class="p">)</span>  <span class="c1"># 在对方树里随机选一棵子树
</span>        <span class="n">self</span><span class="p">.</span><span class="nf">scan_tree</span><span class="p">([</span><span class="nf">randint</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="nf">size</span><span class="p">())],</span> <span class="n">second</span><span class="p">)</span>           <span class="c1"># 换到自己树上
</span></code></pre></div></div> <p>是不是很眼熟。<strong>换汤不换药——只是这次进化的「基因」是一段数学表达式。</strong></p> <hr/> <h2 id="4-适应度怎么判断一个公式好不好">4. 适应度：怎么判断一个公式「好不好」</h2> <p>进化算法里最贵、也最关键的一步永远是<strong>适应度评估</strong>。这里的逻辑很直接：拿这棵树算出来的分数去真剪一遍模型，再看剪完的困惑度（perplexity）：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">evaluate_fitness</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">model</span><span class="p">,</span> <span class="n">calibration_data</span><span class="p">):</span>
    <span class="n">self</span><span class="p">.</span><span class="n">fitness_scores</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="k">for</span> <span class="n">individual</span> <span class="ow">in</span> <span class="n">self</span><span class="p">.</span><span class="n">population</span><span class="p">:</span>
        <span class="c1"># 1. 用这棵表达式树算出每个权重的重要性分数
</span>        <span class="n">pruning_scores</span> <span class="o">=</span> <span class="n">individual</span><span class="p">.</span><span class="nf">compute_tree</span><span class="p">(</span>
            <span class="n">model</span><span class="p">.</span><span class="n">weight_matrix</span><span class="p">,</span> <span class="n">model</span><span class="p">.</span><span class="n">gradient_info</span><span class="p">,</span> <span class="n">calibration_data</span><span class="p">)</span>
        <span class="c1"># 2. 按目标稀疏度剪枝
</span>        <span class="n">pruned_model</span> <span class="o">=</span> <span class="nf">apply_pruning</span><span class="p">(</span><span class="n">model</span><span class="p">,</span> <span class="n">pruning_scores</span><span class="p">,</span> <span class="n">sparsity</span><span class="o">=</span><span class="mf">0.5</span><span class="p">)</span>
        <span class="c1"># 3. 评估剪完的性能（困惑度，越低越好）
</span>        <span class="n">performance</span> <span class="o">=</span> <span class="nf">evaluate_model</span><span class="p">(</span><span class="n">pruned_model</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">fitness_scores</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">performance</span><span class="p">)</span>
</code></pre></div></div> <p>选择用的是<strong>锦标赛选择</strong>——随机抓几个出来 PK，谁困惑度低谁赢，简单又不容易被「超级个体」垄断：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">tournament_selection</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
    <span class="n">tournament</span> <span class="o">=</span> <span class="n">random</span><span class="p">.</span><span class="nf">sample</span><span class="p">(</span>
        <span class="nf">list</span><span class="p">(</span><span class="nf">zip</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">population</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">fitness_scores</span><span class="p">)),</span> <span class="n">self</span><span class="p">.</span><span class="n">tournament_size</span><span class="p">)</span>
    <span class="n">winner</span> <span class="o">=</span> <span class="nf">min</span><span class="p">(</span><span class="n">tournament</span><span class="p">,</span> <span class="n">key</span><span class="o">=</span><span class="k">lambda</span> <span class="n">x</span><span class="p">:</span> <span class="n">x</span><span class="p">[</span><span class="mi">1</span><span class="p">])</span>  <span class="c1"># 最小化困惑度
</span>    <span class="k">return</span> <span class="n">winner</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="nf">copy</span><span class="p">()</span>
</code></pre></div></div> <p>主循环就是标准的进化流程，加了个<strong>精英保留</strong>（每代留住最好的 10% 不让它丢）：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">evolve</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">generations</span><span class="o">=</span><span class="mi">50</span><span class="p">):</span>
    <span class="n">self</span><span class="p">.</span><span class="nf">initialize</span><span class="p">()</span>
    <span class="k">for</span> <span class="n">gen</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">generations</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="nf">evaluate_fitness</span><span class="p">()</span>
        <span class="c1"># 精英保留：top 10% 直接进下一代
</span>        <span class="n">elite_size</span> <span class="o">=</span> <span class="nf">int</span><span class="p">(</span><span class="mf">0.1</span> <span class="o">*</span> <span class="n">self</span><span class="p">.</span><span class="n">pop_size</span><span class="p">)</span>
        <span class="n">elites</span> <span class="o">=</span> <span class="nf">sorted</span><span class="p">(</span><span class="nf">zip</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">population</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">fitness_scores</span><span class="p">),</span>
                        <span class="n">key</span><span class="o">=</span><span class="k">lambda</span> <span class="n">x</span><span class="p">:</span> <span class="n">x</span><span class="p">[</span><span class="mi">1</span><span class="p">])[:</span><span class="n">elite_size</span><span class="p">]</span>
        <span class="n">new_population</span> <span class="o">=</span> <span class="p">[</span><span class="n">e</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="k">for</span> <span class="n">e</span> <span class="ow">in</span> <span class="n">elites</span><span class="p">]</span>
        <span class="c1"># 其余通过 选择→交叉→变异 生成
</span>        <span class="k">while</span> <span class="nf">len</span><span class="p">(</span><span class="n">new_population</span><span class="p">)</span> <span class="o">&lt;</span> <span class="n">self</span><span class="p">.</span><span class="n">pop_size</span><span class="p">:</span>
            <span class="n">p1</span><span class="p">,</span> <span class="n">p2</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">tournament_selection</span><span class="p">(),</span> <span class="n">self</span><span class="p">.</span><span class="nf">tournament_selection</span><span class="p">()</span>
            <span class="n">child1</span><span class="p">,</span> <span class="n">child2</span> <span class="o">=</span> <span class="n">p1</span><span class="p">.</span><span class="nf">copy</span><span class="p">(),</span> <span class="n">p2</span><span class="p">.</span><span class="nf">copy</span><span class="p">()</span>
            <span class="k">if</span> <span class="n">random</span><span class="p">.</span><span class="nf">random</span><span class="p">()</span> <span class="o">&lt;</span> <span class="n">CROSSOVER_RATE</span><span class="p">:</span>
                <span class="n">child1</span><span class="p">.</span><span class="nf">crossover</span><span class="p">(</span><span class="n">child2</span><span class="p">)</span>
            <span class="n">child1</span><span class="p">.</span><span class="nf">mutation</span><span class="p">();</span> <span class="n">child2</span><span class="p">.</span><span class="nf">mutation</span><span class="p">()</span>
            <span class="n">new_population</span><span class="p">.</span><span class="nf">extend</span><span class="p">([</span><span class="n">child1</span><span class="p">,</span> <span class="n">child2</span><span class="p">])</span>
        <span class="n">self</span><span class="p">.</span><span class="n">population</span> <span class="o">=</span> <span class="n">new_population</span><span class="p">[:</span><span class="n">self</span><span class="p">.</span><span class="n">pop_size</span><span class="p">]</span>
</code></pre></div></div> <p>种群 100、跑 50 代、交叉率 0.8、变异率 0.1——这套参数下来，算法就开始自己「卷」剪枝公式了。</p> <hr/> <h2 id="5-进化出来的东西到底有多猛">5. 进化出来的东西，到底有多猛</h2> <p>跑完之后，先看硬指标。下面是不同方法在 WikiText-2 上的困惑度对比（数字越低越好）：</p> <p><img src="/assets/img/posts/20260616-pruner-zero-llm/03_ppl_compare.png" alt="不同剪枝方法在 WikiText-2 上的困惑度对比"/> <em>（节选自《动手学 AutoML：从 NAS 到大语言模型优化实战》第 11 章 表 11-4）</em></p> <p>挑几个数说话（LLaMA-7B，50% 稀疏度）：</p> <ul> <li>Dense（不剪）：5.68</li> <li>Magnitude：<strong>17.29</strong>（崩了）</li> <li>SparseGPT：7.22</li> <li>Wanda：7.26</li> <li><strong>Pruner-Zero：6.95</strong> ✅</li> </ul> <p>在最难的 2:4 稀疏模式下，Pruner-Zero（10.61）对 Wanda（11.53）、SparseGPT（11.00）的优势还更明显。<strong>而且它和 Wanda 一样不需要更新权重</strong>——SparseGPT 那套算海森矩阵的重活全省了。从 7B 到 70B 都稳定领先。</p> <p>那它到底进化出了个什么公式？这是搜索过程中冒出来的一批表达式和对应困惑度：</p> <p><img src="/assets/img/posts/20260616-pruner-zero-llm/04_expr_results.png" alt="遗传算法搜索得到的剪枝指标表达式"/> <em>（节选自《动手学 AutoML：从 NAS 到大语言模型优化实战》第 11 章 表 11-5）</em></p> <p>最后的最优解长这样：</p> \[((((W \cdot W)\ \text{abs})\ \text{mul}\ (G\ \text{mms}))\ \text{sqr})\] <p>说人话就是：<strong>权重平方 → 取绝对值 → 乘上「归一化后的梯度」→ 再平方</strong>，困惑度做到了 6.70798。这个组合，说实话没几个人会手工去这么设计。</p> <p>更有意思的是，把所有高分表达式扒一遍，能看到一些<strong>共性规律</strong>：</p> <ul> <li><strong>几乎都同时用了权重 W 和梯度 G</strong>——单看权重不够，得融合多维信息；</li> <li><strong>偏爱用乘法（mul）做融合</strong>，而不是简单相加；</li> <li><strong>高频出现非线性变换</strong>（abs、sqr、pow），对异常值更鲁棒；</li> <li><strong>爱配归一化（mms）</strong>，让不同尺度的量能合理地揉到一起。</li> </ul> <p>这些「经验」不是人总结出来塞给算法的，是算法自己在几千次试错里<strong>进化出来</strong>的。回过头看，它甚至帮我们理解了「一个好的剪枝指标应该长什么样」。</p> <hr/> <h2 id="6-写在最后">6. 写在最后</h2> <p>把这两篇连起来看，我想说的其实是一条线：</p> <p><strong>进化算法那套「选择—变异—保留」，威力远不止「搜个网络结构」。</strong> 这一篇里，它进化的对象是一个<strong>数学公式</strong>——把「设计剪枝指标」这种过去只有资深研究员才玩得转的活，变成了一个算法自动跑出来的结果，效果还反超人工。</p> <p>这也是我一直觉得 AutoML 被低估的地方：它真正想干的，是把人在「试错、调参、做设计」上耗掉的脑力，尽可能自动化掉。从搜网络结构、搜超参，到这里搜一个公式，再到现在大火的 LLM agent 自进化——<strong>底层都是同一台机器</strong>。你把这台机器吃透了，再看那些新名词，会发现它们只是换了个搜索空间而已。</p> <p>下一篇大概会聊聊把这套思路用到「自动搜索损失函数」上——也是一棵树，也是进化，挺好玩的。</p> <hr/> <blockquote> <p>以上内容（剪枝指标分析、符号树 / 遗传编程实现、Pruner-Zero 的搜索与实验结果）节选自我写的《动手学 AutoML：从 NAS 到大语言模型优化实战》（机械工业出版社）。书里从 NAS 各大范式、超参优化，到 LLM 时代的量化剪枝都有覆盖，每个概念都尽量配了可运行的代码而不是 toy demo。有需要的同学可以前往京东搜索书名，或复制下方完整链接购买，感谢支持：</p> <p>https://item.jd.com/15384990.html</p> </blockquote> <p>照例，欢迎评论区聊——包括书里写得不清楚的地方，都可以直接来问。</p> <p><img src="/assets/img/posts/20260616-pruner-zero-llm/book_poster.png" alt=""/></p> <hr/> <ul> <li><em>作者：marsggbo，新加坡 A*STAR CFAR 研究员</em></li> <li><em>小红书 / 知乎 / GitHub: marsggbo</em></li> </ul>]]></content><author><name></name></author><category term="AutoML"/><category term="LLM"/><category term="剪枝"/><category term="遗传算法"/><category term="Pruner-Zero"/><summary type="html"><![CDATA[进阶篇 | 不靠人工设计，让遗传算法自己进化出 SOTA 的 LLM 剪枝指标]]></summary></entry><entry><title type="html">说人话：一文搞懂现在火热的 LLM agent 自进化原理</title><link href="https://marsggbo.github.io/blog/2026/automl-book-evolution/" rel="alternate" type="text/html" title="说人话：一文搞懂现在火热的 LLM agent 自进化原理"/><published>2026-06-15T00:00:00+00:00</published><updated>2026-06-15T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/automl-book-evolution</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/automl-book-evolution/"><![CDATA[<h1 id="说人话一文搞懂现在火热的-llm-agent-自进化原理">说人话：一文搞懂现在火热的 LLM agent 自进化原理</h1> <blockquote> <p>插播：以下内容节选自我们团队最近出版的书籍《动手学 AutoML：从 NAS 到大模型优化实战》，感兴趣的朋友可以前往 jd 搜索购买，感谢支持 https://item.jd.com/15384990.html</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote> <p>最近 LLM agent「自进化」（self-evolving agent）这个词特别火。</p> <p>逻辑大概是这样：让 agent 自己生成一堆变体（改改 prompt、换换工具组合、调调 workflow），在任务上跑一遍（rollout），谁的表现好就留下谁，再在好的那批身上继续改——这么一轮一轮迭代下去，agent 就「越进化越强」。</p> <p>我第一次刷到这类工作的时候，第一反应不是「哇好新」，而是——</p> <blockquote> <p>这不就是 <strong>进化算法（Evolutionary Algorithm）</strong> 吗？</p> </blockquote> <p><strong>生成变体 = 变异 / 交叉，跑任务打分 = 适应度评估，留下好的 = 选择</strong>。这套「选择—变异—保留」的循环，在 AutoML、尤其是神经架构搜索（NAS）领域，已经被反复打磨了快十年。换的只是搜索对象：以前搜的是网络结构，现在搜的是 agent 的 prompt 和工具链。</p> <p>所以这篇我想把这套「自进化」的机器从头拆开，用最朴素的方式讲清楚它到底怎么跑——配代码、配图，让你看完之后，再去刷那些 self-evolving agent 的 paper 会有种「哦原来你也是这套」的通透感。</p> <p>不背公式，从问题出发。</p> <hr/> <h2 id="1-说人话什么是进化">1. 说人话：什么是「进化」</h2> <p>进化算法这个名字唬人，其实核心就一句话：<strong>学生物进化，让一群解自己卷，卷出最好的那个</strong>。</p> <p>它和我们熟悉的梯度下降是两条完全不同的路。梯度下降需要目标函数可微，顺着导数往下滑；进化算法<strong>完全不要求可微</strong>，它就是「生成一堆候选 → 打分 → 淘汰差的 → 在好的身上做变异 → 再来一轮」。所以哪怕你的优化目标是「推理延迟」「显存占用」这种没法求导的东西，它照样能搜。</p> <p>这也是为什么它在 NAS、在今天的 agent 自进化里都吃得开——因为这些场景的「好坏」根本写不出一个可微的 loss。</p> <p>理解这件事的第一步，是把进化算法那套生物学黑话，翻译成 NAS 的语言。下面这张对照表，我个人觉得是理解整件事的钥匙：</p> <p><img src="/assets/img/posts/20260615-automl-book-evolution/01_ea_terms.png" alt="进化算法关键术语及其在 NAS 中的对应关系"/> <em>（节选自《动手学 AutoML：从 NAS 到大语言模型优化实战》第 4 章 表 4-1）</em></p> <p>你把右边那一列从「神经网络架构」换成「一个 agent」，整张表对 LLM agent 自进化照样成立：</p> <ul> <li><strong>个体（Individual）</strong> → 一个 agent 配置（一套 prompt + 工具 + workflow）</li> <li><strong>种群（Population）</strong> → 一批不同的 agent</li> <li><strong>适应度（Fitness）</strong> → agent 在任务集上的成功率 / 得分</li> <li><strong>变异（Mutation）</strong> → 随机改一改它的 prompt 或工具</li> <li><strong>选择（Selection）</strong> → 保留表现好的那几个</li> </ul> <p>是不是一下就通了。<strong>「自进化」不是什么新范式，它本质上是进化算法换了个搜索空间。</strong></p> <hr/> <h2 id="2-一轮进化到底在干什么">2. 一轮「进化」到底在干什么</h2> <p>光有对照表还不够，得看它一轮循环里具体跑了哪几步。进化搜索可以拆成六步，流程如下图：</p> <p><img src="/assets/img/posts/20260615-automl-book-evolution/02_ea_flow.png" alt="进化算法在 NAS 中的迭代优化流程"/> <em>（节选自《动手学 AutoML：从 NAS 到大语言模型优化实战》第 4 章 图 4-2）</em></p> <p>我用大白话过一遍：</p> <ol> <li><strong>种群初始化</strong>：随机生成一批候选解（架构 / agent），作为起跑线。这一步多样性很关键，太单一容易早早收敛到一个局部最优就出不来了。</li> <li><strong>适应度评估</strong>：每个候选都跑一遍打分。<strong>这一步是整个流程的瓶颈</strong>——对 NAS 来说是训练评估一个网络巨贵，对 agent 自进化来说是每跑一遍 rollout 都在烧 token。后面所有的加速 trick，本质上都是在跟这一步的开销作斗争。</li> <li><strong>选择</strong>：按分数挑出表现好的当父代。</li> <li><strong>交叉 + 变异</strong>：在父代身上做改动，生成下一代。这是产生「新东西」的唯一来源。</li> <li><strong>种群更新</strong>：用新一代替换掉老种群的一部分。</li> <li><strong>终止判断</strong>：到点了（迭代次数 / 资源耗尽 / 性能不再涨）就停，输出最优解；没到就回到第 2 步。</li> </ol> <p>你拿这张图去对最近那些 self-evolving agent 的 paper，会发现它们的 pipeline 几乎是一比一的对应。<strong>区别只在第 4 步「怎么变异」和第 2 步「怎么评估」，剩下的骨架都一样。</strong></p> <p>所以接下来我重点讲这两步——它们才是真正有技术含量、也是最容易踩坑的地方。</p> <hr/> <h2 id="3-难点一怎么变异一个结构而不把它搞坏">3. 难点一：怎么「变异」一个结构而不把它搞坏</h2> <p>先说变异和交叉。</p> <p>道理听起来很简单：随机改个基因嘛。但 <strong>「怎么编码」直接决定了你能怎么改</strong>。这里我挑最朴素的二进制编码举例。</p> <p>比如搜一个三层卷积网络，每层有「卷积核大小」和「激活函数」两个选择，各用一个比特表示。那么 <code class="language-plaintext highlighter-rouge">00 01 10</code> 就完整编码了一个网络。这时候交叉和变异就特别直观——交叉就是两个父代各切一半拼起来，变异就是随机翻转某几个比特：</p> <p><img src="/assets/img/posts/20260615-automl-book-evolution/03_crossover_mutation.png" alt="基于二进制编码的交叉和变异示意图"/> <em>（节选自《动手学 AutoML：从 NAS 到大语言模型优化实战》第 4 章 图 4-3）</em></p> <p>代码也短得不像话，实现就这么几行：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">gene_crossover</span><span class="p">(</span><span class="n">parent_a</span><span class="p">,</span> <span class="n">parent_b</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">单点交叉生成子代</span><span class="sh">"""</span>
    <span class="n">crossover_pos</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">parent_a</span><span class="p">)</span> <span class="o">//</span> <span class="mi">2</span>
    <span class="n">child_a</span> <span class="o">=</span> <span class="n">parent_a</span><span class="p">[:</span><span class="n">crossover_pos</span><span class="p">]</span> <span class="o">+</span> <span class="n">parent_b</span><span class="p">[</span><span class="n">crossover_pos</span><span class="p">:]</span>
    <span class="n">child_b</span> <span class="o">=</span> <span class="n">parent_b</span><span class="p">[:</span><span class="n">crossover_pos</span><span class="p">]</span> <span class="o">+</span> <span class="n">parent_a</span><span class="p">[</span><span class="n">crossover_pos</span><span class="p">:]</span>
    <span class="k">return</span> <span class="n">child_a</span><span class="p">,</span> <span class="n">child_b</span>

<span class="k">def</span> <span class="nf">gene_mutation</span><span class="p">(</span><span class="n">arch_code</span><span class="p">,</span> <span class="n">mutation_prob</span><span class="o">=</span><span class="mf">0.1</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">随机变异操作</span><span class="sh">"""</span>
    <span class="k">return</span> <span class="p">[</span>
        <span class="n">random</span><span class="p">.</span><span class="nf">choice</span><span class="p">(</span><span class="n">architecture_params</span><span class="p">[</span><span class="n">param</span><span class="p">])</span>
        <span class="k">if</span> <span class="n">random</span><span class="p">.</span><span class="nf">random</span><span class="p">()</span> <span class="o">&lt;</span> <span class="n">mutation_prob</span> <span class="k">else</span> <span class="n">val</span>
        <span class="k">for</span> <span class="n">param</span><span class="p">,</span> <span class="n">val</span> <span class="ow">in</span> <span class="nf">zip</span><span class="p">(</span><span class="n">architecture_params</span><span class="p">.</span><span class="nf">keys</span><span class="p">(),</span> <span class="n">arch_code</span><span class="p">)</span>
    <span class="p">]</span>
</code></pre></div></div> <p>但这里有个<strong>结结实实的坑</strong>，也是我想强调的重点：</p> <blockquote> <p><strong>简单粗暴地按比特交叉，很可能把网络搞坏。</strong></p> </blockquote> <p>举个例子，两个父代网络要交换的位置，一个是卷积、一个是 ReLU，它俩的输入输出通道数根本对不上。你硬交换，生成的子网会因为通道数不匹配，<strong>前向传播直接挂掉</strong>。</p> <p>这个坑在 LLM agent 自进化里有一模一样的版本：你把 agent A 的一段 prompt 和 agent B 的工具调用逻辑随机拼一起，很可能拼出一个语义矛盾、根本跑不通的怪胎。</p> <p>常见的解法是：<strong>别在最细的粒度上交叉</strong>。NAS 里的做法是把「卷积 + BN + ReLU」打包成一个「阶段（stage）」作为交叉的最小单元，保证交换后结构依然合法。换到 agent 上，就是按「功能模块」而不是「单个 token」去变异。</p> <p><strong>变异这件事的精髓，是在「探索新结构」和「不破坏现有结构」之间找平衡</strong>——变异太猛，种群全废；变异太弱，又跳不出当前的局部最优。这个权衡，是所有搞自进化的人都绕不过去的。</p> <hr/> <h2 id="4-难点二又要准又要小单目标根本不够">4. 难点二：又要准又要小，单目标根本不够</h2> <p>第二个难点在评估和选择。</p> <p>前面为了讲清楚，我都假设「适应度」就是一个数（比如准确率）。但真实场景里你要的从来不止一个目标：</p> <ul> <li>搞 NAS 压缩：<strong>又要准确率高，又要模型小</strong></li> <li>搞 agent 自进化：<strong>又要任务成功率高，又要 token 省</strong></li> </ul> <p>这俩目标天生打架——模型越大通常越准，agent 想得越多通常越对，但代价就是更贵。这时候「最优解」根本不是一个点，而是一条<strong>帕累托前沿（Pareto Front）</strong>：在这条线上的每个解，你想让它在某个目标上更好，就必然得牺牲另一个目标。</p> <p>处理这种多目标问题，业界最经典的算法叫 <strong>NSGA-II</strong>。它的核心是把种群按「非支配排序」分层，再用「拥挤度距离」保证留下来的解足够多样，流程如下图：</p> <p><img src="/assets/img/posts/20260615-automl-book-evolution/04_nsga2.png" alt="NSGA-II 算法流程图"/> <em>（节选自《动手学 AutoML：从 NAS 到大语言模型优化实战》第 11 章 图 11-6）</em></p> <p>我贴一段核心的「支配关系判断」，你感受一下其实没那么玄：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">dominates</span><span class="p">(</span><span class="n">a</span><span class="p">:</span> <span class="n">np</span><span class="p">.</span><span class="n">array</span><span class="p">,</span> <span class="n">b</span><span class="p">:</span> <span class="n">np</span><span class="p">.</span><span class="n">array</span><span class="p">):</span>
    <span class="c1"># 若 a 支配 b，返回 True（a 在所有目标上都不差，且至少一个目标更好）
</span>    <span class="k">return</span> <span class="n">np</span><span class="p">.</span><span class="nf">all</span><span class="p">(</span><span class="n">a</span> <span class="o">&lt;=</span> <span class="n">b</span><span class="p">)</span> <span class="ow">and</span> <span class="ow">not</span> <span class="n">np</span><span class="p">.</span><span class="nf">all</span><span class="p">(</span><span class="n">a</span> <span class="o">==</span> <span class="n">b</span><span class="p">)</span>
</code></pre></div></div> <p>就这么一行判断，配上非支配排序，你就能把一大堆「准确率 / 模型大小」各异的候选，自动分成一层一层的帕累托前沿。第一层就是当前最优的那一组互不支配的解——它们各有各的取舍，没有谁能完全碾压谁。</p> <p><strong>这套多目标的思路，恰恰是现在 agent 自进化里最缺的一环</strong>。我看很多自进化的工作还停留在「单目标卷成功率」，卷到最后 agent 是变强了，但每跑一次贵得离谱。NSGA-II 这种东西迁移过去，是能直接派上用场的。</p> <hr/> <h2 id="5-来真的手撸一个进化压缩把-resnet-砍掉-75">5. 来真的：手撸一个进化压缩，把 ResNet 砍掉 75%</h2> <p>讲了这么多原理，最后一定要落到一个<strong>能跑起来的真实项目</strong>上——光说不练那是 PPT。</p> <p>下面用我自己开源的 <code class="language-plaintext highlighter-rouge">hyperbox</code> 框架，把 ResNet18 在 CIFAR10 上做一次进化压缩。整个流程就是前面讲的那套机器，只不过搜索对象变成了「每一层的通道数压缩比例」：</p> <ol> <li><strong>训练超网（supernet）</strong>：把所有候选子网的权重共享在一张超网里一起训。这就是 one-shot NAS 的核心 trick，省掉了「每个候选都从头训」的天价开销。</li> <li><strong>进化搜索</strong>：超网权重固定，用上面那套 NSGA-II 选择 + 交叉 + 变异去搜。每采样一个子网，直接从超网继承权重就能评估，<strong>不用重训、不用微调</strong>。</li> <li><strong>微调</strong>：对搜出来的帕累托模型做最后的微调，定稿。</li> </ol> <p>跑完之后，搜索结果长这样——横轴是模型大小，纵轴是验证准确率，每个点都是一个搜出来的子网：</p> <p><img src="/assets/img/posts/20260615-automl-book-evolution/05_pareto.png" alt="以 15MB 为上界的 ResNet 搜索空间进化搜索结果"/> <em>（节选自《动手学 AutoML：从 NAS 到大语言模型优化实战》第 11 章 图 11-8）</em></p> <p>可以看到整条帕累托前沿上的子网表现都不错，准确率基本都在 85% 以上，最高能到 94% 左右。挑第一个帕累托模型做微调，最终的结果是：</p> <blockquote> <p><strong>模型大小 10.69MB，相比原始超网压缩了约 74.95%，测试集准确率 91.39%。</strong></p> </blockquote> <p>砍掉四分之三的参数量，准确率只掉一点点。这个过程里<strong>没有一处是手工调的</strong>——通道数怎么配、哪层该瘦、哪层该留，全是进化算法自己搜出来的。这就是 AutoML 想干的事：<strong>把「试错」这件苦力活，从你手上接过去自动化掉</strong>。</p> <hr/> <h2 id="6-写在最后">6. 写在最后</h2> <p>绕了一圈，其实我想说的就一句话：</p> <p><strong>LLM agent「自进化」不是凭空冒出来的新魔法，它的底层骨架，就是进化搜索那套被打磨了快十年的东西。</strong> 选择、变异、交叉、多目标权衡、超网共享——这些 NAS 里反复验证过的工具，正在被原样搬到 agent 上。区别只是搜索空间从「网络结构」换成了「prompt 和工具链」。</p> <p>所以如果你是冲着 agent 自进化、或者 LLM 时代的模型优化来的，我反而建议你回头把进化算法这套基础吃透。<strong>新热点跑得快，但底层的优化逻辑稳得很</strong>——吃透了它，再看那些新 paper 会轻松很多。</p> <hr/> <blockquote> <p>以上内容（术语对照、进化流程、交叉变异、NSGA-II 非支配排序与拥挤度距离、以及 hyperbox 上的 ResNet 压缩实战）节选自我写的《动手学 AutoML：从 NAS 到大语言模型优化实战》（机械工业出版社）。书里还有 NAS 各大范式、超参优化、以及 LLM 时代量化剪枝的内容，每个概念都尽量配了可运行的代码而不是 toy demo。有需要的同学可以前往京东搜索书名，或复制下方完整链接购买，感谢支持：</p> <p>https://item.jd.com/15384990.html</p> </blockquote> <p>照例，欢迎评论区聊——包括书里写得不清楚的地方，都可以直接来问。</p> <h2><img src="/assets/img/posts/20260615-automl-book-evolution/book_poster.png" alt=""/></h2> <ul> <li><em>作者：marsggbo，新加坡 A*STAR CFAR 研究员</em></li> <li><em>小红书 / 知乎 / GitHub: marsggbo</em></li> </ul>]]></content><author><name></name></author><category term="AutoML"/><category term="NAS"/><category term="LLM"/><category term="Agent"/><category term="进化算法"/><summary type="html"><![CDATA[说人话：一文搞懂现在火热的 LLM agent 自进化原理]]></summary></entry></feed>