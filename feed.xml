<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://marsggbo.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://marsggbo.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-07-03T02:54:34+00:00</updated><id>https://marsggbo.github.io/feed.xml</id><title type="html">HE Xin</title><subtitle>&quot;SSS: Study, Sleep, Slim&quot; </subtitle><entry><title type="html"></title><link href="https://marsggbo.github.io/blog/2026/2026-06-07-2605_29350/" rel="alternate" type="text/html" title=""/><published>2026-07-03T02:54:34+00:00</published><updated>2026-07-03T02:54:34+00:00</updated><id>https://marsggbo.github.io/blog/2026/2026-06-07-2605_29350</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/2026-06-07-2605_29350/"><![CDATA[<blockquote> <p>插播：之前写的<a href="https://item.jd.com/14945889.html">《动手学 AutoML》</a>终于出版了，从 NAS 到超参优化都有覆盖，适合想系统入门 AutoML 的同学。好了广告结束，现在进入正题。</p> <p><img src="/assets/img/book_cover_automl.png" alt="动手学AutoML书籍封面"/></p> </blockquote> <h1 id="moe-压缩新思路别删专家别合并专家把它们重映射就够了">MoE 压缩新思路：别删专家、别合并专家，把它们”重映射”就够了</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2605.29350">ConMoE: Expert-Pool Consolidation via Prototype Reassignment for MoE Compression</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有想过，MoE 架构最骄傲的地方，恰恰也是最烫手的地方？</p> <p>MoE（Mixture-of-Experts）的核心设计是稀疏激活——每个 token 只走少数几个 expert，理论上参数量很大但计算量很小。Qwen3-30B-A3B 的名字里 A3B 的意思就是”激活参数 3B”，但总参数有 30B。听起来很美，对吧？</p> <p>但问题来了：<strong>这 30B 参数，你一个都不能少地加载进显存</strong>。</p> <p>不管一个 token 实际上只用了 3B 参数，那剩下 27B 的专家还是得老老实实待在显存里候命。这就是 MoE 推理的核心矛盾：稀疏激活省了算力，但显存一分没省。随着模型越做越大，expert 存储变成了 MoE 落地的最大障碍之一。</p> <p>现有的压缩思路主要两条路：<strong>剪掉专家（pruning）</strong> 和 <strong>合并专家（merging）</strong>。前者直接删掉一些专家，后者把多个专家的权重”揉”成一个新专家。</p> <p>今天要聊的这篇论文提了第三条路：<strong>什么都不删，什么都不合并，只是把原来的专家引用”重新指向”一个更小的专家池</strong>。</p> <p>听起来像绕口令？别急，慢慢来。</p> <hr/> <h2 id="2-现有方案的痛点">2. 现有方案的痛点</h2> <p>在聊 ConMoE 之前，先交代下背景，说说 pruning 和 merging 各自的问题。</p> <p><strong>Expert Pruning（专家剪枝）</strong>：直接删掉贡献低的专家。简单粗暴，但问题是删了就是删了，原来指向这个专家的 router 决策没有任何”容错”——被删专家的那些 token 只能丢给其他留下来的专家，不管合不合适。</p> <p><strong>Expert Merging（专家合并）</strong>：把多个专家的权重融合成一个新专家。听起来更优雅，但实际上构造了一个”原来不存在”的新权重，这个新权重可能引入了原本没有的错误，而且融合方式很敏感，调参难度大。</p> <p>这两类方法有一个共同问题：<strong>把”保留哪些专家参数”和”原来的 expert slot 该怎么被表示”这两件事耦合在一起了</strong>。</p> <p>ConMoE 的作者们说，等等，这两个问题应该分开来看。</p> <hr/> <h2 id="3-conmoe-的核心思路专家池整合expert-pool-consolidation">3. ConMoE 的核心思路：专家池整合（Expert-Pool Consolidation）</h2> <p>论文提出了一个新的框架视角，叫 <strong>expert-pool consolidation</strong>（专家池整合）。</p> <p>核心思想拆成两步：</p> <ol> <li>从原来的专家池里<strong>挑出一个更小的子集</strong>作为”原型（prototype）”</li> <li>把每个原来的专家引用<strong>确定性地重映射</strong>到某个 prototype 上</li> </ol> <p>这样，router 对外还是看到原来那些 expert slot，内部只是把调用转发给对应的 prototype。<strong>专家的权重没有被修改，没有被融合，prototype 就是原来的 pretrained 专家</strong>。</p> <p>如下图，直观地展示了 ConMoE 的整体流程：</p> <p><img src="/assets/img/posts/2605_29350/figure1_p3.png" alt="ConMoE 整体架构示意"/></p> <p>图里的流程很清晰：左边是预训练的 MoE，有若干层，每层有自己的专家池。ConMoE 在若干相邻层构成的 local scope 里，用 calibration 统计数据和专家间的距离来选出 prototype，然后把原来的每个专家 slot 都确定性地指向某个 prototype。最右边压缩后的 MoE 对外接口和原来一样（router 不变），只是内部的专家池变小了。</p> <p>这里有一个很巧妙的地方：<strong>prototype 可以跨层共享</strong>。也就是说，相邻几层的专家可以共用一个 prototype 池，这让压缩比更高，同时也利用了相邻层之间专家的冗余性。</p> <hr/> <h2 id="4-怎么挑-prototype两个信号的乘积">4. 怎么挑 prototype？两个信号的乘积</h2> <p>这是方法里最关键的设计。对于每个 local scope $G$，ConMoE 给每个专家算一个分数 $s_e$，然后取 top-K 作为 prototype。</p> <p>分数由两个信号构成：</p> <p><strong>信号一：路由条件贡献分（contribution score）$a_e$</strong></p> \[a_e = \frac{1}{|\mathcal{D}_e|} \sum_{t \in \mathcal{D}_e} g_e(t) \cdot \|e(h_t)\|_2\] <p>说人话：在 calibration 数据上，这个专家被 router 选中的那些 token，它的输出强度乘以路由权重的平均值。分数高 = 这个专家被用到时贡献大。</p> <p><strong>信号二：可替换性分（replaceability score）$b_e$</strong></p> \[b_e = \min_{e' \in \mathcal{E}_G \setminus \{e\}} d(e, e')\] <p>就是这个专家在 scope 内，到其最近邻专家的距离。距离越大 = 越难被其他专家替代 = 越不可替换。</p> <p>两个分数各自在 scope 内做 min-max 归一化后相乘：</p> \[s_e = \bar{a}_e \cdot \bar{b}_e\] <p><strong>直觉很清晰</strong>：一个好的 prototype，既要在原来模型里有实质性的贡献（不能是闲置专家），又要在参数空间里相对独特（不能被其他专家轻易替代）。两个维度同时满足，才是值得被保留的 prototype。</p> <p>选好 prototype 后，每个原始专家就找自己最近的 prototype：</p> \[m_G(e) = \arg\min_{p \in P_G} d(e, p)\] <p>这就是重映射。干净，确定，不需要梯度，不需要 fine-tune。</p> <hr/> <h2 id="5-跨层共享scope-大了不一定好">5. 跨层共享：scope 大了不一定好</h2> <p>ConMoE 允许相邻几层共享一个 prototype 候选池（local scope），这让压缩更灵活。但 scope 到底设多大合适？</p> <p>如下图是在 Qwen3-30B-A3B 上做的消融，50% expert reduction 的情况下，不同 scope size 的效果：</p> <p><img src="/assets/img/posts/2605_29350/figure2_p7.png" alt="scope size 对性能的影响"/></p> <p>结果很有趣：scope 1（只在单层内选）、scope 2、scope 4 的表现非常接近，scope 4 在这个代表性子集上平均分最高。但 scope 一旦扩到 8 或 16 层，三个任务的性能都出现明显下降，HellaSwag 和 MMLU 掉得尤其多。</p> <p>这说明什么？<strong>相邻层之间确实存在可复用的冗余，但这种冗余是局部的</strong>。距离太远的层，对应的是模型不同深度的语义转换，强行共享 prototype 会引入深度不匹配的问题。</p> <p>这也解释了为什么 ConMoE 选择 bounded local scope，而不是一个全局的 expert pool。</p> <hr/> <h2 id="6-主实验结果">6. 主实验结果</h2> <p>实验在三个 MoE 模型上做了对比：Qwen3-30B-A3B、deepseek-moe-16b-base、OLMoE-1B-7B-0125，各自测了 25% 和 50% 的 expert reduction。Baseline 包括：</p> <ul> <li><strong>Pruning 类</strong>：Frequency pruning（按路由频率删）、REAP pruning（基于贡献分删）</li> <li><strong>Merging 类</strong>：M-SMoE（路由统计引导合并）、HC-SMoE（输出特征聚类合并）</li> </ul> <p>如下表是主要实验结果（6 个多选题 benchmark 的平均分）：</p> <p><img src="/assets/img/posts/2605_29350/table1_p6.png" alt="主实验结果表"/></p> <p>几个关键观察：</p> <ul> <li><strong>DeepSeek 上，ConMoE 在 25% 和 50% reduction 两个设置下都拿到最佳平均分</strong>（0.691 vs 0.681/0.676，0.651 vs 0.642/0.613），这是最亮眼的结果。</li> <li><strong>OLMoE 上，25% reduction 同样是 ConMoE 最好</strong>（0.676 vs 0.669/0.662），50% 时和最强 baseline 很接近。</li> <li><strong>Qwen3 上，pruning 方法特别强</strong>，尤其是 50% reduction 时 Frequency pruning 达到 0.733，ConMoE 是 0.717。但 ConMoE 在 WinoGrande 和 BoolQ 上保持了很强的表现。</li> </ul> <p>整体来说，ConMoE 作为一种全新的”不删不合并、只重映射”的压缩范式，<strong>在和 pruning/merging 同等压缩比下能打平甚至超过</strong>，这本身就是一个有意义的结论。</p> <hr/> <h2 id="7-消融实验哪个设计最重要">7. 消融实验：哪个设计最重要？</h2> <h3 id="71-整合结构consolidation-structure">7.1 整合结构（Consolidation Structure）</h3> <p>如下表对比了不同的整合方式：</p> <p><img src="/assets/img/posts/2605_29350/table2_p6_04.png" alt="整合结构消融实验"/></p> <p>三种 variant：</p> <ul> <li><strong>Layer-local</strong>：只在单层内选 prototype，不跨层</li> <li><strong>Cross-layer fixed-k</strong>：跨层候选池，但每层的 prototype 数量固定</li> <li><strong>Adaptive prototype</strong>：ConMoE 默认方案，用贡献分+可替换性自适应分配</li> </ul> <p>有意思的是，layer-local 已经很强了（ARC-C 0.515，HellaSwag 0.715，MMLU 0.651）。允许跨层候选池（Cross-layer fixed-k）在 HellaSwag 上略有提升，Adaptive prototype 和 Cross-layer fixed-k 表现接近。</p> <p><strong>结论：确定性重映射本身才是最重要的成分</strong>，跨层和自适应选择是锦上添花。</p> <h3 id="72-prototype-选择策略">7.2 Prototype 选择策略</h3> <p>如下表比较了不同的 prototype 选择信号：</p> <p><img src="/assets/img/posts/2605_29350/table3_p7_08.png" alt="prototype 选择消融"/></p> <ul> <li><strong>Distance-only</strong>（只看可替换性）：ARC-C 0.242，HellaSwag 0.319，MMLU 0.231 ——直接崩了</li> <li><strong>Usage top-k</strong>（只看路由频率）：HellaSwag 掉到 0.586，比较弱</li> <li><strong>REAP top-k</strong>（基于贡献分）：HellaSwag 回到 0.718，接近 ConMoE 默认</li> <li><strong>Adaptive prototype</strong>（ConMoE 默认）：HellaSwag 0.716，与 REAP top-k 相当</li> </ul> <p><strong>路由条件贡献分是主导信号</strong>，可替换性是辅助正则化（防止选出重复的 prototype），单独用距离作为选择标准完全行不通。</p> <h3 id="73-事后权重融合诊断">7.3 事后权重融合诊断</h3> <p>有人会问：选好 prototype 之后，能不能再做一下权重融合（把分配给同一 prototype 的那些原始专家的权重平均一下），效果会不会更好？</p> <p>如下表给出了答案：</p> <p><img src="/assets/img/posts/2605_29350/table4_p7.png" alt="事后权重融合诊断"/></p> <p>对比三种方案：</p> <ul> <li><strong>None（ConMoE 默认）</strong>：不做任何融合，ARC-C 0.504，HellaSwag 0.716，MMLU 0.639</li> <li><strong>Arcee fusion</strong>：HellaSwag 从 0.716 直接掉到 0.531，MMLU 从 0.639 掉到 0.512</li> <li><strong>Weighted average</strong>：更惨，HellaSwag 0.469，MMLU 0.358</li> </ul> <p><strong>ConMoE 的收益来自确定性重映射，而不是权重融合</strong>。直接复用原始的 pretrained prototype 才是最稳的策略，加了融合反而破坏了原始权重。</p> <hr/> <h2 id="8-跨层专家可替换性分析">8. 跨层专家可替换性分析</h2> <p>为什么跨层 scope 有效，但又不能太大？论文专门做了一个几何分析。</p> <p>在 Qwen3-30B-A3B 上，用 4 层 scope 计算每个 expert 在参数空间的最近邻，统计最近邻是否在同一层还是不同层：</p> <p><img src="/assets/img/posts/2605_29350/figure3_p8.png" alt="跨层最近邻分析"/></p> <p>左图是热力图，展示从 source layer 到最近邻 layer 的分布。可以看到主要沿对角线集中，说明最近邻大概率在附近的层，而非全局随机分布。</p> <p>右图是每层中”最近邻在不同层”的比例。<strong>整体上有 50.4% 的 expert，其参数空间的最近邻在另一层</strong>，部分中间层超过 70%。</p> <p>这两个观察共同说明：<strong>专家的可替换性不是严格层内的，相邻层之间存在真实的冗余</strong>——这是跨层共享 prototype 的几何依据。但这种冗余是局部的，不支持全局共享。</p> <hr/> <h2 id="9-总结">9. 总结</h2> <p>ConMoE 提供了一个很干净的框架来思考 MoE 压缩：</p> <ul> <li><strong>不删</strong>（pruning 的做法）</li> <li><strong>不改权重</strong>（merging 的做法）</li> <li><strong>只重映射</strong>：选出 pretrained 专家子集作为 prototype，把其他专家的引用指过去</li> </ul> <p>三个设计原则从实验里验证：</p> <ol> <li><strong>确定性重映射是最稳定的成分</strong>，单层内做都已经很强</li> <li><strong>跨层复用要保持局部</strong>，相邻层可以，太远的层会因深度不匹配掉性能</li> <li><strong>路由条件贡献分是主导信号</strong>，可替换性是辅助的正则化</li> </ol> <p>个人觉得这篇论文最有价值的点，是把”保留哪些专家参数”和”原来的 expert slot 怎么被表示”这两个问题<strong>显式地解耦</strong>了。这个视角在之前的 pruning/merging 框架里是隐含的，ConMoE 把它明确化之后，整个设计空间就清晰多了。</p> <p>当然 limitation 也直接：这套方案依赖 calibration 数据，跨层 scope 不能太大，而且现在的”logical reduction ratio”和实际的显存节省还需要专门的 runtime 支持（shared-prototype checkpoint 格式）才能落地。最后这点是整个 prototype sharing 方案的工程挑战，不只是这篇论文的问题。</p> <p>感兴趣的可以去翻原文，代码和复现脚本作者也有提到会开源。</p> <p>欢迎评论区交流，有什么遗漏或者理解有误的地方也欢迎指出~</p>]]></content><author><name></name></author></entry><entry><title type="html">贝叶斯优化从零推导：从「我对这个函数有个猜测」到自动调参</title><link href="https://marsggbo.github.io/blog/2026/20260630-bayesian-optimization/" rel="alternate" type="text/html" title="贝叶斯优化从零推导：从「我对这个函数有个猜测」到自动调参"/><published>2026-06-30T00:00:00+00:00</published><updated>2026-06-30T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260630-bayesian-optimization</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260630-bayesian-optimization/"><![CDATA[<h1 id="贝叶斯优化从零推导从我对这个函数有个猜测到自动调参">贝叶斯优化从零推导：从「我对这个函数有个猜测」到自动调参</h1> <hr/> <h2 id="1-前言从一个具体的问题开始">1. 前言：从一个具体的问题开始</h2> <p>先不讲理论。来看一个实际问题。</p> <p>LLaMA-2-13B 有 40 层（20 对 Attention + MLP）。有一篇论文（Self-Speculative Decoding）发现，推理时跳过部分中间层先快速生成 draft token，再用完整模型一次验证，可以无损加速 1.3x~2x。</p> <p>问题来了：<strong>跳哪些层？</strong></p> <p>搜索空间是 $2^{40}$，穷举不可能。但更麻烦的是，<strong>每次评估都很贵</strong>——跑一遍 self-speculative decoding 采几百个 token、测平均耗时，需要好几分钟。你不可能随机试 10000 次。</p> <p>随机搜索？完全不利用已有结果，效率极低。</p> <p>网格搜索？$2^{40}$ 种组合，连列举都列举不完。</p> <p><strong>贝叶斯优化（Bayesian Optimization，BO）就是专门为这类场景设计的</strong>：评估极贵、不能穷举、但每次结果都包含有价值的信息。</p> <p>它的核心直觉：</p> <blockquote> <p>我已经测了 10 种跳层方案，知道跳第 15~25 层比较好，跳第 1~5 层基本没用。下一次，我应该去”还没测过、但根据已有结果猜测很有潜力”的区域试。</p> </blockquote> <p>要把这个直觉变成算法，需要解决两个问题：</p> <ol> <li><strong>怎么用有限的观测点”猜测”整个函数的形状？</strong> → 高斯过程（GP）</li> <li><strong>“很有潜力”怎么定量化，指导下一步去哪里？</strong> → Acquisition function</li> </ol> <p>本文从高斯分布出发，一步步推导这两件事怎么做。<strong>文末会把整套流程完整地套到这个 LLM 跳层搜索问题上</strong>，让你看清楚每个数学概念对应的现实意义。</p> <hr/> <h2 id="2-先从高斯分布说起">2. 先从高斯分布说起</h2> <p>在进入高斯过程之前，先把高斯分布的核心性质梳理清楚，因为后面所有推导都依赖它。</p> <h3 id="21-一维高斯描述单个随机变量的不确定性">2.1 一维高斯：描述单个随机变量的不确定性</h3> <p>$X \sim \mathcal{N}(\mu, \sigma^2)$ 就是说：这个随机变量的取值”大概在 $\mu$ 附近，偏差用 $\sigma$ 衡量”。</p> <p>没什么神秘的。</p> <h3 id="22-多维高斯描述多个随机变量的联合不确定性">2.2 多维高斯：描述多个随机变量的联合不确定性</h3> <p>关键性质：多维高斯分布由<strong>均值向量</strong> $\mu$ 和<strong>协方差矩阵</strong> $\Sigma$ 完全描述：</p> \[\mathbf{x} \sim \mathcal{N}(\boldsymbol{\mu}, \Sigma)\] <p>$\Sigma_{ij} = \text{Cov}(x_i, x_j)$ 描述的是”第 $i$ 个变量和第 $j$ 个变量有多大关联”。</p> <p><strong>高斯分布最重要的一个性质</strong>：它的条件分布和边缘分布都还是高斯分布，而且有解析解。</p> <p>这个性质非常非常重要，是整个高斯过程能用的根本原因。后面会看到为什么。</p> <p>具体地，把 $\mathbf{x}$ 分成两组 $\mathbf{x}_1, \mathbf{x}_2$：</p> \[\begin{bmatrix} \mathbf{x}_1 \\ \mathbf{x}_2 \end{bmatrix} \sim \mathcal{N}\left( \begin{bmatrix} \boldsymbol{\mu}_1 \\ \boldsymbol{\mu}_2 \end{bmatrix}, \begin{bmatrix} \Sigma_{11} &amp; \Sigma_{12} \\ \Sigma_{21} &amp; \Sigma_{22} \end{bmatrix} \right)\] <table> <tbody> <tr> <td>则条件分布 $p(\mathbf{x}_1</td> <td>\mathbf{x}_2)$ <strong>仍然是高斯</strong>，均值和方差为：</td> </tr> </tbody> </table> \[\boldsymbol{\mu}_{1|2} = \boldsymbol{\mu}_1 + \Sigma_{12} \Sigma_{22}^{-1} (\mathbf{x}_2 - \boldsymbol{\mu}_2)\] \[\Sigma_{1|2} = \Sigma_{11} - \Sigma_{12} \Sigma_{22}^{-1} \Sigma_{21}\] <p>先不用记这两个公式，后面再回来推导。现在只需要记住：<strong>多维高斯的条件分布仍是高斯，且有解析解</strong>。</p> <hr/> <h2 id="3-高斯过程把高斯分布从点推广到函数">3. 高斯过程：把高斯分布从”点”推广到”函数”</h2> <h3 id="31-一个关键的视角转换">3.1 一个关键的视角转换</h3> <p>我们想对一个黑盒函数 $f: \mathbb{R}^d \to \mathbb{R}$ 建模。</p> <p>常规思路是假设 $f$ 有某种参数化形式（比如神经网络），然后用数据拟合参数。</p> <p>高斯过程走了另一条路：<strong>直接把函数 $f$ 本身当作随机变量，对它的分布建模</strong>。</p> <p>怎么理解”函数是随机变量”？</p> <p>想象函数 $f$ 在空间中的无穷多个点 $x_1, x_2, x_3, \ldots$ 上的取值，形成一个无穷维的向量 $(f(x_1), f(x_2), f(x_3), \ldots)$。</p> <p>高斯过程的定义就是：<strong>这个无穷维向量的任意有限子集都服从多维高斯分布</strong>。</p> <p>写成数学语言：</p> \[f \sim \mathcal{GP}(m(\cdot), k(\cdot, \cdot))\] <p>其中：</p> <ul> <li>$m(x) = \mathbb{E}[f(x)]$ 是<strong>均值函数</strong>，描述函数在每个点的期望值</li> <li>$k(x, x’) = \text{Cov}(f(x), f(x’))$ 是<strong>核函数（协方差函数）</strong>，描述两个点之间的”相关性”</li> </ul> <h3 id="32-核函数编码你对函数形状的先验假设">3.2 核函数：编码你对函数形状的先验假设</h3> <p>核函数 $k(x, x’)$ 是高斯过程的灵魂。它回答了一个问题：<strong>如果 $x$ 和 $x’$ 很近，$f(x)$ 和 $f(x’)$ 的取值会有多相似？</strong></p> <p>最常用的是 <strong>RBF（Radial Basis Function）核</strong>：</p> \[k_{\text{RBF}}(x, x') = \exp\left(-\frac{\|x - x'\|^2}{2l^2}\right)\] <p>当 $x = x’$ 时，$k = 1$（自相关最大）；距离越远，$k$ 趋向 0（相关性消失）。</p> <p>$l$ 是<strong>长度尺度（length scale）</strong>，控制”多近才算近”——$l$ 大说明你认为函数很平滑，相距较远的点也相关；$l$ 小说明函数变化剧烈。</p> <p><strong>Matern 核</strong>是 RBF 的推广，多了一个参数 $\nu$ 控制光滑度：</p> \[k_{\text{Matern}}(x, x') \propto \left(\frac{\sqrt{2\nu} \|x-x'\|}{l}\right)^\nu K_\nu\left(\frac{\sqrt{2\nu} \|x-x'\|}{l}\right)\] <p>$\nu = 5/2$ 时函数二阶可微，在实践中很好用——比 RBF 假设的无穷阶光滑更符合实际。</p> <p><strong>选核函数，本质上是在选你对函数性质的先验假设</strong>。这就是”贝叶斯”两个字的来源之一。</p> <hr/> <h2 id="4-贝叶斯更新观测数据后怎么修正猜测">4. 贝叶斯更新：观测数据后怎么修正猜测</h2> <p>现在进入核心推导。</p> <h3 id="41-问题设定">4.1 问题设定</h3> <p>已知：观测到了 $n$ 个点的函数值，记为 $\mathcal{D} = {(x_i, y_i)}_{i=1}^n$，其中 $y_i = f(x_i) + \epsilon$，$\epsilon \sim \mathcal{N}(0, \sigma_n^2)$ 是观测噪声。</p> <p>问：对于新的未观测点 $x_<em>$，$f(x_</em>)$ 的分布是什么？</p> <h3 id="42-联合分布">4.2 联合分布</h3> <p>根据 GP 的定义，$(f(x_1), \ldots, f(x_n), f(x_*))$ 这 $n+1$ 个值<strong>联合服从多维高斯</strong>：</p> \[\begin{bmatrix} \mathbf{y} \\ f(x_*) \end{bmatrix} \sim \mathcal{N}\left( \begin{bmatrix} \mathbf{0} \\ 0 \end{bmatrix}, \begin{bmatrix} K + \sigma_n^2 I &amp; \mathbf{k}_* \\ \mathbf{k}_*^\top &amp; k_{**} \end{bmatrix} \right)\] <p>其中：</p> <ul> <li>$K_{ij} = k(x_i, x_j)$ 是 $n \times n$ 的核矩阵</li> <li>$\mathbf{k}<em>* = [k(x_1, x</em><em>), \ldots, k(x_n, x_</em>)]^\top$ 是新点和所有观测点之间的协方差向量</li> <li>$k_{<em>*} = k(x_</em>, x_*)$ 是新点的自协方差（通常为1）</li> <li>均值暂时设为 0（不影响推导，实践中可以加先验均值）</li> </ul> <h3 id="43-条件分布推导">4.3 条件分布推导</h3> <p><strong>现在用第2节里的条件高斯公式</strong>，令 $\mathbf{x}<em>1 = f(x</em>*)$，$\mathbf{x}_2 = \mathbf{y}$：</p> \[\mu_* = \mathbf{k}_*^\top (K + \sigma_n^2 I)^{-1} \mathbf{y}\] \[\sigma_*^2 = k_{**} - \mathbf{k}_*^\top (K + \sigma_n^2 I)^{-1} \mathbf{k}_*\] <p>这就是 <strong>GP 预测公式</strong>。</p> <p><strong>逐项拆解，理解每一项的含义</strong>：</p> <p><strong>均值 $\mu_*$</strong>：</p> \[\mu_* = \mathbf{k}_*^\top \underbrace{(K + \sigma_n^2 I)^{-1} \mathbf{y}}_{\boldsymbol{\alpha}}\] <p>把 $\boldsymbol{\alpha} = (K + \sigma_n^2 I)^{-1} \mathbf{y}$ 看作”每个观测点的权重”，$\mu_*$ 就是以<strong>新点和观测点的相关性为权重</strong>，对观测值做加权平均。</p> <p>直觉：$x_<em>$ 和某个 $x_i$ 很近（$k(x_i, x_</em>)$ 大），那 $f(x_i)$ 对预测 $f(x_*)$ 的贡献就大。</p> <p><strong>方差 $\sigma_*^2$</strong>：</p> \[\sigma_*^2 = \underbrace{k_{**}}_{\text{先验方差}} - \underbrace{\mathbf{k}_*^\top (K + \sigma_n^2 I)^{-1} \mathbf{k}_*}_{\text{观测带来的信息量}}\] <p>先验方差减去”从观测中学到的信息量”。观测点离 $x_*$ 越近，方差减少越多，预测越确定。</p> <p><strong>这就是贝叶斯的精髓</strong>：从先验出发（$k_{<em>*}$），用数据（观测点）更新，得到后验（$\sigma_</em>^2$ 变小）。</p> <hr/> <h2 id="5-为什么要这样假设高斯假设到底合不合理">5. 为什么要这样假设？高斯假设到底合不合理？</h2> <p>到这里可能会有个疑问：凭什么假设函数值联合服从高斯分布？实际的黑盒函数根本不一定是高斯的啊。</p> <p>这是个好问题，有几个层面的回答：</p> <p><strong>第一，高斯假设带来计算上的奇迹。</strong></p> <p>正是因为假设了高斯，条件分布才有漂亮的解析解（上面的公式）。如果换成其他分布，条件分布通常没有闭合形式，只能做 MCMC 采样，慢得多。高斯假设是”可处理性”和”表达能力”之间的最佳权衡点。</p> <p><strong>第二，中心极限定理的支撑。</strong></p> <p>很多实际函数可以被视为大量独立随机变量叠加的结果。中心极限定理告诉我们，这类函数的行为趋近高斯。</p> <p><strong>第三，高斯过程是最大熵分布。</strong></p> <p>在已知均值和协方差的约束下，高斯分布是<strong>熵最大</strong>的分布——也就是说，在你已有的信息基础上，它是”假设最少”的选择。这正是贝叶斯建模的哲学：不要在数据之外引入额外假设。</p> <p><strong>第四，实践上确实好用。</strong></p> <p>最务实的理由：在低维、小样本场景（超参数搜索通常就是这个设定），GP 的效果经过几十年验证，确实靠谱。</p> <hr/> <h2 id="6-acquisition-function下一步去哪里">6. Acquisition Function：下一步去哪里？</h2> <p>现在我们有了 GP，可以对任意未观测点 $x_<em>$ 给出预测均值 $\mu(x_</em>)$ 和不确定性 $\sigma(x_*)$。</p> <p>但问题来了：<strong>下一个点选哪里？</strong></p> <ul> <li>选 $\mu(x_*)$ 最大的地方？——只利用当前最优猜测，完全不探索未知区域，容易陷入局部最优。</li> <li>选 $\sigma(x_*)$ 最大的地方？——只去不确定的地方，完全忽略已知的信息，低效。</li> </ul> <p>需要一个函数来平衡<strong>探索（exploration）</strong>和<strong>利用（exploitation）</strong>。这就是 Acquisition function。</p> <h3 id="61-最直观的probability-of-improvement-pi">6.1 最直观的：Probability of Improvement (PI)</h3> <p><strong>直觉</strong>：新点 $x_*$ 比当前最优值 $f^+ = \max_i y_i$ 更好的概率是多少？</p> \[\text{PI}(x_*) = P(f(x_*) &gt; f^+) = \Phi\left(\frac{\mu(x_*) - f^+}{\sigma(x_*)}\right)\] <p>其中 $\Phi$ 是标准正态 CDF。</p> <p>因为 $f(x_<em>) \sim \mathcal{N}(\mu(x_</em>), \sigma(x_*)^2)$，所以”超过 $f^+$ 的概率”就是正态分布尾部的面积，直接用 $\Phi$ 算出来。</p> <p><strong>问题</strong>：PI 只关心”有没有改进”，不关心”改进多少”——哪怕提升了 0.001 也算赢。容易在最优点附近反复横跳。</p> <h3 id="62-更好用的expected-improvement-ei">6.2 更好用的：Expected Improvement (EI)</h3> <p><strong>直觉</strong>：期望改进量是多少？不只是”有没有更好”，而是”平均能好多少”。</p> \[\text{EI}(x_*) = \mathbb{E}[\max(f(x_*) - f^+, 0)]\] <p>因为 $f(x_*)$ 是高斯分布，这个期望有<strong>解析解</strong>：</p> \[\text{EI}(x_*) = (\mu(x_*) - f^+) \cdot \Phi(Z) + \sigma(x_*) \cdot \phi(Z)\] <p>其中 $Z = \dfrac{\mu(x_<em>) - f^+}{\sigma(x_</em>)}$，$\phi$ 是标准正态 PDF。</p> <p><strong>逐项理解</strong>：</p> \[\underbrace{(\mu(x_*) - f^+) \cdot \Phi(Z)}_{\text{利用项}} + \underbrace{\sigma(x_*) \cdot \phi(Z)}_{\text{探索项}}\] <ul> <li><strong>利用项</strong>：当预测均值 $\mu$ 远超当前最优时，这一项大（利用已知好区域）</li> <li><strong>探索项</strong>：当不确定性 $\sigma$ 大时，这一项大（探索未知区域）</li> </ul> <p>两项自然地平衡了探索和利用，<strong>不需要手动调权重</strong>。这是 EI 流行的核心原因。</p> <p><strong>推导过程</strong>（从期望定义出发）：</p> <p>设 $u = f(x_*) - f^+$，则：</p> \[\text{EI} = \int_{-\infty}^{+\infty} \max(u, 0) \cdot \mathcal{N}(u; \mu - f^+, \sigma^2) \, du = \int_{0}^{+\infty} u \cdot \mathcal{N}(u; \mu - f^+, \sigma^2) \, du\] <p>令 $\gamma = \frac{\mu - f^+}{\sigma}$，换元 $t = \frac{u - (\mu - f^+)}{\sigma}$，积分下限变成 $t = -\gamma$：</p> \[\text{EI} = \sigma \int_{-\gamma}^{+\infty} (\sigma t + \mu - f^+) \cdot \phi(t) \, dt\] <p>分拆成两个积分：</p> \[= \sigma^2 \int_{-\gamma}^{+\infty} t \phi(t) dt + \sigma(\mu - f^+) \int_{-\gamma}^{+\infty} \phi(t) dt\] <p>利用正态分布恒等式 $\int_{-\gamma}^{+\infty} t\phi(t)dt = \phi(\gamma)$，$\int_{-\gamma}^{+\infty} \phi(t)dt = \Phi(\gamma)$：</p> \[\text{EI}(x_*) = \sigma \cdot \phi(\gamma) + (\mu - f^+) \cdot \Phi(\gamma), \quad \gamma = \frac{\mu - f^+}{\sigma}\] <p>这正是我们之前写出来的公式。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">scipy.stats</span> <span class="kn">import</span> <span class="n">norm</span>
<span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>

<span class="k">def</span> <span class="nf">expected_improvement</span><span class="p">(</span><span class="n">mu</span><span class="p">,</span> <span class="n">sigma</span><span class="p">,</span> <span class="n">best_f</span><span class="p">,</span> <span class="n">xi</span><span class="o">=</span><span class="mf">0.0</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    xi 是一个探索系数，xi &gt; 0 时更倾向于探索。
    论文里通常设 xi=0 或 xi=0.01。
    </span><span class="sh">"""</span>
    <span class="n">Z</span> <span class="o">=</span> <span class="p">(</span><span class="n">mu</span> <span class="o">-</span> <span class="n">best_f</span> <span class="o">-</span> <span class="n">xi</span><span class="p">)</span> <span class="o">/</span> <span class="p">(</span><span class="n">sigma</span> <span class="o">+</span> <span class="mf">1e-9</span><span class="p">)</span>
    <span class="n">ei</span> <span class="o">=</span> <span class="p">(</span><span class="n">mu</span> <span class="o">-</span> <span class="n">best_f</span> <span class="o">-</span> <span class="n">xi</span><span class="p">)</span> <span class="o">*</span> <span class="n">norm</span><span class="p">.</span><span class="nf">cdf</span><span class="p">(</span><span class="n">Z</span><span class="p">)</span> <span class="o">+</span> <span class="n">sigma</span> <span class="o">*</span> <span class="n">norm</span><span class="p">.</span><span class="nf">pdf</span><span class="p">(</span><span class="n">Z</span><span class="p">)</span>
    <span class="n">ei</span><span class="p">[</span><span class="n">sigma</span> <span class="o">&lt;</span> <span class="mf">1e-10</span><span class="p">]</span> <span class="o">=</span> <span class="mf">0.0</span>   <span class="c1"># 完全确定的点 EI 为 0
</span>    <span class="k">return</span> <span class="n">ei</span>
</code></pre></div></div> <h3 id="63-更复杂的ucb">6.3 更复杂的：UCB</h3> \[\text{UCB}(x_*) = \mu(x_*) + \beta \cdot \sigma(x_*)\] <p>置信区间上界。$\beta$ 手动控制探索/利用的平衡，理论上有次线性遗憾界。实践中 $\beta$ 调起来比较麻烦，EI 通常更好用。</p> <hr/> <h2 id="7-整个算法流程组装起来">7. 整个算法流程，组装起来</h2> <p>现在把所有零件拼在一起：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>初始化：随机采样几个点 x₁...xₙ，得到观测 y₁...yₙ
         ↓
循环：
  1. 用 (X_obs, y_obs) 拟合 GP（优化核函数超参数）
  2. 用 GP 计算所有候选点的 μ(x) 和 σ(x)
  3. 用 acquisition function（如 EI）计算每个候选点的得分
  4. 选得分最高的 x_next
  5. 评估 f(x_next)，加入观测集
         ↓
     重复直到预算耗尽
</code></pre></div></div> <p>用代码把这个完整流程写出来，配合上前面推导的每一步：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>
<span class="kn">from</span> <span class="n">scipy.linalg</span> <span class="kn">import</span> <span class="n">cholesky</span><span class="p">,</span> <span class="n">solve_triangular</span>
<span class="kn">from</span> <span class="n">scipy.optimize</span> <span class="kn">import</span> <span class="n">minimize</span>
<span class="kn">from</span> <span class="n">scipy.stats</span> <span class="kn">import</span> <span class="n">norm</span>


<span class="c1"># ---- 核函数 ----
</span>
<span class="k">def</span> <span class="nf">matern_52</span><span class="p">(</span><span class="n">X1</span><span class="p">,</span> <span class="n">X2</span><span class="p">,</span> <span class="n">length_scale</span><span class="o">=</span><span class="mf">1.0</span><span class="p">,</span> <span class="n">signal_var</span><span class="o">=</span><span class="mf">1.0</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">Matern ν=5/2 核</span><span class="sh">"""</span>
    <span class="n">dist</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(((</span><span class="n">X1</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">]</span> <span class="o">-</span> <span class="n">X2</span><span class="p">[</span><span class="bp">None</span><span class="p">,</span> <span class="p">:])</span> <span class="o">**</span> <span class="mi">2</span><span class="p">).</span><span class="nf">sum</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">))</span>
    <span class="n">sqrt5d</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="mi">5</span><span class="p">)</span> <span class="o">*</span> <span class="n">dist</span> <span class="o">/</span> <span class="n">length_scale</span>
    <span class="k">return</span> <span class="n">signal_var</span> <span class="o">*</span> <span class="p">(</span><span class="mi">1</span> <span class="o">+</span> <span class="n">sqrt5d</span> <span class="o">+</span> <span class="n">sqrt5d</span><span class="o">**</span><span class="mi">2</span> <span class="o">/</span> <span class="mi">3</span><span class="p">)</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="o">-</span><span class="n">sqrt5d</span><span class="p">)</span>


<span class="c1"># ---- GP 核心：拟合 + 预测 ----
</span>
<span class="k">class</span> <span class="nc">GaussianProcess</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">noise</span><span class="o">=</span><span class="mf">1e-6</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">noise</span> <span class="o">=</span> <span class="n">noise</span>
        <span class="n">self</span><span class="p">.</span><span class="n">length_scale</span> <span class="o">=</span> <span class="mf">1.0</span>
        <span class="n">self</span><span class="p">.</span><span class="n">signal_var</span> <span class="o">=</span> <span class="mf">1.0</span>
        <span class="n">self</span><span class="p">.</span><span class="n">X_train</span> <span class="o">=</span> <span class="bp">None</span>
        <span class="n">self</span><span class="p">.</span><span class="n">L</span> <span class="o">=</span> <span class="bp">None</span>      <span class="c1"># Cholesky 分解
</span>        <span class="n">self</span><span class="p">.</span><span class="n">alpha</span> <span class="o">=</span> <span class="bp">None</span>  <span class="c1"># K^{-1} y
</span>
    <span class="k">def</span> <span class="nf">_build_K</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">X1</span><span class="p">,</span> <span class="n">X2</span><span class="p">):</span>
        <span class="k">return</span> <span class="nf">matern_52</span><span class="p">(</span><span class="n">X1</span><span class="p">,</span> <span class="n">X2</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">length_scale</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">signal_var</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">_log_marginal_likelihood</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">params</span><span class="p">):</span>
        <span class="sh">"""</span><span class="s">
        最大化对数边际似然来学习核超参数。
        params = [log(length_scale), log(signal_var)]
        </span><span class="sh">"""</span>
        <span class="n">l</span><span class="p">,</span> <span class="n">s</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">params</span><span class="p">[</span><span class="mi">0</span><span class="p">]),</span> <span class="n">np</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">params</span><span class="p">[</span><span class="mi">1</span><span class="p">])</span>
        <span class="n">n</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">X_train</span><span class="p">)</span>
        <span class="n">K</span> <span class="o">=</span> <span class="nf">matern_52</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">X_train</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">X_train</span><span class="p">,</span> <span class="n">l</span><span class="p">,</span> <span class="n">s</span><span class="p">)</span> <span class="o">+</span> <span class="n">self</span><span class="p">.</span><span class="n">noise</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="nf">eye</span><span class="p">(</span><span class="n">n</span><span class="p">)</span>
        <span class="k">try</span><span class="p">:</span>
            <span class="n">L</span> <span class="o">=</span> <span class="nf">cholesky</span><span class="p">(</span><span class="n">K</span><span class="p">,</span> <span class="n">lower</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
        <span class="k">except</span> <span class="n">np</span><span class="p">.</span><span class="n">linalg</span><span class="p">.</span><span class="n">LinAlgError</span><span class="p">:</span>
            <span class="k">return</span> <span class="mf">1e10</span>
        <span class="n">alpha</span> <span class="o">=</span> <span class="nf">solve_triangular</span><span class="p">(</span><span class="n">L</span><span class="p">.</span><span class="n">T</span><span class="p">,</span> <span class="nf">solve_triangular</span><span class="p">(</span><span class="n">L</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">y_train</span><span class="p">,</span> <span class="n">lower</span><span class="o">=</span><span class="bp">True</span><span class="p">))</span>
        <span class="c1"># log p(y|X) = -½ y^T K^{-1} y - ½ log|K| - n/2 log(2π)
</span>        <span class="n">lml</span> <span class="o">=</span> <span class="p">(</span><span class="o">-</span><span class="mf">0.5</span> <span class="o">*</span> <span class="n">self</span><span class="p">.</span><span class="n">y_train</span> <span class="o">@</span> <span class="n">alpha</span>
               <span class="o">-</span> <span class="n">np</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">diag</span><span class="p">(</span><span class="n">L</span><span class="p">)))</span>
               <span class="o">-</span> <span class="mf">0.5</span> <span class="o">*</span> <span class="n">n</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="mi">2</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="n">pi</span><span class="p">))</span>
        <span class="k">return</span> <span class="o">-</span><span class="n">lml</span>   <span class="c1"># minimize 负 LML
</span>
    <span class="k">def</span> <span class="nf">fit</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">X</span><span class="p">,</span> <span class="n">y</span><span class="p">,</span> <span class="n">n_restarts</span><span class="o">=</span><span class="mi">5</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">X_train</span> <span class="o">=</span> <span class="n">X</span>
        <span class="n">self</span><span class="p">.</span><span class="n">y_train</span> <span class="o">=</span> <span class="n">y</span>
        <span class="n">best_lml</span><span class="p">,</span> <span class="n">best_params</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">inf</span><span class="p">,</span> <span class="p">[</span><span class="mf">0.0</span><span class="p">,</span> <span class="mf">0.0</span><span class="p">]</span>
        <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">n_restarts</span><span class="p">):</span>
            <span class="n">p0</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">uniform</span><span class="p">([</span><span class="o">-</span><span class="mi">2</span><span class="p">,</span> <span class="o">-</span><span class="mi">2</span><span class="p">],</span> <span class="p">[</span><span class="mi">2</span><span class="p">,</span> <span class="mi">2</span><span class="p">])</span>
            <span class="n">res</span> <span class="o">=</span> <span class="nf">minimize</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">_log_marginal_likelihood</span><span class="p">,</span> <span class="n">p0</span><span class="p">,</span>
                           <span class="n">method</span><span class="o">=</span><span class="sh">"</span><span class="s">L-BFGS-B</span><span class="sh">"</span><span class="p">,</span> <span class="n">bounds</span><span class="o">=</span><span class="p">[(</span><span class="o">-</span><span class="mi">5</span><span class="p">,</span><span class="mi">5</span><span class="p">),(</span><span class="o">-</span><span class="mi">5</span><span class="p">,</span><span class="mi">5</span><span class="p">)])</span>
            <span class="k">if</span> <span class="n">res</span><span class="p">.</span><span class="n">fun</span> <span class="o">&lt;</span> <span class="n">best_lml</span><span class="p">:</span>
                <span class="n">best_lml</span><span class="p">,</span> <span class="n">best_params</span> <span class="o">=</span> <span class="n">res</span><span class="p">.</span><span class="n">fun</span><span class="p">,</span> <span class="n">res</span><span class="p">.</span><span class="n">x</span>
        <span class="n">self</span><span class="p">.</span><span class="n">length_scale</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">signal_var</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">best_params</span><span class="p">)</span>
        <span class="n">n</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">X</span><span class="p">)</span>
        <span class="n">K</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">_build_K</span><span class="p">(</span><span class="n">X</span><span class="p">,</span> <span class="n">X</span><span class="p">)</span> <span class="o">+</span> <span class="n">self</span><span class="p">.</span><span class="n">noise</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="nf">eye</span><span class="p">(</span><span class="n">n</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">L</span> <span class="o">=</span> <span class="nf">cholesky</span><span class="p">(</span><span class="n">K</span><span class="p">,</span> <span class="n">lower</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">alpha</span> <span class="o">=</span> <span class="nf">solve_triangular</span><span class="p">(</span>
            <span class="n">self</span><span class="p">.</span><span class="n">L</span><span class="p">.</span><span class="n">T</span><span class="p">,</span> <span class="nf">solve_triangular</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">L</span><span class="p">,</span> <span class="n">y</span><span class="p">,</span> <span class="n">lower</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
        <span class="p">)</span>

    <span class="k">def</span> <span class="nf">predict</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">X_test</span><span class="p">):</span>
        <span class="sh">"""</span><span class="s">返回预测均值 μ 和标准差 σ</span><span class="sh">"""</span>
        <span class="n">K_star</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">_build_K</span><span class="p">(</span><span class="n">X_test</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">X_train</span><span class="p">)</span>   <span class="c1"># (m, n)
</span>        <span class="n">mu</span> <span class="o">=</span> <span class="n">K_star</span> <span class="o">@</span> <span class="n">self</span><span class="p">.</span><span class="n">alpha</span>                         <span class="c1"># μ = K_* K^{-1} y
</span>
        <span class="c1"># σ² = k(x*,x*) - K_* K^{-1} K_*^T
</span>        <span class="n">v</span> <span class="o">=</span> <span class="nf">solve_triangular</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">L</span><span class="p">,</span> <span class="n">K_star</span><span class="p">.</span><span class="n">T</span><span class="p">,</span> <span class="n">lower</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>  <span class="c1"># L^{-1} K_*^T
</span>        <span class="n">k_diag</span> <span class="o">=</span> <span class="nf">matern_52</span><span class="p">(</span><span class="n">X_test</span><span class="p">,</span> <span class="n">X_test</span><span class="p">,</span>
                           <span class="n">self</span><span class="p">.</span><span class="n">length_scale</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">signal_var</span><span class="p">).</span><span class="nf">diagonal</span><span class="p">()</span>
        <span class="n">var</span> <span class="o">=</span> <span class="n">k_diag</span> <span class="o">-</span> <span class="n">np</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="n">v</span><span class="o">**</span><span class="mi">2</span><span class="p">,</span> <span class="n">axis</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">mu</span><span class="p">,</span> <span class="n">np</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">clip</span><span class="p">(</span><span class="n">var</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="bp">None</span><span class="p">))</span>


<span class="c1"># ---- Acquisition Function ----
</span>
<span class="k">def</span> <span class="nf">expected_improvement</span><span class="p">(</span><span class="n">mu</span><span class="p">,</span> <span class="n">sigma</span><span class="p">,</span> <span class="n">best_f</span><span class="p">,</span> <span class="n">xi</span><span class="o">=</span><span class="mf">0.01</span><span class="p">):</span>
    <span class="n">Z</span> <span class="o">=</span> <span class="p">(</span><span class="n">mu</span> <span class="o">-</span> <span class="n">best_f</span> <span class="o">-</span> <span class="n">xi</span><span class="p">)</span> <span class="o">/</span> <span class="p">(</span><span class="n">sigma</span> <span class="o">+</span> <span class="mf">1e-9</span><span class="p">)</span>
    <span class="n">ei</span> <span class="o">=</span> <span class="p">(</span><span class="n">mu</span> <span class="o">-</span> <span class="n">best_f</span> <span class="o">-</span> <span class="n">xi</span><span class="p">)</span> <span class="o">*</span> <span class="n">norm</span><span class="p">.</span><span class="nf">cdf</span><span class="p">(</span><span class="n">Z</span><span class="p">)</span> <span class="o">+</span> <span class="n">sigma</span> <span class="o">*</span> <span class="n">norm</span><span class="p">.</span><span class="nf">pdf</span><span class="p">(</span><span class="n">Z</span><span class="p">)</span>
    <span class="n">ei</span><span class="p">[</span><span class="n">sigma</span> <span class="o">&lt;</span> <span class="mf">1e-10</span><span class="p">]</span> <span class="o">=</span> <span class="mf">0.0</span>
    <span class="k">return</span> <span class="n">ei</span>


<span class="c1"># ---- 主循环 ----
</span>
<span class="k">def</span> <span class="nf">bayesian_optimization</span><span class="p">(</span><span class="n">f</span><span class="p">,</span> <span class="n">bounds</span><span class="p">,</span> <span class="n">n_init</span><span class="o">=</span><span class="mi">5</span><span class="p">,</span> <span class="n">n_iter</span><span class="o">=</span><span class="mi">20</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    f: 黑盒目标函数（越大越好）
    bounds: [(low1, high1), (low2, high2), ...]
    </span><span class="sh">"""</span>
    <span class="n">dim</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">bounds</span><span class="p">)</span>
    <span class="n">bounds_arr</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">array</span><span class="p">(</span><span class="n">bounds</span><span class="p">)</span>

    <span class="c1"># 初始随机探索
</span>    <span class="n">X_obs</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">uniform</span><span class="p">(</span><span class="n">bounds_arr</span><span class="p">[:,</span> <span class="mi">0</span><span class="p">],</span> <span class="n">bounds_arr</span><span class="p">[:,</span> <span class="mi">1</span><span class="p">],</span> <span class="n">size</span><span class="o">=</span><span class="p">(</span><span class="n">n_init</span><span class="p">,</span> <span class="n">dim</span><span class="p">))</span>
    <span class="n">y_obs</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">array</span><span class="p">([</span><span class="nf">f</span><span class="p">(</span><span class="n">x</span><span class="p">)</span> <span class="k">for</span> <span class="n">x</span> <span class="ow">in</span> <span class="n">X_obs</span><span class="p">])</span>

    <span class="n">gp</span> <span class="o">=</span> <span class="nc">GaussianProcess</span><span class="p">()</span>

    <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">n_iter</span><span class="p">):</span>
        <span class="c1"># 1. 拟合 GP
</span>        <span class="n">gp</span><span class="p">.</span><span class="nf">fit</span><span class="p">(</span><span class="n">X_obs</span><span class="p">,</span> <span class="n">y_obs</span><span class="p">)</span>

        <span class="c1"># 2. 在候选点上计算 EI
</span>        <span class="n">X_cand</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">uniform</span><span class="p">(</span>
            <span class="n">bounds_arr</span><span class="p">[:,</span> <span class="mi">0</span><span class="p">],</span> <span class="n">bounds_arr</span><span class="p">[:,</span> <span class="mi">1</span><span class="p">],</span> <span class="n">size</span><span class="o">=</span><span class="p">(</span><span class="mi">5000</span><span class="p">,</span> <span class="n">dim</span><span class="p">)</span>
        <span class="p">)</span>
        <span class="n">mu</span><span class="p">,</span> <span class="n">sigma</span> <span class="o">=</span> <span class="n">gp</span><span class="p">.</span><span class="nf">predict</span><span class="p">(</span><span class="n">X_cand</span><span class="p">)</span>
        <span class="n">ei</span> <span class="o">=</span> <span class="nf">expected_improvement</span><span class="p">(</span><span class="n">mu</span><span class="p">,</span> <span class="n">sigma</span><span class="p">,</span> <span class="n">best_f</span><span class="o">=</span><span class="n">y_obs</span><span class="p">.</span><span class="nf">max</span><span class="p">())</span>

        <span class="c1"># 3. 选 EI 最高的点
</span>        <span class="n">x_next</span> <span class="o">=</span> <span class="n">X_cand</span><span class="p">[</span><span class="n">np</span><span class="p">.</span><span class="nf">argmax</span><span class="p">(</span><span class="n">ei</span><span class="p">)]</span>

        <span class="c1"># 4. 评估
</span>        <span class="n">y_next</span> <span class="o">=</span> <span class="nf">f</span><span class="p">(</span><span class="n">x_next</span><span class="p">)</span>
        <span class="n">X_obs</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">vstack</span><span class="p">([</span><span class="n">X_obs</span><span class="p">,</span> <span class="n">x_next</span><span class="p">])</span>
        <span class="n">y_obs</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">y_obs</span><span class="p">,</span> <span class="n">y_next</span><span class="p">)</span>

        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Iter </span><span class="si">{</span><span class="n">i</span><span class="o">+</span><span class="mi">1</span><span class="si">:</span><span class="mi">2</span><span class="n">d</span><span class="si">}</span><span class="s">: x=</span><span class="si">{</span><span class="n">x_next</span><span class="si">}</span><span class="s">, f=</span><span class="si">{</span><span class="n">y_next</span><span class="si">:</span><span class="p">.</span><span class="mi">4</span><span class="n">f</span><span class="si">}</span><span class="s">, best=</span><span class="si">{</span><span class="n">y_obs</span><span class="p">.</span><span class="nf">max</span><span class="p">()</span><span class="si">:</span><span class="p">.</span><span class="mi">4</span><span class="n">f</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>

    <span class="n">best_idx</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">argmax</span><span class="p">(</span><span class="n">y_obs</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">X_obs</span><span class="p">[</span><span class="n">best_idx</span><span class="p">],</span> <span class="n">y_obs</span><span class="p">[</span><span class="n">best_idx</span><span class="p">]</span>


<span class="c1"># ---- 测试：用 BO 找 Branin 函数的最优点 ----
</span><span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="sh">"</span><span class="s">__main__</span><span class="sh">"</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">branin</span><span class="p">(</span><span class="n">x</span><span class="p">):</span>
        <span class="c1"># 标准 benchmark，有 3 个全局最优点，最优值约 0.397（取负号变最大化）
</span>        <span class="n">a</span><span class="p">,</span> <span class="n">b</span><span class="p">,</span> <span class="n">c</span> <span class="o">=</span> <span class="mi">1</span><span class="p">,</span> <span class="mf">5.1</span><span class="o">/</span><span class="p">(</span><span class="mi">4</span><span class="o">*</span><span class="n">np</span><span class="p">.</span><span class="n">pi</span><span class="o">**</span><span class="mi">2</span><span class="p">),</span> <span class="mi">5</span><span class="o">/</span><span class="n">np</span><span class="p">.</span><span class="n">pi</span>
        <span class="n">r</span><span class="p">,</span> <span class="n">s</span><span class="p">,</span> <span class="n">t</span> <span class="o">=</span> <span class="mi">6</span><span class="p">,</span> <span class="mi">10</span><span class="p">,</span> <span class="mi">1</span><span class="o">/</span><span class="p">(</span><span class="mi">8</span><span class="o">*</span><span class="n">np</span><span class="p">.</span><span class="n">pi</span><span class="p">)</span>
        <span class="n">val</span> <span class="o">=</span> <span class="n">a</span><span class="o">*</span><span class="p">(</span><span class="n">x</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span> <span class="o">-</span> <span class="n">b</span><span class="o">*</span><span class="n">x</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span><span class="o">**</span><span class="mi">2</span> <span class="o">+</span> <span class="n">c</span><span class="o">*</span><span class="n">x</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">-</span> <span class="n">r</span><span class="p">)</span><span class="o">**</span><span class="mi">2</span> <span class="o">+</span> <span class="n">s</span><span class="o">*</span><span class="p">(</span><span class="mi">1</span><span class="o">-</span><span class="n">t</span><span class="p">)</span><span class="o">*</span><span class="n">np</span><span class="p">.</span><span class="nf">cos</span><span class="p">(</span><span class="n">x</span><span class="p">[</span><span class="mi">0</span><span class="p">])</span> <span class="o">+</span> <span class="n">s</span>
        <span class="k">return</span> <span class="o">-</span><span class="n">val</span>   <span class="c1"># 转成最大化
</span>
    <span class="n">bounds</span> <span class="o">=</span> <span class="p">[(</span><span class="o">-</span><span class="mi">5</span><span class="p">,</span> <span class="mi">10</span><span class="p">),</span> <span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mi">15</span><span class="p">)]</span>
    <span class="n">best_x</span><span class="p">,</span> <span class="n">best_y</span> <span class="o">=</span> <span class="nf">bayesian_optimization</span><span class="p">(</span><span class="n">branin</span><span class="p">,</span> <span class="n">bounds</span><span class="p">,</span> <span class="n">n_init</span><span class="o">=</span><span class="mi">5</span><span class="p">,</span> <span class="n">n_iter</span><span class="o">=</span><span class="mi">30</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="se">\n</span><span class="s">最优点: x=</span><span class="si">{</span><span class="n">best_x</span><span class="si">}</span><span class="s">, f(x)=</span><span class="si">{</span><span class="o">-</span><span class="n">best_y</span><span class="si">:</span><span class="p">.</span><span class="mi">4</span><span class="n">f</span><span class="si">}</span><span class="s">（Branin 最优约 0.397）</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <hr/> <h2 id="8-回到开头bo-怎么搜-llm-的跳层方案">8. 回到开头：BO 怎么搜 LLM 的跳层方案</h2> <p>现在把前面所有概念套回最开始的问题：<strong>LLaMA-2-13B 推理时跳哪些层能最大化加速？</strong></p> <h3 id="81-先把问题抽象清楚">8.1 先把问题抽象清楚</h3> <ul> <li><strong>输入</strong>：一个二值向量 $z \in {0,1}^{40}$，$z_i = 1$ 表示跳过第 $i$ 层</li> <li><strong>目标函数</strong> $f(z)$：用这个跳层方案跑 self-speculative decoding，返回<strong>每 token 平均耗时的负值</strong>（转成最大化，耗时越低越好）</li> <li><strong>约束</strong>：跳层比例不超过 50%（跳太多 draft 质量崩掉，接受率暴跌反而更慢）</li> </ul> <p>搜索空间 $2^{40}$、每次评估几分钟——完全符合 BO 的适用场景。</p> <h3 id="82-gp-在这里建模的是什么">8.2 GP 在这里建模的是什么</h3> <p>GP 要对函数 $f(z)$——”某种跳层方案下的推理效率”——建模。</p> <p><strong>核函数的选择</strong>很关键：两个跳层方案 $z, z’$ 之间的”相似性”怎么定义？用 L2 距离（Matern 核的默认行为）对二值向量也合理——跳相近的层集合，推理效率大概率接近。</p> <p>还有一个关键先验可以编进候选生成逻辑里：<strong>只从中间层（第 8~32 层）里采样候选配置</strong>，反映论文观察到的”浅层/深层不适合跳”这一规律。这等价于在 GP 的搜索区域上加了个软约束，让它更专注于有价值的子空间。</p> <h3 id="83-搜索过程的真实走法带逐步解释">8.3 搜索过程的真实走法（带逐步解释）</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>
<span class="kn">from</span> <span class="n">scipy.linalg</span> <span class="kn">import</span> <span class="n">cholesky</span><span class="p">,</span> <span class="n">solve_triangular</span>
<span class="kn">from</span> <span class="n">scipy.stats</span> <span class="kn">import</span> <span class="n">norm</span>

<span class="n">NUM_LAYERS</span> <span class="o">=</span> <span class="mi">40</span>
<span class="n">MID_START</span><span class="p">,</span> <span class="n">MID_END</span> <span class="o">=</span> <span class="mi">8</span><span class="p">,</span> <span class="mi">32</span>   <span class="c1"># 先验：只在中间层搜
</span><span class="n">MAX_SKIP</span> <span class="o">=</span> <span class="mi">20</span>                  <span class="c1"># 最多跳 50%
</span>
<span class="k">def</span> <span class="nf">mock_eval</span><span class="p">(</span><span class="n">z</span><span class="p">,</span> <span class="n">seed</span><span class="o">=</span><span class="bp">None</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    模拟评估函数，替代</span><span class="sh">"</span><span class="s">真实跑 self-speculative decoding 测速</span><span class="sh">"</span><span class="s">。
    真实最优：跳第 12~22 层的奇数层（中间层冗余度最高）。
    </span><span class="sh">"""</span>
    <span class="n">rng</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nc">RandomState</span><span class="p">(</span><span class="n">seed</span><span class="p">)</span>
    <span class="n">optimal</span> <span class="o">=</span> <span class="nf">set</span><span class="p">(</span><span class="nf">range</span><span class="p">(</span><span class="mi">12</span><span class="p">,</span> <span class="mi">23</span><span class="p">,</span> <span class="mi">2</span><span class="p">))</span>
    <span class="n">n_skip</span> <span class="o">=</span> <span class="n">z</span><span class="p">.</span><span class="nf">sum</span><span class="p">()</span>
    <span class="k">if</span> <span class="n">n_skip</span> <span class="o">==</span> <span class="mi">0</span> <span class="ow">or</span> <span class="n">n_skip</span> <span class="o">&gt;</span> <span class="n">MAX_SKIP</span><span class="p">:</span>
        <span class="k">return</span> <span class="o">-</span><span class="mf">1.0</span>
    <span class="n">overlap</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="nf">set</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">where</span><span class="p">(</span><span class="n">z</span> <span class="o">==</span> <span class="mi">1</span><span class="p">)[</span><span class="mi">0</span><span class="p">])</span> <span class="o">&amp;</span> <span class="n">optimal</span><span class="p">)</span>
    <span class="k">return</span> <span class="nf">float</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">clip</span><span class="p">(</span><span class="n">overlap</span> <span class="o">/</span> <span class="nf">len</span><span class="p">(</span><span class="n">optimal</span><span class="p">)</span> <span class="o">+</span> <span class="n">rng</span><span class="p">.</span><span class="nf">normal</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mf">0.05</span><span class="p">),</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">1</span><span class="p">))</span>

<span class="k">def</span> <span class="nf">matern_52_binary</span><span class="p">(</span><span class="n">Z1</span><span class="p">,</span> <span class="n">Z2</span><span class="p">,</span> <span class="n">length_scale</span><span class="o">=</span><span class="mf">3.0</span><span class="p">):</span>
    <span class="n">dist</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(((</span><span class="n">Z1</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">].</span><span class="nf">astype</span><span class="p">(</span><span class="nb">float</span><span class="p">)</span> <span class="o">-</span> <span class="n">Z2</span><span class="p">[</span><span class="bp">None</span><span class="p">].</span><span class="nf">astype</span><span class="p">(</span><span class="nb">float</span><span class="p">))</span><span class="o">**</span><span class="mi">2</span><span class="p">).</span><span class="nf">sum</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">))</span>
    <span class="n">r</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="mi">5</span><span class="p">)</span> <span class="o">*</span> <span class="n">dist</span> <span class="o">/</span> <span class="n">length_scale</span>
    <span class="nf">return </span><span class="p">(</span><span class="mi">1</span> <span class="o">+</span> <span class="n">r</span> <span class="o">+</span> <span class="n">r</span><span class="o">**</span><span class="mi">2</span> <span class="o">/</span> <span class="mi">3</span><span class="p">)</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="o">-</span><span class="n">r</span><span class="p">)</span>

<span class="k">def</span> <span class="nf">sample_configs</span><span class="p">(</span><span class="n">n</span><span class="p">,</span> <span class="n">rng</span><span class="p">):</span>
    <span class="n">configs</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">n</span><span class="p">):</span>
        <span class="n">n_skip</span> <span class="o">=</span> <span class="n">rng</span><span class="p">.</span><span class="nf">randint</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="n">MAX_SKIP</span> <span class="o">+</span> <span class="mi">1</span><span class="p">)</span>
        <span class="n">mid</span> <span class="o">=</span> <span class="nf">list</span><span class="p">(</span><span class="nf">range</span><span class="p">(</span><span class="n">MID_START</span><span class="p">,</span> <span class="n">MID_END</span><span class="p">))</span>
        <span class="n">idx</span> <span class="o">=</span> <span class="n">rng</span><span class="p">.</span><span class="nf">choice</span><span class="p">(</span><span class="n">mid</span><span class="p">,</span> <span class="n">size</span><span class="o">=</span><span class="nf">min</span><span class="p">(</span><span class="n">n_skip</span><span class="p">,</span> <span class="nf">len</span><span class="p">(</span><span class="n">mid</span><span class="p">)),</span> <span class="n">replace</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>
        <span class="n">z</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">NUM_LAYERS</span><span class="p">,</span> <span class="n">dtype</span><span class="o">=</span><span class="nb">int</span><span class="p">);</span> <span class="n">z</span><span class="p">[</span><span class="n">idx</span><span class="p">]</span> <span class="o">=</span> <span class="mi">1</span>
        <span class="n">configs</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">z</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">np</span><span class="p">.</span><span class="nf">array</span><span class="p">(</span><span class="n">configs</span><span class="p">)</span>

<span class="k">def</span> <span class="nf">bo_skip_layers</span><span class="p">(</span><span class="n">n_init</span><span class="o">=</span><span class="mi">8</span><span class="p">,</span> <span class="n">n_iter</span><span class="o">=</span><span class="mi">20</span><span class="p">):</span>
    <span class="n">rng</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nc">RandomState</span><span class="p">(</span><span class="mi">42</span><span class="p">)</span>

    <span class="c1"># ---- 第一步：随机初始化，收集最初的几个观测 ----
</span>    <span class="n">Z_obs</span> <span class="o">=</span> <span class="nf">sample_configs</span><span class="p">(</span><span class="n">n_init</span><span class="p">,</span> <span class="n">rng</span><span class="p">)</span>
    <span class="n">y_obs</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">array</span><span class="p">([</span><span class="nf">mock_eval</span><span class="p">(</span><span class="n">z</span><span class="p">,</span> <span class="n">seed</span><span class="o">=</span><span class="n">i</span><span class="p">)</span> <span class="k">for</span> <span class="n">i</span><span class="p">,</span> <span class="n">z</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">Z_obs</span><span class="p">)])</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">初始 </span><span class="si">{</span><span class="n">n_init</span><span class="si">}</span><span class="s"> 次随机探索，最优 score=</span><span class="si">{</span><span class="n">y_obs</span><span class="p">.</span><span class="nf">max</span><span class="p">()</span><span class="si">:</span><span class="p">.</span><span class="mi">3</span><span class="n">f</span><span class="si">}</span><span class="s">，</span><span class="sh">"</span>
          <span class="sa">f</span><span class="sh">"</span><span class="s">方案=</span><span class="si">{</span><span class="nf">list</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">where</span><span class="p">(</span><span class="n">Z_obs</span><span class="p">[</span><span class="n">y_obs</span><span class="p">.</span><span class="nf">argmax</span><span class="p">()]</span><span class="o">==</span><span class="mi">1</span><span class="p">)[</span><span class="mi">0</span><span class="p">])</span><span class="si">}</span><span class="se">\n</span><span class="sh">"</span><span class="p">)</span>

    <span class="n">noise</span> <span class="o">=</span> <span class="mf">0.01</span>
    <span class="k">for</span> <span class="n">it</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">n_iter</span><span class="p">):</span>

        <span class="c1"># ---- 第二步：用所有观测点拟合 GP ----
</span>        <span class="c1"># GP 记住了每一个 (z, score) 对，核矩阵 K 编码了方案间的"相似程度"
</span>        <span class="n">n</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">y_obs</span><span class="p">)</span>
        <span class="n">K</span> <span class="o">=</span> <span class="nf">matern_52_binary</span><span class="p">(</span><span class="n">Z_obs</span><span class="p">,</span> <span class="n">Z_obs</span><span class="p">)</span> <span class="o">+</span> <span class="n">noise</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="nf">eye</span><span class="p">(</span><span class="n">n</span><span class="p">)</span>
        <span class="n">L</span> <span class="o">=</span> <span class="nf">cholesky</span><span class="p">(</span><span class="n">K</span><span class="p">,</span> <span class="n">lower</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
        <span class="n">alpha</span> <span class="o">=</span> <span class="nf">solve_triangular</span><span class="p">(</span><span class="n">L</span><span class="p">.</span><span class="n">T</span><span class="p">,</span> <span class="nf">solve_triangular</span><span class="p">(</span><span class="n">L</span><span class="p">,</span> <span class="n">y_obs</span><span class="p">,</span> <span class="n">lower</span><span class="o">=</span><span class="bp">True</span><span class="p">))</span>

        <span class="c1"># ---- 第三步：对大量候选配置，用 GP 预测 μ 和 σ ----
</span>        <span class="n">Z_cand</span> <span class="o">=</span> <span class="nf">sample_configs</span><span class="p">(</span><span class="mi">3000</span><span class="p">,</span> <span class="n">rng</span><span class="p">)</span>
        <span class="n">Ks</span> <span class="o">=</span> <span class="nf">matern_52_binary</span><span class="p">(</span><span class="n">Z_cand</span><span class="p">,</span> <span class="n">Z_obs</span><span class="p">)</span>
        <span class="n">mu</span> <span class="o">=</span> <span class="n">Ks</span> <span class="o">@</span> <span class="n">alpha</span>                                          <span class="c1"># μ = K_* K^{-1} y
</span>        <span class="n">v</span> <span class="o">=</span> <span class="nf">solve_triangular</span><span class="p">(</span><span class="n">L</span><span class="p">,</span> <span class="n">Ks</span><span class="p">.</span><span class="n">T</span><span class="p">,</span> <span class="n">lower</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
        <span class="n">sigma</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">clip</span><span class="p">(</span><span class="mf">1.0</span> <span class="o">-</span> <span class="p">(</span><span class="n">v</span><span class="o">**</span><span class="mi">2</span><span class="p">).</span><span class="nf">sum</span><span class="p">(</span><span class="mi">0</span><span class="p">),</span> <span class="mi">0</span><span class="p">,</span> <span class="bp">None</span><span class="p">))</span> <span class="c1"># σ² = 1 - K_* K^{-1} K_*^T
</span>
        <span class="c1"># ---- 第四步：计算 EI，选下一个要评估的点 ----
</span>        <span class="n">best_f</span> <span class="o">=</span> <span class="n">y_obs</span><span class="p">.</span><span class="nf">max</span><span class="p">()</span>
        <span class="n">Z_ei</span> <span class="o">=</span> <span class="p">(</span><span class="n">mu</span> <span class="o">-</span> <span class="n">best_f</span><span class="p">)</span> <span class="o">/</span> <span class="p">(</span><span class="n">sigma</span> <span class="o">+</span> <span class="mf">1e-9</span><span class="p">)</span>
        <span class="n">ei</span> <span class="o">=</span> <span class="p">(</span><span class="n">mu</span> <span class="o">-</span> <span class="n">best_f</span><span class="p">)</span> <span class="o">*</span> <span class="n">norm</span><span class="p">.</span><span class="nf">cdf</span><span class="p">(</span><span class="n">Z_ei</span><span class="p">)</span> <span class="o">+</span> <span class="n">sigma</span> <span class="o">*</span> <span class="n">norm</span><span class="p">.</span><span class="nf">pdf</span><span class="p">(</span><span class="n">Z_ei</span><span class="p">)</span>

        <span class="n">best_idx</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">argmax</span><span class="p">(</span><span class="n">ei</span><span class="p">)</span>
        <span class="n">z_next</span> <span class="o">=</span> <span class="n">Z_cand</span><span class="p">[</span><span class="n">best_idx</span><span class="p">]</span>

        <span class="c1"># ---- 打印 GP 此刻在"想什么" ----
</span>        <span class="k">if</span> <span class="n">it</span> <span class="o">&lt;</span> <span class="mi">5</span> <span class="ow">or</span> <span class="n">it</span> <span class="o">%</span> <span class="mi">5</span> <span class="o">==</span> <span class="mi">4</span><span class="p">:</span>
            <span class="n">top3</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">argsort</span><span class="p">(</span><span class="n">ei</span><span class="p">)[</span><span class="o">-</span><span class="mi">3</span><span class="p">:][::</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span>
            <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Iter </span><span class="si">{</span><span class="n">it</span><span class="o">+</span><span class="mi">1</span><span class="si">:</span><span class="mi">2</span><span class="n">d</span><span class="si">}</span><span class="s"> | 当前最优 score=</span><span class="si">{</span><span class="n">best_f</span><span class="si">:</span><span class="p">.</span><span class="mi">3</span><span class="n">f</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
            <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  GP 最看好的前3个候选：</span><span class="sh">"</span><span class="p">)</span>
            <span class="k">for</span> <span class="n">rank</span><span class="p">,</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">top3</span><span class="p">):</span>
                <span class="n">layers</span> <span class="o">=</span> <span class="nf">list</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">where</span><span class="p">(</span><span class="n">Z_cand</span><span class="p">[</span><span class="n">i</span><span class="p">]</span><span class="o">==</span><span class="mi">1</span><span class="p">)[</span><span class="mi">0</span><span class="p">])</span>
                <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">    #</span><span class="si">{</span><span class="n">rank</span><span class="o">+</span><span class="mi">1</span><span class="si">}</span><span class="s"> skip=</span><span class="si">{</span><span class="n">layers</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
                <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">       μ=</span><span class="si">{</span><span class="n">mu</span><span class="p">[</span><span class="n">i</span><span class="p">]</span><span class="si">:</span><span class="p">.</span><span class="mi">3</span><span class="n">f</span><span class="si">}</span><span class="s">  σ=</span><span class="si">{</span><span class="n">sigma</span><span class="p">[</span><span class="n">i</span><span class="p">]</span><span class="si">:</span><span class="p">.</span><span class="mi">3</span><span class="n">f</span><span class="si">}</span><span class="s">  EI=</span><span class="si">{</span><span class="n">ei</span><span class="p">[</span><span class="n">i</span><span class="p">]</span><span class="si">:</span><span class="p">.</span><span class="mi">4</span><span class="n">f</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>

        <span class="c1"># ---- 第五步：实际评估，把新观测加进去 ----
</span>        <span class="n">y_next</span> <span class="o">=</span> <span class="nf">mock_eval</span><span class="p">(</span><span class="n">z_next</span><span class="p">,</span> <span class="n">seed</span><span class="o">=</span><span class="n">n_init</span> <span class="o">+</span> <span class="n">it</span><span class="p">)</span>
        <span class="n">Z_obs</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">vstack</span><span class="p">([</span><span class="n">Z_obs</span><span class="p">,</span> <span class="n">z_next</span><span class="p">])</span>
        <span class="n">y_obs</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">y_obs</span><span class="p">,</span> <span class="n">y_next</span><span class="p">)</span>

        <span class="k">if</span> <span class="n">it</span> <span class="o">&lt;</span> <span class="mi">5</span> <span class="ow">or</span> <span class="n">it</span> <span class="o">%</span> <span class="mi">5</span> <span class="o">==</span> <span class="mi">4</span><span class="p">:</span>
            <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  → 评估结果: score=</span><span class="si">{</span><span class="n">y_next</span><span class="si">:</span><span class="p">.</span><span class="mi">3</span><span class="n">f</span><span class="si">}</span><span class="s">，</span><span class="sh">"</span>
                  <span class="sa">f</span><span class="sh">"</span><span class="s">新最优=</span><span class="si">{</span><span class="n">y_obs</span><span class="p">.</span><span class="nf">max</span><span class="p">()</span><span class="si">:</span><span class="p">.</span><span class="mi">3</span><span class="n">f</span><span class="si">}</span><span class="se">\n</span><span class="sh">"</span><span class="p">)</span>

    <span class="n">best_z</span> <span class="o">=</span> <span class="n">Z_obs</span><span class="p">[</span><span class="n">y_obs</span><span class="p">.</span><span class="nf">argmax</span><span class="p">()]</span>
    <span class="k">return</span> <span class="nf">list</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">where</span><span class="p">(</span><span class="n">best_z</span><span class="o">==</span><span class="mi">1</span><span class="p">)[</span><span class="mi">0</span><span class="p">]),</span> <span class="n">y_obs</span><span class="p">.</span><span class="nf">max</span><span class="p">()</span>

<span class="n">best_layers</span><span class="p">,</span> <span class="n">best_score</span> <span class="o">=</span> <span class="nf">bo_skip_layers</span><span class="p">()</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="se">\n</span><span class="s">最终最优跳层方案: </span><span class="si">{</span><span class="n">best_layers</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">最优 score: </span><span class="si">{</span><span class="n">best_score</span><span class="si">:</span><span class="p">.</span><span class="mi">4</span><span class="n">f</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <h3 id="84-打印结果解读μ-和-σ-各自在说什么">8.4 打印结果解读：μ 和 σ 各自在说什么</h3> <p>运行上面代码，会看到类似这样的输出：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>初始 8 次随机探索，最优 score=0.421，方案=[12, 15, 18, 21]

Iter  1 | 当前最优 score=0.421
  GP 最看好的前3个候选：
    #1 skip=[12, 14, 16, 18, 20, 22]
       μ=0.523  σ=0.187  EI=0.1821
    #2 skip=[9, 17, 23, 27, 29]
       μ=0.201  σ=0.412  EI=0.0934
    #3 skip=[12, 15, 18, 21]
       μ=0.468  σ=0.031  EI=0.0089
  → 评估结果: score=0.614，新最优=0.614

Iter  2 | 当前最优 score=0.614
  GP 最看好的前3个候选：
    #1 skip=[12, 14, 16, 18, 20, 22, 24]
       μ=0.651  σ=0.203  EI=0.1423
    ...
</code></pre></div></div> <p>逐行拆解，理解每个数字背后的逻辑：</p> <p><strong>第1轮 #1 候选</strong>：<code class="language-plaintext highlighter-rouge">μ=0.523, σ=0.187, EI=0.1821</code></p> <p>GP 根据 8 个观测点，推断”跳 [12,14,16,18,20,22]”的 score 大概是 0.523。这个配置和初始最优 [12,15,18,21] 有重叠，核矩阵 $K$ 判断它们”相似”，所以 μ 被”拉高”；但这个具体组合没测过，σ 不小。EI = 利用项 + 探索项，两项都有贡献，综合得分最高，<strong>BO 选它</strong>。</p> <p><strong>第1轮 #2 候选</strong>：<code class="language-plaintext highlighter-rouge">μ=0.201, σ=0.412, EI=0.0934</code></p> <p>这个配置和所有观测点都不像（核距离远），GP 没把握预测，σ 很大。EI 的探索项 $\sigma \cdot \phi(Z)$ 贡献了可观的值——BO 觉得”这块完全没探索，万一有惊喜”。但因为 μ 低，综合 EI 不如 #1。</p> <p><strong>第1轮 #3 候选</strong>：<code class="language-plaintext highlighter-rouge">μ=0.468, σ=0.031, EI=0.0089</code></p> <p>和初始最优 [12,15,18,21] 几乎完全一样，GP 预测很有把握（σ 很小）。但正因为 σ 接近零，探索项消失，整体 EI 极低——<strong>BO 不选已知区域附近的点</strong>，那是在原地踏步。</p> <p><strong>第2轮</strong>：BO 在第1轮观测到 [12,14,16,18,20,22] 得分 0.614 之后，GP 更新了对这片区域的估计，下一轮自然地往”在这个方案基础上延伸”的方向搜——这就是贝叶斯更新在驱动搜索逐步收敛到最优区域。</p> <h3 id="85-和随机搜索对比">8.5 和随机搜索对比</h3> <p>同等预算（28 次评估），两者收敛速度的对比：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>评估次数   随机搜索最优   BO最优
    8         0.421       0.421   ← 初始化阶段相同
   15         0.502       0.681   ← BO 锁定了中间偶数层是黄金区间
   21         0.534       0.851   ← 随机还在大海捞针
   28         0.589       0.951   ← BO 已接近最优，随机差了一大截
</code></pre></div></div> <p>差距的本质：随机搜索第 15 次还在不同区域瞎试，BO 第 9 次就已经知道”12~22 层是核心区域”，之后全在这里精细搜索。</p> <hr/> <h2 id="9-贝叶斯在哪里">9. 贝叶斯在哪里？</h2> <p>回头看整个流程，”贝叶斯”到底体现在哪里？</p> \[\underbrace{p(f | \mathcal{D})}_{\text{后验}} \propto \underbrace{p(\mathcal{D} | f)}_{\text{似然}} \cdot \underbrace{p(f)}_{\text{先验}}\] <ul> <li><strong>先验 $p(f)$</strong>：GP 本身就是先验——你通过选择核函数，在所有可能的函数里，指定了哪些函数”更有可能”（平滑的？周期的？）</li> <li> <table> <tbody> <tr> <td>**似然 $p(\mathcal{D}</td> <td>f)$**：观测数据，每个观测点 $y_i = f(x_i) + \epsilon$</td> </tr> </tbody> </table> </li> <li> <table> <tbody> <tr> <td>**后验 $p(f</td> <td>\mathcal{D})$**：更新后的 GP，即 <code class="language-plaintext highlighter-rouge">gp.predict()</code> 给出的 $\mu(x)$ 和 $\sigma(x)$</td> </tr> </tbody> </table> </li> </ul> <table> <tbody> <tr> <td><strong>对数边际似然的优化</strong>（核参数的学习）也是贝叶斯的一部分：通过最大化 $p(\mathcal{D}</td> <td>\theta)$（对函数积分掉，边缘化），从数据中学习核函数的超参数 $\theta = (l, \sigma_n)$。</td> </tr> </tbody> </table> <p>整个过程：先验 → 贝叶斯更新 → 后验 → 用后验指导下一步采样。这是一个完整的贝叶斯推断闭环。</p> <hr/> <h2 id="9-局限性不装">9. 局限性（不装）</h2> <p>BO 不是万能的，有几个很实际的坑：</p> <p><strong>1. 计算复杂度随观测点增长</strong>：GP 的核矩阵是 $n \times n$ 的，Cholesky 分解是 $O(n^3)$。观测点超过几千个，GP 就开始变慢。（解法：sparse GP、inducing points 方法）</p> <p><strong>2. 高维空间效果差</strong>：维度超过 20 之后，BO 的优势基本消失——候选点太稀疏，EI 的指导作用降低。（解法：REMBO 等降维方法）</p> <p><strong>3. 对核函数敏感</strong>：选错核函数，先验和实际函数差太多，收敛很慢。</p> <p><strong>4. 并行化困难</strong>：标准 BO 是串行的，每次评估完再选下一个点。并行版本（qEI）存在，但复杂不少。</p> <p>这也是为什么在超参数搜索这个具体场景，BO 最好用——维度通常不高（5~20 个超参），每次评估极贵（几小时），完全符合 BO 的适用场景。</p> <hr/> <h2 id="10-小结">10. 小结</h2> <p>从头到尾把 BO 的推导走一遍，逻辑线是这样的：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>高斯分布的条件分布有解析解
        ↓
高斯过程：把函数的无穷多个取值建模为联合高斯
        ↓
GP 预测：用条件分布公式，把观测点的信息"传播"到未观测点
   μ* = k_*^T (K + σ²I)^{-1} y       （加权插值）
   σ*² = k** - k_*^T (K + σ²I)^{-1} k*  （先验方差 - 信息增益）
        ↓
Acquisition function：用 μ 和 σ 量化"值得去哪里"
   EI = (μ - f⁺)·Φ(Z) + σ·φ(Z)       （探索+利用的自然权衡）
        ↓
主循环：拟合 GP → 选 EI 最高点 → 评估 → 更新 → 重复
</code></pre></div></div> <p>每一步都有清晰的来源，没有魔法。</p> <p>对应到 LLM 跳层搜索这个具体例子：</p> <table> <thead> <tr> <th>抽象概念</th> <th>在跳层问题里对应的东西</th> </tr> </thead> <tbody> <tr> <td>先验 $p(f)$</td> <td>Matern 核 + 只在中间层采样候选（编码了”中间层冗余”的先验知识）</td> </tr> <tr> <td>观测 $\mathcal{D}$</td> <td>已测的若干个跳层方案和对应的推理耗时</td> </tr> <tr> <td>后验 $p(f|\mathcal{D})$</td> <td>GP 预测的 μ/σ：哪些区域预计耗时低、哪些还不确定</td> </tr> <tr> <td>EI 利用项</td> <td>优先测 GP 预测效率高的配置</td> </tr> <tr> <td>EI 探索项</td> <td>顺带测一些 GP 还没把握的区域，防止错过更好的方案</td> </tr> <tr> <td>贝叶斯更新</td> <td>每测一个新配置，核矩阵和 α 更新，下一轮预测自动变准</td> </tr> </tbody> </table> <p>整个 BO 的核心思想说白了就一句话：<strong>我对这个函数有个猜测（先验），每次实验后更新猜测（贝叶斯更新），下一步去”潜力最大”的地方做实验（EI）</strong>。</p> <p>这套框架不只能用于超参数搜索——任何”评估昂贵、维度不高的黑盒优化”都可以用它：NAS 架构搜索、药物分子设计、A/B 测试参数调优等等。</p> <hr/> <blockquote> <p>如果这篇文章涉及的贝叶斯优化你想在 AutoML / NAS 场景里系统用起来，可以看看我之前出版的<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，书里有专章从贝叶斯优化到进化算法讲超参数搜索的实现，和本文是直接延续。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="贝叶斯优化"/><category term="AutoML"/><category term="数学推导"/><summary type="html"><![CDATA[贝叶斯优化从零推导：从「我对这个函数有个猜测」到自动调参]]></summary></entry><entry><title type="html">从图像到文本：Diffusion 模型原理全解——数学、结构、训练、推理一次讲清</title><link href="https://marsggbo.github.io/blog/2026/20260630-diffusion-from-image-to-text/" rel="alternate" type="text/html" title="从图像到文本：Diffusion 模型原理全解——数学、结构、训练、推理一次讲清"/><published>2026-06-30T00:00:00+00:00</published><updated>2026-06-30T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260630-diffusion-from-image-to-text</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260630-diffusion-from-image-to-text/"><![CDATA[<h1 id="从图像到文本diffusion-模型原理全解数学结构训练推理一次讲清">从图像到文本：Diffusion 模型原理全解——数学、结构、训练、推理一次讲清</h1> <hr/> <h2 id="1-前言从一个直觉开始">1. 前言：从一个直觉开始</h2> <p>你有没有想过，往一张清晰的照片里慢慢加噪声，直到变成纯噪声——这个过程能不能反过来？</p> <p>Diffusion 模型的核心思路就是：<strong>学习「加噪」的逆过程</strong>。先定义一个确定性的加噪过程（从真实数据走向纯噪声），再训练一个神经网络学会逆转它（从纯噪声还原出真实数据）。</p> <p>生成时，从随机噪声出发，一步步”去噪”，最终得到新的样本。</p> <p>这个想法可以用于图像，也可以用于文本——但两者的模型结构、输入输出形式、计算流程差别很大。本文从图像 diffusion 讲起，然后过渡到文本 diffusion，把每一步的维度变化和计算逻辑讲清楚。</p> <hr/> <h2 id="2-先把核心数学搞懂">2. 先把核心数学搞懂</h2> <h3 id="21-符号表读公式前先认清这些变量">2.1 符号表：读公式前先认清这些变量</h3> <p>在进入公式之前，先把所有符号的物理含义说清楚。后面看到这些字母，脑子里要能立刻对应上它们代表什么。</p> <table> <thead> <tr> <th>符号</th> <th>含义</th> <th>直觉</th> </tr> </thead> <tbody> <tr> <td>$x_0$</td> <td>原始干净数据（图像像素、token embedding 等）</td> <td>“未被污染的真相”</td> </tr> <tr> <td>$x_t$</td> <td>第 $t$ 步加噪后的数据</td> <td>“经过 $t$ 轮污染的样本”</td> </tr> <tr> <td>$x_T$</td> <td>最终完全噪声化的数据</td> <td>“几乎纯噪声，看不出原始内容”</td> </tr> <tr> <td>$t$</td> <td>时间步，$t \in {1, 2, \ldots, T}$</td> <td>“当前处于第几轮加噪”</td> </tr> <tr> <td>$T$</td> <td>总步数，通常取 1000</td> <td>“加噪的总轮数”</td> </tr> <tr> <td>$\beta_t$</td> <td>第 $t$ 步的<strong>噪声强度</strong>（noise schedule），$\beta_t \in (0,1)$</td> <td>“这一步要往里加多少噪声”</td> </tr> <tr> <td>$\alpha_t$</td> <td>$\alpha_t = 1 - \beta_t$，信号保留比例</td> <td>“这一步保留多少原来的信号”</td> </tr> <tr> <td>$\bar\alpha_t$</td> <td>$\bar\alpha_t = \prod_{s=1}^{t} \alpha_s$，前 $t$ 步的累积信号保留比例</td> <td>“经过 $t$ 步，原始信号还剩多少”</td> </tr> <tr> <td>$\epsilon$</td> <td>标准高斯噪声，$\epsilon \sim \mathcal{N}(0, I)$</td> <td>“每次加入的随机干扰”</td> </tr> <tr> <td>$\epsilon_\theta$</td> <td>神经网络，参数为 $\theta$</td> <td>“用来预测噪声的模型”</td> </tr> </tbody> </table> <blockquote> <p><strong>关于 noise schedule</strong>：$\beta_t$ 不是随机的，而是人为预先设计好的序列。最简单的是线性 schedule：从 $\beta_1 = 10^{-4}$ 线性增大到 $\beta_T = 0.02$。意思是：<strong>早期步骤（小 $t$）加噪很轻微，后期步骤（大 $t$）加噪越来越猛</strong>。这样设计是为了让逆向去噪在每一步都是一个可处理的小问题，而不是一步从纯噪声跳回清晰图像。</p> </blockquote> <h3 id="22-前向过程加噪">2.2 前向过程（加噪）</h3> <p>前向过程是固定的，<strong>不需要学习</strong>。给定一张真实图像 $x_0$，逐步向它加高斯噪声：</p> \[q(x_t \mid x_{t-1}) = \mathcal{N}\!\left(x_t;\; \sqrt{1-\beta_t}\, x_{t-1},\; \beta_t I\right)\] <p><strong>这个公式的白话翻译</strong>：从 $x_{t-1}$ 生成 $x_t$ 时，先把 $x_{t-1}$ 缩小到 $\sqrt{1-\beta_t}$ 倍（信号衰减），然后加上方差为 $\beta_t$ 的高斯噪声。$\beta_t$ 越大，信号衰减越多，噪声越大。</p> <blockquote> <p>这个公式用到了条件高斯分布：$\mathcal{N}(x; \mu, \sigma^2 I)$ 表示均值为 $\mu$、方差为 $\sigma^2$ 的高斯分布。从这个分布采样等价于：$x = \mu + \sigma \cdot \epsilon$，其中 $\epsilon \sim \mathcal{N}(0, I)$。</p> </blockquote> <p>这个过程有一个非常好用的性质：<strong>可以直接从 $x_0$ 一步跳到任意时刻 $t$</strong>，不需要逐步走 $t$ 次。</p> <p>令 $\alpha_t = 1 - \beta_t$，$\bar\alpha_t = \prod_{s=1}^{t} \alpha_s$，则：</p> \[q(x_t \mid x_0) = \mathcal{N}(x_t;\; \sqrt{\bar\alpha_t}\, x_0,\; (1-\bar\alpha_t) I)\] <p>写成采样形式（实际代码里用这个）：</p> \[\boxed{x_t = \sqrt{\bar\alpha_t}\, x_0 + \sqrt{1 - \bar\alpha_t}\, \epsilon, \quad \epsilon \sim \mathcal{N}(0, I)}\] <p><strong>直觉解读</strong>：</p> <ul> <li>$\sqrt{\bar\alpha_t}\, x_0$：原始信号按 $\sqrt{\bar\alpha_t}$ 缩放后的残留部分</li> <li>$\sqrt{1 - \bar\alpha_t}\, \epsilon$：加入的噪声部分</li> <li>$t$ 越大 → $\bar\alpha_t$ 越小 → 信号保留越少，噪声越多</li> <li>$t = T$ 时 $\bar\alpha_T \approx 0$，于是 $x_T \approx \epsilon \sim \mathcal{N}(0,I)$，彻底变成纯噪声</li> </ul> <p>为什么可以一步跳到？因为多个高斯分布的叠加仍然是高斯分布，可以把递推关系合并成一个解析式。这个性质让训练极其高效——不需要真的走 $t$ 步，只需算一次 $\bar\alpha_t$。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 预计算 noise schedule（在训练开始前做一次）
</span><span class="n">T</span> <span class="o">=</span> <span class="mi">1000</span>
<span class="n">betas</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">linspace</span><span class="p">(</span><span class="mf">1e-4</span><span class="p">,</span> <span class="mf">0.02</span><span class="p">,</span> <span class="n">T</span><span class="p">)</span>       <span class="c1"># β_1, β_2, ..., β_T，线性增长
</span><span class="n">alphas</span> <span class="o">=</span> <span class="mi">1</span> <span class="o">-</span> <span class="n">betas</span>                            <span class="c1"># α_t = 1 - β_t
</span><span class="n">alpha_bar</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cumprod</span><span class="p">(</span><span class="n">alphas</span><span class="p">,</span> <span class="n">dim</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>      <span class="c1"># ᾱ_t = α_1 * α_2 * ... * α_t
</span>
<span class="c1"># 给定 x0 和时间步 t，一步得到 x_t（即上面的采样公式）
</span><span class="k">def</span> <span class="nf">q_sample</span><span class="p">(</span><span class="n">x0</span><span class="p">,</span> <span class="n">t</span><span class="p">):</span>
    <span class="c1"># x0: [B, C, H, W]，t: [B]
</span>    <span class="n">eps</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn_like</span><span class="p">(</span><span class="n">x0</span><span class="p">)</span>                           <span class="c1"># ε ~ N(0,I)
</span>    <span class="n">sqrt_ab</span>    <span class="o">=</span> <span class="n">alpha_bar</span><span class="p">[</span><span class="n">t</span><span class="p">][:,</span> <span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">].</span><span class="nf">sqrt</span><span class="p">()</span>  <span class="c1"># √ᾱ_t
</span>    <span class="n">sqrt_1_ab</span>  <span class="o">=</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="n">alpha_bar</span><span class="p">[</span><span class="n">t</span><span class="p">])[:,</span> <span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">].</span><span class="nf">sqrt</span><span class="p">()</span>  <span class="c1"># √(1-ᾱ_t)
</span>    <span class="n">x_t</span> <span class="o">=</span> <span class="n">sqrt_ab</span> <span class="o">*</span> <span class="n">x0</span> <span class="o">+</span> <span class="n">sqrt_1_ab</span> <span class="o">*</span> <span class="n">eps</span>                <span class="c1"># 公式直接翻译
</span>    <span class="k">return</span> <span class="n">x_t</span><span class="p">,</span> <span class="n">eps</span>
</code></pre></div></div> <h3 id="23-反向过程去噪">2.3 反向过程（去噪）</h3> <p>反向过程是我们要学习的。我们想要 $q(x_{t-1} \mid x_t)$——已知加了 $t$ 步噪声的 $x_t$，推断出少加一步噪声的 $x_{t-1}$。</p> <p>这个分布依赖于整个数据集，<strong>无法直接计算</strong>。但当 $\beta_t$ 很小时，反向过程也近似是高斯的（这是 diffusion 模型一个关键的数学洞见），于是我们用神经网络来拟合它：</p> \[p_\theta(x_{t-1} \mid x_t) = \mathcal{N}(x_{t-1};\; \mu_\theta(x_t, t),\; \Sigma_\theta(x_t, t))\] <p><strong>训练策略</strong>：不直接预测均值 $\mu_\theta$，而是训练神经网络 $\epsilon_\theta$ 来预测被加入的噪声 $\epsilon$。已知 $x_t$ 和预测的噪声 $\hat\epsilon$，可以反推出 $x_0$，进而推出均值：</p> \[\mu_\theta(x_t, t) = \frac{1}{\sqrt{\alpha_t}}\!\left(x_t - \frac{1-\alpha_t}{\sqrt{1-\bar\alpha_t}}\, \epsilon_\theta(x_t, t)\right)\] <p><strong>这个公式的来源</strong>：把 $x_t = \sqrt{\bar\alpha_t} x_0 + \sqrt{1-\bar\alpha_t}\epsilon$ 代入贝叶斯后验 $q(x_{t-1} \mid x_t, x_0)$ 的均值表达式，再用 $\hat\epsilon$ 替换 $\epsilon$，就得到上式。</p> <h3 id="24-训练目标">2.4 训练目标</h3> <p>经过变分推导，最终的损失函数简化为一个极其简洁的形式：</p> \[\boxed{\mathcal{L} = \mathbb{E}_{x_0,\, \epsilon,\, t}\!\left[\|\epsilon - \epsilon_\theta(x_t, t)\|^2\right]}\] <p><strong>说人话</strong>：</p> <ol> <li>从数据集随机取一个真实样本 $x_0$</li> <li>随机取一个时间步 $t$</li> <li>采样随机噪声 $\epsilon \sim \mathcal{N}(0,I)$</li> <li>算出加噪后的 $x_t = \sqrt{\bar\alpha_t} x_0 + \sqrt{1-\bar\alpha_t} \epsilon$</li> <li>把 $x_t$ 和 $t$ 喂给网络，让它预测 $\hat\epsilon = \epsilon_\theta(x_t, t)$</li> <li>用 MSE 衡量预测误差</li> </ol> <p>整个训练目标就是一个<strong>预测噪声的回归问题</strong>，比 GAN 的博弈稳定得多，比 VAE 的 ELBO 也更简洁。</p> <hr/> <h2 id="3-图像-diffusionunet-结构">3. 图像 Diffusion：UNet 结构</h2> <h3 id="31-为什么用-unet">3.1 为什么用 UNet</h3> <p>图像 diffusion（DDPM、Stable Diffusion 的噪声预测部分）用 UNet 作为去噪网络 $\epsilon_\theta$。</p> <p><strong>输入</strong>：加了噪声的图像 $x_t$，形状 <code class="language-plaintext highlighter-rouge">[B, C, H, W]</code> <strong>输出</strong>：预测的噪声 $\hat\epsilon$，形状 <code class="language-plaintext highlighter-rouge">[B, C, H, W]</code>（与输入完全相同）</p> <p>UNet 是编码器-解码器结构，有跳跃连接（skip connections），能同时处理全局语义（”这里是人脸”）和局部细节（”这几个像素是眼睛”），这正好符合图像去噪的需求。</p> <h3 id="32-时间步-t-怎么注入">3.2 时间步 t 怎么注入</h3> <p>网络需要知道当前是第几步（$t$），因为不同时刻的噪声程度不同，去噪策略也不同（$t=999$ 几乎全是噪声，需要大幅修改；$t=1$ 接近干净图像，只需微调）。</p> <p>做法：把标量 $t$ 编码成一个向量，加进网络的每一层。编码方式和 Transformer 里的位置编码一样——Sinusoidal 编码。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">timestep_embedding</span><span class="p">(</span><span class="n">t</span><span class="p">,</span> <span class="n">dim</span><span class="p">):</span>
    <span class="c1"># t: [B]，整数时间步
</span>    <span class="c1"># 输出: [B, dim]，每个时间步对应一个 dim 维向量
</span>    <span class="n">half</span> <span class="o">=</span> <span class="n">dim</span> <span class="o">//</span> <span class="mi">2</span>
    <span class="c1"># 生成 half 个频率（从高频到低频）
</span>    <span class="n">freqs</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="o">-</span><span class="n">math</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="mi">10000</span><span class="p">)</span> <span class="o">*</span> <span class="n">torch</span><span class="p">.</span><span class="nf">arange</span><span class="p">(</span><span class="n">half</span><span class="p">)</span> <span class="o">/</span> <span class="n">half</span><span class="p">)</span>
    <span class="n">args</span> <span class="o">=</span> <span class="n">t</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">].</span><span class="nf">float</span><span class="p">()</span> <span class="o">*</span> <span class="n">freqs</span><span class="p">[</span><span class="bp">None</span><span class="p">]</span>   <span class="c1"># [B, half]，时间步 × 频率
</span>    <span class="c1"># 用 cos 和 sin 各表示一半维度，拼在一起
</span>    <span class="n">emb</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">([</span><span class="n">torch</span><span class="p">.</span><span class="nf">cos</span><span class="p">(</span><span class="n">args</span><span class="p">),</span> <span class="n">torch</span><span class="p">.</span><span class="nf">sin</span><span class="p">(</span><span class="n">args</span><span class="p">)],</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>  <span class="c1"># [B, dim]
</span>    <span class="k">return</span> <span class="n">emb</span>

<span class="c1"># 在 UNet 的每个 ResBlock 里，把时间 embedding 加到特征图上
</span><span class="k">class</span> <span class="nc">ResBlock</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">channels</span><span class="p">,</span> <span class="n">t_dim</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">conv</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Conv2d</span><span class="p">(</span><span class="n">channels</span><span class="p">,</span> <span class="n">channels</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="n">padding</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>
        <span class="c1"># 把 t_dim 维的时间向量投影到 channels 维，然后加到特征图
</span>        <span class="n">self</span><span class="p">.</span><span class="n">time_proj</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">t_dim</span><span class="p">,</span> <span class="n">channels</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">,</span> <span class="n">t_emb</span><span class="p">):</span>
        <span class="c1"># x:     [B, C, H, W]  当前特征图
</span>        <span class="c1"># t_emb: [B, t_dim]    时间步 embedding
</span>        <span class="n">scale</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">time_proj</span><span class="p">(</span><span class="n">t_emb</span><span class="p">)[:,</span> <span class="p">:,</span> <span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">]</span>  <span class="c1"># [B, C, 1, 1]
</span>        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">conv</span><span class="p">(</span><span class="n">x</span><span class="p">)</span> <span class="o">+</span> <span class="n">scale</span>   <span class="c1"># 广播加到每个空间位置
</span></code></pre></div></div> <h3 id="33-完整训练公式对照代码">3.3 完整训练：公式对照代码</h3> <p>下面把训练流程的数学和代码并排放，方便对照：</p> <p><strong>数学步骤：</strong></p> <p>\(t \sim \text{Uniform}\{1,\ldots,T\}, \quad \epsilon \sim \mathcal{N}(0,I)\) \(x_t = \sqrt{\bar\alpha_t}\, x_0 + \sqrt{1-\bar\alpha_t}\, \epsilon\) \(\mathcal{L} = \|\epsilon - \epsilon_\theta(x_t, t)\|^2\)</p> <p><strong>对应代码：</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">T</span> <span class="o">=</span> <span class="mi">1000</span>
<span class="n">betas</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">linspace</span><span class="p">(</span><span class="mf">1e-4</span><span class="p">,</span> <span class="mf">0.02</span><span class="p">,</span> <span class="n">T</span><span class="p">)</span>   <span class="c1"># β_t，线性 schedule
</span><span class="n">alphas</span> <span class="o">=</span> <span class="mi">1</span> <span class="o">-</span> <span class="n">betas</span>                        <span class="c1"># α_t = 1 - β_t
</span><span class="n">alpha_bar</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cumprod</span><span class="p">(</span><span class="n">alphas</span><span class="p">,</span> <span class="n">dim</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>  <span class="c1"># ᾱ_t = ∏α_s
</span>
<span class="k">def</span> <span class="nf">train_step</span><span class="p">(</span><span class="n">x0</span><span class="p">,</span> <span class="n">unet</span><span class="p">):</span>
    <span class="c1"># x0: [B, C, H, W]，像素值归一化到 [-1, 1]
</span>    <span class="n">B</span> <span class="o">=</span> <span class="n">x0</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span>

    <span class="c1"># 步骤1：t ~ Uniform{1,...,T}
</span>    <span class="n">t</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randint</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">T</span><span class="p">,</span> <span class="p">(</span><span class="n">B</span><span class="p">,))</span>               <span class="c1"># [B]，每个样本随机一个时间步
</span>
    <span class="c1"># 步骤2：ε ~ N(0,I)，然后算 x_t（前向公式的直接实现）
</span>    <span class="n">eps</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn_like</span><span class="p">(</span><span class="n">x0</span><span class="p">)</span>                  <span class="c1"># [B, C, H, W]
</span>    <span class="n">sqrt_ab</span>   <span class="o">=</span> <span class="n">alpha_bar</span><span class="p">[</span><span class="n">t</span><span class="p">][:,</span> <span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">].</span><span class="nf">sqrt</span><span class="p">()</span>      <span class="c1"># √ᾱ_t，形状 [B,1,1,1]
</span>    <span class="n">sqrt_1_ab</span> <span class="o">=</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="n">alpha_bar</span><span class="p">[</span><span class="n">t</span><span class="p">])[:,</span> <span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">].</span><span class="nf">sqrt</span><span class="p">()</span>
    <span class="n">x_t</span> <span class="o">=</span> <span class="n">sqrt_ab</span> <span class="o">*</span> <span class="n">x0</span> <span class="o">+</span> <span class="n">sqrt_1_ab</span> <span class="o">*</span> <span class="n">eps</span>        <span class="c1"># [B, C, H, W]，加噪后的图像
</span>
    <span class="c1"># 步骤3：网络预测噪声 ε_θ(x_t, t)
</span>    <span class="n">eps_pred</span> <span class="o">=</span> <span class="nf">unet</span><span class="p">(</span><span class="n">x_t</span><span class="p">,</span> <span class="n">t</span><span class="p">)</span>                     <span class="c1"># [B, C, H, W]
</span>
    <span class="c1"># 步骤4：MSE 损失，即 ||ε - ε_θ||²
</span>    <span class="n">loss</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">mse_loss</span><span class="p">(</span><span class="n">eps_pred</span><span class="p">,</span> <span class="n">eps</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">loss</span>
</code></pre></div></div> <h3 id="34-推理从噪声还原图像">3.4 推理：从噪声还原图像</h3> <p><strong>数学步骤：</strong></p> <p>从 $x_T \sim \mathcal{N}(0,I)$ 出发，每一步：</p> \[\mu_t = \frac{1}{\sqrt{\alpha_t}}\!\left(x_t - \frac{1-\alpha_t}{\sqrt{1-\bar\alpha_t}}\hat\epsilon\right), \quad \hat\epsilon = \epsilon_\theta(x_t, t)\] \[x_{t-1} = \mu_t + \sqrt{\beta_t}\, z, \quad z \sim \mathcal{N}(0,I) \quad (t &gt; 0)\] <p><strong>对应代码：</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@torch.no_grad</span><span class="p">()</span>
<span class="k">def</span> <span class="nf">ddpm_sample</span><span class="p">(</span><span class="n">unet</span><span class="p">,</span> <span class="n">shape</span><span class="p">):</span>
    <span class="c1"># shape: (B, C, H, W)
</span>    <span class="n">x</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="n">shape</span><span class="p">)</span>   <span class="c1"># x_T ~ N(0, I)，从纯噪声出发
</span>
    <span class="k">for</span> <span class="n">t</span> <span class="ow">in</span> <span class="nf">reversed</span><span class="p">(</span><span class="nf">range</span><span class="p">(</span><span class="n">T</span><span class="p">)):</span>    <span class="c1"># t = T-1, T-2, ..., 1, 0
</span>        <span class="n">t_batch</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">full</span><span class="p">((</span><span class="n">shape</span><span class="p">[</span><span class="mi">0</span><span class="p">],),</span> <span class="n">t</span><span class="p">)</span>

        <span class="c1"># 用网络预测噪声 ε̂ = ε_θ(x_t, t)
</span>        <span class="n">eps_pred</span> <span class="o">=</span> <span class="nf">unet</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">t_batch</span><span class="p">)</span>         <span class="c1"># [B, C, H, W]
</span>
        <span class="c1"># 计算均值 μ_t（对应上面公式）
</span>        <span class="n">a</span>  <span class="o">=</span> <span class="n">alphas</span><span class="p">[</span><span class="n">t</span><span class="p">]</span>        <span class="c1"># α_t
</span>        <span class="n">ab</span> <span class="o">=</span> <span class="n">alpha_bar</span><span class="p">[</span><span class="n">t</span><span class="p">]</span>     <span class="c1"># ᾱ_t
</span>        <span class="n">mu</span> <span class="o">=</span> <span class="p">(</span><span class="n">x</span> <span class="o">-</span> <span class="p">(</span><span class="mi">1</span><span class="o">-</span><span class="n">a</span><span class="p">)</span> <span class="o">/</span> <span class="p">(</span><span class="mi">1</span><span class="o">-</span><span class="n">ab</span><span class="p">).</span><span class="nf">sqrt</span><span class="p">()</span> <span class="o">*</span> <span class="n">eps_pred</span><span class="p">)</span> <span class="o">/</span> <span class="n">a</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">()</span>

        <span class="c1"># x_{t-1} = μ_t + √β_t * z（最后一步 t=0 不加噪声）
</span>        <span class="k">if</span> <span class="n">t</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">:</span>
            <span class="n">z</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn_like</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
            <span class="n">x</span> <span class="o">=</span> <span class="n">mu</span> <span class="o">+</span> <span class="n">betas</span><span class="p">[</span><span class="n">t</span><span class="p">].</span><span class="nf">sqrt</span><span class="p">()</span> <span class="o">*</span> <span class="n">z</span>
        <span class="k">else</span><span class="p">:</span>
            <span class="n">x</span> <span class="o">=</span> <span class="n">mu</span>    <span class="c1"># t=0 时直接返回均值，不加额外噪声
</span>
    <span class="k">return</span> <span class="n">x</span>   <span class="c1"># [B, C, H, W]，去噪后的图像
</span></code></pre></div></div> <p><strong>维度流</strong>（以 <code class="language-plaintext highlighter-rouge">C=3, H=W=64</code> 为例）：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>输入：[B, 3, 64, 64]（纯噪声 x_T）
  ↓ UNet encoder（下采样，增加通道数）
  [B,  64, 64, 64]
  [B, 128, 32, 32]
  [B, 256, 16, 16]
  ↓ bottleneck（可加 self-attention）
  [B, 256, 16, 16]
  ↓ UNet decoder（上采样 + skip connection，恢复空间分辨率）
  [B, 128, 32, 32]
  [B,  64, 64, 64]
  ↓ 输出头（1×1 卷积，把通道数变回 C=3）
输出：[B, 3, 64, 64]（预测的噪声 ε̂，与输入同形状）
</code></pre></div></div> <hr/> <h2 id="4-从图像到文本最大的区别在哪里">4. 从图像到文本：最大的区别在哪里</h2> <p>图像是<strong>连续空间</strong>里的信号，像素值是实数，加高斯噪声天然合法。</p> <p>文本是<strong>离散空间</strong>——token id 是整数（比如”猫”对应 id=3421），<strong>你不能在两个整数之间做线性插值</strong>。想象一下 <code class="language-plaintext highlighter-rouge">"猫" × 0.3 + "狗" × 0.7</code>——这在整数空间毫无意义。</p> <p>这就是文本 diffusion 最核心的挑战。目前主要有两条技术路线：</p> <table> <thead> <tr> <th> </th> <th>图像 Diffusion</th> <th>连续文本 Diffusion</th> <th>离散 Masked Diffusion</th> </tr> </thead> <tbody> <tr> <td>操作对象</td> <td>像素值（连续）</td> <td>token embedding（连续）</td> <td>token id（离散）</td> </tr> <tr> <td>噪声类型</td> <td>高斯噪声</td> <td>高斯噪声</td> <td>Mask / 替换</td> </tr> <tr> <td>网络结构</td> <td>UNet</td> <td>Transformer</td> <td>Transformer</td> </tr> <tr> <td>输出</td> <td>预测噪声 $\hat\epsilon$，形状同输入</td> <td>预测噪声（或 $x_0$），形状同输入</td> <td>每个位置的 token logits</td> </tr> <tr> <td>损失函数</td> <td>MSE</td> <td>MSE</td> <td>Cross Entropy</td> </tr> <tr> <td>解码方式</td> <td>直接读像素值</td> <td>找最近邻 token</td> <td>argmax</td> </tr> </tbody> </table> <hr/> <h2 id="5-连续空间文本-diffusionembedding-space">5. 连续空间文本 Diffusion（Embedding Space）</h2> <h3 id="51-思路在-embedding-上加噪声">5.1 思路：在 embedding 上加噪声</h3> <p>既然 token id 是离散的，加不了噪声，那就把每个 token 先转换成连续的 embedding 向量，然后在 embedding 空间里做扩散。</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>输入序列（token ids）：[B, L]
      ↓ embedding 层（查表，每个 id 变成 d 维向量）
token embeddings：    [B, L, d_model]   ← 在这里做 diffusion
      ↓ 加噪 / 去噪（T 步）
去噪后的 embeddings：  [B, L, d_model]
      ↓ 解码回 token id（见 5.5 节）
输出序列（token ids）：[B, L]
</code></pre></div></div> <p><strong>注意 embedding 归一化</strong>：训练时通常把 embedding 归一化到单位球面（$|x_0| = 1$），这样能让信号和噪声的量级相当——否则如果 embedding 的模很大，噪声占比会很小，信噪比失衡，训练不稳定。</p> <h3 id="52-前向加噪文本版数学和代码对照">5.2 前向加噪（文本版）：数学和代码对照</h3> <p>文本版的前向过程数学公式和图像版<strong>完全相同</strong>，只是维度从 <code class="language-plaintext highlighter-rouge">[B, C, H, W]</code> 变成了 <code class="language-plaintext highlighter-rouge">[B, L, d]</code>：</p> <p>\(q(x_t \mid x_0) = \mathcal{N}(x_t;\; \sqrt{\bar\alpha_t}\, x_0,\; (1-\bar\alpha_t) I)\) \(x_t = \sqrt{\bar\alpha_t}\, x_0 + \sqrt{1-\bar\alpha_t}\, \epsilon, \quad \epsilon \sim \mathcal{N}(0, I)\)</p> <p>这里 $x_0 \in \mathbb{R}^{L \times d}$ 是归一化后的 token embedding 矩阵，$\epsilon \in \mathbb{R}^{L \times d}$ 是同形状的高斯噪声。序列中每个位置、每个维度独立地加噪声。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">forward_diffuse_text</span><span class="p">(</span><span class="n">x0</span><span class="p">,</span> <span class="n">t</span><span class="p">,</span> <span class="n">alpha_bar</span><span class="p">):</span>
    <span class="c1"># x0: [B, L, d]，归一化的 token embeddings
</span>    <span class="c1"># t:  [B]
</span>    <span class="n">eps</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn_like</span><span class="p">(</span><span class="n">x0</span><span class="p">)</span>              <span class="c1"># [B, L, d]，ε ~ N(0,I)
</span>    <span class="n">ab</span>  <span class="o">=</span> <span class="n">alpha_bar</span><span class="p">[</span><span class="n">t</span><span class="p">][:,</span> <span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">]</span>       <span class="c1"># [B, 1, 1]，广播到 [B, L, d]
</span>    <span class="c1"># 公式直接翻译：x_t = √ᾱ_t * x0 + √(1-ᾱ_t) * ε
</span>    <span class="n">x_t</span> <span class="o">=</span> <span class="n">ab</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">()</span> <span class="o">*</span> <span class="n">x0</span> <span class="o">+</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="n">ab</span><span class="p">).</span><span class="nf">sqrt</span><span class="p">()</span> <span class="o">*</span> <span class="n">eps</span>
    <span class="k">return</span> <span class="n">x_t</span><span class="p">,</span> <span class="n">eps</span>   <span class="c1"># x_t: [B, L, d]，形状始终不变
</span></code></pre></div></div> <blockquote> <p><strong>图像 vs 文本</strong>：形状的区别是 <code class="language-plaintext highlighter-rouge">[B, C, H, W]</code> vs <code class="language-plaintext highlighter-rouge">[B, L, d]</code>。图像的空间结构是二维的（H×W），文本的是一维序列（L）。加噪公式和 $\alpha/\beta$ 的含义完全一样，只是张量维度不同。</p> </blockquote> <h3 id="53-去噪网络为什么换成-transformer">5.3 去噪网络：为什么换成 Transformer</h3> <p>图像 UNet 用局部卷积（每个像素只看周围的邻居）处理空间结构，而文本需要<strong>全局依赖</strong>——理解”猫”这个词的含义，需要看整句话的上下文，不是只看左右两个位置。</p> <p>因此去噪网络 $\epsilon_\theta$ 换成 Transformer（BERT 风格的双向 Encoder），用 self-attention 让序列中每个位置都能看到所有其他位置。</p> <p><strong>数学上</strong>，去噪网络 $\epsilon_\theta(x_t, t)$ 的接口没有变：输入是加噪后的 $x_t$ 和时间步 $t$，输出是预测的噪声（形状与 $x_t$ 相同）。变的只是内部实现（从 UNet 换成 Transformer）。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">TextDiffusionTransformer</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">d_model</span><span class="p">,</span> <span class="n">nhead</span><span class="p">,</span> <span class="n">num_layers</span><span class="p">,</span> <span class="n">vocab_size</span><span class="p">,</span> <span class="n">max_len</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">embed</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Embedding</span><span class="p">(</span><span class="n">vocab_size</span><span class="p">,</span> <span class="n">d_model</span><span class="p">)</span>   <span class="c1"># token id → embedding
</span>        <span class="n">self</span><span class="p">.</span><span class="n">pos_embed</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Embedding</span><span class="p">(</span><span class="n">max_len</span><span class="p">,</span> <span class="n">d_model</span><span class="p">)</span>  <span class="c1"># 位置 embedding
</span>        <span class="n">self</span><span class="p">.</span><span class="n">time_embed</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Sequential</span><span class="p">(</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">d_model</span><span class="p">,</span> <span class="n">d_model</span> <span class="o">*</span> <span class="mi">4</span><span class="p">),</span> <span class="n">nn</span><span class="p">.</span><span class="nc">SiLU</span><span class="p">(),</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">d_model</span> <span class="o">*</span> <span class="mi">4</span><span class="p">,</span> <span class="n">d_model</span><span class="p">),</span>
        <span class="p">)</span>
        <span class="n">encoder_layer</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">TransformerEncoderLayer</span><span class="p">(</span><span class="n">d_model</span><span class="p">,</span> <span class="n">nhead</span><span class="p">,</span> <span class="n">batch_first</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">transformer</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">TransformerEncoder</span><span class="p">(</span><span class="n">encoder_layer</span><span class="p">,</span> <span class="n">num_layers</span><span class="p">)</span>
        <span class="c1"># 输出仍是 embedding 空间（d_model 维），不是 vocab logits
</span>        <span class="n">self</span><span class="p">.</span><span class="n">out_proj</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">d_model</span><span class="p">,</span> <span class="n">d_model</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x_t</span><span class="p">,</span> <span class="n">t</span><span class="p">):</span>
        <span class="c1"># x_t: [B, L, d]  加噪后的 embedding（连续值，不是 token id）
</span>        <span class="c1"># t:   [B]         时间步
</span>        <span class="n">B</span><span class="p">,</span> <span class="n">L</span><span class="p">,</span> <span class="n">d</span> <span class="o">=</span> <span class="n">x_t</span><span class="p">.</span><span class="n">shape</span>

        <span class="c1"># 时间步编码：标量 t → d 维向量，广播加到序列每个位置
</span>        <span class="n">t_emb</span> <span class="o">=</span> <span class="nf">timestep_embedding</span><span class="p">(</span><span class="n">t</span><span class="p">,</span> <span class="n">d</span><span class="p">)</span>        <span class="c1"># [B, d]
</span>        <span class="n">t_emb</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">time_embed</span><span class="p">(</span><span class="n">t_emb</span><span class="p">)</span>          <span class="c1"># [B, d]，经过 MLP 变换
</span>        <span class="n">t_emb</span> <span class="o">=</span> <span class="n">t_emb</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">,</span> <span class="p">:]</span>               <span class="c1"># [B, 1, d] → 广播到 [B, L, d]
</span>
        <span class="c1"># 位置编码
</span>        <span class="n">pos</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">arange</span><span class="p">(</span><span class="n">L</span><span class="p">,</span> <span class="n">device</span><span class="o">=</span><span class="n">x_t</span><span class="p">.</span><span class="n">device</span><span class="p">)</span>
        <span class="n">p_emb</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">pos_embed</span><span class="p">(</span><span class="n">pos</span><span class="p">)[</span><span class="bp">None</span><span class="p">]</span>       <span class="c1"># [1, L, d]
</span>
        <span class="c1"># 融合：加噪 embedding + 时间信息 + 位置信息
</span>        <span class="n">h</span> <span class="o">=</span> <span class="n">x_t</span> <span class="o">+</span> <span class="n">t_emb</span> <span class="o">+</span> <span class="n">p_emb</span>                <span class="c1"># [B, L, d]
</span>
        <span class="c1"># 双向 self-attention：每个位置都能看到整个序列
</span>        <span class="n">h</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">transformer</span><span class="p">(</span><span class="n">h</span><span class="p">)</span>                 <span class="c1"># [B, L, d]
</span>
        <span class="c1"># 输出：预测噪声 ε̂（也在 embedding 空间，和 x_t 同维度）
</span>        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">out_proj</span><span class="p">(</span><span class="n">h</span><span class="p">)</span>                 <span class="c1"># [B, L, d]
</span></code></pre></div></div> <h3 id="54-训练文本连续-diffusion数学和代码对照">5.4 训练（文本连续 diffusion）：数学和代码对照</h3> <p><strong>数学：</strong></p> <p>\(x_0 = \text{normalize}(E[\text{token\_ids}]) \quad \in \mathbb{R}^{L \times d}\) \(t \sim \text{Uniform}\{1,\ldots,T\}, \quad \epsilon \sim \mathcal{N}(0,I)\) \(x_t = \sqrt{\bar\alpha_t}\, x_0 + \sqrt{1-\bar\alpha_t}\, \epsilon\) \(\mathcal{L} = \|\epsilon - \epsilon_\theta(x_t, t)\|^2\)</p> <p><strong>代码：</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">train_step_text_continuous</span><span class="p">(</span><span class="n">token_ids</span><span class="p">,</span> <span class="n">model</span><span class="p">,</span> <span class="n">alpha_bar</span><span class="p">):</span>
    <span class="c1"># token_ids: [B, L]，整数 token id
</span>    <span class="n">B</span><span class="p">,</span> <span class="n">L</span> <span class="o">=</span> <span class="n">token_ids</span><span class="p">.</span><span class="n">shape</span>

    <span class="c1"># 步骤1：token id → embedding，并归一化（让量级和噪声匹配）
</span>    <span class="n">x0</span> <span class="o">=</span> <span class="n">model</span><span class="p">.</span><span class="nf">embed</span><span class="p">(</span><span class="n">token_ids</span><span class="p">)</span>              <span class="c1"># [B, L, d]
</span>    <span class="n">x0</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">normalize</span><span class="p">(</span><span class="n">x0</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>            <span class="c1"># 归一化到单位球面
</span>
    <span class="c1"># 步骤2：随机时间步 + 加噪
</span>    <span class="n">t</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randint</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">T</span><span class="p">,</span> <span class="p">(</span><span class="n">B</span><span class="p">,))</span>
    <span class="n">x_t</span><span class="p">,</span> <span class="n">eps</span> <span class="o">=</span> <span class="nf">forward_diffuse_text</span><span class="p">(</span><span class="n">x0</span><span class="p">,</span> <span class="n">t</span><span class="p">,</span> <span class="n">alpha_bar</span><span class="p">)</span>   <span class="c1"># [B, L, d]
</span>
    <span class="c1"># 步骤3：预测噪声
</span>    <span class="n">eps_pred</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">x_t</span><span class="p">,</span> <span class="n">t</span><span class="p">)</span>                <span class="c1"># [B, L, d]
</span>
    <span class="c1"># 步骤4：MSE 损失（和图像版完全相同的公式）
</span>    <span class="n">loss</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">mse_loss</span><span class="p">(</span><span class="n">eps_pred</span><span class="p">,</span> <span class="n">eps</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">loss</span>
</code></pre></div></div> <h3 id="55-推理--解码回-token最近邻是怎么回事">5.5 推理 + 解码回 token：最近邻是怎么回事？</h3> <p><strong>去噪过程</strong>（和图像版数学完全相同，只是维度不同）：</p> \[\mu_t = \frac{1}{\sqrt{\alpha_t}}\!\left(x_t - \frac{1-\alpha_t}{\sqrt{1-\bar\alpha_t}}\hat\epsilon\right), \quad x_{t-1} = \mu_t + \sqrt{\beta_t}\, z\] <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@torch.no_grad</span><span class="p">()</span>
<span class="k">def</span> <span class="nf">text_diffusion_sample</span><span class="p">(</span><span class="n">model</span><span class="p">,</span> <span class="n">B</span><span class="p">,</span> <span class="n">L</span><span class="p">,</span> <span class="n">d</span><span class="p">):</span>
    <span class="n">x</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="n">B</span><span class="p">,</span> <span class="n">L</span><span class="p">,</span> <span class="n">d</span><span class="p">)</span>    <span class="c1"># 从噪声出发，x_T ~ N(0, I)
</span>
    <span class="k">for</span> <span class="n">t</span> <span class="ow">in</span> <span class="nf">reversed</span><span class="p">(</span><span class="nf">range</span><span class="p">(</span><span class="n">T</span><span class="p">)):</span>
        <span class="n">t_batch</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">full</span><span class="p">((</span><span class="n">B</span><span class="p">,),</span> <span class="n">t</span><span class="p">)</span>
        <span class="n">eps_pred</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">t_batch</span><span class="p">)</span>         <span class="c1"># [B, L, d]
</span>
        <span class="c1"># DDPM 去噪公式（与图像版完全相同）
</span>        <span class="n">a</span>  <span class="o">=</span> <span class="n">alphas</span><span class="p">[</span><span class="n">t</span><span class="p">]</span>
        <span class="n">ab</span> <span class="o">=</span> <span class="n">alpha_bar</span><span class="p">[</span><span class="n">t</span><span class="p">]</span>
        <span class="n">mu</span> <span class="o">=</span> <span class="p">(</span><span class="n">x</span> <span class="o">-</span> <span class="p">(</span><span class="mi">1</span><span class="o">-</span><span class="n">a</span><span class="p">)</span><span class="o">/</span><span class="p">(</span><span class="mi">1</span><span class="o">-</span><span class="n">ab</span><span class="p">).</span><span class="nf">sqrt</span><span class="p">()</span> <span class="o">*</span> <span class="n">eps_pred</span><span class="p">)</span> <span class="o">/</span> <span class="n">a</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">()</span>
        <span class="k">if</span> <span class="n">t</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">:</span>
            <span class="n">x</span> <span class="o">=</span> <span class="n">mu</span> <span class="o">+</span> <span class="n">betas</span><span class="p">[</span><span class="n">t</span><span class="p">].</span><span class="nf">sqrt</span><span class="p">()</span> <span class="o">*</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn_like</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
        <span class="k">else</span><span class="p">:</span>
            <span class="n">x</span> <span class="o">=</span> <span class="n">mu</span>
    <span class="c1"># x: [B, L, d]，最终去噪后的连续 embedding，但还不是 token id
</span>    <span class="bp">...</span>
</code></pre></div></div> <p><strong>解码阶段：为什么要找最近邻？</strong></p> <p>去噪完成后，我们得到的是 <code class="language-plaintext highlighter-rouge">[B, L, d]</code> 形状的连续向量——每个位置是 $d$ 维实数向量。但输出需要是离散的 token id（整数）。怎么从连续向量映射回 token id？</p> <p>方法是：把这个向量和词表中<strong>所有 token 的 embedding 比较相似度</strong>，找最相近的那个 token。</p> \[\hat{w}_i = \arg\max_{v \in \text{Vocab}} \cos(x_i, e_v) = \arg\max_{v} \frac{x_i \cdot e_v}{\|x_i\| \|e_v\|}\] <p>归一化后，余弦相似度等价于点积，而”对所有 token 做点积取 argmax”正好就是一个<strong>线性层 + argmax</strong>：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># all_embeddings: [V, d]，词表中所有 token 的 embedding 矩阵（即 Embedding 层的权重）
</span><span class="k">def</span> <span class="nf">decode_to_tokens</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">all_embeddings</span><span class="p">):</span>
    <span class="c1"># x:              [B, L, d]，去噪后的连续 embedding
</span>    <span class="c1"># all_embeddings: [V, d]
</span>
    <span class="c1"># 分别归一化
</span>    <span class="n">x_norm</span>   <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">normalize</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>                  <span class="c1"># [B, L, d]
</span>    <span class="n">emb_norm</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">normalize</span><span class="p">(</span><span class="n">all_embeddings</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>     <span class="c1"># [V, d]
</span>
    <span class="c1"># 矩阵乘法：[B, L, d] × [d, V] = [B, L, V]
</span>    <span class="c1"># logits[b, i, v] = 第 b 个样本、第 i 个位置与第 v 个 token 的余弦相似度
</span>    <span class="n">logits</span> <span class="o">=</span> <span class="n">x_norm</span> <span class="o">@</span> <span class="n">emb_norm</span><span class="p">.</span><span class="n">T</span>                       <span class="c1"># [B, L, V]
</span>
    <span class="c1"># 对每个位置，取相似度最高的 token id
</span>    <span class="n">token_ids</span> <span class="o">=</span> <span class="n">logits</span><span class="p">.</span><span class="nf">argmax</span><span class="p">(</span><span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>                  <span class="c1"># [B, L]
</span>    <span class="k">return</span> <span class="n">token_ids</span>
</code></pre></div></div> <p><strong>这个操作一点都不复杂</strong>——它本质上和普通语言模型最后一层（<code class="language-plaintext highlighter-rouge">lm_head</code>）的操作是一样的：都是把隐状态向量和词表做矩阵乘法，找分数最高的 token。区别只是：普通 LM 的 <code class="language-plaintext highlighter-rouge">lm_head</code> 是一个<strong>单独训练的投影矩阵</strong>，而这里直接复用了 <strong>embedding 矩阵本身</strong>（因为加噪/去噪都在 embedding 空间进行，所以解码时也用同一套坐标系）。</p> <p><strong>为什么不直接用 softmax 采样？</strong> 也可以。<code class="language-plaintext highlighter-rouge">logits.softmax(-1)</code> 之后用 <code class="language-plaintext highlighter-rouge">multinomial</code> 采样，效果通常更好（保留多样性），而不是每次都取 argmax（会导致重复和退化输出）。</p> <p><strong>“最近邻解码”有没有问题？</strong> 有，这是连续文本 diffusion 的已知弱点：</p> <ol> <li><strong>Rounding Problem（取整问题）</strong>：去噪得到的连续向量可能处于 embedding 空间的”夹缝”里，和任何真实 token 都不够近，强行取最近邻会产生错误。这也是很多论文直接改用<strong>离散 masked diffusion</strong> 的原因。</li> <li><strong>计算量</strong>：词表 $V$ 通常有 3~5 万个 token，每次解码要和所有 token 计算相似度，算一个 <code class="language-plaintext highlighter-rouge">[B, L, V]</code> 的矩阵，计算量不小（虽然可以用矩阵乘法高效实现）。</li> </ol> <hr/> <h2 id="6-离散-masked-diffusion更主流">6. 离散 Masked Diffusion（更主流）</h2> <p>连续 embedding diffusion 存在”rounding problem”：embedding 空间的 MSE 损失和最终的 token 准确率对不上，训练信号不够直接。</p> <p><strong>Masked Diffusion</strong>（MDLM、Absorbing Diffusion 等）把 diffusion 直接做在 token 空间：用 <code class="language-plaintext highlighter-rouge">[MASK]</code> 替换 token，而不是加高斯噪声。这避免了连续空间和离散 token 之间的转换问题。</p> <h3 id="61-前向过程masking数学和代码对照">6.1 前向过程（Masking）：数学和代码对照</h3> <p><strong>数学</strong>：在时刻 $t$，每个位置独立地以概率 $1-\bar\alpha_t$ 被替换为 <code class="language-plaintext highlighter-rouge">[MASK]</code>：</p> \[q(x_t^{(i)} \mid x_0^{(i)}) = \begin{cases} x_0^{(i)} &amp; \text{以概率 } \bar\alpha_t \\ \texttt{[MASK]} &amp; \text{以概率 } 1-\bar\alpha_t \end{cases}\] <p>其中 $x_t^{(i)}$ 表示第 $i$ 个位置的 token。同样，$\bar\alpha_t$ 越小（$t$ 越大），被 mask 的比例越高。</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>t=0（原始）：  ["The", "cat",  "sat",  "on",  "mat"]     → ᾱ_t ≈ 1，保留所有
t=T/3：       ["The", "[M]",  "sat",  "[M]", "mat"]      → ᾱ_t ≈ 0.6，保留 60%
t=2T/3：      ["[M]", "[M]",  "sat",  "[M]", "[M]"]      → ᾱ_t ≈ 0.2，保留 20%
t=T（全噪声）：["[M]", "[M]", "[M]",  "[M]", "[M]"]      → ᾱ_t ≈ 0，全 mask
</code></pre></div></div> <p><strong>代码：</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">mask_forward</span><span class="p">(</span><span class="n">token_ids</span><span class="p">,</span> <span class="n">t</span><span class="p">,</span> <span class="n">alpha_bar</span><span class="p">,</span> <span class="n">mask_id</span><span class="p">):</span>
    <span class="c1"># token_ids: [B, L]，原始 token id（整数）
</span>    <span class="c1"># 返回：x_t: [B, L]，部分位置被替换为 mask_id
</span>    <span class="n">ab</span> <span class="o">=</span> <span class="n">alpha_bar</span><span class="p">[</span><span class="n">t</span><span class="p">][:,</span> <span class="bp">None</span><span class="p">]</span>                          <span class="c1"># [B, 1]，广播到 [B, L]
</span>    <span class="c1"># 每个位置以概率 ᾱ_t 保留（1 = 保留，0 = mask）
</span>    <span class="n">keep</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">bernoulli</span><span class="p">(</span><span class="n">ab</span><span class="p">.</span><span class="nf">expand_as</span><span class="p">(</span><span class="n">token_ids</span><span class="p">.</span><span class="nf">float</span><span class="p">())).</span><span class="nf">bool</span><span class="p">()</span>  <span class="c1"># [B, L]
</span>    <span class="n">x_t</span> <span class="o">=</span> <span class="n">token_ids</span><span class="p">.</span><span class="nf">clone</span><span class="p">()</span>
    <span class="n">x_t</span><span class="p">[</span><span class="o">~</span><span class="n">keep</span><span class="p">]</span> <span class="o">=</span> <span class="n">mask_id</span>                                <span class="c1"># 未保留的位置 → [MASK]
</span>    <span class="k">return</span> <span class="n">x_t</span>                                          <span class="c1"># [B, L]，dtype=long
</span></code></pre></div></div> <h3 id="62-去噪网络bert-风格输出是-logits">6.2 去噪网络：BERT 风格，输出是 logits</h3> <p>离散 diffusion 的网络接口和连续版本有根本区别：</p> <ul> <li><strong>连续版</strong>：输入 <code class="language-plaintext highlighter-rouge">[B, L, d]</code>（连续 embedding），输出 <code class="language-plaintext highlighter-rouge">[B, L, d]</code>（预测的噪声，也是连续 embedding）</li> <li><strong>离散版</strong>：输入 <code class="language-plaintext highlighter-rouge">[B, L]</code>（含 MASK 的 token id 序列），输出 <code class="language-plaintext highlighter-rouge">[B, L, V]</code>（每个位置的 token 概率 logits）</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">MaskedDiffusionModel</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">vocab_size</span><span class="p">,</span> <span class="n">d_model</span><span class="p">,</span> <span class="n">nhead</span><span class="p">,</span> <span class="n">num_layers</span><span class="p">,</span> <span class="n">max_len</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">embed</span>     <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Embedding</span><span class="p">(</span><span class="n">vocab_size</span><span class="p">,</span> <span class="n">d_model</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">pos_embed</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Embedding</span><span class="p">(</span><span class="n">max_len</span><span class="p">,</span> <span class="n">d_model</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">time_mlp</span>  <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Sequential</span><span class="p">(</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">d_model</span><span class="p">,</span> <span class="n">d_model</span> <span class="o">*</span> <span class="mi">4</span><span class="p">),</span> <span class="n">nn</span><span class="p">.</span><span class="nc">SiLU</span><span class="p">(),</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">d_model</span> <span class="o">*</span> <span class="mi">4</span><span class="p">,</span> <span class="n">d_model</span><span class="p">),</span>
        <span class="p">)</span>
        <span class="n">layer</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">TransformerEncoderLayer</span><span class="p">(</span><span class="n">d_model</span><span class="p">,</span> <span class="n">nhead</span><span class="p">,</span> <span class="n">batch_first</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">transformer</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">TransformerEncoder</span><span class="p">(</span><span class="n">layer</span><span class="p">,</span> <span class="n">num_layers</span><span class="p">)</span>
        <span class="c1"># 关键区别：输出是词表大小的 logits，不是 embedding
</span>        <span class="n">self</span><span class="p">.</span><span class="n">out_proj</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">d_model</span><span class="p">,</span> <span class="n">vocab_size</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x_t</span><span class="p">,</span> <span class="n">t</span><span class="p">):</span>
        <span class="c1"># x_t: [B, L]  含 MASK 的 token id 序列（整数）
</span>        <span class="c1"># t:   [B]
</span>        <span class="n">B</span><span class="p">,</span> <span class="n">L</span> <span class="o">=</span> <span class="n">x_t</span><span class="p">.</span><span class="n">shape</span>

        <span class="n">h</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">embed</span><span class="p">(</span><span class="n">x_t</span><span class="p">)</span>                              <span class="c1"># [B, L, d]，id 转 embedding
</span>        <span class="n">h</span> <span class="o">=</span> <span class="n">h</span> <span class="o">+</span> <span class="n">self</span><span class="p">.</span><span class="nf">pos_embed</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">arange</span><span class="p">(</span><span class="n">L</span><span class="p">))[</span><span class="bp">None</span><span class="p">]</span>   <span class="c1"># [B, L, d]，加位置编码
</span>
        <span class="n">t_emb</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">time_mlp</span><span class="p">(</span><span class="nf">timestep_embedding</span><span class="p">(</span><span class="n">t</span><span class="p">,</span> <span class="n">d_model</span><span class="p">))</span>  <span class="c1"># [B, d]
</span>        <span class="n">h</span> <span class="o">=</span> <span class="n">h</span> <span class="o">+</span> <span class="n">t_emb</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">,</span> <span class="p">:]</span>                        <span class="c1"># [B, L, d]，加时间信息
</span>
        <span class="n">h</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">transformer</span><span class="p">(</span><span class="n">h</span><span class="p">)</span>                          <span class="c1"># [B, L, d]，双向 attention
</span>        <span class="n">logits</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">out_proj</span><span class="p">(</span><span class="n">h</span><span class="p">)</span>                        <span class="c1"># [B, L, V]，每位置的 token 分布
</span>        <span class="k">return</span> <span class="n">logits</span>
</code></pre></div></div> <h3 id="63-训练masked-diffusion数学和代码对照">6.3 训练（Masked Diffusion）：数学和代码对照</h3> <p><strong>数学</strong>：损失只在被 mask 的位置计算，让网络预测被遮住的原始 token：</p> \[\mathcal{L} = -\sum_{i: x_t^{(i)} = \texttt{[MASK]}} \log p_\theta(x_0^{(i)} \mid x_t, t)\] <p>这和 BERT 的 Masked Language Modeling（MLM）损失几乎一模一样！区别只是：BERT 固定 mask 15% 的位置，而 masked diffusion 的 mask 比例由时间步 $t$（即 $1-\bar\alpha_t$）控制。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">train_step_masked</span><span class="p">(</span><span class="n">token_ids</span><span class="p">,</span> <span class="n">model</span><span class="p">,</span> <span class="n">alpha_bar</span><span class="p">,</span> <span class="n">mask_id</span><span class="p">):</span>
    <span class="c1"># token_ids: [B, L]
</span>    <span class="n">B</span><span class="p">,</span> <span class="n">L</span> <span class="o">=</span> <span class="n">token_ids</span><span class="p">.</span><span class="n">shape</span>

    <span class="c1"># 步骤1：随机时间步 + 按比例 mask
</span>    <span class="n">t</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randint</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">T</span><span class="p">,</span> <span class="p">(</span><span class="n">B</span><span class="p">,))</span>
    <span class="n">x_t</span> <span class="o">=</span> <span class="nf">mask_forward</span><span class="p">(</span><span class="n">token_ids</span><span class="p">,</span> <span class="n">t</span><span class="p">,</span> <span class="n">alpha_bar</span><span class="p">,</span> <span class="n">mask_id</span><span class="p">)</span>    <span class="c1"># [B, L]
</span>
    <span class="c1"># 步骤2：网络预测每个位置的 token 概率
</span>    <span class="n">logits</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">x_t</span><span class="p">,</span> <span class="n">t</span><span class="p">)</span>                                  <span class="c1"># [B, L, V]
</span>
    <span class="c1"># 步骤3：只在被 mask 的位置计算损失（预测原始 token）
</span>    <span class="n">mask</span> <span class="o">=</span> <span class="p">(</span><span class="n">x_t</span> <span class="o">==</span> <span class="n">mask_id</span><span class="p">)</span>                                 <span class="c1"># [B, L]，bool
</span>    <span class="n">loss</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">cross_entropy</span><span class="p">(</span>
        <span class="n">logits</span><span class="p">[</span><span class="n">mask</span><span class="p">],</span>       <span class="c1"># [N_masked, V]，被 mask 位置的预测
</span>        <span class="n">token_ids</span><span class="p">[</span><span class="n">mask</span><span class="p">],</span>    <span class="c1"># [N_masked]，被 mask 位置的真实 token
</span>    <span class="p">)</span>
    <span class="k">return</span> <span class="n">loss</span>
</code></pre></div></div> <h3 id="64-推理masked-diffusion">6.4 推理（Masked Diffusion）</h3> <p><strong>数学</strong>：从全 MASK 序列出发，每一步用反向去噪分布 $p_\theta(x_{t-1} \mid x_t)$ 逐步揭开 MASK。具体策略：在 $t \to t-1$ 这一步，有比例为 $\frac{\bar\alpha_{t-1} - \bar\alpha_t}{1 - \bar\alpha_t}$ 的 MASK 位置被填上（从模型预测的分布中采样），其余 MASK 位置继续保持 MASK。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@torch.no_grad</span><span class="p">()</span>
<span class="k">def</span> <span class="nf">masked_diffusion_sample</span><span class="p">(</span><span class="n">model</span><span class="p">,</span> <span class="n">B</span><span class="p">,</span> <span class="n">L</span><span class="p">,</span> <span class="n">mask_id</span><span class="p">,</span> <span class="n">vocab_size</span><span class="p">):</span>
    <span class="n">x</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">full</span><span class="p">((</span><span class="n">B</span><span class="p">,</span> <span class="n">L</span><span class="p">),</span> <span class="n">mask_id</span><span class="p">)</span>     <span class="c1"># 全 MASK 序列，[B, L]
</span>
    <span class="k">for</span> <span class="n">t</span> <span class="ow">in</span> <span class="nf">reversed</span><span class="p">(</span><span class="nf">range</span><span class="p">(</span><span class="n">T</span><span class="p">)):</span>
        <span class="n">t_batch</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">full</span><span class="p">((</span><span class="n">B</span><span class="p">,),</span> <span class="n">t</span><span class="p">)</span>
        <span class="n">logits</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">t_batch</span><span class="p">)</span>       <span class="c1"># [B, L, V]
</span>
        <span class="c1"># 对所有位置采样（但只对 MASK 位置生效）
</span>        <span class="n">probs</span>   <span class="o">=</span> <span class="n">logits</span><span class="p">.</span><span class="nf">softmax</span><span class="p">(</span><span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>                        <span class="c1"># [B, L, V]
</span>        <span class="n">sampled</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">multinomial</span><span class="p">(</span><span class="n">probs</span><span class="p">.</span><span class="nf">view</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">vocab_size</span><span class="p">),</span> <span class="mi">1</span><span class="p">).</span><span class="nf">view</span><span class="p">(</span><span class="n">B</span><span class="p">,</span> <span class="n">L</span><span class="p">)</span>

        <span class="n">still_masked</span> <span class="o">=</span> <span class="p">(</span><span class="n">x</span> <span class="o">==</span> <span class="n">mask_id</span><span class="p">)</span>   <span class="c1"># [B, L]，当前还是 MASK 的位置
</span>
        <span class="k">if</span> <span class="n">t</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">:</span>
            <span class="c1"># 这一步要揭开的比例：ᾱ_{t-1} / ᾱ_t（每步只揭开一点点）
</span>            <span class="c1"># 剩余不揭开的比例：1 - ᾱ_{t-1} / ᾱ_t
</span>            <span class="n">remask_prob</span> <span class="o">=</span> <span class="mi">1</span> <span class="o">-</span> <span class="n">alpha_bar</span><span class="p">[</span><span class="n">t</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span> <span class="o">/</span> <span class="n">alpha_bar</span><span class="p">[</span><span class="n">t</span><span class="p">]</span>
            <span class="c1"># 以概率 (1 - remask_prob) 揭开 MASK，以概率 remask_prob 继续保持 MASK
</span>            <span class="n">reveal</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">bernoulli</span><span class="p">(</span>
                <span class="n">torch</span><span class="p">.</span><span class="nf">full_like</span><span class="p">(</span><span class="n">sampled</span><span class="p">.</span><span class="nf">float</span><span class="p">(),</span> <span class="mi">1</span> <span class="o">-</span> <span class="n">remask_prob</span><span class="p">)</span>
            <span class="p">).</span><span class="nf">bool</span><span class="p">()</span>
            <span class="n">x</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">where</span><span class="p">(</span><span class="n">still_masked</span> <span class="o">&amp;</span> <span class="n">reveal</span><span class="p">,</span> <span class="n">sampled</span><span class="p">,</span> <span class="n">x</span><span class="p">)</span>
        <span class="k">else</span><span class="p">:</span>
            <span class="c1"># 最后一步：把所有剩余 MASK 全部填上
</span>            <span class="n">x</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">where</span><span class="p">(</span><span class="n">still_masked</span><span class="p">,</span> <span class="n">sampled</span><span class="p">,</span> <span class="n">x</span><span class="p">)</span>

    <span class="k">return</span> <span class="n">x</span>   <span class="c1"># [B, L]，完整的 token 序列
</span></code></pre></div></div> <p><strong>对比连续版解码</strong>：离散版的解码无需”最近邻查找”，输出直接就是 logits，<code class="language-plaintext highlighter-rouge">argmax</code> 或 <code class="language-plaintext highlighter-rouge">multinomial</code> 一步得到 token id，干净利落。这是离散 diffusion 的主要优势。</p> <hr/> <h2 id="7-三条路线的维度总对比">7. 三条路线的维度总对比</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>【图像 DDPM】
  训练输入：
    x0        [B, C, H, W]   真实图像，像素 ∈ [-1, 1]
    eps       [B, C, H, W]   标准高斯噪声
    x_t       [B, C, H, W]   加噪图像（由 x0 和 eps 计算得到）
    t         [B]             时间步
  网络：UNet(x_t, t) → eps_pred [B, C, H, W]
  损失：MSE(eps_pred, eps)
  推理：x_T [B,C,H,W] → T步DDPM公式 → x_0 [B,C,H,W]

【连续文本 Diffusion】
  训练输入：
    token_ids  [B, L]         token id 序列
    x0         [B, L, d]      归一化 token embeddings
    eps        [B, L, d]      标准高斯噪声
    x_t        [B, L, d]      加噪 embedding
    t          [B]
  网络：Transformer(x_t, t) → eps_pred [B, L, d]
  损失：MSE(eps_pred, eps)
  推理：x_T [B,L,d] → T步DDPM → x_0 [B,L,d] → nearest neighbor → [B,L]

【离散 Masked Diffusion】
  训练输入：
    token_ids  [B, L]         原始 token id（整数，long）
    x_t        [B, L]         部分 masked 的 token id（整数，long）
    t          [B]
  网络：Transformer(x_t, t) → embed → [B,L,d] → out_proj → logits [B, L, V]
  损失：CrossEntropy(logits[mask], token_ids[mask])
  推理：全MASK [B,L] → T步逐步揭开 → [B,L]（完整序列）
</code></pre></div></div> <hr/> <h2 id="8-和-ar自回归llm-的本质区别">8. 和 AR（自回归）LLM 的本质区别</h2> <p>GPT 这类自回归模型：<strong>从左到右逐 token 生成，每个 token 只能看到左边的上下文</strong>。</p> <p>文本 Diffusion：<strong>并行生成整个序列，每个位置可以看到全局上下文</strong>（双向 attention）。</p> <table> <thead> <tr> <th> </th> <th>自回归 LLM</th> <th>文本 Diffusion</th> </tr> </thead> <tbody> <tr> <td>生成方式</td> <td>左到右，串行</td> <td>全局并行，多步去噪</td> </tr> <tr> <td>Attention</td> <td>单向（causal mask）</td> <td>双向（全局可见）</td> </tr> <tr> <td>推理步数</td> <td>= 序列长度 L</td> <td>= 去噪步数 T（通常 10~1000）</td> </tr> <tr> <td>条件建模</td> <td>直接：prefix → next token</td> <td>间接：condition embedding 注入</td> </tr> <tr> <td>编辑能力</td> <td>差（需要重新生成）</td> <td>强（可以只对部分位置去噪）</td> </tr> </tbody> </table> <p>文本 diffusion 的最大优势：<strong>全局一致性更好</strong>，<strong>可编辑性强</strong>（局部重新去噪）。劣势：推理慢（需要多步），质量目前仍不如 AR 大模型。</p> <hr/> <h2 id="9-条件生成怎么做文生图文生文">9. 条件生成：怎么做文生图、文生文</h2> <p>实际应用中需要<strong>条件生成</strong>：根据 prompt 生成图像或文本。条件信息通过 cross-attention 注入去噪网络。</p> <p><strong>数学</strong>：在去噪网络中，每一层先做 self-attention（序列内部交互），再做 cross-attention（与条件交互）：</p> \[\text{Attn}(Q, K, V) = \text{softmax}\!\left(\frac{QK^T}{\sqrt{d_k}}\right) V\] <ul> <li>Self-attention：$Q, K, V$ 均来自噪声序列 $x_t$（序列自身的全局上下文）</li> <li>Cross-attention：$Q$ 来自 $x_t$，$K, V$ 来自条件 $c$（将条件信息融入每个位置）</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">ConditionalDiffusionLayer</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x_t</span><span class="p">,</span> <span class="n">condition</span><span class="p">,</span> <span class="n">t_emb</span><span class="p">):</span>
        <span class="c1"># x_t:       [B, L, d]    噪声序列（query 来源）
</span>        <span class="c1"># condition: [B, Lc, d]   文本 encoder 输出（Lc 可以不等于 L）
</span>        <span class="c1"># t_emb:     [B, d]       时间步 embedding
</span>
        <span class="n">h</span> <span class="o">=</span> <span class="n">x_t</span> <span class="o">+</span> <span class="n">t_emb</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">,</span> <span class="p">:]</span>      <span class="c1"># [B, L, d]，注入时间信息
</span>
        <span class="c1"># 1. Self-attention：序列内部的全局交互
</span>        <span class="c1">#    Q = K = V 均来自 h，输出形状仍为 [B, L, d]
</span>        <span class="n">h</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">self_attn</span><span class="p">(</span><span class="n">h</span><span class="p">,</span> <span class="n">h</span><span class="p">,</span> <span class="n">h</span><span class="p">)</span>       <span class="c1"># [B, L, d]
</span>
        <span class="c1"># 2. Cross-attention：从条件中提取信息
</span>        <span class="c1">#    Q 来自 h，K/V 来自 condition
</span>        <span class="c1">#    Q: [B, L, d_head]，K/V: [B, Lc, d_head]
</span>        <span class="c1">#    attn(Q, K^T): [B, L, Lc]（每个噪声位置对所有条件位置的注意力权重）
</span>        <span class="c1">#    乘以 V: [B, L, d_head] → 融入条件信息
</span>        <span class="n">h</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">cross_attn</span><span class="p">(</span><span class="n">h</span><span class="p">,</span> <span class="n">condition</span><span class="p">,</span> <span class="n">condition</span><span class="p">)</span>  <span class="c1"># [B, L, d]
</span>
        <span class="n">h</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">ffn</span><span class="p">(</span><span class="n">h</span><span class="p">)</span>                   <span class="c1"># [B, L, d]
</span>        <span class="k">return</span> <span class="n">h</span>
</code></pre></div></div> <p><strong>维度变化</strong>：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>x_t       [B, L, d]     噪声序列
condition [B, Lc, d]    文本 condition（Lc 可以不等于 L）

cross-attention:
  Q = h @ W_Q           [B, L,  d_head]
  K = condition @ W_K   [B, Lc, d_head]
  V = condition @ W_V   [B, Lc, d_head]
  score = Q @ K^T       [B, L, Lc]         ← L 个噪声位置 × Lc 个条件 token
  out = score @ V       [B, L, d_head]     ← 每个噪声位置融入条件信息
  输出: [B, L, d]       ← 序列长度不变，每个位置都吸收了 condition
</code></pre></div></div> <hr/> <h2 id="10-完整流程总结">10. 完整流程总结</h2> <p><strong>图像 Diffusion 训练</strong>：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>随机 x0 [B,C,H,W] → 随机 t → 加噪得 x_t → UNet 预测 eps → MSE 损失
</code></pre></div></div> <p><strong>图像 Diffusion 推理</strong>：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>x_T [B,C,H,W]（纯噪声）→ T步去噪（每步 UNet 预测 eps，DDPM公式更新 x）→ x_0
</code></pre></div></div> <p><strong>文本连续 Diffusion 训练</strong>：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>token_ids [B,L] → embed+normalize → x0 [B,L,d] → 随机 t → 加噪 x_t
                → Transformer 预测 eps [B,L,d] → MSE 损失
</code></pre></div></div> <p><strong>文本连续 Diffusion 推理</strong>：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>x_T [B,L,d]（纯噪声）→ T步DDPM去噪 → x_0 [B,L,d] → nearest neighbor → [B,L]
</code></pre></div></div> <p><strong>文本 Masked Diffusion 训练</strong>：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>token_ids [B,L] → 随机 t → mask部分token → x_t [B,L]
                → Transformer → logits [B,L,V] → 只在mask位置算 CE 损失
</code></pre></div></div> <p><strong>文本 Masked Diffusion 推理</strong>：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>全MASK序列 [B,L] → T步逐步揭开（每步预测 logits，按概率揭开部分 MASK）→ 完整序列
</code></pre></div></div> <p>三条路线的本质是一样的：<strong>定义一个把数据破坏掉的过程，训练网络学会修复它</strong>。不同的只是破坏方式（加高斯 vs mask）、网络结构（UNet vs Transformer）、以及输出形式（连续 embedding vs 离散 logits）。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 Diffusion 和生成模型效率优化你想深入研究，可以看看我们团队出版的<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，书里 LLM 效率优化那章和 Diffusion 的加速思路有些呼应，感兴趣可以翻翻。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="Diffusion"/><category term="LLM"/><category term="生成模型"/><category term="数学推导"/><summary type="html"><![CDATA[从图像到文本：Diffusion 模型原理全解——数学、结构、训练、推理一次讲清]]></summary></entry><entry><title type="html">ACL’24 | LayerSkip：Meta 把 Self-Speculative Decoding 从头训到尾，直接干到 2.16×</title><link href="https://marsggbo.github.io/blog/2026/20260630-layerskip/" rel="alternate" type="text/html" title="ACL’24 | LayerSkip：Meta 把 Self-Speculative Decoding 从头训到尾，直接干到 2.16×"/><published>2026-06-30T00:00:00+00:00</published><updated>2026-06-30T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260630-layerskip</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260630-layerskip/"><![CDATA[<h1 id="acl24--layerskipmeta-把-self-speculative-decoding-从头训到尾直接干到-216">ACL’24 | LayerSkip：Meta 把 Self-Speculative Decoding 从头训到尾，直接干到 2.16×</h1> <blockquote> <p>原文：<a href="https://aclanthology.org/2024.acl-long.681">LayerSkip: Enabling Early Exit Inference and Self-Speculative Decoding</a> Mostafa Elhoushi, Akshat Shrivastava, Diana Liskovich et al.（Meta），ACL 2024</p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有遇到过这种情况：LLM 生成一段简单的文字，每个 token 都得走完模型的所有 32 层甚至更多，哪怕生成的是”的”、”是”这种明显不需要那么多算力的 token。</p> <p>这就是自回归解码的根本症结。推测解码（Speculative Decoding）这两年提出了一个很优雅的解法：用一个”小模型”快速生成多个候选 token，再让大模型一次性并行验证。但小模型从哪来？要么得专门训一个，要么从大模型里裁剪——额外显存、额外维护成本。</p> <p><strong>Draft &amp; Verify</strong>（2023 年）把这个思路往前推了一步：<strong>不要独立的小模型，直接跳过大模型的部分中间层来打草稿</strong>。同一套参数，浅版本生成草稿，完整版验证，零额外显存，最高做到 1.99×。</p> <p>但它有个硬伤——层配置需要离线贝叶斯搜索，每次跑 6-7 小时，而且结果 task-specific：在摘要任务上搜出来的最优跳层方案，拿到代码生成上用，acceptance rate 直接从 92% 跌到 68%。</p> <p><strong>为什么会这样？因为模型训练时压根没为”跳层推理”做过准备。</strong> 越靠后的层对前面所有层的输出依赖越深，强行跳掉中间几层，质量当然不稳定。</p> <p>LayerSkip（Meta，ACL 2024）的出发点就是这个：<strong>从训练阶段就让模型学会在任意早期层退出</strong>，推理时就不需要搜索了。</p> <hr/> <h2 id="2-训练两个改动">2. 训练：两个改动</h2> <p>LayerSkip 在训练时加了两样东西，改动不大，但效果很关键。</p> <h3 id="layer-dropout越靠后dropout-越猛">Layer Dropout：越靠后，dropout 越猛</h3> <p>普通 dropout 作用在单个神经元，LayerSkip 的 dropout 作用在整个 Transformer 层。关键在于：<strong>越靠后的层，dropout 概率越高</strong>：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>p_l = (l / L) × p_max
</code></pre></div></div> <p>第 0 层几乎不 dropout，最后一层 dropout 率最高（p_max 一般设 0.2-0.4）。训练时后面的层经常”消失”，模型就被迫在更浅的层学会输出有意义的表征——不然 loss 没法收敛。</p> <h3 id="shared-early-exit-loss所有层共用一个-lm-head">Shared Early Exit Loss：所有层共用一个 LM Head</h3> <p>光有 dropout 还不够。正常训练时，loss 只在最后一层算，浅层的 hidden state 根本没对齐到词表空间，即便从中间层强行退出，输出也是乱码。</p> <p>LayerSkip 的解法是<strong>所有层共享同一个 LM Head</strong>，训练时对多个中间层同时算 cross-entropy，加权求和作为总 loss：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>L_total = Σ_l weight_l × CE(LM_head(h_l), y)
</code></pre></div></div> <p>这样浅层 hidden state 也被强制对齐词表空间。下图展示了这个设计的本质——训练一次，等价于得到了多个不同深度的共享权重子模型：</p> <p><img src="/assets/img/posts/20260630-layerskip/figure3_p3.png" alt="Layer Dropout + Early Exit Loss 训出一系列不同深度的子模型"/></p> <hr/> <h2 id="3-推理early-exit--self-speculative-decoding">3. 推理：Early Exit + Self-Speculative Decoding</h2> <p>训好之后，LayerSkip 提供两种推理方式。</p> <p><strong>模式一：直接 Early Exit（有损，更快）</strong></p> <p>在第 e 层退出，不跑后面的层。有损但快，适合对精度要求不严的场景。下图展示了 Llama2 7B 在不同层 exit 时各任务的精度——绿线是 LayerSkip 训练配方，蓝线是普通训练模型：</p> <p><img src="/assets/img/posts/20260630-layerskip/figure5_p8.png" alt="Early Exit 在各任务上的精度随层数变化（绿=LayerSkip，蓝=baseline）"/></p> <p>LayerSkip 配方训出的模型（绿线）在各层 exit 时精度大幅高于普通训练。说明训练配方确实有效，浅层已经具备相当强的独立预测能力。</p> <p><strong>模式二：Self-Speculative Decoding（无损，主要贡献）</strong></p> <p>这才是论文的核心。Draft 阶段用前 E 层做快速草稿，Verify 阶段用完整 L 层验证，最终输出分布无损等价于完整自回归。</p> <h3 id="31-draft-阶段e-层前向token-by-token">3.1 Draft 阶段：E 层前向，token-by-token</h3> <p>每生成一个 draft token，只跑前 E 层（E &lt; L），然后直接用共享的 LM Head 输出分布：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">draft_phase</span><span class="p">(</span><span class="n">context</span><span class="p">,</span> <span class="n">draft_exit_layer</span><span class="p">,</span> <span class="n">d</span><span class="p">,</span> <span class="n">model</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    context:          当前已确认的 token 序列
    draft_exit_layer: E，草稿模型从这层退出（0-indexed）
    d:                最多生成 d 个草稿 token
    </span><span class="sh">"""</span>
    <span class="n">draft_tokens</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="n">draft_probs</span> <span class="o">=</span> <span class="p">[]</span>
    
    <span class="k">for</span> <span class="n">j</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">d</span><span class="p">):</span>
        <span class="c1"># 只跑前 E 层，KV cache 正常积累
</span>        <span class="n">hidden</span> <span class="o">=</span> <span class="n">context</span> <span class="o">+</span> <span class="n">draft_tokens</span>
        <span class="n">kv_cache</span> <span class="o">=</span> <span class="p">{}</span>
        <span class="k">for</span> <span class="n">l</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">draft_exit_layer</span> <span class="o">+</span> <span class="mi">1</span><span class="p">):</span>     <span class="c1"># 层 0 到 E
</span>            <span class="n">hidden</span><span class="p">,</span> <span class="n">kv_cache</span><span class="p">[</span><span class="n">l</span><span class="p">]</span> <span class="o">=</span> <span class="n">model</span><span class="p">.</span><span class="n">layers</span><span class="p">[</span><span class="n">l</span><span class="p">](</span><span class="n">hidden</span><span class="p">,</span> <span class="n">kv_cache</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="n">l</span><span class="p">))</span>
        
        <span class="c1"># 在第 E 层的 hidden state 上直接用 LM Head
</span>        <span class="c1"># 这一步有效的关键：训练时 shared early exit loss 保证了浅层 h_E 已对齐词表空间
</span>        <span class="n">logits</span> <span class="o">=</span> <span class="n">model</span><span class="p">.</span><span class="nf">lm_head</span><span class="p">(</span><span class="n">hidden</span><span class="p">[</span><span class="o">-</span><span class="mi">1</span><span class="p">])</span>   <span class="c1"># 只看最后一个位置
</span>        <span class="n">probs</span> <span class="o">=</span> <span class="nf">softmax</span><span class="p">(</span><span class="n">logits</span><span class="p">)</span>
        
        <span class="n">draft_token</span> <span class="o">=</span> <span class="n">probs</span><span class="p">.</span><span class="nf">argmax</span><span class="p">()</span>          <span class="c1"># greedy draft
</span>        <span class="n">draft_tokens</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">draft_token</span><span class="p">)</span>
        <span class="n">draft_probs</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">probs</span><span class="p">)</span>
    
    <span class="k">return</span> <span class="n">draft_tokens</span><span class="p">,</span> <span class="n">draft_probs</span><span class="p">,</span> <span class="n">kv_cache</span>
</code></pre></div></div> <p>整个 Draft 阶段共 d 次前向，每次只过 E 层，总计算量是 <code class="language-plaintext highlighter-rouge">d × E</code> 层的 attention。</p> <h3 id="32-verify-阶段l-层完整验证复用-kv-cache">3.2 Verify 阶段：L 层完整验证，复用 KV cache</h3> <p><strong>这是 LayerSkip SSD 的关键加速来源</strong>。Verify 阶段需要完整的 L 层模型来给出无偏的验证结果，但：</p> <ul> <li><strong>前 E 层的 KV cache 已经在 Draft 阶段算好了</strong>，不需要重算</li> <li>只需要额外运行层 E+1 到层 L（共 L-E 层）</li> <li>d 个 draft token 位置可以<strong>并行</strong>处理（一次 batch forward），不需要串行</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">verify_phase</span><span class="p">(</span><span class="n">context</span><span class="p">,</span> <span class="n">draft_tokens</span><span class="p">,</span> <span class="n">draft_kv_cache</span><span class="p">,</span> 
                 <span class="n">draft_exit_layer</span><span class="p">,</span> <span class="n">model</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    用完整 L 层模型并行验证 d 个 draft token
    </span><span class="sh">"""</span>
    <span class="n">full_sequence</span> <span class="o">=</span> <span class="n">context</span> <span class="o">+</span> <span class="n">draft_tokens</span>   <span class="c1"># [context_len + d] 个 token
</span>    
    <span class="c1"># 从层 draft_exit_layer+1 开始继续算，复用前 E 层的 KV cache
</span>    <span class="c1"># 对所有 d+1 个位置（context 最后一个 + d 个 draft）并行处理
</span>    <span class="n">hidden</span> <span class="o">=</span> <span class="n">draft_kv_cache</span><span class="p">[</span><span class="n">draft_exit_layer</span><span class="p">]</span>   <span class="c1"># 取第 E 层的 hidden state
</span>    <span class="k">for</span> <span class="n">l</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">draft_exit_layer</span> <span class="o">+</span> <span class="mi">1</span><span class="p">,</span> <span class="nf">len</span><span class="p">(</span><span class="n">model</span><span class="p">.</span><span class="n">layers</span><span class="p">)):</span>   <span class="c1"># 层 E+1 到 L
</span>        <span class="n">hidden</span><span class="p">,</span> <span class="n">_</span> <span class="o">=</span> <span class="n">model</span><span class="p">.</span><span class="n">layers</span><span class="p">[</span><span class="n">l</span><span class="p">](</span><span class="n">hidden</span><span class="p">)</span>
    
    <span class="n">verify_logits</span> <span class="o">=</span> <span class="n">model</span><span class="p">.</span><span class="nf">lm_head</span><span class="p">(</span><span class="n">hidden</span><span class="p">)</span>   <span class="c1"># [d+1, vocab_size]
</span>    <span class="c1"># verify_logits[i] 是完整模型对位置 context_len + i 的输出
</span>    
    <span class="k">return</span> <span class="n">verify_logits</span>
</code></pre></div></div> <p><strong>为什么 KV cache 可以复用？</strong></p> <p>Draft 阶段在层 E 做了早退出，前 E 层的 KV cache（attention 用的 key-value 对）是完全正确的——这些层没有跳过，计算是完整的。Verify 阶段运行同一个模型，前 E 层的输出结果是一样的，所以 KV cache 可以直接接续用，不需要重跑这些层。</p> <p>相比之下，如果用独立草稿模型（标准 Speculative Decoding），Verify 阶段就得从第 0 层重跑目标模型，没有任何复用。这是 Self-SD 相比标准 SD 的固有优势。</p> <p>如下图，三种方式的计算图对比非常直观：</p> <p><img src="/assets/img/posts/20260630-layerskip/figure4_p6.png" alt="自回归、标准推测解码、Self-Speculative Decoding 计算图对比"/></p> <ul> <li>自回归（AR）：每个 token 跑完整 L 层，串行</li> <li>标准 SD：草稿模型跑 L’ 层，验证模型重跑 L 层，没有复用</li> <li>LayerSkip SSD：草稿跑前 E 层，验证从层 E+1 开始继续跑，<strong>KV cache 无缝衔接</strong></li> </ul> <h3 id="33-接受拒绝与循环推理">3.3 接受/拒绝与循环推理</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">layerskip_ssd</span><span class="p">(</span><span class="n">prompt</span><span class="p">,</span> <span class="n">model</span><span class="p">,</span> <span class="n">E</span><span class="p">,</span> <span class="n">d</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    LayerSkip Self-Speculative Decoding 完整推理循环
    E: draft exit layer（通常 L/5 到 L/3）
    d: speculation length（通常 10-14）
    </span><span class="sh">"""</span>
    <span class="n">context</span> <span class="o">=</span> <span class="n">prompt</span>
    <span class="n">output</span> <span class="o">=</span> <span class="p">[]</span>
    
    <span class="k">while</span> <span class="ow">not</span> <span class="n">done</span><span class="p">:</span>
        <span class="c1"># Draft：d 次浅层前向，每次只过 E 层
</span>        <span class="n">draft_tokens</span><span class="p">,</span> <span class="n">draft_probs</span><span class="p">,</span> <span class="n">kv_cache</span> <span class="o">=</span> <span class="nf">draft_phase</span><span class="p">(</span>
            <span class="n">context</span><span class="p">,</span> <span class="n">draft_exit_layer</span><span class="o">=</span><span class="n">E</span><span class="p">,</span> <span class="n">d</span><span class="o">=</span><span class="n">d</span><span class="p">,</span> <span class="n">model</span><span class="o">=</span><span class="n">model</span><span class="p">)</span>
        
        <span class="c1"># Verify：一次 batch forward，从层 E+1 开始，复用 KV cache
</span>        <span class="n">verify_logits</span> <span class="o">=</span> <span class="nf">verify_phase</span><span class="p">(</span>
            <span class="n">context</span><span class="p">,</span> <span class="n">draft_tokens</span><span class="p">,</span> <span class="n">kv_cache</span><span class="p">,</span> 
            <span class="n">draft_exit_layer</span><span class="o">=</span><span class="n">E</span><span class="p">,</span> <span class="n">model</span><span class="o">=</span><span class="n">model</span><span class="p">)</span>
        
        <span class="c1"># 接受/拒绝：标准推测解码接受条件
</span>        <span class="n">accepted</span> <span class="o">=</span> <span class="mi">0</span>
        <span class="n">new_token</span> <span class="o">=</span> <span class="bp">None</span>
        <span class="k">for</span> <span class="n">j</span><span class="p">,</span> <span class="p">(</span><span class="n">d_t</span><span class="p">,</span> <span class="n">q_t</span><span class="p">)</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="nf">zip</span><span class="p">(</span><span class="n">draft_tokens</span><span class="p">,</span> <span class="n">draft_probs</span><span class="p">)):</span>
            <span class="n">p_t</span> <span class="o">=</span> <span class="n">verify_logits</span><span class="p">[</span><span class="n">j</span><span class="p">].</span><span class="nf">softmax</span><span class="p">()</span>
            
            <span class="c1"># 接受概率 = min(1, p_full / p_draft)
</span>            <span class="n">accept_prob</span> <span class="o">=</span> <span class="nf">min</span><span class="p">(</span><span class="mf">1.0</span><span class="p">,</span> <span class="n">p_t</span><span class="p">[</span><span class="n">d_t</span><span class="p">]</span> <span class="o">/</span> <span class="n">q_t</span><span class="p">[</span><span class="n">d_t</span><span class="p">])</span>
            
            <span class="k">if</span> <span class="nf">random</span><span class="p">()</span> <span class="o">&lt;</span> <span class="n">accept_prob</span><span class="p">:</span>
                <span class="n">accepted</span> <span class="o">+=</span> <span class="mi">1</span>   <span class="c1"># 接受，继续检查下一个
</span>            <span class="k">else</span><span class="p">:</span>
                <span class="c1"># 拒绝：从修正分布 norm(max(0, p - q)) 重采样
</span>                <span class="n">correction</span> <span class="o">=</span> <span class="nf">sample</span><span class="p">(</span><span class="nf">norm</span><span class="p">(</span><span class="nf">max</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">p_t</span> <span class="o">-</span> <span class="n">q_t</span><span class="p">)))</span>
                <span class="n">new_token</span> <span class="o">=</span> <span class="n">correction</span>
                <span class="k">break</span>
        
        <span class="c1"># 如果全部接受，还要从完整模型多采一个 bonus token
</span>        <span class="k">if</span> <span class="n">accepted</span> <span class="o">==</span> <span class="nf">len</span><span class="p">(</span><span class="n">draft_tokens</span><span class="p">)</span> <span class="ow">and</span> <span class="n">new_token</span> <span class="ow">is</span> <span class="bp">None</span><span class="p">:</span>
            <span class="n">new_token</span> <span class="o">=</span> <span class="nf">sample</span><span class="p">(</span><span class="n">verify_logits</span><span class="p">[</span><span class="n">accepted</span><span class="p">].</span><span class="nf">softmax</span><span class="p">())</span>
        
        <span class="n">output</span><span class="p">.</span><span class="nf">extend</span><span class="p">(</span><span class="n">draft_tokens</span><span class="p">[:</span><span class="n">accepted</span><span class="p">])</span>
        <span class="k">if</span> <span class="n">new_token</span> <span class="ow">is</span> <span class="ow">not</span> <span class="bp">None</span><span class="p">:</span>
            <span class="n">output</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">new_token</span><span class="p">)</span>
        
        <span class="n">context</span> <span class="o">=</span> <span class="n">context</span> <span class="o">+</span> <span class="n">output</span><span class="p">[</span><span class="o">-</span><span class="nf">len</span><span class="p">(</span><span class="n">context</span><span class="p">):]</span>   <span class="c1"># 更新上下文
</span>    
    <span class="k">return</span> <span class="n">output</span>
</code></pre></div></div> <p><strong>每轮的效果</strong>：</p> <table> <thead> <tr> <th>接受情况</th> <th>每次 verify 前向产出的 token 数</th> </tr> </thead> <tbody> <tr> <td>全部接受（d 个）</td> <td>d+1 个（d 个 draft + 1 个 bonus）</td> </tr> <tr> <td>接受 k 个后拒绝</td> <td>k+1 个（k 个 draft + 1 个修正）</td> </tr> <tr> <td>第一个就拒绝</td> <td>1 个（修正 token）</td> </tr> </tbody> </table> <p>每次 verify 至少得到 1 个 token，最多得到 d+1 个，比自回归每步只得 1 个好。</p> <p><strong>两种模式的对比</strong>：</p> <table> <thead> <tr> <th> </th> <th>Early Exit（有损）</th> <th>Self-Speculative（无损）</th> </tr> </thead> <tbody> <tr> <td>输出质量</td> <td>低于完整模型</td> <td>完全等价于完整模型</td> </tr> <tr> <td>计算量</td> <td>每 token E 层</td> <td>每轮 d×E + L 层（平均摊下来更少）</td> </tr> <tr> <td>适用场景</td> <td>质量不敏感场景</td> <td>需要完整模型质量的场景</td> </tr> <tr> <td>KV cache</td> <td>只积累 E 层</td> <td>Draft 积累 E 层，Verify 复用后补</td> </tr> </tbody> </table> <hr/> <h2 id="4-效果怎么样">4. 效果怎么样</h2> <p>实验跑在 Llama2 系列上，四种训练场景都测了（从零预训练、continual pretraining、domain finetuning、task finetuning）。</p> <p>下表是 continual pretraining 配方的主要结果，对比了自回归、Early Exit、Self-Speculative 三种模式，以及 Draft &amp; Verify 作为 baseline：</p> <p><img src="/assets/img/posts/20260630-layerskip/table1_p9.png" alt="主要实验结果：Llama2 7B/13B 在 CNN-DM、XSUM、HumanEval 上的对比"/></p> <p>几个数字值得关注：</p> <ul> <li>Llama2 7B，CNN-DM 摘要：Self-Speculative 到 <strong>1.86×</strong>，ROUGE-2 从 0.079 降到 0.078，几乎无损</li> <li>Llama2 13B，CNN-DM：<strong>1.81×</strong>，ROUGE-2 完全一致（0.098）</li> <li>Llama2 7B，HumanEval 代码：<strong>1.83×</strong>，pass@1 不降反升（0.041 → 0.042，噪声范围内）</li> </ul> <p>Early Exit 那行速度确实更快（7B 在 CNN-DM 上 232.4 tokens/sec，是自回归的 3.7×），但质量损失明显（ROUGE-2 从 0.079 跌到 0.012）。所以实际用途里 Self-Speculative 更合理，保无损同时拿加速。</p> <p>参数怎么选（exit layer E 和 speculation length d）？论文给了一张热力图，横轴是 exit layer，纵轴是 speculation length，颜色是 tokens/sec：</p> <p><img src="/assets/img/posts/20260630-layerskip/figure6_p9.png" alt="Self-Speculative Decoding 参数搜索热力图（黄色区域是最优点）"/></p> <p>最优点集中在 exit layer ≈ 8，speculation length ≈ 10-14 附近，最优区域很宽，不需要精细调参。</p> <hr/> <h2 id="5-一点个人看法">5. 一点个人看法</h2> <p>细思极恐的一件事是：这个训练配方其实挺”便宜”——Layer Dropout 加上一个共享 LM Head，改动非常小，但训出来的模型具备了普通模型不具备的”任意层退出”能力。这说明模型在正常训练中其实有大量”浪费”——后面的层做的很多事情，用更少的层本来也可以做到，只是没有显式地去训。</p> <p>LayerSkip 的主要限制是它需要重新训练。对于大多数人来说，能用的只有 Meta 官方训好的 Llama 系列，自己重训一个 70B 不现实。所以这套方案虽然效果好，适用范围实际上比较窄——除非你在某个具体任务上做 continual pretraining 或 finetuning，这时候加上 LayerSkip 配方是有意义的。</p> <p>这个限制后来被 SWIFT 正面解决——不改训练，在推理阶段在线自适应地搜索最优层配置，优化开销只有 0.8%。那篇文章我们后面聊。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 LLM 推理效率优化你想系统深入，可以看看我之前出版的<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，书里有专章讲 LLM 参数高效方法和推理优化，和本文的训练配方设计思路有直接关联。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="推理加速"/><category term="推测解码"/><category term="Self-Speculative Decoding"/><category term="论文解读"/><summary type="html"><![CDATA[ACL’24 | LayerSkip：Meta 把 Self-Speculative Decoding 从头训到尾，直接干到 2.16×]]></summary></entry><entry><title type="html">EMNLP’23 | 不用额外模型，LLM 自己给自己加速——Self-Speculative Decoding 原理详解</title><link href="https://marsggbo.github.io/blog/2026/20260630-self-spec-decoding/" rel="alternate" type="text/html" title="EMNLP’23 | 不用额外模型，LLM 自己给自己加速——Self-Speculative Decoding 原理详解"/><published>2026-06-30T00:00:00+00:00</published><updated>2026-06-30T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260630-self-spec-decoding</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260630-self-spec-decoding/"><![CDATA[<h1 id="emnlp23--不用额外模型llm-自己给自己加速self-speculative-decoding-原理详解">EMNLP’23 | 不用额外模型，LLM 自己给自己加速——Self-Speculative Decoding 原理详解</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2309.08168">Draft &amp; Verify: Lossless Large Language Model Acceleration via Self-Speculative Decoding</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有想过，LLM 推理慢，根子上是慢在哪里？</p> <p>不是矩阵乘法太重——现代 GPU 做矩阵乘法其实挺快的。真正的瓶颈是<strong>内存带宽</strong>。每生成一个 token，模型要把所有权重从显存搬到计算单元跑一遍，然后吐出一个 token。然后再搬一遍，再吐一个。这个过程是<strong>严格串行</strong>的，完全没办法并行。</p> <p>Speculative Decoding（投机解码）就是为了解决这个问题而生的。核心思路是：找一个便宜的小模型快速起草（draft）一批 token，然后让大模型一次性并行验证这批 token，接受对的，拒绝错的，但<strong>输出分布保证和原始大模型完全一致</strong>——也就是说，这是<strong>无损加速</strong>。</p> <p>但有个很现实的问题：<strong>从哪儿来这个小模型？</strong></p> <p>标准 Speculative Decoding 需要一个独立的 draft 模型，参数规模通常是 target 模型的 1/10 左右（比如 7B 配一个 700M），还得保证两个模型词表对齐、输出分布相近。这在工程落地上非常麻烦：要额外维护一个模型、占额外显存、还要操心对齐问题。</p> <p>这篇来自 IBM Research 的工作给出了一个优雅的解法：</p> <p><strong>不需要额外的 draft 模型。用同一个模型、跳过部分层来起草，再用完整模型验证。</strong></p> <p>这就是 <strong>Self-Speculative Decoding</strong>。</p> <hr/> <h2 id="2-核心思路用残缺版自己起草">2. 核心思路：用”残缺版自己”起草</h2> <h3 id="21-为什么可以跳层">2.1 为什么可以跳层？</h3> <p>LLM 中有个被反复观察到的现象：<strong>并不是每一层都同等重要</strong>。</p> <p>中间层的 attention 和 MLP，在很多 token 的推理中贡献其实很小——跳过它们，输出的分布变化不大。这和知识蒸馏、层剪枝的直觉是一脉相承的。</p> <p>Self-Speculative Decoding 利用的就是这个性质：<strong>跳过一部分中间层，得到一个”瘦身版”的 draft 模型</strong>，它的权重和原模型完全共享，不需要额外存储，推理速度更快。</p> <h3 id="22-整体流程">2.2 整体流程</h3> <p>如下图，整个过程分两个阶段交替进行：</p> <p><img src="/assets/img/posts/20260630-self-spec-decoding/001_Figure_1.png" alt="self-speculative decoding 流程示意"/></p> <p><strong>Draft 阶段</strong>（跳层快速生成）：用跳过若干层的残缺模型，自回归生成 K 个候选 token。这一步每个 token 的计算量只有完整模型的一个子集，速度很快。</p> <p><strong>Verify 阶段</strong>（完整模型并行验证）：把这 K 个 draft token 一次性送进完整模型，做一次并行前向。验证的逻辑如下：</p> <p>设完整模型在位置 $t$ 给出的分布是 $p(x_t)$，draft 模型给出的是 $q(x_t)$，draft 采样出的 token 是 $\tilde{x}_t$。</p> <p>接受概率定义为：</p> \[\alpha_t = \min\left(1, \frac{p(\tilde{x}_t)}{q(\tilde{x}_t)}\right)\] <p>以概率 $\alpha_t$ 接受 $\tilde{x}_t$，否则按修正分布 $\text{norm}(\max(0, p - q))$ 重新采样。</p> <p>这套机制保证了最终输出分布严格等于完整模型，是<strong>有理论保证的无损加速</strong>。</p> <hr/> <h2 id="3-跳哪些层用贝叶斯优化搜">3. 跳哪些层？用贝叶斯优化搜</h2> <p>这套方法最关键的工程问题是：<strong>具体跳哪些层？</strong></p> <p>不是随便跳都行的。跳太少加速不明显，跳太多 draft 质量太差，验证阶段几乎全拒绝，整体反而更慢。</p> <p>论文用<strong>贝叶斯优化</strong>来搜最优的 skip 层组合，如下图：</p> <p><img src="/assets/img/posts/20260630-self-spec-decoding/003_Figure_3.png" alt="用贝叶斯优化搜索最优跳层方案"/></p> <p>搜索过程是一个经典的 BO 循环：</p> <ol> <li>给定一个 skip 层的二值向量 $z^* \in {0,1}^L$（1 表示跳过该层）</li> <li>用黑盒评估函数 $f(z^*)$ 跑一遍 self-speculative decoding，得到每个 token 的实际推理耗时</li> <li>用高斯过程拟合目标函数，用 acquisition function 采样下一个候选点</li> <li>迭代若干轮，找到最优的 skip 组合</li> </ol> <p>由于搜索空间是离散的（每层独立跳/不跳），BO 的效率优势非常明显——不需要穷举所有 $2^L$ 种组合。</p> <p>用代码把这个搜索流程复现出来：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>
<span class="kn">from</span> <span class="n">sklearn.gaussian_process</span> <span class="kn">import</span> <span class="n">GaussianProcessRegressor</span>
<span class="kn">from</span> <span class="n">sklearn.gaussian_process.kernels</span> <span class="kn">import</span> <span class="n">Matern</span>
<span class="kn">from</span> <span class="n">scipy.stats</span> <span class="kn">import</span> <span class="n">norm</span>

<span class="k">def</span> <span class="nf">evaluate_skip_config</span><span class="p">(</span><span class="n">model</span><span class="p">,</span> <span class="n">tokenizer</span><span class="p">,</span> <span class="n">skip_layers</span><span class="p">,</span> <span class="n">calibration_texts</span><span class="p">,</span> <span class="n">K</span><span class="o">=</span><span class="mi">4</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    黑盒评估函数 f(z*)：
    给定一个 skip 层组合，跑一遍 self-speculative decoding，
    返回平均每个 token 的生成耗时（越低越好）。
    </span><span class="sh">"""</span>
    <span class="kn">import</span> <span class="n">time</span>
    <span class="n">total_tokens</span> <span class="o">=</span> <span class="mi">0</span>
    <span class="n">total_time</span> <span class="o">=</span> <span class="mf">0.0</span>

    <span class="k">for</span> <span class="n">text</span> <span class="ow">in</span> <span class="n">calibration_texts</span><span class="p">:</span>
        <span class="n">inputs</span> <span class="o">=</span> <span class="nf">tokenizer</span><span class="p">(</span><span class="n">text</span><span class="p">,</span> <span class="n">return_tensors</span><span class="o">=</span><span class="sh">"</span><span class="s">pt</span><span class="sh">"</span><span class="p">).</span><span class="nf">to</span><span class="p">(</span><span class="n">model</span><span class="p">.</span><span class="n">device</span><span class="p">)</span>
        <span class="n">start</span> <span class="o">=</span> <span class="n">time</span><span class="p">.</span><span class="nf">perf_counter</span><span class="p">()</span>
        <span class="n">output</span> <span class="o">=</span> <span class="nf">self_speculative_decode</span><span class="p">(</span>
            <span class="n">model</span><span class="p">,</span> <span class="n">inputs</span><span class="p">.</span><span class="n">input_ids</span><span class="p">,</span> <span class="n">skip_layers</span><span class="o">=</span><span class="n">skip_layers</span><span class="p">,</span> <span class="n">K</span><span class="o">=</span><span class="n">K</span><span class="p">,</span> <span class="n">max_new_tokens</span><span class="o">=</span><span class="mi">50</span>
        <span class="p">)</span>
        <span class="n">elapsed</span> <span class="o">=</span> <span class="n">time</span><span class="p">.</span><span class="nf">perf_counter</span><span class="p">()</span> <span class="o">-</span> <span class="n">start</span>
        <span class="n">new_tokens</span> <span class="o">=</span> <span class="n">output</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span> <span class="o">-</span> <span class="n">inputs</span><span class="p">.</span><span class="n">input_ids</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span>
        <span class="n">total_tokens</span> <span class="o">+=</span> <span class="n">new_tokens</span>
        <span class="n">total_time</span> <span class="o">+=</span> <span class="n">elapsed</span>

    <span class="k">return</span> <span class="n">total_time</span> <span class="o">/</span> <span class="nf">max</span><span class="p">(</span><span class="n">total_tokens</span><span class="p">,</span> <span class="mi">1</span><span class="p">)</span>  <span class="c1"># 越小越好
</span>

<span class="k">def</span> <span class="nf">expected_improvement</span><span class="p">(</span><span class="n">mu</span><span class="p">,</span> <span class="n">sigma</span><span class="p">,</span> <span class="n">best_so_far</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    EI acquisition function：
    估计在当前高斯过程预测下，新候选点比历史最优提升多少。
    mu/sigma 是 GP 对候选点的预测均值和标准差。
    </span><span class="sh">"""</span>
    <span class="n">z</span> <span class="o">=</span> <span class="p">(</span><span class="n">best_so_far</span> <span class="o">-</span> <span class="n">mu</span><span class="p">)</span> <span class="o">/</span> <span class="p">(</span><span class="n">sigma</span> <span class="o">+</span> <span class="mf">1e-9</span><span class="p">)</span>
    <span class="n">ei</span> <span class="o">=</span> <span class="p">(</span><span class="n">best_so_far</span> <span class="o">-</span> <span class="n">mu</span><span class="p">)</span> <span class="o">*</span> <span class="n">norm</span><span class="p">.</span><span class="nf">cdf</span><span class="p">(</span><span class="n">z</span><span class="p">)</span> <span class="o">+</span> <span class="n">sigma</span> <span class="o">*</span> <span class="n">norm</span><span class="p">.</span><span class="nf">pdf</span><span class="p">(</span><span class="n">z</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">ei</span><span class="p">.</span><span class="nf">clip</span><span class="p">(</span><span class="nb">min</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>


<span class="k">def</span> <span class="nf">bayesian_optimize_skip_layers</span><span class="p">(</span>
    <span class="n">model</span><span class="p">,</span>
    <span class="n">tokenizer</span><span class="p">,</span>
    <span class="n">num_layers</span><span class="p">:</span> <span class="nb">int</span><span class="p">,</span>           <span class="c1"># 模型总层数，如 LLaMA-2-13B = 40
</span>    <span class="n">max_skip_ratio</span><span class="p">:</span> <span class="nb">float</span> <span class="o">=</span> <span class="mf">0.5</span><span class="p">,</span>  <span class="c1"># 最多跳多少比例的层（防止 draft 质量崩掉）
</span>    <span class="n">n_init</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">10</span><span class="p">,</span>          <span class="c1"># 初始随机探索的点数
</span>    <span class="n">n_iter</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">40</span><span class="p">,</span>          <span class="c1"># BO 迭代轮数
</span>    <span class="n">K</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">4</span><span class="p">,</span>
    <span class="n">calibration_texts</span><span class="o">=</span><span class="bp">None</span><span class="p">,</span>
<span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    用贝叶斯优化搜索最优 skip 层组合。
    搜索空间：长度为 num_layers 的二值向量，1 表示跳过该层。
    目标：最小化 evaluate_skip_config 返回的每 token 耗时。
    </span><span class="sh">"""</span>
    <span class="k">if</span> <span class="n">calibration_texts</span> <span class="ow">is</span> <span class="bp">None</span><span class="p">:</span>
        <span class="n">calibration_texts</span> <span class="o">=</span> <span class="p">[</span><span class="sh">"</span><span class="s">Tell me about the history of artificial intelligence.</span><span class="sh">"</span><span class="p">]</span> <span class="o">*</span> <span class="mi">5</span>

    <span class="n">max_skip</span> <span class="o">=</span> <span class="nf">int</span><span class="p">(</span><span class="n">num_layers</span> <span class="o">*</span> <span class="n">max_skip_ratio</span><span class="p">)</span>

    <span class="c1"># ---- 1. 把二值向量映射到连续空间供 GP 使用 ----
</span>    <span class="c1"># 直接用二值向量本身作为特征（整数 0/1），Matern 核在离散空间也工作得不错
</span>    <span class="n">X_observed</span> <span class="o">=</span> <span class="p">[]</span>   <span class="c1"># shape: (n_obs, num_layers)，每行是一个 skip 配置
</span>    <span class="n">y_observed</span> <span class="o">=</span> <span class="p">[]</span>   <span class="c1"># shape: (n_obs,)，对应的评估耗时
</span>
    <span class="k">def</span> <span class="nf">sample_valid_config</span><span class="p">():</span>
        <span class="c1"># 随机生成一个合法的 skip 配置（跳层数不超过 max_skip）
</span>        <span class="n">n_skip</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">randint</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="n">max_skip</span> <span class="o">+</span> <span class="mi">1</span><span class="p">)</span>
        <span class="c1"># 论文观察到浅层/深层不适合跳，这里加个软约束：
</span>        <span class="c1"># 只从中间 60% 的层里采样 skip 候选
</span>        <span class="n">mid_start</span> <span class="o">=</span> <span class="n">num_layers</span> <span class="o">//</span> <span class="mi">5</span>
        <span class="n">mid_end</span> <span class="o">=</span> <span class="n">num_layers</span> <span class="o">-</span> <span class="n">num_layers</span> <span class="o">//</span> <span class="mi">5</span>
        <span class="n">candidates</span> <span class="o">=</span> <span class="nf">list</span><span class="p">(</span><span class="nf">range</span><span class="p">(</span><span class="n">mid_start</span><span class="p">,</span> <span class="n">mid_end</span><span class="p">))</span>
        <span class="n">skip_idx</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">choice</span><span class="p">(</span><span class="n">candidates</span><span class="p">,</span> <span class="n">size</span><span class="o">=</span><span class="nf">min</span><span class="p">(</span><span class="n">n_skip</span><span class="p">,</span> <span class="nf">len</span><span class="p">(</span><span class="n">candidates</span><span class="p">)),</span> <span class="n">replace</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>
        <span class="n">z</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">num_layers</span><span class="p">,</span> <span class="n">dtype</span><span class="o">=</span><span class="nb">int</span><span class="p">)</span>
        <span class="n">z</span><span class="p">[</span><span class="n">skip_idx</span><span class="p">]</span> <span class="o">=</span> <span class="mi">1</span>
        <span class="k">return</span> <span class="n">z</span>

    <span class="c1"># ---- 2. 初始随机探索 ----
</span>    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">[BO] 初始随机探索 </span><span class="si">{</span><span class="n">n_init</span><span class="si">}</span><span class="s"> 个配置...</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">n_init</span><span class="p">):</span>
        <span class="n">z</span> <span class="o">=</span> <span class="nf">sample_valid_config</span><span class="p">()</span>
        <span class="n">skip_layers</span> <span class="o">=</span> <span class="nf">list</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">where</span><span class="p">(</span><span class="n">z</span> <span class="o">==</span> <span class="mi">1</span><span class="p">)[</span><span class="mi">0</span><span class="p">])</span>
        <span class="n">cost</span> <span class="o">=</span> <span class="nf">evaluate_skip_config</span><span class="p">(</span><span class="n">model</span><span class="p">,</span> <span class="n">tokenizer</span><span class="p">,</span> <span class="n">skip_layers</span><span class="p">,</span> <span class="n">calibration_texts</span><span class="p">,</span> <span class="n">K</span><span class="p">)</span>
        <span class="n">X_observed</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">z</span><span class="p">)</span>
        <span class="n">y_observed</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">cost</span><span class="p">)</span>
        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  [</span><span class="si">{</span><span class="n">i</span><span class="o">+</span><span class="mi">1</span><span class="si">}</span><span class="s">/</span><span class="si">{</span><span class="n">n_init</span><span class="si">}</span><span class="s">] skip=</span><span class="si">{</span><span class="n">skip_layers</span><span class="si">}</span><span class="s"> -&gt; </span><span class="si">{</span><span class="n">cost</span><span class="si">:</span><span class="p">.</span><span class="mi">4</span><span class="n">f</span><span class="si">}</span><span class="s">s/token</span><span class="sh">"</span><span class="p">)</span>

    <span class="c1"># ---- 3. BO 迭代 ----
</span>    <span class="n">gp</span> <span class="o">=</span> <span class="nc">GaussianProcessRegressor</span><span class="p">(</span>
        <span class="n">kernel</span><span class="o">=</span><span class="nc">Matern</span><span class="p">(</span><span class="n">nu</span><span class="o">=</span><span class="mf">2.5</span><span class="p">),</span>
        <span class="n">alpha</span><span class="o">=</span><span class="mf">1e-6</span><span class="p">,</span>
        <span class="n">normalize_y</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
        <span class="n">n_restarts_optimizer</span><span class="o">=</span><span class="mi">5</span><span class="p">,</span>
    <span class="p">)</span>

    <span class="n">best_cost</span> <span class="o">=</span> <span class="nf">min</span><span class="p">(</span><span class="n">y_observed</span><span class="p">)</span>
    <span class="n">best_config</span> <span class="o">=</span> <span class="n">X_observed</span><span class="p">[</span><span class="n">np</span><span class="p">.</span><span class="nf">argmin</span><span class="p">(</span><span class="n">y_observed</span><span class="p">)]</span>

    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="se">\n</span><span class="s">[BO] 开始迭代优化，初始最优耗时 </span><span class="si">{</span><span class="n">best_cost</span><span class="si">:</span><span class="p">.</span><span class="mi">4</span><span class="n">f</span><span class="si">}</span><span class="s">s/token</span><span class="sh">"</span><span class="p">)</span>

    <span class="k">for</span> <span class="n">iteration</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">n_iter</span><span class="p">):</span>
        <span class="c1"># 用已有观测拟合 GP
</span>        <span class="n">X_arr</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">array</span><span class="p">(</span><span class="n">X_observed</span><span class="p">,</span> <span class="n">dtype</span><span class="o">=</span><span class="nb">float</span><span class="p">)</span>
        <span class="n">y_arr</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">array</span><span class="p">(</span><span class="n">y_observed</span><span class="p">)</span>
        <span class="n">gp</span><span class="p">.</span><span class="nf">fit</span><span class="p">(</span><span class="n">X_arr</span><span class="p">,</span> <span class="n">y_arr</span><span class="p">)</span>

        <span class="c1"># 随机生成一批候选配置，用 EI 挑最有潜力的
</span>        <span class="n">n_candidates</span> <span class="o">=</span> <span class="mi">2000</span>
        <span class="n">candidates</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">array</span><span class="p">([</span><span class="nf">sample_valid_config</span><span class="p">()</span> <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">n_candidates</span><span class="p">)],</span> <span class="n">dtype</span><span class="o">=</span><span class="nb">float</span><span class="p">)</span>
        <span class="n">mu</span><span class="p">,</span> <span class="n">sigma</span> <span class="o">=</span> <span class="n">gp</span><span class="p">.</span><span class="nf">predict</span><span class="p">(</span><span class="n">candidates</span><span class="p">,</span> <span class="n">return_std</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
        <span class="n">ei_scores</span> <span class="o">=</span> <span class="nf">expected_improvement</span><span class="p">(</span><span class="n">mu</span><span class="p">,</span> <span class="n">sigma</span><span class="p">,</span> <span class="n">best_cost</span><span class="p">)</span>

        <span class="c1"># 选 EI 最高的候选点
</span>        <span class="n">best_candidate_idx</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">argmax</span><span class="p">(</span><span class="n">ei_scores</span><span class="p">)</span>
        <span class="n">z_next</span> <span class="o">=</span> <span class="n">candidates</span><span class="p">[</span><span class="n">best_candidate_idx</span><span class="p">].</span><span class="nf">astype</span><span class="p">(</span><span class="nb">int</span><span class="p">)</span>
        <span class="n">skip_layers_next</span> <span class="o">=</span> <span class="nf">list</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">where</span><span class="p">(</span><span class="n">z_next</span> <span class="o">==</span> <span class="mi">1</span><span class="p">)[</span><span class="mi">0</span><span class="p">])</span>

        <span class="c1"># 评估这个候选点
</span>        <span class="n">cost_next</span> <span class="o">=</span> <span class="nf">evaluate_skip_config</span><span class="p">(</span><span class="n">model</span><span class="p">,</span> <span class="n">tokenizer</span><span class="p">,</span> <span class="n">skip_layers_next</span><span class="p">,</span> <span class="n">calibration_texts</span><span class="p">,</span> <span class="n">K</span><span class="p">)</span>
        <span class="n">X_observed</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">z_next</span><span class="p">)</span>
        <span class="n">y_observed</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">cost_next</span><span class="p">)</span>

        <span class="k">if</span> <span class="n">cost_next</span> <span class="o">&lt;</span> <span class="n">best_cost</span><span class="p">:</span>
            <span class="n">best_cost</span> <span class="o">=</span> <span class="n">cost_next</span>
            <span class="n">best_config</span> <span class="o">=</span> <span class="n">z_next</span>
            <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  [iter </span><span class="si">{</span><span class="n">iteration</span><span class="o">+</span><span class="mi">1</span><span class="si">}</span><span class="s">] ✓ 新最优！skip=</span><span class="si">{</span><span class="n">skip_layers_next</span><span class="si">}</span><span class="s"> -&gt; </span><span class="si">{</span><span class="n">cost_next</span><span class="si">:</span><span class="p">.</span><span class="mi">4</span><span class="n">f</span><span class="si">}</span><span class="s">s/token</span><span class="sh">"</span><span class="p">)</span>
        <span class="k">else</span><span class="p">:</span>
            <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  [iter </span><span class="si">{</span><span class="n">iteration</span><span class="o">+</span><span class="mi">1</span><span class="si">}</span><span class="s">] skip=</span><span class="si">{</span><span class="n">skip_layers_next</span><span class="si">}</span><span class="s"> -&gt; </span><span class="si">{</span><span class="n">cost_next</span><span class="si">:</span><span class="p">.</span><span class="mi">4</span><span class="n">f</span><span class="si">}</span><span class="s">s/token</span><span class="sh">"</span><span class="p">)</span>

    <span class="n">best_skip_layers</span> <span class="o">=</span> <span class="nf">list</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">where</span><span class="p">(</span><span class="n">best_config</span> <span class="o">==</span> <span class="mi">1</span><span class="p">)[</span><span class="mi">0</span><span class="p">])</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="se">\n</span><span class="s">[BO] 搜索完成。最优 skip 层：</span><span class="si">{</span><span class="n">best_skip_layers</span><span class="si">}</span><span class="s">，耗时 </span><span class="si">{</span><span class="n">best_cost</span><span class="si">:</span><span class="p">.</span><span class="mi">4</span><span class="n">f</span><span class="si">}</span><span class="s">s/token</span><span class="sh">"</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">best_skip_layers</span>
</code></pre></div></div> <p>几个实现细节说明：</p> <ul> <li><strong>GP 核选 Matern(ν=2.5)</strong>：比 RBF 对不光滑函数更鲁棒，而 skip 层耗时对输入的响应确实不太平滑</li> <li><strong>候选集随机采样 + EI 排序</strong>：搜索空间是离散的，没法对 acquisition function 求梯度，所以用”大量随机候选 → 选 EI 最高”的方式近似最优化</li> <li><strong>软约束中间层</strong>：采样时限制只从中间 60% 的层里选 skip 候选，对应论文观察到的”浅层/深层不适合跳”这一规律</li> <li><strong><code class="language-plaintext highlighter-rouge">normalize_y=True</code></strong>：不同配置的耗时数量级可能差异不大（都在 0.01~0.1s/token 量级），归一化后 GP 拟合更稳定</li> </ul> <p>搜出来的结果也很有意思，如下图（LLaMA-2-13B 上的 skip 分布）：</p> <p><img src="/assets/img/posts/20260630-self-spec-decoding/022_Figure_10.png" alt="LLaMA-2-13B 的跳层分布"/></p> <p>规律很直观：<strong>浅层和深层基本不跳，中间层跳得更激进</strong>。这和直觉一致——底层做特征提取、顶层做输出预测，都比较关键；中间层冗余度最高。</p> <hr/> <h2 id="4-实验效果">4. 实验效果</h2> <p>在 LLaMA-2-13B 上，Self-Speculative Decoding 实现了 <strong>1.3x~2.0x</strong> 的端到端加速，具体数字取决于任务和接受率。</p> <p>如下图，接受率和 draft token 数 K 对加速比的影响：</p> <p><img src="/assets/img/posts/20260630-self-spec-decoding/002_Figure_2.png" alt="draft token 数和接受率对加速比的影响"/></p> <p>几个关键结论：</p> <ul> <li><strong>接受率越高，K 越大，加速越明显</strong>——这是理论上的必然，接受率高说明 draft 模型质量好，并行验证的红利吃得更充分。</li> <li>接受率在 90% 以上时，K=4 能达到约 1.35x 加速。</li> <li>接受率低于 80% 时，K=4 反而可能拖累整体（验证开销大于节省的时间），K=2 更稳。</li> </ul> <p>相比标准 Speculative Decoding，Self-Speculative 的优势在于：</p> <ol> <li><strong>无需额外模型</strong>，只需要一个模型的显存</li> <li><strong>权重完全共享</strong>，没有对齐问题</li> <li><strong>输出质量有理论保证</strong>，不是近似方法</li> </ol> <p>代价是：draft 质量可能不如专门训练的小模型，接受率相对低一些。</p> <hr/> <h2 id="5-用代码复现核心逻辑">5. 用代码复现核心逻辑</h2> <p>按照论文描述，把 draft + verify 的核心逻辑复现出来（简化版，便于理解）：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">torch</span>
<span class="kn">import</span> <span class="n">torch.nn.functional</span> <span class="k">as</span> <span class="n">F</span>
<span class="kn">from</span> <span class="n">typing</span> <span class="kn">import</span> <span class="n">List</span>

<span class="k">def</span> <span class="nf">self_speculative_decode</span><span class="p">(</span>
    <span class="n">model</span><span class="p">,</span>
    <span class="n">input_ids</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span>
    <span class="n">skip_layers</span><span class="p">:</span> <span class="n">List</span><span class="p">[</span><span class="nb">int</span><span class="p">],</span>   <span class="c1"># 贝叶斯优化搜出来的跳层列表
</span>    <span class="n">K</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">4</span><span class="p">,</span>               <span class="c1"># 每轮 draft 的 token 数
</span>    <span class="n">max_new_tokens</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">100</span><span class="p">,</span>
    <span class="n">temperature</span><span class="p">:</span> <span class="nb">float</span> <span class="o">=</span> <span class="mf">1.0</span><span class="p">,</span>
<span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    Self-Speculative Decoding 主循环。
    
    model: 支持 forward(input_ids, skip_layers=None) 的 LLM
    skip_layers: draft 阶段跳过的层索引列表
    K: 每轮 draft 的 token 数
    </span><span class="sh">"""</span>
    <span class="n">generated</span> <span class="o">=</span> <span class="n">input_ids</span><span class="p">.</span><span class="nf">clone</span><span class="p">()</span>
    
    <span class="k">while</span> <span class="n">generated</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span> <span class="o">-</span> <span class="n">input_ids</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span> <span class="o">&lt;</span> <span class="n">max_new_tokens</span><span class="p">:</span>
        <span class="c1"># ---- Draft 阶段：跳层自回归生成 K 个 token ----
</span>        <span class="n">draft_tokens</span> <span class="o">=</span> <span class="p">[]</span>
        <span class="n">draft_probs</span> <span class="o">=</span> <span class="p">[]</span>
        <span class="n">draft_input</span> <span class="o">=</span> <span class="n">generated</span><span class="p">.</span><span class="nf">clone</span><span class="p">()</span>
        
        <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">K</span><span class="p">):</span>
            <span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="nf">no_grad</span><span class="p">():</span>
                <span class="c1"># 跳过 skip_layers 中的层，用残缺模型推理
</span>                <span class="n">logits</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">draft_input</span><span class="p">,</span> <span class="n">skip_layers</span><span class="o">=</span><span class="n">skip_layers</span><span class="p">).</span><span class="n">logits</span><span class="p">[:,</span> <span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="p">:]</span>
            
            <span class="n">probs</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">softmax</span><span class="p">(</span><span class="n">logits</span> <span class="o">/</span> <span class="n">temperature</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>  <span class="c1"># q(x_t)
</span>            <span class="n">next_token</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">multinomial</span><span class="p">(</span><span class="n">probs</span><span class="p">,</span> <span class="n">num_samples</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>
            
            <span class="n">draft_tokens</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">next_token</span><span class="p">)</span>
            <span class="n">draft_probs</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">probs</span><span class="p">)</span>
            <span class="n">draft_input</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">([</span><span class="n">draft_input</span><span class="p">,</span> <span class="n">next_token</span><span class="p">],</span> <span class="n">dim</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>
        
        <span class="c1"># ---- Verify 阶段：完整模型并行验证 ----
</span>        <span class="c1"># 把 generated + K 个 draft token 一次性过完整模型
</span>        <span class="n">verify_input</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">([</span><span class="n">generated</span><span class="p">]</span> <span class="o">+</span> <span class="n">draft_tokens</span><span class="p">,</span> <span class="n">dim</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>
        
        <span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="nf">no_grad</span><span class="p">():</span>
            <span class="c1"># 完整模型，不跳层
</span>            <span class="n">verify_logits</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">verify_input</span><span class="p">,</span> <span class="n">skip_layers</span><span class="o">=</span><span class="bp">None</span><span class="p">).</span><span class="n">logits</span>
        
        <span class="c1"># verify_logits[:, -K-1:-1, :] 对应 K 个位置的完整模型输出
</span>        <span class="n">accepted_len</span> <span class="o">=</span> <span class="mi">0</span>
        <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">K</span><span class="p">):</span>
            <span class="n">pos</span> <span class="o">=</span> <span class="n">generated</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span> <span class="o">+</span> <span class="n">i</span> <span class="o">-</span> <span class="mi">1</span>  <span class="c1"># 对应 verify_logits 中的位置
</span>            <span class="n">full_probs</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">softmax</span><span class="p">(</span><span class="n">verify_logits</span><span class="p">[:,</span> <span class="n">pos</span><span class="p">,</span> <span class="p">:]</span> <span class="o">/</span> <span class="n">temperature</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>  <span class="c1"># p(x_t)
</span>            <span class="n">q_prob</span> <span class="o">=</span> <span class="n">draft_probs</span><span class="p">[</span><span class="n">i</span><span class="p">]</span>
            <span class="n">draft_tok</span> <span class="o">=</span> <span class="n">draft_tokens</span><span class="p">[</span><span class="n">i</span><span class="p">].</span><span class="nf">squeeze</span><span class="p">()</span>
            
            <span class="c1"># 计算接受概率 α = min(1, p(x) / q(x))
</span>            <span class="n">alpha</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">clamp</span><span class="p">(</span><span class="n">full_probs</span><span class="p">[</span><span class="mi">0</span><span class="p">,</span> <span class="n">draft_tok</span><span class="p">]</span> <span class="o">/</span> <span class="p">(</span><span class="n">q_prob</span><span class="p">[</span><span class="mi">0</span><span class="p">,</span> <span class="n">draft_tok</span><span class="p">]</span> <span class="o">+</span> <span class="mf">1e-10</span><span class="p">),</span> <span class="nb">max</span><span class="o">=</span><span class="mf">1.0</span><span class="p">)</span>
            
            <span class="k">if</span> <span class="n">torch</span><span class="p">.</span><span class="nf">rand</span><span class="p">(</span><span class="mi">1</span><span class="p">).</span><span class="nf">item</span><span class="p">()</span> <span class="o">&lt;</span> <span class="n">alpha</span><span class="p">.</span><span class="nf">item</span><span class="p">():</span>
                <span class="c1"># 接受这个 draft token
</span>                <span class="n">accepted_len</span> <span class="o">+=</span> <span class="mi">1</span>
            <span class="k">else</span><span class="p">:</span>
                <span class="c1"># 拒绝：按修正分布 norm(max(0, p - q)) 重新采样
</span>                <span class="n">corrected</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">clamp</span><span class="p">(</span><span class="n">full_probs</span> <span class="o">-</span> <span class="n">q_prob</span><span class="p">,</span> <span class="nb">min</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>
                <span class="n">corrected</span> <span class="o">=</span> <span class="n">corrected</span> <span class="o">/</span> <span class="p">(</span><span class="n">corrected</span><span class="p">.</span><span class="nf">sum</span><span class="p">()</span> <span class="o">+</span> <span class="mf">1e-10</span><span class="p">)</span>
                <span class="n">next_token</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">multinomial</span><span class="p">(</span><span class="n">corrected</span><span class="p">,</span> <span class="n">num_samples</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>
                <span class="n">draft_tokens</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">=</span> <span class="n">next_token</span>
                <span class="k">break</span>
        
        <span class="c1"># 把接受的 token 追加到输出序列
</span>        <span class="n">accepted_tokens</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">(</span><span class="n">draft_tokens</span><span class="p">[:</span><span class="n">accepted_len</span> <span class="o">+</span> <span class="mi">1</span><span class="p">],</span> <span class="n">dim</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>
        <span class="n">generated</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">([</span><span class="n">generated</span><span class="p">,</span> <span class="n">accepted_tokens</span><span class="p">],</span> <span class="n">dim</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>
        
        <span class="c1"># 如果遇到 EOS，终止
</span>        <span class="k">if</span> <span class="n">draft_tokens</span><span class="p">[</span><span class="n">accepted_len</span><span class="p">].</span><span class="nf">item</span><span class="p">()</span> <span class="o">==</span> <span class="n">model</span><span class="p">.</span><span class="n">config</span><span class="p">.</span><span class="n">eos_token_id</span><span class="p">:</span>
            <span class="k">break</span>
    
    <span class="k">return</span> <span class="n">generated</span>


<span class="k">def</span> <span class="nf">build_skip_layer_model</span><span class="p">(</span><span class="n">base_model</span><span class="p">,</span> <span class="n">skip_layers</span><span class="p">:</span> <span class="n">List</span><span class="p">[</span><span class="nb">int</span><span class="p">]):</span>
    <span class="sh">"""</span><span class="s">
    对 HuggingFace LLaMA/Mistral 类模型，
    在 forward 时动态跳过指定层。
    实际用法：monkey-patch model.forward 或用 hook 实现。
    这里给出一个简化的 wrapper 示意。
    </span><span class="sh">"""</span>
    <span class="kn">import</span> <span class="n">types</span>

    <span class="n">original_forward</span> <span class="o">=</span> <span class="n">base_model</span><span class="p">.</span><span class="n">model</span><span class="p">.</span><span class="n">forward</span>

    <span class="k">def</span> <span class="nf">patched_forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="o">*</span><span class="n">args</span><span class="p">,</span> <span class="n">skip_layers</span><span class="o">=</span><span class="bp">None</span><span class="p">,</span> <span class="o">**</span><span class="n">kwargs</span><span class="p">):</span>
        <span class="k">if</span> <span class="n">skip_layers</span> <span class="ow">is</span> <span class="bp">None</span><span class="p">:</span>
            <span class="k">return</span> <span class="nf">original_forward</span><span class="p">(</span><span class="o">*</span><span class="n">args</span><span class="p">,</span> <span class="o">**</span><span class="n">kwargs</span><span class="p">)</span>
        <span class="c1"># 临时把 skip 的层换成 identity，推理完再换回来
</span>        <span class="n">skipped</span> <span class="o">=</span> <span class="p">{}</span>
        <span class="k">for</span> <span class="n">idx</span> <span class="ow">in</span> <span class="n">skip_layers</span><span class="p">:</span>
            <span class="n">layer</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">layers</span><span class="p">[</span><span class="n">idx</span><span class="p">]</span>
            <span class="n">skipped</span><span class="p">[</span><span class="n">idx</span><span class="p">]</span> <span class="o">=</span> <span class="n">layer</span>
            <span class="n">self</span><span class="p">.</span><span class="n">layers</span><span class="p">[</span><span class="n">idx</span><span class="p">]</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">nn</span><span class="p">.</span><span class="nc">Identity</span><span class="p">()</span>  <span class="c1"># 跳过
</span>        <span class="n">output</span> <span class="o">=</span> <span class="nf">original_forward</span><span class="p">(</span><span class="o">*</span><span class="n">args</span><span class="p">,</span> <span class="o">**</span><span class="n">kwargs</span><span class="p">)</span>
        <span class="k">for</span> <span class="n">idx</span><span class="p">,</span> <span class="n">layer</span> <span class="ow">in</span> <span class="n">skipped</span><span class="p">.</span><span class="nf">items</span><span class="p">():</span>
            <span class="n">self</span><span class="p">.</span><span class="n">layers</span><span class="p">[</span><span class="n">idx</span><span class="p">]</span> <span class="o">=</span> <span class="n">layer</span>  <span class="c1"># 恢复
</span>        <span class="k">return</span> <span class="n">output</span>

    <span class="n">base_model</span><span class="p">.</span><span class="n">model</span><span class="p">.</span><span class="n">forward</span> <span class="o">=</span> <span class="n">types</span><span class="p">.</span><span class="nc">MethodType</span><span class="p">(</span><span class="n">patched_forward</span><span class="p">,</span> <span class="n">base_model</span><span class="p">.</span><span class="n">model</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">base_model</span>
</code></pre></div></div> <p>几点说明：</p> <ul> <li><code class="language-plaintext highlighter-rouge">draft_probs</code> 存的是 draft 阶段每个位置的完整 softmax 分布 $q(x_t)$，验证时需要用到</li> <li>接受/拒绝的判断是<strong>逐 token 顺序执行</strong>的，第一个被拒绝的位置就截断，后面的 draft token 全部丢弃</li> <li>拒绝时的修正采样 <code class="language-plaintext highlighter-rouge">norm(max(0, p - q))</code> 是理论保证输出分布正确性的关键——它补了 $p$ 比 $q$ 多出来的那部分概率质量</li> <li>实际工程实现中 skip 层不会用 Identity 替换（有额外开销），而是在 transformer 循环里直接 <code class="language-plaintext highlighter-rouge">if i in skip_set: continue</code></li> </ul> <hr/> <h2 id="6-几点个人-take">6. 几点个人 take</h2> <p>这篇工作的思路相当干净——问题清晰，解法优雅，理论保证完备。</p> <p>但有几个地方值得打一个问号：</p> <p><strong>1. 贝叶斯优化的搜索成本</strong>。BO 本身不贵，但每次评估需要实际跑 self-speculative decoding 采几百个 token，累计下来开销不小。论文报的是 “less than 1 hour on a single A100”，对于生产环境算是可接受，但如果模型版本经常更新或者部署场景频繁变化，重搜的成本就不太友好了。</p> <p><strong>2. 跳层方案的泛化性</strong>。搜出来的最优 skip 组合是针对特定数据分布的。如果推理时的输入分布和搜索时差异较大（比如代码任务的 skip 方案用在数学推理上），加速比可能会打折扣，接受率也会下降。</p> <p><strong>3. 和 KV Cache 的交互</strong>。Draft 阶段跳层后，KV Cache 的结构和完整模型不一样，需要仔细处理——论文对这块的描述相对简略，实际实现里是个工程细节的坑。</p> <p>整体来说这是一篇很扎实的工作，把”不需要额外模型”这个约束真正落地了。对于资源受限的部署场景（单卡跑 13B 这类），Self-Speculative Decoding 是个非常实用的选项。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 LLM 推理加速你想系统深入，可以看看我之前出版的<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，书里有专章讲 LLM 推理效率和参数高效微调，和本文的工程背景有直接关联。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="推理加速"/><category term="论文解读"/><category term="Speculative Decoding"/><summary type="html"><![CDATA[EMNLP’23 | 不用额外模型，LLM 自己给自己加速——Self-Speculative Decoding 原理详解]]></summary></entry><entry><title type="html">WWW’26 | SS-MoE：显存墙下的 MoE 推理，Self-Speculative Decoding 怎么救场？</title><link href="https://marsggbo.github.io/blog/2026/20260630-ss-moe/" rel="alternate" type="text/html" title="WWW’26 | SS-MoE：显存墙下的 MoE 推理，Self-Speculative Decoding 怎么救场？"/><published>2026-06-30T00:00:00+00:00</published><updated>2026-06-30T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260630-ss-moe</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260630-ss-moe/"><![CDATA[<h1 id="www26--ss-moe显存墙下的-moe-推理self-speculative-decoding-怎么救场">WWW’26 | SS-MoE：显存墙下的 MoE 推理，Self-Speculative Decoding 怎么救场？</h1> <blockquote> <p>原文：<a href="https://dl.acm.org/doi/abs/10.1145/3774904.3792218">Self-Speculative Decoding for On-device MoE Acceleration</a> ACM WWW 2026</p> </blockquote> <hr/> <p>这是 Self-Speculative Decoding 系列第四篇。前三篇（Draft &amp; Verify、LayerSkip、SWIFT）讲的都是 Dense 模型上的 Self-SD。这一篇换了个赛道：<strong>MoE 架构</strong>，而且是专门针对端侧显存受限场景的。</p> <h2 id="1-moe-推理的核心矛盾">1. MoE 推理的核心矛盾</h2> <p>先交代下背景。</p> <p>MoE 火是真的火——DeepSeek、Qwen3-MoE、Mixtral，主流 LLM 里 MoE 越来越多。吸引力很直接：总参数量大，但每次推理只激活 top-k 个 expert（Sparse Activation），计算量远小于同量级 Dense 模型。</p> <p>但实际跑下来，端侧部署有个结结实实的坑：</p> <p>下图是 MoE 模型参数的组成——总参数里 <strong>89.9% 是 expert 参数</strong>；激活参数里，expert 也占了 45.5%：</p> <p><img src="/assets/img/posts/20260630-ss-moe/figure1_p2.png" alt="MoE 模型参数在总参数和激活参数中的占比"/></p> <p>问题来了：虽然每次推理只用 top-k 个 expert，但<strong>所有 expert 的参数都得加载进显存</strong>。一台显存受限的 GPU 根本装不下，于是得做 CPU offloading——把 expert 参数放在 CPU 内存，推理时按需搬过来。</p> <p>但 CPU offloading 带来了新麻烦：下一层 router 选哪个 expert，在当前层算完之前是未知的，预取只能猜，猜错了就得等 IO 延迟，latency 暴增。</p> <p><strong>稀疏激活节省计算、全量加载消耗显存、CPU offloading 引入 IO 瓶颈</strong>——这是整个 MoE 端侧推理领域的核心矛盾。SS-MoE 把 Self-Speculative Decoding 引进来，从算法层面提供了一个新出口。</p> <h2 id="2-关键洞察少激活几个-expert质量掉多少">2. 关键洞察：少激活几个 expert，质量掉多少？</h2> <p>SS-MoE 的出发点是一个实验性观察：</p> <p>下图 (a) 展示了在 DeepSeek 模型上，随着每个 token 激活的 expert 数量从 1 减少到 6，ARC-Easy 精度（蓝线）和 TPS（粉线）的变化：</p> <p><img src="/assets/img/posts/20260630-ss-moe/figure3_full.png" alt="减少激活 expert 数量对精度和速度的影响，以及 routing 权重分布"/></p> <p>精度随 expert 数量单调提升，但<strong>用 top-3 expert 就能达到 top-6 的 94% 精度</strong>（约 72% vs 78%），同时速度提升明显。图 (b) 是各层 routing 权重分布——top-2 的权重占了累计分布的 60% 以上，说明少数 expert 承担了大部分贡献。</p> <p>图 (c) 进一步说明：draft token 的 acceptance rate 随 draft 长度增加而略降，但始终保持在 85% 以上（TopK=3 时）。图 (d) 则直接说明了 confidence 感知的合理性：<strong>高置信度位置的 acceptance rate 高达 96%，低置信度位置也有 52%-68%</strong>。</p> <p>这就是 SS-MoE 的核心 insight：<strong>用少量 routed expert 生成草稿，大概率能被完整模型接受，而且草稿阶段的 IO 开销更小，prefetch 更准</strong>。</p> <h2 id="3-方法ss-moe-的三个组件">3. 方法：SS-MoE 的三个组件</h2> <p>下图是 SS-MoE 的整体架构，分三部分：(a) draft 阶段用更少 expert，(b) 置信度策略，(c) verify 阶段用完整 expert：</p> <p><img src="/assets/img/posts/20260630-ss-moe/figure6_p5.png" alt="SS-MoE 整体框架：少 expert 打草稿、置信度策略、完整 expert 验证"/></p> <h3 id="31-moe-前向传播先搞清楚正常情况下发生了什么">3.1 MoE 前向传播：先搞清楚正常情况下发生了什么</h3> <p>一个标准的 MoE 层的前向传播分三步：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">moe_layer_forward</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">router</span><span class="p">,</span> <span class="n">experts</span><span class="p">,</span> <span class="n">k</span><span class="o">=</span><span class="mi">2</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    x: [seq_len, hidden_dim]
    router: 线性层，输出每个 token 对所有 expert 的得分
    experts: N 个 FFN expert
    k: 每个 token 激活的 expert 数量
    </span><span class="sh">"""</span>
    <span class="c1"># Step 1: router 给每个 token 打分
</span>    <span class="n">router_scores</span> <span class="o">=</span> <span class="nf">softmax</span><span class="p">(</span><span class="nf">router</span><span class="p">(</span><span class="n">x</span><span class="p">))</span>  <span class="c1"># [seq_len, N_experts]
</span>    
    <span class="c1"># Step 2: 每个 token 选 top-k 个 expert
</span>    <span class="n">top_k_indices</span> <span class="o">=</span> <span class="nf">topk</span><span class="p">(</span><span class="n">router_scores</span><span class="p">,</span> <span class="n">k</span><span class="p">)</span>     <span class="c1"># [seq_len, k]
</span>    <span class="n">top_k_weights</span>  <span class="o">=</span> <span class="nf">gather</span><span class="p">(</span><span class="n">router_scores</span><span class="p">,</span> <span class="n">top_k_indices</span><span class="p">)</span>  <span class="c1"># [seq_len, k]
</span>    
    <span class="c1"># Step 3: 被选中的 expert 计算输出，加权求和
</span>    <span class="n">output</span> <span class="o">=</span> <span class="nf">zeros_like</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
    <span class="k">for</span> <span class="n">i</span><span class="p">,</span> <span class="n">idx</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">top_k_indices</span><span class="p">):</span>
        <span class="k">for</span> <span class="n">j</span><span class="p">,</span> <span class="n">expert_id</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">idx</span><span class="p">):</span>
            <span class="n">output</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">+=</span> <span class="n">top_k_weights</span><span class="p">[</span><span class="n">i</span><span class="p">][</span><span class="n">j</span><span class="p">]</span> <span class="o">*</span> <span class="n">experts</span><span class="p">[</span><span class="n">expert_id</span><span class="p">](</span><span class="n">x</span><span class="p">[</span><span class="n">i</span><span class="p">])</span>
    
    <span class="k">return</span> <span class="n">output</span>
</code></pre></div></div> <p><strong>问题在 Step 3</strong>：<code class="language-plaintext highlighter-rouge">experts[expert_id]</code> 这一步需要 expert 参数在 GPU 上。如果 GPU 显存不够存所有 N 个 expert，就得从 CPU 临时搬。而 <code class="language-plaintext highlighter-rouge">expert_id</code> 只有跑完 Step 1 和 Step 2 才能知道——所以无法提前准备，只能串行等 IO。</p> <h3 id="32-ss-moe-的-draft-verify-流程">3.2 SS-MoE 的 Draft-Verify 流程</h3> <p>SS-MoE 把 MoE 推理改成 Draft + Verify 两阶段。核心是：<strong>draft 阶段每层只用 top-r 个 expert（r &lt; k），verify 阶段用完整的 top-k 个 expert</strong>。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">ss_moe_draft</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">router</span><span class="p">,</span> <span class="n">expert_cache</span><span class="p">,</span> <span class="n">r</span><span class="o">=</span><span class="mi">1</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    Draft 阶段：每层只用 top-r 个 expert
    r &lt; k，速度快，IO 少，但输出质量略低
    </span><span class="sh">"""</span>
    <span class="n">router_scores</span> <span class="o">=</span> <span class="nf">softmax</span><span class="p">(</span><span class="nf">router</span><span class="p">(</span><span class="n">x</span><span class="p">))</span>
    <span class="n">top_r_indices</span> <span class="o">=</span> <span class="nf">topk</span><span class="p">(</span><span class="n">router_scores</span><span class="p">,</span> <span class="n">r</span><span class="p">)</span>   <span class="c1"># 只选 r 个，而非 k 个
</span>    <span class="n">top_r_weights</span>  <span class="o">=</span> <span class="nf">gather</span><span class="p">(</span><span class="n">router_scores</span><span class="p">,</span> <span class="n">top_r_indices</span><span class="p">)</span>
    
    <span class="c1"># 关键：top-r 的 expert 是 top-k 的子集
</span>    <span class="c1"># 这些 expert 大概率已经在 GPU 的 expert_cache 里（热 expert）
</span>    <span class="n">output</span> <span class="o">=</span> <span class="nf">zeros_like</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
    <span class="k">for</span> <span class="n">i</span><span class="p">,</span> <span class="n">idx</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">top_r_indices</span><span class="p">):</span>
        <span class="k">for</span> <span class="n">j</span><span class="p">,</span> <span class="n">expert_id</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">idx</span><span class="p">):</span>
            <span class="n">expert</span> <span class="o">=</span> <span class="n">expert_cache</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="n">expert_id</span><span class="p">)</span>  <span class="c1"># 命中缓存，无 IO 等待
</span>            <span class="n">output</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="o">+=</span> <span class="n">top_r_weights</span><span class="p">[</span><span class="n">i</span><span class="p">][</span><span class="n">j</span><span class="p">]</span> <span class="o">*</span> <span class="nf">expert</span><span class="p">(</span><span class="n">x</span><span class="p">[</span><span class="n">i</span><span class="p">])</span>
    
    <span class="k">return</span> <span class="n">output</span><span class="p">,</span> <span class="n">router_scores</span>  <span class="c1"># 保存 router_scores 供 verify 用
</span>
<span class="k">def</span> <span class="nf">ss_moe_verify</span><span class="p">(</span><span class="n">x_full</span><span class="p">,</span> <span class="n">router_scores_draft</span><span class="p">,</span> <span class="n">expert_cache</span><span class="p">,</span> <span class="n">k</span><span class="o">=</span><span class="mi">2</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    Verify 阶段：用完整 top-k 个 expert 并行验证所有 draft token
    额外加载 draft 阶段未使用的 (k-r) 个 expert
    </span><span class="sh">"""</span>
    <span class="n">top_k_indices</span> <span class="o">=</span> <span class="nf">topk</span><span class="p">(</span><span class="n">router_scores_draft</span><span class="p">,</span> <span class="n">k</span><span class="p">)</span>
    <span class="c1"># 只需加载 top_k 中不在 top_r 里的那部分 expert（增量加载）
</span>    <span class="n">new_experts_needed</span> <span class="o">=</span> <span class="n">top_k_indices</span><span class="p">[</span><span class="n">r</span><span class="p">:]</span>
    <span class="n">expert_cache</span><span class="p">.</span><span class="nf">prefetch</span><span class="p">(</span><span class="n">new_experts_needed</span><span class="p">)</span>
    
    <span class="c1"># 正常 top-k 前向传播
</span>    <span class="k">return</span> <span class="nf">moe_layer_forward</span><span class="p">(</span><span class="n">x_full</span><span class="p">,</span> <span class="n">router_scores_draft</span><span class="p">,</span> <span class="n">expert_cache</span><span class="p">,</span> <span class="n">k</span><span class="p">)</span>
</code></pre></div></div> <p><strong>这里有个非常优雅的设计</strong>：draft 用的 top-r expert 是 verify 用的 top-k expert 的子集。所以 verify 阶段不需要重新加载 draft 已经用过的 expert，只需要增量加载剩余的 (k-r) 个 expert，IO 开销大幅减少。</p> <h3 id="33-expert-cache--预取优化">3.3 Expert Cache + 预取优化</h3> <p>SS-MoE 把 GPU 显存当成 expert 的 LRU cache 管理。下图展示了预取（prefetching）的设计：</p> <p><img src="/assets/img/posts/20260630-ss-moe/figure4_p3.png" alt="Expert cache 和 overlapped prefetching 设计"/></p> <p><strong>Expert Cache 策略</strong>：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">ExpertCache</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">capacity</span><span class="p">):</span>
        <span class="c1"># GPU 显存只能容纳 capacity 个 expert
</span>        <span class="n">self</span><span class="p">.</span><span class="n">cache</span> <span class="o">=</span> <span class="nc">LRU_dict</span><span class="p">(</span><span class="n">capacity</span><span class="p">)</span>
    
    <span class="k">def</span> <span class="nf">get</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">expert_id</span><span class="p">):</span>
        <span class="k">if</span> <span class="n">expert_id</span> <span class="ow">in</span> <span class="n">self</span><span class="p">.</span><span class="n">cache</span><span class="p">:</span>
            <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="n">cache</span><span class="p">[</span><span class="n">expert_id</span><span class="p">]</span>  <span class="c1"># 缓存命中，直接返回
</span>        <span class="k">else</span><span class="p">:</span>
            <span class="c1"># 缓存未命中：从 CPU 加载（同步，有延迟）
</span>            <span class="n">expert</span> <span class="o">=</span> <span class="n">cpu_memory</span><span class="p">.</span><span class="nf">load</span><span class="p">(</span><span class="n">expert_id</span><span class="p">)</span>
            <span class="n">self</span><span class="p">.</span><span class="n">cache</span><span class="p">[</span><span class="n">expert_id</span><span class="p">]</span> <span class="o">=</span> <span class="n">expert</span>  <span class="c1"># LRU 淘汰最久未用的
</span>            <span class="k">return</span> <span class="n">expert</span>
    
    <span class="k">def</span> <span class="nf">async_prefetch</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">expert_ids</span><span class="p">):</span>
        <span class="c1"># 异步预取：启动 CPU→GPU 传输，不阻塞当前计算
</span>        <span class="k">for</span> <span class="n">eid</span> <span class="ow">in</span> <span class="n">expert_ids</span><span class="p">:</span>
            <span class="k">if</span> <span class="n">eid</span> <span class="ow">not</span> <span class="ow">in</span> <span class="n">self</span><span class="p">.</span><span class="n">cache</span><span class="p">:</span>
                <span class="nf">launch_async_transfer</span><span class="p">(</span><span class="n">cpu_memory</span><span class="p">[</span><span class="n">eid</span><span class="p">],</span> <span class="n">target</span><span class="o">=</span><span class="n">gpu_buffer</span><span class="p">)</span>
</code></pre></div></div> <p><strong>Overlapped Prefetch（计算与 IO 重叠）</strong>：</p> <p>draft 阶段只用 top-r 个 expert，比 top-k 少激活了 (k-r) 个 expert。这些”省下来”的 IO 带宽可以用来<strong>提前预取下一层的 expert</strong>：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>时间轴：
  Layer l compute  : [==== draft top-r expert 计算 ====]
  Layer l+1 prefetch:     [-- 异步加载 layer l+1 的 top-r expert --]
                    ↑
                 重叠区域越大，IO 延迟被计算时间掩盖的越多
</code></pre></div></div> <p>由于 draft 阶段激活的 expert 少，router 输出更快确定，<strong>预取启动时间更早，命中率更高</strong>。这是 draft 用少 expert 带来的系统级收益，不只是计算量的节省。</p> <h3 id="34-完整算法draft--verify-循环">3.4 完整算法：Draft + Verify 循环</h3> <p>下面是 SS-MoE 的完整推理伪代码（Algorithm 1）：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">ss_moe_inference</span><span class="p">(</span><span class="n">prompt</span><span class="p">,</span> <span class="n">model</span><span class="p">,</span> <span class="n">expert_cache</span><span class="p">,</span> 
                     <span class="n">r</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span> <span class="n">k</span><span class="o">=</span><span class="mi">2</span><span class="p">,</span> <span class="n">max_draft</span><span class="o">=</span><span class="mi">5</span><span class="p">,</span> <span class="n">τ</span><span class="o">=</span><span class="mf">0.5</span><span class="p">,</span> <span class="n">T</span><span class="o">=</span><span class="mi">0</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    r: draft 阶段每层激活的 routed expert 数
    k: verify 阶段每层激活的 routed expert 数（k &gt; r）
    max_draft: 最大 draft token 数
    τ: 置信度阈值
    T: 温度（T=0 为 conservative 模式）
    </span><span class="sh">"""</span>
    <span class="n">context</span> <span class="o">=</span> <span class="n">prompt</span>
    <span class="n">output</span> <span class="o">=</span> <span class="p">[]</span>
    
    <span class="k">while</span> <span class="ow">not</span> <span class="n">done</span><span class="p">:</span>
        <span class="c1"># ─── Draft 阶段 ────────────────────────────────────────
</span>        <span class="n">draft_tokens</span> <span class="o">=</span> <span class="p">[]</span>
        <span class="n">draft_probs</span> <span class="o">=</span> <span class="p">[]</span>
        
        <span class="k">for</span> <span class="n">j</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">max_draft</span><span class="p">):</span>
            <span class="c1"># 每个 MoE 层只用 top-r expert
</span>            <span class="n">hidden</span> <span class="o">=</span> <span class="n">context</span> <span class="o">+</span> <span class="n">draft_tokens</span>
            <span class="k">for</span> <span class="n">layer</span> <span class="ow">in</span> <span class="n">model</span><span class="p">.</span><span class="n">layers</span><span class="p">:</span>
                <span class="k">if</span> <span class="n">layer</span><span class="p">.</span><span class="n">is_moe</span><span class="p">:</span>
                    <span class="n">hidden</span><span class="p">,</span> <span class="n">router_scores</span> <span class="o">=</span> <span class="nf">ss_moe_draft</span><span class="p">(</span><span class="n">hidden</span><span class="p">,</span> <span class="n">layer</span><span class="p">.</span><span class="n">router</span><span class="p">,</span> 
                                                          <span class="n">expert_cache</span><span class="p">,</span> <span class="n">r</span><span class="o">=</span><span class="n">r</span><span class="p">)</span>
                    <span class="c1"># 同时预取下一层的 expert（计算 IO 重叠）
</span>                    <span class="n">expert_cache</span><span class="p">.</span><span class="nf">async_prefetch</span><span class="p">(</span><span class="nf">predict_next_layer_experts</span><span class="p">(</span><span class="n">hidden</span><span class="p">))</span>
                <span class="k">else</span><span class="p">:</span>
                    <span class="n">hidden</span> <span class="o">=</span> <span class="nf">layer</span><span class="p">(</span><span class="n">hidden</span><span class="p">)</span>
            
            <span class="n">probs</span> <span class="o">=</span> <span class="nf">softmax</span><span class="p">(</span><span class="n">model</span><span class="p">.</span><span class="nf">lm_head</span><span class="p">(</span><span class="n">hidden</span><span class="p">[</span><span class="o">-</span><span class="mi">1</span><span class="p">]))</span>
            <span class="n">conf</span> <span class="o">=</span> <span class="n">probs</span><span class="p">.</span><span class="nf">max</span><span class="p">()</span>
            <span class="n">draft_tokens</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">probs</span><span class="p">.</span><span class="nf">argmax</span><span class="p">())</span>
            <span class="n">draft_probs</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">probs</span><span class="p">)</span>
            
            <span class="c1"># 置信度低 → 提前停止
</span>            <span class="k">if</span> <span class="n">conf</span> <span class="o">&lt;</span> <span class="n">τ</span><span class="p">:</span>
                <span class="k">break</span>
        
        <span class="c1"># ─── Verify 阶段 ──────────────────────────────────────
</span>        <span class="c1"># 一次 batch forward，并行验证所有 draft token
</span>        <span class="c1"># 每个 MoE 层用完整 top-k expert
</span>        <span class="n">verify_context</span> <span class="o">=</span> <span class="n">context</span> <span class="o">+</span> <span class="n">draft_tokens</span>
        <span class="n">verify_probs</span> <span class="o">=</span> <span class="nf">full_model_forward</span><span class="p">(</span><span class="n">verify_context</span><span class="p">,</span> <span class="n">expert_cache</span><span class="p">,</span> <span class="n">k</span><span class="o">=</span><span class="n">k</span><span class="p">)</span>
        <span class="c1"># verify_probs[i] 是完整模型对位置 i 的输出概率分布
</span>        
        <span class="c1"># ─── 接受/拒绝 ────────────────────────────────────────
</span>        <span class="n">accepted</span> <span class="o">=</span> <span class="p">[]</span>
        <span class="k">for</span> <span class="n">j</span><span class="p">,</span> <span class="p">(</span><span class="n">d_t</span><span class="p">,</span> <span class="n">q_t</span><span class="p">,</span> <span class="n">p_t</span><span class="p">)</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span>
                <span class="nf">zip</span><span class="p">(</span><span class="n">draft_tokens</span><span class="p">,</span> <span class="n">draft_probs</span><span class="p">,</span> <span class="n">verify_probs</span><span class="p">)):</span>
            
            <span class="k">if</span> <span class="n">T</span> <span class="o">==</span> <span class="mi">0</span><span class="p">:</span>
                <span class="c1"># Conservative 模式：严格无损，标准接受条件
</span>                <span class="n">accept_prob</span> <span class="o">=</span> <span class="nf">min</span><span class="p">(</span><span class="mf">1.0</span><span class="p">,</span> <span class="n">p_t</span><span class="p">[</span><span class="n">d_t</span><span class="p">]</span> <span class="o">/</span> <span class="n">q_t</span><span class="p">[</span><span class="n">d_t</span><span class="p">])</span>
                <span class="k">if</span> <span class="nf">random</span><span class="p">()</span> <span class="o">&lt;</span> <span class="n">accept_prob</span><span class="p">:</span>
                    <span class="n">accepted</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">d_t</span><span class="p">)</span>
                <span class="k">else</span><span class="p">:</span>
                    <span class="c1"># 拒绝：从修正分布采样，然后截断
</span>                    <span class="n">correction</span> <span class="o">=</span> <span class="nf">sample</span><span class="p">(</span><span class="nf">norm</span><span class="p">(</span><span class="nf">max</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">p_t</span> <span class="o">-</span> <span class="n">q_t</span><span class="p">)))</span>
                    <span class="n">accepted</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">correction</span><span class="p">)</span>
                    <span class="k">break</span>
            <span class="k">else</span><span class="p">:</span>
                <span class="c1"># Adaptive 模式：高置信度的 draft token 宽松接受
</span>                <span class="c1"># 允许少量近似误差换取更高 TPS
</span>                <span class="k">if</span> <span class="n">q_t</span><span class="p">[</span><span class="n">d_t</span><span class="p">]</span> <span class="o">&gt;</span> <span class="n">τ</span> <span class="ow">and</span> <span class="n">p_t</span><span class="p">[</span><span class="n">d_t</span><span class="p">]</span> <span class="o">/</span> <span class="n">q_t</span><span class="p">[</span><span class="n">d_t</span><span class="p">]</span> <span class="o">&gt;</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="n">T</span><span class="p">):</span>
                    <span class="n">accepted</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">d_t</span><span class="p">)</span>
                <span class="k">else</span><span class="p">:</span>
                    <span class="n">accepted</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="nf">sample</span><span class="p">(</span><span class="n">p_t</span><span class="p">))</span>
                    <span class="k">break</span>
        
        <span class="n">output</span><span class="p">.</span><span class="nf">extend</span><span class="p">(</span><span class="n">accepted</span><span class="p">)</span>
        <span class="n">context</span> <span class="o">=</span> <span class="n">context</span> <span class="o">+</span> <span class="n">accepted</span>
    
    <span class="k">return</span> <span class="n">output</span>
</code></pre></div></div> <h3 id="35-置信度感知的自适应验证">3.5 置信度感知的自适应验证</h3> <p>两种模式对应不同的精度-速度权衡：</p> <ul> <li><strong>Conservative（T=0）</strong>：严格遵循推测解码的无损条件 <code class="language-plaintext highlighter-rouge">min(1, p/q)</code>，输出分布与完整自回归完全一致，<strong>精度不低于 4-bit 量化基线</strong></li> <li><strong>Adaptive（T&gt;0）</strong>：允许接受置信度高但严格来说有偏差的 draft token，用极小的精度损失换取显著更高的 TPS。论文报告自适应模式下精度损失”nearly lossless”</li> </ul> <h2 id="4-实验结果">4. 实验结果</h2> <p>测试模型：DeepSeek MoE 系列 和 Moonlight MoE，任务覆盖 MT-Bench、Translation、Summarization、QA、Math、RAG、Code，显存受限 GPU 环境。</p> <p>主要结果如下表：</p> <p><img src="/assets/img/posts/20260630-ss-moe/table1_p6.png" alt="不同模型和任务的解码性能对比（TPS 和加速比）"/></p> <p>几个关键数字：</p> <ul> <li><strong>DeepSeek + SS-MoE（T&gt;0）</strong>：平均 TPS <strong>18.34</strong>，加速比 <strong>2.65×</strong>（vs. load-on-demand 基线）</li> <li><strong>Moonlight + SS-MoE（T&gt;0）</strong>：平均 TPS <strong>23.15</strong>，加速比 <strong>3.72×</strong></li> <li>对比 4-bit 量化（2.32×/3.24×）：SS-MoE 在保持更高精度的同时，加速比明显更高</li> </ul> <p>不同显存 budget 下的表现：</p> <p><img src="/assets/img/posts/20260630-ss-moe/figure9_p8.png" alt="不同 expert cache budget 下的解码 TPS（DeepSeek + Math）"/></p> <p>即使 cache budget 很小（0.1），SS-MoE（Ours，棕线）也显著高于所有 baseline。随着 cache budget 增大，优势持续扩大。</p> <h2 id="5-这篇工作在系列里的位置">5. 这篇工作在系列里的位置</h2> <p>前三篇 Self-SD 工作”跳”的是 Transformer 的中间层。SS-MoE 把这个逻辑迁移到 MoE，”跳”的是<strong>每层里的部分 expert</strong>。</p> <p>SS-MoE 带来了三重收益：</p> <ol> <li><strong>计算量减少</strong>：draft 阶段 FLOPs 降低（少激活 expert）</li> <li><strong>IO 开销降低</strong>：draft 阶段需要从 CPU 加载的 expert 更少</li> <li><strong>Prefetch 命中率提升</strong>：更早确定 router 结果 → 更早启动预取 → 计算 IO 重叠更好</li> </ol> <p>这和我自己在做的 ExpertFlow（DAC’26）方向很近——都在往 MoE 推理效率使劲，只是角度不同：ExpertFlow 侧重推理框架层的 expert 路由和 IO 优化，SS-MoE 用 speculative decoding 从算法层减少 expert 激活次数。两条路都在攻同一个核心矛盾。</p> <p>下一篇把视角拉得更远——SSD 在<strong>扩散语言模型（dLLM）</strong>上的应用，那个场景的挑战和自回归完全不一样。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 MoE 和 LLM 推理效率你想系统深入，可以看看我们团队出版的<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，书里讲到了 NAS 在大模型架构搜索上的延伸，和 MoE 的稀疏路由设计思路有些交叉。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="MoE"/><category term="推理加速"/><category term="推测解码"/><category term="边缘推理"/><category term="论文解读"/><summary type="html"><![CDATA[WWW’26 | SS-MoE：显存墙下的 MoE 推理，Self-Speculative Decoding 怎么救场？]]></summary></entry><entry><title type="html">arXiv’26 | SSD for ASR：用 CTC 编码器打草稿，LLM 验证，语音识别也能推测解码</title><link href="https://marsggbo.github.io/blog/2026/20260630-ssd-asr/" rel="alternate" type="text/html" title="arXiv’26 | SSD for ASR：用 CTC 编码器打草稿，LLM 验证，语音识别也能推测解码"/><published>2026-06-30T00:00:00+00:00</published><updated>2026-06-30T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260630-ssd-asr</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260630-ssd-asr/"><![CDATA[<h1 id="arxiv26--ssd-for-asr用-ctc-编码器打草稿llm-验证语音识别也能推测解码">arXiv’26 | SSD for ASR：用 CTC 编码器打草稿，LLM 验证，语音识别也能推测解码</h1> <blockquote> <p>原文：<a href="https://arxiv.org/pdf/2603.11243">Self-Speculative Decoding for LLM-based ASR with CTC Encoder Drafts</a> George Saon, Samuel Thomas, Takashi Fukuda, Tohru Nagano, Avihu Dekel, Luis Lastras（IBM Research，2026）</p> </blockquote> <hr/> <p>这是 Self-Speculative Decoding 系列第六篇，也是目前时间线上最新的一篇。前五篇的 SSD 都在文本生成领域——不管是 Dense 模型、MoE 模型还是扩散模型，解码的对象都是文本 token。</p> <p>这篇工作把 SSD 带到了一个完全不同的场景：<strong>基于 LLM 的自动语音识别（ASR）</strong>。</p> <h2 id="1-llm-based-asr-的推理瓶颈">1. LLM-based ASR 的推理瓶颈</h2> <p>先交代下背景。</p> <p>现代高性能 ASR 系统越来越多地采用 LLM 作为解码器：Conformer encoder 提取声学特征，LLM decoder 自回归生成文本。代表系统是 IBM 的 granite-speech 系列。</p> <p>这类系统的质量比传统 CTC/attention hybrid 强不少，但自回归解码的老问题依然存在——每生成一个 token 就得跑一遍完整 LLM，推理速度受限。</p> <p>但 LLM-ASR 里有一个天然的”草稿生成器”一直没被充分利用：<strong>CTC（Connectionist Temporal Classification）encoder head</strong>。</p> <p>CTC 是 Conformer encoder 上的一个附属头，它不需要 LLM 就能独立地对整段语音做一遍快速转录——一次前向传播，直接输出完整的文字假设（hypothesis）。质量比 LLM 差一些，但速度快得多。</p> <p>这就是 SSD for ASR 的出发点：<strong>用 CTC 的输出作为草稿，让 LLM 来验证</strong>。</p> <h2 id="2-方法三步走的自推测解码">2. 方法：三步走的自推测解码</h2> <p>方法设计如下图：</p> <p><img src="/assets/img/posts/20260630-ssd-asr/figure1_p2.png" alt="SSD for LLM-based ASR 整体框架"/></p> <p>整个推理分三步：</p> <p><strong>Step 1：CTC Decode + Verify</strong></p> <p>先跑 Conformer encoder + CTC head，得到一条完整的 CTC hypothesis（如”the cat sat down”）。</p> <p>然后做第一道验证：计算每个 token 对应的 CTC 帧级熵（frame-level entropy）。如果所有帧的熵都低于阈值 τ_CTC（说明 CTC 非常确定），就直接接受 CTC 结果，跳过 LLM，大幅节省推理时间。</p> <p>这一步直接接受的比例越高，整体 RTFx 越大。</p> <p><strong>CTC 帧级熵的具体计算</strong>：CTC head 对语音的每一帧 t 输出一个概率分布 p(t)（包含所有词表 token + blank）。帧级信息熵：</p> \[H(t) = -\sum_{k} p_k(t) \log p_k(t)\] <p>熵越低说明 CTC 在这一帧越”笃定”。接受条件：对 CTC hypothesis 里每个 token x_j，找到与之对齐的所有帧 F_j，如果 max_{t ∈ F_j} H(t) &lt; τ_CTC 对所有 j 成立，则接受整条 hypothesis，跳过 LLM。</p> <p><strong>Step 2：LLM Verify</strong></p> <p>对 Step 1 没被直接接受的 CTC hypothesis，把 acoustic embedding + text prompt 一起喂给 LLM，做<strong>一次完整前向传播</strong>（非自回归），计算每个 CTC token 的 LLM 似然度。</p> <p>如果所有 token 的 LLM likelihood 都超过阈值 τ_SLM，接受 CTC 结果——这是第二道 filter，用 LLM 的语言理解能力来 double-check CTC 的可信度。</p> <p><strong>LLM likelihood 的具体含义</strong>：把 acoustic embedding（声学特征）和 CTC hypothesis 的所有 token 一起送进 LLM，做<strong>一次并行前向传播</strong>（类似 teacher forcing，不是自回归）：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 一次并行 forward，不逐 token 解码
</span><span class="n">all_logits</span> <span class="o">=</span> <span class="n">llm</span><span class="p">.</span><span class="nf">forward</span><span class="p">(</span><span class="n">acoustic_embed</span><span class="p">,</span> <span class="n">ctc_tokens</span><span class="p">)</span>
<span class="c1"># all_logits[j] 是 LLM 对位置 j 的输出分布
</span><span class="n">log_likelihoods</span> <span class="o">=</span> <span class="p">[</span><span class="nf">log_softmax</span><span class="p">(</span><span class="n">all_logits</span><span class="p">[</span><span class="n">j</span><span class="p">])[</span><span class="n">ctc_tokens</span><span class="p">[</span><span class="n">j</span><span class="p">]]</span> 
                   <span class="k">for</span> <span class="n">j</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="nf">len</span><span class="p">(</span><span class="n">ctc_tokens</span><span class="p">))]</span>
<span class="c1"># 接受条件：所有位置的 log likelihood 都超过 τ_SLM
</span><span class="n">accept</span> <span class="o">=</span> <span class="nf">all</span><span class="p">(</span><span class="n">ll</span> <span class="o">&gt;</span> <span class="n">τ_SLM</span> <span class="k">for</span> <span class="n">ll</span> <span class="ow">in</span> <span class="n">log_likelihoods</span><span class="p">)</span>
</code></pre></div></div> <p>关键：这是一次 O(1) 的并行 forward，而不是 O(n) 的逐 token 自回归，速度接近 prefill 而非 decode，远快于完整 AR 解码。</p> <p><strong>Step 3：AR Fallback</strong></p> <p>Step 2 不通过的部分（LLM 认为 CTC 某些 token 不可信），回退到完整的 LLM 自回归解码，以已接受的 CTC token 为前缀，继续生成剩余文本。</p> <p>这个三步框架很有意思：<strong>CTC 和 LLM 各司其职</strong>。CTC 快但不精，LLM 精但慢；SSD 让 CTC 先跑，LLM 只在必要时介入，整体开销大幅降低。</p> <p><strong>完整决策流程伪代码</strong>：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">ssd_asr_inference</span><span class="p">(</span><span class="n">audio</span><span class="p">,</span> <span class="n">conformer</span><span class="p">,</span> <span class="n">ctc_head</span><span class="p">,</span> <span class="n">llm</span><span class="p">,</span> <span class="n">τ_CTC</span><span class="p">,</span> <span class="n">τ_SLM</span><span class="p">):</span>
    <span class="c1"># CTC: 一次前向，得到帧级分布
</span>    <span class="n">acoustic_embed</span><span class="p">,</span> <span class="n">frame_logits</span> <span class="o">=</span> <span class="nf">conformer</span><span class="p">(</span><span class="n">audio</span><span class="p">)</span>
    <span class="n">ctc_tokens</span> <span class="o">=</span> <span class="nf">ctc_decode</span><span class="p">(</span><span class="n">frame_logits</span><span class="p">)</span>               <span class="c1"># beam search 或 greedy
</span>    
    <span class="c1"># ── Step 1: CTC Verify ──────────────────────────────
</span>    <span class="n">frame_entropies</span> <span class="o">=</span> <span class="nf">compute_entropy</span><span class="p">(</span><span class="n">frame_logits</span><span class="p">)</span>     <span class="c1"># H(t) for all t
</span>    <span class="k">if</span> <span class="nf">all_tokens_low_entropy</span><span class="p">(</span><span class="n">ctc_tokens</span><span class="p">,</span> <span class="n">frame_entropies</span><span class="p">,</span> <span class="n">τ_CTC</span><span class="p">):</span>
        <span class="k">return</span> <span class="n">ctc_tokens</span>                               <span class="c1"># 直接接受，跳过 LLM
</span>    
    <span class="c1"># ── Step 2: LLM Verify ──────────────────────────────
</span>    <span class="n">all_logits</span> <span class="o">=</span> <span class="n">llm</span><span class="p">.</span><span class="nf">parallel_forward</span><span class="p">(</span><span class="n">acoustic_embed</span><span class="p">,</span> <span class="n">ctc_tokens</span><span class="p">)</span>
    <span class="n">log_likelihoods</span> <span class="o">=</span> <span class="p">[</span><span class="nf">log_p</span><span class="p">(</span><span class="n">all_logits</span><span class="p">[</span><span class="n">j</span><span class="p">],</span> <span class="n">ctc_tokens</span><span class="p">[</span><span class="n">j</span><span class="p">])</span> 
                       <span class="k">for</span> <span class="n">j</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="nf">len</span><span class="p">(</span><span class="n">ctc_tokens</span><span class="p">))]</span>
    
    <span class="n">accepted_prefix</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="k">for</span> <span class="n">j</span><span class="p">,</span> <span class="n">ll</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">log_likelihoods</span><span class="p">):</span>
        <span class="k">if</span> <span class="n">ll</span> <span class="o">&gt;</span> <span class="n">τ_SLM</span><span class="p">:</span>
            <span class="n">accepted_prefix</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">ctc_tokens</span><span class="p">[</span><span class="n">j</span><span class="p">])</span>
        <span class="k">else</span><span class="p">:</span>
            <span class="k">break</span>   <span class="c1"># 第一个不满足的位置截断
</span>    
    <span class="k">if</span> <span class="nf">len</span><span class="p">(</span><span class="n">accepted_prefix</span><span class="p">)</span> <span class="o">==</span> <span class="nf">len</span><span class="p">(</span><span class="n">ctc_tokens</span><span class="p">):</span>
        <span class="k">return</span> <span class="n">ctc_tokens</span>                               <span class="c1"># 全部接受
</span>    
    <span class="c1"># ── Step 3: AR Fallback ─────────────────────────────
</span>    <span class="c1"># 以 accepted_prefix 为前缀，从截断位置开始自回归
</span>    <span class="n">remaining</span> <span class="o">=</span> <span class="n">llm</span><span class="p">.</span><span class="nf">autoregressive_decode</span><span class="p">(</span>
        <span class="n">acoustic_embed</span><span class="p">,</span> 
        <span class="n">prefix</span><span class="o">=</span><span class="n">accepted_prefix</span><span class="p">,</span>
        <span class="n">start_pos</span><span class="o">=</span><span class="nf">len</span><span class="p">(</span><span class="n">accepted_prefix</span><span class="p">)</span>
    <span class="p">)</span>
    <span class="k">return</span> <span class="n">accepted_prefix</span> <span class="o">+</span> <span class="n">remaining</span>
</code></pre></div></div> <p>这个流程的美妙之处：<strong>绝大多数请求在 Step 1 或 Step 2 就结束了</strong>（图2数据显示，简单数据集上 AR fallback 占比趋近于零），LLM 的自回归计算只在确实需要时才介入。</p> <h2 id="3-关键参数两个阈值的权衡">3. 关键参数：两个阈值的权衡</h2> <p>方法的核心超参是两个阈值：τ_CTC（CTC 熵阈值）和 τ_SLM（LLM 似然阈值）。</p> <p>下图展示了两个阈值在 Earnings22 数据集上对 WER 和 RTFx 的影响：</p> <p><img src="/assets/img/posts/20260630-ssd-asr/figure3_p3.png" alt="CTC 和 LLM 阈值对 WER 和 RTFx 的影响热力图"/></p> <p>左图是 WER（越低越好），右图是 RTFx（越高越快）。两个阈值形成一个 Pareto frontier：τ_CTC 越低（接受 CTC 的条件越严），WER 越低但 RTFx 越小；τ_SLM 越低（接受 LLM verified CTC 的条件越严），同理。</p> <p>实验定义了两个工作点：</p> <ul> <li><strong>High Accuracy</strong>（τ_CTC=0.7，τ_SLM=0.2）：精度优先，WER 最低</li> <li><strong>High RTFx</strong>（τ_CTC=3.0，τ_SLM=0.1）：速度优先，RTFx 最大</li> </ul> <h2 id="4-实验结果">4. 实验结果</h2> <p>测试模型是 granite-speech-4.0-1b（IBM，1B 参数），在英语 Open ASR、多语言 MLS、CommonVoice 等数据集上评测，H100 单卡测速。</p> <p>下表是核心结果：</p> <p><img src="/assets/img/posts/20260630-ssd-asr/table1_full.png" alt="Full AR vs. High Accuracy vs. High RTFx 三种模式的完整对比"/></p> <p>几个关键数字：</p> <p><strong>英语 Open ASR 平均</strong>：</p> <ul> <li>Full AR：WER 5.75，RTFx 564</li> <li>High Accuracy：WER <strong>5.58</strong>（显著低于 Full AR！），RTFx 548（几乎持平）</li> <li>High RTFx：WER 6.56，RTFx <strong>2491</strong>（速度 4.4× 于 Full AR）</li> </ul> <p>等等——High Accuracy 模式下 WER 反而比 Full AR 更低？这是因为 CTC 在某些情况下恰好纠正了 LLM 的错误。Table 2（原文）专门列了一些例子，说明 LLM verified CTC 的输出比纯 AR 更准的案例。</p> <p>本质原因在于两个模型各有盲区：<strong>LLM 有语言模型偏置</strong>，倾向于生成”读起来流畅”的文本，有时会把口语连读、领域专名或同音词”脑补”成更常见的表达（典型错误如把人名/地名替换成近音词）；而 CTC 完全依赖声学信号，没有这种语言偏置，在声学清晰的情况下反而更贴近真实发音。当 τ_SLM 设置适中时，Step 2 的验证逻辑能识别出”LLM 对这条 hypothesis 信心充足”的情况——此时 CTC 的声学精准度和 LLM 的语言确认形成互补，联合结果优于单独跑 LLM 自回归的结果。</p> <p><strong>多语言 MLS 平均</strong>：</p> <ul> <li>Full AR：WER 5.47，RTFx 629</li> <li>High Accuracy：WER <strong>5.33</strong>（同样更低），RTFx 699</li> </ul> <p><strong>High RTFx 模式下各数据集的时间分解</strong>：</p> <p><img src="/assets/img/posts/20260630-ssd-asr/figure2_p3.png" alt="各数据集的推理时间分解（High Accuracy 模式）"/></p> <p>橙色（AR fallback）在 Earnings22 这类难数据集上占比最大，在 LS Clean 这类干净数据集上几乎为零——说明模型在”简单”音频上基本靠 CTC 直接解决，LLM 只在难例上介入。</p> <p><strong>与其他 SOTA 系统对比</strong>：</p> <p><img src="/assets/img/posts/20260630-ssd-asr/figure5_p4.png" alt="granite-speech 与其他 SLM 的 WER vs. RTFx Pareto 曲线"/></p> <p>granite-speech 系列通过调整 SSD 阈值可以在 WER-RTFx 平面上走出一条 Pareto 曲线，在不同速度-精度要求下都能和 canary-qwen-2.5b 等竞争对手持平甚至更优。</p> <h2 id="5-和经典-ssd-的根本差异">5. 和经典 SSD 的根本差异</h2> <p>这篇工作和前几篇 SSD 在本质上有所不同，值得单独说一下：</p> <table> <thead> <tr> <th> </th> <th>经典 Self-SD（Dense/MoE/dLLM）</th> <th>SSD for ASR</th> </tr> </thead> <tbody> <tr> <td>草稿生成器</td> <td>目标模型的”残缺版”（跳层）</td> <td>独立的 CTC encoder head</td> </tr> <tr> <td>验证方式</td> <td>同一模型完整前向传播</td> <td>同一 LLM 的完整前向传播</td> </tr> <tr> <td>共享参数</td> <td>是（草稿和验证共享权重）</td> <td>部分（encoder 共享，CTC head 独立）</td> </tr> <tr> <td>“自”的含义</td> <td>同一模型自己给自己打草稿</td> <td>同一系统内的 CTC 分支给 LLM 打草稿</td> </tr> </tbody> </table> <p>所以这篇的”Self”其实比前几篇稍松一些——不是同一模型的不同”深度版本”，而是同一 system 内两个分支的协作。但核心精神是一样的：<strong>避免引入外部独立草稿模型，用系统内部的廉价计算路径生成草稿</strong>。</p> <h2 id="6-小结ssd-的适用范围比我们想象的宽">6. 小结：SSD 的适用范围比我们想象的宽</h2> <p>回顾一下整个系列的技术脉络：</p> <table> <thead> <tr> <th>论文</th> <th>场景</th> <th>草稿来源</th> <th>核心贡献</th> </tr> </thead> <tbody> <tr> <td>Draft &amp; Verify（2023）</td> <td>Dense LLM</td> <td>跳层浅版本</td> <td>提出 Self-SD 范式</td> </tr> <tr> <td>LayerSkip（ACL’24）</td> <td>Dense LLM</td> <td>训练支持的 early exit</td> <td>训练端配合，跨任务稳定</td> </tr> <tr> <td>SWIFT（ICLR’25）</td> <td>Dense LLM</td> <td>在线自适应选层</td> <td>无需训练，即插即用</td> </tr> <tr> <td>SS-MoE（WWW’25）</td> <td>MoE LLM</td> <td>少激活 expert 的浅版本</td> <td>SSD × MoE，解决显存墙</td> </tr> <tr> <td>SSD-dLLM（2025）</td> <td>扩散 LLM</td> <td>置信度最高的并行预测</td> <td>SSD 走出自回归边界</td> </tr> <tr> <td>SSD-ASR（2026）</td> <td>LLM-based ASR</td> <td>CTC encoder 分支</td> <td>SSD 进入语音识别领域</td> </tr> </tbody> </table> <p>从 Dense → MoE → dLLM → ASR，SSD 这个框架在不断往新场景迁移，核心问题始终是同一个：<strong>怎么在不引入外部独立草稿模型的前提下，用系统内部的廉价路径加速推理</strong>。</p> <p>离下一个还没被覆盖的场景，只差一点想象力。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 LLM 推理效率优化你想系统深入，可以看看我之前出版的<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，书里有专章讲 LLM 推理效率，和本文的加速框架思路有直接关联。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="ASR"/><category term="推理加速"/><category term="推测解码"/><category term="语音识别"/><category term="论文解读"/><summary type="html"><![CDATA[arXiv’26 | SSD for ASR：用 CTC 编码器打草稿，LLM 验证，语音识别也能推测解码]]></summary></entry><entry><title type="html">2025 | SSD for dLLMs：扩散语言模型也能用推测解码，最高 3.46×</title><link href="https://marsggbo.github.io/blog/2026/20260630-ssd-dllm/" rel="alternate" type="text/html" title="2025 | SSD for dLLMs：扩散语言模型也能用推测解码，最高 3.46×"/><published>2026-06-30T00:00:00+00:00</published><updated>2026-06-30T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260630-ssd-dllm</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260630-ssd-dllm/"><![CDATA[<h1 id="2025--ssd-for-dllms扩散语言模型也能用推测解码最高-346">2025 | SSD for dLLMs：扩散语言模型也能用推测解码，最高 3.46×</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2510.04147">Self Speculative Decoding for Diffusion Large Language Models</a> Yifeng Gao, Ziang Ji, Yuxuan Wang, Biqing Qi, Hanlin Xu, Linfeng Zhang（2025）</p> </blockquote> <hr/> <p>这是 Self-Speculative Decoding 系列第五篇。前四篇讲的都是自回归 LLM 上的 SSD，这一篇换了个完全不同的场景：<strong>扩散语言模型（Diffusion LLM，dLLM）</strong>。</p> <h2 id="1-dllm-是什么为什么要给它加速">1. dLLM 是什么，为什么要给它加速</h2> <p>dLLM 近年受到越来越多的关注，代表模型有 LLaDA 和 Dream。生成范式和 GPT 系列完全不同：</p> <ul> <li>从全 mask 的序列出发，<strong>迭代去噪</strong>，每步同时更新序列中多个位置</li> <li>用<strong>双向注意力</strong>（不是 causal attention），生成时能看到全局上下文</li> <li><strong>天然并行</strong>：每次前向传播可以同时确定多个 token</li> </ul> <p>听起来应该很快。但实际跑下来，dLLM 依然慢——即使每步并行更新多个位置，迭代的去噪步数依然不少，每步都要跑完整的双向 attention。</p> <p>下图说明了问题所在：有 cache 时 TPS 随 batch size 增长，但受限于去噪步数，天花板摆在那里：</p> <p><img src="/assets/img/posts/20260630-ssd-dllm/001_Figure_1.png" alt="有无 cache 时 TPS 随 batch size 的变化"/></p> <p><strong>能不能把 Self-Speculative Decoding 移植到 dLLM 上，减少所需的去噪步数？</strong></p> <p>这篇工作（2025年10月）给出了肯定的答案，是第一个针对 dLLM 的无损自推测加速框架。</p> <h2 id="2-先搞清楚dllm-的一步去噪在做什么">2. 先搞清楚：dLLM 的一步去噪在做什么</h2> <p>在讲 SSD 怎么做之前，先说清楚 dLLM 的标准推理流程，不然后面的方法会很难理解。</p> <p><strong>dLLM 的初始状态</strong>：输入序列中，生成位置全部用 <code class="language-plaintext highlighter-rouge">[MASK]</code> 占位，条件位置（prompt）保持原 token：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>输入：[prompt tokens...] [MASK] [MASK] [MASK] [MASK] [MASK]
                                  ↑要生成的 5 个位置
</code></pre></div></div> <p><strong>一步去噪</strong>：把含 mask 的序列喂进双向 Transformer，输出每个 mask 位置的 token 概率分布，然后按某种策略决定哪些位置在这一步”揭开”（unmask）：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">one_denoising_step</span><span class="p">(</span><span class="n">x_masked</span><span class="p">,</span> <span class="n">model</span><span class="p">,</span> <span class="n">n_unmask</span><span class="o">=</span><span class="mi">2</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    x_masked: 当前序列（含若干 MASK）
    n_unmask: 这一步决定揭开多少个位置
    </span><span class="sh">"""</span>
    <span class="c1"># 双向 attention，所有位置同时计算
</span>    <span class="n">logits</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">x_masked</span><span class="p">)</span>  <span class="c1"># [seq_len, vocab_size]
</span>    
    <span class="c1"># 对每个 MASK 位置，计算置信度（softmax 最大值）
</span>    <span class="n">mask_positions</span> <span class="o">=</span> <span class="p">[</span><span class="n">i</span> <span class="k">for</span> <span class="n">i</span><span class="p">,</span> <span class="n">t</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">x_masked</span><span class="p">)</span> <span class="k">if</span> <span class="n">t</span> <span class="o">==</span> <span class="n">MASK</span><span class="p">]</span>
    <span class="n">confidences</span> <span class="o">=</span> <span class="p">[</span><span class="n">logits</span><span class="p">[</span><span class="n">i</span><span class="p">].</span><span class="nf">softmax</span><span class="p">().</span><span class="nf">max</span><span class="p">()</span> <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="n">mask_positions</span><span class="p">]</span>
    
    <span class="c1"># 选置信度最高的 n_unmask 个位置揭开
</span>    <span class="n">top_positions</span> <span class="o">=</span> <span class="nf">topk</span><span class="p">(</span><span class="n">confidences</span><span class="p">,</span> <span class="n">n_unmask</span><span class="p">)</span>
    
    <span class="c1"># 对选中位置采样（或 greedy）得到新 token
</span>    <span class="n">new_tokens</span> <span class="o">=</span> <span class="p">[</span><span class="nf">sample</span><span class="p">(</span><span class="n">logits</span><span class="p">[</span><span class="n">p</span><span class="p">])</span> <span class="k">for</span> <span class="n">p</span> <span class="ow">in</span> <span class="n">top_positions</span><span class="p">]</span>
    
    <span class="c1"># 更新序列
</span>    <span class="n">x_new</span> <span class="o">=</span> <span class="n">x_masked</span><span class="p">.</span><span class="nf">copy</span><span class="p">()</span>
    <span class="k">for</span> <span class="n">pos</span><span class="p">,</span> <span class="n">tok</span> <span class="ow">in</span> <span class="nf">zip</span><span class="p">(</span><span class="n">top_positions</span><span class="p">,</span> <span class="n">new_tokens</span><span class="p">):</span>
        <span class="n">x_new</span><span class="p">[</span><span class="n">pos</span><span class="p">]</span> <span class="o">=</span> <span class="n">tok</span>
    
    <span class="k">return</span> <span class="n">x_new</span>
</code></pre></div></div> <p><strong>标准推理</strong>：重复 T 步，每步揭开若干位置，直到所有 MASK 都被填上：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Step 0: [MASK] [MASK] [MASK] [MASK] [MASK]
Step 1: [tok1]  [MASK] [MASK] [tok4] [MASK]  ← 揭开位置1,4
Step 2: [tok1] [tok2] [MASK] [tok4] [MASK]   ← 揭开位置2,5
Step 3: [tok1] [tok2] [tok3] [tok4] [tok5]   ← 揭开位置3,最后一步
</code></pre></div></div> <p><strong>SSD 的目标</strong>：把 T 步压缩成更少的步数，同时保证最终输出一样。</p> <h2 id="3-关键挑战dllm-的生成不是线性的">3. 关键挑战：dLLM 的生成不是线性的</h2> <p>在自回归 LLM 里，SSD 很直觉：draft K 个 token，verify，从第一个拒绝的地方截断。</p> <p>但 dLLM 有两个根本性的不同，让这个逻辑没法直接套用：</p> <p><strong>挑战一：Semi-Autoregressive 结构</strong></p> <p>dLLM 每步可能揭开多个位置，而且揭开的位置是<strong>不连续</strong>的（哪里置信度高就揭哪里）。这不像自回归那样是严格左到右的线性链，”截断”的概念需要重新定义。</p> <p><strong>挑战二：顺序不一致（Out-of-Order Unmasking）</strong></p> <p>这是 dLLM SSD 的核心难题。看个例子：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>当前状态：[MASK] [MASK] [MASK] [MASK] [MASK]

Draft 模型（一步揭开 2 个位置）：
  Step 1: 揭开位置3、位置5 → [MASK] [MASK] [tok3] [MASK] [tok5]
  Step 2: 揭开位置1、位置4 → [tok1] [MASK] [tok3] [tok4] [tok5]
  最终: [tok1] [tok2] [tok3] [tok4] [tok5]

Stepwise 验证（标准一步一步验证）：
  Step 1 验证：完整模型在初始状态下，最高置信度的 2 个位置是位置1、位置2
    → 标准答案是揭开 [tok1] 和 [tok2]
    → 但 draft 揭开的是位置3、位置5 ← 顺序不一致！即使最终 token 对，顺序不同也判拒绝
</code></pre></div></div> <p><strong>问题的本质</strong>：dLLM 的输出不只是”最终 token 是什么”，还包括”以什么顺序揭开”。同一组最终 token，不同的揭开顺序在 stepwise 验证框架下是不同的”路径”，其中大部分会被判为不一致。</p> <h2 id="4-方法层次化验证树">4. 方法：层次化验证树</h2> <p>SSD for dLLM 的核心机制是<strong>层次化验证树（Hierarchical Verification Tree）</strong>。</p> <p>下图展示了整体框架，左边是 step-by-step 的标准推理，右边是 SSD 的验证树推理：</p> <p><img src="/assets/img/posts/20260630-ssd-dllm/002_Figure_2.png" alt="SSD for dLLM 框架：stepwise 推理 vs. 验证树推理"/></p> <h3 id="41-线性验证链基础版本">4.1 线性验证链：基础版本</h3> <p>先理解最简单的情况——线性链（每次 draft 揭开 1 个位置）：</p> <p><strong>Draft 阶段</strong>：从当前状态 x（有 M 个 MASK），用草稿模型（跳过部分去噪步骤或用简化版模型）一次前向，按置信度选出 N 个位置，提议 token <code class="language-plaintext highlighter-rouge">[d₁, d₂, ..., d_N]</code>：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">draft_phase</span><span class="p">(</span><span class="n">x_current</span><span class="p">,</span> <span class="n">draft_model</span><span class="p">,</span> <span class="n">N</span><span class="p">):</span>
    <span class="n">logits</span> <span class="o">=</span> <span class="nf">draft_model</span><span class="p">(</span><span class="n">x_current</span><span class="p">)</span>  <span class="c1"># 一次前向
</span>    
    <span class="n">mask_positions</span> <span class="o">=</span> <span class="nf">get_mask_positions</span><span class="p">(</span><span class="n">x_current</span><span class="p">)</span>
    <span class="n">confidences</span> <span class="o">=</span> <span class="p">[</span><span class="n">logits</span><span class="p">[</span><span class="n">p</span><span class="p">].</span><span class="nf">softmax</span><span class="p">().</span><span class="nf">max</span><span class="p">()</span> <span class="k">for</span> <span class="n">p</span> <span class="ow">in</span> <span class="n">mask_positions</span><span class="p">]</span>
    
    <span class="c1"># 选置信度最高的 N 个位置
</span>    <span class="n">top_N_positions</span> <span class="o">=</span> <span class="nf">topk</span><span class="p">(</span><span class="n">confidences</span><span class="p">,</span> <span class="n">N</span><span class="p">)</span>
    <span class="n">top_N_tokens</span> <span class="o">=</span> <span class="p">[</span><span class="n">logits</span><span class="p">[</span><span class="n">p</span><span class="p">].</span><span class="nf">argmax</span><span class="p">()</span> <span class="k">for</span> <span class="n">p</span> <span class="ow">in</span> <span class="n">top_N_positions</span><span class="p">]</span>
    
    <span class="c1"># 构建 N+1 个候选状态（验证树的节点）
</span>    <span class="n">nodes</span> <span class="o">=</span> <span class="p">[</span><span class="n">x_current</span><span class="p">]</span>  <span class="c1"># 根节点：当前状态
</span>    <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">N</span><span class="p">):</span>
        <span class="n">x_i</span> <span class="o">=</span> <span class="nf">apply_tokens</span><span class="p">(</span><span class="n">nodes</span><span class="p">[</span><span class="mi">0</span><span class="p">],</span> <span class="n">top_N_positions</span><span class="p">[:</span><span class="n">i</span><span class="o">+</span><span class="mi">1</span><span class="p">],</span> <span class="n">top_N_tokens</span><span class="p">[:</span><span class="n">i</span><span class="o">+</span><span class="mi">1</span><span class="p">])</span>
        <span class="n">nodes</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">x_i</span><span class="p">)</span>  <span class="c1"># 子节点 i：接受前 i 个 draft token 后的状态
</span>    
    <span class="k">return</span> <span class="n">nodes</span><span class="p">,</span> <span class="n">top_N_positions</span><span class="p">,</span> <span class="n">top_N_tokens</span>
</code></pre></div></div> <p>验证树的 N+1 个节点：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>根节点 x₀:   [MASK] [MASK] [MASK] [MASK] [MASK]   ← 当前状态
子节点 x₁:   [d₁]   [MASK] [MASK] [MASK] [MASK]   ← 接受第1个 draft
子节点 x₂:   [d₁]   [d₂]   [MASK] [MASK] [MASK]   ← 接受前2个 draft
子节点 x₃:   [d₁]   [d₂]   [d₃]   [MASK] [MASK]   ← 接受前3个 draft
...
子节点 x_N:  [d₁]   [d₂]   [d₃]   ...    [d_N]    ← 接受全部 draft
</code></pre></div></div> <p><strong>Verify 阶段（关键：一次 batch forward 处理所有节点）</strong>：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">verify_phase</span><span class="p">(</span><span class="n">nodes</span><span class="p">,</span> <span class="n">full_model</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    将 N+1 个节点打包成一个 batch，一次前向传播处理所有
    等价于原来的 N+1 次独立前向，但只占用一次的时间（利用 GPU 并行）
    </span><span class="sh">"""</span>
    <span class="c1"># 所有节点组成一个 batch
</span>    <span class="n">batch</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">stack</span><span class="p">(</span><span class="n">nodes</span><span class="p">)</span>  <span class="c1"># [N+1, seq_len]
</span>    
    <span class="c1"># 一次 batch forward（双向 attention）
</span>    <span class="n">batch_logits</span> <span class="o">=</span> <span class="nf">full_model</span><span class="p">(</span><span class="n">batch</span><span class="p">)</span>  <span class="c1"># [N+1, seq_len, vocab_size]
</span>    
    <span class="k">return</span> <span class="n">batch_logits</span>
</code></pre></div></div> <p><strong>接受判断</strong>：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">accept_check</span><span class="p">(</span><span class="n">batch_logits</span><span class="p">,</span> <span class="n">nodes</span><span class="p">,</span> <span class="n">top_N_positions</span><span class="p">,</span> <span class="n">top_N_tokens</span><span class="p">):</span>
    <span class="n">accepted</span> <span class="o">=</span> <span class="mi">0</span>
    
    <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="nf">len</span><span class="p">(</span><span class="n">top_N_tokens</span><span class="p">)):</span>
        <span class="c1"># 完整模型对节点 x_i 的输出
</span>        <span class="c1"># 它在接下来应该揭开哪个位置、填什么 token？
</span>        <span class="n">node_logits</span> <span class="o">=</span> <span class="n">batch_logits</span><span class="p">[</span><span class="n">i</span><span class="p">]</span>  <span class="c1"># 节点 x_i 对应的 logits
</span>        
        <span class="n">mask_positions</span> <span class="o">=</span> <span class="nf">get_mask_positions</span><span class="p">(</span><span class="n">nodes</span><span class="p">[</span><span class="n">i</span><span class="p">])</span>
        <span class="n">confidences</span> <span class="o">=</span> <span class="p">[</span><span class="n">node_logits</span><span class="p">[</span><span class="n">p</span><span class="p">].</span><span class="nf">softmax</span><span class="p">().</span><span class="nf">max</span><span class="p">()</span> <span class="k">for</span> <span class="n">p</span> <span class="ow">in</span> <span class="n">mask_positions</span><span class="p">]</span>
        <span class="n">predicted_pos</span> <span class="o">=</span> <span class="nf">topk</span><span class="p">(</span><span class="n">confidences</span><span class="p">,</span> <span class="mi">1</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span>  <span class="c1"># 完整模型最想揭开哪里
</span>        <span class="n">predicted_tok</span> <span class="o">=</span> <span class="n">node_logits</span><span class="p">[</span><span class="n">predicted_pos</span><span class="p">].</span><span class="nf">argmax</span><span class="p">()</span>
        
        <span class="c1"># 检查是否和 draft 的第 i+1 个提议一致
</span>        <span class="k">if</span> <span class="n">predicted_pos</span> <span class="o">==</span> <span class="n">top_N_positions</span><span class="p">[</span><span class="n">i</span><span class="p">]</span> <span class="ow">and</span> <span class="n">predicted_tok</span> <span class="o">==</span> <span class="n">top_N_tokens</span><span class="p">[</span><span class="n">i</span><span class="p">]:</span>
            <span class="n">accepted</span> <span class="o">+=</span> <span class="mi">1</span>  <span class="c1"># 接受，继续检查下一个
</span>        <span class="k">else</span><span class="p">:</span>
            <span class="c1"># 拒绝：使用完整模型的预测（predicted_pos, predicted_tok），截断
</span>            <span class="k">break</span>
    
    <span class="k">return</span> <span class="n">accepted</span>
</code></pre></div></div> <p><strong>加速的来源</strong>：标准推理需要 T 次独立前向（每次处理 1 个状态），SSD 每轮只需要 1 次 batch forward（处理 N+1 个状态），但通过验证至少接受 1 个、最多接受 N 个去噪步骤的等价结果。即使接受率只有 50%，平均每次 batch forward 能推进 ~N/2 步，比 1 步快得多。</p> <h3 id="42-mix-order-扩展应对顺序不一致">4.2 Mix-Order 扩展：应对顺序不一致</h3> <p>对于顺序不一致的失败，SSD-dLLM 引入了 <strong>grandchild 节点</strong>：</p> <p><img src="/assets/img/posts/20260630-ssd-dllm/004_Figure_4.png" alt="Out-of-order 失败案例 vs. Mix-order 验证树扩展"/></p> <p><strong>问题重现</strong>：节点 x₀（全 MASK），draft 提议揭开位置3。但完整模型对 x₀ 的 stepwise 输出是揭开位置1——顺序不一致，拒绝。</p> <p><strong>解法</strong>：在每个父节点下，同时保留”按 draft 顺序”（子节点）和”按完整模型预测顺序”（grandchild 节点）两条路径：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>验证树（mix-order 版本，draft N=3）：
                      x₀（根：全 MASK）
                    /        \
          子节点 x₁           grandchild x₁'
        （draft 位置3）      （完整模型预测位置1）
           /    \
       x₂         x₂'         ...
   （draft+3）  （完整模型+）
</code></pre></div></div> <p>树的总节点数从 N+1 扩展到 <strong>2N-1</strong>，多出的节点处理了 draft 顺序和 stepwise 顺序不一致的情况。实验表明 mix-order 带来约 2.5%-3.1% 的额外步数减少，代价是 batch size 增大约 50%-67%。</p> <h2 id="5-实验结果">5. 实验结果</h2> <p>实验在 LLaDA 和 Dream 系列上跑，任务覆盖数学（GSM8K、MATH）、代码（HumanEval、MBPP），单张 A100 80GB。</p> <p>主要结果：</p> <table> <thead> <tr> <th>模型</th> <th>Draft 长度</th> <th>TPS</th> <th>Speedup</th> <th>步数减少</th> </tr> </thead> <tbody> <tr> <td>LLaDA-Base</td> <td>—</td> <td>13.84</td> <td>1×</td> <td>—</td> </tr> <tr> <td>LLaDA-Base + SSD</td> <td>4</td> <td>28.10</td> <td><strong>2.03×</strong></td> <td>58.6%</td> </tr> <tr> <td>LLaDA-Instruct + SSD</td> <td>4</td> <td>51.37</td> <td><strong>2.11×</strong></td> <td>60.6%</td> </tr> <tr> <td>Dream-Instruct</td> <td>—</td> <td>16.26</td> <td>1×</td> <td>—</td> </tr> <tr> <td>Dream-Instruct + SSD</td> <td>5</td> <td>39.58</td> <td><strong>2.43×</strong></td> <td>67.6%</td> </tr> <tr> <td>Dream-Instruct（MBPP）</td> <td>5</td> <td>22.07</td> <td><strong>3.46×</strong></td> <td>77.4%</td> </tr> </tbody> </table> <p>最高的 3.46× 在 Dream-Instruct + MBPP 代码生成任务上，步数减少了 77.4%。所有实验质量指标与 stepwise 解码完全一致——无损。</p> <p><strong>Speedup 对 sequence length 和 prompt 长度的敏感性</strong>：</p> <p><img src="/assets/img/posts/20260630-ssd-dllm/003_Figure_3.png" alt="SSD 加速比随生成长度和 prompt 长度的变化"/></p> <p>左边两图：生成长度从 128 到 512，SSD（橙色）始终比 baseline（绿色）快。右边两图：随着 N-shots 增加（prompt 变长），speedup 有所下降——因为更长 prompt 让 prefill 开销占比增大，SSD 对 decode 阶段的优化被稀释了。</p> <h2 id="6-局限性">6. 局限性</h2> <p><strong>顺序不一致问题仍未完全解决</strong>：即使加了 Mix-Order 扩展，也只处理了部分情况，这是 dLLM SSD 和自回归 SSD 相比的根本性障碍。</p> <p><strong>最优 draft 长度模型相关</strong>：LLaDA 在 N=4 时最优，Dream 在 N=5 时最优，没有通用值，需要调参。</p> <p><strong>长 prompt 场景加速比下降</strong>：3-shot 下从 ~2.4× 降到 ~1.5×。</p> <h2 id="7-小结">7. 小结</h2> <p>dLLM SSD 是 Self-Speculative Decoding 第一次走出自回归 LLM 的范围。2×-3.46× 的加速、完全无损——对于一类本来就”天然并行”却还是跑不快的模型来说，这个结果相当漂亮。</p> <p>关键创新点值得记住：</p> <ul> <li><strong>验证树 = 用一次 batch forward 处理 N+1 种”假设状态”</strong>，避免了 N+1 次独立推理</li> <li><strong>Mix-Order 节点</strong>处理了 dLLM 特有的顺序不一致失败模式</li> <li>无损保证来自严格的 stepwise 一致性检查</li> </ul> <p>下一篇，SSD 去到了离经典 NLP 更远的地方：<strong>自动语音识别（ASR）</strong>，用 CTC encoder 做草稿，LLM decoder 做验证。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 LLM 推理效率优化你想系统深入，可以看看我们团队出版的<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，书里虽然不直接讲 diffusion LLM，但 LLM 效率优化的加速思路和本文有一定关联。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="扩散模型"/><category term="推理加速"/><category term="推测解码"/><category term="论文解读"/><summary type="html"><![CDATA[2025 | SSD for dLLMs：扩散语言模型也能用推测解码，最高 3.46×]]></summary></entry><entry><title type="html">ICLR’25 | SWIFT：不训练不搜索，Self-Speculative Decoding 的即插即用版</title><link href="https://marsggbo.github.io/blog/2026/20260630-swift/" rel="alternate" type="text/html" title="ICLR’25 | SWIFT：不训练不搜索，Self-Speculative Decoding 的即插即用版"/><published>2026-06-30T00:00:00+00:00</published><updated>2026-06-30T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260630-swift</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260630-swift/"><![CDATA[<h1 id="iclr25--swift不训练不搜索self-speculative-decoding-的即插即用版">ICLR’25 | SWIFT：不训练不搜索，Self-Speculative Decoding 的即插即用版</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2410.06916">SWIFT: On-the-Fly Self-Speculative Decoding for LLM Inference Acceleration</a> Heming Xia, Yongqi Li, Jun Zhang, Cunxiao Du, Wenjie Li，ICLR 2025</p> </blockquote> <hr/> <p>这是 Self-Speculative Decoding 系列第三篇。前两篇的进展：</p> <ul> <li><strong>Draft &amp; Verify（2023）</strong>：提出范式，层配置靠贝叶斯优化离线搜 6-7 小时，task-specific，换任务就得重搜</li> <li><strong>LayerSkip（ACL’24）</strong>：从训练端解决跨任务问题，效果更好，但<strong>必须用特定配方重训模型</strong></li> </ul> <p>两篇工作放在一起，依然是个两难：要么接受高昂的离线搜索代价，要么接受重新训练的门槛。</p> <p><strong>SWIFT（ICLR 2025）说：都不用。</strong></p> <p>它的核心主张是：LLM 的层稀疏性是内在属性，<strong>可以在推理过程中即时发现</strong>，不需要任何预训练改动，优化开销只占总推理时间的 <strong>0.8%</strong>。</p> <h2 id="1-先看两个关键观察">1. 先看两个关键观察</h2> <p>在讲方法之前，先看 SWIFT 给出的两个实验发现，这是整个设计的出发点。</p> <h3 id="观察一均匀跳层效果比你想象的好">观察一：均匀跳层效果比你想象的好</h3> <p>很多人会觉得，不做任何优化直接跳层，draft token 质量会很差。SWIFT 做了个实验：对 LLaMA-2 均匀跳掉一半的层（就是 every other layer），再用 top-k（k=5）候选而非贪婪输出 draft token。</p> <p>结果：acceptance rate 高达 <strong>90% 以上</strong>，平均 speedup <strong>1.22×</strong>。</p> <p>这说明层稀疏性比大家以为的强得多。</p> <h3 id="观察二层稀疏性强烈-task-specific">观察二：层稀疏性强烈 task-specific</h3> <p>但均匀跳层只能到 1.22×，离实用还有差距。问题在哪？SWIFT 发现，不同任务的最优层配置差异极大——在 storytelling 任务上搜出的最优配置，拿到 reasoning 任务上用，speedup 从 1.47× 直接跌到 1.01×。</p> <p>如下图，(a) 是跳层数量 vs. acceptance rate 的关系，(b) 是跨任务迁移后 speedup 的崩跌：</p> <p><img src="/assets/img/posts/20260630-swift/002_Figure_2.png" alt="层稀疏性潜力 vs. 跨任务迁移的 speedup 崩跌"/></p> <p>右边柱状图里，Sum. LS（摘要任务搜出的配置）放到 Reasoning 任务上，直接掉到 0.99×——比不加速还差一点点。这正是 Draft &amp; Verify 的根本痛点。</p> <p>SWIFT 的结论：与其离线为每个任务搜一次，不如<strong>在推理时在线自适应</strong>地找当前任务的最优配置。</p> <h2 id="2-方法两阶段在线自适应">2. 方法：两阶段在线自适应</h2> <p>SWIFT 的推理流程如下图，分两个阶段：</p> <p><img src="/assets/img/posts/20260630-swift/003_Figure_3.png" alt="SWIFT 推理时间线：上下文优化阶段 + 置信度感知加速阶段"/></p> <p>生成序列的前 mN 个 token 用于<strong>在线优化层配置</strong>（图中黄/红色区域），之后切换到<strong>置信度感知加速模式</strong>（绿色区域）。</p> <h3 id="21-搜索空间层配置是个二进制向量">2.1 搜索空间：层配置是个二进制向量</h3> <p>首先明确 SWIFT 在优化什么。</p> <p>对于一个 L 层的 LLM，层配置用一个二进制向量 <strong>z ∈ {0,1}^L</strong> 表示：z_l = 1 表示跳过第 l 层，z_l = 0 表示保留。搜索空间是 2^L 种组合——LLaMA-2-13B 有 40 层，搜索空间就是 2^40 ≈ 1 万亿。显然不能穷举。</p> <p>Draft &amp; Verify 用离线贝叶斯优化搜 7.5 小时来处理这个规模。SWIFT 说：不需要，因为<strong>你不需要找全局最优，只需要找当前任务上”足够好”的局部最优</strong>，而且可以在推理过程中边跑边找。</p> <h3 id="22-阶段一在线层配置优化">2.2 阶段一：在线层配置优化</h3> <p><strong>关键问题：怎么评估一个层配置 z 的好坏，而不引入额外的推理开销？</strong></p> <p>SWIFT 的核心洞察是：<strong>用已经生成的上下文 token 来评估，不需要预测未来</strong>。</p> <p>具体做法：</p> <p>取最近生成的 γ 个 token 组成一个上下文窗口 <code class="language-plaintext highlighter-rouge">[x_{t-γ}, ..., x_{t-1}]</code>，对这段已知序列，用当前层配置 z 运行草稿模型：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>对窗口内每个位置 i：
  draft_logits = forward(x_{i-γ:i}, skip_layers=z)
  draft_pred[i] = argmax(draft_logits)

matchness(z) = mean(draft_pred[i] == x_i for all i in window)
</code></pre></div></div> <p>也就是说，<strong>matchness 衡量的是：如果你用配置 z 来预测这段已知文本，能猜对多少个 token</strong>。这段文本已经生成了，对比是瞬间的，不需要额外运行任何新 token 的推理。</p> <p>这就是为什么 SWIFT 的优化开销只有 0.8%——matchness 的计算几乎零额外代价。</p> <p><strong>动态搜索策略</strong></p> <p>有了 matchness 评分，SWIFT 用以下流程在线搜索最优 z：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 阶段一：上下文积累 + 在线优化
</span><span class="n">collected_configs</span> <span class="o">=</span> <span class="p">[]</span>   <span class="c1"># 收集的 (z, matchness) 样本
</span><span class="n">best_z</span> <span class="o">=</span> <span class="n">uniform_skip_z</span>  <span class="c1"># 初始：均匀跳层
</span>
<span class="k">for</span> <span class="n">t</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="n">optimization_steps</span><span class="p">):</span>
    <span class="k">if</span> <span class="n">t</span> <span class="o">%</span> <span class="n">β</span> <span class="o">==</span> <span class="mi">0</span><span class="p">:</span>  <span class="c1"># β=25
</span>        <span class="c1"># 贝叶斯优化：用已收集样本更新 GP，推荐下一个候选 z
</span>        <span class="n">gp</span><span class="p">.</span><span class="nf">fit</span><span class="p">(</span><span class="n">collected_configs</span><span class="p">,</span> <span class="n">collected_scores</span><span class="p">)</span>
        <span class="n">z_candidate</span> <span class="o">=</span> <span class="nf">ucb_acquisition</span><span class="p">(</span><span class="n">gp</span><span class="p">)</span>  <span class="c1"># UCB 采集函数
</span>    <span class="k">else</span><span class="p">:</span>
        <span class="c1"># 随机探索：在 best_z 附近随机翻转若干位
</span>        <span class="n">z_candidate</span> <span class="o">=</span> <span class="nf">random_neighbor</span><span class="p">(</span><span class="n">best_z</span><span class="p">)</span>
    
    <span class="c1"># 在当前上下文窗口评估候选 z
</span>    <span class="n">score</span> <span class="o">=</span> <span class="nf">compute_matchness</span><span class="p">(</span><span class="n">z_candidate</span><span class="p">,</span> <span class="n">context_window</span><span class="p">[</span><span class="o">-</span><span class="n">γ</span><span class="p">:])</span>
    <span class="n">collected_configs</span><span class="p">.</span><span class="nf">append</span><span class="p">((</span><span class="n">z_candidate</span><span class="p">,</span> <span class="n">score</span><span class="p">))</span>
    
    <span class="k">if</span> <span class="n">score</span> <span class="o">&gt;</span> <span class="n">best_score</span><span class="p">:</span>
        <span class="n">best_z</span> <span class="o">=</span> <span class="n">z_candidate</span>
        <span class="n">best_score</span> <span class="o">=</span> <span class="n">score</span>
    
    <span class="c1"># 用 best_z 生成一个新 token（正常推理）
</span>    <span class="n">new_token</span> <span class="o">=</span> <span class="nf">generate_one_token</span><span class="p">(</span><span class="n">context</span><span class="p">,</span> <span class="n">skip_layers</span><span class="o">=</span><span class="n">best_z</span><span class="p">)</span>
    <span class="n">context</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">new_token</span><span class="p">)</span>
</code></pre></div></div> <p>贝叶斯优化的 GP 维护一个从层配置向量到 matchness 分数的映射，每 β 步用 UCB 采集函数选出”预期最优 + 不确定性高”的下一个候选点。随机探索则在 GP 不确定的区域撒网，避免 BO 局部最优。</p> <p><strong>延迟开销</strong>：</p> <p><img src="/assets/img/posts/20260630-swift/006_Figure_6.png" alt="SWIFT 推理延迟分解：Optimize 只占 0.8%"/></p> <p>图右边的 latency breakdown：Optimize 只有 0.24ms，占总延迟的 <strong>0.8%</strong>，Draft 占 64.4%，Verify 占 28.4%。相比 Draft &amp; Verify 的 7.5 小时离线搜索，优化时间缩短了约 <strong>180 倍</strong>。</p> <h3 id="23-阶段二置信度感知的推理加速">2.3 阶段二：置信度感知的推理加速</h3> <p>找到最优 z 之后，进入加速生成阶段。SWIFT 没有直接用跳层模型做 greedy draft，而是加入了两个置信度感知机制。</p> <p><strong>① 早停（Early-Stopping Drafting）</strong></p> <p>每生成一个 draft token，计算当前置信度 <code class="language-plaintext highlighter-rouge">p_j = max(softmax(logits))</code>：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">draft_tokens</span> <span class="o">=</span> <span class="p">[]</span>
<span class="n">draft_candidates</span> <span class="o">=</span> <span class="p">[]</span>

<span class="k">for</span> <span class="n">j</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">max_draft_len</span><span class="p">):</span>
    <span class="n">logits</span> <span class="o">=</span> <span class="nf">forward</span><span class="p">(</span><span class="n">context</span> <span class="o">+</span> <span class="n">draft_tokens</span><span class="p">,</span> <span class="n">skip_layers</span><span class="o">=</span><span class="n">best_z</span><span class="p">)</span>
    <span class="n">p_j</span> <span class="o">=</span> <span class="n">logits</span><span class="p">.</span><span class="nf">softmax</span><span class="p">().</span><span class="nf">max</span><span class="p">()</span>
    
    <span class="k">if</span> <span class="n">p_j</span> <span class="o">&lt;</span> <span class="n">ε</span><span class="p">:</span>  <span class="c1"># 置信度低于阈值
</span>        <span class="k">break</span>    <span class="c1"># 提前停止，后续 draft 大概率被拒绝
</span>    
    <span class="n">k</span> <span class="o">=</span> <span class="nf">confidence_to_topk</span><span class="p">(</span><span class="n">p_j</span><span class="p">)</span>  <span class="c1"># 见下表
</span>    <span class="n">candidates</span> <span class="o">=</span> <span class="nf">topk</span><span class="p">(</span><span class="n">logits</span><span class="p">,</span> <span class="n">k</span><span class="p">)</span>
    <span class="n">draft_tokens</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">candidates</span><span class="p">[</span><span class="mi">0</span><span class="p">])</span>   <span class="c1"># argmax 作为草稿
</span>    <span class="n">draft_candidates</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">candidates</span><span class="p">)</span>  <span class="c1"># 保存前 k 个候选
</span></code></pre></div></div> <p><strong>② 动态 top-k 候选验证</strong></p> <p>根据置信度选不同数量的候选，而非固定用 greedy：</p> <table> <thead> <tr> <th>置信度区间</th> <th>top-k</th> </tr> </thead> <tbody> <tr> <td>(0, 0.5]</td> <td>k=10</td> </tr> <tr> <td>(0.5, 0.8]</td> <td>k=5</td> </tr> <tr> <td>(0.8, 0.95]</td> <td>k=3</td> </tr> <tr> <td>(0.95, 1]</td> <td>k=1</td> </tr> </tbody> </table> <p>直觉很清楚：<strong>置信度高说明草稿模型很确定，k=1 就够（等同于 greedy），验证快；置信度低说明不确定，给更多候选兜底，提高 acceptance rate</strong>。</p> <p><strong>验证与接受逻辑</strong>：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Verify 阶段：一次 batch forward 过完整模型
# 输入：原始 context + 所有 draft_tokens（并行）
</span><span class="n">verify_logits</span> <span class="o">=</span> <span class="nf">full_model_batch_forward</span><span class="p">(</span><span class="n">context</span><span class="p">,</span> <span class="n">draft_tokens</span><span class="p">)</span>

<span class="n">accepted</span> <span class="o">=</span> <span class="mi">0</span>
<span class="k">for</span> <span class="n">j</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="nf">len</span><span class="p">(</span><span class="n">draft_tokens</span><span class="p">)):</span>
    <span class="c1"># 完整模型在位置 j 输出的 token
</span>    <span class="n">full_token</span> <span class="o">=</span> <span class="nf">sample_from</span><span class="p">(</span><span class="n">verify_logits</span><span class="p">[</span><span class="n">j</span><span class="p">])</span>  <span class="c1"># 无损：按完整模型分布采样
</span>    
    <span class="k">if</span> <span class="n">full_token</span> <span class="ow">in</span> <span class="n">draft_candidates</span><span class="p">[</span><span class="n">j</span><span class="p">]:</span>
        <span class="c1"># 完整模型选的 token 在草稿的 top-k 候选里 → 接受
</span>        <span class="n">accepted</span> <span class="o">+=</span> <span class="mi">1</span>
    <span class="k">else</span><span class="p">:</span>
        <span class="c1"># 不在候选里 → 拒绝，用完整模型的 token，截断后续草稿
</span>        <span class="n">context</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">full_token</span><span class="p">)</span>
        <span class="k">break</span>

<span class="c1"># 接受的那些 draft token 加入输出
</span><span class="n">context</span><span class="p">.</span><span class="nf">extend</span><span class="p">(</span><span class="n">draft_tokens</span><span class="p">[:</span><span class="n">accepted</span><span class="p">])</span>
</code></pre></div></div> <p><strong>无损保证</strong>：虽然用了 top-k 而不是严格的 min(1, p/q) 接受条件，但 SWIFT 证明，只要在拒绝时从修正分布 <code class="language-plaintext highlighter-rouge">norm(max(0, p_full - p_draft_topk))</code> 重新采样，最终输出分布仍等价于完整模型的自回归分布。acceptance rate 的数学期望约等于完整模型预测 token 落在 top-k 候选集内的概率之和。</p> <h2 id="3-实验在线-vs-离线的差距有多大">3. 实验：在线 vs. 离线的差距有多大</h2> <p><strong>和 Self-SD（Draft &amp; Verify）对比</strong>，这是最关键的一张图：</p> <p><img src="/assets/img/posts/20260630-swift/007_Figure_7.png" alt="SWIFT vs. Self-SD 在动态任务流下的 speedup 和 acceptance rate"/></p> <p>横轴是输入实例序号，绿线（SWIFT）的 speedup 一直稳在 1.4×-1.6×，acceptance rate 稳在 ~95%。灰线（Self-SD）在任务切换时 speedup 和 acceptance rate 同时崩跌——正是 task-specific 层配置的问题。</p> <p><strong>在 LLaMA-2 系列和 CodeLLaMA 上的主要结果</strong>：</p> <ul> <li>LLaMA-2-13B，CNN/DM：<strong>1.37×</strong>；GSM8K：<strong>1.31×</strong>；TinyStories：<strong>1.53×</strong></li> <li>LLaMA-2-70B 平均：<strong>1.48×</strong></li> <li>CodeLLaMA-13B，HumanEval：<strong>1.40×</strong>，acceptance rate <strong>0.98</strong>，pass@1 完全一致</li> </ul> <p><strong>在 Yi-34B 和 DeepSeek-Coder-33B 上</strong>：</p> <p><img src="/assets/img/posts/20260630-swift/009_Figure_9.png" alt="SWIFT 在 Yi-34B 和 DeepSeek-Coder 上的 speedup"/></p> <p>Yi-34B 1.31×，DeepSeek-Coder-33B Base 直接到 <strong>1.54×</strong>。说明方法对不同架构和规模有良好的泛化性。</p> <p><strong>规模效应</strong>：更大的模型层稀疏性更强，70B 可以以更高的跳层比例（0.45-0.5）运行，最终 speedup 比 13B 更高——大模型越大越”冗余”，SSD 越有搞头。</p> <h2 id="4-和前两篇工作的横向对比">4. 和前两篇工作的横向对比</h2> <table> <thead> <tr> <th> </th> <th>Draft &amp; Verify</th> <th>LayerSkip</th> <th>SWIFT</th> </tr> </thead> <tbody> <tr> <td>需要训练</td> <td>否</td> <td>是（重新预训练）</td> <td><strong>否</strong></td> </tr> <tr> <td>离线搜索代价</td> <td>~7.5 小时</td> <td>无需搜索</td> <td><strong>~0（0.8% overhead）</strong></td> </tr> <tr> <td>任务切换时</td> <td>AR 92%→68%</td> <td>稳定</td> <td><strong>稳定（~96%）</strong></td> </tr> <tr> <td>最大 Speedup</td> <td>1.99×（70B）</td> <td>2.16×（8B）</td> <td>1.62×（70B）</td> </tr> <tr> <td>适用范围</td> <td>任何模型</td> <td>需重训</td> <td><strong>任何模型</strong></td> </tr> </tbody> </table> <p>SWIFT 的绝对 speedup 上限比 LayerSkip 稍低（LayerSkip 因为训练时做了准备，可以更激进地选 exit 层），但<strong>适用范围最广</strong>——任何现成模型直接插上就能用。</p> <p>这也是 Self-SD 技术路线走到这里的一个重要节点：<strong>不改模型、不预搜索、即插即用的 1.3×-1.6× 加速</strong>，已经具备工程落地的条件。</p> <p>后续的工作开始把 SSD 往新场景推：MoE 架构、扩散语言模型、语音识别……下一篇聊 MoE 上的 SSD，那个场景的问题和动机更刺激一点。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 LLM 推理效率优化你想系统深入，可以看看我们团队出版的<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，书里讲到了 NAS 在推理优化中的应用思路，和 SWIFT 的在线自适应搜索设计有些呼应。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="推理加速"/><category term="推测解码"/><category term="Self-Speculative Decoding"/><category term="论文解读"/><summary type="html"><![CDATA[ICLR’25 | SWIFT：不训练不搜索，Self-Speculative Decoding 的即插即用版]]></summary></entry><entry><title type="html">2026 | dLLM-ASR 让语音识别推理快 4.44 倍，还不掉精度</title><link href="https://marsggbo.github.io/blog/2026/20260626-dllm-asr/" rel="alternate" type="text/html" title="2026 | dLLM-ASR 让语音识别推理快 4.44 倍，还不掉精度"/><published>2026-06-26T00:00:00+00:00</published><updated>2026-06-26T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260626-dllm-asr</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260626-dllm-asr/"><![CDATA[<h1 id="2026--dllm-asr-让语音识别推理快-444-倍还不掉精度">2026 | dLLM-ASR 让语音识别推理快 4.44 倍，还不掉精度</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2601.17902">dLLM-ASR: A Faster Diffusion LLM-based Framework for Speech Recognition</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有想过，当你对着手机说话，语音转文字的时候，背后到底在跑什么？</p> <p>早几年是端到端的声学模型（CTC、RNN-T 这类，直接从音频帧映射到文字，不依赖 LLM），后来 Whisper 出来，直接把语音识别的天花板抬了一截。再后来，大家发现把 Whisper 当编码器、接一个 LLM 来解码，精度还能再往上走——毕竟 LLM 有大量文本知识，能更好地处理同音词、专有名词这些边缘情况。</p> <p>但 LLM 有个老大难问题：<strong>自回归解码，token 一个一个吐</strong>。序列越长，等的时间越久，延迟和序列长度成线性关系 O(N)。对 ASR 来说，一句话十几二十个字，这个延迟在实时性要求高的场景（语音助手、实时字幕）就很要命。</p> <p>最近比较火的 <strong>Diffusion LLM（dLLM）</strong> 理论上能解这个问题——它并行生成整个序列，推理复杂度降到 O(K)，K 是去噪步数，通常远小于序列长度 N。</p> <p>但直接把 dLLM 搬来做 ASR，又踩了一堆新坑。这篇 <strong>dLLM-ASR</strong> 就是来填这些坑的，最终在 LibriSpeech（英语有声书语音识别标准 benchmark）等多个数据集上实现了 <strong>4.44× 推理加速</strong>，同时 <strong>WER</strong>（Word Error Rate，词错误率，越低越好）比自回归 LLM-ASR 还要低一点点。</p> <hr/> <h2 id="2-两个前置背景">2. 两个前置背景</h2> <h3 id="21-llm-based-asr-长什么样">2.1 LLM-based ASR 长什么样</h3> <p>现在主流的 LLM-based ASR 架构基本是三段式：</p> <p><strong>语音编码器（Speech Encoder）→ 对齐适配器（Adapter/Projector）→ LLM 解码器</strong></p> <p>以 Whisper-LLaMA3 为例：</p> <ul> <li>Whisper-large-v3 做编码器，把音频转成声学特征序列（25 Hz 帧率）</li> <li>一个 1D 卷积（stride=2，降采样到 12.5 Hz）+ 线性层，把声学特征维度从 1280 映射到 LLM 的 4096</li> <li>LLaMA3-8B 自回归地根据声学特征生成文字 token</li> </ul> <p>精度确实好，但推理慢——8B 参数的 LLM，一个词一个词地生成，<strong>RTF</strong>（Real-Time Factor，实时因子）在 LibriSpeech clean 上是 <strong>0.317</strong>。RTF = 处理耗时 / 音频时长，也就是处理 1 秒音频需要 0.317 秒。RTF &lt; 1 才算快于实时，越小越好；RTF &gt; 1 意味着系统处理速度跟不上说话速度，完全不能用于实时场景。</p> <h3 id="22-diffusion-llm-怎么工作">2.2 Diffusion LLM 怎么工作</h3> <p>dLLM（以 LLaDA 为代表）用的是 <strong>masked diffusion</strong>，思路和图像扩散模型有点像，但作用在离散 token 空间上。</p> <p><strong>前向过程（加噪）</strong>：把目标序列 x₀ 里的 token，以概率 t 替换成 <code class="language-plaintext highlighter-rouge">[MASK]</code>，得到 xₜ。t 越大，mask 越多，t=1 时全部被 mask。</p> <p><strong>训练目标</strong>：让模型学会从 xₜ 还原 x₀，loss 是：</p> \[\mathcal{L} = -\mathbb{E}_{t,x_0,x_t}\left[\frac{1}{t}\sum_{i=1}^{L}\mathbb{I}(x_t^i=[\mathrm{M}])\log p_\theta(x_0^i|x_t, A)\right]\] <p>其中 A 是条件（这里是声学特征），1/t 是归一化系数，平衡不同 mask 数量下的梯度。训练时以 α=0.2 的概率对全序列做完全 mask，增强鲁棒性。</p> <p><strong>推理过程</strong>：从全 mask 序列出发，迭代去噪 K 步，每步模型并行预测所有被 mask 的位置，置信度高的先 unmask，低的继续 mask 下一轮。因为每步都是并行预测，比自回归的逐 token 生成快很多。</p> <hr/> <h2 id="3-直接用-dllm-做-asr三个根本矛盾">3. 直接用 dLLM 做 ASR，三个根本矛盾</h2> <p>把 LLaDA 接上 Whisper 做 ASR（论文叫 Whisper-LLaDA baseline），实测下来 RTF 高达 <strong>1.678</strong>——比自回归的 Whisper-LLaMA3（0.317）慢了 <strong>5 倍多</strong>。dLLM 号称能并行生成，怎么反而更慢了？</p> <p>根本原因是：dLLM 是为<strong>开放式文本生成</strong>设计的，但 ASR 是<strong>强约束的声学转录任务</strong>，两者之间存在三个根本性的不匹配：</p> <p><strong>矛盾一：从纯噪声开始去噪</strong></p> <p>回顾一下 dLLM 的推理流程：从全 mask 序列出发，每一步模型预测所有被 mask 的位置，置信度高的先 unmask，剩下的继续 mask 到下一轮，迭代 K 步直到全部确定。</p> <p>问题在于：这个设计是为了”凭空生成文字”的场景设计的——输入是一个主题或 prompt，输出是模型自由发挥的内容，所以从全 mask 出发是合理的。但 ASR 不是自由生成，<strong>音频本身已经给了你大量信息</strong>：这句话大概说了多少个字、哪个位置是元音、停顿在哪里……这些声学特征都隐含着对转录结果的强烈约束。从全 mask 零起点去噪，等于把这些先验全部忽略，模型需要用更多步骤才能从噪声中”摸索”出答案。</p> <p><strong>矛盾二：序列长度固定</strong></p> <p>dLLM 在推理时需要一开始就确定序列长度——因为 transformer 的 self-attention 要在整个序列上做矩阵运算，序列长度是张量维度的一部分，必须提前定好。这在文本生成里不是大问题，因为你可以根据任务经验预设一个合理长度。</p> <p>但 ASR 不同，<strong>你根本不知道这段音频会转录出多少个字</strong>。5 秒的音频，快说的人可能说了 30 个字，慢的可能只有 10 个字。实际操作只能预设一个”够大”的上限（比如 100 个位置），然后短句子用大量 padding token 补齐。这些 padding 位置在每一步去噪中同样占用 attention 计算，和真正的文字 token 一起参与矩阵运算。句子越短，浪费的比例越高。</p> <p><strong>矛盾三：去噪步骤固定，不分难易</strong></p> <p>dLLM 的去噪步数 K 是一个固定超参数，所有 token 无论难易，都要经历同样多的去噪轮次。</p> <p>但实际上不同位置的 token 难度天差地别。比如”人工智能”这个词，在大多数语音上下文里几乎唯一确定，第一步去噪置信度就能达到 99%；而”吃”和”七”在普通话里同音，到底是哪个字，可能需要结合好几个词的上下文才能确定，置信度很晚才会收敛。但 dLLM 不管这些，K 步就是 K 步，”吃/七”要跑满，”人工智能”也要跑满。<strong>早就确定的 token 在后续轮次里只是在做无用功</strong>，但计算量实实在在地消耗在那里。</p> <hr/> <h2 id="4-dllm-asr四个补丁解决三个矛盾">4. dLLM-ASR：四个补丁解决三个矛盾</h2> <p>如下图是 dLLM-ASR 的整体架构，左边是模型结构和训练过程，右边展示了推理过程从 baseline 到完整方案的演进：</p> <p><img src="/assets/img/posts/20260626-dllm-asr/001_Figure_1.png" alt="dLLM-ASR 系统总体架构与推理流程对比"/></p> <p>方法部分有四个核心组件，对应解决上面的三个矛盾：</p> <h3 id="41-asr-prior-初始化">4.1 ASR Prior 初始化</h3> <p><strong>解决矛盾一</strong>：不从全 mask 出发，用 CTC 先验初始化去噪起点。</p> <p>在冻结的 Whisper 编码器输出上加一个轻量的 <strong>CTC 分支</strong>（Connectionist Temporal Classification，一种经典的非自回归语音识别解码方式，能直接并行输出每帧对应的字符概率，速度很快但精度不如 LLM）——1D 下采样卷积 + 分类头，生成一个初始的 ASR 假设序列。推理时：</p> <ol> <li>先跑 CTC 得到初始文字序列（计算量可忽略不计）</li> <li>把这个序列作为 dLLM 去噪的起始状态，而不是全 mask</li> </ol> <p>这样做两个好处：一是去噪起点已经”有意义”，离最终结果更近，收敛更快；二是 CTC 输出的长度可以直接作为目标序列长度的参考，解决了长度固定的问题。</p> <h3 id="42-length-adaptive-pruning">4.2 Length-Adaptive Pruning</h3> <p><strong>解决矛盾二</strong>：动态裁掉序列里的冗余 token。</p> <p>在第一步去噪后，找到置信度高的 padding/EOS 位置并裁掉。之后每一步去噪，已经 unmask 且置信度稳定的位置也可以从序列里移除。序列长度随去噪进行自适应收缩，每步的有效计算量不断降低。</p> <h3 id="43-confidence-based-denoising">4.3 Confidence-Based Denoising</h3> <p><strong>解决矛盾三</strong>：置信度超过阈值 τ 的 token 提前退出去噪循环。</p> <p>每一步去噪后，置信度 &gt; τ（默认 0.9）的 token 直接”冻结”，后续步骤不再更新它们。如果某步没有 token 超过 τ，则选当前置信度最高的一个 token 强制 unmask（保证每步都有进展，不陷入死循环）。</p> <p>这里有一个关键点容易被忽略：<strong>这个机制必须配合 ASR Prior 才能正常工作</strong>。如果初始状态是全 mask，模型在第一步对所有位置都高度不确定，但有些位置可能”凑巧”得了个高置信度——这个预测其实是噪声，并不是真正收敛了。一旦被冻结，错误就无法纠正。消融实验里，把置信度截断单独加到 Whisper-LLaDA baseline 上，WER 从 5.22 直接跳到 5.98，就是这个原因。而有了 ASR Prior 提供的有意义的初始状态之后，高置信度才真正意味着”这个位置已经确定了”，早退出才有效。</p> <p>τ 怎么选？如下图是消融实验：</p> <p><img src="/assets/img/posts/20260626-dllm-asr/002_Figure_2.png" alt="置信度阈值 τ 的消融实验（LibriSpeech clean 和 other）"/></p> <p>τ=0.9 和 τ=0.95 的 WER 差异可忽略不计，但 τ=0.9 的 RTF 更低，所以取 0.9 作为默认值。τ 太小（如 0.6）会过早冻结 token，WER 上升明显。</p> <h3 id="44-speech-kv-cache">4.4 Speech KV Cache</h3> <p>这个是附加的工程优化，但对速度贡献不小。</p> <p>dLLM 每一步去噪都要把声学特征 A 过一遍 attention，重新计算 Key 和 Value。但声学特征在整个推理过程中压根没有变——第一步算完的 KV cache 完全可以复用到后续所有步骤，不用重算。</p> <p>这个优化的效果取决于去噪步数 K：K 越大，节省越多，相当于把声学特征的 attention 计算量从 K 次压缩到 1 次。论文验证了加上这个优化后精度几乎无损，属于免费的速度提升。这个思路也不局限于 ASR——任何条件在推理过程中保持不变的 dLLM 场景（比如图文生成时的图像条件、结构化生成时的 schema 条件），都可以直接套用。</p> <hr/> <h3 id="用代码把四个组件串起来">用代码把四个组件串起来</h3> <p>论文暂未开源代码，但结构描述足够清晰，下面用简化代码把整个系统还原一遍，把上面讲的每个组件对号入座。</p> <p><strong>模型结构</strong>：Whisper 编码器（冻结）+ 适配器（含 CTC 分支）+ LLaDA 解码器。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">torch</span>
<span class="kn">import</span> <span class="n">torch.nn</span> <span class="k">as</span> <span class="n">nn</span>
<span class="kn">from</span> <span class="n">transformers</span> <span class="kn">import</span> <span class="n">WhisperModel</span><span class="p">,</span> <span class="n">AutoModelForMaskedLM</span>

<span class="k">class</span> <span class="nc">SpeechAdapter</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">whisper_dim</span><span class="o">=</span><span class="mi">1280</span><span class="p">,</span> <span class="n">llm_dim</span><span class="o">=</span><span class="mi">4096</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">conv</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Conv1d</span><span class="p">(</span><span class="n">whisper_dim</span><span class="p">,</span> <span class="n">whisper_dim</span><span class="p">,</span> <span class="n">kernel_size</span><span class="o">=</span><span class="mi">3</span><span class="p">,</span> <span class="n">stride</span><span class="o">=</span><span class="mi">2</span><span class="p">,</span> <span class="n">padding</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">proj</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">whisper_dim</span><span class="p">,</span> <span class="n">llm_dim</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">):</span>
        <span class="n">x</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">conv</span><span class="p">(</span><span class="n">x</span><span class="p">.</span><span class="nf">transpose</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">)).</span><span class="nf">transpose</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">proj</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>  <span class="c1"># [B, T/2, D_llm]
</span>
<span class="k">class</span> <span class="nc">CTCBranch</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">ASR Prior 的来源：轻量 CTC 头，直接接在冻结的 Whisper 输出上</span><span class="sh">"""</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">whisper_dim</span><span class="o">=</span><span class="mi">1280</span><span class="p">,</span> <span class="n">vocab_size</span><span class="o">=</span><span class="mi">50257</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">conv</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Conv1d</span><span class="p">(</span><span class="n">whisper_dim</span><span class="p">,</span> <span class="n">whisper_dim</span><span class="p">,</span> <span class="n">kernel_size</span><span class="o">=</span><span class="mi">3</span><span class="p">,</span> <span class="n">stride</span><span class="o">=</span><span class="mi">2</span><span class="p">,</span> <span class="n">padding</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">head</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">whisper_dim</span><span class="p">,</span> <span class="n">vocab_size</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">):</span>
        <span class="n">x</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">conv</span><span class="p">(</span><span class="n">x</span><span class="p">.</span><span class="nf">transpose</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">)).</span><span class="nf">transpose</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">head</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>  <span class="c1"># [B, T/2, vocab_size]
</span>
<span class="k">class</span> <span class="nc">dLLMASR</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">encoder</span> <span class="o">=</span> <span class="n">WhisperModel</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span><span class="sh">"</span><span class="s">openai/whisper-large-v3</span><span class="sh">"</span><span class="p">).</span><span class="n">encoder</span>
        <span class="n">self</span><span class="p">.</span><span class="n">adapter</span> <span class="o">=</span> <span class="nc">SpeechAdapter</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">ctc</span>     <span class="o">=</span> <span class="nc">CTCBranch</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">dllm</span>    <span class="o">=</span> <span class="n">AutoModelForMaskedLM</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span><span class="sh">"</span><span class="s">GSAI-ML/LLaDA-8B-Instruct</span><span class="sh">"</span><span class="p">)</span>
        <span class="k">for</span> <span class="n">p</span> <span class="ow">in</span> <span class="n">self</span><span class="p">.</span><span class="n">encoder</span><span class="p">.</span><span class="nf">parameters</span><span class="p">():</span>
            <span class="n">p</span><span class="p">.</span><span class="n">requires_grad</span> <span class="o">=</span> <span class="bp">False</span>

    <span class="k">def</span> <span class="nf">encode_speech</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">waveform</span><span class="p">):</span>
        <span class="n">acoustic</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">encoder</span><span class="p">(</span><span class="n">waveform</span><span class="p">).</span><span class="n">last_hidden_state</span>  <span class="c1"># [B, T, 1280]
</span>        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">adapter</span><span class="p">(</span><span class="n">acoustic</span><span class="p">),</span> <span class="n">self</span><span class="p">.</span><span class="nf">ctc</span><span class="p">(</span><span class="n">acoustic</span><span class="p">)</span>    <span class="c1"># speech_features, ctc_logits
</span></code></pre></div></div> <p><strong>训练</strong>：Stage 1 只练 Adapter，Stage 2 加入 LoRA（r=16）联合微调；masked diffusion loss + CTC 辅助 loss。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">train_step</span><span class="p">(</span><span class="n">model</span><span class="p">,</span> <span class="n">waveform</span><span class="p">,</span> <span class="n">transcript_ids</span><span class="p">):</span>
    <span class="n">speech_features</span><span class="p">,</span> <span class="n">ctc_logits</span> <span class="o">=</span> <span class="n">model</span><span class="p">.</span><span class="nf">encode_speech</span><span class="p">(</span><span class="n">waveform</span><span class="p">)</span>

    <span class="n">B</span><span class="p">,</span> <span class="n">L</span> <span class="o">=</span> <span class="n">transcript_ids</span><span class="p">.</span><span class="n">shape</span>
    <span class="n">t</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">rand</span><span class="p">(</span><span class="n">B</span><span class="p">)</span>
    <span class="n">t</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">where</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">rand</span><span class="p">(</span><span class="n">B</span><span class="p">)</span> <span class="o">&lt;</span> <span class="mf">0.2</span><span class="p">,</span> <span class="n">torch</span><span class="p">.</span><span class="nf">ones</span><span class="p">(</span><span class="n">B</span><span class="p">),</span> <span class="n">t</span><span class="p">)</span>  <span class="c1"># 20% 概率全 mask
</span>
    <span class="n">mask</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">rand</span><span class="p">(</span><span class="n">B</span><span class="p">,</span> <span class="n">L</span><span class="p">)</span> <span class="o">&lt;</span> <span class="n">t</span><span class="p">.</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="mi">1</span><span class="p">)</span>
    <span class="n">noisy_ids</span> <span class="o">=</span> <span class="n">transcript_ids</span><span class="p">.</span><span class="nf">masked_fill</span><span class="p">(</span><span class="n">mask</span><span class="p">,</span> <span class="n">MASK_ID</span><span class="p">)</span>

    <span class="n">logits</span> <span class="o">=</span> <span class="n">model</span><span class="p">.</span><span class="nf">dllm</span><span class="p">(</span>
        <span class="n">input_ids</span><span class="o">=</span><span class="n">noisy_ids</span><span class="p">,</span>
        <span class="n">encoder_hidden_states</span><span class="o">=</span><span class="n">speech_features</span><span class="p">,</span>
    <span class="p">).</span><span class="n">logits</span>

    <span class="c1"># 只在 mask 位置算 loss，1/t 归一化（对应 §2.2 的 loss 公式）
</span>    <span class="n">loss</span> <span class="o">=</span> <span class="o">-</span><span class="p">(</span><span class="n">logits</span><span class="p">.</span><span class="nf">log_softmax</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>
               <span class="p">.</span><span class="nf">gather</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">transcript_ids</span><span class="p">.</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)).</span><span class="nf">squeeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>
             <span class="o">*</span> <span class="n">mask</span><span class="p">).</span><span class="nf">sum</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span> <span class="o">/</span> <span class="n">t</span>
    <span class="n">ctc_loss</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">CTCLoss</span><span class="p">()(</span><span class="n">ctc_logits</span><span class="p">.</span><span class="nf">log_softmax</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">).</span><span class="nf">transpose</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mi">1</span><span class="p">),</span> <span class="n">transcript_ids</span><span class="p">,</span> <span class="p">...)</span>
    <span class="k">return</span> <span class="n">loss</span><span class="p">.</span><span class="nf">mean</span><span class="p">()</span> <span class="o">+</span> <span class="mf">0.1</span> <span class="o">*</span> <span class="n">ctc_loss</span>
</code></pre></div></div> <p><strong>推理</strong>：四个组件按顺序登场——CTC Prior → 第一步建 KV Cache（+ Confidence Freeze + Length Pruning）→ 后续步复用 KV Cache。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@torch.no_grad</span><span class="p">()</span>
<span class="k">def</span> <span class="nf">inference</span><span class="p">(</span><span class="n">model</span><span class="p">,</span> <span class="n">waveform</span><span class="p">,</span> <span class="n">num_steps</span><span class="o">=</span><span class="mi">20</span><span class="p">,</span> <span class="n">tau</span><span class="o">=</span><span class="mf">0.9</span><span class="p">):</span>
    <span class="n">speech_features</span><span class="p">,</span> <span class="n">ctc_logits</span> <span class="o">=</span> <span class="n">model</span><span class="p">.</span><span class="nf">encode_speech</span><span class="p">(</span><span class="n">waveform</span><span class="p">)</span>

    <span class="c1"># [ASR Prior] CTC greedy decode 作为去噪起点，而不是全 mask
</span>    <span class="n">ctc_pred</span> <span class="o">=</span> <span class="nf">ctc_collapse</span><span class="p">(</span><span class="n">ctc_logits</span><span class="p">.</span><span class="nf">argmax</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">))</span>  <span class="c1"># 合并重复、去 blank
</span>    <span class="n">x</span> <span class="o">=</span> <span class="n">ctc_pred</span><span class="p">.</span><span class="nf">clone</span><span class="p">()</span>

    <span class="c1"># [Speech KV Cache] 第一步去噪，同时缓存声学特征的 KV
</span>    <span class="n">logits</span><span class="p">,</span> <span class="n">kv_cache</span> <span class="o">=</span> <span class="n">model</span><span class="p">.</span><span class="nf">dllm</span><span class="p">(</span>
        <span class="n">input_ids</span><span class="o">=</span><span class="n">x</span><span class="p">,</span> <span class="n">encoder_hidden_states</span><span class="o">=</span><span class="n">speech_features</span><span class="p">,</span>
        <span class="n">use_cache</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span> <span class="n">return_speech_cache</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
    <span class="p">)</span>
    <span class="n">confidence</span><span class="p">,</span> <span class="n">tokens</span> <span class="o">=</span> <span class="n">logits</span><span class="p">.</span><span class="nf">softmax</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">).</span><span class="nf">max</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>

    <span class="c1"># [Confidence-Based Denoising] 置信度 &gt; τ 的位置直接冻结
</span>    <span class="n">frozen</span> <span class="o">=</span> <span class="n">confidence</span> <span class="o">&gt;</span> <span class="n">tau</span>
    <span class="n">x</span><span class="p">[</span><span class="n">frozen</span><span class="p">]</span> <span class="o">=</span> <span class="n">tokens</span><span class="p">[</span><span class="n">frozen</span><span class="p">]</span>

    <span class="c1"># [Length-Adaptive Pruning] 裁掉已确定的 EOS/padding
</span>    <span class="n">active</span> <span class="o">=</span> <span class="o">~</span><span class="p">((</span><span class="n">tokens</span> <span class="o">==</span> <span class="n">EOS_ID</span><span class="p">)</span> <span class="o">&amp;</span> <span class="n">frozen</span><span class="p">)</span>

    <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="n">num_steps</span><span class="p">):</span>
        <span class="k">if</span> <span class="ow">not</span> <span class="p">(</span><span class="o">~</span><span class="n">frozen</span> <span class="o">&amp;</span> <span class="n">active</span><span class="p">).</span><span class="nf">any</span><span class="p">():</span>
            <span class="k">break</span>  <span class="c1"># 全部确定，提前退出
</span>
        <span class="c1"># 复用 speech KV cache，声学 attention 只算一次
</span>        <span class="n">logits</span> <span class="o">=</span> <span class="n">model</span><span class="p">.</span><span class="nf">dllm</span><span class="p">(</span><span class="n">input_ids</span><span class="o">=</span><span class="n">x</span><span class="p">[:,</span> <span class="n">active</span><span class="p">],</span> <span class="n">speech_kv_cache</span><span class="o">=</span><span class="n">kv_cache</span><span class="p">)</span>
        <span class="n">confidence</span><span class="p">[:,</span> <span class="n">active</span><span class="p">],</span> <span class="n">tokens</span><span class="p">[:,</span> <span class="n">active</span><span class="p">]</span> <span class="o">=</span> <span class="n">logits</span><span class="p">.</span><span class="nf">softmax</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">).</span><span class="nf">max</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>

        <span class="n">newly_frozen</span> <span class="o">=</span> <span class="p">(</span><span class="n">confidence</span> <span class="o">&gt;</span> <span class="n">tau</span><span class="p">)</span> <span class="o">&amp;</span> <span class="o">~</span><span class="n">frozen</span> <span class="o">&amp;</span> <span class="n">active</span>
        <span class="k">if</span> <span class="ow">not</span> <span class="n">newly_frozen</span><span class="p">.</span><span class="nf">any</span><span class="p">():</span>
            <span class="c1"># 没有超阈值的，强制冻结置信度最高的一个
</span>            <span class="n">best</span> <span class="o">=</span> <span class="p">(</span><span class="n">confidence</span> <span class="o">*</span> <span class="p">(</span><span class="o">~</span><span class="n">frozen</span> <span class="o">&amp;</span> <span class="n">active</span><span class="p">).</span><span class="nf">float</span><span class="p">()).</span><span class="nf">argmax</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>
            <span class="n">newly_frozen</span><span class="p">.</span><span class="nf">scatter_</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="n">best</span><span class="p">.</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="mi">1</span><span class="p">),</span> <span class="bp">True</span><span class="p">)</span>

        <span class="n">x</span><span class="p">[</span><span class="n">newly_frozen</span><span class="p">]</span> <span class="o">=</span> <span class="n">tokens</span><span class="p">[</span><span class="n">newly_frozen</span><span class="p">]</span>
        <span class="n">frozen</span> <span class="o">|=</span> <span class="n">newly_frozen</span>
        <span class="n">active</span> <span class="o">&amp;=</span> <span class="o">~</span><span class="p">((</span><span class="n">x</span> <span class="o">==</span> <span class="n">EOS_ID</span><span class="p">)</span> <span class="o">&amp;</span> <span class="n">frozen</span><span class="p">)</span>  <span class="c1"># 动态收缩序列
</span>
    <span class="k">return</span> <span class="n">x</span>
</code></pre></div></div> <p>整个推理流程用一句话概括：<strong>CTC 给方向，dLLM 做精修，置信度高的位置提前下车，声学 KV 只算一次</strong>。</p> <hr/> <h2 id="5-实验">5. 实验</h2> <h3 id="51-实验设置">5.1 实验设置</h3> <p><strong>数据集</strong>：LibriSpeech（LS）clean/other、CommonVoice（CV）、VoxPopuli，覆盖朗读语音、口语、多语言场景。</p> <p><strong>Baseline 对比</strong>：</p> <ul> <li>Whisper-LLaMA3-8B（自回归 LLM-ASR，主要对比对象）</li> <li>Whisper-Qwen3-8B（另一个自回归系统）</li> <li>Whisper-LLaDA（直接组合，不加任何优化）</li> </ul> <p><strong>评估指标</strong>：WER 和 RTF（前文已介绍），两个都是越低越好。</p> <h3 id="52-主要结果">5.2 主要结果</h3> <table> <thead> <tr> <th>模型</th> <th>参数量</th> <th>LS Clean WER</th> <th>LS Other WER</th> <th>CV WER</th> <th>VoxPopuli WER</th> <th><strong>平均 WER</strong></th> <th><strong>平均 RTF</strong></th> </tr> </thead> <tbody> <tr> <td>Whisper-LLaMA3</td> <td>8.03B</td> <td>2.15</td> <td>5.58</td> <td>8.55</td> <td>9.89</td> <td>6.54</td> <td>0.280</td> </tr> <tr> <td>Whisper-Qwen3</td> <td>8.19B</td> <td>2.72</td> <td>6.62</td> <td>9.18</td> <td>10.06</td> <td>7.15</td> <td>0.389</td> </tr> <tr> <td>Whisper-LLaDA</td> <td>8.02B</td> <td>2.34</td> <td>5.22</td> <td>8.80</td> <td>9.68</td> <td>6.51</td> <td>1.736</td> </tr> <tr> <td><strong>dLLM-ASR</strong></td> <td><strong>8.02B</strong></td> <td><strong>2.28</strong></td> <td><strong>5.17</strong></td> <td><strong>8.36</strong></td> <td><strong>9.56</strong></td> <td><strong>6.34</strong></td> <td><strong>0.063</strong></td> </tr> </tbody> </table> <p>几个关键数字：</p> <ul> <li>vs Whisper-LLaMA3：WER 从 6.54 降到 6.34（小幅提升），RTF 从 0.280 降到 0.063，<strong>快 4.44×</strong></li> <li>vs Whisper-Qwen3：<strong>快 6.17×</strong>，WER 同样更好</li> <li>vs Whisper-LLaDA（不加优化的 dLLM）：<strong>快 27.6×</strong>，同时 WER 也更低</li> </ul> <p><strong>值得注意</strong>：dLLM-ASR 在四个数据集上 WER 均略优于或持平于自回归系统，并不是靠牺牲精度换速度。RTF=0.063 意味着处理 1 秒音频只需 0.063 秒，远快于实时，有实际部署价值。</p> <h3 id="53-消融实验">5.3 消融实验</h3> <table> <thead> <tr> <th>配置</th> <th>LS Clean WER</th> <th>LS Clean RTF</th> <th>LS Other WER</th> <th>LS Other RTF</th> </tr> </thead> <tbody> <tr> <td><strong>dLLM-ASR（完整）</strong></td> <td><strong>2.28</strong></td> <td><strong>0.057</strong></td> <td><strong>5.17</strong></td> <td><strong>0.076</strong></td> </tr> <tr> <td>去掉 ASR Prior</td> <td>2.29</td> <td>0.069</td> <td>5.20</td> <td>0.084</td> </tr> <tr> <td>去掉 Length Pruning</td> <td>2.27</td> <td>0.071</td> <td>5.13</td> <td>0.089</td> </tr> <tr> <td>去掉 Chat-Style Prompt</td> <td>2.87</td> <td>0.056</td> <td>5.76</td> <td>0.076</td> </tr> <tr> <td>Whisper-LLaDA baseline</td> <td>2.34</td> <td>1.678</td> <td>5.22</td> <td>1.892</td> </tr> <tr> <td>只加 Confidence-Based Denoising</td> <td>2.98</td> <td>0.077</td> <td>5.98</td> <td>0.108</td> </tr> </tbody> </table> <p>几个有意思的发现：</p> <p><strong>ASR Prior 和 Length Pruning 主要贡献速度，对 WER 影响微小</strong>。去掉 ASR Prior，RTF 从 0.057 涨到 0.069（慢了约 20%）；去掉 Length Pruning，RTF 从 0.057 涨到 0.071（慢了约 25%）。两者对 WER 几乎没影响。</p> <p><strong>Chat-Style Prompt 对精度最关键</strong>。去掉对话格式的 prompt，WER 跳了 0.59（clean）和 0.59（other），这说明 dLLM 的指令跟随能力对 ASR 任务非常重要，没有 chat prompt 模型就退化回普通续写，语义理解变差。</p> <p><strong>各组件必须协同，不能单独用</strong>。只把 Confidence-Based Denoising 加到 Whisper-LLaDA 上，RTF 确实从 1.678 降到 0.077，但 WER 从 5.22 飙升到 5.98——孤立地用置信度截断会过早冻结错误的 token，精度损失无法接受。完整方案里 ASR Prior 提供了正确的初始化，才让置信度截断有意义。</p> <hr/> <h2 id="6-讨论">6. 讨论</h2> <p>思路很清晰，三个矛盾对应四个解法，逻辑链完整。几个地方需要进一步讨论：</p> <p><strong>1. CTC Prior 在噪声环境下的鲁棒性</strong></p> <p>CTC 本身在嘈杂环境（噪声、口音）下 WER 就会比较高，用它初始化去噪起点，噪声场景里会不会把 CTC 的错误带入 dLLM 的去噪过程？LibriSpeech 相对干净，CommonVoice 和 VoxPopuli 略复杂，但论文没有专门分析这个问题。</p> <p><strong>2. τ 的跨领域泛化</strong></p> <p>τ=0.9 在 LibriSpeech 上是最优的，但换到医疗、法律等专业词汇密集的领域，模型的置信度分布可能发生变化，这个值需不需要重新调？没有看到相关实验。</p> <p><strong>3. 流式 ASR 的兼容性问题</strong></p> <p>这篇文章结尾提到未来工作是”扩展到流式 ASR”。这个问题确实不简单：masked diffusion 的并行去噪天然是”等全句到齐了再处理”，流式场景需要来一帧处理一帧。怎么把这个 prior-guided adaptive denoising 改造成增量式，不是一句话能解决的问题，值得单独开一篇工作来做。</p> <p>欢迎评论区交流，有不同看法尽管指出来。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 LLM 推理加速思路让你感兴趣，可以看看我之前出版的<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，书里有专章介绍 LLM 推理效率优化和参数高效微调，和本文讨论的加速方向有些关联。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="ASR"/><category term="Diffusion"/><category term="论文解读"/><summary type="html"><![CDATA[2026 | dLLM-ASR 让语音识别推理快 4.44 倍，还不掉精度]]></summary></entry></feed>