<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://marsggbo.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://marsggbo.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-09-03T09:07:08+00:00</updated><id>https://marsggbo.github.io/feed.xml</id><title type="html">HE Xin</title><subtitle>&quot;SSS: Study, Sleep, Slim&quot; </subtitle><entry><title type="html">Kimi K3技术详解系列（二）：AttnRes 与 Stable LatentMoE</title><link href="https://marsggbo.github.io/blog/2026/20260902-kimi-k3-attnres-moe/" rel="alternate" type="text/html" title="Kimi K3技术详解系列（二）：AttnRes 与 Stable LatentMoE"/><published>2026-09-02T00:00:00+00:00</published><updated>2026-09-02T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260902-kimi-k3-attnres-moe</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260902-kimi-k3-attnres-moe/"><![CDATA[<h1 id="kimi-k3技术详解系列二attnres-与-stable-latentmoe">Kimi K3技术详解系列（二）：AttnRes 与 Stable LatentMoE</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2607.24653">Kimi K3: Open Frontier Intelligence</a></p> </blockquote> <p>这是 Kimi K3 系列的第二篇。上一篇讲了序列维度上的 KDA：历史 token 怎么压缩、怎么遗忘、怎么在 GPU 上并行。本篇转到另外两个维度：层与层之间的信息怎么传，专家与专家之间的容量怎么扩展。</p> <h2 id="系列目录">系列目录</h2> <ol> <li><a href="https://marsggbo.github.io/blog/2026/20260902-kimi-k3-kda/">Kimi K3技术详解系列（一）：KDA 与 Hybrid Attention</a></li> <li><a href="https://marsggbo.github.io/blog/2026/20260902-kimi-k3-attnres-moe/">Kimi K3技术详解系列（二）：AttnRes 与 Stable LatentMoE</a></li> <li><a href="https://marsggbo.github.io/blog/2026/20260902-kimi-k3-infra/">Kimi K3技术详解系列（三）：训练与 Serving Infra</a></li> </ol> <p>第一篇处理的是时间轴：一个 token 怎样访问很长的历史。本篇换两个坐标轴。深度轴问的是“第 80 层怎样直接利用更早层的表示”；宽度轴问的是“每个 token 要从 896 个专家里选 16 个时，怎样组织这些分支才具备可训练、可通信的成本”。MoE 是把 FFN 拆成多位专家、由 router 为每个 token 选择少数分支的结构；其中所有 token 都经过的分支叫 shared experts，按 router 选择的分支叫 routed experts。</p> <p><img src="/assets/img/posts/20260825-kimi-k3-02-attnres-moe/depth_width_flow.png" alt="K3 的深度与宽度信息流"/></p> <p>图上半部分是宽度方向：full-width 的 shared experts 留住通用能力，routed experts 在较窄的 latent space（中间维度更小的计算空间）做特化。下半部分是深度方向：当前层从 embedding、已完成 block 和当前 block 的已有结果中选来源。后面的公式和模块都围绕这两条信息流展开。</p> <h2 id="1-深度方向的默认残差有什么限制">1. 深度方向的默认残差有什么限制</h2> <p>标准 Transformer 每层通常做这样的事情：</p> \[h_l=h_{l-1}+F_l(h_{l-1})\] <p>$h_{l-1}$ 是上一层的 hidden state，$F_l$ 是第 $l$ 层的 attention 或 FFN 变换。这个结构并没有“丢掉”更早层的信息，残差会把它们一路带下来；限制在于，它们已经被压成一份统一的 $h_{l-1}$。第 $l$ 层不能显式决定“这次直接多取一点第 10 层的特征、少取一点第 60 层的特征”，只能接受前面层一路加工后的混合结果。</p> <p>假设一个 90 层模型的第 80 层需要某个早期层提取的局部语法特征。这个特征只能混在第 10 层到第 79 层的连续变换里传过来，中间每一层都可能改变它。模型当然可以学着把信息藏在残差里，但这条通路不是显式的“回到第 10 层查一下”。</p> <p>这和第一篇里的 recurrent state 很像：信息没有消失，却被压进一个不断更新的状态。Transformer 在时间方向用 attention 解决了“从历史 token 中选择”的问题；K3 的 AttnRes（Attention Residuals）把同一件事搬到深度方向，把“只接上一层”改成“从前面多个层级中检索”。</p> <h2 id="2-attnres把残差连接改成深度上的-attention">2. AttnRes：把残差连接改成深度上的 attention</h2> <p>以下先只看一个 token 位置。假设当前是第 $l$ 层，该位置在前面已经产生了 $l$ 个表示：</p> \[H_{&lt;l}=[h_0,h_1,\ldots,h_{l-1}]\in\mathbb R^{l\times d}\] <p>其中 $h_0$ 是 token embedding，$d$ 是 hidden dimension。AttnRes 为当前层准备一个可学习的 pseudo query $\bm w_l\in\mathbb R^d$，把前面模块的输出当作 key/value。这个 query 对第 $l$ 层是固定可学习的，但每个 token 位置的 $H_{&lt;l}$ 不同，所以最后的深度权重仍会随 token 内容变化：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># H_prev: [l, d]，前面 l 个层级的表示
# w_l: [d]，第 l 层自己的可学习 query
</span><span class="n">keys</span> <span class="o">=</span> <span class="nf">rmsnorm</span><span class="p">(</span><span class="n">H_prev</span><span class="p">)</span>                 <span class="c1"># [l, d]
</span><span class="n">weights</span> <span class="o">=</span> <span class="nf">softmax</span><span class="p">(</span><span class="n">w_l</span> <span class="o">@</span> <span class="n">keys</span><span class="p">.</span><span class="n">T</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span> <span class="c1"># [l]
</span><span class="n">residual</span> <span class="o">=</span> <span class="n">weights</span> <span class="o">@</span> <span class="n">H_prev</span>             <span class="c1"># [d]
</span></code></pre></div></div> <p>公式写出来是：</p> \[\bm a_l=\operatorname{softmax}\left(\bm w_l\,\operatorname{RMSNorm}(H_{&lt;l})^\top\right), \qquad r_l=\bm a_lH_{&lt;l}\] <p>这里的 attention 没有发生在 token 之间，而是发生在<strong>层的深度轴</strong>上。它所回答的不是“这个词该看前文哪个词”，而是“当前 token 送入第 $l$ 个模块时，哪一个处理深度的表示更适合作为残差信息”。</p> <p>为什么必须先做 RMSNorm？不同层的输出幅值可能差别很大。如果直接点积，某一层仅仅因为数值范数大，就可能在 softmax 中占据绝大多数权重；RMSNorm 把比较重点放回方向和内容，而不是谁的向量更长。</p> <h3 id="21-用-4-层的例子走一遍">2.1 用 4 层的例子走一遍</h3> <p>假设前面有 embedding、layer 1、layer 2、layer 3 四个表示，当前 layer 4 的 pseudo query 计算出：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>attention over depth = [0.05, 0.10, 0.70, 0.15]
</code></pre></div></div> <p>那么 layer 4 使用的 residual 不是 <code class="language-plaintext highlighter-rouge">h_3</code>，而是：</p> \[r_4=0.05h_0+0.10h_1+0.70h_2+0.15h_3\] <p>这时第 4 层不必把所有深度信息都塞进上一层状态里再硬传下来；它能直接复用第 2 层的表征。换一个 token，前面各层输出变了，即便 pseudo query 不变，权重也可能移向 $h_3$。这比沿深度无差别累积多了一层选择能力。</p> <h2 id="3-为什么-k3-使用-block-attnres">3. 为什么 K3 使用 Block AttnRes</h2> <p>Full AttnRes 的算术量其实不算可怕，模型深度不到 100；真正贵的是，为了让后面层随时检索，每一层输出都不能立刻释放。若有 $L$ 层、hidden dimension 为 $d$，持久保存和跨 pipeline stage 传输的规模是 $O(Ld)$。这正好把“给深度更多选择”变成了一笔显存和通信账。</p> <p>K3 的解法不是随手每 12 层取一个点，而是把层分成 block。已完成的 block 用块内输出的和压成一个 block representation；在当前 block 内，还保留“到上一层为止”的 partial sum。于是当前层既能看 embedding、以前各 block 的摘要，也不会偷看到同 block 后面尚未计算的层。以 93 层为例，8 个约 12 层的 block 加 embedding，共约 9 个长期保存的来源，而不是 93 个。</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>embedding  ───────────────┐
layers 1-12  -&gt; block 1 ──┤
layers 13-24 -&gt; block 2 ──┤──&gt; 当前层的 depth attention
...                       │
当前 block 已过的层 -&gt; partial sum ─┘
</code></pre></div></div> <p>这个近似的直觉是：同一个 block 内相邻层的表示往往比较接近，没必要每一层都单独留一个长期入口；跨 block 的语义变化更大，值得保留独立入口。论文的消融显示，约 8 个 block 可以获得 Full 版本的大部分收益。</p> <p>Block AttnRes 把“保留多个深度层级的可检索性”和“持久状态只保留约 9 份”放在同一个折中里。</p> <p><img src="/assets/img/posts/20260825-kimi-k3-02-attnres-moe/figure2_p3.png" alt="Kimi K3 的三维架构"/></p> <p>上图把 K3 的三个方向放在一起：token mixing 对应 KDA/MLA，layer mixing 对应 AttnRes，channel mixing 对应 MoE。接下来进入宽度方向。</p> <h2 id="4-896-个专家为什么要先进入-latent-space">4. 896 个专家为什么要先进入 latent space</h2> <p>MoE 的目标是扩大“可选的计算分支数”：总参数可以很大，但单个 token 只激活少数专家。</p> <p>K3 使用 896 个 routed expert，每个 token 选择 16 个，另有 2 个所有 token 都经过的 shared expert。问题在于，选择 16 个专家不代表只多算 16 次小函数：若每个 routed expert 都在 7168 维 hidden space 里做完整 FFN，权重容量、token dispatch 的通信量和专家 GEMM 都会按全宽放大。</p> <p>K3 让 routed 分支先把输入从 7168 维压到 3584 维 latent space，在较窄的空间里完成专家计算，再投影回 7168 维。简化后的路径是：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>x [7168]
  ├─&gt; shared expert 1/2（保持全宽）
  └─&gt; W_down: 7168 -&gt; 3584
        └─&gt; 选中的 16 个 routed experts
              └─&gt; W_up: 3584 -&gt; 7168
                     └─&gt; 与 shared 分支相加
</code></pre></div></div> <p>如果所有路径都必须先压到 3584 维，所有通用计算都会经过一个低维瓶颈；shared experts 保留 full-width 的公共通路，routed experts 则只在 latent space 提供稀疏特化。于是 896 个专家的成本落在窄空间，通用信息仍有不经压缩的路径。</p> <h2 id="5-激活为什么会爆先看-routed-分支的乘法链">5. 激活为什么会爆：先看 routed 分支的乘法链</h2> <p>一条 routed 分支要依次经过 down projection、专家内部的 GLU、16 个专家的加权聚合和 up projection，再与 shared 分支相加。这不是普通 FFN 里一次矩阵乘之后的小波动：前一段的异常幅值会成为后一段矩阵乘的输入，16 个分支的尺度差异又会在聚合时叠加。2.8T 规模和低精度训练下，这条乘法链比“某一层输出略大”危险得多。</p> <p>K3 的第一道保险是在 routed experts 聚合之后、up projection 之前加 RMSNorm：</p> \[u=\sum_{i\in\mathrm{TopK}}p_iE_i(W_{down}x), \qquad y=W_{up}\operatorname{RMSNorm}(u)+y_{shared}\] <p>它不改变 token 选择哪个专家，也不把专家输出变成同一个方向；它只在进入最容易放大幅值的 $W_{up}$ 前，把 16 个专家聚合后的 scale 拉回可控范围。这里的 RMSNorm 是为 latent 路径带来的尺度波动配的一道闸门。</p> <p>第二道保险是把 SwiGLU 换成 SiTU-GLU。SwiGLU 的两个分支在大输入上没有硬上限，低精度训练时更容易产生极端值。SiTU-GLU 给两个分支加 soft cap：</p> \[f(x)=\left[\beta_1\tanh(W_gx/\beta_1)\odot\sigma(W_gx)\right] \odot\left[\beta_2\tanh(W_ux/\beta_2)\right]\] <p>K3 取 $\beta_1=4$、$\beta_2=25$。在输入接近 0 时，tanh 近似线性，函数仍像 SwiGLU 一样工作；输入很大时，每个分支会平滑封顶，输出幅值上界约为 $\beta_1\beta_2=100$。</p> <p><img src="/assets/img/posts/20260825-kimi-k3-02-attnres-moe/figure4_p7.png" alt="SiTU-GLU 的幅值控制"/></p> <p>看图时重点看两段：原点附近红色 SiTU-GLU 跟 SwiGLU 接近，说明普通输入区域的表达能力没有被明显改写；大输入区域红线逐渐压平，说明低精度路径不会任由异常激活继续放大。</p> <h2 id="6-896-个专家为什么会负载不均">6. 896 个专家为什么会负载不均</h2> <p>即使激活稳定，router 还有另一个独立问题：它可能反复把 token 送到少数热门专家。假设一个 batch 有 8 个 token、4 个专家，每个 token 只选 1 个，朴素 Top-k 可能得到负载：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>expert 1: 4 tokens
expert 2: 3 tokens
expert 3: 1 token
expert 4: 0 token
</code></pre></div></div> <p>在专家并行（EP）中，专家 1 所在的 GPU 要处理 4 个 token，专家 4 所在的 GPU 没活干；整个 batch 的时间由最忙的 GPU 决定。专家越来越多时，少量偏差更容易把某些专家推成“过热”或“濒死”。</p> <p>过去常见的 auxiliary loss 会在训练目标里惩罚不均衡；auxiliary-loss-free 路由则为每个专家维护一个 bias，热门专家减一点、冷门专家加一点。后者避免了额外损失，却要在“每步太小，调不过来”和“每步太大，来回震荡”之间调步长。K3 注意到，<strong>一次 router 前向其实已经给出了更直接的信息：每个 token 要让某个专家进入 Top-k，究竟还差多少分。</strong></p> <h2 id="7-quantile-balancing从门槛直接反推-bias">7. Quantile Balancing：从门槛直接反推 bias</h2> <p>令 $s_{i,j}$ 是 token $i$ 对专家 $j$ 的原始 router 分数。K3 用 bias 只调 Top-k 的<strong>选择</strong>，而不把 bias 混入最终专家权重；这样 bias 是调度旋钮，不会直接篡改已选专家之间的混合比例。对 token $i$，在当前带 bias 的分数中取第 $k+1$ 大值 $\alpha_i$。它就是“进入这个 token 的 Top-k 必须超过的 cutoff”。下一步若给专家 $j$ 候选 bias $b_j$，它能进入 Top-k，当且仅当：</p> \[s_{i,j}+b_j&gt;\alpha_i\] <p>移项以后：</p> \[b_j&gt;\alpha_i-s_{i,j}\] <p>换句话说，$s_{i,j}-\alpha_i$ 表示专家 $j$ 相对 token $i$ cutoff 的余量：正数已经够格，负数还差一点。若一个 batch 有 $m$ 个 token、$n$ 个专家、每个 token 选 $k$ 个，理想平均负载是 $q=mk/n$。我们不需要猜一个小步长，只需把 $b_j$ 设到恰好让 $q$ 个 token 跨过 cutoff 的位置，也就是余量分布的相应分位点。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># s: [m, n]，m 个 token 对 n 个专家的原始 router 分数
# alpha: [m]，当前带 bias 的 Top-(k+1) cutoff
</span><span class="n">headroom</span> <span class="o">=</span> <span class="n">s</span> <span class="o">-</span> <span class="n">alpha</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">]</span>
<span class="n">b_hat</span> <span class="o">=</span> <span class="o">-</span><span class="n">torch</span><span class="p">.</span><span class="nf">quantile</span><span class="p">(</span><span class="n">headroom</span><span class="p">,</span> <span class="n">q</span><span class="o">=</span><span class="mi">1</span> <span class="o">-</span> <span class="n">k</span> <span class="o">/</span> <span class="n">n</span><span class="p">,</span> <span class="n">dim</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>
<span class="n">b</span> <span class="o">=</span> <span class="n">b_hat</span> <span class="o">-</span> <span class="n">b_hat</span><span class="p">.</span><span class="nf">mean</span><span class="p">()</span>
</code></pre></div></div> <p>为什么最后要减均值？给所有专家 bias 同时加上同一个常数，不会改变 Top-k 排序；减掉均值只是去掉这个无意义的公共偏移。</p> <p>以 8 token、4 expert、$k=1$ 为例，目标负载是 $q=8\times1/4=2$。Quantile Balancing 不是让 bias 每轮只移动一点，而是根据当前余量分布直接估计一个能让每个专家接近 2 个 token 的 bias；为避免同一 batch 用自己算出的 bias 重新路由，它在下一步才生效。</p> <p><img src="/assets/img/posts/20260825-kimi-k3-02-attnres-moe/figure5_p8.png" alt="Quantile Balancing 示意"/></p> <p>图中左侧是热门专家和“死亡专家”的负载差异，中间把每个专家离 cutoff 的余量排成一列，右侧用分位点推得的 bias 重新对齐。实际训练中，K3 不会把百万级余量全部 gather 到一张卡上，而是每张卡先统计每个专家的 1000-bin 直方图，再 all-reduce 整数计数，最后从合并直方图近似读取分位点。通信量从所有浮点余量降到每个专家固定数量的 bin，误差约为 bin 宽度级别。</p> <h2 id="8-为什么-k3-的宽度设计要和稳定性绑在一起">8. 为什么 K3 的宽度设计要和稳定性绑在一起</h2> <p>把 routed experts 放进 0.5× latent space，并不是单纯为了少算一半矩阵。它同时降低了：</p> <ul> <li>896 个专家各自保存 FFN 权重的成本；</li> <li>token dispatch 到专家后的 GEMM 规模；</li> <li>多专家聚合后传回全宽 hidden space 的压力。</li> </ul> <p>但压缩宽度以后，shared expert、RMSNorm 和 SiTU-GLU 就变成了必要的稳定性配套；专家数量变多以后，Quantile Balancing 又承担了把计算量分回各个 rank 的职责。<strong>latent space、激活 soft cap、路由 bias 不是三个互不相关的小技巧，而是同一个“超大稀疏 FFN 如何可训练、可并行”的问题的不同答案。</strong></p> <p>论文的 scaling law 图报告 K3 相比 K2 约 2.5× 的 scaling efficiency 提升。这里的 scaling efficiency 是在相同训练损失/能力目标下所需计算量的拟合比较，不等同于单个 kernel 的 2.5× 加速；它还包含架构、训练配方和系统效率的共同影响。</p> <p><img src="/assets/img/posts/20260825-kimi-k3-02-attnres-moe/figure7_p11.png" alt="K2 与 K3 的 scaling law"/></p> <h2 id="9-小结">9. 小结</h2> <p>本篇可以这样记：</p> <ol> <li>标准残差只把上一层送给下一层，AttnRes 把“从前面哪些层取信息”变成了可学习的 depth attention；</li> <li>Full AttnRes 会保存所有层，Block AttnRes 用约 8 个 block 表示保留大部分效果；</li> <li>896 个 routed experts 如果都在全宽空间运行，参数和通信太贵，因此先进入 0.5× latent space；</li> <li>shared expert 保住全宽通路，RMSNorm 和 SiTU-GLU 控制激活幅值；</li> <li>Quantile Balancing 从第 $k+1$ 个分数提供的门槛反推 bias，让负载均衡从“小步试错”变成“按 margin 分布直接估计”。</li> </ol> <p>下一篇进入系统部分：MoonEP 如何让专家并行的 token 数固定，KDA 的状态如何跨卡传递，1M context 的 Agentic RL 和 prefix cache 又为什么需要重新设计。</p> <hr/> <blockquote> <p>扯一句题外话：Stable LatentMoE 和量化、剪枝属于相邻但不同的问题。《动手学 AutoML：从 NAS 到大语言模型优化实战》第 8、11 章整理了 LLM 压缩和后训练剪枝，适合从模型压缩的角度补充“如何让大模型在更低成本下运行”。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="MoE"/><category term="Attention Residuals"/><category term="模型稳定性"/><category term="论文解读"/><summary type="html"><![CDATA[Kimi K3技术详解系列（二）：AttnRes 与 Stable LatentMoE]]></summary></entry><entry><title type="html">Kimi K3技术详解系列（三）：训练与 Serving Infra</title><link href="https://marsggbo.github.io/blog/2026/20260902-kimi-k3-infra/" rel="alternate" type="text/html" title="Kimi K3技术详解系列（三）：训练与 Serving Infra"/><published>2026-09-02T00:00:00+00:00</published><updated>2026-09-02T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260902-kimi-k3-infra</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260902-kimi-k3-infra/"><![CDATA[<h1 id="kimi-k3技术详解系列三训练与-serving-infra">Kimi K3技术详解系列（三）：训练与 Serving Infra</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2607.24653">Kimi K3: Open Frontier Intelligence</a></p> </blockquote> <p>这是 Kimi K3 系列的第三篇。前两篇分别讲了 KDA/Hybrid Attention，以及 AttnRes/Stable LatentMoE。本篇不把它们当作几个可以独立替换的层，而是顺着一个问题看：结构变了以后，训练和 serving 原先默认的“数据怎么分、状态怎么存、请求怎么恢复”为什么都要改。</p> <h2 id="系列目录">系列目录</h2> <ol> <li><a href="https://marsggbo.github.io/blog/2026/20260902-kimi-k3-kda/">Kimi K3技术详解系列（一）：KDA 与 Hybrid Attention</a></li> <li><a href="https://marsggbo.github.io/blog/2026/20260902-kimi-k3-attnres-moe/">Kimi K3技术详解系列（二）：AttnRes 与 Stable LatentMoE</a></li> <li><a href="https://marsggbo.github.io/blog/2026/20260902-kimi-k3-infra/">Kimi K3技术详解系列（三）：训练与 Serving Infra</a></li> </ol> <h2 id="1-先分清每种并行究竟在切什么">1. 先分清：每种并行究竟在切什么</h2> <p>训练 2.8T 参数模型时，单卡肯定放不下，因此必须把不同维度的工作切到多张 GPU。几个缩写容易一起出现，但它们各自切的是不同对象：</p> <ul> <li><strong>TP（Tensor Parallelism）</strong>：把一个矩阵切到多张卡，每张卡算一部分列或行；</li> <li><strong>EP（Expert Parallelism）</strong>：不同 GPU 保存不同的 MoE experts，token 通过通信送到对应专家；</li> <li><strong>PP（Pipeline Parallelism）</strong>：把连续的模型层切成多个 stage，数据像流水线一样经过这些 stage；</li> <li><strong>DP（Data Parallelism）</strong>：每张卡/每组卡保存模型副本，处理不同 batch，再同步梯度；</li> <li><strong>CP（Context Parallelism）</strong>：把一条很长的序列切给不同 GPU，各卡负责不同的上下文片段。</li> </ul> <p>K3 把这些方式组合起来，再加上 ZeRO（把 optimizer state、梯度等分片保存）和 offload（把暂时不用的状态移到 GPU 外）。这不是“并行方式越多越先进”：TP 解决单个矩阵太大，EP 解决专家太多，PP 解决层数太深，CP 解决序列太长。下面的 MoonEP、KCP 和 cache 机制，分别是在 EP、CP 和请求生命周期上处理这些切分留下的后果。</p> <h2 id="2-moonep为什么专家并行最怕动态负载">2. MoonEP：为什么专家并行最怕动态负载</h2> <h3 id="21-一个-batch-就能制造负载倾斜">2.1 一个 batch 就能制造负载倾斜</h3> <p>假设有 4 个 experts，8 个 token，每个 token 只选 1 个 expert。一次 router 可能给出：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>expert 1: 4 tokens
expert 2: 3 tokens
expert 3: 1 token
expert 4: 0 token
</code></pre></div></div> <p>如果 4 个 expert 分别放在 4 张 GPU 上，GPU 1 要处理 4 个 token，GPU 4 没有工作；即使 GPU 2、3 算完了，整个 batch 也要等 GPU 1。专家并行的吞吐由最忙的 rank 决定。</p> <p>通常 token 会先通过 all-to-all 发送到目标专家所在的 GPU，再做 grouped GEMM（把不同专家的 token 合并成一批矩阵乘法）。如果每步 token 数不稳定，通信 buffer 和 GEMM shape 也跟着变化，host-device synchronization 和动态 kernel launch 会进一步拖慢训练。</p> <h3 id="22-moonep-的思路先规划再迁移专家">2.2 MoonEP 的思路：先规划，再迁移专家</h3> <p>MoonEP 是 K3 报告中的专家并行方案。router 的选择不变，但系统可以把热门 expert 的冗余副本临时放到其他 rank，让原本都要找同一位专家的 token 分流处理。目标不是“平均差不多”，而是让每个 EP rank 在一层恰好接收 $S\times K$ 个 token，其中 $S$ 是该 rank 的序列 token 数，$K$ 是每个 token 选择的专家数。</p> <p>这个固定数量带来两个系统收益：</p> <ol> <li>每个 rank 的 GEMM shape 在编译或计划阶段就知道，不需要等 GPU 统计完 token 数再决定下一步；</li> <li>通信 buffer 可以按 $S\times K$ 预分配，而不用按最坏情况扩成 $S\times K\times R$，其中 $R$ 是 EP rank 数。</li> </ol> <p>一次执行可以按四步理解：router 先给出 token-expert 配对；planning kernel 决定哪些热门 expert 要有冗余副本、每个配对该去哪个 rank；all-to-all 把 token 直接写进远端已经按 expert 分组的位置；随后 grouped GEMM 直接把这个 buffer 当输入。最后一步不再重排一次，这就是 zero-copy 所说的“通信 buffer 直接作为计算 buffer”。</p> <p><img src="/assets/img/posts/20260825-kimi-k3-03-infra/moonep_flow.png" alt="MoonEP 将动态路由变成固定 shape 执行"/></p> <p>论文还给出一个冗余槽位的存在性结论：只要每个 rank 预留约 $E/R$ 个冗余 expert 槽位（$E$ 为 expert 数，$R$ 为 rank 数），就能找到满足均衡的专家迁移方案。这个 bound 说明“总能找到方案”的条件，但真实收益仍取决于迁移成本、expert locality 和通信拓扑。</p> <h3 id="23-为什么固定-shape-对大模型很重要">2.3 为什么固定 shape 对大模型很重要</h3> <p>在小模型里，多一次同步可能只是一点微秒；在 93 层、数万步的训练里，微秒会乘上层数、pipeline stage 和训练步数。传统 EP 里，每个专家实际收到多少 token 要等 router 在 GPU 上跑完才知道，host 因而要等 shape 再发起后续计算。MoonEP 把每个 rank 的总 token 数固定后，通信 buffer、GEMM shape 和 kernel 调度都可以提前确定，CUDA Graph 也更容易复用。</p> <p>所以 MoonEP 的价值不只在负载均衡公式，而在于把<strong>动态路由问题变成固定 shape 的执行问题</strong>。专家是否热门仍然会变化，但每个 rank 最终接收多少 token 被系统约束住了。</p> <h2 id="3-moonep-解决时间显存策略解决容量">3. MoonEP 解决时间，显存策略解决容量</h2> <p>MoonEP 让卡不再互相等，但不会让模型本身少占一字节。训练时显存通常被四类东西占满：模型参数、激活、梯度和 optimizer state。K3 的做法不是给所有 tensor 规定同一种存储方式，而是按“反向时能否便宜重算、是否马上还会用、搬运多少字节”分别选择重计算、FP8 量化、offload 到 CPU 或其他 GPU。</p> <h3 id="31-激活用重计算换空间">3.1 激活：用重计算换空间</h3> <p>如果保存每层所有中间激活，反向传播时可以直接读取，但显存随层数和序列长度增长。重计算（recomputation）则只保存检查点，反向时重新跑一小段前向，用额外计算换掉激活存储。</p> <p>K3 对大块激活使用 block-wise FP8 量化再 offload，对逐元素算子更多采用重计算。量化减少传输字节数，重计算减少需要传输的 tensor 数量，两者针对的是不同的成本。</p> <h3 id="32-moe-反向不要保存完整-dispatch-中间量">3.2 MoE 反向：不要保存完整 dispatch 中间量</h3> <p>MoE 前向要把 token 按 expert 重新排列，得到 dispatch buffer；反向如果把所有排列后的中间激活完整保存，显存和通信都很可观。K3 利用梯度表达式，只保存必要的 dispatch 输入，反向时重算排列和 GEMM，并与通信流水线重叠。</p> <h3 id="33-attnres-并不一定让显存线性增加">3.3 AttnRes 并不一定让显存线性增加</h3> <p>Full AttnRes 确实需要保留多层表示，但 K3 使用 Block AttnRes。一个 block 内的输出先汇总，后续层只访问少量 block 表示；把这段计算纳入 checkpointing 后，每层保存的激活量可以接近标准残差架构，而不是保存 93 份独立层输出。</p> <h2 id="4-kda-的状态怎样跨卡难点是顺序不是状态大小">4. KDA 的状态怎样跨卡：难点是顺序，不是状态大小</h2> <p>KDA 的优势是状态 $\mathbf S$ 不随序列长度增长，正因如此跨卡传它本身并不大；难点是状态更新具有顺序依赖。假设把一条长序列切成两段：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GPU 0: token 0 ... token 49999
GPU 1: token 50000 ... token 99999
</code></pre></div></div> <p>GPU 1 不能从空状态开始算，因为 token 50000 的结果依赖前 50000 个 token 的状态。最朴素的做法是 GPU 0 先完整算完、传一次固定大小的 $S$ 给 GPU 1；通信并不贵，但 GPU 1 全程闲着。KCP 要解决的是：<strong>两段能否先并行做自己的工作，最后再精确接回同一条状态链。</strong></p> <h3 id="41-普通线性-attention-为什么可以简单相加">4.1 普通线性 attention 为什么可以简单相加</h3> <p>最简单的线性 attention 更新是：</p> \[\mathbf S_t=\mathbf S_{t-1}+\bm k_t\bm v_t^\top\] <p>每段从零开始算出本地贡献，最后把此前各段贡献相加即可。这是因为后面 token 只是在旧状态上加一项，没有改变旧状态本身。</p> <p>KDA 不满足这个条件。当前 token 不仅写入一项，还会遗忘和擦除旧状态，因此它对入口状态施加一个由 token 决定的转移：</p> \[\mathbf S_t=\mathbf M_t\mathbf S_{t-1}+\mathbf b_t\] <p>其中 $\mathbf M_t=(\mathbf I-\beta_t\bm k_t\bm k_t^\top)\operatorname{Diag}(\bm\alpha_t)$，$\mathbf b_t=\beta_t\bm k_t\bm v_t^\top$。所以后半段即使从零算出了自己的 $\tilde{\mathbf S}$，它仍必须先把前半段传来的状态乘上本段累计 $\mathbf M$；两段本地状态不能简单相加。</p> <h3 id="42-kcp传固定大小的转移信息">4.2 KCP：传固定大小的转移信息</h3> <p>Kimi 的 KDA Context Parallelism（KCP）把每段的作用总结成一句“仿射变换”：给我任意入口状态 $S_{in}$，我都会输出 $\mathbf M_{seg}S_{in}+\tilde{\mathbf S}_{seg}$。其中两项都能只根据本段 token 预先算出：</p> <ol> <li>这一段的累积转移矩阵 $\mathbf M$；</li> <li>假设输入状态为零时，这一段自身产生的状态 $\tilde{\mathbf S}$。</li> </ol> <p>例如 GPU 0 的 fragment 是 $(\mathbf M^{(0)},\tilde{\mathbf S}^{(0)})$，GPU 1 的 fragment 是 $(\mathbf M^{(1)},\tilde{\mathbf S}^{(1)})$。从初始零状态出发，GPU 1 的真实入口就是 GPU 0 的输出：</p> \[\mathbf S_{in}^{(1)}=\mathbf M^{(0)}\mathbf S_{in}^{(0)}+\tilde{\mathbf S}^{(0)}.\] <p>GPU 1 随后把这个入口代入自己的 fragment，得到</p> \[\mathbf S_{out}^{(1)}=\mathbf M^{(1)}\mathbf S_{in}^{(1)}+\tilde{\mathbf S}^{(1)}.\] <p>两段的组合仍是同一种“先乘 $\mathbf M$、再加 $\tilde{\mathbf S}$”的形式，因此可以用 prefix scan 连续合并更多 rank。</p> <p>各 GPU 先并行计算自己的 fragment，再交换这些固定大小对象，按 token 段顺序做一次 prefix scan，就能恢复每段真实的入口状态，继而得到输出。关键是 fragment 的复合仍保持顺序，却不需要等待上一段完成 token 级递推；通信量与状态矩阵大小相关，而不随整条序列线性增长。</p> <p><img src="/assets/img/posts/20260825-kimi-k3-03-infra/kcp_flow.png" alt="KCP 先并行总结，再恢复状态顺序"/></p> <p>这和“把序列直接切两半、各算各的”不同：KCP 传的是每段如何变换状态的信息，最后仍然遵守原始 token 顺序。</p> <h2 id="5-1m-context-的-agentic-rl状态要能暂停和恢复">5. 1M context 的 Agentic RL：状态要能暂停和恢复</h2> <p>到这里讨论的还是一次前向或一次训练 step。Agentic RL 则把任务拉成很长的生命周期：模型调用工具、等待工具结果、继续读写文件和执行下一步，再根据最终结果训练。一个 1M token rollout 可能有几百次这样的往返，因此系统要同时管理两类状态：模型已经读过前缀的 cache，以及工具环境里的文件、进程和磁盘状态。</p> <h3 id="51-cache-池只为离开-gpu-的前缀付费">5.1 Cache 池：只为离开 GPU 的前缀付费</h3> <p>正在 decode 的请求必须把 cache 留在 GPU；正在等待工具结果的请求暂时不用算，却也不该丢失已经 prefill 的长前缀。若所有这类闲置前缀都常驻 GPU，cache 很快爆掉；若每次都从头 prefill，1M token 的等待又会变得很贵。K3 用 write-back：只在前缀被 GPU 驱逐时才写回 CPU DRAM，下次重新调度前再预取。它和 write-through 的区别是，仍在 GPU 活跃的 cache 不额外复制一份到 CPU。</p> <p>KDA 状态与对应 MLA KV block 一起 offload，因为它们必须在相同的 token 边界恢复。模型权重和 optimizer state 还会继续 offload 到 NVMe，把 DRAM 主要留给 cache 池。</p> <h3 id="52-为什么沙箱要用-microvm">5.2 为什么沙箱要用 microVM</h3> <p>cache 恢复了模型的上下文，工具沙箱还要恢复“任务做到哪里”。Agent 可能会创建文件、运行代码、启动容器，甚至触发内核级操作；普通容器隔离不够强时，一次异常就可能影响整台训练机器。K3 使用 Firecracker microVM，把每个 AgentENV 当作可暂停、可复制的轻量虚拟机。</p> <p>它支持增量 checkpoint，只保存脏页；报告中的 checkpoint 约 133ms、resume 约 49ms。Agent 等待工具结果时，沙箱可以暂停，不继续占用 CPU 和内存；需要比较不同动作的 reward 时，还可以 fork 出副本，在副本里试错而不影响原环境。</p> <p>这类机制对 1M context 很关键：长任务的瓶颈不只是 token 数，还包括“一个任务的外部世界状态要活多久”。</p> <h2 id="6-prefix-cachekda-让已经成熟的-cache-又变复杂">6. Prefix Cache：KDA 让已经成熟的 cache 又变复杂</h2> <p>传统 KV cache 是“每个 token 一份 K/V”，按页保存后只要前缀 hash 命中，就能在相应边界继续。KDA 的 cache 不是 token 列表，而是该前缀累积得到的一整块状态；要从某个边界恢复，必须正好有该边界的 state checkpoint。于是一个请求可能出现：MLA 的前缀已经命中，但同一边界没有 KDA checkpoint，仍不能安全继续。</p> <p>K3 的解决思路是把“匹配有多细”和“物理块有多大”解耦：</p> <ul> <li>逻辑匹配在 512-token hash block 上进行，保证 prefix 可以细粒度比较；</li> <li>物理内存用较大的 block 分配，例如一个 6144-token physical block 里包含 12 个 hash block；</li> <li>KDA checkpoint 只在稀疏边界保存，通常优先选择对话轮次边界；</li> <li>命中后恢复最近 checkpoint，中间未覆盖的 token 用 copy-on-write 继续 prefill。</li> </ul> <p>下图先把一次请求的决策走一遍。真正可用的命中必须同时满足 MLA 前缀 hash 与各个 KDA 组的 checkpoint；命中后复制出私有的可写部分，再 prefill 新增加的 token，不能直接修改共享 cache。</p> <p><img src="/assets/img/posts/20260825-kimi-k3-03-infra/serving_cache_flow.png" alt="Prefix cache 的匹配、恢复与 write-back 流程"/></p> <p><img src="/assets/img/posts/20260825-kimi-k3-03-infra/figure12_p23.png" alt="KDA 感知的 prefix cache"/></p> <p>图中蓝色部分是已缓存的 MLA hash block，下面的点表示 KDA checkpoint。比如请求前 2800 个 token 都相同，最长可用边界也许是 2560：此处既有 5 个 512-token hash block，又有 KDA checkpoint。系统复制共享 MLA 的未满物理页，恢复 2560 处的 KDA 状态，然后只 prefill 后面的 240 个 token；它不会为了复用 MLA 而从一个没有 KDA 状态的 2800 边界冒险继续。</p> <p>这里的关键不是“缓存粒度越细越好”。Checkpoint 本身比单个 token 的 KV 大得多，存得太密会抵消 KDA 的空间优势；存得太稀又会增加恢复时需要重算的 token。K3 的设计是在匹配粒度和状态保存成本之间折中。</p> <h2 id="7-speculative-decodingkda-状态不能随便回滚">7. Speculative Decoding：KDA 状态不能随便回滚</h2> <p>Speculative decoding（推测解码）让一个较小的 draft model 一次猜多个 token，再让大模型验证。接受的 token 被写入正式状态，被拒绝的 token 则需要丢弃；它的收益来自一次验证多步候选，而不是让大模型少做验证。</p> <p>在普通 KV cache 里，回滚通常是释放被拒绝 token 对应的 cache block；KDA 每生成一个 token 都会更新同一个状态矩阵，如果 draft 连续走了 7 步，再拒绝第 3 步，状态已经多更新了几次，不能只改一个长度指针。</p> <p>K3 的思路是：状态并不需要存下来，只要保存每个 draft token 驱动状态更新所需的 q/k/v 投影输入。验证 kernel 确认接受到第几步后，在片上从最后一个正式状态重放 accepted token 的更新，最后只写回合法状态。投影输入远小于为每个候选都复制一份完整 $\mathbf S$，这就是“重放”比“逐步快照”划算的原因。</p> <h2 id="8-量化和-vllm训练时就面对部署格式">8. 量化和 vLLM：训练时就面对部署格式</h2> <p>K3 的 post-training 使用 MXFP4 量化感知训练：MoE expert 权重以 MXFP4 保存/计算，激活使用 MXFP8，attention projection 和 router 保持更高精度。MXFP4/MXFP8 是带微缩放因子的低精度浮点格式；量化感知训练的意思是，训练过程中就模拟部署时的低精度，而不是训练完成后突然把 BF16 权重转换成 MXFP4。</p> <p>这样做是为了减少 train-inference mismatch：训练时模型已经见过量化误差，rollout 和训练使用同一套数值格式，部署后不容易出现突然掉点。</p> <p>开源 serving 侧，vLLM 已提供 K3 的 Day-0 支持。一个最简启动方式是：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>vllm serve moonshotai/Kimi-K3 <span class="se">\</span>
  <span class="nt">--tensor-parallel-size</span> 8 <span class="se">\</span>
  <span class="nt">--trust-remote-code</span> <span class="se">\</span>
  <span class="nt">--load-format</span> fastsafetensors <span class="se">\</span>
  <span class="nt">--enable-prefix-caching</span> <span class="se">\</span>
  <span class="nt">--enable-auto-tool-choice</span> <span class="se">\</span>
  <span class="nt">--tool-call-parser</span> kimi_k3 <span class="se">\</span>
  <span class="nt">--reasoning-parser</span> kimi_k3
</code></pre></div></div> <p>这里的 <code class="language-plaintext highlighter-rouge">tensor-parallel-size 8</code> 表示用 8 张 GPU 切分张量；<code class="language-plaintext highlighter-rouge">enable-prefix-caching</code> 打开前面讲的混合 cache；tool-call 和 reasoning parser 则负责把模型输出解析成工具调用和思考/答案结构。</p> <p>vLLM 的 scheduler 需要同时管理两种 cache：MLA 的分页 KV block 和 KDA 的固定状态 block。prefill 使用 FlashKDA，decode 使用融合 CUDA kernel。高吞吐部署还可以把 prefill 和 decode 分到不同机器：prefill 侧用 TEP（把 attention 做 TP、MoE 做 EP 的并行组合）快速处理长 prompt，decode 侧用 DEP（以专家并行为主的解码并行）提高逐 token 吞吐，中间通过 NIXL 这个跨节点数据传输层发送 MLA KV、KDA 状态和 block table。</p> <p><img src="/assets/img/posts/20260825-kimi-k3-03-infra/figure11_p19.png" alt="K3 serving 的计算与通信重叠"/></p> <p>这张图反映的是另一个系统事实：当计算、通信和 offload 能够重叠时，单独优化某一个 kernel 未必决定最终性能；调度顺序和 buffer 生命周期同样重要。</p> <h2 id="9-系列小结新架构把哪些老问题重新打开了">9. 系列小结：新架构把哪些老问题重新打开了</h2> <p>KDA 减少了随 context 增长的状态，但引入了状态传递、checkpoint 和回滚问题；AttnRes 提供了深度方向的检索，但要求系统管理跨层表示；LatentMoE 压低了专家计算成本，却要求更严格的负载均衡和激活稳定性。</p> <p>对应到 infra，就是一串连锁反应：</p> <ol> <li>MoonEP 把动态专家路由约束成固定 shape，减少 EP 的尾部等待；</li> <li>KCP 用固定大小 fragment 做 KDA 的 context parallelism；</li> <li>混合 prefix cache 同时管理分页 KV 和稀疏 checkpoint；</li> <li>speculative decoding 通过重放状态避免保存大量快照；</li> <li>量化感知训练让训练、rollout 和 serving 使用接近的数值格式；</li> <li>vLLM 把这些结构暴露成 scheduler、kernel 和部署参数，而不是只停留在论文里的模块图。</li> </ol> <p>Kimi K3 这份报告的系统价值，主要就在这里：它没有把“新 attention”“超大 MoE”“1M context”当成互不相干的卖点，而是展示了每个结构变化会把哪些系统假设推翻。真正部署时，决定成败的往往不是某个公式单独快多少，而是状态、通信、内存和调度能不能在同一条 pipeline 里对上。</p> <hr/> <blockquote> <p>K3 的 infra 细节和 LLM 压缩不是同一件事。《动手学 AutoML：从 NAS 到大语言模型优化实战》第 8、11 章整理了剪枝、量化和后训练压缩，适合从模型压缩角度补充“如何降低模型本身的成本”。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="MoE"/><category term="分布式训练"/><category term="Serving"/><category term="KV Cache"/><category term="论文解读"/><summary type="html"><![CDATA[Kimi K3技术详解系列（三）：训练与 Serving Infra]]></summary></entry><entry><title type="html">Kimi K3技术详解系列（一）：KDA 与 Hybrid Attention</title><link href="https://marsggbo.github.io/blog/2026/20260902-kimi-k3-kda/" rel="alternate" type="text/html" title="Kimi K3技术详解系列（一）：KDA 与 Hybrid Attention"/><published>2026-09-02T00:00:00+00:00</published><updated>2026-09-02T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260902-kimi-k3-kda</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260902-kimi-k3-kda/"><![CDATA[<h1 id="kimi-k3技术详解系列一kda-与-hybrid-attention">Kimi K3技术详解系列（一）：KDA 与 Hybrid Attention</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2607.24653">Kimi K3: Open Frontier Intelligence</a></p> </blockquote> <p>这是 Kimi K3 系列的第一篇，专门讲序列方向的改动：KDA（Kimi Delta Attention）和它怎样在 GPU 上运行。后两篇分别讨论深度方向的 AttnRes、宽度方向的 Stable LatentMoE，以及训练和 serving 系统。</p> <h2 id="系列目录">系列目录</h2> <ol> <li><a href="https://marsggbo.github.io/blog/2026/20260902-kimi-k3-kda/">Kimi K3技术详解系列（一）：KDA 与 Hybrid Attention</a></li> <li><a href="https://marsggbo.github.io/blog/2026/20260902-kimi-k3-attnres-moe/">Kimi K3技术详解系列（二）：AttnRes 与 Stable LatentMoE</a></li> <li><a href="https://marsggbo.github.io/blog/2026/20260902-kimi-k3-infra/">Kimi K3技术详解系列（三）：训练与 Serving Infra</a></li> </ol> <h2 id="1-1m-context-先逼出一个取舍">1. 1M context 先逼出一个取舍</h2> <p>模型支持 1M context，真正棘手的不是“能否存下一百万个 token”，而是生成第 $t$ 个 token 时怎样访问前 $t-1$ 个 token。标准 attention 会把当前 hidden state 投影成 query，逐一比较所有历史 key，再按比较结果聚合 value。KV cache 省掉了历史 token 的重复投影，但每次 decode 仍要读完整段历史 KV。</p> <p>对超长 context，理想上同时想要三件事：</p> <ol> <li><strong>精确的 softmax attention</strong>：任意 query 都能对每个历史 token 单独打分；</li> <li><strong>固定大小的历史状态</strong>：历史从 1K 增长到 1M，显存不随长度增长；</li> <li><strong>固定的单步读取量</strong>：生成下一个 token 时，不再扫描全部历史。</li> </ol> <p>这三件事不能同时成立。标准 softmax attention 的输出是：</p> \[\bm o_t=\sum_{j\le t} a_{t,j}\bm v_j, \qquad a_{t,j}=\frac{\exp(\bm q_t^\top\bm k_j)} {\sum_{r\le t}\exp(\bm q_t^\top\bm k_r)}.\] <p>新的 query 会以不同方式给历史 key 排序，分母也依赖整段历史。要对任意 query 保持这个结果，系统就得保留每个历史 key/value；一个与长度无关的小状态无法包含所有可能 query 的精确响应。用核函数的语言说，$\exp(\bm q^\top\bm k)$ 没有一个有限维、可精确分解的 feature map，不能写成固定维度的 $\phi(\bm q)^\top\phi(\bm k)$。</p> <p>MLA（Multi-head Latent Attention）选择保留精确的全局读取，只把每个 token 的 KV 压得更小，因此降低的是“每个位置有多大”，不是“位置有多少”。KDA 选择固定状态，接受它不能保留逐 token 的精确检索，换取不随长度增长的状态和读取量。</p> <p><img src="/assets/img/posts/20260825-kimi-k3-01-kda/hybrid_attention_tradeoff.png" alt="Hybrid Attention 的设计取舍"/></p> <p>K3 没有要求一种机制承担全部工作，而是把两种取舍交替使用：每个 block 里 3 层 KDA 负责低成本地传播和更新压缩记忆，随后 1 层 MLA 保留一次完整的全局内容检索；最后一层也使用 MLA。Hybrid Attention 的 3:1 不是由某个公式必然推出来的常数，而是在“多数层不扫全历史”和“模型仍定期能精确看历史”之间作出的架构选择。</p> <h2 id="2-要得到固定状态attention-必须具备什么结构">2. 要得到固定状态，attention 必须具备什么结构</h2> <p>目标是让历史在 query 到来前就先被汇总成固定形状的对象。把历史记作 $H_t={(\bm k_j,\bm v_j)}_{j\le t}$，需要的计算形式是：</p> \[\bm o_t=R\bigl(\bm q_t,\;G(H_t)\bigr),\] <p>其中 $G(H_t)$ 的形状不能随 $t$ 增长。要让 $G$ 能在线更新、又保留 query 相关的内容检索，最直接的条件是相似度核可以拆成 query 的一部分和 key 的一部分：</p> \[\kappa(\bm q,\bm k)=\phi(\bm q)^\top\phi(\bm k), \qquad \phi(\cdot)\in\mathbb R^r,\] <p>其中 $r$ 是固定维度。将这种核代入 attention，历史项可以提前合并为两个状态：</p> \[\mathbf S_t=\sum_{j\le t}\phi(\bm k_j)\bm v_j^\top, \qquad \bm z_t=\sum_{j\le t}\phi(\bm k_j),\] \[\bm o_t= \frac{\phi(\bm q_t)^\top\mathbf S_t} {\phi(\bm q_t)^\top\bm z_t}.\] <p>分子保留“相似 key 的 value 该怎样聚合”，分母完成对应的归一化；两者的形状只由 $r$ 和 value 维度决定。关键变化在于，softmax 的全局竞争被换成了一个有限维的相似度空间：历史 token 写入 $\phi(\bm k)$ 指向的方向，query 用 $\phi(\bm q)$ 读取这些方向。这就是固定状态能成立的数学条件。</p> <p>为了把状态本身看清楚，先取最简单的 $\phi(\bm x)=\bm x$，并暂时省去归一化项。得到：</p> \[\bm o_t=\sum_{j\le t}(\bm q_t^\top\bm k_j)\bm v_j =\bm q_t^\top\underbrace{\sum_{j\le t}\bm k_j\bm v_j^\top}_{\mathbf S_t}, \qquad \mathbf S_t=\mathbf S_{t-1}+\bm k_t\bm v_t^\top.\] <p>这里的 $\mathbf S_t\in\mathbb R^{d_k\times d_v}$ 是上式历史和的充分统计量。每个外积 $\bm k_j\bm v_j^\top$ 记录“key 方向 $\bm k_j$ 对应 value $\bm v_j$”的关联；把它们相加后，用 query 左乘 $\bm q_t^\top\mathbf S_t$，就是把与 query 对齐的关联读出来。</p> <p><img src="/assets/img/posts/20260825-kimi-k3-01-kda/linear_state_flow.png" alt="从逐 token KV 到固定状态"/></p> <p>这不是在近似实现原来的 softmax，而是定义一种不同的记忆算子：$W_q/W_k$ 可以学习哪些内容应写到相近方向，query 也能按方向读取；但状态不再保留每个 token 的独立身份，两个相近 key 写进去会在同一片状态中叠加。KDA 的 $q/k/v$ 投影、短卷积和 L2Norm 在这个可学习的状态空间里构造更合适的写入和读取方向；下一节的 delta rule 再处理“叠加以后如何覆盖旧内容”。</p> <h2 id="3-kda遗忘擦除再写入">3. KDA：遗忘、擦除，再写入</h2> <p>先看一个具体冲突。某个 key 方向原先对应“项目延期”，后来同样的方向又写入“项目已按期交付”。纯加法会让 query 同时读到两段 value；我们真正希望的是新证据能修正旧记忆，而不相关方向尽量保留。</p> <p>最简单的记忆管理是先给旧状态乘一个遗忘因子：</p> \[\mathbf S_t=\alpha_t\mathbf S_{t-1}+\bm k_t\bm v_t^\top\] <p>这类 gated linear attention 会让旧信息逐步衰减，但整体打折仍然比较粗糙：每个方向都一起淡忘，并没有针对“刚发生冲突的 key”纠错。更合理的目标是，当前 key 来查询状态时，状态应该返回当前 value。用与上一节一致的状态方向写，这个目标是：</p> \[\mathbf S_t^\top\bm k_t\approx\bm v_t, \qquad \mathcal L(\mathbf S)=\frac12\|\mathbf S^\top\bm k_t-\bm v_t\|^2.\] <p>这个目标直接说“用这个 key 回读时，读出的内容要接近这次写入的 value”。先把旧状态按通道遗忘，记作 $\mathbf A_t=\operatorname{Diag}(\bm\alpha_t)\mathbf S_{t-1}$；再从 $\mathbf A_t$ 对上面的误差做一步梯度下降，步长为 $\beta_t$：</p> \[\begin{aligned} \mathbf S_t &amp;=\mathbf A_t-\beta_t\bm k_t(\bm k_t^\top\mathbf A_t-\bm v_t^\top)\\ &amp;=(\mathbf I-\beta_t\bm k_t\bm k_t^\top)\operatorname{Diag}(\bm\alpha_t)\mathbf S_{t-1} +\beta_t\bm k_t\bm v_t^\top \end{aligned}\] <p>第二行正是论文的 KDA 核心更新。$\bm\alpha_t$ 负责按通道处理“多久以前的记忆该保留多少”；括号中的投影负责针对当前 key 清掉旧响应；最后一项把新 value 写回同一个方向。$\beta_t$ 决定这次修正力度：接近 0 时几乎不改，接近 1 时更接近覆盖。</p> <p>这里的外积 $\bm k_t(\bm k_t^\top\mathbf A_t-\bm v_t^\top)$ 也有很具体的含义：括号里是“当前 key 读到的旧答案减去应写入的新答案”，左乘 $\bm k_t$ 表示只沿当前 key 指向的状态方向修正。与当前 key 几乎正交的方向不会被这次更新直接抹掉，这就是 KDA 比整体遗忘更细的地方。</p> <p>之后仍用 query 读状态：</p> \[\tilde{\bm o}_t=\mathbf S_t^\top\bm q_t\] <p>所以一次 KDA 更新的顺序是：</p> <ol> <li>每个通道按自己的 $\alpha$ 给旧状态打折；</li> <li>用当前 key 读出旧状态在这个方向上的回答；</li> <li>按误差擦掉这个方向的旧回答；</li> <li>用当前 value 写入新记忆；</li> <li>用 query 从更新后的状态读出结果。</li> </ol> <p><img src="/assets/img/posts/20260825-kimi-k3-01-kda/kda_state_flow.png" alt="KDA 单 token 的状态更新流程"/></p> <p>这就是 KDA 所谓的“固定大小状态”：一张会遗忘、会覆盖的联想表。设计链条也由此闭合：固定状态来自可结合的线性 attention；压缩记忆会发生写入碰撞；delta rule 则把覆盖规则写成最小化当前 key 回读误差。</p> <h2 id="4-先写一个能跑的-kda-head">4. 先写一个能跑的 KDA head</h2> <p>下面是一个教学版单 head 实现，它就是上一节公式的直接翻译。它省略了论文里的 ShortConv（先让相邻 token 局部混合）、L2Norm（把 $q/k$ 长度控制住）和输出门，只保留“投影出 $q/k/v$，再更新和读取 $S$”这条主线。假设 batch size 是 $B$，序列长度是 $T$，输入维度是 $d_{model}$，状态大小是 $[d_k,d_v]$。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">torch</span>
<span class="kn">import</span> <span class="n">torch.nn</span> <span class="k">as</span> <span class="n">nn</span>
<span class="kn">import</span> <span class="n">torch.nn.functional</span> <span class="k">as</span> <span class="n">F</span>

<span class="k">class</span> <span class="nc">KDAHead</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">d_model</span><span class="p">,</span> <span class="n">d_k</span><span class="o">=</span><span class="mi">128</span><span class="p">,</span> <span class="n">d_v</span><span class="o">=</span><span class="mi">128</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">wq</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">d_model</span><span class="p">,</span> <span class="n">d_k</span><span class="p">,</span> <span class="n">bias</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">wk</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">d_model</span><span class="p">,</span> <span class="n">d_k</span><span class="p">,</span> <span class="n">bias</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">wv</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">d_model</span><span class="p">,</span> <span class="n">d_v</span><span class="p">,</span> <span class="n">bias</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">w_beta</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">d_model</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">bias</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">w_alpha</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">d_model</span><span class="p">,</span> <span class="n">d_k</span><span class="p">,</span> <span class="n">bias</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">):</span>                 <span class="c1"># x: [B, T, d_model]
</span>        <span class="n">B</span><span class="p">,</span> <span class="n">T</span><span class="p">,</span> <span class="n">_</span> <span class="o">=</span> <span class="n">x</span><span class="p">.</span><span class="n">shape</span>
        <span class="n">q</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">silu</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">wq</span><span class="p">(</span><span class="n">x</span><span class="p">))</span>             <span class="c1"># [B, T, d_k]
</span>        <span class="n">k</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">silu</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">wk</span><span class="p">(</span><span class="n">x</span><span class="p">))</span>             <span class="c1"># [B, T, d_k]
</span>        <span class="n">v</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">silu</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">wv</span><span class="p">(</span><span class="n">x</span><span class="p">))</span>             <span class="c1"># [B, T, d_v]
</span>        <span class="n">beta</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">sigmoid</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">w_beta</span><span class="p">(</span><span class="n">x</span><span class="p">))</span>
        <span class="n">alpha</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">sigmoid</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">w_alpha</span><span class="p">(</span><span class="n">x</span><span class="p">))</span>

        <span class="n">S</span> <span class="o">=</span> <span class="n">x</span><span class="p">.</span><span class="nf">new_zeros</span><span class="p">(</span><span class="n">B</span><span class="p">,</span> <span class="n">q</span><span class="p">.</span><span class="nf">size</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">),</span> <span class="n">v</span><span class="p">.</span><span class="nf">size</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">))</span>  <span class="c1"># [B, d_k, d_v]
</span>        <span class="n">outputs</span> <span class="o">=</span> <span class="p">[]</span>
        <span class="k">for</span> <span class="n">t</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">T</span><span class="p">):</span>
            <span class="n">q_t</span><span class="p">,</span> <span class="n">k_t</span><span class="p">,</span> <span class="n">v_t</span> <span class="o">=</span> <span class="n">q</span><span class="p">[:,</span> <span class="n">t</span><span class="p">],</span> <span class="n">k</span><span class="p">[:,</span> <span class="n">t</span><span class="p">],</span> <span class="n">v</span><span class="p">[:,</span> <span class="n">t</span><span class="p">]</span>
            <span class="n">Sa</span> <span class="o">=</span> <span class="n">alpha</span><span class="p">[:,</span> <span class="n">t</span><span class="p">,</span> <span class="p">:,</span> <span class="bp">None</span><span class="p">]</span> <span class="o">*</span> <span class="n">S</span>
            <span class="n">old</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">einsum</span><span class="p">(</span><span class="sh">"</span><span class="s">bk,bkv-&gt;bv</span><span class="sh">"</span><span class="p">,</span> <span class="n">k_t</span><span class="p">,</span> <span class="n">Sa</span><span class="p">)</span>
            <span class="n">erase</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">einsum</span><span class="p">(</span><span class="sh">"</span><span class="s">bk,bv-&gt;bkv</span><span class="sh">"</span><span class="p">,</span> <span class="n">beta</span><span class="p">[:,</span> <span class="n">t</span><span class="p">]</span> <span class="o">*</span> <span class="n">k_t</span><span class="p">,</span> <span class="n">old</span><span class="p">)</span>
            <span class="n">write</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">einsum</span><span class="p">(</span><span class="sh">"</span><span class="s">bk,bv-&gt;bkv</span><span class="sh">"</span><span class="p">,</span> <span class="n">beta</span><span class="p">[:,</span> <span class="n">t</span><span class="p">]</span> <span class="o">*</span> <span class="n">k_t</span><span class="p">,</span> <span class="n">v_t</span><span class="p">)</span>
            <span class="n">S</span> <span class="o">=</span> <span class="n">Sa</span> <span class="o">-</span> <span class="n">erase</span> <span class="o">+</span> <span class="n">write</span>
            <span class="n">outputs</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">einsum</span><span class="p">(</span><span class="sh">"</span><span class="s">bk,bkv-&gt;bv</span><span class="sh">"</span><span class="p">,</span> <span class="n">q_t</span><span class="p">,</span> <span class="n">S</span><span class="p">))</span>
        <span class="k">return</span> <span class="n">torch</span><span class="p">.</span><span class="nf">stack</span><span class="p">(</span><span class="n">outputs</span><span class="p">,</span> <span class="n">dim</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>  <span class="c1"># [B, T, d_v]
</span></code></pre></div></div> <p>读代码时可以把每一轮对应回上面的推导：<code class="language-plaintext highlighter-rouge">Sa</code> 是 $\operatorname{Diag}(\alpha)S$，<code class="language-plaintext highlighter-rouge">old</code> 是 $k^\top Sa$，也就是旧记忆对当前 key 的回答；<code class="language-plaintext highlighter-rouge">erase</code> 和 <code class="language-plaintext highlighter-rouge">write</code> 合起来是 $-\beta k(k^\top Sa-v^\top)$；最后一个 <code class="language-plaintext highlighter-rouge">einsum</code> 是 $q^\top S$。代码只是把“先遗忘，再按误差纠正”的式子拆开执行。</p> <p>这段代码的输入来自上一层的 hidden states，返回一个 <code class="language-plaintext highlighter-rouge">[B,T,d_v]</code> 的序列表示，交给后面的输出投影和残差连接。它最大的优点是状态 <code class="language-plaintext highlighter-rouge">S</code> 始终是 <code class="language-plaintext highlighter-rouge">[B,d_k,d_v]</code>，不会因为 <code class="language-plaintext highlighter-rouge">T</code> 从 4K 变成 1M 而扩大。</p> <p>但它还有一个 GPU 不喜欢的问题：<code class="language-plaintext highlighter-rouge">for t in range(T)</code>。每一步都要等上一步更新完 <code class="language-plaintext highlighter-rouge">S</code>，然后执行几个很小的矩阵运算。算法上没问题，硬件利用率却很差。</p> <h2 id="5-chunkwise同一个递推换一种执行顺序">5. Chunkwise：同一个递推，换一种执行顺序</h2> <p>假设序列被切成每块 4 个 token：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>chunk 0: [token 0, token 1, token 2, token 3]
chunk 1: [token 4, token 5, token 6, token 7]
chunk 2: [token 8, token 9, token 10, token 11]
</code></pre></div></div> <p><img src="/assets/img/posts/20260825-kimi-k3-01-kda/chunkwise_flow.png" alt="Chunkwise KDA 的状态传递与块内并行"/></p> <p>每个 chunk 只从前一个 chunk 接收一个入口状态 $\mathbf S_{in}$。拿到入口状态以后，当前 chunk 内任意位置的输出都可以拆成：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>token 6 的输出
├── 入口状态 S_in 经过 token 4、5、6 的遗忘后，被 q_6 读取
└── token 4、5、6 在当前 chunk 写入的内容，分别遗忘到 token 6 后被 q_6 读取
</code></pre></div></div> <p>接下来关心同一个递推怎样适合 GPU。完整 delta rule 的块内变换比较重，先用只含遗忘和写入的递推展示重排条件；它对应 KDA 更新中省去按 key 擦除后的骨架：</p> \[\mathbf S_i=\alpha_i\mathbf S_{i-1}+\bm k_i\bm v_i^\top\] <p>展开 chunk 内第 $i$ 个状态：</p> \[\mathbf S_i= \underbrace{\left(\prod_{r=1}^{i}\alpha_r\right)\mathbf S_{in}}_{\text{chunk 之前的记忆}} + \underbrace{\sum_{j=1}^{i}\left(\prod_{r=j+1}^{i}\alpha_r\right)\bm k_j\bm v_j^\top}_{\text{当前 chunk 的写入}}\] <p>假设 chunk 内 token 2 的衰减因子分别为 $\alpha_1,\alpha_2$，那么：</p> \[\mathbf S_2=\alpha_2\alpha_1\mathbf S_{in}+\alpha_2\bm k_1\bm v_1^\top+\bm k_2\bm v_2^\top\] <p>第一项是旧记忆，第二项是 token 1 留下的内容，第三项是 token 2 刚写入的内容。token 3 不能出现在 token 2 的状态里，这就是 causal 约束。</p> <p>接着定义累计衰减：</p> \[\Gamma_i=\prod_{r=1}^{i}\alpha_r\] <p>从位置 $j$ 写入、到位置 $i$ 读取的衰减就是 $\Gamma_i/\Gamma_j$。于是可以把衰减分别乘到 query 和 key 上：</p> \[\left(\bm q_i\Gamma_i\right)^\top\left(\bm k_j/\Gamma_j\right) =\bm q_i^\top\bm k_j\frac{\Gamma_i}{\Gamma_j}\] <p>这意味着整个 chunk 的 query 可以堆成矩阵 $Q$，key 堆成 $K$，一次 $QK^\top$ 就得到所有 $(i,j)$ 对的贡献；再把上三角屏蔽掉，只保留 $j\le i$ 的 causal 部分。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># C=4；每一行代表一个 token
# Q, K: [C, d_k], V: [C, d_v], S_in: [d_k, d_v]
</span><span class="n">Gamma</span> <span class="o">=</span> <span class="n">alpha</span><span class="p">.</span><span class="nf">cumprod</span><span class="p">(</span><span class="n">dim</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>              <span class="c1"># [C, d_k]
</span>
<span class="n">from_past</span> <span class="o">=</span> <span class="p">(</span><span class="n">Q</span> <span class="o">*</span> <span class="n">Gamma</span><span class="p">)</span> <span class="o">@</span> <span class="n">S_in</span>             <span class="c1"># [C, d_v]
</span><span class="n">within</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">tril</span><span class="p">((</span><span class="n">Q</span> <span class="o">*</span> <span class="n">Gamma</span><span class="p">)</span> <span class="o">@</span> <span class="p">(</span><span class="n">K</span> <span class="o">/</span> <span class="n">Gamma</span><span class="p">).</span><span class="n">T</span><span class="p">)</span> <span class="o">@</span> <span class="n">V</span>
<span class="n">O</span> <span class="o">=</span> <span class="n">from_past</span> <span class="o">+</span> <span class="n">within</span>                     <span class="c1"># [C, d_v]
</span></code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">from_past</code> 是入口状态对当前 chunk 的贡献，<code class="language-plaintext highlighter-rouge">within</code> 是当前 chunk 内历史写入的贡献。两者都变成了矩阵乘法，GPU 可以把一个 chunk 的多个 token 一起交给 Tensor Core。</p> <p>这里并没有把顺序打乱：chunk 1 仍然要等待 chunk 0 产出 $\mathbf S_{out}$。变化的是串行依赖的粒度：原来 $T$ 个 token 需要 $T$ 次状态传递，现在大约只需要 $T/C$ 次 chunk 传递，chunk 内部则批量并行。</p> <p>真实 KDA 还包含 delta rule 的擦除项，不能简单删掉。工程实现用 UT transform 把擦除、遗忘和写入一起整理成类似的 causal block 运算。上面的简化推导只负责证明一件事：状态更新虽然有顺序，但一个 chunk 内的输出能够拆成“入口状态的贡献”和“块内历史的贡献”；它不代表实现真的丢掉了擦除机制。</p> <h2 id="6-lower-bounded-decay为什么要给遗忘设下限">6. Lower-bounded decay：为什么要给遗忘设下限</h2> <p>Chunkwise 里出现了 <code class="language-plaintext highlighter-rouge">K / Gamma</code>。如果每一步的遗忘因子都小于 1，连续相乘以后 $\Gamma$ 会越来越小；它的倒数可能非常大。低精度训练使用 BF16 时，这种除法容易溢出，数值不稳定会直接变成训练或 kernel 的问题。</p> <p>Kimi Linear 使用的负 Softplus 映射允许 log-decay 趋近 $-\infty$。K3 把 log-decay 限制在一个有限区间：</p> \[\bm g_t^h=g_{min}\cdot\operatorname{Sigmoid}(e^{A_h}\bm z_t^h), \qquad g_{min}=-5\] <p>于是每个通道的遗忘因子不会低于 $e^{-5}\approx6.7\times10^{-3}$。对一个 16-token tile，累计 log-decay 被控制在大约 $(-80,0)$，倒数的范围也被限制住。</p> <p>这个改动的价值不只是“训练不溢出”。数值范围受控以后，原本需要逐位置处理的对角 tile 也可以使用 dense Tensor Core 矩阵乘法，kernel 不必为对角区域保留一条慢路径。算法上只是给遗忘设了一个下限，系统上却换来了统一的 dense tile 执行。</p> <p><img src="/assets/img/posts/20260825-kimi-k3-01-kda/figure3_p5.png" alt="KDA 的有界 decay"/></p> <p>上图左侧对比了无界的负 Softplus 和 K3 的 bounded sigmoid；右侧展示了为什么 bounded decay 能让所有 causal tile 走矩阵乘法，而不是只在非对角区域快。</p> <h2 id="7-gated-mla为什么不能所有层都用-kda">7. Gated MLA：为什么不能所有层都用 KDA</h2> <p>KDA 的状态固定大小，但它毕竟是压缩记忆：某个任务若需要精确访问很久以前的一段原文，有限状态就可能不如直接读取原始 KV。这正是第 2 节那次架构交换的代价。K3 因此保留 24 层 MLA，让模型在部分深度上仍然可以做全局 attention。</p> <p>K3 对 MLA 做了两个配合性改动：</p> <ol> <li>MLA 层使用 NoPE，不再单独依赖 RoPE 的长上下文外推；KDA 的衰减提供了一部分顺序和远近信号，MLA 更专注于内容检索。</li> <li>MLA 使用与 KDA 类似的输出门，让每个 token 通过输入相关的 gate 调制全局 attention 的输出通道。</li> </ol> <p>因此 Hybrid Attention 不是“69 层便宜层加 24 层昂贵层”的简单拼接，而是两个记忆系统分工：KDA 负责高频、低成本的状态更新，MLA 负责少量需要精确全局访问的场景。比例和层位置是模型设计与硬件成本之间的折中，不能直接推广成所有模型都应该采用 3:1。</p> <h2 id="8-实验应该怎样读">8. 实验应该怎样读</h2> <p>论文在 H800 集群上比较 Kimi K2 和 K3 的推理 cost per million tokens。prefill 是一次性读入 prompt 的阶段，decode 是逐 token 生成的阶段。随着 token position 接近 128K，K3 的成本曲线增长更慢，decode 阶段的差距尤其明显。</p> <p>这里要注意两个边界：第一，这个结果测的是完整系统，不是只测 KDA 状态更新；第二，indexer、chunkwise kernel、MLA 层比例和部署配置都会影响绝对数值。KDA 的核心结论是主状态不再随着长度保存完整 KV，但端到端收益仍然需要硬件实现来兑现。</p> <h2 id="9-小结">9. 小结</h2> <p>KDA 的设计可以按一条因果链记住：</p> <ol> <li>全量 KV cache 在超长 context 下带来持续增长的读取成本；</li> <li>线性 attention 用固定矩阵状态代替历史，但只写不擦会产生干扰；</li> <li>delta rule 让写入变成“按 key 方向纠错”，再配合逐通道遗忘；</li> <li>逐 token 更新无法喂饱 GPU，于是把递推重排成 chunk 间传状态、chunk 内矩阵并行；</li> <li>衰减连乘会造成数值问题，K3 用 lower-bounded decay 换取统一的 Tensor Core kernel；</li> <li>少量 Gated MLA 保留精确的全局访问，补上固定状态的记忆边界。</li> </ol> <p>下一篇继续沿着“为什么会这样设计”的思路，讲 K3 的 AttnRes 和 Stable LatentMoE：一个处理层与层之间的信息流，一个处理 896 个专家如何稳定训练。</p> <hr/> <blockquote> <p>扯一句题外话：KDA 是推理系统层面的新 attention 结构，书里不会替你讲完 chunkwise kernel；《动手学 AutoML：从 NAS 到大语言模型优化实战》第 8、11 章整理了 LLM 剪枝、量化和后训练压缩，切入点不同，但同样是在模型能力和计算/存储成本之间找折中。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="KDA"/><category term="线性注意力"/><category term="KV Cache"/><category term="推理优化"/><category term="论文解读"/><summary type="html"><![CDATA[Kimi K3技术详解系列（一）：KDA 与 Hybrid Attention]]></summary></entry><entry><title type="html">2026 | Voice Memory：ASR 纠错不训权重只训一个 memory.md，学到的是’克制’</title><link href="https://marsggbo.github.io/blog/2026/20260902-voicememory/" rel="alternate" type="text/html" title="2026 | Voice Memory：ASR 纠错不训权重只训一个 memory.md，学到的是’克制’"/><published>2026-09-02T00:00:00+00:00</published><updated>2026-09-02T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260902-voicememory</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260902-voicememory/"><![CDATA[<h1 id="2026--voice-memoryasr-纠错不训权重只训一个-memorymd学到的是克制">2026 | Voice Memory：ASR 纠错不训权重只训一个 memory.md，学到的是”克制”</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2607.26410">Voice Memory for Agentic Speech Recognition</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有遇到过这种情况：语音识别（ASR，Automatic Speech Recognition，把语音转成文字的模型）的结果已经挺准了，但你还是想”再抢救一下”——把识别结果丢给大模型 LLM 纠错，结果反而越改越差？</p> <p>这个问题比表面看起来复杂得多。纠错模型最大的毛病不是不够聪明，而是<strong>太勤快</strong>：它总想改点什么。NVIDIA 这篇 Voice Memory 给出的答案很反直觉——<strong>最好的纠错是大部分时候什么都不做</strong>，而且实现方式更反直觉：不改任何权重，只在推理时读一个几百字节的文本文件。</p> <p>先交代下背景。把 ASR 的输出交给 LLM 做二次纠错，这条路线在业界叫生成式纠错（GER，Generative Error Correction，让 LLM 把识别出的文字重写一遍以消除错误）。论文里有个数字特别扎眼：在金融新闻这种数据上，无约束的 GER 每做 100 次编辑，最多有 64 次是把本来对的 token 改坏了。Voice Memory 把这个数字压到了 35%。在 10 个公开 benchmark 上，加权 WER 从 8.36% 降到 7.52%，<strong>没有任何一个数据集比不改还差</strong>——不过这个提升的幅度和分布，后面第 5、8 节会拆开看，别指望它是全面碾压。</p> <p>这套方法有个名字很贴切：Voice Memory——把纠错技能写进一个叫 <code class="language-plaintext highlighter-rouge">memory.md</code> 的文本文件里。</p> <p>先别急着往下看，把这篇工作的定位说清楚，免得期待错位：<strong>它的新意不在原理，而在组合和工程化</strong>。拆开看，几个部件没一个是新的——LLM 读 n-best 做生成式纠错（GER）是 2023 年就有的老路线；用验证集反馈自动改进文本提示是 GEPA、Trace2Skill 这类轨迹优化同款玩法；”LLM 别乱改”也是做 GER 的人早就知道的常识。Voice Memory 做的是把它们组装成一个带显式决策的闭环：纠错技能外置成一个可审计、可自动学习的 <code class="language-plaintext highlighter-rouge">memory.md</code>，纠错器每句只做一件事——决定动手改还是 abstain（放弃）。这个组装带来一个实打实的实证发现：<strong>无约束的纠错会过度纠错（最多 64% 的编辑改坏正确 token），而学会”克制”后有害编辑率降到 35%</strong>。后面所有实验都在验证这个发现，以及它的适用范围和边界。如果读完只想带走一个数字，记住 64%→35%——它说明纠错系统的头号敌人不是”不够聪明”，而是”不知道什么时候不该动”。</p> <h2 id="2-背景asr-纠错的两种老路为什么都不够好">2. 背景：ASR 纠错的两种老路，为什么都不够好</h2> <p>先建立概念。ASR 模型解码时通常会输出多个候选结果，按得分从高到低排成一列，得分最高的叫 1-best（第一名），整列叫 n-best（前 n 个候选）。纠错模型的输入就是这个 n-best 列表，输出一条更准的文字。评测指标是 WER（Word Error Rate，词错误率：要改动多少个词才能把识别结果变成标准答案，替换、删除、插入都算，越低越好）。</p> <p>想给 ASR 注入领域知识，历史上主要有两条路：</p> <p><strong>第一条路：上下文偏置（contextual biasing）</strong>。提前准备一个领域热词表（比如航空公司常见的地名、航班号），在解码时”喂”给声学模型，让它更倾向于识别这些词。这条路的问题是：需要专门的 bias 词表、要重新适配模型甚至重新训练，而且词表外的知识一概无效。</p> <p><strong>第二条路：LLM 生成式纠错（GER）</strong>。不碰声学模型，把纠错当成一个纯文本任务：LLM 读 n-best，重写一条更合理的文字。这条路灵活得多，但它有个致命问题——<strong>LLM 不知道什么时候该住手</strong>。它会把 “dollars” 改成 “$”、把正确的词”润色”成另一个正确的词、把缩写展开、把语序”优化”一下。改完一测 WER，反而涨了。</p> <p>论文用表 2 把两条路的代价摊开对比：微调（fine-tune，在预训练模型基础上用领域数据继续训练）和 LoRA（Low-Rank Adaptation，一种只训练少量低秩参数的轻量微调方法）要 forward + backward 反传梯度，耗时以小时计、需要 10³~10⁴ 条数据、产物是 GB/MB 级的权重文件；而 Voice Memory 全程只有前向推理，分钟级搞定，~10² 条数据就够，产物不到 10KB，还能被人读、被审计、随时撤销。</p> <p><img src="/assets/img/posts/20260902-voicememory/table2_p4.png" alt="微调与 Voice Memory 的对比"/></p> <h2 id="3-核心思想listener-thinker-架构">3. 核心思想：listener-thinker 架构</h2> <p>Voice Memory 把纠错拆成了两个角色，论文称之为 listener-thinker（监听者-思考者）架构：</p> <ul> <li><strong>listener（监听者）</strong>：推理时同步工作的那条路径。一个<strong>完全冻结</strong>的纠错模型读入语音识别出的 n-best，外加一份当前生效的 <code class="language-plaintext highlighter-rouge">memory.md</code>，然后做出决策——这一条 utterance（一句话）是动手改，还是 abstain（放弃，直接保留 1-best）。注意是”决策”，不是”重写”，这是全文最关键的设计。</li> <li><strong>thinker（思考者）</strong>：推理之外异步工作的那条路径。一个优化器离线地对 <code class="language-plaintext highlighter-rouge">memory.md</code> 做有界编辑（一次最多改几个条目），只有在改完后的验证分数<strong>严格变好</strong>时才接受这次编辑。</li> </ul> <p>两个角色唯一的耦合点是那个文本文件。权重一个字不动，所以这套系统天然可审计、可移植、可回滚——这些都是权重里的知识给不了的。下图把整个结构画得很清楚：推理时（GPU 侧）冻结的纠错器读 memory 做决策，离线时（CPU 侧）优化器只改那个文本状态，二者通过文件解耦。</p> <p><img src="/assets/img/posts/20260902-voicememory/figure1_p2.png" alt="Voice Memory 总体架构"/></p> <p>论文还把这种格式放在整个语音系统的谱系里看（下表），只有 listener-thinker 这一列同时拥有”证据、外部文本状态、逐句动作决策、测试时自我改进”四个槽位。</p> <p><img src="/assets/img/posts/20260902-voicememory/table1_p3.png" alt="三类语音系统的对比"/></p> <p>这里要诚实交代一个适用范围问题：<strong>这套框架强依赖 n-best，论文的整个评估都建立在 Whisper 的候选列表之上</strong>。HyPoradise 这个 benchmark 本身就是 Whisper large-v2 的 5-best 数据（模型卡里白纸黑字写着：”ρ depends on the n-best list (Whisper large-v2 5-best throughout)”），所以深度研究（MiniMax 当纠错端）和广度研究（Qwen3-30B-A3B 当纠错端）里，listener 一直是传统声学模型，纠错端换的是 LLM。对 Qwen3-ASR 这类 LLM-based ASR，情况就尴尬了：它们通常是端到端单输出（argmax 取 token），不产出候选列表——没有 n-best 就没有 oracle（上帝视角上界），ρ 这套”恢复了多少 headroom”的分析直接无法定义。act/abstain 的主干机制理论上还能套（决定改不改唯一的 1-best），但收益会大幅缩水，memory 能补的大概只剩领域专有名词。论文对这条路径没有任何实验，所以严格说：<strong>它解决的是”传统 ASR + LLM 纠错”级联的适配问题，不是 LLM-based ASR 的问题</strong>。</p> <h2 id="4-memorymd-里到底写了什么以及它是怎么被优化出来的">4. memory.md 里到底写了什么，以及它是怎么被优化出来的</h2> <p>整个系统学到的”技能”，最后都沉淀在一个人类可读的 Markdown 文件里。论文给的例子长这样：</p> <div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code>keep the verbatim ASR token; do not normalize.
place 'dollars' after the amount, not '$' before it.
</code></pre></div></div> <p>第一条意思是”保留 ASR 的原始 token，别做规范化”——这就是典型的<strong>克制型规则</strong>：它在教模型不要手痒。第二条是领域约定（金额的表达习惯）。部署下来的 wsj 领域记忆只有 <strong>776 字节</strong>。</p> <p>这个文件不是人写的，是优化器自己从训练集里”长”出来的。整个流程见算法 1：</p> <p><img src="/assets/img/posts/20260902-voicememory/algorithm1_p6.png" alt="Voice Memory 优化算法"/></p> <p>大致逻辑是：</p> <ol> <li>拿当前 <code class="language-plaintext highlighter-rouge">memory.md</code>，让冻结的纠错模型对训练集做 rollout（跑一遍推理得到输出），把失败的样本和成功的样本分开；</li> <li>优化器基于失败样本提出一组候选编辑（增、删、改条目，有预算上限）；</li> <li>在<strong>独立的验证集</strong>上打分，只有分数严格高于当前最优才接受这次编辑，否则回滚；</li> <li>迭代若干轮（默认 4 epochs），返回最终文件。</li> </ol> <p>关于这个文件的生命周期，有几个值得展开的点：它会不会越攒越大？会不会过拟合？是不是每个场景都要写一条规则？论文都有交代，只是散在各处，这里集中说。</p> <p><strong>规模是受控的。</strong> 优化过程是”有界编辑”——每次迭代最多增删改几个条目，而不是无限制地累积规则；部署产物也印证了这一点：wsj 记忆只有 776 字节，最大的自动生成版也才 9.0 KB，而且论文还观察到”质量胜过覆盖”——5.4 KB 的 Claude 生成记忆效果比 9.0 KB 的自动记忆更好。</p> <p><strong>过拟合有防线，但没被彻底解决。</strong> 门控是主要防线：每次编辑都要在独立验证集（论文叫选择集）上严格变好才接受，比直接优化训练集稳得多；语义门控又比 WER 门控对随机种子稳定 4 倍。但诚实地讲：跨模型和翻译实验只有 40~100 条 utterance 的小样本，而且论文自己在 wsj（数据量大的领域）观察到自形成记忆会过度纠错（6.2 vs 无记忆 6.1）——<strong>这就是过拟合的实锤</strong>，论文没有回避。”记忆规模与过拟合的关系”没有被系统研究，是空白。</p> <p><strong>不是每个场景写一条规则。</strong> memory 是 per-domain 的：每个领域一份文件，里面是通用克制规则（keep verbatim、do not normalize）加少量领域规则，而不是把每个错误都记一条；跨域迁移实验（一份记忆换到别的领域）显示学到的规则更接近”通用的克制策略”而非领域特化修补。代价是每个新领域确实要单独优化一份 memory，这个成本要算进去。</p> <p>全程只有前向推理，没有任何梯度。打分用什么指标很关键，论文后面专门分析过（第 7 节），先按下不表。</p> <p>看到这里可能有人会觉得：这不就是”给 LLM 换了个更好的 prompt”吗？说实话，<strong>这个质疑一半是对的</strong>。作者自己发布的 notebook 里，用一个 30 行的规则引擎就复现了完整效果：正则解析 memory 里的替换规则（<code class="language-plaintext highlighter-rouge">"x" -&gt; "y"</code>）和偏好词表，按命中数给候选打分选一条，没有命中就 abstain——LLM 在这里只是规则的”通用执行器”，规则本身是机械可执行的。这恰好说明：<strong>memory 本质上就是一组显式规则，说它”像 skill”不是贬低，而是准确描述</strong>。</p> <p>那剩下的那一半新意在哪？两处：一是这套规则不是人写的，是优化器在独立验证集上门控”搜索”出来的——搜索空间是文本规则，搜索策略是迭代编辑加”严格变好才接受”的收益判断，这跟 NAS 搜网络结构是同一个味道；二是它配了 ρ、HER 两把尺子（下面马上讲），让”纠错有没有用、有没有改坏”第一次可以量化——没有这两把尺子，这套搜索无从谈起。诚实地说，连”验证集门控优化提示词”这个玩法本身也不是它首创的（GEPA、Trace2Skill 都在做），它的增量是把这套搬进了 ASR 纠错，并实证了”克制”是收益来源。</p> <p>为了量化”纠错到底帮了多少”，论文定义了两个指标，都值得记住：</p> <ul> <li><strong>ρ（可恢复信息比，Recoverable Information Ratio）</strong>：纠错后缩小的错误差距占”1-best 到 oracle”差距的比例。oracle（上帝视角）指的是每句都从 n-best 里挑出 WER 最低那条的极限。ρ=1 说明完全吃满了 n-best 能提供的收益；ρ&lt;0 说明越纠越差（损伤区）；ρ&gt;1 说明它产出了 n-best 里根本不存在的 token，比 oracle 还强。</li> <li><strong>HER（有害编辑率，Harmful Edit Rate）</strong>：所有编辑里，把本来正确的 token 改坏的那部分占比。这个指标专门用来抓”过度纠错”。</li> </ul> <h2 id="5-核心发现这个循环学到的技能是克制">5. 核心发现：这个循环学到的技能是”克制”</h2> <p>论文最重要的结论藏在实验里：<strong>无约束的 GER 之所以有害，是因为它把纠正当成了改写；而 Voice Memory 学到的恰恰是”哪些 token 不要碰”</strong>。</p> <p>先看深度研究：用 MiniMax-M3428B（428B 参数、激活 23B 的模型）当冻结纠错器，在 HyPoradise（一个开源的 ASR 纠错 benchmark 套件，覆盖 9 个领域）全测试集上跑，结果如表 3：</p> <p><img src="/assets/img/posts/20260902-voicememory/table3_p6.png" alt="九个领域的主结果"/></p> <p>几个数字值得细看：</p> <ul> <li><strong>高错误率领域收益最大</strong>：atis（航班信息查询）1-best 是 7.9，oracle 是 4.7，Voice Memory 干到 4.9，ρ=0.96——几乎吃满了 n-best 的全部潜力。</li> <li><strong>错误率越低收益越少，甚至为负</strong>：ls_clean（干净的朗读语音）1-best 已经 1.8，再纠反而到 1.9（ρ=-0.11）。这印证了 ρ 和 1-best WER 的相关系数高达 +0.90——<strong>收益集中在有头寸（headroom）可恢复的地方，到了错误率地板就没什么可纠的了</strong>。</li> <li><strong>零样本 GER 在三成领域把 WER 抬高了</strong>：ls_clean 1.8→2.4、ls_other 3.7→4.1、td3 4.1→4.4，典型的过度纠错。</li> </ul> <p>有害编辑率的变化更能说明问题（下图）：四个领域上静态 GER（static GER，即不做记忆、直接让 LLM 硬纠）的 HER 高达 0.25~0.74，Voice Memory 把它们全部降下来，wsj 从 0.64 压到 0.35。</p> <p><img src="/assets/img/posts/20260902-voicememory/figure2_p7.png" alt="有害编辑率对比"/></p> <p>在噪声环境下结论一样成立（表 4）：CHiME-4 这类真实录音噪声语料上，静态 GER 反而把 WER 抬高（9.8→9.9），Voice Memory 则降到 9.5；NOIZEUS 0dB 极端噪声下 42.9→41.4。而在”干净的朗读语音 + 人为加噪”这种本来就没多少可恢复空间的场景，Voice Memory 学会的是<strong>老老实实 abstain</strong>。</p> <p><img src="/assets/img/posts/20260902-voicememory/table4_p7.png" alt="噪声鲁棒性结果"/></p> <h2 id="6-换个模型换个任务记忆照样好用">6. 换个模型、换个任务，记忆照样好用</h2> <p>文本形式的最大红利是<strong>可移植</strong>。论文做了两个方向的验证：</p> <p><strong>换模型</strong>：把 MiniMax 优化出的记忆直接给 Anthropic Claude-4.6-Sonnet（一个完全不同的模型家族）读。在 atis 上，Claude 无记忆时 WER 6.7，读 MiniMax 写的记忆降到 6.1——<strong>一个模型学到的技能，另一个模型直接继承</strong>。更妙的是让 Claude 用自己的 loop 重新优化一份记忆，在 atis 上达到 3.9，ρ=1.28，<strong>超过了 n-best oracle</strong>（因为它恢复了 n-best 里不存在的 token）。注意这是 100 条 utterance 的小样本实验，数字当定性证据看，别当定量结论。边界也很诚实：在数据量大的 wsj 上自形成记忆会过度纠错（6.2 vs 无记忆 6.1），在错误率地板处（ls_clean）不读记忆最好。</p> <p><img src="/assets/img/posts/20260902-voicememory/table5_p8.png" alt="跨模型迁移实验"/></p> <p><strong>换任务</strong>：把同一个 loop 搬到语音翻译纠错上（X→En，即把非英语语音先识别再翻译成英语，让 LLM 从 n-best 翻译里调出一条更好的，用 BLEU 打分——BLEU 衡量机器翻译和参考译文的重合度，越高越好）。四个语向上 BLEU 全部提升，恢复了 oracle 头寸的 13%~32%。40 条 utterance 的小实验，但机制完全一致：优化器甚至拒绝了”抄一条参考译文就能刷分”的投机编辑——它学到的还是克制。</p> <p><img src="/assets/img/posts/20260902-voicememory/table6_p8.png" alt="翻译纠错结果"/></p> <h2 id="7-分析为什么优化语义比优化-wer更好">7. 分析：为什么”优化语义”比”优化 WER”更好</h2> <p>论文第 7 节是我个人觉得最有意思的部分，它回答了一个方法论问题：优化器打分时该盯着哪个指标？</p> <p>答案是<strong>不要盯着 WER</strong>。表 7 的消融实验直接对比两种 gate（验证闸门，决定是否接受一次编辑的信号）：WER gate（按 WER 打分）和 semantic gate（按纠错结果与参考句的语义相似度打分，用端侧句向量模型算余弦相似度）。结果是 semantic gate 在两个领域两个随机种子下 WER 都更低，而且在 ls_clean 上对随机种子稳定 4 倍（结果波动范围 0.03 vs 0.17）。</p> <p><img src="/assets/img/posts/20260902-voicememory/table7_p9.png" alt="gate 消融实验"/></p> <p>原因值得细品：<strong>WER 是按”表面字符”打分的，它奖励任何消除表面不匹配的编辑——包括同音字、拼写变体这类”改了也没意义甚至更糟”的改动</strong>。而语义打分只认”意思对不对”，天然对这类表面追逐免疫。用论文的话说：优化你上报的指标（WER），不如优化你真正在乎的东西（语义），WER 只是语义的带噪近似。</p> <p>表 8 把这个”表面到语义的鸿沟”量化了：解耦斜率（decoupling slope）只有 0.38~0.60，意味着 WER 变动一个单位，语义只动大约半个单位；而且残余错误里有 53%~68% 是”良性的”（语义距离小于 0.15，即意思完全没变）。也就是说，<strong>剩下的 WER 里有大半根本不该去纠</strong>——这给”克制”提供了最硬核的论据。</p> <p><img src="/assets/img/posts/20260902-voicememory/table8_p10.png" alt="表面与语义的鸿沟"/></p> <h2 id="8-广度验证开源模型--全部-10-个数据集">8. 广度验证：开源模型 + 全部 10 个数据集</h2> <p>深度研究用的是闭源 MiniMax，论文还用全开源组合验证了广度：Whisper-v2-Large 当声学模型 + Qwen3-30B-A3B 当纠错器（一个稀疏 MoE 模型，MoE 是混合专家架构，前向时只激活约 30B 里的 3B 参数），覆盖 16,108 条测试样本、10 个数据集。</p> <p><img src="/assets/img/posts/20260902-voicememory/table9_p15.png" alt="开放纠错器的广度结果"/></p> <p>结果和深度研究完全一致：加权 WER 8.36% → 7.52%（再加三个 in-context 示例则到 7.47%），<strong>没有任何数据集跌破 1-best 基线</strong>。收益最大的还是 ATIS：8.40% → 3.40%，因为它缺失的领域词汇（航班、地名）恰恰是 memory 能补上的；而本就很难的对话语音（CORAAL、SWBD）几乎不动——那是声学歧义，语言先验救不了。</p> <p>消融实验还揭示了一个反直觉的功臣：<strong>负向规则（negative rules，教模型”不要做什么”的规则）是承重墙</strong>。单独只保留负向规则，加权 WER 就从 7.66 降到 7.52，比任何其他组件贡献都大。它们的职责就是阻止纠错器去改写本来正确的 token——压缩写的展开、压 paraphrase、压数字转单词。整个 agent 发现的最优策略，本质就是”知道什么时候别动”。</p> <h2 id="9-我的-take">9. 我的 take</h2> <p>先说结论：<strong>这不是一篇原理上有突破的论文，但也不是水文</strong>。它的贡献要分清楚哪些是实的、哪些只是包装。</p> <p><strong>实的地方有两个。</strong> 一是实证发现”克制”是纠错收益的主要来源：64% 有害编辑率这个数字本身就说明”重写式纠错”的路线有问题，压到 35% 靠的不是更聪明的重写技巧，而是学会了 abstain；第 7 节的语义门控（优化语义比优化 WER 更好）也是可迁移的方法论结论。二是工程上的”技能外置”：把纠错技能写成几百字节的可读文件，让冻结模型加一份外部状态就能改进，可审计、可移植、可回滚——对”部署侧零成本提效”这个命题，它给了个朴素但能用的答案。</p> <p><strong>包装过度的也有两处。</strong> 一是”agentic”这个词：act/abstain 这个决策建模本身是合理的，但把它说成”智能体”有点往脸上贴金，本质就是”加了一个是否改写的门控”。二是”对模型本身有什么贡献”——它对模型（权重）没有任何改动，这不是缺陷，但也别拔高成”模型层贡献”，它做的就是提示词优化那一套，只是产物从连续向量换成了可读文本。</p> <p><strong>最硬的两个边界，论文自己其实交代了，但容易被读者忽略：</strong></p> <ul> <li><strong>强依赖 n-best</strong>：ρ、oracle headroom 全部建立在候选列表存在的前提上（Whisper large-v2 5-best throughout）。对 Qwen3-ASR 这类单输出 LLM-based ASR，评估体系直接失效，act/abstain 机制勉强能套但收益缩水，论文未验证——它解决的是”传统 ASR + LLM 纠错”级联的适配，不是 LLM-based ASR 的问题。</li> <li><strong>收益幅度有限</strong>：加权 WER 8.36%→7.52%，绝对收益不到一个百分点；深度研究（表 3）里 Voice Memory 相对零样本 GER 在 9 个领域里 8 个更好（唯一更差的是 coraal 25.3 vs 25.1），但相比 1-best，在错误率地板处是负收益（td3 4.1→4.2、ls_c 1.8→1.9，ρ&lt;0，靠 abstain 兜底不更差）。收益高度集中在 ATIS 这类”领域词汇密集”的任务上（7.9→4.9，一次砍 3 个点），其余大多是 0.1~1 个点的改善。</li> </ul> <p>一句话总结：<strong>它值得一读，是因为”纠错要学会克制”这个实证发现和语义门控的方法论；它不值得被神化，是因为机制上它就是”LLM 纠错 + 验证集门控的提示词优化”，而评估体系被 n-best 这个前提牢牢绑住。</strong></p> <hr/> <p>顺带一提，这篇文章讲的”推理时自适应、不动权重”其实和我在 LLM 压缩、推理效率方向上的研究是同一个底层命题：<strong>模型训练完之后，还能怎么零成本地变强</strong>。我们把这些年的积累整理成了《动手学 AutoML：从 NAS 到大语言模型优化实战》，书里第 8、11 章讲了 LLM 压缩（剪枝、量化）这类”训练后提效”的手段，跟本文角度不同但目标相近——都是不动训练流程、在部署侧榨出剩余价值。有兴趣的读者可以翻翻。</p> <blockquote> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="ASR"/><category term="LLM"/><category term="论文解读"/><summary type="html"><![CDATA[2026 | Voice Memory：ASR 纠错不训权重只训一个 memory.md，学到的是”克制”]]></summary></entry><entry><title type="html">arXiv’26 | TurboBias 2.0 让流式 ASR 的个性化词表真正跑进生产环境</title><link href="https://marsggbo.github.io/blog/2026/20260826-turbobias2/" rel="alternate" type="text/html" title="arXiv’26 | TurboBias 2.0 让流式 ASR 的个性化词表真正跑进生产环境"/><published>2026-08-26T00:00:00+00:00</published><updated>2026-08-26T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260826-turbobias2</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260826-turbobias2/"><![CDATA[<h1 id="arxiv26--turbobias-20-让流式-asr-的个性化词表真正跑进生产环境">arXiv’26 | TurboBias 2.0 让流式 ASR 的个性化词表真正跑进生产环境</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2608.21343v1">TurboBias 2.0: Streaming Context-Biasing for Production-Efficient ASR Systems</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有想过，当你在财报电话会里听到 CEO 快速念出 “Husqvarna”、”GlobalSign” 这种公司名，或者医生口述 “linezolid”、”abciximab” 这种药名时，ASR 系统是怎么做到不把它们听错的？</p> <p>这个问题比表面看起来复杂得多。</p> <p>现代的 Transducer ASR（比如 NVIDIA 家的 Parakeet 系列）在通用集上已经很强了，但一碰到<strong>低频的、用户自定义的领域词</strong>就容易翻车。偏偏这些词在财报、医疗、会议这种生产场景里，错一个就很要命。今天想和大家聊聊 NVIDIA 刚放出来的这篇 <strong>TurboBias 2.0</strong>，它不是在刷榜，而是在解决 <strong>“准确率和生产可用性之间的核心矛盾”</strong>。</p> <p>一句话总结：<strong>让上下文 biasing 真正在流式、低延迟、多用户批处理的生产环境里跑起来，且不靠重训、不加额外模型、不过度加延迟</strong>。</p> <p>作为做 LLM 推理效率的牛马，看到 ASR 领域也在卷 “streaming + batching + GPU 效率” 这套，真的很有共鸣——本质上和 LLM 的 KV Cache、beam search 优化是同一类工程问题。</p> <h2 id="2-为什么现有的-context-biasing-不够用">2. 为什么现有的 context-biasing 不够用？</h2> <p>论文开头就把行业现状捋得很清楚。我帮大家提炼一下现有方案的坑：</p> <p><strong>1）Deep-fusion / Adapter 类方法</strong>：把上下文信息直接塞进模型里，靠 attention / adapter / 特殊 loss 去学。效果可以，但<strong>要改模型、要重训</strong>。生产环境里你不可能为每个客户的词表都重训一个 600M 的 Transducer。</p> <p><strong>2）Prompt 类方法</strong>：最近的 speech-LLM 也能用文本 prompt 塞词表，但<strong>对 prompt 设计和词表长度极度敏感</strong>，延迟也不可控。</p> <p><strong>3）Shallow-fusion / Keyword boosting</strong>：在解码时给匹配到词表的假设加分，不用重训，最适合生产。但问题是——<strong>已有的实现，离真正的”生产级”还差很远</strong>。</p> <p>具体差在哪？作者一针见血指出了三个没被解决的痛点，也是本文的三个核心贡献点：</p> <ul> <li> <p><strong>大小写不鲁棒</strong>：现在的 ASR 输出是带格式化的（有大小写、标点），但用户给的词表往往是小写或随意写的。原来的 TurboBias 是大小写敏感的，<code class="language-plaintext highlighter-rouge">hello</code> 匹配不到 <code class="language-plaintext highlighter-rouge">Hello</code>/<code class="language-plaintext highlighter-rouge">HELLO</code>。简单粗暴地把每个词展开成 lower/upper/title-case 三份，会让 boosting tree 膨胀、编译变慢、显存变多。</p> </li> <li> <p><strong>批处理里无法个性化</strong>：生产服务是多用户并发的，一台 GPU 要同时处理 100+ 路音频流，每路对应不同的会议/病人/客户，词表各不相同。如果全用一份全局共享的 boosting tree，A 客户的词会污染 B 客户的识别，<strong>distractor 互相干扰，误触率飙升</strong>。但此前的方法只支持单棵全局树。</p> </li> <li> <p><strong>不支持流式 + beam search</strong>：很多应用要<strong>低延迟的 streaming 识别</strong>，Chunk 到了就要出字。而之前的 TurboBias 只支持 offline beam search。流式 greedy 能跑，但 beam search 带来的词表召回提升就吃不到了。</p> </li> </ul> <p>这三个问题，单独看都不”学术”，但合在一起就是生产落地绕不开的坎。</p> <h2 id="3-turbobias-20-是怎么解的">3. TurboBias 2.0 是怎么解的？</h2> <p>框架整体是在 TurboBias 1.0 的 GPU phrase-boosting tree 基础上做扩展，底层依旧是 <strong>NGPU-LM</strong> 的 weighted acceptor 结构，但加了三板斧。</p> <h3 id="31-case-insensitive-的可变-bpe-图不展开词表也能大小写不敏感">3.1 Case-insensitive 的可变 BPE 图：不展开词表，也能大小写不敏感</h3> <p>这是我觉得最巧的一点。</p> <p>思路分两层：</p> <p><strong>Character 模型很简单</strong>：如图 (d)，每个字符弧直接替换成两条并行的弧，<code class="language-plaintext highlighter-rouge">h/H</code>、<code class="language-plaintext highlighter-rouge">e/E</code> 同时接受，图依然紧凑，匹配与大小写无关。</p> <p><img src="/assets/img/posts/20260826-turbobias2/fig1_tokenization.png" alt="大小写不敏感的 tokenization 图"/></p> <p><strong>BPE 模型才是难点</strong>：BPE 词表里不是每个 token 都有大小写变体，比如 <code class="language-plaintext highlighter-rouge">_he</code> 存在，但 <code class="language-plaintext highlighter-rouge">_He</code>、<code class="language-plaintext highlighter-rouge">_HE</code> 可能根本不在词表里，且 tokenizer 对不同大小写的切分完全不一样。直接照搬字符的”双弧” trick 会失效。</p> <p>作者的解法叫 <strong>Variative BPE representation</strong>，如图 (c)(e)：</p> <ol> <li>先对词做 greedy SentencePiece 切分，得到 token 序列；</li> <li>把每个 token 再分解到最细的 <strong>canonical character chain</strong>（SentencePiece 本就是从字符拼起来的，这条链是现成的）；</li> <li>在这条字符链上，<strong>所有单字符 piece 对应的弧都保留</strong>，且对所有”能完整覆盖链上某一段连续字符”的词表 token，都加一条<strong>跳跃的 merged arc</strong>。</li> </ol> <p>Case-insensitive 版本就是把第 2 步的 canonical 形式全转成小写，任何词表 token，只要它小写后的分解能匹配小写词表路径，就允许作为一条弧。这样，不同大小写、不同 BPE 切分但小写后相同的路径，都会收敛到同一个 state。</p> <p>图不再是树了，因为有跨不同长度的跳边，但<strong>除了 suffix link 外依然是 DAG</strong>，所以仍能编译进 NGPU-LM 的数据结构。</p> <p><strong>Score 怎么给？</strong> 这是细节但很重要。原来一棵树上一个弧就是一个 greedy token，分数直接挂弧上。现在一条词可能有 3 个 token 的路径，也可能是 5 个字符的路径，如果每条弧都给一样分，字符路径总分就会偏高。</p> <p>解法是：<strong>在 state 上存 cumulative potential，弧的分数 = 终点 potential - 起点 potential</strong>。Potential 从小写的 greedy BPE 分数 <code class="language-plaintext highlighter-rouge">w_k</code> 按公式分配：</p> \[\Delta_{k,i} = w_k \frac{\exp((i+1)^{\tau})}{\sum_j \exp((j+1)^{\tau})}\] <p><code class="language-plaintext highlighter-rouge">τ=0</code> 时均分，<code class="language-plaintext highlighter-rouge">τ</code> 越大越接近原来”只在 BPE 末尾给分”的保守策略。论文发现，<strong>beam search 时平坦一点的分配更有利于早期匹配</strong>，而 greedy 时保守一点不易误触。这个 <code class="language-plaintext highlighter-rouge">τ</code> 的 trade-off 讲得很诚实。</p> <p>效果很直接：<strong>任何拼写变体，只要小写后一致，总分一致</strong>，不再依赖词表是怎么给的。</p> <h3 id="32-per-stream-biasing让-128-路并发各用各的词表还不掉速">3.2 Per-stream biasing：让 128 路并发各用各的词表，还不掉速</h3> <p>这是最”生产”的部分。</p> <p>底层数据结构：每棵 boosting tree 本质上是一组 tensor（arc 的 label/target/weight，state 的出边范围/suffix link/final weight）。TurboBias 2.0 把<strong>所有活跃的 tree 的 tensor 在 GPU 上 concat 成一份 merged multi-model storage</strong>，并为每个 model id 记录它在 merged tensor 中的 state/arc 偏移量。</p> <p>解码时，每条音频流带一个 <code class="language-plaintext highlighter-rouge">biasing model id</code>，<code class="language-plaintext highlighter-rouge">−1</code> 表示不 bias，非负表示选某棵树。GPU kernel 在做 NGPU-LM 的 <code class="language-plaintext highlighter-rouge">advance</code> 之前，先根据 <code class="language-plaintext highlighter-rouge">model id</code> 把指针偏移到对应的区间，再跑同一套 suffix-link 遍历逻辑。对 <code class="language-plaintext highlighter-rouge">-1</code> 的流直接返回 0 分和无效 state，相当于无 bias。</p> <p><strong>关键点有三：</strong></p> <ul> <li><strong>零交叉污染</strong>：每路流只查自己的树，A 的词表不会成为 B 的 distractor。</li> <li><strong>动态增删</strong>：添加就是 append tensor 并记偏移；删除就标记 id 失效并把后面的 tensor 区间左移补洞——全是 contiguous tensor 操作，GPU 并行很快。</li> <li><strong>性能几乎无损</strong>：相比单棵全局树，额外开销只有”读 model id + 读两个 offset”，实测<strong>没有可观测的解码速度下降</strong>。</li> </ul> <p>真正的瓶颈是<strong>建树和 Host→GPU 传输</strong>。每棵树通常 <code class="language-plaintext highlighter-rouge">&lt;1MB</code>，存几百棵完全可行，但频繁创建/销毁 128 路流如果每次都编译+拷贝就炸了。所以作者做了<strong>三级 cache</strong>：</p> <ul> <li><strong>Disk</strong>：持久化预编译的大词表；</li> <li><strong>CPU memory</strong>：已编译的模型放内存，按需拷贝到 GPU merged 容器，开销约 9–17%；</li> <li><strong>Decoder-level registration</strong>：会话开始前就把模型注册到 decoder，<strong>开销完全隐藏在解码循环之外</strong>，速度与全局树无异。</li> </ul> <p>没有 cache 时，128 流 greedy 的 RTFx 会从 1726 暴跌到 61（慢 28 倍），而有了 decoder cache 能拉回 1717——这个数字很能说明问题：<strong>工程优化比算法本身还重要</strong>。</p> <h3 id="33-streaming-beam-search把-offline-的加速搬到流式">3.3 Streaming beam search：把 offline 的加速搬到流式</h3> <p>之前已有的工作 [21] 把 Transducer 的 beam search 用 tree-structured hypothesis + 全 GPU + CUDA Graph 做到了接近 greedy 的延迟。TurboBias 2.0 把这套扩展到 <strong>streaming</strong>：</p> <ul> <li>按 chunk 处理 encoder 输出，<strong>beam state 跨 chunk 保留</strong>；</li> <li>用 flattened tree 存上一个 chunk 的 finalized hypotheses，连同它们的累计分数和 boundary decoder states 一起作为下一 chunk 的 anchor；</li> <li>新的假设在此基础上继续扩展、合并、剪枝。</li> </ul> <p>整个实现<strong>依然兼容 CUDA Graph 和 batched GPU 操作</strong>，所以才能在 0.56s 甚至 1.12s 的 worst-case latency 下依然跑 beam search。</p> <p>说人话：以前流式只能跑 greedy，现在流式也能吃到 beam search 对词表召回的红利，且延迟不炸。</p> <h2 id="4-效果怎么样数字说话">4. 效果怎么样？数字说话</h2> <p>实验在两个数据集上跑：<strong>Contextual Earnings-22</strong>（公开，738 个关键词，平均每段 15s，财报场景）和 <strong>Internal Medical</strong>（3 小时，489 个关键词，疾病/手术/药名）。模型是三档 600M 的 Parakeet / Nemotron 家族，统一测 <strong>WER</strong> + <strong>F-score (P/R)</strong> + <strong>RTFx</strong>。</p> <p>如下图，横轴 WER 越左越好，纵轴 F-score 越高越好，曲线越往左上越优。</p> <p><img src="/assets/img/posts/20260826-turbobias2/fig2_wer_fscore.png" alt="上下文 biasing 效果：WER vs F-Score"/></p> <p>几个核心结论，我直接上数字：</p> <p><strong>1）直接在 Transducer 解码里加分，比 CTC-WS 那套双模型+后处理更优雅且更强</strong></p> <ul> <li>Earnings22 上，作者复现的 <code class="language-plaintext highlighter-rouge">TDT-v2 + CTC-WS</code> 把 no-context baseline 从 <strong>64.9 → 82.0 F-score，14.7% → 13.7% WER</strong>；</li> <li>但 TurboBias 2.0 用 <strong>Unified 模型 + offline beam + per-stream global</strong> 做到 <strong>87.5 F-score / 12.6% WER</strong>，<strong>几乎追平了 benchmark 报告的 Argmax CTC-WS (88.2 / 12.7%)</strong>，且 WER 更低，更重要的是——<strong>不需要额外的 CTC 推理分支和文本纠错阶段，也不需要 hybrid TDT-CTC 模型</strong>。</li> </ul> <p>在 Internal Medical 上趋势一致，直接 boosting 就能显著拉高 F-score 并降低 WER。</p> <p><strong>2）大小写不敏感是实打实的提升，不是”小 tricks”</strong></p> <p>在 Earnings22 + Unified 模型上（见 Table II）：</p> <ul> <li>Greedy 时，<strong>lowercase 词表 66.0 F-score</strong>，几乎没提升；<strong>target-cased (正确大小写) 74.8</strong>，提升明显；<strong>case-insensitive 75.4</strong>，<strong>直接超过了人工保证大小写正确的情况</strong>，且对输入大小写完全不敏感。</li> <li>Beam 时同样：<code class="language-plaintext highlighter-rouge">72.5 → 80.6 → 81.4</code>。说明如果你的用户词表是随便打的（大概率全小写），没有这个图，就会白丢 9 个点的 F-score。</li> </ul> <p><strong>3）Per-stream 的价值：去 distractor</strong></p> <ul> <li>在 beam 解码下，Full (全局共享) case-ins. 是 <strong>81.4 F-score / 12.8% WER</strong>；</li> <li>换成 <strong>Per-stream Global 就到 87.5 / 12.6%</strong>，<strong>6.1 个点的 F-score 提升</strong>，就是因为每路不再被别的路的词干扰。</li> <li>Per-stream Local (只含当前 utterance 的词) 能到 <strong>91.2 / 12.2%</strong>，但这属于”开卷考试”，不太现实。作者也明确把 <strong>Per-stream Global 作为主推荐 operating point</strong>，很克制。</li> </ul> <p><strong>4）流式场景下，模型选型比 biasing 本身更关键</strong></p> <ul> <li>TDT-v2 在 streaming 下相对 offline 掉点严重，biasing 补不回来；</li> <li><strong>Unified 模型在 1.12s 延迟下，biasing 后 62.2 → 81.7 F-score，16.3% → 14.3% WER</strong>，提升幅度和 offline 相当。</li> <li>更低的 <strong>0.56s 延迟</strong>趋势一致。</li> <li>Nemotron-streaming 在 Earnings22 上和 Unified 接近，但在 Medical 上明显弱于 Unified（1.12s 时 68.2/18.0% vs 71.1/14.0%），说明 <strong>Unified 的 MCR-RNNT 训练对缩小 offline-streaming gap 确实有效</strong>。</li> </ul> <p><strong>5）速度：128 路并发，cache 策略决定生死</strong></p> <p>以 Earnings22 + Unified 为例（Table III）：</p> <ul> <li><strong>Greedy，无 bias：1812 RTFx</strong>；Full bias 无 per-stream：1726（几乎无损）；</li> <li>Per-stream 但<strong>无 cache：61 RTFx</strong>，直接慢 28 倍——建树+传输成了瓶颈；</li> <li><strong>+ memory cache：1493 (Full) / 1467 (Global)</strong>，只慢 9–17%；</li> <li><strong>+ decoder cache：1717 / 1714</strong>，<strong>基本拉回无 bias 水平</strong>。</li> </ul> <p>Beam search 同理：无 cache 59 RTFx → decoder cache 989 RTFx，拉回 1150 的 86%。</p> <p>所以结论很清晰：<strong>Per-stream 的算法开销 negligible，工程上的 model lifecycle 优化才是决定能否上生产的关键</strong>。</p> <h2 id="5-我的-take">5. 我的 take</h2> <p>这篇工作没有炫技的”大模型结构创新”，但它解决的是<strong>真正的生产矛盾</strong>：论文里那些 F-score 很漂亮的 biasing 方法，一放到”流式 + 多用户并发 + 低延迟 + 个性化词表”的真实环境里，往往就不可用了。</p> <p>TurboBias 2.0 的三个设计，我觉得都很”工程正确”：</p> <ul> <li><strong>Variative BPE 图</strong>用”分解到字符再按词表重合并”解决了大小写问题，而不是让用户去保证词表格式——这是典型的 <strong>把复杂度留给系统，把简单留给用户</strong>；</li> <li><strong>Per-stream 的 merged tensor + offset 路由</strong>，保持了 GPU kernel 的单一执行路径，没有为个性化引入分支开销，这才是能 scale 到 128 流的根本；</li> <li><strong>Streaming beam search 的状态跨 chunk 保留 + CUDA Graph 兼容</strong>，让 biasing 在流式场景也能吃到 beam 的收益。</li> </ul> <p>当然，局限也写得很坦诚：目前只在英文字 Transducer 上验证，中文等多语言、Attention Encoder-Decoder 的流式行为、更大的 biasing weight 自适应，可能都是下一步。</p> <p>如果你在做 ASR 或语音相关的 infra，这篇的代码已经合进 <strong>NVIDIA NeMo</strong>（PR #15800/#15125/#15753），直接可用。做 LLM 推理的同学也可以对照着看——<strong>per-stream 词表</strong>和我们做 <strong>per-request LoRA / per-user KV cache 隔离</strong>时遇到的批处理污染问题，本质上是同一个。</p> <hr/> <blockquote> <p>如果这篇文章涉及的流式推理和批量调度你想系统深入，可以看看我之前出版的<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，书里有专章讲 LLM 推理效率和参数高效微调，正好和本文的 streaming + batched decoding 工程背景有关联。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="ASR"/><category term="语音识别"/><category term="论文解读"/><summary type="html"><![CDATA[arXiv’26 | TurboBias 2.0 让流式 ASR 的个性化词表真正跑进生产环境]]></summary></entry><entry><title type="html">AgenticASR：语音识别不用等你说完一整句话，就能边听边改</title><link href="https://marsggbo.github.io/blog/2026/20260820-agenticasr/" rel="alternate" type="text/html" title="AgenticASR：语音识别不用等你说完一整句话，就能边听边改"/><published>2026-08-20T00:00:00+00:00</published><updated>2026-08-20T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260820-agenticasr</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260820-agenticasr/"><![CDATA[<h1 id="agenticasr语音识别不用等你说完一整句话就能边听边改">AgenticASR：语音识别不用等你说完一整句话，就能边听边改</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2607.28175">AgenticASR: Refining Speech Recognition in Real-World Scenarios via an Agentic Approach</a></p> </blockquote> <hr/> <h2 id="1-前言识别对了不代表读得懂">1. 前言：识别对了，不代表读得懂</h2> <p>这几年语音识别（ASR，Automatic Speech Recognition）的准确率已经卷得很高了，不管是 Whisper 还是 Qwen3-ASR 这类模型，字错率都压到了个位数。但把识别出来的文字直接读一遍，你会发现一个挺反直觉的现象：识别是对的，转录出来的文字却很难读。</p> <p>原因很简单，人说话不是照着稿子念的。会有”嗯”“啊”这类填充词，会重复（”给他，给他发一下报告”），会说错了立刻改口，甚至说到一半改变主意重新组织句子。传统 ASR 做的是逐字转录（verbatim transcription）——你说什么它记什么，包括所有这些口语现象。这对法律取证之类的场景是刚需，但对语音助手、会议转录、口述录入这些场景来说，这些内容只会增加阅读负担，甚至可能被后面的意图识别、对话系统误解。</p> <p>于是有了”口语转书面语”（spoken-to-written）这类后处理方案：等一整句话说完，再用一个模型把逐字稿转写成干净的书面文字，去掉填充词、修正自我纠错。Typeless 是这类系统的代表。</p> <p>问题出在”等一整句话说完”这个前提上。真实对话里，说话人经常在后半句才修正前半句的内容——比如先说了”发给 Mary”，说完又想起对方名字拼法不一样，补一句”哦对了，是那个结尾 R-I-E 的 Marie”。这时候 offline 的后处理系统已经把”Mary”这段文字提交完了，没有机制回头改。</p> <p>所以这里真正要解决的问题，不是”要不要清理口语内容”——这个已经有答案了——而是：<strong>能不能一边监听语音一边吐出干净文字，同时保留”过会儿反悔”的能力，用后面听到的内容去修正已经吐出来的那部分文本？</strong></p> <p>这篇论文给出的答案叫 AgenticASR，来自上海交大 X-LANCE 实验室。</p> <hr/> <h2 id="2-先分清两个名字任务定义和系统实现">2. 先分清两个名字：任务定义和系统实现</h2> <p>论文里有两个很像的名字，容易混。</p> <p><strong>AgenticSR</strong>（Agentic Speech Recognition）是论文定义的一个<strong>任务类别</strong>——把带口语现象的语音，识别成一份保留说话人最终意图的干净书面转录，而且要支持在说话过程中持续修正。它不是某个具体模型的名字，是一类问题的统称：去掉语义上空的填充词、重复和被放弃的自我修正片段；保留所有真正想表达的内容；把口语数字、日期这类表达转成书面形式（这个操作叫 ITN，逆文本归一化，比如把”三点二五”变成”3.25”）；对已经干净的输入原样返回。</p> <p><strong>AgenticASR</strong> 则是论文提出的一套具体实现这个任务的系统。下图对比了传统 ASR、offline 后处理（以 Typeless 为代表）和 AgenticASR 三种做法的差异：</p> <p><img src="/assets/img/posts/20260820-agenticasr/figure1_full.png" alt="三种转录方式对比"/></p> <p>图里的例子很典型：用户说”帮我把报告发给 Mary”，中间用了”嗯”“对”这些填充词。传统 ASR 原样转录，连填充词和识别错的”Mary”都保留。Typeless 这类 offline 系统等一句话说完才处理，转出干净版本但名字还是错的——直到用户后面又补充”抱歉，我是说 Marie，那个结尾是 R-I-E 的”，Typeless 已经提交过的文本没法再改，只能靠用户自己发现说”哎，名字拼错了”。而 AgenticASR 在用户说完第一句时就先吐出一版干净转录，等听到后面的纠正后，直接把之前吐出的”Mary”原地替换成”Marie”。</p> <hr/> <h2 id="3-架构现成的-asr-前端加一个专门翻译口语的小模型">3. 架构：现成的 ASR 前端，加一个专门”翻译口语”的小模型</h2> <p>AgenticASR 的设计思路很直接：<strong>不去动 ASR 模型本身，在它后面接一个专门做口语转书面语的小模型</strong>，论文里管这个小模型叫 Refiner。整体架构如下：</p> <p><img src="/assets/img/posts/20260820-agenticasr/figure2_full.png" alt="AgenticASR 系统架构"/></p> <p>右半部分（b）是推理时的工作流程，分 offline 和 online 两种模式：</p> <ul> <li><strong>Offline 模式</strong>：等一段完整音频识别完，ASR 前端产出完整的口语版转录 $H$，Refiner 只调用一次，把整段 $H$ 转成干净文本 $Y$。</li> <li><strong>Online 模式</strong>：这是论文的重点。用 VAD（voice activity detection，检测语音里的停顿和句子边界）配合一个 Chunk Manager，把持续到来的 ASR 文本切成一个个 chunk（每个 chunk 最多 80 个字符，在标点处断开）。每次更新时，Chunk Manager 取出”当前 chunk 加上前面 $K-1$ 个 chunk”（默认 $K=3$）拼在一起喂给 Refiner，Refiner 吐出一份干净文本，<strong>替换掉这个窗口原来对应的旧输出</strong>。等下一个 chunk 到来，窗口往后滑一格，重复这个过程。</li> </ul> <p>这样设计有两个好处：Refiner 每次只处理一个有限长度的窗口，不需要每次都从头重算完整历史；而且因为窗口是滑动的，后面新到的语音有机会”回头”修正前面最多 $K-1$ 个 chunk 之内的内容——这正是图 1 里”Mary”变成”Marie”的机制来源。</p> <p>左半部分（a）是训练数据怎么来的问题：现实里没有大量”口语转录—书面转录”的配对语料可以直接拿来微调，所以作者搭了一条五阶段的合成 pipeline，全程用 Gemma-4-31B-IT 生成和质检：先按场景生成候选实体、术语池（seed generation）；基于这些 seed 生成带口语现象的 Oral 文本（比如插入填充词、重复、多阶段自我修正）；把 Oral 转写成对应的 Clean 书面版本作为训练目标；再故意在 20% 的样本里模拟 ASR 识别错误（词漏识别、截断、误判），剩下 80% 保持不变；最后用 LLM 筛掉不忠实的样本，并用文本 3-gram Jaccard 相似度（阈值 0.75）去重。这套流程一共产出了 10 万条训练对，85% 用来训练、15% 用来验证，把 Refiner 从 MiniCPM-5-1B 微调了 5 个 epoch。默认的 ASR 前端是 Qwen3-ASR-1.7B。</p> <hr/> <h2 id="4-aasr-bench为什么不能直接拿-wer-来评这类任务">4. AASR-Bench：为什么不能直接拿 WER 来评这类任务</h2> <p>评测这类任务本身是个问题。传统的 WER / CER / MER（word / character / match error rate，字错率、词错率，都是逐字比对识别结果和标准答案差多少，数值越低越好）只会比较逐字匹配度，没法区分”格式变了但语义对”和”语义真的错了”这两种情况——比如把口语的”三点二五”写成书面的”3.25”，WER 会把这个当成一次完整的替换错误来算，但这恰恰是我们想要的行为。</p> <p>所以作者搭了 <strong>AASR-Bench</strong>，一个专门给 AgenticSR 用的双语（中英文）基准。它包含 917 条 utterance（510 条中文 + 407 条英文，共 4.218 小时音频），覆盖 10 个使用场景（日常聊天、客服、口述录入、解释澄清、会议、导航、技术讨论、编程口述、语音搜索、学术场景），每条样本配一组”原子级”的判断题，一共 6637 道，平均每条 7.24 道。判断题分四个维度：Content（该保留的语义内容有没有保留）、Format（数字日期符号有没有规范化）、Filter（填充词重复有没有清干净）、Rephrase（自我修正、多阶段修订、解释澄清有没有处理对），每道题由 Gemma-4-31B-IT 当裁判打分。</p> <p>这套裁判打分靠不靠谱？作者拉了两位独立的领域专家做双盲评估，跟裁判的判断对比，Spearman 相关系数在 0.6B 和 1.7B 前端上分别是 0.8222 和 0.8064，加权 kappa 分别是 0.8313 和 0.7918——一致性相当高，说明这套基于 rubric 的评测框架站得住。</p> <hr/> <h2 id="5-效果大模型后处理慢一个量级小-refiner-更快也更准">5. 效果：大模型后处理慢一个量级，小 Refiner 更快也更准</h2> <p>主要结果对比了两类 baseline：一类是把完整识别结果扔给一个大模型 API 做后处理（比如 Qwen3.5-Flash、Gemini-2.5-Flash），一类是 FormalASR——直接对 ASR 模型做微调，让它自己输出干净文字，不需要额外的 LM。</p> <p><img src="/assets/img/posts/20260820-agenticasr/table3_full.png" alt="主要结果对比"/></p> <p>用 Qwen3-ASR-1.7B 当 ASR 前端时，AgenticASR 拿到 79.95 的 Overall 分数，大幅超过 Qwen3.5-Flash 后处理的 69.93 和 FormalASR 的 52.50，在四个维度都是最好——尤其是 Format（65.19 对 19.69）和 Rephrase（72.83 对 15.70），这两项 FormalASR 差距最明显，说明单纯让 ASR 模型自己学着输出干净文字，处理复杂的格式化和自我修正逻辑力有不逮，还是需要一个专门的转换步骤。</p> <p>更值得注意的是延迟那一列：Qwen3.5-Flash 做后处理平均要 60.89 秒，而 AgenticASR 只要 9.59 秒，差了六倍多。这不是说 AgenticASR 的算法效率有多离谱，更本质的原因是问题被拆小了——大模型 API 是对着完整转录文本做一次性、无状态的重写；AgenticASR 用一个专门训练过的 1B 级别小 Refiner，每次只处理局部的、有限长度的窗口。</p> <p>把结果拆到 10 个场景看：</p> <p><img src="/assets/img/posts/20260820-agenticasr/figure3_full.png" alt="各场景细分表现"/></p> <p>配 Qwen3-ASR-1.7B 时，AgenticASR 在全部 10 个场景加 pass-through 控制组里都领先两个 baseline；换成 0.6B 前端时 9 个场景领先，唯一输的是 Tech（技术讨论）场景，Qwen3.5-Flash 后处理反而高了将近 2 分——技术类内容里专业术语密度高，直接靠大模型的世界知识兜底可能更稳。</p> <p>作者还换了 Whisper 系列（Base / Small / Large）当 ASR 前端做泛化验证，趋势一致：换成 Whisper Large 时，AgenticASR 的 Overall 从 62.90（Gemini-2.5-Flash 后处理）提升到 70.29，延迟只要 4.42 秒，是 Gemini 后处理（8.04 秒）的一半左右。但也有一个诚实的局限——用最弱的 Whisper Base 当前端时，AgenticASR 的 Content 分数反而比 Gemini-2.5-Flash 低了 8.35 分，说明 Refiner 再强，也补不齐 ASR 前端本身漏掉的语义内容，这是这套两阶段架构绕不开的天花板。</p> <hr/> <h2 id="6-消融refiner-多大够用窗口开多大才能追上-offline">6. 消融：Refiner 多大够用？窗口开多大才能追上 offline？</h2> <p><strong>Refiner 容量的影响。</strong> 固定 Qwen3-ASR-1.7B 当前端，换三种 Refiner 规格对比：</p> <p><img src="/assets/img/posts/20260820-agenticasr/table5_full.png" alt="Refiner 容量对比"/></p> <p>分数随参数量单调上升——Qwen2.5-0.5B-Instruct 78.76 分，默认用的 MiniCPM-5-1B 79.95 分，Qwen2.5-4B-Instruct 83.42 分——但延迟只从 9.21 秒涨到 10.77 秒，涨幅不到 20%。说明 Refiner 这一层的计算量相对 ASR 主干本身不算重，拿更大的 Refiner 换效果在延迟上是划算的。</p> <p><strong>窗口大小的影响，这是最有意思的一组消融：</strong></p> <p><img src="/assets/img/posts/20260820-agenticasr/table6_full.png" alt="在线窗口大小对比"/></p> <p>Offline 模式（等完整识别完再 refine 一次）的 Rephrase 分数是 72.83、Explanation 场景分数是 75.20。切换成 online 流式模式后，窗口只留 1 个 chunk 时，这两个分数直接崩到 36.17 和 19.43；窗口开到 2 个 chunk，回升到 65.08 和 55.06；开到 3 个 chunk（论文的默认设置），几乎追平 offline 水平——70.47 和 74.00。</p> <p>为什么窗口大小影响这么大？下图给了一个具体例子：用户先说”我的火车从北京南站出发”，接着自我修正”不对，是北京西站”，最后又改口”应该是北京朝阳站”。</p> <p><img src="/assets/img/posts/20260820-agenticasr/figure4_full.png" alt="窗口大小对在线修正的影响"/></p> <p>如果窗口只有 1 个 chunk，Refiner 每次只能看到当前这一小段，前两次修正各自被当成独立片段处理，结果转录里同时留着”南站”和”西站”两个已经被放弃的地名。窗口开到 2 个 chunk，能把最后一次修正和紧挨着它的上一个候选连起来、删掉那个候选，但最早提到的”南站”已经滑出窗口之外，救不回来。只有窗口开到 3 个 chunk，才能把整段修正序列完整覆盖，一边保留没被修正过的”出发时间”，一边把所有被放弃的地名统一替换成最终的”朝阳站”。</p> <p>这个例子说明，online revision 不是”看到新证据就无脑覆盖上一段”这么简单——修正的证据链条可能跨越好几个语音片段，窗口开小了，系统会陷入”知道错了但改不回去”的状态。</p> <hr/> <h2 id="7-一些冷静的讨论">7. 一些冷静的讨论</h2> <p>这篇工作最实在的地方，是没有去卷 ASR 模型本身的准确率，而是把”清理口语内容”这件事拆成一个可以流式处理、可以回头修正的独立任务，用一个专门训练的小 Refiner 配合滑动窗口来做。但几个地方值得留意：</p> <ol> <li><strong>两阶段架构有天花板。</strong> Whisper Base 那组结果已经说明，Refiner 再强也补不齐 ASR 前端漏掉的语义内容——这套架构本质上是”锦上添花”，不是”点石成金”。</li> <li><strong>固定 $K=3$ 窗口是个经验选择。</strong> 如果修正链条跨度超过 3 个 chunk（比如同一个地名被改了四五次），现在的机制依然会漏，论文没有讨论自适应窗口大小的方案。</li> <li><strong>60 秒级别的 API 延迟对比稍显夸张。</strong> 这更像是在对比”专门为这个任务训练过的小模型”和”没针对这个任务优化、拿现成大模型硬跑完整重写”，两者不是同一计算量级的公平对比，但确实反映了一个真实的工程选择：要不要为一个高频子任务单独训练一个小模型。</li> <li><strong>评测本身依赖 LLM 当裁判。</strong> rubric 由 Qwen3.7-Plus 生成，打分由 Gemma-4-31B-IT 完成，尽管做了人类一致性验证，但 rubric 的覆盖面和潜在偏见终究取决于生成它的那个 LLM。</li> </ol> <p>顺带扯句题外话：AgenticASR 做的事情严格说属于语音识别和实时交互系统，书里没有直接讲 ASR 或者流式修正这块。不过它”用一个专门训练过的小模型处理局部窗口，取代对着全量文本重跑一次大模型”的设计思路，跟我们在《动手学 AutoML：从 NAS 到大语言模型优化实战》里讨论 LLM 压缩、模型融合时反复出现的取舍是一致的——很多时候提速不是靠更聪明的算法，而是靠把一个大任务拆成可以用小模型 cover 的局部子任务。</p> <blockquote> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="ASR"/><category term="语音识别"/><category term="论文解读"/><summary type="html"><![CDATA[AgenticASR：语音识别不用等你说完一整句话，就能边听边改]]></summary></entry><entry><title type="html">arXiv’26 | APEX：端侧 MoE 的专家预取，别再 top-k 一刀切了</title><link href="https://marsggbo.github.io/blog/2026/20260816-apex/" rel="alternate" type="text/html" title="arXiv’26 | APEX：端侧 MoE 的专家预取，别再 top-k 一刀切了"/><published>2026-08-16T00:00:00+00:00</published><updated>2026-08-16T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260816-apex</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260816-apex/"><![CDATA[<h1 id="arxiv26--apex端侧-moe-的专家预取别再-top-k-一刀切了">arXiv’26 | APEX：端侧 MoE 的专家预取，别再 top-k 一刀切了</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2608.11688">APEX: Adaptive Expert Prefetching for Edge MoE Inference</a></p> </blockquote> <hr/> <h2 id="1-前言先把背景交代清楚">1. 前言：先把背景交代清楚</h2> <p>你有没有想过这样一个问题：<strong>MoE 模型号称”稀疏激活、天生适合端侧”，为什么真放到手机/边缘盒子上，还是慢得像老牛拉车？</strong></p> <p>先说清楚 MoE 的工作方式，这是理解全文的前提。每个 token 进到 MoE 层，router（一个很小的分类头）从 N 个专家里挑出 top-k 个（比如 Granite-3B 是 40 选 8），只有这 k 个专家的 FFN 参与计算——<strong>这就是”稀疏激活”：计算量只跟 k 走，跟 N 无关</strong>。所以一个 3B 总参的 MoE，每 token 实际只动 800M 参数。</p> <p>听起来是端侧的完美配方？坏就坏在<strong>“激活稀疏”不等于”加载稀疏”</strong>。端侧芯片的片上内存（SRAM/封装内存）只有几个 GB，装不下全部专家权重，绝大多数专家只能躺在片外的 LPDDR5X 里。于是每来一个 token，流程是：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>route（挑专家）→ load（从 LPDDR5X 搬权重）→ execute（算）
                    ↑
              这一段是纯等待，计算阵列全程闲置
</code></pre></div></div> <p>论文在 Granite-3.1-3B-A800M（1024 token 上下文）上量了这笔账：<strong>专家加载占延迟的 43%、总能耗的 29%</strong>。搬权重的时间比算权重的时间还长，而且计算阵列闲置期间还在烧静态功耗——花钱买了个电暖气。</p> <p><img src="/assets/img/posts/20260816-apex/figure1_p1_01.png" alt="端侧 MoE 的延迟与能耗拆解"/></p> <p>标准解法是<strong>预取（prefetching）</strong>：趁当前层还在算，提前把下一层要用的专家搬进来，把 load 藏进 execute 里。这条线我之前写过一篇：<a href="https://marsggbo.github.io/blog/2026/20260722-expert-prefetch/">《ST-MoE expert-prefetch》</a>——它的核心洞察是 <strong>expert 激活不是随机的，有时空相关性</strong>（相邻 token、相邻层倾向复用同一批专家），所以可以拿历史激活模式预测下一层要哪些专家，提前搬上片。另一篇 <a href="https://marsggbo.github.io/blog/2026/20260722-smoe/">《SMoE》</a>走的是完全不同的路：专家不在 GPU 上就别死等，<strong>找个”平替”专家直接算</strong>——用一点精度换完全不 stall。</p> <p>今天这篇 APEX（arXiv 2608.11688）要指出的是：<strong>现有预取的”固定 top-k”策略，在端侧是一个两头堵的死局</strong>，而它的答案是让<strong>预取预算本身随 token 自适应</strong>。在模拟的高端端侧平台上：重叠准确率保住 97–98%（ProMoE 会掉到 79%），延迟比无预取低 42%，能耗比固定超采低 21.8%，EDP 最高改善 41%。</p> <hr/> <h2 id="2-病根平均准确率是个谎言">2. 病根：平均准确率是个谎言</h2> <p>固定 top-k 预取的代表是 ProMoE：预取时多搬 δ 个”预测最可能的”专家。它的问题是 <strong>δ 是个全局常数</strong>：</p> <ul> <li>δ 给小了（比如 +2）：难 token 的预测 miss，router 要的专家不在片上，照样 stall；</li> <li>δ 给大了（比如 +8）：简单 token 白搬一堆用不上的专家，能耗爆炸。</li> </ul> <p>你可能会问：预测准确率 70–85%（ProMoE 的水平）听起来也不差啊？<strong>这里有个很隐蔽的度量陷阱</strong>。论文强调 overlap 必须按 <strong>per-layer、per-token</strong> 算：每个 token 的每一层都要求”router 选的 k 个专家 ⊆ 预取集合”才算成功。为什么这么苛刻？因为平均数会骗人——k=4 时哪怕每层平均重叠 75%（4 个里对 3 个），<strong>每个 token 每层都缺一个专家，每个 token 都 stall</strong>。串行系统里，99 层完美 + 1 层 miss = 全部白干，这一点我在<a href="https://marsggbo.github.io/blog/2026/kvcomm/">《KVCOMM》</a>那篇讲依赖链的时候就提过：<strong>流水线的吞吐由最慢的一环决定，不由平均环决定</strong>。</p> <p>而 token 之间的”预测难度”差异极大——论文 profiling 发现：<strong>大多数 token 只要 δ=2 或 δ=4 的小额预算就能全覆盖，少数难 token 才需要大预算</strong>。这个长尾分布就是”固定 δ 必然两头堵”的实证根据，也是自适应方案的立足点。</p> <hr/> <h2 id="3-平台先把端侧设备说清楚">3. 平台：先把”端侧设备”说清楚</h2> <p>这是篇<strong>算法-硬件协同设计</strong>的论文，评估平台是 CHIPSIM 综合校准联合仿真器（不是真机）。读这类论文第一步永远是搞清楚平台假设，这里的目标设备定位在”近端旗舰边缘加速器”（Jetson Thor / Hailo-10H 一档），chiplet 架构：</p> <p><strong>计算 chiplet</strong>：</p> <ul> <li>4 个 vector processing array，每个是 16×16 的 vector unit 阵列、vector length 32，BF16 matmul @ 750MHz → 峰值 <strong>24 TFLOPS</strong>；</li> <li>每阵列 2MB SRAM（共 8MB）+ 32 个 SFU 处理非线性；</li> <li>阵列是 <strong>RTL 级实现、TSMC 28nm 综合、Synopsys PrimeTime 功耗表征</strong>——不是拍脑袋的性能模型。</li> </ul> <p><strong>内存层级</strong>（这是全文的关键变量）：</p> <ul> <li>封装内：HBM3-class <strong>819 GB/s</strong>（7 pJ/bit）——放共享的热数据；</li> <li>片外：专家权重在 LPDDR5X（3 pJ/bit），走 PCIe 6.0 ×16、<strong>256 GB/s</strong>（5 pJ/bit）；</li> <li>片间 UCIe 0.5 pJ/bit。</li> </ul> <p>预取按<strong>真实 DMA 建模</strong>——受带宽竞争和排队延迟影响，不是理想化的”并行搬运不要钱”。内存用 Ramulator 2.0 建模，整体是 cycle-accurate 的联合仿真。这个严谨度比”在 A100 上模拟手机”高一档。</p> <hr/> <h2 id="4-方法一个会看人下菜的预取路由器">4. 方法：一个会看人下菜的预取路由器</h2> <p>APEX 的 pipeline 把 <code class="language-plaintext highlighter-rouge">route → load → execute</code> 改成 <code class="language-plaintext highlighter-rouge">predict → prefetch → execute</code>，由三个组件构成。</p> <h3 id="41-prefetch-router预测本层专家">4.1 Prefetch Router：预测本层专家</h3> <p>一个线性层 + softmax，从冻结的原 router 蒸馏（KL loss）。有两个设计决策值得划重点：</p> <p><strong>（1）它放在本层 attention 之前</strong>，用当前层的 hidden state 预测<strong>本层 FFN</strong> 将要路由的专家。这和 Pre-gated MoE 的”用第 L 层预测第 L+1 层”不同——APEX 的理由是同层内的 hidden state 和最终路由决策相关性最强（跨层预测等于又叠了一层不确定性）。而且预取发起得早，DMA 传输可以藏进 attention 的计算时间里。</p> <p><strong>（2）原 router 保留，依然是最终决策者</strong>。prefetch router 只负责内存调度（搬谁），不参与计算路由（用谁）——这样预测错了最多是多搬/漏搬，<strong>不会改变模型的计算语义</strong>。这一点和 Pre-gated MoE 直接用预测结果做路由的激进路线形成对比。</p> <p>蒸馏 loss 就是标准 KL：</p> \[\mathcal{L}_\text{KL} = \sum_i q_r(i) \log \frac{q_r(i)}{q_p(i)}\] <p>$q_r$ 是原 router 的专家分布，$q_p$ 是 prefetch router 的，梯度只更新后者，<strong>基座模型完全不动</strong>。</p> <h3 id="42-自适应预算-δ̂x这篇的灵魂">4.2 自适应预算 δ̂(x)：这篇的灵魂</h3> <p>固定 δ 的替代方案：给每个 token 实时决定”多取几个”。先定义 oracle——<strong>δ*</strong>（这个 token 的真实难度）：</p> \[\delta^* = \min\{\delta \in \{0, \dots, N-k\} : \mathcal{K}_r \subseteq \mathcal{K}_p^{(\delta)}\}\] <p>即”让 router 真正选的 k 个专家全部落进 top-(k+δ) 预取列表”所需的最小额外预算。δ*=0 表示预测全对，δ* 大表示这个 token 难猜。</p> <p>然后训一个<strong>序数 logistic CDF 模型</strong>来预测 δ* 的分布：</p> \[p_\delta(x) = \Pr(\delta \geq \delta^* \mid x) = \sigma(\theta_\delta - w^\top x)\] <p>$x$ 是该层 attention 之前的 token 表征，$w$ 是一个权重向量（就这么简单，一个线性打分器），$\theta_0 \leq \theta_1 \leq \dots \leq \theta_{N-k}$ 是有序阈值。推理时的规则：</p> \[\hat{\delta}(x) = \min\{\delta : p_\delta(x) \geq \tau\}\] <p><strong>“取最小的 δ 使覆盖率有 τ 的把握”</strong>——τ=0.90 是默认值。有把握的 token 只多取一两个，没把握的 token 才大手笔超采，预算精确花在长尾上。</p> <p>训练分两阶段（都是离线的，且训一次全任务通用）：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Stage 1: KL 蒸馏 prefetch router（见上式）
# Stage 2: 用累积二元交叉熵拟合 CDF
</span><span class="k">def</span> <span class="nf">cdf_loss</span><span class="p">(</span><span class="n">theta</span><span class="p">,</span> <span class="n">w</span><span class="p">,</span> <span class="n">x</span><span class="p">,</span> <span class="n">delta_star</span><span class="p">):</span>
    <span class="c1"># delta_star: profiling 得到的 oracle 标签
</span>    <span class="c1"># 对每个候选预算 delta，目标是 1[delta &gt;= delta_star]
</span>    <span class="n">loss</span> <span class="o">=</span> <span class="mi">0</span>
    <span class="k">for</span> <span class="n">delta</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">N</span> <span class="o">-</span> <span class="n">k</span> <span class="o">+</span> <span class="mi">1</span><span class="p">):</span>
        <span class="n">p</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">sigmoid</span><span class="p">(</span><span class="n">theta</span><span class="p">[</span><span class="n">delta</span><span class="p">]</span> <span class="o">-</span> <span class="n">w</span> <span class="o">@</span> <span class="n">x</span><span class="p">)</span>        <span class="c1"># P(delta* &lt;= delta)
</span>        <span class="n">target</span> <span class="o">=</span> <span class="mf">1.0</span> <span class="k">if</span> <span class="n">delta</span> <span class="o">&gt;=</span> <span class="n">delta_star</span> <span class="k">else</span> <span class="mf">0.0</span>
        <span class="n">loss</span> <span class="o">+=</span> <span class="n">F</span><span class="p">.</span><span class="nf">binary_cross_entropy</span><span class="p">(</span><span class="n">p</span><span class="p">,</span> <span class="n">torch</span><span class="p">.</span><span class="nf">tensor</span><span class="p">(</span><span class="n">target</span><span class="p">))</span>
    <span class="k">return</span> <span class="n">loss</span>
</code></pre></div></div> <p>训练配置很轻：WikiText 上 lr 5e-4、batch 8、序列长 1024、1000 步，<strong>每个模型 10 分钟到 1 小时</strong>。整个 APEX 新增参数 0.79M–34.11M，只占模型参数的 <strong>0.022%–0.060%</strong>，推理性能开销 &lt;0.051%。</p> <h3 id="43-两种执行模式语义严格-vs-绝不等待">4.3 两种执行模式：语义严格 vs 绝不等待</h3> <p>预取 miss 了怎么办？两种模式：</p> <p><strong>Correctness-preserving（默认）</strong>：router 要的专家有缺失时，<strong>先把已就绪的专家算起来，缺失的异步补取</strong>，聚合前补齐——最终聚合用的是完整的原始专家集合，<strong>语义与原 MoE 严格一致</strong>，代价只是没藏住的纠错延迟。</p> <p><strong>Stall-free（可选）</strong>：绝不等待。缺失的专家直接用”已预取集合里、<strong>原 router softmax 分数最高的候选</strong>“顶替——这就是 SMoE 那篇”平替”思想的端侧版。Algorithm 1 的逻辑：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">stall_free_select</span><span class="p">(</span><span class="n">K_routed</span><span class="p">,</span> <span class="n">q_r</span><span class="p">,</span> <span class="n">K_prefetched</span><span class="p">):</span>
    <span class="c1"># K_routed: 原 router 选的 k 个专家; q_r: 原 router 的 logits
</span>    <span class="c1"># K_prefetched: 按 delta_hat(x) 预取的 k+delta 个专家
</span>    <span class="n">alpha</span> <span class="o">=</span> <span class="nf">softmax</span><span class="p">(</span><span class="n">q_r</span><span class="p">)</span>                          <span class="c1"># 用原 router 的权重当替补排序依据
</span>    <span class="k">if</span> <span class="n">K_routed</span> <span class="o">&lt;=</span> <span class="n">K_prefetched</span><span class="p">:</span>                  <span class="c1"># 全命中，直接用
</span>        <span class="k">return</span> <span class="n">K_routed</span>
    <span class="n">keep</span>  <span class="o">=</span> <span class="n">K_routed</span> <span class="o">&amp;</span> <span class="n">K_prefetched</span>               <span class="c1"># 预测对的保留
</span>    <span class="n">missing</span> <span class="o">=</span> <span class="n">K_routed</span> <span class="o">-</span> <span class="n">K_prefetched</span>             <span class="c1"># 缺失的
</span>    <span class="n">subs</span>  <span class="o">=</span> <span class="nf">top_n_by_weight</span><span class="p">(</span><span class="n">K_prefetched</span> <span class="o">-</span> <span class="n">keep</span><span class="p">,</span> <span class="n">alpha</span><span class="p">,</span> <span class="n">n</span><span class="o">=</span><span class="nf">len</span><span class="p">(</span><span class="n">missing</span><span class="p">))</span>
    <span class="k">return</span> <span class="n">keep</span> <span class="o">|</span> <span class="n">subs</span>                            <span class="c1"># 用预取池里分数最高的顶上
</span></code></pre></div></div> <p>注意替补的排序依据是<strong>原 router</strong> 的分数（不是 prefetch router 的）——原 router 才知道”哪些专家真的合适”，prefetch router 只管”搬谁”。</p> <hr/> <h2 id="5-效果">5. 效果</h2> <h3 id="51-重叠准确率自适应的碾压局">5.1 重叠准确率：自适应的碾压局</h3> <p>τ=0.90 下，APEX 在<strong>所有层</strong>保持 <strong>&gt;97–98%</strong> 的专家重叠率；ProMoE 在 Granite-1B 的第 5 层会掉到 <strong>79%</strong>。平均每个 token 额外预取的专家数：Granite-1B 4.17、Granite-3B 2.86、Phi-7B 0.67、DeepSeek-V2-Lite 1.98——<strong>模型越”好预测”，预算自动越小</strong>，这就是自适应的意义（Phi 只有 16 个专家、路由集中，0.67 个额外预算就够 98%）。</p> <p>τ 的扫描直接展示”把握-预算”的交换关系：Granite-1B 上 τ 从 0.60→0.90→0.97，重叠率 93.4%→98.2%→99.4%，平均 δ̂ 从 1.36→4.17→6.70——<strong>想要多稳，明码标价</strong>。</p> <p><img src="/assets/img/posts/20260816-apex/figure9_p9.png" alt="重叠准确率随预算的变化"/></p> <h3 id="52-延迟与能耗预取生意的本质">5.2 延迟与能耗：预取生意的本质</h3> <p>Granite-3B、512 token、correctness-preserving 模式：</p> <ul> <li>延迟 <strong>11.41ms</strong>：比无预取（19.77ms）<strong>低 42%</strong>，比 ProMoE（15.39ms）低 26%，比固定超采 (k+4)/(k+8) 分别低 20%/40%；</li> <li>能耗 <strong>287.3mJ</strong>：比无预取低 9.5%、比 ProMoE 低 5.8%；<strong>固定 k+8 超采最惨（比 APEX 多 21.8%）</strong>——固定预算省下的延迟全在能耗上还回去了；</li> <li>Stall-free 再加 2.0–2.8% 的延迟改善，聊胜于无。</li> </ul> <p><img src="/assets/img/posts/20260816-apex/figure12_p11_18.png" alt="延迟与能耗对比"/></p> <p>能耗的拆解（1024 context）最能说明”预取生意”的本质：开 APEX 后，片外 I/O 从 48mJ 涨到 56mJ（多搬了 8mJ），但计算阵列的<strong>闲置漏电从 57mJ 崩到 8mJ</strong>（少烧 49mJ）——<strong>用 8mJ 的搬运换 49mJ 的干等</strong>，净赚 41mJ。这就是所有预取技术的账本：带宽是便宜的，闲置是昂贵的。</p> <h3 id="53-鲁棒性与精度代价">5.3 鲁棒性与精度代价</h3> <ul> <li><strong>EDP</strong>（延迟×能量积）：相对无预取改善 36–49%、相对 ProMoE 改善 16–30%（视模型而定），Phi-7B 上 stall-free 的增益尤其大（再 +14/8/7%）——因为它 k=2，miss 一个就废一个，绝不等候的价值最大；</li> <li><strong>带宽扫描</strong>：32–1024 GB/s 全区间延迟收益 14–42% 成立——带宽越紧张，预取越值钱；</li> <li><strong>精度扫描</strong>：4/8/16/32-bit 专家权重下收益都在——<strong>和量化正交可叠加</strong>（量化缩体积、APEX 缩次数）；</li> <li><strong>Stall-free 的精度代价</strong>（明码标价）：Granite-1B PPL 7.88→7.99、平均分 43.3→42.8；DeepSeek-V2-Lite PPL 7.02→7.10。<strong>Phi-7B 掉得明显（64.6→60.4）</strong>——16 选 2 的模型每个专家都是命根子，替代一个就伤筋动骨。论文因此建议 k 小的模型关掉 stall-free，用 correctness-preserving——这个诚实的边界标注很加分。</li> </ul> <hr/> <h2 id="6-我的-take">6. 我的 Take</h2> <p><strong>一，预取问题的正确度量是尾部覆盖率，不是平均准确率。</strong> ProMoE 的 80% 平均准确率听起来能打，但任何一层的 miss 都会把前面所有层的完美预取全部白干。”平均”在串行依赖链上是个没有意义的统计量——这个认识对做流水线/调度系统的同学是通用的。APEX 用一个 ordinal logistic CDF 把”这个 token 需要多少预算”建模出来，本质上是把<strong>置信度校准</strong>这套工具用到了内存搬运调度上，这个迁移本身就很漂亮。</p> <p><strong>二，端侧 MoE 的解题空间基本铺开了，三个角各站一篇。</strong> 预测得准（ST-MoE 时空相关性）、取得巧（APEX 自适应预算）、算得动（SMoE 平替）——三者还是正交的：APEX 的 prefetch router 可以吃任何更好的预测器，stall-free 的替补池也可以换成 SMoE 式的更聪明的替代选择。往后端侧 MoE 的论文大概率是在这三个角之间做组合。</p> <p><strong>三，对”仿真论文”的一点辩护。</strong> 这篇没有真机，但 RTL 综合 + PrimeTime 功耗 + Ramulator 内存 + DMA 排队的建模链，比多数”在服务器 GPU 上假装端侧”的论文可信得多。硬件协同设计类工作本来就该先在仿真里把设计空间扫清楚——当然，上真机验证仍是它（和所有同类工作）欠社区的一步。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 MoE 推理优化你想系统深入，可以看看我之前出版的<a href="https://item.jd.com/14945889.html">《动手学AutoML：从 NAS 到大语言模型优化实战》</a>，书里讲 NAS 在大模型上的延伸时正好覆盖稀疏模型结构搜索这条线，和 MoE 效率优化是同一套设计思路。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="MoE"/><category term="端侧推理"/><category term="推理优化"/><category term="论文解读"/><summary type="html"><![CDATA[arXiv’26 | APEX：端侧 MoE 的专家预取，别再 top-k 一刀切了]]></summary></entry><entry><title type="html">arXiv’26 | EcoSpec：MoE 投机解码别只顾接受率，草稿会把专家撒得到处都是</title><link href="https://marsggbo.github.io/blog/2026/20260816-ecospec/" rel="alternate" type="text/html" title="arXiv’26 | EcoSpec：MoE 投机解码别只顾接受率，草稿会把专家撒得到处都是"/><published>2026-08-16T00:00:00+00:00</published><updated>2026-08-16T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260816-ecospec</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260816-ecospec/"><![CDATA[<h1 id="arxiv26--ecospecmoe-投机解码别只顾接受率草稿会把专家撒得到处都是">arXiv’26 | EcoSpec：MoE 投机解码别只顾接受率，草稿会把专家撒得到处都是</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2607.12696">Less Experts, Faster Decoding: Cost-Aware Speculative Decoding for Mixture-of-Experts</a></p> </blockquote> <hr/> <h2 id="1-前言先把背景交代清楚">1. 前言：先把背景交代清楚</h2> <p>你有没有想过这样一个问题：<strong>在 MoE 模型上做投机解码，”验收”到底要付多少钱？</strong></p> <p>先把投机解码的账本摆出来。每轮解码：drafter 打出 γ 个草稿 token，target 模型一次并行 forward 验收，验收通过的直接收下。标准叙事里这笔账很好算：target 的验收是 prefill 式的批量计算，本来就要跑一遍，多算几个 token 的边际成本很低——所以只要草稿接受率够高就稳赚。<a href="https://marsggbo.github.io/blog/2026/20260814-dspark/">DSpark</a>（DeepSeek 线上方案：半自回归草稿 + 置信度决定每轮验收多长）优化的就是”验收预算怎么花”这个问题。</p> <p>但这个”边际成本很低”的前提，在 <strong>MoE</strong> 上悄悄变质了。原因一句话：<strong>MoE 层的计算成本不由 token 数决定，而由这批 token 激活的专家并集决定。</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Dense 模型验收 γ 个 token：多算几个 token ≈ 多一点 FLOPs，几乎免费
MoE  模型验收 γ 个 token：每个 token 各自路由 top-k 专家，
                          需要加载的权重 = 这 γ+1 组专家的并集
</code></pre></div></div> <p>如果这 γ 个 token 路由高度重叠（都走同样 8 个专家），验收确实只多花一点 FLOPs；如果互不相交，专家加载量直接 ×γ。而坏消息是：<strong>按”接受概率最高”选出来的草稿 token，天然往第二种情况跑偏</strong>——概率高的候选分布在语义空间的不同区域，路由自然分散。论文给这个现象起了名字：<strong>expert scattering（专家散射）</strong>——草稿在概率空间里抱团，在专家空间里四散开花。</p> <p>量化一下有多疼：EAGLE-3 在 Qwen3-235B 上，每个验收步平均激活 <strong>23.7 个专家/层</strong>（AR 解码 top-8 只需 8 个，3 倍带宽）；DeepSeek-V3.1 配 MTP 更是 31.4 个。一个 DeepSeek 专家（FP8）就是 <strong>44MB</strong> 的 HBM 流量——在 61 层 MoE 上，<strong>每层每步只要多”撒”出 0.2 个专家，一轮投机解码就多烧约 0.5GB 的带宽</strong>。</p> <p>EcoSpec（arXiv 2607.12696）做的事一句话讲完：<strong>在选草稿的时候，把”会激活哪些专家”算进成本函数</strong>——验收规则一个字不动（严格无损），在 DeepSeek-V3.1（671B）、Qwen3-235B、GPT-OSS-120B 上端到端最高 <strong>1.62×</strong> 加速。</p> <p>这条问题和两篇旧文有直接的血缘：<a href="https://marsggbo.github.io/blog/2026/20260722-expert-prefetch/">《ST-MoE expert-prefetch》</a>的洞察是<strong>expert 激活有很强的时空相关性、可预测</strong>——那篇用它做”提前把专家搬上片”的预取，这篇用它做”选草稿时避开冷门专家”的调度，同一个洞察的两种变现；而”MoE 推理被内存带宽卡死”这个大前提，是 <a href="https://marsggbo.github.io/blog/2026/20260722-smoe/">《SMoE》</a>那篇的主线——expert 权重搬不动，才是 MoE 部署的核心矛盾，本文不过是把这个矛盾推进到了投机解码场景。</p> <hr/> <h2 id="2-病根验收成本--专家并集而草稿选择对并集一无所知">2. 病根：验收成本 = 专家并集，而草稿选择对并集一无所知</h2> <p>把这笔账算细一点。投机解码每轮的流程：</p> <ol> <li>drafter 打出一棵草稿树（EAGLE-3：3 步 forward、每步留 top-2，长出候选树）；</li> <li>从树里选 γ 个节点送去 target 验收（γ=4 是常用值）；</li> <li>target 一次 forward 验收这 γ+1 个 token。</li> </ol> <p>第 3 步在 MoE 层 $\ell$ 上需要加载的专家集合：</p> \[\mathcal{E}_\text{verify}^{\ell} = \bigcup_{t \in S} \mathcal{S}_\ell(x_t)\] <table> <tbody> <tr> <td>$S$ 是被验收的 token 集合，$\mathcal{S}_\ell(x_t)$ 是 token $t$ 在第 $\ell$ 层路由到的专家。**现有多数工作的草稿选择只优化 $</td> <td>S</td> <td>$ 里被接受的期望 token 数，对 $\mathcal{E}_\text{verify}$ 的大小完全无感知。**</td> </tr> </tbody> </table> <p>更糟的是 batch 场景。论文扫了 batch size：<strong>batch=4 时 EAGLE-3 在 Qwen3-235B 上的加速直接掉到 1.00×</strong>（不赚不赔），因为每步的专家足迹膨胀到 54.9 个/层，撞上 HBM 带宽墙——验收慢到把投机赚的 时间 全吐回去。<strong>带宽是 MoE 投机解码的隐形天花板，而现有方法连这个天花板的存在都没意识到。</strong></p> <p><img src="/assets/img/posts/20260816-ecospec/figure1_p2.png" alt="MoE 投机解码的带宽瓶颈"/></p> <hr/> <h2 id="3-方法把专家成本写进草稿选择">3. 方法：把专家成本写进草稿选择</h2> <p>EcoSpec 在 drafter 和验收之间插了三个组件，<strong>target 的验证规则原封不动</strong>——所以输出分布和标准投机解码严格一致，无损保证照单全收。</p> <p><img src="/assets/img/posts/20260816-ecospec/figure2_p4.png" alt="EcoSpec 框架"/></p> <h3 id="31-轻量-expert-predictor预测每个草稿-token-会路由到哪">3.1 轻量 Expert Predictor：预测每个草稿 token 会路由到哪</h3> <p>第一个组件是一个小 decoder LM（Qwen3-0.6B 或 DeepSeek-R1-Distill-Qwen-1.5B，视目标模型家族而定），输入草稿 token（用目标模型的 embedding 层投影到自己的 hidden 空间），<strong>一次性输出所有 MoE 层的路由 logits</strong> $z \in \mathbb{R}^{L \times E}$，softmax 后每层取 Top-K（K 与目标模型的路由配置一致，如 Qwen3 的 Top-8、GPT-OSS 的 Top-4）得到预测专家集 $\mathcal{E}_\text{pred}$。</p> <p>训练是纯监督的：把目标模型在七个评测数据集上跑出来的<strong>真实 router 轨迹</strong>收集起来，逐层交叉熵训练——监督目标是个把质量 1/K 均摊到 k 个被激活专家上的归一化分布。80/20 划分、100 epochs、Adam lr=1e-5。测试集上的 Top-K 路由预测准确率：<strong>GPT-OSS-120B 93%、Qwen3-235B 82%、DeepSeek-V3.1 80%</strong>。</p> <p>开销：预测器每步约 <strong>4ms</strong>（对比：Qwen3 的验收本身 830ms），可以忽略。</p> <h3 id="32-全局-expert-buffer只有新专家才算成本">3.2 全局 Expert Buffer：只有”新”专家才算成本</h3> <p>第二个组件维护一个集合 $\mathcal{B}$：<strong>已被本轮已选草稿覆盖的专家</strong>。它把成本从”绝对量”变成”边际量”——第一个走某专家的 token 付全款，后面的搭便车。这个设计直接决定了打分函数的性质（见 3.3 的前缀一致性）。</p> <p>注意 $\mathcal{B}$ 是<strong>每个投机步重建的</strong>（从空集 $\mathcal{B}_0$ 开始，随贪心选择增长），不跨步持久——跨步的复用由系统层的 expert cache 负责，那不是这篇的战场。</p> <h3 id="33-cost-aware-draft-selection贪心--边际成本">3.3 Cost-Aware Draft Selection：贪心 + 边际成本</h3> <p>第三个组件是核心打分。对草稿树里每个候选节点 $t_i$：</p> \[S(t_i) = \frac{P(t_i)}{\Delta\text{Cost}(t_i \mid \mathcal{B}) + \epsilon}\] <ul> <li>$P(t_i)$：从根到 $t_i$ 路径上草稿概率的连乘——”这条路被接受的概率”代理（分子：接受率收益）；</li> <li> <table> <tbody> <tr> <td>$\Delta\text{Cost}(t_i \mid \mathcal{B}) =</td> <td>\mathcal{E}_\text{traj}(t_i) \setminus \mathcal{B}</td> <td>$：$t_i$ 整条路径的预测专家足迹中，<strong>还没被 buffer 覆盖的新增专家数</strong>（分母：带宽成本）；</td> </tr> </tbody> </table> </li> <li>$\epsilon$ 防除零。</li> </ul> <p>选择是标准贪心：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">ecospec_select</span><span class="p">(</span><span class="n">draft_tree</span><span class="p">,</span> <span class="n">P</span><span class="p">,</span> <span class="n">E_pred</span><span class="p">,</span> <span class="n">gamma</span><span class="p">):</span>
    <span class="c1"># P[node]: 根到该节点路径的草稿概率连乘
</span>    <span class="c1"># E_pred[node]: 该节点全路径的预测专家足迹 (layer, expert) 集合
</span>    <span class="n">S</span><span class="p">,</span> <span class="n">B</span> <span class="o">=</span> <span class="nf">set</span><span class="p">(),</span> <span class="nf">set</span><span class="p">()</span>                    <span class="c1"># 选中的节点 / 专家 buffer
</span>    <span class="k">while</span> <span class="nf">len</span><span class="p">(</span><span class="n">S</span><span class="p">)</span> <span class="o">&lt;</span> <span class="n">gamma</span><span class="p">:</span>
        <span class="n">best</span> <span class="o">=</span> <span class="nf">argmax</span><span class="p">(</span><span class="n">t</span> <span class="k">for</span> <span class="n">t</span> <span class="ow">in</span> <span class="n">tree</span> <span class="o">-</span> <span class="n">S</span><span class="p">,</span>
                      <span class="n">key</span><span class="o">=</span><span class="k">lambda</span> <span class="n">t</span><span class="p">:</span> <span class="n">P</span><span class="p">[</span><span class="n">t</span><span class="p">]</span> <span class="o">/</span> <span class="p">(</span><span class="nf">len</span><span class="p">(</span><span class="n">E_pred</span><span class="p">[</span><span class="n">t</span><span class="p">]</span> <span class="o">-</span> <span class="n">B</span><span class="p">)</span> <span class="o">+</span> <span class="n">eps</span><span class="p">))</span>
        <span class="n">S</span><span class="p">.</span><span class="nf">add</span><span class="p">(</span><span class="n">best</span><span class="p">)</span>
        <span class="n">B</span> <span class="o">|=</span> <span class="n">E_pred</span><span class="p">[</span><span class="n">best</span><span class="p">]</span>                  <span class="c1"># 后来的候选共享这些专家 = 边际成本归零
</span>    <span class="k">return</span> <span class="n">S</span>                               <span class="c1"># 一次 target forward 验收
</span></code></pre></div></div> <table> <tbody> <tr> <td>这个打分有一个很妙的结构性质——<strong>天然前缀一致（prefix-consistent）</strong>。因为子节点的路径概率 $P(\text{child}) = P(\text{parent}) \cdot P(\text{child}</td> <td>\text{parent}) \leq P(\text{parent})$（条件概率 ≤ 1），而父节点的专家足迹是子节点足迹的子集（$\Delta\text{Cost}$ 只会更小），所以<strong>子节点的分数不可能超过父节点</strong>（除并列外）。这意味着贪心选出的集合天然前缀闭合，和树形验证的 attention mask 结构严丝合缝，<strong>不需要任何额外的结构约束或修补项</strong>。</td> </tr> </tbody> </table> <h3 id="34-和-eagle-3--gto-的关系正交可叠加">3.4 和 EAGLE-3 / GTO 的关系：正交可叠加</h3> <p>EcoSpec 不动 drafter 本身——EAGLE-3 照常生成草稿树和候选概率，EcoSpec 只接管”从树里挑哪些节点去验收”这一步。对比实验里 drafter 配置在 baseline 和 EcoSpec 之间严格保持一致，把变量隔离在选择阶段。它甚至可以叠加在 GTO（一种改进 drafter 训练的方法）上：GTO+EcoSpec 在 Qwen3 HumanEval 上 1.13×→1.29×——<strong>改草稿生成（GTO）和改验收选择（EcoSpec）是两个正交的轴</strong>。</p> <hr/> <h2 id="4-效果">4. 效果</h2> <h3 id="41-主结果8h200γ4贪心-t07-个-benchmark">4.1 主结果（8×H200、γ=4、贪心 T=0、7 个 benchmark）</h3> <table> <thead> <tr> <th>模型</th> <th>baseline</th> <th>EcoSpec</th> <th>专家/层</th> </tr> </thead> <tbody> <tr> <td>Qwen3-235B-A22B</td> <td>EAGLE-3 1.22×</td> <td><strong>1.36×</strong>（MTBench 最高 <strong>1.62×</strong>）</td> <td>23.7→20.5</td> </tr> <tr> <td>GPT-OSS-120B</td> <td>EAGLE-3 1.14×</td> <td><strong>1.31×</strong>（MMStar/AMC 最高 1.45×）</td> <td>11.6→10.6</td> </tr> <tr> <td>DeepSeek-V3.1</td> <td>MTP 1.10×</td> <td><strong>1.15×</strong></td> <td>31.4→31.2</td> </tr> </tbody> </table> <p>采样（T=1）下同样成立：Qwen3 1.28×→1.38×、GPT-OSS 1.18×→1.30×、DeepSeek 1.28×→1.33×。</p> <p><img src="/assets/img/posts/20260816-ecospec/table1_p7.png" alt="主结果表"/></p> <p><strong>最关键的佐证是接受长度几乎没动</strong>（Qwen3 贪心 2.41→2.32，GPT-OSS 1.88→1.86）：提速全部来自”验收变便宜”，而不是”草稿变准”。这和 EAGLE 系列纯提 τ 的路线是<strong>完全不同的另一条轴</strong>——以前大家以为投机解码只有”接受率”一个旋钮，现在多了”验收成本”这个旋钮。</p> <h3 id="42-hbm-流量与延迟拆解">4.2 HBM 流量与延迟拆解</h3> <p>Table 2（每验收步的 HBM 读取量估算）：</p> <table> <thead> <tr> <th>模型</th> <th>baseline</th> <th>EcoSpec</th> <th>降幅</th> </tr> </thead> <tbody> <tr> <td>Qwen3-235B</td> <td>99.3 GB</td> <td>88.1 GB</td> <td><strong>−11.2 GB（11.3%）</strong></td> </tr> <tr> <td>GPT-OSS-120B</td> <td>6.0 GB</td> <td>5.5 GB</td> <td>−8.0%</td> </tr> <tr> <td>DeepSeek-V3.1</td> <td>97.3 GB</td> <td>96.8 GB</td> <td>−1.0%</td> </tr> </tbody> </table> <p>Qwen3 的验收延迟从 0.832s 降到 0.730s（每步总延迟 0.840→0.742s）。三个模型的差异很有信息量：<strong>Qwen3 和 GPT-OSS 的专家使用模式集中</strong>（token 倾向复用热门专家），选择空间大，省得多；<strong>DeepSeek 的均衡路由</strong>（训得好的负载均衡反而让专家使用更分散）最难省——但它的单专家最大（44MB），每层每步省 0.2 个专家就值 0.5GB，所以哪怕百分比只有 1%，绝对收益仍然划算。</p> <h3 id="43-消融边际成本预测器batch">4.3 消融：边际成本、预测器、batch</h3> <p><strong>边际成本 vs 静态全局成本</strong>：把 buffer 增量换成”路径足迹大小”的静态打分（不随选择更新），Qwen3 GSM8K 只有 1.28×；换成边际成本打分升到 <strong>1.39×</strong>（GPT-OSS 1.06×→1.11×、DeepSeek 1.16×→1.19× 同向）。<strong>“谁先来谁付全款”的建模是必要的</strong>，静态成本看不到候选之间的共享结构。</p> <p><strong>预测器鲁棒性</strong>：预测准确率从收敛值掉到 50%，Qwen3 GSM8K 加速从 1.39× 退到 1.17×——有影响但 gracefully degrade；更重要的是 <strong>oracle 专家集的结果（20.4 专家/层）和收敛预测器（20.5）几乎一样</strong>——说明 82–93% 的准确率已经不是瓶颈，继续卷预测器精度没有边际收益。</p> <p><strong>batch 扩展</strong>：batch=4 时 EAGLE-3 归零（1.00×），EcoSpec 保住 <strong>1.09×</strong>（专家足迹 45.7 vs 54.9）——<strong>带宽越紧张的并发场景，成本感知越值钱</strong>。这对线上部署（几乎永远有 batch）是最有说服力的一组数字。</p> <p><strong>γ 扫描</strong>：γ=3/4/5/6 上 EcoSpec 全区间领先，γ=4 是甜点——γ 再大，验收的专家并集膨胀，收益反而缩水。<strong>“多验收几个”在 MoE 上不是免费的</strong>，这条曲线就是证据。</p> <hr/> <h2 id="5-我的-take">5. 我的 Take</h2> <p><strong>一，这篇换掉的是投机解码的那把尺子。</strong> 收益函数从”接受了几个 token”换成”每 GB 带宽换几个 token”。在 dense 模型上这两者单调对应，在 MoE 上中间隔着一个路由并集——以前的论文不是没优化成本，是这个成本项在 dense 假设下根本不存在。类似的”隐形式成本”在其他系统里也常见（比如 prefill 的 KV 产出、allreduce 的小包合并），<strong>每换一次底层架构，成本模型就得重写一遍</strong>——这是系统研究永远有的可做之处。</p> <p><strong>二，和 DSpark 是公式里的两个不同变量，可以叠。</strong> DSpark 用置信度管理 verify 预算（分子上的 $T_\text{verify}$ 分配），EcoSpec 用专家成本管理 draft 选择（分母上的验收成本）——一个决定”验收多长”，一个决定”验收哪些”，组合起来就是完整的 MoE 投机解码调度器。我预期很快会有人把这两件事放进同一个目标函数里做联合优化。</p> <p><strong>三，”草稿的专家足迹”会成为一等公民。</strong> 随着 MoE 成为旗舰标配、投机解码成为默认配置，每个草稿 token 附带的”预计激活专家”会变成推理栈里需要显式管理的量——就像 KV Cache 的显存、batch 的槽位一样。预测路由这个组件（80–93% 准确率、4ms 开销）已经在 expert-prefetch（预取侧）、EcoSpec（选择侧）两篇里出场了，往后大概会变成 MoE 推理引擎的标准件。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 MoE 推理、投机解码优化你想系统深入，可以看看我之前出版的<a href="https://item.jd.com/14945889.html">《动手学AutoML：从 NAS 到大语言模型优化实战》</a>，书里大模型优化章节讲的稀疏化与效率权衡，和本文”专家足迹管理”是同一套设计思路。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="MoE"/><category term="投机解码"/><category term="推理优化"/><category term="论文解读"/><summary type="html"><![CDATA[arXiv’26 | EcoSpec：MoE 投机解码别只顾接受率，草稿会把专家撒得到处都是]]></summary></entry><entry><title type="html">arXiv’26 | Cross-Model KV Transfer：小模型算好的 KV Cache，大模型直接接着用</title><link href="https://marsggbo.github.io/blog/2026/20260816-kvtransfer/" rel="alternate" type="text/html" title="arXiv’26 | Cross-Model KV Transfer：小模型算好的 KV Cache，大模型直接接着用"/><published>2026-08-16T00:00:00+00:00</published><updated>2026-08-16T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260816-kvtransfer</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260816-kvtransfer/"><![CDATA[<h1 id="arxiv26--cross-model-kv-transfer小模型算好的-kv-cache大模型直接接着用">arXiv’26 | Cross-Model KV Transfer：小模型算好的 KV Cache，大模型直接接着用</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2608.03893">Cross-Model KV Cache Transfer in LLM Families: A Closed-Form Linear Mapping for Prefill Reuse</a></p> </blockquote> <hr/> <h2 id="1-前言先把背景交代清楚">1. 前言：先把背景交代清楚</h2> <p>你有没有想过这样一个问题：<strong>同一个对话里，为什么从小模型切换到大模型，前面聊过的内容要整个重新算一遍？</strong></p> <p>先把”重新算一遍”这件事的代价说具体。LLM 推理分两个阶段：prefill 阶段把整段 prompt 并行喂进去，产出两样东西——第一个 token 的概率分布，和<strong>每个 token 位置的 K/V 向量</strong>（也就是 KV Cache）；decode 阶段每生成一个 token，都要把历史 KV 从显存里读一遍。注意一个常被忽略的事实：<strong>prefill 的计算产出物本质上就是 KV Cache</strong>。也就是说，”重新 prefill”和”重新算一遍 KV Cache”是同一件事。</p> <p>那为什么会有”重新 prefill”的需求？因为现在的线上系统越来越离不开<strong>模型家族</strong>这个概念。Qwen3 有 4B/8B/14B/32B 一整排，Llama 3.1 有 8B/70B，Ministral 有 3B/8B/14B。围绕家族出现了三种很自然的玩法：</p> <ul> <li><strong>级联调度（cascade）</strong>：简单问题走小模型省钱，检测到难题再升级大模型；</li> <li><strong>会话中途换模型</strong>：对话前几轮用便宜模型顶着，用户开始问硬核问题了再切大模型；</li> <li><strong>家族内路由（routing）</strong>：一个 router 在家族成员间动态选模型。</li> </ul> <p>这三种玩法有一个共同的隐形税：<strong>每次切换，接收方模型都要把累积的上下文从头 prefill 一遍。</strong> 长对话切一次模型，TTFT 直接多几秒——这在产品体验上是很难接受的。</p> <p>所以这个问题可以抽象得非常干净：<strong>A 模型算好的 KV Cache，能不能直接变换成 B 模型的 KV Cache？</strong> 而且注意这是双向需求的：小→大是质量升级（升级后不用重算），大→小是成本降级（长上下文阶段切回小模型省 decode 成本）。</p> <p>在讲这篇论文的方法之前，先把我之前写过的两篇 KV 复用文章放这，它们和本文正好构成一条完整的线：</p> <ul> <li><a href="https://marsggbo.github.io/blog/2026/kvcomm/">《KVCOMM》</a>：<strong>同一个模型</strong>内部，多个 Agent 共享相同前缀的 KV——它的洞察是不同 Agent 的 system prompt / 工具描述高度重叠，把这份 KV 在 Agent 间”通起来”，TTFT 砍 7.8 倍；</li> <li><a href="https://marsggbo.github.io/blog/2026/semsharekv/">《SemShareKV》</a>：更进一步，用 LSH 做<strong>语义级</strong>共享——不用前缀完全相同，意思相近的段落也能共享 KV。</li> </ul> <p>这两篇的共同前提都是”<strong>同一个模型</strong>“：KV Cache 是模型私有的记忆，换个模型就作废。今天这篇（arXiv 2608.03893）补的就是最后这块拼图：<strong>跨模型</strong>的 KV 空间对齐。它的结论出人意料地简单粗暴：<strong>同一家族的两个模型之间，KV Cache 近似差一个线性映射——而线性映射有闭式解，一行矩阵求逆就能算出来，完全不需要训练。</strong></p> <p>在 Qwen3 14B→32B 上，转换后的 KV 直接喂给 32B，五个 benchmark 平均保住 <strong>97.6%</strong> 的准确率；32K token 的上下文，重新 prefill 要 6975ms，KV 转换只要 <strong>277.6ms，快 25 倍</strong>。</p> <hr/> <h2 id="2-前置知识跨模型-kv-复用前人卡在哪">2. 前置知识：跨模型 KV 复用，前人卡在哪</h2> <p>跨模型 KV 这个方向其实已经有几篇工作了，先看它们为什么不够用，才能理解这篇的定位：</p> <table> <thead> <tr> <th>方法</th> <th>核心思路</th> <th>问题</th> </tr> </thead> <tbody> <tr> <td><strong>C2C</strong>（Cache-to-Cache）</td> <td>给每个模型对训练一个神经 fuser，融合两个模型的 KV</td> <td>每一对模型都要梯度训练，成本高、泛化差</td> </tr> <tr> <td><strong>LatentAlign</strong></td> <td>给每个模型学一个 adapter，把各自 KV 投到共享 latent 空间</td> <td>同样需要训练每个模型的 adapter</td> </tr> <tr> <td><strong>IAM</strong></td> <td>不搬 KV 值，把小模型的 <strong>attention pattern</strong>（注意力模式）代入大模型</td> <td>无梯度，但传的是 pattern 不是 cache，复用度有限</td> </tr> <tr> <td><strong>DroidSpeak</strong></td> <td>在<strong>架构完全相同</strong>的微调变体之间（如同 base 模型和它的 SFT 版）直接共享 KV</td> <td>无梯度，但只支持同构微调对，不支持跨规模（8B→70B 这种）</td> </tr> </tbody> </table> <p>理想形态需要同时满足四条：<strong>无梯度训练、跨规模、传输 KV 值本身、闭式解</strong>。前四篇各占一到两条，这篇是第一个全占的。为什么”无梯度”这么重要？因为跨模型的组合是 $O(n^2)$ 的——一个四模型家族就有 12 个有序对，如果每对都要训练，这个方法就只能是论文 demo；如果是闭式解，每对就是”跑一次最小二乘”的事，部署成本完全不同量级。</p> <hr/> <h2 id="3-关键观察跨模型的-kv-有线性结构">3. 关键观察：跨模型的 KV 有线性结构</h2> <p>整个方法能成立，全压在一个实证观察上：<strong>同一家族里，两个模型的 KV Cache 之间存在显著的线性关系。</strong></p> <p>怎么验证？把源模型第 $l$ 层的（处理过的）key/value 作为自变量 $X$，目标模型对应位置的 key/value 作为因变量 $Y$，做线性回归，看 $R^2$（方差解释率）。在 Qwen3 14B→32B 上：</p> <table> <thead> <tr> <th>源层数 k</th> <th>K 的 R²</th> <th>V 的 R²</th> </tr> </thead> <tbody> <tr> <td>1</td> <td>0.5572</td> <td>0.3249</td> </tr> <tr> <td>8</td> <td>0.7914</td> <td>0.6541</td> </tr> <tr> <td>全部</td> <td>0.8451</td> <td>0.7645</td> </tr> </tbody> </table> <p><img src="/assets/img/posts/20260816-kvtransfer/figure2_p3.png" alt="跨模型 KV 的线性结构"/></p> <p>三个值得划重点的细节：</p> <p><strong>（1）单层就能解释 56% 的 key 方差</strong>。这说明两个模型的中间表征不是”语义上有点像”那种模糊的相似，而是<strong>近似线性可达</strong>的——这个数字给了线性映射合法性。</p> <p><strong>（2）key 比 value 好预测约 0.2 个 R²</strong>。为什么？key 承载的主要是”检索信息”（这个 token 该被谁注意到），在同族模型里检索结构高度保守；value 承载的是”内容信息”，模型规模变大后内容表征会重新组织，漂移更大。这个不对称后面选 MLP 补救时还会用到。</p> <p><strong>（3）RoPE 会污染拟合</strong>。key 在存进 cache 前被 RoPE 旋转过——位置信息乘在内容上，导致”第 5 个 token 的 key”和”第 5000 个 token 的 key”即使内容一样，数值也完全不同。直接拿带 RoPE 的 key 拟合，$R^2$ 会显著掉，而且”哪层源最相关”的结构变得模糊；<strong>把 RoPE 剥掉再拟合，层与层之间的对角结构立刻清晰</strong>。这个观察直接催生了方法的第三个组件。</p> <p>为什么同族模型的 KV 会有线性结构？我的理解是：<strong>家族模型本来就是同一份数据、相近的 tokenizer、相近的配方练出来的</strong>，中间层学到的特征空间高度同构——尤其 key 这种低频检索结构，几乎是”家族指纹”级别的保守量。论文没有从理论上证明这一点（也不需要），但把它量化到”一个 per-head 线性映射能搬 97% 的信息”，本身就是很有价值的实证贡献。</p> <hr/> <h2 id="4-方法每头一个闭式-ridge三个组件拼起来">4. 方法：每头一个闭式 Ridge，三个组件拼起来</h2> <p>整条 pipeline 长这样：源模型正常 prefill 出 KV Cache → <strong>per-head 线性映射</strong>把它变换成目标模型的 KV 格式 → 目标模型跳过 prefill 直接 decode：</p> <p><img src="/assets/img/posts/20260816-kvtransfer/figure1_p2.png" alt="KV 转换 pipeline"/></p> <p>方法由三个组件构成，逐个拆。</p> <h3 id="41-per-head-ridge-回归一行闭式解">4.1 Per-head Ridge 回归：一行闭式解</h3> <p>对目标模型的每个 (layer $l$, head $h$)，K 和 V 各学一个独立的线性映射。设计矩阵 $X$ 是源模型 top-k 个源层的 KV 拼接（$N \times (k \cdot n_{kv} \cdot d_h)$ 维），$Y$ 是目标模型该 head 的 K 或 V（$N \times d_h^t$ 维）。解就是标准 ridge：</p> \[W^* = (X^\top X + \lambda I)^{-1} X^\top Y, \quad \lambda = 0.01\] <p>几个工程细节值得说：</p> <ul> <li><strong>为什么是 ridge 不是 OLS</strong>：top-k 源层是按”最相关”挑出来的，它们彼此特征高度相关，$X^\top X$ 接近奇异，裸 OLS 数值上会炸。加 $\lambda I$ 正则后稳定，且 $\lambda$ 在四个数量级上扫描结果都很平（只有 $\lambda=1$ 这种离谱值才崩，HellaSwag 掉 15.79pp）——说明这个超参很钝，不挑；</li> <li><strong>bias 的处理</strong>：$X$、$Y$ 先中心化再求解，$W^<em>$ 只估斜率，事后按 $\ b = \bar{Y} - \bar{X}W^</em>$ 把偏置补回来；</li> <li><strong>计算量的去向</strong>：拟合的大头是 $X^\top X$（$O(N d_s^2)$），<strong>每个目标层只算一次、层内所有 head 共享</strong>——这就是”47 分钟拟合一整对模型”的来源；</li> <li><strong>head 之间零共享</strong>：每个 (l, h) 一个独立的 $W_K^{l,h}$ 和 $W_V^{l,h}$，不跨 head、不跨 K/V 共享参数。线性映射本来就便宜，没必要共享。</li> </ul> <p>翻译成代码，核心就是这几行（拟合阶段）：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">fit_head_mapper</span><span class="p">(</span><span class="n">X</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> <span class="n">Y</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> <span class="n">lam</span><span class="p">:</span> <span class="nb">float</span> <span class="o">=</span> <span class="mf">0.01</span><span class="p">):</span>
    <span class="c1"># X: [N, d_src]   源模型 top-k 层拼接的 K（或 V），已剥 RoPE、已中心化
</span>    <span class="c1"># Y: [N, d_tgt]   目标模型同位置的 K（或 V），已剥 RoPE、已中心化
</span>    <span class="n">d_src</span> <span class="o">=</span> <span class="n">X</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span>
    <span class="n">XtX</span> <span class="o">=</span> <span class="n">X</span><span class="p">.</span><span class="n">T</span> <span class="o">@</span> <span class="n">X</span>                          <span class="c1"># 每层一次，层内所有 head 共享
</span>    <span class="n">XtY</span> <span class="o">=</span> <span class="n">X</span><span class="p">.</span><span class="n">T</span> <span class="o">@</span> <span class="n">Y</span>
    <span class="n">W</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">linalg</span><span class="p">.</span><span class="nf">solve</span><span class="p">(</span><span class="n">XtX</span> <span class="o">+</span> <span class="n">lam</span> <span class="o">*</span> <span class="n">torch</span><span class="p">.</span><span class="nf">eye</span><span class="p">(</span><span class="n">d_src</span><span class="p">),</span> <span class="n">XtY</span><span class="p">)</span>   <span class="c1"># 闭式解，无梯度
</span>    <span class="k">return</span> <span class="n">W</span>                                <span class="c1"># 推理时: Y_hat = X_centered @ W + b
</span></code></pre></div></div> <h3 id="42-跨层源选择不是同层对同层">4.2 跨层源选择：不是同层对同层</h3> <p>第二个反直觉的组件：<strong>目标层的 KV 不是只从源模型的同层映射，而是从”最有预测力”的 k 个源层拼起来映射。</strong></p> <p>注意这和我们训练模型时的直觉相反——第 20 层的表征不一定最像另一个模型的第 20 层（两模型深度、宽度都不同），真正该问的是”目标第 20 层的 K 能被源模型的哪几层线性表出”。论文用<strong>贪心前向选择（greedy forward selection）</strong>回答这个问题：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">greedy_forward_selection</span><span class="p">(</span><span class="n">X_layers</span><span class="p">,</span> <span class="n">Y</span><span class="p">,</span> <span class="n">max_k</span><span class="p">):</span>
    <span class="c1"># X_layers: {源层id: 该层所有 token 的 K}   Y: 目标层所有 token 的 K
</span>    <span class="n">selected</span> <span class="o">=</span> <span class="p">[</span><span class="nf">best_single_layer_by_R2</span><span class="p">(</span><span class="n">X_layers</span><span class="p">,</span> <span class="n">Y</span><span class="p">)]</span>   <span class="c1"># 1. 从单层 R² 最高的源层起步
</span>    <span class="k">while</span> <span class="nf">len</span><span class="p">(</span><span class="n">selected</span><span class="p">)</span> <span class="o">&lt;</span> <span class="n">max_k</span><span class="p">:</span>
        <span class="c1"># 2. 每轮加入能让"联合 R²"提升最大的那个源层
</span>        <span class="n">gains</span> <span class="o">=</span> <span class="p">{</span><span class="n">j</span><span class="p">:</span> <span class="nf">joint_r2</span><span class="p">(</span><span class="n">X_layers</span><span class="p">[</span><span class="n">selected</span> <span class="o">+</span> <span class="p">[</span><span class="n">j</span><span class="p">]],</span> <span class="n">Y</span><span class="p">)</span> <span class="k">for</span> <span class="n">j</span> <span class="ow">in</span> <span class="n">all_layers</span> <span class="o">-</span> <span class="nf">set</span><span class="p">(</span><span class="n">selected</span><span class="p">)}</span>
        <span class="n">selected</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="nf">argmax</span><span class="p">(</span><span class="n">gains</span><span class="p">))</span>
    <span class="k">return</span> <span class="n">selected</span>                                        <span class="c1"># 3. 产出 k=1..all 的整条曲线
</span></code></pre></div></div> <p>结论：<strong>收益集中在 k=1→4，k=6 就到全层 R² 的 92.3%（key）/ 87.7%（value）</strong>，之后再堆源层是边际递减。生产上不用逐对跑贪心（那是分析工具），直接按”单源 head 平均 R² 排序取 top-k”固定下来，同层所有 head 共享一套源层选择。</p> <p>消融里这个组件是<strong>贡献最大的单项</strong>：k 从 8 砍到 1，ARC-C 从 61.60 崩到 27.65，PPL 从 7.33 崩到 22.73——单层信息量远不够，跨层拼接才是主力。</p> <h3 id="43-rope-因式分解让映射与位置解耦">4.3 RoPE 因式分解：让映射与位置解耦</h3> <p>第三个组件解决”位置编码污染”。源 key 存的是 $K_s R_{\Theta_s}$（内容乘上源模型的 RoPE），直接映射会把”位置分布”也学进权重里——校准用 1024 token 的短序列，权重就绑死在这个位置分布上，换上下文长度就失效。</p> <p>做法是把位置”剥掉-映射-再穿上”：</p> \[\hat{K}_t = \big(K_s\, R_{\Theta_s}^{-1}\, W_K + b_K\big)\, R_{\Theta_t}\] <ul> <li>$R_{\Theta_s}^{-1}$：源模型 RoPE 的逆旋转——RoPE 是正交旋转，<strong>求逆是精确且免费的</strong>（转置即可），剥完得到无位置的 content key；</li> <li>$W_K$：在无位置空间里做线性映射（校准时目标侧同样剥 RoPE，权重完全 position-free）；</li> <li>$R_{\Theta_t}$：套上<strong>目标模型自己的</strong> RoPE——两模型 RoPE 配置不同也无所谓，反正位置是在最后一步才穿上的。</li> </ul> <p><strong>value 不编码位置</strong>，直接 $ \hat{V}_t = V_s W_V + b_V$。</p> <p>这一步的必要性有硬数据：推理时不做重旋转，MMLU 从 78.09 崩到 <strong>25.79</strong>（随机水平）、GSM8K 崩到 4.17；而 HellaSwag 只掉 5pp——因为 HellaSwag 这种局部语言建模任务对位置不敏感，MMLU/GSM8K 这种需要全局结构的任务会立刻暴露位置的错位。这个对比也侧面说明：<strong>评测集选不对，坏掉的组件根本测不出来。</strong></p> <h3 id="44-校准与部署开销">4.4 校准与部署开销</h3> <ul> <li><strong>数据</strong>：FineWeb-Edu 500 条 × 1024 token，stride-4 子采样后每个目标 head 约 <strong>128K 个 token 级观测</strong>。稳健性：200 条之后饱和，50 条也只差 1.6pp；<strong>校准域比数据量重要</strong>（CodeAlpaca 校准比 FineWeb-Edu 掉 5.24pp，Wikipedia 只掉 1.05pp）；</li> <li><strong>拟合算力</strong>：单节点 8×H100，每对模型 <strong>47–87 分钟</strong>（Qwen3 14B→32B 约 52 分钟），全程无反向传播；精度上前向 bf16、协方差 fp32；</li> <li><strong>mapper 体积</strong>：每对 1.01–3.36B 参数（4–12GB）——不小，但<strong>不需要常驻 GPU</strong>，可以从 CPU/磁盘按需 page in（80–480ms，每次切换付一次），因为切换模型本来就是低频事件；</li> <li><strong>适用边界</strong>：同家族、KV head 数与 head dim 匹配、dense full attention。GQA 结构不匹配的不行，MLA 这种 latent 压缩的更不行；每个方向各需一个 mapper（14B→32B 和 32B→14B 是两个不同的映射）。</li> </ul> <hr/> <h2 id="5-实验保真度延迟漂移三本账">5. 实验：保真度、延迟、漂移三本账</h2> <h3 id="51-保真度能保住多少table-1">5.1 保真度：能保住多少？（Table 1）</h3> <p>先定义指标。<strong>Retention = 转换后准确率 ÷ 目标模型独立 prefill 的准确率 × 100%</strong>。还有个更严苛的 <strong>floor-normalized</strong> 版本：把随机_chance 定为 0%、目标独立运行定为 100%，专治”低分对看起来还有 40%”的错觉。</p> <table> <thead> <tr> <th>迁移方向</th> <th>k</th> <th>平均 retention</th> <th>关键项</th> </tr> </thead> <tbody> <tr> <td>Qwen3 14B→32B</td> <td>8</td> <td><strong>97.6%</strong></td> <td>HellaSwag 101.0%（超过独立运行），GSM8K 95.6%</td> </tr> <tr> <td>Qwen3 8B→32B</td> <td>12</td> <td>87.5%</td> <td>GSM8K 掉到 68.8%</td> </tr> <tr> <td>Llama 3.1 8B→70B</td> <td>20</td> <td>72.8%</td> <td>HellaSwag 94.4%，但 GSM8K 只有 18.2%</td> </tr> <tr> <td>Ministral 3 3B→8B</td> <td>all</td> <td>76.2%</td> <td> </td> </tr> <tr> <td>Ministral 3 3B→14B</td> <td>20</td> <td>44.2%</td> <td>floor-normalized 只剩 14.7%，基本不可用</td> </tr> <tr> <td>Ministral 3 8B→14B</td> <td>12</td> <td>41.6%</td> <td>floor-normalized 11.1%，不可用</td> </tr> </tbody> </table> <p><img src="/assets/img/posts/20260816-kvtransfer/table1_p6.png" alt="主结果表：各迁移对的保真度"/></p> <p>规律很清楚：<strong>同代、尺寸相近的家族对是甜点</strong>（14B→32B 几乎无损），跨得越远崩得越狠；推理类任务（GSM8K）比语言建模类任务（HellaSwag）敏感得多。这个边界本身就有工程价值——级联系统可以据此决定哪几档之间值得开 KV 转移、哪几档老实重新 prefill。</p> <h3 id="52-延迟省多少table-5">5.2 延迟：省多少？（Table 5）</h3> <p>Qwen3 14B↔32B、32K token：重新 prefill <strong>6975ms</strong> vs mapper <strong>277.6ms = 25×</strong>（小→大）；大→小是 2952.7ms vs 427.1ms = <strong>7×</strong>。整个 64–32K 区间、七个迁移对、<strong>70 个测试格 mapper 全部更快</strong>，总范围 2.7×–25.1×。</p> <p><img src="/assets/img/posts/20260816-kvtransfer/table5_p9.png" alt="重 prefill vs KV 转换延迟"/></p> <p>为什么大→小省得少？因为小模型的 prefill 本来就便宜，能省的基数小。这也符合业务直觉：升级方向的收益（用户等不起大模型 7 秒 prefill）恰恰是最大的。</p> <h3 id="53-多轮会话会不会漂">5.3 多轮会话会不会漂？</h3> <p>CoQA 十轮多轮 handoff（14B↔32B 交替使用对方的 KV）：小→大的差距从第 1 轮到第 10 轮只扩大 1.7pp；大→小每轮线性漂移 0.33pp。<strong>十轮之内不会级联崩坏</strong>——误差没有复利效应，这是”会话中途换模型”能上线的安全垫。</p> <h3 id="54-闭式解不够的地方mlp-补位以及一个有趣的归因">5.4 闭式解不够的地方：MLP 补位，以及一个有趣的归因</h3> <p>Ridge 在它擅长的对上和 MLP 打平（97.6% vs 97.3%），但在崩掉的对上 MLP 能救回来很多：Ministral 3B→14B 从 68.0% 拉到 92.3%（<strong>+24.3pp</strong>）、8B→14B 从 58.7% 到 95.5%（<strong>+36.8pp</strong>）。</p> <p>更有意思的是论文对”为什么 MLP 能救”的归因分析：<strong>决定保真度的不是误差的大小，而是误差落在哪个子空间</strong>。MLP 的增益恰好对应把残差从 attention 敏感的方向挪开（失败对上 K-concentration Δ≈−2.5、cosine Δ≈+0.45）。两个佐证：</p> <ul> <li>attention 输出的 cosine 相似度与最终 retention 的相关性 <strong>r=+0.57</strong>；</li> <li>校准时的 R² 与 retention 的相关性 <strong>r=−0.20</strong>——基本不相关！</li> </ul> <p><strong>“拟合得好”和”用得好”是两件事</strong>：R² 衡量的是全空间平均误差，而 attention 只对某些子空间敏感。这个结论对以后所有做 KV 压缩/量化的工作都适用：<strong>评测拟合质量请直接测下游，别只看 R²</strong>。顺带地，attention-output cosine 可以当一个便宜的”这个模型对能不能转移”的事前预测器。</p> <hr/> <h2 id="6-我的-take">6. 我的 Take</h2> <p><strong>一，KV 复用这条线的版图补全了。</strong> 从”同模型同前缀”（prefix caching，vLLM/SGLang 标配）→”同模型语义共享”（SemShareKV）→”多 Agent 共享”（KVCOMM）→”跨模型搬运”（本文），每一层的适用范围和代价都清晰了。工程组合拳也顺理成章：同族切换用 KV Transfer，跨族切换乖乖 prefill。</p> <p><strong>二，”闭式解”这个形态选对了。</strong> 它把跨模型 KV 复用从”研究问题”压成了”部署时的一次性成本”（一对模型 52 分钟 + 一次磁盘存放），这是能进推理框架的形态。哪天 vLLM 里出现 <code class="language-plaintext highlighter-rouge">--kv-transfer-adapter</code> 这种启动参数，我不会意外。</p> <p><strong>三，边界要诚实。</strong> 只支持 dense attention 同构 KV 家族对，GQA 不匹配、MLA 压缩都无解；Ministral 那两个失败对说明”家族”的假设并不总成立。它买到的是<strong>家族内</strong>的廉价切换，不是万能 KV 翻译器——但就这个受限场景（而它恰好是 cascade/routing 的主流场景），97.6% retention + 25× prefill 节省，这笔账很划算。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 KV Cache、LLM 推理优化你想系统深入，可以看看我之前出版的<a href="https://item.jd.com/14945889.html">《动手学AutoML：从 NAS 到大语言模型优化实战》</a>，书里有专门一章讲 LLM 推理效率与 KV Cache 优化，本文的 prefill 复用正是那一章的主线问题。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="KV Cache"/><category term="推理优化"/><category term="论文解读"/><summary type="html"><![CDATA[arXiv’26 | Cross-Model KV Transfer：小模型算好的 KV Cache，大模型直接接着用]]></summary></entry><entry><title type="html">arXiv’26 | Wind-MTP：百万上下文里，MTP 草稿头才是那个隐形税</title><link href="https://marsggbo.github.io/blog/2026/20260816-windmtp/" rel="alternate" type="text/html" title="arXiv’26 | Wind-MTP：百万上下文里，MTP 草稿头才是那个隐形税"/><published>2026-08-16T00:00:00+00:00</published><updated>2026-08-16T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260816-windmtp</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260816-windmtp/"><![CDATA[<h1 id="arxiv26--wind-mtp百万上下文里mtp-草稿头才是那个隐形税">arXiv’26 | Wind-MTP：百万上下文里，MTP 草稿头才是那个隐形税</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2607.21535">Wind-MTP: Removing the Full-Context Draft-KV Tax</a></p> </blockquote> <hr/> <h2 id="1-前言先把背景交代清楚">1. 前言：先把背景交代清楚</h2> <p>你有没有想过这样一个问题：<strong>投机解码里的草稿模型，真的是”几乎免费”的吗？</strong></p> <p>先把投机解码的账本摆出来。每轮解码：</p> \[L = \frac{T_\text{draft} + T_\text{verify}}{\tau}\] <p>$T_\text{draft}$ 是打草稿的时间，$T_\text{verify}$ 是大模型验收的时间，$\tau$ 是平均每轮被接受的 token 数。过去两年这个领域的工作几乎都能塞进这个公式：</p> <ul> <li><a href="https://marsggbo.github.io/blog/2026/20260814-dflash/">DFlash</a>（ICML’26）：用 Diffusion drafter 替掉 AR drafter，一次 forward 并行草拟整个 block，把 $T_\text{draft}$ 从”随草稿长度线性涨”压成常数；</li> <li><a href="https://marsggbo.github.io/blog/2026/20260814-dspark/">DSpark</a>：DeepSeek 线上方案，半自回归 + 置信度调度，同时优化 $\tau$ 和验收预算的分配；</li> <li><a href="https://marsggbo.github.io/blog/2026/20260722-cassandra/">Cassandra</a>：不训练新模型，直接从 target 模型的权重 bitstream 里抠出一个 drafter，把草稿的<strong>训练成本</strong>都省了。</li> </ul> <p>这条线的共识是：<strong>草稿阶段必须便宜</strong>。但所有这些工作（包括这些论文自己）都默认了一个前提——<strong>验收那边（target）才是大头，草稿头那点计算可以忽略</strong>。</p> <p>这个前提在 dense 模型 + 短上下文时代基本成立。但这篇 Wind-MTP（arXiv 2607.21535）指出，它正在两个趋势的夹击下失效：</p> <ol> <li><strong>上下文爆炸</strong>：旗舰模型的上下文已经冲到 1M token。草稿头再轻，它用的也是<strong>全量 attention</strong>——每猜一个草稿 token，都要把全部历史 KV 读一遍；</li> <li><strong>target 越来越快</strong>：新一代模型的 target 主干纷纷换上 GDN（Gated DeltaNet）、Mamba2 这类线性/递归注意力，decode 一步的成本本来就被压得很低。</li> </ol> <p>一边膨胀、一边瘦身，结果就是倒挂。论文给出的数字：<strong>1M context、γ=6 时，草稿阶段给每个 decode step 白加 92%–138% 的时间——比验收本身还贵</strong>。在难任务上原生 MTP 甚至跑输完全不开投机解码的 dense 解码（Qwen-122B code QA 只有 0.80×）——<strong>开了投机解码反而更慢</strong>，这是最打脸的情况。</p> <p>解法说破了不值钱：<strong>给草稿头的 attention 开一个 4K 的滑动窗口，验收侧原封不动</strong>。零训练、无新 kernel、严格无损，1M context 端到端再提速 <strong>11%–53%</strong>。但”为什么敢砍”和”为什么砍了不崩”，里面的分析做得非常扎实，这才是这篇论文的含金量。</p> <hr/> <h2 id="2-病根草稿头也在读全量-kv而且读-γ-遍">2. 病根：草稿头也在读全量 KV，而且读 γ 遍</h2> <p>先把这笔税的来源拆开。设上下文长度 $S$、草稿深度 $d$（drafter 层数）、每轮草稿长度 $\gamma$。一个 decode 迭代：</p> \[T_\text{iter} = \underbrace{t_\text{verify}}_{\text{验收 1 次}} + \underbrace{t_\text{draft}^{\text{ctx}}}_{\text{每步读历史 KV}} + \gamma \cdot \underbrace{t_\text{draft}^{\text{fwd}}}_{\text{草稿层 forward}}\] <p>关键在中间那项：MTP 头虽然是”挂在主干上的小头”，但它的 attention 是标准的 full attention——<strong>每生成一个草稿 token，都要对全部 $S$ 个历史位置做 attention，把它们的 KV 从 HBM 读一遍</strong>。$\gamma$ 个草稿 token 就是 $\gamma$ 遍。</p> <p>短上下文时这笔开销无感：单层 draft 的 KV 每 token 只有 1–2KB，8–32K 上下文里整个 draft KV 就 8–64MB，全在 L2 cache 里躺着，读了也不心疼。但到 1M context：</p> <ul> <li>draft 的 KV 膨胀到 <strong>1–2GB</strong>，每一步 γ 次从 HBM 全量读；</li> <li>论文实测三个模型的 draft KV pool 已占总 KV 显存的 <strong>7.7%–11.1%</strong>（Qwen-122B 7.7%、Qwen-35B 9.1%、Nemotron 11.1%，正好等于架构里 $1/(F+1)$，F 是 full-attention 层数）。</li> </ul> <p>Table 1 给了三个模型的每步成本（1M、d=7、B=1、bf16 KV、单张 B200）：</p> <table> <thead> <tr> <th>模型</th> <th>原生 MTP</th> <th>窗口化后</th> <th>草稿税</th> </tr> </thead> <tbody> <tr> <td>Qwen3.6-35B-A3B</td> <td>26.4 ms</td> <td>18.3 ms</td> <td>+44.3%</td> </tr> <tr> <td>Qwen3.5-122B-A10B</td> <td>34.5 ms</td> <td>26.5 ms</td> <td>+30.2%</td> </tr> <tr> <td>Nemotron-3-120B-A12B</td> <td>33.1 ms</td> <td>25.8 ms</td> <td>+28.3%</td> </tr> </tbody> </table> <p><img src="/assets/img/posts/20260816-windmtp/table1_p5.png" alt="每步成本对比"/></p> <p>nsys 的 kernel 级分解（Table 7，γ=6）更能说明钱的去向——每个迭代拆成 $t_\text{verify}^\text{base}$（裸验收）、$t_\text{verify}^\text{ovh}$（投机记账）和 $t_\text{draft}$ 三段，以 Qwen-35B 为例：12.5 / 4.6 / <strong>12.7</strong> ms，总计 29.7ms。<strong>草稿和裸验收几乎一样贵</strong>（12.7 vs 12.5）！窗口化后 $t_\text{draft}$ 从 12.7ms 掉到 4.3ms，验收两段纹丝不动——省的全是草稿。</p> <p>而 $t_\text{draft}$ 里面最大的单个 kernel 是什么？<code class="language-plaintext highlighter-rouge">generate_draft_decode_kv_indices</code>——<strong>生成草稿 attention 的 KV 索引</strong>这一个 kernel 就吃掉 3588μs/iter（Qwen-35B），窗口化后 3.0μs，<strong>三个数量级</strong>。这个 kernel 干的事就是”枚举全部历史位置给草稿头做 attention”——1M 上下文下枚举本身就是灾难。</p> <hr/> <h2 id="3-解法只给草稿开窗口">3. 解法：只给草稿开窗口</h2> <h3 id="31-改动本体小到一个-pr">3.1 改动本体：小到一个 PR</h3> <p>Wind-MTP 的实现是<strong>把草稿头的 paged block table 截短为 sink 块 + 最近 W 块</strong>：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>原生草稿 attention 的 block table:
[1, 2, 3, ..., 999993, 999994, ..., 1000000]   ← 全部 1M 块

窗口化后:
[1, 2, ..., 64, 995969, 995970, ..., 1000000]   ← 64 个 sink + 最近 W=4032 块
</code></pre></div></div> <p>W=4032、n_sink=64，即 StreamingLLM 式的”attention sink + 滑动窗口”——4K 窗口只占 1M 的 <strong>0.4%</strong>，等于把 99% 的草稿 KV 从读取路径上删掉。<strong>target 的验收 attention 一个字不动。</strong></p> <p>配套的第二个改动是 <strong>ring buffer</strong>：既然草稿只读 n_sink+W 个位置，那 draft KV pool 就没必要按 1M 分配——物理上只开 <code class="language-plaintext highlighter-rouge">n_sink + W + d</code> 个槽位循环使用（树宽 k 的 drafter 就是 n_sink + W + d·k），逻辑位置映射到 <code class="language-plaintext highlighter-rouge">ring_base(r) + pos mod (n_sink+W+d)</code>。RoPE 已经烘焙在存好的 key 里，块在缩减后的表里排第几不影响得分，所以这个替换对数值完全透明，而且 CUDA-graph 安全。<strong>省下来的 7.7–11.1% 显存直接变成多塞一个 1M 请求的能力</strong>：原生 MTP 在 B=6 就 OOM 的批次，窗口化能接着跑。</p> <p>唯一的实现代价：需要跑 Triton 后端做 ring 索引重映射（原生跑 FlashInfer）。后文有消融证明赢的是窗口不是后端。</p> <h3 id="32-为什么严格无损">3.2 为什么严格无损</h3> <p>这点必须单独讲清楚，因为它是所有”改草稿”方案里最容易被质疑的地方。</p> <p>投机解码的数学保证：<strong>输出分布永远由 target 的验收决定</strong>——每个草稿 token 都要过 target 的 rejection sampling，被接受的 token 是 target 自己采样的。草稿头只影响”提议什么”，<strong>不影响”接受什么”</strong>。</p> <p>所以改草稿头的 attention 不存在分布层面的损失。实测 greedy 输出的差异全部落在验收侧本来就有的 bf16 batched-verify 噪声带内。<strong>这是”改草稿”类方案的红利：可以放心对草稿做各种激进的近似，只要验收不动，正确性就有底。</strong>（对比那些改验收侧做 lossy verification 的工作，性质完全不同。）</p> <p><img src="/assets/img/posts/20260816-windmtp/figure1_p6.png" alt="草稿 attention 窗口化前后"/></p> <h3 id="33-为什么-acceptance-几乎不掉三层证据">3.3 为什么 acceptance 几乎不掉：三层证据</h3> <p>真正的问题是：把草稿的视野从 1M 砍到 4K，$\tau$ 为什么不崩？这是论文做得最扎实的部分。</p> <p><strong>第一层：剂量反应曲线（dose-response）。</strong> 窗口从 1K/2K/4K/8K/原生 扫过去，Qwen-35B（d=7，多 needle 检索任务）的接受长度 AL = 3.79 / 4.74 / 4.74 / 4.16 / <strong>4.49</strong>，TPOT = 4.89 / 3.85 / 3.85 / 4.50 / 6.04 ms。</p> <p>注意两个反直觉的点：<strong>4K 窗口的 AL（4.74）比原生全量（4.49）还高一点</strong>，同时 TPOT 从 6.04 降到 3.85。窗口不是”伤质量换速度”，而是存在一个<strong>联合最优</strong>——太小（1K）不够用，太大（8K/原生）除了变慢没有额外收益。</p> <p><strong>第二层：逐位置条件接受率 $\alpha_j$。</strong> 论文用 SGLang 的逐请求接受直方图 $H[n]$（每轮恰好接受 n 个草稿 token 的步数）定义：</p> \[\alpha_j = \Pr[\text{accept } j \mid \text{reached } j] = \frac{\sum_{n \ge j} H[n]}{\sum_{n \ge j-1} H[n]}, \qquad \text{AL} = 1 + \sum_{j \ge 1} \prod_{k \le j} \alpha_k\] <p>窗口化后的 $\alpha_j$ 曲线在所有位置上都落在原生曲线的 95% Wilson 置信区间内；$\alpha_1$（只依赖局部预测的第一个位置）完全不变，差距只出现在需要远距离信息的深位置。<strong>伤不到要害。</strong></p> <p><strong>第三层：运行中 A/B 探针——草稿到底改没改主意？</strong> 直接在推理过程中对比窗口化前后草稿的 top-1 提议：<strong>86%–94% 的步骤完全不变</strong>。翻转发生在哪里？统计 flip margin（翻转处 top-1 与 top-2 的概率差）：Qwen-35B 和 Nemotron 的翻转集中在 margin≈0.49/0.54 的<strong>接近平局</strong>位置（全平均 margin 是 5.16/3.02）——本来就要掷硬币的地方，换不换窗口无所谓。唯一的例外是 Qwen-122B：它的翻转发生在 margin 6.80 的<strong>高置信</strong>位置——这个 drafter 是真的在用远距离上下文（它的 AL 也确实从 5.58 掉到 4.73），但 TPOT 依然净赚。</p> <p><strong>机制解释</strong>收尾：三个模型的草稿头都架在递归混合底座上（Qwen 系是 GDN-MoE，Nemotron 是 Mamba2-hybrid）。<strong>远距离信息本来就由递归状态携带</strong>，attention 只需要管局部。一个漂亮的证据：窗口内 attention 质量占比，Qwen-35B 0.748、Qwen-122B 0.805，而 Nemotron（NoPE）只有 <strong>0.160</strong>——它的草稿 attention 本来就不怎么依赖位置，top-1 提议照样保住 86%，因为远处信息在 Mamba2 状态里。</p> <p>一句话总结这个洞察：<strong>这类混合架构的草稿头根本不怎么用长程 attention，却一直在为它付全量的钱。</strong></p> <hr/> <h2 id="4-端到端效果">4. 端到端效果</h2> <h3 id="41-主结果1md7b1ruler-niah_multiquery">4.1 主结果（1M、d=7、B=1、RULER niah_multiquery）</h3> <table> <thead> <tr> <th>模型</th> <th>vs 原生 MTP</th> <th>vs dense</th> <th>AL 变化</th> <th>TPOT</th> </tr> </thead> <tbody> <tr> <td>Qwen3.6-35B</td> <td><strong>+53%</strong></td> <td>2.55×</td> <td>4.49→4.74</td> <td>5.94→3.89 ms</td> </tr> <tr> <td>Qwen3.5-122B</td> <td>+11%</td> <td>1.75×</td> <td>5.57→4.74</td> <td>6.09→5.46 ms</td> </tr> <tr> <td>Nemotron-3-120B</td> <td>+23%</td> <td>1.58×</td> <td>3.75→3.61</td> <td>8.77→7.15 ms</td> </tr> </tbody> </table> <p><img src="/assets/img/posts/20260816-windmtp/table2_p10.png" alt="端到端加速"/></p> <p>122B 那行值得单独看：AL 明明掉了（5.57→4.74），TPOT 还是降了 11%。<strong>成本端的赢大于接受端的输</strong>——这也是为什么不能只盯着 acceptance length 评价投机解码系统。</p> <p>更值得注意的是任务难度维度的规律：窗口优势随难度<strong>单调放大</strong>——简单多值任务 +4%（AL 5.9）、中等 VT 任务 +17%（AL 4.2）、困难 CWE 任务 <strong>+38%</strong>（AL 2.8）。原因很简单：低接受率场景原生 MTP 本来就在亏本，窗口把它的成本砍下来，等于把负收益救成正收益。<strong>越难的任务越该开窗口</strong>，这个方向性结论对线上配置很有指导意义。</p> <h3 id="42-窗口楔子随上下文增长">4.2 “窗口楔子”随上下文增长</h3> <p>Figure 3 定义了一个干净的指标——matched-acceptance 成本比（用 $\text{AL} \cdot \text{TPOT} = T_\text{iter}$ 消掉接受率，只留纯成本侧的 step_nat/step_win − 1），从 261K 扫到 1M：</p> <ul> <li>Qwen-35B：+27% → <strong>+43%</strong></li> <li>Qwen-122B：+24% → +30%</li> <li>Nemotron：+17% → +26%</li> <li>每步绝对节省接近翻倍（Qwen-35B：3.4→7.9ms）</li> </ul> <p><img src="/assets/img/posts/20260816-windmtp/figure3_p10.png" alt="窗口优势随上下文增长"/></p> <p><strong>上下文越长，省得越多</strong>——和行业”上下文越来越长”的方向完全同向。这也是为什么我说这个技巧会快速变成默认配置。</p> <h3 id="43-批量张量并行与硬件复现">4.3 批量、张量并行与硬件复现</h3> <ul> <li><strong>单卡 B200 批量扫描</strong>（Figure 4）：6 个面板里 5 个窗口化 Pareto 占优。Qwen-35B 单用户 218 tok/s（dense 的 2.2×），B=5 峰值吞吐 473 tok/s/GPU（dense 2.1×、原生 1.5×）。唯一例外是 Nemotron + FWE（接受率最低的任务）高并发下 dense 赢峰值吞吐——接受率太低时投机本身就不划算，这锅不该窗口背；</li> <li><strong>2×B200 TP2</strong>（Figure 5）：全批量、全模型 Pareto 占优，单用户比 dense 快 1.4–2.5×，吞吐天花板比原生再抬 1.15–1.25×；</li> <li><strong>H100 + FP8 复现</strong>（Table 4，公开 checkpoint RedHatAI/Qwen3.6-35B-A3B-FP8）：dense 12.26ms → 原生 7.48ms（1.64×）→ 窗口化 <strong>5.05ms（2.43×）</strong>，AL 还从 4.34 升到 4.53——换卡、换精度结论不变；</li> <li><strong>fp8-KV 扫描</strong>：窗口化全 cell 赢（+14–46%），但 KV 减半后 Qwen 的百分比收益按比例缩水（1M 处 +46%→+22%）——符合”税 ∝ KV 字节数”的预测，Nemotron 反而几乎不缩水。</li> </ul> <h3 id="44-后端消融赢的是窗口不是-triton">4.4 后端消融：赢的是窗口，不是 Triton</h3> <p>一个必须做的诚实消融：原生 MTP 跑 FlashInfer（快后端）8.13ms；原生切到 Triton 50.59ms（<strong>6 倍 handicap</strong>）；窗口化跑 Triton 5.37ms。<strong>窗口化在最慢的后端上仍然赢过原生在最快后端上的成绩</strong>——收益 attribution 清清楚楚。</p> <h3 id="45-best-depth-对比">4.5 best-depth 对比</h3> <p>每个方法都扫 $d \in {3,5,7,9}$ 取各自最优深度：窗口化对原生的优势区间 <strong>1.22–1.58×</strong>（Qwen-35B Code-QA 7.95→5.03ms）。没有任何一格的最优深度是 d=9——更深更慢；且 d=9 在 1M 下对 122B/Nemotron 直接 OOM 不可行，窗口化 + ring buffer 反而跑得动。</p> <hr/> <h2 id="5-我的-take">5. 我的 Take</h2> <p><strong>一，这篇是”草稿成本经济学”的当头一棒。</strong> 投机解码这个领域卷了两年”草稿怎么打”（打得更准、打得更长、打得更便宜），这篇提醒大家：<strong>草稿的执行成本本身也在被上下文长度通货膨胀侵蚀</strong>。$T_\text{draft}$ 在公式里从来不是常数——它是 $O(S)$ 的函数，只是短上下文时代把它当常数没出事而已。1M 时代，”草稿头自带全量 attention”就是个 bug 级的设计遗留。</p> <p><strong>二，它顺手示范了”改草稿”方案的正确姿势。</strong> 验收侧不动 = 无损保证白拿；然后剂量反应、逐位置 $\alpha_j$、A/B 探针三层证据把”为什么敢砍”论证到 kernel 级——尤其是”递归底座已经携带远距离信息，attention 只管局部”这个机制发现，对后续所有混合架构的草稿设计都有参考价值。</p> <p><strong>三，工程上这是个”没理由不合并”的 PR。</strong> 零训练、无新 kernel、严格无损、显存还倒赚 8–11%。我预期”草稿侧窗口化/稀疏化”会像 target 侧的窗口化一样迅速变成推理栈默认配置。和 target 侧的稀疏化（我在 <a href="https://marsggbo.github.io/blog/2026/20260814-nsa/">NSA</a> 和 DeepSeek V3.2 DSA 的解读里聊过）对照着看会发现一个对称的图景：<strong>target 的 attention 在变稀，drafter 的 attention 也在变稀，两头一起把 decode 从 memory-bound 里往外拽。</strong></p> <hr/> <blockquote> <p>如果这篇文章涉及的投机解码、长上下文推理优化你想系统深入，可以看看我之前出版的<a href="https://item.jd.com/14945889.html">《动手学AutoML：从 NAS 到大语言模型优化实战》</a>，书里有专门一章讲 LLM 推理效率与 KV Cache 优化，本文讨论的 decode 阶段 memory-bound 问题正是那一章的主线。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="投机解码"/><category term="长上下文"/><category term="推理优化"/><category term="论文解读"/><summary type="html"><![CDATA[arXiv’26 | Wind-MTP：百万上下文里，MTP 草稿头才是那个隐形税]]></summary></entry></feed>