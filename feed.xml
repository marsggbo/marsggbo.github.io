<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://marsggbo.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://marsggbo.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-07-15T02:50:19+00:00</updated><id>https://marsggbo.github.io/feed.xml</id><title type="html">HE Xin</title><subtitle>&quot;SSS: Study, Sleep, Slim&quot; </subtitle><entry><title type="html"></title><link href="https://marsggbo.github.io/blog/2026/2026-06-07-2605_29350/" rel="alternate" type="text/html" title=""/><published>2026-07-15T02:50:19+00:00</published><updated>2026-07-15T02:50:19+00:00</updated><id>https://marsggbo.github.io/blog/2026/2026-06-07-2605_29350</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/2026-06-07-2605_29350/"><![CDATA[<blockquote> <p>插播：之前写的<a href="https://item.jd.com/14945889.html">《动手学 AutoML》</a>终于出版了，从 NAS 到超参优化都有覆盖，适合想系统入门 AutoML 的同学。好了广告结束，现在进入正题。</p> <p><img src="/assets/img/book_cover_automl.png" alt="动手学AutoML书籍封面"/></p> </blockquote> <h1 id="moe-压缩新思路别删专家别合并专家把它们重映射就够了">MoE 压缩新思路：别删专家、别合并专家，把它们”重映射”就够了</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2605.29350">ConMoE: Expert-Pool Consolidation via Prototype Reassignment for MoE Compression</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有想过，MoE 架构最骄傲的地方，恰恰也是最烫手的地方？</p> <p>MoE（Mixture-of-Experts）的核心设计是稀疏激活——每个 token 只走少数几个 expert，理论上参数量很大但计算量很小。Qwen3-30B-A3B 的名字里 A3B 的意思就是”激活参数 3B”，但总参数有 30B。听起来很美，对吧？</p> <p>但问题来了：<strong>这 30B 参数，你一个都不能少地加载进显存</strong>。</p> <p>不管一个 token 实际上只用了 3B 参数，那剩下 27B 的专家还是得老老实实待在显存里候命。这就是 MoE 推理的核心矛盾：稀疏激活省了算力，但显存一分没省。随着模型越做越大，expert 存储变成了 MoE 落地的最大障碍之一。</p> <p>现有的压缩思路主要两条路：<strong>剪掉专家（pruning）</strong> 和 <strong>合并专家（merging）</strong>。前者直接删掉一些专家，后者把多个专家的权重”揉”成一个新专家。</p> <p>今天要聊的这篇论文提了第三条路：<strong>什么都不删，什么都不合并，只是把原来的专家引用”重新指向”一个更小的专家池</strong>。</p> <p>听起来像绕口令？别急，慢慢来。</p> <hr/> <h2 id="2-现有方案的痛点">2. 现有方案的痛点</h2> <p>在聊 ConMoE 之前，先交代下背景，说说 pruning 和 merging 各自的问题。</p> <p><strong>Expert Pruning（专家剪枝）</strong>：直接删掉贡献低的专家。简单粗暴，但问题是删了就是删了，原来指向这个专家的 router 决策没有任何”容错”——被删专家的那些 token 只能丢给其他留下来的专家，不管合不合适。</p> <p><strong>Expert Merging（专家合并）</strong>：把多个专家的权重融合成一个新专家。听起来更优雅，但实际上构造了一个”原来不存在”的新权重，这个新权重可能引入了原本没有的错误，而且融合方式很敏感，调参难度大。</p> <p>这两类方法有一个共同问题：<strong>把”保留哪些专家参数”和”原来的 expert slot 该怎么被表示”这两件事耦合在一起了</strong>。</p> <p>ConMoE 的作者们说，等等，这两个问题应该分开来看。</p> <hr/> <h2 id="3-conmoe-的核心思路专家池整合expert-pool-consolidation">3. ConMoE 的核心思路：专家池整合（Expert-Pool Consolidation）</h2> <p>论文提出了一个新的框架视角，叫 <strong>expert-pool consolidation</strong>（专家池整合）。</p> <p>核心思想拆成两步：</p> <ol> <li>从原来的专家池里<strong>挑出一个更小的子集</strong>作为”原型（prototype）”</li> <li>把每个原来的专家引用<strong>确定性地重映射</strong>到某个 prototype 上</li> </ol> <p>这样，router 对外还是看到原来那些 expert slot，内部只是把调用转发给对应的 prototype。<strong>专家的权重没有被修改，没有被融合，prototype 就是原来的 pretrained 专家</strong>。</p> <p>如下图，直观地展示了 ConMoE 的整体流程：</p> <p><img src="/assets/img/posts/2605_29350/figure1_p3.png" alt="ConMoE 整体架构示意"/></p> <p>图里的流程很清晰：左边是预训练的 MoE，有若干层，每层有自己的专家池。ConMoE 在若干相邻层构成的 local scope 里，用 calibration 统计数据和专家间的距离来选出 prototype，然后把原来的每个专家 slot 都确定性地指向某个 prototype。最右边压缩后的 MoE 对外接口和原来一样（router 不变），只是内部的专家池变小了。</p> <p>这里有一个很巧妙的地方：<strong>prototype 可以跨层共享</strong>。也就是说，相邻几层的专家可以共用一个 prototype 池，这让压缩比更高，同时也利用了相邻层之间专家的冗余性。</p> <hr/> <h2 id="4-怎么挑-prototype两个信号的乘积">4. 怎么挑 prototype？两个信号的乘积</h2> <p>这是方法里最关键的设计。对于每个 local scope $G$，ConMoE 给每个专家算一个分数 $s_e$，然后取 top-K 作为 prototype。</p> <p>分数由两个信号构成：</p> <p><strong>信号一：路由条件贡献分（contribution score）$a_e$</strong></p> \[a_e = \frac{1}{|\mathcal{D}_e|} \sum_{t \in \mathcal{D}_e} g_e(t) \cdot \|e(h_t)\|_2\] <p>说人话：在 calibration 数据上，这个专家被 router 选中的那些 token，它的输出强度乘以路由权重的平均值。分数高 = 这个专家被用到时贡献大。</p> <p><strong>信号二：可替换性分（replaceability score）$b_e$</strong></p> \[b_e = \min_{e' \in \mathcal{E}_G \setminus \{e\}} d(e, e')\] <p>就是这个专家在 scope 内，到其最近邻专家的距离。距离越大 = 越难被其他专家替代 = 越不可替换。</p> <p>两个分数各自在 scope 内做 min-max 归一化后相乘：</p> \[s_e = \bar{a}_e \cdot \bar{b}_e\] <p><strong>直觉很清晰</strong>：一个好的 prototype，既要在原来模型里有实质性的贡献（不能是闲置专家），又要在参数空间里相对独特（不能被其他专家轻易替代）。两个维度同时满足，才是值得被保留的 prototype。</p> <p>选好 prototype 后，每个原始专家就找自己最近的 prototype：</p> \[m_G(e) = \arg\min_{p \in P_G} d(e, p)\] <p>这就是重映射。干净，确定，不需要梯度，不需要 fine-tune。</p> <hr/> <h2 id="5-跨层共享scope-大了不一定好">5. 跨层共享：scope 大了不一定好</h2> <p>ConMoE 允许相邻几层共享一个 prototype 候选池（local scope），这让压缩更灵活。但 scope 到底设多大合适？</p> <p>如下图是在 Qwen3-30B-A3B 上做的消融，50% expert reduction 的情况下，不同 scope size 的效果：</p> <p><img src="/assets/img/posts/2605_29350/figure2_p7.png" alt="scope size 对性能的影响"/></p> <p>结果很有趣：scope 1（只在单层内选）、scope 2、scope 4 的表现非常接近，scope 4 在这个代表性子集上平均分最高。但 scope 一旦扩到 8 或 16 层，三个任务的性能都出现明显下降，HellaSwag 和 MMLU 掉得尤其多。</p> <p>这说明什么？<strong>相邻层之间确实存在可复用的冗余，但这种冗余是局部的</strong>。距离太远的层，对应的是模型不同深度的语义转换，强行共享 prototype 会引入深度不匹配的问题。</p> <p>这也解释了为什么 ConMoE 选择 bounded local scope，而不是一个全局的 expert pool。</p> <hr/> <h2 id="6-主实验结果">6. 主实验结果</h2> <p>实验在三个 MoE 模型上做了对比：Qwen3-30B-A3B、deepseek-moe-16b-base、OLMoE-1B-7B-0125，各自测了 25% 和 50% 的 expert reduction。Baseline 包括：</p> <ul> <li><strong>Pruning 类</strong>：Frequency pruning（按路由频率删）、REAP pruning（基于贡献分删）</li> <li><strong>Merging 类</strong>：M-SMoE（路由统计引导合并）、HC-SMoE（输出特征聚类合并）</li> </ul> <p>如下表是主要实验结果（6 个多选题 benchmark 的平均分）：</p> <p><img src="/assets/img/posts/2605_29350/table1_p6.png" alt="主实验结果表"/></p> <p>几个关键观察：</p> <ul> <li><strong>DeepSeek 上，ConMoE 在 25% 和 50% reduction 两个设置下都拿到最佳平均分</strong>（0.691 vs 0.681/0.676，0.651 vs 0.642/0.613），这是最亮眼的结果。</li> <li><strong>OLMoE 上，25% reduction 同样是 ConMoE 最好</strong>（0.676 vs 0.669/0.662），50% 时和最强 baseline 很接近。</li> <li><strong>Qwen3 上，pruning 方法特别强</strong>，尤其是 50% reduction 时 Frequency pruning 达到 0.733，ConMoE 是 0.717。但 ConMoE 在 WinoGrande 和 BoolQ 上保持了很强的表现。</li> </ul> <p>整体来说，ConMoE 作为一种全新的”不删不合并、只重映射”的压缩范式，<strong>在和 pruning/merging 同等压缩比下能打平甚至超过</strong>，这本身就是一个有意义的结论。</p> <hr/> <h2 id="7-消融实验哪个设计最重要">7. 消融实验：哪个设计最重要？</h2> <h3 id="71-整合结构consolidation-structure">7.1 整合结构（Consolidation Structure）</h3> <p>如下表对比了不同的整合方式：</p> <p><img src="/assets/img/posts/2605_29350/table2_p6_04.png" alt="整合结构消融实验"/></p> <p>三种 variant：</p> <ul> <li><strong>Layer-local</strong>：只在单层内选 prototype，不跨层</li> <li><strong>Cross-layer fixed-k</strong>：跨层候选池，但每层的 prototype 数量固定</li> <li><strong>Adaptive prototype</strong>：ConMoE 默认方案，用贡献分+可替换性自适应分配</li> </ul> <p>有意思的是，layer-local 已经很强了（ARC-C 0.515，HellaSwag 0.715，MMLU 0.651）。允许跨层候选池（Cross-layer fixed-k）在 HellaSwag 上略有提升，Adaptive prototype 和 Cross-layer fixed-k 表现接近。</p> <p><strong>结论：确定性重映射本身才是最重要的成分</strong>，跨层和自适应选择是锦上添花。</p> <h3 id="72-prototype-选择策略">7.2 Prototype 选择策略</h3> <p>如下表比较了不同的 prototype 选择信号：</p> <p><img src="/assets/img/posts/2605_29350/table3_p7_08.png" alt="prototype 选择消融"/></p> <ul> <li><strong>Distance-only</strong>（只看可替换性）：ARC-C 0.242，HellaSwag 0.319，MMLU 0.231 ——直接崩了</li> <li><strong>Usage top-k</strong>（只看路由频率）：HellaSwag 掉到 0.586，比较弱</li> <li><strong>REAP top-k</strong>（基于贡献分）：HellaSwag 回到 0.718，接近 ConMoE 默认</li> <li><strong>Adaptive prototype</strong>（ConMoE 默认）：HellaSwag 0.716，与 REAP top-k 相当</li> </ul> <p><strong>路由条件贡献分是主导信号</strong>，可替换性是辅助正则化（防止选出重复的 prototype），单独用距离作为选择标准完全行不通。</p> <h3 id="73-事后权重融合诊断">7.3 事后权重融合诊断</h3> <p>有人会问：选好 prototype 之后，能不能再做一下权重融合（把分配给同一 prototype 的那些原始专家的权重平均一下），效果会不会更好？</p> <p>如下表给出了答案：</p> <p><img src="/assets/img/posts/2605_29350/table4_p7.png" alt="事后权重融合诊断"/></p> <p>对比三种方案：</p> <ul> <li><strong>None（ConMoE 默认）</strong>：不做任何融合，ARC-C 0.504，HellaSwag 0.716，MMLU 0.639</li> <li><strong>Arcee fusion</strong>：HellaSwag 从 0.716 直接掉到 0.531，MMLU 从 0.639 掉到 0.512</li> <li><strong>Weighted average</strong>：更惨，HellaSwag 0.469，MMLU 0.358</li> </ul> <p><strong>ConMoE 的收益来自确定性重映射，而不是权重融合</strong>。直接复用原始的 pretrained prototype 才是最稳的策略，加了融合反而破坏了原始权重。</p> <hr/> <h2 id="8-跨层专家可替换性分析">8. 跨层专家可替换性分析</h2> <p>为什么跨层 scope 有效，但又不能太大？论文专门做了一个几何分析。</p> <p>在 Qwen3-30B-A3B 上，用 4 层 scope 计算每个 expert 在参数空间的最近邻，统计最近邻是否在同一层还是不同层：</p> <p><img src="/assets/img/posts/2605_29350/figure3_p8.png" alt="跨层最近邻分析"/></p> <p>左图是热力图，展示从 source layer 到最近邻 layer 的分布。可以看到主要沿对角线集中，说明最近邻大概率在附近的层，而非全局随机分布。</p> <p>右图是每层中”最近邻在不同层”的比例。<strong>整体上有 50.4% 的 expert，其参数空间的最近邻在另一层</strong>，部分中间层超过 70%。</p> <p>这两个观察共同说明：<strong>专家的可替换性不是严格层内的，相邻层之间存在真实的冗余</strong>——这是跨层共享 prototype 的几何依据。但这种冗余是局部的，不支持全局共享。</p> <hr/> <h2 id="9-总结">9. 总结</h2> <p>ConMoE 提供了一个很干净的框架来思考 MoE 压缩：</p> <ul> <li><strong>不删</strong>（pruning 的做法）</li> <li><strong>不改权重</strong>（merging 的做法）</li> <li><strong>只重映射</strong>：选出 pretrained 专家子集作为 prototype，把其他专家的引用指过去</li> </ul> <p>三个设计原则从实验里验证：</p> <ol> <li><strong>确定性重映射是最稳定的成分</strong>，单层内做都已经很强</li> <li><strong>跨层复用要保持局部</strong>，相邻层可以，太远的层会因深度不匹配掉性能</li> <li><strong>路由条件贡献分是主导信号</strong>，可替换性是辅助的正则化</li> </ol> <p>个人觉得这篇论文最有价值的点，是把”保留哪些专家参数”和”原来的 expert slot 怎么被表示”这两个问题<strong>显式地解耦</strong>了。这个视角在之前的 pruning/merging 框架里是隐含的，ConMoE 把它明确化之后，整个设计空间就清晰多了。</p> <p>当然 limitation 也直接：这套方案依赖 calibration 数据，跨层 scope 不能太大，而且现在的”logical reduction ratio”和实际的显存节省还需要专门的 runtime 支持（shared-prototype checkpoint 格式）才能落地。最后这点是整个 prototype sharing 方案的工程挑战，不只是这篇论文的问题。</p> <p>感兴趣的可以去翻原文，代码和复现脚本作者也有提到会开源。</p> <p>欢迎评论区交流，有什么遗漏或者理解有误的地方也欢迎指出~</p>]]></content><author><name></name></author></entry><entry><title type="html">长音频转写慢 10 倍、99% 的 Attention 在白做功——MURMUR 是怎么解决的</title><link href="https://marsggbo.github.io/blog/2026/20260713-murmur-asr/" rel="alternate" type="text/html" title="长音频转写慢 10 倍、99% 的 Attention 在白做功——MURMUR 是怎么解决的"/><published>2026-07-13T00:00:00+00:00</published><updated>2026-07-13T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260713-murmur-asr</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260713-murmur-asr/"><![CDATA[<h1 id="长音频转写慢-10-倍99-的-attention-在白做功murmur-是怎么解决的">长音频转写慢 10 倍、99% 的 Attention 在白做功——MURMUR 是怎么解决的</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2606.01483">MURMUR: An Efficient Inference System for Long-Form ASR</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有遇到过这种场景：一小时的会议录音，想转成带时间戳和说话人标注的文字稿，发现要等将近 6 分钟才出来？</p> <p>现在语音识别领域其实分两派：</p> <p><strong>切片派（WhisperX 为代表）</strong>：把录音切成 30 秒小段，并行跑 ASR，再拼起来。速度快，但跨片段的说话人和时间戳对不上，识别质量差。</p> <p><strong>全段派（VibeVoice-ASR 为代表）</strong>：整段音频一次性塞进长上下文模型，自回归输出文字+时间戳+说话人，质量好，但慢整整 10 倍。而且有个更致命的问题——<strong>自回归模型有时候会陷入”重复循环崩溃”，整段录音直接废掉</strong>（后面详细说）。</p> <p>MURMUR 这篇论文做的事情是：在几乎不损失精度的前提下，把全段派的延迟压到只比切片派慢 2 倍左右——从 370.7 秒压到 88.43 秒，实现 <strong>4.2 倍加速</strong>。</p> <p>今天聊聊它的思路。</p> <hr/> <h2 id="2-背景几个必要的前置概念">2. 背景：几个必要的前置概念</h2> <h3 id="21-为什么长音频这么难">2.1 为什么长音频这么难？</h3> <p>现代 ASR 本质上是 Transformer 的 Encoder-Decoder（或 Decoder-only）结构。解码（生成文字）是<strong>逐 token 自回归</strong>的：生成第 1 个词，把这个词加入上下文，再预测第 2 个词……每一步都要 attend 到之前所有的 token。</p> <p>问题在于：序列越长，每步 decode 的 attention 计算量越大，KV cache 占用越多。一段 300 秒的音频比 30 秒多产出约 10× 的 token，但延迟不止增加 10×。</p> <p>这是长上下文模型慢的根本原因，也是 MURMUR 要优化的目标。</p> <h3 id="22-什么是-werdertcpwer">2.2 什么是 WER、DER、tcpWER？</h3> <p>先把这三个指标说清楚，不然看论文结果会云里雾里：</p> <ul> <li><strong>WER（Word Error Rate）</strong>：字词错误率，只看”说了什么”，不管谁说的、什么时间说的</li> <li><strong>DER（Diarization Error Rate）</strong>：说话人识别错误率，判断”谁在说话”</li> <li><strong>tcpWER（Time-Constrained minimum Permutation WER）</strong>：综合考虑”说了什么”、”谁说的”、”什么时间说的”，是最贴近实际场景的指标，也是这篇论文的核心优化目标</li> </ul> <p>WhisperX 的 WER 是 22.5%，VibeVoice-ASR 是 19.2%，差距看起来不大。但 tcpWER 上，WhisperX 是 35.5%，VibeVoice-ASR 是 25.7%——差了将近 10 个百分点。</p> <p><strong>这才是两种方案真正的差距所在</strong>：切片系统在处理说话人归属和时间戳时表现很差，这恰恰是会议记录、庭审转录等真实应用最关心的东西。</p> <p>如下图：</p> <p><img src="/assets/img/posts/20260713-murmur-asr/table1_p2.png" alt="WhisperX vs VibeVoice-ASR 精度与延迟"/></p> <h3 id="23-什么是重复循环崩溃">2.3 什么是”重复循环崩溃”？</h3> <p>这是自回归语言模型一个已知的 failure mode：<strong>解码器陷入循环，不停重复同一段短语或 token，直到达到最大长度上限，整段输出废掉。</strong></p> <p>想象模型在转写一段会议录音，转到某个片段时突然开始反复输出”好的好的好的好的好的……”再也停不下来——这就是重复循环崩溃。</p> <p><strong>为什么这个问题在长音频更严重？</strong></p> <p>在切片方案里，崩溃只影响一个 30 秒的片段，其他片段正常转写，损失可控。但在单次全段推理里，一旦某个时刻触发重复循环，整段 44 分钟的录音彻底报废，没有任何恢复手段。</p> <p>这不是小概率事件——在 AMI-IHM 的 14 段测试录音中，<strong>有 2 段因重复循环失败被排除在 VibeVoice-ASR 的评测结果之外</strong>。也就是说，那份”精度更好”的数字，实际上是在数据上做了筛选的。如果把崩溃的录音算进去（以 100% 错误率计），单次全段推理的实际效果比报告数字差很多。</p> <p>这也是为什么 MURMUR 采用 chunk-based 方案之后，真实部署场景下的精度反而比 single-pass 基线更好——因为 chunk 粒度把失败的影响限制住了。</p> <hr/> <h2 id="3-murmur-的整体架构">3. MURMUR 的整体架构</h2> <p>如下图，MURMUR 分三步走：VAD（语音活动检测）分块 → 并行跑长上下文 ASR 模型 → 拼接并做跨 chunk 的说话人对齐。</p> <p><img src="/assets/img/posts/20260713-murmur-asr/figure1_full.png" alt="三种 ASR 推理方式的架构对比"/></p> <p>核心优化分两个层次，原论文称为 <strong>inter-chunk（跨 chunk）</strong> 和 <strong>intra-chunk（chunk 内）</strong> 优化。</p> <ul> <li><strong>inter-chunk</strong>：chunk 之间的粒度设计——把 chunk 大小本身当一个可调超参数，找到精度和延迟的最优平衡点</li> <li><strong>intra-chunk</strong>：单个 chunk 内部——利用 attention 稀疏性做 KV cache eviction，在不影响精度的前提下减少每步 decode 的计算量</li> </ul> <hr/> <h2 id="4-inter-chunk-优化chunk-大小不是固定的-30-秒">4. Inter-chunk 优化：chunk 大小不是固定的 30 秒</h2> <p>传统 chunk-based 系统（如 WhisperX）用固定 30 秒切片，是因为 Whisper 本身的输入长度限制。但 VibeVoice-ASR 这类现代长上下文模型没有硬限制，chunk 大小就成了一个可以调的超参数。</p> <p>MURMUR 做了一个系统性实验，把 chunk size 从 30s 拉到 900s，观察各项指标的变化：</p> <p><img src="/assets/img/posts/20260713-murmur-asr/figure2_p3.png" alt="不同 chunk size 下的各项 error rate"/></p> <p><strong>关键发现</strong>：</p> <p>精度随 chunk 大小增加而提升，但到约 300 秒就基本饱和——继续增大 chunk，边际收益极小，而延迟持续增加。更有意思的是，在噪声较重的场景（AMI 会议录音）里，chunk 超过 300 秒后精度反而会下降：更长的上下文让模型需要追踪更多说话人的声纹，混淆概率反而增大。</p> <p>对于相对干净的音频（TED 演讲、财报电话），饱和点更早；对于嘈杂的会议录音，从更长的上下文中受益更多，但上限也就到 300 秒。</p> <p>下图更直观地展示了 chunk size 和延迟/精度的 trade-off：</p> <p><img src="/assets/img/posts/20260713-murmur-asr/figure3_p3.png" alt="延迟与精度 trade-off 曲线"/></p> <p><strong>MURMUR 选择 300 秒作为 chunk 上限</strong>。这一步本身就把延迟从 370.7 秒压到 100.8 秒，实现 <strong>3.68 倍加速</strong>，且精度不降反升（因为规避了单次推理的失败录音问题）。</p> <hr/> <h2 id="5-intra-chunk-优化kv-cache-里-75-以上的-speech-token-是冗余的">5. Intra-chunk 优化：KV Cache 里 75% 以上的 speech token 是冗余的</h2> <p>chunk 大小定好之后，MURMUR 进一步分析：能不能在单个 chunk 的 decode 过程里再压榨延迟？</p> <p>先 profile 了一段 44 分钟录音：</p> <ul> <li><strong>解码（decode）占总推理时间的 95.3%</strong></li> <li><strong>attention 操作占 decode 时间的 74.6%</strong></li> </ul> <p>所以不优化 attention / KV cache，其他什么都白优化。</p> <h3 id="51-kv-cache-里有哪些-token">5.1 KV Cache 里有哪些 token？</h3> <p>在 VibeVoice-ASR 这类 ASR 模型里，一次推理的输入序列结构大致是这样的：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[system prompt] [speech tokens] [system prompt] [text tokens（逐步生成）]
</code></pre></div></div> <p>其中涉及两类核心 token，<strong>它们的特性完全不同，需要分开处理</strong>：</p> <p><strong>Speech token（音频 token）</strong>：音频先经过 Whisper 风格的 encoder 压缩成特征向量，再展开成 token 序列——约 1 秒音频对应 7.5 个 token。300 秒的音频 ≈ 2,250 个 speech token。关键是：<strong>这些 token 在 prefill 阶段（推理开始时）就一次性全部写入 KV cache，之后每步 decode 都要 attend 它们，是固定的”静态负担”</strong>，不会增长但始终存在。</p> <p><strong>Output token（文本 token）</strong>：解码时逐步生成的文字 token。300 秒的会议音频大约会生成 ~2,700 个 output token，<strong>随着解码进行不断追加到 KV cache 里</strong>，是动态增长的部分。</p> <p>两类 token 在 KV cache 里的位置不同、attention 模式也完全不同，MURMUR 因此对它们设计了不同的 eviction 策略。</p> <h3 id="52-speech-token-的-attention-是稀疏的">5.2 Speech token 的 attention 是稀疏的</h3> <p>MURMUR 可视化了 attention 权重矩阵：</p> <p><img src="/assets/img/posts/20260713-murmur-asr/figure4_p4.png" alt="30s vs 300s chunk 的 attention heatmap"/></p> <p>左边是 30 秒 chunk 的 attention，比较密集；右边是 300 秒 chunk 的，出现了明显的<strong>对角线稀疏模式</strong>——解码推进到哪里，有效 attention 就集中在对应音频位置附近，前面已经转写完的音频基本用不到了。</p> <p>更精确的量化：</p> <p><img src="/assets/img/posts/20260713-murmur-asr/figure5_p4.png" alt="每层 KV cache 中各类 token 占比"/></p> <p><strong>在 28 层 Transformer 里的 24 层，保留不到 25% 的 speech token，就能覆盖 99% 的总 attention 权重</strong>。超过 75% 的 speech token 是可以安全驱逐的。</p> <p>直觉上也很自然：模型在转写”第 200 秒说了什么”时，不需要回头看”第 10 秒的音频”——那段已经处理完了。</p> <h3 id="53-两种-kv-cache-eviction-策略">5.3 两种 KV cache eviction 策略</h3> <p><strong>Output token eviction</strong></p> <p>Output token 的 attention 有两个规律：近端偏向（recency bias）——大部分权重集中在最近生成的 token 上；以及 attention sink——序列最开头的几个特殊 token 会吸收大量权重（这是 Transformer 的常见现象，模型把”不确定该 attend 什么”的权重都堆到了最前面的 token 上）。</p> <p>MURMUR 直接套 StreamingLLM 的方案：保留 4 个 sink token + 最近 1024 个 token，其余全驱逐。</p> <p><strong>Speech token eviction</strong></p> <p>Speech token 不能用同一套策略，因为它的有效 attention 区域是随着解码进度<strong>向后平移</strong>的（那个对角线），不是固定在最近端。</p> <p>MURMUR 设计了一个<strong>带延迟的滑动窗口</strong>：</p> <ul> <li>前 d 步（延迟阶段）：窗口保持在起点不动，让模型充分看到音频开头的上下文</li> <li>之后每解码一步：窗口向后移动一格，追踪解码进度</li> </ul> <p>参数：延迟 d = 512，窗口大小 w = 1024。</p> <p>论文里特别强调：<strong>可驱逐的 speech token 不是位置连续的</strong>，它们散落在各处，所以简单的头部/尾部截断不够用，必须用这套精确追踪的滑动窗口。</p> <hr/> <h2 id="6-实验结果">6. 实验结果</h2> <p>在 AMI-IHM 数据集、300 秒 chunk、batch size 16 下：</p> <p><img src="/assets/img/posts/20260713-murmur-asr/table2_full.png" alt="KV cache eviction 策略消融实验"/></p> <ul> <li><strong>MURMUR 基础版（仅 chunked，无 eviction）</strong>：延迟 100.8s，加速 3.68×，tcpWER 24.92%，略优于 single-pass（25.68%）</li> <li><strong>+ Output token eviction</strong>：延迟 97.6s，加速 3.79×，tcpWER 25.01%，几乎无损</li> <li><strong>+ Speech token eviction（d512 w1024）</strong>：延迟 88.43s，加速 <strong>4.20×</strong>，tcpWER 25.29%，比 single-pass 只多了 0.61%</li> <li><strong>两种 eviction 同时用</strong>：延迟 96.1s，加速 3.85×——反而比单独用 speech eviction 慢，因为同时管理两套 eviction 策略有额外 overhead，相互抵消了部分收益</li> </ul> <p>多数据集泛化结果如下：</p> <p><img src="/assets/img/posts/20260713-murmur-asr/figure7_p7.png" alt="各数据集 WER 随 chunk size 变化"/></p> <p>干净数据集（Tedlium3、Earnings21）在较小 chunk size 就饱和；嘈杂条件（IHM、SDM）受益于更长上下文，但上限也就到 300 秒。</p> <hr/> <h2 id="7-讨论">7. 讨论</h2> <p>MURMUR 的核心 insight 其实很简单：<strong>chunk size 不是固定参数，是超参数</strong>。传统方案非此即彼——30 秒切片或者全段一把梭，中间有大片值得探索的空间，这篇论文把它系统地测了一遍。这个贡献本身就有价值，即便没有 KV cache eviction，单纯把 chunk size 从 30 秒改到 300 秒就已经实现了 3.68 倍加速。</p> <p><strong>语音 token 的注意力稀疏性比通常预期的要高得多</strong>——75% 以上的 speech token 可以安全驱逐，而精度损失不到 1%。这和 LLM 推理优化里的注意力稀疏性观察是一脉相承的，但音频场景下的稀疏模式（对角线时序局部性）和纯文本不一样，在语音领域是相对新颖的系统性研究。</p> <p>有几个地方值得进一步追问：</p> <p><strong>一、speech eviction 的实际加速为什么远低于理论值？</strong> 论文的理论计算显示 speech token eviction 的注意力计算量能降约 1.06×，但实测加速比要更低，因为延迟阶段和 cache 管理 overhead 部分抵消了收益。更根本的原因在于：<strong>可驱逐的 speech token 不是位置连续的</strong>，当前实现需要把不连续的 token 先 gather 出来再算 attention，这个 gather 操作本身就有代价。如果换用稀疏 attention 的专用 kernel（类似 FlashAttention 的 block-sparse 变体），理论和实测之间的差距理应可以收窄。这个实现层面的优化空间目前论文没有展开。</p> <p><strong>二、eviction 策略是位置感知的，但不是内容感知的。</strong> 当前的滑动窗口假设”音频内容和解码进度是线性对应的”——解码推进到哪里，前面对应的音频就可以驱逐。但实际上，录音里某段 30 秒可能包含一个关键的说话人名字，后续所有说话人归属都依赖它——这段 speech token 不该被驱逐，但位置感知的策略不会区分。论文也承认这个局限，指出 query-aware 或 acoustic-feature-guided 的自适应 eviction 是值得探索的后续方向。一旦能精准识别”哪些 speech token 真正重要”，驱逐比例还能进一步提升。</p> <p><strong>三、两种 eviction 合用反而更慢，这个结果比较反直觉。</strong> 理论上 output token eviction 和 speech token eviction 针对的是 KV cache 的不同区域，应该可以正交叠加——加速比应该高于单独用任意一种。但实验结果是合用的加速比（3.85×）反而低于单独用 speech eviction（4.20×）。一个可能的解释是：两套策略的 cache 管理逻辑相互干扰，维护两套独立的 eviction 状态机的 overhead 是非线性叠加的。如果在底层统一成一套 KV cache 管理框架再做优化，这个 overhead 应该可以消除。</p> <p>如果你在做语音推理系统或者对 attention 稀疏性感兴趣，这篇值得读原文。欢迎评论区交流。</p> <hr/> <blockquote> <p>另外，我们团队最近出版了<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，感兴趣的话可以看看。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="ASR"/><category term="LLM"/><category term="KV Cache"/><category term="推理优化"/><category term="论文解读"/><summary type="html"><![CDATA[长音频转写慢 10 倍、99% 的 Attention 在白做功——MURMUR 是怎么解决的]]></summary></entry><entry><title type="html">Gemma 4 拆解 | 多模态怎么塞进端侧，MTP drafter 又是怎么自带投机采样的</title><link href="https://marsggbo.github.io/blog/2026/20260712-gemma4/" rel="alternate" type="text/html" title="Gemma 4 拆解 | 多模态怎么塞进端侧，MTP drafter 又是怎么自带投机采样的"/><published>2026-07-12T00:00:00+00:00</published><updated>2026-07-12T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260712-gemma4</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260712-gemma4/"><![CDATA[<h1 id="gemma-4-拆解--多模态怎么塞进端侧mtp-drafter-又是怎么自带投机采样的">Gemma 4 拆解 | 多模态怎么塞进端侧，MTP drafter 又是怎么自带投机采样的</h1> <blockquote> <p>原文：<a href="https://arxiv.org/pdf/2607.02770">Gemma 4 Technical Report</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>我看 LLM 技术报告有个习惯：跳过刷榜表格，直接翻到架构章节里那些”为了省内存/省算力我们做了什么妥协”的段落。因为<strong>真正决定一个模型能不能跑在你手机上的，从来不是它 MMLU 高了几个点，而是这些抠 overhead 的工程细节</strong>。</p> <p>Gemma 4 这份报告在这方面料很足。这一代最值得聊的两块，恰好都踩在”端侧效率”这个点上：</p> <ul> <li><strong>多模态</strong>：怎么把图像、音频塞进一个本来只吃文本的 decoder？还要支持任意宽高比，还要在最小的 2B 模型上跑得动？更狠的是它的 12B 版本干脆把 vision / audio encoder <strong>整个砍掉</strong>，走”encoder-free”路线。</li> <li><strong>MTP drafter</strong>：模型<strong>自带</strong>了一个用于投机采样（speculative decoding）的多 token 预测 drafter，而且设计上巧妙绕开了”drafter 需要单独 prefill”这个老大难。</li> </ul> <p>这篇就重点扒这两块，配简化源码帮你把数据流理顺。刷榜数据放最后，想看的自己翻。</p> <hr/> <h2 id="2-先认识一下这个模型家族">2. 先认识一下这个模型家族</h2> <p>Gemma 4 一共 5 个尺寸，跨度从手机端一路铺到单卡服务器：</p> <p><img src="/assets/img/posts/20260712-gemma4/t1_full.png" alt="Gemma 4 各模型参数拆解"/></p> <p>对着这张表说几句人话：</p> <ul> <li><strong>E2B / E4B</strong>：<code class="language-plaintext highlighter-rouge">E</code> 是 effective。沿用了 Gemma 3n 那套 per-layer embedding 的把戏——<strong>总参数 5B / 8B，但每步真正激活的只有 2.3B / 4.5B</strong>。这俩也是唯一自带 audio + vision encoder 的（各 305M + 150M），主打端侧全模态。</li> <li><strong>12B</strong>：注意它 audio / vision encoder 两列都是 <code class="language-plaintext highlighter-rouge">-</code>。因为它走的是 <strong>encoder-free 统一架构</strong>（第 3.3 节细讲），这是这代最激进的一处改动。</li> <li><strong>26B-A4B</strong>：带星号的是 <strong>MoE</strong>，24,500M 总参数但只激活 2,800M（约 3.8B active），典型的”稀疏激活”路线。</li> <li><strong>31B</strong>：最大的 dense 模型。</li> </ul> <p>最右边那列 <code class="language-plaintext highlighter-rouge">Drafter</code>——76M 到 500M 不等——就是本文要重点扒的 <strong>MTP drafter</strong>，每个主模型都单独配了一个。词表 262k。</p> <p>顺带提一句跟本文主线相关的省内存操作：global attention 层用 <code class="language-plaintext highlighter-rouge">p-RoPE</code>（p=0.25），并且<strong>在 global 层直接让 value 复用 key</strong>，把 global KV cache 砍掉 <strong>37.5%</strong>。这些都是端侧模型抠内存的常规手段——<code class="language-plaintext highlighter-rouge">OOM</code> 是这类模型永远的敌人。</p> <hr/> <h2 id="3-多模态怎么把图像和音频喂进一个文本模型">3. 多模态：怎么把图像和音频喂进一个文本模型</h2> <h3 id="31-核心矛盾变长宽比-vs-固定-token-预算">3.1 核心矛盾：变长宽比 vs 固定 token 预算</h3> <p>传统 ViT 的做法是把图 resize 成固定的 224×224 或 336×336，简单粗暴。但问题是：<strong>一张 16:9 的截图和一张竖屏文档，硬拉成正方形，信息全糊了</strong>；可你要是保留原始分辨率，token 数又会爆炸，端侧根本扛不住。</p> <p><strong>这就是多模态输入绕不过去的核心矛盾：既要保住宽高比，又要卡住 token 预算。</strong></p> <p>Gemma 4 的 vision encoder 是标准 ViT（patch size = 16），支持变长宽比，靠的是三件套：</p> <ol> <li><strong>axial 2D-RoPE</strong>——把 RoPE 拆成横、竖两个轴分别做旋转位置编码；</li> <li><strong>non-causal attention</strong>——图像 patch 之间当然要双向看；</li> <li><strong>2D 绝对位置编码</strong>。</li> </ol> <p>关键在于它怎么在这对矛盾之间做权衡。报告给了 <strong>Algorithm 1</strong>，逻辑其实非常朴素——<strong>先算一个理想缩放系数 f，让缩放后的总 patch 数刚好卡在预算 N_max 附近，再向下取整到 patch 的整数倍</strong>：</p> <p><img src="/assets/img/posts/20260712-gemma4/algorithm1_p16.png" alt="保持宽高比的图像缩放算法"/></p> <p><code class="language-plaintext highlighter-rouge">N_max</code> 只有 5 个离散档位：<code class="language-plaintext highlighter-rouge">{70, 140, 280, 560, 1120}</code>。翻译成代码就一目了然了：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">math</span>

<span class="k">def</span> <span class="nf">aspect_preserving_resize</span><span class="p">(</span><span class="n">H</span><span class="p">,</span> <span class="n">W</span><span class="p">,</span> <span class="n">p</span><span class="o">=</span><span class="mi">16</span><span class="p">,</span> <span class="n">k</span><span class="o">=</span><span class="mi">3</span><span class="p">,</span> <span class="n">n_max</span><span class="o">=</span><span class="mi">280</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    H, W : 原图高、宽
    p    : patch size (16)
    k    : pooling kernel size（相邻 k×k 个 patch 池化成 1 个 soft token）
    n_max: 目标 soft token 数上限
    </span><span class="sh">"""</span>
    <span class="n">m</span> <span class="o">=</span> <span class="n">k</span> <span class="o">*</span> <span class="n">p</span>                       <span class="c1"># 一个 pooled patch 覆盖的像素边长 = 48
</span>    <span class="n">T</span> <span class="o">=</span> <span class="n">n_max</span> <span class="o">*</span> <span class="p">(</span><span class="n">m</span> <span class="o">**</span> <span class="mi">2</span><span class="p">)</span>            <span class="c1"># 面积预算：缩放后总像素数的目标值
</span>
    <span class="n">f</span> <span class="o">=</span> <span class="n">math</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">T</span> <span class="o">/</span> <span class="p">(</span><span class="n">H</span> <span class="o">*</span> <span class="n">W</span><span class="p">))</span>      <span class="c1"># 理想缩放系数：让缩放后面积 ≈ T
</span>    <span class="n">H_ideal</span><span class="p">,</span> <span class="n">W_ideal</span> <span class="o">=</span> <span class="n">f</span> <span class="o">*</span> <span class="n">H</span><span class="p">,</span> <span class="n">f</span> <span class="o">*</span> <span class="n">W</span>

    <span class="c1"># 向下取整到 m 的整数倍，保证能被 patch + pooling 整除
</span>    <span class="n">H_target</span> <span class="o">=</span> <span class="p">(</span><span class="n">H_ideal</span> <span class="o">//</span> <span class="n">m</span><span class="p">)</span> <span class="o">*</span> <span class="n">m</span>
    <span class="n">W_target</span> <span class="o">=</span> <span class="p">(</span><span class="n">W_ideal</span> <span class="o">//</span> <span class="n">m</span><span class="p">)</span> <span class="o">*</span> <span class="n">m</span>

    <span class="k">return</span> <span class="nf">bicubic_resize</span><span class="p">(</span><span class="n">image</span><span class="p">,</span> <span class="n">H_target</span><span class="p">,</span> <span class="n">W_target</span><span class="p">)</span>
</code></pre></div></div> <p>报告里的例子很直观（下图）：一张 572×1024（约 1:1.79）的图，在 <code class="language-plaintext highlighter-rouge">k=3, N_max=10</code> 的设定下被缩成 <strong>2×4 个 pooled patch</strong>（每个 48px），也就是 96×192；这对应 <strong>72 个 16px 的 patch</strong>，过完 vision encoder 后再做 <strong>3×3 pooling</strong>，最终只剩 <strong>8 个 soft token</strong> 喂给 LLM backbone。</p> <p><img src="/assets/img/posts/20260712-gemma4/fig2_resize.png" alt="图像缩放示意：从整图到 soft token"/></p> <p><strong>注意这条链路的巧劲</strong>：72 个 patch 进 encoder，出来 pool 成 8 个 token 进 LLM。也就是说 vision encoder 内部照常用较多 patch 保住空间分辨率，但真正占用 LLM 上下文的只有池化后那 8 个 token——<strong>算力花在 encoder 里，上下文预算省在 LLM 里</strong>。这个 trade-off 拿捏得很到位。</p> <p>vision encoder 本身有两个尺寸（Table 10）：150M 版是 <code class="language-plaintext highlighter-rouge">d_model=768 / d_MLP=3072 / 12 heads / 16 层</code>，550M 版是 <code class="language-plaintext highlighter-rouge">1152 / 4304 / 16 heads / 27 层</code>。E2B/E4B 用小的，大模型用大的。</p> <h3 id="32-音频直接吃连续表征不做-vq">3.2 音频：直接吃连续表征，不做 VQ</h3> <p>音频这块，E2B/E4B 用的是 <strong>305M 的 USM-based conformer encoder</strong>：40ms 一个 chunk，Mel filterbank 输入，2 层下采样卷积 + 12 层 Conformer。相比 Gemma 3n 的 680M <strong>砍了 55%</strong>。</p> <p>有个设计选择值得单独拎出来说：<strong>它不做 vector quantization（VQ）</strong>，LLM 直接吃 encoder 输出的连续表征。这跟很多”把音频离散化成 token”的方案是反着来的——VQ 会引入量化损失，直接喂连续 embedding 保真度更高，代价是接口上得多挂一个 projection。encoder 在预训练阶段是<strong>冻结的</strong>。</p> <h3 id="33-重头戏encoder-free-的-12b--把-encoder-直接砍了">3.3 重头戏：Encoder-free 的 12B —— 把 encoder 直接砍了</h3> <p>前面 E2B/E4B 还是”外挂 encoder”的经典范式。但 <strong>12B 这版是从头训练的、统一的 encoder-free 架构</strong>，把独立的 vision / audio encoder 换成了<strong>轻量 projection</strong>。这是我觉得整份报告最激进、也最能说明趋势的一处。</p> <p><strong>视觉侧</strong>：12B 直接吃 <code class="language-plaintext highlighter-rouge">48×48×3</code> 的 RGB patch，然后——注意——把原来 550M 的 vision encoder 换成<strong>一个大矩阵乘法（35M 参数）</strong>。空间信息靠给 patch 表征加 <strong>2D 坐标位置编码</strong>、再过一层 LayerNorm 来维持。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">torch</span>
<span class="kn">import</span> <span class="n">torch.nn</span> <span class="k">as</span> <span class="n">nn</span>

<span class="k">class</span> <span class="nc">EncoderFreeVision</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">12B 的 encoder-free 视觉输入：不要 ViT，一个大 matmul 搞定</span><span class="sh">"""</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">d_model</span><span class="p">,</span> <span class="n">patch</span><span class="o">=</span><span class="mi">48</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="c1"># 原来一整个 550M ViT → 现在就一个 (48*48*3 → d_model) 的线性层（~35M）
</span>        <span class="n">self</span><span class="p">.</span><span class="n">proj</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">patch</span> <span class="o">*</span> <span class="n">patch</span> <span class="o">*</span> <span class="mi">3</span><span class="p">,</span> <span class="n">d_model</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">ln</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">LayerNorm</span><span class="p">(</span><span class="n">d_model</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">patches</span><span class="p">,</span> <span class="n">coords</span><span class="p">):</span>
        <span class="c1"># patches: [N, 48*48*3]  已拍平的 RGB patch
</span>        <span class="c1"># coords : [N, d_model]  每个 patch 的 2D 坐标位置编码
</span>        <span class="n">x</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">proj</span><span class="p">(</span><span class="n">patches</span><span class="p">)</span>          <span class="c1"># 一次大矩阵乘 = 全部"视觉编码"
</span>        <span class="n">x</span> <span class="o">=</span> <span class="n">x</span> <span class="o">+</span> <span class="n">coords</span>                  <span class="c1"># 注入 2D 空间感知
</span>        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">ln</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>               <span class="c1"># 直接进 LLM backbone
</span></code></pre></div></div> <p><strong>音频侧更狠</strong>：那个 305M 的 USM conformer encoder <strong>被整个丢掉</strong>。原始音频按 40ms、16kHz 切 chunk，每个 chunk 就是个 <strong>640 维向量</strong>，直接 projection 进 LLM 的 embedding 空间。因为音频本身是时序的，连额外的位置编码都省了。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">EncoderFreeAudio</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">原始波形 → 640 维/chunk → 直接投影进 LLM，没有 conformer</span><span class="sh">"""</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">d_model</span><span class="p">,</span> <span class="n">chunk_dim</span><span class="o">=</span><span class="mi">640</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">proj</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">chunk_dim</span><span class="p">,</span> <span class="n">d_model</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">audio_chunks</span><span class="p">):</span>
        <span class="c1"># audio_chunks: [T, 640]  每个 40ms chunk 一个向量
</span>        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">proj</span><span class="p">(</span><span class="n">audio_chunks</span><span class="p">)</span>  <span class="c1"># 就这一步，时序性天然保留，无需 PE
</span></code></pre></div></div> <p>这背后的思路，本质上还是：<strong>当 LLM backbone 足够强，”模态编码”这件事可以从一个厚重的独立网络，退化成一个 projection 层，让 backbone 自己去学怎么理解 pixel 和 waveform</strong>。省下来的是参数、是显存、是端侧部署的复杂度。代价当然也有——得从头训、要更多数据和算力去 train 这个统一模型。说白了就是<strong>把复杂度从推理侧搬到了训练侧</strong>，而对端侧场景来说，这笔账是划算的。</p> <hr/> <h2 id="4-mtp-drafter给模型自带一个投机采样的打草稿小模型">4. MTP drafter：给模型自带一个投机采样的”打草稿”小模型</h2> <h3 id="41-先说清楚投机采样在解决什么">4.1 先说清楚投机采样在解决什么</h3> <p>自回归解码天生是<strong>串行</strong>的：一次前向只出一个 token，<code class="language-plaintext highlighter-rouge">GPU</code> 在 decode 阶段大部分算力其实是闲着的（memory-bound）。投机采样（speculative decoding）的思路是：<strong>用一个便宜的小模型（drafter）先一口气猜出未来 k 个 token，再让大模型一次前向并行地”验收”这 k 个</strong>。猜对的部分直接白嫖，猜错的从第一个错的地方截断重来。</p> <p>只要 drafter 猜得够准（acceptance rate 高）、又足够便宜，整体 decode 速度就能翻倍。EAGLE（Li et al., 2024）这一系的做法是让 drafter 复用大模型的 <strong>feature / 隐层激活</strong>来提高命中率——Gemma 4 的 MTP drafter 走的就是这条线。</p> <h3 id="42-gemma-4-的-mtp-结构cross-attend-主模型的-kv免-prefill">4.2 Gemma 4 的 MTP 结构：cross-attend 主模型的 KV，免 prefill</h3> <p>先看图，理解它跟主模型的耦合方式：</p> <p><img src="/assets/img/posts/20260712-gemma4/figure1_p3.png" alt="自回归 MTP drafter 结构"/></p> <p>左边灰色是主模型（Layer 1 … Layer N），右边蓝色是 MTP drafter。核心机制报告说得很干脆（2.6 节），我提炼成三点：</p> <ol> <li><strong>输入</strong>：主模型<strong>上一步的 last-layer 激活</strong> + <strong>token embedding</strong>，一起喂进 MTP head。</li> <li><strong>结构</strong>：drafter 是一个<strong>独立的 embedder + 4 层 Transformer block</strong>，它<strong>cross-attend 到主模型的 KV cache</strong>（图里红色 attention 箭头指向 <code class="language-plaintext highlighter-rouge">Last Prefilled Local/Global Layer</code>）。</li> <li><strong>最关键的收益</strong>：正因为它去 cross-attend 主模型现成的 KV，<strong>它自己不需要单独 prefill，而且支持任意 draft 长度</strong>。</li> </ol> <p>第 3 点是我觉得这个设计最漂亮的地方。很多 drafter 方案要么本身是个独立小模型（得自己把 prompt prefill 一遍，长 prompt 下这笔开销不小），要么像 Medusa 那样加多个预测头、但受限于固定结构。Gemma 4 让 drafter <strong>直接寄生在主模型的 KV 上</strong>——prompt 的信息在主模型 prefill 时已经算好并缓存了，drafter 白嫖就行。</p> <p>结构规格：Transformer block 的 model dim，E2B/E4B 是 <strong>256</strong>，26B-A4B/31B 是 <strong>1024</strong>；每个都是 <strong>3 层 local + 1 层 global</strong> attention。</p> <p>用简化代码把这个 forward 串一遍（省了 rope、norm 等细节，只保留数据流）：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">MTPDrafter</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">自带投机采样的 MTP drafter：cross-attend 主模型 KV，免 prefill</span><span class="sh">"""</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">d_main</span><span class="p">,</span> <span class="n">d_draft</span><span class="o">=</span><span class="mi">256</span><span class="p">,</span> <span class="n">n_layers</span><span class="o">=</span><span class="mi">4</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">embed</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Embedding</span><span class="p">(</span><span class="n">VOCAB</span><span class="p">,</span> <span class="n">d_draft</span><span class="p">)</span>     <span class="c1"># 独立 embedder
</span>        <span class="c1"># 把 [主模型激活, token embedding] 拼起来再降维到 drafter 维度
</span>        <span class="n">self</span><span class="p">.</span><span class="n">down_proj</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">d_main</span> <span class="o">+</span> <span class="n">d_draft</span><span class="p">,</span> <span class="n">d_draft</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">layers</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">ModuleList</span><span class="p">([</span>
            <span class="nc">DraftBlock</span><span class="p">(</span><span class="n">d_draft</span><span class="p">,</span> <span class="n">cross_kv_dim</span><span class="o">=</span><span class="n">d_main</span><span class="p">)</span>   <span class="c1"># 每层都能 cross-attend 主模型 KV
</span>            <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">n_layers</span><span class="p">)</span>                   <span class="c1"># 3 local + 1 global
</span>        <span class="p">])</span>
        <span class="n">self</span><span class="p">.</span><span class="n">up_proj</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">d_draft</span><span class="p">,</span> <span class="n">d_main</span><span class="p">)</span>      <span class="c1"># 投回主模型空间
</span>
    <span class="k">def</span> <span class="nf">draft_one_step</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">prev_hidden</span><span class="p">,</span> <span class="n">prev_token_id</span><span class="p">,</span> <span class="n">main_kv</span><span class="p">):</span>
        <span class="c1"># prev_hidden  : [B, d_main]  主模型上一步的 last-layer 激活
</span>        <span class="c1"># prev_token_id: [B]          上一步接受的 token
</span>        <span class="c1"># main_kv      : 主模型 prefill 好的 KV cache，直接复用，无需重算
</span>        <span class="n">tok</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">embed</span><span class="p">(</span><span class="n">prev_token_id</span><span class="p">)</span>                <span class="c1"># [B, d_draft]
</span>        <span class="n">x</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">down_proj</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">([</span><span class="n">prev_hidden</span><span class="p">,</span> <span class="n">tok</span><span class="p">],</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">))</span>
        <span class="k">for</span> <span class="n">layer</span> <span class="ow">in</span> <span class="n">self</span><span class="p">.</span><span class="n">layers</span><span class="p">:</span>
            <span class="n">x</span> <span class="o">=</span> <span class="nf">layer</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">cross_kv</span><span class="o">=</span><span class="n">main_kv</span><span class="p">)</span>             <span class="c1"># ← 寄生在主模型 KV 上
</span>        <span class="n">h</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">up_proj</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>                            <span class="c1"># [B, d_main]
</span>        <span class="k">return</span> <span class="n">h</span>                                        <span class="c1"># 再过主模型 unembed 出下一个草稿 token
</span>
    <span class="nd">@torch.no_grad</span><span class="p">()</span>
    <span class="k">def</span> <span class="nf">draft</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">hidden</span><span class="p">,</span> <span class="n">token_id</span><span class="p">,</span> <span class="n">main_kv</span><span class="p">,</span> <span class="n">k</span><span class="o">=</span><span class="mi">4</span><span class="p">):</span>
        <span class="sh">"""</span><span class="s">自回归地连猜 k 个 token 作为草稿，交给主模型一次性验收</span><span class="sh">"""</span>
        <span class="n">drafts</span> <span class="o">=</span> <span class="p">[]</span>
        <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">k</span><span class="p">):</span>                             <span class="c1"># 支持任意 draft 长度
</span>            <span class="n">hidden</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">draft_one_step</span><span class="p">(</span><span class="n">hidden</span><span class="p">,</span> <span class="n">token_id</span><span class="p">,</span> <span class="n">main_kv</span><span class="p">)</span>
            <span class="n">token_id</span> <span class="o">=</span> <span class="nf">unembed_argmax</span><span class="p">(</span><span class="n">hidden</span><span class="p">)</span>          <span class="c1"># 复用主模型的 unembed
</span>            <span class="n">drafts</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">token_id</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">drafts</span>
</code></pre></div></div> <p>一个完整的投机步长这样（drafter 猜 → 主模型并行验收 → 接受最长匹配前缀）：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">speculative_step</span><span class="p">(</span><span class="n">main_model</span><span class="p">,</span> <span class="n">drafter</span><span class="p">,</span> <span class="n">hidden</span><span class="p">,</span> <span class="n">last_token</span><span class="p">,</span> <span class="n">main_kv</span><span class="p">,</span> <span class="n">k</span><span class="o">=</span><span class="mi">4</span><span class="p">):</span>
    <span class="c1"># 1) drafter 便宜地猜 k 个
</span>    <span class="n">draft_tokens</span> <span class="o">=</span> <span class="n">drafter</span><span class="p">.</span><span class="nf">draft</span><span class="p">(</span><span class="n">hidden</span><span class="p">,</span> <span class="n">last_token</span><span class="p">,</span> <span class="n">main_kv</span><span class="p">,</span> <span class="n">k</span><span class="o">=</span><span class="n">k</span><span class="p">)</span>

    <span class="c1"># 2) 主模型一次 forward 并行验收这 k 个
</span>    <span class="c1">#    （省时间的关键：k 个 token 只花大模型 1 次前向）
</span>    <span class="n">logits</span><span class="p">,</span> <span class="n">new_hidden</span> <span class="o">=</span> <span class="n">main_model</span><span class="p">.</span><span class="nf">verify</span><span class="p">(</span><span class="n">draft_tokens</span><span class="p">,</span> <span class="n">main_kv</span><span class="p">)</span>

    <span class="c1"># 3) 从第一个不匹配处截断，前面的全部接受
</span>    <span class="n">accepted</span> <span class="o">=</span> <span class="nf">accept_longest_prefix</span><span class="p">(</span><span class="n">draft_tokens</span><span class="p">,</span> <span class="n">logits</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">accepted</span><span class="p">,</span> <span class="n">new_hidden</span>
</code></pre></div></div> <h3 id="43-efficient-mtp-decoding别让最后那个大-unembed-拖后腿">4.3 Efficient MTP Decoding：别让最后那个大 unembed 拖后腿</h3> <p>这里有个很实在的工程细节。drafter 每猜一个 token，最后都要过一次 <code class="language-plaintext highlighter-rouge">unembed</code>，也就是投影到 <strong>262,000</strong> 的词表上做 softmax——<code class="language-plaintext highlighter-rouge">d × 262000</code> 这个矩阵乘，在小 drafter 上反而成了 overhead 大头（drafter 本体才 256 维，结果尾巴上挂了个 26 万维的大矩阵乘，头轻脚重）。</p> <p>Gemma 4 对 E2B/E4B 的 drafter 做了优化：<strong>把”投影到整个词表”换成”在 token 聚类上做 top-k”</strong>，最终那个矩阵乘从 <code class="language-plaintext highlighter-rouge">d × 262,000</code> 降到 <code class="language-plaintext highlighter-rouge">d × 4096</code>，<strong>而 acceptance rate 基本不掉</strong>。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">cheap_unembed</span><span class="p">(</span><span class="n">hidden</span><span class="p">,</span> <span class="n">cluster_centroids</span><span class="p">,</span> <span class="n">cluster_tokens</span><span class="p">,</span> <span class="n">top_k</span><span class="o">=</span><span class="mi">4096</span><span class="p">):</span>
    <span class="c1"># 不再算 d × 262000 的完整 logits
</span>    <span class="c1"># 1) 先在 token 聚类(centroid)层面选出最可能的若干簇
</span>    <span class="n">cluster_logits</span> <span class="o">=</span> <span class="n">hidden</span> <span class="o">@</span> <span class="n">cluster_centroids</span><span class="p">.</span><span class="n">T</span>      <span class="c1"># 便宜：簇数远小于 262k
</span>    <span class="n">top_clusters</span> <span class="o">=</span> <span class="n">cluster_logits</span><span class="p">.</span><span class="nf">topk</span><span class="p">(...).</span><span class="n">indices</span>
    <span class="c1"># 2) 只在这些簇覆盖的 ~4096 个候选 token 上算真正的 logits
</span>    <span class="n">cand</span> <span class="o">=</span> <span class="nf">gather_tokens</span><span class="p">(</span><span class="n">cluster_tokens</span><span class="p">,</span> <span class="n">top_clusters</span><span class="p">)</span> <span class="c1"># ~4096 个候选
</span>    <span class="n">logits</span> <span class="o">=</span> <span class="n">hidden</span> <span class="o">@</span> <span class="n">embed_weight</span><span class="p">[</span><span class="n">cand</span><span class="p">].</span><span class="n">T</span>             <span class="c1"># d × 4096，省了一个数量级
</span>    <span class="k">return</span> <span class="n">logits</span><span class="p">,</span> <span class="n">cand</span>
</code></pre></div></div> <p>这个思路跟稀疏 MoE 的 routing 其实同源——<strong>都是”别在全量上算，只在最相关的一小撮上算”</strong>。对 drafter 这种要被反复调用 k 次的模块，把尾部大矩阵乘从 26 万降到 4 千，收益是实打实的。</p> <hr/> <h2 id="5-效果这套组合拳换来了什么">5. 效果：这套组合拳换来了什么</h2> <p>架构讲完，看一眼刷榜（Table 5，跟 Gemma 3 27B 对比）：</p> <p><img src="/assets/img/posts/20260712-gemma4/t5_bench.png" alt="Gemma 4 与 Gemma 3 27B 各 benchmark 对比"/></p> <p>挑几个我关注的：</p> <ul> <li><strong>31B</strong> 在 AIME 2026 上 89.2，LiveCodeBench v6 拿到 80.0、Codeforces Elo 2150，比上一代 27B（29.1 / 110）是断层式提升。当然这里 Gemma 4 开了 thinking mode、Gemma 3 是 non-thinking，不完全公平，但代差很明显。</li> <li><strong>26B-A4B</strong> 这个 MoE 只激活 3.8B，却能在多数 benchmark 上贴着 31B dense 打（AIME 88.3 vs 89.2），再次说明<strong>稀疏激活</strong>这条路线在效果/成本比上的价值——这也正是我一直关注的 MoE 方向的核心卖点。</li> <li>就算是最小的 <strong>E2B</strong>（激活才 2.3B），AIME 也有 37.5，比上一代 27B 的 20.8 还高。端侧模型的天花板在肉眼可见地抬高。</li> </ul> <p>内存这块（Table 3），配合 QAT 量化，E2B 量化后权重只要 <strong>0.8GB</strong>、E4B <strong>2.3GB</strong>——是真能塞进手机的量级。</p> <hr/> <h2 id="6-一点个人-take">6. 一点个人 take</h2> <p>扒完这篇，我自己几个感受：</p> <ol> <li> <p><strong>多模态正在”去 encoder 化”</strong>。12B 那套 encoder-free、用一个 35M 大 matmul 替掉 550M ViT 的做法，方向上跟”让强 backbone 自己吃原始信号”是一致的。虽然代价是训练侧更重，但对端侧部署（少一堆模块、少一份显存）太友好了。这个趋势值得盯。</p> </li> <li> <p><strong>MTP drafter 那个”cross-attend 主模型 KV、免 prefill”的设计是真的巧</strong>。它把 drafter 从”一个需要独立维护上下文的小模型”变成”寄生在主模型 KV 上的一个 4 层小尾巴”，长 prompt 场景下省掉的 prefill 开销很关键；再加上 top-k 聚类砍掉尾部大 unembed，整套东西透着一股”每一处 overhead 都不放过”的抠劲儿——这正是做推理效率的人该有的样子。</p> </li> <li> <p>从 per-layer embedding、value 复用 key 砍 global cache、p-RoPE，到 MTP + QAT，Gemma 4 基本把<strong>端侧 LLM 的效率工具箱</strong>摊开来挨个用了一遍。<strong>单看每一项都不算全新，但把它们整合进一个能真正跑在手机上的模型里，本身就是很硬核的系统工程。</strong></p> </li> </ol> <p>代码都是我按报告描述简化重写的，只为帮理解数据流，跟官方实现肯定有出入，看走眼的地方欢迎评论区拍砖交流。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 LLM 推理效率、模型压缩优化你想系统深入，可以看看我之前出版的<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，书里有专门讲 LLM 推理效率和参数高效优化的章节，和本文这些抠 overhead 的工程思路算是一脉相承。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="多模态"/><category term="投机采样"/><category term="端侧推理"/><category term="论文解读"/><summary type="html"><![CDATA[Gemma 4 拆解 | 多模态怎么塞进端侧，MTP drafter 又是怎么自带投机采样的]]></summary></entry><entry><title type="html">一篇 Markdown，一键发到微信、知乎、小红书、Twitter</title><link href="https://marsggbo.github.io/blog/2026/20260712-markdown2anything/" rel="alternate" type="text/html" title="一篇 Markdown，一键发到微信、知乎、小红书、Twitter"/><published>2026-07-12T00:00:00+00:00</published><updated>2026-07-12T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260712-markdown2anything</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260712-markdown2anything/"><![CDATA[<h1 id="一篇-markdown一键发到微信知乎小红书twitter">一篇 Markdown，一键发到微信、知乎、小红书、Twitter</h1> <blockquote> <p>开源地址：<a href="https://github.com/marsggbo/markdown2anything">marsggbo/markdown2anything</a> VS Code 商店搜 <strong>Markdown2Anything</strong> · CLI：<code class="language-plaintext highlighter-rouge">npm i -g md2any</code></p> </blockquote> <hr/> <h2 id="1-它解决什么">1. 它解决什么</h2> <p>写技术博客我一直是 Markdown 一把梭。但写完之后，真正花时间的从来不是写作，而是<strong>分发</strong>：</p> <p>复制到微信排版全乱、公式变乱码；发知乎图片要一张张重传；发小红书得先把文章截成图再手动拖进去、还得另写一份标题正文标签；发 Twitter 又要重写一遍拆成串推、图再传一次。</p> <p><strong>一篇文章写完，光”搬运”就得再花一个多小时，而且毫无创造性。</strong></p> <p>所以我写了个 VS Code 插件把这事儿干掉：<strong>一篇 <code class="language-plaintext highlighter-rouge">.md</code>，一个面板，四个平台。</strong></p> <p><img src="/assets/img/posts/20260712-markdown2anything/overview.png" alt="全局界面"/></p> <p>左边是<strong>实时渲染预览</strong>，上面一排是<strong>各平台的一键操作</strong>，右边是<strong>发布面板</strong>。整个流程不用离开编辑器。</p> <hr/> <h2 id="2-渲染效果">2. 渲染效果</h2> <p>先看它把 Markdown 渲染成什么样。下面就是我另一篇文章在预览区里的实际效果——<strong>标题层级、加粗、表格、代码高亮、LaTeX 公式、图片说明</strong>全部处理好了：</p> <p><img src="/assets/img/posts/20260712-markdown2anything/preview-render.png" alt="渲染效果"/></p> <p>几个细节：</p> <ul> <li><strong>LaTeX 公式</strong>：KaTeX 渲染，行内 <code class="language-plaintext highlighter-rouge">$...$</code> 和公式块 <code class="language-plaintext highlighter-rouge">$$...$$</code> 都支持。导出微信时会自动转成 <strong>SVG</strong>（不然公众号编辑器会把它洗掉）。</li> <li><strong>代码高亮</strong>：macOS 风格的窗口装饰 + 语法高亮，复制到微信后<strong>高亮不丢</strong>。</li> <li><strong>图片</strong>：本地图片自动转 Base64 内嵌，<strong>不需要图床</strong>。图片的 alt 文字会自动渲染成居中的灰色说明。</li> <li><strong>10 套内置主题</strong>，还能在线改 CSS，所见即所得。</li> </ul> <hr/> <h2 id="3-四个渠道各自怎么用">3. 四个渠道，各自怎么用</h2> <h3 id="31-微信公众号-">3.1 微信公众号 🐧</h3> <p>微信是最难伺候的：它的编辑器会<strong>洗掉外链 CSS</strong>，只认<strong>内联样式</strong>。所以插件做了两件事：</p> <ul> <li><strong>📋 复制微信</strong>：把渲染好的 HTML 用 <code class="language-plaintext highlighter-rouge">juice</code> 把所有 CSS <strong>内联</strong>进标签里，公式转 SVG，代码高亮转内联 style。<strong>直接 <code class="language-plaintext highlighter-rouge">Cmd+V</code> 粘进公众号编辑器，排版一模一样，不用再手动调。</strong></li> <li><strong>☁️ 上传公众号</strong>：配好 AppID / AppSecret，直接推到<strong>草稿箱</strong>，连粘贴都省了。</li> </ul> <p>这是我用得最多的功能——以前一篇长文粘完要手动修二十分钟排版，现在是零。</p> <h3 id="32-知乎-">3.2 知乎 📝</h3> <ul> <li><strong>🚀 发布知乎</strong>：扫码登录后，浏览器自动打开编辑器，<strong>标题、正文、代码块、公式全部自动填好</strong>。</li> <li><strong>代码块自带语言高亮</strong>，不用再手动去下拉框里选语言（这里有个坑：highlight.js 生成的 class 是 <code class="language-plaintext highlighter-rouge">hljs python</code>，很容易把语言错识别成 <code class="language-plaintext highlighter-rouge">hljs</code>，知乎收到一个不认识的语言，<strong>既不高亮、也不当代码块处理，换行会全丢、折叠成一行</strong>）。</li> <li><strong>公式</strong>转成知乎原生的 <code class="language-plaintext highlighter-rouge">eeimg</code> 格式，能正常渲染。</li> </ul> <p><strong>但有一个我没搞定的地方，得老实说：知乎的图片仍然要手动粘贴。</strong></p> <p>知乎编辑器<strong>不会</strong>自动抓取粘贴进来的图片——base64 不行，远程 URL 也不行。只有<strong>剪贴板里躺着一张真正的图片位图</strong>时，它才会上传到自己的图床。我试了各种自动化路线（走它的图片弹窗、起本地图床让它自己抓），<strong>全都失败了</strong>。</p> <p>所以退而求其次，做了个「📋 复制图片」：预览区鼠标移到任意图片上，点一下，<strong>复制的是真正的 PNG 位图</strong>（不是 data URL），到知乎里 <code class="language-plaintext highlighter-rouge">Cmd+V</code> 就会被自动上传。虽然还是手动，但至少不用再去文件夹里翻文件、走上传对话框了。</p> <p><strong>能自动的自动了，不能自动的老实承认。</strong></p> <h3 id="33-小红书--twitter自动发布-">3.3 小红书 &amp; Twitter：自动发布 🚀</h3> <p>这两个是这次的重头戏，放一起说——因为它们的问题是<strong>同一个</strong>：</p> <blockquote> <p>它们要的不是”同一篇文章”，而是 <strong>“同一份内容的另一种表达”</strong>。</p> </blockquote> <p>小红书要的是图 + 一段口语化的种草文案 + 标签；Twitter 要的是拆成好几条的串推。所以<strong>光搬运没用，得重写</strong>。这块插件接了 LLM，并且做到了<strong>全自动发布</strong>。</p> <p><strong>小红书流程：</strong></p> <ol> <li> <p>文章<strong>自动截成多张长图</strong>（智能分片，会尽量在空白行处切，不会把一段话拦腰砍断）</p> <p><img src="/assets/img/posts/20260712-markdown2anything/xhs-longimage-sample.png" alt="长图示例"/></p> </li> <li><strong>AI 自动写好标题、正文、标签</strong>（标题会实时校验小红书 20 字上限，超了标红）</li> <li>登录一次（扫码，Cookie 自动存好，面板上有<strong>有效期倒计时</strong>）</li> <li>点发布 → <strong>浏览器自动打开、自动传图、自动填标题/正文/标签</strong></li> <li>你核对一眼，点页面上的「发布」——完成</li> </ol> <p><img src="/assets/img/posts/20260712-markdown2anything/panel-xhs.png" alt="小红书面板"/></p> <p><strong>Twitter 流程：</strong></p> <p><img src="/assets/img/posts/20260712-markdown2anything/panel-twitter.png" alt="Twitter 面板"/></p> <ul> <li><strong>AI 自动生成中文串推（thread）</strong>，而且<strong>条数不是拍脑袋定的</strong>：<code class="language-plaintext highlighter-rouge">条数 = ⌈长图张数 ÷ 4⌉</code>（X 单条最多带 4 张图）。<strong>每条推文讲的就是它配的那几张图里的内容</strong>，做到文图对得上。</li> <li><strong>字数按 X 的真实规则实时校验</strong>——X 的 280 字上限是<strong>加权</strong>的，<strong>一个中文字符算 2 个</strong>，链接固定算 23。面板上显示的就是这个真实字数，超了标红，发布前直接拦下（这个坑很多人不知道，超限时 X 会直接把「发帖」按钮禁用掉）。</li> <li>发布时浏览器里<strong>自动把整串填好 + 按顺序配好图</strong>，你点「发帖」即可。</li> </ul> <p><strong>共同的设计：</strong></p> <ul> <li><strong>配图会自动检查</strong>：没导出长图？发布时自动帮你导。数量对不对得上也会校验。</li> <li><strong>默认不替你点最后那下「发布」</strong>：内容和图都填好后停下，让你在发出去之前再核对一眼。这是我特意的选择——<strong>自动化到最后一厘米就停，把决定权留给你。</strong></li> </ul> <h3 id="34-ai-文案不配-key-也能用">3.4 AI 文案：不配 Key 也能用</h3> <p>文案生成接的是<strong>任意 OpenAI 兼容接口</strong>（DeepSeek / OpenRouter / Groq / <strong>本地 Ollama</strong> / OpenAI…），面板里下拉一键预设。</p> <p>但<strong>你不配 API Key 也能用</strong>：内置了一套<strong>本地关键词提取算法</strong>（中英混合 n-gram + 停用词过滤 + 长词吸收短词），面板一打开就把标题、正文、标签自动填好了，<strong>零配置零成本</strong>。想要更好的文案，再点「✨ 生成文案」用模型覆盖。</p> <p>另外两个实用的小设计：</p> <ul> <li><strong>Prompt 可编辑</strong>：不满意就改了重新生成。</li> <li><strong>版本管理</strong>：LLM 生成很难一次满意，所以<strong>每次生成都存成一个新版本，旧版全保留</strong>。面板上 <code class="language-plaintext highlighter-rouge">◀ ▶</code> 左右切换对比，看中哪版用哪版，<code class="language-plaintext highlighter-rouge">🗑</code> 单独删掉不要的。全部落盘到文章同目录的 <code class="language-plaintext highlighter-rouge">_social.json</code>，可以跟文章一起 git 备份。</li> </ul> <h3 id="35-关于安全">3.5 关于安全</h3> <ul> <li><strong>API Key 存进系统钥匙串</strong>（VS Code SecretStorage），不落明文、不进 Settings Sync、不会被误提交进 git。</li> <li><strong>Cookie 只存本地</strong>，<strong>唯一的网络去向就是平台自己</strong>，不经过任何第三方服务器。</li> <li>发布走的是<strong>真实浏览器 + Cookie 注入</strong>（模拟真人正常操作），而不是伪造签名的裸 HTTP 请求。</li> </ul> <hr/> <h2 id="4-不只是插件cli--agent-skill">4. 不只是插件：CLI + Agent Skill</h2> <p>写着写着我发现，把它锁死在”编辑器里的一个按钮”太可惜了。所以又做了个<strong>命令行版</strong>，已经发到 npm：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>npm i <span class="nt">-g</span> md2any          <span class="c"># 或者直接 npx md2any</span>

md2any login xhs
md2any images ./post.md              <span class="c"># 渲染长图</span>
md2any gen ./post.md <span class="nt">--to</span> twitter    <span class="c"># 生成串推</span>
md2any publish ./post.md <span class="nt">--to</span> xhs,twitter
</code></pre></div></div> <p>核心逻辑（渲染、切图、LLM 文案、Playwright 发布）<strong>完全复用插件那一套，没有重写</strong>——插件和 CLI 是同一个引擎的两个壳，连文案的版本历史都共用同一个 <code class="language-plaintext highlighter-rouge">_social.json</code>。</p> <p>而且它是<strong>为 Agent 设计的</strong>：</p> <ul> <li><strong>stdout 只出结果，stderr 出日志</strong> → 可以安全 pipe 和解析</li> <li><strong><code class="language-plaintext highlighter-rouge">--json</code> 给结构化输出</strong></li> </ul> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>md2any gen ./post.md <span class="nt">--to</span> twitter <span class="nt">--json</span> 2&gt;/dev/null | jq <span class="s1">'.twitter.tweets'</span>
</code></pre></div></div> <p>仓库里有一份 <code class="language-plaintext highlighter-rouge">SKILL.md</code>，把 <code class="language-plaintext highlighter-rouge">cli/</code> 挂进 Claude Code 之类的 Agent，它就能听懂<strong>“把这篇文章发到小红书和推特”</strong>——从”我的工具”变成<strong>任何大模型都能调用的一种能力</strong>。</p> <p>顺带一提，我在那份 skill 定义里特意写死了一条：</p> <blockquote> <p>⚠️ <strong>不要向用户承诺知乎图片能自动上传。</strong></p> </blockquote> <p>因为 Agent 最容易犯的错，就是<strong>把限制说成能力</strong>。这是我们真金白银试出来的教训，得钉进契约里，不能让下一个 Agent 再去踩、再去骗用户。</p> <hr/> <h2 id="5-怎么装">5. 怎么装</h2> <p><strong>插件</strong>：VS Code 扩展市场搜 <strong><code class="language-plaintext highlighter-rouge">Markdown2Anything</code></strong>。</p> <p><strong>CLI</strong>：<code class="language-plaintext highlighter-rouge">npm i -g md2any</code></p> <p>打开任意 <code class="language-plaintext highlighter-rouge">.md</code>，<code class="language-plaintext highlighter-rouge">Cmd/Ctrl + Shift + W</code> 打开面板即可。首次用到截图/发布功能会自动下载 Chromium（约 150MB），之后无需等待。</p> <hr/> <p><strong>项目地址：<a href="https://github.com/marsggbo/markdown2anything">github.com/marsggbo/markdown2anything</a></strong>（MIT）</p> <p>欢迎 star、提 issue、拍砖，更欢迎 PR。</p> <hr/> <blockquote> <p>最后打个软广宣传一下我们团队最近出版的<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，书里有专门讲大语言模型优化与工程实践的章节，对于想学习大模型技术细节的同学和工程师朋友很有帮助。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="开源"/><category term="工具"/><category term="VSCode"/><category term="LLM"/><category term="自动化"/><summary type="html"><![CDATA[一篇 Markdown，一键发到微信、知乎、小红书、Twitter]]></summary></entry><entry><title type="html">vllm v1 源码精读（五）：从 generate() 到 Speculative Decoding 的完整计算流</title><link href="https://marsggbo.github.io/blog/2026/20260709-vllm-speculative-decoding/" rel="alternate" type="text/html" title="vllm v1 源码精读（五）：从 generate() 到 Speculative Decoding 的完整计算流"/><published>2026-07-09T00:00:00+00:00</published><updated>2026-07-09T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260709-vllm-speculative-decoding</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260709-vllm-speculative-decoding/"><![CDATA[<h1 id="vllm-v1-源码精读五从-generate-到-speculative-decoding-的完整计算流">vllm v1 源码精读（五）：从 generate() 到 Speculative Decoding 的完整计算流</h1> <blockquote> <p>本文基于 vllm <a href="https://github.com/vllm-project/vllm/tree/ba22152">commit ba22152</a>，所有代码均来自本地 clone 的真实源文件，标注了文件路径和行号。</p> </blockquote> <p><strong>系列文章</strong>：</p> <ul> <li><a href="https://marsggbo.github.io/blog/2026/20260706-vllm-v1-01-arch/">（一）为什么要重写，以及 LLM() 这行代码背后发生了什么</a></li> <li><a href="https://marsggbo.github.io/blog/2026/20260706-vllm-v1-02-generate/">（二）generate() 计算流——model.forward() 在哪里被调用？</a></li> <li><a href="https://marsggbo.github.io/blog/2026/20260706-vllm-v1-03-kvcache/">（三）KV Cache 管理、Chunked Prefill 与异步架构</a></li> <li><a href="https://marsggbo.github.io/blog/2026/20260706-vllm-v1-04-plugins/">（四）插件系统——用 Python entry_points 实现零侵入扩展</a></li> <li><strong>（五）从 generate() 到 Speculative Decoding 的完整计算流</strong>（本文）</li> </ul> <hr/> <h2 id="1-前言">1. 前言</h2> <p>Speculative Decoding（SD）的算法原理不难理解：用小模型提前猜几个 token，大模型一次性验证，接受就保留，拒绝就从第一个错误位置重新采样。</p> <p>但落到 vllm 的工程实现里，有一串问题论文里不会告诉你：<strong>draft model 在哪里初始化？它和 target model 是进程间通信还是同进程？input_ids 怎么打包 draft tokens？propose() 返回什么、传给谁？</strong></p> <p>这篇文章从这些问题出发，自上而下把整个计算流讲清楚。</p> <hr/> <h2 id="2-先建立概念sd-涉及的几个关键词">2. 先建立概念：SD 涉及的几个关键词</h2> <p>在看代码之前，先把 SD 里反复出现的几个术语说清楚。</p> <p><strong>Draft model 和 propose</strong>：SD 需要一个”起草者”（drafter），负责提前猜接下来的 K 个 token——这个猜测过程叫 <strong>propose</strong>，猜出来的 token 叫 <strong>draft tokens</strong>。起草者可以是独立的小模型（DraftModel），也可以是 n-gram 匹配、并行预测头等不同方案，第 6 节展开。</p> <p><strong>Target model 和 verify</strong>：大模型（target model）一次 forward 把这 K 个 draft token 全部喂进去，验证它们对不对——这个过程叫 <strong>verify</strong>。</p> <p><strong>Rejection sampling</strong>：verify 的判断标准不是简单的对/错，而是按概率比 <code class="language-plaintext highlighter-rouge">min(1, p(x)/q(x))</code> 来决定接受还是拒绝（p 是 target 分布，q 是 draft 分布）。这样保证了：不管 draft 猜得准不准，最终输出的 token 分布和原始自回归采样<strong>完全一样</strong>，不损精度。</p> <p><strong>Bookkeeping（账本同步）</strong>：rejection sampling 的结果在 GPU 上算出来，但 KV Cache 的 block table 在 CPU 侧维护。bookkeeping 就是把”接受了几个 draft token”的结果同步到 CPU，更新 block table，回收被拒绝的 draft token 占用的 KV Cache slot。</p> <hr/> <h2 id="3-整体架构gpumodelrunner-里住着什么">3. 整体架构：GPUModelRunner 里住着什么</h2> <p><code class="language-plaintext highlighter-rouge">GPUModelRunner</code> 是 SD 相关逻辑的宿主。下图展示了它内部的对象层级、draft model 在哪里、以及运行时数据怎么在 target model 和 drafter 之间流动：</p> <p><img src="/assets/img/posts/20260709-vllm-v1-05-speculative-decoding/sd_internal_structure.png" alt="GPUModelRunner 内部结构与数据流"/></p> <p>三个成员的职责：</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">self.model</code></strong>：target model，标准 <code class="language-plaintext highlighter-rouge">nn.Module</code>，attention 层已替换为 FlashAttention 版本，在 <code class="language-plaintext highlighter-rouge">load_model()</code> 加载</li> <li><strong><code class="language-plaintext highlighter-rouge">self.drafter</code></strong>：Proposer 对象，内部持有 <code class="language-plaintext highlighter-rouge">self.model</code>（draft model），负责 propose，有四种实现（第 6 节展开）</li> <li><strong><code class="language-plaintext highlighter-rouge">self.rejection_sampler</code></strong>：做 rejection sampling，在 <code class="language-plaintext highlighter-rouge">vllm/v1/sample/</code> 下而不是 <code class="language-plaintext highlighter-rouge">spec_decode/</code>——因为 rejection sampling 属于采样层而非 draft 算法层</li> </ul> <p><strong>target model 和 draft model 的关系</strong>：两者都是独立的 <code class="language-plaintext highlighter-rouge">nn.Module</code>，加载在同一 Worker 进程的同一块 GPU 上，<strong>没有进程间通信，没有跨 GPU 传输</strong>。它们之间的”信息交换”就是普通的 Python 函数调用——把一个 GPU tensor 当参数传过去，零拷贝，零开销。</p> <hr/> <h2 id="4-初始化draft-model-怎么加载进来">4. 初始化：draft model 怎么加载进来</h2> <p>搞清楚架构之后，先来看 draft model 是怎么初始化的。</p> <p><code class="language-plaintext highlighter-rouge">GPUModelRunner.__init__()</code> 第 565 行根据配置创建 Proposer 对象：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># vllm/v1/worker/gpu_model_runner.py
# class GPUModelRunner
# def __init__(self, vllm_config, ...)  第 565~632 行
</span><span class="k">if</span> <span class="n">spec_config</span><span class="p">.</span><span class="n">method</span> <span class="o">==</span> <span class="sh">"</span><span class="s">ngram</span><span class="sh">"</span><span class="p">:</span>
    <span class="n">self</span><span class="p">.</span><span class="n">drafter</span> <span class="o">=</span> <span class="nc">NgramProposer</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">vllm_config</span><span class="p">)</span>
<span class="k">elif</span> <span class="n">spec_config</span><span class="p">.</span><span class="nf">uses_draft_model</span><span class="p">():</span>
    <span class="n">self</span><span class="p">.</span><span class="n">drafter</span> <span class="o">=</span> <span class="nc">DraftModelProposer</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">vllm_config</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">device</span><span class="p">,</span> <span class="n">self</span><span class="p">)</span>
<span class="k">elif</span> <span class="n">spec_config</span><span class="p">.</span><span class="nf">use_eagle</span><span class="p">():</span>
    <span class="n">self</span><span class="p">.</span><span class="n">drafter</span> <span class="o">=</span> <span class="nc">EagleProposer</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">vllm_config</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">device</span><span class="p">,</span> <span class="n">self</span><span class="p">)</span>
<span class="k">elif</span> <span class="n">spec_config</span><span class="p">.</span><span class="n">method</span> <span class="o">==</span> <span class="sh">"</span><span class="s">medusa</span><span class="sh">"</span><span class="p">:</span>
    <span class="n">self</span><span class="p">.</span><span class="n">drafter</span> <span class="o">=</span> <span class="nc">MedusaProposer</span><span class="p">(...)</span>
</code></pre></div></div> <p>以 EagleProposer 为例，完整初始化链路如下：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GPUModelRunner.__init__()
  └─ EagleProposer.__init__()          # eagle.py:10
       └─ SpecDecodeBaseProposer.__init__()  # llm_base_proposer.py
            └─ self.load_model()        # 加载 draft model 权重到 GPU
                 └─ self.model = get_model(draft_model_config)
                    # self.model 是一个独立的 nn.Module（更小的 LLM）
                    # 和 GPUModelRunner.self.model 共用同一块 GPU 显存
</code></pre></div></div> <p>NgramProposer 的 <code class="language-plaintext highlighter-rouge">load_model()</code> 是空实现——它不需要任何模型，不占显存。</p> <hr/> <h2 id="5-运行时主线execute_model--sample_tokens--propose">5. 运行时主线：execute_model() → sample_tokens() → propose()</h2> <p>有了架构和初始化的认知，再来看每轮推理里代码按什么顺序跑。</p> <h3 id="51-execute_modeldraft-tokens-怎么打包进-input_ids关键">5.1 execute_model()：draft tokens 怎么打包进 input_ids（关键）</h3> <p>普通 decode 每步只送 1 个 token 给 target model；开启 SD 后，<strong>从第二轮起，<code class="language-plaintext highlighter-rouge">input_ids</code> 里会包含上一轮 propose 出的 K 个 draft tokens，加上 1 个 bonus token（来自上一轮 target 自己采样的结果），共 K+1 个 token 一次性送进 target forward</strong>。</p> <p>这是 SD 提速的核心工程实现——把原来需要串行跑 K 次的 decode，压缩成一次 batched prefill。prefill 是 compute-bound，GPU 利用率远高于 memory-bound 的 decode。</p> <p><code class="language-plaintext highlighter-rouge">_prepare_inputs()</code> 里负责拼接这段 input_ids（第 2212 行），同时构建 <code class="language-plaintext highlighter-rouge">SpecDecodeMetadata</code>：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># vllm/v1/worker/gpu_model_runner.py
# class GPUModelRunner
# def _prepare_inputs(self, scheduler_output)  第 2212 行
</span>
<span class="c1"># 对于 SD 请求，input_ids 的拼接逻辑（伪代码）：
# [prompt tokens]  +  [上一轮 draft_token_ids[0..K-1]]  +  [bonus token]
#                      ↑ 来自上一轮 propose() 的返回值
#                      存在 spec_decode_metadata.draft_token_ids 里
</span>
<span class="n">spec_decode_metadata</span> <span class="o">=</span> <span class="nc">SpecDecodeMetadata</span><span class="p">(</span>
    <span class="n">draft_token_ids</span><span class="o">=</span><span class="n">draft_token_ids</span><span class="p">,</span>      <span class="c1"># [total_draft_tokens] 所有请求的 draft 扁平拼接
</span>    <span class="n">num_draft_tokens</span><span class="o">=</span><span class="n">num_draft_tokens</span><span class="p">,</span>    <span class="c1"># list[int] 每个请求 draft 了多少步
</span>    <span class="n">logits_indices</span><span class="o">=</span><span class="n">logits_indices</span><span class="p">,</span>        <span class="c1"># 在 target logits 里 gather 各 draft 位置的索引
</span>    <span class="n">bonus_logits_indices</span><span class="o">=</span><span class="n">bonus_logits_indices</span><span class="p">,</span>
<span class="p">)</span>
</code></pre></div></div> <p>target forward 完成后，把 logits 和 hidden states 打包，返回 None，等 <code class="language-plaintext highlighter-rouge">sample_tokens()</code> 来取：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># vllm/v1/worker/gpu_model_runner.py
# class GPUModelRunner
# def execute_model(self, scheduler_output)  第 4418~4437 行
</span><span class="n">self</span><span class="p">.</span><span class="n">execute_model_state</span> <span class="o">=</span> <span class="nc">ExecuteModelState</span><span class="p">(</span>
    <span class="n">scheduler_output</span><span class="p">,</span>
    <span class="n">logits</span><span class="p">,</span>           <span class="c1"># [N_tokens, vocab_size]，包含所有 draft 位置的 logits
</span>    <span class="n">spec_decode_metadata</span><span class="p">,</span>
    <span class="n">hidden_states</span><span class="p">,</span>    <span class="c1"># [N_tokens, d_model]，EAGLE/Medusa 的 propose 要用
</span>    <span class="bp">...</span>
<span class="p">)</span>
<span class="k">return</span> <span class="bp">None</span>  <span class="c1"># sampling 留给 sample_tokens() 做，GPU/CPU 真正并行
</span></code></pre></div></div> <h3 id="52-sample_tokens三步走rejection--bookkeeping--propose">5.2 sample_tokens()：三步走——rejection → bookkeeping → propose</h3> <p><code class="language-plaintext highlighter-rouge">sample_tokens()</code> 拿到 <code class="language-plaintext highlighter-rouge">execute_model_state</code> 后，顺序做三件事：</p> <p><img src="/assets/img/posts/20260709-vllm-v1-05-speculative-decoding/diagram_flow.png" alt="sample_tokens() 调用流程"/></p> <p><strong>第一步：<code class="language-plaintext highlighter-rouge">_sample()</code> 做 rejection</strong></p> <p>从 <code class="language-plaintext highlighter-rouge">execute_model_state</code> 取出 target logits，从 <code class="language-plaintext highlighter-rouge">SpecDecodeMetadata</code> 取出 draft token ids，传给 <code class="language-plaintext highlighter-rouge">self.rejection_sampler</code>，逐 token 按概率比检验。接受的 draft 直接保留，第一个被拒绝的位置截断，从 target 分布重新采样一个修正 token。全程在 GPU 上跑 Triton kernel，不涉及 CPU 同步。</p> <p><strong>第二步：<code class="language-plaintext highlighter-rouge">_bookkeeping_sync()</code> 做 CPU 同步</strong></p> <p>把每个请求”接受了几个 draft token”的结果从 GPU 同步到 CPU，更新 KV Cache 的 block table，回收被拒绝的 draft token 对应的 slot。这是纯 CPU 操作。</p> <p><strong>第三步：<code class="language-plaintext highlighter-rouge">propose_draft_token_ids()</code> 生成下一批 draft</strong></p> <p>调 <code class="language-plaintext highlighter-rouge">self.drafter.propose()</code>，为下一轮推理准备 K 个 draft token。不同 drafter 依赖的输入不同：</p> <ul> <li><strong>EAGLE / Medusa</strong>：依赖 target forward 刚产出的 <code class="language-plaintext highlighter-rouge">hidden_states</code>（从 <code class="language-plaintext highlighter-rouge">execute_model_state</code> 取），消费 GPU tensor，可以在 bookkeeping 之前提前开跑（第 4554 行）</li> <li><strong>DraftModel</strong>：不依赖 target hidden_states，独立 autoregressive decode，同样消费 GPU tensor，可以提前 propose（第 4554 行）</li> <li><strong>ngram</strong>：需要 CPU 侧完整的 token ids，必须等 bookkeeping 完成后才能 propose（第 4626 行）</li> </ul> <h3 id="53-多轮流水线时序">5.3 多轮流水线时序</h3> <p>下图展示了多轮推理中 propose 和 verify 如何跨轮流转：</p> <p><img src="/assets/img/posts/20260709-vllm-v1-05-speculative-decoding/diagram_seq.png" alt="多轮推理时序图"/></p> <p>沿着时序图从第 0 轮往下走：</p> <p><strong>第 0 轮（冷启动）</strong>：没有 draft，target model 正常跑一次 forward，采样出第一个 token。同时 drafter 做第一次 propose，预测接下来 K 个 draft token 存起来备用。</p> <p><strong>第 1 轮 execute_model()</strong>：上一轮的 K 个 draft token 被 <code class="language-plaintext highlighter-rouge">_prepare_inputs()</code> 打包进 <code class="language-plaintext highlighter-rouge">input_ids</code>，一次 forward 全部送进 target model 验证。</p> <p><strong>第 1 轮 rejection → bookkeeping → propose</strong>：rejection 决定接受哪些 draft，bookkeeping 同步到 CPU，propose 为第 2 轮准备新一批 draft。EAGLE/DraftModel 的 propose 和 bookkeeping 并行，ngram 串行等待。</p> <p><strong>三个值得记住的结论</strong>：</p> <ul> <li><strong>propose 生成的是下一轮的 draft</strong>，是流水线而非同轮闭环</li> <li><strong>target forward 一次吃 K 个 draft token</strong>，把 K 步串行 decode 压缩成一次 prefill</li> <li><strong>部分 drafter 的 propose 和 bookkeeping 可以重叠</strong>，隐藏 CPU 同步延迟</li> </ul> <hr/> <h2 id="6-proposer-层四种实现的接口与差异">6. Proposer 层：四种实现的接口与差异</h2> <h3 id="61-统一接口propose-返回什么传给谁">6.1 统一接口：propose() 返回什么，传给谁</h3> <p>四种算法生成 draft 的方式差异极大，但对外暴露统一的接口：<code class="language-plaintext highlighter-rouge">propose()</code> 接受 target forward 的中间结果，返回 draft token ids 列表。</p> <p>返回值是一个 <code class="language-plaintext highlighter-rouge">list[Tensor]</code>，长度为 K（speculative_tokens 数），每个 Tensor 的 shape 是 <code class="language-plaintext highlighter-rouge">[batch_size]</code>，对应 batch 里所有请求在该步的 draft token：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>propose() 返回:
  [
    draft_token_ids_step0,   # shape [batch_size]，第 1 个 draft token
    draft_token_ids_step1,   # shape [batch_size]，第 2 个 draft token
    ...
    draft_token_ids_stepK-1, # shape [batch_size]，第 K 个 draft token
  ]
</code></pre></div></div> <p>这个返回值经过 <code class="language-plaintext highlighter-rouge">propose_draft_token_ids()</code>（<code class="language-plaintext highlighter-rouge">gpu_model_runner.py</code> 第 4893 行）存入 <code class="language-plaintext highlighter-rouge">execute_model_state</code>，下一轮被 <code class="language-plaintext highlighter-rouge">_prepare_inputs()</code> 读取，拼进 <code class="language-plaintext highlighter-rouge">input_ids</code> 参与 target forward。这就是 draft 跨轮流转的完整链路。</p> <h3 id="62-specdecodebaseproposerautoregressive-循环的输入输出">6.2 SpecDecodeBaseProposer：autoregressive 循环的输入输出</h3> <p>EAGLE 和 DraftModel 共用这个基类。核心是一个 autoregressive 循环，每步用上一步采样出的 draft token 作为输入，循环 K-1 次（第一个 draft token 在循环外先算出来）：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># vllm/v1/worker/spec_decode/llm_base_proposer.py
# class SpecDecodeBaseProposer
# def propose(self, execute_model_req, ...)  第 682 行
</span>
<span class="n">draft_token_ids_list</span> <span class="o">=</span> <span class="p">[</span><span class="n">initial_draft_token_ids</span><span class="p">]</span>  <span class="c1"># 第一步在循环外产出
</span>
<span class="k">for</span> <span class="n">token_index</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">num_speculative_tokens</span> <span class="o">-</span> <span class="mi">1</span><span class="p">):</span>
    <span class="n">input_ids</span> <span class="o">=</span> <span class="n">draft_token_ids_list</span><span class="p">[</span><span class="o">-</span><span class="mi">1</span><span class="p">].</span><span class="nf">int</span><span class="p">()</span>   <span class="c1"># 上一步的 draft token 作为这步输入
</span>
    <span class="n">model_kwargs</span> <span class="o">=</span> <span class="p">{</span><span class="sh">"</span><span class="s">input_ids</span><span class="sh">"</span><span class="p">:</span> <span class="n">input_ids</span><span class="p">,</span> <span class="sh">"</span><span class="s">positions</span><span class="sh">"</span><span class="p">:</span> <span class="n">positions</span><span class="p">}</span>
    <span class="k">if</span> <span class="n">self</span><span class="p">.</span><span class="n">pass_hidden_states_to_model</span><span class="p">:</span>
        <span class="c1"># EAGLE 走这里：把 target hidden states 拼入 draft model 输入
</span>        <span class="c1"># DraftModel 不走这里：只用 input_ids，完全独立
</span>        <span class="n">model_kwargs</span><span class="p">[</span><span class="sh">"</span><span class="s">hidden_states</span><span class="sh">"</span><span class="p">]</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">hidden_states</span><span class="p">[:</span><span class="n">batch_size</span><span class="p">]</span>

    <span class="n">draft_hidden</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">model</span><span class="p">(</span><span class="o">**</span><span class="n">model_kwargs</span><span class="p">)</span>   <span class="c1"># draft model forward
</span>    <span class="n">draft_token_ids</span><span class="p">,</span> <span class="n">_</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">_sample_draft_tokens</span><span class="p">(</span><span class="n">draft_hidden</span><span class="p">,</span> <span class="p">...)</span>
    <span class="n">draft_token_ids_list</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">draft_token_ids</span><span class="p">)</span>

<span class="k">return</span> <span class="n">draft_token_ids_list</span>  <span class="c1"># list[Tensor]，长度 K，每个 shape [batch_size]
# ↑ 这个返回值最终进入 execute_model_state，下一轮被打包进 input_ids
</span></code></pre></div></div> <h3 id="63-eagle-vs-draftmodel一个布尔开关的实质差异">6.3 EAGLE vs DraftModel：一个布尔开关的实质差异</h3> <p>两者唯一的代码差异就是构造函数里的 <code class="language-plaintext highlighter-rouge">pass_hidden_states_to_model</code>：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># vllm/v1/worker/spec_decode/eagle.py
# class EagleProposer(SpecDecodeBaseProposer)  第 10 行
</span><span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">vllm_config</span><span class="p">,</span> <span class="n">device</span><span class="p">,</span> <span class="n">runner</span><span class="p">):</span>
    <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">(</span>
        <span class="n">vllm_config</span><span class="p">,</span> <span class="n">device</span><span class="p">,</span>
        <span class="n">pass_hidden_states_to_model</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>   <span class="c1"># ← EAGLE
</span>        <span class="n">runner</span><span class="o">=</span><span class="n">runner</span><span class="p">,</span>
    <span class="p">)</span>

<span class="c1"># DraftModelProposer 对应的是：
# pass_hidden_states_to_model=False          ← DraftModel
</span></code></pre></div></div> <p>这个开关在基类循环里（6.2 节代码的 <code class="language-plaintext highlighter-rouge">if self.pass_hidden_states_to_model</code> 分支）产生实质差异：</p> <table> <thead> <tr> <th> </th> <th>EAGLE</th> <th>DraftModel</th> </tr> </thead> <tbody> <tr> <td>draft model 的输入</td> <td><code class="language-plaintext highlighter-rouge">input_ids</code> + target <code class="language-plaintext highlighter-rouge">hidden_states</code> 拼接</td> <td>只有 <code class="language-plaintext highlighter-rouge">input_ids</code></td> </tr> <tr> <td>依赖 target forward</td> <td>是，消费 target 最后一层特征</td> <td>否，完全独立</td> </tr> <tr> <td>接受率</td> <td>更高（站在 target 肩膀上猜）</td> <td>较低（独立猜）</td> </tr> <tr> <td>代码差异</td> <td>23 行，只有这一个开关</td> <td>只有这一个开关</td> </tr> </tbody> </table> <p>EAGLE 的 insight 是：不让 draft model 从零开始猜，而是让它”寄生”在 target 的 residual stream 上——把 target hidden states 和 draft model 的 embedding 拼在一起作为输入，draft model 相当于在 target 已经理解的语义基础上再细化预测。</p> <h3 id="64-medusaproposer并行多头无-autoregressive-循环">6.4 MedusaProposer：并行多头，无 autoregressive 循环</h3> <p>Medusa 不需要 autoregressive 循环，直接用 target 最后一层的 <code class="language-plaintext highlighter-rouge">hidden_states</code> 驱动 K 个独立预测头并行输出：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># vllm/v1/worker/spec_decode/medusa.py
# class MedusaProposer
# def propose(self, num_speculative_tokens, target_hidden_states, ...)
</span><span class="n">blocks</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">model</span><span class="p">(</span><span class="n">target_hidden_states</span><span class="p">)</span>   <span class="c1"># K 个 head 并行 forward
</span><span class="n">logits</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">model</span><span class="p">.</span><span class="nf">compute_logits</span><span class="p">(</span><span class="n">blocks</span><span class="p">)</span>  <span class="c1"># [batch, num_heads, vocab]
</span><span class="n">draft_token_ids</span> <span class="o">=</span> <span class="n">logits</span><span class="p">.</span><span class="nf">argmax</span><span class="p">(</span><span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>     <span class="c1"># [batch, num_heads] = [batch, K]
</span><span class="k">return</span> <span class="p">[</span><span class="n">draft_token_ids</span><span class="p">[:,</span> <span class="n">i</span><span class="p">]</span> <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">num_heads</span><span class="p">)]</span>  <span class="c1"># list[Tensor]，每个 shape [batch]
</span></code></pre></div></div> <p>一次 forward 出所有 K 个 draft，代价是各 head 之间完全独立，无法利用前几步的 draft 信息，接受率通常比 EAGLE 低。</p> <h3 id="65-ngramproposer无模型纯-cpu-匹配">6.5 NgramProposer：无模型，纯 CPU 匹配</h3> <p><code class="language-plaintext highlighter-rouge">load_model()</code> 是空实现，直接在 context token 序列里找重复 pattern——把 context 翻转后用 KMP lps 数组找最长后缀匹配，取匹配位置后 K 个 token 作为 draft。整个 batch 用 Numba <code class="language-plaintext highlighter-rouge">@njit(parallel=True)</code> 并行加速，零额外显存。对 RAG、code completion 等有大量重复 pattern 的场景接受率相当高。</p> <hr/> <h2 id="7-rejectionsampler四阶段-triton-kernel">7. RejectionSampler：四阶段 Triton kernel</h2> <p><code class="language-plaintext highlighter-rouge">rejection_sample()</code>（<code class="language-plaintext highlighter-rouge">rejection_sampler_utils.py</code> 第 864 行）分四个 Triton kernel 执行：</p> <p><img src="/assets/img/posts/20260709-vllm-v1-05-speculative-decoding/diagram_rejection.png" alt="RejectionSampler 四阶段 kernel"/></p> <p>数学原理：给定 target 分布 p 和 draft 分布 q，对每个 draft token x，以 <code class="language-plaintext highlighter-rouge">min(1, p(x)/q(x))</code> 的概率接受；拒绝时从残差分布 <code class="language-plaintext highlighter-rouge">max(p - q, 0)</code> 里重新采样，保证输出分布仍是 p，和原始自回归采样等价。</p> <p>Step 3 的核心 kernel（第 459 行）：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># vllm/v1/sample/rejection_sampler_utils.py
# 第 459 行，Triton kernel（模块级函数）
</span><span class="nd">@triton.jit</span>
<span class="k">def</span> <span class="nf">_rejection_kernel</span><span class="p">(...):</span>
    <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">num_draft_tokens</span><span class="p">):</span>
        <span class="c1"># 非 greedy：概率比检验
</span>        <span class="n">accepted</span> <span class="o">&amp;=</span> <span class="n">target_log_prob</span> <span class="o">&gt;</span> <span class="n">tl</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="n">u</span><span class="p">)</span> <span class="o">+</span> <span class="n">draft_log_prob</span>
        <span class="c1"># greedy：精确匹配
</span>        <span class="n">target_argmax</span> <span class="o">=</span> <span class="nf">compute_global_target_argmax</span><span class="p">(...)</span>
        <span class="n">accepted</span> <span class="o">&amp;=</span> <span class="p">(</span><span class="n">target_argmax</span> <span class="o">==</span> <span class="n">draft_sampled</span><span class="p">)</span>

    <span class="n">rejected_steps</span><span class="p">[</span><span class="n">req</span><span class="p">]</span> <span class="o">=</span> <span class="n">first_rejected_index</span>  <span class="c1"># 记录第一个被拒绝的位置
</span></code></pre></div></div> <p>一旦某个位置被拒绝，后续 draft 全部作废（基于错误前缀生成的），顺序检查并记录第一个拒绝位置即可。</p> <hr/> <h2 id="8-specdecodemetadata贯穿三步的数据胶水">8. SpecDecodeMetadata：贯穿三步的数据胶水</h2> <p><code class="language-plaintext highlighter-rouge">SpecDecodeMetadata</code> 在 <code class="language-plaintext highlighter-rouge">_prepare_inputs()</code> 第 2212 行构建，是 execute_model 和 sample_tokens 之间信息传递的核心：</p> <table> <thead> <tr> <th>字段</th> <th>形状</th> <th>作用</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">draft_token_ids</code></td> <td><code class="language-plaintext highlighter-rouge">[total_num_draft_tokens]</code></td> <td>所有请求的 draft tokens 扁平拼接</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">num_draft_tokens</code></td> <td><code class="language-plaintext highlighter-rouge">list[int]</code></td> <td>每个请求各自 draft 了多少步</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">logits_indices</code></td> <td><code class="language-plaintext highlighter-rouge">[num_draft + batch_size]</code></td> <td>在 target logits 中 gather 对应行的索引</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">bonus_logits_indices</code></td> <td><code class="language-plaintext highlighter-rouge">[batch_size]</code></td> <td>每个请求 bonus token 在 target logits 中的位置</td> </tr> </tbody> </table> <p><code class="language-plaintext highlighter-rouge">logits_indices</code> 的设计原因：batch 里不同请求的 draft 步数可以不一样（context 太长或提前停止），target logits 的位置对应关系不规则。把所有请求的 draft 位置和 bonus 位置拼成一个索引，<code class="language-plaintext highlighter-rouge">RejectionSampler</code> 一次 gather 拿到所有需要的 logits，不用按请求循环。</p> <hr/> <h2 id="9-延伸self-speculative-decoding-为什么难接进-vllm">9. 延伸：Self-Speculative Decoding 为什么难接进 vllm</h2> <p>前面讲的都是”独立 draft 模型”方案。另一个思路是 Self-Speculative Decoding（SSD）——不引入额外模型，用同一个模型本身的子网络生成 draft。</p> <table> <thead> <tr> <th>方案</th> <th>核心思路</th> </tr> </thead> <tbody> <tr> <td>Early Exit</td> <td>前几层 transformer 输出直接过 LM head</td> </tr> <tr> <td>Layer Skip</td> <td>跳过部分中间层，用稀疏子网络做 draft</td> </tr> <tr> <td>Sparse Attention</td> <td>draft 阶段只加载部分 KV，verify 阶段全量</td> </tr> </tbody> </table> <p>vllm v1 目前原生不支持 SSD，有两个根本冲突：<code class="language-plaintext highlighter-rouge">self.drafter</code> 和 target model 是独立对象（SSD 需要两用同一个模型）；KV cache 假设 draft 和 target 各自独立（SSD 的稀疏/全量切换和 block table 设计不兼容）。</p> <p>SparseSpec（arXiv 2512.01278）是稀疏 attention 路线的代表，直接绕开 vllm 实现了整套 serving stack：</p> <p><img src="/assets/img/posts/20260709-vllm-v1-05-speculative-decoding/diagram_sparsespec.png" alt="SparseSpec 自定义 serving stack 架构"/></p> <p>这套设计目前还是 PoC，README 明确说”We plan to upstream a subset of features to vLLM in the future”。</p> <hr/> <h2 id="10-小结">10. 小结</h2> <p><strong>初始化</strong>：draft model 在 <code class="language-plaintext highlighter-rouge">GPUModelRunner.__init__()</code> 里随 Proposer 一起创建，加载到同一 Worker 进程的同一块 GPU，和 target model 之间没有 IPC，只是普通函数调用传 GPU tensor。</p> <p><strong>运行时主线</strong>：每轮推理的核心是 <code class="language-plaintext highlighter-rouge">_prepare_inputs()</code> 把上一轮 propose 出的 K 个 draft token 打包进 <code class="language-plaintext highlighter-rouge">input_ids</code>，target forward 一次验证全部，替代了 K 次串行 decode。<code class="language-plaintext highlighter-rouge">sample_tokens()</code> 随后做 rejection → bookkeeping → propose，为下一轮准备新的 draft。</p> <p><strong>Proposer 设计</strong>：四种实现对外统一调 <code class="language-plaintext highlighter-rouge">propose()</code>，返回 <code class="language-plaintext highlighter-rouge">list[Tensor]</code>（长度 K，每个 shape <code class="language-plaintext highlighter-rouge">[batch_size]</code>）。EAGLE 和 DraftModel 共用基类 autoregressive 循环，唯一差异是 <code class="language-plaintext highlighter-rouge">pass_hidden_states_to_model</code> 开关——EAGLE 把 target hidden states 拼入 draft model 输入，DraftModel 完全独立；Medusa 并行多头一次 forward 出 K 个 draft；ngram 无模型，纯 CPU 匹配。</p> <p><strong>RejectionSampler</strong>：四阶段 Triton kernel，数学上保证输出分布和原始自回归采样等价。<code class="language-plaintext highlighter-rouge">SpecDecodeMetadata</code> 是三步之间的数据胶水，存储 draft token ids 和 logits 索引。</p> <p>如果你在做 Self-Speculative Decoding 方向，如何优雅 fit 进 vllm 的 block table 架构是个值得深挖的工程问题，欢迎评论区交流。</p> <hr/> <blockquote> <p>另外，我们团队最近出版了<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，感兴趣的话可以看看。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="vLLM"/><category term="Speculative Decoding"/><category term="推理优化"/><category term="源码解析"/><summary type="html"><![CDATA[vllm v1 源码精读（五）：从 generate() 到 Speculative Decoding 的完整计算流]]></summary></entry><entry><title type="html">GPU 算力没在算模型，在等 CPU 发号施令——CUDA Graph 是怎么解决这件事的</title><link href="https://marsggbo.github.io/blog/2026/20260708-cuda-graph/" rel="alternate" type="text/html" title="GPU 算力没在算模型，在等 CPU 发号施令——CUDA Graph 是怎么解决这件事的"/><published>2026-07-08T00:00:00+00:00</published><updated>2026-07-08T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260708-cuda-graph</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260708-cuda-graph/"><![CDATA[<h1 id="gpu-算力没在算模型在等-cpu-发号施令cuda-graph-是怎么解决这件事的">GPU 算力没在算模型，在等 CPU 发号施令——CUDA Graph 是怎么解决这件事的</h1> <hr/> <h2 id="1-你大概有这样一段代码">1. 你大概有这样一段代码</h2> <p>用 transformers 加载一个模型，写了个简单的 decode 循环：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">transformers</span> <span class="kn">import</span> <span class="n">AutoModelForCausalLM</span><span class="p">,</span> <span class="n">AutoTokenizer</span>
<span class="kn">import</span> <span class="n">torch</span>

<span class="n">model</span> <span class="o">=</span> <span class="n">AutoModelForCausalLM</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span><span class="sh">"</span><span class="s">Qwen/Qwen2.5-1.5B</span><span class="sh">"</span><span class="p">,</span>
                                              <span class="n">torch_dtype</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="n">float16</span><span class="p">).</span><span class="nf">cuda</span><span class="p">()</span>
<span class="n">model</span><span class="p">.</span><span class="nf">eval</span><span class="p">()</span>
<span class="n">tokenizer</span> <span class="o">=</span> <span class="n">AutoTokenizer</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span><span class="sh">"</span><span class="s">Qwen/Qwen2.5-1.5B</span><span class="sh">"</span><span class="p">)</span>

<span class="n">prompt</span> <span class="o">=</span> <span class="sh">"</span><span class="s">解释一下推测解码的原理</span><span class="sh">"</span>
<span class="n">inputs</span> <span class="o">=</span> <span class="nf">tokenizer</span><span class="p">(</span><span class="n">prompt</span><span class="p">,</span> <span class="n">return_tensors</span><span class="o">=</span><span class="sh">"</span><span class="s">pt</span><span class="sh">"</span><span class="p">).</span><span class="nf">to</span><span class="p">(</span><span class="sh">"</span><span class="s">cuda</span><span class="sh">"</span><span class="p">)</span>

<span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="nf">no_grad</span><span class="p">():</span>
    <span class="n">output</span> <span class="o">=</span> <span class="n">model</span><span class="p">.</span><span class="nf">generate</span><span class="p">(</span><span class="o">**</span><span class="n">inputs</span><span class="p">,</span> <span class="n">max_new_tokens</span><span class="o">=</span><span class="mi">200</span><span class="p">)</span>

<span class="nf">print</span><span class="p">(</span><span class="n">tokenizer</span><span class="p">.</span><span class="nf">decode</span><span class="p">(</span><span class="n">output</span><span class="p">[</span><span class="mi">0</span><span class="p">]))</span>
</code></pre></div></div> <p>能跑，但总觉得慢，想优化。</p> <p>加量化、加 flash attention、换 vLLM……这些方法都有效，但 <strong>CUDA Graph</strong> 是其中最”无副作用”的一个——不改模型权重，不改输出质量，只改”GPU 执行任务的方式”，在 decode 阶段能有 20-50% 的提升，代码改动量也不大。</p> <p>这篇文章就讲怎么给自己的推理代码加 CUDA Graph。</p> <hr/> <h2 id="2-先搞清楚为什么慢kernel-launch-overhead">2. 先搞清楚为什么慢：kernel launch overhead</h2> <p>大模型的一次前向，不是一个大计算，而是几百个小 CUDA kernel 的串行组合：RMSNorm、RoPE、QKV projection、attention、FFN gate/up/down、residual add……每个都是独立的 kernel。</p> <p>每次调用一个 kernel，CPU 都要做一次 <strong>kernel launch</strong>：打包参数、通知 driver、把任务推进 GPU 队列。这个操作本身耗时 <strong>5-20 µs</strong>，看起来不多，但一次前向几百个 kernel 累积下来就是几毫秒。</p> <p>Decode 阶段每步只生成一个 token，单步耗时可能就 10-20 ms，kernel launch overhead 就占了 20-30%。GPU 的相当一部分时间在等 CPU 一遍遍地发号施令，而不是在做计算。</p> <p>用 nsight systems profile 一下会很直观——timeline 上 GPU 活跃段之间有一堆小空隙，那些就是 CPU launch 的等待时间。</p> <p>CUDA Graph 的解法很直接：<strong>第一次推理时把这几百个 kernel 的序列”录制”成一张图（DAG），之后每次推理直接 replay 这张图</strong>——整个 forward 只需要一次 CPU 调用，launch overhead 接近清零。</p> <hr/> <h2 id="3-先跑一个最简单的-demo感受一下">3. 先跑一个最简单的 demo，感受一下</h2> <p>在套到 LLM 之前，先用一个最小的可运行例子感受 CG 的效果——直接复制跑，有 GPU 就行：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">torch</span>
<span class="kn">import</span> <span class="n">time</span>

<span class="n">device</span> <span class="o">=</span> <span class="sh">"</span><span class="s">cuda</span><span class="sh">"</span>

<span class="c1"># 用一个小 MLP 模拟 LLM 里的一层
</span><span class="n">model</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">nn</span><span class="p">.</span><span class="nc">Sequential</span><span class="p">(</span>
    <span class="n">torch</span><span class="p">.</span><span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="mi">2048</span><span class="p">,</span> <span class="mi">8192</span><span class="p">),</span>
    <span class="n">torch</span><span class="p">.</span><span class="n">nn</span><span class="p">.</span><span class="nc">GELU</span><span class="p">(),</span>
    <span class="n">torch</span><span class="p">.</span><span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="mi">8192</span><span class="p">,</span> <span class="mi">2048</span><span class="p">),</span>
<span class="p">).</span><span class="nf">to</span><span class="p">(</span><span class="n">device</span><span class="p">).</span><span class="nf">half</span><span class="p">().</span><span class="nf">eval</span><span class="p">()</span>

<span class="c1"># ⚠️ 关键：输入 tensor 提前分配好，地址整个生命周期不变
</span><span class="n">x</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="mi">8</span><span class="p">,</span> <span class="mi">2048</span><span class="p">,</span> <span class="n">device</span><span class="o">=</span><span class="n">device</span><span class="p">,</span> <span class="n">dtype</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="n">float16</span><span class="p">)</span>

<span class="c1"># --- Step 1: warmup（让 kernel 编译完成、显存分配稳定）---
</span><span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="nf">no_grad</span><span class="p">():</span>
    <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">3</span><span class="p">):</span>
        <span class="n">_</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>

<span class="c1"># --- Step 2: capture ---
</span><span class="n">g</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="nc">CUDAGraph</span><span class="p">()</span>
<span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="nf">no_grad</span><span class="p">():</span>
    <span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="nf">graph</span><span class="p">(</span><span class="n">g</span><span class="p">):</span>
        <span class="n">y</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>   <span class="c1"># 被"录制"进 DAG，这次不真正执行
</span>
<span class="c1"># --- Step 3: replay ---
# 每次推理：就地更新输入（地址不变），然后 replay 整张图
</span><span class="n">x</span><span class="p">.</span><span class="nf">copy_</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">randn_like</span><span class="p">(</span><span class="n">x</span><span class="p">))</span>
<span class="n">g</span><span class="p">.</span><span class="nf">replay</span><span class="p">()</span>
<span class="c1"># y 的地址是 capture 时固定的，直接读
</span><span class="nf">print</span><span class="p">(</span><span class="n">y</span><span class="p">.</span><span class="n">shape</span><span class="p">)</span>   <span class="c1"># torch.Size([8, 2048])
</span>
<span class="c1"># --- 对比一下速度 ---
</span><span class="n">N</span> <span class="o">=</span> <span class="mi">2000</span>
<span class="n">torch</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="nf">synchronize</span><span class="p">()</span>
<span class="n">t0</span> <span class="o">=</span> <span class="n">time</span><span class="p">.</span><span class="nf">time</span><span class="p">()</span>
<span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="nf">no_grad</span><span class="p">():</span>
    <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">N</span><span class="p">):</span>
        <span class="n">x</span><span class="p">.</span><span class="nf">copy_</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">randn_like</span><span class="p">(</span><span class="n">x</span><span class="p">))</span>
        <span class="n">_</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
<span class="n">torch</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="nf">synchronize</span><span class="p">()</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Eager: </span><span class="si">{</span><span class="p">(</span><span class="n">time</span><span class="p">.</span><span class="nf">time</span><span class="p">()</span> <span class="o">-</span> <span class="n">t0</span><span class="p">)</span> <span class="o">*</span> <span class="mi">1000</span> <span class="o">/</span> <span class="n">N</span><span class="si">:</span><span class="p">.</span><span class="mi">3</span><span class="n">f</span><span class="si">}</span><span class="s"> ms/iter</span><span class="sh">"</span><span class="p">)</span>

<span class="n">torch</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="nf">synchronize</span><span class="p">()</span>
<span class="n">t0</span> <span class="o">=</span> <span class="n">time</span><span class="p">.</span><span class="nf">time</span><span class="p">()</span>
<span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">N</span><span class="p">):</span>
    <span class="n">x</span><span class="p">.</span><span class="nf">copy_</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">randn_like</span><span class="p">(</span><span class="n">x</span><span class="p">))</span>
    <span class="n">g</span><span class="p">.</span><span class="nf">replay</span><span class="p">()</span>
<span class="n">torch</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="nf">synchronize</span><span class="p">()</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">CG:    </span><span class="si">{</span><span class="p">(</span><span class="n">time</span><span class="p">.</span><span class="nf">time</span><span class="p">()</span> <span class="o">-</span> <span class="n">t0</span><span class="p">)</span> <span class="o">*</span> <span class="mi">1000</span> <span class="o">/</span> <span class="n">N</span><span class="si">:</span><span class="p">.</span><span class="mi">3</span><span class="n">f</span><span class="si">}</span><span class="s"> ms/iter</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <p>在 A100 上跑出来大概是：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Eager: 0.130 ms/iter
CG:    0.032 ms/iter
</code></pre></div></div> <p>四倍差距，全来自消掉了几十个 kernel 的 launch overhead。这个 MLP 只有 3 个 kernel，LLM 一层有几十个，累计下来收益更大。</p> <p>三个要点记住就够了：</p> <ol> <li><strong>warmup 不能省</strong>，3 次足够</li> <li><strong>输入 <code class="language-plaintext highlighter-rouge">x</code> 的地址不能变</strong>，只能就地 <code class="language-plaintext highlighter-rouge">copy_()</code> 或 <code class="language-plaintext highlighter-rouge">fill_()</code> 更新值</li> <li><strong>replay 后直接读 <code class="language-plaintext highlighter-rouge">y</code></strong>，它的地址也是 capture 时固定的</li> </ol> <hr/> <h2 id="4-套到-llm-上第一个坑kv-cache">4. 套到 LLM 上：第一个坑，KV cache</h2> <p>理解了基本用法，来套到 LLM 推理上。</p> <p>LLM 的 decode 阶段每步输入是当前 token（shape <code class="language-plaintext highlighter-rouge">[batch, 1]</code>），同时维护一个 KV cache 存历史计算结果。问题就出在这里——<strong>标准 transformers 的 KV cache 是一个 tuple，每步都会增长</strong>：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># step 1: past_key_values 里每个 tensor shape = [batch, heads, 1, head_dim]
# step 2: past_key_values 里每个 tensor shape = [batch, heads, 2, head_dim]
# step 3: [batch, heads, 3, head_dim]
# ...
</span></code></pre></div></div> <p>每步 shape 不一样，就意味着每步 kernel 的参数不一样，<strong>图的拓扑在变</strong>。CUDA Graph 要求拓扑固定，这里直接冲突。</p> <p><strong>解法：StaticCache</strong></p> <p>transformers 4.38 之后提供了 <code class="language-plaintext highlighter-rouge">StaticCache</code>——提前分配一块固定大小的显存，KV cache 永远写在这块固定 buffer 里，shape 不变，每步只是更新写入位置：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">transformers</span> <span class="kn">import</span> <span class="n">StaticCache</span>

<span class="c1"># 提前分配好，最多支持 512 个 token 的 KV cache
</span><span class="n">static_cache</span> <span class="o">=</span> <span class="nc">StaticCache</span><span class="p">(</span>
    <span class="n">config</span><span class="o">=</span><span class="n">model</span><span class="p">.</span><span class="n">config</span><span class="p">,</span>
    <span class="n">max_batch_size</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span>
    <span class="n">max_cache_len</span><span class="o">=</span><span class="mi">512</span><span class="p">,</span>
    <span class="n">device</span><span class="o">=</span><span class="sh">"</span><span class="s">cuda</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">dtype</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="n">float16</span><span class="p">,</span>
<span class="p">)</span>
</code></pre></div></div> <p>有了 StaticCache，KV cache 的 shape 整个推理过程都不变，CUDA Graph 的”拓扑固定”约束就满足了。</p> <hr/> <h2 id="5-完整可运行代码">5. 完整可运行代码</h2> <p>把所有东西拼在一起：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">transformers</span> <span class="kn">import</span> <span class="n">AutoModelForCausalLM</span><span class="p">,</span> <span class="n">AutoTokenizer</span><span class="p">,</span> <span class="n">StaticCache</span>
<span class="kn">import</span> <span class="n">torch</span>

<span class="c1"># --- 加载模型 ---
</span><span class="n">model_name</span> <span class="o">=</span> <span class="sh">"</span><span class="s">Qwen/Qwen2.5-1.5B</span><span class="sh">"</span>
<span class="n">model</span> <span class="o">=</span> <span class="n">AutoModelForCausalLM</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span>
    <span class="n">model_name</span><span class="p">,</span> <span class="n">torch_dtype</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="n">float16</span>
<span class="p">).</span><span class="nf">cuda</span><span class="p">().</span><span class="nf">eval</span><span class="p">()</span>
<span class="n">tokenizer</span> <span class="o">=</span> <span class="n">AutoTokenizer</span><span class="p">.</span><span class="nf">from_pretrained</span><span class="p">(</span><span class="n">model_name</span><span class="p">)</span>

<span class="n">MAX_SEQ_LEN</span> <span class="o">=</span> <span class="mi">512</span>
<span class="n">BATCH_SIZE</span>  <span class="o">=</span> <span class="mi">1</span>

<span class="c1"># --- 分配固定地址的输入 buffer（地址整个生命周期不变）---
</span><span class="n">input_ids</span>    <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">BATCH_SIZE</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span>  <span class="n">dtype</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="nb">long</span><span class="p">,</span>  <span class="n">device</span><span class="o">=</span><span class="sh">"</span><span class="s">cuda</span><span class="sh">"</span><span class="p">)</span>
<span class="n">position_ids</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">BATCH_SIZE</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span>  <span class="n">dtype</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="nb">long</span><span class="p">,</span>  <span class="n">device</span><span class="o">=</span><span class="sh">"</span><span class="s">cuda</span><span class="sh">"</span><span class="p">)</span>
<span class="n">cache_pos</span>    <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span>              <span class="n">dtype</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="nb">long</span><span class="p">,</span>  <span class="n">device</span><span class="o">=</span><span class="sh">"</span><span class="s">cuda</span><span class="sh">"</span><span class="p">)</span>

<span class="c1"># --- 分配 StaticCache ---
</span><span class="n">static_cache</span> <span class="o">=</span> <span class="nc">StaticCache</span><span class="p">(</span>
    <span class="n">config</span><span class="o">=</span><span class="n">model</span><span class="p">.</span><span class="n">config</span><span class="p">,</span>
    <span class="n">max_batch_size</span><span class="o">=</span><span class="n">BATCH_SIZE</span><span class="p">,</span>
    <span class="n">max_cache_len</span><span class="o">=</span><span class="n">MAX_SEQ_LEN</span><span class="p">,</span>
    <span class="n">device</span><span class="o">=</span><span class="sh">"</span><span class="s">cuda</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">dtype</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="n">float16</span><span class="p">,</span>
<span class="p">)</span>

<span class="c1"># --- Step 1: warmup ---
</span><span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">3</span><span class="p">):</span>
    <span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="nf">no_grad</span><span class="p">():</span>
        <span class="nf">model</span><span class="p">(</span>
            <span class="n">input_ids</span><span class="o">=</span><span class="n">input_ids</span><span class="p">,</span>
            <span class="n">position_ids</span><span class="o">=</span><span class="n">position_ids</span><span class="p">,</span>
            <span class="n">cache_position</span><span class="o">=</span><span class="n">cache_pos</span><span class="p">,</span>
            <span class="n">past_key_values</span><span class="o">=</span><span class="n">static_cache</span><span class="p">,</span>
            <span class="n">use_cache</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
        <span class="p">)</span>

<span class="c1"># --- Step 2: capture ---
</span><span class="n">static_cache</span><span class="p">.</span><span class="nf">reset</span><span class="p">()</span>   <span class="c1"># 重置 cache 状态（warmup 写进去的内容清掉）
</span>
<span class="n">g</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="nc">CUDAGraph</span><span class="p">()</span>
<span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="nf">no_grad</span><span class="p">():</span>
    <span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="nf">graph</span><span class="p">(</span><span class="n">g</span><span class="p">):</span>
        <span class="n">logits</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span>
            <span class="n">input_ids</span><span class="o">=</span><span class="n">input_ids</span><span class="p">,</span>
            <span class="n">position_ids</span><span class="o">=</span><span class="n">position_ids</span><span class="p">,</span>
            <span class="n">cache_position</span><span class="o">=</span><span class="n">cache_pos</span><span class="p">,</span>
            <span class="n">past_key_values</span><span class="o">=</span><span class="n">static_cache</span><span class="p">,</span>
            <span class="n">use_cache</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
        <span class="p">).</span><span class="n">logits</span>   <span class="c1"># shape: [batch, 1, vocab_size]
</span>
<span class="c1"># --- Step 3: decode 循环 ---
</span><span class="k">def</span> <span class="nf">decode_one_token</span><span class="p">(</span><span class="n">token</span><span class="p">:</span> <span class="nb">int</span><span class="p">,</span> <span class="n">pos</span><span class="p">:</span> <span class="nb">int</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">int</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">一步 decode，返回下一个 token id</span><span class="sh">"""</span>
    <span class="n">input_ids</span><span class="p">.</span><span class="nf">fill_</span><span class="p">(</span><span class="n">token</span><span class="p">)</span>    <span class="c1"># 就地更新，地址不变
</span>    <span class="n">position_ids</span><span class="p">.</span><span class="nf">fill_</span><span class="p">(</span><span class="n">pos</span><span class="p">)</span>
    <span class="n">cache_pos</span><span class="p">.</span><span class="nf">fill_</span><span class="p">(</span><span class="n">pos</span><span class="p">)</span>
    <span class="n">g</span><span class="p">.</span><span class="nf">replay</span><span class="p">()</span>                 <span class="c1"># replay，不重新 launch kernel
</span>    <span class="k">return</span> <span class="n">logits</span><span class="p">[</span><span class="mi">0</span><span class="p">,</span> <span class="mi">0</span><span class="p">].</span><span class="nf">argmax</span><span class="p">().</span><span class="nf">item</span><span class="p">()</span>


<span class="k">def</span> <span class="nf">generate</span><span class="p">(</span><span class="n">prompt</span><span class="p">:</span> <span class="nb">str</span><span class="p">,</span> <span class="n">max_new_tokens</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">100</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
    <span class="n">inputs</span> <span class="o">=</span> <span class="nf">tokenizer</span><span class="p">(</span><span class="n">prompt</span><span class="p">,</span> <span class="n">return_tensors</span><span class="o">=</span><span class="sh">"</span><span class="s">pt</span><span class="sh">"</span><span class="p">).</span><span class="nf">to</span><span class="p">(</span><span class="sh">"</span><span class="s">cuda</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">input_token_ids</span> <span class="o">=</span> <span class="n">inputs</span><span class="p">[</span><span class="sh">"</span><span class="s">input_ids</span><span class="sh">"</span><span class="p">][</span><span class="mi">0</span><span class="p">].</span><span class="nf">tolist</span><span class="p">()</span>

    <span class="c1"># prefill 阶段：正常跑，不用 CUDA Graph
</span>    <span class="c1"># （prefill 序列长度每次不同，不适合 CG）
</span>    <span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="nf">no_grad</span><span class="p">():</span>
        <span class="n">static_cache</span><span class="p">.</span><span class="nf">reset</span><span class="p">()</span>
        <span class="n">prefill_out</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span>
            <span class="n">input_ids</span><span class="o">=</span><span class="n">inputs</span><span class="p">[</span><span class="sh">"</span><span class="s">input_ids</span><span class="sh">"</span><span class="p">],</span>
            <span class="n">cache_position</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="nf">arange</span><span class="p">(</span><span class="nf">len</span><span class="p">(</span><span class="n">input_token_ids</span><span class="p">),</span> <span class="n">device</span><span class="o">=</span><span class="sh">"</span><span class="s">cuda</span><span class="sh">"</span><span class="p">),</span>
            <span class="n">past_key_values</span><span class="o">=</span><span class="n">static_cache</span><span class="p">,</span>
            <span class="n">use_cache</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
        <span class="p">)</span>

    <span class="c1"># 取 prefill 最后一步的 next token
</span>    <span class="n">next_token</span> <span class="o">=</span> <span class="n">prefill_out</span><span class="p">.</span><span class="n">logits</span><span class="p">[</span><span class="mi">0</span><span class="p">,</span> <span class="o">-</span><span class="mi">1</span><span class="p">].</span><span class="nf">argmax</span><span class="p">().</span><span class="nf">item</span><span class="p">()</span>
    <span class="n">generated</span> <span class="o">=</span> <span class="p">[</span><span class="n">next_token</span><span class="p">]</span>
    <span class="n">pos</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">input_token_ids</span><span class="p">)</span>

    <span class="c1"># decode 阶段：每步 replay CG，飞快
</span>    <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">max_new_tokens</span> <span class="o">-</span> <span class="mi">1</span><span class="p">):</span>
        <span class="k">if</span> <span class="n">next_token</span> <span class="o">==</span> <span class="n">tokenizer</span><span class="p">.</span><span class="n">eos_token_id</span><span class="p">:</span>
            <span class="k">break</span>
        <span class="n">next_token</span> <span class="o">=</span> <span class="nf">decode_one_token</span><span class="p">(</span><span class="n">next_token</span><span class="p">,</span> <span class="n">pos</span><span class="p">)</span>
        <span class="n">generated</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">next_token</span><span class="p">)</span>
        <span class="n">pos</span> <span class="o">+=</span> <span class="mi">1</span>

    <span class="k">return</span> <span class="n">tokenizer</span><span class="p">.</span><span class="nf">decode</span><span class="p">(</span><span class="n">generated</span><span class="p">,</span> <span class="n">skip_special_tokens</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>


<span class="nf">print</span><span class="p">(</span><span class="nf">generate</span><span class="p">(</span><span class="sh">"</span><span class="s">解释一下推测解码的原理</span><span class="sh">"</span><span class="p">))</span>
</code></pre></div></div> <p>这段代码和最开始那段的主要区别：</p> <ol> <li><code class="language-plaintext highlighter-rouge">StaticCache</code> 替换了动态增长的 past_key_values</li> <li>单独的固定 buffer（<code class="language-plaintext highlighter-rouge">input_ids</code>、<code class="language-plaintext highlighter-rouge">position_ids</code>、<code class="language-plaintext highlighter-rouge">cache_pos</code>），每步 <code class="language-plaintext highlighter-rouge">fill_()</code> 更新</li> <li>prefill 走正常 eager，decode 走 CUDA Graph replay</li> </ol> <hr/> <h2 id="6-第二个坑batch-size-变了怎么办">6. 第二个坑：batch size 变了怎么办</h2> <p>上面的代码是 batch size = 1 的版本。实际场景里，你可能想同时处理多个请求（batch inference），而且每轮 decode batch size 可能在变（有请求完成就减少，有新请求进来就增加）。</p> <p>CUDA Graph 捕获的是固定 batch size 的图，batch size 变了图就不能用了。</p> <p>解法：<strong>预先捕获几个常用 batch size 的图，放进一个字典，运行时按实际 batch size 选对应的图</strong>。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 预先 capture 这几个 batch size 的图
</span><span class="n">CAPTURE_BATCH_SIZES</span> <span class="o">=</span> <span class="p">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="mi">8</span><span class="p">,</span> <span class="mi">16</span><span class="p">,</span> <span class="mi">32</span><span class="p">]</span>

<span class="n">graphs</span> <span class="o">=</span> <span class="p">{}</span>   <span class="c1"># batch_size → CUDAGraph
</span><span class="n">logits_buffers</span> <span class="o">=</span> <span class="p">{}</span>   <span class="c1"># batch_size → logits tensor（capture 时固定的地址）
</span>
<span class="k">for</span> <span class="n">bs</span> <span class="ow">in</span> <span class="n">CAPTURE_BATCH_SIZES</span><span class="p">:</span>
    <span class="c1"># 每个 batch size 单独分配一套 buffer
</span>    <span class="n">_input_ids</span>    <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">bs</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">dtype</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="nb">long</span><span class="p">,</span>  <span class="n">device</span><span class="o">=</span><span class="sh">"</span><span class="s">cuda</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">_position_ids</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">bs</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">dtype</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="nb">long</span><span class="p">,</span>  <span class="n">device</span><span class="o">=</span><span class="sh">"</span><span class="s">cuda</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">_cache_pos</span>    <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span>    <span class="n">dtype</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="nb">long</span><span class="p">,</span>  <span class="n">device</span><span class="o">=</span><span class="sh">"</span><span class="s">cuda</span><span class="sh">"</span><span class="p">)</span>
    <span class="n">_cache</span> <span class="o">=</span> <span class="nc">StaticCache</span><span class="p">(</span><span class="n">config</span><span class="o">=</span><span class="n">model</span><span class="p">.</span><span class="n">config</span><span class="p">,</span> <span class="n">max_batch_size</span><span class="o">=</span><span class="n">bs</span><span class="p">,</span>
                         <span class="n">max_cache_len</span><span class="o">=</span><span class="n">MAX_SEQ_LEN</span><span class="p">,</span> <span class="n">device</span><span class="o">=</span><span class="sh">"</span><span class="s">cuda</span><span class="sh">"</span><span class="p">,</span>
                         <span class="n">dtype</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="n">float16</span><span class="p">)</span>

    <span class="c1"># warmup
</span>    <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">3</span><span class="p">):</span>
        <span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="nf">no_grad</span><span class="p">():</span>
            <span class="nf">model</span><span class="p">(</span><span class="n">input_ids</span><span class="o">=</span><span class="n">_input_ids</span><span class="p">,</span> <span class="n">position_ids</span><span class="o">=</span><span class="n">_position_ids</span><span class="p">,</span>
                  <span class="n">cache_position</span><span class="o">=</span><span class="n">_cache_pos</span><span class="p">,</span> <span class="n">past_key_values</span><span class="o">=</span><span class="n">_cache</span><span class="p">,</span>
                  <span class="n">use_cache</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>

    <span class="c1"># capture
</span>    <span class="n">_cache</span><span class="p">.</span><span class="nf">reset</span><span class="p">()</span>
    <span class="n">g</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="nc">CUDAGraph</span><span class="p">()</span>
    <span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="nf">no_grad</span><span class="p">():</span>
        <span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="nf">graph</span><span class="p">(</span><span class="n">g</span><span class="p">):</span>
            <span class="n">_logits</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">input_ids</span><span class="o">=</span><span class="n">_input_ids</span><span class="p">,</span> <span class="n">position_ids</span><span class="o">=</span><span class="n">_position_ids</span><span class="p">,</span>
                            <span class="n">cache_position</span><span class="o">=</span><span class="n">_cache_pos</span><span class="p">,</span> <span class="n">past_key_values</span><span class="o">=</span><span class="n">_cache</span><span class="p">,</span>
                            <span class="n">use_cache</span><span class="o">=</span><span class="bp">True</span><span class="p">).</span><span class="n">logits</span>

    <span class="n">graphs</span><span class="p">[</span><span class="n">bs</span><span class="p">]</span> <span class="o">=</span> <span class="p">{</span>
        <span class="sh">"</span><span class="s">graph</span><span class="sh">"</span><span class="p">:</span> <span class="n">g</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">input_ids</span><span class="sh">"</span><span class="p">:</span> <span class="n">_input_ids</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">position_ids</span><span class="sh">"</span><span class="p">:</span> <span class="n">_position_ids</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">cache_pos</span><span class="sh">"</span><span class="p">:</span> <span class="n">_cache_pos</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">logits</span><span class="sh">"</span><span class="p">:</span> <span class="n">_logits</span><span class="p">,</span>
    <span class="p">}</span>


<span class="k">def</span> <span class="nf">decode_batch</span><span class="p">(</span><span class="n">tokens</span><span class="p">:</span> <span class="nb">list</span><span class="p">[</span><span class="nb">int</span><span class="p">],</span> <span class="n">pos</span><span class="p">:</span> <span class="nb">int</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">list</span><span class="p">[</span><span class="nb">int</span><span class="p">]:</span>
    <span class="n">bs</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">tokens</span><span class="p">)</span>

    <span class="c1"># 找最小的已捕获 batch size &gt;= bs，padding 多余位置
</span>    <span class="n">target_bs</span> <span class="o">=</span> <span class="nf">next</span><span class="p">(</span><span class="n">b</span> <span class="k">for</span> <span class="n">b</span> <span class="ow">in</span> <span class="n">CAPTURE_BATCH_SIZES</span> <span class="k">if</span> <span class="n">b</span> <span class="o">&gt;=</span> <span class="n">bs</span><span class="p">)</span>
    <span class="n">entry</span> <span class="o">=</span> <span class="n">graphs</span><span class="p">[</span><span class="n">target_bs</span><span class="p">]</span>

    <span class="n">entry</span><span class="p">[</span><span class="sh">"</span><span class="s">input_ids</span><span class="sh">"</span><span class="p">][:</span><span class="n">bs</span><span class="p">].</span><span class="nf">fill_</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>           <span class="c1"># 先清空
</span>    <span class="k">for</span> <span class="n">i</span><span class="p">,</span> <span class="n">t</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">tokens</span><span class="p">):</span>
        <span class="n">entry</span><span class="p">[</span><span class="sh">"</span><span class="s">input_ids</span><span class="sh">"</span><span class="p">][</span><span class="n">i</span><span class="p">].</span><span class="nf">fill_</span><span class="p">(</span><span class="n">t</span><span class="p">)</span>          <span class="c1"># 就地更新有效 token
</span>    <span class="n">entry</span><span class="p">[</span><span class="sh">"</span><span class="s">position_ids</span><span class="sh">"</span><span class="p">][:</span><span class="n">bs</span><span class="p">].</span><span class="nf">fill_</span><span class="p">(</span><span class="n">pos</span><span class="p">)</span>
    <span class="n">entry</span><span class="p">[</span><span class="sh">"</span><span class="s">cache_pos</span><span class="sh">"</span><span class="p">].</span><span class="nf">fill_</span><span class="p">(</span><span class="n">pos</span><span class="p">)</span>

    <span class="n">entry</span><span class="p">[</span><span class="sh">"</span><span class="s">graph</span><span class="sh">"</span><span class="p">].</span><span class="nf">replay</span><span class="p">()</span>

    <span class="k">return</span> <span class="n">entry</span><span class="p">[</span><span class="sh">"</span><span class="s">logits</span><span class="sh">"</span><span class="p">][:</span><span class="n">bs</span><span class="p">,</span> <span class="mi">0</span><span class="p">].</span><span class="nf">argmax</span><span class="p">(</span><span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">).</span><span class="nf">tolist</span><span class="p">()</span>   <span class="c1"># 只取有效部分
</span></code></pre></div></div> <p>这就是 vLLM 的 batch size bucketing 思路，只是简化版。多几张图，覆盖了实际场景里各种 batch size，运行时零额外开销。</p> <hr/> <h2 id="7-第三个坑moe-模型">7. 第三个坑：MoE 模型</h2> <p>如果你跑的是 Mixtral、DeepSeek-MoE、Qwen-MoE 这类 MoE 架构的模型，还有一个额外的问题。</p> <p>Dense 模型每层所有 token 都过同样的权重矩阵，计算路径固定。MoE 不一样——router 会把 token 分配给不同的 expert，<strong>每个 expert 这一步收到多少 token 是数据依赖的</strong>，每次都可能不同：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>batch=8, top_k=2, 8 个 expert → 理论上每个 expert 平均 2 个 token
实际可能：expert0 收 5 个，expert1 收 0 个，expert2 收 3 个...
</code></pre></div></div> <p>expert 收到不同数量的 token，送进 expert FFN 的 tensor shape 每次都在变，CUDA Graph 直接挂。这也是为什么在一些 GitHub issue 里会看到这样的说法：</p> <blockquote> <p><em>This is due to the tensor in MoE layer has dynamic shape, which is not applicable for CUDA graph. You can partially capture CUDA graph for Attention layer.</em></p> </blockquote> <p><strong>先说好消息：batch=1 的 decode 不受影响</strong></p> <p>batch=1，每步只有 1 个 token，top_k=2 的情况下恰好 2 个 expert 各收到 1 个 token，shape 固定。上面第 5 节的代码直接拿去跑 MoE 模型，decode 阶段完全没问题。</p> <p>batch&gt;1 才是麻烦的地方，下面说两种解法。</p> <h3 id="解法一只-capture-attentionmoe-ffn-跑-eagerpartial-capture">解法一：只 capture attention，MoE FFN 跑 eager（partial capture）</h3> <p>这是最简单、改动最小的方案，也是 Megatron-LM 对 MoE 模型的推荐做法：<strong>每个 transformer layer 里，attention 部分 capture 成 CUDA Graph，MoE FFN 部分继续走 eager</strong>。</p> <p>一个 transformer layer 的结构是：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Input
  → [Attention]   ← shape 固定（batch × 1 × dim），可以 capture
  → [MoE FFN]     ← expert 分配动态，不 capture
  → Output
</code></pre></div></div> <p>把 attention 单独包成一个 CG，MoE FFN 保持 eager，每层的调用变成：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">MoETransformerLayerWithPartialCG</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">attention</span><span class="p">,</span> <span class="n">moe_ffn</span><span class="p">,</span> <span class="n">hidden_size</span><span class="p">,</span> <span class="n">batch_size</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">moe_ffn</span> <span class="o">=</span> <span class="n">moe_ffn</span>

        <span class="c1"># 给 attention 分配固定地址的输入输出 buffer
</span>        <span class="n">self</span><span class="p">.</span><span class="n">attn_input</span>  <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">batch_size</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">hidden_size</span><span class="p">,</span> <span class="n">device</span><span class="o">=</span><span class="sh">"</span><span class="s">cuda</span><span class="sh">"</span><span class="p">,</span> <span class="n">dtype</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="n">float16</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">attn_output</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">batch_size</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">hidden_size</span><span class="p">,</span> <span class="n">device</span><span class="o">=</span><span class="sh">"</span><span class="s">cuda</span><span class="sh">"</span><span class="p">,</span> <span class="n">dtype</span><span class="o">=</span><span class="n">torch</span><span class="p">.</span><span class="n">float16</span><span class="p">)</span>

        <span class="c1"># warmup
</span>        <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">3</span><span class="p">):</span>
            <span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="nf">no_grad</span><span class="p">():</span>
                <span class="n">_</span> <span class="o">=</span> <span class="nf">attention</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">attn_input</span><span class="p">)</span>

        <span class="c1"># 只 capture attention
</span>        <span class="n">self</span><span class="p">.</span><span class="n">attn_graph</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="nc">CUDAGraph</span><span class="p">()</span>
        <span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="nf">no_grad</span><span class="p">():</span>
            <span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="nf">graph</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">attn_graph</span><span class="p">):</span>
                <span class="n">self</span><span class="p">.</span><span class="n">attn_output</span> <span class="o">=</span> <span class="nf">attention</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">attn_input</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">hidden_states</span><span class="p">):</span>
        <span class="c1"># attention：replay CG（无 kernel launch overhead）
</span>        <span class="n">self</span><span class="p">.</span><span class="n">attn_input</span><span class="p">.</span><span class="nf">copy_</span><span class="p">(</span><span class="n">hidden_states</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">attn_graph</span><span class="p">.</span><span class="nf">replay</span><span class="p">()</span>
        <span class="n">attn_out</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">attn_output</span><span class="p">.</span><span class="nf">clone</span><span class="p">()</span>

        <span class="c1"># MoE FFN：正常 eager（shape 动态，无法 capture）
</span>        <span class="n">ffn_out</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">moe_ffn</span><span class="p">(</span><span class="n">attn_out</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">ffn_out</span>
</code></pre></div></div> <p>attention 部分的 kernel launch overhead 消掉了，MoE FFN 部分没动。相比整层都走 eager，能拿到一半左右的 CG 收益——对于 Mixtral 这类模型，attention 占单层耗时的 40-50%，效果还是明显的。</p> <p>Megatron-LM 里通过 <code class="language-plaintext highlighter-rouge">--cuda-graph-modules=attn</code> 参数开启这个模式，正是这个思路。</p> <h3 id="解法二给-moe-ffn-加-expert-capacity整层都-capture">解法二：给 MoE FFN 加 expert capacity，整层都 capture</h3> <p>如果想把 MoE FFN 也 capture 进去，需要消掉 shape 动态的根源：提前规定每个 expert 最多接受 <code class="language-plaintext highlighter-rouge">capacity</code> 个 token，无论 router 实际分了多少，tensor shape 始终是 <code class="language-plaintext highlighter-rouge">[capacity, hidden_dim]</code>——分配超过 capacity 的 token 丢弃，不足的用零 padding：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">math</span>

<span class="k">def</span> <span class="nf">moe_forward_with_capacity</span><span class="p">(</span><span class="n">hidden_states</span><span class="p">,</span> <span class="n">router_logits</span><span class="p">,</span> <span class="n">experts</span><span class="p">,</span>
                               <span class="n">top_k</span><span class="p">,</span> <span class="n">capacity_factor</span><span class="o">=</span><span class="mf">1.25</span><span class="p">):</span>
    <span class="n">batch</span><span class="p">,</span> <span class="n">seq</span><span class="p">,</span> <span class="n">dim</span> <span class="o">=</span> <span class="n">hidden_states</span><span class="p">.</span><span class="n">shape</span>
    <span class="n">hidden_flat</span> <span class="o">=</span> <span class="n">hidden_states</span><span class="p">.</span><span class="nf">view</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">dim</span><span class="p">)</span>     <span class="c1"># [num_tokens, dim]
</span>    <span class="n">num_tokens</span>  <span class="o">=</span> <span class="n">hidden_flat</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span>
    <span class="n">num_experts</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">experts</span><span class="p">)</span>

    <span class="n">routing_weights</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">softmax</span><span class="p">(</span><span class="n">router_logits</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>
    <span class="n">topk_weights</span><span class="p">,</span> <span class="n">topk_ids</span> <span class="o">=</span> <span class="n">routing_weights</span><span class="p">.</span><span class="nf">topk</span><span class="p">(</span><span class="n">top_k</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>

    <span class="c1"># capacity 提前算死，replay 时这个值不会变
</span>    <span class="n">capacity</span> <span class="o">=</span> <span class="n">math</span><span class="p">.</span><span class="nf">ceil</span><span class="p">(</span><span class="n">num_tokens</span> <span class="o">*</span> <span class="n">top_k</span> <span class="o">/</span> <span class="n">num_experts</span> <span class="o">*</span> <span class="n">capacity_factor</span><span class="p">)</span>

    <span class="c1"># 固定 shape 的 buffer：[num_experts, capacity, dim]
</span>    <span class="n">expert_inputs</span>  <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">num_experts</span><span class="p">,</span> <span class="n">capacity</span><span class="p">,</span> <span class="n">dim</span><span class="p">,</span>
                                 <span class="n">device</span><span class="o">=</span><span class="n">hidden_states</span><span class="p">.</span><span class="n">device</span><span class="p">,</span> <span class="n">dtype</span><span class="o">=</span><span class="n">hidden_states</span><span class="p">.</span><span class="n">dtype</span><span class="p">)</span>
    <span class="n">expert_weights</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">num_experts</span><span class="p">,</span> <span class="n">capacity</span><span class="p">,</span>
                                 <span class="n">device</span><span class="o">=</span><span class="n">hidden_states</span><span class="p">.</span><span class="n">device</span><span class="p">,</span> <span class="n">dtype</span><span class="o">=</span><span class="n">hidden_states</span><span class="p">.</span><span class="n">dtype</span><span class="p">)</span>

    <span class="c1"># 把 token 填进对应 expert 的 slot（超出 capacity 就 drop）
</span>    <span class="k">for</span> <span class="n">e</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">num_experts</span><span class="p">):</span>
        <span class="n">assigned</span>   <span class="o">=</span> <span class="p">(</span><span class="n">topk_ids</span> <span class="o">==</span> <span class="n">e</span><span class="p">).</span><span class="nf">any</span><span class="p">(</span><span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>             <span class="c1"># [num_tokens]
</span>        <span class="n">token_idx</span>  <span class="o">=</span> <span class="n">assigned</span><span class="p">.</span><span class="nf">nonzero</span><span class="p">(</span><span class="n">as_tuple</span><span class="o">=</span><span class="bp">True</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span>
        <span class="n">n</span> <span class="o">=</span> <span class="nf">min</span><span class="p">(</span><span class="nf">len</span><span class="p">(</span><span class="n">token_idx</span><span class="p">),</span> <span class="n">capacity</span><span class="p">)</span>
        <span class="k">if</span> <span class="n">n</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">:</span>
            <span class="n">w_pos</span> <span class="o">=</span> <span class="p">(</span><span class="n">topk_ids</span><span class="p">[</span><span class="n">token_idx</span><span class="p">[:</span><span class="n">n</span><span class="p">]]</span> <span class="o">==</span> <span class="n">e</span><span class="p">).</span><span class="nf">float</span><span class="p">().</span><span class="nf">argmax</span><span class="p">(</span><span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>
            <span class="n">expert_inputs</span><span class="p">[</span><span class="n">e</span><span class="p">,</span> <span class="p">:</span><span class="n">n</span><span class="p">]</span>  <span class="o">=</span> <span class="n">hidden_flat</span><span class="p">[</span><span class="n">token_idx</span><span class="p">[:</span><span class="n">n</span><span class="p">]]</span>
            <span class="n">expert_weights</span><span class="p">[</span><span class="n">e</span><span class="p">,</span> <span class="p">:</span><span class="n">n</span><span class="p">]</span> <span class="o">=</span> <span class="n">topk_weights</span><span class="p">[</span><span class="n">token_idx</span><span class="p">[:</span><span class="n">n</span><span class="p">],</span> <span class="n">w_pos</span><span class="p">]</span>

    <span class="c1"># 每个 expert 的输入永远是 [capacity, dim]，shape 固定，CG 可以录制
</span>    <span class="n">outputs</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">stack</span><span class="p">([</span><span class="n">experts</span><span class="p">[</span><span class="n">e</span><span class="p">](</span><span class="n">expert_inputs</span><span class="p">[</span><span class="n">e</span><span class="p">])</span> <span class="k">for</span> <span class="n">e</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">num_experts</span><span class="p">)])</span>

    <span class="c1"># 把结果散回原位置
</span>    <span class="n">result</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros_like</span><span class="p">(</span><span class="n">hidden_flat</span><span class="p">)</span>
    <span class="k">for</span> <span class="n">e</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">num_experts</span><span class="p">):</span>
        <span class="n">assigned</span>  <span class="o">=</span> <span class="p">(</span><span class="n">topk_ids</span> <span class="o">==</span> <span class="n">e</span><span class="p">).</span><span class="nf">any</span><span class="p">(</span><span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>
        <span class="n">token_idx</span> <span class="o">=</span> <span class="n">assigned</span><span class="p">.</span><span class="nf">nonzero</span><span class="p">(</span><span class="n">as_tuple</span><span class="o">=</span><span class="bp">True</span><span class="p">)[</span><span class="mi">0</span><span class="p">]</span>
        <span class="n">n</span> <span class="o">=</span> <span class="nf">min</span><span class="p">(</span><span class="nf">len</span><span class="p">(</span><span class="n">token_idx</span><span class="p">),</span> <span class="n">capacity</span><span class="p">)</span>
        <span class="k">if</span> <span class="n">n</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">:</span>
            <span class="n">result</span><span class="p">[</span><span class="n">token_idx</span><span class="p">[:</span><span class="n">n</span><span class="p">]]</span> <span class="o">+=</span> <span class="n">expert_weights</span><span class="p">[</span><span class="n">e</span><span class="p">,</span> <span class="p">:</span><span class="n">n</span><span class="p">,</span> <span class="bp">None</span><span class="p">]</span> <span class="o">*</span> <span class="n">outputs</span><span class="p">[</span><span class="n">e</span><span class="p">,</span> <span class="p">:</span><span class="n">n</span><span class="p">]</span>

    <span class="k">return</span> <span class="n">result</span><span class="p">.</span><span class="nf">view</span><span class="p">(</span><span class="n">batch</span><span class="p">,</span> <span class="n">seq</span><span class="p">,</span> <span class="n">dim</span><span class="p">)</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">capacity_factor=1.25</code> 给了 25% 余量，生产环境里 token drop 概率很低。代价是每个 expert 都要跑满 <code class="language-plaintext highlighter-rouge">capacity</code> 个位置，padding 的部分算了白算（浪费约 5-10% 算力）。</p> <p>Megatron-LM 的参数是 <code class="language-plaintext highlighter-rouge">--moe-expert-capacity-factor 1.25 --moe-pad-expert-input-to-capacity</code>，开了这两个之后 MoE 层也能 capture。</p> <p>两种方案怎么选：</p> <ul> <li>想最快改完上线，改动最小 → 用 partial capture（解法一）</li> <li>想把 MoE FFN 的 launch overhead 也消掉 → 用 expert capacity（解法二），但要改 MoE 层实现</li> <li>不想自己改，直接用 vLLM → <code class="language-plaintext highlighter-rouge">FusedMoE</code> kernel 内置了 capacity dispatch，CUDA Graph 开箱即用</li> </ul> <hr/> <h2 id="9-prefill-为什么不加-cuda-graph">9. prefill 为什么不加 CUDA Graph</h2> <p>你可能注意到上面的代码里，prefill 阶段始终走的是 eager mode，没有 CUDA Graph。</p> <p>原因很简单：<strong>prefill 的输入序列长度每次都不一样</strong>。你一次处理 “你好” 和 “解释一下量子纠缠的基本原理及其在量子计算中的应用”，这两个序列的长度差了几十倍，对应的 kernel 参数完全不同，图拓扑每次都变，CG 捕获不了。</p> <p>Decode 阶段每步输入永远是一个 token（长度固定为 1），天然适合 CUDA Graph，这也是为什么 vLLM 这类系统里 CG 只作用于 decode。</p> <hr/> <h2 id="10-加了-cg-之后能快多少">10. 加了 CG 之后能快多少</h2> <p>以 Qwen2.5-1.5B 为例，batch size = 1，decode 阶段：</p> <table> <thead> <tr> <th>方式</th> <th>单步延迟</th> <th>吞吐（tokens/s）</th> </tr> </thead> <tbody> <tr> <td>Eager (standard generate)</td> <td>~8 ms</td> <td>~125</td> </tr> <tr> <td>+ CUDA Graph</td> <td>~5 ms</td> <td>~200</td> </tr> <tr> <td>提升</td> <td>~37%</td> <td>~60%</td> </tr> </tbody> </table> <p>小模型收益更大（kernel 数量相对 kernel 计算量的比例更高，launch overhead 占比更大）。大模型（如 70B）每个 kernel 跑的时间本来就长，launch overhead 占比低，CG 的相对收益会小一些，但绝对值（省下的毫秒数）还是有的。</p> <hr/> <h2 id="11-踩坑备忘">11. 踩坑备忘</h2> <p><strong>坑 1：capture 里不能有 Python 控制流依赖 GPU 结果</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># ❌ 这会出问题：capture 时走了某个分支，replay 时永远走那个分支
</span><span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="nf">graph</span><span class="p">(</span><span class="n">g</span><span class="p">):</span>
    <span class="n">out</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
    <span class="k">if</span> <span class="n">out</span><span class="p">.</span><span class="nf">max</span><span class="p">()</span> <span class="o">&gt;</span> <span class="mf">0.5</span><span class="p">:</span>        <span class="c1"># GPU 结果 → Python if → 分支被固化
</span>        <span class="n">out</span> <span class="o">=</span> <span class="nf">postprocess</span><span class="p">(</span><span class="n">out</span><span class="p">)</span>
</code></pre></div></div> <p>把判断逻辑移到 graph 外面，或者改成 <code class="language-plaintext highlighter-rouge">torch.where</code>（GPU tensor 操作，不走 Python 分支）。</p> <p><strong>坑 2：capture 时不能有随机操作</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># ❌ capture 时 dropout 用了随机数，replay 时随机种子不更新
</span><span class="k">with</span> <span class="n">torch</span><span class="p">.</span><span class="n">cuda</span><span class="p">.</span><span class="nf">graph</span><span class="p">(</span><span class="n">g</span><span class="p">):</span>
    <span class="n">out</span> <span class="o">=</span> <span class="nf">model_with_dropout</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
</code></pre></div></div> <p>推理时 <code class="language-plaintext highlighter-rouge">model.eval()</code> 关掉 dropout，这个坑就不存在了。记得 eval。</p> <p><strong>坑 3：capture 里不能有 CPU-GPU 同步</strong></p> <p><code class="language-plaintext highlighter-rouge">tensor.item()</code>、<code class="language-plaintext highlighter-rouge">.cpu()</code>、<code class="language-plaintext highlighter-rouge">print(tensor)</code> 这些都会触发 CPU-GPU 同步，capture 时如果有这些操作会报错或者行为异常。把这类操作移到 graph 外面。</p> <p><strong>坑 4：忘了 warmup 就 capture</strong></p> <p>第一次 capture 之前必须先把 kernel 跑热（3 次以上）。直接 capture 可能录进去的是未完全初始化的 kernel 状态，replay 时结果错误或崩溃。</p> <p><strong>坑 5：StaticCache 忘了 reset</strong></p> <p>capture 前必须调 <code class="language-plaintext highlighter-rouge">static_cache.reset()</code>，否则 warmup 写进去的 KV 数据还在里面，capture 的初始状态不是”空 cache”，之后的推理结果就乱了。</p> <hr/> <h2 id="12-一句话总结">12. 一句话总结</h2> <p>给自己的 transformers 推理代码加 CUDA Graph，核心改动只有两处：一是把 past_key_values 换成 <code class="language-plaintext highlighter-rouge">StaticCache</code>（解决 KV cache shape 动态变化的问题），二是提前分配好固定地址的输入 buffer（解决 tensor 地址不能变的约束）。三步走：warmup → capture → 每步 replay。prefill 不动，只加速 decode。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 LLM 推理效率优化你想系统深入，可以看看我之前出版的<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，书里有专章讲 LLM 推理效率，涵盖量化、kernel 优化和推理框架的工程权衡，和本文讨论的 CUDA 层优化有直接关联。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="推理加速"/><category term="CUDA"/><category term="transformers"/><category term="工程优化"/><summary type="html"><![CDATA[GPU 算力没在算模型，在等 CPU 发号施令——CUDA Graph 是怎么解决这件事的]]></summary></entry><entry><title type="html">投机解码到底能快多少？从理论建模到 closed-form 的完整推导</title><link href="https://marsggbo.github.io/blog/2026/20260707-speculative-decoding-theory/" rel="alternate" type="text/html" title="投机解码到底能快多少？从理论建模到 closed-form 的完整推导"/><published>2026-07-07T00:00:00+00:00</published><updated>2026-07-07T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260707-speculative-decoding-theory</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260707-speculative-decoding-theory/"><![CDATA[<h1 id="投机解码到底能快多少从理论建模到-closed-form-的完整推导">投机解码到底能快多少？从理论建模到 closed-form 的完整推导</h1> <hr/> <h2 id="1-为什么要认真建模这件事">1. 为什么要认真建模这件事</h2> <p>我最近在看 Speculative Decoding（投机解码）和 Self-Speculative Decoding（自投机解码，也叫 SSD）的相关工作，有一个感受：<strong>大家都知道它快，但”快多少”这件事没有被认真量化过</strong>。</p> <p>图里的那个公式（我看到有人分享的参考）给了一个很漂亮的 closed form：</p> \[\text{Speedup} = \frac{1 - \alpha^{\gamma+1}}{(1-\alpha)(\gamma c + 1)}\] <p>其中 $\alpha$ 是逐 token 接受率，$\gamma$ 是草稿长度，$c$ 是草稿模型 vs. 验证（大）模型单 token 推理的计算成本比。</p> <p>这个公式乍看很简洁，但我觉得它藏了不少假设，而且在 SSD（自投机解码）的场景下，这个建模需要做一些修正。今天我想从头把这两条路的性能建模捋一遍，看看各自的 closed form 长什么样，以及哪些因素会让理论和实际产生偏差。</p> <hr/> <h2 id="2-先把基准搞清楚标准自回归的代价">2. 先把基准搞清楚：标准自回归的代价</h2> <p>标准自回归解码生成 $N$ 个 token，每次都要跑一次完整的大模型前向：</p> \[T_\text{baseline} = N \cdot t_\text{big}\] <p>其中 $t_\text{big}$ 是大模型生成一个 token 的时间（包含 prefill 之后的 decode latency，KV cache 命中的情况下可以近似为一个常数）。</p> <p>这是我们的参照系，所有加速比都相对于它来算。</p> <hr/> <h2 id="3-投机解码speculative-decodingsd的性能建模">3. 投机解码（Speculative Decoding，SD）的性能建模</h2> <h3 id="31-一轮的结构">3.1 一轮的结构</h3> <p>经典 SD 的一轮操作是：</p> <ol> <li><strong>Draft 阶段</strong>：用小的 draft model 串行生成 $\gamma$ 个候选 token，耗时 $\gamma \cdot t_\text{draft}$</li> <li><strong>Verify 阶段</strong>：把这 $\gamma$ 个 token 送给大模型（target model）做一次<strong>并行</strong>前向，耗时约 $t_\text{big}(\gamma)$</li> </ol> <p>注意第 2 步不是 $\gamma \cdot t_\text{big}$，因为大模型可以对 $\gamma$ 个 token 同时做 prefill 式的并行计算。但也不完全等于 $t_\text{big}(1)$，因为序列长了 attention 和 KV cache 的 overhead 会稍微变大。</p> <p><strong>简化假设</strong>（也是上面那个公式的隐含假设）：</p> <ul> <li>大模型验证 $\gamma$ 个 token 的时间 $\approx t_\text{big}$（一次 decode cost），即序列长度对单次 forward 耗时影响忽略不计</li> <li>令 $c = t_\text{draft} / t_\text{big}$（草稿比）</li> </ul> <p>那一轮的总耗时：</p> \[T_\text{round} = \gamma \cdot t_\text{draft} + t_\text{big} = (\gamma c + 1) \cdot t_\text{big}\] <h3 id="32-一轮能接受多少-token">3.2 一轮能接受多少 token？</h3> <p>设逐 token 接受率为 $\alpha$（独立同分布假设）。若生成草稿序列为 $d_1, d_2, \ldots, d_\gamma$：</p> <ul> <li>$d_1$ 被接受的概率 $= \alpha$</li> <li>$d_1, d_2$ 都被接受的概率 $= \alpha^2$</li> <li>…</li> <li>全部 $\gamma$ 个都被接受的概率 $= \alpha^\gamma$</li> </ul> <p>每轮接受 token 数的期望（别忘了最后大模型还会贡献一个新 token）：</p> \[\mathbb{E}[\#\text{tokens per round}] = \sum_{k=0}^{\gamma} \alpha^k = \frac{1 - \alpha^{\gamma+1}}{1 - \alpha}\] <p>推导：设 $X$ = 接受的 token 数（从 0 到 $\gamma$），最后大模型总会产出一个 token，所以：</p> \[\mathbb{E}[\#] = 1 + \sum_{k=1}^{\gamma} P(\text{前 } k \text{ 个都接受}) = 1 + \alpha + \alpha^2 + \cdots + \alpha^\gamma = \frac{1 - \alpha^{\gamma+1}}{1 - \alpha}\] <h3 id="33-speedup-的-closed-form">3.3 Speedup 的 closed form</h3> <p>每轮耗时 $(\gamma c + 1) \cdot t_\text{big}$，产出 $\frac{1-\alpha^{\gamma+1}}{1-\alpha}$ 个 token。</p> <p>标准自回归产出同样数量 token 需要 $\frac{1-\alpha^{\gamma+1}}{1-\alpha} \cdot t_\text{big}$。</p> <p>因此加速比为：</p> \[\boxed{\text{Speedup}_\text{SD} = \frac{1 - \alpha^{\gamma+1}}{(1 - \alpha)(\gamma c + 1)}}\] <p>这就是图里的那个公式，完整推导到这里。</p> <h3 id="34-这个公式说了什么">3.4 这个公式说了什么？</h3> <p>对 $\gamma$ 求导，令 $\partial \text{Speedup} / \partial \gamma = 0$，可以找到最优草稿长度 $\gamma^*$。这个导数不太好直接解析出来，但有几个定性结论值得记住：</p> <p><strong>结论 1</strong>：$c$ 越小（草稿模型越轻量），最优 $\gamma^<em>$ 越大，加速上限越高。这就是图里左右两张图的直觉：$c=0.05$ 时 $\gamma^</em> \approx 8$，$c=0.0005$ 时 $\gamma^*$ 更大且加速比可以到 20x。</p> <p><strong>结论 2</strong>：当 $\gamma \to \infty$ 时，若 $\alpha &lt; 1$，Speedup $\to 0$。草稿越长，浪费的验证成本越高，最终得不偿失。</p> <p><strong>结论 3</strong>：$\alpha$ 对加速的影响是非线性的。$\alpha$ 从 0.9 到 0.95 的提升，带来的加速远比 0.7 到 0.75 大。原因是 $\alpha^\gamma$ 在 $\alpha$ 接近 1 时衰减很慢，能撑住更长的草稿序列。</p> <hr/> <h2 id="4-自投机解码self-speculative-decodingssd">4. 自投机解码（Self-Speculative Decoding，SSD）</h2> <h3 id="41-ssd-和-sd-的本质区别">4.1 SSD 和 SD 的本质区别</h3> <p>SSD 的思路是：<strong>不用另一个小模型，直接用大模型自己的 skip layers 来当 draft</strong>。</p> <p>最简单的实现是把大模型的前 $L_d$ 层作为 draft 网络（contiguous prefix），前向到第 $L_d$ 层就直接 decode，生成 $\gamma$ 个草稿 token；然后跑完整的 $L$ 层做验证。但更一般的变体（如 LayerSkip、Draft&amp;Verify 等）会跳过中间某些层，形成<strong>非连续的 skip 模式</strong>，这两种情况的 KV cache 复用行为截然不同——这也是建模里最容易踩坑的地方。</p> <p>好处是：</p> <ul> <li>不需要维护两套权重，内存 footprint 比 SD 低</li> <li>draft 和 verify 共享词表，分布天然对齐</li> <li>在显存受限场景下，省出来的内存可以换更大的 batch size</li> </ul> <p>代价是：draft 不再是独立模型，接受率 $\alpha$ 往往低于精心选型的 draft model。</p> <h3 id="42-kv-cache-复用的前提激活路径必须一致">4.2 KV cache 复用的前提：激活路径必须一致</h3> <p>这里有个经常被忽视的细节，值得单独强调。</p> <p><strong>KV cache 能被 verify 阶段复用，当且仅当对应层的中间激活（hidden state）在 draft 和 verify 路径下完全相同。</strong></p> <p>对于 contiguous prefix（取前 $L_d$ 层）：</p> <ul> <li>draft 路径：层 $1 \to 2 \to \cdots \to L_d$，激活 $h_1, h_2, \ldots, h_{L_d}$</li> <li>verify 路径：层 $1 \to 2 \to \cdots \to L_d \to L_d+1 \to \cdots \to L$，前 $L_d$ 层的激活与 draft <strong>完全相同</strong></li> </ul> <p>所以 verify 阶段可以直接复用 draft 计算过的 KV，只需从第 $L_d+1$ 层开始补算。这是原始 SSD 论文里的场景。</p> <p><strong>但如果 skip 的是非连续的层，情况就不一样了。</strong></p> <p>假设 draft 跳过了第 $s$ 层（$s &lt; L_d$），那么：</p> <ul> <li>draft 路径里，第 $s+1$ 层的输入是第 $s-1$ 层的输出（跳过了 $s$）</li> <li>verify 路径里，第 $s+1$ 层的输入是第 $s$ 层的输出（没有跳过）</li> </ul> <p>两者输入不同，所以第 $s+1$ 层及其之后所有层的 KV 值均不匹配。<strong>一旦某层被跳过，其后所有层的 KV cache 全部作废，无法给 verify 复用。</strong></p> <h3 id="43-一般化建模r_textdraft-和-r_textreuse-拆开">4.3 一般化建模：$r_\text{draft}$ 和 $r_\text{reuse}$ 拆开</h3> <p>针对一般的 skip 模式，需要引入两个独立参数：</p> <ul> <li> <table> <tbody> <tr> <td>$r_\text{draft} =</td> <td>\mathcal{A}</td> <td>/ L$：draft 实际参与计算的层数比例（$\mathcal{A}$ 是 active 层的集合）</td> </tr> </tbody> </table> </li> <li>$r_\text{reuse}$：verify 阶段可以跳过（复用 KV）的层数比例</li> </ul> <p>$r_\text{reuse}$ 的计算规则：设 $s_1 = \min({1,\ldots,L} \setminus \mathcal{A})$ 是第一个被跳过的层，则：</p> \[r_\text{reuse} = \frac{s_1 - 1}{L}\] <p>即：只有第一个 skip 层<strong>之前</strong>的那些层，其 KV 才对 verify 有效。</p> <p>几个特殊情况：</p> <ul> <li><strong>Contiguous prefix</strong>（$\mathcal{A} = {1, \ldots, L_d}$，无跳过）：$s_1 = L_d + 1$，所以 $r_\text{reuse} = r_\text{draft} = r$</li> <li><strong>第一层就跳</strong>（$1 \notin \mathcal{A}$）：$s_1 = 1$，$r_\text{reuse} = 0$，KV cache 完全无法复用</li> <li><strong>中间跳层</strong>（如跳掉第 $s$ 层，$s &lt; L_d$）：$r_\text{reuse} = (s-1)/L &lt; r_\text{draft}$</li> </ul> <p>一轮 SSD 总耗时的一般形式：</p> \[T_\text{round}^\text{SSD} = \gamma \cdot r_\text{draft} \cdot t_\text{big} + (1 - r_\text{reuse}) \cdot t_\text{big} = [\gamma r_\text{draft} + (1 - r_\text{reuse})] \cdot t_\text{big}\] <p>对应的 closed form：</p> \[\boxed{\text{Speedup}_\text{SSD-general} = \frac{1 - \alpha^{\gamma+1}}{(1-\alpha)[\gamma r_\text{draft} + (1 - r_\text{reuse})]}}\] <p>这个公式涵盖了所有 SSD 变体：</p> <table> <thead> <tr> <th>Skip 模式</th> <th>$r_\text{draft}$</th> <th>$r_\text{reuse}$</th> <th>分母</th> <th>退化到</th> </tr> </thead> <tbody> <tr> <td>Contiguous prefix</td> <td>$r$</td> <td>$r$</td> <td>$1 + (\gamma-1)r$</td> <td>原始 SSD 公式</td> </tr> <tr> <td>完全不 skip（$r_\text{draft}=1$）</td> <td>$1$</td> <td>$1$</td> <td>$\gamma$</td> <td>纯串行，无加速</td> </tr> <tr> <td>首层即 skip</td> <td>$r_\text{draft}$</td> <td>$0$</td> <td>$\gamma r_\text{draft} + 1$</td> <td>和 SD 同构，$c = r_\text{draft}$</td> </tr> <tr> <td>中间跳层</td> <td>$r_\text{draft}$</td> <td>$r_\text{reuse} &lt; r_\text{draft}$</td> <td>介于上两者之间</td> <td>—</td> </tr> </tbody> </table> <p><strong>关键结论</strong>：非连续 skip 的 SSD，KV 复用收益会大幅缩水，当 $r_\text{reuse} \ll r_\text{draft}$ 时，其性能模型接近标准 SD（$c = r_\text{draft}$），KV 复用带来的额外优势几乎消失。</p> <h3 id="44-和-sd-的对比">4.4 和 SD 的对比</h3> <p>并排放三个公式：</p> \[\text{Speedup}_\text{SD} = \frac{1 - \alpha^{\gamma+1}}{(1-\alpha)(\gamma c + 1)}\] \[\text{Speedup}_\text{SSD-contiguous} = \frac{1 - \alpha^{\gamma+1}}{(1-\alpha)[1 + (\gamma-1)r]}\] \[\text{Speedup}_\text{SSD-general} = \frac{1 - \alpha^{\gamma+1}}{(1-\alpha)[\gamma r_\text{draft} + (1 - r_\text{reuse})]}\] <p>Contiguous SSD 之所以分母更小（$1 + (\gamma-1)r$ vs $\gamma c + 1$，当 $c \approx r$ 时差了 $1 - r$ 项），正是 KV 复用的贡献。非连续 skip 一旦让 $r_\text{reuse}$ 接近 0，这个优势就彻底没了。</p> <hr/> <h2 id="5-把更多因素塞进来">5. 把更多因素塞进来</h2> <p>上面的 closed form 都基于几个理想化假设。现实里有哪些因素会让这个模型失准？</p> <h3 id="51-验证阶段的-overhead-不是常数">5.1 验证阶段的 overhead 不是常数</h3> <p>我们假设大模型验证 $\gamma$ 个 token 的时间 $= t_\text{big}$（1 个 token 的时间）。但实际上：</p> \[t_\text{verify}(\gamma) = t_\text{big} \cdot f(\gamma)\] <p>其中 $f(\gamma) \geq 1$，且随 $\gamma$ 增大。原因：</p> <ul> <li>KV cache 长了，attention 的 memory bandwidth 开销增加</li> <li>batch size = $\gamma$ 的 prefill 比 decode 慢（decode 是 memory-bound，prefill 是 compute-bound，两者 kernel 调度不同）</li> </ul> <p>一个简单的修正：</p> \[f(\gamma) \approx 1 + \epsilon \cdot (\gamma - 1)\] <p>其中 $\epsilon \ll 1$ 是单 token 增量开销。代入后分母变为：</p> \[\gamma c + f(\gamma) \approx \gamma c + 1 + \epsilon(\gamma-1) = (\gamma-1)(c + \epsilon) + c + 1\] <p>$\epsilon$ 通常很小（0.01-0.05 量级），但当 $\gamma$ 很大时不可忽略。</p> <h3 id="52-接受率-alpha-不是-iid-的">5.2 接受率 $\alpha$ 不是 i.i.d. 的</h3> <p>实际上第 $k$ 个 token 的接受率往往与前面的接受历史相关，而且不同位置的 $\alpha$ 本身不同。更精确的建模是：</p> \[\mathbb{E}[\#\text{tokens}] = 1 + \sum_{k=1}^{\gamma} \prod_{j=1}^{k} \alpha_j\] <p>当 $\alpha_j$ 单调递减（靠后位置的草稿越来越不准）时，最优 $\gamma^*$ 会比 i.i.d. 假设下更小。很多实验论文观察到接受率随位置衰减，这就是实际最优草稿长度往往不超过 8 的原因。</p> <h3 id="53-计算效率比-c-在-batch-推理时会变">5.3 计算效率比 $c$ 在 batch 推理时会变</h3> <p>单请求场景下，$c \approx t_\text{draft} / t_\text{big}$ 主要由参数量决定（memory bandwidth bound 时近似线性于参数量之比）。但在 batch serving 场景：</p> <ul> <li>大模型 batch size 增大时，吞吐趋向 compute-bound，$t_\text{big}$ 在 batch 变大时减慢更显著</li> <li>draft model 参数少，更容易在小 batch 下就达到 compute bound 上限</li> <li>结果：$c_\text{batch} &gt; c_\text{single}$，加速比下降</li> </ul> <p>这是 batch serving 场景下投机解码加速效果不如单请求的核心原因。</p> <h3 id="54-ssd-的层比例-r-和接受率-alpha-之间的-trade-off">5.4 SSD 的层比例 $r$ 和接受率 $\alpha$ 之间的 trade-off</h3> <p>SSD 里，$r_\text{draft}$ 越大（draft 用更多层），draft model 能力越强，$\alpha$ 也越高。但草稿成本也高了。这构成一个联合优化问题：</p> \[\text{Speedup}(r_\text{draft}, r_\text{reuse}, \gamma) = \frac{1 - \alpha(r_\text{draft})^{\gamma+1}}{(1-\alpha(r_\text{draft}))[\gamma r_\text{draft} + (1 - r_\text{reuse})]}\] <p>其中 $\alpha(r_\text{draft})$ 是 $r_\text{draft}$ 的单调递增函数，而 $r_\text{reuse}$ 由 skip 模式决定（见第 4 节）。最优 $(r^<em>, \gamma^</em>)$ 需要联合搜索，没有解析解，但 grid search 配合实测 $\alpha$ 可以很快找到。</p> <hr/> <h2 id="6-ssd-的内存优势能建模进加速比吗">6. SSD 的内存优势：能建模进加速比吗？</h2> <p>这个问题值得单独拎出来讨论，因为很多文章提到”SSD 省内存”但没有说清楚<strong>省出来的内存到底有没有加速价值</strong>。</p> <h3 id="61-省内存本身不直接加速单请求">6.1 省内存本身不直接加速单请求</h3> <p>先把这个坑踩清楚：在单请求场景下，SSD 省掉的 draft model 显存（大约 $r_\text{draft} \times M_\text{big}$，其中 $M_\text{big}$ 是大模型显存占用）对延迟<strong>没有直接帮助</strong>。省出来的显存闲着，加速比还是那个公式。</p> <h3 id="62-内存优势通过-batch-size-转化为-throughput-提升">6.2 内存优势通过 batch size 转化为 throughput 提升</h3> <p>真正的价值在 serving 场景，尤其是 <strong>KV cache 受限时</strong>。</p> <p>设 GPU 总显存为 $M_\text{total}$，模型权重占 $M_\text{weight}$，剩余显存全给 KV cache：</p> \[M_\text{kv} = M_\text{total} - M_\text{weight}\] <p>KV cache 的大小正比于 $B \times L_\text{seq} \times d_\text{kv} \times L$（batch size × 序列长度 × 头维度 × 层数），所以在相同硬件上能支撑的最大 batch size：</p> \[B_\text{max} \propto \frac{M_\text{kv}}{L_\text{seq} \cdot d_\text{kv} \cdot L}\] <p>对于 SD：需要额外维护 draft model 权重，$M_\text{weight}^\text{SD} = M_\text{big} + M_\text{draft}$</p> <p>对于 SSD：只有大模型一套权重，$M_\text{weight}^\text{SSD} = M_\text{big}$</p> <p>因此 SSD 能支撑的 batch size 更大：</p> \[\frac{B_\text{max}^\text{SSD}}{B_\text{max}^\text{SD}} = \frac{M_\text{total} - M_\text{big}}{M_\text{total} - M_\text{big} - M_\text{draft}}\] <p>这个比值在 $M_\text{draft}$ 较大时可以相当显著。举个例子：70B 大模型在 A100 80G 上量化后约占 35-40GB，如果 draft model 是 7B（约 3.5-4GB），释放这部分显存可以让 batch size 多出 ~10%。</p> <h3 id="63-throughput-建模">6.3 Throughput 建模</h3> <p>把 batch size 优势代入 throughput：</p> \[\text{Throughput} = \frac{B \times \mathbb{E}[\#\text{tokens per round}]}{T_\text{round}}\] <p>定义 $\beta = B_\text{max}^\text{SSD} / B_\text{max}^\text{SD} \geq 1$，SSD 相对 SD 的 throughput 比：</p> \[\frac{\text{Throughput}_\text{SSD}}{\text{Throughput}_\text{SD}} = \beta \cdot \frac{\text{Speedup}_\text{SSD}}{\text{Speedup}_\text{SD}}\] <p>当 $\beta &gt; 1$ 时（显存确实受限），SSD 的 throughput 优势 = 加速比的差距 + 显存带来的 batch 增益。</p> <p>但注意：<strong>batch 增大了，$\alpha$ 会下降</strong>（不同请求的分布不同，草稿的命中率降低），这是一个负反馈。实际 $\beta$ 的有效价值需要在特定负载下实测。</p> <h3 id="64-ssd-在企业部署中适用吗">6.4 SSD 在企业部署中适用吗？</h3> <p>直接给结论：<strong>适用场景很窄，但确实存在。</strong></p> <p><strong>SSD 相对 SD 有优势的场景</strong>：</p> <ol> <li><strong>显存极度受限</strong>：单机多卡塞不下大模型 + draft model，但 SSD 能省出 draft model 的权重开销，换来更大的 KV cache 空间或更高的并发</li> <li><strong>单请求低延迟优先</strong>（如代码补全、交互式对话）：此时 batch size = 1，SSD 的 KV 复用优势最大，$\beta = 1$，纯靠 Speedup 公式的分母优势</li> <li><strong>无合适 draft model</strong>：目标模型是定制 fine-tuned 版本，没有对应的小模型；SSD 不需要配对的 draft model，开箱即用</li> </ol> <p><strong>SSD 相对 SD 没有优势的场景</strong>：</p> <ol> <li><strong>大 batch 高吞吐 serving</strong>：$\alpha$ 随 batch 增大而下降，SSD 的 $r_\text{draft}$ 带来的接受率本就低于专用 draft model，大 batch 下差距更大</li> <li><strong>显存充裕</strong>：$\beta \approx 1$，内存优势消失，SSD 只剩 KV 复用这一点，而这要求 skip 模式是 contiguous prefix，限制了 $\alpha$ 的上限</li> <li><strong>对话历史长</strong>：长序列时 KV cache 本身就很大，verify 的 attention overhead $f(\gamma)$ 增大，SSD 节省的验证成本被其他 overhead 稀释</li> </ol> <p>一句话总结：<strong>SSD 是”没有好 draft model 时的替代方案”，而不是”有了 SD 之后还要叠加的优化”</strong>。企业部署如果有资源维护专用 draft model（比如 Llama-3-70B 配 Llama-3-8B），SD 的综合表现通常更好；反之，SSD 的零配置优势确实有价值。</p> <hr/> <h2 id="7-ssd-的-cuda-graph连续-skip-好做非连续-skip-怎么办">7. SSD 的 CUDA Graph：连续 skip 好做，非连续 skip 怎么办？</h2> <p>这一节纯讲工程，和上面的理论建模互补。如果你只关心公式推导可以跳过，但如果你要真的实现 SSD，这里是踩坑最密的地方。</p> <h3 id="71-cuda-graph-的本质约束">7.1 CUDA Graph 的本质约束</h3> <p>先说清楚 CUDA Graph（CG）是什么以及它为什么和 skip layer 有矛盾。</p> <p>CG 的工作方式：先”录制”一次完整的 kernel 执行序列（包括 kernel 类型、参数地址、依赖关系），生成一个固定的 DAG，之后每次推理直接 replay 这个 DAG，绕过 CPU 端的 kernel launch overhead。</p> <p>这带来了一个硬约束：<strong>CG 捕获的是拓扑固定的 kernel 图，不允许图在 replay 时动态增删节点。</strong></p> <p>skip layer 从计算图角度看就是”某些 transformer block 的 kernel 不执行”，一旦要运行时决定跳哪层，图的拓扑就在变——这和 CG 的设计正面冲突。</p> <h3 id="72-连续-skipcontiguous-prefix没有问题">7.2 连续 skip（contiguous prefix）：没有问题</h3> <p>对于原始 SSD（取前 $L_d$ 层做 draft），draft 路径和 verify 路径都是拓扑固定的：</p> <ul> <li>Draft 路径：执行层 $1 \to 2 \to \cdots \to L_d$，固定 $L_d$ 个 block</li> <li>Verify 路径：执行层 $1 \to 2 \to \cdots \to L$，固定 $L$ 个 block</li> </ul> <p>两条路径分别捕获成两个 CG，draft 阶段 replay draft-graph $\gamma$ 次，verify 阶段 replay verify-graph 1 次。整个生命周期里图的拓扑<strong>从未改变</strong>，CG 完全适用。</p> <p>这也是为什么 vLLM / SGLang 对标准投机解码的 CG 支持相对完善——两个模型各自一套图，没有特殊处理。</p> <h3 id="73-固定的非连续-skip同样没问题">7.3 固定的非连续 skip：同样没问题</h3> <p>如果 skip 模式是静态决定的（比如”永远跳第 4、8、12 层”），那虽然跳的不连续，但拓扑依然固定。捕获时只录制实际执行的 kernel，不录制被跳过的层，replay 就按这个固定子图走。</p> <p>唯一需要注意的是 <strong>residual connection 的处理</strong>：跳过第 $l$ 层时，第 $l+1$ 层的输入必须直接连到第 $l-1$ 层的输出（identity pass-through），这通常用一个 no-op copy kernel 或直接共享 tensor 指针来实现，不影响 CG 捕获。</p> <p>所以：<strong>连续 skip 和固定非连续 skip，CG 都没问题，本质上是同一类情况——图拓扑是静态的。</strong></p> <h3 id="74-动态-skip这才是真正的麻烦">7.4 动态 skip：这才是真正的麻烦</h3> <p>真正和 CG 冲突的是<strong>运行时自适应决定跳哪层</strong>，典型场景是基于 token confidence 的 early exit：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 伪代码：动态 early exit，每个 token 可能在不同层退出
</span><span class="k">for</span> <span class="n">layer_idx</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">L</span><span class="p">):</span>
    <span class="n">hidden</span> <span class="o">=</span> <span class="n">transformer_block</span><span class="p">[</span><span class="n">layer_idx</span><span class="p">](</span><span class="n">hidden</span><span class="p">)</span>
    <span class="k">if</span> <span class="nf">confidence</span><span class="p">(</span><span class="n">hidden</span><span class="p">)</span> <span class="o">&gt;</span> <span class="n">threshold</span><span class="p">:</span>  <span class="c1"># 运行时判断，拓扑在变！
</span>        <span class="k">break</span>  <span class="c1"># 提前退出
</span></code></pre></div></div> <p>每次前向退出的层数不同，图的拓扑每次都可能不一样，CG 无法直接捕获。</p> <h3 id="75-解法-1topology-preserving-no-op主流做法">7.5 解法 1：Topology-Preserving No-op（主流做法）</h3> <p>思路：<strong>不改变图的拓扑，改变数据流</strong>。</p> <p>把”跳过某层”改成”这层做一个 identity 操作”：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 改造后：拓扑固定，但通过 mask 控制层是否生效
</span><span class="k">for</span> <span class="n">layer_idx</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">L</span><span class="p">):</span>
    <span class="k">if</span> <span class="n">skip_mask</span><span class="p">[</span><span class="n">layer_idx</span><span class="p">]:</span>   <span class="c1"># skip_mask 是 GPU 上的 tensor，不是 Python bool
</span>        <span class="n">hidden</span> <span class="o">=</span> <span class="n">hidden</span>        <span class="c1"># identity，实现上是零乘或直接传递
</span>    <span class="k">else</span><span class="p">:</span>
        <span class="n">hidden</span> <span class="o">=</span> <span class="n">transformer_block</span><span class="p">[</span><span class="n">layer_idx</span><span class="p">](</span><span class="n">hidden</span><span class="p">)</span>
</code></pre></div></div> <p>关键是 <code class="language-plaintext highlighter-rouge">skip_mask</code> 必须是 <strong>GPU tensor</strong>，不能是 Python 控制流——Python <code class="language-plaintext highlighter-rouge">if</code> 会让 CG 捕获时走固定分支，replay 时永远走同一条路。</p> <p>具体实现方式有两种：</p> <p><strong>方式 A：乘以 0/1 mask（最简单但有额外计算）</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">output</span> <span class="o">=</span> <span class="n">transformer_block</span><span class="p">[</span><span class="n">layer_idx</span><span class="p">](</span><span class="n">hidden</span><span class="p">)</span>
<span class="n">hidden</span> <span class="o">=</span> <span class="n">hidden</span> <span class="o">+</span> <span class="n">skip_mask</span><span class="p">[</span><span class="n">layer_idx</span><span class="p">]</span> <span class="o">*</span> <span class="p">(</span><span class="n">output</span> <span class="o">-</span> <span class="n">hidden</span><span class="p">)</span>
<span class="c1"># skip_mask[i] = 0 → hidden 不变（跳过）
# skip_mask[i] = 1 → hidden = output（正常执行）
</span></code></pre></div></div> <p>坏处：即使跳过，transformer block 的 forward 还是跑了一遍，只是最后结果被 mask 掉，<strong>计算资源没有节省</strong>。</p> <p><strong>方式 B：cudaStreamWaitEvent + conditional copy（细粒度控制）</strong></p> <p>只 launch 需要执行的 kernel，但用 CUDA event 保持图的 DAG 结构完整，跳过的层用 memcpy no-op 保持依赖边。这种方式能真正节省被跳层的计算，但实现复杂，需要手动管理 event。</p> <p>vLLM 对类似场景（MoE 的 expert routing）的处理和方式 B 思路接近，但具体实现会根据 backend 有所不同。</p> <h3 id="76-解法-2multi-graph-预捕获适合枚举可控的场景">7.6 解法 2：Multi-Graph 预捕获（适合枚举可控的场景）</h3> <p>如果 skip 模式的数量有限（比如只有几种固定的 skip 配置），可以预先把所有可能的图都捕获好，运行时根据当前 skip 配置选对应的图 replay：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 离线预捕获
</span><span class="n">graphs</span> <span class="o">=</span> <span class="p">{}</span>
<span class="k">for</span> <span class="n">skip_config</span> <span class="ow">in</span> <span class="n">all_skip_configs</span><span class="p">:</span>
    <span class="n">graphs</span><span class="p">[</span><span class="n">skip_config</span><span class="p">]</span> <span class="o">=</span> <span class="nf">capture_cuda_graph</span><span class="p">(</span><span class="n">model</span><span class="p">,</span> <span class="n">skip_config</span><span class="p">)</span>

<span class="c1"># 在线 replay
</span><span class="n">current_config</span> <span class="o">=</span> <span class="nf">decide_skip_config</span><span class="p">(...)</span>  <span class="c1"># 可以是 CPU 逻辑
</span><span class="n">graphs</span><span class="p">[</span><span class="n">current_config</span><span class="p">].</span><span class="nf">replay</span><span class="p">()</span>
</code></pre></div></div> <p>这个方案的优势：每个图都只包含实际执行的 kernel，<strong>没有方式 A 的无效计算</strong>。</p> <p>限制：skip_config 的枚举空间不能太大。$L=32$ 层的模型如果每层都能独立跳，理论上有 $2^{32}$ 种配置，显然无法全部预捕获。但如果限制”只能在 4 个固定位置的层选一个退出”，配置数就是 4，完全可以。</p> <p>这也是为什么很多实际 SSD 实现会把 skip 模式约束成离散的几档（比如退出层只能是 $L/4, L/2, 3L/4, L$），既能做 CG，又给了 $r$ 一定的调节自由度。</p> <h3 id="77-解法-3persistent-cuda-graph--pointer-swap适合-verify-阶段的复用">7.7 解法 3：Persistent CUDA Graph + pointer swap（适合 verify 阶段的复用）</h3> <p>这个方法专门针对 SSD 的一个特殊需求：verify 阶段需要跑完整 $L$ 层，但前 $L_d$ 层的权重和 draft 阶段完全一样，能不能在 verify 时复用 draft 阶段的 CG 节点？</p> <p>答案是<strong>不能直接复用图节点</strong>，但可以通过 <strong>tensor pointer swap</strong> 实现 KV cache 的零拷贝共享：</p> <ol> <li>Draft CG 捕获时，KV cache 写入到一个预分配的 buffer（地址 $P_\text{kv}$）</li> <li>Verify CG 捕获时，前 $L_d$ 层的 KV cache 输入指针指向同一个 $P_\text{kv}$</li> <li>Replay 时：先 replay draft CG 写 KV，再 replay verify CG 读 KV——中间不需要任何数据搬运</li> </ol> <p><code class="language-plaintext highlighter-rouge">cudaGraphExecKernelNodeSetParams</code> 允许在不重新捕获的情况下更新 kernel 参数（包括指针），这是实现这个 trick 的 API 基础。vLLM 的 CG 实现里大量用到了这个 API 来在不同 batch size 下复用同一个图。</p> <h3 id="78-小结什么情况下-ssd-能用-cg">7.8 小结：什么情况下 SSD 能用 CG</h3> <table> <thead> <tr> <th>Skip 模式</th> <th>CG 可行性</th> <th>推荐方案</th> </tr> </thead> <tbody> <tr> <td>Contiguous prefix（固定 $L_d$）</td> <td>完全可行</td> <td>两张图分别捕获，verify 复用 KV</td> </tr> <tr> <td>固定非连续 skip</td> <td>完全可行</td> <td>单图捕获实际执行路径</td> </tr> <tr> <td>少量离散配置（如 4-8 档）</td> <td>可行，有开销</td> <td>Multi-graph 预捕获 + 运行时选图</td> </tr> <tr> <td>动态 early exit（基于 confidence）</td> <td>受限</td> <td>No-op mask（有冗余计算）或放弃 CG</td> </tr> <tr> <td>完全自由的运行时 skip</td> <td>不可行</td> <td>必须用 eager mode，接受 launch overhead</td> </tr> </tbody> </table> <p><strong>核心结论</strong>：SSD 想用 CG 的关键是<strong>提前固定 skip 模式</strong>。如果 skip 模式在离线阶段确定（无论连续还是非连续），CG 都没问题；如果要运行时动态决定，要么接受 no-op 的冗余计算，要么把配置空间限制到可预捕获的范围内。这也反过来约束了 SSD 里 skip 策略的设计空间——<strong>越灵活的 skip 策略，越难和 CG 兼容，工程上往往只能选有限档位的静态配置</strong>。</p> <h3 id="79-和-moe-的对比同是条件计算动态性的维度不一样">7.9 和 MoE 的对比：同是”条件计算”，动态性的维度不一样</h3> <p>这里值得单独展开，因为 MoE 模型同样有动态计算的问题，和 SSD skip 放在一起对比能把 CG 适配思路讲得更清楚。</p> <p><strong>MoE 是”宽度动态”，SSD skip 是”深度动态”</strong>：</p> <table> <thead> <tr> <th> </th> <th>MoE</th> <th>SSD dynamic skip</th> </tr> </thead> <tbody> <tr> <td>固定的是</td> <td>层数 $L$</td> <td>token 数</td> </tr> <tr> <td>变化的是</td> <td>每层激活哪些 expert、每个 expert 收多少 token</td> <td>执行哪些层</td> </tr> <tr> <td>动态性来源</td> <td>router 的 top-k 输出（数据依赖）</td> <td>confidence/early-exit 判断（数据依赖）</td> </tr> <tr> <td>图拓扑变化</td> <td>expert kernel 的 input size 在变，但 kernel 集合固定</td> <td>kernel 集合本身在变（某些层整个不执行）</td> </tr> </tbody> </table> <p>理解这个维度差异很重要，因为它决定了 CG 适配难度的本质差异。</p> <p><strong>MoE 的标准 CG 适配方案：padding to fixed shape</strong></p> <p>MoE 的动态在于每个 expert 收到的 token 数不固定。比如 8 个 expert、batch=64，理论上每个 expert 平均收 16 个 token，但实际可能是 5、30、0、21……直接按实际 token 数 launch kernel，shape 每次都变，CG 无法捕获。</p> <p>标准解法：<strong>把每个 expert 的输入 padding 到一个固定上限 $K_\text{max}$</strong>，保证所有 expert 的 kernel 每次都以相同 shape 执行。对应的 CG 拓扑完全固定，只需在 replay 前更新 token 分配的 mask tensor。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 伪代码：MoE CG 适配
# 捕获时：expert_inputs shape = [num_experts, K_max, hidden_dim]，固定
# replay 时：更新 routing_mask（GPU tensor），无需重新捕获
</span><span class="n">graph</span><span class="p">.</span><span class="nf">replay</span><span class="p">()</span>  <span class="c1"># kernel shape 不变，routing_mask 控制哪些 slot 有效
</span></code></pre></div></div> <p>padding 引入的开销：无效 token 的 expert forward 算了但不用（类似 SSD no-op mask）。但 MoE 的 router 通常做 load balancing，每个 expert 的负载比较均匀，padding 浪费的算力有限（通常 &lt; 5%）。vLLM 和 SGLang 对 MoE 的 CG 支持基本都走这条路。</p> <p><strong>SSD dynamic skip 的类比方案：layer no-op padding</strong></p> <p>用 MoE padding 的思路类比到 SSD：把”跳过某层”改成”这层做 no-op forward”，保持所有层的 kernel 都执行，用 GPU tensor mask 控制结果是否生效。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># SSD no-op mask 方案（类比 MoE padding）
</span><span class="k">for</span> <span class="n">layer_idx</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">L</span><span class="p">):</span>
    <span class="n">candidate</span> <span class="o">=</span> <span class="n">transformer_block</span><span class="p">[</span><span class="n">layer_idx</span><span class="p">](</span><span class="n">hidden</span><span class="p">)</span>
    <span class="n">hidden</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">where</span><span class="p">(</span><span class="n">skip_mask</span><span class="p">[</span><span class="n">layer_idx</span><span class="p">],</span> <span class="n">hidden</span><span class="p">,</span> <span class="n">candidate</span><span class="p">)</span>
    <span class="c1"># skip_mask 是 GPU tensor，replay 前更新，不重新捕获
</span></code></pre></div></div> <p>但这里有一个 MoE 没有的问题：<strong>MoE padding 浪费的是”部分 expert 的少量 slot”，而 SSD no-op 浪费的是”整个 transformer block 的 forward”</strong>。一个 transformer block 包含完整的 self-attention + FFN，相当于整层的计算全白做了。如果 SSD 设计的初衷是跳过 30-50% 的层（$r_\text{draft} = 0.5$-$0.7$），no-op 方案把跳层省下的计算全部还回去，<strong>加速效果清零</strong>。</p> <p>所以 SSD 的 no-op 方案只适合一种情况：<strong>你在乎的是吞吐而不是单请求延迟</strong>，需要 CG 带来的 launch overhead 节省（通常几十到几百 µs），而不在乎 GPU 算力的利用率。</p> <p><strong>MoE + SSD 叠加：双重动态</strong></p> <p>当大模型本身是 MoE 架构（比如 Mixtral、DeepSeek-MoE），然后在上面做 SSD，两种动态就叠在一起了：</p> <ul> <li>每层内部：expert routing 动态（宽度动态）</li> <li>跨层：skip 决策动态（深度动态）</li> </ul> <p>分别处理：</p> <ul> <li>宽度动态：每层内部继续用 MoE padding 方案，CG 可以捕获</li> <li>深度动态：如果 skip 是静态的（离线确定），直接从图里去掉被 skip 的层；如果是动态的，要么 no-op（但开销更大，因为被跳的是整个 MoE block），要么预捕获多档</li> </ul> <p>组合下来，<strong>MoE + 动态 SSD skip 的最实用方案</strong>通常是：</p> <ol> <li>把 SSD 的 skip 配置限制为 2-4 个静态档位（比如草稿用前 $L/4$、$L/2$、$3L/4$ 层三档）</li> <li>每档各预捕获一张 CG，每档内部用 MoE padding 处理 expert 的宽度动态</li> <li>档位切换在 CPU 端做（选哪张图 replay），不进入 CG 的捕获范围</li> </ol> <p>这样既保住了 CG 的 launch overhead 收益，又保住了 SSD skip 真正节省计算的优势（没有 no-op 冗余）。代价是 skip 策略的灵活性受限——但从上面第 4 节的分析可以知道，$r_\text{draft}$ 在几个离散档位之间搜索，本来也足够覆盖大部分的 Speedup 最优点。</p> <p><strong>一句话总结两者的差异</strong>：MoE 的动态是”图中每个节点的 input size 在变”，CG 用 padding 填平就行；SSD dynamic skip 的动态是”图中的节点集合在变”，padding 没法解决，只能要么固化节点集合（静态 skip），要么接受 no-op 的冗余计算，要么预捕获多图切换。工程取舍的核心永远是：<strong>动态性换来多少加速收益，值不值得为了 CG 兼容性付出对应的代价</strong>。</p> <h3 id="710-实际案例sparsespec-怎么做的">7.10 实际案例：SparseSpec 怎么做的</h3> <p>上面讨论的都是理论层面的方案，来看一个真实实现：<a href="https://github.com/sspec-project/SparseSpec">SparseSpec</a>，一个针对 RLM（推理大模型，即做 CoT 推理的那类）的批量推理加速框架，主打”稀疏自推测解码”（Sparse Self-Speculative Decoding），声称最高 2.3x 吞吐量提升。</p> <h4 id="sparsespec-的核心思路动态不是层的-skip而是-attention-的-kv-范围">SparseSpec 的核心思路：动态不是层的 skip，而是 attention 的 KV 范围</h4> <p>SparseSpec 的 SSD 变体和传统 skip layer 有一个本质区别：<strong>它 skip 的不是 transformer block，而是 attention 的 KV 访问范围</strong>。</p> <p>具体做法叫 PillarAttn：</p> <ol> <li><strong>验证阶段</strong>：跑完整 attention，同时把每个 token 的 attention score 转储到全局内存（<code class="language-plaintext highlighter-rouge">dump_logits</code>）</li> <li><strong>Top-K 筛选</strong>：从这批 attention score 里找出对当前 query 贡献最大的 Top-K 个 KV 位置，作为下一轮草稿的”重要 token”集合</li> <li><strong>草稿阶段</strong>：不访问完整 KV cache，只访问这 Top-K 个稀疏位置</li> </ol> <p>这样 skip 的是”大量不重要的 KV”，而不是”整个 transformer 层”。skip 的粒度细得多，也不会破坏激活路径的一致性（每层都还是完整执行的）。</p> <h4 id="cuda-graph-适配同一张图处理三种请求类型">CUDA Graph 适配：同一张图处理三种请求类型</h4> <p>SparseSpec 的 CG 方案是本文讨论思路里最干净的实现之一——<strong>不改变图的拓扑，把所有动态性压进 GPU tensor，通过 <code class="language-plaintext highlighter-rouge">request_type</code> 在一张图里同时处理 NORMAL / DRAFT / VERIFY 三种请求</strong>。</p> <p>核心在 attention kernel 的 InputTransform（<code class="language-plaintext highlighter-rouge">serve/attention/backend.py</code>）：</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// CUDA JIT attention kernel 内部</span>
<span class="k">if</span> <span class="p">(</span><span class="n">request_type</span> <span class="o">==</span> <span class="mi">1</span><span class="p">)</span> <span class="p">{</span>   <span class="c1">// DRAFT：用稀疏索引</span>
    <span class="n">indices</span> <span class="o">=</span> <span class="n">params</span><span class="p">.</span><span class="n">flatten_indices</span> <span class="o">+</span> <span class="n">kv_head_idx</span> <span class="o">*</span> <span class="n">MAX_TOTAL_DRAFT_KV_LEN</span><span class="p">;</span>
<span class="p">}</span> <span class="k">else</span> <span class="p">{</span>                   <span class="c1">// NORMAL / VERIFY：用完整索引</span>
    <span class="n">indices</span> <span class="o">=</span> <span class="n">params</span><span class="p">.</span><span class="n">kv_indices</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">request_type</code> 是一个 GPU tensor，每次 replay 前按实际请求更新，图拓扑本身不动。同一张图，草稿请求走稀疏 KV 路径，验证请求走完整 KV 路径，混在一个 batch 里一起跑。</p> <p>验证阶段的 attention score 转储也类似（<code class="language-plaintext highlighter-rouge">LogitsTransform</code>）：</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="p">(</span><span class="n">request_type</span> <span class="o">==</span> <span class="mi">2</span><span class="p">)</span> <span class="p">{</span>   <span class="c1">// VERIFY 才写分数</span>
    <span class="n">params</span><span class="p">.</span><span class="n">dump_logits</span><span class="p">[</span><span class="n">offset</span> <span class="o">+</span> <span class="n">kv_idx</span><span class="p">]</span> <span class="o">=</span> <span class="n">__float2half</span><span class="p">(</span><span class="n">logits</span> <span class="o">/</span> <span class="n">packed_qo_len</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <p>只有 VERIFY 请求的 kernel 执行会写入 <code class="language-plaintext highlighter-rouge">dump_logits</code>，DRAFT 请求执行时写入目标不同，结果被自然隔离。</p> <h4 id="双缓冲-cg消除-cpu-准备和-gpu-执行之间的同步气泡">双缓冲 CG：消除 CPU 准备和 GPU 执行之间的同步气泡</h4> <p>SparseSpec 的另一个工程细节值得记一下：<strong>双缓冲 CUDA Graph</strong>（<code class="language-plaintext highlighter-rouge">serve/model/model.py</code> 的 <code class="language-plaintext highlighter-rouge">KVPool</code> 类）。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">KVPool</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="p">...):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">phase_idx</span> <span class="o">=</span> <span class="mi">0</span>
        <span class="n">self</span><span class="p">.</span><span class="n">_wrappers</span> <span class="o">=</span> <span class="p">[]</span>           <span class="c1"># 两份 wrapper
</span>        <span class="n">self</span><span class="p">.</span><span class="n">_attn_fwd_metadata</span> <span class="o">=</span> <span class="p">[]</span>  <span class="c1"># 两份 metadata
</span>    
    <span class="k">def</span> <span class="nf">step</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">phase_idx</span> <span class="o">^=</span> <span class="mi">1</span>           <span class="c1"># 0 ↔ 1 交替
</span></code></pre></div></div> <p>标准 CG 的流程是：CPU 准备元数据 → GPU replay 图 → 等 GPU 完成 → CPU 准备下一批 → …，中间有等待的 bubble。双缓冲的做法：两张预捕获的图交替使用，第 0 张在 GPU 上 replay 时，CPU 已经在准备第 1 张图的输入参数，GPU 执行完第 0 张立刻 replay 第 1 张，不需要等待。</p> <p>捕获时也预捕获了一系列 batch size 的图（余弦分桶：1, 2, 4, 8, 16, 32, 64, 128, 256…），每个 batch size 下捕获两张（双缓冲各一张）：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">eager_cuda_graph_mode</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">max_batch_size</span><span class="o">=</span><span class="mi">256</span><span class="p">,</span> <span class="n">num_cuda_graphs</span><span class="o">=</span><span class="mi">16</span><span class="p">):</span>
    <span class="n">captured_args</span> <span class="o">=</span> <span class="p">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="mi">8</span><span class="p">,</span> <span class="mi">16</span><span class="p">,</span> <span class="mi">32</span><span class="p">,</span> <span class="mi">64</span><span class="p">,</span> <span class="mi">128</span><span class="p">,</span> <span class="mi">256</span><span class="p">,</span> <span class="mi">384</span><span class="p">,</span> <span class="mi">512</span><span class="p">,</span> <span class="mi">640</span><span class="p">]</span>
    <span class="k">for</span> <span class="n">batch_size</span> <span class="ow">in</span> <span class="n">captured_args</span><span class="p">:</span>
        <span class="k">for</span> <span class="n">phase</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">2</span><span class="p">):</span>      <span class="c1"># 双缓冲
</span>            <span class="n">runner</span> <span class="o">=</span> <span class="nf">_CUDAGraphModelRunner</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">model</span><span class="p">)</span>
            <span class="n">runner</span><span class="p">.</span><span class="nf">capture</span><span class="p">(</span><span class="n">batch_size</span><span class="o">=</span><span class="n">batch_size</span><span class="p">,</span> <span class="p">...)</span>
            <span class="n">self</span><span class="p">.</span><span class="n">graph_runners</span><span class="p">[</span><span class="n">batch_size</span><span class="p">].</span><span class="nf">append</span><span class="p">(</span><span class="n">runner</span><span class="p">)</span>
</code></pre></div></div> <p>实际 forward 时，按当前 batch size 选最近的已捕获档位（允许 padding 到上一档），再按当前 <code class="language-plaintext highlighter-rouge">phase</code> 选双缓冲里的哪一张：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="o">**</span><span class="n">kwargs</span><span class="p">):</span>
    <span class="n">batch_size</span> <span class="o">=</span> <span class="n">kwargs</span><span class="p">[</span><span class="sh">"</span><span class="s">token_ids</span><span class="sh">"</span><span class="p">].</span><span class="nf">size</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>
    <span class="n">idx</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">searchsorted</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">captured_bsz</span><span class="p">,</span> <span class="n">batch_size</span><span class="p">,</span> <span class="n">side</span><span class="o">=</span><span class="sh">"</span><span class="s">left</span><span class="sh">"</span><span class="p">)</span>
    <span class="c1"># 在 padding 比例允许范围内走 CG，否则 eager fallback
</span>    <span class="n">model_executor</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">graph_runners</span><span class="p">[</span><span class="n">padded_bsz</span><span class="p">][</span><span class="n">self</span><span class="p">.</span><span class="n">kv_cache</span><span class="p">.</span><span class="n">phase</span><span class="p">]</span>
    <span class="k">return</span> <span class="nf">model_executor</span><span class="p">(</span><span class="o">**</span><span class="n">kwargs</span><span class="p">)</span>
</code></pre></div></div> <h4 id="sparsespec-和本文讨论方案的对应关系">SparseSpec 和本文讨论方案的对应关系</h4> <p>回头看 7.5-7.8 节讨论的几个方案，SparseSpec 实际走的是<strong>方案 1（topology-preserving）的精细版</strong>：</p> <ul> <li><strong>不是 layer-level 的 no-op</strong>，而是 <strong>attention KV-range 的条件访问</strong>——代价比整层 no-op 小得多，被跳过的只是 attention 里不重要的 KV 读取，transformer block 本身的 MLP、归一化等还是完整执行</li> <li><strong>同一张图支持三种请求类型</strong>，不需要 multi-graph 切换，靠 <code class="language-plaintext highlighter-rouge">request_type</code> tensor 在运行时路由</li> <li><strong>双缓冲消除同步气泡</strong>，这是在 7.7 节 pointer swap 思路之外的另一个工程优化维度</li> </ul> <p>这个设计也解释了为什么 SparseSpec 不需要显式处理 skip layer 的 KV 复用问题（第 4.2 节）：它的稀疏性在 attention 的 KV 索引层面而非 transformer 层层面，$r_\text{reuse}$ 的分析框架对它并不直接适用——它的”跳过”不改变激活路径，所有层的 hidden state 仍然是 full-path 计算出来的。</p> <table> <thead> <tr> <th> </th> <th>本文理论方案</th> <th>SparseSpec 实现</th> </tr> </thead> <tbody> <tr> <td>动态性来源</td> <td>transformer layer 级别的 skip</td> <td>attention KV range 的稀疏访问</td> </tr> <tr> <td>CG 适配思路</td> <td>no-op mask / multi-graph</td> <td>单图 + <code class="language-plaintext highlighter-rouge">request_type</code> tensor 路由</td> </tr> <tr> <td>冗余计算</td> <td>整层 no-op（代价大）</td> <td>仅 attention 的冗余 KV 访问（代价小）</td> </tr> <tr> <td>KV 复用</td> <td>依赖 contiguous prefix</td> <td>不适用（层不 skip，复用问题不存在）</td> </tr> <tr> <td>batch 内混合</td> <td>需要多图切换</td> <td>天然支持（三种类型共享一张图）</td> </tr> </tbody> </table> <hr/> <h2 id="8-最优草稿长度的-closed-form-近似">8. 最优草稿长度的 closed-form 近似</h2> <p>对于标准 SD 公式，对 $\gamma$ 求导：</p> \[\frac{\partial}{\partial \gamma}\text{Speedup} = \frac{(1-\alpha)[-\alpha^{\gamma+1}\ln\alpha(\gamma c + 1) - c(1-\alpha^{\gamma+1})]}{(1-\alpha)^2(\gamma c+1)^2}\] <p>令分子中括号 = 0：</p> \[-\alpha^{\gamma+1}\ln\alpha(\gamma c + 1) = c(1-\alpha^{\gamma+1})\] <p>这个方程没有解析解。但若 $\alpha^{\gamma+1} \ll 1$（即 $\gamma$ 足够大使接受率衰减显著）时，右边 $\approx c$，左边变成 $-\alpha^{\gamma+1}\ln\alpha \cdot \gamma c$（近似 $\gamma^* c \gg 1$），化简得：</p> \[\alpha^{\gamma+1} \approx \frac{-1}{\gamma^* \ln\alpha}\] <table> <tbody> <tr> <td>因为 $\ln\alpha &lt; 0$（$\alpha &lt; 1$），令 $</td> <td>\ln\alpha</td> <td>= -\ln\alpha$：</td> </tr> </tbody> </table> \[\gamma^* \approx \frac{1}{|\ln\alpha|} \cdot \frac{1}{\alpha^{\gamma^*+1}}\] <table> <tbody> <tr> <td>这是个隐式方程，但可以用一阶近似：令右边的 $\gamma^*$ 用初始猜测 $\gamma_0 = \frac{1}{</td> <td>\ln\alpha</td> <td>}$ 代入，迭代一次：</td> </tr> </tbody> </table> \[\gamma^* \approx \frac{1}{|\ln\alpha|} \cdot e^{(\gamma_0+1)|\ln\alpha|} = \frac{e^{(\frac{1}{|\ln\alpha|}+1)|\ln\alpha|}}{|\ln\alpha|}\] <p>这个近似在 $\alpha$ 接近 1 时比较准，但说实话不够优雅。工程上直接 grid search 更实用。</p> <hr/> <h2 id="8-数值校验两个极端场景">8. 数值校验：两个极端场景</h2> <h3 id="场景-ac--005-alpha--09-gamma--8">场景 A：$c = 0.05, \alpha = 0.9, \gamma = 8$</h3> \[\text{Speedup} = \frac{1 - 0.9^9}{(1-0.9)(8 \times 0.05 + 1)} = \frac{1 - 0.387}{0.1 \times 1.4} = \frac{0.613}{0.14} \approx 4.4\] <p>和图里 $\alpha=0.90, \gamma=8$ 那条线的值（约 4.5）吻合，没问题。</p> <h3 id="场景-bc--00005-alpha--095-gamma--64">场景 B：$c = 0.0005, \alpha = 0.95, \gamma = 64$</h3> \[\text{Speedup} = \frac{1 - 0.95^{65}}{(1-0.95)(64 \times 0.0005 + 1)} = \frac{1 - 0.0356}{0.05 \times 1.032} = \frac{0.964}{0.0516} \approx 18.7\] <p>图里显示约 16-17，稍有偏差，说明在 $\gamma = 64$ 时验证 overhead $f(\gamma) &gt; 1$ 的影响已经不可忽略了，实际比理想值低一点，这是预期内的。</p> <hr/> <h2 id="9-一张对比表总结">9. 一张对比表总结</h2> <table> <thead> <tr> <th>因素</th> <th>SD</th> <th>SSD-Contiguous</th> <th>SSD-Skip</th> <th> </th> <th> </th> </tr> </thead> <tbody> <tr> <td>草稿成本比</td> <td>$c = t_\text{draft}/t_\text{big}$</td> <td>$r_\text{draft} = L_d/L$</td> <td>$r_\text{draft} =</td> <td>\mathcal{A}</td> <td>/L$</td> </tr> <tr> <td>KV 可复用比例</td> <td>0（两套模型）</td> <td>$r_\text{reuse} = r_\text{draft}$</td> <td>$r_\text{reuse} = (s_1-1)/L \leq r_\text{draft}$</td> <td> </td> <td> </td> </tr> <tr> <td>Speedup 分母</td> <td>$\gamma c + 1$</td> <td>$1 + (\gamma-1)r$</td> <td>$\gamma r_\text{draft} + (1-r_\text{reuse})$</td> <td> </td> <td> </td> </tr> <tr> <td>接受率 $\alpha$</td> <td>依赖 draft model 质量</td> <td>依赖前缀层能力，偏低</td> <td>依赖 skip 模式，更低</td> <td> </td> <td> </td> </tr> <tr> <td>内存占用</td> <td>两套权重</td> <td>一套权重</td> <td>一套权重</td> <td> </td> <td> </td> </tr> <tr> <td>Batch 并发优势</td> <td>无（内存较高）</td> <td>有（显存受限时）</td> <td>有（显存受限时）</td> <td> </td> <td> </td> </tr> <tr> <td>企业部署适用性</td> <td>有专用 draft model 时最优</td> <td>显存受限 / 无 draft model</td> <td>通常不如 Contiguous</td> <td> </td> <td> </td> </tr> </tbody> </table> <hr/> <h2 id="10-结语">10. 结语</h2> <p>把所有 closed form 并排放一起：</p> \[\text{Speedup}_\text{SD} = \frac{1 - \alpha^{\gamma+1}}{(1-\alpha)(\gamma c + 1)}\] \[\text{Speedup}_\text{SSD-contiguous} = \frac{1 - \alpha^{\gamma+1}}{(1-\alpha)[1 + (\gamma-1)r]}\] \[\text{Speedup}_\text{SSD-general} = \frac{1 - \alpha^{\gamma+1}}{(1-\alpha)[\gamma r_\text{draft} + (1 - r_\text{reuse})]}\] \[\text{Throughput}_\text{SSD} = \beta \cdot \text{Speedup}_\text{SSD} \cdot \text{Throughput}_\text{baseline}, \quad \beta = \frac{M_\text{total} - M_\text{big}}{M_\text{total} - M_\text{big} - M_\text{draft}}\] <p>这几个公式说清楚了几件事：</p> <p><strong>第一，KV 复用的前提是激活路径一致</strong>。非连续 skip 一旦让 $r_\text{reuse} \ll r_\text{draft}$，SSD 的 KV 复用优势彻底消失，退化成和 SD 等价的形式。所以很多 skip-layer SSD 的实现里，”KV cache 可以复用”这个说法要打问号，得看 skip 的是哪些层。</p> <p><strong>第二，内存优势不是”直接加速”，是”换 batch size”</strong>。SSD 省出来的 draft model 显存，在显存受限时能换成更高的并发量，通过 $\beta &gt; 1$ 提升 throughput。但 $\alpha$ 会随 batch size 增大而下降，这个收益不是线性的，需要实测。</p> <p><strong>第三，SSD 是”没有 draft model 时的 fallback”，不是”叠在 SD 上的加分项”</strong>。如果资源允许维护专用 draft model，SD + 好 draft model 的 $\alpha$ 优势几乎必然超过 SSD 的 KV 复用优势。SSD 真正发光的场景是：显存极度受限、或者目标模型是自定义 fine-tune 版本、找不到合适的 draft model。</p> <p>真正有意思的后续问题是：SSD 的 skip 层怎么搜？随机跳、等间隔跳、还是用 NAS 方法学出来的 skip 模式？不同 skip 模式下 $r_\text{draft}$ 和 $r_\text{reuse}$ 的差距有多大？这些直接影响 SSD 的实际加速，感觉可以专门写一篇。欢迎评论区交流。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 LLM 推理效率和加速方法你想系统深入，可以看看我之前出版的<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，书里有专章讲 LLM 推理效率优化，和本文讨论的理论框架有直接关联。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="推理优化"/><category term="投机解码"/><category term="理论分析"/><summary type="html"><![CDATA[投机解码到底能快多少？从理论建模到 closed-form 的完整推导]]></summary></entry><entry><title type="html">推理模型做 decode，GPU 只有 3% 在干活——SparseSpec 把稀疏 Attention 变成了 2.1× 加速</title><link href="https://marsggbo.github.io/blog/2026/20260707-vllm-v1/" rel="alternate" type="text/html" title="推理模型做 decode，GPU 只有 3% 在干活——SparseSpec 把稀疏 Attention 变成了 2.1× 加速"/><published>2026-07-07T00:00:00+00:00</published><updated>2026-07-07T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260707-vllm-v1</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260707-vllm-v1/"><![CDATA[<h1 id="推理模型做-decodegpu-只有-3-在干活sparsespec-把稀疏-attention-变成了-21-加速">推理模型做 decode，GPU 只有 3% 在干活——SparseSpec 把稀疏 Attention 变成了 2.1× 加速</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2512.01278">Accelerating Large-Scale Reasoning Model Inference with Sparse Self-Speculative Decoding</a></p> </blockquote> <hr/> <p>一张 H100，跑 Qwen3-8B 做推理解码，attention 阶段的 GPU <strong>计算利用率只有 3%</strong>。</p> <p>内存带宽跑满了 85%，计算单元闲着 97%。</p> <p><img src="/assets/img/posts/20260707-vllm-v1/002_Figure_2.png" alt="Qwen3-8B decode 阶段的计算和内存带宽利用率"/></p> <p>这张图是整篇论文的核心前提。在这个基础上，才能理解为什么”减少 FLOPs”这个直觉在推理模型场景下完全打不中要害，也才能理解 SparseSpec 的设计逻辑。</p> <hr/> <h2 id="1-推理模型和普通-llm-的-decode-不是一回事">1. 推理模型和普通 LLM 的 decode 不是一回事</h2> <p>普通 LLM 生成一段对话，200 个 token，KV cache 很小，attention 很快。推理模型不一样——DeepSeek-R1、Qwen3 这类模型，做一道数学题要先想几千步，CoT 动辄 3000-8000 个 token，KV cache 巨大。</p> <p>KV cache 越大，每个 decode step 要搬运的数据就越多。而 GPU 的计算峰值（FLOPs）和内存带宽（GB/s）是两个独立的天花板——一旦 decode 落入 memory-bandwidth bound 的区域，堆计算完全没用，瓶颈在搬数据的速度。</p> <p>这就是上面那张图的意思：97% 的计算白白浪费了，GPU 的时间全花在等 KV cache 从显存搬过来。</p> <p>现有的加速方案有两大类：一是 KV cache 压缩（量化、逐出、稀疏 attention），二是推测解码（用小模型快速生成草稿，大模型并行验证）。这篇论文选了第二条路，但做法有本质区别。</p> <hr/> <h2 id="2-推理模型的-attention-天然是稀疏的">2. 推理模型的 Attention 天然是稀疏的</h2> <p>做稀疏 attention 的前提是：大部分 attention score 接近零，只有少数”关键 token”真正重要。这个直觉在推理模型上被验证得特别彻底。</p> <p><img src="/assets/img/posts/20260707-vllm-v1/004_Figure_4.png" alt="Qwen3-8B 在 AIME 数学推理任务上的 attention score 热力图"/></p> <p>Qwen3-8B 在 AIME 数学题上的 attention 热力图——矩阵的绝大部分是接近零的蓝色，只有极少数位置（红色）有高分值。两个不同的 head 都是这个模式。</p> <p>问题是：稀疏 attention 和推测解码怎么结合？</p> <hr/> <h2 id="3-之前的方案为什么没做到理论加速上限">3. 之前的方案为什么没做到理论加速上限</h2> <p>先看一个有意思的对比图：</p> <p><img src="/assets/img/posts/20260707-vllm-v1/003_Figure_3.png" alt="理论加速上限 vs. 现有方法实际加速"/></p> <p>横轴是 acceptance rate，纵轴是 speedup。蓝线是理论上限——如果 acceptance rate 是 0.6，理论上应该能拿到接近 2.0× 的加速。</p> <p>但实际上：</p> <ul> <li><strong>MagicDec</strong>（稀疏 attention + 标准推测解码）：acceptance rate 0.6，只拿到 <strong>1.47×</strong>，比理论低了 36%</li> <li><strong>Oracle Top-K Attn</strong>（用 oracle 完美稀疏 attention）：acceptance rate 0.88，也只有 <strong>2.39×</strong>，低于理论峰值</li> </ul> <p>差距从哪来？<strong>批处理</strong>。</p> <p>标准推测解码为单请求设计：草稿阶段串行生成 d 个 token，验证阶段一次 batch 前向。单请求效率很高。但在实际推理服务里，同时有几十上百个请求，草稿和验证不得不交替执行——草稿阶段只处理部分请求，GPU 利用率低；验证阶段又切换回来。两边都低效，加速折扣大。</p> <p>SparseSpec 要解的核心问题就是：<strong>在大 batch 在线服务场景下，让稀疏 self-speculative decoding 做到接近理论的加速比</strong>。</p> <hr/> <h2 id="4-sparsespec-的三个设计">4. SparseSpec 的三个设计</h2> <h3 id="41-pillarattn用验证阶段的结果指导草稿阶段的稀疏-pattern">4.1 PillarAttn：用验证阶段的结果指导草稿阶段的稀疏 pattern</h3> <p>Self-speculative decoding 的 draft 阶段和 verify 阶段用同一套参数——draft 跑稀疏 attention，verify 跑完整 attention。但草稿阶段该忽略哪些 token？</p> <p>SparseSpec 的做法是：<strong>在验证阶段识别出关键 token，在下一轮草稿阶段只 attend 这些 token</strong>。</p> <p><img src="/assets/img/posts/20260707-vllm-v1/007_Figure_7.png" alt="PillarAttention 的工作原理"/></p> <p>每行是一个 decode step，红色格子是被选出的关键 token（critical tokens），蓝色是 attention 权重高的位置，灰色是跳过的位置。验证阶段（底部高亮框）做完整 attention，同时记录哪些 token 的 attention score 高；下一轮草稿阶段（Drafting 框）就只看这些关键 token，跳过其余大部分。</p> <p>名字叫 “PillarAttn” 是因为关键 token 在 attention 矩阵里形成了一列列”柱子”的形状。</p> <h3 id="42-unified-batching草稿和验证混进同一个-batch">4.2 Unified Batching：草稿和验证混进同一个 batch</h3> <p>解决批处理效率问题的关键设计。</p> <p>传统做法是”所有请求先全部草稿，再全部验证”，这样草稿和验证交替执行，GPU 在切换时利用率骤降。SparseSpec 把草稿和验证请求混进同一个 batch，同时处理：</p> <p><img src="/assets/img/posts/20260707-vllm-v1/006_Figure_6.png" alt="不同请求在同一 batch 里混合执行草稿和验证"/></p> <p>Unified Batch Scheduler 负责分配每个请求在这一轮做草稿还是验证，然后整批一起推理。对模型来说就是一次正常的 batch forward——有些请求用稀疏 attention（draft），有些用完整 attention（verify），结构统一。</p> <p>附带的一个收益是 GEMM 效率。推测解码的一个经典问题是：草稿阶段 batch size 小，GEMM 算子远未打满 GPU——下图左边是 Naive 方案的 GEMM batch size，剧烈抖动；右边是 Unified Batching 之后，batch size 稳定在 GPU 饱和点附近。</p> <p><img src="/assets/img/posts/20260707-vllm-v1/014_Figure_14.png" alt="Naive vs. Unified Batching 的 GEMM batch size 对比"/></p> <h3 id="43-异步-cpu-gpu-执行把验证的-cpu-开销藏起来">4.3 异步 CPU-GPU 执行：把验证的 CPU 开销藏起来</h3> <p>Self-speculative decoding 的验证阶段有一步 CPU 计算：对比 draft token 和 verify token 的概率分布，决定接受还是拒绝。在同步执行模式下，这步 CPU 计算会卡住 GPU：</p> <p><img src="/assets/img/posts/20260707-vllm-v1/009_Figure_9.png" alt="同步 vs. 异步 CPU-GPU 执行时间线"/></p> <p>(a) 同步模式：每轮验证完要等 CPU 跑完接受/拒绝逻辑，GPU 空转。(b) 异步模式：把 CPU 计算延迟一步，和下一轮 GPU 执行重叠，GPU 不再空转。</p> <p>代价是”延迟验证”——接受/拒绝的结果晚一步才能用，相当于有一轮 draft 可能白跑。但实验数据表明这个代价远小于消除 GPU idle 的收益。</p> <hr/> <h2 id="5-数字说话">5. 数字说话</h2> <h3 id="端到端吞吐量">端到端吞吐量</h3> <p>在 Qwen3 系列（1.7B/8B/14B）的三个推理 benchmark（AIME、LiveCodeBench、OlympiadBench）上对比 vLLM、vLLM-ngram、MagicDec、TriForce：</p> <p><img src="/assets/img/posts/20260707-vllm-v1/010_Figure_10.png" alt="端到端吞吐量对比（vLLM/ngram/MagicDec/TriForce vs. SparseSpec）"/></p> <p>SparseSpec 全面领先，对 vLLM baseline 的加速比在不同配置下：</p> <ul> <li>Qwen3-1.7B AIME：<strong>2.13×</strong></li> <li>Qwen3-8B AIME：<strong>1.79×</strong></li> <li>Qwen3-14B AIME：<strong>1.51×</strong>（模型越大，TP degree 越高，通信 overhead 越重，加速有所压缩）</li> </ul> <h3 id="不需要额外-draft-模型还能超过-eagle-3">不需要额外 draft 模型，还能超过 EAGLE-3</h3> <p>EAGLE-3 是目前最强的 draft-model 推测解码方案之一，需要专门 fine-tune 一个草稿模型，参数量通常是目标模型的 1/7 左右，还要占额外显存。</p> <p>SparseSpec 用同一套模型参数，不需要 fine-tune 任何额外模型：</p> <p><img src="/assets/img/posts/20260707-vllm-v1/011_Figure_11.png" alt="SparseSpec vs. EAGLE-3 吞吐量对比"/></p> <p>三个模型上，SparseSpec 都略高于或持平 EAGLE-3，最大优势在 Qwen3-1.7B（4632 vs 4043 tokens/s）。</p> <p>为什么同一个模型的稀疏版本能接受这么多 token？对比接受 token 数：</p> <p><img src="/assets/img/posts/20260707-vllm-v1/012_Figure_12.png" alt="各方法每轮接受 token 数对比"/></p> <p>Eagle-3 = 1.1，N-gram = 1.6，StreamingLLM = 4.8，<strong>SparseSpec = 6.1</strong>。</p> <p>原因在于：稀疏 attention 保留了完整的模型语义，draft 分布和 verify 分布非常接近，接受率自然高。相比之下，独立 draft 模型在长 CoT 场景下分布偏差更大，在 batched setting 里接受率反而低。</p> <h3 id="消融实验每个设计贡献多少">消融实验：每个设计贡献多少</h3> <p><img src="/assets/img/posts/20260707-vllm-v1/013_Figure_13.png" alt="各组件贡献的消融实验"/></p> <p>从 Naive Spec 到最终 SparseSpec：</p> <ul> <li>+Unified Batch：1.23×</li> <li>+KV Cache Offload：1.61×（内存管理优化，让更多请求能同时服务）</li> <li>+Async CPU（最终方案）：4722 tokens/s，<strong>2.21×</strong> over Naive Spec</li> </ul> <p>每个设计都有实质贡献，不是凑数的。</p> <hr/> <h2 id="6-一点个人-take">6. 一点个人 take</h2> <p>这篇论文让我觉得有意思的地方在于它的出发点——<strong>从系统瓶颈倒推算法设计</strong>，而不是从算法的优雅性出发。GPU 利用率只有 3% 这个观察是可测量的，”所以要减少 attention 的数据搬运”是直接推出来的结论，不是先有稀疏 attention 再找理由。</p> <p>PillarAttn 的设计也体现了这个思路：不是离线算好一个静态的稀疏 pattern（那需要 profiling），而是用 verify 阶段已经算过的 attention score 来动态指导下一轮 draft——信息复用，零额外开销。</p> <p>有几个局限值得说：</p> <p><strong>一是依赖 attention 天然稀疏</strong>。推理模型（Qwen3、DeepSeek-R1 这类）的长 CoT 场景，这个假设成立很好。但在短输出场景（比如简单对话、分类任务），attention 未必足够稀疏，稀疏 draft 的接受率可能大幅下降，整套机制就没那么有吸引力了。</p> <p><strong>二是系统工程复杂度</strong>。Unified Batching + Async CPU + Dynamic KV Cache 三个模块组合，和标准 vLLM 架构差异很大，维护成本不低。论文是在自己的框架上实现的，想集成进 vLLM 主线还有不少工作量。</p> <p><strong>三是加速比随模型变大而缩小</strong>（从 2.13× 降到 1.51×）。原因是大模型需要更多 GPU 做 tensor parallel，TP 通信本身就成了新瓶颈，稀疏 attention 省下来的时间被通信抵消了一部分。这是 TP + speculative decoding 的通用矛盾，不是 SparseSpec 特有的问题，但确实限制了在超大模型上的收益空间。</p> <p>推理模型是 2025 年之后 LLM 推理优化最有意思的方向之一——长 CoT 的特殊性质（稀疏 attention、memory-bandwidth bound、对延迟不那么敏感）和普通对话模型差异很大，很多老结论都要重新验证。这篇论文把这个特殊性利用得比较彻底。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 LLM 推理效率优化和稀疏推理你想系统深入，可以看看我之前出版的<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，书里有专章讲 LLM 推理效率（量化、蒸馏、推理框架），和本文的工程优化思路有直接关联。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="推理加速"/><category term="推测解码"/><category term="Self-Speculative Decoding"/><category term="稀疏 Attention"/><category term="论文解读"/><summary type="html"><![CDATA[推理模型做 decode，GPU 只有 3% 在干活——SparseSpec 把稀疏 Attention 变成了 2.1× 加速]]></summary></entry><entry><title type="html">160 个 AI 比 80 个效果更差？这篇论文量化了一件大家隐约知道但说不清楚的事</title><link href="https://marsggbo.github.io/blog/2026/20260706-llm-position-paper/" rel="alternate" type="text/html" title="160 个 AI 比 80 个效果更差？这篇论文量化了一件大家隐约知道但说不清楚的事"/><published>2026-07-06T00:00:00+00:00</published><updated>2026-07-06T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260706-llm-position-paper</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260706-llm-position-paper/"><![CDATA[<h1 id="160-个-ai-比-80-个效果更差这篇论文量化了一件大家隐约知道但说不清楚的事">160 个 AI 比 80 个效果更差？这篇论文量化了一件大家隐约知道但说不清楚的事</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2605.19915">LLM Agents Make Collective Belief Dynamics Programmable: Challenges and Research Directions</a> Javier Kvitko, Petter Törnberg et al.（2025）</p> </blockquote> <hr/> <p>“AI 会影响舆论”——这句话大家都会说，但说完也就说完了，说不出更多。</p> <p>这篇文章让我觉得有意思的地方不是它提出了这个担忧，而是<strong>它把这件事变成了一个可以做实验、可以调参数的系统</strong>，然后发现了一堆反直觉的结果。</p> <p>最让我意外的是这个：<strong>往社区里注入 160 个 AI agent，比注入 80 个效果反而更差。</strong> 多了一倍，结果回弹了。</p> <p>为什么？作者的解释是，太多明显异质的声音会触发人类 agent 某种对抗反应。这事放到现实里也说得通——一个人在评论区发表不同意见，可能让你动摇；一百个人同时说同样的话，你第一反应是”这是水军”。但一个阈值在哪、为什么在那、不同人群有没有差异……这篇文章没有答案，只是观察到了这个现象。</p> <hr/> <h2 id="影响资本主义话题的效果是女权的七倍">影响资本主义话题的效果是女权的七倍</h2> <p>实验设置是：用真实社交媒体数据初始化一批”人类 agent”，注入 80 个持相反立场的”AI agent”，让它们混着讨论 50 步，看人类立场怎么变。四个话题：堕胎、资本主义、女权、脱欧。</p> <p>结果如下：</p> <p><img src="/assets/img/posts/20260706-llm-position-paper/table2_p3.png" alt="80 个反对派 AI 注入后，各话题立场变化"/></p> <p>资本主义话题的支持比例从 89.4% 跌到 58.8%，<strong>跌了 30 个百分点</strong>。女权话题，支持只降了 4 个百分点，基本没动。</p> <p>同样的实验，效果差了七八倍。</p> <p>作者说这是因为不同议题对人的”身份锚定程度”不同——女权更接近身份认同，所以更稳固。这个解释我觉得对了一半。另一半可能是：资本主义作为一个政治经济议题，本身就是一个”可以被论据影响”的领域，人们对它的态度更有弹性；而女权相关的立场背后连着大量个人经历，不是几轮讨论能动摇的。</p> <p>这个差异比”AI 有没有影响力”本身更值得研究——<strong>人类的信念系统不是均匀的，有些地方有铠甲，有些地方没有</strong>，理解这个分布才是真正重要的问题。</p> <hr/> <h2 id="ai-做的不是说服是瓦解惯性">AI 做的不是”说服”，是”瓦解惯性”</h2> <p>文章还分析了每一步的立场转变概率，这张图比最终结果更能说明机制：</p> <p><img src="/assets/img/posts/20260706-llm-position-paper/figure1_p4.png" alt="有无 AI 介入时的立场转变概率矩阵"/></p> <p>还是资本主义。原本持支持立场的人，下一步继续支持的概率从 0.866 降到了 0.685——<strong>不是被扭转到反对，而是先变得不那么确定了</strong>。</p> <p>这和”说服”不是一回事。说服是拿出更好的论据让你改变主意，你清楚地知道发生了什么。这里的机制更像是：持续的异质声音让你对自己的原有判断产生了不确定性，然后在这个不确定性里慢慢漂移。没有明显的转折点，很难察觉，也很难主动抵抗。</p> <p>这个发现让我想到另一件事：做 RLHF 的人都知道，preference data 里有个问题叫”annotation fatigue”，标注者标久了会开始随机选或向多数倾斜。这里的机制是不是类似——不是逻辑上被说服，而是判断的稳定性被磨损了？不一定对，但方向值得想。</p> <hr/> <h2 id="几个调参实验的观察">几个调参实验的观察</h2> <p>文章还测了发帖频率、介入时机、说服风格：</p> <p><img src="/assets/img/posts/20260706-llm-position-paper/figure2_p5.png" alt="AI 介入参数对信念动态的影响"/></p> <p><strong>频率</strong>：每步发一次效果最强，每 4 步一次效果已经明显减弱。持续比间歇有效得多。</p> <p><strong>时机</strong>：早期介入后撤出，效果不会完全消退，但幅度有限。早期影响有一定锁定效应，但没有持续介入那么强。</p> <p><strong>风格</strong>：悲悯型 vs 道德谴责型，效果差不多。说明内容方向比表达风格重要，怎么说没有说什么重要。</p> <hr/> <h2 id="实验的局限必须说">实验的局限（必须说）</h2> <p>最大的问题：<strong>“人类 agent”是 LLM 扮演的，不是真人。</strong> 这个实验严格意义上测的是”LLM agent 能不能影响另一个 LLM 的输出”，外推到真实人类需要大量额外工作。真实的人有信源判断、情绪、社交压力……都没有被建模进去。</p> <p>另外 50 步太短，四个话题太窄，这些都是 position paper 的局限——提出问题框架，不给最终答案。</p> <hr/> <h2 id="最后说一句">最后说一句</h2> <p>做 LLM 系统的人平时很少看这个方向的文章，觉得离技术太远。但有一个连接值得注意：<strong>推理成本还在降，agent 大规模部署的门槛也跟着降</strong>。这篇文章里 80 个 agent 是个实验规模，再往后不是。</p> <p>这篇文章的贡献不在结论，在于<strong>把一个以前只能说”可能有风险”的问题，变成了一个可以测量、可以调参、可以迭代研究的对象</strong>。这本身是一种进展。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 LLM 推理和 agent 系统话题你想深入，可以看看<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，书里有专章讲 LLM 推理效率，和 agent 大规模部署的底层成本问题有直接关联。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="AI Agent"/><category term="社会影响"/><category term="论文解读"/><summary type="html"><![CDATA[160 个 AI 比 80 个效果更差？这篇论文量化了一件大家隐约知道但说不清楚的事]]></summary></entry><entry><title type="html">vllm v1 源码精读（一）：为什么要重写，以及 LLM() 这行代码背后发生了什么</title><link href="https://marsggbo.github.io/blog/2026/20260706-vllm-v1-01-arch/" rel="alternate" type="text/html" title="vllm v1 源码精读（一）：为什么要重写，以及 LLM() 这行代码背后发生了什么"/><published>2026-07-06T00:00:00+00:00</published><updated>2026-07-06T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260706-vllm-v1-01-arch</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260706-vllm-v1-01-arch/"><![CDATA[<h1 id="vllm-v1-源码精读一为什么要重写以及-llm-这行代码背后发生了什么">vllm v1 源码精读（一）：为什么要重写，以及 LLM() 这行代码背后发生了什么</h1> <blockquote> <p><strong>本文基于 vllm <code class="language-plaintext highlighter-rouge">ba22152</code>（2026-07-06），源码链接均指向该 commit 的固定行号。</strong> 代码浏览入口：<a href="https://github.com/vllm-project/vllm/tree/ba22152096b2484faa3579624a253d54804d876d">github.com/vllm-project/vllm/tree/ba22152</a></p> </blockquote> <hr/> <h2 id="1-接口没变但内部几乎全换了为什么">1. 接口没变，但内部几乎全换了——为什么？</h2> <p>如果你最近更新了 vllm，会发现同样这两行代码：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">llm</span> <span class="o">=</span> <span class="nc">LLM</span><span class="p">(</span><span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">facebook/opt-125m</span><span class="sh">"</span><span class="p">)</span>
<span class="n">outputs</span> <span class="o">=</span> <span class="n">llm</span><span class="p">.</span><span class="nf">generate</span><span class="p">(</span><span class="n">prompts</span><span class="p">,</span> <span class="n">sampling_params</span><span class="p">)</span>
</code></pre></div></div> <p>行为看起来和以前没什么两样，但打开源码就会发现，<code class="language-plaintext highlighter-rouge">vllm/engine/llm_engine.py</code> 这个原来的核心文件，现在只剩<strong>一行</strong>：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">LLMEngine</span> <span class="o">=</span> <span class="n">V1LLMEngine</span>
</code></pre></div></div> <p>整个引擎几乎被完整替换掉了。做这么大规模的重构，肯定不是为了重构而重构——v0 一定有什么本质问题，撑不住了。</p> <p>所以在看新代码之前，先搞清楚 v0 到底出了什么问题，是理解这次重写的前提。</p> <hr/> <h2 id="2-v0-的三个问题">2. v0 的三个问题</h2> <p><strong>问题一：调度和 GPU forward 串行，GPU 在等 CPU</strong></p> <p>v0 的每个推理步骤，大致是这样跑的：Python 做调度决策 → tokenization → KV 块分配 → 打包数据 → GPU forward → 回收结果。这一串全在同一个 Python 进程里顺序执行。由于 Python GIL 的存在，CPU 侧的调度逻辑和 GPU forward 无法真正并行，GPU 经常在等 Python 干完那堆”杂活”才能开始计算。</p> <p><strong>问题二：prefill 和 decode 阶段割裂</strong></p> <p>v0 把 prefill 和 decode 当成两个独立的 batch 来跑：先把所有等待中的请求 prefill 一遍，再切换到 decode 阶段。这两个阶段的计算特性完全不同——prefill 是 compute-bound（算力瓶颈），decode 是 memory-bound（带宽瓶颈）——来回切换导致 GPU 利用率不稳定，而且 decode 中的请求还会被 prefill 阻塞，TTFT（Time to First Token，首个 token 的延迟）受到明显影响。</p> <p><strong>问题三：每步都传完整请求对象</strong></p> <p>每次调度都要把完整的 request 对象序列化传给 worker，包括整个 token 序列、所有 sampling 参数等，开销随请求变长而线性增大。</p> <p>有了这三个问题，v1 的设计思路就很清晰了：</p> <ol> <li><strong>EngineCore 独立进程 + ZMQ 通信</strong>：把调度器和 GPU worker 放到独立后台进程，前端只做 tokenization 和 detokenization，两边真正并行。ZMQ（ZeroMQ）是一个高性能消息队列库，专门用于进程间高效传递数据，延迟极低。</li> <li><strong>统一 prefill/decode 调度</strong>：用 <code class="language-plaintext highlighter-rouge">num_computed_tokens</code> 跟踪每个请求的进度，prefill 和 decode 同框处理，chunked prefill 自然支持</li> <li><strong>差量传输</strong>：第一次调度发完整数据，后续步骤只发 diff（新分配的块 id + 新 token）</li> </ol> <p>解决思路明确了，下一步是看 v1 到底怎么组织这套新架构。</p> <hr/> <h2 id="3-整体架构三层分工">3. 整体架构：三层分工</h2> <p>在看任何代码之前，先把 v1 的架构全貌建立起来。</p> <p>下图是 vllm v1 的核心对象持有关系，用 <code class="language-plaintext highlighter-rouge">self.xxx = ...</code> 的方式呈现各模块之间怎么连接：</p> <p><img src="/assets/img/posts/20260706-vllm-v1-01-arch/object_relations.png" alt="vllm v1 核心对象持有关系"/></p> <p>图里有<strong>两条关键边界线</strong>，理解这两条线就理解了 v1 的分层逻辑：</p> <p><strong>边界一：<code class="language-plaintext highlighter-rouge">self.engine_core = EngineCoreClient</code>（<code class="language-plaintext highlighter-rouge">core_client.py:83</code>）——前后端分界</strong></p> <p><code class="language-plaintext highlighter-rouge">LLMEngine</code> 之上是<strong>前端</strong>（IO 密集型：tokenize 字符串、管理请求生命周期、detokenize 输出）；<code class="language-plaintext highlighter-rouge">EngineCore</code> 之下是<strong>后端</strong>（CPU/GPU 密集型：调度、GPU forward、采样）。</p> <p>这条边界的设计依据不是 HTTP server，而是 <strong>Python GIL</strong>。前端是 asyncio 驱动的，后端是紧密的调度循环，两者放在同一进程里会互相争 GIL，谁都跑不快。分到独立进程，才能真正并行。</p> <p><code class="language-plaintext highlighter-rouge">EngineCoreClient</code> 是这条边界的透明代理——同进程时直接调用（<code class="language-plaintext highlighter-rouge">InprocClient</code>），跨进程时通过 ZMQ 传消息（<code class="language-plaintext highlighter-rouge">AsyncMPClient</code>），上层代码感知不到区别。</p> <p><strong>边界二：<code class="language-plaintext highlighter-rouge">self.executor = Executor</code>（<code class="language-plaintext highlighter-rouge">executor/abstract.py:48</code>）——单机/多机分界</strong></p> <p><code class="language-plaintext highlighter-rouge">EngineCore</code> 只调 <code class="language-plaintext highlighter-rouge">executor.execute_model()</code>，不感知底层是 1 卡还是 8 卡还是 Ray 集群，具体实现由 <code class="language-plaintext highlighter-rouge">Executor.get_class()</code> 在启动时按配置选好。</p> <p>有了这两条边界，整个系统自然分成三层：</p> <table> <thead> <tr> <th>层</th> <th>模块</th> <th>职责</th> </tr> </thead> <tbody> <tr> <td>前端（IO）</td> <td><code class="language-plaintext highlighter-rouge">LLM</code> / <code class="language-plaintext highlighter-rouge">LLMEngine</code> / <code class="language-plaintext highlighter-rouge">InputProcessor</code> / <code class="language-plaintext highlighter-rouge">OutputProcessor</code></td> <td>tokenize、管理请求生命周期、detokenize</td> </tr> <tr> <td>后端调度</td> <td><code class="language-plaintext highlighter-rouge">EngineCore</code> / <code class="language-plaintext highlighter-rouge">Scheduler</code> / <code class="language-plaintext highlighter-rouge">KVCacheManager</code></td> <td>调度决策、KV Cache 分配</td> </tr> <tr> <td>GPU 执行</td> <td><code class="language-plaintext highlighter-rouge">Executor</code> / <code class="language-plaintext highlighter-rouge">Worker</code> / <code class="language-plaintext highlighter-rouge">GPUModelRunner</code></td> <td>model.forward()、采样</td> </tr> </tbody> </table> <hr/> <h2 id="4-目录结构代码在哪里">4. 目录结构：代码在哪里</h2> <p>有了架构全貌，再看目录结构，就知道每个文件对应哪一层了。</p> <p>顶层目录：</p> <p><img src="/assets/img/posts/20260706-vllm-v1-01-arch/dir_tree_01_toplevel.png" alt="vllm 顶层目录结构"/></p> <p>用户入口层（前端）：</p> <p><img src="/assets/img/posts/20260706-vllm-v1-01-arch/dir_tree_02_entrypoints.png" alt="vllm/entrypoints/ 用户入口层"/></p> <p>v1 引擎层（前端 + 后端调度）：</p> <p><img src="/assets/img/posts/20260706-vllm-v1-01-arch/dir_tree_03_v1_engine.png" alt="vllm/v1/engine/ 前端引擎层"/></p> <p>调度 + KV Cache（后端调度）：</p> <p><img src="/assets/img/posts/20260706-vllm-v1-01-arch/dir_tree_04_v1_core.png" alt="vllm/v1/core/ 调度与KV Cache层"/></p> <p>执行层（GPU 执行）：</p> <p><img src="/assets/img/posts/20260706-vllm-v1-01-arch/dir_tree_05_v1_executor_worker.png" alt="vllm/v1/executor/ + worker/ 执行层"/></p> <p><strong>一个关键认知：<code class="language-plaintext highlighter-rouge">vllm/v1/</code> 才是真正的引擎</strong>，<code class="language-plaintext highlighter-rouge">vllm/engine/</code> 只是个 alias 壳（只有一行 <code class="language-plaintext highlighter-rouge">LLMEngine = V1LLMEngine</code>）。遇到 bug 想找源码，直接去 <code class="language-plaintext highlighter-rouge">vllm/v1/</code> 里找。</p> <hr/> <h2 id="5-一次-generate-的数据流">5. 一次 generate() 的数据流</h2> <p>架构和目录都清楚了，再看一次完整的推理过程就很好理解了。</p> <p><code class="language-plaintext highlighter-rouge">llm.generate(["给我讲个笑话"])</code> 从字符串进去、字符串出来，中间经过 5 步：</p> <table> <thead> <tr> <th>步骤</th> <th>做了什么</th> <th>由谁负责（对应上面哪一层）</th> </tr> </thead> <tbody> <tr> <td>① Tokenize</td> <td>字符串 → token id 列表</td> <td><code class="language-plaintext highlighter-rouge">InputProcessor</code>（前端）</td> </tr> <tr> <td>② Schedule</td> <td>决定这步处理哪些请求、分配 KV Cache 块</td> <td><code class="language-plaintext highlighter-rouge">Scheduler</code> + <code class="language-plaintext highlighter-rouge">KVCacheManager</code>（后端调度）</td> </tr> <tr> <td>③ Execute</td> <td>GPU 跑 <code class="language-plaintext highlighter-rouge">model.forward()</code>，得到 logits</td> <td><code class="language-plaintext highlighter-rouge">Executor</code> → <code class="language-plaintext highlighter-rouge">Worker</code> → <code class="language-plaintext highlighter-rouge">GPUModelRunner</code>（GPU 执行）</td> </tr> <tr> <td>④ Sample</td> <td>从 logits 采样出下一个 token id</td> <td><code class="language-plaintext highlighter-rouge">Sampler</code>（GPU 执行）</td> </tr> <tr> <td>⑤ Detokenize</td> <td>token id 增量还原成字符串</td> <td><code class="language-plaintext highlighter-rouge">IncrementalDetokenizer</code>（前端）</td> </tr> </tbody> </table> <p>生成是一个循环：每轮走完 ②③④⑤ 产出一个新 token，直到 EOS 或达到最大长度。步骤 ① 只在第一次执行，后续 decode 每轮只有那一个新 token 需要送进 ③。</p> <p>有了这个全景，下面跟着 <code class="language-plaintext highlighter-rouge">LLM.__init__()</code> 的调用链走一遍，看这些模块是怎么一步步初始化就绪的。</p> <hr/> <h2 id="6-llm__init__-全链路">6. <code class="language-plaintext highlighter-rouge">LLM.__init__()</code> 全链路</h2> <p>现在跟着 <code class="language-plaintext highlighter-rouge">__init__</code> 的调用顺序，一层一层走下去。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">llm</span> <span class="o">=</span> <span class="nc">LLM</span><span class="p">(</span><span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">facebook/opt-125m</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <p><img src="/assets/img/posts/20260706-vllm-v1-01-arch/seq_init.png" alt="LLM() 初始化完整时序"/></p> <p>沿着时序图从上往下走一遍（每个泳道对应一个模块，虚线是跨进程边界）：</p> <p><strong>① LLM → LLMEngine</strong>：<code class="language-plaintext highlighter-rouge">LLM.__init__()</code> 把所有参数打包成 <code class="language-plaintext highlighter-rouge">VllmConfig</code>，创建 <code class="language-plaintext highlighter-rouge">LLMEngine</code>。<code class="language-plaintext highlighter-rouge">InputProcessor</code> 和 <code class="language-plaintext highlighter-rouge">OutputProcessor</code> 在这一步就绪，负责前端的 tokenize / detokenize。</p> <p><strong>② LLMEngine → EngineCoreClient</strong>：<code class="language-plaintext highlighter-rouge">make_client()</code> 根据部署场景选择 <code class="language-plaintext highlighter-rouge">InprocClient</code>（离线推理）或 <code class="language-plaintext highlighter-rouge">AsyncMPClient</code>（HTTP serving）。这是前后端的分界线——之后 <code class="language-plaintext highlighter-rouge">LLMEngine</code> 只通过 <code class="language-plaintext highlighter-rouge">EngineCoreClient</code> 和后端通信。</p> <p><strong>③ EngineCoreClient → EngineCore</strong>：EngineCore 进入初始化（后台进程模式时，此步跨进程边界）。</p> <p><strong>④ Executor 选型</strong>：<code class="language-plaintext highlighter-rouge">Executor.get_class()</code> 按 <code class="language-plaintext highlighter-rouge">world_size</code> 和 Ray 配置选好具体实现（UniProc / Multiproc / RayDistributed），<code class="language-plaintext highlighter-rouge">EngineCore</code> 之后只调 <code class="language-plaintext highlighter-rouge">executor.xxx()</code>，不感知底层差异。</p> <p><strong>⑤ collective_rpc(“init_worker”)</strong>：广播到所有 Worker 进程，触发 CUDA context 初始化和 NCCL 通信初始化。多 GPU 时所有 rank 同时执行，主进程等全部完成才继续。</p> <p><strong>⑥ collective_rpc(“load_model”)</strong>：权重加载进 GPU，<code class="language-plaintext highlighter-rouge">GPUModelRunner.load_model()</code> 执行，<code class="language-plaintext highlighter-rouge">self.model</code>（FlashAttention 版本的 <code class="language-plaintext highlighter-rouge">nn.Module</code>）就绪。</p> <p><strong>⑦ _initialize_kv_caches()</strong>：跑 profiling dummy forward 量一下峰值显存，算出可用块数，分配 KV Cache tensor，<code class="language-plaintext highlighter-rouge">KVCacheManager</code> 和 <code class="language-plaintext highlighter-rouge">Scheduler</code> 就绪。</p> <p><strong>⑧ 就绪信号逐层回传</strong>：EngineCore ready → EngineCoreClient ready → LLMEngine ready → LLM 对象就绪，<code class="language-plaintext highlighter-rouge">llm.generate()</code> 可以被调用。</p> <h3 id="61-llminit把参数打包创建-llmengine">6.1 LLM.<strong>init</strong>()：把参数打包，创建 LLMEngine</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># LLM.__init__()  —  vllm/entrypoints/llm.py:176
</span><span class="k">class</span> <span class="nc">LLM</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">model</span><span class="p">,</span> <span class="n">tensor_parallel_size</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span> <span class="p">...):</span>
        <span class="n">engine_args</span> <span class="o">=</span> <span class="nc">EngineArgs</span><span class="p">(</span><span class="n">model</span><span class="o">=</span><span class="n">model</span><span class="p">,</span> <span class="n">tensor_parallel_size</span><span class="o">=</span><span class="n">tp_size</span><span class="p">,</span> <span class="p">...)</span>
        <span class="n">vllm_config</span> <span class="o">=</span> <span class="n">engine_args</span><span class="p">.</span><span class="nf">create_engine_config</span><span class="p">()</span>  <span class="c1"># 所有配置归一为一个对象
</span>        <span class="n">self</span><span class="p">.</span><span class="n">llm_engine</span> <span class="o">=</span> <span class="nc">LLMEngine</span><span class="p">(</span><span class="n">vllm_config</span><span class="p">)</span>          <span class="c1"># ← 进入下一层
</span></code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">VllmConfig</code> 是后续所有模块共享的单一配置入口：</p> <table> <thead> <tr> <th>子配置</th> <th>包含什么</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">ModelConfig</code></td> <td>模型路径、dtype、max_model_len</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">CacheConfig</code></td> <td>block_size、gpu_memory_utilization</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">ParallelConfig</code></td> <td>tp_size、pp_size、<strong>worker_cls（字符串，见下）</strong></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">SchedulerConfig</code></td> <td>max_num_seqs、max_num_batched_tokens、enable_chunked_prefill</td> </tr> </tbody> </table> <blockquote> <p><strong>为什么 worker_cls 是字符串？</strong></p> <p><code class="language-plaintext highlighter-rouge">ParallelConfig.worker_cls = "vllm.v1.worker.gpu_worker.Worker"</code>，由 <code class="language-plaintext highlighter-rouge">vllm/platforms/cuda.py:307</code> 注入。不直接存类引用，是因为 <code class="language-plaintext highlighter-rouge">import gpu_worker</code> 会触发 CUDA context 初始化——而主进程不应该持有 CUDA context，那是 worker 子进程的事。字符串延迟到子进程里通过 <code class="language-plaintext highlighter-rouge">resolve_obj_by_qualname()</code> 动态 import，做到零副作用。不同平台只需注入不同字符串，上层代码完全不感知。</p> </blockquote> <h3 id="62-llmengineinit前端三件套--前后端分界">6.2 LLMEngine.<strong>init</strong>()：前端三件套 + 前后端分界</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># LLMEngine.__init__()  —  vllm/v1/engine/llm_engine.py
</span><span class="k">class</span> <span class="nc">LLMEngine</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">vllm_config</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">input_processor</span>  <span class="o">=</span> <span class="nc">InputProcessor</span><span class="p">(</span><span class="n">vllm_config</span><span class="p">)</span>   <span class="c1"># tokenize 请求
</span>        <span class="n">self</span><span class="p">.</span><span class="n">output_processor</span> <span class="o">=</span> <span class="nc">OutputProcessor</span><span class="p">(</span><span class="n">vllm_config</span><span class="p">)</span>  <span class="c1"># 组装输出（含 detokenize）
</span>        <span class="n">self</span><span class="p">.</span><span class="n">engine_core</span> <span class="o">=</span> <span class="n">EngineCoreClient</span><span class="p">.</span><span class="nf">make_client</span><span class="p">(</span>       <span class="c1"># ← 前后端分界线
</span>            <span class="n">vllm_config</span><span class="p">,</span> <span class="n">asyncio_mode</span><span class="o">=</span><span class="bp">False</span><span class="p">,</span> <span class="bp">...</span>
        <span class="p">)</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">make_client()</code> 根据部署场景选择三种实现：</p> <table> <thead> <tr> <th>实现类</th> <th>场景</th> <th>通信方式</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">InprocClient</code>（<code class="language-plaintext highlighter-rouge">:276</code>）</td> <td>离线推理（<code class="language-plaintext highlighter-rouge">LLM</code> 类）</td> <td>直接调用 EngineCore 方法，零序列化</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">SyncMPClient</code>（<code class="language-plaintext highlighter-rouge">:779</code>）</td> <td>多进程离线推理</td> <td>ZMQ + msgpack，同步</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">AsyncMPClient</code>（<code class="language-plaintext highlighter-rouge">:950</code>）</td> <td>HTTP serving（<code class="language-plaintext highlighter-rouge">AsyncLLM</code>）</td> <td>ZMQ + asyncio，后台独立进程</td> </tr> </tbody> </table> <p><strong>为什么 HTTP server 必须用独立进程？</strong> Python GIL 让同一进程里的 asyncio 事件循环和 EngineCore 调度循环互相争 CPU 时间。分到独立进程，两边才能真正并行——EngineCore 在跑 GPU forward 的同时，HTTP server 可以同步接收新请求。</p> <p><strong><code class="language-plaintext highlighter-rouge">EngineCoreClient</code> 不只是初始化时的路由，而是前端对后端的唯一通信界面，贯穿整个推理生命周期。</strong> <code class="language-plaintext highlighter-rouge">LLMEngine</code> 的所有代码永远只调 <code class="language-plaintext highlighter-rouge">self.engine_core.xxx()</code>，不直接接触 <code class="language-plaintext highlighter-rouge">EngineCore</code>：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 前端代码——无论底层走哪种通信，写法完全一样
</span><span class="n">self</span><span class="p">.</span><span class="n">engine_core</span><span class="p">.</span><span class="nf">add_request</span><span class="p">(</span><span class="n">request</span><span class="p">)</span>       <span class="c1"># 送入新请求
</span><span class="n">self</span><span class="p">.</span><span class="n">engine_core</span><span class="p">.</span><span class="nf">get_output</span><span class="p">()</span>               <span class="c1"># 取回这步的输出
</span><span class="n">self</span><span class="p">.</span><span class="n">engine_core</span><span class="p">.</span><span class="nf">abort_requests</span><span class="p">(</span><span class="n">req_ids</span><span class="p">)</span>    <span class="c1"># 中止请求
</span><span class="n">self</span><span class="p">.</span><span class="n">engine_core</span><span class="p">.</span><span class="nf">reset_prefix_cache</span><span class="p">()</span>       <span class="c1"># 清空前缀缓存
</span><span class="n">self</span><span class="p">.</span><span class="n">engine_core</span><span class="p">.</span><span class="nf">collective_rpc</span><span class="p">(</span><span class="sh">"</span><span class="s">method</span><span class="sh">"</span><span class="p">)</span>   <span class="c1"># 广播 RPC 给所有 Worker
</span><span class="n">self</span><span class="p">.</span><span class="n">engine_core</span><span class="p">.</span><span class="nf">profile</span><span class="p">(</span><span class="n">is_start</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>     <span class="c1"># 开关 GPU profiler
</span></code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">InprocClient</code> 就是简单的直接转发（<code class="language-plaintext highlighter-rouge">core_client.py:297</code>）；<code class="language-plaintext highlighter-rouge">SyncMPClient</code>/<code class="language-plaintext highlighter-rouge">AsyncMPClient</code> 的同一个方法变成 ZMQ 消息——接口完全一样，前端感知不到差异。</p> <h3 id="63-enginecoreinit后端四步初始化">6.3 EngineCore.<strong>init</strong>()：后端四步初始化</h3> <p><img src="/assets/img/posts/20260706-vllm-v1-01-arch/diagram_enginecore_init.png" alt="EngineCore 初始化四步"/></p> <p>图里四个方块从左到右顺序执行，每步完成后才进入下一步：</p> <p><strong>步骤一（选 Executor）</strong>：<code class="language-plaintext highlighter-rouge">Executor.get_class()</code> 按 <code class="language-plaintext highlighter-rouge">world_size</code> 和是否用 Ray 选好实现类。选好之后 <code class="language-plaintext highlighter-rouge">EngineCore</code> 就和具体实现解耦了——后续全部通过 <code class="language-plaintext highlighter-rouge">self.executor.xxx()</code> 调用。</p> <p><strong>步骤二（初始化 Worker）</strong>：<code class="language-plaintext highlighter-rouge">collective_rpc("init_worker")</code> 广播，Worker 进程（或线程）启动，做 CUDA context 初始化和 NCCL 通信握手。多卡时所有 rank 并行初始化，主进程在这步可能阻塞几秒。</p> <p><strong>步骤三（加载模型 + KV Cache）</strong>：<code class="language-plaintext highlighter-rouge">collective_rpc("load_model")</code> 把权重加载进 GPU；随后 <code class="language-plaintext highlighter-rouge">_initialize_kv_caches()</code> 跑 profiling dummy forward 量峰值显存，算出可用 KV Cache 块数，分配 tensor。这一步是启动最慢的地方，大模型 + 大显存可能要十几秒。</p> <p><strong>步骤四（调度器上线）</strong>：<code class="language-plaintext highlighter-rouge">KVCacheManager</code> 和 <code class="language-plaintext highlighter-rouge">Scheduler</code> 就绪，开始等待请求进来。到这步 <code class="language-plaintext highlighter-rouge">EngineCore</code> 才算真正”活了”。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># EngineCore.__init__()  —  vllm/v1/engine/core.py:99
</span><span class="k">class</span> <span class="nc">EngineCore</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">vllm_config</span><span class="p">):</span>
        <span class="c1"># 步骤一：根据配置选择 Executor，初始化 Worker
</span>        <span class="n">executor_class</span> <span class="o">=</span> <span class="n">Executor</span><span class="p">.</span><span class="nf">get_class</span><span class="p">(</span><span class="n">vllm_config</span><span class="p">)</span>   <span class="c1"># abstract.py:48
</span>        <span class="c1">#   world_size==1  → UniProcExecutor   (uniproc_executor.py:45)
</span>        <span class="c1">#   world_size&gt;1   → MultiprocExecutor (multiproc_executor.py:103)
</span>        <span class="c1">#   ray 集群       → RayDistributedExecutor (ray_executor.py:64)
</span>        <span class="n">self</span><span class="p">.</span><span class="n">executor</span> <span class="o">=</span> <span class="nf">executor_class</span><span class="p">(</span><span class="n">vllm_config</span><span class="p">)</span>

        <span class="c1"># 步骤二：广播 RPC 给所有 Worker，初始化 CUDA + 加载模型权重
</span>        <span class="n">self</span><span class="p">.</span><span class="n">executor</span><span class="p">.</span><span class="nf">collective_rpc</span><span class="p">(</span><span class="sh">"</span><span class="s">init_worker</span><span class="sh">"</span><span class="p">)</span>  <span class="c1"># CUDA init, NCCL init
</span>        <span class="n">self</span><span class="p">.</span><span class="n">executor</span><span class="p">.</span><span class="nf">collective_rpc</span><span class="p">(</span><span class="sh">"</span><span class="s">load_model</span><span class="sh">"</span><span class="p">)</span>   <span class="c1"># 权重加载进 GPU
</span>
        <span class="c1"># 步骤三：KV Cache 初始化（三小步，见下）
</span>        <span class="n">self</span><span class="p">.</span><span class="nf">_initialize_kv_caches</span><span class="p">(</span><span class="n">vllm_config</span><span class="p">)</span>

        <span class="c1"># 步骤四：调度器上线，等待请求
</span>        <span class="n">self</span><span class="p">.</span><span class="n">scheduler</span> <span class="o">=</span> <span class="nc">Scheduler</span><span class="p">(</span><span class="n">vllm_config</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">kv_cache_manager</span><span class="p">)</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">collective_rpc</code> 是广播调用——单 GPU 直接调 Worker 方法，多 GPU 时所有 rank 同时执行，主进程等所有 rank 完成才继续。</p> <h3 id="64-workerinit--gpumodelrunnerload_model权重进-gpu">6.4 Worker.<strong>init</strong>() / GPUModelRunner.load_model()：权重进 GPU</h3> <p><code class="language-plaintext highlighter-rouge">load_model</code> RPC 最终落到：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Worker.__init__()  —  vllm/v1/worker/gpu_worker.py:126
</span><span class="k">class</span> <span class="nc">Worker</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">vllm_config</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">model_runner</span> <span class="o">=</span> <span class="nc">GPUModelRunner</span><span class="p">(</span><span class="n">vllm_config</span><span class="p">)</span>   <span class="c1"># gpu_model_runner.py:438
</span>
<span class="c1"># GPUModelRunner.load_model()  —  vllm/v1/worker/gpu_model_runner.py:5176
</span><span class="k">class</span> <span class="nc">GPUModelRunner</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">load_model</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">model</span> <span class="o">=</span> <span class="nf">get_model_loader</span><span class="p">(</span><span class="n">load_config</span><span class="p">).</span><span class="nf">load_model</span><span class="p">(</span><span class="n">vllm_config</span><span class="p">)</span>
        <span class="c1"># self.model 是标准 torch.nn.Module
</span>        <span class="c1"># attention 层已被 vllm 替换为调用 FlashAttention kernel 的实现
</span></code></pre></div></div> <p>FlashAttention 是一种 IO-aware 注意力算法，通过 tiling 减少 HBM 读写次数，比标准 <code class="language-plaintext highlighter-rouge">softmax(QKᵀ/√d)V</code> 快 2-4×。权重加载完后，<code class="language-plaintext highlighter-rouge">self.model</code> 就是这样一个 FlashAttention 版本的 <code class="language-plaintext highlighter-rouge">nn.Module</code>，可以直接 <code class="language-plaintext highlighter-rouge">forward()</code>。</p> <h3 id="65-kv-cache-初始化量一下算一下分一下">6.5 KV Cache 初始化：量一下，算一下，分一下</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># EngineCore._initialize_kv_caches()  —  vllm/v1/engine/core.py（精简）
</span><span class="k">def</span> <span class="nf">_initialize_kv_caches</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">vllm_config</span><span class="p">):</span>
    <span class="c1"># 1. 查询每层 KV Cache 的块规格（head 数、head dim、dtype）
</span>    <span class="n">kv_cache_specs</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">executor</span><span class="p">.</span><span class="nf">get_kv_cache_specs</span><span class="p">()</span>

    <span class="c1"># 2. 跑一次 profiling dummy forward，测量峰值显存
</span>    <span class="c1">#    available = gpu_memory_utilization × total_vram - peak_forward
</span>    <span class="n">num_gpu_blocks</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">executor</span><span class="p">.</span><span class="nf">determine_available_memory</span><span class="p">(</span><span class="n">kv_cache_specs</span><span class="p">)</span>

    <span class="c1"># 3. 在 GPU 上分配 KV Cache tensor，初始化 BlockPool
</span>    <span class="n">self</span><span class="p">.</span><span class="n">executor</span><span class="p">.</span><span class="nf">initialize_cache</span><span class="p">(</span><span class="n">num_gpu_blocks</span><span class="p">)</span>
    <span class="n">self</span><span class="p">.</span><span class="n">kv_cache_manager</span> <span class="o">=</span> <span class="nc">KVCacheManager</span><span class="p">(</span>   <span class="c1"># kv_cache_manager.py:110
</span>        <span class="n">num_gpu_blocks</span><span class="p">,</span> <span class="n">kv_cache_specs</span>
    <span class="p">)</span>
</code></pre></div></div> <p>为什么要 profiling？vllm 不知道推理时 activation 峰值是多少——它取决于 batch size 和序列长度，无法静态估算。直接跑一次”最坏情况”的 dummy forward 量一下，剩下的显存全给 KV Cache。代价是启动多花几秒，换来分配精准。</p> <p>至此，<code class="language-plaintext highlighter-rouge">LLM()</code> 完成：模型在 GPU 上就绪，KV Cache 分配好，调度器等待请求。</p> <hr/> <h2 id="7-小结初始化完成时系统是什么状态">7. 小结：初始化完成时，系统是什么状态</h2> <p><code class="language-plaintext highlighter-rouge">LLM("facebook/opt-125m")</code> 这一行干完了四件事：</p> <ol> <li>解析配置，确定执行后端和通信模式</li> <li>Worker 初始化，CUDA init，HuggingFace 权重加载到 GPU（<code class="language-plaintext highlighter-rouge">torch.nn.Module</code> 就绪）</li> <li>Profiling forward pass → 计算可用 KV Cache 块数 → 分配 KV Cache tensor</li> <li>调度器就绪，等待请求</li> </ol> <p><strong>常见报错对应的模块：</strong></p> <table> <thead> <tr> <th>报错场景</th> <th>大概位置</th> </tr> </thead> <tbody> <tr> <td>多 GPU 启动失败 / NCCL 报错</td> <td><code class="language-plaintext highlighter-rouge">vllm/v1/executor/multiproc_executor.py</code></td> </tr> <tr> <td>模型加载失败 / 权重格式不支持</td> <td><code class="language-plaintext highlighter-rouge">vllm/model_executor/model_loader.py</code></td> </tr> <tr> <td>显存不够 / KV Cache 分配失败</td> <td><code class="language-plaintext highlighter-rouge">EngineCore._initialize_kv_caches()</code> → <code class="language-plaintext highlighter-rouge">determine_available_memory()</code></td> </tr> <tr> <td>平台不兼容 / worker_cls 找不到</td> <td><code class="language-plaintext highlighter-rouge">vllm/platforms/cuda.py</code>（或对应平台文件）</td> </tr> <tr> <td>TP/PP 通信超时</td> <td><code class="language-plaintext highlighter-rouge">MultiprocExecutor</code>，检查 NCCL 版本和网络配置</td> </tr> </tbody> </table> <hr/> <blockquote> <p>另外，我们团队最近出版了<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，感兴趣的话可以看看。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="vllm"/><category term="LLM推理"/><category term="系统设计"/><category term="源码分析"/><summary type="html"><![CDATA[vllm v1 源码精读（一）：为什么要重写，以及 LLM() 这行代码背后发生了什么]]></summary></entry></feed>