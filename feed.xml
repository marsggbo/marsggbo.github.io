<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://marsggbo.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://marsggbo.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-10T07:43:40+00:00</updated><id>https://marsggbo.github.io/feed.xml</id><title type="html">HE Xin</title><subtitle>&quot;SSS: Study, Sleep, Slim&quot; </subtitle><entry><title type="html">说人话理解 EPIC：KV Cache 复用的「编译-链接」范式（附可运行代码复现）</title><link href="https://marsggbo.github.io/blog/2026/2410_15332/" rel="alternate" type="text/html" title="说人话理解 EPIC：KV Cache 复用的「编译-链接」范式（附可运行代码复现）"/><published>2026-05-10T00:00:00+00:00</published><updated>2026-05-10T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/2410_15332</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/2410_15332/"><![CDATA[<h1 id="说人话理解-epickv-cache-复用的编译-链接范式附可运行代码复现">说人话理解 EPIC：KV Cache 复用的「编译-链接」范式（附可运行代码复现）</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2410.15332">EPIC: Efficient Position-Independent Caching for Serving Large Language Models</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有想过，当你用 RAG 系统给 LLM 塞了 5 篇文档 + 一个问题时，这 5 篇文档的 KV cache 其实在不同请求之间是可以复用的？</p> <p>毕竟文档内容没变，变的只是用户的问题。但现实是，<strong>绝大多数系统只支持前缀匹配的 KV cache 复用</strong>——也就是说，只有当两个请求的开头完全一致时，缓存才能命中。这就很尴尬了：你 RAG 检索出来的文档顺序稍微变一下，或者 system prompt 改了一个字，前面缓存的 KV 全部失效，得重算。</p> <p>这也是整个行业在 KV cache 复用上的<strong>核心矛盾</strong>：你想复用的 chunk（文档/few-shot 示例）在不同请求里出现的位置不一样，但 KV 向量里偏偏编码了位置信息（RoPE），导致”位置一变，缓存作废”。</p> <p>今天想和大家聊聊这篇 EPIC，它把这个问题用一个非常漂亮的类比讲明白了——<strong>位置无关代码（PIC）</strong>。学过操作系统的同学应该不陌生：动态链接库之所以能被加载到内存的任意地址，靠的就是位置无关代码。EPIC 要做的，是让 KV cache 也能实现”位置无关”。</p> <h2 id="2-核心问题为什么-kv-cache-不能随便拼">2. 核心问题：为什么 KV Cache 不能随便拼？</h2> <p>先交代下背景。LLM 的 prefill 阶段要给所有 prompt token 算 KV 向量，这一步是 compute-bound 的，也是 TTFT（Time-To-First-Token）的主要瓶颈。Context caching（也叫 prompt caching）的思路很简单：<strong>重复出现的 token 序列，把它们的 KV 向量缓存下来，下次直接用</strong>。</p> <p>vLLM、SGLang 这些推理框架都实现了 prefix caching——但问题是，它只能复用<strong>公共前缀</strong>。</p> <p><img src="/assets/img/posts/2410_15332/002_Figure_2.png" alt="位置无关缓存 vs 位置无关代码的类比"/></p> <p>如上图，EPIC 把 KV cache 的复用类比成编译和链接：</p> <ul> <li><strong>Compile 步骤</strong>：每个文档 chunk 被独立送进 LLM，生成 KV 向量并缓存（类比把 C 源文件编译成 .o 文件）</li> <li><strong>Link 步骤</strong>：使用时把多个 chunk 的 KV 缓存拼起来，加上用户 query，重算一部分 KV 来保证精度（类比链接 .o 文件生成可执行程序）</li> </ul> <p>但这里有个关键问题：<strong>每个 chunk 独立编译时，position ID 都是从 0 开始的</strong>。这意味着拼接后，第二个 chunk 的第一个 token 的位置信息是”0”，但它在完整 prompt 里的实际位置可能是 1024。</p> <p>RoPE 位置编码直接编进了 K 和 Q 向量，位置信息的错位导致注意力计算出错。更具体地说，<strong>每个 chunk 开头的 token 会产生”attention sink”效应——它们会过度吸引注意力，让后面的 token 无法有效关注到真正重要的内容</strong>。</p> <h2 id="3-现有方案的痛点">3. 现有方案的痛点</h2> <p>在 EPIC 之前，CacheBlend 是第一个尝试解决 Position-Independent Caching (PIC) 的工作。但它有两个硬伤：</p> <p><strong>第一，复杂度太高。</strong> CacheBlend 需要先在第一层完整重算所有 KV，通过对比 attention map 选出”变化最大”的 15% token，然后在剩余层只重算这 15%。但别忘了，第一层的完整重算就已经是 O(N²) 了——当 N 到达 35000 token 时，CacheBlend 直接 OOM。</p> <p><strong>第二，动态选择 token 的开销巨大。</strong> CacheBlend 用的是 dynamic sparsity，需要运行时计算 attention map 来选 token，这个”选 token”本身就占了 TTFT 的 <strong>16% - 64%</strong>。</p> <p><img src="/assets/img/posts/2410_15332/009_Figure_9.png" alt="TTFT 随 context 长度变化"/></p> <p>如上图，随着 context 变长，CacheBlend-15 的 TTFT 以二次方增长，在约 35000 token 时 OOM；而 LegoLink-16 几乎是线性增长，50000 token 也没问题。</p> <h2 id="4-legolink简单到离谱的核心算法">4. LegoLink：简单到离谱的核心算法</h2> <p>EPIC 提出的 LegoLink 算法，核心思想简单到让人拍大腿：</p> <blockquote> <p><strong>只重算每个 chunk 开头的 k 个 token（第一个 chunk 除外），让它们”意识到”自己不在序列起始位置。</strong></p> </blockquote> <p>就这么简单。k 通常设为 2~16，在论文实验中 <strong>k=2 就足以把精度损失控制在 7% 以内</strong>。</p> <p><img src="/assets/img/posts/2410_15332/004_Figure_4.png" alt="四种 PIC 算法对比"/></p> <p>如上图对比了 Naive（不重算）、FR（全重算）、CacheBlend（重算 15% token）和 LegoLink（只重算每个 chunk 开头 k 个 token）。右边的 attention map 非常直观：</p> <ul> <li><strong>Naive</strong>：每个 chunk 开头有明显的亮色竖线（attention sink），注意力被这些 token 吸走了</li> <li><strong>FR</strong>：attention 分布正常，chunk 开头 token 虽然仍有一定 attention（因为 BOS token 的特殊性），但不再过度集中</li> <li><strong>LegoLink</strong>：通过只重算开头 k 个 token，attention map 几乎和 FR 一致</li> </ul> <p>为什么 <strong>只重算开头 k 个 token 就够了</strong>？因为 attention sink 本质上是位置编码导致的：chunk 独立编译时，开头 token 被编码为”位置 0”，在 causal attention 中它们接收了后续所有 token 的注意力，变成了 attention 的”黑洞”。只要在 link 阶段用正确的全局位置重新算这几个 token 的 KV，它们的 attention sink 效应就会消失。</p> <p>复杂度对比：</p> <ul> <li><strong>FR</strong>：O(N²) — 全部重算</li> <li><strong>CacheBlend-15</strong>：O(15%·N²) — 还是 O(N²) 量级</li> <li><strong>LegoLink</strong>：O(k·N) ≈ O(N) — k 是常数（跟 chunk 数有关，不跟 N 有关），<strong>近线性</strong></li> </ul> <h2 id="5-系统架构">5. 系统架构</h2> <p><img src="/assets/img/posts/2410_15332/003_Figure_3.png" alt="EPIC 系统架构"/></p> <p>EPIC 的系统架构很清晰：</p> <ul> <li><strong>KVCompile</strong>：接收用户提交的 immutable chunk，执行标准 prefill 生成 KV 向量，存入 KVCache，返回 cache ID</li> <li><strong>KVLink</strong>：收到请求时，根据 cache ID 取出 KV 缓存，执行 LegoLink 重算，然后正常 decode</li> <li><strong>KVCache</strong>：支持 HBM / DRAM / SSD 多级存储</li> </ul> <p>用户通过两个 API 交互：</p> <ol> <li><code class="language-plaintext highlighter-rouge">generate_context_cache(chunks)</code> → 获得 cache IDs</li> <li><code class="language-plaintext highlighter-rouge">chat_completion(cache_ids, query)</code> → 获得回复</li> </ol> <p>这种 explicit caching 的设计跟 Google Gemini 和 Mooncake 类似，用户自己管理缓存的生命周期。</p> <h2 id="6-实验结果">6. 实验结果</h2> <h3 id="61-精度-延迟-pareto-前沿">6.1 精度-延迟 Pareto 前沿</h3> <p><img src="/assets/img/posts/2410_15332/006_Figure_6.png" alt="精度 vs TTFT"/></p> <p>如上图（6 个数据集 × 3 个模型），LegoLink（蓝色星星系列）在大多数场景下建立了新的 Pareto 前沿。核心发现：</p> <ul> <li><strong>LegoLink-2 就能把精度损失控制在 0-7%</strong>，同时 TTFT 比 CacheBlend-15 减少最多 <strong>3×</strong></li> <li>CacheBlend-1 或 CacheBlend-5（重算类似数量的 token）精度会崩——因为它选错了要重算的 token</li> <li>增大 k 带来的精度增益递减：重算几个开头 token 就够了</li> </ul> <h3 id="62-系统级性能">6.2 系统级性能</h3> <p><img src="/assets/img/posts/2410_15332/008_Figure_8.png" alt="延迟和吞吐"/></p> <p>在异步工作负载下：</p> <ul> <li>LegoLink-16 相比 CacheBlend-15 实现 <strong>最高 8× TTFT 降低</strong></li> <li>吞吐提升 <strong>最高 7×</strong></li> <li>随着 CCR（Context Cache Ratio）增加，LegoLink 的 TTFT 保持稳定，而 CacheBlend 持续波动</li> </ul> <h3 id="63-legolink-0零开销变体">6.3 LegoLink-0：零开销变体</h3> <p>论文还提出了一个骚操作：<strong>LegoLink-0</strong>。在 compile 阶段给每个 chunk 前面加 4 个 dummy token（如 BOS token），编译完后把这些 dummy 的 KV 丢掉。这样 attention sink 在编译时就被”预消费”了，link 阶段完全不需要重算！虽然精度比 LegoLink-2 略差一点，但 <strong>link 开销为零</strong>。</p> <h2 id="7-代码复现">7. 代码复现</h2> <p>光说不练假把式。我用一个最简的 Transformer 模型复现了 LegoLink 的核心逻辑，不依赖 vLLM，只需要 PyTorch，CPU 就能跑。</p> <p>代码在这里：<a href="https://github.com/marsggbo/easy-kvcache">https://github.com/marsggbo/easy-kvcache</a></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/marsggbo/easy-kvcache.git
<span class="nb">cd </span>easy-kvcache
python epic/epic_legolink.py
</code></pre></div></div> <p>核心实现大概 400 行，把 compile/link 两步和四种算法（Naive / FR / LegoLink / LegoLink-0）都实现了。这里挑几个关键部分讲一下。</p> <h3 id="71-compile-步骤独立编码-chunk">7.1 Compile 步骤：独立编码 chunk</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">compile_chunk</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">token_ids</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">CompiledChunk</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">Compile：独立编码一个 chunk，position 从 0 开始</span><span class="sh">"""</span>
    <span class="n">chunk_len</span> <span class="o">=</span> <span class="n">token_ids</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span>
    <span class="c1"># ⚠️ 关键：position IDs 从 0 开始
</span>    <span class="n">positions</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">arange</span><span class="p">(</span><span class="n">chunk_len</span><span class="p">,</span> <span class="n">device</span><span class="o">=</span><span class="n">token_ids</span><span class="p">.</span><span class="n">device</span><span class="p">)</span>
    <span class="n">_</span><span class="p">,</span> <span class="n">kv_caches</span><span class="p">,</span> <span class="n">_</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">model</span><span class="p">(</span><span class="n">token_ids</span><span class="p">,</span> <span class="n">positions</span><span class="p">)</span>
    <span class="k">return</span> <span class="nc">CompiledChunk</span><span class="p">(</span><span class="n">token_ids</span><span class="o">=</span><span class="n">token_ids</span><span class="p">,</span> <span class="n">kv_caches</span><span class="o">=</span><span class="n">kv_caches</span><span class="p">,</span> <span class="n">chunk_len</span><span class="o">=</span><span class="n">chunk_len</span><span class="p">)</span>
</code></pre></div></div> <p>注意 <code class="language-plaintext highlighter-rouge">positions = torch.arange(chunk_len)</code> ——不管这个 chunk 在最终 prompt 里排第几，编译时 position 永远从 0 开始。这就是”位置无关”的含义，也是后续需要重算的原因。</p> <h3 id="72-legolink只重算开头-k-个-token">7.2 LegoLink：只重算开头 k 个 token</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">link_legolink</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">query_ids</span><span class="p">,</span> <span class="n">k</span><span class="o">=</span><span class="mi">4</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">LegoLink：只重算每个 chunk（除第一个）开头的 k 个 token</span><span class="sh">"""</span>
    <span class="c1"># Step 1: 确定重算位置
</span>    <span class="n">recompute_indices</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="n">offset</span> <span class="o">=</span> <span class="mi">0</span>
    <span class="k">for</span> <span class="n">i</span><span class="p">,</span> <span class="n">chunk</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">compiled_chunks</span><span class="p">):</span>
        <span class="k">if</span> <span class="n">i</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">:</span>  <span class="c1"># 第一个 chunk 不需要重算
</span>            <span class="k">for</span> <span class="n">j</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="nf">min</span><span class="p">(</span><span class="n">k</span><span class="p">,</span> <span class="n">chunk</span><span class="p">.</span><span class="n">chunk_len</span><span class="p">)):</span>
                <span class="n">recompute_indices</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">offset</span> <span class="o">+</span> <span class="n">j</span><span class="p">)</span>
        <span class="n">offset</span> <span class="o">+=</span> <span class="n">chunk</span><span class="p">.</span><span class="n">chunk_len</span>
    <span class="c1"># 加上 query tokens
</span>    <span class="n">query_indices</span> <span class="o">=</span> <span class="nf">list</span><span class="p">(</span><span class="nf">range</span><span class="p">(</span><span class="n">total_ctx_len</span><span class="p">,</span> <span class="n">total_ctx_len</span> <span class="o">+</span> <span class="n">query_len</span><span class="p">))</span>
    <span class="n">all_recompute_indices</span> <span class="o">=</span> <span class="n">recompute_indices</span> <span class="o">+</span> <span class="n">query_indices</span>
    
    <span class="c1"># Step 2: 用 **正确的全局位置** 重算这些 token
</span>    <span class="n">recompute_positions</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">tensor</span><span class="p">(</span><span class="n">all_recompute_indices</span><span class="p">)</span>  <span class="c1"># 全局位置！
</span>    
    <span class="c1"># Step 3: 逐层执行——新算的 KV 替换缓存中对应位置
</span>    <span class="k">for</span> <span class="n">layer_idx</span><span class="p">,</span> <span class="n">layer</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">model</span><span class="p">.</span><span class="n">layers</span><span class="p">):</span>
        <span class="c1"># 计算新的 Q, K, V（用正确位置的 RoPE）
</span>        <span class="n">Q</span><span class="p">,</span> <span class="n">K_new</span><span class="p">,</span> <span class="n">V_new</span> <span class="o">=</span> <span class="nf">compute_qkv</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">recompute_positions</span><span class="p">)</span>
        <span class="c1"># 在缓存 KV 的对应位置覆盖
</span>        <span class="n">K_exp</span><span class="p">[:,</span> <span class="p">:,</span> <span class="n">recompute_indices</span><span class="p">]</span> <span class="o">=</span> <span class="n">K_new</span>
        <span class="n">V_exp</span><span class="p">[:,</span> <span class="p">:,</span> <span class="n">recompute_indices</span><span class="p">]</span> <span class="o">=</span> <span class="n">V_new</span>
        <span class="c1"># Q (k' tokens) attends to all N tokens
</span>        <span class="n">attn</span> <span class="o">=</span> <span class="nf">softmax</span><span class="p">(</span><span class="n">Q</span> <span class="o">@</span> <span class="n">K_exp</span><span class="p">.</span><span class="n">T</span> <span class="o">/</span> <span class="nf">sqrt</span><span class="p">(</span><span class="n">d</span><span class="p">))</span> <span class="o">@</span> <span class="n">V_exp</span>
</code></pre></div></div> <p><strong>关键点</strong>：重算时用的是 token 在完整 prompt 中的<strong>全局位置</strong>（<code class="language-plaintext highlighter-rouge">recompute_positions</code>），而不是 chunk 内的局部位置。这样 RoPE 编码就是正确的，attention sink 自然消失。</p> <h3 id="73-legolink-0编译时预处理">7.3 LegoLink-0：编译时预处理</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">link_legolink_zero</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">query_ids</span><span class="p">,</span> <span class="n">n_dummy</span><span class="o">=</span><span class="mi">4</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">LegoLink-0：compile 时加 dummy prefix，link 时零开销</span><span class="sh">"""</span>
    <span class="k">for</span> <span class="n">chunk</span> <span class="ow">in</span> <span class="n">self</span><span class="p">.</span><span class="n">compiled_chunks</span><span class="p">:</span>
        <span class="c1"># 在 chunk 前面加 n_dummy 个 dummy token（如 BOS）
</span>        <span class="n">dummy_ids</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">full</span><span class="p">((</span><span class="mi">1</span><span class="p">,</span> <span class="n">n_dummy</span><span class="p">),</span> <span class="n">BOS_TOKEN_ID</span><span class="p">)</span>
        <span class="n">padded_ids</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">([</span><span class="n">dummy_ids</span><span class="p">,</span> <span class="n">chunk</span><span class="p">.</span><span class="n">token_ids</span><span class="p">],</span> <span class="n">dim</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>
        <span class="c1"># 编译带 dummy 的版本
</span>        <span class="n">_</span><span class="p">,</span> <span class="n">kv_caches</span><span class="p">,</span> <span class="n">_</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">model</span><span class="p">(</span><span class="n">padded_ids</span><span class="p">,</span> <span class="n">positions</span><span class="p">)</span>
        <span class="c1"># 丢掉 dummy 的 KV，只保留原始 chunk 的 KV
</span>        <span class="n">kv_caches</span> <span class="o">=</span> <span class="p">[(</span><span class="n">K</span><span class="p">[:,:,</span><span class="n">n_dummy</span><span class="p">:,:],</span> <span class="n">V</span><span class="p">[:,:,</span><span class="n">n_dummy</span><span class="p">:,:])</span> <span class="k">for</span> <span class="n">K</span><span class="p">,</span> <span class="n">V</span> <span class="ow">in</span> <span class="n">kv_caches</span><span class="p">]</span>
</code></pre></div></div> <p>dummy token “吃掉”了 attention sink，之后 link 阶段直接 naive 拼接就行。</p> <h3 id="74-运行结果">7.4 运行结果</h3> <p>跑一下就能看到效果：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Step 5: 不同 k 值对 LegoLink 精度的影响

    k      Cosine Sim     Recompute Tokens   % of Total
  -------------------------------------------------------
    0          0.9975                    0         0.0%
    1          0.9995                   10         9.6%
    2          0.9996                   12        11.5%
    4          0.9996                   16        15.4%
   32          1.0000                   72        69.2%
</code></pre></div></div> <p>和论文结论完全一致：<strong>k=2 就够了</strong>，继续加大 k 收益递减。</p> <h2 id="8-一些个人-take">8. 一些个人 Take</h2> <ol> <li> <p><strong>类比很精髓</strong>。用编译/链接来类比 PIC 的 compile/link，这个抽象做得很漂亮。看完论文你会觉得”这也太自然了”，但自然的东西往往是最难想到的。</p> </li> <li> <p><strong>Attention sink 是个被低估的问题</strong>。之前 StreamingLLM 那篇讲 attention sink 更多是在 decode 阶段（evict 过程中保留 sink token），EPIC 把它放到了 PIC 的场景下，而且给出了比 StreamingLLM 更细粒度的解决方案。</p> </li> <li> <p><strong>Static sparsity &gt; Dynamic sparsity</strong>（在这个场景下）。CacheBlend 用 dynamic sparsity 去选 token 重算，看起来更”智能”，但实际上选 token 的开销本身就很大。LegoLink 预先知道该重算谁（每个 chunk 开头），反而又快又准。这给了我们一个启发：<strong>不是所有问题都需要”动态”方案，如果你对问题结构足够了解，静态方案往往更优</strong>。</p> </li> <li> <p><strong>这篇工作对 RAG 场景特别有价值</strong>。目前 RAG 系统的瓶颈之一就是长 context 的 prefill 开销。如果 document chunk 的 KV cache 可以跨请求复用，而且不需要 prefix 完全匹配，那 TTFT 可以大幅降低。可以预见未来的推理框架会原生支持 PIC。</p> </li> <li> <p><strong>代码质量</strong>。官方代码基于 vLLM 实现，改动了约 2K 行 Python，主要在 attention backend 和 scheduler 两处。代码结构还是比较清晰的，感兴趣的可以 diff 一下和 vLLM 0.7.0 的差异。</p> </li> </ol> <p>欢迎评论区交流，如果觉得有帮助也欢迎 star 一下 <a href="https://github.com/marsggbo/easy-kvcache">easy-kvcache</a> 项目，后续会继续加入更多 KV cache 优化算法的简化复现。</p>]]></content><author><name></name></author><category term="LLM"/><category term="KV Cache"/><category term="论文解读"/><category term="Attention"/><category term="推理优化"/><summary type="html"><![CDATA[说人话理解 EPIC：KV Cache 复用的「编译-链接」范式（附可运行代码复现）]]></summary></entry><entry><title type="html">把 Dense LLM 变成 MoE 还能推理提速？NeurIPS 2024 Read-ME 做到了</title><link href="https://marsggbo.github.io/blog/2026/read-me/" rel="alternate" type="text/html" title="把 Dense LLM 变成 MoE 还能推理提速？NeurIPS 2024 Read-ME 做到了"/><published>2026-05-10T00:00:00+00:00</published><updated>2026-05-10T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/read-me</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/read-me/"><![CDATA[<h1 id="把-dense-llm-变成-moe-还能推理提速neurips-2024-read-me-做到了">把 Dense LLM 变成 MoE 还能推理提速？NeurIPS 2024 Read-ME 做到了</h1> <blockquote> <p>原文：<a href="https://proceedings.neurips.cc/paper_files/paper/2024/file/d298cf34e4539f9134db7f38b42f69fe-Paper-Conference.pdf">Read-ME: Refactorizing LLMs as Router-Decoupled Mixture of Experts with System Co-Design</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>做 MoE 推理优化的牛马应该对这个场景不陌生：</p> <p>你好不容易把 Mixtral-8x7B 或者 DeepSeek-MoE 部署起来，满心期待地等着”稀疏激活带来的效率红利”落地，结果一跑推理，GPU 显存占用跟 dense 模型差不了多少，延迟也没降多少。</p> <p>为什么？</p> <p>因为<strong>MoE 的优势是推理时只激活少数 expert，但落地被”全量加载 + 低效调度”的系统设计给卡死了</strong>。</p> <p>这也是整个 MoE 推理领域的核心矛盾之一：模型架构设计和系统推理策略之间的脱节。</p> <p>今天想聊的这篇 Read-ME（NeurIPS 2024）正面刚了这个问题，而且是从算法和系统两侧同时动刀——它不只是在模型结构上做文章，而是提出了一套 algorithm-system co-design 的框架，实现了在更小规模下更好的精度，同时端到端延迟也降下来了。</p> <hr/> <h2 id="2-问题出在哪">2. 问题出在哪</h2> <p>先交代下背景，MoE 落地有两个经典难题：</p> <p><strong>第一个：从头训练 MoE 太贵了。</strong></p> <p>Mixtral、DeepSeek 这些模型，要么是专门设计的 MoE 架构从头预训练，要么是对已有 dense 模型做 “upcycling”（把 dense FFN 直接复制成多个 expert，再继续训练）。无论哪条路，都要消耗海量 token。</p> <p><strong>第二个：传统 layer-wise router 设计在系统层面效率很差。</strong></p> <p>这是 Read-ME 真正重点解决的问题。</p> <p>现在主流 MoE（Mixtral、OpenMoE 等）的 router 是 <strong>layer-wise</strong> 的：每一层都有独立的 router，每层的 expert 选择要等上一层的 hidden state 出来之后才能决定。这带来两个直接的系统问题：</p> <ol> <li> <p><strong>没法提前 prefetch expert</strong>：expert 权重可能分散在不同 GPU/机器上，等到运行时才决定要哪个 expert，来不及预加载，要么 OOM 全加载，要么边算边取等 I/O。</p> </li> <li> <p><strong>batching 效率极差</strong>：一个 batch 里不同 token 需要不同 expert，GPU 利用率拉稀。<strong>这里有个很离谱的数据：Mixtral-8x7B 在 batch size 为 56.8 时，平均每层激活的 unique expert 数量是 7.63 个（总共才 8 个）</strong>——等于说稀疏激活的优势几乎全丢了。</p> </li> </ol> <p>如下图右边的散点图，unique expert 数量和平均延迟几乎线性相关——expert 越分散，latency 越高。左边展示了 batching 困境：同一个 batch 里每个 token 走的 expert 路径都不同，系统调度根本没法做优化：</p> <p><img src="/assets/img/posts/read-me/fig3_batching.png" alt="MoE batching困境：unique expert数量与延迟线性相关"/></p> <hr/> <h2 id="3-read-me-的核心-idea">3. Read-ME 的核心 idea</h2> <p>Read-ME 的框架如下图所示：左半部分是<strong>算法侧</strong>，把预训练好的 dense 模型（黄色）”重构”成两个稀疏 expert（红/绿），并训练一个轻量 pre-gating router；右半部分是<strong>系统侧</strong>，利用 pre-gating 在请求到达时就预先分配 expert，实现 expert-aware batching：</p> <p><img src="/assets/img/posts/read-me/fig1_overview.png" alt="Read-ME系统全貌：dense模型重构为MoE并支持推理时expert-aware调度"/></p> <h3 id="31-算法侧从-dense-提-expert再解耦-router">3.1 算法侧：从 Dense 提 Expert，再解耦 Router</h3> <p>Read-ME 首先在 Llama-2-7B-chat 这类预训练 dense 模型上做 MoE 化，不从头训练，只用 <strong>1B tokens</strong> 做 continual tuning，代价极小。</p> <p>具体是怎么提取 expert 的？基于<strong>激活稀疏性</strong>（activation sparsity）。Dense LLM 里的 FFN，不同数据域对应的激活 channel 是不同的（科学文章、小说、代码、QA 各自激活不同的神经元），把这些”domain-specific”的 channel 组合成独立的 expert，就得到了领域对齐的 MoE 结构——本质是在 activation magnitude 上做 top-k channel 选择，再用 mask 矩阵把 FFN 参数切分成 N 个 expert。</p> <hr/> <p>关键创新在 <strong>router 设计</strong>。</p> <p>作者发现了一个很有意思的现象：<strong>相邻层之间的 expert 选择高度相关</strong>。如下图(a)所示，Mixtral-8x7B 第 30、31 层之间的转移矩阵几乎是行稀疏的——每行只有少数几个非零项，说明”知道第 30 层选了哪个 expert，第 31 层几乎就确定了”。图(b)的 mutual information 曲线也印证了这一点：相邻层之间的 MI 明显高于条件独立的基线。图(c)是对应的 router 蒸馏训练方案：</p> <p><img src="/assets/img/posts/read-me/fig2_router_redundancy.png" alt="层间router冗余性分析：转移矩阵行稀疏（a）、层间MI高（b）、router蒸馏方案（c）"/></p> <p>这意味着什么？<strong>Layer-wise router 大量在做重复工作</strong>，完全可以用一个统一的 router 在推理最开始就预测所有层的 expert 选择。</p> <p>Read-ME 把这个 router 叫做 <strong>pre-gating router</strong>，它和 MoE backbone 完全解耦。结构很简单：一个 1-layer transformer block，只有 <strong>18M 参数</strong>（可以忽略不计的 overhead）。它在 token 进入 MoE 之前就跑完，一次性输出所有层的 expert 路由决策。训练时用 <strong>routing distillation loss</strong>（KL 散度）对齐原始模型的激活稀疏模式，监督信号非常自然。</p> <h3 id="32-系统侧pre-gating-带来的三个优化">3.2 系统侧：Pre-gating 带来的三个优化</h3> <p>有了 pre-gating router，推理系统可以做三件事：</p> <p><strong>① Fine-grained Expert Prefetching</strong></p> <p>知道了接下来要用哪些 expert，就能把加载 expert 权重和当前层的 forward pass 完全 overlap 起来——算第 i 层的同时，后台异步加载第 i+1 层需要的 expert，完全 hide I/O latency。</p> <p><strong>② Belady-inspired Caching</strong></p> <p>Read-ME 这里有个漂亮的 insight：Belady 算法（替换”未来最晚被访问”的 cache item）是理论上最优的离线 cache 策略，但普通场景里你不知道”未来”。而有了 pre-gating，<strong>你恰好知道未来每个请求会用哪个 expert</strong>，可以直接实现 Belady 策略——论文也在理论上证明了这个 caching 是 provably optimal 的。</p> <p><strong>③ Expert-aware Batching</strong></p> <p>Pre-gating 让系统在 batch 组建时就知道每个 token 要用哪些 expert，可以把激活相同 expert 的 token 放同一个 batch，从根本上减少每个 batch 里 unique expert 的数量。</p> <p>如下图对比了两种 pipeline：传统 layer-wise MoE（上）每个 token 各走自己的 route，batching 完全没有 locality 可利用；Read-ME（下）在请求到达时就做 expert-aware 分批，Expert 计算可以高效合并：</p> <p><img src="/assets/img/posts/read-me/fig3_batching.png" alt="传统MoE vs Read-ME推理pipeline：pre-gating在请求入队时就完成expert分配"/></p> <hr/> <h2 id="4-效果怎么样">4. 效果怎么样</h2> <h3 id="41-模型质量">4.1 模型质量</h3> <p>用 Llama-2-7B-chat 做 base，Read-ME 模型 activated parameters 为 4.7B（总参数 17B），训练代价<strong>只用了 1B tokens</strong>。如下表，和各类 baseline 的综合对比：</p> <p><img src="/assets/img/posts/read-me/tbl3_downstream.png" alt="各模型在下游任务上的benchmark对比（MMLU、Hellaswag、WinoGrande等）"/></p> <p>Read-ME 在 MMLU 上达到 <strong>38.9%</strong>，综合 avg 达到 <strong>55.5%</strong>，显著领先同规模的所有 baseline——而对面的 Pythia 用了 300B tokens，Open-Llama-v2 用了 1T tokens，Read-ME 只用 1B tokens 就超过了它们。</p> <p><strong>本质上还是 Llama-2-7B-chat 预训练带来的底子</strong>，continual tuning 只是把 dense 知识迁移到 MoE 结构。</p> <p>如下散点图更直观地展示了 Read-ME 在”MMLU performance vs activated parameters”维度上的位置——相比同等参数量的对手，它处于 Pareto frontier 之上（只有 Llama-2-7B 这种大模型才能超过它，而 Llama-2-7B 激活参数量是它的 1.5 倍）：</p> <p><img src="/assets/img/posts/read-me/fig4_mmlu_scatter.png" alt="MMLU性能与激活参数量散点图：Read-ME处于小参数量区间的最优位置"/></p> <p>（细思极恐：Open-Llama-v2 用 1T tokens 才训到 25.7% MMLU，Read-ME 1B tokens 就做到 38.9%……这就是 pretrain-then-convert 的威力。）</p> <h3 id="42-推理速度">4.2 推理速度</h3> <p>如下图展示了三组实验：左图是单次 124 token 生成任务的 latency 分解（OpenMoE / Llama2-7b / Read-ME），Read-ME 的 Expert/MLP 耗时明显更短；中图是 batched inference 场景下的 p95 latency 分布，Read-ME 的分布更集中且整体更低；右图展示了 temporal locality 分析——Read-ME 的 router 让同序列内 token 倾向于选相同 expert（2921 个 token 跟随上一个 token 的选择 vs Mixtral 的 850 个），这进一步降低 unique expert 的切换开销：</p> <p><img src="/assets/img/posts/read-me/fig5_latency.png" alt="延迟评估与temporal locality分析：Read-ME在三个维度均优于OpenMoE和Mixtral"/></p> <p>综合下来：平均 end-to-end latency 降低 <strong>6.1%</strong>，tail latency（p95）改善 <strong>9.5-10.0%</strong>。</p> <p>在内存受限场景（只缓存 k 个 expert）下，expert prefetching 的效果如下图——在所有 cache capacity 设置下，蓝色（Prefetching）都比橙色（On-demand Loading）有稳定优势，cache capacity 越小时差距越大，最多 <strong>30% 的 latency 节省</strong>：</p> <p><img src="/assets/img/posts/read-me/fig6_prefetch.png" alt="不同cache容量下prefetching vs on-demand loading的延迟对比"/></p> <hr/> <h2 id="5-我的一些看法">5. 我的一些看法</h2> <p>这篇工作让我觉得最有意思的地方，不是最终数字，而是那个关于<strong>层间 router 冗余性</strong>的观察。</p> <p>大家做 MoE 时候默认每层都需要独立的 router，没人细想过这个设计是不是多余的。Read-ME 用转移矩阵和 mutual information 两个角度说话，发现<strong>相邻层 router 的决策几乎是确定性的，layer-wise 设计是过设计（overdesign）</strong>。</p> <p>这个 insight 的迁移价值很高：如果我们在一开始就设计 MoE 架构（而不是从 dense 转换），是不是也可以考虑 shared router？参数量少了，推理也更友好。这是个值得跟进的方向。</p> <p>另外值得注意的是，这套框架的前提是”从预训练 dense 模型转换 MoE”，且需要原始模型本身有足够强的激活稀疏性，才能做出质量够好的 expert 划分。如果原始模型的 FFN 激活很稠密，这条路可能就走不通。</p> <p>不管怎样，<strong>algorithm-system co-design 这个思路是对的</strong>——光在模型架构上优化，忽视系统层面适配，落地效果必然打折。Read-ME 这篇给出了一个完整的闭环方案，理论上证明了 caching 最优性，工程上也量化了每个优化模块的贡献，是那种”可以直接参考来设计自己系统”的工作。</p> <p>欢迎评论区交流，有 MoE 推理踩坑经历的也欢迎来聊。</p> <hr/> <p><em>代码开源在：<a href="https://github.com/VITA-Group/READ-ME">https://github.com/VITA-Group/READ-ME</a></em></p>]]></content><author><name></name></author><category term="LLM"/><category term="MoE"/><category term="论文解读"/><category term="推理优化"/><summary type="html"><![CDATA[把 Dense LLM 变成 MoE 还能推理提速？NeurIPS 2024 Read-ME 做到了]]></summary></entry><entry><title type="html">KV Cache 复用的第三条路：FAST 2026 CacheSlide 是怎么解决 Agent 推理的位置漂移问题的 - marsggbo</title><link href="https://marsggbo.github.io/blog/2026/kv-cache-fast-2026-cacheslide-agent-marsggbo/" rel="alternate" type="text/html" title="KV Cache 复用的第三条路：FAST 2026 CacheSlide 是怎么解决 Agent 推理的位置漂移问题的 - marsggbo"/><published>2026-04-29T07:35:00+00:00</published><updated>2026-04-29T07:35:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/kv-cache-fast-2026-cacheslide--agent----marsggbo</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/kv-cache-fast-2026-cacheslide-agent-marsggbo/"><![CDATA[]]></content><author><name></name></author><summary type="html"><![CDATA[原文：CacheSlide: Unlocking Cross Position-Aware KV Cache Reuse for Accelerating LLM Serving 1. 前言：两种方案都不够用 作为一个天天和 LLM 推理打交道的牛马，我对 KV Cache 这个话题有复杂感情——它]]></summary></entry><entry><title type="html">MoE 推理的内存墙，被一块多芯粒芯片打穿了？ - marsggbo</title><link href="https://marsggbo.github.io/blog/2026/moe-marsggbo/" rel="alternate" type="text/html" title="MoE 推理的内存墙，被一块多芯粒芯片打穿了？ - marsggbo"/><published>2026-04-29T07:35:00+00:00</published><updated>2026-04-29T07:35:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/moe----marsggbo</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/moe-marsggbo/"><![CDATA[]]></content><author><name></name></author><summary type="html"><![CDATA[今天想和大家聊聊这篇来自港科大的工作 —— Expert Streaming，最近在 arXiv 上出现，是少见的从芯片架构角度直接解决 MoE 推理内存瓶颈的硬核工作。 先交代下背景：MoE 火是真的火，DeepSeek、Qwen3 都在往 MoE 走，但我们自己跑的时候，却结结实实踩了个大坑 —]]></summary></entry><entry><title type="html">KVCOMM：让多 Agent 系统的 KV Cache 真正“通起来”，TTFT 直接砍掉 7.8 倍 - marsggbo</title><link href="https://marsggbo.github.io/blog/2026/kvcomm-agent-kv-cache-ttft-78-marsggbo/" rel="alternate" type="text/html" title="KVCOMM：让多 Agent 系统的 KV Cache 真正“通起来”，TTFT 直接砍掉 7.8 倍 - marsggbo"/><published>2026-04-29T07:34:00+00:00</published><updated>2026-04-29T07:34:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/kvcomm-agent--kv-cache-ttft--78----marsggbo</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/kvcomm-agent-kv-cache-ttft-78-marsggbo/"><![CDATA[]]></content><author><name></name></author><summary type="html"><![CDATA[原文：KVCOMM: Online Cross-context KV-cache Communication for Efficient LLM-based Multi-agent Systems（Duke / MIT / NVIDIA，OpenReview 投稿） 代码：https://githu]]></summary></entry><entry><title type="html">MoE 推理的内存墙，被一块多芯粒芯片打穿了？</title><link href="https://marsggbo.github.io/blog/2026/2603_27624/" rel="alternate" type="text/html" title="MoE 推理的内存墙，被一块多芯粒芯片打穿了？"/><published>2026-04-29T00:00:00+00:00</published><updated>2026-04-29T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/2603_27624</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/2603_27624/"><![CDATA[<h1 id="moe-推理的内存墙被一块多芯粒芯片打穿了">MoE 推理的内存墙，被一块多芯粒芯片打穿了？</h1> <p>今天想和大家聊聊这篇来自港科大的工作 —— <strong>Expert Streaming</strong>，最近在 arXiv 上出现，是少见的从芯片架构角度直接解决 MoE 推理内存瓶颈的硬核工作。</p> <p>先交代下背景：MoE 火是真的火，DeepSeek、Qwen3 都在往 MoE 走，但我们自己跑的时候，却结结实实踩了个大坑 —— <strong>显存根本不够用</strong>。稀疏激活的好处（计算量小）在 edge 设备上被”要把所有 expert 权重全量加载进来”这一条给彻底抵消了。</p> <hr/> <h2 id="1-问题到底出在哪里">1. 问题到底出在哪里</h2> <p>MoE 的逻辑是：每次 forward，一个 token 只激活 top-K 个 expert，90% 以上的 expert 权重全程闲着。理论上很省计算，但现实是：</p> <p><strong>你得把所有 expert 的权重都放进 on-chip memory，才能在激活时立刻取用。</strong></p> <p>Qwen3-30B-A3B 有 128 个 expert，全量加载就是海量的 on-chip 存储需求。在 edge 多芯粒（multi-chiplet）加速器上，每块芯粒的 SRAM 是有限的，放不下。</p> <p>大家的常见做法是 Expert Parallelism（EP）—— 把不同的 expert 分到不同芯粒上，每个芯粒只存一部分 expert。但这带来两个问题：</p> <ol> <li><strong>all-to-all 通信开销</strong>：token 要跨芯粒路由到对应 expert 所在的芯粒，通信量随 batch size 下降而急剧放大</li> <li><strong>负载不均衡</strong>：MoE 有个很典型的”长尾效应”——</li> </ol> <p><img src="/assets/img/posts/2603_27624/002_Figure_2.png" alt="Figure 2: MoE 模型的长尾激活效应（不同 batch size 下，expert 的 token 处理数量）"/></p> <p>如上图，小 batch 下这个长尾效应更严重：少数 hot expert 处理大量 token，大量 cold expert 几乎空转，芯粒利用率极不均匀。</p> <p>还有个 state-of-the-art 的方案叫 <strong>Hydra</strong>，思路是利用 cross-layer expert popularity 来提前预测哪些 expert 会被激活，减少跨片通信。但本质还是基于 EP 的思路，没有从根上解决 on-chip memory 压力。</p> <p>这也是整个行业 MoE edge 部署面临的<strong>核心矛盾</strong>：稀疏激活带来的计算收益，被全量权重加载的内存代价完全吃掉了。</p> <hr/> <h2 id="2-expert-streaming-的核心思路">2. Expert Streaming 的核心思路</h2> <p>这篇工作的出发点很有意思 —— 既然 D2D（die-to-die）高带宽互联（比如 UCIe）越来越成熟，<strong>为什么不把它当成一种计算资源，而不是单纯的通信开销来对待？</strong></p> <p><img src="/assets/img/posts/2603_27624/001_Figure_1.png" alt="Figure 1: 多芯粒 AI 加速器架构模板，以及 MoE 网络结构示意"/></p> <p>核心方案叫 <strong>FSE-DP（Fully Sharded Expert Data Parallelism）</strong>，可以拆成三个互相咬合的设计：</p> <h3 id="21-micro-slice-streaming">2.1 Micro-slice Streaming</h3> <p>把每个 expert 的权重切成小块（micro-slice），在芯粒之间像流水线一样循环传输。</p> <p>每个芯粒同时做三件事：</p> <ul> <li><strong>计算</strong>当前 micro-slice 对应的矩阵乘</li> <li><strong>接收</strong>下一块从邻居芯粒传来的 micro-slice</li> <li><strong>转发</strong>刚算完的 micro-slice 给下一个芯粒</li> </ul> <p>这样，D2D 通信和计算完全 overlap，expert 权重不需要全量驻留在任何一个芯粒上。</p> <p><img src="/assets/img/posts/2603_27624/004_Figure_4.png" alt="Figure 4: micro-slice 流水线：D2D 通信与计算 overlap 的详细示意"/></p> <p><strong>关键效果：on-chip memory 只需要存若干个 micro-slice 大小的 buffer，而不是整个 expert 的权重。</strong></p> <p>更进一步，他们做了”eager micro-slice usage”的优化 —— 芯粒收到 micro-slice 后立刻转发，不等计算完，这样每块 micro-slice 在 ring 上停留时间更短，buffer 占用更低。</p> <h3 id="22-多芯粒下的全分片并行fse-dp">2.2 多芯粒下的全分片并行（FSE-DP）</h3> <p>一个 expert 不再绑定在某一个芯粒上，而是<strong>均匀切分到所有芯粒上</strong>。每个芯粒持有这个 expert 的一个 slice，token 在芯粒间做 redispatch 以均衡负载。</p> <p><img src="/assets/img/posts/2603_27624/003_Figure_3.png" alt="Figure 3: FSE-DP 示意：4 个芯粒共同计算 expert 1，两种等价的数据流方式"/></p> <p>两种等价的执行方式：</p> <ul> <li>(a) token sequence 不动，expert slice 在芯粒间流动</li> <li>(b) expert slice 不动，token sequence 在芯粒间流动</li> </ul> <p>本质是一样的，调度器统一抽象为”trajectory”来管理。</p> <h3 id="23-paired-load--token-buffering">2.3 Paired-load + Token Buffering</h3> <p>光有 micro-slice streaming 还不够，还有个利用率问题：DDR 加载 expert 权重的延迟（off-chip）比 D2D 通信慢得多。</p> <p><strong>Paired-load</strong>：把 hot expert（处理 token 多，compute-bound）和 cold expert（处理 token 少，memory-bound）配对，在同一个时间窗口内交错执行，让计算和 DDR 加载互相掩盖对方的 idle 时间。</p> <p><strong>Token Buffering</strong>：遇到 cold expert 时，不急着立刻处理这个 request，而是在 MoE 层边界暂停，等积累到一定数量的 token 之后再一起处理，提升 expert 利用率。</p> <p><img src="/assets/img/posts/2603_27624/005_Figure_5.png" alt="Figure 5: Paired-load 和 Token Buffering 策略的示意图"/></p> <p>这两个策略把 DDR 加载的 overhead 和 D2D 的 micro-slice 流完全融合进了一个统一的 pipeline：</p> <p><img src="/assets/img/posts/2603_27624/006_Figure_6.png" alt="Figure 6: DDR 加载与 D2D micro-slice 流的 flow fusion 示意"/></p> <hr/> <h2 id="3-调度器设计">3. 调度器设计</h2> <p>上面这套方案能运转，有个前提：需要一个硬件调度器来实时决定”哪些 expert 的 micro-slice 走哪条 trajectory”。</p> <p>他们把这个调度器实现在 IO die 上，面积仅 <strong>0.43 mm²（28nm 工艺）</strong>，调度延迟在<strong>亚微秒级别</strong>，不会成为 bottleneck。</p> <p><img src="/assets/img/posts/2603_27624/008_Figure_8.png" alt="Figure 8: 调度器硬件设计，实现在 IO die 上"/></p> <p>“Virtualization”是调度器抽象层的关键概念 —— 无论 expert 在各芯粒上如何不均匀分布，调度器只需要保证 micro-slice 按规定的 trajectory 流动，底层实现细节对上层完全透明。</p> <p><img src="/assets/img/posts/2603_27624/007_Figure_7.png" alt="Figure 7: Virtualization 示意：irregular 分布下 micro-slice 仍能正确高效流动"/></p> <hr/> <h2 id="4-实验效果">4. 实验效果</h2> <p>测试了 4 个模型：Phi-3.5-MoE（16 experts）、Yuan2.0-M32（32 experts）、DeepSeek-MoE-16B（64 experts）、Qwen3-30B-A3B（128 experts），数据集覆盖 Wikitext-2、C4、WinoGrande。</p> <h3 id="单层-latency">单层 latency</h3> <p><img src="/assets/img/posts/2603_27624/009_Figure_9.png" alt="Figure 9: 不同模型、数据集、输入 token 数下的单层 MoE 延迟对比"/></p> <p>FSE-DP 在 16–1024 token 的 low-batch 范围内，单层 latency 均优于 EP 和 Hydra。</p> <h3 id="端到端-throughput">端到端 throughput</h3> <p><img src="/assets/img/posts/2603_27624/014_Figure_14.png" alt="Figure 14: 端到端 throughput 对比（多个模型-数据集组合）"/></p> <p><strong>端到端 throughput 相比 state-of-the-art 基线，实现了 1.22×–2.00× 的提升。</strong></p> <h3 id="on-chip-memory-savings">On-chip memory savings</h3> <p>与 Expert Parallelism 相比，<strong>节省了高达 78.8% 的 on-chip memory</strong>。</p> <p><img src="/assets/img/posts/2603_27624/012_Figure_12.png" alt="Figure 12: 不同模型下的 on-chip memory 用量对比"/></p> <h3 id="ablation-study">Ablation study</h3> <p><img src="/assets/img/posts/2603_27624/015_Figure_15.png" alt="Figure 15: FSE-DP 各设计组件的 ablation 实验"/></p> <p>micro-slice streaming、paired-load、token buffering 三个模块各自都有贡献，叠加后达到最优。</p> <hr/> <h2 id="5-我的-take">5. 我的 take</h2> <p>这篇工作有几个地方让我觉得很有意思：</p> <p><strong>1. 视角的转换很关键。</strong> 之前大家把 D2D 通信当成 MoE 并行的”代价”来优化，这篇工作直接把它当成”流水线资源”来利用，思路上有个质的跳转。</p> <p><strong>2. 真正的硬件协同设计。</strong> 光有算法不够，他们实际流片了（Figure 10 有芯片照片），在真实芯片上验证了整套方案，这在学术界还是比较稀缺的。</p> <p><img src="/assets/img/posts/2603_27624/010_Figure_10.png" alt="Figure 10: 芯片 floorplan 和封装照片"/></p> <p><strong>3. 对 edge MoE 的意义。</strong> 2.00× throughput + 78.8% memory savings，在资源受限的 edge 场景下，这个 combo 是实质性的提升，不是那种”提了 3%，但只在特定条件下”的 benchmark 游戏。</p> <p>当然，也有局限性：token buffering 会引入额外的 latency（在 request 层面），这对 latency-sensitive 的场景有影响；另外这套方案对 D2D 带宽有一定要求，不同代际芯片的迁移成本还不清楚。</p> <p>总体来说，<strong>这是一篇把系统架构、调度算法、硬件实现做得比较扎实的 MoE 推理优化工作</strong>，推荐做 LLM 系统和 edge 部署方向的同学认真看看。</p> <p>欢迎评论区交流，有问题也欢迎指出。</p>]]></content><author><name></name></author><category term="LLM"/><category term="MoE"/><category term="论文解读"/><category term="推理优化"/><category term="芯片架构"/><summary type="html"><![CDATA[MoE 推理的内存墙，被一块多芯粒芯片打穿了？]]></summary></entry><entry><title type="html">多 Agent 协作不需要说「人话」？LatentMAS 让 LLM 在隐空间里直接协作</title><link href="https://marsggbo.github.io/blog/2026/latentmas/" rel="alternate" type="text/html" title="多 Agent 协作不需要说「人话」？LatentMAS 让 LLM 在隐空间里直接协作"/><published>2026-04-27T00:00:00+00:00</published><updated>2026-04-27T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/latentmas</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/latentmas/"><![CDATA[<h1 id="多-agent-协作不需要说人话latentmas-让-llm-在隐空间里直接协作">多 Agent 协作不需要说「人话」？LatentMAS 让 LLM 在隐空间里直接协作</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2511.20639">Latent Collaboration in Multi-Agent Systems</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有想过，当我们让多个 LLM agent 协作解题的时候，它们之间到底在交流什么？</p> <p>答案是：<strong>文本</strong>。一个 agent 花了几千个 token 把推理过程写成自然语言，传给下一个 agent，下一个 agent 再花几千个 token 来理解和继续推理。整个过程就像两个程序员用微信聊天来 pair programming——能用，但效率低得离谱。</p> <p>这个问题在当前的多 Agent 系统（MAS）里是普遍存在的。无论是 AutoGen、CAMEL 这类经典框架，还是 Chain-of-Agents 这种流水线设计，agent 之间的通信全部依赖自然语言。<strong>每个 agent 都要先把”想法”编码成离散 token，传给下一个 agent，再从 token 解码出”想法”——这个过程本身就在丢信息、加延迟</strong>。</p> <p>但仔细想想，LLM 内部推理的时候，用的根本不是 token，而是 hidden states——一个高维连续向量空间里的表示。token 只是最后被 LM head 解码出来给人看的”翻译”。那一个自然的问题就来了：<strong>agent 之间能不能直接传 hidden states，跳过文本这个”中间商”？</strong></p> <p>来自 Princeton、UIUC 和 Stanford 的一个团队最近提出了 <strong>LatentMAS</strong>，一个完全在隐空间（latent space）里实现多 Agent 协作的框架——<strong>不训练、不微调、不生成中间文本</strong>，agent 之间直接通过 KV cache 传递”思维”。在 9 个 benchmark 上，准确率最高提升 14.6%，推理速度快 4-4.3×，token 消耗减少 70.8%-83.7%。</p> <p>今天想和大家聊聊这个工作背后的设计思路，以及它为什么可能是多 Agent 系统的一个新范式。</p> <hr/> <h2 id="2-为什么文本通信是瓶颈">2. 为什么文本通信是瓶颈？</h2> <p>先交代下背景。现在的 LLM-based MAS 主要有两种架构：</p> <p><img src="/assets/img/posts/latentmas/002_Figure_2.png" alt="Sequential 和 Hierarchical 两种 MAS 架构"/></p> <ul> <li><strong>Sequential（链式）</strong>：planner → critic → refiner → solver，前一个 agent 的输出喂给后一个</li> <li><strong>Hierarchical（层级式）</strong>：多个领域专家 agent 各自推理，最后由一个 summarizer 汇总</li> </ul> <p>不管哪种架构，agent 之间的通信介质都是文本。这带来三个问题：</p> <table> <tbody> <tr> <td><strong>第一，信息瓶颈</strong>。LLM 内部 hidden state 的维度通常是 2048-5120，一个向量就能编码丰富的语义信息。但编码成 token 之后，每个 token 只是一个离散符号，从 $</td> <td>V</td> <td>\approx 150000$ 的词表里选一个——信息密度断崖式下降。论文里有个很直观的定理：**要用 token 无损表达 $m$ 步 latent thoughts，需要的文本长度至少是 $\Omega(d_h \cdot m / \log</td> <td>V</td> <td>)$<strong>，其中 $d_h$ 是 hidden dimension。对于 Qwen3-8B（$d_h = 3584$），这意味着 latent thoughts 可以比文本高效 **377 倍</strong>。</td> </tr> </tbody> </table> <p><strong>第二，延迟爆炸</strong>。每个 agent 都要做一轮完整的 auto-regressive 解码（一个 token 一个 token 地吐），然后下一个 agent 再把这堆 token 全部 prefill 进去。4 个 agent 的 sequential MAS，光文本生成和重新 prefill 就要跑 8 轮前向传播。</p> <p><strong>第三，错误放大</strong>。文本是有歧义的。planner 写的方案可能措辞不精确，critic 理解偏了，refiner 基于偏了的理解做修改……<strong>表面文字传递中每一环都可能引入误解，而 hidden states 是精确的数学向量，不存在”理解偏差”</strong>。</p> <hr/> <h2 id="3-latentmas-的核心设计">3. LatentMAS 的核心设计</h2> <p>LatentMAS 的完整流程如下图：</p> <p><img src="/assets/img/posts/latentmas/003_Figure_3.png" alt="LatentMAS 系统架构总览"/></p> <p>整个设计分两块：<strong>agent 内部的 latent thoughts 生成</strong>和<strong>agent 之间的 latent working memory 传递</strong>。</p> <h3 id="31-latent-thoughts让-agent-在隐空间里思考">3.1 Latent Thoughts：让 Agent 在隐空间里”思考”</h3> <p>标准 LLM 推理的流程是：hidden state $h_t$ → LM head 解码成 token $x_{t+1}$ → token embedding $e_{t+1}$ → 进入下一轮前向传播。</p> <p>LatentMAS 把中间的”解码-重编码”去掉了：<strong>直接把最后一层的 hidden state $h_t$ 当作下一步的输入 embedding，跳过 token 这个中介</strong>。</p> <p>但这里有个工程上的坑：$h_t$ 是最后一层输出，分布和输入 embedding $e$ 差异很大。直接把 $h_t$ 塞回浅层会出现 out-of-distribution 的问题。怎么解？</p> <p>论文提出了一个很巧妙的 <strong>Input-Output Alignment</strong>：构造一个对齐矩阵 $W_a$，把 $h_t$ 映射回输入 embedding 空间：</p> \[e = h \cdot W_a, \quad \text{其中} \quad W_a \approx W_{out}^{-1} \cdot W_{in}\] <p>这里 $W_{out}$ 是 LM head，$W_{in}$ 是 token embedding 层。$W_a$ 只需要算一次，后面所有 latent step 复用。实际实现用的是 ridge regression 的闭式解来保证数值稳定性。</p> <p><strong>这个设计完全不需要训练</strong>——只用了模型自带的 $W_{in}$ 和 $W_{out}$，是一个纯推理时的操作。</p> <h3 id="32-latent-working-memory用-kv-cache-做跨-agent-通信">3.2 Latent Working Memory：用 KV Cache 做跨 Agent 通信</h3> <p>这是全文最有意思的部分。</p> <p>agent $A_1$ 完成 $m$ 步 latent thoughts 生成后，它的所有 transformer 层都积累了一整套 KV cache。这些 KV cache 不仅包含了原始输入的信息，还包含了 $m$ 步 latent reasoning 产生的新信息。论文把这整套 KV cache 定义为 agent $A_1$ 的 <strong>latent working memory</strong>：</p> \[\mathcal{M}_{A_1} = \{(K^{(l)}_{A_1,cache}, V^{(l)}_{A_1,cache}) \mid l = 1, 2, \ldots, L\}\] <p>下一个 agent $A_2$ 接手时，做一步简单操作：<strong>把 $A_1$ 的 KV cache 拼接到自己对应层的 KV cache 前面</strong>。就这样，$A_2$ 就”看到”了 $A_1$ 所有的思考过程——包括它对原始 prompt 的处理和 $m$ 步 latent reasoning 的结果。</p> <p>论文还证明了一个关键定理：<strong>通过 KV cache 传递 working memory 和直接把前序 agent 的完整输出重新 prefill 一遍，产生的结果是完全等价的（信息无损）</strong>。</p> <p>实现上，KV cache 的传递直接通过 HuggingFace Transformers 的 <code class="language-plaintext highlighter-rouge">past_key_values</code> 接口完成——<strong>不需要改任何模型代码</strong>。</p> <h3 id="33-只有最后一个-agent-需要说人话">3.3 只有最后一个 Agent 需要”说人话”</h3> <p>整个流水线里，前面所有 agent 都在隐空间里思考和传递，<strong>只有最后一个 agent 负责解码出文本答案</strong>。这大幅减少了 token 生成量和相应的解码开销。</p> <hr/> <h2 id="4-实验结果">4. 实验结果</h2> <h3 id="41-全面压制文本-mas">4.1 全面压制文本 MAS</h3> <p>如下图，在 9 个 benchmark 上（包括数学推理、常识推理、代码生成），分别用 Qwen3-4B / 8B / 14B 三种规模测试：</p> <p><img src="/assets/img/posts/latentmas/001_Figure_1.png" alt="LatentMAS 在 9 个 benchmark 上的表现"/></p> <p>几个关键数字：</p> <ul> <li><strong>准确率</strong>：比单模型平均提升 13.3-14.6%，比 TextMAS 平均提升 2.8-4.6%</li> <li><strong>推理速度</strong>：比 TextMAS 快 <strong>4×-4.3×</strong>（即使 TextMAS 已经用了 vLLM 加速）</li> <li><strong>Token 用量</strong>：减少 <strong>70.8%-83.7%</strong></li> </ul> <p>下面这张效率对比图更直观：</p> <p><img src="/assets/img/posts/latentmas/004_Figure_4.png" alt="LatentMAS 的效率增益"/></p> <p>左图是推理速度对比，右图是 token 用量对比。注意 TextMAS 的 baseline 已经用了 vLLM 做加速了，LatentMAS 还能在这个基础上快 2.6×-7×。</p> <p>在推理密集型任务（AIME24/25、GPQA-Diamond）上，TextMAS 动辄需要 20K+ token 来完成完整的 text-based CoT，而 LatentMAS 只用不到 50 步 latent step 就能达到同等甚至更好的效果。</p> <h3 id="42-latent-thoughts-是否真的在思考">4.2 Latent Thoughts 是否真的在”思考”？</h3> <p>一个合理的怀疑是：latent step 生成的 hidden states 到底有没有语义？会不会只是无意义的噪声恰好歪打正着？</p> <p>论文做了一个很有说服力的分析。如下图，把 LatentMAS 生成的 hidden embeddings（红色）和 TextMAS 生成的 token embeddings（蓝色）做 t-SNE 可视化：</p> <p><img src="/assets/img/posts/latentmas/005_Figure_5.png" alt="Latent thoughts 的语义表达"/></p> <p>两个关键观察：</p> <ol> <li><strong>LatentMAS 的 hidden embeddings 和 TextMAS 的 token embeddings 覆盖了几乎相同的 embedding 区域</strong>——说明 latent thoughts 编码了和正确文本回答相同的语义</li> <li><strong>LatentMAS 的分布更广</strong>——说明 latent thoughts 比离散 token 具有更高的多样性和表达能力</li> </ol> <h3 id="43-对齐矩阵-w_a-的作用">4.3 对齐矩阵 $W_a$ 的作用</h3> <p>如下图，不做对齐的 hidden state $h_t$（橙色）和原始 input embedding $e_t$（蓝色）分布差异很大。做完 $W_a$ 对齐后的 $e_{t+1}$（绿色）重新和 $e_t$ 对齐了：</p> <p><img src="/assets/img/posts/latentmas/006_Figure_6.png" alt="Input-Output 对齐效果"/></p> <p>去掉 $W_a$ 后，下游任务准确率下降 2.3%-5.3%。</p> <h3 id="44-最佳-latent-step-深度">4.4 最佳 Latent Step 深度</h3> <p>Latent step 并不是越多越好。论文在三个任务上做了 ablation，发现 <strong>40-80 步</strong>是最佳范围。超过这个范围，性能会平台期甚至下降——过多的 latent step 可能引入冗余信息。</p> <hr/> <h2 id="5-几点个人-take">5. 几点个人 Take</h2> <p>这篇工作最让我兴奋的地方不是具体的数字提升，而是它开辟了一个新的思路：<strong>多 Agent 之间的通信介质不必是人类可读的文本，可以是模型内部的连续表示</strong>。</p> <p>几个值得关注的点：</p> <p><strong>1. 完全 training-free 这件事很 impressive</strong>。$W_a$ 的构造只用了现有的 $W_{in}$ 和 $W_{out}$，KV cache 传递用的是 HuggingFace 原生接口。没有额外参数、没有训练数据，直接即插即用。这大大降低了实际落地的门槛。</p> <p><strong>2. 但同构 agent 的假设是个限制</strong>。LatentMAS 要求所有 agent 使用相同架构的模型（same transformer layer shape），因为 KV cache 的 dimension 要对齐才能拼接。现实中，强大的 MAS 往往需要不同规模甚至不同架构的模型分工协作。论文也提到了可以用 adapter 做异构对齐，但这就引入了训练，breaking 了 training-free 的优势。</p> <p><strong>3. 和 KV cache 通信的结合值得深挖</strong>。这篇工作的 KV cache 传递策略非常朴素——全量拼接。随着 agent 数量增加和 latent step 加深，KV cache 会持续膨胀。能不能结合 KV cache 压缩（比如上一篇聊的 SemShareKV 的思路）做选择性传递？这可能是一个有意思的后续方向。</p> <p><strong>4. 对 latent reasoning 本身的理解还有空间</strong>。论文证明了 latent thoughts 的理论表达上界，但对于”模型到底在 latent step 里做了什么”这个问题，目前主要靠 t-SNE 可视化来间接验证。更深入的 mechanistic interpretability 分析会让这个 story 更完整。</p> <p>总体来说，LatentMAS 把”在隐空间里做多 agent 协作”这个想法走通了，而且效果很好。从文本通信到 latent 通信，本质上是信息传递的效率升级。这个方向和我们做 MoE 推理优化的思路有异曲同工之处——<strong>核心都是减少不必要的计算和数据搬运，把算力花在真正重要的地方</strong>。</p> <p>欢迎评论区交流，特别是做多 Agent 系统或 LLM 推理优化的同学，欢迎讨论哈哈。</p>]]></content><author><name></name></author><category term="LLM"/><category term="Multi-Agent"/><category term="推理优化"/><category term="KV Cache"/><category term="论文解读"/><summary type="html"><![CDATA[多 Agent 协作不需要说「人话」？LatentMAS 让 LLM 在隐空间里直接协作]]></summary></entry><entry><title type="html">写完 Markdown 还要手动排版？我写了个 VS Code 插件一键搞定微信公众号、知乎、小红书</title><link href="https://marsggbo.github.io/blog/2026/md2wechat/" rel="alternate" type="text/html" title="写完 Markdown 还要手动排版？我写了个 VS Code 插件一键搞定微信公众号、知乎、小红书"/><published>2026-04-27T00:00:00+00:00</published><updated>2026-04-27T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/md2wechat</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/md2wechat/"><![CDATA[<h1 id="写完-markdown-还要手动排版我写了个-vs-code-插件一键搞定微信公众号知乎小红书">写完 Markdown 还要手动排版？我写了个 VS Code 插件一键搞定微信公众号、知乎、小红书</h1> <hr/> <p>作为一个用 Markdown 写所有东西的牛马，最烦的事情不是写文章本身，而是写完之后的”排版搬运”——</p> <p>微信公众号编辑器不认 Markdown，公式渲染不了，代码块丑得没法看。知乎编辑器好一点但也有各种格式丢失。小红书更别提了，想发个技术笔记还得截图……</p> <p>每次写完一篇技术博客，光是把内容搬到各个平台就要花半小时甚至更久。<strong>写作 20 分钟，排版 2 小时</strong>，这也太离谱了。</p> <p>所以我花了一天时间（当然是和 Claude 一起），撸了一个 VS Code 插件：<strong>MD2WeChat</strong>。</p> <blockquote> <p>GitHub：<a href="https://github.com/marsggbo/md2wechat">https://github.com/marsggbo/md2wechat</a></p> </blockquote> <hr/> <h2 id="它能干什么">它能干什么？</h2> <p>一句话：<strong>在 VS Code 里写完 Markdown，一键生成微信公众号 / 知乎 / 小红书三个平台的排版内容</strong>。</p> <p><img src="/assets/img/posts/md2wechat/preview.png" alt="插件预览效果"/></p> <p>具体功能：</p> <ul> <li><strong>📋 复制微信</strong>：一键复制排版好的 HTML，直接粘贴到微信公众号编辑器，格式完美保留</li> <li><strong>📋 复制知乎</strong>：自动适配知乎的排版样式，粘贴即用</li> <li><strong>📱 导出小红书</strong>：生成适合小红书的图片截图，直接发帖</li> <li><strong>📐 LaTeX 公式</strong>：支持行内 <code class="language-plaintext highlighter-rouge">$...$</code> 和独立 <code class="language-plaintext highlighter-rouge">$$...$$</code> 公式块，基于 KaTeX 渲染，微信公众号里也能显示公式了</li> <li><strong>🖼️ 图片说明文字</strong>：Markdown 图片的 alt 文本自动渲染为居中灰色 caption</li> <li><strong>👁️ 实时预览</strong>：右侧独立预览面板，保存即刷新</li> <li><strong>🎨 修改样式</strong>：内置 CSS 编辑器，所见即所得</li> <li><strong>☁️ 上传公众号</strong>：直接上传到微信草稿箱（通过 FastPen 服务）</li> <li><strong>💾 导出 HTML</strong>：导出带完整内联样式的 HTML 文件</li> </ul> <p><strong>图片自动转 Base64 内嵌，不需要图床</strong>。这点对我来说太重要了，不用再折腾图床链接。</p> <hr/> <h2 id="怎么用">怎么用？</h2> <h3 id="安装">安装</h3> <p>目前支持两种安装方式：</p> <p><strong>方式一：VS Code 扩展市场搜索 <code class="language-plaintext highlighter-rouge">MD2WeChat</code> 一键安装</strong>（推荐）</p> <p><strong>方式二：从源码安装</strong></p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/marsggbo/md2wechat.git
<span class="nb">cd </span>md2wechat
npm <span class="nb">install</span>
<span class="c"># 在 VS Code 中打开并按 F5 启动调试</span>
</code></pre></div></div> <h3 id="使用">使用</h3> <ol> <li>在 VS Code 里打开任意 <code class="language-plaintext highlighter-rouge">.md</code> 文件</li> <li>快捷键 <code class="language-plaintext highlighter-rouge">Cmd+Shift+W</code>（macOS）/ <code class="language-plaintext highlighter-rouge">Ctrl+Shift+W</code>（Windows/Linux）</li> <li>右侧弹出预览面板，顶部工具栏选择你要的操作</li> </ol> <p>就这么简单。写完 Markdown，按一下快捷键，点「复制微信」，去公众号编辑器里 <code class="language-plaintext highlighter-rouge">Cmd+V</code>，完事。</p> <h3 id="公式效果">公式效果</h3> <p>行内公式 <code class="language-plaintext highlighter-rouge">$E = mc^2$</code> 和独立公式块都支持：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>$$
\int_{-\infty}^{\infty} e^{-x^2} dx = \sqrt{\pi}
$$
</code></pre></div></div> <p>对于我们这种写技术文章动不动就要插公式的人来说，这个功能是刚需。</p> <hr/> <h2 id="为什么不用现有的工具">为什么不用现有的工具？</h2> <p>市面上做 Markdown 转微信的工具不少，但大多数都有这些问题：</p> <ol> <li><strong>不支持公式</strong>——对技术写作者来说是硬伤</li> <li><strong>需要在线编辑器</strong>——离开了本地 Markdown 工作流</li> <li><strong>不支持多平台</strong>——只能转微信，知乎和小红书还得另外处理</li> <li><strong>图片需要图床</strong>——多一步操作多一份麻烦</li> </ol> <p>MD2WeChat 的思路就是：<strong>不要让你离开 VS Code，不要让你改变写作习惯，写完 Markdown 之后的所有事情插件帮你搞定</strong>。</p> <hr/> <p>插件刚写好，功能还在迭代中，欢迎试用和反馈。有 bug 或者 feature request 直接去 GitHub 提 issue 就行：</p> <p>👉 <a href="https://github.com/marsggbo/md2wechat">https://github.com/marsggbo/md2wechat</a></p> <p>觉得有用的话顺手给个 star 哈哈。</p>]]></content><author><name></name></author><category term="工具"/><category term="VS Code"/><category term="Markdown"/><category term="效率"/><summary type="html"><![CDATA[写完 Markdown 还要手动排版？我写了个 VS Code 插件一键搞定微信公众号、知乎、小红书]]></summary></entry><entry><title type="html">KV Cache 也能「语义共享」？SemShareKV 用 LSH 做到了</title><link href="https://marsggbo.github.io/blog/2026/semsharekv/" rel="alternate" type="text/html" title="KV Cache 也能「语义共享」？SemShareKV 用 LSH 做到了"/><published>2026-04-27T00:00:00+00:00</published><updated>2026-04-27T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/semsharekv</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/semsharekv/"><![CDATA[<h1 id="kv-cache-也能语义共享semsharekv-用-lsh-做到了">KV Cache 也能「语义共享」？SemShareKV 用 LSH 做到了</h1> <blockquote> <p>原文：<a href="https://aclanthology.org/2025.findings-ijcnlp.25.pdf">SemShareKV: Efficient KVCache Sharing for Semantically Similar Prompts via Token-Level LSH Matching</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有想过，当两个用户几乎同时向 LLM 提问，一个问的是”请总结这篇关于气候变化的报道”，另一个问的是”帮我概括一下这篇气候危机的新闻”，服务器里会发生什么？</p> <p>大概率：<strong>两份几乎一样的 KV cache 被从头算了两遍</strong>。</p> <p>这件事搁在单用户场景里不算大问题，但现实中的 LLM 推理服务是高并发的——多文档摘要、对话系统、RAG 检索……同质化 prompt 批量打进来。<strong>KV cache 的内存占用和重复计算已经成了推理吞吐量的核心瓶颈之一</strong>。</p> <p>现有的优化思路集中在两类：一是压缩单个 prompt 的 KV cache（比如 StreamingLLM、H2O 这些做 token 剪枝的），二是复用多个 prompt 之间共享的前缀（prefix caching）或频繁出现的文本片段。但这两类方案都有一个硬伤：<strong>只认”一模一样”的 token，认不出”意思相近”的 prompt</strong>。</p> <p>IJCNLP-AACL 2025 Findings 的这篇工作 <strong>SemShareKV</strong> 做了一件有意思的事：把 KV cache 的复用从”精确匹配”扩展到了”语义匹配”——用 LSH（Locality-Sensitive Hashing）在 token embedding 级别做模糊匹配，让语义相似但措辞不同的 prompt 也能共享彼此的 KV cache。</p> <p>今天想和大家聊聊这篇工作解决了什么真问题，以及它背后有哪些值得细品的设计。</p> <hr/> <h2 id="2-现有方案的局限">2. 现有方案的局限</h2> <p>先说说 prefix caching 为什么不够用。</p> <p>prefix caching 的逻辑很直接：如果两个 prompt 的开头部分 token 完全一致（比如 system prompt 一模一样），那这段的 KV cache 可以复用，不用重新计算。vLLM 的 automatic prefix caching 就是这个思路，工程上做得很成熟。</p> <p>但现实场景里，语义相似的 prompt 往往在措辞上差异很大：</p> <ul> <li>“请总结这篇新闻” vs. “帮我概括一下这篇报道”</li> <li>用户把同一篇文章换了个说法重新问一遍</li> <li>RAG 检索召回的文档块，内容重叠但 token 序列不同</li> </ul> <p>这时候 prefix caching 完全失效，只能从头算。<strong>问题的本质是：现有的 cache 复用依赖于 token 级别的精确匹配，而”语义相似”和”字面相同”之间有一道鸿沟</strong>。</p> <p>SemShareKV 要做的，就是跨过这道鸿沟。</p> <hr/> <h2 id="3-三个关键洞察">3. 三个关键洞察</h2> <p>SemShareKV 能行得通，背后有三个实验观察作为支撑，我觉得这部分是全文最有意思的地方。</p> <h3 id="31-高偏差-token-的排名在不同层之间高度一致">3.1 高偏差 token 的排名在不同层之间高度一致</h3> <p>如下图，作者统计了不同模型（LLaMA3.1-8B、Mistral-7B、MPT-7B）相邻层之间”偏差 token”排名的 Spearman 相关系数：</p> <p><img src="/assets/img/posts/semsharekv/figure2_p3.png" alt="相邻层高偏差 token 排名的 Spearman 相关系数"/></p> <p><strong>Spearman 相关系数普遍在 0.85 以上，中间层甚至接近 0.95</strong>。</p> <p>这意味着什么？<strong>你只需要在浅层识别出”哪些 token 比较重要”，这个判断在深层依然成立</strong>。不用每一层都重新计算一遍重要性排序，省了大量 overhead。</p> <h3 id="32-越深的层关注的-token-越少">3.2 越深的层关注的 token 越少</h3> <p>如下图，随着层深度增加，attention 实际上只集中在越来越少的 token 上：</p> <p><img src="/assets/img/posts/semsharekv/figure3_p3.png" alt="越深的层关注的 token 越少"/></p> <p>这个现象很多做 KV cache 压缩的工作都观察到过。深层的 attention 更”挑剔”，只盯着少数关键 token。这意味着深层的 KV cache 有更大的压缩空间。</p> <h3 id="33-越深的层包含越多冗余信息">3.3 越深的层包含越多冗余信息</h3> <p>如下图，作者测量了不同层 KV cache 的”冗余度”（相邻层之间的相似度）：</p> <p><img src="/assets/img/posts/semsharekv/figure4_p4.png" alt="越深的层包含越多冗余信息"/></p> <p>深层 KV cache 和上一层的差异越来越小，<strong>很多计算都是在做重复的事情</strong>。</p> <p>把这三个洞察合在一起：重要 token 的集合是稳定的，深层关注更少的 token，深层信息更冗余。这给了 SemShareKV 一个操作空间：<strong>可以只复用”重要 token”对应的 KV cache，略去冗余部分，损失可控</strong>。</p> <hr/> <h2 id="4-semsharekv-怎么做">4. SemShareKV 怎么做</h2> <p>系统的整体流程如下图：</p> <p><img src="/assets/img/posts/semsharekv/figure1_p3.png" alt="SemShareKV 系统总体架构"/></p> <p>核心步骤分三块：LSH 模糊匹配、RoPE 的重新处理、KV cache 重排与复用。</p> <h3 id="41-lsh-做语义级别的-token-匹配">4.1 LSH 做语义级别的 token 匹配</h3> <p>传统 prefix caching 用的是精确的 token ID 匹配。SemShareKV 换了个思路：<strong>在 token embedding 空间里做近似最近邻搜索，用 LSH 找到语义相近的 token 对</strong>。</p> <p>具体做法：对每个 token，用 LSH 把它的 embedding 映射成一个 hash，语义相近的 token 有很高概率被映射到同一个 hash bucket 里。新 prompt 进来之后，先对所有 token 做 LSH 编码，和 cache 里已有的 reference prompt 的 token hash 做匹配，找出配对。</p> <p>这样，即便新 prompt 和已缓存的 prompt 一个字都不一样，<strong>只要语义足够接近，对应的 token 就能被匹配上，KV cache 就能复用</strong>。</p> <h3 id="42-rope-的巧妙处理">4.2 RoPE 的巧妙处理</h3> <p>这里有个工程上的坑——KV cache 复用时，位置信息怎么处理？</p> <p>标准的 LLM 在生成 K 时会把 RoPE（Rotary Position Embedding）加进去，position encoding 是和 token 位置绑定的。如果直接把 reference prompt 的 K cache 搬过来用，位置信息就乱了——因为匹配上的 token 在两个 prompt 里位置不同。</p> <p>SemShareKV 的解决方案如下图：</p> <p><img src="/assets/img/posts/semsharekv/figure5_p5.png" alt="修改后的 KV cache 存储方式"/></p> <p>左边是标准流程：RoPE 在存入 cache 之前就已经加到 K 上了。右边是修改后的流程：<strong>K cache 里存的是加 RoPE 之前的原始 K 值，position encoding 单独存</strong>。</p> <p>这样，复用 cache 时可以按照新 prompt 里的 token 位置重新应用 RoPE，而不是硬搬 reference prompt 的位置信息过来。位置准了，后续的 attention 计算才对。</p> <h3 id="43-kv-cache-重排与复用">4.3 KV cache 重排与复用</h3> <p>匹配完 token 之后，要做一次重排（Rearranged Cache）：把 reference prompt 的 KV cache 按照匹配关系重新排列，让它对应到新 prompt 的 token 位置上。</p> <p>匹配上的 token，直接复用重排后的 KV cache；没匹配上的 token，走正常的 attention 计算，重新算 KV。</p> <p><strong>核心收益就在这里：减少了需要重新计算的 token 数量，越相似的两个 prompt，复用率越高，省的算力越多。</strong></p> <hr/> <h2 id="5-实验结果">5. 实验结果</h2> <h3 id="51-质量在多个摘要数据集上几乎无损">5.1 质量：在多个摘要数据集上几乎无损</h3> <p>如下表，在 MultiNews、Wikihow、SAMSum、PubMed、BookSum、BigPatent、LCC 等 8 个摘要数据集上，分别用 Mistral-7B 和 LLaMA3.1-8B 做测试：</p> <p><img src="/assets/img/posts/semsharekv/table1_p7.png" alt="性能对比表格"/></p> <p>SemShareKV 在大多数数据集上的 ROUGE-L 和 baseline 相差不大，部分数据集甚至略有提升（匹配到的 KV cache 相当于引入了额外的”上下文信息”）。质量损失在可接受范围内。</p> <h3 id="52-效率最高-625-加速42-显存节省">5.2 效率：最高 6.25× 加速，42% 显存节省</h3> <p>这是最直接的收益。在 5k tokens 长输入场景下：</p> <ul> <li><strong>推理速度最高提升 6.25×</strong></li> <li><strong>GPU 显存使用减少 42%</strong></li> </ul> <p>数字不算夸张，但对于长文档摘要、高并发推理服务这类场景来说，这个量级的提升已经相当可观了。</p> <h3 id="53-prompt-相似度的影响">5.3 Prompt 相似度的影响</h3> <p>如下图，不出意料，两个 prompt 越相似，匹配率越高，加速效果越好：</p> <p><img src="/assets/img/posts/semsharekv/figure9_p8.png" alt="Prompt 相似度对效果的影响"/></p> <p>实线（Replacement，用 reference 的 KV 替换）和虚线（Elimination，直接删掉未匹配 token）相比，Replacement 策略在高替换比例下降得更慢——说明直接复用 KV 比暴力删 token 更温和。</p> <h3 id="54-消融实验">5.4 消融实验</h3> <p>如下表，消融实验验证了三个设计选择的必要性：LSH 匹配、RoPE 处理方式、层级选择策略：</p> <p><img src="/assets/img/posts/semsharekv/table2_p8.png" alt="消融实验结果"/></p> <p>去掉任何一个组件，ROUGE-L 都有可见的下降。</p> <hr/> <h2 id="6-几点个人想法">6. 几点个人想法</h2> <p>这篇工作解决的场景非常实际——<strong>语义相似但 token 不同的 prompt 之间的 KV cache 复用</strong>，是 prefix caching 一直以来的盲区。用 LSH 做 token 级别的模糊匹配，这个方向本身很有意思，工程上也可以落地。</p> <p>但也有几个地方值得继续打磨：</p> <ol> <li> <p><strong>LSH 匹配本身有开销</strong>。对每个 token 做 hash 并查找 reference prompt，在高并发场景下也是一笔算力支出，文章里对这块的分析不够细。</p> </li> <li> <p><strong>Reference prompt 怎么选</strong>？当 cache 里积累了大量历史 prompt 后，如何快速找到最合适的 reference，是个工程上的检索问题，文章里没有深入讨论。</p> </li> <li> <p><strong>动态 prompt 场景的泛化能力</strong>。论文主要在摘要任务上测，摘要的 prompt 相似度天然偏高，泛化到其他场景（比如代码生成、多轮对话）效果如何，需要更多验证。</p> </li> </ol> <p>总体来说，这是一篇方向清晰、动机充分的工程论文，把”语义级别的 KV cache 共享”这个想法走通了。KV cache 优化这个方向还有很大空间，期待后续有更多工作在”跨请求复用”上继续挖。</p> <p>欢迎评论区交流，有问题也可以指出来哈哈。</p>]]></content><author><name></name></author><category term="LLM"/><category term="KV Cache"/><category term="推理优化"/><category term="论文解读"/><summary type="html"><![CDATA[KV Cache 也能「语义共享」？SemShareKV 用 LSH 做到了]]></summary></entry><entry><title type="html">NSDI26 | DroidSpeak让不同 LLM 之间共享 KV Cache - marsggbo</title><link href="https://marsggbo.github.io/blog/2026/nsdi26-droidspeak-llm-kv-cache-marsggbo/" rel="alternate" type="text/html" title="NSDI26 | DroidSpeak让不同 LLM 之间共享 KV Cache - marsggbo"/><published>2026-04-26T07:11:00+00:00</published><updated>2026-04-26T07:11:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/nsdi26--droidspeak-llm--kv-cache---marsggbo</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/nsdi26-droidspeak-llm-kv-cache-marsggbo/"><![CDATA[]]></content><author><name></name></author><summary type="html"><![CDATA[原文：DroidSpeak: KV Cache Sharing for Cross-LLM Communication and Multi-LLM Serving 1. 前言：一个很自然但从没人解决过的问题 KV Cache 共享这件事，在单个模型的场景里已经做得很成熟了——vLLM 的 prefi]]></summary></entry></feed>