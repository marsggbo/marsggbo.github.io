<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://marsggbo.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://marsggbo.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-08-14T14:27:38+00:00</updated><id>https://marsggbo.github.io/feed.xml</id><title type="html">HE Xin</title><subtitle>&quot;SSS: Study, Sleep, Slim&quot; </subtitle><entry><title type="html">ICML’26 | DFlash：用 Diffusion 模型干掉 AR Drafter，投机解码提速 6×</title><link href="https://marsggbo.github.io/blog/2026/20260814-dflash/" rel="alternate" type="text/html" title="ICML’26 | DFlash：用 Diffusion 模型干掉 AR Drafter，投机解码提速 6×"/><published>2026-08-14T00:00:00+00:00</published><updated>2026-08-14T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260814-dflash</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260814-dflash/"><![CDATA[<h1 id="icml26--dflash用-diffusion-模型干掉-ar-drafter投机解码提速-6">ICML’26 | DFlash：用 Diffusion 模型干掉 AR Drafter，投机解码提速 6×</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2602.06036">DFlash: Block Diffusion for Flash Speculative Decoding</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>投机解码（Speculative Decoding）的思路很简单：用一个小模型快速草拟几个候选 token，然后扔给大模型并行验证——验证是 free 的，因为大模型本来就支持并行 prefill。</p> <p>问题出在 drafting 这一步。现在主流方案（比如 EAGLE-3）用的是轻量 AR 头，一次只生成 1 个 token，想草拟 16 个就得跑 16 次串行 forward。序列越长，drafting 延迟越高，慢慢地就把验证那边省下来的时间给吃掉了。</p> <p>DFlash（ICML 2026）给了一个很直接的解法：<strong>把 AR drafter 换成 Diffusion 模型</strong>，整个 block 的 token 一次 forward pass 全部并行生成，drafting latency 不再随生成长度增长。再配合从目标模型注入 context feature，草稿质量也上来了。</p> <p>最终在 Qwen3 系列上，对比 AR baseline 加速 <strong>超过 6×</strong>，比 EAGLE-3（树大小 60）还快 <strong>2.5×</strong>。</p> <hr/> <h2 id="2-为什么换成-diffusion">2. 为什么换成 Diffusion</h2> <p>先把两种 drafter 的本质区别说清楚。</p> <p><strong>AR drafter</strong> 的生成过程：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>t=0: 已知 [x₁, x₂, x₃]（prompt），预测 x₄
t=1: 已知 [x₁, x₂, x₃, x₄]，预测 x₅
t=2: 已知 [x₁, ..., x₅]，预测 x₆
...（草拟 γ 个 token 需要 γ 次 forward）
</code></pre></div></div> <p><strong>Diffusion drafter</strong> 的生成过程：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>初始化：把 γ 个待生成位置全部设为 &lt;mask&gt;
       输入 = [x₁, x₂, x₃, &lt;mask&gt;, &lt;mask&gt;, &lt;mask&gt;]
一次 forward：同时预测 x₄, x₅, x₆ 的 logits
              γ 个 token，1 次 forward
</code></pre></div></div> <p>代价是 block 内部 token 之间没有自回归依赖——预测 x₆ 时不知道 x₄、x₅ 是什么，只能靠全局 context 盲猜。但 DFlash 用 target context feature 注入来弥补这个缺陷（后面展开）。</p> <p>如下图，5 层 DFlash 草拟 16 个 token 只需 ~5ms，而 1 层 EAGLE-3 草拟 16 个 token 需要 ~26ms：</p> <p><img src="/assets/img/posts/20260814-dflash/figure3_p3.png" alt="EAGLE-3 vs DFlash drafting 延迟对比"/></p> <p>这个延迟优势意味着 DFlash 可以在不超过 latency budget 的前提下，用更深的 drafter（更高质量），这就是它能超过 AR drafter 的根本原因。</p> <hr/> <h2 id="3-核心设计从目标模型注入-context-feature">3. 核心设计：从目标模型注入 Context Feature</h2> <p>Block diffusion 单靠自己能做到的 acceptance length 很有限——论文里专门验证过，不加任何 conditioning 的 5 层 diffusion drafter，speedup 只有 2-3×。</p> <p>原因直接：drafter 必须在不知道目标模型内部状态的情况下凭空预测未来 token，等于从零开始猜。</p> <p>DFlash 的解法：<strong>在目标模型 prefill 的时候，把它中间层的 hidden state 抽出来，注入到 drafter 的每一层 KV cache 里</strong>。</p> <h3 id="31-提取-target-context-feature">3.1 提取 Target Context Feature</h3> <p>目标模型跑 prefill 时，从若干中间层（均匀采样 5 层，从第 2 层到倒数第 3 层）抽取 hidden state，拼接后过一个轻量投影层：</p> \[H_t = \text{RMSNorm}\left(W_c[H^{(l_1)}; \ldots; H^{(l_5)}]\right)\] <p>$W_c \in \mathbb{R}^{D \times 5D}$，把多层信息压缩到 draft 模型的 hidden dimension，得到 context feature $H_t$。</p> <p>用 PyTorch 写大概是这样：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">ContextFeatureExtractor</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">target_hidden</span><span class="p">,</span> <span class="n">draft_hidden</span><span class="p">,</span> <span class="n">num_layers</span><span class="o">=</span><span class="mi">5</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">proj</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">target_hidden</span> <span class="o">*</span> <span class="n">num_layers</span><span class="p">,</span> <span class="n">draft_hidden</span><span class="p">,</span> <span class="n">bias</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">norm</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">RMSNorm</span><span class="p">(</span><span class="n">draft_hidden</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">hidden_states</span><span class="p">:</span> <span class="nb">list</span><span class="p">[</span><span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">])</span> <span class="o">-&gt;</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">:</span>
        <span class="c1"># hidden_states: list of [seq_len, target_hidden], len=num_layers
</span>        <span class="n">fused</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">(</span><span class="n">hidden_states</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>   <span class="c1"># [seq_len, target_hidden * num_layers]
</span>        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">norm</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">proj</span><span class="p">(</span><span class="n">fused</span><span class="p">))</span>          <span class="c1"># [seq_len, draft_hidden]
</span></code></pre></div></div> <p>这个 context feature 只需要计算一次，之后所有 drafting iteration 都复用。</p> <h3 id="32-kv-injection注入到每一层">3.2 KV Injection：注入到每一层</h3> <p>拿到 $H_t$ 后，不是把它和 draft token embedding 拼在一起作为输入（EAGLE-3 的做法），而是直接注入到 drafter 每一层的 KV 里：</p> <p>\(Q_i = W^Q_i H_d\) \(K_i = [W^K_i H_t;\ W^K_i H_d]_{\text{seq}}\) \(V_i = [W^V_i H_t;\ W^V_i H_d]_{\text{seq}}\)</p> <p>$H_d$ 是 draft token 的 hidden state，$[\ ;\ ]_{\text{seq}}$ 表示在 sequence 维度拼接。</p> <p>用 PyTorch 写大概是这样：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">DraftAttentionLayer</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">h_draft</span><span class="p">,</span> <span class="n">h_ctx</span><span class="p">,</span> <span class="n">attn_mask</span><span class="o">=</span><span class="bp">None</span><span class="p">):</span>
        <span class="c1"># h_draft: [batch, γ, d]  ← γ 个 mask token 的 hidden states
</span>        <span class="c1"># h_ctx:   [batch, N, d]  ← target context feature（整个序列）
</span>
        <span class="n">Q</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">wq</span><span class="p">(</span><span class="n">h_draft</span><span class="p">)</span>                      <span class="c1"># [batch, γ, d]
</span>
        <span class="n">K_ctx</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">wk</span><span class="p">(</span><span class="n">h_ctx</span><span class="p">)</span>                    <span class="c1"># [batch, N, d]
</span>        <span class="n">K_draft</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">wk</span><span class="p">(</span><span class="n">h_draft</span><span class="p">)</span>                <span class="c1"># [batch, γ, d]
</span>        <span class="n">K</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">([</span><span class="n">K_ctx</span><span class="p">,</span> <span class="n">K_draft</span><span class="p">],</span> <span class="n">dim</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>    <span class="c1"># [batch, N+γ, d]
</span>
        <span class="n">V_ctx</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">wv</span><span class="p">(</span><span class="n">h_ctx</span><span class="p">)</span>
        <span class="n">V_draft</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">wv</span><span class="p">(</span><span class="n">h_draft</span><span class="p">)</span>
        <span class="n">V</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">([</span><span class="n">V_ctx</span><span class="p">,</span> <span class="n">V_draft</span><span class="p">],</span> <span class="n">dim</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>    <span class="c1"># [batch, N+γ, d]
</span>
        <span class="c1"># bidirectional attention：block 内部 mask token 互相可见
</span>        <span class="n">out</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">scaled_dot_product_attention</span><span class="p">(</span><span class="n">Q</span><span class="p">,</span> <span class="n">K</span><span class="p">,</span> <span class="n">V</span><span class="p">,</span> <span class="n">attn_mask</span><span class="o">=</span><span class="n">attn_mask</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">wo</span><span class="p">(</span><span class="n">out</span><span class="p">)</span>                       <span class="c1"># [batch, γ, d]
</span></code></pre></div></div> <p>关键点：<strong>draft token 只产生 Query，context feature 只作为额外的 KV entry</strong>，不走 Q、output projection、FFN，只相当于给每层的 attention 提供了更丰富的 key-value 池。</p> <p>这样无论 drafter 有多少层，每一层都能直接 attend 到目标模型的 context feature，信息不会随深度衰减。</p> <p>如下图，DFlash 的整体推理设计（可以看到 context feature 如何注入进 KV cache）：</p> <p><img src="/assets/img/posts/20260814-dflash/figure2_full.png" alt="DFlash 推理设计：context feature 注入 KV cache"/></p> <h3 id="33-一次-forward-完成-block-预测">3.3 一次 Forward 完成 Block 预测</h3> <p>有了 context feature 之后，整个 drafting 过程是：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">draft_block</span><span class="p">(</span><span class="n">anchor_token</span><span class="p">,</span> <span class="n">ctx_feature</span><span class="p">,</span> <span class="n">drafter</span><span class="p">,</span> <span class="n">block_size</span><span class="o">=</span><span class="mi">16</span><span class="p">):</span>
    <span class="c1"># anchor_token: 目标模型上一轮验证后产生的最后一个 token
</span>    <span class="c1"># ctx_feature:  从目标模型 prefill 时提取的 context feature
</span>
    <span class="c1"># 初始化：anchor + γ 个 mask token
</span>    <span class="n">mask_token_id</span> <span class="o">=</span> <span class="n">tokenizer</span><span class="p">.</span><span class="n">mask_token_id</span>
    <span class="n">draft_input</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">([</span>
        <span class="n">anchor_token</span><span class="p">.</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="mi">1</span><span class="p">),</span>                              <span class="c1"># [B, 1]
</span>        <span class="n">torch</span><span class="p">.</span><span class="nf">full</span><span class="p">((</span><span class="n">B</span><span class="p">,</span> <span class="n">block_size</span><span class="p">),</span> <span class="n">mask_token_id</span><span class="p">)</span>              <span class="c1"># [B, γ]
</span>    <span class="p">],</span> <span class="n">dim</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>                                                   <span class="c1"># [B, 1+γ]
</span>
    <span class="c1"># embed → drafter forward（bidirectional attention within block）
</span>    <span class="n">h</span> <span class="o">=</span> <span class="n">drafter</span><span class="p">.</span><span class="nf">embed</span><span class="p">(</span><span class="n">draft_input</span><span class="p">)</span>                             <span class="c1"># [B, 1+γ, d]
</span>    <span class="k">for</span> <span class="n">layer</span> <span class="ow">in</span> <span class="n">drafter</span><span class="p">.</span><span class="n">layers</span><span class="p">:</span>
        <span class="n">h</span> <span class="o">=</span> <span class="nf">layer</span><span class="p">(</span><span class="n">h</span><span class="p">,</span> <span class="n">ctx_feature</span><span class="p">)</span>                              <span class="c1"># KV injection
</span>
    <span class="c1"># 只取 mask 位置的 logits
</span>    <span class="n">logits</span> <span class="o">=</span> <span class="n">drafter</span><span class="p">.</span><span class="nf">lm_head</span><span class="p">(</span><span class="n">h</span><span class="p">[:,</span> <span class="mi">1</span><span class="p">:,</span> <span class="p">:])</span>                     <span class="c1"># [B, γ, V]
</span>
    <span class="c1"># 并行采样（或 argmax）
</span>    <span class="n">draft_tokens</span> <span class="o">=</span> <span class="n">logits</span><span class="p">.</span><span class="nf">argmax</span><span class="p">(</span><span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>                       <span class="c1"># [B, γ]
</span>    <span class="k">return</span> <span class="n">draft_tokens</span>
</code></pre></div></div> <p>整个 block 的 γ 个 token 一次 forward 搞定，无论 γ 多大延迟都不变。</p> <hr/> <h2 id="4-训练block-masking">4. 训练：Block Masking</h2> <p>推理时 drafter 拿到的是全部 mask 的 block，训练时也要对齐这个行为。</p> <p>如下图，DFlash 的训练时 attention mask：</p> <p><img src="/assets/img/posts/20260814-dflash/figure4_p5.png" alt="DFlash 训练时的 attention mask 设计"/></p> <p>具体做法：</p> <ol> <li>从 response 序列里随机采样 anchor token 位置</li> <li>以 anchor 为起点，把接下来 <code class="language-plaintext highlighter-rouge">block_size - 1</code> 个 token 随机 mask 掉一部分（绿色）</li> <li>drafter 的任务：给定 anchor + 剩余可见 token + context feature，预测所有 mask 位置</li> </ol> <p>训练时多个 block 拼在一起，用 block 内部双向 attention + 跨 block 单向 causal mask（Flex Attention 实现），可以在一次 forward + backward 里处理整个长序列，效率很高。</p> <p>损失函数带一个位置衰减权重 $w_k = e^{-(k-1)/\gamma}$，让靠前的位置有更高的训练权重——因为前几个 token 被接受的概率更高，对整体 acceptance length 的贡献更大：</p> \[\mathcal{L} = -\sum_{k=1}^{\gamma} w_k \log p_d(x_k^*)\] <hr/> <h2 id="5-实验结果">5. 实验结果</h2> <h3 id="51-主结果">5.1 主结果</h3> <p>如下表，在 Qwen3-4B/8B（thinking 模式关闭）上，DFlash 对比 AR baseline 平均加速 <strong>4.9×（greedy）</strong>，在 Math 任务上 acceptance length τ 最高达到 <strong>7.87</strong>。</p> <p>EAGLE-3 用了树大小 60（验证 overhead 很大），DFlash 只用 block size 16，speedup 反而更高：</p> <p><img src="/assets/img/posts/20260814-dflash/table1_full.png" alt="主结果表：DFlash vs EAGLE-3 speedup 对比"/></p> <h3 id="52-为什么-diffusion-能超过-ar">5.2 为什么 Diffusion 能超过 AR？</h3> <p>这里有个看起来反直觉的地方：diffusion 没有 block 内依赖，质量理论上比 AR 差，为什么 acceptance length 更高？</p> <p>根本原因是 <strong>latency budget 的不同分配</strong>：</p> <ul> <li>EAGLE-3 草拟 16 个 token 需要 16 次 forward，延迟 ~26ms，只能用浅层 drafter（否则更慢）</li> <li>DFlash 草拟 16 个 token 只需 1 次 forward，延迟 ~5ms，可以用 5 层更深的 drafter</li> </ul> <p>同样的 latency budget 下，DFlash 能用更大、更深的模型，综合质量反而更高。这是典型的”用延迟换质量”的 Pareto 优化。</p> <hr/> <h2 id="6-个人-take">6. 个人 Take</h2> <p>DFlash 抓住了一个真正的系统瓶颈：之前大家都在想怎么让 drafter 预测得更准（更大的 tree、更好的 AR 头），但核心矛盾是<strong>串行 drafting 的延迟随草拟长度线性增长</strong>，从根本上限制了能草拟多少 token。</p> <p>换成 diffusion 之后，drafting latency 和草拟长度解耦，这个约束就没了。可以放心堆更深的 drafter、更大的 block，在相同延迟下拿到更高的 acceptance length。</p> <p>当然，diffusion drafter 的训练比 AR 复杂——block masking 设计、context conditioning、position-weighted loss，每个细节都有讲究，开箱复现的成本不低。目前结果也主要在 Qwen3 系列上，跨架构的泛化性还需要更多验证。</p> <p>但”用并行替换串行”这个方向是对的，DFlash 算是把这条路走通的第一个工作。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 LLM 推理效率优化你想系统深入，可以看看我之前出版的<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，书里有专章讲 LLM 推理效率和参数高效微调，和本文的工程背景有直接关联。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="投机解码"/><category term="Diffusion"/><category term="推理优化"/><category term="论文解读"/><summary type="html"><![CDATA[ICML’26 | DFlash：用 Diffusion 模型干掉 AR Drafter，投机解码提速 6×]]></summary></entry><entry><title type="html">DSpark：DeepSeek 线上跑的投机解码，半自回归 + 置信度调度怎么做到的</title><link href="https://marsggbo.github.io/blog/2026/20260814-dspark/" rel="alternate" type="text/html" title="DSpark：DeepSeek 线上跑的投机解码，半自回归 + 置信度调度怎么做到的"/><published>2026-08-14T00:00:00+00:00</published><updated>2026-08-14T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260814-dspark</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260814-dspark/"><![CDATA[<h1 id="dsparkdeepseek-线上跑的投机解码半自回归--置信度调度怎么做到的">DSpark：DeepSeek 线上跑的投机解码，半自回归 + 置信度调度怎么做到的</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2607.05147">DSpark: Confidence-Scheduled Speculative Decoding with Semi-Autoregressive Generation</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有想过，投机解码的加速上限到底在哪？</p> <p>一个简单的分析：每轮 decoding 的 per-token 延迟是</p> \[L = \frac{T_\text{draft} + T_\text{verify}}{\tau}\] <p>$\tau$ 是平均 acceptance length，分子是每轮花的时间，分母是每轮接受的 token 数。想让 $L$ 小，要么压分子（更快的 draft/verify），要么拉 $\tau$。</p> <p>现有方案各有问题：</p> <ul> <li><strong>AR drafter</strong>（如 EAGLE-3）：$\tau$ 高，但 $T_\text{draft} \propto \gamma$（串行，草拟 token 数越多越慢），被迫用小 $\gamma$</li> <li><strong>Parallel drafter</strong>（如 DFlash）：$T_\text{draft}$ 极低（一次 forward），但 $\tau$ 会随 block 内位置衰减——位置越靠后，没有前序 token 的依赖，预测越离谱，acceptance rate 快速下跌</li> </ul> <p>这个”位置越靠后越不准”的现象有个名字：<strong>suffix decay</strong>。</p> <p>DSpark 是 DeepSeek 在自己的 V4 生产系统上实际部署的方案。它的核心思路是：<strong>parallel drafter 的低延迟要保留，但要给 block 内部引入序列依赖来修复 suffix decay；同时，不是所有草稿 token 都值得送给大模型验证——引入置信度调度，按 token 质量和系统负载动态裁减验证长度。</strong></p> <hr/> <h2 id="2-suffix-decay并行-drafter-的问题在哪">2. Suffix Decay：并行 Drafter 的问题在哪</h2> <p>先把问题讲清楚。</p> <p>假设你要草拟一个 block <code class="language-plaintext highlighter-rouge">[x₁, x₂, x₃, x₄]</code>，并行 drafter 一次 forward 同时预测这 4 个位置：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>并行预测：
x₁ = argmax P(x₁ | context)         ← 只依赖 context，还好
x₂ = argmax P(x₂ | context)         ← 不知道 x₁ 是什么！
x₃ = argmax P(x₃ | context)         ← 不知道 x₁, x₂ 是什么！
x₄ = argmax P(x₄ | context)         ← 完全盲猜 x₁-x₃
</code></pre></div></div> <p>举个极端的例子，假设语言模型在这个位置有两种等可能的续写：</p> <ul> <li><code class="language-plaintext highlighter-rouge">"of course"</code></li> <li><code class="language-plaintext highlighter-rouge">"no problem"</code></li> </ul> <p>并行 drafter 可能给出 <code class="language-plaintext highlighter-rouge">"of problem"</code> 或 <code class="language-plaintext highlighter-rouge">"no course"</code>——每个位置独立取最高概率，但组合起来逻辑不通，目标模型一验证就拒掉。</p> <p>如下图，对 DFlash（蓝色，纯并行）和 DSpark（绿色）在不同 draft 位置的条件 acceptance rate 做对比：</p> <p><img src="/assets/img/posts/20260814-dspark/figure2_p12.png" alt="位置条件 acceptance rate 对比"/></p> <p>蓝色曲线在 Chat 任务上非常典型——第 1 个位置还不错，第 4、5 个位置就明显下跌。DSpark 的绿色曲线则保持平稳甚至略有上升。原因正是加入了序列依赖。</p> <hr/> <h2 id="3-semi-autoregressive-架构">3. Semi-Autoregressive 架构</h2> <p>DSpark 的 drafter 分两阶段，如下图：</p> <p><img src="/assets/img/posts/20260814-dspark/figure1_p5.png" alt="DSpark 整体架构和 decoding 循环"/></p> <h3 id="31-parallel-stage骨干并行生成">3.1 Parallel Stage：骨干并行生成</h3> <p>第一阶段直接用 DFlash 作为 parallel backbone，一次 forward 并行生成整个 block 的 hidden states 和 base logits：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>输入：anchor token x₀ + γ 个 &lt;mask&gt;
一次 forward → h₁, ..., hᵧ（hidden states）
              U₁, ..., Uᵧ（base logits，shape: [γ, V]）
</code></pre></div></div> <p>这一步 $T_\text{draft}$ 极低，不随 $\gamma$ 增长。</p> <h3 id="32-sequential-stage轻量序列头修正">3.2 Sequential Stage：轻量序列头修正</h3> <p>第二阶段是一个很轻量的模块，从左到右依次给每个位置的 logits 加上一个”转移偏置” $B_k$，让第 $k$ 个 token 的预测依赖前面已经采样出来的 token：</p> \[p_k(v \mid x_0, x_{&lt;k}) = \frac{\exp(U_k(v) + B_k(x_0, x_{&lt;k}, v))}{\sum_{u \in \mathcal{V}} \exp(U_k(u) + B_k(x_0, x_{&lt;k}, u))}\] <p>$U_k$ 是 parallel stage 给出的 base logit（已经有对全局 context 的感知），$B_k$ 是对 intra-block 依赖的修正。</p> <p>两种实现：</p> <p><strong>Markov Head（默认）</strong>：最简单，只依赖前一个 token，低秩分解 $B = W_1 W_2$：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">MarkovHead</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">vocab_size</span><span class="p">,</span> <span class="n">rank</span><span class="o">=</span><span class="mi">256</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">W1</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Embedding</span><span class="p">(</span><span class="n">vocab_size</span><span class="p">,</span> <span class="n">rank</span><span class="p">)</span>   <span class="c1"># 上一个 token -&gt; rank 维向量
</span>        <span class="n">self</span><span class="p">.</span><span class="n">W2</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">rank</span><span class="p">,</span> <span class="n">vocab_size</span><span class="p">,</span> <span class="n">bias</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>  <span class="c1"># rank -&gt; V
</span>
    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">prev_token_id</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">:</span>
        <span class="c1"># prev_token_id: [batch]
</span>        <span class="n">embed</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nc">W1</span><span class="p">(</span><span class="n">prev_token_id</span><span class="p">)</span>   <span class="c1"># [batch, rank]
</span>        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nc">W2</span><span class="p">(</span><span class="n">embed</span><span class="p">)</span>            <span class="c1"># [batch, V]  ← 转移偏置 B(x_{k-1}, ·)
</span></code></pre></div></div> <p><strong>RNN Head</strong>：维护一个跨位置的 recurrent state $s_k$，能看到整个 prefix 历史：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">RNNHead</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">vocab_size</span><span class="p">,</span> <span class="n">backbone_dim</span><span class="p">,</span> <span class="n">rank</span><span class="o">=</span><span class="mi">256</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">W1</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Embedding</span><span class="p">(</span><span class="n">vocab_size</span><span class="p">,</span> <span class="n">rank</span><span class="p">)</span>
        <span class="c1"># 单个 linear 拆成 gate / candidate / output 三部分
</span>        <span class="n">self</span><span class="p">.</span><span class="n">Wgco</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="mi">2</span> <span class="o">*</span> <span class="n">rank</span> <span class="o">+</span> <span class="n">backbone_dim</span><span class="p">,</span> <span class="mi">3</span> <span class="o">*</span> <span class="n">rank</span><span class="p">,</span> <span class="n">bias</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">W2</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">rank</span><span class="p">,</span> <span class="n">vocab_size</span><span class="p">,</span> <span class="n">bias</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">step</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">s_prev</span><span class="p">,</span> <span class="n">prev_token_id</span><span class="p">,</span> <span class="n">h_k</span><span class="p">):</span>
        <span class="c1"># s_prev: [batch, rank]，h_k: [batch, backbone_dim]
</span>        <span class="n">z</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">([</span><span class="n">s_prev</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="nc">W1</span><span class="p">(</span><span class="n">prev_token_id</span><span class="p">),</span> <span class="n">h_k</span><span class="p">],</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>  <span class="c1"># [batch, 2r+d]
</span>        <span class="n">gates</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nc">Wgco</span><span class="p">(</span><span class="n">z</span><span class="p">).</span><span class="nf">chunk</span><span class="p">(</span><span class="mi">3</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>   <span class="c1"># gate, cand, out 各 [batch, rank]
</span>        <span class="n">g</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">sigmoid</span><span class="p">(</span><span class="n">gates</span><span class="p">[</span><span class="mi">0</span><span class="p">])</span>
        <span class="n">s_k</span> <span class="o">=</span> <span class="n">g</span> <span class="o">*</span> <span class="n">s_prev</span> <span class="o">+</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="n">g</span><span class="p">)</span> <span class="o">*</span> <span class="n">torch</span><span class="p">.</span><span class="nf">tanh</span><span class="p">(</span><span class="n">gates</span><span class="p">[</span><span class="mi">1</span><span class="p">])</span>             <span class="c1"># GRU-like update
</span>        <span class="n">B_k</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nc">W2</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">tanh</span><span class="p">(</span><span class="n">gates</span><span class="p">[</span><span class="mi">2</span><span class="p">]))</span>                            <span class="c1"># [batch, V]
</span>        <span class="k">return</span> <span class="n">s_k</span><span class="p">,</span> <span class="n">B_k</span>
</code></pre></div></div> <p>整个 sequential stage 的推理循环：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">sequential_stage</span><span class="p">(</span><span class="n">base_logits</span><span class="p">,</span> <span class="n">hidden_states</span><span class="p">,</span> <span class="n">seq_head</span><span class="p">,</span> <span class="n">anchor_id</span><span class="p">):</span>
    <span class="c1"># base_logits:   [batch, γ, V]  ← 来自 parallel stage
</span>    <span class="c1"># hidden_states: [batch, γ, d]  ← 来自 parallel stage
</span>    <span class="n">B</span><span class="p">,</span> <span class="n">gamma</span><span class="p">,</span> <span class="n">V</span> <span class="o">=</span> <span class="n">base_logits</span><span class="p">.</span><span class="n">shape</span>
    <span class="n">draft_tokens</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="n">prev_id</span> <span class="o">=</span> <span class="n">anchor_id</span>           <span class="c1"># 第一个"前序 token"是 anchor
</span>    <span class="n">s</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">B</span><span class="p">,</span> <span class="n">seq_head</span><span class="p">.</span><span class="n">rank</span><span class="p">)</span>  <span class="c1"># RNN state 初始为 0
</span>
    <span class="k">for</span> <span class="n">k</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">gamma</span><span class="p">):</span>
        <span class="n">s</span><span class="p">,</span> <span class="n">bias</span> <span class="o">=</span> <span class="n">seq_head</span><span class="p">.</span><span class="nf">step</span><span class="p">(</span><span class="n">s</span><span class="p">,</span> <span class="n">prev_id</span><span class="p">,</span> <span class="n">hidden_states</span><span class="p">[:,</span> <span class="n">k</span><span class="p">,</span> <span class="p">:])</span>
        <span class="n">logits_k</span> <span class="o">=</span> <span class="n">base_logits</span><span class="p">[:,</span> <span class="n">k</span><span class="p">,</span> <span class="p">:]</span> <span class="o">+</span> <span class="n">bias</span>     <span class="c1"># 并行 logit + 转移修正
</span>        <span class="n">x_k</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">multinomial</span><span class="p">(</span><span class="n">logits_k</span><span class="p">.</span><span class="nf">softmax</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">),</span> <span class="mi">1</span><span class="p">).</span><span class="nf">squeeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>
        <span class="n">draft_tokens</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">x_k</span><span class="p">)</span>
        <span class="n">prev_id</span> <span class="o">=</span> <span class="n">x_k</span>              <span class="c1"># 下一步知道当前采样结果
</span>
    <span class="k">return</span> <span class="n">torch</span><span class="p">.</span><span class="nf">stack</span><span class="p">(</span><span class="n">draft_tokens</span><span class="p">,</span> <span class="n">dim</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>  <span class="c1"># [batch, γ]
</span></code></pre></div></div> <p>这个 sequential loop 虽然是串行的，但 seq_head 极其轻量（只有 embedding lookup + 低秩矩阵乘法），每步的计算量远小于 parallel backbone，所以 $T_\text{sequential} \ll T_\text{parallel}$，总延迟仍然由并行阶段主导。</p> <p>如下图，drafter 层数对 acceptance length 的影响——2 层 DSpark 就已经超过了 5 层 DFlash：</p> <p><img src="/assets/img/posts/20260814-dspark/figure3_p13.png" alt="drafter 深度对 acceptance length 的影响"/></p> <hr/> <h2 id="4-confidence-scheduled-verification">4. Confidence-Scheduled Verification</h2> <p>第二个核心设计解决”验证浪费”的问题。</p> <p><strong>问题的本质</strong>：投机解码送给目标模型验证的 token 越多，一旦某个位置被拒，后面的都白算了。如果 draft 质量很差，每次送 8 个 token 去验证，平均只接受 2 个，剩下 6 个 token 的验证纯属浪费 GPU 显存和计算。</p> <p><strong>在高并发系统里更严重</strong>：每个被白验证的 token 都占用了 target model 的 batch capacity，这些 capacity 本可以服务其他用户请求。</p> <h3 id="41-confidence-head预测每个-token-的接受概率">4.1 Confidence Head：预测每个 token 的接受概率</h3> <p>DSpark 在 sequential stage 的基础上，为每个草稿 token 输出一个置信度 $c_k \in (0, 1)$，代表”在前面所有 token 都被接受的条件下，第 $k$ 个 token 也被接受的概率”：</p> \[c_k = \sigma\left(w^\top [h_k;\ W_1[x_{k-1}]]\right)\] <p>监督信号来自 draft 和 target 分布之间的 TV 距离：</p> \[c_k^* = 1 - \frac{1}{2} \|p_d_k - p_t_k\|_1\] <p>这个公式来自投机解码的理论分析——per-step acceptance rate 等于 1 减去 TV 距离。用这个作为监督，模型就学会了预测自己草稿的质量。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">ConfidenceHead</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">backbone_dim</span><span class="p">,</span> <span class="n">markov_rank</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">w</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">backbone_dim</span> <span class="o">+</span> <span class="n">markov_rank</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">bias</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">h_k</span><span class="p">,</span> <span class="n">prev_embed</span><span class="p">):</span>
        <span class="c1"># h_k: [batch, d], prev_embed: [batch, rank]
</span>        <span class="k">return</span> <span class="n">torch</span><span class="p">.</span><span class="nf">sigmoid</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">w</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">([</span><span class="n">h_k</span><span class="p">,</span> <span class="n">prev_embed</span><span class="p">],</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">))).</span><span class="nf">squeeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>
</code></pre></div></div> <p>训练时用 BCE loss：</p> \[\mathcal{L}_\text{conf} = -\sum_{k=1}^\gamma w_k \left[ c_k^* \log c_k + (1-c_k^*) \log(1-c_k) \right]\] <h3 id="42-sequential-temperature-scaling校准">4.2 Sequential Temperature Scaling（校准）</h3> <p>神经网络输出的概率通常偏高（overconfident），直接用 $c_k$ 来估算 acceptance rate 误差大。</p> <p>DSpark 引入了 <strong>Sequential Temperature Scaling（STS）</strong>：prefix survival probability 是各位置 confidence 的连乘 $\prod_{i \leq k} c_i$，STS 从左到右逐位置校准，每次用一维 grid search 找让该位置 ECE 最小的温度参数，且已校准的前序位置不变。</p> <p>这是个 order-preserving 变换（不改变 token 质量的相对排序），只修正绝对概率量级，让 scheduler 的估算更准。</p> <h3 id="43-hardware-aware-prefix-scheduler">4.3 Hardware-Aware Prefix Scheduler</h3> <p>有了校准后的 $c_k$，position $j$ 的 prefix survival probability 是：</p> \[a_{r,j} = \prod_{i \leq j} c_{r,i}\] <p>即”前 $j$ 个 token 全部被接受的概率”，天然单调递减。</p> <p>Scheduler 的目标：在整个 batch 的 $R$ 个请求里，选择每个请求的验证长度 $\ell_r$，使得系统 <strong>期望总吞吐最大化</strong>：</p> \[\Theta = \tau \cdot \text{SPS}(B)\] <p>其中 $B = \sum_r (1 + \ell_r)$ 是送给目标模型的总 token 数，$\text{SPS}(B)$ 是在 batch size $B$ 下的引擎 steps/s（提前 profile 好的）。</p> <p>由于 $a_{r,j}$ 单调递减，这个优化问题可以用贪心算法高效求解：把所有候选 token 按 $a_{r,j}$ 从高到低排序，依次加入 batch，每加一个 token 计算一次当前期望吞吐，一旦吞吐不再增加就停止。</p> <p>整个 Algorithm 1 的逻辑是：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">hardware_aware_scheduler</span><span class="p">(</span><span class="n">confidence_scores</span><span class="p">,</span> <span class="n">sps_table</span><span class="p">,</span> <span class="n">R</span><span class="p">,</span> <span class="n">gamma</span><span class="p">):</span>
    <span class="c1"># confidence_scores: [R, gamma]
</span>    <span class="c1"># sps_table: dict {batch_size -&gt; steps_per_sec}
</span>
    <span class="c1"># 计算每个请求每个位置的 survival probability
</span>    <span class="n">a</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cumprod</span><span class="p">(</span><span class="n">confidence_scores</span><span class="p">,</span> <span class="n">dim</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>  <span class="c1"># [R, gamma]
</span>
    <span class="c1"># 所有候选 (request, position) 按 a 从大到小排序
</span>    <span class="n">candidates</span> <span class="o">=</span> <span class="p">[(</span><span class="n">a</span><span class="p">[</span><span class="n">r</span><span class="p">,</span> <span class="n">j</span><span class="p">].</span><span class="nf">item</span><span class="p">(),</span> <span class="n">r</span><span class="p">,</span> <span class="n">j</span><span class="p">)</span>
                  <span class="k">for</span> <span class="n">r</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">R</span><span class="p">)</span> <span class="k">for</span> <span class="n">j</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">gamma</span><span class="p">)</span> <span class="k">if</span> <span class="n">a</span><span class="p">[</span><span class="n">r</span><span class="p">,</span> <span class="n">j</span><span class="p">]</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">]</span>
    <span class="n">candidates</span><span class="p">.</span><span class="nf">sort</span><span class="p">(</span><span class="n">reverse</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>

    <span class="n">lengths</span> <span class="o">=</span> <span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">*</span> <span class="n">R</span>          <span class="c1"># 初始每个请求验证长度为 0
</span>    <span class="n">B</span> <span class="o">=</span> <span class="n">R</span>                      <span class="c1"># 初始 batch size（每个请求至少发 anchor token）
</span>    <span class="n">tau_expected</span> <span class="o">=</span> <span class="n">R</span>           <span class="c1"># 初始期望接受数（每个请求至少接受 anchor）
</span>    <span class="n">best_theta</span> <span class="o">=</span> <span class="n">tau_expected</span> <span class="o">*</span> <span class="n">sps_table</span><span class="p">[</span><span class="n">B</span><span class="p">]</span>
    <span class="n">best_lengths</span> <span class="o">=</span> <span class="n">lengths</span><span class="p">.</span><span class="nf">copy</span><span class="p">()</span>

    <span class="k">for</span> <span class="n">a_val</span><span class="p">,</span> <span class="n">r</span><span class="p">,</span> <span class="n">j</span> <span class="ow">in</span> <span class="n">candidates</span><span class="p">:</span>
        <span class="k">if</span> <span class="n">j</span> <span class="o">!=</span> <span class="n">lengths</span><span class="p">[</span><span class="n">r</span><span class="p">]:</span>    <span class="c1"># 必须按顺序扩展（prefix 约束）
</span>            <span class="k">continue</span>
        <span class="n">lengths</span><span class="p">[</span><span class="n">r</span><span class="p">]</span> <span class="o">+=</span> <span class="mi">1</span>
        <span class="n">B</span> <span class="o">+=</span> <span class="mi">1</span>
        <span class="n">tau_expected</span> <span class="o">+=</span> <span class="n">a_val</span>
        <span class="n">theta</span> <span class="o">=</span> <span class="n">tau_expected</span> <span class="o">*</span> <span class="n">sps_table</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="n">B</span><span class="p">,</span> <span class="mi">0</span><span class="p">)</span>
        <span class="k">if</span> <span class="n">theta</span> <span class="o">&gt;</span> <span class="n">best_theta</span><span class="p">:</span>
            <span class="n">best_theta</span> <span class="o">=</span> <span class="n">theta</span>
            <span class="n">best_lengths</span> <span class="o">=</span> <span class="n">lengths</span><span class="p">.</span><span class="nf">copy</span><span class="p">()</span>
        <span class="k">else</span><span class="p">:</span>
            <span class="k">break</span>              <span class="c1"># 单调性保证可以早停
</span>
    <span class="k">return</span> <span class="n">best_lengths</span>
</code></pre></div></div> <p>这个设计的妙处在于：<strong>系统负载高时 $\text{SPS}(B)$ 下降快，scheduler 自动收紧每个请求的验证长度；负载低时自动放开，让每个请求验证更多 token</strong>。MTP-1（原来的 baseline）验证长度固定为 2，完全没有这种弹性。</p> <p>如下图，confidence threshold 的 sweep 实验——随着阈值提高，acceptance rate 从 ~77% 上升到 92.5%，因为低质量草稿被剪掉了：</p> <p><img src="/assets/img/posts/20260814-dspark/figure5_p15.png" alt="Confidence threshold 和 acceptance rate 的 tradeoff"/></p> <hr/> <h2 id="5-训练目标">5. 训练目标</h2> <p>DSpark 的训练损失由三项组成，都带位置衰减权重 $w_k = e^{-(k-1)/\gamma}$（越靠前权重越大）：</p> \[\mathcal{L} = \alpha_\text{ce} \mathcal{L}_\text{ce} + \alpha_\text{tv} \mathcal{L}_\text{tv} + \alpha_\text{conf} \mathcal{L}_\text{conf}\] <ul> <li><strong>$\mathcal{L}_\text{ce}$</strong>（交叉熵，$\alpha=0.1$）：让 drafter 预测正确 token</li> <li><strong>$\mathcal{L}_\text{tv}$</strong>（TV 距离，$\alpha=0.9$）：最小化 draft 和 target 分布之间的 total variation distance，直接最大化期望 acceptance rate</li> <li><strong>$\mathcal{L}_\text{conf}$</strong>（置信度，$\alpha=1.0$）：训练 confidence head 准确预测自己草稿会不会被接受</li> </ul> <p>$\mathcal{L}<em>\text{tv}$ 权重远大于 $\mathcal{L}</em>\text{ce}$ 是有意设计的——因为 acceptance rate 和 TV 距离直接挂钩，最小化 TV 比最大化 token 准确率对最终 speedup 更有帮助。</p> <hr/> <h2 id="6-线上部署结果">6. 线上部署结果</h2> <p>这篇论文最有说服力的部分是 DeepSeek-V4 生产系统的真实流量数据。</p> <p>如下图，throughput（系统吞吐）vs. TPS（每个用户的生成速度）的 Pareto frontier：</p> <p><img src="/assets/img/posts/20260814-dspark/figure7_p18.png" alt="Throughput vs TPS：DeepSeek-V4 线上表现"/></p> <p>和 MTP-1（生产 baseline）相比，DSpark 把这条 Pareto 曲线整体右移——相同用户 TPS 下吞吐提升 <strong>+51~52%</strong>，相同系统吞吐下 TPS 提升 <strong>+60~57%</strong>，在极低负载的高 TPS 区间提升高达 <strong>+85%</strong>。</p> <p>再看 load-adaptive 的调度效果：</p> <p><img src="/assets/img/posts/20260814-dspark/figure8_p19.png" alt="负载自适应吞吐和 verification budget"/></p> <p>并发请求数增多时，DSpark 的 verification budget（下图折线）自动从 ~5 收缩到 ~3.5，始终保持最优系统吞吐。MTP-1 固定在 2，无论负载高低都不动。</p> <hr/> <h2 id="7-offline-benchmark-主结果">7. Offline Benchmark 主结果</h2> <p>如下表，在 Qwen3-4B/8B/14B 和 Gemma4-12B 上，DSpark 的 acceptance length 全面超过 Eagle3 和 DFlash：</p> <p><img src="/assets/img/posts/20260814-dspark/table1_p11.png" alt="主结果：acceptance length 对比"/></p> <p>以 Qwen3-8B + GSM8K 为例：Eagle3 是 5.30，DFlash 是 5.33，DSpark 是 <strong>6.17</strong>。Chat 类任务（MT-Bench、Alpaca）提升更显著，恰恰说明序列依赖建模对”语境连贯性”要求高的任务更重要。</p> <hr/> <h2 id="8-个人-take">8. 个人 Take</h2> <p>DSpark 有两个地方让我觉得设计得很干净。</p> <p>一个是 semi-autoregressive 架构的分工：parallel backbone 承担全部的”大计算量”（对全局 context 的感知），sequential head 只做”小修正”（block 内依赖），整体延迟由前者主导，后者几乎是免费的。这种”重骨干 + 轻修正”的组合是个值得借鉴的范式。</p> <p>另一个是 confidence-scheduled verification 的视角——它把投机解码从”单请求 drafting 问题”扩展成了”多请求 batch scheduling 问题”。这个视角转变很重要：在高并发系统里，draft token 的边际价值不是固定的，取决于当前系统负载。把这两者联合优化，才能真正释放 speculative decoding 在生产场景下的潜力。</p> <p>这类工作的说服力有相当一部分来自真实线上数据。DeepSeek 能把自家 V4 的生产流量拿来验证，这个条件不是每个研究组都有的。但方法本身的思路——用轻量序列头修 suffix decay，用置信度驱动动态调度——是普适的，不依赖特定的系统环境。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 LLM 推理效率优化你想系统深入，可以看看我之前出版的<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，书里有专章讲 LLM 推理效率和参数高效微调，和本文的工程背景有直接关联。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="投机解码"/><category term="推理优化"/><category term="DeepSeek"/><category term="论文解读"/><summary type="html"><![CDATA[DSpark：DeepSeek 线上跑的投机解码，半自回归 + 置信度调度怎么做到的]]></summary></entry><entry><title type="html">DeepSeek NSA：Sparse Attention 不是新概念，但这次真的做对了</title><link href="https://marsggbo.github.io/blog/2026/20260814-nsa/" rel="alternate" type="text/html" title="DeepSeek NSA：Sparse Attention 不是新概念，但这次真的做对了"/><published>2026-08-14T00:00:00+00:00</published><updated>2026-08-14T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260814-nsa</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260814-nsa/"><![CDATA[<h1 id="deepseek-nsasparse-attention-不是新概念但这次真的做对了">DeepSeek NSA：Sparse Attention 不是新概念，但这次真的做对了</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2502.11089">Native Sparse Attention: Hardware-Aligned and Natively Trainable Sparse Attention</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>Sparse Attention 这个概念老得很——BigBird、Longformer、Reformer，2020 年那波论文基本把”哪些 token 可以不算”这件事在算法层面研究透了。</p> <p><strong>但真正好用的 sparse attention 工程实现，现在才开始出现。</strong></p> <p>这不是在贬前人，是实事求是：以前的 sparse attention 在 GPU 上的实现大多很尴尬——跳过了一些 token 的计算，但 memory access pattern 还是按 dense 来，HBM 带宽没节省，SRAM 利用率也差，结果理论 FLOPs 降了不少，实际 wall-clock time 省不了多少。</p> <p>DeepSeek 这篇 NSA（Native Sparse Attention）做的事情是：<strong>把 sparse attention 的算法设计和 GPU kernel 实现一起考虑</strong>，让 sparsity 真正落地为实际加速，同时支持从头预训练而不是事后近似。</p> <p>64k 长度序列上，对比 FlashAttention-2：decode <strong>快 11.6×</strong>，forward <strong>快 9×</strong>，backward <strong>快 6×</strong>。而且模型质量不降反升——在通用 benchmark 平均分上超过 Full Attention baseline。</p> <hr/> <h2 id="2-老-sparse-attention-为什么没做好">2. 老 Sparse Attention 为什么没做好</h2> <p>先说清楚问题在哪，这样才能理解 NSA 解决了什么。</p> <p>Attention 的计算复杂度是 $O(N^2)$，序列长度翻倍，计算量翻四倍。Sparse attention 的思路是：不是所有 token 对之间的 attention 都有意义，跳过那些”不重要的”token，把 $O(N^2)$ 压下来。</p> <p>算法层面没问题。但落到 GPU 上：</p> <p><strong>GPU 喜欢连续、规整的内存访问。</strong> 你告诉它”第 17、43、128、512 号 token 的 KV 我要，其他不要”，GPU 就得在 HBM 里东一块西一块地 load，内存访问变成 random access，带宽利用率极差。哪怕你 FLOPs 少了 90%，memory bound 没解决，速度未必快多少。</p> <p><strong>另一个问题是可训练性。</strong> 很多 sparse attention 是训练完一个 full attention 模型，推理时再做近似（比如 H2O、StreamingLLM）。这种方式不可能做到最优——模型本身没有针对 sparse 模式优化过，近似误差是固定存在的。</p> <p>NSA 两个问题一起解决。</p> <hr/> <h2 id="3-nsa-的三路并行架构">3. NSA 的三路并行架构</h2> <p>NSA 把 attention 拆成三个并行分支，每个分支处理不同粒度的信息，最后 gated 加权合并：</p> <p><img src="/assets/img/posts/20260814-nsa/figure2_p3.png" alt="NSA 三路并行架构概览"/></p> <h3 id="31-compressed-attention粗粒度全局感知">3.1 Compressed Attention（粗粒度全局感知）</h3> <p>这一路把整个序列的 KV 按块压缩——每个块的 token 用一个 MLP 压成一个代表向量，然后对这些压缩后的 KV 做 attention。</p> <p>作用：<strong>保留全局上下文的粗粒度感知</strong>。模型能”看到”整个历史序列的大致走向，不会因为 sparsity 完全丢失远距离信息。</p> <p>计算复杂度：从 $O(N^2)$ 降到 $O(N \cdot N/b)$，$b$ 是块大小。</p> <h3 id="32-selected-attention精细-top-n-块选择">3.2 Selected Attention（精细 top-n 块选择）</h3> <p>这一路做精细选择：先用 compressed attention 的分数估算哪些块”最重要”，然后 top-n 挑出来，对这些选定块的原始 KV 做 full attention。</p> <p>作用：<strong>精确捕捉真正重要的远距离依赖</strong>。比如你在写代码，当前 token 可能需要精确地 attend 到几百行之前定义的变量——compressed 那路能粗略找到它在哪，selected 这路负责精确计算。</p> <p>关键设计：选择的粒度是<strong>块</strong>而非单个 token，这是 hardware-aware 的核心。块级选择意味着 memory access 是连续的，GPU 可以高效地 load 整块 KV，而不是 random access 单个 token。</p> <h3 id="33-sliding-window-attention局部精确">3.3 Sliding Window Attention（局部精确）</h3> <p>这一路对当前 token 最近的 $w$ 个 token 做完整 attention。</p> <p>作用：<strong>局部连贯性</strong>。语言本质上是局部有依赖的，相邻 token 的关联最强。这路保证了不会因为 selected attention 的块选择遗漏掉紧邻的上下文。</p> <p>实现上这也是最简单的一路，标准 causal attention with a fixed window，FlashAttention 直接支持。</p> <hr/> <h2 id="4-hardware-aware-kernel-设计">4. Hardware-Aware Kernel 设计</h2> <p>三路架构是 NSA 能真正快起来的算法基础，但真正的加速来自 kernel 实现。</p> <p>如下图，NSA 的 Triton kernel 设计：</p> <p><img src="/assets/img/posts/20260814-nsa/figure3_full.png" alt="NSA Kernel 设计：GQA 分组 load，SRAM 计算"/></p> <p>两个关键设计：</p> <p><strong>1. GQA 分组共享 KV load（Grid Loop）</strong></p> <p>NSA 基于 GQA（Grouped Query Attention）架构，一组 query head 共享同一套 KV。Kernel 的 Grid Loop 按 GQA group 来遍历 query，这样同一组内的 query head 对同一块 KV 的 load 只发生一次，然后 SRAM 里的 KV 被这一组所有 query head 复用。</p> <p><strong>结果：HBM → SRAM 的 load 次数大幅减少，bandwidth 利用率提升。</strong></p> <p><strong>2. 块级 KV 选择（Inner Loop）</strong></p> <p>Selected attention 的稀疏索引是按块存储的连续 index，Inner Loop 按这个 index 顺序 load 对应的 KV 块进 SRAM，然后做计算。因为选的是整块，load 都是顺序 coalesced access，不存在 random access 的问题。</p> <p>这和”每次 load 一个 token 的 KV”有本质区别。</p> <p>Table 4 的分析显示，64k 序列下 NSA 的 memory access volume 仅是 FlashAttention-2 的约 1/9，对应了 forward 9× 的实测加速。</p> <hr/> <h2 id="5-端到端可训练不是事后近似">5. 端到端可训练：不是事后近似</h2> <p>NSA 支持<strong>从头预训练</strong>，这一点值得单独说一下。</p> <p>三路分支的 gated 合并参数、selected attention 的块选择逻辑，在训练时全部参与梯度更新。模型在训练过程中就学会了”哪些块重要、如何分配三路的权重”，而不是在推理时用启发式规则去近似一个 full attention 模型。</p> <p>实验用的是 27B 总参数（3B active，MoE 架构），同等 token 预训练下，NSA 的 loss 曲线和 Full Attention 几乎重合，甚至略低。</p> <hr/> <h2 id="6-实验结果">6. 实验结果</h2> <h3 id="61-通用-benchmark">6.1 通用 Benchmark</h3> <p>如下表，NSA 在大多数通用 benchmark 上<strong>超过</strong>了同等规模的 Full Attention baseline：</p> <p><img src="/assets/img/posts/20260814-nsa/table1_p10.png" alt="通用 benchmark 对比"/></p> <p>平均分 0.456 vs Full Attention 的 0.443。这是在模型整体计算量大幅下降的情况下做到的。核心原因：稀疏注意力减少了无意义 token 对的干扰，某种程度上相当于对 attention pattern 做了一次”去噪”。</p> <h3 id="62-长上下文needle-in-a-haystack">6.2 长上下文：Needle-in-a-Haystack</h3> <p>64k 上下文的 Needle-in-a-Haystack 测试，NSA 全绿：</p> <p><img src="/assets/img/posts/20260814-nsa/figure5_p12.png" alt="Needle-in-a-Haystack 64k 满分"/></p> <p>所有位置、所有深度 accuracy = 1.0。这说明 Compressed + Selected 的双路设计确实覆盖了长距离依赖，没有因为 sparsity 丢失关键信息。</p> <h3 id="63-实际加速">6.3 实际加速</h3> <p>如下图，对比 Triton-based FlashAttention-2：</p> <p><img src="/assets/img/posts/20260814-nsa/figure6_p13.png" alt="NSA vs FlashAttention-2 实际速度对比"/></p> <ul> <li>8k 序列：forward 2.1×，backward 1.1×（短序列时 sparsity 收益还不明显）</li> <li>32k 序列：forward 6.3×，backward 3.4×</li> <li>64k 序列：forward <strong>9.0×</strong>，backward <strong>6.0×</strong></li> </ul> <p>序列越长，收益越大——这正是 sparse attention 应有的表现，$O(N^2)$ vs $O(N \cdot k)$ 的差距在长序列上才显著放大。</p> <hr/> <h2 id="7-和以往-sparse-attention-的本质区别">7. 和以往 Sparse Attention 的本质区别</h2> <table> <thead> <tr> <th>维度</th> <th>老方案（H2O/StreamingLLM 等）</th> <th>NSA</th> </tr> </thead> <tbody> <tr> <td>训练方式</td> <td>训练 Full Attention，推理时近似</td> <td>原生稀疏预训练</td> </tr> <tr> <td>sparsity 粒度</td> <td>token 级（random access）</td> <td>block 级（coalesced access）</td> </tr> <tr> <td>实际加速</td> <td>理论 FLOPs 降，wall-clock 未必</td> <td>显著真实加速（6-9×）</td> </tr> <tr> <td>质量</td> <td>通常有精度损失</td> <td>达到甚至超过 Full Attention</td> </tr> </tbody> </table> <p>最本质的区别就一句话：<strong>以前是把 dense 模型变稀疏，NSA 是把稀疏作为训练目标本身</strong>。</p> <hr/> <h2 id="8-个人-take">8. 个人 Take</h2> <p>这篇工作让我想起 FlashAttention 刚出来时的感觉——大家都知道 attention 很慢，也都在想各种算法层面的优化，但 FlashAttention 的核心洞察是”IO-awareness 比 FLOPs 更重要”，通过 tiling 和 online softmax 在不改变计算结果的前提下大幅减少 HBM 访问，从而真正快起来。</p> <p>NSA 做的事情类似：sparse attention 的算法价值大家认可，但落地的 gap 在于 memory access pattern。NSA 用块级选择把 random access 变成 sequential access，再配合 GQA 的 KV 共享，让 GPU 能高效执行稀疏计算。</p> <p><strong>算法 + 硬件要一起考虑</strong>，这条路 DeepSeek 走得很稳。</p> <p>当然，现在 NSA 的实验主要是 27B MoE 模型，在更大规模（百亿 dense 模型）和更长序列（128k+）上的表现还需要观察。另外，块选择的准确性依赖 compressed attention 的估算质量，在某些极端 attention pattern 的任务上是否会出问题，也是个值得追的问题。</p> <p>但作为”把 sparse attention 真正做到生产可用”的工作，NSA 应该是 2025 年最值得关注的 attention 效率论文之一。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 LLM 推理效率优化你想系统深入，可以看看我之前出版的<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，书里有专章讲 LLM 推理效率和参数高效微调，和本文讨论的 attention 优化工程背景有直接关联。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="Attention"/><category term="长上下文"/><category term="推理优化"/><category term="论文解读"/><summary type="html"><![CDATA[DeepSeek NSA：Sparse Attention 不是新概念，但这次真的做对了]]></summary></entry><entry><title type="html">arXiv’26 | vToken：KV Cache 淘汰后，为什么显存还是回收不回来？</title><link href="https://marsggbo.github.io/blog/2026/20260814-vtoken/" rel="alternate" type="text/html" title="arXiv’26 | vToken：KV Cache 淘汰后，为什么显存还是回收不回来？"/><published>2026-08-14T00:00:00+00:00</published><updated>2026-08-14T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260814-vtoken</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260814-vtoken/"><![CDATA[<h1 id="arxiv26--vtokenkv-cache-淘汰后为什么显存还是回收不回来">arXiv’26 | vToken：KV Cache 淘汰后，为什么显存还是回收不回来？</h1> <blockquote> <p>原文：<a href="https://arxiv.org/pdf/2608.13263">vToken: Token-Level Virtualization for Reclaimable KV Caches</a></p> </blockquote> <hr/> <h2 id="1-先说一个很容易被忽略的浪费">1. 先说一个很容易被忽略的浪费</h2> <p>长上下文 LLM 推理里，KV Cache 的大小会随着上下文长度和并发请求一起涨。大家很自然会想到：既然历史 token 的重要性不一样，那就把不重要的 token 淘汰掉，只留下 attention 真正会用到的部分。</p> <p>这个方向已经有不少工作了。H2O（Heavy-Hitter Oracle）保留累计 attention 分数高的 token，StreamingLLM 保留 attention sink 加最近窗口，Scissorhands 则根据 token 在多步 attention 中的稳定性做筛选。算法层面都在回答同一个问题：<strong>哪些 token 可以删？</strong></p> <p>但把它们接到 vLLM 之后，事情会变得有点离谱：token 确实删掉了，GPU 显存却不一定回来。</p> <p>原因是 vLLM 的 PagedAttention 把 KV Cache 按固定大小的 block 管理，典型配置是一个 block 放 16 个 token。假设某个 block 里 16 个 token 有 6 个被淘汰、10 个还活着，这个 block 仍然不能释放。那 6 个 token 对模型已经没用了，但它们占的物理空间也不能给别的请求用。</p> <p><strong>逻辑上已经释放，物理上还被占着。</strong>这就是 vToken 要解决的核心矛盾。</p> <h2 id="2-背景pagedattention-解决了哪种-fragmentation">2. 背景：PagedAttention 解决了哪种 fragmentation</h2> <h3 id="21-kv-cache-为什么会撑爆显存">2.1 KV Cache 为什么会撑爆显存</h3> <p>KV Cache 是每一层 attention 的 Key 和 Value。生成新 token 时，模型不需要重新计算历史 token 的 K/V，直接读取缓存即可，所以 attention 的历史部分从“反复重算”变成“读取已有状态”。代价是这些状态要为每个请求、每个历史 token 保存在 GPU 上。</p> <p>一个 token 的 KV 大小近似是：</p> \[M_{token}=2\times L\times H\times D\times \text{dtype\_size}\] <p>其中 $L$ 是 layer 数，$H$ 是 KV head 数，$D$ 是 head dimension，乘 2 是 K 和 V 各一份。论文用一个 13B LLaMA 作为例子：40 层、hidden size 5120、FP16 下，一个 token 的 KV 约 0.8 MB；32K context 单请求就超过 25 GB。模型权重还没算，显存已经先被“记忆”吃掉了。</p> <h3 id="22-pagedattention-的-block-视角">2.2 PagedAttention 的 block 视角</h3> <p>PagedAttention 借鉴操作系统虚拟内存：逻辑上的连续 token 序列不必放在连续物理显存里，而是通过 block table 映射到一组不连续的物理 block。这样做主要解决两件事：减少 allocator 层面的外部 fragmentation，并支持 prefix sharing。</p> <p>但它的管理接口天然是 block-level 的：申请 block、释放 block、给 attention kernel 提供 slot，都以 block 为单位。它并不知道“一个 block 里面有几个 token 其实已经死了”。</p> <p>而 token eviction 关注的是另一套语义：token 级别的保留和淘汰。两边各自都合理，接在一起却缺了中间层。</p> <h2 id="3-真正的问题不是淘汰策略而是粒度错位">3. 真正的问题不是淘汰策略，而是粒度错位</h2> <p>设一个物理 block 能放 $S$ 个 token，第 $b$ 个 block 里还有 $n_b$ 个有效 token。它的利用率是：</p> \[u_b=\frac{n_b}{S}\] <p>如果总共分配了 $N$ 个 block，那么 block 内部的 waste ratio 可以写成：</p> \[F=1-\frac{\sum_{b=1}^{N}n_b}{N\times S}=1-\frac{1}{N}\sum_{b=1}^{N}u_b\] <p>这个 $F$ 不是 allocator 统计的“外部碎片”，而是 block 已经拿到手之后，里面有多少位置因为 token 被淘汰而空着，却仍然不能复用。</p> <p>论文在 Llama-3.1-8B、16K context、batch size 16 上做了一个很直接的实验。对 H2O、Random 和 Scissorhands 三种 token-level policy，ShareGPT 上低于 50% 利用率的 block 占比是 56%、58%、56%；LongBench 上是 58%、67%、57%。其中最深的 0–25% 利用率区间占了相当大一部分。</p> <p><img src="/assets/img/posts/20260814-vtoken/figure1_full.png" alt="Token 淘汰后的低利用率 block"/></p> <p>换句话说，token eviction 已经把逻辑 KV 需求压下去了，但 block runtime 只能看到“这个 block 里还有一个活 token”，于是整块显存继续被占用。论文报告的 intra-block waste ratio 达到 40%–60%。这不是一个边角 case，而是 token 被分散在不同 block 时的必然结果。</p> <h3 id="31-为什么不能简单把-block-改小">3.1 为什么不能简单把 block 改小</h3> <p>有人可能会说：既然一个 block 里塞 16 个 token 会浪费，那改成 4 个甚至 1 个不就行了？</p> <p>问题在于 block size 不是免费的。block 越小，metadata 越多，逻辑到物理的映射更复杂，KV copy 和 attention 需要处理更多不连续的小块，带宽效率也会下降。反过来，如果真的按 token 单独向 GPU allocator 申请，元数据和分配开销又会把系统拖回另一种极端。</p> <p>所以需要的不是“把 block 改成 token”，而是<strong>保留 block 的物理效率，同时在上层暴露 token-level 的逻辑语义</strong>。</p> <h2 id="4-vtoken给-block-runtime-加一层虚拟化">4. vToken：给 block runtime 加一层虚拟化</h2> <p>vToken 的核心不是提出一种新的 token importance score。它做的是在 eviction policy 和 PagedAttention 之间插入一个 token-level virtualization layer：</p> <ul> <li>policy 只负责说哪些 logical token 不再需要；</li> <li>vToken 维护 token 的逻辑地址和生存状态；</li> <li>reclamation backend 再决定什么时候搬、搬到哪里、哪些物理 block 可以真正释放。</li> </ul> <p>如下图，vToken substrate 上接各种 KV eviction policy，下接 vLLM 原来的 Block Manager 和 Scheduler。attention kernel 本身不需要改成 token allocator。</p> <p><img src="/assets/img/posts/20260814-vtoken/figure2_p4.png" alt="vToken 系统分层"/></p> <p>这层边界解决了三个问题：</p> <ol> <li><strong>Dual-view consistency</strong>：同一个 token 同时有逻辑身份和物理位置，淘汰不会马上破坏物理布局。</li> <li><strong>Safe reclamation</strong>：token 搬家时，slot mapping 和 CUDA stream 依赖能保持一致。</li> <li><strong>Policy-neutral integration</strong>：H2O、Random、Scissorhands 复用同一套 runtime，不需要各自侵入 block manager。</li> </ol> <h2 id="5-token-table逻辑-token-和物理-slot-之间的页表">5. Token table：逻辑 token 和物理 slot 之间的页表</h2> <p>vToken 的中心数据结构是 per-sequence token table。每个 logical token ID 记录：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>token_id -&gt; (physical block id, offset, live/dead)
</code></pre></div></div> <p>它维护的是“这个 token 现在在哪”，而不是“这个 block 现在属于谁”。比如请求中的 token 0、1、3 原来在物理 block 5 的 offset 0、1、3；另一个请求的 token 1、2 可能在 block 0 和 block 2。逻辑序列仍然连续，物理存储可以完全不连续。</p> <p><img src="/assets/img/posts/20260814-vtoken/figure3_p4.png" alt="Token table 如何映射逻辑地址"/></p> <p>上层 policy 只需要三个接口：</p> <ul> <li><code class="language-plaintext highlighter-rouge">evict_token(req_id, token_id)</code>：把 token 标记为逻辑 inactive，不立即搬数据；</li> <li><code class="language-plaintext highlighter-rouge">sync_new_tokens(req_id, block_ids, total_len)</code>：注册新生成 token 的物理位置；</li> <li><code class="language-plaintext highlighter-rouge">apply_moves(req_id, moves)</code>：后台搬迁完成后，原子更新 token 的新位置。</li> </ul> <p>举个具体的 slot 计算例子。假设 block size $S=16$，token table 记录 token 42 在物理 block 12、offset 3，那么 attention 需要的 slot 是：</p> \[slot=12\times16+3=195\] <p>如果后台把它搬到 block 20、offset 1，逻辑 token ID 42 不变，但下一轮 input preparation 会根据新表计算出 $20\times16+1=321$。这个 indirection 在 attention kernel 之外完成，kernel 继续接收熟悉的 slot mapping。</p> <p>因此，evict token 和 free block 被明确拆成两个时间点：<strong>先改变逻辑可见性，后在合适的时机重排物理布局。</strong></p> <h2 id="6-物理回收不是-compact-一下就完事">6. 物理回收：不是 compact 一下就完事</h2> <h3 id="61-什么时候值得搬">6.1 什么时候值得搬</h3> <p>vToken 没有每一步都扫描整张 token table，而是增量维护每个 block 的 live-token 数量。当全局 waste ratio $F$ 超过阈值、free block 接近 low watermark，同时存在足够多低利用率 block 时，才触发 planner。</p> <p>论文默认 fragmentation threshold 是 0.25。阈值太低，系统会频繁搬家；阈值太高，显存碎片积累得更严重。更重要的是，out-of-place relocation 需要先申请 destination block：如果等 free list 完全空了才开始回收，正好会陷入“没有空间搬，所以永远回收不了”的死结。</p> <p>所以 scheduler 在同一个 KV block budget 内预留 bounded evacuation headroom。这不是额外显存，而是暂时留给搬迁目的地的工作空间。只有预计释放的 source block 数量严格大于 destination block 数量，计划才会被接受。</p> <p>如果选中的 source blocks 一共有 $T_{live}$ 个活 token，每个 block 容量是 $S$，需要的目的 block 数量就是：</p> \[N_{dst}=\left\lceil\frac{T_{live}}{S}\right\rceil\] <p>例如 5 个碎片 block 里总共还活着 31 个 token，$S=16$，搬到 2 个新 block 后可以释放 3 个旧 block；这次 relocation 才是“赚的”。如果搬 3 个目的 block 只能释放 2 个 source block，planner 直接放弃。</p> <h3 id="62-三阶段搬迁流程">6.2 三阶段搬迁流程</h3> <p>物理回收大致分三步：先扫描低利用率 source block 并生成 move list；再把活 token 按顺序搬到 packed destination block；最后提交 token table、刷新 slot mapping、归还空 source block。</p> <p><img src="/assets/img/posts/20260814-vtoken/figure4_p5.png" alt="物理回收的三阶段流程"/></p> <p>注意这里维护的是 token 的逻辑顺序，而不是简单 memcpy 整个 block。move list 会记录每个 token 的 source <code class="language-plaintext highlighter-rouge">(block, offset)</code> 和 destination <code class="language-plaintext highlighter-rouge">(block, offset)</code>，这样搬迁之后 token table 仍然能逐项更新。</p> <h2 id="7-关键工程细节copy-不能和-attention-抢-hbm">7. 关键工程细节：copy 不能和 attention 抢 HBM</h2> <p>最容易写成一句“异步 copy”的地方，恰恰是 vToken 最有工程味的地方。</p> <p>一个 decode step 大致有三类阶段：attention forward 读 KV、FFN forward 更偏 compute-bound、post-forward 做 sampling 和 scheduler 逻辑。若把 KV copy 从 step 开始就和 attention 重叠，两者会同时争抢 HBM 带宽，结果是 attention 被 copy 拖慢。</p> <p>vToken 的 stage-aware async copy 做法是：</p> <ol> <li>当前 step 的 attention/FFN 先使用旧的 slot mapping 完成 forward；</li> <li>forward 返回后，在独立 relocation stream 上启动 KV copy；</li> <li>copy 与 sampling、调度和下一轮输入准备重叠，这些阶段不会读取 KV；</li> <li>下一轮 attention 前，只在确实需要时通过 CUDA event <code class="language-plaintext highlighter-rouge">wait_event</code> 等待 copy 完成。</li> </ol> <p><img src="/assets/img/posts/20260814-vtoken/figure5_p6.png" alt="Stage-aware asynchronous copy"/></p> <p>这里的关键不是“完全没有同步”，而是把 host blocking 换成 GPU stream dependency。CPU 不需要停下来等 copy，下一次 attention 只对相关 relocation 建立依赖。论文还保留了 CUDA Graph：graph 里的 KV tensor 和 input buffer 不变，只在 replay 前更新可变的 slot-mapping buffer。</p> <h2 id="8-对-eviction-policy-的接入有多轻">8. 对 eviction policy 的接入有多轻</h2> <p>vToken 把 policy adapter 固定成一个很短的流程：注册新 token，调用 policy 选 victim，逐个 <code class="language-plaintext highlighter-rouge">evict_token</code>，构造 slot mapping，异步回收，然后 decode。</p> <p><img src="/assets/img/posts/20260814-vtoken/figure6_full.png" alt="Policy adapter 和集成开销"/></p> <p>论文给出的 integration footprint 很直观：传统 block-native 接入需要改 4–6 个 scheduler/worker 文件、500+ 行代码；vToken 只需 1–2 个 adapter 文件、少于 50 行 policy-specific code。H2O 只实现“选择最低分 token”，Scissorhands 只实现“选择低 persistence token”，Random 只负责随机采样可淘汰 token，token table、slot remapping 和 reclamation 都是共享逻辑。</p> <p>这也是我觉得这篇工作比“又一个 eviction heuristic”更有价值的地方：它把 policy 研究和 runtime 工程解耦了。以后出现新的 token importance 方法，不必重复踩 vLLM block manager 的坑（希望不要再回忆了…）。</p> <h2 id="9-实验结果回收的是被困住的容量">9. 实验结果：回收的是被困住的容量</h2> <h3 id="91-实验设置">9.1 实验设置</h3> <p>原型基于 vLLM v0.18.0 和 PyTorch 2.10.0，运行在单张 80GB H100 上，主要模型是 Mistral-7B 和 Llama-3.1-8B，工作负载包括 ShareGPT 和 LongBench，另外用 Qwen2.5-14B 做容量 frontier 检查。对比三种 token eviction policy：H2O、Scissorhands、Random。</p> <p>三个系统变体要分清：Native vLLM 是 full-retention baseline；Naive-Evict 使用同样的 token eviction 决策，但没有物理回收，所以碎片 block 继续占着；vToken 在相同 policy 和 memory budget 上打开 token table 与 physical reclamation。这个 paired comparison 很重要，否则很容易把 policy 的收益误算成 vToken 的收益。</p> <h3 id="92-memory-utilization-和-retained-blocks">9.2 memory utilization 和 retained blocks</h3> <p>vToken 在 Llama-3.1-8B 上平均提升 21.88% 的 memory utilization，在 Mistral-7B 上提升 21.67%。这里的 utilization 不是“GPU 显存用了多少”，而是<strong>已经分配的 KV block 里，有多少位置确实存着 retained token</strong>。</p> <p><img src="/assets/img/posts/20260814-vtoken/figure7_full.png" alt="Token-level eviction 的内存利用率"/></p> <p>右侧 retained blocks per request 更直接：Naive-Evict 由于 live token 分散在 partially live blocks 中，物理 block 数量仍然偏高；vToken 把这些 live token 重新 packed 后，空出来的 block 才能回到 allocator。</p> <h3 id="93-sla-frontier吞吐不是盲目把-concurrency-拉高">9.3 SLA frontier：吞吐不是盲目把 concurrency 拉高</h3> <p>论文没有只报一个峰值 throughput，而是给每个设置加了 p95 latency 约束。SLA 阈值设为 Naive-Evict 在参考并发 $C_{ref}=8$ 时 p95 的 1.05 倍，$C_{sel}$ 是满足 SLA 的最大吞吐点。</p> <p><img src="/assets/img/posts/20260814-vtoken/figure8_full.png" alt="SLA 约束下的吞吐 frontier"/></p> <p>在 Mistral-7B 上，vToken 在不同 workload-policy 组合下把 selected feasible throughput 提升 9.9%–37.3%，同时 p95 latency 降低 9.9%–27.5%。Llama-3.1-8B 上平均吞吐提升 18.9%，p95 降低 14.7%。Random 的收益通常更大，因为随机淘汰会把 live token 撒得更散；Scissorhands 的收益也很明显，在两个模型和 workload 上达到 33.3%–103.7% 的 throughput 提升。</p> <p>这点要说清楚：vToken 不是让 attention kernel 突然快了一倍，而是把原本已经“逻辑上省出来”的容量真正变成 admission headroom，于是系统能在同一个 SLA 下接更多请求。</p> <h3 id="94-capacity-frontier同一块显存能塞下多少活请求">9.4 capacity frontier：同一块显存能塞下多少活请求</h3> <p>在 Llama-3.1-8B、<code class="language-plaintext highlighter-rouge">gpu_mem_util=0.35</code> 下，H100 有 5,427 个可用 KV blocks。Native vLLM 和 Naive-Evict 的可行并发边界是 $C=5$，vToken 把它推到 $C=8$，提升 60%。在 <code class="language-plaintext highlighter-rouge">gpu_mem_util=0.50</code> 下，边界从 $C=11$ 推到 $C=22$，直接翻倍。</p> <p><img src="/assets/img/posts/20260814-vtoken/figure9_full.png" alt="Active-KV capacity frontier"/></p> <p>为什么会出现这么大的差距？以 16-token block 为例，6 个完整保留的请求需要 6,120 blocks，超过 5,427 的池子；H2O 淘汰后理想 packed footprint 只需要 512 blocks，但 Naive-Evict 因为活 token 分散，实际需求仍然接近 full retention。vToken 做的就是把“512 blocks 的逻辑需求”逼近物理布局。</p> <p>在 Qwen2.5-14B 的额外检查里，3,261 个 usable blocks 下，Native/Naive 的边界是 $C=3$，vToken 能到 $C=6$。这说明收益不只属于某个 8B 模型。</p> <p><img src="/assets/img/posts/20260814-vtoken/figure10_full.png" alt="KV block demand 与容量预算"/></p> <h2 id="10-开销稳定性和边界">10. 开销、稳定性和边界</h2> <h3 id="101-异步回收不是零成本">10.1 异步回收不是零成本</h3> <p>indirection-only ablation 显示，只安装 vToken hooks、不开 eviction 和 reclamation 时，吞吐和 p95 相对 Native vLLM 的变化小于 1%。真正的成本来自 pressure-activated planner 和 relocation backend，而不是每次 attention 都多查一次表。</p> <p>论文把开销拆开后发现，CPU 侧主要是 planner 的 opportunity checks；KV copy 本身很短，但会和 decode 共享 GPU 资源。force-sync ablation 则会暴露每次 relocation 的 CPU blocking，异步路径通过 CUDA event 避免了显式同步等待。</p> <p><img src="/assets/img/posts/20260814-vtoken/figure11_full.png" alt="Physical reclamation 的开销与重叠"/></p> <p>所以 vToken 的“async”不是魔法：它消除的是同步 stall，不保证 copy 和 attention 没有 bandwidth contention。在更高压力下，copy/decode 争抢资源仍然可能出现。</p> <h3 id="102-正确性和-prefix-cache">10.2 正确性和 prefix cache</h3> <p>论文在线检查四个 invariant：token 不丢不重复、每个 live token 唯一映射、attention 只在 copy 完成后读取、规划基于一致的 block/liveness snapshot。所有 relocation event 都通过了 token/KV hash 校验。</p> <p>在 144 条匹配生成上，vToken 相对 Naive-Evict 的 ROUGE-L F1 平均差是 -0.0016，93.1% 的样本差异不超过 0.01；输出长度比值中位数是 0.99，没有观察到系统性变长。</p> <p>prefix cache 目前采取保守策略：shared-prefix block 不搬，只回收 private suffix。共享度为 2、4、8 时，shared blocks 全部跳过，retained block 减少 33.0%、32.3%、42.2%，吞吐分别提升 76.1%、97.2%、146.5%。注意这不是 prefix hit rate 的功劳，两边都启用了 prefix cache，收益来自 private suffix 的物理回收。</p> <p><img src="/assets/img/posts/20260814-vtoken/table1_full.png" alt="Prefix cache 兼容性"/></p> <h3 id="103-这篇工作的边界">10.3 这篇工作的边界</h3> <p>第一，vToken 不是 eviction policy。H2O 如果淘汰错了 token，vToken 只会忠实地把错误的逻辑状态压紧，它不会替你恢复模型质量。eviction ratio 和 accuracy-preserving budget 仍然要由 policy 自己负责。</p> <p>第二，当前原型聚焦 single-node、single-GPU decode fast path，一个 KV cache group，并且保守跳过 shared-prefix block。多 GPU、多个 KV group、跨设备搬运需要额外处理，但这与 vToken 解决的 granularity mismatch 是正交问题。</p> <p>第三，回收需要 headroom。显存完全耗尽时，系统没有 destination block 可以搬，vToken 也救不了。因此它是一个 pressure-activated runtime extension，不是“显存满了再凭空变出空间”。Native vLLM 在没有内存压力时仍然是更简单的路径。</p> <h2 id="11-我的-take这是一层-runtime-abstraction不是又一个-cache-trick">11. 我的 take：这是一层 runtime abstraction，不是又一个 cache trick</h2> <p>KV Cache 优化大致有三条轴：</p> <ul> <li>表示层：quantization、compression，减少每个 KV 元素的字节数；</li> <li>访问层：sparse attention、token eviction，减少要看的历史 token；</li> <li>管理层：allocation、mapping、reclamation，决定省下来的空间能否被下一个请求使用。</li> </ul> <p>很多工作把前两层做得很漂亮，但最后一层仍然用 block-level runtime，于是优化结果卡在“逻辑上省了，物理上没释放”。vToken 的贡献就是把管理层补上：<strong>token-level liveness 必须成为 serving runtime 的一等公民。</strong></p> <p>这和刚刚解读的 OasisKV 是两条不同路线。OasisKV 解决的是“KV 已经在远端，怎么预测并预取”；vToken 解决的是“KV 已经淘汰，为什么 block 还不能释放”。一个优化时间/空间层次之间的数据搬运，一个修复逻辑生命周期和物理布局之间的接口。以后真正的 serving system 很可能需要把两者组合起来：先用 token-level policy 控制保留集合，再用 vToken 把碎片回收，最后用分层存储和 prefetch 把剩下的 KV 搬到合适的位置。</p> <h2 id="12-总结">12. 总结</h2> <p>如果只记住一句话：<strong>vToken 没有发明新的 token eviction，而是给 token eviction 补上了 block runtime 缺失的虚拟化层。</strong></p> <p>policy 负责回答“哪些 token 死了”，token table 负责记录“活 token 现在在哪”，reclamation backend 负责“什么时候把它们重新打包”，CUDA event 则负责“搬家时下一次 attention 不能读到半成品”。</p> <p>这套设计把低利用率 block 中被困住的显存变成了真正可复用的 KV capacity，在相同 SLA 和 allocator budget 下提高并发和吞吐，同时把新 eviction policy 的接入成本从 500+ 行降到 50 行以内。说到底，<strong>KV Cache 优化的终点不是少存几个 token，而是让省下来的每一个 token 真正回到系统的 free list。</strong></p> <hr/> <p>顺带一提，我们把 AutoML、NAS 和 LLM 压缩的相关积累整理成了《动手学 AutoML：从 NAS 到大语言模型优化实战》。这篇 vToken 讲的是 serving runtime 的内存管理，书里更偏模型和搜索层：第 8 章讨论 LLM 剪枝、量化和模型融合，第 11 章有 LLM 后训练剪枝实战。一个是在 runtime 里回收被浪费的空间，一个是在模型侧减少不必要的计算和参数，角度不同，但目标都是把有限资源用在真正影响结果的地方。</p> <blockquote> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="KV Cache"/><category term="vLLM"/><category term="推理系统"/><category term="论文解读"/><summary type="html"><![CDATA[arXiv’26 | vToken：KV Cache 淘汰后，为什么显存还是回收不回来？]]></summary></entry><entry><title type="html">arXiv’26 | OasisKV：KV Cache 超出 HBM 后，如何用 Lookahead Sparse Prefetch 把等待藏起来</title><link href="https://marsggbo.github.io/blog/2026/20260813-oasiskv/" rel="alternate" type="text/html" title="arXiv’26 | OasisKV：KV Cache 超出 HBM 后，如何用 Lookahead Sparse Prefetch 把等待藏起来"/><published>2026-08-13T00:00:00+00:00</published><updated>2026-08-13T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260813-oasiskv</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260813-oasiskv/"><![CDATA[<h1 id="arxiv26--oasiskvkv-cache-超出-hbm-后如何用-lookahead-sparse-prefetch-把等待藏起来">arXiv’26 | OasisKV：KV Cache 超出 HBM 后，如何用 Lookahead Sparse Prefetch 把等待藏起来</h1> <blockquote> <p>原文：<a href="https://arxiv.org/html/2608.08097v1">OasisKV: Scaling In-Decode KV Cache Beyond HBM with Lookahead Sparse Prefetching</a></p> </blockquote> <h2 id="1-先说结论这不是把-kv-cache-挪到-cpu这么简单">1. 先说结论：这不是“把 KV Cache 挪到 CPU”这么简单</h2> <p>长上下文 LLM 推理最容易被低估的地方，是生成阶段（decode）其实更像一个内存系统 benchmark，而不是一个 FLOP benchmark。每生成一个 token，模型新增的矩阵计算并不大，却要反复读取几千、几万甚至几十万历史 token 的 Key/Value（KV）向量。</p> <p>当 KV Cache 放不进 GPU 的 HBM（高带宽显存）时，最直觉的方案是把它放到 CPU 内存，算 attention 前再把需要的 block 搬回来。问题也正是从这里开始：<strong>如果“选 block + PCIe/网络传输”在关键路径上，稀疏 attention 节省的计算会被等待时间吃掉。</strong></p> <p>OasisKV 的切入点很朴素，但需要把几个环节同时做对：用一个便宜的 draft token 预测下一步会访问哪些 KV block；当前 token 做正常 attention 的同时，后台完成选择和预取；下一步到来时，GPU 只处理已经准备好的 sparse working set。论文把这套机制叫 <strong>Lookahead Sparse Prefetching</strong>。</p> <p>这篇工作的价值不在于又发明了一个 top-K 算法，而在于它把“稀疏性、分层存储、异步调度”放进同一条 decode pipeline 里，直接对准了远端 KV 的核心矛盾：<strong>容量在远端，带宽在本地，延迟不能暴露给前台。</strong></p> <h2 id="2-先把背景讲清楚llm-decode-为什么越来越像内存系统问题">2. 先把背景讲清楚：LLM decode 为什么越来越像内存系统问题</h2> <h3 id="21-prefill-和-decode-是两种完全不同的工作">2.1 Prefill 和 Decode 是两种完全不同的工作</h3> <p>LLM 推理通常分两段。</p> <ul> <li><strong>Prefill</strong>：把用户输入的整段 prompt 一次性读进去，计算每个 token 的表示。</li> <li><strong>Decode</strong>：模型已经开始生成后，每次只追加一个 token，再生成下一个 token。</li> </ul> <p>Prefill 更像大矩阵乘法，GPU 的计算单元容易吃饱。Decode 则相反：每一步新增的计算量不大，却要反复读取历史上下文。历史 token 在每一层注意力里的 Key 和 Value 会被缓存下来，这就是 KV Cache。</p> <p>可以把 KV Cache 想成模型的“工作记忆”。生成第 10000 个 token 时，模型要拿当前 Query 去和前面许多 token 的 Key 做匹配，再用匹配结果加权 Value。上下文越长，历史 KV 越大。</p> <h3 id="22-为什么-kv-cache-会成为瓶颈">2.2 为什么 KV Cache 会成为瓶颈</h3> <p>粗略地说，一个 batch 中长度为 $L$ 的请求，其 KV Cache 内存量近似为：</p> \[M_{KV} \approx B \times L \times N_{layer} \times 2N_{KV}d_h \times s\] <p>这里 $B$ 是 batch size，$N_{layer}$ 是层数，$N_{KV}$ 是 KV head 数量，$d_h$ 是 head dimension，$s$ 是每个元素的字节数，乘以 2 是因为同时保存 Key 和 Value。</p> <p>这条式子没有什么神秘之处：上下文翻倍，KV 就翻倍；并发请求翻倍，KV 也翻倍。模型权重可以通过量化和共享来压缩，但 KV Cache 是跟着每个请求、每个历史 token 增长的。</p> <p>于是 GPU 里出现了一个很实际的分层结构：HBM 容量小但带宽高，CPU 内存容量大但访问慢，远端内存或网络容量更大但延迟和带宽更差。问题从“GPU 能不能算”变成了“GPU 需要的数据能不能及时到”。</p> <p><img src="/assets/img/posts/20260813-oasiskv/figure1_full.png" alt="KV Cache 内存 roofline"/></p> <p>这张 roofline 图先把坐标轴说清楚：横轴是 <strong>Token-KV intensity</strong>，即每搬运一个 KV byte 能生成多少 token；纵轴是 decode throughput。黑、蓝、红三条斜线分别对应 HBM、CPU-GPU、远端内存带宽上限。往右走代表 KV reuse 更高或 attention 更稀疏，系统才有机会从“内存受限”爬回计算上限。图里的 OasisKV 星标沿着绿色箭头移动，含义不是凭空增加 HBM，而是通过更高的有效 intensity 和预取，把工作点从远端带宽限制区推向更高吞吐。</p> <h3 id="23-稀疏注意力解决了什么又没有解决什么">2.3 稀疏注意力解决了什么，又没有解决什么</h3> <p>全注意力会扫描整个历史 KV。稀疏注意力则只选择最重要的一小部分 KV block。一个 block 可以理解成连续的一小段历史 token，系统先估计每个 block 对当前 Query 的重要性，再只对 Top-K block 做精确 attention。</p> <p>这样做同时减少了计算量和需要读取的数据量，但它有一个隐含前提：<strong>Top-K 选择必须足够准</strong>。如果选错，模型的答案可能变差；如果每一步选出来的 block 都要从 CPU 搬到 GPU，搬运也可能成为新的瓶颈。</p> <p>这就是 OasisKV 和普通 sparse attention 的分界线：它关心的不只是哪一块 KV 重要，还关心下一步会重要哪一块，以及能不能在计算当前 token 的时候把下一步的 KV 搬过来。</p> <h2 id="3-现有方法的问题算得少不代表等得少">3. 现有方法的问题：算得少，不代表等得少</h2> <p>论文在 Qwen3-32B、H100、32K context 上给了一个很有杀伤力的 latency breakdown：on-demand KV retrieval 把选择、PCIe transfer 和 attention 串在一起，单步 TPOT（time per output token，生成一个 token 的平均耗时）里，传输直接变成大头。也就是说，<strong>把 full KV 放到 CPU 只解决了容量，不解决 decode critical path。</strong></p> <p><img src="/assets/img/posts/20260813-oasiskv/figure2_full.png" alt="按需 KV retrieval 的 TPOT 分解"/></p> <p>先看四种 decode 流程的差别。</p> <p><img src="/assets/img/posts/20260813-oasiskv/figure3_p3.png" alt="四种 Decode 流程对比"/></p> <p>全注意力把所有 KV 放在 GPU 上，计算慢、容量也受限。稀疏注意力只计算 Top-K，但 KV 仍然需要在 GPU 上。KV retrieval 允许大部分 KV 放在 CPU，却在每一步先 fetch，再 attention，于是容量问题缓解了，延迟问题没有。</p> <p>KV prefetch 的目标是把 fetch 和当前 attention 重叠起来：当前步骤做前景计算，后台同时准备下一步数据。只要预取足够早、预测足够准，下一步 attention 就不必再停下来等 PCIe 或网络。</p> <h2 id="4-oasiskv-的关键观察下一个-query-可以提前预测">4. OasisKV 的关键观察：下一个 Query 可以提前预测</h2> <h3 id="41-draft-token-提供了一个便宜的-lookahead">4.1 Draft token 提供了一个便宜的 lookahead</h3> <p>自回归生成的第 $t+1$ 个 token，通常由第 $t$ 步的模型输出决定。OasisKV 利用这个顺序关系，在当前 token 的正常 forward 之外，构造一个轻量的 draft token，然后用 draft token 对下一步的 Query 做预测。这里的 draft 不是凭空训练出来的另一个模型，而是借用了 speculative decoding 里已经比较成熟的 <strong>MTP（multi-token prediction，多 token 预测）</strong>信号；论文实验采用 EAGLE-3 draft head。它的作用类似“先用一个便宜的小头猜后续 token”，主模型仍然负责最终输出。</p> <p>更细一点看，draft token 先在当前层得到 draft hidden state，随后沿着 transformer layer 传播出 draft Query。这个传播过程不需要为每一层重新读取 full KV：GPU 只维护每个 block 的 compressed key 摘要，draft Query 对摘要做一次便宜的 Top-K ranking。真正完整的 Key/Value 只在 block 被选中后才从 CPU pool 搬运。于是 lookahead 的成本主要是两条短路径：共享一次 QKV/权重计算，再扫描每个 block 的两个压缩向量。</p> <p>预测并不需要把下一步完整算完。论文观察到，用 lookahead token 做 KV block 预测，比用 previous token 更准：所有层上的 Top-20 KV-block 预测准确率，前者平均约 96.0%，后者约 83.9%。这里的准确率是预测集合与下一步真实 Top-K 集合的重合程度，越高意味着预取的浪费越少、cache miss 越少。</p> <p><img src="/assets/img/posts/20260813-oasiskv/figure4_p4.png" alt="Lookahead token 的 KV block 预测准确率"/></p> <p>这个差别很关键。预取的本质是“提前下注”：猜对，传输可以隐藏在计算后面；猜错，GPU 要么白搬数据，要么下一步仍然要等待。</p> <h3 id="42-look-ahead-attention-做两件事">4.2 Look-ahead attention 做两件事</h3> <p>OasisKV 的 look-ahead attention 可以拆成前景和后台两条路径：</p> <p>前景路径使用当前 Query，在当前的 sparse KV working set 上完成真正的 attention，并继续生成下一个 token。</p> <p>后台路径使用 draft Query，先扫描 GPU 上更小的 compressed key pool，预测下一步的 Top-K block；然后从 CPU KV pool 选出这些 block，发起 KV transfer。</p> <p>compressed key pool 是怎么来的？CPU 上保留完整 KV，GPU 上则额外保存每个 block 的压缩 Key。论文使用 block 内 Key 的 min/max pooling 来构造紧凑表示。它不负责最后的精确 attention，只负责快速判断“这个 block 可能重要吗”。真正的 Value 和完整 Key，仍然只对入选 block 搬运和计算。这个设计的关键是把“候选筛选”和“精确 attention”拆开：前者必须便宜到能和前台并行，后者才值得消耗带宽。</p> <p><img src="/assets/img/posts/20260813-oasiskv/figure7_p6.png" alt="Look-ahead attention 的数据流"/></p> <p>图中左侧是 CPU 的 full KV 如何映射成 GPU 上的 compressed key 和 sparse KV 两种缓存；右侧是正常 Query 做 attention 的同时，draft Query 扫 compressed key，预测下一步需要的 block。</p> <h3 id="43-预取流水线为什么能隐藏延迟">4.3 预取流水线为什么能隐藏延迟</h3> <p>它不是简单地把“预测”挪到前面，而是把跨两个 decode step 的依赖排成流水线：</p> <p><img src="/assets/img/posts/20260813-oasiskv/figure8_p6.png" alt="跨 decode step 的异步预取流水线"/></p> <p>在 step $t$，模型一边执行当前层的 QKV projection、attention、FFN，一边为 step $t+1$ 做 Top-K prediction、KV selection 和 KV transfer。下一步开始时，被预测的 KV 应该已经抵达 GPU。</p> <p>这里有一个工程上的取舍：预取不能无限大。预取太少，命中不了下一步所需的 block；预取太多，又会浪费 PCIe/网络带宽和 GPU 内存。OasisKV 因此提供 fetch-cap，并结合 cache eviction 和 request scheduling 管理有限的工作集。</p> <p>论文的 ablation 很说明问题：在 Qwen3-8B 的 AIME24 workload 上，fetch ratio 设为 0.05 时，吞吐约 2,083 tok/s，准确率比 dense 只低 0.1 个百分点；如果把 cap 放开到“几乎全取”，吞吐反而掉到约 824 tok/s，而准确率只从 74.9 小幅涨到 77.4。<strong>多搬 6 倍数据，换不到同等比例的正确率。</strong>这也是系统论文和单纯 sparse attention 论文的区别：预算不是一个离线超参数，而是每一步都在和互联带宽竞争的在线资源。</p> <h3 id="44-为什么需要-head-wise-映射">4.4 为什么需要 head-wise 映射</h3> <p>GPU 的 sparse working set 和 CPU 的 full KV 并不是同一张 page table。OasisKV 为每个 KV head 维护额外的 GPU-logical-block 到 CPU-logical-block 映射；原有 page table 仍然负责请求内的逻辑 token 到物理页转换。这样做看起来像 bookkeeping，但它决定了“命中一个 GPU block”到底对应 CPU 上哪一段连续 KV，也决定了 eviction 后能否无锁地复用 GPU page。</p> <p><img src="/assets/img/posts/20260813-oasiskv/figure9_p7.png" alt="GPU working set 到 CPU full KV 的映射"/></p> <p>举个具体例子：假设 block size 是 16，某个 head 的 GPU logical block 7 当前映射到 CPU logical block 31，那么 token 120（$120//16=7$）在 GPU 上查到 block 7 后，miss 时要从 CPU 的 block 31 取 offset 8，而不是重新扫描整条 KV。这个额外映射把“稀疏选择”变成真正可执行的 block transfer。</p> <h2 id="5-系统架构三层-kv-pool-加两个平面">5. 系统架构：三层 KV Pool 加两个平面</h2> <p><img src="/assets/img/posts/20260813-oasiskv/figure5_p5.png" alt="OasisKV 系统架构"/></p> <p>OasisKV 的架构可以按“数据放在哪里”和“谁负责调度”来理解。</p> <p>数据侧有三类 pool：</p> <ul> <li>GPU 上的 Sparse KV Pool：当前真正参与 attention 的稀疏工作集。</li> <li>GPU 上的 Compressed Key Pool：用于快速预测下一步重要 block。</li> <li>CPU/远端内存上的完整 KV Pool：容量足够大，保存完整历史。</li> </ul> <p>系统侧有三个平面：</p> <ul> <li>Compute plane：执行前景 forward 和后台 lookahead attention。</li> <li>Control plane：由 scheduler 和 pool manager 负责请求调度、缓存准入、淘汰和映射。</li> <li>Memory plane：负责 GPU、CPU 和远端内存之间的数据传输。</li> </ul> <p>这个拆分的价值在于，KV 不再被当成“必须全驻留 GPU 的一块连续张量”，而是被当成可按 block 管理、按 head 访问、可异步迁移的分层数据。</p> <h2 id="6-远端内存场景不必每次都传完整-block">6. 远端内存场景：不必每次都传完整 block</h2> <p>当 KV 放在另一台机器上时，瓶颈更明显：网络往返和传输时间可能比一次 sparse attention 还长。OasisKV 进一步设计了 remote partial fetching。</p> <p><img src="/assets/img/posts/20260813-oasiskv/figure10_p8.png" alt="Remote partial fetching"/></p> <p>在 admission 阶段，系统可以先把部分 KV 从远端搬入 CPU/GPU 层级；在 decode 阶段，如果某些 block 已经在本地，就只通过网络取缺失部分。对 draft KV 也可以做部分传输，减少不必要的网络流量。具体来说，decode worker 初次接收请求时只拿三样东西：第一步 working set、compressed-key cache、draft token 的 KV state。prefill worker 可以在 host DRAM staging 的同时释放自己的 GPU page，不必等整条 full KV 传完。这一点对 prefill-decode disaggregation 很重要，否则 decode 节点的 host memory 会被“还没开始生成的请求”提前吃满。</p> <p>这背后的原则是：<strong>访问粒度、缓存粒度和传输粒度不一定要完全相同</strong>。把整条 KV Cache 作为一个不可分割的对象，系统很难做容量控制；拆成 block 后，才能做 Top-K 选择、部分预取和按需淘汰。</p> <h2 id="7-实验结果准确率只是入场券重叠才决定速度">7. 实验结果：准确率只是入场券，重叠才决定速度</h2> <p>OasisKV 的实验重点不是单独证明某一个 kernel 更快，而是验证整条链路是否成立：</p> <ol> <li>lookahead token 能否准确预测下一步的 KV block；</li> <li>预取是否真的和前景计算重叠；</li> <li>GPU HBM 不够时，额外容量是否能换来更好的吞吐和并发；</li> <li>放到 disaggregated serving 后，网络传输是否仍然可控。</li> </ol> <p>论文报告的端到端结果是：在 Qwen3-8B 的 AIME24 reasoning workload 上，OasisKV 达到 2,083 tok/s，而 dense vLLM 是 1,235 tok/s，即约 1.69x throughput，准确率只低 0.1 个百分点；multi-GPU long-context serving 的最高收益是 2.1x。prefill-decode disaggregation 场景下，系统吞吐是 dense 的约 2.1–2.3x。它不会先搬完整条 KV，而是把 admission 时的网络传输量降到 full transfer 的 1/6.5–1/9.7；对应地，decode 节点的每请求 host-memory 占用可降到约 1/2.2–1/2.6。这里的 accuracy point 指任务准确率的绝对百分点差，不是相对百分比，论文的意思是用很小的质量代价换吞吐。</p> <p>但这些数字不能脱离前提看：lookahead token 的预测集合要足够准，异步 transfer 要能被当前层的 attention/FFN 覆盖，而且请求调度不能让多个 request 同时把 PCIe 或网络打爆。换句话说，<strong>预测准确率是入场券，真正决定收益的是 transfer 是否被重叠。</strong></p> <p>但这里也要看清边界：OasisKV 不是把 HBM 变成了同等速度的“大内存”。如果工作负载的访问模式变化剧烈，或者网络/PCIe 带宽不足以覆盖预取窗口，仍然会出现 cache miss 和等待。它做的是把可预测的远端访问尽可能提前，并把剩余等待压缩到最小。</p> <h2 id="8-我对这篇工作的理解">8. 我对这篇工作的理解</h2> <p>OasisKV 最有意思的地方，是它把 sparse attention 的问题从“选哪些 token”推进到了“什么时候开始搬、搬多少、放在哪一层”。这一步很重要，因为真实推理系统里的延迟通常不是某个算子孤立决定的，而是计算、内存和调度共同决定的。</p> <p>它也提醒我们，KV Cache 优化有三个不同层次：</p> <ul> <li><strong>表示层</strong>：量化、压缩，减少每个 KV 元素的字节数；</li> <li><strong>访问层</strong>：稀疏注意力，只访问少数重要 block；</li> <li><strong>时间层</strong>：预取和流水线，把访问延迟藏到当前计算后面。</li> </ul> <p>只做其中一层，往往会把瓶颈推给下一层。OasisKV 的贡献在于把三层放到一个 serving architecture 里共同设计。</p> <p>当然，lookahead 也不是免费午餐。它需要额外的 draft/预测计算，需要更复杂的 cache manager 和 scheduler，还要处理预测错误、淘汰冲突和多请求之间的带宽竞争。未来真正落地时，模型架构、量化格式、GPU 到 CPU 的互联，以及请求长度分布都会影响收益。</p> <h2 id="9-总结">9. 总结</h2> <p>如果只记住一句话：<strong>OasisKV 用当前 decode 的“草稿未来”预测下一步会访问哪些 KV block，再把这些 block 在后台提前搬到 GPU，从而让超出 HBM 容量的 KV Cache 也能参与低等待生成。</strong></p> <p>它不是简单地把显存换成内存，而是在做一个面向 LLM decode 的分层缓存系统：GPU 保存稀疏工作集和压缩索引，CPU/远端保存完整 KV，预测器和传输线程负责把下一步需要的数据提前准备好。</p> <p>原文链接：<a href="https://arxiv.org/html/2608.08097v1">OasisKV: Scaling In-Decode KV Cache Beyond HBM with Lookahead Sparse Prefetching</a></p> <hr/> <p>顺带一提，我们把 AutoML、NAS 以及 LLM 压缩的相关积累整理成了一本《动手学 AutoML：从 NAS 到大语言模型优化实战》。这篇文章讲的是 KV Cache 的系统层搬运，书里更偏模型和搜索层：第 8 章讨论 LLM 剪枝、量化和模型融合，第 11 章有 LLM 后训练剪枝的完整实战。角度不同，但核心都是同一件事——在有限的计算和内存预算下，把每一份资源花在真正影响结果的地方。</p> <blockquote> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="KV Cache"/><category term="稀疏注意力"/><category term="推理系统"/><category term="论文解读"/><summary type="html"><![CDATA[arXiv’26 | OasisKV：KV Cache 超出 HBM 后，如何用 Lookahead Sparse Prefetch 把等待藏起来]]></summary></entry><entry><title type="html">ICML’24 | Quest：不用扫描全部 KV Cache，Query-aware Sparsity 如何加速长上下文推理</title><link href="https://marsggbo.github.io/blog/2026/20260813-quest/" rel="alternate" type="text/html" title="ICML’24 | Quest：不用扫描全部 KV Cache，Query-aware Sparsity 如何加速长上下文推理"/><published>2026-08-13T00:00:00+00:00</published><updated>2026-08-13T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260813-quest</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260813-quest/"><![CDATA[<h1 id="icml24--quest不用扫描全部-kv-cachequery-aware-sparsity-如何加速长上下文推理">ICML’24 | Quest：不用扫描全部 KV Cache，Query-aware Sparsity 如何加速长上下文推理</h1> <blockquote> <p>原文：<a href="https://arxiv.org/pdf/2406.10774">Quest: Query-Aware Sparsity for Efficient Long-Context LLM Inference</a></p> </blockquote> <h2 id="1-前言">1. 前言</h2> <p>长上下文 LLM 的一个直觉误区是：上下文窗口能做到 128K、1M token，模型就会“均匀地使用”这么长的上下文。实际生成时，当前问题往往只需要历史中的一小部分信息。</p> <p>全注意力的问题在于，它每生成一个 token，都要让当前 Query 和整个历史 KV Cache 做匹配。历史越长，读取和计算越重；而真正有用的 token 可能只占很小一部分。</p> <p>Quest 的问题非常直接：</p> <blockquote> <p>如果每个 Query 只需要少数几个 KV page，能不能先找出这些 page，再只对它们做 attention？</p> </blockquote> <p>论文给出的答案是可以，而且关键不在于固定保留“最近 token”或“最早 token”，而在于让<strong>当前 Query 决定当前该看哪里</strong>。这就是 Query-aware sparsity。</p> <h2 id="2-背景attention-到底在算什么">2. 背景：Attention 到底在算什么</h2> <h3 id="21-一次-attention-就像一次相关性检索">2.1 一次 attention 就像一次相关性检索</h3> <p>对当前 token 来说，模型会产生一个 Query $q$；历史 token 各自有 Key $k_i$ 和 Value $v_i$。注意力大致是：</p> \[Attention(q,K,V)=softmax(qK^T)V\] <p>第一步 $qK^T$ 是相关性打分：当前问题和哪些历史 token 更相关？第二步用这些分数加权 Value，把信息取回来。</p> <p>因此，Attention 也可以看成一个“以 Query 为条件的检索器”。问题是传统实现会把所有 Key 都扫一遍，哪怕最后只有少数 token 的分数真正重要。</p> <h3 id="22-kv-cache-为什么拖慢-decode">2.2 KV Cache 为什么拖慢 Decode</h3> <p>Prefill 阶段可以并行处理 prompt；Decode 阶段每次只生成一个 token，却要重复读取不断增长的历史 K/V。这个阶段经常不是算力不够，而是内存带宽不够：GPU 的计算单元在等待 KV 从显存中流过来。</p> <p>把历史 KV 切成 page/block 后，系统可以把 page 当成检索单元。Quest 的目标不是预测每一个 token 的精确 attention 分数，而是先判断哪些 page 值得保留，然后对入选 page 做精确 attention。</p> <h3 id="23-为什么常见的稀疏策略不够">2.3 为什么常见的稀疏策略不够</h3> <p>常见方法各有合理性，但都带着固定偏好：</p> <ul> <li>Sliding window 只看最近 token，长距离引用会丢失；</li> <li>StreamingLLM 保留开头的 attention sink 和最近窗口，但中间的重要事实未必在这里；</li> <li>H2O/TOVA 等方法根据历史累计分数或特定规则淘汰，未必适合下一个 Query；</li> <li>固定比例稀疏虽然简单，但不同 Query 需要的预算和位置可能完全不同。</li> </ul> <p>论文 Figure 2 用一个很小的例子说明了这一点：同一段文本里，当前 Query 变成不同 token 后，真正重要的历史位置也会变。稀疏性必须 query-aware，而不是只看位置或历史平均分数。</p> <p><img src="/assets/img/posts/20260813-quest/figure2_p2.png" alt="Query 改变时的注意力位置"/></p> <h2 id="3-quest-的核心先找重要-page再做精确-attention">3. Quest 的核心：先找重要 page，再做精确 attention</h2> <h3 id="31-为什么要用-page而不是逐-token-选择">3.1 为什么要用 page，而不是逐 token 选择</h3> <p>逐 token 选择看似精细，但会产生大量索引和不连续访存。Quest 把连续的历史 token 分成 page，每个 page 内有一段 Key/Value。这样做有三个好处：</p> <ol> <li>page 是 GPU 友好的连续内存块；</li> <li>选择的索引数量从 token 数降到 page 数；</li> <li>page 粒度可以在选择开销和稀疏收益之间调节。</li> </ol> <p>page 越大，索引开销越小，但一个 page 里混入无关 token 的概率越高；page 越小，选择更精细，但 metadata 和 gather 开销会上升。</p> <h3 id="32-用-page-的-key-统计量近似-page-重要性">3.2 用 page 的 Key 统计量近似 page 重要性</h3> <p>对于一个 page，Quest 不必先把 page 内所有 Key 都拿出来做完整 attention。它对 page 内 Key 做 min/max pooling，得到一个很小的摘要。</p> <p>对当前 Query $q$，摘要可以快速给出该 page 的相关性上界或近似分数。然后把 page 按分数排序，选择 Top-K page。最后才读取入选 page 的完整 KV，执行精确 attention。</p> <p>可以把整个过程想成搜索引擎：先用索引筛掉明显不相关的文档，再对候选文档逐字阅读。索引阶段允许是近似的，但候选集必须尽量不要漏掉真正相关的 page。</p> <p><img src="/assets/img/posts/20260813-quest/figure3_p3.png" alt="Quest 的 page 选择流程"/></p> <h3 id="33-top-k-page-预测不是另一个完整-attention">3.3 Top-K page 预测不是另一个完整 attention</h3> <p>Quest 的关键优化是让 page selection 足够便宜。它利用 page 级别的 Key 摘要完成粗排，再把少量候选 page 的完整 KV 交给 attention kernel。</p> <p>最终计算近似为：</p> \[Attention(q,K,V) \approx Attention(q,K_{TopK},V_{TopK})\] <p>这里的近似成立依赖于一个条件：Top-K page 覆盖真正有贡献的历史位置。论文的实验显示，在很多长依赖任务中，Query-aware 的选择可以用很小的 KV budget 保持接近 full attention 的效果。</p> <h2 id="4-系统实现算法和-kernel-必须一起设计">4. 系统实现：算法和 kernel 必须一起设计</h2> <p>如果只在 Python 层做 page 索引，然后把挑出来的 KV 拼接成一个新张量，索引和内存拷贝很可能吃掉稀疏带来的收益。因此 Quest 还需要专门的 GPU 实现。</p> <p>一个高效实现通常包含三步：</p> <ol> <li>对 page 摘要计算与 Query 的相关性；</li> <li>用并行 reduction 找出 Top-K page；</li> <li>在不产生大规模中间张量的情况下，对入选 page 做 attention。</li> </ol> <p>这也是长上下文优化经常被低估的地方：理论上只算 1/16 的 KV，不代表 wall-clock 就是 1/16。gather、排序、kernel launch 和非连续访存都要付钱。Quest 的贡献是把 query-aware page selection 做成了可实际运行的 self-attention 实现，而不是只提出一个离线剪枝指标。</p> <h2 id="5-准确率稀疏预算不是越小越好">5. 准确率：稀疏预算不是越小越好</h2> <p>Quest 的稀疏 attention 通常会给每层一个 KV budget，同时保留模型前两层的 full cache。原因很直观：早期层的表示和全局信息传播更敏感，把它们完整保留可以降低误差；后面的层再使用 query-aware sparsity。</p> <p>实验中，Quest 在不同 budget 下都明显优于 H2O、TOVA 和 StreamingLLM。一个很直观的结果是，在较小 page budget 下，Quest 仍能保持较高的检索准确率，而其他方法很快掉到接近零的命中水平。</p> <p><img src="/assets/img/posts/20260813-quest/figure6_p6.png" alt="不同 KV budget 下的检索准确率"/></p> <p>对长文本中的事实问答，这种差异尤其重要。固定保留最近窗口的方法可能把答案所在的中间位置丢掉；Quest 每一步重新根据 Query 选择，因此能把远距离但当前相关的信息找回来。</p> <h2 id="6-速度真正要看端到端而不是只看稀疏比例">6. 速度：真正要看端到端，而不是只看稀疏比例</h2> <p>论文报告 Quest 最高可以获得 7.03 倍的 self-attention 加速，并带来最高 2.23 倍的 decode 阶段端到端延迟下降。与已有稀疏方法在相同准确率目标下比较，Quest 的 self-attention 延迟最高降低 4.5 倍。</p> <p><img src="/assets/img/posts/20260813-quest/figure9_p8.png" alt="不同上下文长度下的 attention 延迟"/></p> <p>这几个数字要正确理解：7.03 倍是 attention 子模块的最高加速，2.23 倍是整个 decode 阶段的最高延迟收益，二者不是同一个指标。端到端收益更小是正常的，因为每一步还包含 QKV projection、FFN、采样、调度和其他固定开销。</p> <p>在相同准确率约束下，Quest 相比基线在 GovReport 上最高有 3.82 倍推理速度提升，在 TriviaQA 上最高有 4.54 倍提升。换句话说，Quest 的优势不只是“可以更激进地丢 KV”，而是同样的效果下需要更少的 KV 访问。</p> <p><img src="/assets/img/posts/20260813-quest/figure11_p9.png" alt="相同准确率约束下的效率比较"/></p> <h2 id="7-一个具体例子为什么-query-aware-比最近窗口更合理">7. 一个具体例子：为什么 Query-aware 比最近窗口更合理</h2> <p>假设上下文里有这样一段内容：</p> <blockquote> <p>A is B. C is D. A is …</p> </blockquote> <p>当当前 Query 需要回答和 A 相关的问题，答案可能在很早的 B 附近；当 Query 需要回答和 D 相关的问题，重要位置又会变成 D 所在 page。最近窗口策略只知道“最后一段最重要”，但 Quest 会用当前 Query 重新计算 page 的相关性。</p> <p>这也是我觉得 Quest 最值得记住的抽象：长上下文并不等于每次都需要长计算。模型的上下文窗口可以很长，但每个 decode step 的有效感受野可能是稀疏的，而且这个稀疏模式由问题决定。</p> <h2 id="8-局限性和工程代价">8. 局限性和工程代价</h2> <p>Quest 不是无条件替代 full attention。</p> <p>首先，page 选择本身需要计算。如果 page 数很少，或者上下文很短，选择开销可能抵消收益。其次，page 太大时会引入无关 token，page 太小时又会增加索引和访存开销。再次，某些任务确实需要聚合大量上下文，稀疏预算太小会损失信息。</p> <p>此外，模型和硬件实现必须配合。不同 GPU、不同 batch size、不同 KV layout 下，Top-K、gather 和 attention kernel 的开销不同。论文展示的是一套有效的 query-aware 实现，但部署时仍然需要按真实 workload 调 page size 和 budget。</p> <h2 id="9-总结">9. 总结</h2> <p>Quest 的一句话版本是：<strong>把 Attention 当成 Query 驱动的 KV 检索，先在 page 粒度找 Top-K，再对这些 page 做精确 attention。</strong></p> <p>它解决了长上下文 decode 中“每一步都扫描完整 KV Cache”的浪费，并且把准确率、page 粒度和 GPU kernel 放在一起考虑。后来的 KV 分层和预取工作，正是在这个方向上继续追问：既然只需要少数 KV page，这些 page 能不能还没用到时就提前搬到 GPU？</p> <p>原文链接：<a href="https://arxiv.org/pdf/2406.10774">Quest: Query-Aware Sparsity for Efficient Long-Context LLM Inference</a></p>]]></content><author><name></name></author><category term="LLM"/><category term="KV Cache"/><category term="Attention"/><category term="长上下文"/><category term="论文解读"/><summary type="html"><![CDATA[ICML’24 | Quest：不用扫描全部 KV Cache，Query-aware Sparsity 如何加速长上下文推理]]></summary></entry><entry><title type="html">Codex 同时兼容 AWS Bedrock 和 OneAPI 的配置教程</title><link href="https://marsggbo.github.io/blog/2026/Codex-%E5%90%8C%E6%97%B6%E5%85%BC%E5%AE%B9-AWS-Bedrock-%E5%92%8C-OneAPI-%E9%85%8D%E7%BD%AE%E6%95%99%E7%A8%8B/" rel="alternate" type="text/html" title="Codex 同时兼容 AWS Bedrock 和 OneAPI 的配置教程"/><published>2026-08-13T00:00:00+00:00</published><updated>2026-08-13T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/Codex-%E5%90%8C%E6%97%B6%E5%85%BC%E5%AE%B9-AWS-Bedrock-%E5%92%8C-OneAPI-%E9%85%8D%E7%BD%AE%E6%95%99%E7%A8%8B</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/Codex-%E5%90%8C%E6%97%B6%E5%85%BC%E5%AE%B9-AWS-Bedrock-%E5%92%8C-OneAPI-%E9%85%8D%E7%BD%AE%E6%95%99%E7%A8%8B/"><![CDATA[<h1 id="codex-同时兼容-aws-bedrock-和-oneapi-的配置教程">Codex 同时兼容 AWS Bedrock 和 OneAPI 的配置教程</h1> <p>最近公司内部同时提供了两种 LLM 接入方式：</p> <ul> <li><code class="language-plaintext highlighter-rouge">AWS Bedrock</code></li> <li><code class="language-plaintext highlighter-rouge">OneAPI</code></li> </ul> <p>而 Codex 默认只会读取一套 <code class="language-plaintext highlighter-rouge">~/.codex/config.toml</code>。如果直接在同一个文件里来回改 <code class="language-plaintext highlighter-rouge">model_provider</code>，短期能用，长期一定会乱：</p> <ul> <li>今天切到 Bedrock，明天忘了切回来；</li> <li>OneAPI 的 key 和 Bedrock 的配置混在一起，不容易排查；</li> <li>以后再加第三种 provider，整个配置会更难维护。</li> </ul> <p>我最后采用的方案是：</p> <blockquote> <p><strong>保留一份基础配置 + 用 Codex 原生 <code class="language-plaintext highlighter-rouge">profile</code> 机制切换不同 provider。</strong></p> </blockquote> <p>这个方案有几个优点：</p> <ul> <li>不需要维护多份 <code class="language-plaintext highlighter-rouge">CODEX_HOME</code></li> <li>不需要来回覆盖 <code class="language-plaintext highlighter-rouge">config.toml</code></li> <li>切换命令简单，<code class="language-plaintext highlighter-rouge">codex --profile xxx</code> 即可</li> <li>Bedrock 和 OneAPI 的配置可以完全分开，后面继续扩展也方便</li> </ul> <p>下面把完整做法记录一下。</p> <hr/> <h2 id="1-最终目录结构">1. 最终目录结构</h2> <p>我最后采用的是下面这套结构：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>~/.codex/
├── .env
├── config.toml
├── bedrock.config.toml
└── oneapi.config.toml
</code></pre></div></div> <p>含义分别是：</p> <ul> <li><code class="language-plaintext highlighter-rouge">config.toml</code>：基础配置</li> <li><code class="language-plaintext highlighter-rouge">bedrock.config.toml</code>：AWS Bedrock 专用 profile</li> <li><code class="language-plaintext highlighter-rouge">oneapi.config.toml</code>：OneAPI 专用 profile</li> <li><code class="language-plaintext highlighter-rouge">.env</code>：OneAPI 的 API Key</li> </ul> <hr/> <h2 id="2-为什么不用一份-configtoml-硬切">2. 为什么不用一份 config.toml 硬切</h2> <p>最直接的想法其实是：</p> <div class="language-toml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">model_provider</span> <span class="o">=</span><span class="w"> </span><span class="s">"amazon-bedrock"</span>
</code></pre></div></div> <p>用 Bedrock 时写这个，用 OneAPI 时改成：</p> <div class="language-toml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">model_provider</span> <span class="o">=</span><span class="w"> </span><span class="s">"oneapi"</span>
</code></pre></div></div> <p>但这个方案的问题是：<strong>每次切 provider 都要改配置文件本身</strong>。</p> <p>一旦你还有下面这些差异项：</p> <ul> <li><code class="language-plaintext highlighter-rouge">model</code></li> <li><code class="language-plaintext highlighter-rouge">model_reasoning_effort</code></li> <li><code class="language-plaintext highlighter-rouge">base_url</code></li> <li><code class="language-plaintext highlighter-rouge">API Key</code></li> <li><code class="language-plaintext highlighter-rouge">AWS region</code></li> </ul> <p>那来回切换就会很烦，而且很容易忘。</p> <p>Codex 本身原生支持 <code class="language-plaintext highlighter-rouge">profile</code> 文件，所以更好的思路是：</p> <ul> <li><code class="language-plaintext highlighter-rouge">~/.codex/config.toml</code> 放基础配置</li> <li><code class="language-plaintext highlighter-rouge">~/.codex/bedrock.config.toml</code> 放 Bedrock 差异</li> <li><code class="language-plaintext highlighter-rouge">~/.codex/oneapi.config.toml</code> 放 OneAPI 差异</li> </ul> <p>然后使用：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>codex <span class="nt">--profile</span> bedrock
codex <span class="nt">--profile</span> oneapi
</code></pre></div></div> <p>来切换。</p> <hr/> <h2 id="3-基础配置codexconfigtoml">3. 基础配置：<code class="language-plaintext highlighter-rouge">~/.codex/config.toml</code></h2> <p>我这里保留了当前默认使用 Bedrock 的基础配置：</p> <div class="language-toml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">model</span> <span class="o">=</span><span class="w"> </span><span class="s">"openai.gpt-5.6-sol"</span>
<span class="n">model_reasoning_effort</span> <span class="o">=</span><span class="w"> </span><span class="s">"medium"</span>
<span class="n">model_provider</span> <span class="o">=</span><span class="w"> </span><span class="s">"amazon-bedrock"</span>

<span class="k">[</span><span class="n">model_providers</span><span class="k">.</span><span class="n">amazon-bedrock</span><span class="k">.</span><span class="n">aws</span><span class="k">]</span>
<span class="n">region</span> <span class="o">=</span><span class="w"> </span><span class="s">"us-east-2"</span>
</code></pre></div></div> <p>这里有两个注意点：</p> <ol> <li>这份基础配置可以直接默认走 Bedrock。</li> <li>如果你本机还有 <code class="language-plaintext highlighter-rouge">trusted project</code> 之类的个人配置，可以继续保留，但不要写进公开教程。</li> </ol> <hr/> <h2 id="4-bedrock-profilecodexbedrockconfigtoml">4. Bedrock profile：<code class="language-plaintext highlighter-rouge">~/.codex/bedrock.config.toml</code></h2> <p>Bedrock 的 profile 单独拆出来如下：</p> <div class="language-toml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">model</span> <span class="o">=</span><span class="w"> </span><span class="s">"openai.gpt-5.6-sol"</span>
<span class="n">model_reasoning_effort</span> <span class="o">=</span><span class="w"> </span><span class="s">"medium"</span>
<span class="n">model_provider</span> <span class="o">=</span><span class="w"> </span><span class="s">"amazon-bedrock"</span>

<span class="k">[</span><span class="n">model_providers</span><span class="k">.</span><span class="n">amazon-bedrock</span><span class="k">.</span><span class="n">aws</span><span class="k">]</span>
<span class="n">region</span> <span class="o">=</span><span class="w"> </span><span class="s">"us-east-2"</span>
</code></pre></div></div> <p>这份配置很薄，只保留跟 Bedrock 强相关的差异项即可。</p> <p>如果你们公司用的是 AWS SSO 或命名好的 profile，也可以继续走 AWS 标准凭证链，例如：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">AWS_PROFILE</span><span class="o">=</span>codex-bedrock
</code></pre></div></div> <p>或者事先：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>aws sso login <span class="nt">--profile</span> codex-bedrock
</code></pre></div></div> <hr/> <h2 id="5-oneapi-profilecodexoneapiconfigtoml">5. OneAPI profile：<code class="language-plaintext highlighter-rouge">~/.codex/oneapi.config.toml</code></h2> <p>OneAPI 这边的 profile 如下：</p> <div class="language-toml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">model</span> <span class="o">=</span><span class="w"> </span><span class="s">"gpt-5.5"</span>
<span class="n">model_reasoning_effort</span> <span class="o">=</span><span class="w"> </span><span class="s">"xhigh"</span>
<span class="n">model_provider</span> <span class="o">=</span><span class="w"> </span><span class="s">"oneapi"</span>
<span class="n">disable_response_storage</span> <span class="o">=</span><span class="w"> </span><span class="kc">true</span>

<span class="k">[</span><span class="n">model_providers</span><span class="k">.</span><span class="n">oneapi</span><span class="k">]</span>
<span class="n">name</span> <span class="o">=</span><span class="w"> </span><span class="s">"oneapi"</span>
<span class="n">base_url</span> <span class="o">=</span><span class="w"> </span><span class="s">"https://oneapi-comate.baidu-int.com/v1"</span>
<span class="n">wire_api</span> <span class="o">=</span><span class="w"> </span><span class="s">"responses"</span>
<span class="n">env_key</span> <span class="o">=</span><span class="w"> </span><span class="s">"ONEAPI_API_KEY"</span>
</code></pre></div></div> <p>这里我<strong>没有</strong>采用把 key 写进 <code class="language-plaintext highlighter-rouge">auth.json</code> 的方案，而是改成：</p> <div class="language-toml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">env_key</span> <span class="o">=</span><span class="w"> </span><span class="s">"ONEAPI_API_KEY"</span>
</code></pre></div></div> <p>原因很简单：</p> <ul> <li><code class="language-plaintext highlighter-rouge">OneAPI</code> 在 Codex 里本质上是一个<strong>自定义 provider</strong></li> <li>对自定义 provider，使用环境变量提供 key 更干净</li> <li>这样不会和 Bedrock 的认证逻辑混在一起</li> </ul> <p>换句话说，<strong>Bedrock 走 AWS 凭证链，OneAPI 走环境变量 key，各自独立，边界清晰。</strong></p> <hr/> <h2 id="6-oneapi-的-key-放哪里">6. OneAPI 的 key 放哪里</h2> <p>我最后把 OneAPI 的 key 放进了：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>~/.codex/.env
</code></pre></div></div> <p>内容如下：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">ONEAPI_API_KEY</span><span class="o">=</span>你的_oneapi_key
</code></pre></div></div> <p>这样做的好处是：</p> <ul> <li>key 不需要硬编码进 <code class="language-plaintext highlighter-rouge">config.toml</code></li> <li>后面如果还有第二个自定义 provider，也可以继续往 <code class="language-plaintext highlighter-rouge">.env</code> 里加</li> <li><code class="language-plaintext highlighter-rouge">~/.codex</code> 本身就是 Codex 的本地目录，管理起来更集中</li> </ul> <hr/> <h2 id="7-让-zsh-自动加载-codexenv">7. 让 zsh 自动加载 <code class="language-plaintext highlighter-rouge">~/.codex/.env</code></h2> <p>如果只是把 key 写进 <code class="language-plaintext highlighter-rouge">~/.codex/.env</code>，但 shell 没有自动导出这个变量，那么运行 <code class="language-plaintext highlighter-rouge">codex --profile oneapi</code> 时还是读不到它。</p> <p>所以我在 <code class="language-plaintext highlighter-rouge">~/.zshrc</code> 里加了这段：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="o">[</span> <span class="nt">-f</span> <span class="s2">"</span><span class="nv">$HOME</span><span class="s2">/.codex/.env"</span> <span class="o">]</span><span class="p">;</span> <span class="k">then
  </span><span class="nb">set</span> <span class="nt">-a</span>
  <span class="nb">.</span> <span class="s2">"</span><span class="nv">$HOME</span><span class="s2">/.codex/.env"</span>
  <span class="nb">set</span> +a
<span class="k">fi</span>
</code></pre></div></div> <p>这段的作用是：</p> <ul> <li>自动读取 <code class="language-plaintext highlighter-rouge">~/.codex/.env</code></li> <li>并把里面的变量导出成环境变量</li> </ul> <p>这样 OneAPI 的 <code class="language-plaintext highlighter-rouge">env_key = "ONEAPI_API_KEY"</code> 才能真正生效。</p> <hr/> <h2 id="8-再加两个快捷命令">8. 再加两个快捷命令</h2> <p>为了避免每次都手敲完整参数，我在 <code class="language-plaintext highlighter-rouge">~/.zshrc</code> 里顺手加了两个 alias：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">alias </span><span class="nv">codexb</span><span class="o">=</span><span class="s1">'codex --profile bedrock'</span>
<span class="nb">alias </span><span class="nv">codexo</span><span class="o">=</span><span class="s1">'codex --profile oneapi'</span>
</code></pre></div></div> <p>然后重新加载 shell：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">source</span> ~/.zshrc
</code></pre></div></div> <p>之后就可以直接：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>codexb
codexo
</code></pre></div></div> <p>来切换。</p> <hr/> <h2 id="9-实际使用方式">9. 实际使用方式</h2> <h3 id="91-使用-bedrock">9.1 使用 Bedrock</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>codexb
</code></pre></div></div> <p>或者显式写成：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>codex <span class="nt">--profile</span> bedrock
</code></pre></div></div> <h3 id="92-使用-oneapi">9.2 使用 OneAPI</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>codexo
</code></pre></div></div> <p>或者：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>codex <span class="nt">--profile</span> oneapi
</code></pre></div></div> <hr/> <h2 id="10-如何验证-oneapi-配置是否真的生效">10. 如何验证 OneAPI 配置是否真的生效</h2> <p>最简单的办法不是盯着配置文件看，而是直接跑一个最小请求。</p> <p>例如：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>codex <span class="nb">exec</span> <span class="nt">--profile</span> oneapi <span class="nt">--ephemeral</span> <span class="nt">--sandbox</span> read-only <span class="s2">"Reply with exactly OK."</span>
</code></pre></div></div> <p>如果配置正确，Codex 启动时通常会显示类似信息：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>model: gpt-5.5
provider: oneapi
reasoning effort: xhigh
</code></pre></div></div> <p>然后正常返回：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>OK
</code></pre></div></div> <p>我本地实际验证过，这套配置是可以跑通的。</p> <hr/> <h2 id="11-最后总结">11. 最后总结</h2> <p>如果你也遇到 Codex 需要同时兼容多种 LLM provider 的场景，我的建议是：</p> <ul> <li><strong>不要</strong>在同一个 <code class="language-plaintext highlighter-rouge">config.toml</code> 里来回手改 <code class="language-plaintext highlighter-rouge">model_provider</code></li> <li><strong>优先使用</strong> Codex 原生的 <code class="language-plaintext highlighter-rouge">profile</code> 机制</li> <li><strong>自定义 provider 的 key</strong> 优先走 <code class="language-plaintext highlighter-rouge">env_key</code></li> <li><strong>Bedrock</strong> 继续沿用 AWS 的标准认证链</li> </ul> <p>最终这套方案的核心只有一句话：</p> <blockquote> <p><strong>基础配置放 <code class="language-plaintext highlighter-rouge">config.toml</code>，provider 差异拆到 <code class="language-plaintext highlighter-rouge">xxx.config.toml</code>，用 <code class="language-plaintext highlighter-rouge">--profile</code> 切换。</strong></p> </blockquote> <p>这是目前我觉得最简单、最稳、也最好维护的做法。</p>]]></content><author><name></name></author><category term="techniques"/><category term="Codex"/><category term="AWS Bedrock"/><category term="OneAPI"/><category term="LLM"/><category term="工具"/><summary type="html"><![CDATA[Codex 同时兼容 AWS Bedrock 和 OneAPI 的配置教程]]></summary></entry><entry><title type="html">ASPLOS’26 | SpeContext 让小模型先圈出重要 KV Cache，长上下文推理吞吐最高提升 24.89 倍</title><link href="https://marsggbo.github.io/blog/2026/20260812-specontex/" rel="alternate" type="text/html" title="ASPLOS’26 | SpeContext 让小模型先圈出重要 KV Cache，长上下文推理吞吐最高提升 24.89 倍"/><published>2026-08-12T00:00:00+00:00</published><updated>2026-08-12T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260812-specontex</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260812-specontex/"><![CDATA[<h1 id="asplos26--specontext-让小模型先圈出重要-kv-cache长上下文推理吞吐最高提升-2489-倍">ASPLOS’26 | SpeContext 让小模型先圈出重要 KV Cache，长上下文推理吞吐最高提升 24.89 倍</h1> <blockquote> <p>原文：<a href="https://doi.org/10.1145/3779212.3790224">SpeContext: Enabling Efficient Long-context Reasoning with Speculative Context Sparsity in LLMs</a></p> </blockquote> <hr/> <h2 id="1-前言长推理真正麻烦的不是-prompt而是越想越长">1. 前言：长推理真正麻烦的不是 prompt，而是越想越长</h2> <p>现在很多 reasoning model 会先生成很长的思维链，再给出答案。对模型能力来说，这叫 <strong>test-time scaling</strong>：不继续扩大模型参数，而是在推理时多花一些计算，让模型一步步思考。</p> <p>问题是，模型每多“想”一个 token，都要把这个 token 的 Key 和 Value 状态追加到 <strong>KV Cache</strong> 里。KV Cache 可以理解成模型对历史内容做过的中间笔记，有了它，生成下一个 token 时就不用把之前的文字重新计算一遍；代价是这本笔记会随着上下文长度线性增长。</p> <p>以 Llama3.1-8B 为例，论文给出的数据是：32K 上下文仅 KV Cache 就要约 4GB。放到 24GB 的 RTX 4090 上，模型权重已经占去大头，再同时服务多个长请求，显存很快就不够了。</p> <p>更麻烦的是，长上下文推理和“读一篇很长的文档再回答一个短问题”不是一回事：</p> <ul> <li><strong>长上下文输入</strong>：prompt 很长，但输出可能只有几十个 token，KV Cache 的主体在 prefill 阶段一次性产生。</li> <li><strong>长上下文推理</strong>：prompt 可能不长，模型却会连续生成几千甚至几万个 token，KV Cache 在 decode 过程中不断长大。</li> </ul> <p>很多已有方案只在 prefill 结束后整理一次 prompt 的 KV Cache，之后新生成的 KV 全部保留。拿它们直接跑长推理，相当于只收拾了一次房间，后面东西继续往里堆，优化效果很快就没了。</p> <p>SpeContext 想解决的就是这个问题：<strong>在长推理过程中，不再让大模型每层自己临时翻遍所有历史 KV，而是让一个小模型提前猜出“下一步最可能用到哪些历史 token”，把对应 KV 预取到 GPU。</strong></p> <p>这个思路和 speculative decoding 有点像，但猜的东西不同：</p> <ul> <li>speculative decoding 猜的是“接下来会生成什么 token”</li> <li>SpeContext 猜的是“接下来会关注哪些历史 token”</li> </ul> <p>所以论文把它叫 <strong>speculative context sparsity</strong>，可以理解成“投机式地预测上下文稀疏性”。</p> <hr/> <h2 id="2-为什么-kv-cache-可以只读一部分">2. 为什么 KV Cache 可以只读一部分</h2> <p>标准 attention 会让当前 query 和所有历史 key 做点积，再经过 softmax 得到注意力权重：</p> \[\text{AttentionWeight}=\text{softmax}\left(\frac{QK^\top}{\sqrt d}\right)\] <p>虽然公式把所有历史 token 都算了一遍，但最后的权重通常很不均匀：少数 token 拿走了大部分注意力，大量 token 的权重接近 0。</p> <p>比如模型正在回答“太平洋为什么是世界上最大的海洋”，它可能重点关注前文里的“面积”“太平洋”“最大”等词，而不是平均关注每一个逗号和虚词。</p> <p>这就给了 <strong>sparse attention（稀疏注意力）</strong> 一个机会：每次只取 Top-K 个最重要的历史 token 参与 attention。这里的 K 通常也叫 <strong>KV budget</strong>，表示这一轮、每个 attention head 最多允许读取多少个历史位置。budget 越大越接近 full attention，准确率通常越好，但显存读取和计算也越多。</p> <p>如下图左边是普通 full attention，右边是只读取被选中 KV 的 sparse attention。下方两张散点图展示了速度和准确率之间的 Pareto frontier：越靠右表示吞吐越高，越靠上表示精度越好。</p> <p><img src="/assets/img/posts/20260812-specontex/01_pareto_full.png" alt="KV Cache 稀疏选择的收益"/></p> <p>麻烦在于：<strong>你怎么知道该选哪 K 个 token？</strong></p> <p>Quest 会把 KV 切成 page，用每页的最大最小值估算重要性；ClusterKV 先做聚类，用聚类中心代替一组 KV；ShadowKV 则把 key 量化以后再计算近似分数。它们本质上都在做一件事：先把历史 KV 压成便宜的索引，再用当前 query 检索候选。</p> <p>如下图右半部分列出了三条典型路线：paging、clustering 和 quantization。它们比遍历原始 KV 便宜，但都需要预处理。</p> <p><img src="/assets/img/posts/20260812-specontex/04_kv_background_full.png" alt="现有 KV Cache 优化路线"/></p> <p>这些方法处理长 prompt 很合适，因为 prompt 的 KV 在 prefill 后基本不再变化，预处理一次可以反复使用。但到了长推理，每一轮都会新增 KV，如果持续分页、聚类或者量化，预处理本身就会变成新的瓶颈。</p> <p>论文总结了三个具体问题：</p> <ol> <li><strong>逐层检索太慢。</strong> 每一层都要先检索、再搬 KV、最后才能计算 attention，三者有数据依赖，只能串行。检索开销会随模型层数增长，最高占到 60% 延迟。</li> <li><strong>新生成的 KV 越积越多。</strong> 已有方法为了避免反复预处理，通常完整保留 decode 阶段的新 KV，正好错过了长推理里增长最快的部分。</li> <li><strong>静态 offloading 会出现性能悬崖。</strong> 推理开始前决定“全放 GPU”或“全放 CPU”，上下文稍微长一点导致显存不够，系统就可能突然把大量 KV 搬到 CPU，性能下降超过 80%。</li> </ol> <p>这三件事决定了 SpeContext 不能只换一个检索算法，而要同时改算法、执行流水线和显存管理。</p> <hr/> <h2 id="3-核心-insight小模型不仅会模仿答案也会模仿看哪里">3. 核心 insight：小模型不仅会模仿答案，也会模仿“看哪里”</h2> <p>SpeContext 最有意思的地方，是把 <strong>knowledge distillation（知识蒸馏）</strong> 和 KV 检索连了起来。</p> <p>知识蒸馏会让一个较小的 student model 学习大模型 teacher 的输出概率分布。比如大模型看到某段上下文后，认为下一个 token 是 A/B/C 的概率分别为 0.8/0.15/0.05，小模型训练的目标就是尽量给出相似分布。</p> <p>论文的推理是：<strong>如果小模型和大模型最后给出的概率分布很像，它们从上下文里提取的重要信息也不应该完全不同。</strong> 否则小模型忽略了大模型真正依赖的信息，却还能长期模仿大模型输出，这件事很难成立。</p> <p>作者用 mutual information（互信息，衡量两个变量共享多少信息）和 data processing inequality（数据处理不等式，信息经过处理不会凭空增加）解释了这个直觉：</p> <ul> <li>大模型输出和输入上下文高度相关</li> <li>蒸馏迫使小模型输出接近大模型</li> <li>那么小模型内部表示也必须保留足够多的上下文信息</li> <li>所以小模型 attention 关注的位置，可以作为大模型重要 token 的廉价预测</li> </ul> <p>注意，这不是在说小模型每一层、每一个 head 的 attention 都和大模型完全一致。论文真正需要的是一个弱得多的条件：</p> <p><strong>小模型选出的 token，能覆盖大模型 attention 权重的大头即可。</strong></p> <p>这个差别很关键。SpeContext 不需要复制大模型的完整思考过程，只要让小模型当一个“划重点工具”。</p> <hr/> <h2 id="4-整体架构先选位置再边算边搬">4. 整体架构：先选位置，再边算边搬</h2> <p>下面这张架构图建议从上往下看。</p> <p><img src="/assets/img/posts/20260812-specontex/03_system_architecture_full.png" alt="SpeContext 系统架构"/></p> <p>最上面是输入条件：模型结构、硬件规格和请求负载。系统先在 compilation 阶段根据这些信息计算显存切换阈值。</p> <p>真正推理时，流程分成两条并行路径：</p> <ul> <li><strong>Lightweight Retrieval Head</strong> 读取当前输入，输出下一轮需要的 KV 位置索引</li> <li><strong>原始 LLM</strong> 使用上一轮已经准备好的稀疏 KV 做 forward</li> </ul> <p>中间的 Asynchronous Prefetcher 收到位置索引后，在另一条 CUDA stream 上把缺失的 KV 从 CPU 搬到 GPU。理想情况下，大模型还在计算当前 token 时，下一个 token 需要的 KV 已经在路上了。</p> <p>整个循环可以压成下面几行：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 当前 token 到来后，轻量检索头先预测下一轮会关注的位置
</span><span class="n">selected</span> <span class="o">=</span> <span class="nf">retrieval_head</span><span class="p">(</span><span class="n">input_ids</span><span class="p">).</span><span class="nf">topk</span><span class="p">(</span><span class="n">k</span><span class="o">=</span><span class="n">kv_budget</span><span class="p">)</span>

<span class="c1"># GPU 已有的 KV 不重复搬，只预取新增部分
</span><span class="n">to_load</span> <span class="o">=</span> <span class="n">selected</span> <span class="o">-</span> <span class="n">resident_kv_indices</span>
<span class="nf">async_prefetch</span><span class="p">(</span><span class="n">kv_cache</span><span class="p">[</span><span class="n">to_load</span><span class="p">])</span>

<span class="c1"># 大模型使用上一轮准备好的稀疏 KV 继续生成
</span><span class="n">next_token</span> <span class="o">=</span> <span class="n">llm</span><span class="p">.</span><span class="nf">forward</span><span class="p">(</span><span class="n">input_ids</span><span class="p">,</span> <span class="n">sparse_kv</span><span class="o">=</span><span class="n">resident_kv</span><span class="p">)</span>

<span class="n">resident_kv_indices</span> <span class="o">=</span> <span class="n">selected</span>
<span class="n">input_ids</span> <span class="o">=</span> <span class="n">next_token</span>
</code></pre></div></div> <p>这段伪代码省略了多层、多 head 和流水线 warmup，但核心关系就是：<strong>小模型负责预测位置，大模型负责真正计算；预测第 $t+1$ 步和执行第 $t$ 步重叠。</strong></p> <p>下面分别拆开三个组件。</p> <hr/> <h2 id="5-c1把-05b-蒸馏模型剪成只会划重点的-retrieval-head">5. C1：把 0.5B 蒸馏模型剪成只会“划重点”的 retrieval head</h2> <p>作者直接使用 EAGLE-3 训练好的 distilled language model（DLM，蒸馏语言模型）。完整 DLM 大约有 0.5B 参数，也有 embedding、attention、FFN、output projection 和 LM head。</p> <p>如果把它完整跑一遍，额外开销大约是 20%。问题很明显：为了少读一点 KV，先额外跑一个不算小的模型，可能省下来的时间还没花出去的多。</p> <p>但 SpeContext 根本不需要 DLM 生成 token，只需要它的 attention weight。所以作者把后半条链路几乎全剪了：</p> <ul> <li>保留 embedding</li> <li>保留 Query/Key projection</li> <li>保留用于计算 attention weight 的矩阵乘</li> <li>删掉 Value、attention output、FFN、output projection、LM head 和采样</li> </ul> <p>最后得到约 0.03B 参数、约 60MB 的 retrieval head，参数量减少 90% 以上。</p> <p>下图左上角的绿色部分是保留下来的路径，右侧灰掉的 FFN、LM Head、Logits 都不再计算。右边几组小图说明它还能适配 MHA、GQA、MQA 和 MLA 四种 attention 结构。</p> <p><img src="/assets/img/posts/20260812-specontex/05_retrieval_head_full.png" alt="轻量 retrieval head 设计"/></p> <p>这里还有一个容易忽略的设计选择：到底是整个 batch 共用一组重要 token，还是每个 attention head 各选各的？</p> <p>作者最终选择 <strong>head-level retrieval</strong>。原因也很直观：不同 head 本来就可能负责不同模式，有的关注局部语法，有的关注实体，有的关注开头指令。把所有 head 强行合并成一份 Top-K，粒度太粗。</p> <p>实验也支持这个判断。在长输入、budget=1024 时：</p> <ul> <li>head-level 选出的 token 能覆盖大模型约 0.92 到 0.97 的 attention weight</li> <li>batch-level 只能覆盖约 0.73 到 0.83</li> </ul> <p>所以 SpeContext 虽然只跑一个单层 retrieval head，但它会给不同 head 保留不同的 token 集合。</p> <p>对于 GQA（Grouped-Query Attention，多组 query head 共享一个 KV head），系统先对同一组里的 attention score 做 element-wise max，再按 group 选 Top-K；对于 MQA，所有 query head 共用一组 KV；对于 DeepSeek 使用的 MLA，则选择低维 latent cache，再只对命中的部分做升维。</p> <hr/> <h2 id="6-c2异步预取不难难的是不要每次把-k-个-kv-全搬一遍">6. C2：异步预取不难，难的是不要每次把 K 个 KV 全搬一遍</h2> <p>检索位置提前算出来后，SpeContext 就能把 KV transfer 和 LLM forward 放到两条 CUDA stream 上并行。</p> <p>但并行不等于免费。如果每一步都从 CPU 重新搬 K 个 KV，而 PCIe 搬运时间比一层 LLM 计算还长，最后关键路径仍然会被 I/O 卡住。</p> <p>论文测了不同 KV budget 下的预取延迟。budget 越大，搬运时间越长；到 512、1024 以后，完整预取已经明显超过单层模型计算时间。</p> <p><img src="/assets/img/posts/20260812-specontex/06_prefetch_overlap_full.png" alt="预取开销与相邻命中重合率"/></p> <p>转机在右图：相邻两个生成 step 选出的 token，高度重合，重合率超过 80%。</p> <p>这很符合直觉。模型生成一句话时，连续两个 token 面对的上下文几乎一样，关注点不会突然全部换掉。比如刚生成“太平”，下一步生成“洋”时，依赖的历史信息大概率仍然是同一批。</p> <p>因此 SpeContext 提出 <strong>elastic loading</strong>：</p> <p>假设上一轮 GPU 上有：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>S_last = {1, 8, 21, 35}
</code></pre></div></div> <p>新一轮预测需要：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>S_now  = {1, 8, 21, 50}
</code></pre></div></div> <p>那就没必要重新搬 4 份 KV，只要把位置 35 替换成位置 50。实际加载量从 K 变成了两个集合的差集：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>to_load = S_now - S_last = {50}
</code></pre></div></div> <p>论文报告这种增量更新最多能减少 90% 数据传输。</p> <p>下面这张时序图对比了五种路线。最下面 SpeContext 的绿色箭头表示 KV prefetch 和 FFN/attention 计算发生重叠，红色短条只搬相邻 step 之间变化的部分。</p> <p><img src="/assets/img/posts/20260812-specontex/07_async_dataflow.png" alt="异步预取与弹性加载"/></p> <p>从系统角度看，SpeContext 真正的收益不是“检索头比聚类快一点”，而是<strong>提前打破了 retrieve -&gt; load -&gt; attention 的逐层串行依赖</strong>。</p> <hr/> <h2 id="7-c3显存不够时不要突然全量-offload">7. C3：显存不够时，不要突然全量 offload</h2> <p>长推理还有一个动态问题：你很难提前知道模型最终会生成 2K、8K 还是 32K token。</p> <p>传统静态策略可能在推理开始时发现 KV 放得下，于是全留 GPU；等长度跨过显存上限，再一次性切到 CPU offloading。结果就是上下文只增加一点，延迟却突然恶化。</p> <p>SpeContext 的做法是按层渐进 offload。</p> <p>系统根据四类信息预先算出一组序列长度阈值：</p> <ul> <li>模型权重和层数</li> <li>KV head 数量与 head dimension</li> <li>GPU 可用显存</li> <li>并发请求数和 KV budget</li> </ul> <p>目标很直接：<strong>在不 OOM 的前提下，让尽可能多的层继续把完整 KV 留在 GPU。</strong></p> <p>例如 Llama3-8B 有 32 层。开始时 32 层 KV 都在 GPU；上下文超过第一个阈值，就把第 32 层 KV 放到 CPU，只在 GPU 留一个 budget 大小的工作区；再超过下一个阈值，就继续迁移第 31 层。</p> <p>可以把运行时逻辑理解成：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">while</span> <span class="n">generating</span><span class="p">:</span>
    <span class="k">while</span> <span class="n">seq_len</span> <span class="o">&gt;=</span> <span class="n">threshold</span><span class="p">[</span><span class="n">num_cpu_layers</span><span class="p">]:</span>
        <span class="nf">offload_kv</span><span class="p">(</span><span class="n">layer</span><span class="o">=</span><span class="n">last_gpu_layer</span><span class="p">)</span>
        <span class="n">num_cpu_layers</span> <span class="o">+=</span> <span class="mi">1</span>

    <span class="n">next_token</span> <span class="o">=</span> <span class="n">llm</span><span class="p">.</span><span class="nf">generate_one_token</span><span class="p">()</span>
    <span class="n">seq_len</span> <span class="o">+=</span> <span class="mi">1</span>
</code></pre></div></div> <p>它不是什么复杂调度算法，关键是用理论内存模型提前算好阈值，把“突然全搬走”的性能悬崖改成逐层、可预测的变化。</p> <hr/> <h2 id="8-效果精度能不能保住">8. 效果：精度能不能保住</h2> <p>稀疏 attention 最大的风险永远不是速度，而是漏掉关键上下文。</p> <p>论文先在 LongBench 上测试长文本问答。这里的 F1 score 衡量生成答案与标准答案的词级重合，越高越好；Passage Count 则测试模型能否从长文档里正确计数。</p> <p>在 Llama3.1-8B 上，budget 只有 512 时，SpeContext 因为只做一次全局选择，精度略低于逐层检索的 ClusterKV；当 budget 达到 1024，SpeContext 基本追平 full attention，并超过几个 sparse attention baseline。</p> <p><img src="/assets/img/posts/20260812-specontex/08_longbench_full.png" alt="LongBench 准确率"/></p> <p>长推理则使用 LongWriter，让 GPT-4o 从相关性、准确性、连贯性、清晰度、深度和阅读体验六个维度打分。这里的分数越高越好。</p> <p>论文里有个挺有意思的现象：部分设置下，SpeContext 甚至略高于 full attention。作者检查后认为，full attention 的输出存在重复，而稀疏 attention 恰好缓解了重复。</p> <p><img src="/assets/img/posts/20260812-specontex/11_longwriter_full.png" alt="LongWriter 生成质量"/></p> <p>不过这个结果不要过度解读。它说明 sparse attention 未必必然伤害长文本生成，<strong>不等于删 KV 能稳定提升质量</strong>；这里还受到 GPT-4o judge、采样随机性和具体模型重复倾向的影响。</p> <hr/> <h2 id="9-性能2489-倍到底是怎么来的">9. 性能：24.89 倍到底是怎么来的</h2> <p>云端实验使用 80GB GPU，重点测试多请求并发。下表里的 <code class="language-plaintext highlighter-rouge">[2k, 32k]</code> 表示输入 2K token、输出 32K token；括号中的数字是并发请求数，绿色倍数以 HuggingFace Eager 为基准。</p> <p><img src="/assets/img/posts/20260812-specontex/10_cloud_throughput_full.png" alt="云端多请求吞吐"/></p> <p>以 DeepSeek-Distill-Llama-8B 的 <code class="language-plaintext highlighter-rouge">[2k, 32k]</code> 为例：</p> <ul> <li>HuggingFace Eager：27.74 tokens/s，4 个请求</li> <li>FlashInfer：314.25 tokens/s，8 个请求</li> <li>SpeContext：690.59 tokens/s，32 个请求</li> </ul> <p>换算下来，SpeContext 相对 Eager 是 <strong>24.89 倍</strong>，相对 FlashInfer 是约 <strong>2.20 倍</strong>。</p> <p>这里一定要看清 24.89 倍的来源：它不只是单请求 attention kernel 更快，更重要的是 sparse KV 大幅减少显存占用，让同一张 GPU 能同时塞进更多请求。也就是说，这个数字同时包含了<strong>单请求加速和并发容量提升</strong>。</p> <p>单请求结果如下图。左边是云端，右边是把 GPU 显存限制到 4GB 的端侧环境。论文报告端侧相对 HuggingFace Eager 最高加速 10.06 倍；与同样做 offloading 的 ShadowKV 相比，正文报告最高 1.17 倍。</p> <p><img src="/assets/img/posts/20260812-specontex/12_single_request_full.png" alt="云端与端侧单请求吞吐"/></p> <p>消融实验把三个组件逐步叠加：</p> <ul> <li><code class="language-plaintext highlighter-rouge">C1</code>：轻量 retrieval head + sparse attention</li> <li><code class="language-plaintext highlighter-rouge">C2</code>：异步预取 + elastic loading</li> <li><code class="language-plaintext highlighter-rouge">C3</code>：自适应显存管理</li> </ul> <p><img src="/assets/img/posts/20260812-specontex/13_ablation_full.png" alt="SpeContext 消融实验"/></p> <p>图里可以看到，C1 先降低 attention 计算和显存压力，C2 解决 CPU-GPU 搬运，C3 再通过尽量多留 GPU KV 提升速度。三层优化缺一块，长推理场景都会在别处重新卡住。</p> <hr/> <h2 id="10-一些冷静的讨论">10. 一些冷静的讨论</h2> <p>这篇工作最让我觉得有意思的，不是又设计了一个 Top-K 算法，而是它换了检索信号：</p> <p><strong>过去从 KV 本身构造索引，SpeContext 则让一个与大模型同源的小模型直接预测大模型会看哪里。</strong></p> <p>这个视角把 speculative inference 从“预测结果”扩展到了“预测计算路径”，后面可以自然延伸到 attention block 预取、MoE expert 预取，甚至不同 memory tier 之间的数据调度。</p> <p>但它还不是银弹：</p> <ol> <li><strong>需要对应的 DLM。</strong> 论文直接复用 EAGLE-3 的蒸馏模型。换一个没有现成 DLM 的模型，仍然要付训练成本。</li> <li><strong>互信息分析给的是合理性，不是严格保证。</strong> 输出分布接近，并不能数学上保证每个 attention head 都关注相同 token，最终可靠性还是靠实验验证。</li> <li><strong>小 budget 下有精度代价。</strong> budget=512 时，全局选择不如 ClusterKV 的逐层选择；SpeContext 用更少同步换来了更粗的选择粒度。</li> <li><strong>fallback 还只是 future work。</strong> 作者计划在 retrieval head 的注意力不够集中时退回 full attention，但当前系统还没有这个置信度兜底。</li> <li><strong>部分加速来自框架和更高并发。</strong> 24.89 倍是相对 HuggingFace Eager，若和 FlashInfer 比，最高提升约 2.20 倍。看系统论文的数字，baseline 和并发配置必须一起看。</li> </ol> <p>总结一下，SpeContext 的核心逻辑其实很简单：</p> <p><strong>既然蒸馏小模型已经学会模仿大模型的输出，那就让它顺便提前告诉系统，大模型下一步大概率会读取哪些历史信息。</strong></p> <p>有了这个提前量，KV 检索从逐层串行操作变成了 LLM 前面的轻量预测；再配合增量预取和动态显存管理，长上下文推理里不断增长的 KV Cache 才真正被控制住。</p> <hr/> <p>顺带扯句题外话：SpeContext 属于 LLM 推理系统优化，书里没有直接讲这套 KV Cache 预取机制。不过它“用一个便宜代理预测昂贵模型行为”的思路，和 AutoML 里用 proxy 评估架构、用小成本信号减少完整执行是一脉相承的。我们把 NAS、搜索策略、LLM 剪枝量化和后训练压缩整理进了《动手学 AutoML：从 NAS 到大语言模型优化实战》，角度不同，但目标都是少做不必要的昂贵计算。</p> <blockquote> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="KV Cache"/><category term="长上下文推理"/><category term="稀疏注意力"/><category term="论文解读"/><summary type="html"><![CDATA[ASPLOS’26 | SpeContext 让小模型先圈出重要 KV Cache，长上下文推理吞吐最高提升 24.89 倍]]></summary></entry><entry><title type="html">LMSYS’26 | Unified Radix Cache：同一个 prefix，为什么不能只用一个 cache boundary？</title><link href="https://marsggbo.github.io/blog/2026/20260812-unified-radix-cache/" rel="alternate" type="text/html" title="LMSYS’26 | Unified Radix Cache：同一个 prefix，为什么不能只用一个 cache boundary？"/><published>2026-08-12T00:00:00+00:00</published><updated>2026-08-12T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260812-unified-radix-cache</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260812-unified-radix-cache/"><![CDATA[<h1 id="lmsys26--unified-radix-cache同一个-prefix为什么不能只用一个-cache-boundary">LMSYS’26 | Unified Radix Cache：同一个 prefix，为什么不能只用一个 cache boundary？</h1> <blockquote> <p>原文：<a href="https://www.lmsys.org/blog/2026-08-11-unified-radix-cache">Unified Radix Cache: One Tree for Hybrid Model Prefix Caching</a></p> <p>相关项目：<a href="https://github.com/sgl-project/sglang">SGLang</a></p> </blockquote> <h2 id="1-先把问题说人话缓存的不是字符串而是模型状态">1. 先把问题说人话：缓存的不是“字符串”，而是模型状态</h2> <p>你有没有想过，为什么同一个 prompt，第一次请求很慢，第二次却可能快很多？</p> <p>模型处理输入时，先有一段 <strong>prefill</strong>：把整段 prompt 一次性送进模型，计算每个 token 对应的中间状态；随后进入 <strong>decode</strong>，一个 token 一个 token 地生成答案。对于 Transformer 来说，注意力层会为历史 token 保存 key/value，后续 token 可以直接读取，这些历史状态就叫 <strong>KV cache</strong>。</p> <p><strong>Prefix caching</strong> 做的事情，就是把多个请求共同拥有的 prompt 前缀的 KV cache 留下来。第二个请求如果前 10,000 个 token 一样，就不必重新计算这 10,000 个 token 的 prefill。对长上下文、agent 多轮轨迹、代码仓库问答来说，这往往比再挤出一点 GPU FLOPS 更有价值。</p> <p>传统实现通常隐含了一个很强的假设：</p> <blockquote> <p>一段 token prefix 对应一个连续的、大家都认可的 reusable boundary（可复用边界）。</p> </blockquote> <p>在纯 <strong>full attention</strong> Transformer 中，这个假设几乎没问题。full attention 指每个 token 都可以访问完整历史；因此前缀相同，前缀产生的 KV 就可以从头到尾接着用。</p> <p>但模型开始混搭之后，事情就不再这么简单了。现在的 hybrid model 往往在同一个网络里同时出现 full attention、<strong>sliding window attention（SWA，滑动窗口注意力）</strong>，以及 <strong>Mamba/SSM（用 recurrent state 压缩历史的状态空间模块）</strong>。它们看到的是同一串 token，却不一定保存同一种状态，也不一定在同一个位置允许复用。</p> <p>这正是 Unified Radix Cache 要解决的工程问题：<strong>如何让不同组件共享一棵前缀树，但各自保留自己的复用语义。</strong></p> <h2 id="2-读懂后面的设计只需要先分清三个概念">2. 读懂后面的设计，只需要先分清三个概念</h2> <p>原文默认读者已经熟悉 serving 系统，所以几个关键概念一闪而过。这里先把它们拆开，否则后面很容易把“树上的同一个节点”误解成“所有组件都能使用同一份物理 cache”。</p> <h3 id="21-prefix-identity这段-token-在哪里">2.1 Prefix identity：这段 token 在哪里</h3> <p>假设有三条请求：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>system, 你是助手, 解释 MoE
system, 你是助手, 解释 prefix cache
system, 你是助手, 解释 SWA
</code></pre></div></div> <p>三条请求共享前两个片段。Radix tree（基数树）就是把共享前缀压缩成一条路径，分叉部分再各自展开。它维护的是一种<strong>规范化的 token 坐标</strong>：某个 token prefix 是谁，它在树上和哪些请求共享。</p> <p>这棵树本身并不承诺“所有状态都能复用”。它更像缓存的目录和索引，而不是某一个模型组件的完整实现。</p> <h3 id="22-reuse-validity这个组件能安全用到哪里">2.2 Reuse validity：这个组件能安全用到哪里</h3> <p>同一个树节点，对不同组件可能有不同答案。full attention 可能接受整条路径；SWA 只接受最近的窗口；Mamba 只接受某个合法的 recurrent checkpoint。</p> <p>所以必须把“前缀相同”与“状态可复用”分开：前者是身份问题，后者是组件语义问题。</p> <h3 id="23-payload-location状态现在放在哪">2.3 Payload location：状态现在放在哪</h3> <p>即使某份状态有效，它也可能在 GPU、CPU 内存或外部内存池。HiCache 的 L1/L2/L3 正是在管理这个维度：L1 通常是 GPU 显存，L2 是 Host 内存，L3 是更大的远端或分布式存储。</p> <p><strong>换存储层不应该改变 prefix identity。</strong> 一段前缀从 GPU 被淘汰到 Host，之后又被预取回 GPU，树上仍然是同一个节点，只是 payload 的住址变了。</p> <p>这三个概念对应三个问题：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>prefix identity       这是谁？
reuse validity         谁能用？能用到哪？
payload location       现在放在哪？
</code></pre></div></div> <p>Unified Radix Cache 的关键，就是不再用一个 cache class 同时回答这三个问题。</p> <h2 id="3-为什么-fullswamamba-共享同一个-prefix却不能共享同一个边界">3. 为什么 FULL、SWA、MAMBA 共享同一个 prefix，却不能共享同一个边界</h2> <p>用一段具体前缀看最清楚。假设 token 是：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>h0  h1  h2  h3  h4  h5
</code></pre></div></div> <h3 id="31-full整条路径都是状态">3.1 FULL：整条路径都是状态</h3> <p>full attention 为每个位置保存 KV。可以把它想成自己的物理池：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>FULL: F0  F1  F2  F3  F4  F5
</code></pre></div></div> <p>只要 token 序列从 <code class="language-plaintext highlighter-rouge">h0</code> 到 <code class="language-plaintext highlighter-rouge">h5</code> 完全一致，FULL 就可以把整条匹配路径交给新请求。</p> <h3 id="32-swa文本前缀很长真正需要的可能只有尾窗">3.2 SWA：文本前缀很长，真正需要的可能只有尾窗</h3> <p>SWA 每层只看最近 <code class="language-plaintext highlighter-rouge">W</code> 个 token。假设窗口大小为 2，那么在处理后续 token 时，<code class="language-plaintext highlighter-rouge">h0</code> 到 <code class="language-plaintext highlighter-rouge">h3</code> 已经不在这层的可见窗口内了。它仍然存在于 canonical tree 中，却不代表 SWA 还需要或能够复用它。</p> <p>SWA 可以维护自己的物理池：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>SWA:       S0  S1       （对应逻辑上的 h4, h5）
</code></pre></div></div> <p>这里 <code class="language-plaintext highlighter-rouge">S0/S1</code> 不是 <code class="language-plaintext highlighter-rouge">F4/F5</code> 的别名，而是 SWA 自己的 slot。逻辑上两者对齐，物理上仍然独立。这样做是因为不同组件可能采用不同布局、页大小、生命周期或复制策略。</p> <h3 id="33-mamba复用的是-checkpoint不是任意一段-kv">3.3 MAMBA：复用的是 checkpoint，不是任意一段 KV</h3> <p>Mamba/SSM 不把完整历史表示成 attention KV，而是递推一个 state：处理 <code class="language-plaintext highlighter-rouge">h0</code> 得到 <code class="language-plaintext highlighter-rouge">s0</code>，处理 <code class="language-plaintext highlighter-rouge">h1</code> 得到 <code class="language-plaintext highlighter-rouge">s1</code>，依次更新。新请求要从某个位置继续，必须拿到该位置对应的合法 state checkpoint。</p> <p>更麻烦的是，后续 decode 会继续修改这个 recurrent state。多个请求不能直接共同写同一个 state，否则一个请求生成的 token 会污染另一个请求。因此常见做法是：命中共享 checkpoint 后，把它复制到请求私有的 slot，再继续更新。</p> <p>于是，对同一段 <code class="language-plaintext highlighter-rouge">h0...h5</code>：</p> <ul> <li>FULL 的 reusable boundary 可以在 <code class="language-plaintext highlighter-rouge">h5</code>，并且整条路径都可用；</li> <li>SWA 可能只对 <code class="language-plaintext highlighter-rouge">h4,h5</code> 建立可用窗口；</li> <li>MAMBA 可能只在 <code class="language-plaintext highlighter-rouge">h5</code> 保存一个可复制的 state checkpoint。</li> </ul> <p><strong>同一个 prefix identity，不等于同一个 reusable boundary，更不等于同一个物理 index space。</strong> 这句话如果不提前说清楚，Figure 1 里三张卡片看起来就像“同一份 cache 被重复画了三遍”。</p> <h2 id="4-一棵树到底统一了什么">4. 一棵树到底统一了什么</h2> <p>既然三类组件的状态不同，最直接的做法似乎是分别实现三套 cache。但一旦模型组合变多，工程会迅速变成 class matrix explosion：FULL、FULL+SWA、FULL+MAMBA、FULL+SWA+MAMBA，每加一种组件还要和 HiCache 的每个层级再组合一次。</p> <p>Unified Radix Cache 选择了一个更克制的拆分：</p> <ul> <li><strong>树核心统一 prefix identity</strong>：所有请求沿同一套 token radix topology 匹配、分裂和插入；</li> <li><strong>组件自己验证 reuse validity</strong>：FULL、SWA、MAMBA 各自决定某个位置是否安全；</li> <li><strong>HiCache 管理 payload location</strong>：把有效 payload 在 L1/L2/L3 之间搬运；</li> <li><strong>sidecar 跟随 source pool</strong>：附属数据跟着主组件移动，不额外制造复用语义。</li> </ul> <p>因此，“Unified”不是强迫不同模块使用同一种 cache，而是让它们共享不需要重复实现的那部分。</p> <p><img src="/assets/img/posts/20260812-unified-radix-cache/image1.svg" alt="Unified Radix Cache 总览"/></p> <p>Figure 1 放在这里，应该当作这套拆分的总览，而不是某个抽象口号的插图：中间是一棵 canonical token tree，左边是不同组件的复用规则，右边是同一份 payload 的分层驻留位置。</p> <h2 id="5-最长匹配不等于安全复用边界">5. 最长匹配不等于安全复用边界</h2> <p>有了统一树，匹配流程仍然要解决一个细节：<strong>树可以继续往深处走，但组件的验证结果可能不一致。</strong></p> <p>假设请求的 token 与树上的 <code class="language-plaintext highlighter-rouge">n1 -&gt; n2 -&gt; n3 -&gt; n4</code> 一直匹配。遍历当然可以走到 <code class="language-plaintext highlighter-rouge">n4</code>，但系统要为所有 active components 找一个共同安全边界：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>             n1    n2    n3    n4
FULL          ✓     ✓     ✓     ✓
SWA           ✓     ✓     ✗     ✗
MAMBA         ✓     ✓     ✗     ✓
</code></pre></div></div> <p>这里的 <code class="language-plaintext highlighter-rouge">✗</code> 不是“整棵树到此为止”。它只表示某个组件在这个候选位置没有可用状态。MAMBA 在更深的 <code class="language-plaintext highlighter-rouge">n4</code> 重新出现 <code class="language-plaintext highlighter-rouge">✓</code> 也说明了：组件的合法边界未必单调，不能遇到一次失败就停止遍历。</p> <p>真正提交给请求的 reusable boundary，需要所有参与本次复用的组件都同意。因此上面的例子最终停在 <code class="language-plaintext highlighter-rouge">n2</code>，即使 traversal 已经看到了 <code class="language-plaintext highlighter-rouge">n4</code>。</p> <p>这也是实现上最容易写错的地方：</p> <ol> <li><strong>遍历层</strong>负责沿 canonical token path 找候选节点；</li> <li><strong>组件验证层</strong>对候选节点给出自己的接受/拒绝；</li> <li><strong>边界聚合层</strong>取 active components 的共同安全结果。</li> </ol> <p>如果只实现第 1 步，系统会把“最长文本匹配”误当成“最长状态匹配”，在 SWA 或 recurrent state 上产生错误复用。</p> <p><img src="/assets/img/posts/20260812-unified-radix-cache/image2.svg" alt="match_prefix 如何选安全边界"/></p> <p>Figure 2 的意义也就在这里：它不是在展示一次普通的树遍历，而是在强调“走得更深”和“能安全复用”是两件事。</p> <h2 id="6-componentanchorsidecar三种对象不要混为一谈">6. Component、anchor、sidecar：三种对象不要混为一谈</h2> <p>组件化并不意味着每个数据结构都应该成为 component。更准确的分工是：</p> <ul> <li><strong>Component</strong> 会参与边界判断，定义自己的状态池和生命周期；</li> <li><strong>Anchor</strong> 是组件在 canonical tree 上的对齐位置，用来表达“这个状态对应哪段 token”；</li> <li><strong>Sidecar</strong> 不定义新的复用边界，只跟随某个 source pool 的分配、迁移和淘汰。</li> </ul> <p>仍然看 <code class="language-plaintext highlighter-rouge">h0...h5</code> 的例子：FULL 可以覆盖 <code class="language-plaintext highlighter-rouge">[0:6)</code>，SWA 只覆盖 <code class="language-plaintext highlighter-rouge">[4:6)</code>，并通过 <code class="language-plaintext highlighter-rouge">F4 -&gt; S0、F5 -&gt; S1</code> 建立逻辑对齐。<code class="language-plaintext highlighter-rouge">C4 KV</code>、<code class="language-plaintext highlighter-rouge">C128 KV</code>、indexer 或 compressor state 之类附属数据，则可以声明自己跟随 FULL 或 SWA。</p> <p><img src="/assets/img/posts/20260812-unified-radix-cache/image3.svg" alt="DeepSeek-V4 组件与 sidecar 关系"/></p> <p>为什么要这么严格？因为 sidecar 如果也向树里注册一套独立语义，系统就会把“压缩数据有没有搬到位”误认为“前缀是否可复用”。最后不仅类变多，边界判断也会被无关的数据结构绑架。</p> <h2 id="7-树核心提供生命周期组件提供语义钩子">7. 树核心提供生命周期，组件提供语义钩子</h2> <p>统一树不是一个只读索引。真实 serving 系统要不断执行 match、split、insert、lock、evict：请求来了要匹配，前缀在中间分叉要切节点，新 token 要插入，正在使用的区域要加锁，空间不足要淘汰。</p> <p>这些动作的树形 bookkeeping 可以统一，但每种组件在动作发生时要做的事情不同。例如：</p> <ul> <li>match 时，FULL 检查连续 KV，SWA 检查窗口范围，MAMBA 检查 checkpoint 是否存在且可复制；</li> <li>split 时，组件要决定自己的 pool 是否也需要切分，或者只保留一个 anchor；</li> <li>insert 时，SWA 可能只写入窗口，MAMBA 则更新 recurrent state；</li> <li>lock/evict 时，各组件根据自己的占用和引用关系处理物理 slot。</li> </ul> <p>因此合理的接口不是“所有组件实现一棵树”，而是树核心提供统一生命周期，组件通过 hooks 注入语义。新增加一种 hybrid block，主要工作就变成实现这些 hooks，而不是复制整套 radix-tree 代码。</p> <h2 id="8-hicache-改变的是-payload-位置不是-prefix-身份">8. HiCache 改变的是 payload 位置，不是 prefix 身份</h2> <p>单层 GPU cache 的容量很容易被长上下文和多会话挤爆。HiCache 把缓存拆成多层：GPU L1 低延迟但容量小，Host L2 容量更大，external L3 可以进一步扩展容量。请求命中 L2/L3 时，需要把 payload 搬回更快的层级，或者直接在较慢层级上完成一部分准备工作。</p> <p>这里最重要的不是“多了两层存储”，而是身份和位置被分开：</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>同一个 radix node
    ├── FULL payload: 可能在 GPU L1
    ├── SWA payload: 可能在 Host L2
    └── MAMBA checkpoint: 可能在 external L3
</code></pre></div></div> <p>它们仍然对应同一个 token prefix，只是不同组件、不同 tier 的 payload 状态不同。这样做也解释了为什么 sidecar 能自然接入：sidecar 只需要跟随 source pool 做迁移，而不必重新定义一棵树。</p> <p><img src="/assets/img/posts/20260812-unified-radix-cache/image4.png" alt="HiCache 多轮 benchmark"/></p> <p>原文的 multi-turn benchmark 测了 <code class="language-plaintext highlighter-rouge">DeepSeek-V4-Flash</code> 和 <code class="language-plaintext highlighter-rouge">Inkling-Small</code>。<code class="language-plaintext highlighter-rouge">TTFT</code>（Time To First Token）是从请求开始到第一个输出 token 的时间，越低越好；<code class="language-plaintext highlighter-rouge">prompt token hit rate</code> 是输入 token 中被缓存直接复用的比例，越高越好；<code class="language-plaintext highlighter-rouge">effective input token throughput</code> 还把省掉的前缀计算折算进去，更接近真实服务能力。</p> <p>只有 L1 时，缓存很快被后续轮次挤掉，hit rate 下降、TTFT 上升；加入 L2 后能多撑一段；L3 则把不断增长的多轮共享前缀保留得更久。以图中 <code class="language-plaintext highlighter-rouge">DeepSeek-V4-Flash</code> 为例，effective throughput 从 L1 的 <code class="language-plaintext highlighter-rouge">9.4k</code>，经过 L1+L2 的 <code class="language-plaintext highlighter-rouge">14.3k</code>，到三层的 <code class="language-plaintext highlighter-rouge">145.5k</code>。这不是 GPU 突然变快了，而是重复 prefill 被大幅避免了。</p> <h2 id="9-lru-只知道刚才用过不知道下一轮还会用">9. LRU 只知道“刚才用过”，不知道“下一轮还会用”</h2> <p>传统 <strong>LRU（least recently used）</strong> 淘汰策略按最近访问时间排序。对独立请求这通常够用，但 agent 或多轮 session 有明显的时间结构：一个 session 可能刚完成一轮，下一轮还没到达，却大概率继续复用同一段前缀。</p> <p>如果只看最近访问时间，这段数据会被当作冷数据淘汰。Unified Radix Cache 因此引入 <strong>session reference</strong>：每个请求携带稳定的 <code class="language-plaintext highlighter-rouge">session_id</code>，请求结束后，把该 session 仍可能复用的区域登记下来：FULL 记 path，SWA 记 trailing window，MAMBA 记 reusable frontier。</p> <p>这里的 reference 是 eviction 的软信号，不是 pin。内存真的不足时，仍然可以淘汰所有节点；只是引用数低的区域优先被淘汰。关闭 session 时，先释放引用，不必立刻删除 cache entry，避免短暂的 session 生命周期把缓存反复抖动。generation 机制则用来防止旧请求晚到，把已经关闭的 session 错误地重新标活。</p> <p><img src="/assets/img/posts/20260812-unified-radix-cache/image6.svg" alt="Session-aware eviction 示意图"/></p> <p>Figure 5 里的 <code class="language-plaintext highlighter-rouge">ref=3/2/1</code> 应理解为组件本地的未来复用信号，而不是全局的“这个节点有几个人正在读”。FULL、SWA、MAMBA 统计的都是各自真正关心的 region。</p> <h2 id="10-实验应该怎么看命中率不是唯一答案">10. 实验应该怎么看：命中率不是唯一答案</h2> <p>在 SWE-bench 这类 agent workload 上，session-aware eviction 的价值不一定体现为总 hit ratio 大幅增加。更重要的是：命中是否发生在 GPU 这种更快的层级。</p> <p><img src="/assets/img/posts/20260812-unified-radix-cache/image7.png" alt="SWE-bench 上的 cache residency 和 TTFT"/></p> <p>原文中，<code class="language-plaintext highlighter-rouge">DeepSeek-V4-Pro</code> 在 batch size 128 时 device hit ratio 从 <code class="language-plaintext highlighter-rouge">41.8%</code> 提升到 <code class="language-plaintext highlighter-rouge">51.0%</code>，总 hit ratio 只从 <code class="language-plaintext highlighter-rouge">96.3%</code> 到 <code class="language-plaintext highlighter-rouge">96.5%</code>，但 TTFT 仍下降 <code class="language-plaintext highlighter-rouge">11.0%</code>。这说明“总命中率几乎不变”并不等于“系统没有收益”：host 命中和 device 命中的延迟完全不同。</p> <p><code class="language-plaintext highlighter-rouge">Qwen3.5-397B-A17B</code> 的变化更明显，batch size 32 时 device hit ratio 从 <code class="language-plaintext highlighter-rouge">5.0%</code> 到 <code class="language-plaintext highlighter-rouge">33.6%</code>，batch size 64 时总 hit ratio 从 <code class="language-plaintext highlighter-rouge">57.2%</code> 到 <code class="language-plaintext highlighter-rouge">66.6%</code>，TTFT 分别下降 <code class="language-plaintext highlighter-rouge">13.5%</code> 和 <code class="language-plaintext highlighter-rouge">16.6%</code>。</p> <p>不过这里要保留一个实验 caveat：这不是只切换 session-aware 开关的纯净 ablation，同时包含了 Unified Radix Cache 实现与普通 HiRadixCache + LRU 的整体对比。因此这些数字能证明方案在该 workload 上有效，但不能把全部增益严格归因到某一个模块。</p> <h2 id="11-rust-tree-core优化的是-bookkeeping不是模型计算">11. Rust tree core：优化的是 bookkeeping，不是模型计算</h2> <p>当 prefix 很长、并发 session 很多时，树遍历、节点分裂、锁管理、LRU 更新和 eviction scan 会进入 scheduler 的关键路径。它们不是 GPU forward，却会影响请求什么时候真正开始 prefill。</p> <p><img src="/assets/img/posts/20260812-unified-radix-cache/image5.png" alt="Rust vs Python tree core"/></p> <p>原文的 Rust 实验是 L1-only prototype，测的是 tree core 的 native 化收益，并不是把整个 serving 系统重写成 Rust。<code class="language-plaintext highlighter-rouge">gpt-oss-20b</code> 的 SWA 场景平均 TTFT 降低约 <code class="language-plaintext highlighter-rouge">38%</code>，最后 25 轮降低约 <code class="language-plaintext highlighter-rouge">42%</code>；FULL 场景也有收益。hybrid SSM/Mamba 场景的 residual bookkeeping 开销下降明显，但总 TTFT 被更重的 GPU forward 稀释，所以端到端变化没有那么夸张。</p> <p>这个结果的正确解读是：<strong>当树管理成为瓶颈时，native tree core 值得做；但它不会消除模型本身的计算时间。</strong></p> <h2 id="12-我的-take这篇文章真正重新定义的是缓存边界">12. 我的 take：这篇文章真正重新定义的是“缓存边界”</h2> <p>Unified Radix Cache 的价值不在于又发明了一种 radix tree，也不在于把某个 benchmark 刷到更高。它把过去经常揉在一起的三个问题拆开了：</p> <ol> <li>token prefix 的身份由一棵 canonical tree 统一维护；</li> <li>每种模型组件自己决定状态在哪个边界可复用；</li> <li>有效 payload 放在 GPU、Host 还是 external tier，由 HiCache 管理；</li> <li>sidecar 只跟随 source pool，不抢占复用语义；</li> <li>session reference 给淘汰策略补上“未来还会不会用”的信息。</li> </ol> <p>这套抽象直接回答了开头那个疑问：为什么 FULL、SWA、MAMBA 明明拥有同一个 prefix，KV cache 复用却不同？因为它们共享的只是 <strong>prefix identity</strong>，而不是状态表示、合法边界和物理内存布局。</p> <p>我认为这是 hybrid model 时代更合理的 prefix caching 抽象。模型结构只会越来越混搭，如果每增加一种 attention/state 模块就复制一套 cache 实现，系统迟早会被组合数拖垮。<strong>一棵树负责“这段前缀是谁”，组件负责“我能安全用到哪”，分层缓存负责“状态现在在哪”</strong>。边界立对了，后面的 HiCache、session-aware eviction 和 Rust tree core 才能各自接上，而不必互相污染。</p>]]></content><author><name></name></author><category term="LLM"/><category term="推理优化"/><category term="Prefix Caching"/><category term="SGLang"/><category term="系统解读"/><summary type="html"><![CDATA[LMSYS’26 | Unified Radix Cache：同一个 prefix，为什么不能只用一个 cache boundary？]]></summary></entry><entry><title type="html">arXiv’26 | 流式 SpeechLLM：MoChA 让 LLM 边听边转写，延迟降 62.5%</title><link href="https://marsggbo.github.io/blog/2026/20260805-streaming-asr-llm/" rel="alternate" type="text/html" title="arXiv’26 | 流式 SpeechLLM：MoChA 让 LLM 边听边转写，延迟降 62.5%"/><published>2026-08-05T00:00:00+00:00</published><updated>2026-08-05T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260805-streaming-asr-llm</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260805-streaming-asr-llm/"><![CDATA[<h1 id="arxiv26--流式-speechllmmocha-让-llm-边听边转写延迟降-625">arXiv’26 | 流式 SpeechLLM：MoChA 让 LLM 边听边转写，延迟降 62.5%</h1> <blockquote> <p>原文：<a href="https://arxiv.org/pdf/2601.22779v1">Streaming Speech Recognition with Decoder-Only Large Language Models and Latency Optimization</a>（USTC + iFLYTEK，arXiv 2026.01）</p> </blockquote> <hr/> <h2 id="1-前言吐槽">1. 前言吐槽</h2> <p>最近 ASR 这个方向的进展基本被一类东西统治了——SpeechLLM。</p> <p>所谓 SpeechLLM，不是某个模型的名字，是一类架构的统称：把一个音频编码器接到 decoder-only 的 LLM 前面，音频先编成 embedding，当成 token 喂给 LLM，由 LLM 自回归地把文字解码出来。Qwen3-ASR、Kimi-Audio、SALM 这些都属于这一类。这个范式的好处很实在：精度高、prompt 天然就是条件通道，不改架构就能加各种额外信息。</p> <p>但这套东西有个让人头疼的地方：<strong>它天然不支持流式识别</strong>。</p> <p>非流式 SpeechLLM 的工作流是：音频进来 → 全量编码 → 所有 audio token 拼成 prefix 塞进 LLM → 自回归解码出完整文字。这意味着模型必须等整段音频输完才能开始出字。</p> <p>在 benchmark 上没问题，但一碰到真实场景就卡住了。字幕机、同声传译、语音助手，这些场景要求<strong>边说边出字</strong>，不能等人把话说完了再统一告知。传统的 encoder-decoder ASR（RNN-T、CTC、MoChA attention）在这方面有一套成熟的”读/写策略”：什么时候等更多音频（read），什么时候把当前音频解码成文字输出（write）。但这套逻辑怎么搬进 decoder-only 的 LLM 里，一直没有干净的解法。</p> <p>现有的几个尝试（BESTOW、SpeechLLM-XL、BTI）大多绕不过一个坑：<strong>先跑一个 CTC 或者混合 ASR 做 forced alignment（强制对齐——用额外模型预先算出每个字对应哪几帧音频），再把这个对齐结果喂给 LLM</strong>。这是个级联管道，前一步的错误会传到后一步，两阶段合在一起根本没法端到端优化。</p> <p>这篇来自中科大和讯飞的工作想做的事情是：<strong>把流式能力直接内化进 SpeechLLM，端到端训练，不依赖外部 forced alignment</strong>（只有 minLT 损失那一步例外，后面说）。</p> <hr/> <h2 id="2-非流式-vs-流式先把本质区别说清楚">2. 非流式 vs 流式：先把本质区别说清楚</h2> <p>读这篇论文之前，必须先想清楚一件事：<strong>非流式和流式 SpeechLLM 的根本区别在哪</strong>。这个想清楚了，后面所有设计决策都是自然而然的。</p> <p>先看非流式（Figure 1）：</p> <p><img src="/assets/img/posts/20260805-streaming-asr-llm/001_Fig_1.png" alt="非流式 SpeechLLM 架构"/></p> <p>工作流程：</p> <ol> <li>等整段音频说完（比如 5 秒的一句话）</li> <li>整段音频送进 Conformer（一种结合了卷积和 Transformer 的语音编码器）编码，得到全量的 audio embedding，记作 $h_1, h_2, \ldots, h_N$（N 帧）</li> <li>这 N 个 audio token 全部拼成 prefix，加上 BOS（句首标记）送进 LLM</li> <li>LLM 自回归解码：每次生成一个字，直到 EOS（句末标记）</li> </ol> <p>LLM 是 Qwen2.5-1.5B，用 <strong>LoRA</strong>（Low-Rank Adaptation，只在注意力层加两个低秩矩阵做微调，不动原始权重）适配，rank=32，α=64。</p> <p>这个架构的关键特点：<strong>每次生成字时，LLM 的 attention 能覆盖所有 N 帧音频</strong>。所以它知道整句话的全局声学信息，再来决定每个字怎么写。比如”机器学习”里的”习”，它能看到后面没有更多字了，不会误认成”机器学习气”。</p> <p><strong>代价：</strong> 必须等整段音频说完才能开始出字。5 秒的话 latency 至少 5 秒，10 秒的话 10 秒。</p> <hr/> <p>流式的目标是：<strong>音频还在继续录入时，已经说完的部分就能出字</strong>。</p> <p>但这里有个内在矛盾：<strong>你看不到未来的音频，就必须用已有的信息做决策</strong>。</p> <p>这不是这篇论文特有的问题——RNN-T、CTC、所有流式 ASR 系统都面对这个矛盾。解决方案不是消除这个限制，而是接受它：<strong>用仅到当前时刻为止的音频来识别，允许一定程度的精度损失，换取实时输出</strong>。</p> <p>这也是为什么论文里流式 CER（5.5%）比非流式（5.0%）高 0.5 个点——那 0.5% 就是”没看到未来音频”的代价，没有办法完全消除，只能优化。</p> <hr/> <h2 id="3-流式架构的三个核心设计">3. 流式架构的三个核心设计</h2> <p>搞清楚了”流式必然牺牲未来上下文”这个前提，再来看这篇怎么把 SpeechLLM 改成流式的。改造后的架构如下：</p> <p><img src="/assets/img/posts/20260805-streaming-asr-llm/002_Fig_2.png" alt="流式 SpeechLLM 架构"/></p> <p>图从底部往上看：音频输入 → Chunking（分块）→ Speech encoder → Adaptor → Policy network（MoChA）→ 对齐矩阵 → 交错序列 Hy → LLM → 输出文字 + 三路损失（$L_{LLM}$、$L_{MoChA}$、$L_{minLT}$）。</p> <p>三个核心设计依次解释。</p> <h3 id="31-音频分块编码04-秒等一次">3.1 音频分块编码：0.4 秒等一次</h3> <p>“流式”不是逐帧实时处理，而是<strong>按固定时间窗口批量处理</strong>——这是所有实际流式 ASR 系统的工作方式，Conformer 这类模型计算量太大，帧级触发不现实。</p> <p>先定几个具体数字方便理解（实际参数类似）：</p> <ul> <li>Conformer 处理后帧率：每 40ms 一帧</li> <li>chunk 大小：0.4s = <strong>10 帧</strong></li> <li>历史 context：1.6s = <strong>40 帧</strong></li> <li>Conformer 输出维度：每帧是一个 d 维向量（比如 256）</li> </ul> <p><strong>以处理第 2 个 chunk（0.4s~0.8s）为例，走一遍完整流程：</strong></p> <p><strong>第一步：拼接输入</strong></p> <p>把”历史 40 帧 + 当前 10 帧”拼成一个序列送进 Conformer：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>输入 shape: [50, d]
位置  0~39：第 1 个 chunk 的音频（历史 context）
位置 40~49：第 2 个 chunk 的音频（当前，要编码的目标）
</code></pre></div></div> <p><strong>第二步：Conformer 前向</strong></p> <p>Conformer 是序列模型，每个输出位置对应同一位置的输入时间步：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>输出 shape: [50, d]
output[0~39]：历史帧的表示（这些上一个 chunk 已经算过了）
output[40~49]：当前帧的表示，关键——这 10 个向量在做 attention 时
               能"回头看"位置 0~39 的历史帧，所以质量比孤立处理更好
</code></pre></div></div> <p><strong>第三步：切片，只保留当前帧</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">chunk_embedding</span> <span class="o">=</span> <span class="n">output</span><span class="p">[</span><span class="mi">40</span><span class="p">:]</span>   <span class="c1"># shape: [10, d]
# 等价于 output[-10:]
</span></code></pre></div></div> <p>就是一个普通的张量切片，<code class="language-plaintext highlighter-rouge">output[0:40]</code> 直接丢掉。</p> <p><strong>第四步：拼到全局序列</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>处理完 chunk 1 后：全局序列 shape [10, d]
处理完 chunk 2 后：全局序列 shape [20, d]  ← 把这 10 帧 append 进来
处理完 chunk 3 后：全局序列 shape [30, d]
...
</code></pre></div></div> <p>每个 chunk 无论编号多少，都产出固定 10 帧，维度完全一致。</p> <hr/> <p><strong>“历史帧输出直接丢掉”为什么可行？</strong></p> <p>Conformer 在流式模式下使用<strong>因果掩码（causal mask）</strong>：位置 i 的 attention 只能看到位置 0..i，不能看未来。这意味着 <code class="language-plaintext highlighter-rouge">output[39]</code>（历史最后一帧）的值完全由位置 0..39 的输入决定，不受位置 40..49 影响——无论这次有没有把当前帧拼进来，历史帧的输出结果完全一样。</p> <p>所以历史帧的输出是<strong>冗余重算</strong>，上个 chunk 已经算过了，丢掉没有任何信息损失。喂历史帧进来的唯一目的，是让 <strong>output[40~49]</strong>（当前帧）的 attention 能回头看它们，从而得到更好的表示。</p> <p>实现上的常见优化：可以缓存历史帧的 Key/Value（类似 LLM 推理时的 KV cache），新 chunk 进来只算 query，历史帧不需要重新前向——结果等价，计算量更小。</p> <hr/> <p><strong>为什么 1.6 秒历史 context 不增加延迟？</strong></p> <p>延迟 = 你要等多久才能开始处理。1.6 秒历史是<strong>已经发生的音频</strong>，处理第 k 个 chunk 时它早就在缓冲区里，不需要等任何东西。反过来，如果要看”未来 0.4 秒”来辅助当前 chunk，就必须等那段音频说完——每看一个 chunk 的未来就多等 0.4 秒。整个系统的最小延迟只由 <strong>chunk 大小（0.4 秒）</strong> 决定，context 窗口多大完全不影响。</p> <p>所以”0.4 秒切成 chunk”的意思是：<strong>音频来了 0.4 秒就处理一批，不是等整段说完</strong>。第一个字要等的时间取决于 MoChA 何时触发，理论下界就是一个 chunk 的时长（0.4 秒），实际上几个 chunk 之后就能开始出字。</p> <p>历史窗口只能向后看，不能向前看。确实存在”需要后面的音频才能确定这个字”的情况（比如多音字、上下文依赖强的词），这种情况下流式就会出错，也是精度损失的主要来源。这是流式 ASR 的基本代价。</p> <h3 id="32-mocha逐帧扫描决定何时触发">3.2 MoChA：逐帧扫描，决定何时触发</h3> <p>分块编码解决了”音频怎么持续喂进来”，接下来的问题是：<strong>什么时候可以把当前积累的音频交给 LLM 解码一个字？</strong></p> <p>这就是 Policy network 里 <strong>MoChA（Monotonic Chunkwise Attention，单调分块注意力）</strong> 负责的事。MoChA 最早在 ICLR 2018 提出，用于 encoder-decoder 模型的流式解码，这篇把它移植进了 decoder-only 框架。</p> <p>结合 Figure 2 中间的<strong>对齐矩阵</strong>来解释。矩阵的每一行对应一个输出 token，每一列对应一个音频帧：</p> <blockquote> <p><strong>对齐矩阵的含义</strong>：行 $i$ 里被”选中”的那个帧（图中的深色圆点）就是 $t_i$——这是 MoChA 认为”第 $i$ 个字的音频到这帧为止就够了”的边界。</p> </blockquote> <p>MoChA 的扫描过程，以预测第 $i$ 个字为例：</p> <ol> <li>从上一个字的边界 $t_{i-1}$ 之后开始，逐帧往后扫</li> <li>对每一帧 $j$，计算选择概率 $p_{i,j}$（用一个轻量 decoder 实现）</li> <li>第一个 $p_{i,j}$ 超过阈值的帧，就是边界 $t_i$</li> <li>触发：把第 $t_{i-1}+1$ 帧到第 $t_i$ 帧这段音频，交给 LLM 解码第 $i$ 个字</li> </ol> <p>“Monotonic（单调）”是指 $t_1 \leq t_2 \leq \cdots \leq t_L$——对齐只能向前走，不能回头。这是现实约束：你不能用”之前已经说完的音频”重新决定更早的字。</p> <p>对齐跑完整句，得到一个序列 $[t_1, t_2, \ldots, t_L]$，和输出文字 $[y_2, \ldots, y_L]$ 一一对应（$y_1$ 是 BOS，不需要对应音频；$t_1 = 0$ 作为起点）。</p> <h3 id="33-interleaving改变-llm-的看法">3.3 Interleaving：改变 LLM 的”看法”</h3> <p>有了 MoChA 的对齐，接下来要解决的问题是：<strong>怎么让 LLM 适应流式解码？</strong></p> <p>非流式 LLM 在训练时学到的是”看到全部音频再出字”——这个能力不能直接迁移到流式。需要重新训练 LLM，让它学会”每次只看对应这个字的那一小段音频就能出字”。</p> <p>做法是把训练序列的格式从：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[全部 N 帧音频 | BOS | y₂ | y₃ | ... | y_L]
</code></pre></div></div> <p>改成音频段和文字<strong>交错拼接</strong>的格式：</p> \[H_y = [\underbrace{h_1\cdots h_{t_2}_{y_2的音频},\ \underbrace{y_1}_{BOS},\ \underbrace{h_{t_2+1}\cdots h_{t_3}_{y_3的音频},\ \underbrace{y_2}_{第1字},\ \ldots,\ \underbrace{h_{t_{L-1}+1}\cdots h_{t_L}_{y_L的音频},\ \underbrace{y_{L-1}_{倒数第2字}]\] <p>用一个具体的例子来说明。假设识别目标是”你好啊”，加上 BOS 和 EOS，共 5 个 token：$y_1=\text{BOS}$，$y_2=\text{你}$，$y_3=\text{好}$，$y_4=\text{啊}$，$y_5=\text{EOS}$。</p> <p>MoChA 跑完之后，假设对齐结果是（$t_1=0$ 是起点）：</p> <ul> <li>$t_2=3$：第 1-3 帧是”你”的音频</li> <li>$t_3=6$：第 4-6 帧是”好”的音频</li> <li>$t_4=9$：第 7-9 帧是”啊”的音频</li> <li>$t_5=10$：第 10 帧对应 EOS</li> </ul> <p>那么交错序列 $H_y$ 是：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[h₁, h₂, h₃,  BOS,  h₄, h₅, h₆,  你,  h₇, h₈, h₉,  好,  h₁₀,  啊]
 ↑___________  ↑     ↑___________  ↑    ↑___________  ↑    ↑     ↑
 "你"的音频    BOS   "好"的音频     你   "啊"的音频    好   EOS前  啊
</code></pre></div></div> <p>LLM 拿着这个序列做训练，学到的是：</p> <ul> <li>看到 <code class="language-plaintext highlighter-rouge">[h₁,h₂,h₃, BOS]</code> → 预测”你”</li> <li>看到 <code class="language-plaintext highlighter-rouge">[..., h₄,h₅,h₆, 你]</code> → 预测”好”</li> <li>看到 <code class="language-plaintext highlighter-rouge">[..., h₇,h₈,h₉, 好]</code> → 预测”啊”</li> <li>看到 <code class="language-plaintext highlighter-rouge">[..., h₁₀, 啊]</code> → 预测 EOS</li> </ul> <p><strong>这就是 interleaving 的本质</strong>：把”用整段音频预测整句话”的训练目标，拆解成”用这段字对应的音频 + 上一个字，预测下一个字”。LLM 学完之后，推理时直接可以流式工作。</p> <h3 id="34-推理时的流式循环">3.4 推理时的流式循环</h3> <p>明白了 interleaving，推理就很直白了：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>初始状态：BOS token，KV cache 为空

循环：
  1. 持续录入音频，编码新 chunk
  2. MoChA 从上次边界处开始扫描，直到触发（找到 t_i）
  3. 截出这段新音频 h_{t_{i-1}+1 : t_i}，拼上上一个预测字
  4. 送进 LLM（续接 KV cache，只处理新增的 token）→ 得到下一个字
  5. 把这个字回馈给 MoChA，更新 t_{i-1} = t_i，继续等新音频
  直到 LLM 输出 EOS
</code></pre></div></div> <p>注意第 4 步的 KV cache：每次 LLM 只处理新增的几帧音频 + 上一个字，不需要从头重算，效率没有问题。</p> <p><strong>和传统非流式 SpeechLLM 的本质区别</strong>就在这里：</p> <table> <thead> <tr> <th> </th> <th>非流式</th> <th>流式（本文）</th> </tr> </thead> <tbody> <tr> <td>开始出字的时机</td> <td>整段音频说完之后</td> <td>积累够 MoChA 触发所需的音频后</td> </tr> <tr> <td>LLM 每次看到的音频</td> <td>全部 N 帧</td> <td>当前字对应的那几帧</td> </tr> <tr> <td>能否利用未来音频</td> <td>能（整句编码）</td> <td>不能（只有历史）</td> </tr> <tr> <td>精度</td> <td>高（5.0% CER）</td> <td>略低（5.5% CER）</td> </tr> <tr> <td>首字延迟</td> <td>≈ 整段录音时长</td> <td>≈ 第一个字说完后的几帧</td> </tr> </tbody> </table> <hr/> <h2 id="4-两个训练技巧">4. 两个训练技巧</h2> <h3 id="41-minlt直接优化延迟">4.1 minLT：直接优化延迟</h3> <p>MoChA 学出来的对齐不一定是”最省延迟”的——它只管对齐准不准，不管触发时机早不早。</p> <p><strong>minLT（Minimal Latency Training）</strong> 损失干的是：用一个 HMM 混合 ASR 系统先跑 forced alignment，得到每个字的黄金边界 b_i，然后对 MoChA 的期望对齐位置和 b_i 的偏差做惩罚。损失是可微的（用了 MoChA 的边际对齐概率 α_{i,j}），可以端到端优化：</p> \[L_{total} = L_{LLM} + L_{MoChA} + \lambda L_{minLT}\] <p>λ=0.1。策略网络的输出只在训练时参与 loss 计算，推理时由 MoChA 实时触发，不需要额外模块。</p> <p>要说清楚的是：<strong>minLT 仍然依赖 HMM forced alignment，这一步没有完全端到端</strong>。想显式控制 token 生成延迟，就得知道”黄金时机”在哪，目前绕不过去。</p> <h3 id="42-联合训练一套参数两种模式">4.2 联合训练：一套参数，两种模式</h3> <p>流式和非流式模型完全共享参数，只在前向路径上有区别（流式走 MoChA + interleaving，非流式走全量 prefix）。训练时每个 batch 随机分配到其中一种模式。</p> <p>好处很直接：<strong>一套参数搞定两个模式，不需要维护两套模型</strong>，而且两个任务互相增益——非流式模式让 LLM 充分学语言建模，流式模式强迫对齐能力。</p> <hr/> <h2 id="5-实验结果">5. 实验结果</h2> <p>评测数据集：AISHELL-1（165 小时普通话）、AISHELL-2（1000 小时）、内部多领域数据集 MD（约 1 小时，金融/教育/影视）。</p> <p>指标是 <strong>CER（Character Error Rate，字错率）</strong>——把识别结果改成标准答案需要改动多少个字，除以标准答案总字数，越低越好。中文按字算所以用 CER，英文按词算叫 WER。</p> <h3 id="51-与-baseline-的比较">5.1 与 baseline 的比较</h3> <p><img src="/assets/img/posts/20260805-streaming-asr-llm/table12_aishell.png" alt="AISHELL-1 和 AISHELL-2 各方法 CER 对比"/></p> <p>在 AISHELL-1 上，非流式 CER <strong>4.9%</strong>，流式 <strong>5.1%</strong>。之前最好的流式 decoder-only 方法是 BESTOW，5.3%，被超过了。encoder-decoder 的流式 baseline 是 6.9%——可以看到 LLM-based 方法和传统 encoder-decoder 已经拉开了差距。</p> <p>AISHELL-2 上类似，流式 <strong>5.5%</strong> vs BESTOW 的 5.6%，差距不大但稳定。</p> <p>一个值得注意的地方：<strong>流式和非流式的 CER 差距只有 0.5 个点</strong>（AISHELL-2 上 5.0% vs 5.5%），说明 MoChA + interleaving 对精度的影响比预想的小得多——传统印象里”流式比非流式差一截”，这里被打了折扣。</p> <h3 id="52-延迟优化效果">5.2 延迟优化效果</h3> <p><img src="/assets/img/posts/20260805-streaming-asr-llm/table4_latency.png" alt="延迟对比（AISHELL-2 测试集）"/></p> <p>延迟以帧为单位，1 帧 = 40 ms。测 First（第一个 token 延迟）、Mid.（中间 token）、Last（最后 token）和平均 Avg.。</p> <p>baseline 流式系统（encoder-decoder + MoChA）平均延迟 15 帧（600 ms）。本文不加 minLT 时平均 16 帧——MoChA 学出的对齐不够紧，反而比 baseline 略高。</p> <p><strong>加上 minLT 之后，平均降到 6 帧（240 ms），降幅 62.5%</strong>，CER 只从 5.4% 涨到 5.5%——用 0.1 个点的精度换了 2.5 倍的延迟降幅。</p> <p>Last token 延迟从 9 帧降到 2 帧（80 ms），说话人说完最后一个字之后，系统基本立即输出，不需要等。</p> <h3 id="53-消融实验">5.3 消融实验</h3> <p><img src="/assets/img/posts/20260805-streaming-asr-llm/table5_ablation.png" alt="消融实验（AISHELL-2 测试集）"/></p> <p>三个关键设计的贡献：</p> <ul> <li><strong>w/o joint-train</strong>：流式和非流式单独训，CER 5.1%/5.6%，比联合训练的 5.0%/5.5% 略差，差距不大。联合训练是基本免费的增益。</li> <li><strong>w/o LoRA</strong>：冻结 LLM 只靠编码器和适配器，CER 5.4%/5.7%，退化明显。LoRA 是必要的。</li> <li><strong>w/o Qwen init.</strong>：LLM 从随机初始化开始，CER 6.5%/7.2%，退化最显著——<strong>预训练 LLM 的知识是这套方案最核心的底气</strong>，没有它，decoder-only ASR 的优势基本消失。</li> </ul> <hr/> <h2 id="6-几点-take">6. 几点 take</h2> <p><strong>第一，interleaving 是整篇最值得记住的设计</strong>。把音频段和文字标签交错喂给 LLM，这不只是训练技巧，而是从根本上改变了 LLM 的注意力上下文——让它学会”一小段音频对应一个字”，而不是”整段音频对应整个句子”。思路上和 LLM 里的 prefix interleaving、multimodal interleaving 是一脉相承的。</p> <p><strong>第二，MoChA 从 encoder-decoder 移植到 decoder-only 框架，这件事有个细节值得注意</strong>。原来 MoChA 在 encoder-decoder 里，decoder 是单独的 RNN，策略网络和 decoder 融为一体；这里 LLM decoder 太重，不能直接当 policy，所以策略网络是独立的轻量模块，只在训练时贡献损失，推理时只跑 MoChA 的触发机制。<strong>解耦本身就是这篇的一个工程贡献</strong>。</p> <p><strong>第三，minLT 还是需要 HMM，没有完全端到端</strong>。论文里写得比较轻描淡写，但这是实际的工程成本：需要维护一个 HMM 系统来生成 forced alignment。如果未来能用 CTC 对齐代替 HMM、或者完全用强化学习去优化延迟，实用性会更强。</p> <p><strong>第四，Qwen 初始化是整套方案的地基</strong>。去掉预训练权重，流式 CER 从 5.5% 直接到 7.2%，退化 1.7 个绝对点——幅度远超流式带来的额外损失（0.5 点）。SpeechLLM 的核心优势永远是那个 LLM，不是外面包的那层 wrapper。新加的模块（MoChA、interleaving）是在复用 LLM 的语言建模能力，而不是取代它。</p> <p><strong>第五，流式精度损失小，背后有个值得挖的原因</strong>。中文字符粒度的决策依赖的上下文比英文 word 少，单字在大多数情况下一小段音频就能确定，MoChA 的局部对齐基本够用。如果换成英文长词，或者上下文依赖更强的语言，这个 0.5 点的差距可能会扩大。这篇只测了普通话，英文场景的泛化性还是个问号。</p> <hr/> <p>扯一句题外话。这篇把 MoChA 从 encoder-decoder 移植到 decoder-only 框架，本质上解决的是”怎么在预算约束（延迟）下，让模型把已有的表达能力重新分配到新任务上”。和 AutoML 里研究”怎么在参数/算力约束下自动找最优架构配置”是同一类问题的两个侧面——都是效率与能力的 trade-off 优化。</p> <p>我和合作者把这方面的积累整理成了《动手学 AutoML：从 NAS 到大语言模型优化实战》。书里有专门的章节讲 LoRA 这类参数高效微调方法的机制（rank 怎么选、加在哪些层），以及 LLM 后训练压缩（剪枝/量化）。和本文”给定预训练 LLM，在最小精度损失下加流式能力”的思路是话题相邻的。感兴趣的同学可以搜一下。</p> <blockquote> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master//assets/img/posts/20260805-streaming-asr-llm/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="ASR"/><category term="SpeechLLM"/><category term="流式识别"/><category term="论文解读"/><summary type="html"><![CDATA[arXiv’26 | 流式 SpeechLLM：MoChA 让 LLM 边听边转写，延迟降 62.5%]]></summary></entry></feed>