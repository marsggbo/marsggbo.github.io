<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://marsggbo.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://marsggbo.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-19T04:29:19+00:00</updated><id>https://marsggbo.github.io/feed.xml</id><title type="html">HE Xin</title><subtitle>&quot;SSS: Study, Sleep, Slim&quot; </subtitle><entry><title type="html">LLM Agent 真的有「记忆」吗？一篇综述，把这件事说清楚</title><link href="https://marsggbo.github.io/blog/2026/llm-agent-memory/" rel="alternate" type="text/html" title="LLM Agent 真的有「记忆」吗？一篇综述，把这件事说清楚"/><published>2026-05-19T00:00:00+00:00</published><updated>2026-05-19T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/llm-agent-memory</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/llm-agent-memory/"><![CDATA[<h1 id="llm-agent-真的有记忆吗一篇综述把这件事说清楚">LLM Agent 真的有「记忆」吗？一篇综述，把这件事说清楚</h1> <blockquote> <p>原文：<a href="https://www.preprints.org/frontend/manuscript/cb9b2d755639339002ad1d6da7a9e230/download_pub">LLM Agent Memory: A Survey from a Unified Representation–Management Perspective</a> 作者：Zhenheng Tang, Xin He, Tiancheng Zhao 等（A*STAR CFAR / HKUST）</p> </blockquote> <hr/> <h2 id="1-前言llm-memory-的研究已经乱成一锅粥了">1. 前言：LLM memory 的研究，已经乱成一锅粥了</h2> <p>你有没有遇到过这种情况——</p> <p>看了三篇关于 LLM “long-term memory” 的文章，发现第一篇讲的是 RAG，第二篇讲的是 KV cache 压缩，第三篇讲的是 knowledge editing。三篇用的术语完全不同，解决的问题表面上一样，但技术路线、工程约束、适用场景天差地别，读完反而更困惑了。</p> <p>这不是个别现象。<strong>LLM memory 这个领域当前的问题就是碎片化太严重</strong>。</p> <p>同一个机制，有人叫 “episodic memory”，有人叫 “agentic memory”，有人叫 “session-level memory”——你不知道他们说的是不是同一件事。现有的综述要么只聚焦 RAG，要么只看 KV cache 优化，要么只谈 knowledge editing，<strong>没有人把这些东西放在同一个框架下统一比较</strong>。</p> <p>HKUST 和 A*STAR CFAR 团队最近发了一篇综述，尝试用一个统一框架把所有 LLM memory 机制梳理清楚。今天聊聊这篇工作的核心思路，以及我觉得对工程师最有用的几个 insight。</p> <hr/> <h2 id="2-为什么-llm-的记忆这么难搞">2. 为什么 LLM 的「记忆」这么难搞？</h2> <p>先交代下背景。</p> <p>LLM 有一个根本性的限制：<strong>context window 是有限的</strong>。不管是 4K、128K 还是 1M token，总有个上限。但实际应用场景——多轮对话、长期任务、agent 系统——往往要求模型能跨会话保留信息，记住用户偏好，甚至在几十轮交互之后还能准确回忆起第 3 轮说过的事情。</p> <p>这就产生了一个根本矛盾：<strong>有限的上下文窗口 vs. 无限延伸的交互需求</strong>。</p> <p>为了破这个局，学术界和工业界提出了各种各样的解法，大致可以分三类：</p> <ul> <li><strong>塞更多文字进 context</strong>（RAG、agentic memory）</li> <li><strong>压缩/缓存中间状态</strong>（KV cache 复用、streaming attention）</li> <li><strong>把知识直接写进模型权重</strong>（continual learning、model editing）</li> </ul> <p>问题是，这三条路子背后的哲学完全不同，工程实现也天差地别，但文献里经常混在一起讲，导致读者很难建立整体认知。这篇综述的切入点，就是用一个<strong>统一的抽象</strong>把三条路子穿起来。</p> <hr/> <h2 id="3-核心框架两个维度一套接口">3. 核心框架：两个维度，一套接口</h2> <p>这篇综述提出的框架沿着两个维度展开：</p> <h3 id="31-memory-representation存在哪存成什么形式">3.1 Memory Representation（存在哪、存成什么形式）</h3> <p><strong>三种 representation</strong>，对应三套完全不同的工程逻辑：</p> <p><strong>① Token-level Memory（文本 token）</strong> 最直觉的形式——把信息存成自然语言，塞回 context 窗口。RAG 是典型代表，把外部知识库检索出来的片段插进 prompt 里。透明度最高，最容易调试，但有两个硬伤：计算复杂度 $O(n^2)$，以及超长 context 下模型容易忽略中间内容（”lost in the middle” 问题）。</p> <p><strong>② Intermediate Latent Memory（中间表示，KV Cache）</strong> 把注意力机制的 KV 矩阵当作记忆载体。KV cache 本质上记录了模型处理过的所有 token 的内部状态，可以跨 decode 步骤复用，甚至跨请求复用（prefix caching）。效率最高，latency 最低，但容量受限、不跨会话持久、也很难人工检查里面存了什么。</p> <p><strong>③ Parameter-level Memory（模型参数）</strong> 知识直接编码进权重——这是 pretraining 和 fine-tuning 的本质。知识最持久，不依赖任何外部存储，但写入代价极高，一旦更新容易产生 catastrophic forgetting。</p> <p>如下图，三种 representation 各自对应不同的任务场景和工程约束：</p> <p><img src="/assets/img/posts/llm-agent-memory/emb_004_p3.png" alt="三种 memory representation 的统一框架概览"/></p> <h3 id="32-memory-management怎么操作这些记忆">3.2 Memory Management（怎么操作这些记忆）</h3> <p><strong>不管哪种 representation，都要经历三个管理操作</strong>，这是这篇综述最核心的贡献——用同一套接口描述所有 memory 机制：</p> <ul> <li><strong>Construction（建立）</strong>：往记忆里写什么、怎么组织索引</li> <li><strong>Update（更新）</strong>：如何增量修改、如何避免遗忘旧知识</li> <li><strong>Query（查询）</strong>：如何在推理时高效检索到相关内容</li> </ul> <p>每种 representation 在这三个操作上的挑战分布截然不同，这也是这篇综述里我觉得最值得反复咀嚼的一张表：</p> <p><img src="/assets/img/posts/llm-agent-memory/table1_decision_matrix.png" alt="Memory 类型与管理挑战的决策矩阵"/></p> <p>从表里可以读出一个反直觉的结论：<strong>representation 的选择，本质上是在把系统瓶颈移到不同的管理操作上，而不是消灭瓶颈</strong>。</p> <ul> <li>选 Token-level → 问题变成”query 怎么做才精准”</li> <li>选 KV cache → 问题变成”cache 满了怎么 evict、怎么管预算”</li> <li>选参数级 → 问题变成”写入代价能不能压下来、怎么防止遗忘”</li> </ul> <p>这个视角非常有用：下次看到一篇新的 memory 论文，先问”它把瓶颈推到哪里了？”，比直接看实验数字更能判断这个工作的价值。</p> <hr/> <h2 id="4-逐层拆解每种-memory-具体怎么做">4. 逐层拆解：每种 Memory 具体怎么做</h2> <h3 id="41-token-levelrag-和-agentic-memory">4.1 Token-level：RAG 和 Agentic Memory</h3> <p>Token-level 涵盖两大方向：</p> <p><strong>RAG</strong> 大家都熟——建向量索引、检索、插 context。但细节里有很多门道：</p> <ul> <li><strong>检索粒度</strong>（chunk 大小）直接影响 precision/recall 的 trade-off。chunk 太小，单条信息不完整；chunk 太大，检索到的噪声多</li> <li><strong>混合稀疏-稠密检索</strong>（BM25 + embedding）能覆盖不同的召回模式，适合生产环境</li> <li><strong>Query 改写</strong>（扩展、分解、step-back prompting）能显著提升召回质量，对 underspecified query 尤其有用</li> <li><strong>索引结构的演化</strong>：从 flat chunking → hierarchical → graph-based，是为了在 memory 越来越大时保持检索精度</li> </ul> <p>如下是论文里关于 Agentic Memory 的构建与更新机制的详细论述，这里面有很多不那么常见但工程上很实用的设计：</p> <p><img src="/assets/img/posts/llm-agent-memory/page_005.png" alt="Agentic Memory 的 construction 与 update 机制"/></p> <p><strong>Agentic Memory</strong> 更进一步——agent 把自己的历史行动、观察、结论都存成文本，通过摘要压缩和选择性遗忘来维护一个可用的长期记忆库。这类系统面临的核心挑战是：<strong>随着 memory 越积越多，如何保证检索出来的还是高信噪比的内容</strong>。</p> <p>一个值得关注的方向是 memory evolution——让 agent 的记忆可以被编辑、链接、随时间重组（A-MEM、Synapse、SCM 等），而不是只进不出的 append-only 日志。这和传统数据库的 CRUD 思路已经非常接近了。</p> <h3 id="42-intermediate-latentkv-cache-的花式玩法">4.2 Intermediate Latent：KV Cache 的花式玩法</h3> <p>KV cache 这部分是目前工程实践里变化最快的方向。</p> <p>除了标准的 prefix caching，论文里覆盖了一大批工作，主要分四条技术路线：</p> <ul> <li><strong>Eviction &amp; Dropping</strong>（StreamingLLM、SnapKV、H₂O）：选择性丢弃不重要的 KV entry，保留 attention 权重高的</li> <li><strong>Merging &amp; Semantic Compression</strong>：不是删掉，而是把相似的 KV entry 合并，减少冗余而不损失语义</li> <li><strong>Quantization &amp; Low-rank</strong>（KIVI、KVQuant 等）：降低每个 KV entry 的存储精度，4-bit 甚至更低</li> <li><strong>System-aware Allocation</strong>：根据部署约束（多卡、显存预算）动态分配 KV 空间</li> </ul> <p>如下是 KV cache 的 update 策略详细对比，可以看到每条路线在不同场景下的取舍：</p> <p><img src="/assets/img/posts/llm-agent-memory/page_007.png" alt="KV cache memory update 的四类主要策略"/></p> <p>这条路的核心矛盾：<strong>KV cache 高度依赖特定模型架构，换个模型就得重新存</strong>。而且 KV 里存的是什么、哪些 token 更”重要”，目前还没有统一的理论，各家方法用的重要性度量（attention score、gate activation、gradient norm）都不一样，导致效果在不同任务上差异很大。</p> <h3 id="43-parameter-level把知识写进权重">4.3 Parameter-level：把知识写进权重</h3> <p>这部分最”重”，但也最值得仔细看，因为这里有很多最近新冒出来的有趣工作：</p> <ul> <li><strong>Knowledge Editing</strong>（ROME、MEMIT、MemoryLLM）：外科手术式地修改特定事实，不重新训练。但研究表明，这类方法改动一个事实往往会无意中影响关联事实，”局部修改、全局波动”是还没解决的硬问题</li> <li><strong>Continual Learning</strong>（EWC、TaSL、POCL）：正则化保护旧知识，对抗 catastrophic forgetting</li> <li><strong>Task Arithmetic / Model Merging</strong>（TIES、AdaMerging、TwinMerge）：把多个 fine-tune 后的权重直接相加或加权合并，不需要重新训练——这个方向最近在实际应用里越来越多，因为省事</li> <li><strong>PEFT / LoRA</strong>：轻量级 adapter，用少量参数记录任务特定知识，是目前 parameter-level update 里性价比最高的方案</li> </ul> <p>如下是参数级 memory 的 update 机制综述，涵盖了 continual learning、PEFT、model merging、model editing 四个方向：</p> <p><img src="/assets/img/posts/llm-agent-memory/page_009.png" alt="参数级 memory 的 update 机制：从 continual learning 到 model editing"/></p> <p><strong>参数级记忆的核心矛盾</strong>：写入代价太高，更新风险大。更深层的问题是——参数里的”知识”是隐式存储的，你很难知道某个事实存在哪几层的哪几个参数里，这使得精确查询和精确修改都非常困难。这也是为什么实际系统往往把稳定的通用知识放权重，把动态个性化的放外部 memory。</p> <hr/> <h2 id="5-最有价值的-takeaway如何选择-memory-机制">5. 最有价值的 Takeaway：如何选择 Memory 机制</h2> <p>论文里的这个决策矩阵（上面已截图）给出了一套选择框架，核心逻辑基于两个维度：</p> <ul> <li><strong>Retention</strong>：需要短期（会话内）还是长期（跨会话）记住？</li> <li><strong>Functional form</strong>：记的是 episodic（具体事件）、semantic（概念知识）还是 procedural（操作技能）？</li> </ul> <p>从矩阵里可以直接读出实践建议：</p> <blockquote> <ul> <li><strong>Token memory</strong>：短期 + 需要 editability → 用于频繁变化或需要接地的信息（RAG、用户历史对话）</li> <li><strong>KV cache</strong>：短期 + 需要 efficiency → 用于会话内的推理状态，快但不持久</li> <li><strong>参数级</strong>：长期 + 需要 persistence → 用于稳定知识，写入成本高但一劳永逸</li> </ul> </blockquote> <p><strong>实际的健壮系统往往三层都有</strong>，组成一个 memory hierarchy，在灵活性、效率和持久性之间取得平衡。</p> <p>这和操作系统的存储层次（寄存器 → L1/L2 cache → RAM → 磁盘）高度同构：</p> <table> <thead> <tr> <th>OS 存储</th> <th>LLM Memory</th> <th>特性</th> </tr> </thead> <tbody> <tr> <td>寄存器 / L1 cache</td> <td>KV cache（attention 窗口内）</td> <td>最快，容量极小</td> </tr> <tr> <td>RAM</td> <td>KV cache pool / token memory</td> <td>中等速度，会话级持久</td> </tr> <tr> <td>磁盘</td> <td>外部向量库 / parametric</td> <td>慢但持久，跨会话可用</td> </tr> </tbody> </table> <p>这个类比不只是好看，它也给了工程师一个直觉：<strong>不要指望用同一种 memory 解决所有问题</strong>，就像没人会用 L1 cache 存数据库一样。</p> <hr/> <h2 id="6-深一点的问题这篇综述没说清楚什么">6. 深一点的问题：这篇综述没说清楚什么？</h2> <p>读这类综述有个姿势：不只是看它说了什么，也要看它没说什么或者说得模糊的地方，那些往往是下一波研究机会所在。</p> <p>这篇综述里我觉得还没讲透的问题：</p> <p><strong>① 跨 memory 类型的联合优化</strong></p> <p>Token memory 和 KV cache memory 目前是完全割裂设计的——RAG 系统决定检索什么，KV cache 系统决定保留哪些 KV，两件事没有任何协同。但它们共同影响的是同一个目标：用有限的计算预算保留最有用的历史信息。有没有统一的系统可以联合调度这两层？目前基本是空白。</p> <p><strong>② memory 的评测标准严重缺失</strong></p> <p>RAG 用 BEIR/RAGAS，KV cache 压缩用 LongBench，knowledge editing 用 ROME 的测试集——各做各的，没有统一 benchmark。这导致跨方法的比较基本上做不了，也很难判断”我的任务到底用哪种 memory 更好”。这是整个领域的基础设施欠债。</p> <p><strong>③ OS/DB 领域的技术迁移远没结束</strong></p> <p>LRU/LFU 缓存替换、B-tree 索引、MVCC 多版本并发控制——这些在数据库领域做了几十年的技术，在 LLM memory 里的应用才刚刚开始。比如 KV cache eviction 本质上就是缓存替换问题，但目前大多数方法还在用基于 attention score 的启发式，而不是基于访问模式的自适应策略。这里有很多可以直接借鉴的成熟技术。</p> <hr/> <p>好了，今天就聊到这里。</p> <p>如果你正在构建 LLM agent 系统，这篇综述是个不错的起点——特别是那张 decision matrix 和三层 memory hierarchy 的设计思路，可以直接用来指导技术选型。原文链接在文章开头，欢迎评论区交流。</p>]]></content><author><name></name></author><category term="LLM"/><category term="Agent"/><category term="Memory"/><category term="KV Cache"/><category term="RAG"/><category term="论文解读"/><summary type="html"><![CDATA[LLM Agent 真的有「记忆」吗？一篇综述，把这件事说清楚]]></summary></entry><entry><title type="html">EuroSys’26 | TokenFlow：让 LLM 流式输出真正「流」起来</title><link href="https://marsggbo.github.io/blog/2026/tokenflow/" rel="alternate" type="text/html" title="EuroSys’26 | TokenFlow：让 LLM 流式输出真正「流」起来"/><published>2026-05-17T01:00:00+00:00</published><updated>2026-05-17T01:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/tokenflow</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/tokenflow/"><![CDATA[<h1 id="eurosys26--tokenflow让-llm-流式输出真正流起来">EuroSys’26 | TokenFlow：让 LLM 流式输出真正「流」起来</h1> <blockquote> <p>原文：<a href="https://dl.acm.org/doi/10.1145/3689031.3696083">TokenFlow: Responsive LLM Text Streaming Serving under Request Burst via Preemptive Scheduling</a></p> </blockquote> <hr/> <p>用过 ChatGPT 或者任何一个 AI 对话产品的人都有同样的体验：你发出一条消息，页面上的文字一个一个「流」出来，就像打字机一样。这种「流式输出」（text streaming）是现代 LLM 交互的标配，给人一种 AI 正在「思考」的感觉，体验比等待一整段文字突然出现好得多。</p> <p>但如果你仔细观察，有时候这个「流」并不稳定——刚开始等了很久才出现第一个字（TTFT 高），或者文字时快时慢、突然卡顿（TBT 不稳）。这篇来自 EuroSys 2026 的论文 TokenFlow 就是要彻底解决这个问题。</p> <h2 id="1-流式输出的两难困境">1. 流式输出的两难困境</h2> <p>LLM 流式服务的核心矛盾，可以用两个指标来描述：</p> <ul> <li><strong>TTFT（Time-to-First-Token）</strong>：用户发出请求到看到第一个 token 的延迟。越低越好，这决定了用户感知到的「响应速度」。</li> <li><strong>TBT（Time-Between-Tokens）</strong>：相邻两个 token 之间的间隔。需要足够稳定，避免出现「卡一下、蹦一下」的感觉。</li> </ul> <p>问题在于，<strong>这两个目标天生矛盾</strong>。</p> <p>当大量请求同时涌入（request burst），系统资源有限，服务器必须做取舍。SGLang 等主流框架采用 FCFS（先来先服务）策略：正在处理的请求继续跑，新来的请求乖乖排队。结果是——</p> <ul> <li>正在跑的请求：生成速度远超用户阅读速度（几十 tokens/秒 vs 用户 5 tokens/秒），白白浪费算力</li> <li>排队等待的请求：TTFT 飙升到 10 秒以上，用户以为系统挂了</li> </ul> <p>如下图所示，SGLang 在 burst 负载下，P99 TTFT 轻松突破 30 秒，而生成速度却远超用户阅读需求——两头都没做好。</p> <p><img src="/assets/img/posts/tokenflow/fig02.png" alt="SGLang burst 问题"/></p> <h2 id="2-被忽视的优化机会token-buffer">2. 被忽视的优化机会：Token Buffer</h2> <p>这里有一个关键观察，也是 TokenFlow 的核心洞察：</p> <p><strong>LLM 的生成速度远远快于用户的阅读速度。</strong></p> <p>实测数据（Figure 1）显示，用户平均阅读速度约 3-5 tokens/秒，而模型生成速度可以达到 30-100 tokens/秒，相差一个数量级。</p> <p><img src="/assets/img/posts/tokenflow/fig01.png" alt="token 消费速度分布"/></p> <p>这意味着，服务端可以提前生成一批 token 存在 <strong>output buffer</strong> 里，用户慢慢消费，系统不用死等用户。只要 buffer 不空，用户体验就是流畅的。</p> <p>这个 buffer 的存在，给了系统一个「喘息空间」——<strong>可以暂停一个正在跑的请求，先处理一个新来的请求的 prefill（生成第一个 token），只要 buffer 里还有剩余 token，暂停的那个用户不会察觉任何异常。</strong></p> <p>这就是 preemptive scheduling（抢占式调度）的机会所在。</p> <h2 id="3-tokenflow-系统设计">3. TokenFlow 系统设计</h2> <p>TokenFlow 的整体架构如下图所示，核心由两部分组成：<strong>Buffer-Aware 调度器</strong> + <strong>Proactive KV Cache 管理器</strong>。</p> <p><img src="/assets/img/posts/tokenflow/fig03.png" alt="TokenFlow 系统架构"/></p> <h3 id="31-buffer-aware-抢占式调度">3.1 Buffer-Aware 抢占式调度</h3> <p>传统抢占式调度（如 Andes）的思路是：新请求来了就强制暂停旧请求。问题是这会导致频繁的 context switch，干扰正在运行的请求。</p> <p>TokenFlow 更聪明——它引入了 <strong>Virtual Buffer Counter</strong>，实时追踪每个请求的：</p> <ul> <li>当前 buffer 中还剩多少 token（buffer occupancy）</li> <li>用户的 token 消费速率（通过请求头或估计得到）</li> </ul> <p>基于这两个信息，调度器动态决定：<strong>什么时候该暂停哪个请求</strong>。</p> <p>具体来说，调度策略分两步（如 Figure 7 所示）：</p> <ol> <li><strong>Soft Preemption</strong>：当某请求的 buffer 充足（用户短期内不会体验到卡顿），就把它暂时移出运行批次，腾出 GPU 资源给队列里等待的请求做 prefill</li> <li><strong>Hard Preemption</strong>：当 GPU 内存即将耗尽，才触发强制抢占 + KV cache 卸载到 CPU</li> </ol> <p><img src="/assets/img/posts/tokenflow/fig06.png" alt="Buffer-Aware 调度示意图"/></p> <p>这样的设计，让系统在”稳住老用户”和”快速响应新用户”之间找到最优平衡点。</p> <h3 id="32-proactive-kv-cache-管理">3.2 Proactive KV Cache 管理</h3> <p>这是 TokenFlow 另一个关键创新。</p> <p>传统系统（包括 Andes）的 KV cache 管理是<strong>被动的（reactive）</strong>：只有当 GPU 内存快满时，才开始把 KV cache 卸载（evict）到 CPU；需要恢复请求时，再从 CPU 加载（load）回 GPU。这个过程 I/O 开销极大，会导致系统从 compute-bound 变成 I/O-bound。</p> <p>TokenFlow 的做法是<strong>主动的（proactive）</strong>：</p> <p><strong>Write-Through 策略</strong>：每次生成新 token，就立刻把对应的 KV cache 同步写到 CPU 内存（类似 CPU cache 的 write-through 策略）。这样，当真正需要抢占请求时，KV cache 已经在 CPU 里了，evict 操作几乎是零开销。</p> <p><img src="/assets/img/posts/tokenflow/fig08.png" alt="Write-Through vs Write-Back 对比"/></p> <p><strong>Chunked + Overlap 策略</strong>：KV cache 的 load/evict 操作被拆成小块（chunks），与 GPU 计算并行执行，利用 PCIe 带宽不影响 CUDA 核心的特点，做到 I/O 和计算的充分重叠。</p> <p><img src="/assets/img/posts/tokenflow/fig10.png" alt="Load-Evict Overlap 技术"/></p> <p>具体实现上，TokenFlow 基于 SGLang 构建，用 CUDA streams + Python 多线程实现了完全重叠的计算与内存操作，代码约 4000 行 Python。</p> <h2 id="4-新的评估指标effective-throughput--qos">4. 新的评估指标：Effective Throughput + QoS</h2> <p>这篇论文还提出了一个重要观点：<strong>传统的 tokens/second 吞吐量指标不足以评估流式服务。</strong></p> <p>原因是：如果服务器狂生 token 但用户 buffer 已经满了，那些多余的 token 对用户没有任何价值，反而浪费了 GPU 算力。</p> <p>TokenFlow 定义了 <strong>Effective Throughput</strong>：只统计用户实际消费到的 token，而不是系统生成的所有 token。这个指标综合了 TTFT、TBT 稳定性和 buffer 利用率，更贴近真实的用户体验。</p> <p>同时，论文定义了 <strong>QoS（Quality of Service）</strong> 指标，综合考虑：</p> <ul> <li>首 token 等待惩罚（TTFT 相关）</li> <li>Playback stall 惩罚（TBT 过长时的卡顿）</li> <li>Token utility（实际被消费的比例）</li> </ul> <p><img src="/assets/img/posts/tokenflow/fig05.png" alt="QoS 指标体系"/></p> <h2 id="5-实验效果">5. 实验效果</h2> <p>实验在多种硬件（RTX 4090、A6000、H200）和模型（Llama3-8B、Qwen2.5-8B/32B）上进行，基线包括 SGLang 和 Andes。</p> <p><strong>使用真实 workload（BurstGPT trace + 内部运营商 trace）的结果：</strong></p> <ul> <li>Effective Throughput：<strong>平均提升 45.1%（A6000），37.1%（H200）</strong>，最高达 82.5%</li> <li>Mean TTFT：<strong>平均降低 52.6%</strong>，P50 最高降低 88.7%</li> </ul> <p><img src="/assets/img/posts/tokenflow/fig12.png" alt="H200 端到端性能对比"/></p> <p><strong>Burst 场景下（合成 workload）：</strong></p> <p>TokenFlow 在 burst 到来时明显减少了排队请求数量，同时保持更高的并发运行请求数——说明系统在压力下能更高效地利用 GPU。</p> <p><img src="/assets/img/posts/tokenflow/fig14.png" alt="Burst 场景排队请求变化"/></p> <p><strong>消融实验（Table 2）</strong>验证了两个核心模块的贡献：单独使用 buffer-aware 调度器或 proactive KV cache 管理都有提升，但两者协同才能达到最优效果——co-design 的价值不言而喻。</p> <p><img src="/assets/img/posts/tokenflow/table2.png" alt="消融实验"/></p> <h2 id="6-一点感想">6. 一点感想</h2> <p>TokenFlow 这篇论文最打动我的地方，是它对”token buffer”这个平时被忽视的结构的洞察。大家都知道 LLM 生成比用户读快，但把这个速度差转化成调度的优化空间，需要一个跨越层次的系统思维。</p> <p><strong>Buffer-aware 调度 + Proactive KV cache 管理</strong>，这两个设计形成了一个自洽的闭环：调度器知道什么时候可以安全抢占（因为 buffer 里还有余量），KV cache 管理器提前把数据备好让抢占近乎无代价，两者协作，最终让”流式输出”真正流起来。</p> <p>这个思路对做 LLM serving 的朋友很有参考价值——不只是优化单个请求的延迟，而是从整个 request burst 的角度来看资源分配和调度策略。下一步自然的延伸问题是：如果用户的阅读速率是动态变化的（比如暂停了 App），或者有不同 SLA 的混合请求，buffer-aware 策略该如何自适应调整？这应该是后续工作有意思的方向。</p>]]></content><author><name></name></author><category term="LLM Inference"/><category term="System"/><category term="LLM Serving"/><category term="Preemptive Scheduling"/><category term="KV Cache"/><category term="Streaming"/><category term="EuroSys"/><summary type="html"><![CDATA[EuroSys’26 | TokenFlow：让 LLM 流式输出真正「流」起来]]></summary></entry><entry><title type="html">KBS’21 | 我写的 AutoML 综述被引 2700+ 次，今天来聊聊这篇文章的来龙去脉</title><link href="https://marsggbo.github.io/blog/2026/automl-survey/" rel="alternate" type="text/html" title="KBS’21 | 我写的 AutoML 综述被引 2700+ 次，今天来聊聊这篇文章的来龙去脉"/><published>2026-05-17T00:00:00+00:00</published><updated>2026-05-17T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/automl-survey</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/automl-survey/"><![CDATA[<h1 id="kbs21--我写的-automl-综述被引-2700-次今天来聊聊这篇文章的来龙去脉">KBS’21 | 我写的 AutoML 综述被引 2700+ 次，今天来聊聊这篇文章的来龙去脉</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/1908.00709">AutoML: A Survey of the State-of-the-Art</a></p> </blockquote> <hr/> <h2 id="1-一个不太谦虚的开场">1. 一个不太谦虚的开场</h2> <p>这篇综述是我读博早期（2019年）写的，发表在 Knowledge-Based Systems（KBS），现在谷歌学术上显示引用量已经超过 <strong>2700 次</strong>。每次看到这个数字都有点不真实的感觉——一篇综述能有这种引用量，说明当时确实踩在了一个节点上：<strong>整个 AutoML/NAS 领域刚刚爆发，大家都急需一个系统性的梳理</strong>。</p> <p>今天想和大家聊聊这篇文章在写什么、当时的研究背景是什么，以及五年多过去了，这个领域发生了什么变化。</p> <p>（不排除穿插一些个人感受和私货，哈哈）</p> <hr/> <h2 id="2-为什么要写-automl-综述">2. 为什么要写 AutoML 综述？</h2> <p>2019 年前后，深度学习已经在图像分类、目标检测、语言模型等方向都取得了突破性进展。但有一个问题一直存在：</p> <p><strong>搭一个好的深度学习系统，高度依赖人类专家。</strong></p> <p>从数据预处理到特征工程，从超参数调整到网络结构设计，每一步都需要领域知识和大量试错。这是深度学习大规模落地的最大障碍之一。</p> <p>而”自动化机器学习”（AutoML）就是为了解决这个问题的一系列方法的总称。问题是，当时 AutoML 领域的综述要么只覆盖 NAS，要么只覆盖 HPO（超参数优化），没有一个完整覆盖整个 ML pipeline 的综述。</p> <p>如下图，我们的综述覆盖了完整的 AutoML 流程：</p> <p><img src="/assets/img/posts/automl-survey/fig01_pipeline.png" alt="AutoML完整pipeline：数据准备→特征工程→模型生成→模型评估"/></p> <p>对比当时已有的综述：</p> <p><img src="/assets/img/posts/automl-survey/table1_comparison.png" alt="不同AutoML综述的覆盖范围对比"/></p> <p>我们是第一个同时覆盖 Data Preparation、Feature Engineering、HPO 和 NAS 的综述。</p> <hr/> <h2 id="3-综述内容速览">3. 综述内容速览</h2> <h3 id="31-数据准备">3.1 数据准备</h3> <p>数据是 ML 的基础，AutoML 在数据层面的自动化主要包括三块：</p> <p><strong>数据收集</strong>：如何自动爬取、众包标注、主动学习标注等。</p> <p><strong>数据清洗</strong>：从简单的缺失值填充到 AlphaClean（把清洗操作建模成超参数优化问题）。</p> <p><strong>数据增强</strong>：这是 2019 年的热点话题，AutoAugment（Google）用 RL 搜索最优增强策略轰动一时。如下图，我们系统整理了图像、音频、NLP 三个模态的增强方法：</p> <p><img src="/assets/img/posts/automl-survey/fig03_da_taxonomy.png" alt="数据增强方法分类：图像/音频/NLP三个模态"/></p> <h3 id="32-特征工程">3.2 特征工程</h3> <p>特征选择和特征构造的自动化。这一块在 NAS 出现后关注度有所下降，但在表格数据（非深度学习场景）里仍然很重要，AutoFeat、Featuretools 等工具就在做这件事。</p> <h3 id="33-超参数优化hpo">3.3 超参数优化（HPO）</h3> <p>HPO 是 AutoML 里最”经典”的部分，方法分几类：</p> <ul> <li><strong>Grid Search / Random Search</strong>：简单粗暴，但高维效率极差</li> <li><strong>贝叶斯优化</strong>（BO）：用代理模型（Gaussian Process、Tree Parzen Estimator）建模 f(超参)→性能 的关系，再用 acquisition function 决定下一个采样点。Hyperopt、SMAC3 都是这个方向的代表。</li> <li><strong>进化算法</strong>：把每组超参数当成一个”个体”，交叉变异选优。</li> <li><strong>Hyperband / BOHB</strong>：在资源分配上做了创新，用 early stopping 快速淘汰差的配置。</li> </ul> <h3 id="34-神经架构搜索nas重头戏">3.4 神经架构搜索（NAS）——重头戏</h3> <p>这也是综述最重头的部分（40%+ 的篇幅都在讲 NAS），因为 2019 年 NAS 正在爆炸。</p> <h4 id="搜索空间">搜索空间</h4> <p>NAS 的搜索空间定义了”可以搜索哪些架构”，主要分三种：</p> <p><strong>Entire-structured</strong>：整个网络从头搜——最灵活但搜索代价极大。如下图，节点是 layer，边是操作（conv 3×3、max pool 等）：</p> <p><img src="/assets/img/posts/automl-survey/fig06_search_space.png" alt="全局搜索空间：每个节点是一层，边是操作"/></p> <p><strong>Cell-based</strong>：把网络分成可重复的 cell（block），只搜 cell 的结构再堆叠——大幅降低搜索空间，且天然可迁移。NASNet、DARTS、AmoebaNet 都在这个范式下。</p> <p><strong>Hierarchical</strong>：多级 cell 嵌套——cell 里面还有 cell。</p> <h4 id="架构优化方法">架构优化方法</h4> <ul> <li><strong>强化学习（RL）</strong>：用 controller RNN 生成架构，用验证集精度作为 reward。NASNet 是代表，但当时需要 450 块 GPU 跑 4 天，离谱的消耗。</li> <li><strong>进化算法（EA）</strong>：把架构当成基因，交叉变异。AmoebaNet 系列走这条路。</li> <li><strong>梯度下降（Gradient-based）</strong>：最划时代的突破来自 DARTS（2018）——把离散的架构搜索松弛成连续的，用梯度优化，一块 GPU 跑几小时就搞定。如下图，DARTS 和 P-DARTS 的搜索阶段对比：</li> </ul> <p><img src="/assets/img/posts/automl-survey/fig08_darts.png" alt="DARTS vs P-DARTS：搜索阶段cell数量和操作数量的变化"/></p> <p>P-DARTS 是对 DARTS 的改进——把搜索分成多阶段，逐步增加 cell 数、减少候选操作数，桥接了搜索和评估阶段的 gap。</p> <ul> <li><strong>代理模型（Surrogate）/ 权重共享（One-shot NAS）</strong>：ENAS 用参数共享让所有子网络共享权重，搜索效率又上了一个台阶。</li> </ul> <hr/> <h2 id="4-写这篇综述的感受">4. 写这篇综述的感受</h2> <p>说实话，当时写这篇综述的时候，我刚开始读博不久，主要动机是<strong>搞清楚这个领域到底在干嘛</strong>。</p> <p>那时候 NAS 论文每周都在刷 SOTA，看不过来。写综述迫使我把几十篇论文系统读了一遍，整理出一个有逻辑的 taxonomy。这个过程对自己后续做 NAS 研究（比如我们后来的 EAGAN、NAS-LID 等工作）有很大帮助。</p> <p>引用量高的原因大概是：</p> <ol> <li><strong>时机好</strong>——NAS 正在从小众变大众，需要入门材料</li> <li><strong>覆盖面广</strong>——唯一覆盖完整 pipeline 的综述</li> <li><strong>持续更新</strong>——从 2019 发到 2021 年更新了 6 个版本，保持了一定的时效性</li> </ol> <hr/> <h2 id="5-现在来看哪些判断对了哪些没预测到">5. 现在来看，哪些判断对了，哪些没预测到？</h2> <p><strong>说对了的：</strong></p> <ul> <li>One-shot NAS（权重共享）会成为主流 ✓ —— NASBench、DARTS-PT、β-DARTS、BossNAS 等大量工作涌现</li> <li>NAS 会往更实用方向走（移动端、医疗等垂直场景）✓</li> <li>数据增强自动化（AutoAugment 方向）会持续发展 ✓</li> </ul> <p><strong>没预测到的：</strong></p> <ul> <li>LLM 的爆发让 NAS 在 NLP 侧几乎”消失”了——BERT/GPT 的出现让 Transformer 一统江湖，NLP 里的架构设计问题不再是搜索问题</li> <li>Foundation model 时代，”训一个大模型+微调”的范式让 AutoML 的部分工作（数据增强、架构搜索）在 NLP 里变得不再那么重要</li> <li>但 MoE 架构兴起之后，路由机制的设计又带来了新的 NAS-like 问题——这也是我现在做 ExpertFlow 这类工作的动机之一</li> </ul> <p><strong>一个感悟：</strong> AutoML 的核心精神（用自动化方法替代人工试错）没有过时，只是形态变了——现在更多是 prompt optimization、agent workflow 自动化、MoE 路由优化。</p> <hr/> <h2 id="6-写在最后">6. 写在最后</h2> <p>如果你现在才入门 ML/深度学习，这篇 2021 年的综述作为了解 AutoML 历史背景的读物还是有用的，但 NAS 那部分的很多具体工作现在已经是”历史”了（在 LLM 时代）。</p> <p>如果你对 NAS / AutoML 这个方向有兴趣，欢迎私信聊聊，或者看我们后续更新的工作。</p> <blockquote> <p>论文地址：<a href="https://arxiv.org/abs/1908.00709">https://arxiv.org/abs/1908.00709</a> 代码/工具：<a href="https://github.com/marsggbo/hyperbox">hyperbox</a>（我们组开发的 AutoML 框架）</p> </blockquote>]]></content><author><name></name></author><category term="AutoML"/><category term="NAS"/><category term="超参数优化"/><category term="综述"/><category term="论文解读"/><summary type="html"><![CDATA[KBS’21 | 我写的 AutoML 综述被引 2700+ 次，今天来聊聊这篇文章的来龙去脉]]></summary></entry><entry><title type="html">KDD’25 | BurstGPT：我们收集了 1031 万条 Azure OpenAI 真实 trace，LLM 推理系统没你想的那么稳</title><link href="https://marsggbo.github.io/blog/2026/burstgpt/" rel="alternate" type="text/html" title="KDD’25 | BurstGPT：我们收集了 1031 万条 Azure OpenAI 真实 trace，LLM 推理系统没你想的那么稳"/><published>2026-05-17T00:00:00+00:00</published><updated>2026-05-17T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/burstgpt</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/burstgpt/"><![CDATA[<h1 id="kdd25--burstgpt我们收集了-1031-万条-azure-openai-真实-tracellm-推理系统没你想的那么稳">KDD’25 | BurstGPT：我们收集了 1031 万条 Azure OpenAI 真实 trace，LLM 推理系统没你想的那么稳</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2401.17644">BurstGPT: A Real-world Workload Dataset to Optimize LLM Serving Systems</a> 数据集：<a href="https://github.com/HPMLL/BurstGPT">github.com/HPMLL/BurstGPT</a></p> </blockquote> <hr/> <h2 id="1-一个让人不舒服的真相">1. 一个让人不舒服的真相</h2> <p>做 LLM 推理系统优化的人都知道，系统好不好，要跑 benchmark 才算数。</p> <p>但问题是：<strong>大多数 benchmark 用的 workload 是假的</strong>。</p> <p>要么是用微软 Azure Functions（MAF）这类非 LLM 的 serverless workload，要么是靠参数化的合成数据（比如 Zipf 分布的 request length）自己捏一个。这些 workload 跟 LLM 的真实使用场景差距很大：</p> <ul> <li>请求频率：MAF 平均 1.64 RPS，真实 ChatGPT 对话服务只有 <strong>0.019 RPS</strong>（差了近 100 倍）</li> <li>并发模式：合成数据基本是稳定的，而真实 LLM 服务<strong>充满突发和不规律</strong></li> <li>响应长度：LLM 的输出长度是 auto-regressive 生成的，不确定性极高</li> </ul> <p>我们在香港科技大学（广州）和华为合作，采集了一家区域性 Azure OpenAI GPT 服务提供商（3000+ 用户）的数据，拿到了 <strong>213 天、1031 万条真实 trace</strong>，做成了 BurstGPT 数据集。</p> <hr/> <h2 id="2-burstgpt-是什么">2. BurstGPT 是什么？</h2> <p><img src="/assets/img/posts/burstgpt/001_Figure_1.png" alt="BurstGPT数据收集与使用方法：从Azure OpenAI采集→采样缩放→系统优化"/></p> <p>每条 trace 包含：</p> <table> <thead> <tr> <th>字段</th> <th>说明</th> </tr> </thead> <tbody> <tr> <td>Service Type</td> <td>API 或 Conversation（对话）</td> </tr> <tr> <td>LLM Type</td> <td>GPT-3.5 / GPT-4 / GPT-4v</td> </tr> <tr> <td>Request Start Time</td> <td>请求开始时间</td> </tr> <tr> <td>Request Length</td> <td>输入 token 数</td> </tr> <tr> <td>Response Length</td> <td>输出 token 数（0 = 失败）</td> </tr> </tbody> </table> <p>数据构成：</p> <ul> <li>ChatGPT API：<strong>869 万条</strong></li> <li>GPT-4 API：<strong>95 万条</strong></li> <li>ChatGPT 对话：<strong>30 万条</strong></li> <li>GPT-4 对话：<strong>16 万条</strong></li> </ul> <p>两种服务模式（Conversation vs API）的使用模式完全不同，这一点很关键。</p> <hr/> <h2 id="3-关键发现真实-workload-长什么样">3. 关键发现：真实 workload 长什么样</h2> <h3 id="31-并发模式对话服务周期性api-服务随机突发">3.1 并发模式：对话服务周期性，API 服务随机突发</h3> <p><strong>对话服务</strong>（用户主动使用）：</p> <ul> <li>长期：工作日高、周末低，明显周期性</li> <li>短期：工作时间高（9-18 点），夜间低</li> </ul> <p><strong>API 服务</strong>（程序调用）：</p> <ul> <li>长期：完全不规律，无明显周期</li> <li>短期：随机突发，burst 强度变化剧烈</li> </ul> <p>用 Gamma 分布建模并发度，参数 $\alpha$（形状，$\alpha$ 越小 burst 越强）和 $\beta$（速率）每 20 分钟都在变：</p> <p><img src="/assets/img/posts/burstgpt/006_Figure_6.png" alt="Gamma分布参数随时间变化：对话服务 vs API服务"/></p> <p>对话服务的 $\alpha$ 在上班高峰时剧烈变化，API 服务的 $\alpha$ 和 $\beta$ 整天都在高频波动。这说明：<strong>用固定的合成 workload 评估 LLM 系统，看到的稳定性是假象</strong>。</p> <h3 id="32-token-分布输入短但输出差异大">3.2 Token 分布：输入短但输出差异大</h3> <p><img src="/assets/img/posts/burstgpt/007_Figure_7.png" alt="Request/Response token分布：ChatGPT和Llama-2-13b-chat"/></p> <p>ChatGPT 对话服务：</p> <ul> <li>Request token：Zipf 分布，短请求居多</li> <li>Response token：<strong>双峰分布</strong>——一堆很短的（直接回答），一堆很长的（长文本生成）</li> </ul> <p>Llama-2-13b-chat：</p> <ul> <li>Response token：大量请求直接打到 2048 token 上限</li> </ul> <p>这个双峰现象对 KV cache 管理影响很大——你无法预测一个请求到底会用多少内存。</p> <h3 id="33-失败率chatgpt-对话失败率超过-5">3.3 失败率：ChatGPT 对话失败率超过 5%</h3> <p>这个数据出乎意料。</p> <p>一般云服务的失败率在 0.1% 以内，但我们测到的 ChatGPT 对话服务平均失败率 <strong>&gt;5%</strong>，显著高于 API 服务。</p> <p>原因分析：突发请求导致 KV cache 内存耗尽，系统来不及驱逐就 OOM，直接失败。这是 KV cache 管理不够精细的直接体现。</p> <hr/> <h2 id="4-burstgpt-perf配套-benchmark-工具">4. BurstGPT-Perf：配套 benchmark 工具</h2> <p>数据集有了，但怎么用？我们同时开源了 BurstGPT-Perf：</p> <ul> <li><strong>Workload Generator</strong>：把 BurstGPT trace 缩放到目标系统规模（RPS 缩放 or 参数化缩放）</li> <li><strong>Prompt Pool</strong>：按 Zipf 分布采样 prompt 长度，对齐真实 input 分布</li> <li><strong>Metrics</strong>：涵盖效率（latency、throughput）、稳定性（latency jitter）和可靠性（failure rate）</li> </ul> <p>两种缩放方式：</p> <ol> <li><strong>RPS Scaling</strong>：直接用参数 $c$ 乘时间戳，让平均 RPS 对齐目标系统</li> <li><strong>Modeled Scaling</strong>：调整 Gamma 分布参数，保留 burst 特性同时适配系统规模</li> </ol> <hr/> <h2 id="5-burstgpt-告诉了我们什么">5. BurstGPT 告诉了我们什么？</h2> <h3 id="51-burstgpt-vs-maf2同-rps-下burstgpt-更难伺候">5.1 BurstGPT vs MAF2：同 RPS 下，BurstGPT 更难伺候</h3> <p><img src="/assets/img/posts/burstgpt/014_Figure_14.png" alt="vLLM在BurstGPT和MAF2 workload下的平均延迟和吞吐量"/></p> <p>在同样 RPS（0.326）下，BurstGPT workload 下 vLLM 的平均 token latency（0.0651s）比 MAF2（0.0499s）高 30%，吞吐量也更高（因为实际处理了更多并发）。</p> <p><strong>结论</strong>：用 MAF2 测出来系统很稳，一换真实 workload 就原形毕露。合成数据评测的结论没法信。</p> <h3 id="52-同一优化在对话服务和-api-服务上效果可能相反">5.2 同一优化，在对话服务和 API 服务上效果可能相反</h3> <p>我们用 BurstGPT 的对话 trace 和 API trace 分别评估三种调度策略（FCFS、SRF、LRF）：</p> <ul> <li><strong>对话服务</strong>：LRF（Long Request First）在延迟上优于 FCFS</li> <li><strong>API 服务</strong>：上面的结论完全不成立，LRF 变成了劣势</li> </ul> <p>这说明：<strong>一个 workload 上调优的系统，未必能泛化到另一个 workload</strong>。现在很多论文只报一种 workload 的结果，结论可信度存疑。</p> <h3 id="53-huawei-pd-解耦系统的实际应用">5.3 Huawei PD 解耦系统的实际应用</h3> <p>BurstGPT 还被用于华为 Prefill-Decode（PD）解耦推理系统的原型开发：</p> <ul> <li>用 BurstGPT 的 trace 在仿真平台模拟不同 PD ratio 下的 goodput</li> <li>在工作日 6:00 时刻，随着 workload 从大量短请求（解码密集）切换到长请求（预填充密集），最优 PD ratio 从 2:6 切换到 6:2</li> <li>动态 PD ratio + beam search 进一步提升 goodput</li> </ul> <p>这个 case 说明 BurstGPT 不只是学术玩具，在工业系统设计中也有实际价值。</p> <hr/> <h2 id="6-为什么要做这个工作">6. 为什么要做这个工作？</h2> <p>这是一篇偏向系统的数据论文。我们在 2024 年初就把数据集挂到 arXiv 上了，然后持续更新了一年多（5 个版本），最终发表在 KDD 2025。</p> <p>这个工作的出发点其实很朴素：做 LLM serving 系统研究，需要真实 workload。但当时根本没有公开的 LLM trace 数据集。用合成数据做出来的结论，面对真实场景时经常失灵。</p> <p>我们有机会从一个运行 ChatGPT 区域服务的合作伙伴那里采集到这些数据（当然做了隐私处理，只保留了统计特征），就把它开源了。</p> <p>现在这个数据集在工业界用得比较多，vLLM、SGLang、Mooncake 等框架的测试中都看到过 BurstGPT 的影子。</p> <hr/> <h2 id="7-总结">7. 总结</h2> <table> <thead> <tr> <th>特征</th> <th>BurstGPT</th> </tr> </thead> <tbody> <tr> <td>数据量</td> <td>1031 万条 trace</td> </tr> <tr> <td>时间跨度</td> <td>213 天</td> </tr> <tr> <td>来源</td> <td>Azure OpenAI GPT 区域服务</td> </tr> <tr> <td>服务类型</td> <td>Conversation + API</td> </tr> <tr> <td>模型类型</td> <td>GPT-3.5、GPT-4、GPT-4v</td> </tr> <tr> <td>覆盖维度</td> <td>并发模式、对话模式、响应长度、失败率</td> </tr> </tbody> </table> <p><strong>核心结论：用合成 workload 优化出来的 LLM 推理系统，在真实场景里大概率会让你踩坑。</strong></p> <blockquote> <p>数据集：<a href="https://github.com/HPMLL/BurstGPT">github.com/HPMLL/BurstGPT</a> 论文：<a href="https://arxiv.org/abs/2401.17644">arxiv.org/abs/2401.17644</a></p> </blockquote>]]></content><author><name></name></author><category term="LLM推理"/><category term="Workload分析"/><category term="系统优化"/><category term="数据集"/><category term="论文解读"/><summary type="html"><![CDATA[KDD’25 | BurstGPT：我们收集了 1031 万条 Azure OpenAI 真实 trace，LLM 推理系统没你想的那么稳]]></summary></entry><entry><title type="html">DAC’26 | ExpertFlow：让 MoE 大模型在单卡上跑起来，内存省 93%、速度快 10 倍</title><link href="https://marsggbo.github.io/blog/2026/expertflow/" rel="alternate" type="text/html" title="DAC’26 | ExpertFlow：让 MoE 大模型在单卡上跑起来，内存省 93%、速度快 10 倍"/><published>2026-05-17T00:00:00+00:00</published><updated>2026-05-17T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/expertflow</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/expertflow/"><![CDATA[<h1 id="dac26--expertflow让-moe-大模型在单卡上跑起来内存省-93速度快-10-倍">DAC’26 | ExpertFlow：让 MoE 大模型在单卡上跑起来，内存省 93%、速度快 10 倍</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2410.17954">ExpertFlow: Efficient Mixture-of-Experts Inference via Predictive Expert Caching and Token Scheduling</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>作为研究 LLM 推理效率的牛马，我们组最近有一篇工作被 DAC’26（63rd ACM/IEEE Design Automation Conference）接收了，今天想和大家聊聊这篇工作到底解决了什么真问题，以及我们踩过哪些坑。</p> <p>先交代下背景：MoE（Mixture-of-Experts）模型最近非常火——DeepSeek、Mixtral、Qwen-MoE 这些模型的核心都是 MoE 架构。MoE 的核心思路是”稀疏激活”：一个有 46.7B 参数的 Mixtral-8×7B，每次推理只激活约 12.9B 参数，理论上算力消耗比同体量的 dense 模型少很多。</p> <p>听起来很美？但我们自己跑的时候，结结实实踩了个大坑：</p> <p><strong>Mixtral-8×7B 需要超过 96GB 显存才能跑全参数，直接超出了 80GB 的 A100 容量。</strong></p> <p>这是整个行业 MoE 落地的核心矛盾：<strong>参数是稀疏激活的，但内存必须全量加载</strong>。如果你只有一张消费级 GPU（24GB、48GB），直接跑 MoE 大模型是不可能的。</p> <p>一个自然的解决思路是 offloading——把暂时用不到的 expert 放到 CPU 内存里，用时再加载。但现有方法要么预测不准，要么加载策略太笨，throughput 损耗严重。</p> <p>我们做的 <strong>ExpertFlow</strong> 就是为了解决这个问题。</p> <hr/> <h2 id="2-moe-推理的三个核心瓶颈">2. MoE 推理的三个核心瓶颈</h2> <p>要理解我们为什么要设计这三个组件，先得搞清楚 offloading 推理里到底被什么卡死了。</p> <h3 id="21-expert-预测不准">2.1 Expert 预测不准</h3> <p>做 offloading 的关键是：<strong>提前知道下一步要用哪些 expert，提前把它们加载到 GPU</strong>。</p> <p>现有方法分两类：</p> <ul> <li><strong>回归类</strong>（如 Pregated-MoE）：训练一个 MLP 来预测 router 的打分，但分数稍有偏差就影响输出质量，而且需要大量 fine-tuning。</li> <li><strong>启发式类</strong>（如 LRU、时间局部性）：轻量但完全忽略了 input-dependent 的路由行为，预测精度差。</li> </ul> <p>更关键的问题是：现有方法都是<strong>逐层</strong>预测，当上一层执行完才知道下一层要用哪些 expert——留给调度和预取的窗口极短。</p> <h3 id="22-expert-利用率低">2.2 Expert 利用率低</h3> <p>在 decode 阶段，一个典型的糟糕情况是：每个 batch 里的 token 激活了不同的 expert，导致每个 batch 几乎要加载<strong>所有</strong> expert，而每个 expert 只处理一个 token。</p> <p>这意味着每处理一个 token 就要做一次 CPU→GPU 的传输，IO overhead 极大，实际吞吐量惨不忍睹。</p> <h3 id="23-expert-缓存策略差">2.3 Expert 缓存策略差</h3> <p>最常用的 LRU 策略只看”最近用没用”，完全不考虑路由模式，在 MoE 动态路由下 cache hit rate 极不稳定。SE-MoE 的 ring buffer 设计会把两层的全部 expert 都缓存进来，显存浪费严重。</p> <hr/> <h2 id="3-expertflow-的设计思路">3. ExpertFlow 的设计思路</h2> <p>我们提出了 ExpertFlow，核心是三个协同工作的组件：<strong>RPP + TS + ECE</strong>。</p> <p>如下图，用两个 batch 的例子展示整个流程：</p> <p><img src="/assets/img/posts/expertflow/fig01.png" alt="ExpertFlow整体架构：RPP预测路由，TS重排token，ECE预取expert"/></p> <p>① <strong>RPP</strong>（Routing Path Predictor）在 MoE 计算开始前，一次性预测出所有层的 expert 激活情况； ② <strong>TS</strong>（Token Scheduler）根据预测结果，把路由路径相似的 token 重新分组； ③ <strong>ECE</strong>（Expert Cache Engine）只加载需要的 expert，并实时纠正预测错误。</p> <p>下面逐一细讲。</p> <hr/> <h2 id="4-routing-path-predictorrpp一次性看穿所有层">4. Routing Path Predictor（RPP）：一次性看穿所有层</h2> <h3 id="41-核心设计">4.1 核心设计</h3> <p>现有 predictor 的问题在于<strong>逐层预测</strong>——必须等第 l 层执行完，才能开始预测第 l+1 层的 expert，调度窗口极短。</p> <p>我们把 RPP 设计成 <strong>T5 风格的 encoder-decoder 架构</strong>：</p> <ul> <li><strong>Encoder</strong> 对整个输入序列做 embedding；</li> <li><strong>Decoder</strong> 一次性输出所有 MoE 层的 expert 激活预测（shape: B×S×L×E）；</li> <li>每一层挂一个轻量 classification head，直接输出该层各 expert 的激活概率。</li> </ul> <p>如下图：</p> <p><img src="/assets/img/posts/expertflow/fig02.png" alt="RPP架构：T5 encoder-decoder一次性预测所有层的expert激活"/></p> <p>整个预测在第一个 MoE 层执行<strong>之前</strong>就完成，给调度和预取留出充足的时间。</p> <h3 id="42-训练方式">4.2 训练方式</h3> <p>训练非常简单：把每个 token 的 expert 选择记录为二值矩阵 r ∈ {0,1}^(L×E)，predictor 输出概率矩阵 p，用 binary cross-entropy 训练即可。最终的 RPP <strong>模型只有 7.21 MB</strong>，极其轻量。</p> <h3 id="43-效果">4.3 效果</h3> <p>在 Qwen1.5 上，in-domain 精度超过 <strong>95%</strong>；即使跨域（在 Alpaca 上训练，在 XSUM/WMT16 上预测），精度仍维持在 80-90%，而 TLP、SLP 等启发式方法在很多情况下不到 20%。</p> <hr/> <h2 id="5-token-schedulerts让-token-不再各奔东西">5. Token Scheduler（TS）：让 token 不再”各奔东西”</h2> <h3 id="51-问题直觉">5.1 问题直觉</h3> <p>想象 decode 阶段，Batch1 的 4 个 token 各自激活了不同的 expert，Batch2 也一样——结果每个 batch 都要加载全部 expert，每个 expert 只处理 1 个 token：</p> <p><img src="/assets/img/posts/expertflow/fig03.png" alt="Token Scheduler：左侧是最坏情况，右侧是TS重排后的优化结果"/></p> <p><strong>TS 的思路</strong>：把两个连续 batch 的 token 合并，然后根据路由路径相似度重新分组。路由相似的 token 归同一个 batch，这样每个 batch 激活的 expert 数量减半，每个 expert 处理的 token 数量翻倍，cache reuse 和 GPU 利用率大幅提升。</p> <h3 id="52-实现k-means-近似">5.2 实现：K-Means 近似</h3> <p>精确求解最优分组是 NP 难的，我们用 K-Means 风格的近似：基于 Hamming 距离构造相似度矩阵，把 2T 个 token 聚成 2 组，CPU overhead &lt; 10ms。</p> <h3 id="53-kv-cache-的一致性问题">5.3 KV Cache 的一致性问题</h3> <p>重排 token 会打乱 attention 的 KV cache 顺序。TS 加入了 <strong>Merge</strong> 和 <strong>Reindex</strong> 两个原语，保证 attention 语义的正确性。</p> <h3 id="54-dual-batch-流水线">5.4 Dual-Batch 流水线</h3> <p>RPP 和 TS 的计算本身有 overhead。我们设计了<strong>双 batch 流水线</strong>：当前 batch 做 prefill/decode 时，RPP 和 TS 已经在异步处理下一个 batch，完全隐藏 overhead。</p> <p><img src="/assets/img/posts/expertflow/fig04.png" alt="Sequential pipeline vs Dual-Batch pipeline，overhead完全隐藏"/></p> <hr/> <h2 id="6-expert-cache-engineece预测性缓存--实时纠错">6. Expert Cache Engine（ECE）：预测性缓存 + 实时纠错</h2> <h3 id="61-plec预测驱动的-cache-分配">6.1 PLEC：预测驱动的 cache 分配</h3> <p>传统 LRU 对所有层平均分配 cache slots，完全不考虑实际需求。</p> <p><strong>PLEC</strong>（Predictive Locality-aware Expert Caching）根据 RPP 的预测结果，按需分配 cache slots：预测某层需要 3 个 expert、另一层需要 2 个 expert，就按 3:2 的比例分配，并提前把最可能用到的 expert prefetch 进 GPU。</p> <p>如下图展示了 ECE 的完整工作流：</p> <p><img src="/assets/img/posts/expertflow/fig05.png" alt="ECE工作流：基于路由预测预取expert，实时纠错misprediction"/></p> <p>早层 expert 计算完成释放 slots 后，这些 slots 会立即被复用来加载后续层的 expert，<strong>I/O 和计算完全重叠</strong>。</p> <h3 id="62-real-time-correction预测错了怎么办">6.2 Real-time Correction：预测错了怎么办？</h3> <p>预测不是 100% 准确的。ECE 在计算过程中实时检测 misprediction：</p> <ul> <li>如果某个预测的 expert 实际没用到（unwanted），把它放到低优先级队列；</li> <li>如果某个 expert 没被预测到但实际用到了（missed），立刻以高优先级加载。</li> </ul> <p>整个纠错过程和正在运行的 expert 计算并行，最大化 throughput。</p> <hr/> <h2 id="7-实验结果">7. 实验结果</h2> <p>实验平台：单张 NVIDIA A40 GPU（48GB）+ Intel Xeon Gold CPU。 测试模型：Switch-32/64/128、Mixtral-8×7B、Qwen1.5-MoE、Deepseek-MoE。 测试任务：Alpaca（对话）、WMT16（翻译）、XSUM（摘要）、AIME2024（数学）。</p> <h3 id="71-吞吐量提升">7.1 吞吐量提升</h3> <p>如下图，我们在多个模型和数据集上对比了三个基线（Cache-MoE、SE-MoE、Pregated-MoE）：</p> <p><img src="/assets/img/posts/expertflow/fig06.png" alt="不同MoE模型和数据集上的吞吐量对比（使用in-domain predictor）"/></p> <p><strong>关键数字：</strong></p> <ul> <li>Switch 系列（WMT16）：随着 expert 数量增多，优势越来越大。Switch-128 在 CS=16/BS=32 时比 SE-MoE 快 <strong>5.86×</strong>；收紧 cache（CS=4）时加速比进一步提升到 <strong>9.99×</strong>。</li> <li>Mixtral-8×7B：比 Cache-MoE 快 <strong>1.99×</strong></li> <li>Qwen1.5-MoE：比 Cache-MoE 快 <strong>2.12×</strong></li> <li>Deepseek-MoE：比 Cache-MoE 快 <strong>1.94×</strong></li> </ul> <p>跨域测试（RPP 在 Alpaca 上训练，在 XSUM/WMT16 上测试）：</p> <p><img src="/assets/img/posts/expertflow/fig07.png" alt="Qwen1.5跨域throughput对比（cross-domain predictor）"/></p> <p>即使用 cross-domain predictor，在 CS=4/BS=16 时仍有 <strong>2.18×</strong>（WMT16）和 <strong>2.21×</strong>（XSUM）的加速，说明 RPP 学到的 expert 激活模式确实能泛化。</p> <h3 id="72-显存节省">7.2 显存节省</h3> <p>对比 All-In-GPU（AIG）基线（所有参数常驻 GPU）：</p> <p><img src="/assets/img/posts/expertflow/fig08.png" alt="各MoE模型的GPU显存占用对比（AIG vs ExpertFlow）"/></p> <ul> <li>Switch-128：15.26 GB → <strong>1.03 GB</strong>（节省 <strong>93.25%</strong>）</li> <li>Deepseek-MoE：31.35 GB → 6.38 GB</li> <li>Qwen1.5-MoE：35.21 GB → 6.52 GB</li> <li><strong>Mixtral-8×7B：AIG 直接 OOM，ExpertFlow 仅用 15.99 GB 顺利完成推理</strong></li> </ul> <h3 id="73-rpp-预测精度">7.3 RPP 预测精度</h3> <p>如下图，在 6 种 MoE 模型上的 layer-wise 预测精度：</p> <p><img src="/assets/img/posts/expertflow/fig09.png" alt="各MoE模型的逐层expert预测精度：ExpertFlow vs TLP vs SLP"/></p> <ul> <li>大多数 in-domain 场景超过 <strong>90%</strong> 精度</li> <li>Cross-domain 只下降 5-10%</li> <li>而 TLP、SLP 两个启发式 baseline 在大多数情况下精度不超过 20-30%</li> </ul> <h3 id="74-cache-hit-rate-vs-消融">7.4 Cache Hit Rate vs 消融</h3> <p>如下图，PLEC 在不同 batch size 和 cache size 下都稳定优于 LRU：</p> <p><img src="/assets/img/posts/expertflow/fig10.png" alt="PLEC vs LRU的expert cache命中率对比（Switch-32）"/></p> <p>CS=16 时，PLEC 在 BS 从 4 增大到 16 的过程中命中率仅从 91.90% 下降到 85.91%（下降 6.05%）；而 LRU 从 76.61% 暴跌到 58.37%（下降 18.24%）。</p> <p>Token Scheduler 的效果随 expert 数量增大而提升：Switch-32 有 1.03× 提升，Switch-64 有 1.15×，Switch-128 有 1.17×。</p> <hr/> <h2 id="8-总结和思考">8. 总结和思考</h2> <p>ExpertFlow 整体就是把”预测”这件事做到极致，然后让整个系统围绕预测结果协同工作：</p> <ul> <li><strong>RPP</strong> 提供全局、提前的路由预测（不是逐层、事后的）</li> <li><strong>TS</strong> 利用预测来消除 token 分散导致的 expert 碎片化</li> <li><strong>ECE</strong> 利用预测做 cache 分配和 prefetch，再用实时纠错兜底</li> </ul> <p>三者缺一不可，拿掉任何一个效果都会大幅下降（论文里有消融实验验证）。</p> <p><strong>这个工作让我最有感触的一点：</strong> MoE 的”稀疏激活”优势在训练时体现得很好，但推理系统如果不做专门设计，这个优势会被内存管理开销完全抵消。系统层面的设计和算法层面的分析同样重要，这也是为什么 DAC 这种 design automation 的顶会非常适合这类工作。</p> <p>代码已开源，有兴趣的同学可以 star 一下：<a href="https://github.com/marsggbo/ExpertFlow">https://github.com/marsggbo/ExpertFlow</a></p> <p>欢迎在评论区讨论，或者在知乎私信我，有什么问题一起聊聊。</p>]]></content><author><name></name></author><category term="LLM"/><category term="MoE"/><category term="推理优化"/><category term="论文解读"/><summary type="html"><![CDATA[DAC’26 | ExpertFlow：让 MoE 大模型在单卡上跑起来，内存省 93%、速度快 10 倍]]></summary></entry><entry><title type="html">ICLR’26 Workshop Spotlight | Lang-PINN：让 LLM 多智能体帮你从自然语言一键搭建物理信息神经网络</title><link href="https://marsggbo.github.io/blog/2026/lang-pinn/" rel="alternate" type="text/html" title="ICLR’26 Workshop Spotlight | Lang-PINN：让 LLM 多智能体帮你从自然语言一键搭建物理信息神经网络"/><published>2026-05-17T00:00:00+00:00</published><updated>2026-05-17T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/lang-pinn</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/lang-pinn/"><![CDATA[<h1 id="iclr26-workshop-spotlight--lang-pinn让-llm-多智能体帮你从自然语言一键搭建物理信息神经网络">ICLR’26 Workshop Spotlight | Lang-PINN：让 LLM 多智能体帮你从自然语言一键搭建物理信息神经网络</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2510.05158">Lang-PINN: From Language to Physics-Informed Neural Networks via a Multi-Agent Framework</a></p> </blockquote> <hr/> <h2 id="1-这是一篇跨界论文">1. 这是一篇跨界论文</h2> <p>写这篇 post 需要解释一下背景——我做 AutoML 和 NAS 出身，然后做 LLM 推理系统，Lang-PINN 这个工作看起来有点”出圈”：怎么又跑去做物理信息神经网络（PINN）了？</p> <p>其实逻辑很直接：<strong>PINN 的搭建本质上是一个 AutoML 问题</strong>，需要：</p> <ol> <li>把问题描述转化为偏微分方程（PDE）形式——等价于问题形式化</li> <li>选择合适的神经网络架构——这是 NAS 问题</li> <li>生成正确的 loss 函数和训练代码——这是代码生成</li> <li>调试、验证和迭代——这是超参数优化+反馈</li> </ol> <p>以前这四步全靠专家手工完成。现在有了 LLM，就可以把这四步自动化——于是就有了 Lang-PINN。</p> <hr/> <h2 id="2-pinn-是什么为什么搭建它这么麻烦">2. PINN 是什么？为什么搭建它这么麻烦？</h2> <p>PINN（Physics-Informed Neural Network）是一种把物理方程嵌进神经网络训练过程的方法。核心思路：不只用数据训练，而是把 PDE（偏微分方程）的残差作为 loss 的一部分，让网络在满足物理定律的前提下拟合数据。</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>总 Loss = 数据拟合 loss + PDE残差 loss + 边界条件 loss
</code></pre></div></div> <p>好处是在数据稀缺场景（物理实验数据贵！）也能训练出合理的模型。应用在流体力学、热传导、电磁场等科学计算领域。</p> <p><strong>但是为什么难用？</strong> 一个科学家想用 PINN 解一个实际问题，需要：</p> <ul> <li>把问题描述翻译成严格的 PDE 数学形式（算子、边界条件、初始条件）</li> <li>选择合适的网络架构——MLP、CNN、GNN、Transformer，不同 PDE 适合不同架构</li> <li>手写 loss 函数代码（要正确实现 PDE 残差）</li> <li>还得懂梯度病态、激活函数选择、采样策略……</li> </ul> <p>对领域科学家（非 DL 专家）来说，这是极高的门槛。对 DL 工程师来说，每换一个物理问题又得重来一遍。</p> <hr/> <h2 id="3-lang-pinn四个-agent-的流水线">3. Lang-PINN：四个 Agent 的流水线</h2> <p><img src="/assets/img/posts/lang-pinn/001_Figure_1.png" alt="Lang-PINN系统概览：四个Agent协作，从自然语言到可执行PINN代码"/></p> <p>Lang-PINN 把整个 PINN 构建流程分解为四个专门的 Agent：</p> <h3 id="agent-1pde-agent--把自然语言翻译成-pde">Agent 1：PDE Agent —— 把自然语言翻译成 PDE</h3> <p><strong>问题</strong>：同一个物理问题，不同科学家的描述方式差异极大。有人说”热扩散方程”，有人说”温度随时间变化满足拉普拉斯算子”，有人描述里还夹杂了不相关的背景信息（”今天实验室咖啡机声音很大”）。</p> <p>直接让 LLM 翻译 PDE，在描述复杂度增加时准确率急剧下降：</p> <p><img src="/assets/img/posts/lang-pinn/002_Figure_2.png" alt="不同语言复杂度下PDE翻译精度对比：Level 1-4"/></p> <p><strong>解决方案</strong>：PDE Agent 采用三步流程：</p> <ol> <li><strong>CoT 多路采样</strong>：采样 K 条思维链，每条生成一个候选 PDE</li> <li><strong>双重验证</strong>：符号等价（AST 树匹配）+ 语义一致性（embedding 余弦相似度）</li> <li><strong>共识投票</strong>：选择与其他候选平均相似度最高的那个</li> </ol> <p>这样即使输入有噪声，也能鲁棒地输出正确 PDE 形式。</p> <h3 id="agent-2pinn-agent--不是所有-pde-都适合-mlp">Agent 2：PINN Agent —— 不是所有 PDE 都适合 MLP</h3> <p>这张图说明了一个反直觉的事实：</p> <p><img src="/assets/img/posts/lang-pinn/003_Figure_3.png" alt="不同PINN架构在四类PDE上的MSE对比（对数坐标）"/></p> <ul> <li>Shallow Water 方程：各架构差不多</li> <li>Convection（对流）方程：MLP 和 GNN <strong>差了两个数量级</strong>以上</li> <li>Poisson 方程：CNN 最好</li> <li>Heat 方程：MLP 和 GNN 几乎不收敛</li> </ul> <p>所以 PINN Agent 做的是：根据 PDE 的物理特征（周期性、几何复杂度、多尺度需求），把每个架构的”能力向量”和 PDE 的”特征向量”做加权余弦相似度匹配，选出最合适的架构。</p> <p>还有一个 History Reuse 机制：如果遇到之前见过的相似 PDE，直接复用历史上效果最好的架构选择，不重复搜索。</p> <h3 id="agent-3code-agent--模块化代码生成">Agent 3：Code Agent —— 模块化代码生成</h3> <p>整段代码一次生成（monolithic）的问题：一个地方出错，整个脚本报废，只能全部重写。</p> <p>改为<strong>模块化生成</strong>：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>① model definition  ② PDE loss  ③ data preprocessing
④ training loop  ⑤ validation  ⑥ main function
</code></pre></div></div> <p>六个模块独立生成，标准化接口连接，出错了只修那个模块，不影响其他部分。</p> <p>实验验证：模块化比 monolithic 在六类 PDE 上成功率翻倍以上。</p> <h3 id="agent-4feedback-agent--闭环迭代">Agent 4：Feedback Agent —— 闭环迭代</h3> <p>执行代码后，Feedback Agent 做两件事：</p> <ol> <li><strong>错误定位</strong>：把 runtime error 归因到具体模块（model/loss/training），指导 Code Agent 只重生成那部分</li> <li><strong>质量评估</strong>：从三个维度打分——Effectiveness（MSE）、Efficiency（收敛速度）、Robustness（loss 平滑度 + 梯度健康度）</li> </ol> <p>质量分 $S(C^t) \geq S(C^{t-1})$ 才接受新版本，否则回滚。</p> <hr/> <h2 id="4-实验结果">4. 实验结果</h2> <h3 id="41-端到端成功率">4.1 端到端成功率</h3> <p><img src="/assets/img/posts/lang-pinn/005_Figure_5.png" alt="Lang-PINN在1D/2D/3D/ND PDE上的成功率对比"/></p> <p>在 1D 和 2D PDE 上，Lang-PINN 成功率超过 80%，而最强基线（SCoT、Self-Debug）只有 55-60%。</p> <p><strong>注意</strong>：Lang-PINN 是从自然语言开始的，其他 baseline 是直接给 canonical PDE 作为输入。换言之，Lang-PINN 多做了一步（NL→PDE），还取得了更好的结果。</p> <h3 id="42-mse-精度">4.2 MSE 精度</h3> <p>在 14 个不同 PDE 上：</p> <ul> <li><strong>KS 方程</strong>（Kuramoto-Sivashinsky，混沌系统）：Lang-PINN MSE = 1.62e-3，baseline 最好 1.09（差了近 3 个数量级）</li> <li><strong>Poisson-MA</strong>（任意形状 Poisson）：Lang-PINN MSE = 2.25e-3，baseline 最好 1.83（差 3 个数量级）</li> <li><strong>Heat-ND</strong>（高维热方程）：Lang-PINN MSE = 4.72e-4，PINNacle reference = 8.52（差近 4 个数量级）</li> </ul> <h3 id="43-时间效率">4.3 时间效率</h3> <p>Lang-PINN 平均 <strong>8 次迭代</strong>收敛，比最差基线（BayesianAgent，31 次）节省 <strong>74%</strong> 的迭代开销。</p> <hr/> <h2 id="5-这个工作的定位">5. 这个工作的定位</h2> <p>Lang-PINN 是 ICLR 2026 的 Workshop Spotlight（AI with Recursive Self-Improvement 方向）。</p> <p>Workshop paper 的好处是可以做一些比较探索性的工作，不需要 full paper 那么大的 benchmark 体量。这个工作的贡献更多在于：<strong>提出了一个框架</strong>，证明了”自然语言→可执行 PINN”这条路是通的。</p> <p><strong>从我自己的视角来看</strong>，这是我把 AutoML 思想（架构搜索 + 超参优化 + 自动评估）和 LLM Agent 结合的一次尝试。PINN 只是一个验证场景，背后的逻辑可以推广到更多科学计算工具的自动化。</p> <p>如果你是做科学机器学习（Scientific ML）的，这个框架可能有直接参考价值。如果你是做 LLM Agent 的，这里面的 modular code generation + feedback-driven refinement 思路也可以复用。</p> <hr/> <h2 id="6-局限性没必要回避">6. 局限性（没必要回避）</h2> <ul> <li><strong>场景限制</strong>：目前只在 PINNacle benchmark 上测试，都是标准 PDE，真实工业场景的不规则几何、多物理耦合还没验证</li> <li><strong>LLM 依赖</strong>：PDE Agent 的质量高度依赖底层 LLM（用的是 DeepSeek-V3、Qwen 等），不同 LLM 效果差异较大</li> <li><strong>时间开销</strong>：虽然比 baseline 少 74%，但 8 次迭代对于简单问题仍然有点重——未来应该有更快的 cold-start 路径</li> </ul> <hr/> <h2 id="7-总结">7. 总结</h2> <p>Lang-PINN 用一句话概括：<strong>四个专门化的 LLM Agent 协作，把”用自然语言描述的物理问题”全自动转化为”可执行的 PINN 训练代码”</strong>。</p> <table> <thead> <tr> <th>维度</th> <th>Lang-PINN</th> <th>最强 baseline</th> </tr> </thead> <tbody> <tr> <td>是否支持 NL 输入</td> <td>✓</td> <td>✗（需手写 PDE）</td> </tr> <tr> <td>MSE 改善</td> <td>3-5 个数量级</td> <td>-</td> </tr> <tr> <td>成功率提升</td> <td>&gt;50%</td> <td>-</td> </tr> <tr> <td>收敛迭代数</td> <td>8 次</td> <td>14-31 次</td> </tr> </tbody> </table> <blockquote> <p>论文：<a href="https://arxiv.org/abs/2510.05158">arxiv.org/abs/2510.05158</a></p> </blockquote>]]></content><author><name></name></author><category term="PINN"/><category term="LLM"/><category term="Agent"/><category term="科学计算"/><category term="论文解读"/><summary type="html"><![CDATA[ICLR’26 Workshop Spotlight | Lang-PINN：让 LLM 多智能体帮你从自然语言一键搭建物理信息神经网络]]></summary></entry><entry><title type="html">AAAI’23 | NAS-LID：用「局部内在维度」给超网做体检，省 86% 显存</title><link href="https://marsggbo.github.io/blog/2026/naslid/" rel="alternate" type="text/html" title="AAAI’23 | NAS-LID：用「局部内在维度」给超网做体检，省 86% 显存"/><published>2026-05-17T00:00:00+00:00</published><updated>2026-05-17T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/naslid</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/naslid/"><![CDATA[<h1 id="aaai23--nas-lid用局部内在维度给超网做体检省-86-显存">AAAI’23 | NAS-LID：用「局部内在维度」给超网做体检，省 86% 显存</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2211.12759">NAS-LID: Efficient Neural Architecture Search with Local Intrinsic Dimension</a> 代码：<a href="https://github.com/marsggbo/NAS-LID">github.com/marsggbo/NAS-LID</a></p> </blockquote> <hr/> <h2 id="1-问题背景one-shot-nas-的排名失灵问题">1. 问题背景：One-shot NAS 的排名失灵问题</h2> <p>One-shot NAS（又叫 Weight-sharing NAS）是 AutoML 领域在 2019-2022 年最火的研究方向之一。核心思路很简单：</p> <p>不是搜一个架构就从头训练，而是训练一个包含所有候选架构的<strong>超网（Supernet）</strong>，所有子网（subnet）共享权重，搜索时直接评估 subnet 的性能。这样搜索效率从「450 GPU 天」降到了「4 GPU 时」（对比 NASNet 和 DARTS）。</p> <p>但有一个老大难问题：<strong>超网训出来的权重，给子网的性能排名严重不准</strong>。</p> <p>具体来说，同一个超网里，子网结构差异很大（有的选 conv3×3，有的选 skip-connect），但它们的权重被强制共享，训练时互相干扰，导致从超网继承权重估计出的性能排名，和子网单独训练的真实排名对不上。</p> <p>当时的解决方案（GM-NAS 等）是：<strong>按梯度相似度把超网拆成多个子超网</strong>，每个子超网只包含结构相似的架构，减少干扰。方向是对的，但有两个痛点：</p> <ol> <li>计算梯度需要 forward + backward，内存消耗大（对大 batch 不友好）</li> <li>梯度是高维稀疏的，用它衡量架构相似度，<strong>可分性（separability）低</strong></li> </ol> <p>我们就是从这里切入，提出了 <strong>NAS-LID</strong>。</p> <hr/> <h2 id="2-核心-idea用局部内在维度代替梯度">2. 核心 idea：用「局部内在维度」代替梯度</h2> <p><strong>局部内在维度（Local Intrinsic Dimension，LID）</strong> 是一个来自拓扑学的概念，描述数据点周围邻域的有效维度（内在维度），反映了高维数据实际”躺”在的低维流形的维数。</p> <p>形式上，对于数据分布 $\mathbf{R}$，点 $x$ 的 LID 定义为：</p> \[\widehat{\text{LID}}(x) = -\left(\frac{1}{k}\sum_{i=1}^{k}\ln\frac{r_i(x)}{r_k(x)}\right)^{-1}\] <p>其中 $r_i(x)$ 是 $x$ 到第 $i$ 个近邻的距离，$r_k(x)$ 是最大近邻距离（本文取 $k=20$）。</p> <p>在 NAS 里，我们把这个工具用来描述架构的<strong>几何性质</strong>：</p> <ul> <li>对超网的每一层，提取该层的输出表示，算出层级 LID 值 $\text{LID}(x_i)$</li> <li>把所有层的 LID 拼成向量 $[\text{LID}_1, \ldots, \text{LID}_L]^T$，作为这个架构的”几何指纹”</li> <li>用这个指纹来判断两个架构是否”相似”</li> </ul> <p><img src="/assets/img/posts/naslid/fig01_lid_profiles.png" alt="不同架构的LID profile：arch-shaped vs 单调递减两种典型形态"/></p> <p>有意思的现象：性能好的架构往往呈”拱形”LID profile（先升后降），而性能差的呈单调递减。这说明 LID 不只是一个分组工具，它本身就包含了架构性能的信息。</p> <p><strong>为什么 LID 比梯度更好？</strong></p> <ul> <li><strong>计算量小</strong>：只需 forward，不需要 backward（梯度）。对于一个 $L$ 层超网分成 $n$ 子超网，NAS-LID 只需 $n \times L$ 个参数，而 GM-NAS 需要 $n \times M$ 个（$M$ 是超网参数量，$M \gg L$）</li> <li><strong>可分性高</strong>：梯度在高维稀疏空间里很难区分相似架构，而 LID 衡量的是低维流形的维数，在低维空间里更好区分</li> </ul> <hr/> <h2 id="3-方法如何用-lid-拆超网">3. 方法：如何用 LID 拆超网</h2> <p>核心流程如下图：</p> <p><img src="/assets/img/posts/naslid/fig02_supernet_partition.png" alt="Supernet partition via LID：从超网到多个子超网的分组过程"/></p> <p><strong>第一步</strong>：对超网每一层的每个候选操作（conv3×3, skip-connect 等），计算该操作对应子超网的层级 LID。</p> <p><strong>第二步</strong>：构造 LID 相似度矩阵。两个子超网的相似度定义为：</p> \[S(\mathcal{A}_{O_i}, \mathcal{A}_{O_j}) = \frac{1}{\|\mathcal{L}_{\mathcal{A}_{O_i}} - \mathcal{L}_{\mathcal{A}_{O_j}}\|_2 + \epsilon}\] <p><strong>第三步</strong>：用图最小割（Graph Min-Cut）把操作分成两组，最大化同组内相似度之和（partition score）。</p> <p><strong>第四步</strong>：迭代拆分，直到子超网数量满足需求。</p> <p>对比 GM-NAS 的双向传播方式，NAS-LID 只需要 forward，内存节省显著：</p> <p><img src="/assets/img/posts/naslid/fig03_characterization.png" alt="NAS-LID vs GM-NAS 表征对比：只需forward vs forward+backward"/></p> <p><img src="/assets/img/posts/naslid/table1_memory.png" alt="GPU显存消耗对比：NAS-LID vs GM-NAS on V100 32GB"/></p> <p>在 NASBench-201 上 batch size 32×3×224×224 时，NAS-LID 只用 <strong>3,413 MB</strong>，GM-NAS 用 <strong>24,073 MB</strong>，省了 <strong>86%</strong> 的 GPU 显存。</p> <hr/> <h2 id="4-实验结果">4. 实验结果</h2> <h3 id="可分性separability">可分性（Separability）</h3> <p>核心指标之一：LID 的可分性比梯度高出两个数量级。</p> <p><img src="/assets/img/posts/naslid/fig04_separability.png" alt="NAS-LID vs GM-NAS separability scores on NASBench-201 edges"/></p> <p>所有 edge 上，NAS-LID 的可分性分数都远高于 GM-NAS，说明用 LID 做超网分组更”确定”，不同组之间的边界更清晰。</p> <h3 id="排名相关性--最终架构性能">排名相关性 &amp; 最终架构性能</h3> <p><img src="/assets/img/posts/naslid/table23_results.png" alt="Table 2: 排名相关性 + Table 3: SOTA对比"/></p> <ul> <li><strong>排名相关性（Table 2）</strong>：NAS-LID 在 Top50/100/150 上的 Kendall 和 Spearman 系数均显著优于 SPOS 和 GM-NAS。更低的标准差说明 LID 驱动的分组更稳定。</li> <li><strong>最终架构性能（Table 3）</strong>：NAS-LID+RSPS 在 CIFAR-10 test 上达到 <strong>92.90%</strong>（接近 optimal 94.37%），CIFAR-100 test 上达到 <strong>69.39%</strong>，均超过 GM-NAS+RSPS。</li> </ul> <p><img src="/assets/img/posts/naslid/fig05_accuracy_curve.png" alt="训练过程中验证精度曲线：NAS-LID更稳定"/></p> <p>进化搜索过程中，NAS-LID 的验证精度波动明显小于 GM-NAS（蓝线 vs 红线），说明子超网干扰减少了，搜索更稳定。</p> <hr/> <h2 id="5-踩坑记录">5. 踩坑记录</h2> <p><strong>坑 1：LID 估计的近邻数 k 怎么选？</strong></p> <p>我们在代码里硬编码了 $k=20$。实际上 $k$ 太小会导致估计不稳定，$k$ 太大又引入了远邻噪声。20 是个经验值，在 NASBench-201 的 batch size 32 下表现稳定。如果换更大的 batch size 或不同数据集，可能需要重新调。</p> <p><strong>坑 2：子超网数量的平衡</strong></p> <p>拆太多子超网会增加 fine-tune 的开销（每个子超网都要单独 fine-tune），但拆太少减少干扰的效果有限。我们最终选择 $T=2$ 轮拆分（$2^2=4$ 个子超网）作为默认配置，兼顾效率和效果。</p> <p><strong>坑 3：迁移到 OFA/ProxylessNAS 空间</strong></p> <p>NASBench-201 是个比较小的 tabular 搜索空间，迁移到更大的 OFA 和 ProxylessNAS 空间时，LID 计算的 batch size 需要相应调大，否则近邻不准。这也是文章最后指出的一个局限。</p> <hr/> <h2 id="6-总结">6. 总结</h2> <p>NAS-LID 的贡献可以用一句话概括：<strong>用更轻量（只需 forward）、更有可分性（LID vs 梯度）的几何指标来拆超网，解决 one-shot NAS 里的权重共享干扰问题。</strong></p> <p>从 86% 的显存节省和更高的排名相关性来看，这个方向是对的。</p> <p>从现在（2026 年）回看：one-shot NAS 这个方向在大模型时代确实沉寂了不少，但超网权重干扰这个根本问题依然存在——换个场景，比如 MoE 的路由器训练干扰、多任务学习的梯度冲突，本质上是同类问题。NAS-LID 里的 LID-based 分组思路，或许在这些新场景里还有用武之地。</p> <blockquote> <p>论文：<a href="https://arxiv.org/abs/2211.12759">arxiv.org/abs/2211.12759</a> 代码：<a href="https://github.com/marsggbo/NAS-LID">github.com/marsggbo/NAS-LID</a></p> </blockquote>]]></content><author><name></name></author><category term="NAS"/><category term="神经架构搜索"/><category term="AutoML"/><category term="One-shot NAS"/><category term="论文解读"/><summary type="html"><![CDATA[AAAI’23 | NAS-LID：用「局部内在维度」给超网做体检，省 86% 显存]]></summary></entry><entry><title type="html">Eurosys26 | FineMoE如何用「细粒度」打破MoE推理的显存-延迟死局 - marsggbo</title><link href="https://marsggbo.github.io/blog/2026/eurosys26-finemoemoe-marsggbo/" rel="alternate" type="text/html" title="Eurosys26 | FineMoE如何用「细粒度」打破MoE推理的显存-延迟死局 - marsggbo"/><published>2026-05-16T03:28:00+00:00</published><updated>2026-05-16T03:28:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/eurosys26--finemoemoe----marsggbo</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/eurosys26-finemoemoe-marsggbo/"><![CDATA[]]></content><author><name></name></author><summary type="html"><![CDATA[layout: post title: &quot;延迟降47%！FineMoE如何用「细粒度」打破MoE推理的显存-延迟死局&quot; date: 2026-05-14 tags: [LLM, MoE, 论文解读, 推理优化, Expert Offloading] Eurosys26 | FineMoE如何用「细粒]]></summary></entry><entry><title type="html">LoRA fine-tune吞吐量提升1.96倍！LoRAFusion如何把内存带宽浪费和pipeline bubble一起干掉 - marsggbo</title><link href="https://marsggbo.github.io/blog/2026/lora-fine-tune196lorafusionpipeline-bubble-marsggbo/" rel="alternate" type="text/html" title="LoRA fine-tune吞吐量提升1.96倍！LoRAFusion如何把内存带宽浪费和pipeline bubble一起干掉 - marsggbo"/><published>2026-05-16T03:27:00+00:00</published><updated>2026-05-16T03:27:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/lora-fine-tune196lorafusionpipeline-bubble---marsggbo</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/lora-fine-tune196lorafusionpipeline-bubble-marsggbo/"><![CDATA[]]></content><author><name></name></author><summary type="html"><![CDATA[layout: post title: &quot;LoRA fine-tune吞吐量提升1.96倍！LoRAFusion如何把内存带宽浪费和pipeline bubble一起干掉&quot; date: 2026-05-14 tags: [LLM, LoRA, 训练优化, Kernel Fusion, 分布式训练,]]></summary></entry><entry><title type="html">Fast26 | LLM 推理启动慢？华为用一个「可编程 Page Cache」把模型加载砍了 79% - marsggbo</title><link href="https://marsggbo.github.io/blog/2026/fast26-llm-page-cache-79-marsggbo/" rel="alternate" type="text/html" title="Fast26 | LLM 推理启动慢？华为用一个「可编程 Page Cache」把模型加载砍了 79% - marsggbo"/><published>2026-05-16T03:26:00+00:00</published><updated>2026-05-16T03:26:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/fast26--llm--page-cache-79---marsggbo</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/fast26-llm-page-cache-79-marsggbo/"><![CDATA[]]></content><author><name></name></author><summary type="html"><![CDATA[layout: post title: &quot;LLM 推理启动慢？华为用一个「可编程 Page Cache」把模型加载砍了 79%&quot; date: 2026-05-14 tags: [LLM, 推理优化, 系统, 论文解读, FAST] LLM 推理启动慢？华为用一个「可编程 Page Cache」把模型]]></summary></entry></feed>