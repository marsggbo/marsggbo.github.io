<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://marsggbo.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://marsggbo.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-07-07T10:02:24+00:00</updated><id>https://marsggbo.github.io/feed.xml</id><title type="html">HE Xin</title><subtitle>&quot;SSS: Study, Sleep, Slim&quot; </subtitle><entry><title type="html"></title><link href="https://marsggbo.github.io/blog/2026/2026-06-07-2605_29350/" rel="alternate" type="text/html" title=""/><published>2026-07-07T10:02:24+00:00</published><updated>2026-07-07T10:02:24+00:00</updated><id>https://marsggbo.github.io/blog/2026/2026-06-07-2605_29350</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/2026-06-07-2605_29350/"><![CDATA[<blockquote> <p>插播：之前写的<a href="https://item.jd.com/14945889.html">《动手学 AutoML》</a>终于出版了，从 NAS 到超参优化都有覆盖，适合想系统入门 AutoML 的同学。好了广告结束，现在进入正题。</p> <p><img src="/assets/img/book_cover_automl.png" alt="动手学AutoML书籍封面"/></p> </blockquote> <h1 id="moe-压缩新思路别删专家别合并专家把它们重映射就够了">MoE 压缩新思路：别删专家、别合并专家，把它们”重映射”就够了</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2605.29350">ConMoE: Expert-Pool Consolidation via Prototype Reassignment for MoE Compression</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有想过，MoE 架构最骄傲的地方，恰恰也是最烫手的地方？</p> <p>MoE（Mixture-of-Experts）的核心设计是稀疏激活——每个 token 只走少数几个 expert，理论上参数量很大但计算量很小。Qwen3-30B-A3B 的名字里 A3B 的意思就是”激活参数 3B”，但总参数有 30B。听起来很美，对吧？</p> <p>但问题来了：<strong>这 30B 参数，你一个都不能少地加载进显存</strong>。</p> <p>不管一个 token 实际上只用了 3B 参数，那剩下 27B 的专家还是得老老实实待在显存里候命。这就是 MoE 推理的核心矛盾：稀疏激活省了算力，但显存一分没省。随着模型越做越大，expert 存储变成了 MoE 落地的最大障碍之一。</p> <p>现有的压缩思路主要两条路：<strong>剪掉专家（pruning）</strong> 和 <strong>合并专家（merging）</strong>。前者直接删掉一些专家，后者把多个专家的权重”揉”成一个新专家。</p> <p>今天要聊的这篇论文提了第三条路：<strong>什么都不删，什么都不合并，只是把原来的专家引用”重新指向”一个更小的专家池</strong>。</p> <p>听起来像绕口令？别急，慢慢来。</p> <hr/> <h2 id="2-现有方案的痛点">2. 现有方案的痛点</h2> <p>在聊 ConMoE 之前，先交代下背景，说说 pruning 和 merging 各自的问题。</p> <p><strong>Expert Pruning（专家剪枝）</strong>：直接删掉贡献低的专家。简单粗暴，但问题是删了就是删了，原来指向这个专家的 router 决策没有任何”容错”——被删专家的那些 token 只能丢给其他留下来的专家，不管合不合适。</p> <p><strong>Expert Merging（专家合并）</strong>：把多个专家的权重融合成一个新专家。听起来更优雅，但实际上构造了一个”原来不存在”的新权重，这个新权重可能引入了原本没有的错误，而且融合方式很敏感，调参难度大。</p> <p>这两类方法有一个共同问题：<strong>把”保留哪些专家参数”和”原来的 expert slot 该怎么被表示”这两件事耦合在一起了</strong>。</p> <p>ConMoE 的作者们说，等等，这两个问题应该分开来看。</p> <hr/> <h2 id="3-conmoe-的核心思路专家池整合expert-pool-consolidation">3. ConMoE 的核心思路：专家池整合（Expert-Pool Consolidation）</h2> <p>论文提出了一个新的框架视角，叫 <strong>expert-pool consolidation</strong>（专家池整合）。</p> <p>核心思想拆成两步：</p> <ol> <li>从原来的专家池里<strong>挑出一个更小的子集</strong>作为”原型（prototype）”</li> <li>把每个原来的专家引用<strong>确定性地重映射</strong>到某个 prototype 上</li> </ol> <p>这样，router 对外还是看到原来那些 expert slot，内部只是把调用转发给对应的 prototype。<strong>专家的权重没有被修改，没有被融合，prototype 就是原来的 pretrained 专家</strong>。</p> <p>如下图，直观地展示了 ConMoE 的整体流程：</p> <p><img src="/assets/img/posts/2605_29350/figure1_p3.png" alt="ConMoE 整体架构示意"/></p> <p>图里的流程很清晰：左边是预训练的 MoE，有若干层，每层有自己的专家池。ConMoE 在若干相邻层构成的 local scope 里，用 calibration 统计数据和专家间的距离来选出 prototype，然后把原来的每个专家 slot 都确定性地指向某个 prototype。最右边压缩后的 MoE 对外接口和原来一样（router 不变），只是内部的专家池变小了。</p> <p>这里有一个很巧妙的地方：<strong>prototype 可以跨层共享</strong>。也就是说，相邻几层的专家可以共用一个 prototype 池，这让压缩比更高，同时也利用了相邻层之间专家的冗余性。</p> <hr/> <h2 id="4-怎么挑-prototype两个信号的乘积">4. 怎么挑 prototype？两个信号的乘积</h2> <p>这是方法里最关键的设计。对于每个 local scope $G$，ConMoE 给每个专家算一个分数 $s_e$，然后取 top-K 作为 prototype。</p> <p>分数由两个信号构成：</p> <p><strong>信号一：路由条件贡献分（contribution score）$a_e$</strong></p> \[a_e = \frac{1}{|\mathcal{D}_e|} \sum_{t \in \mathcal{D}_e} g_e(t) \cdot \|e(h_t)\|_2\] <p>说人话：在 calibration 数据上，这个专家被 router 选中的那些 token，它的输出强度乘以路由权重的平均值。分数高 = 这个专家被用到时贡献大。</p> <p><strong>信号二：可替换性分（replaceability score）$b_e$</strong></p> \[b_e = \min_{e' \in \mathcal{E}_G \setminus \{e\}} d(e, e')\] <p>就是这个专家在 scope 内，到其最近邻专家的距离。距离越大 = 越难被其他专家替代 = 越不可替换。</p> <p>两个分数各自在 scope 内做 min-max 归一化后相乘：</p> \[s_e = \bar{a}_e \cdot \bar{b}_e\] <p><strong>直觉很清晰</strong>：一个好的 prototype，既要在原来模型里有实质性的贡献（不能是闲置专家），又要在参数空间里相对独特（不能被其他专家轻易替代）。两个维度同时满足，才是值得被保留的 prototype。</p> <p>选好 prototype 后，每个原始专家就找自己最近的 prototype：</p> \[m_G(e) = \arg\min_{p \in P_G} d(e, p)\] <p>这就是重映射。干净，确定，不需要梯度，不需要 fine-tune。</p> <hr/> <h2 id="5-跨层共享scope-大了不一定好">5. 跨层共享：scope 大了不一定好</h2> <p>ConMoE 允许相邻几层共享一个 prototype 候选池（local scope），这让压缩更灵活。但 scope 到底设多大合适？</p> <p>如下图是在 Qwen3-30B-A3B 上做的消融，50% expert reduction 的情况下，不同 scope size 的效果：</p> <p><img src="/assets/img/posts/2605_29350/figure2_p7.png" alt="scope size 对性能的影响"/></p> <p>结果很有趣：scope 1（只在单层内选）、scope 2、scope 4 的表现非常接近，scope 4 在这个代表性子集上平均分最高。但 scope 一旦扩到 8 或 16 层，三个任务的性能都出现明显下降，HellaSwag 和 MMLU 掉得尤其多。</p> <p>这说明什么？<strong>相邻层之间确实存在可复用的冗余，但这种冗余是局部的</strong>。距离太远的层，对应的是模型不同深度的语义转换，强行共享 prototype 会引入深度不匹配的问题。</p> <p>这也解释了为什么 ConMoE 选择 bounded local scope，而不是一个全局的 expert pool。</p> <hr/> <h2 id="6-主实验结果">6. 主实验结果</h2> <p>实验在三个 MoE 模型上做了对比：Qwen3-30B-A3B、deepseek-moe-16b-base、OLMoE-1B-7B-0125，各自测了 25% 和 50% 的 expert reduction。Baseline 包括：</p> <ul> <li><strong>Pruning 类</strong>：Frequency pruning（按路由频率删）、REAP pruning（基于贡献分删）</li> <li><strong>Merging 类</strong>：M-SMoE（路由统计引导合并）、HC-SMoE（输出特征聚类合并）</li> </ul> <p>如下表是主要实验结果（6 个多选题 benchmark 的平均分）：</p> <p><img src="/assets/img/posts/2605_29350/table1_p6.png" alt="主实验结果表"/></p> <p>几个关键观察：</p> <ul> <li><strong>DeepSeek 上，ConMoE 在 25% 和 50% reduction 两个设置下都拿到最佳平均分</strong>（0.691 vs 0.681/0.676，0.651 vs 0.642/0.613），这是最亮眼的结果。</li> <li><strong>OLMoE 上，25% reduction 同样是 ConMoE 最好</strong>（0.676 vs 0.669/0.662），50% 时和最强 baseline 很接近。</li> <li><strong>Qwen3 上，pruning 方法特别强</strong>，尤其是 50% reduction 时 Frequency pruning 达到 0.733，ConMoE 是 0.717。但 ConMoE 在 WinoGrande 和 BoolQ 上保持了很强的表现。</li> </ul> <p>整体来说，ConMoE 作为一种全新的”不删不合并、只重映射”的压缩范式，<strong>在和 pruning/merging 同等压缩比下能打平甚至超过</strong>，这本身就是一个有意义的结论。</p> <hr/> <h2 id="7-消融实验哪个设计最重要">7. 消融实验：哪个设计最重要？</h2> <h3 id="71-整合结构consolidation-structure">7.1 整合结构（Consolidation Structure）</h3> <p>如下表对比了不同的整合方式：</p> <p><img src="/assets/img/posts/2605_29350/table2_p6_04.png" alt="整合结构消融实验"/></p> <p>三种 variant：</p> <ul> <li><strong>Layer-local</strong>：只在单层内选 prototype，不跨层</li> <li><strong>Cross-layer fixed-k</strong>：跨层候选池，但每层的 prototype 数量固定</li> <li><strong>Adaptive prototype</strong>：ConMoE 默认方案，用贡献分+可替换性自适应分配</li> </ul> <p>有意思的是，layer-local 已经很强了（ARC-C 0.515，HellaSwag 0.715，MMLU 0.651）。允许跨层候选池（Cross-layer fixed-k）在 HellaSwag 上略有提升，Adaptive prototype 和 Cross-layer fixed-k 表现接近。</p> <p><strong>结论：确定性重映射本身才是最重要的成分</strong>，跨层和自适应选择是锦上添花。</p> <h3 id="72-prototype-选择策略">7.2 Prototype 选择策略</h3> <p>如下表比较了不同的 prototype 选择信号：</p> <p><img src="/assets/img/posts/2605_29350/table3_p7_08.png" alt="prototype 选择消融"/></p> <ul> <li><strong>Distance-only</strong>（只看可替换性）：ARC-C 0.242，HellaSwag 0.319，MMLU 0.231 ——直接崩了</li> <li><strong>Usage top-k</strong>（只看路由频率）：HellaSwag 掉到 0.586，比较弱</li> <li><strong>REAP top-k</strong>（基于贡献分）：HellaSwag 回到 0.718，接近 ConMoE 默认</li> <li><strong>Adaptive prototype</strong>（ConMoE 默认）：HellaSwag 0.716，与 REAP top-k 相当</li> </ul> <p><strong>路由条件贡献分是主导信号</strong>，可替换性是辅助正则化（防止选出重复的 prototype），单独用距离作为选择标准完全行不通。</p> <h3 id="73-事后权重融合诊断">7.3 事后权重融合诊断</h3> <p>有人会问：选好 prototype 之后，能不能再做一下权重融合（把分配给同一 prototype 的那些原始专家的权重平均一下），效果会不会更好？</p> <p>如下表给出了答案：</p> <p><img src="/assets/img/posts/2605_29350/table4_p7.png" alt="事后权重融合诊断"/></p> <p>对比三种方案：</p> <ul> <li><strong>None（ConMoE 默认）</strong>：不做任何融合，ARC-C 0.504，HellaSwag 0.716，MMLU 0.639</li> <li><strong>Arcee fusion</strong>：HellaSwag 从 0.716 直接掉到 0.531，MMLU 从 0.639 掉到 0.512</li> <li><strong>Weighted average</strong>：更惨，HellaSwag 0.469，MMLU 0.358</li> </ul> <p><strong>ConMoE 的收益来自确定性重映射，而不是权重融合</strong>。直接复用原始的 pretrained prototype 才是最稳的策略，加了融合反而破坏了原始权重。</p> <hr/> <h2 id="8-跨层专家可替换性分析">8. 跨层专家可替换性分析</h2> <p>为什么跨层 scope 有效，但又不能太大？论文专门做了一个几何分析。</p> <p>在 Qwen3-30B-A3B 上，用 4 层 scope 计算每个 expert 在参数空间的最近邻，统计最近邻是否在同一层还是不同层：</p> <p><img src="/assets/img/posts/2605_29350/figure3_p8.png" alt="跨层最近邻分析"/></p> <p>左图是热力图，展示从 source layer 到最近邻 layer 的分布。可以看到主要沿对角线集中，说明最近邻大概率在附近的层，而非全局随机分布。</p> <p>右图是每层中”最近邻在不同层”的比例。<strong>整体上有 50.4% 的 expert，其参数空间的最近邻在另一层</strong>，部分中间层超过 70%。</p> <p>这两个观察共同说明：<strong>专家的可替换性不是严格层内的，相邻层之间存在真实的冗余</strong>——这是跨层共享 prototype 的几何依据。但这种冗余是局部的，不支持全局共享。</p> <hr/> <h2 id="9-总结">9. 总结</h2> <p>ConMoE 提供了一个很干净的框架来思考 MoE 压缩：</p> <ul> <li><strong>不删</strong>（pruning 的做法）</li> <li><strong>不改权重</strong>（merging 的做法）</li> <li><strong>只重映射</strong>：选出 pretrained 专家子集作为 prototype，把其他专家的引用指过去</li> </ul> <p>三个设计原则从实验里验证：</p> <ol> <li><strong>确定性重映射是最稳定的成分</strong>，单层内做都已经很强</li> <li><strong>跨层复用要保持局部</strong>，相邻层可以，太远的层会因深度不匹配掉性能</li> <li><strong>路由条件贡献分是主导信号</strong>，可替换性是辅助的正则化</li> </ol> <p>个人觉得这篇论文最有价值的点，是把”保留哪些专家参数”和”原来的 expert slot 怎么被表示”这两个问题<strong>显式地解耦</strong>了。这个视角在之前的 pruning/merging 框架里是隐含的，ConMoE 把它明确化之后，整个设计空间就清晰多了。</p> <p>当然 limitation 也直接：这套方案依赖 calibration 数据，跨层 scope 不能太大，而且现在的”logical reduction ratio”和实际的显存节省还需要专门的 runtime 支持（shared-prototype checkpoint 格式）才能落地。最后这点是整个 prototype sharing 方案的工程挑战，不只是这篇论文的问题。</p> <p>感兴趣的可以去翻原文，代码和复现脚本作者也有提到会开源。</p> <p>欢迎评论区交流，有什么遗漏或者理解有误的地方也欢迎指出~</p>]]></content><author><name></name></author></entry><entry><title type="html">投机解码到底能快多少？从理论建模到 closed-form 的完整推导</title><link href="https://marsggbo.github.io/blog/2026/20260707-speculative-decoding-theory/" rel="alternate" type="text/html" title="投机解码到底能快多少？从理论建模到 closed-form 的完整推导"/><published>2026-07-07T00:00:00+00:00</published><updated>2026-07-07T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260707-speculative-decoding-theory</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260707-speculative-decoding-theory/"><![CDATA[<h1 id="投机解码到底能快多少从理论建模到-closed-form-的完整推导">投机解码到底能快多少？从理论建模到 closed-form 的完整推导</h1> <hr/> <h2 id="1-为什么要认真建模这件事">1. 为什么要认真建模这件事</h2> <p>我最近在看 Speculative Decoding（投机解码）和 Self-Speculative Decoding（自投机解码，也叫 SSD）的相关工作，有一个感受：<strong>大家都知道它快，但”快多少”这件事没有被认真量化过</strong>。</p> <p>图里的那个公式（我看到有人分享的参考）给了一个很漂亮的 closed form：</p> \[\text{Speedup} = \frac{1 - \alpha^{\gamma+1}}{(1-\alpha)(\gamma c + 1)}\] <p>其中 $\alpha$ 是逐 token 接受率，$\gamma$ 是草稿长度，$c$ 是草稿模型 vs. 验证（大）模型单 token 推理的计算成本比。</p> <p>这个公式乍看很简洁，但我觉得它藏了不少假设，而且在 SSD（自投机解码）的场景下，这个建模需要做一些修正。今天我想从头把这两条路的性能建模捋一遍，看看各自的 closed form 长什么样，以及哪些因素会让理论和实际产生偏差。</p> <hr/> <h2 id="2-先把基准搞清楚标准自回归的代价">2. 先把基准搞清楚：标准自回归的代价</h2> <p>标准自回归解码生成 $N$ 个 token，每次都要跑一次完整的大模型前向：</p> \[T_\text{baseline} = N \cdot t_\text{big}\] <p>其中 $t_\text{big}$ 是大模型生成一个 token 的时间（包含 prefill 之后的 decode latency，KV cache 命中的情况下可以近似为一个常数）。</p> <p>这是我们的参照系，所有加速比都相对于它来算。</p> <hr/> <h2 id="3-投机解码speculative-decodingsd的性能建模">3. 投机解码（Speculative Decoding，SD）的性能建模</h2> <h3 id="31-一轮的结构">3.1 一轮的结构</h3> <p>经典 SD 的一轮操作是：</p> <ol> <li><strong>Draft 阶段</strong>：用小的 draft model 串行生成 $\gamma$ 个候选 token，耗时 $\gamma \cdot t_\text{draft}$</li> <li><strong>Verify 阶段</strong>：把这 $\gamma$ 个 token 送给大模型（target model）做一次<strong>并行</strong>前向，耗时约 $t_\text{big}(\gamma)$</li> </ol> <p>注意第 2 步不是 $\gamma \cdot t_\text{big}$，因为大模型可以对 $\gamma$ 个 token 同时做 prefill 式的并行计算。但也不完全等于 $t_\text{big}(1)$，因为序列长了 attention 和 KV cache 的 overhead 会稍微变大。</p> <p><strong>简化假设</strong>（也是上面那个公式的隐含假设）：</p> <ul> <li>大模型验证 $\gamma$ 个 token 的时间 $\approx t_\text{big}$（一次 decode cost），即序列长度对单次 forward 耗时影响忽略不计</li> <li>令 $c = t_\text{draft} / t_\text{big}$（草稿比）</li> </ul> <p>那一轮的总耗时：</p> \[T_\text{round} = \gamma \cdot t_\text{draft} + t_\text{big} = (\gamma c + 1) \cdot t_\text{big}\] <h3 id="32-一轮能接受多少-token">3.2 一轮能接受多少 token？</h3> <p>设逐 token 接受率为 $\alpha$（独立同分布假设）。若生成草稿序列为 $d_1, d_2, \ldots, d_\gamma$：</p> <ul> <li>$d_1$ 被接受的概率 $= \alpha$</li> <li>$d_1, d_2$ 都被接受的概率 $= \alpha^2$</li> <li>…</li> <li>全部 $\gamma$ 个都被接受的概率 $= \alpha^\gamma$</li> </ul> <p>每轮接受 token 数的期望（别忘了最后大模型还会贡献一个新 token）：</p> \[\mathbb{E}[\#\text{tokens per round}] = \sum_{k=0}^{\gamma} \alpha^k = \frac{1 - \alpha^{\gamma+1}}{1 - \alpha}\] <p>推导：设 $X$ = 接受的 token 数（从 0 到 $\gamma$），最后大模型总会产出一个 token，所以：</p> \[\mathbb{E}[\#] = 1 + \sum_{k=1}^{\gamma} P(\text{前 } k \text{ 个都接受}) = 1 + \alpha + \alpha^2 + \cdots + \alpha^\gamma = \frac{1 - \alpha^{\gamma+1}}{1 - \alpha}\] <h3 id="33-speedup-的-closed-form">3.3 Speedup 的 closed form</h3> <p>每轮耗时 $(\gamma c + 1) \cdot t_\text{big}$，产出 $\frac{1-\alpha^{\gamma+1}}{1-\alpha}$ 个 token。</p> <p>标准自回归产出同样数量 token 需要 $\frac{1-\alpha^{\gamma+1}}{1-\alpha} \cdot t_\text{big}$。</p> <p>因此加速比为：</p> \[\boxed{\text{Speedup}_\text{SD} = \frac{1 - \alpha^{\gamma+1}}{(1 - \alpha)(\gamma c + 1)}}\] <p>这就是图里的那个公式，完整推导到这里。</p> <h3 id="34-这个公式说了什么">3.4 这个公式说了什么？</h3> <p>对 $\gamma$ 求导，令 $\partial \text{Speedup} / \partial \gamma = 0$，可以找到最优草稿长度 $\gamma^*$。这个导数不太好直接解析出来，但有几个定性结论值得记住：</p> <p><strong>结论 1</strong>：$c$ 越小（草稿模型越轻量），最优 $\gamma^<em>$ 越大，加速上限越高。这就是图里左右两张图的直觉：$c=0.05$ 时 $\gamma^</em> \approx 8$，$c=0.0005$ 时 $\gamma^*$ 更大且加速比可以到 20x。</p> <p><strong>结论 2</strong>：当 $\gamma \to \infty$ 时，若 $\alpha &lt; 1$，Speedup $\to 0$。草稿越长，浪费的验证成本越高，最终得不偿失。</p> <p><strong>结论 3</strong>：$\alpha$ 对加速的影响是非线性的。$\alpha$ 从 0.9 到 0.95 的提升，带来的加速远比 0.7 到 0.75 大。原因是 $\alpha^\gamma$ 在 $\alpha$ 接近 1 时衰减很慢，能撑住更长的草稿序列。</p> <hr/> <h2 id="4-自投机解码self-speculative-decodingssd">4. 自投机解码（Self-Speculative Decoding，SSD）</h2> <h3 id="41-ssd-和-sd-的本质区别">4.1 SSD 和 SD 的本质区别</h3> <p>SSD 的思路是：<strong>不用另一个小模型，直接用大模型自己的 skip layers 来当 draft</strong>。</p> <p>最简单的实现是把大模型的前 $L_d$ 层作为 draft 网络（contiguous prefix），前向到第 $L_d$ 层就直接 decode，生成 $\gamma$ 个草稿 token；然后跑完整的 $L$ 层做验证。但更一般的变体（如 LayerSkip、Draft&amp;Verify 等）会跳过中间某些层，形成<strong>非连续的 skip 模式</strong>，这两种情况的 KV cache 复用行为截然不同——这也是建模里最容易踩坑的地方。</p> <p>好处是：</p> <ul> <li>不需要维护两套权重，内存 footprint 比 SD 低</li> <li>draft 和 verify 共享词表，分布天然对齐</li> <li>在显存受限场景下，省出来的内存可以换更大的 batch size</li> </ul> <p>代价是：draft 不再是独立模型，接受率 $\alpha$ 往往低于精心选型的 draft model。</p> <h3 id="42-kv-cache-复用的前提激活路径必须一致">4.2 KV cache 复用的前提：激活路径必须一致</h3> <p>这里有个经常被忽视的细节，值得单独强调。</p> <p><strong>KV cache 能被 verify 阶段复用，当且仅当对应层的中间激活（hidden state）在 draft 和 verify 路径下完全相同。</strong></p> <p>对于 contiguous prefix（取前 $L_d$ 层）：</p> <ul> <li>draft 路径：层 $1 \to 2 \to \cdots \to L_d$，激活 $h_1, h_2, \ldots, h_{L_d}$</li> <li>verify 路径：层 $1 \to 2 \to \cdots \to L_d \to L_d+1 \to \cdots \to L$，前 $L_d$ 层的激活与 draft <strong>完全相同</strong></li> </ul> <p>所以 verify 阶段可以直接复用 draft 计算过的 KV，只需从第 $L_d+1$ 层开始补算。这是原始 SSD 论文里的场景。</p> <p><strong>但如果 skip 的是非连续的层，情况就不一样了。</strong></p> <p>假设 draft 跳过了第 $s$ 层（$s &lt; L_d$），那么：</p> <ul> <li>draft 路径里，第 $s+1$ 层的输入是第 $s-1$ 层的输出（跳过了 $s$）</li> <li>verify 路径里，第 $s+1$ 层的输入是第 $s$ 层的输出（没有跳过）</li> </ul> <p>两者输入不同，所以第 $s+1$ 层及其之后所有层的 KV 值均不匹配。<strong>一旦某层被跳过，其后所有层的 KV cache 全部作废，无法给 verify 复用。</strong></p> <h3 id="43-一般化建模r_textdraft-和-r_textreuse-拆开">4.3 一般化建模：$r_\text{draft}$ 和 $r_\text{reuse}$ 拆开</h3> <p>针对一般的 skip 模式，需要引入两个独立参数：</p> <ul> <li> <table> <tbody> <tr> <td>$r_\text{draft} =</td> <td>\mathcal{A}</td> <td>/ L$：draft 实际参与计算的层数比例（$\mathcal{A}$ 是 active 层的集合）</td> </tr> </tbody> </table> </li> <li>$r_\text{reuse}$：verify 阶段可以跳过（复用 KV）的层数比例</li> </ul> <p>$r_\text{reuse}$ 的计算规则：设 $s_1 = \min({1,\ldots,L} \setminus \mathcal{A})$ 是第一个被跳过的层，则：</p> \[r_\text{reuse} = \frac{s_1 - 1}{L}\] <p>即：只有第一个 skip 层<strong>之前</strong>的那些层，其 KV 才对 verify 有效。</p> <p>几个特殊情况：</p> <ul> <li><strong>Contiguous prefix</strong>（$\mathcal{A} = {1, \ldots, L_d}$，无跳过）：$s_1 = L_d + 1$，所以 $r_\text{reuse} = r_\text{draft} = r$</li> <li><strong>第一层就跳</strong>（$1 \notin \mathcal{A}$）：$s_1 = 1$，$r_\text{reuse} = 0$，KV cache 完全无法复用</li> <li><strong>中间跳层</strong>（如跳掉第 $s$ 层，$s &lt; L_d$）：$r_\text{reuse} = (s-1)/L &lt; r_\text{draft}$</li> </ul> <p>一轮 SSD 总耗时的一般形式：</p> \[T_\text{round}^\text{SSD} = \gamma \cdot r_\text{draft} \cdot t_\text{big} + (1 - r_\text{reuse}) \cdot t_\text{big} = [\gamma r_\text{draft} + (1 - r_\text{reuse})] \cdot t_\text{big}\] <p>对应的 closed form：</p> \[\boxed{\text{Speedup}_\text{SSD-general} = \frac{1 - \alpha^{\gamma+1}}{(1-\alpha)[\gamma r_\text{draft} + (1 - r_\text{reuse})]}}\] <p>这个公式涵盖了所有 SSD 变体：</p> <table> <thead> <tr> <th>Skip 模式</th> <th>$r_\text{draft}$</th> <th>$r_\text{reuse}$</th> <th>分母</th> <th>退化到</th> </tr> </thead> <tbody> <tr> <td>Contiguous prefix</td> <td>$r$</td> <td>$r$</td> <td>$1 + (\gamma-1)r$</td> <td>原始 SSD 公式</td> </tr> <tr> <td>完全不 skip（$r_\text{draft}=1$）</td> <td>$1$</td> <td>$1$</td> <td>$\gamma$</td> <td>纯串行，无加速</td> </tr> <tr> <td>首层即 skip</td> <td>$r_\text{draft}$</td> <td>$0$</td> <td>$\gamma r_\text{draft} + 1$</td> <td>和 SD 同构，$c = r_\text{draft}$</td> </tr> <tr> <td>中间跳层</td> <td>$r_\text{draft}$</td> <td>$r_\text{reuse} &lt; r_\text{draft}$</td> <td>介于上两者之间</td> <td>—</td> </tr> </tbody> </table> <p><strong>关键结论</strong>：非连续 skip 的 SSD，KV 复用收益会大幅缩水，当 $r_\text{reuse} \ll r_\text{draft}$ 时，其性能模型接近标准 SD（$c = r_\text{draft}$），KV 复用带来的额外优势几乎消失。</p> <h3 id="44-和-sd-的对比">4.4 和 SD 的对比</h3> <p>并排放三个公式：</p> \[\text{Speedup}_\text{SD} = \frac{1 - \alpha^{\gamma+1}}{(1-\alpha)(\gamma c + 1)}\] \[\text{Speedup}_\text{SSD-contiguous} = \frac{1 - \alpha^{\gamma+1}}{(1-\alpha)[1 + (\gamma-1)r]}\] \[\text{Speedup}_\text{SSD-general} = \frac{1 - \alpha^{\gamma+1}}{(1-\alpha)[\gamma r_\text{draft} + (1 - r_\text{reuse})]}\] <p>Contiguous SSD 之所以分母更小（$1 + (\gamma-1)r$ vs $\gamma c + 1$，当 $c \approx r$ 时差了 $1 - r$ 项），正是 KV 复用的贡献。非连续 skip 一旦让 $r_\text{reuse}$ 接近 0，这个优势就彻底没了。</p> <hr/> <h2 id="5-把更多因素塞进来">5. 把更多因素塞进来</h2> <p>上面的 closed form 都基于几个理想化假设。现实里有哪些因素会让这个模型失准？</p> <h3 id="51-验证阶段的-overhead-不是常数">5.1 验证阶段的 overhead 不是常数</h3> <p>我们假设大模型验证 $\gamma$ 个 token 的时间 $= t_\text{big}$（1 个 token 的时间）。但实际上：</p> \[t_\text{verify}(\gamma) = t_\text{big} \cdot f(\gamma)\] <p>其中 $f(\gamma) \geq 1$，且随 $\gamma$ 增大。原因：</p> <ul> <li>KV cache 长了，attention 的 memory bandwidth 开销增加</li> <li>batch size = $\gamma$ 的 prefill 比 decode 慢（decode 是 memory-bound，prefill 是 compute-bound，两者 kernel 调度不同）</li> </ul> <p>一个简单的修正：</p> \[f(\gamma) \approx 1 + \epsilon \cdot (\gamma - 1)\] <p>其中 $\epsilon \ll 1$ 是单 token 增量开销。代入后分母变为：</p> \[\gamma c + f(\gamma) \approx \gamma c + 1 + \epsilon(\gamma-1) = (\gamma-1)(c + \epsilon) + c + 1\] <p>$\epsilon$ 通常很小（0.01-0.05 量级），但当 $\gamma$ 很大时不可忽略。</p> <h3 id="52-接受率-alpha-不是-iid-的">5.2 接受率 $\alpha$ 不是 i.i.d. 的</h3> <p>实际上第 $k$ 个 token 的接受率往往与前面的接受历史相关，而且不同位置的 $\alpha$ 本身不同。更精确的建模是：</p> \[\mathbb{E}[\#\text{tokens}] = 1 + \sum_{k=1}^{\gamma} \prod_{j=1}^{k} \alpha_j\] <p>当 $\alpha_j$ 单调递减（靠后位置的草稿越来越不准）时，最优 $\gamma^*$ 会比 i.i.d. 假设下更小。很多实验论文观察到接受率随位置衰减，这就是实际最优草稿长度往往不超过 8 的原因。</p> <h3 id="53-计算效率比-c-在-batch-推理时会变">5.3 计算效率比 $c$ 在 batch 推理时会变</h3> <p>单请求场景下，$c \approx t_\text{draft} / t_\text{big}$ 主要由参数量决定（memory bandwidth bound 时近似线性于参数量之比）。但在 batch serving 场景：</p> <ul> <li>大模型 batch size 增大时，吞吐趋向 compute-bound，$t_\text{big}$ 在 batch 变大时减慢更显著</li> <li>draft model 参数少，更容易在小 batch 下就达到 compute bound 上限</li> <li>结果：$c_\text{batch} &gt; c_\text{single}$，加速比下降</li> </ul> <p>这是 batch serving 场景下投机解码加速效果不如单请求的核心原因。</p> <h3 id="54-ssd-的层比例-r-和接受率-alpha-之间的-trade-off">5.4 SSD 的层比例 $r$ 和接受率 $\alpha$ 之间的 trade-off</h3> <p>SSD 里，$r_\text{draft}$ 越大（draft 用更多层），draft model 能力越强，$\alpha$ 也越高。但草稿成本也高了。这构成一个联合优化问题：</p> \[\text{Speedup}(r_\text{draft}, r_\text{reuse}, \gamma) = \frac{1 - \alpha(r_\text{draft})^{\gamma+1}}{(1-\alpha(r_\text{draft}))[\gamma r_\text{draft} + (1 - r_\text{reuse})]}\] <p>其中 $\alpha(r_\text{draft})$ 是 $r_\text{draft}$ 的单调递增函数，而 $r_\text{reuse}$ 由 skip 模式决定（见第 4 节）。最优 $(r^<em>, \gamma^</em>)$ 需要联合搜索，没有解析解，但 grid search 配合实测 $\alpha$ 可以很快找到。</p> <hr/> <h2 id="6-ssd-的内存优势能建模进加速比吗">6. SSD 的内存优势：能建模进加速比吗？</h2> <p>这个问题值得单独拎出来讨论，因为很多文章提到”SSD 省内存”但没有说清楚<strong>省出来的内存到底有没有加速价值</strong>。</p> <h3 id="61-省内存本身不直接加速单请求">6.1 省内存本身不直接加速单请求</h3> <p>先把这个坑踩清楚：在单请求场景下，SSD 省掉的 draft model 显存（大约 $r_\text{draft} \times M_\text{big}$，其中 $M_\text{big}$ 是大模型显存占用）对延迟<strong>没有直接帮助</strong>。省出来的显存闲着，加速比还是那个公式。</p> <h3 id="62-内存优势通过-batch-size-转化为-throughput-提升">6.2 内存优势通过 batch size 转化为 throughput 提升</h3> <p>真正的价值在 serving 场景，尤其是 <strong>KV cache 受限时</strong>。</p> <p>设 GPU 总显存为 $M_\text{total}$，模型权重占 $M_\text{weight}$，剩余显存全给 KV cache：</p> \[M_\text{kv} = M_\text{total} - M_\text{weight}\] <p>KV cache 的大小正比于 $B \times L_\text{seq} \times d_\text{kv} \times L$（batch size × 序列长度 × 头维度 × 层数），所以在相同硬件上能支撑的最大 batch size：</p> \[B_\text{max} \propto \frac{M_\text{kv}}{L_\text{seq} \cdot d_\text{kv} \cdot L}\] <p>对于 SD：需要额外维护 draft model 权重，$M_\text{weight}^\text{SD} = M_\text{big} + M_\text{draft}$</p> <p>对于 SSD：只有大模型一套权重，$M_\text{weight}^\text{SSD} = M_\text{big}$</p> <p>因此 SSD 能支撑的 batch size 更大：</p> \[\frac{B_\text{max}^\text{SSD}}{B_\text{max}^\text{SD}} = \frac{M_\text{total} - M_\text{big}}{M_\text{total} - M_\text{big} - M_\text{draft}}\] <p>这个比值在 $M_\text{draft}$ 较大时可以相当显著。举个例子：70B 大模型在 A100 80G 上量化后约占 35-40GB，如果 draft model 是 7B（约 3.5-4GB），释放这部分显存可以让 batch size 多出 ~10%。</p> <h3 id="63-throughput-建模">6.3 Throughput 建模</h3> <p>把 batch size 优势代入 throughput：</p> \[\text{Throughput} = \frac{B \times \mathbb{E}[\#\text{tokens per round}]}{T_\text{round}}\] <p>定义 $\beta = B_\text{max}^\text{SSD} / B_\text{max}^\text{SD} \geq 1$，SSD 相对 SD 的 throughput 比：</p> \[\frac{\text{Throughput}_\text{SSD}}{\text{Throughput}_\text{SD}} = \beta \cdot \frac{\text{Speedup}_\text{SSD}}{\text{Speedup}_\text{SD}}\] <p>当 $\beta &gt; 1$ 时（显存确实受限），SSD 的 throughput 优势 = 加速比的差距 + 显存带来的 batch 增益。</p> <p>但注意：<strong>batch 增大了，$\alpha$ 会下降</strong>（不同请求的分布不同，草稿的命中率降低），这是一个负反馈。实际 $\beta$ 的有效价值需要在特定负载下实测。</p> <h3 id="64-ssd-在企业部署中适用吗">6.4 SSD 在企业部署中适用吗？</h3> <p>直接给结论：<strong>适用场景很窄，但确实存在。</strong></p> <p><strong>SSD 相对 SD 有优势的场景</strong>：</p> <ol> <li><strong>显存极度受限</strong>：单机多卡塞不下大模型 + draft model，但 SSD 能省出 draft model 的权重开销，换来更大的 KV cache 空间或更高的并发</li> <li><strong>单请求低延迟优先</strong>（如代码补全、交互式对话）：此时 batch size = 1，SSD 的 KV 复用优势最大，$\beta = 1$，纯靠 Speedup 公式的分母优势</li> <li><strong>无合适 draft model</strong>：目标模型是定制 fine-tuned 版本，没有对应的小模型；SSD 不需要配对的 draft model，开箱即用</li> </ol> <p><strong>SSD 相对 SD 没有优势的场景</strong>：</p> <ol> <li><strong>大 batch 高吞吐 serving</strong>：$\alpha$ 随 batch 增大而下降，SSD 的 $r_\text{draft}$ 带来的接受率本就低于专用 draft model，大 batch 下差距更大</li> <li><strong>显存充裕</strong>：$\beta \approx 1$，内存优势消失，SSD 只剩 KV 复用这一点，而这要求 skip 模式是 contiguous prefix，限制了 $\alpha$ 的上限</li> <li><strong>对话历史长</strong>：长序列时 KV cache 本身就很大，verify 的 attention overhead $f(\gamma)$ 增大，SSD 节省的验证成本被其他 overhead 稀释</li> </ol> <p>一句话总结：<strong>SSD 是”没有好 draft model 时的替代方案”，而不是”有了 SD 之后还要叠加的优化”</strong>。企业部署如果有资源维护专用 draft model（比如 Llama-3-70B 配 Llama-3-8B），SD 的综合表现通常更好；反之，SSD 的零配置优势确实有价值。</p> <hr/> <h2 id="7-ssd-的-cuda-graph连续-skip-好做非连续-skip-怎么办">7. SSD 的 CUDA Graph：连续 skip 好做，非连续 skip 怎么办？</h2> <p>这一节纯讲工程，和上面的理论建模互补。如果你只关心公式推导可以跳过，但如果你要真的实现 SSD，这里是踩坑最密的地方。</p> <h3 id="71-cuda-graph-的本质约束">7.1 CUDA Graph 的本质约束</h3> <p>先说清楚 CUDA Graph（CG）是什么以及它为什么和 skip layer 有矛盾。</p> <p>CG 的工作方式：先”录制”一次完整的 kernel 执行序列（包括 kernel 类型、参数地址、依赖关系），生成一个固定的 DAG，之后每次推理直接 replay 这个 DAG，绕过 CPU 端的 kernel launch overhead。</p> <p>这带来了一个硬约束：<strong>CG 捕获的是拓扑固定的 kernel 图，不允许图在 replay 时动态增删节点。</strong></p> <p>skip layer 从计算图角度看就是”某些 transformer block 的 kernel 不执行”，一旦要运行时决定跳哪层，图的拓扑就在变——这和 CG 的设计正面冲突。</p> <h3 id="72-连续-skipcontiguous-prefix没有问题">7.2 连续 skip（contiguous prefix）：没有问题</h3> <p>对于原始 SSD（取前 $L_d$ 层做 draft），draft 路径和 verify 路径都是拓扑固定的：</p> <ul> <li>Draft 路径：执行层 $1 \to 2 \to \cdots \to L_d$，固定 $L_d$ 个 block</li> <li>Verify 路径：执行层 $1 \to 2 \to \cdots \to L$，固定 $L$ 个 block</li> </ul> <p>两条路径分别捕获成两个 CG，draft 阶段 replay draft-graph $\gamma$ 次，verify 阶段 replay verify-graph 1 次。整个生命周期里图的拓扑<strong>从未改变</strong>，CG 完全适用。</p> <p>这也是为什么 vLLM / SGLang 对标准投机解码的 CG 支持相对完善——两个模型各自一套图，没有特殊处理。</p> <h3 id="73-固定的非连续-skip同样没问题">7.3 固定的非连续 skip：同样没问题</h3> <p>如果 skip 模式是静态决定的（比如”永远跳第 4、8、12 层”），那虽然跳的不连续，但拓扑依然固定。捕获时只录制实际执行的 kernel，不录制被跳过的层，replay 就按这个固定子图走。</p> <p>唯一需要注意的是 <strong>residual connection 的处理</strong>：跳过第 $l$ 层时，第 $l+1$ 层的输入必须直接连到第 $l-1$ 层的输出（identity pass-through），这通常用一个 no-op copy kernel 或直接共享 tensor 指针来实现，不影响 CG 捕获。</p> <p>所以：<strong>连续 skip 和固定非连续 skip，CG 都没问题，本质上是同一类情况——图拓扑是静态的。</strong></p> <h3 id="74-动态-skip这才是真正的麻烦">7.4 动态 skip：这才是真正的麻烦</h3> <p>真正和 CG 冲突的是<strong>运行时自适应决定跳哪层</strong>，典型场景是基于 token confidence 的 early exit：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 伪代码：动态 early exit，每个 token 可能在不同层退出
</span><span class="k">for</span> <span class="n">layer_idx</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">L</span><span class="p">):</span>
    <span class="n">hidden</span> <span class="o">=</span> <span class="n">transformer_block</span><span class="p">[</span><span class="n">layer_idx</span><span class="p">](</span><span class="n">hidden</span><span class="p">)</span>
    <span class="k">if</span> <span class="nf">confidence</span><span class="p">(</span><span class="n">hidden</span><span class="p">)</span> <span class="o">&gt;</span> <span class="n">threshold</span><span class="p">:</span>  <span class="c1"># 运行时判断，拓扑在变！
</span>        <span class="k">break</span>  <span class="c1"># 提前退出
</span></code></pre></div></div> <p>每次前向退出的层数不同，图的拓扑每次都可能不一样，CG 无法直接捕获。</p> <h3 id="75-解法-1topology-preserving-no-op主流做法">7.5 解法 1：Topology-Preserving No-op（主流做法）</h3> <p>思路：<strong>不改变图的拓扑，改变数据流</strong>。</p> <p>把”跳过某层”改成”这层做一个 identity 操作”：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 改造后：拓扑固定，但通过 mask 控制层是否生效
</span><span class="k">for</span> <span class="n">layer_idx</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">L</span><span class="p">):</span>
    <span class="k">if</span> <span class="n">skip_mask</span><span class="p">[</span><span class="n">layer_idx</span><span class="p">]:</span>   <span class="c1"># skip_mask 是 GPU 上的 tensor，不是 Python bool
</span>        <span class="n">hidden</span> <span class="o">=</span> <span class="n">hidden</span>        <span class="c1"># identity，实现上是零乘或直接传递
</span>    <span class="k">else</span><span class="p">:</span>
        <span class="n">hidden</span> <span class="o">=</span> <span class="n">transformer_block</span><span class="p">[</span><span class="n">layer_idx</span><span class="p">](</span><span class="n">hidden</span><span class="p">)</span>
</code></pre></div></div> <p>关键是 <code class="language-plaintext highlighter-rouge">skip_mask</code> 必须是 <strong>GPU tensor</strong>，不能是 Python 控制流——Python <code class="language-plaintext highlighter-rouge">if</code> 会让 CG 捕获时走固定分支，replay 时永远走同一条路。</p> <p>具体实现方式有两种：</p> <p><strong>方式 A：乘以 0/1 mask（最简单但有额外计算）</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">output</span> <span class="o">=</span> <span class="n">transformer_block</span><span class="p">[</span><span class="n">layer_idx</span><span class="p">](</span><span class="n">hidden</span><span class="p">)</span>
<span class="n">hidden</span> <span class="o">=</span> <span class="n">hidden</span> <span class="o">+</span> <span class="n">skip_mask</span><span class="p">[</span><span class="n">layer_idx</span><span class="p">]</span> <span class="o">*</span> <span class="p">(</span><span class="n">output</span> <span class="o">-</span> <span class="n">hidden</span><span class="p">)</span>
<span class="c1"># skip_mask[i] = 0 → hidden 不变（跳过）
# skip_mask[i] = 1 → hidden = output（正常执行）
</span></code></pre></div></div> <p>坏处：即使跳过，transformer block 的 forward 还是跑了一遍，只是最后结果被 mask 掉，<strong>计算资源没有节省</strong>。</p> <p><strong>方式 B：cudaStreamWaitEvent + conditional copy（细粒度控制）</strong></p> <p>只 launch 需要执行的 kernel，但用 CUDA event 保持图的 DAG 结构完整，跳过的层用 memcpy no-op 保持依赖边。这种方式能真正节省被跳层的计算，但实现复杂，需要手动管理 event。</p> <p>vLLM 对类似场景（MoE 的 expert routing）的处理和方式 B 思路接近，但具体实现会根据 backend 有所不同。</p> <h3 id="76-解法-2multi-graph-预捕获适合枚举可控的场景">7.6 解法 2：Multi-Graph 预捕获（适合枚举可控的场景）</h3> <p>如果 skip 模式的数量有限（比如只有几种固定的 skip 配置），可以预先把所有可能的图都捕获好，运行时根据当前 skip 配置选对应的图 replay：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 离线预捕获
</span><span class="n">graphs</span> <span class="o">=</span> <span class="p">{}</span>
<span class="k">for</span> <span class="n">skip_config</span> <span class="ow">in</span> <span class="n">all_skip_configs</span><span class="p">:</span>
    <span class="n">graphs</span><span class="p">[</span><span class="n">skip_config</span><span class="p">]</span> <span class="o">=</span> <span class="nf">capture_cuda_graph</span><span class="p">(</span><span class="n">model</span><span class="p">,</span> <span class="n">skip_config</span><span class="p">)</span>

<span class="c1"># 在线 replay
</span><span class="n">current_config</span> <span class="o">=</span> <span class="nf">decide_skip_config</span><span class="p">(...)</span>  <span class="c1"># 可以是 CPU 逻辑
</span><span class="n">graphs</span><span class="p">[</span><span class="n">current_config</span><span class="p">].</span><span class="nf">replay</span><span class="p">()</span>
</code></pre></div></div> <p>这个方案的优势：每个图都只包含实际执行的 kernel，<strong>没有方式 A 的无效计算</strong>。</p> <p>限制：skip_config 的枚举空间不能太大。$L=32$ 层的模型如果每层都能独立跳，理论上有 $2^{32}$ 种配置，显然无法全部预捕获。但如果限制”只能在 4 个固定位置的层选一个退出”，配置数就是 4，完全可以。</p> <p>这也是为什么很多实际 SSD 实现会把 skip 模式约束成离散的几档（比如退出层只能是 $L/4, L/2, 3L/4, L$），既能做 CG，又给了 $r$ 一定的调节自由度。</p> <h3 id="77-解法-3persistent-cuda-graph--pointer-swap适合-verify-阶段的复用">7.7 解法 3：Persistent CUDA Graph + pointer swap（适合 verify 阶段的复用）</h3> <p>这个方法专门针对 SSD 的一个特殊需求：verify 阶段需要跑完整 $L$ 层，但前 $L_d$ 层的权重和 draft 阶段完全一样，能不能在 verify 时复用 draft 阶段的 CG 节点？</p> <p>答案是<strong>不能直接复用图节点</strong>，但可以通过 <strong>tensor pointer swap</strong> 实现 KV cache 的零拷贝共享：</p> <ol> <li>Draft CG 捕获时，KV cache 写入到一个预分配的 buffer（地址 $P_\text{kv}$）</li> <li>Verify CG 捕获时，前 $L_d$ 层的 KV cache 输入指针指向同一个 $P_\text{kv}$</li> <li>Replay 时：先 replay draft CG 写 KV，再 replay verify CG 读 KV——中间不需要任何数据搬运</li> </ol> <p><code class="language-plaintext highlighter-rouge">cudaGraphExecKernelNodeSetParams</code> 允许在不重新捕获的情况下更新 kernel 参数（包括指针），这是实现这个 trick 的 API 基础。vLLM 的 CG 实现里大量用到了这个 API 来在不同 batch size 下复用同一个图。</p> <h3 id="78-小结什么情况下-ssd-能用-cg">7.8 小结：什么情况下 SSD 能用 CG</h3> <table> <thead> <tr> <th>Skip 模式</th> <th>CG 可行性</th> <th>推荐方案</th> </tr> </thead> <tbody> <tr> <td>Contiguous prefix（固定 $L_d$）</td> <td>完全可行</td> <td>两张图分别捕获，verify 复用 KV</td> </tr> <tr> <td>固定非连续 skip</td> <td>完全可行</td> <td>单图捕获实际执行路径</td> </tr> <tr> <td>少量离散配置（如 4-8 档）</td> <td>可行，有开销</td> <td>Multi-graph 预捕获 + 运行时选图</td> </tr> <tr> <td>动态 early exit（基于 confidence）</td> <td>受限</td> <td>No-op mask（有冗余计算）或放弃 CG</td> </tr> <tr> <td>完全自由的运行时 skip</td> <td>不可行</td> <td>必须用 eager mode，接受 launch overhead</td> </tr> </tbody> </table> <p><strong>核心结论</strong>：SSD 想用 CG 的关键是<strong>提前固定 skip 模式</strong>。如果 skip 模式在离线阶段确定（无论连续还是非连续），CG 都没问题；如果要运行时动态决定，要么接受 no-op 的冗余计算，要么把配置空间限制到可预捕获的范围内。这也反过来约束了 SSD 里 skip 策略的设计空间——<strong>越灵活的 skip 策略，越难和 CG 兼容，工程上往往只能选有限档位的静态配置</strong>。</p> <h3 id="79-和-moe-的对比同是条件计算动态性的维度不一样">7.9 和 MoE 的对比：同是”条件计算”，动态性的维度不一样</h3> <p>这里值得单独展开，因为 MoE 模型同样有动态计算的问题，和 SSD skip 放在一起对比能把 CG 适配思路讲得更清楚。</p> <p><strong>MoE 是”宽度动态”，SSD skip 是”深度动态”</strong>：</p> <table> <thead> <tr> <th> </th> <th>MoE</th> <th>SSD dynamic skip</th> </tr> </thead> <tbody> <tr> <td>固定的是</td> <td>层数 $L$</td> <td>token 数</td> </tr> <tr> <td>变化的是</td> <td>每层激活哪些 expert、每个 expert 收多少 token</td> <td>执行哪些层</td> </tr> <tr> <td>动态性来源</td> <td>router 的 top-k 输出（数据依赖）</td> <td>confidence/early-exit 判断（数据依赖）</td> </tr> <tr> <td>图拓扑变化</td> <td>expert kernel 的 input size 在变，但 kernel 集合固定</td> <td>kernel 集合本身在变（某些层整个不执行）</td> </tr> </tbody> </table> <p>理解这个维度差异很重要，因为它决定了 CG 适配难度的本质差异。</p> <p><strong>MoE 的标准 CG 适配方案：padding to fixed shape</strong></p> <p>MoE 的动态在于每个 expert 收到的 token 数不固定。比如 8 个 expert、batch=64，理论上每个 expert 平均收 16 个 token，但实际可能是 5、30、0、21……直接按实际 token 数 launch kernel，shape 每次都变，CG 无法捕获。</p> <p>标准解法：<strong>把每个 expert 的输入 padding 到一个固定上限 $K_\text{max}$</strong>，保证所有 expert 的 kernel 每次都以相同 shape 执行。对应的 CG 拓扑完全固定，只需在 replay 前更新 token 分配的 mask tensor。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 伪代码：MoE CG 适配
# 捕获时：expert_inputs shape = [num_experts, K_max, hidden_dim]，固定
# replay 时：更新 routing_mask（GPU tensor），无需重新捕获
</span><span class="n">graph</span><span class="p">.</span><span class="nf">replay</span><span class="p">()</span>  <span class="c1"># kernel shape 不变，routing_mask 控制哪些 slot 有效
</span></code></pre></div></div> <p>padding 引入的开销：无效 token 的 expert forward 算了但不用（类似 SSD no-op mask）。但 MoE 的 router 通常做 load balancing，每个 expert 的负载比较均匀，padding 浪费的算力有限（通常 &lt; 5%）。vLLM 和 SGLang 对 MoE 的 CG 支持基本都走这条路。</p> <p><strong>SSD dynamic skip 的类比方案：layer no-op padding</strong></p> <p>用 MoE padding 的思路类比到 SSD：把”跳过某层”改成”这层做 no-op forward”，保持所有层的 kernel 都执行，用 GPU tensor mask 控制结果是否生效。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># SSD no-op mask 方案（类比 MoE padding）
</span><span class="k">for</span> <span class="n">layer_idx</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">L</span><span class="p">):</span>
    <span class="n">candidate</span> <span class="o">=</span> <span class="n">transformer_block</span><span class="p">[</span><span class="n">layer_idx</span><span class="p">](</span><span class="n">hidden</span><span class="p">)</span>
    <span class="n">hidden</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">where</span><span class="p">(</span><span class="n">skip_mask</span><span class="p">[</span><span class="n">layer_idx</span><span class="p">],</span> <span class="n">hidden</span><span class="p">,</span> <span class="n">candidate</span><span class="p">)</span>
    <span class="c1"># skip_mask 是 GPU tensor，replay 前更新，不重新捕获
</span></code></pre></div></div> <p>但这里有一个 MoE 没有的问题：<strong>MoE padding 浪费的是”部分 expert 的少量 slot”，而 SSD no-op 浪费的是”整个 transformer block 的 forward”</strong>。一个 transformer block 包含完整的 self-attention + FFN，相当于整层的计算全白做了。如果 SSD 设计的初衷是跳过 30-50% 的层（$r_\text{draft} = 0.5$-$0.7$），no-op 方案把跳层省下的计算全部还回去，<strong>加速效果清零</strong>。</p> <p>所以 SSD 的 no-op 方案只适合一种情况：<strong>你在乎的是吞吐而不是单请求延迟</strong>，需要 CG 带来的 launch overhead 节省（通常几十到几百 µs），而不在乎 GPU 算力的利用率。</p> <p><strong>MoE + SSD 叠加：双重动态</strong></p> <p>当大模型本身是 MoE 架构（比如 Mixtral、DeepSeek-MoE），然后在上面做 SSD，两种动态就叠在一起了：</p> <ul> <li>每层内部：expert routing 动态（宽度动态）</li> <li>跨层：skip 决策动态（深度动态）</li> </ul> <p>分别处理：</p> <ul> <li>宽度动态：每层内部继续用 MoE padding 方案，CG 可以捕获</li> <li>深度动态：如果 skip 是静态的（离线确定），直接从图里去掉被 skip 的层；如果是动态的，要么 no-op（但开销更大，因为被跳的是整个 MoE block），要么预捕获多档</li> </ul> <p>组合下来，<strong>MoE + 动态 SSD skip 的最实用方案</strong>通常是：</p> <ol> <li>把 SSD 的 skip 配置限制为 2-4 个静态档位（比如草稿用前 $L/4$、$L/2$、$3L/4$ 层三档）</li> <li>每档各预捕获一张 CG，每档内部用 MoE padding 处理 expert 的宽度动态</li> <li>档位切换在 CPU 端做（选哪张图 replay），不进入 CG 的捕获范围</li> </ol> <p>这样既保住了 CG 的 launch overhead 收益，又保住了 SSD skip 真正节省计算的优势（没有 no-op 冗余）。代价是 skip 策略的灵活性受限——但从上面第 4 节的分析可以知道，$r_\text{draft}$ 在几个离散档位之间搜索，本来也足够覆盖大部分的 Speedup 最优点。</p> <p><strong>一句话总结两者的差异</strong>：MoE 的动态是”图中每个节点的 input size 在变”，CG 用 padding 填平就行；SSD dynamic skip 的动态是”图中的节点集合在变”，padding 没法解决，只能要么固化节点集合（静态 skip），要么接受 no-op 的冗余计算，要么预捕获多图切换。工程取舍的核心永远是：<strong>动态性换来多少加速收益，值不值得为了 CG 兼容性付出对应的代价</strong>。</p> <h3 id="710-实际案例sparsespec-怎么做的">7.10 实际案例：SparseSpec 怎么做的</h3> <p>上面讨论的都是理论层面的方案，来看一个真实实现：<a href="https://github.com/sspec-project/SparseSpec">SparseSpec</a>，一个针对 RLM（推理大模型，即做 CoT 推理的那类）的批量推理加速框架，主打”稀疏自推测解码”（Sparse Self-Speculative Decoding），声称最高 2.3x 吞吐量提升。</p> <h4 id="sparsespec-的核心思路动态不是层的-skip而是-attention-的-kv-范围">SparseSpec 的核心思路：动态不是层的 skip，而是 attention 的 KV 范围</h4> <p>SparseSpec 的 SSD 变体和传统 skip layer 有一个本质区别：<strong>它 skip 的不是 transformer block，而是 attention 的 KV 访问范围</strong>。</p> <p>具体做法叫 PillarAttn：</p> <ol> <li><strong>验证阶段</strong>：跑完整 attention，同时把每个 token 的 attention score 转储到全局内存（<code class="language-plaintext highlighter-rouge">dump_logits</code>）</li> <li><strong>Top-K 筛选</strong>：从这批 attention score 里找出对当前 query 贡献最大的 Top-K 个 KV 位置，作为下一轮草稿的”重要 token”集合</li> <li><strong>草稿阶段</strong>：不访问完整 KV cache，只访问这 Top-K 个稀疏位置</li> </ol> <p>这样 skip 的是”大量不重要的 KV”，而不是”整个 transformer 层”。skip 的粒度细得多，也不会破坏激活路径的一致性（每层都还是完整执行的）。</p> <h4 id="cuda-graph-适配同一张图处理三种请求类型">CUDA Graph 适配：同一张图处理三种请求类型</h4> <p>SparseSpec 的 CG 方案是本文讨论思路里最干净的实现之一——<strong>不改变图的拓扑，把所有动态性压进 GPU tensor，通过 <code class="language-plaintext highlighter-rouge">request_type</code> 在一张图里同时处理 NORMAL / DRAFT / VERIFY 三种请求</strong>。</p> <p>核心在 attention kernel 的 InputTransform（<code class="language-plaintext highlighter-rouge">serve/attention/backend.py</code>）：</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// CUDA JIT attention kernel 内部</span>
<span class="k">if</span> <span class="p">(</span><span class="n">request_type</span> <span class="o">==</span> <span class="mi">1</span><span class="p">)</span> <span class="p">{</span>   <span class="c1">// DRAFT：用稀疏索引</span>
    <span class="n">indices</span> <span class="o">=</span> <span class="n">params</span><span class="p">.</span><span class="n">flatten_indices</span> <span class="o">+</span> <span class="n">kv_head_idx</span> <span class="o">*</span> <span class="n">MAX_TOTAL_DRAFT_KV_LEN</span><span class="p">;</span>
<span class="p">}</span> <span class="k">else</span> <span class="p">{</span>                   <span class="c1">// NORMAL / VERIFY：用完整索引</span>
    <span class="n">indices</span> <span class="o">=</span> <span class="n">params</span><span class="p">.</span><span class="n">kv_indices</span><span class="p">;</span>
<span class="p">}</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">request_type</code> 是一个 GPU tensor，每次 replay 前按实际请求更新，图拓扑本身不动。同一张图，草稿请求走稀疏 KV 路径，验证请求走完整 KV 路径，混在一个 batch 里一起跑。</p> <p>验证阶段的 attention score 转储也类似（<code class="language-plaintext highlighter-rouge">LogitsTransform</code>）：</p> <div class="language-cpp highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="p">(</span><span class="n">request_type</span> <span class="o">==</span> <span class="mi">2</span><span class="p">)</span> <span class="p">{</span>   <span class="c1">// VERIFY 才写分数</span>
    <span class="n">params</span><span class="p">.</span><span class="n">dump_logits</span><span class="p">[</span><span class="n">offset</span> <span class="o">+</span> <span class="n">kv_idx</span><span class="p">]</span> <span class="o">=</span> <span class="n">__float2half</span><span class="p">(</span><span class="n">logits</span> <span class="o">/</span> <span class="n">packed_qo_len</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <p>只有 VERIFY 请求的 kernel 执行会写入 <code class="language-plaintext highlighter-rouge">dump_logits</code>，DRAFT 请求执行时写入目标不同，结果被自然隔离。</p> <h4 id="双缓冲-cg消除-cpu-准备和-gpu-执行之间的同步气泡">双缓冲 CG：消除 CPU 准备和 GPU 执行之间的同步气泡</h4> <p>SparseSpec 的另一个工程细节值得记一下：<strong>双缓冲 CUDA Graph</strong>（<code class="language-plaintext highlighter-rouge">serve/model/model.py</code> 的 <code class="language-plaintext highlighter-rouge">KVPool</code> 类）。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">KVPool</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="p">...):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">phase_idx</span> <span class="o">=</span> <span class="mi">0</span>
        <span class="n">self</span><span class="p">.</span><span class="n">_wrappers</span> <span class="o">=</span> <span class="p">[]</span>           <span class="c1"># 两份 wrapper
</span>        <span class="n">self</span><span class="p">.</span><span class="n">_attn_fwd_metadata</span> <span class="o">=</span> <span class="p">[]</span>  <span class="c1"># 两份 metadata
</span>    
    <span class="k">def</span> <span class="nf">step</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">phase_idx</span> <span class="o">^=</span> <span class="mi">1</span>           <span class="c1"># 0 ↔ 1 交替
</span></code></pre></div></div> <p>标准 CG 的流程是：CPU 准备元数据 → GPU replay 图 → 等 GPU 完成 → CPU 准备下一批 → …，中间有等待的 bubble。双缓冲的做法：两张预捕获的图交替使用，第 0 张在 GPU 上 replay 时，CPU 已经在准备第 1 张图的输入参数，GPU 执行完第 0 张立刻 replay 第 1 张，不需要等待。</p> <p>捕获时也预捕获了一系列 batch size 的图（余弦分桶：1, 2, 4, 8, 16, 32, 64, 128, 256…），每个 batch size 下捕获两张（双缓冲各一张）：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">eager_cuda_graph_mode</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">max_batch_size</span><span class="o">=</span><span class="mi">256</span><span class="p">,</span> <span class="n">num_cuda_graphs</span><span class="o">=</span><span class="mi">16</span><span class="p">):</span>
    <span class="n">captured_args</span> <span class="o">=</span> <span class="p">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">4</span><span class="p">,</span> <span class="mi">8</span><span class="p">,</span> <span class="mi">16</span><span class="p">,</span> <span class="mi">32</span><span class="p">,</span> <span class="mi">64</span><span class="p">,</span> <span class="mi">128</span><span class="p">,</span> <span class="mi">256</span><span class="p">,</span> <span class="mi">384</span><span class="p">,</span> <span class="mi">512</span><span class="p">,</span> <span class="mi">640</span><span class="p">]</span>
    <span class="k">for</span> <span class="n">batch_size</span> <span class="ow">in</span> <span class="n">captured_args</span><span class="p">:</span>
        <span class="k">for</span> <span class="n">phase</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">2</span><span class="p">):</span>      <span class="c1"># 双缓冲
</span>            <span class="n">runner</span> <span class="o">=</span> <span class="nf">_CUDAGraphModelRunner</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">model</span><span class="p">)</span>
            <span class="n">runner</span><span class="p">.</span><span class="nf">capture</span><span class="p">(</span><span class="n">batch_size</span><span class="o">=</span><span class="n">batch_size</span><span class="p">,</span> <span class="p">...)</span>
            <span class="n">self</span><span class="p">.</span><span class="n">graph_runners</span><span class="p">[</span><span class="n">batch_size</span><span class="p">].</span><span class="nf">append</span><span class="p">(</span><span class="n">runner</span><span class="p">)</span>
</code></pre></div></div> <p>实际 forward 时，按当前 batch size 选最近的已捕获档位（允许 padding 到上一档），再按当前 <code class="language-plaintext highlighter-rouge">phase</code> 选双缓冲里的哪一张：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="o">**</span><span class="n">kwargs</span><span class="p">):</span>
    <span class="n">batch_size</span> <span class="o">=</span> <span class="n">kwargs</span><span class="p">[</span><span class="sh">"</span><span class="s">token_ids</span><span class="sh">"</span><span class="p">].</span><span class="nf">size</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>
    <span class="n">idx</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">searchsorted</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">captured_bsz</span><span class="p">,</span> <span class="n">batch_size</span><span class="p">,</span> <span class="n">side</span><span class="o">=</span><span class="sh">"</span><span class="s">left</span><span class="sh">"</span><span class="p">)</span>
    <span class="c1"># 在 padding 比例允许范围内走 CG，否则 eager fallback
</span>    <span class="n">model_executor</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">graph_runners</span><span class="p">[</span><span class="n">padded_bsz</span><span class="p">][</span><span class="n">self</span><span class="p">.</span><span class="n">kv_cache</span><span class="p">.</span><span class="n">phase</span><span class="p">]</span>
    <span class="k">return</span> <span class="nf">model_executor</span><span class="p">(</span><span class="o">**</span><span class="n">kwargs</span><span class="p">)</span>
</code></pre></div></div> <h4 id="sparsespec-和本文讨论方案的对应关系">SparseSpec 和本文讨论方案的对应关系</h4> <p>回头看 7.5-7.8 节讨论的几个方案，SparseSpec 实际走的是<strong>方案 1（topology-preserving）的精细版</strong>：</p> <ul> <li><strong>不是 layer-level 的 no-op</strong>，而是 <strong>attention KV-range 的条件访问</strong>——代价比整层 no-op 小得多，被跳过的只是 attention 里不重要的 KV 读取，transformer block 本身的 MLP、归一化等还是完整执行</li> <li><strong>同一张图支持三种请求类型</strong>，不需要 multi-graph 切换，靠 <code class="language-plaintext highlighter-rouge">request_type</code> tensor 在运行时路由</li> <li><strong>双缓冲消除同步气泡</strong>，这是在 7.7 节 pointer swap 思路之外的另一个工程优化维度</li> </ul> <p>这个设计也解释了为什么 SparseSpec 不需要显式处理 skip layer 的 KV 复用问题（第 4.2 节）：它的稀疏性在 attention 的 KV 索引层面而非 transformer 层层面，$r_\text{reuse}$ 的分析框架对它并不直接适用——它的”跳过”不改变激活路径，所有层的 hidden state 仍然是 full-path 计算出来的。</p> <table> <thead> <tr> <th> </th> <th>本文理论方案</th> <th>SparseSpec 实现</th> </tr> </thead> <tbody> <tr> <td>动态性来源</td> <td>transformer layer 级别的 skip</td> <td>attention KV range 的稀疏访问</td> </tr> <tr> <td>CG 适配思路</td> <td>no-op mask / multi-graph</td> <td>单图 + <code class="language-plaintext highlighter-rouge">request_type</code> tensor 路由</td> </tr> <tr> <td>冗余计算</td> <td>整层 no-op（代价大）</td> <td>仅 attention 的冗余 KV 访问（代价小）</td> </tr> <tr> <td>KV 复用</td> <td>依赖 contiguous prefix</td> <td>不适用（层不 skip，复用问题不存在）</td> </tr> <tr> <td>batch 内混合</td> <td>需要多图切换</td> <td>天然支持（三种类型共享一张图）</td> </tr> </tbody> </table> <hr/> <h2 id="8-最优草稿长度的-closed-form-近似">8. 最优草稿长度的 closed-form 近似</h2> <p>对于标准 SD 公式，对 $\gamma$ 求导：</p> \[\frac{\partial}{\partial \gamma}\text{Speedup} = \frac{(1-\alpha)[-\alpha^{\gamma+1}\ln\alpha(\gamma c + 1) - c(1-\alpha^{\gamma+1})]}{(1-\alpha)^2(\gamma c+1)^2}\] <p>令分子中括号 = 0：</p> \[-\alpha^{\gamma+1}\ln\alpha(\gamma c + 1) = c(1-\alpha^{\gamma+1})\] <p>这个方程没有解析解。但若 $\alpha^{\gamma+1} \ll 1$（即 $\gamma$ 足够大使接受率衰减显著）时，右边 $\approx c$，左边变成 $-\alpha^{\gamma+1}\ln\alpha \cdot \gamma c$（近似 $\gamma^* c \gg 1$），化简得：</p> \[\alpha^{\gamma+1} \approx \frac{-1}{\gamma^* \ln\alpha}\] <table> <tbody> <tr> <td>因为 $\ln\alpha &lt; 0$（$\alpha &lt; 1$），令 $</td> <td>\ln\alpha</td> <td>= -\ln\alpha$：</td> </tr> </tbody> </table> \[\gamma^* \approx \frac{1}{|\ln\alpha|} \cdot \frac{1}{\alpha^{\gamma^*+1}}\] <table> <tbody> <tr> <td>这是个隐式方程，但可以用一阶近似：令右边的 $\gamma^*$ 用初始猜测 $\gamma_0 = \frac{1}{</td> <td>\ln\alpha</td> <td>}$ 代入，迭代一次：</td> </tr> </tbody> </table> \[\gamma^* \approx \frac{1}{|\ln\alpha|} \cdot e^{(\gamma_0+1)|\ln\alpha|} = \frac{e^{(\frac{1}{|\ln\alpha|}+1)|\ln\alpha|}}{|\ln\alpha|}\] <p>这个近似在 $\alpha$ 接近 1 时比较准，但说实话不够优雅。工程上直接 grid search 更实用。</p> <hr/> <h2 id="8-数值校验两个极端场景">8. 数值校验：两个极端场景</h2> <h3 id="场景-ac--005-alpha--09-gamma--8">场景 A：$c = 0.05, \alpha = 0.9, \gamma = 8$</h3> \[\text{Speedup} = \frac{1 - 0.9^9}{(1-0.9)(8 \times 0.05 + 1)} = \frac{1 - 0.387}{0.1 \times 1.4} = \frac{0.613}{0.14} \approx 4.4\] <p>和图里 $\alpha=0.90, \gamma=8$ 那条线的值（约 4.5）吻合，没问题。</p> <h3 id="场景-bc--00005-alpha--095-gamma--64">场景 B：$c = 0.0005, \alpha = 0.95, \gamma = 64$</h3> \[\text{Speedup} = \frac{1 - 0.95^{65}}{(1-0.95)(64 \times 0.0005 + 1)} = \frac{1 - 0.0356}{0.05 \times 1.032} = \frac{0.964}{0.0516} \approx 18.7\] <p>图里显示约 16-17，稍有偏差，说明在 $\gamma = 64$ 时验证 overhead $f(\gamma) &gt; 1$ 的影响已经不可忽略了，实际比理想值低一点，这是预期内的。</p> <hr/> <h2 id="9-一张对比表总结">9. 一张对比表总结</h2> <table> <thead> <tr> <th>因素</th> <th>SD</th> <th>SSD-Contiguous</th> <th>SSD-Skip</th> <th> </th> <th> </th> </tr> </thead> <tbody> <tr> <td>草稿成本比</td> <td>$c = t_\text{draft}/t_\text{big}$</td> <td>$r_\text{draft} = L_d/L$</td> <td>$r_\text{draft} =</td> <td>\mathcal{A}</td> <td>/L$</td> </tr> <tr> <td>KV 可复用比例</td> <td>0（两套模型）</td> <td>$r_\text{reuse} = r_\text{draft}$</td> <td>$r_\text{reuse} = (s_1-1)/L \leq r_\text{draft}$</td> <td> </td> <td> </td> </tr> <tr> <td>Speedup 分母</td> <td>$\gamma c + 1$</td> <td>$1 + (\gamma-1)r$</td> <td>$\gamma r_\text{draft} + (1-r_\text{reuse})$</td> <td> </td> <td> </td> </tr> <tr> <td>接受率 $\alpha$</td> <td>依赖 draft model 质量</td> <td>依赖前缀层能力，偏低</td> <td>依赖 skip 模式，更低</td> <td> </td> <td> </td> </tr> <tr> <td>内存占用</td> <td>两套权重</td> <td>一套权重</td> <td>一套权重</td> <td> </td> <td> </td> </tr> <tr> <td>Batch 并发优势</td> <td>无（内存较高）</td> <td>有（显存受限时）</td> <td>有（显存受限时）</td> <td> </td> <td> </td> </tr> <tr> <td>企业部署适用性</td> <td>有专用 draft model 时最优</td> <td>显存受限 / 无 draft model</td> <td>通常不如 Contiguous</td> <td> </td> <td> </td> </tr> </tbody> </table> <hr/> <h2 id="10-结语">10. 结语</h2> <p>把所有 closed form 并排放一起：</p> \[\text{Speedup}_\text{SD} = \frac{1 - \alpha^{\gamma+1}}{(1-\alpha)(\gamma c + 1)}\] \[\text{Speedup}_\text{SSD-contiguous} = \frac{1 - \alpha^{\gamma+1}}{(1-\alpha)[1 + (\gamma-1)r]}\] \[\text{Speedup}_\text{SSD-general} = \frac{1 - \alpha^{\gamma+1}}{(1-\alpha)[\gamma r_\text{draft} + (1 - r_\text{reuse})]}\] \[\text{Throughput}_\text{SSD} = \beta \cdot \text{Speedup}_\text{SSD} \cdot \text{Throughput}_\text{baseline}, \quad \beta = \frac{M_\text{total} - M_\text{big}}{M_\text{total} - M_\text{big} - M_\text{draft}}\] <p>这几个公式说清楚了几件事：</p> <p><strong>第一，KV 复用的前提是激活路径一致</strong>。非连续 skip 一旦让 $r_\text{reuse} \ll r_\text{draft}$，SSD 的 KV 复用优势彻底消失，退化成和 SD 等价的形式。所以很多 skip-layer SSD 的实现里，”KV cache 可以复用”这个说法要打问号，得看 skip 的是哪些层。</p> <p><strong>第二，内存优势不是”直接加速”，是”换 batch size”</strong>。SSD 省出来的 draft model 显存，在显存受限时能换成更高的并发量，通过 $\beta &gt; 1$ 提升 throughput。但 $\alpha$ 会随 batch size 增大而下降，这个收益不是线性的，需要实测。</p> <p><strong>第三，SSD 是”没有 draft model 时的 fallback”，不是”叠在 SD 上的加分项”</strong>。如果资源允许维护专用 draft model，SD + 好 draft model 的 $\alpha$ 优势几乎必然超过 SSD 的 KV 复用优势。SSD 真正发光的场景是：显存极度受限、或者目标模型是自定义 fine-tune 版本、找不到合适的 draft model。</p> <p>真正有意思的后续问题是：SSD 的 skip 层怎么搜？随机跳、等间隔跳、还是用 NAS 方法学出来的 skip 模式？不同 skip 模式下 $r_\text{draft}$ 和 $r_\text{reuse}$ 的差距有多大？这些直接影响 SSD 的实际加速，感觉可以专门写一篇。欢迎评论区交流。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 LLM 推理效率和加速方法你想系统深入，可以看看我之前出版的<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，书里有专章讲 LLM 推理效率优化，和本文讨论的理论框架有直接关联。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="推理优化"/><category term="投机解码"/><category term="理论分析"/><summary type="html"><![CDATA[投机解码到底能快多少？从理论建模到 closed-form 的完整推导]]></summary></entry><entry><title type="html">推理模型做 decode，GPU 只有 3% 在干活——SparseSpec 把稀疏 Attention 变成了 2.1× 加速</title><link href="https://marsggbo.github.io/blog/2026/20260707-vllm-v1/" rel="alternate" type="text/html" title="推理模型做 decode，GPU 只有 3% 在干活——SparseSpec 把稀疏 Attention 变成了 2.1× 加速"/><published>2026-07-07T00:00:00+00:00</published><updated>2026-07-07T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260707-vllm-v1</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260707-vllm-v1/"><![CDATA[<h1 id="推理模型做-decodegpu-只有-3-在干活sparsespec-把稀疏-attention-变成了-21-加速">推理模型做 decode，GPU 只有 3% 在干活——SparseSpec 把稀疏 Attention 变成了 2.1× 加速</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2512.01278">Accelerating Large-Scale Reasoning Model Inference with Sparse Self-Speculative Decoding</a></p> </blockquote> <hr/> <p>一张 H100，跑 Qwen3-8B 做推理解码，attention 阶段的 GPU <strong>计算利用率只有 3%</strong>。</p> <p>内存带宽跑满了 85%，计算单元闲着 97%。</p> <p><img src="/assets/img/posts/20260707-vllm-v1/002_Figure_2.png" alt="Qwen3-8B decode 阶段的计算和内存带宽利用率"/></p> <p>这张图是整篇论文的核心前提。在这个基础上，才能理解为什么”减少 FLOPs”这个直觉在推理模型场景下完全打不中要害，也才能理解 SparseSpec 的设计逻辑。</p> <hr/> <h2 id="1-推理模型和普通-llm-的-decode-不是一回事">1. 推理模型和普通 LLM 的 decode 不是一回事</h2> <p>普通 LLM 生成一段对话，200 个 token，KV cache 很小，attention 很快。推理模型不一样——DeepSeek-R1、Qwen3 这类模型，做一道数学题要先想几千步，CoT 动辄 3000-8000 个 token，KV cache 巨大。</p> <p>KV cache 越大，每个 decode step 要搬运的数据就越多。而 GPU 的计算峰值（FLOPs）和内存带宽（GB/s）是两个独立的天花板——一旦 decode 落入 memory-bandwidth bound 的区域，堆计算完全没用，瓶颈在搬数据的速度。</p> <p>这就是上面那张图的意思：97% 的计算白白浪费了，GPU 的时间全花在等 KV cache 从显存搬过来。</p> <p>现有的加速方案有两大类：一是 KV cache 压缩（量化、逐出、稀疏 attention），二是推测解码（用小模型快速生成草稿，大模型并行验证）。这篇论文选了第二条路，但做法有本质区别。</p> <hr/> <h2 id="2-推理模型的-attention-天然是稀疏的">2. 推理模型的 Attention 天然是稀疏的</h2> <p>做稀疏 attention 的前提是：大部分 attention score 接近零，只有少数”关键 token”真正重要。这个直觉在推理模型上被验证得特别彻底。</p> <p><img src="/assets/img/posts/20260707-vllm-v1/004_Figure_4.png" alt="Qwen3-8B 在 AIME 数学推理任务上的 attention score 热力图"/></p> <p>Qwen3-8B 在 AIME 数学题上的 attention 热力图——矩阵的绝大部分是接近零的蓝色，只有极少数位置（红色）有高分值。两个不同的 head 都是这个模式。</p> <p>问题是：稀疏 attention 和推测解码怎么结合？</p> <hr/> <h2 id="3-之前的方案为什么没做到理论加速上限">3. 之前的方案为什么没做到理论加速上限</h2> <p>先看一个有意思的对比图：</p> <p><img src="/assets/img/posts/20260707-vllm-v1/003_Figure_3.png" alt="理论加速上限 vs. 现有方法实际加速"/></p> <p>横轴是 acceptance rate，纵轴是 speedup。蓝线是理论上限——如果 acceptance rate 是 0.6，理论上应该能拿到接近 2.0× 的加速。</p> <p>但实际上：</p> <ul> <li><strong>MagicDec</strong>（稀疏 attention + 标准推测解码）：acceptance rate 0.6，只拿到 <strong>1.47×</strong>，比理论低了 36%</li> <li><strong>Oracle Top-K Attn</strong>（用 oracle 完美稀疏 attention）：acceptance rate 0.88，也只有 <strong>2.39×</strong>，低于理论峰值</li> </ul> <p>差距从哪来？<strong>批处理</strong>。</p> <p>标准推测解码为单请求设计：草稿阶段串行生成 d 个 token，验证阶段一次 batch 前向。单请求效率很高。但在实际推理服务里，同时有几十上百个请求，草稿和验证不得不交替执行——草稿阶段只处理部分请求，GPU 利用率低；验证阶段又切换回来。两边都低效，加速折扣大。</p> <p>SparseSpec 要解的核心问题就是：<strong>在大 batch 在线服务场景下，让稀疏 self-speculative decoding 做到接近理论的加速比</strong>。</p> <hr/> <h2 id="4-sparsespec-的三个设计">4. SparseSpec 的三个设计</h2> <h3 id="41-pillarattn用验证阶段的结果指导草稿阶段的稀疏-pattern">4.1 PillarAttn：用验证阶段的结果指导草稿阶段的稀疏 pattern</h3> <p>Self-speculative decoding 的 draft 阶段和 verify 阶段用同一套参数——draft 跑稀疏 attention，verify 跑完整 attention。但草稿阶段该忽略哪些 token？</p> <p>SparseSpec 的做法是：<strong>在验证阶段识别出关键 token，在下一轮草稿阶段只 attend 这些 token</strong>。</p> <p><img src="/assets/img/posts/20260707-vllm-v1/007_Figure_7.png" alt="PillarAttention 的工作原理"/></p> <p>每行是一个 decode step，红色格子是被选出的关键 token（critical tokens），蓝色是 attention 权重高的位置，灰色是跳过的位置。验证阶段（底部高亮框）做完整 attention，同时记录哪些 token 的 attention score 高；下一轮草稿阶段（Drafting 框）就只看这些关键 token，跳过其余大部分。</p> <p>名字叫 “PillarAttn” 是因为关键 token 在 attention 矩阵里形成了一列列”柱子”的形状。</p> <h3 id="42-unified-batching草稿和验证混进同一个-batch">4.2 Unified Batching：草稿和验证混进同一个 batch</h3> <p>解决批处理效率问题的关键设计。</p> <p>传统做法是”所有请求先全部草稿，再全部验证”，这样草稿和验证交替执行，GPU 在切换时利用率骤降。SparseSpec 把草稿和验证请求混进同一个 batch，同时处理：</p> <p><img src="/assets/img/posts/20260707-vllm-v1/006_Figure_6.png" alt="不同请求在同一 batch 里混合执行草稿和验证"/></p> <p>Unified Batch Scheduler 负责分配每个请求在这一轮做草稿还是验证，然后整批一起推理。对模型来说就是一次正常的 batch forward——有些请求用稀疏 attention（draft），有些用完整 attention（verify），结构统一。</p> <p>附带的一个收益是 GEMM 效率。推测解码的一个经典问题是：草稿阶段 batch size 小，GEMM 算子远未打满 GPU——下图左边是 Naive 方案的 GEMM batch size，剧烈抖动；右边是 Unified Batching 之后，batch size 稳定在 GPU 饱和点附近。</p> <p><img src="/assets/img/posts/20260707-vllm-v1/014_Figure_14.png" alt="Naive vs. Unified Batching 的 GEMM batch size 对比"/></p> <h3 id="43-异步-cpu-gpu-执行把验证的-cpu-开销藏起来">4.3 异步 CPU-GPU 执行：把验证的 CPU 开销藏起来</h3> <p>Self-speculative decoding 的验证阶段有一步 CPU 计算：对比 draft token 和 verify token 的概率分布，决定接受还是拒绝。在同步执行模式下，这步 CPU 计算会卡住 GPU：</p> <p><img src="/assets/img/posts/20260707-vllm-v1/009_Figure_9.png" alt="同步 vs. 异步 CPU-GPU 执行时间线"/></p> <p>(a) 同步模式：每轮验证完要等 CPU 跑完接受/拒绝逻辑，GPU 空转。(b) 异步模式：把 CPU 计算延迟一步，和下一轮 GPU 执行重叠，GPU 不再空转。</p> <p>代价是”延迟验证”——接受/拒绝的结果晚一步才能用，相当于有一轮 draft 可能白跑。但实验数据表明这个代价远小于消除 GPU idle 的收益。</p> <hr/> <h2 id="5-数字说话">5. 数字说话</h2> <h3 id="端到端吞吐量">端到端吞吐量</h3> <p>在 Qwen3 系列（1.7B/8B/14B）的三个推理 benchmark（AIME、LiveCodeBench、OlympiadBench）上对比 vLLM、vLLM-ngram、MagicDec、TriForce：</p> <p><img src="/assets/img/posts/20260707-vllm-v1/010_Figure_10.png" alt="端到端吞吐量对比（vLLM/ngram/MagicDec/TriForce vs. SparseSpec）"/></p> <p>SparseSpec 全面领先，对 vLLM baseline 的加速比在不同配置下：</p> <ul> <li>Qwen3-1.7B AIME：<strong>2.13×</strong></li> <li>Qwen3-8B AIME：<strong>1.79×</strong></li> <li>Qwen3-14B AIME：<strong>1.51×</strong>（模型越大，TP degree 越高，通信 overhead 越重，加速有所压缩）</li> </ul> <h3 id="不需要额外-draft-模型还能超过-eagle-3">不需要额外 draft 模型，还能超过 EAGLE-3</h3> <p>EAGLE-3 是目前最强的 draft-model 推测解码方案之一，需要专门 fine-tune 一个草稿模型，参数量通常是目标模型的 1/7 左右，还要占额外显存。</p> <p>SparseSpec 用同一套模型参数，不需要 fine-tune 任何额外模型：</p> <p><img src="/assets/img/posts/20260707-vllm-v1/011_Figure_11.png" alt="SparseSpec vs. EAGLE-3 吞吐量对比"/></p> <p>三个模型上，SparseSpec 都略高于或持平 EAGLE-3，最大优势在 Qwen3-1.7B（4632 vs 4043 tokens/s）。</p> <p>为什么同一个模型的稀疏版本能接受这么多 token？对比接受 token 数：</p> <p><img src="/assets/img/posts/20260707-vllm-v1/012_Figure_12.png" alt="各方法每轮接受 token 数对比"/></p> <p>Eagle-3 = 1.1，N-gram = 1.6，StreamingLLM = 4.8，<strong>SparseSpec = 6.1</strong>。</p> <p>原因在于：稀疏 attention 保留了完整的模型语义，draft 分布和 verify 分布非常接近，接受率自然高。相比之下，独立 draft 模型在长 CoT 场景下分布偏差更大，在 batched setting 里接受率反而低。</p> <h3 id="消融实验每个设计贡献多少">消融实验：每个设计贡献多少</h3> <p><img src="/assets/img/posts/20260707-vllm-v1/013_Figure_13.png" alt="各组件贡献的消融实验"/></p> <p>从 Naive Spec 到最终 SparseSpec：</p> <ul> <li>+Unified Batch：1.23×</li> <li>+KV Cache Offload：1.61×（内存管理优化，让更多请求能同时服务）</li> <li>+Async CPU（最终方案）：4722 tokens/s，<strong>2.21×</strong> over Naive Spec</li> </ul> <p>每个设计都有实质贡献，不是凑数的。</p> <hr/> <h2 id="6-一点个人-take">6. 一点个人 take</h2> <p>这篇论文让我觉得有意思的地方在于它的出发点——<strong>从系统瓶颈倒推算法设计</strong>，而不是从算法的优雅性出发。GPU 利用率只有 3% 这个观察是可测量的，”所以要减少 attention 的数据搬运”是直接推出来的结论，不是先有稀疏 attention 再找理由。</p> <p>PillarAttn 的设计也体现了这个思路：不是离线算好一个静态的稀疏 pattern（那需要 profiling），而是用 verify 阶段已经算过的 attention score 来动态指导下一轮 draft——信息复用，零额外开销。</p> <p>有几个局限值得说：</p> <p><strong>一是依赖 attention 天然稀疏</strong>。推理模型（Qwen3、DeepSeek-R1 这类）的长 CoT 场景，这个假设成立很好。但在短输出场景（比如简单对话、分类任务），attention 未必足够稀疏，稀疏 draft 的接受率可能大幅下降，整套机制就没那么有吸引力了。</p> <p><strong>二是系统工程复杂度</strong>。Unified Batching + Async CPU + Dynamic KV Cache 三个模块组合，和标准 vLLM 架构差异很大，维护成本不低。论文是在自己的框架上实现的，想集成进 vLLM 主线还有不少工作量。</p> <p><strong>三是加速比随模型变大而缩小</strong>（从 2.13× 降到 1.51×）。原因是大模型需要更多 GPU 做 tensor parallel，TP 通信本身就成了新瓶颈，稀疏 attention 省下来的时间被通信抵消了一部分。这是 TP + speculative decoding 的通用矛盾，不是 SparseSpec 特有的问题，但确实限制了在超大模型上的收益空间。</p> <p>推理模型是 2025 年之后 LLM 推理优化最有意思的方向之一——长 CoT 的特殊性质（稀疏 attention、memory-bandwidth bound、对延迟不那么敏感）和普通对话模型差异很大，很多老结论都要重新验证。这篇论文把这个特殊性利用得比较彻底。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 LLM 推理效率优化和稀疏推理你想系统深入，可以看看我之前出版的<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，书里有专章讲 LLM 推理效率（量化、蒸馏、推理框架），和本文的工程优化思路有直接关联。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="推理加速"/><category term="推测解码"/><category term="Self-Speculative Decoding"/><category term="稀疏 Attention"/><category term="论文解读"/><summary type="html"><![CDATA[推理模型做 decode，GPU 只有 3% 在干活——SparseSpec 把稀疏 Attention 变成了 2.1× 加速]]></summary></entry><entry><title type="html">160 个 AI 比 80 个效果更差？这篇论文量化了一件大家隐约知道但说不清楚的事</title><link href="https://marsggbo.github.io/blog/2026/20260706-llm-position-paper/" rel="alternate" type="text/html" title="160 个 AI 比 80 个效果更差？这篇论文量化了一件大家隐约知道但说不清楚的事"/><published>2026-07-06T00:00:00+00:00</published><updated>2026-07-06T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260706-llm-position-paper</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260706-llm-position-paper/"><![CDATA[<h1 id="160-个-ai-比-80-个效果更差这篇论文量化了一件大家隐约知道但说不清楚的事">160 个 AI 比 80 个效果更差？这篇论文量化了一件大家隐约知道但说不清楚的事</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2605.19915">LLM Agents Make Collective Belief Dynamics Programmable: Challenges and Research Directions</a> Javier Kvitko, Petter Törnberg et al.（2025）</p> </blockquote> <hr/> <p>“AI 会影响舆论”——这句话大家都会说，但说完也就说完了，说不出更多。</p> <p>这篇文章让我觉得有意思的地方不是它提出了这个担忧，而是<strong>它把这件事变成了一个可以做实验、可以调参数的系统</strong>，然后发现了一堆反直觉的结果。</p> <p>最让我意外的是这个：<strong>往社区里注入 160 个 AI agent，比注入 80 个效果反而更差。</strong> 多了一倍，结果回弹了。</p> <p>为什么？作者的解释是，太多明显异质的声音会触发人类 agent 某种对抗反应。这事放到现实里也说得通——一个人在评论区发表不同意见，可能让你动摇；一百个人同时说同样的话，你第一反应是”这是水军”。但一个阈值在哪、为什么在那、不同人群有没有差异……这篇文章没有答案，只是观察到了这个现象。</p> <hr/> <h2 id="影响资本主义话题的效果是女权的七倍">影响资本主义话题的效果是女权的七倍</h2> <p>实验设置是：用真实社交媒体数据初始化一批”人类 agent”，注入 80 个持相反立场的”AI agent”，让它们混着讨论 50 步，看人类立场怎么变。四个话题：堕胎、资本主义、女权、脱欧。</p> <p>结果如下：</p> <p><img src="/assets/img/posts/20260706-llm-position-paper/table2_p3.png" alt="80 个反对派 AI 注入后，各话题立场变化"/></p> <p>资本主义话题的支持比例从 89.4% 跌到 58.8%，<strong>跌了 30 个百分点</strong>。女权话题，支持只降了 4 个百分点，基本没动。</p> <p>同样的实验，效果差了七八倍。</p> <p>作者说这是因为不同议题对人的”身份锚定程度”不同——女权更接近身份认同，所以更稳固。这个解释我觉得对了一半。另一半可能是：资本主义作为一个政治经济议题，本身就是一个”可以被论据影响”的领域，人们对它的态度更有弹性；而女权相关的立场背后连着大量个人经历，不是几轮讨论能动摇的。</p> <p>这个差异比”AI 有没有影响力”本身更值得研究——<strong>人类的信念系统不是均匀的，有些地方有铠甲，有些地方没有</strong>，理解这个分布才是真正重要的问题。</p> <hr/> <h2 id="ai-做的不是说服是瓦解惯性">AI 做的不是”说服”，是”瓦解惯性”</h2> <p>文章还分析了每一步的立场转变概率，这张图比最终结果更能说明机制：</p> <p><img src="/assets/img/posts/20260706-llm-position-paper/figure1_p4.png" alt="有无 AI 介入时的立场转变概率矩阵"/></p> <p>还是资本主义。原本持支持立场的人，下一步继续支持的概率从 0.866 降到了 0.685——<strong>不是被扭转到反对，而是先变得不那么确定了</strong>。</p> <p>这和”说服”不是一回事。说服是拿出更好的论据让你改变主意，你清楚地知道发生了什么。这里的机制更像是：持续的异质声音让你对自己的原有判断产生了不确定性，然后在这个不确定性里慢慢漂移。没有明显的转折点，很难察觉，也很难主动抵抗。</p> <p>这个发现让我想到另一件事：做 RLHF 的人都知道，preference data 里有个问题叫”annotation fatigue”，标注者标久了会开始随机选或向多数倾斜。这里的机制是不是类似——不是逻辑上被说服，而是判断的稳定性被磨损了？不一定对，但方向值得想。</p> <hr/> <h2 id="几个调参实验的观察">几个调参实验的观察</h2> <p>文章还测了发帖频率、介入时机、说服风格：</p> <p><img src="/assets/img/posts/20260706-llm-position-paper/figure2_p5.png" alt="AI 介入参数对信念动态的影响"/></p> <p><strong>频率</strong>：每步发一次效果最强，每 4 步一次效果已经明显减弱。持续比间歇有效得多。</p> <p><strong>时机</strong>：早期介入后撤出，效果不会完全消退，但幅度有限。早期影响有一定锁定效应，但没有持续介入那么强。</p> <p><strong>风格</strong>：悲悯型 vs 道德谴责型，效果差不多。说明内容方向比表达风格重要，怎么说没有说什么重要。</p> <hr/> <h2 id="实验的局限必须说">实验的局限（必须说）</h2> <p>最大的问题：<strong>“人类 agent”是 LLM 扮演的，不是真人。</strong> 这个实验严格意义上测的是”LLM agent 能不能影响另一个 LLM 的输出”，外推到真实人类需要大量额外工作。真实的人有信源判断、情绪、社交压力……都没有被建模进去。</p> <p>另外 50 步太短，四个话题太窄，这些都是 position paper 的局限——提出问题框架，不给最终答案。</p> <hr/> <h2 id="最后说一句">最后说一句</h2> <p>做 LLM 系统的人平时很少看这个方向的文章，觉得离技术太远。但有一个连接值得注意：<strong>推理成本还在降，agent 大规模部署的门槛也跟着降</strong>。这篇文章里 80 个 agent 是个实验规模，再往后不是。</p> <p>这篇文章的贡献不在结论，在于<strong>把一个以前只能说”可能有风险”的问题，变成了一个可以测量、可以调参、可以迭代研究的对象</strong>。这本身是一种进展。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 LLM 推理和 agent 系统话题你想深入，可以看看<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，书里有专章讲 LLM 推理效率，和 agent 大规模部署的底层成本问题有直接关联。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="AI Agent"/><category term="社会影响"/><category term="论文解读"/><summary type="html"><![CDATA[160 个 AI 比 80 个效果更差？这篇论文量化了一件大家隐约知道但说不清楚的事]]></summary></entry><entry><title type="html">vllm v1 源码精读（一）：为什么要重写，以及 LLM() 这行代码背后发生了什么</title><link href="https://marsggbo.github.io/blog/2026/20260706-vllm-v1-01-arch/" rel="alternate" type="text/html" title="vllm v1 源码精读（一）：为什么要重写，以及 LLM() 这行代码背后发生了什么"/><published>2026-07-06T00:00:00+00:00</published><updated>2026-07-06T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260706-vllm-v1-01-arch</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260706-vllm-v1-01-arch/"><![CDATA[<h1 id="vllm-v1-源码精读一为什么要重写以及-llm-这行代码背后发生了什么">vllm v1 源码精读（一）：为什么要重写，以及 LLM() 这行代码背后发生了什么</h1> <blockquote> <p><strong>本文基于 vllm <code class="language-plaintext highlighter-rouge">ba22152</code>（2026-07-06），源码链接均指向该 commit 的固定行号。</strong> 代码浏览入口：<a href="https://github.com/vllm-project/vllm/tree/ba22152096b2484faa3579624a253d54804d876d">github.com/vllm-project/vllm/tree/ba22152</a></p> </blockquote> <hr/> <h2 id="1-接口没变但内部几乎全换了为什么">1. 接口没变，但内部几乎全换了——为什么？</h2> <p>如果你最近更新了 vllm，会发现同样这两行代码：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">llm</span> <span class="o">=</span> <span class="nc">LLM</span><span class="p">(</span><span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">facebook/opt-125m</span><span class="sh">"</span><span class="p">)</span>
<span class="n">outputs</span> <span class="o">=</span> <span class="n">llm</span><span class="p">.</span><span class="nf">generate</span><span class="p">(</span><span class="n">prompts</span><span class="p">,</span> <span class="n">sampling_params</span><span class="p">)</span>
</code></pre></div></div> <p>行为看起来和以前没什么两样，但打开源码就会发现，<code class="language-plaintext highlighter-rouge">vllm/engine/llm_engine.py</code> 这个原来的核心文件，现在只剩<strong>一行</strong>：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">LLMEngine</span> <span class="o">=</span> <span class="n">V1LLMEngine</span>
</code></pre></div></div> <p>整个引擎几乎被完整替换掉了。做这么大规模的重构，肯定不是为了重构而重构——v0 一定有什么本质问题，撑不住了。</p> <p>所以在看新代码之前，先搞清楚 v0 到底出了什么问题，是理解这次重写的前提。</p> <hr/> <h2 id="2-v0-的三个问题">2. v0 的三个问题</h2> <p><strong>问题一：调度和 GPU forward 串行，GPU 在等 CPU</strong></p> <p>v0 的每个推理步骤，大致是这样跑的：Python 做调度决策 → tokenization → KV 块分配 → 打包数据 → GPU forward → 回收结果。这一串全在同一个 Python 进程里顺序执行。由于 Python GIL 的存在，CPU 侧的调度逻辑和 GPU forward 无法真正并行，GPU 经常在等 Python 干完那堆”杂活”才能开始计算。</p> <p><strong>问题二：prefill 和 decode 阶段割裂</strong></p> <p>v0 把 prefill 和 decode 当成两个独立的 batch 来跑：先把所有等待中的请求 prefill 一遍，再切换到 decode 阶段。这两个阶段的计算特性完全不同——prefill 是 compute-bound（算力瓶颈），decode 是 memory-bound（带宽瓶颈）——来回切换导致 GPU 利用率不稳定，而且 decode 中的请求还会被 prefill 阻塞，TTFT（Time to First Token，首个 token 的延迟）受到明显影响。</p> <p><strong>问题三：每步都传完整请求对象</strong></p> <p>每次调度都要把完整的 request 对象序列化传给 worker，包括整个 token 序列、所有 sampling 参数等，开销随请求变长而线性增大。</p> <p>有了这三个问题，v1 的设计思路就很清晰了：</p> <ol> <li><strong>EngineCore 独立进程 + ZMQ 通信</strong>：把调度器和 GPU worker 放到独立后台进程，前端只做 tokenization 和 detokenization，两边真正并行。ZMQ（ZeroMQ）是一个高性能消息队列库，专门用于进程间高效传递数据，延迟极低。</li> <li><strong>统一 prefill/decode 调度</strong>：用 <code class="language-plaintext highlighter-rouge">num_computed_tokens</code> 跟踪每个请求的进度，prefill 和 decode 同框处理，chunked prefill 自然支持</li> <li><strong>差量传输</strong>：第一次调度发完整数据，后续步骤只发 diff（新分配的块 id + 新 token）</li> </ol> <p>解决思路明确了，下一步是看 v1 到底怎么组织这套新架构。</p> <hr/> <h2 id="3-一次-generation经历了哪些步骤">3. 一次 generation，经历了哪些步骤？</h2> <p>在看代码之前，先用最简单的语言把整个过程走一遍。你调用 <code class="language-plaintext highlighter-rouge">llm.generate(["给我讲个笑话"])</code> 之后，从字符串进去，到字符串出来，中间发生了 5 件事：</p> <table> <thead> <tr> <th>步骤</th> <th>做了什么</th> <th>由谁负责</th> </tr> </thead> <tbody> <tr> <td>① Tokenize</td> <td>把 “给我讲个笑话” 变成 token id 列表</td> <td><code class="language-plaintext highlighter-rouge">InputProcessor</code></td> </tr> <tr> <td>② Schedule</td> <td>决定这步处理哪些请求、分配 KV Cache 块</td> <td><code class="language-plaintext highlighter-rouge">Scheduler</code> + <code class="language-plaintext highlighter-rouge">KVCacheManager</code></td> </tr> <tr> <td>③ Execute</td> <td>GPU 跑 <code class="language-plaintext highlighter-rouge">model.forward()</code>，得到 logits</td> <td><code class="language-plaintext highlighter-rouge">Executor</code> → <code class="language-plaintext highlighter-rouge">Worker</code> → <code class="language-plaintext highlighter-rouge">GPUModelRunner</code></td> </tr> <tr> <td>④ Sample</td> <td>从 logits 采样出下一个 token id</td> <td><code class="language-plaintext highlighter-rouge">Sampler</code></td> </tr> <tr> <td>⑤ Detokenize</td> <td>把 token id 增量还原成字符串</td> <td><code class="language-plaintext highlighter-rouge">IncrementalDetokenizer</code></td> </tr> </tbody> </table> <p>生成过程是一个循环：每次循环走完 ②③④⑤，产出一个新 token，追加到输出里；下一轮循环再来，直到遇到 EOS token 或达到最大长度，请求结束。步骤 ① 只在第一次执行一次，后续 decode 每轮只有那一个新 token 需要送进 ③。</p> <p>有了这张地图，再看后面每个模块，就能对应到”它在哪个步骤干什么”了。</p> <hr/> <h2 id="4-为什么要这么分层">4. 为什么要这么分层？</h2> <p>5 个步骤有了，但为什么要拆成这么多模块，而不是一个大函数从头跑到尾？</p> <p><strong>主线一：关注点分离</strong></p> <p>每个模块只做一件事，接口干净，可以独立替换：</p> <ul> <li><strong>调度（Scheduler）和执行（Executor）分开</strong>：调度决策需要对全局 KV 内存可见，必须集中管理；GPU 执行只需要拿调度结果去跑，不关心调度逻辑</li> <li><strong>前端和后端分开</strong>：前端（<code class="language-plaintext highlighter-rouge">LLMEngine</code> / <code class="language-plaintext highlighter-rouge">AsyncLLM</code>）处理用户交互——tokenize、管理请求生命周期、返回结果；后端（<code class="language-plaintext highlighter-rouge">EngineCore</code>）只负责调度和 GPU 执行</li> </ul> <p><strong>“前端/后端”的划分依据不是 HTTP server，而是 Python GIL。</strong> <code class="language-plaintext highlighter-rouge">LLMEngine</code> 这一层是 IO 密集型——处理请求进出、detokenize 输出；<code class="language-plaintext highlighter-rouge">EngineCore</code> 是 CPU/GPU 密集型——紧密循环做调度和 forward。把两者放在同一个进程里，asyncio 事件循环和 EngineCore 调度会互相争 GIL，谁都跑不快。分到独立进程，才能真正并行。</p> <p><code class="language-plaintext highlighter-rouge">EngineCoreClient</code>（<code class="language-plaintext highlighter-rouge">vllm/v1/engine/core_client.py:83</code>）就是这条边界的代理：同一进程时直接调用（<code class="language-plaintext highlighter-rouge">InprocClient</code>），不同进程时通过 ZMQ 传消息（<code class="language-plaintext highlighter-rouge">AsyncMPClient</code>）——上层代码感知不到区别。</p> <p><strong>主线二：屏蔽变化点</strong></p> <ul> <li><code class="language-plaintext highlighter-rouge">Executor</code> 抽象屏蔽了单机/多机的差异：不管 1 卡还是 8 卡还是 Ray 集群，<code class="language-plaintext highlighter-rouge">EngineCore</code> 只调 <code class="language-plaintext highlighter-rouge">executor.execute_model()</code>，不感知底层是什么</li> <li>字符串注册（<code class="language-plaintext highlighter-rouge">worker_cls</code>、attention backend 等）让平台替换（CUDA / ROCm / 自定义芯片）不影响上层代码</li> </ul> <p>理解了这两条主线，后面看到”为什么不直接调，要封装一层”，大概率就是其中一条的体现。</p> <hr/> <h2 id="5-整体目录结构">5. 整体目录结构</h2> <p>先建立全局视角，后面看具体流程才不会迷路。</p> <p>顶层目录：</p> <p><img src="/assets/img/posts/20260706-vllm-v1-01-arch/dir_tree_01_toplevel.png" alt="vllm 顶层目录结构"/></p> <p>用户入口层展开：</p> <p><img src="/assets/img/posts/20260706-vllm-v1-01-arch/dir_tree_02_entrypoints.png" alt="vllm/entrypoints/ 用户入口层"/></p> <p>v1 引擎层展开：</p> <p><img src="/assets/img/posts/20260706-vllm-v1-01-arch/dir_tree_03_v1_engine.png" alt="vllm/v1/engine/ 前端引擎层"/></p> <p>调度 + KV Cache 层展开：</p> <p><img src="/assets/img/posts/20260706-vllm-v1-01-arch/dir_tree_04_v1_core.png" alt="vllm/v1/core/ 调度与KV Cache层"/></p> <p>执行层展开：</p> <p><img src="/assets/img/posts/20260706-vllm-v1-01-arch/dir_tree_05_v1_executor_worker.png" alt="vllm/v1/executor/ + worker/ 执行层"/></p> <p>一个关键认知：<strong><code class="language-plaintext highlighter-rouge">vllm/v1/</code> 才是真正的引擎</strong>，<code class="language-plaintext highlighter-rouge">vllm/engine/</code> 只是个 alias 壳，遇到 bug 想找源码，直接去 <code class="language-plaintext highlighter-rouge">vllm/v1/</code> 里找。</p> <hr/> <h2 id="6-核心对象关系">6. 核心对象关系</h2> <p>把前面 5 个步骤对应到实际的类和文件，用 <code class="language-plaintext highlighter-rouge">self.xxx = ...</code> 的持有关系来呈现（这是读懂任何 Python 框架最快的切入点）：</p> <p><img src="/assets/img/posts/20260706-vllm-v1-01-arch/object_relations.png" alt="vllm v1 核心对象持有关系"/></p> <p>图里有两条关键边界线值得单独解释：</p> <p><strong>边界一：<code class="language-plaintext highlighter-rouge">self.engine_core = EngineCoreClient</code>（<code class="language-plaintext highlighter-rouge">core_client.py:83</code>）</strong> 这是前端和后端的分界点。<code class="language-plaintext highlighter-rouge">LLMEngine</code> 之上是前端（tokenize、管理请求生命周期），<code class="language-plaintext highlighter-rouge">EngineCore</code> 之下是后端（调度 + GPU 执行）。<code class="language-plaintext highlighter-rouge">EngineCoreClient</code> 是一个透明代理——同进程时它直接调用 <code class="language-plaintext highlighter-rouge">EngineCore</code> 方法，跨进程时它把调用序列化成 ZMQ 消息发出去，调用方完全感知不到区别。</p> <p><strong>边界二：<code class="language-plaintext highlighter-rouge">self.executor = Executor</code>（<code class="language-plaintext highlighter-rouge">executor/abstract.py:48</code>）</strong> 这是”单机/多机”的分界点。<code class="language-plaintext highlighter-rouge">EngineCore</code> 只调 <code class="language-plaintext highlighter-rouge">executor.execute_model()</code>，不感知底层是 1 卡还是 8 卡还是 Ray 集群，具体实现由 <code class="language-plaintext highlighter-rouge">Executor.get_class()</code> 在启动时按配置选好。</p> <p>有了这两条边界，整个系统分成了三个相互隔离的层：前端（IO）/ 后端调度 / GPU 执行，这正是 v1 重写的核心意图。</p> <hr/> <h2 id="7-llm__init__-全链路">7. <code class="language-plaintext highlighter-rouge">LLM.__init__()</code> 全链路</h2> <p>现在跟着 <code class="language-plaintext highlighter-rouge">__init__</code> 的调用顺序，一层一层走下去。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">llm</span> <span class="o">=</span> <span class="nc">LLM</span><span class="p">(</span><span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">facebook/opt-125m</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <h3 id="71-llminit把参数打包创建-llmengine">7.1 LLM.<strong>init</strong>()：把参数打包，创建 LLMEngine</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># LLM.__init__()  —  vllm/entrypoints/llm.py:176
</span><span class="k">class</span> <span class="nc">LLM</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">model</span><span class="p">,</span> <span class="n">tensor_parallel_size</span><span class="o">=</span><span class="mi">1</span><span class="p">,</span> <span class="p">...):</span>
        <span class="n">engine_args</span> <span class="o">=</span> <span class="nc">EngineArgs</span><span class="p">(</span><span class="n">model</span><span class="o">=</span><span class="n">model</span><span class="p">,</span> <span class="n">tensor_parallel_size</span><span class="o">=</span><span class="n">tp_size</span><span class="p">,</span> <span class="p">...)</span>
        <span class="n">vllm_config</span> <span class="o">=</span> <span class="n">engine_args</span><span class="p">.</span><span class="nf">create_engine_config</span><span class="p">()</span>  <span class="c1"># 所有配置归一为一个对象
</span>        <span class="n">self</span><span class="p">.</span><span class="n">llm_engine</span> <span class="o">=</span> <span class="nc">LLMEngine</span><span class="p">(</span><span class="n">vllm_config</span><span class="p">)</span>          <span class="c1"># ← 进入下一层
</span></code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">VllmConfig</code> 是后续所有模块共享的单一配置入口：</p> <table> <thead> <tr> <th>子配置</th> <th>包含什么</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">ModelConfig</code></td> <td>模型路径、dtype、max_model_len</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">CacheConfig</code></td> <td>block_size、gpu_memory_utilization</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">ParallelConfig</code></td> <td>tp_size、pp_size、<strong>worker_cls（字符串，见下）</strong></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">SchedulerConfig</code></td> <td>max_num_seqs、max_num_batched_tokens、enable_chunked_prefill</td> </tr> </tbody> </table> <blockquote> <p><strong>为什么 worker_cls 是字符串？</strong></p> <p><code class="language-plaintext highlighter-rouge">ParallelConfig.worker_cls = "vllm.v1.worker.gpu_worker.Worker"</code>，由 <code class="language-plaintext highlighter-rouge">vllm/platforms/cuda.py:307</code> 注入。不直接存类引用，是因为 <code class="language-plaintext highlighter-rouge">import gpu_worker</code> 会触发 CUDA context 初始化——而主进程不应该持有 CUDA context，那是 worker 子进程的事。字符串延迟到子进程里通过 <code class="language-plaintext highlighter-rouge">resolve_obj_by_qualname()</code> 动态 import，做到零副作用。不同平台只需注入不同字符串，上层代码完全不感知。</p> </blockquote> <h3 id="72-llmengineinit前端三件套--前后端分界">7.2 LLMEngine.<strong>init</strong>()：前端三件套 + 前后端分界</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># LLMEngine.__init__()  —  vllm/v1/engine/llm_engine.py
</span><span class="k">class</span> <span class="nc">LLMEngine</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">vllm_config</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">input_processor</span>  <span class="o">=</span> <span class="nc">InputProcessor</span><span class="p">(</span><span class="n">vllm_config</span><span class="p">)</span>   <span class="c1"># tokenize 请求
</span>        <span class="n">self</span><span class="p">.</span><span class="n">output_processor</span> <span class="o">=</span> <span class="nc">OutputProcessor</span><span class="p">(</span><span class="n">vllm_config</span><span class="p">)</span>  <span class="c1"># 组装输出（含 detokenize）
</span>        <span class="n">self</span><span class="p">.</span><span class="n">engine_core</span> <span class="o">=</span> <span class="n">EngineCoreClient</span><span class="p">.</span><span class="nf">make_client</span><span class="p">(</span>       <span class="c1"># ← 前后端分界线
</span>            <span class="n">vllm_config</span><span class="p">,</span> <span class="n">asyncio_mode</span><span class="o">=</span><span class="bp">False</span><span class="p">,</span> <span class="bp">...</span>
        <span class="p">)</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">make_client()</code> 的选择逻辑：</p> <table> <thead> <tr> <th>场景</th> <th>返回类型</th> <th>通信方式</th> </tr> </thead> <tbody> <tr> <td>默认（<code class="language-plaintext highlighter-rouge">LLM</code> 类，离线推理）</td> <td><code class="language-plaintext highlighter-rouge">InprocClient</code></td> <td>直接函数调用，零序列化</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">VLLM_ENABLE_V1_MULTIPROCESSING=1</code></td> <td><code class="language-plaintext highlighter-rouge">SyncMPClient</code></td> <td>ZMQ + msgpack，后台进程</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">AsyncLLM</code>（HTTP server）</td> <td><code class="language-plaintext highlighter-rouge">AsyncMPClient</code></td> <td>ZMQ + asyncio，后台进程</td> </tr> </tbody> </table> <p><code class="language-plaintext highlighter-rouge">InprocClient</code> 内部就是直接持有一个 <code class="language-plaintext highlighter-rouge">EngineCore</code> 实例并调用其方法。<code class="language-plaintext highlighter-rouge">AsyncMPClient</code> 则会 <code class="language-plaintext highlighter-rouge">Process.start()</code> 出一个独立后台进程跑 <code class="language-plaintext highlighter-rouge">EngineCore</code>，通过 ZMQ socket 收发消息。</p> <blockquote> <p>为什么 HTTP server 需要独立进程？Python GIL 导致同一进程里的多线程无法真正并行执行 CPU 密集型代码。asyncio HTTP server 和 EngineCore 调度循环放在一起会互相抢 GIL，独立进程才能真正并行。</p> </blockquote> <h3 id="73-enginecoreinit后端四步初始化">7.3 EngineCore.<strong>init</strong>()：后端四步初始化</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># EngineCore.__init__()  —  vllm/v1/engine/core.py:99
</span><span class="k">class</span> <span class="nc">EngineCore</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">vllm_config</span><span class="p">):</span>
        <span class="c1"># 步骤一：根据配置选择 Executor，初始化 Worker
</span>        <span class="n">executor_class</span> <span class="o">=</span> <span class="n">Executor</span><span class="p">.</span><span class="nf">get_class</span><span class="p">(</span><span class="n">vllm_config</span><span class="p">)</span>   <span class="c1"># abstract.py:48
</span>        <span class="c1">#   world_size==1  → UniProcExecutor   (uniproc_executor.py:45)
</span>        <span class="c1">#   world_size&gt;1   → MultiprocExecutor (multiproc_executor.py:103)
</span>        <span class="c1">#   ray 集群       → RayDistributedExecutor (ray_executor.py:64)
</span>        <span class="n">self</span><span class="p">.</span><span class="n">executor</span> <span class="o">=</span> <span class="nf">executor_class</span><span class="p">(</span><span class="n">vllm_config</span><span class="p">)</span>

        <span class="c1"># 步骤二：广播 RPC 给所有 Worker，初始化 CUDA + 加载模型权重
</span>        <span class="n">self</span><span class="p">.</span><span class="n">executor</span><span class="p">.</span><span class="nf">collective_rpc</span><span class="p">(</span><span class="sh">"</span><span class="s">init_worker</span><span class="sh">"</span><span class="p">)</span>  <span class="c1"># CUDA init, NCCL init
</span>        <span class="n">self</span><span class="p">.</span><span class="n">executor</span><span class="p">.</span><span class="nf">collective_rpc</span><span class="p">(</span><span class="sh">"</span><span class="s">load_model</span><span class="sh">"</span><span class="p">)</span>   <span class="c1"># 权重加载进 GPU
</span>
        <span class="c1"># 步骤三：KV Cache 初始化（三小步，见下）
</span>        <span class="n">self</span><span class="p">.</span><span class="nf">_initialize_kv_caches</span><span class="p">(</span><span class="n">vllm_config</span><span class="p">)</span>

        <span class="c1"># 步骤四：调度器上线，等待请求
</span>        <span class="n">self</span><span class="p">.</span><span class="n">scheduler</span> <span class="o">=</span> <span class="nc">Scheduler</span><span class="p">(</span><span class="n">vllm_config</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">kv_cache_manager</span><span class="p">)</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">collective_rpc</code> 是广播调用——单 GPU 直接调 Worker 方法，多 GPU 时所有 rank 同时执行，主进程等所有 rank 完成才继续。</p> <h3 id="74-workerinit--gpumodelrunnerload_model权重进-gpu">7.4 Worker.<strong>init</strong>() / GPUModelRunner.load_model()：权重进 GPU</h3> <p><code class="language-plaintext highlighter-rouge">load_model</code> RPC 最终落到：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Worker.__init__()  —  vllm/v1/worker/gpu_worker.py:126
</span><span class="k">class</span> <span class="nc">Worker</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">vllm_config</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">model_runner</span> <span class="o">=</span> <span class="nc">GPUModelRunner</span><span class="p">(</span><span class="n">vllm_config</span><span class="p">)</span>   <span class="c1"># gpu_model_runner.py:438
</span>
<span class="c1"># GPUModelRunner.load_model()  —  vllm/v1/worker/gpu_model_runner.py:5176
</span><span class="k">class</span> <span class="nc">GPUModelRunner</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">load_model</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">model</span> <span class="o">=</span> <span class="nf">get_model_loader</span><span class="p">(</span><span class="n">load_config</span><span class="p">).</span><span class="nf">load_model</span><span class="p">(</span><span class="n">vllm_config</span><span class="p">)</span>
        <span class="c1"># self.model 是标准 torch.nn.Module
</span>        <span class="c1"># attention 层已被 vllm 替换为调用 FlashAttention kernel 的实现
</span></code></pre></div></div> <p>FlashAttention 是一种 IO-aware 注意力算法，通过 tiling 减少 HBM 读写次数，比标准 <code class="language-plaintext highlighter-rouge">softmax(QKᵀ/√d)V</code> 快 2-4×。权重加载完后，<code class="language-plaintext highlighter-rouge">self.model</code> 就是这样一个 FlashAttention 版本的 <code class="language-plaintext highlighter-rouge">nn.Module</code>，可以直接 <code class="language-plaintext highlighter-rouge">forward()</code>。</p> <h3 id="75-kv-cache-初始化量一下算一下分一下">7.5 KV Cache 初始化：量一下，算一下，分一下</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># EngineCore._initialize_kv_caches()  —  vllm/v1/engine/core.py（精简）
</span><span class="k">def</span> <span class="nf">_initialize_kv_caches</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">vllm_config</span><span class="p">):</span>
    <span class="c1"># 1. 查询每层 KV Cache 的块规格（head 数、head dim、dtype）
</span>    <span class="n">kv_cache_specs</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">executor</span><span class="p">.</span><span class="nf">get_kv_cache_specs</span><span class="p">()</span>

    <span class="c1"># 2. 跑一次 profiling dummy forward，测量峰值显存
</span>    <span class="c1">#    available = gpu_memory_utilization × total_vram - peak_forward
</span>    <span class="n">num_gpu_blocks</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">executor</span><span class="p">.</span><span class="nf">determine_available_memory</span><span class="p">(</span><span class="n">kv_cache_specs</span><span class="p">)</span>

    <span class="c1"># 3. 在 GPU 上分配 KV Cache tensor，初始化 BlockPool
</span>    <span class="n">self</span><span class="p">.</span><span class="n">executor</span><span class="p">.</span><span class="nf">initialize_cache</span><span class="p">(</span><span class="n">num_gpu_blocks</span><span class="p">)</span>
    <span class="n">self</span><span class="p">.</span><span class="n">kv_cache_manager</span> <span class="o">=</span> <span class="nc">KVCacheManager</span><span class="p">(</span>   <span class="c1"># kv_cache_manager.py:110
</span>        <span class="n">num_gpu_blocks</span><span class="p">,</span> <span class="n">kv_cache_specs</span>
    <span class="p">)</span>
</code></pre></div></div> <p>为什么要 profiling？vllm 不知道推理时 activation 峰值是多少——它取决于 batch size 和序列长度，无法静态估算。直接跑一次”最坏情况”的 dummy forward 量一下，剩下的显存全给 KV Cache。代价是启动多花几秒，换来分配精准。</p> <p>至此，<code class="language-plaintext highlighter-rouge">LLM()</code> 完成：模型在 GPU 上就绪，KV Cache 分配好，调度器等待请求。</p> <hr/> <h2 id="8-小结初始化完成时系统是什么状态">8. 小结：初始化完成时，系统是什么状态</h2> <p><code class="language-plaintext highlighter-rouge">LLM("facebook/opt-125m")</code> 这一行干完了四件事：</p> <ol> <li>解析配置，确定执行后端和通信模式</li> <li>Worker 初始化，CUDA init，HuggingFace 权重加载到 GPU（<code class="language-plaintext highlighter-rouge">torch.nn.Module</code> 就绪）</li> <li>Profiling forward pass → 计算可用 KV Cache 块数 → 分配 KV Cache tensor</li> <li>调度器就绪，等待请求</li> </ol> <p><strong>常见报错对应的模块：</strong></p> <table> <thead> <tr> <th>报错场景</th> <th>大概位置</th> </tr> </thead> <tbody> <tr> <td>多 GPU 启动失败 / NCCL 报错</td> <td><code class="language-plaintext highlighter-rouge">vllm/v1/executor/multiproc_executor.py</code></td> </tr> <tr> <td>模型加载失败 / 权重格式不支持</td> <td><code class="language-plaintext highlighter-rouge">vllm/model_executor/model_loader.py</code></td> </tr> <tr> <td>显存不够 / KV Cache 分配失败</td> <td><code class="language-plaintext highlighter-rouge">EngineCore._initialize_kv_caches()</code> → <code class="language-plaintext highlighter-rouge">determine_available_memory()</code></td> </tr> <tr> <td>平台不兼容 / worker_cls 找不到</td> <td><code class="language-plaintext highlighter-rouge">vllm/platforms/cuda.py</code>（或对应平台文件）</td> </tr> <tr> <td>TP/PP 通信超时</td> <td><code class="language-plaintext highlighter-rouge">MultiprocExecutor</code>，检查 NCCL 版本和网络配置</td> </tr> </tbody> </table> <hr/> <blockquote> <p>做 LLM 推理效率研究，一直绕不开 vllm 这类框架——既要用它，也要改它。如果你对 LLM 效率优化、NAS、AutoML 有兴趣，可以看看我之前出版的<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，书里 LLM 推理效率那章和本文的工程背景有直接关联。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="vllm"/><category term="LLM推理"/><category term="系统设计"/><category term="源码分析"/><summary type="html"><![CDATA[vllm v1 源码精读（一）：为什么要重写，以及 LLM() 这行代码背后发生了什么]]></summary></entry><entry><title type="html">vllm v1 源码精读（二）：generate() 计算流——model.forward() 在哪里被调用？</title><link href="https://marsggbo.github.io/blog/2026/20260706-vllm-v1-02-generate/" rel="alternate" type="text/html" title="vllm v1 源码精读（二）：generate() 计算流——model.forward() 在哪里被调用？"/><published>2026-07-06T00:00:00+00:00</published><updated>2026-07-06T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260706-vllm-v1-02-generate</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260706-vllm-v1-02-generate/"><![CDATA[<h1 id="vllm-v1-源码精读二generate-计算流modelforward-在哪里被调用">vllm v1 源码精读（二）：generate() 计算流——model.forward() 在哪里被调用？</h1> <blockquote> <p><strong>本文基于 vllm <code class="language-plaintext highlighter-rouge">ba22152</code>（2026-07-06），源码链接均指向该 commit 的固定行号。</strong> 代码浏览入口：<a href="https://github.com/vllm-project/vllm/tree/ba22152096b2484faa3579624a253d54804d876d">github.com/vllm-project/vllm/tree/ba22152</a></p> </blockquote> <hr/> <h2 id="1-上篇交代了初始化这篇看数据怎么流">1. 上篇交代了初始化，这篇看数据怎么流</h2> <p>上一篇讲了 <code class="language-plaintext highlighter-rouge">LLM()</code> 初始化：权重加载到 GPU、KV Cache 分配好、调度器就位。现在来看 <code class="language-plaintext highlighter-rouge">generate()</code> 被调用时，数据怎么从一个字符串变成输出 token。</p> <p>先把调用栈摊开来——这是整篇文章的骨架：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># LLM.generate()  —  vllm/entrypoints/llm.py:339
</span><span class="k">def</span> <span class="nf">generate</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">prompts</span><span class="p">,</span> <span class="n">sampling_params</span><span class="p">,</span> <span class="p">...):</span>
    <span class="bp">...</span>
    <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">_run_engine</span><span class="p">(</span><span class="n">use_tqdm</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>

<span class="c1"># LLM._run_engine()  —  vllm/entrypoints/offline_utils.py（精简）
</span><span class="k">def</span> <span class="nf">_run_engine</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
    <span class="k">while</span> <span class="n">self</span><span class="p">.</span><span class="n">llm_engine</span><span class="p">.</span><span class="nf">has_unfinished_requests</span><span class="p">():</span>
        <span class="n">step_outputs</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">llm_engine</span><span class="p">.</span><span class="nf">step</span><span class="p">()</span>   <span class="c1"># 每步产出一批新 token
</span>        <span class="k">for</span> <span class="n">output</span> <span class="ow">in</span> <span class="n">step_outputs</span><span class="p">:</span>
            <span class="k">if</span> <span class="n">output</span><span class="p">.</span><span class="n">finished</span><span class="p">:</span>
                <span class="n">outputs</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">output</span><span class="p">)</span>
    <span class="k">return</span> <span class="nf">sorted</span><span class="p">(</span><span class="n">outputs</span><span class="p">,</span> <span class="n">key</span><span class="o">=</span><span class="k">lambda</span> <span class="n">x</span><span class="p">:</span> <span class="nf">int</span><span class="p">(</span><span class="n">x</span><span class="p">.</span><span class="n">request_id</span><span class="p">))</span>

<span class="c1"># LLMEngine.step()  —  vllm/v1/engine/llm_engine.py
</span><span class="k">def</span> <span class="nf">step</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
    <span class="n">engine_core_outputs</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">engine_core</span><span class="p">.</span><span class="nf">step</span><span class="p">()</span>   <span class="c1"># 调度 + GPU 执行 + 采样
</span>    <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="n">output_processor</span><span class="p">.</span><span class="nf">process_outputs</span><span class="p">(</span><span class="n">engine_core_outputs</span><span class="p">)</span>

<span class="c1"># EngineCore.step()  —  vllm/v1/engine/core.py
</span><span class="k">def</span> <span class="nf">step</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
    <span class="n">scheduler_output</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">scheduler</span><span class="p">.</span><span class="nf">schedule</span><span class="p">()</span>                           <span class="c1"># 决定这步算哪些 token
</span>    <span class="n">self</span><span class="p">.</span><span class="n">executor</span><span class="p">.</span><span class="nf">execute_model</span><span class="p">(</span><span class="n">scheduler_output</span><span class="p">,</span> <span class="n">non_block</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>          <span class="c1"># GPU forward（非阻塞）
</span>    <span class="n">grammar_output</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">scheduler</span><span class="p">.</span><span class="nf">get_grammar_bitmask</span><span class="p">(</span><span class="n">scheduler_output</span><span class="p">)</span>  <span class="c1"># CPU 并行推进语法状态机
</span>    <span class="n">model_output</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">executor</span><span class="p">.</span><span class="nf">sample_tokens</span><span class="p">(</span><span class="n">grammar_output</span><span class="p">)</span>             <span class="c1"># 等 GPU + 采样
</span>    <span class="n">self</span><span class="p">.</span><span class="n">scheduler</span><span class="p">.</span><span class="nf">update_from_output</span><span class="p">(</span><span class="n">scheduler_output</span><span class="p">,</span> <span class="n">model_output</span><span class="p">)</span>      <span class="c1"># 更新状态、检查停止
</span>    <span class="k">return</span> <span class="n">engine_core_outputs</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">EngineCore.step()</code> 里这四行是核心：<strong>调度 → GPU forward → 采样 → 状态更新</strong>，每次循环都走一遍，直到所有请求完成。<code class="language-plaintext highlighter-rouge">_run_engine()</code> 末尾的排序确保第 3 条 prompt 对应第 3 条输出——调度器可能先完成后加入的短请求，顺序未必和输入一致。</p> <p>本篇沿着这条路走一遍，重点拆清楚几个非直觉的地方：</p> <ul> <li><strong><code class="language-plaintext highlighter-rouge">model.forward()</code> 到底在哪一行被调用</strong>，调用时的输入长什么样</li> <li>为什么 vllm 里 <code class="language-plaintext highlighter-rouge">input_ids</code> 是一维的，而不是普通的 <code class="language-plaintext highlighter-rouge">[batch, seq_len]</code> 二维 tensor</li> <li><code class="language-plaintext highlighter-rouge">execute_model</code> 和 <code class="language-plaintext highlighter-rouge">sample_tokens</code> 为什么拆成两步，不一起做</li> <li>输出的 token id 怎么变回文字，为什么要用”双 token 解码”</li> </ul> <hr/> <h2 id="2-整体流程一图看懂">2. 整体流程一图看懂</h2> <p>上面的调用栈对应到时序上就是这张图——每个方块对应后面一个章节：</p> <p><img src="/assets/img/posts/20260706-vllm-v1-02-generate/generate_flow.png" alt="generate() 一个 step 内的完整计算流"/></p> <p>有了全局视角，下面逐步拆开来看。</p> <hr/> <h2 id="3-第一步tokenization">3. 第一步：Tokenization</h2> <p>数据流的起点是文字变数字。<code class="language-plaintext highlighter-rouge">InputProcessor</code> 负责把原始字符串转成 <code class="language-plaintext highlighter-rouge">EngineCoreRequest</code>：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># LLM.generate() → LLM._run_engine() → LLMEngine.step()
#   → LLMEngine.engine_core.step() → [step() 之前] LLMEngine.input_processor.process_inputs()
# InputProcessor.process_inputs()  —  vllm/v1/engine/input_processor.py
</span><span class="k">def</span> <span class="nf">process_inputs</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">request_id</span><span class="p">,</span> <span class="n">inputs</span><span class="p">,</span> <span class="n">params</span><span class="p">):</span>
    <span class="n">prompt_token_ids</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">tokenizer</span><span class="p">.</span><span class="nf">encode</span><span class="p">(</span><span class="n">inputs</span><span class="p">.</span><span class="n">prompt</span><span class="p">)</span>  <span class="c1"># str → list[int]
</span>    <span class="c1"># 超过 max_model_len 时截断或报错
</span>    <span class="nf">_validate_token_ids</span><span class="p">(</span><span class="n">prompt_token_ids</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">model_config</span><span class="p">)</span>
    <span class="k">return</span> <span class="nc">EngineCoreRequest</span><span class="p">(</span>
        <span class="n">request_id</span><span class="o">=</span><span class="n">request_id</span><span class="p">,</span>
        <span class="n">prompt_token_ids</span><span class="o">=</span><span class="n">prompt_token_ids</span><span class="p">,</span>
        <span class="n">sampling_params</span><span class="o">=</span><span class="n">params</span><span class="p">,</span>
        <span class="n">arrival_time</span><span class="o">=</span><span class="n">time</span><span class="p">.</span><span class="nf">monotonic</span><span class="p">(),</span>
    <span class="p">)</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">EngineCoreRequest</code> 是整个 v1 系统的”请求载体”，用 <code class="language-plaintext highlighter-rouge">msgspec.Struct(array_like=True, omit_defaults=True)</code> 定义。</p> <p><code class="language-plaintext highlighter-rouge">array_like=True</code> 的效果是把字段名从序列化结果里抹掉。对比一下同一个对象的两种序列化：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 默认（字典形式）——字段名随数据一起传
</span><span class="p">{</span><span class="sh">"</span><span class="s">request_id</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">r1</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">prompt_token_ids</span><span class="sh">"</span><span class="p">:</span> <span class="p">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">],</span> <span class="sh">"</span><span class="s">sampling_params</span><span class="sh">"</span><span class="p">:</span> <span class="p">{...},</span> <span class="p">...}</span>

<span class="c1"># array_like=True（数组形式）——只传值，按位置对应字段
</span><span class="p">[</span><span class="sh">"</span><span class="s">r1</span><span class="sh">"</span><span class="p">,</span> <span class="p">[</span><span class="mi">1</span><span class="p">,</span> <span class="mi">2</span><span class="p">,</span> <span class="mi">3</span><span class="p">],</span> <span class="p">{...},</span> <span class="p">...]</span>
</code></pre></div></div> <p>字段名字符串在每条消息里要占几十字节，在结构复杂的 Struct 里这个开销相当可观。<code class="language-plaintext highlighter-rouge">omit_defaults=True</code> 再进一步——采样参数里大多数字段用户根本没改，是默认值，直接从序列化结果里消失。</p> <blockquote> <p><strong>为什么这个细节值得关注？</strong> 在多进程架构下，EngineCoreRequest 每次从前端进程序列化后经 ZMQ 传到 EngineCore 进程，再反序列化。高吞吐场景每秒几百条请求，序列化在关键路径上，稍有臃肿就会变成瓶颈。msgspec 加上这两个参数后比普通 dataclass + pickle 快一个数量级，是 vllm 选它的主要原因。</p> </blockquote> <p>tokenization 完成后，请求被送进调度器的等待队列。</p> <hr/> <h2 id="4-第二步请求入队">4. 第二步：请求入队</h2> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># LLM.generate() → LLM._run_engine() → LLMEngine.add_request()
#   → EngineCoreClient.add_request() → EngineCore.add_request() → Scheduler.add_request()
# Scheduler.add_request()  —  vllm/v1/core/sched/scheduler.py
</span><span class="k">def</span> <span class="nf">add_request</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">req</span><span class="p">:</span> <span class="n">EngineCoreRequest</span><span class="p">):</span>
    <span class="n">request</span> <span class="o">=</span> <span class="n">Request</span><span class="p">.</span><span class="nf">from_engine_core_request</span><span class="p">(</span><span class="n">req</span><span class="p">)</span>
    <span class="n">request</span><span class="p">.</span><span class="n">status</span> <span class="o">=</span> <span class="n">RequestStatus</span><span class="p">.</span><span class="n">WAITING</span>
    <span class="n">self</span><span class="p">.</span><span class="n">waiting</span><span class="p">.</span><span class="nf">add</span><span class="p">(</span><span class="n">request</span><span class="p">)</span>
    <span class="n">self</span><span class="p">.</span><span class="n">requests</span><span class="p">[</span><span class="n">request</span><span class="p">.</span><span class="n">request_id</span><span class="p">]</span> <span class="o">=</span> <span class="n">request</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">Request</code> 是调度器侧最核心的状态对象，后续所有调度决策都围绕它展开：</p> <table> <thead> <tr> <th>字段</th> <th>含义</th> <th>初始值</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">prompt_token_ids</code></td> <td>原始 prompt</td> <td>来自 EngineCoreRequest</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">output_token_ids</code></td> <td>已生成的 token</td> <td><code class="language-plaintext highlighter-rouge">[]</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">num_computed_tokens</code></td> <td>已经过 forward pass 的 token 数</td> <td><code class="language-plaintext highlighter-rouge">0</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">status</code></td> <td>请求状态</td> <td><code class="language-plaintext highlighter-rouge">WAITING</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">block_hashes</code></td> <td>每个 KV 块的内容 hash</td> <td><code class="language-plaintext highlighter-rouge">[]</code>（用于前缀缓存）</td> </tr> </tbody> </table> <p><code class="language-plaintext highlighter-rouge">num_computed_tokens</code> 这个字段是 v1 统一 prefill/decode 调度的关键——它跟踪每个请求当前计算到了哪个位置，调度器据此判断哪些 token 还没算，不再区分”是 prefill 还是 decode”。</p> <hr/> <h2 id="5-第三步调度器-schedule">5. 第三步：调度器 schedule()</h2> <p>有了请求队列，调度器每步做两件事：决定这步处理哪些请求的哪些 token，以及分配 KV Cache 块。这是 chunked prefill 的实现之处（第三篇会专门展开），这里先看骨架：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># LLM.generate() → LLM._run_engine() → LLMEngine.step()
#   → EngineCoreClient.step() → EngineCore.step() → Scheduler.schedule()
# Scheduler.schedule()  —  vllm/v1/core/sched/scheduler.py:396
</span><span class="k">def</span> <span class="nf">schedule</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
    <span class="c1"># Phase 1: 处理 running 队列
</span>    <span class="k">for</span> <span class="n">request</span> <span class="ow">in</span> <span class="n">self</span><span class="p">.</span><span class="n">running</span><span class="p">:</span>
        <span class="n">num_new_tokens</span> <span class="o">=</span> <span class="n">request</span><span class="p">.</span><span class="n">num_tokens</span> <span class="o">-</span> <span class="n">request</span><span class="p">.</span><span class="n">num_computed_tokens</span>
        <span class="n">num_new_tokens</span> <span class="o">=</span> <span class="nf">min</span><span class="p">(</span><span class="n">num_new_tokens</span><span class="p">,</span> <span class="n">remaining_token_budget</span><span class="p">)</span>
        <span class="n">ok</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">kv_cache_manager</span><span class="p">.</span><span class="nf">allocate_slots</span><span class="p">(</span><span class="n">request</span><span class="p">,</span> <span class="n">num_new_tokens</span><span class="p">)</span>
        <span class="k">if</span> <span class="ow">not</span> <span class="n">ok</span><span class="p">:</span>
            <span class="n">self</span><span class="p">.</span><span class="nf">_preempt</span><span class="p">(</span><span class="n">request</span><span class="p">)</span>  <span class="c1"># KV Cache 不够，踢回 waiting
</span>    
    <span class="c1"># Phase 2: 从 waiting 队列准入新请求
</span>    <span class="k">for</span> <span class="n">request</span> <span class="ow">in</span> <span class="n">self</span><span class="p">.</span><span class="n">waiting</span><span class="p">:</span>
        <span class="c1"># 查前缀缓存命中
</span>        <span class="n">computed_blocks</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">kv_cache_manager</span><span class="p">.</span><span class="nf">get_computed_blocks</span><span class="p">(</span><span class="n">request</span><span class="p">)</span>
        <span class="n">request</span><span class="p">.</span><span class="n">num_computed_tokens</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">computed_blocks</span><span class="p">)</span> <span class="o">*</span> <span class="n">block_size</span>
        
        <span class="n">num_new</span> <span class="o">=</span> <span class="nf">min</span><span class="p">(</span><span class="n">remaining_prompt_tokens</span><span class="p">,</span> <span class="n">remaining_token_budget</span><span class="p">)</span>
        <span class="n">ok</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">kv_cache_manager</span><span class="p">.</span><span class="nf">allocate_slots</span><span class="p">(</span><span class="n">request</span><span class="p">,</span> <span class="n">num_new</span><span class="p">,</span>
                                                   <span class="n">new_computed_blocks</span><span class="o">=</span><span class="n">computed_blocks</span><span class="p">)</span>
        <span class="k">if</span> <span class="ow">not</span> <span class="n">ok</span><span class="p">:</span>
            <span class="k">break</span>  <span class="c1"># KV Cache 不够，后面的请求等下一步
</span>        
        <span class="n">self</span><span class="p">.</span><span class="n">running</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">request</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">waiting</span><span class="p">.</span><span class="nf">remove</span><span class="p">(</span><span class="n">request</span><span class="p">)</span>
    
    <span class="k">return</span> <span class="nc">SchedulerOutput</span><span class="p">(</span><span class="n">num_scheduled_tokens</span><span class="o">=</span><span class="p">{...},</span> <span class="p">...)</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">SchedulerOutput</code> 是调度结果的载体：</p> <ul> <li><code class="language-plaintext highlighter-rouge">NewRequestData</code>：第一次被调度的请求，带完整数据</li> <li><code class="language-plaintext highlighter-rouge">CachedRequestData</code>：后续步骤，只带 diff（新分配的块 id、新 token）</li> <li><code class="language-plaintext highlighter-rouge">num_scheduled_tokens</code>：每个请求本步要处理多少 token</li> </ul> <blockquote> <p><strong>这里有个重要的设计决策</strong>：<code class="language-plaintext highlighter-rouge">CachedRequestData</code> 只传 diff，而不是每步都传完整请求。这意味着 Worker 侧需要自己维护每个请求的状态（通过 <code class="language-plaintext highlighter-rouge">_update_states</code> 应用 diff）。好处是序列化开销接近零；代价是 Worker 侧的状态管理变复杂了——这是一个典型的”以复杂性换性能”的工程取舍。</p> </blockquote> <p>调度结果出来之后，就可以交给 GPU 执行了。</p> <hr/> <h2 id="6-第四步execute_model-modelforward-在这里">6. 第四步：execute_model() ——model.forward() 在这里</h2> <p><code class="language-plaintext highlighter-rouge">EngineCore.step()</code> 拿到 <code class="language-plaintext highlighter-rouge">SchedulerOutput</code> 后，调用链向下穿三层：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># LLM.generate() → LLM._run_engine() → LLMEngine.step()
#   → EngineCoreClient.step() → EngineCore.step() → Executor.execute_model()
# EngineCore.step()  —  vllm/v1/engine/core.py
</span><span class="n">self</span><span class="p">.</span><span class="n">executor</span><span class="p">.</span><span class="nf">execute_model</span><span class="p">(</span><span class="n">scheduler_output</span><span class="p">)</span>
  <span class="c1"># ↓ Executor 广播 RPC 给所有 Worker
</span>  <span class="c1"># Worker.execute_model()  —  vllm/v1/worker/gpu_worker.py:955
</span>  <span class="n">self</span><span class="p">.</span><span class="n">model_runner</span><span class="p">.</span><span class="nf">execute_model</span><span class="p">(</span><span class="n">scheduler_output</span><span class="p">)</span>
    <span class="c1"># ↓ 真正干活的地方
</span>    <span class="c1"># GPUModelRunner.execute_model()  —  vllm/v1/worker/gpu_model_runner.py:4069
</span></code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">GPUModelRunner.execute_model()</code> 是重点，分 6 个子步骤：</p> <h3 id="61-_update_states应用-diff">6.1 _update_states：应用 diff</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># GPUModelRunner._update_states()  —  gpu_model_runner.py（精简）
</span></code></pre></div></div> <p>把 <code class="language-plaintext highlighter-rouge">SchedulerOutput</code> 里的 diff 应用到 <code class="language-plaintext highlighter-rouge">InputBatch</code>（模型运行器维护的 CPU 状态结构）：</p> <ul> <li>新请求：分配 InputBatch 中的槽位，初始化 block table 行</li> <li>结束请求：释放槽位</li> <li>新 KV 块：更新 block table</li> </ul> <p>状态更新完成，才能准备这步的 GPU 输入。</p> <h3 id="62-_prepare_inputs为什么是-1d">6.2 _prepare_inputs：为什么是 1D？</h3> <p>这一步把 CPU 侧的 <code class="language-plaintext highlighter-rouge">InputBatch</code> 打包成 GPU tensor，输出两个核心 tensor：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">input_ids</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span>   <span class="c1"># shape [N_tokens]
</span><span class="n">positions</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span>   <span class="c1"># shape [N_tokens]
</span></code></pre></div></div> <p>注意：<strong>是 1D 的，不是 <code class="language-plaintext highlighter-rouge">[batch, seq_len]</code> 的 2D</strong>。为什么？</p> <p><img src="/assets/img/posts/20260706-vllm-v1-02-generate/forward_input.png" alt="为什么 model.forward() 的输入是 1D tensor？"/></p> <p>同一个 batch 里，不同请求当前步的 token 数差异极大：prefill 中的请求可能有几百个 token，decode 中的请求只有 1 个。如果 padding 成 <code class="language-plaintext highlighter-rouge">[batch, max_len]</code>，灰色的 PAD 位置全是无效计算，浪费率极高。</p> <p>vllm 的做法是把所有请求的 token 直接<strong>拼成 1D</strong>，配合 <code class="language-plaintext highlighter-rouge">cu_seqlens</code>（各序列长度的累积前缀和）告诉 attention 层每个请求的边界在哪里。4 个请求（5+1+3+1 个 token）拼成 <code class="language-plaintext highlighter-rouge">[10]</code>，只有 10 次有效计算，零浪费。</p> <p>FlashAttention（一种 IO-aware 的高效注意力算法）的 2/3 版本原生支持这种 varlen 格式（<code class="language-plaintext highlighter-rouge">flash_attn_varlen_func</code>），这是两者的配合点。</p> <h3 id="63-_build_attention_metadata寻址-kv-cache-的关键">6.3 _build_attention_metadata：寻址 KV Cache 的关键</h3> <p>有了 1D 的输入，还需要告诉 attention 层每个 token 对应的 KV Cache 在哪里：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">attn_metadata</span> <span class="o">=</span> <span class="n">FlashAttentionMetadataBuilder</span><span class="p">.</span><span class="nf">build</span><span class="p">(</span>
    <span class="n">block_table</span><span class="o">=</span><span class="n">block_table_gpu</span><span class="p">,</span>  <span class="c1"># [num_reqs, max_blocks]
</span>    <span class="n">slot_mapping</span><span class="o">=</span><span class="n">slot_mapping</span><span class="p">,</span>    <span class="c1"># [N_tokens]
</span>    <span class="n">seq_lens</span><span class="o">=</span><span class="n">seq_lens</span><span class="p">,</span>
    <span class="bp">...</span>
<span class="p">)</span>
</code></pre></div></div> <p>其中 <code class="language-plaintext highlighter-rouge">slot_mapping</code> 是 KV Cache 寻址的核心——每个 token 写入哪个物理 KV Cache 槽？</p> \[\text{slot\_id} = \text{block\_table}[\text{req}][\text{pos} \div B] \times B + (\text{pos} \bmod B)\] <p>$B$ 是 block_size，<code class="language-plaintext highlighter-rouge">pos</code> 是 token 在请求中的绝对位置。这个映射由 Triton kernel 并行计算。</p> <h3 id="64-set_forward_context一个优雅的解耦设计">6.4 set_forward_context：一个优雅的解耦设计</h3> <p>metadata 准备好了，但有一个问题：怎么把 <code class="language-plaintext highlighter-rouge">attn_metadata</code> 传进模型？</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># GPUModelRunner.execute_model()  —  vllm/v1/worker/gpu_model_runner.py:4069（精简）
</span><span class="k">with</span> <span class="nf">set_forward_context</span><span class="p">(</span><span class="n">attn_metadata</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">vllm_config</span><span class="p">):</span>
    <span class="n">hidden_states</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">model</span><span class="p">(</span>
        <span class="n">input_ids</span><span class="o">=</span><span class="n">input_ids</span><span class="p">,</span>   <span class="c1"># [N_tokens]
</span>        <span class="n">positions</span><span class="o">=</span><span class="n">positions</span><span class="p">,</span>   <span class="c1"># [N_tokens]
</span>    <span class="p">)</span>
</code></pre></div></div> <p>注意：<code class="language-plaintext highlighter-rouge">attn_metadata</code> <strong>没有</strong>作为参数传给 <code class="language-plaintext highlighter-rouge">self.model()</code>。它被存入了线程局部变量（<code class="language-plaintext highlighter-rouge">set_forward_context</code> 是个 context manager），模型内部的 attention 层自己去取。</p> <blockquote> <p><strong>为什么不直接把 <code class="language-plaintext highlighter-rouge">attn_metadata</code> 传进 <code class="language-plaintext highlighter-rouge">self.model()</code> 的参数里？</strong></p> <p>现实问题：HuggingFace 社区里跑得动的模型有几百个，每个 attention 层的 <code class="language-plaintext highlighter-rouge">forward</code> 签名各不相同，你没办法要求它们统一新增一个 vllm 专属参数。改完一个还好，全改一遍根本不现实，而且每次上游模型更新你都得跟着维护。</p> <p>线程局部变量绕开了这个问题。vllm 把原始 attention 层的实现换掉，换成自己调 FlashAttention kernel 的版本，这个替换版知道去全局上下文里拿 <code class="language-plaintext highlighter-rouge">attn_metadata</code>；模型里其他所有部件——embedding、FFN、LayerNorm——完全感知不到有这么个东西，它们的接口和 HuggingFace 保持一样。这是把”外部依赖”藏进调用上下文、而不是塞进函数参数的经典做法。</p> <p>碰到 attention 相关的奇怪报错，第一件事是确认 <code class="language-plaintext highlighter-rouge">set_forward_context</code> 有没有正确执行，以及当前使用的 attention backend 和环境是否匹配。</p> </blockquote> <h3 id="65-compute_logits只算最后一个-token">6.5 compute_logits：只算最后一个 token</h3> <p>forward 完成后，<code class="language-plaintext highlighter-rouge">hidden_states</code> 是 <code class="language-plaintext highlighter-rouge">[N_tokens, d_model]</code>，但我们实际上只关心每个请求”下一个 token 是什么”，所以只需要对每个请求的最后一个 token 的 hidden state 做 logits 投影：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># GPUModelRunner.execute_model()  —  gpu_model_runner.py（精简）
# 只对每个请求"最后一个 token"的 hidden state 计算 logits
</span><span class="n">logits</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">model</span><span class="p">.</span><span class="nf">compute_logits</span><span class="p">(</span><span class="n">hidden_states</span><span class="p">[</span><span class="n">logits_indices</span><span class="p">])</span>
<span class="c1"># logits: [N_req, vocab_size]
</span></code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">logits_indices</code> 是每个请求”当前步最后一个 token 在 <code class="language-plaintext highlighter-rouge">input_ids</code> 中的位置”。prefill 请求取 prompt 最后一个 token，decode 请求取其唯一 token。这一步把维度从 <code class="language-plaintext highlighter-rouge">[N_tokens, d_model]</code> 压缩到 <code class="language-plaintext highlighter-rouge">[N_req, vocab_size]</code>，减少了大量不必要的投影计算。</p> <h3 id="66-存入-executemodelstate返回-none">6.6 存入 ExecuteModelState，返回 None</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">self</span><span class="p">.</span><span class="n">_execute_model_state</span> <span class="o">=</span> <span class="nc">ExecuteModelState</span><span class="p">(</span><span class="n">logits</span><span class="o">=</span><span class="n">logits</span><span class="p">,</span> <span class="p">...)</span>
<span class="k">return</span> <span class="bp">None</span>   <span class="c1"># 注意这里返回 None！
</span></code></pre></div></div> <p><strong>为什么不直接返回 logits，而是存起来等 sample_tokens() 来取？</strong> 这个问题引出了下一节。</p> <hr/> <h2 id="7-execute-和-sample-为什么拆成两步">7. execute 和 sample 为什么拆成两步</h2> <p>这是 v1 里一个很精妙的优化。看 <code class="language-plaintext highlighter-rouge">EngineCore.step()</code> 的结构：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># EngineCore.step()  —  vllm/v1/engine/core.py
</span><span class="k">def</span> <span class="nf">step</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
    <span class="n">scheduler_output</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">scheduler</span><span class="p">.</span><span class="nf">schedule</span><span class="p">()</span>
    
    <span class="n">self</span><span class="p">.</span><span class="n">executor</span><span class="p">.</span><span class="nf">execute_model</span><span class="p">(</span><span class="n">scheduler_output</span><span class="p">,</span> <span class="n">non_block</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>   <span class="c1"># GPU forward（非阻塞）
</span>    <span class="n">grammar_output</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">scheduler</span><span class="p">.</span><span class="nf">get_grammar_bitmask</span><span class="p">(</span><span class="n">scheduler_output</span><span class="p">)</span>  <span class="c1"># CPU 并行
</span>    <span class="n">model_output</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">executor</span><span class="p">.</span><span class="nf">sample_tokens</span><span class="p">(</span><span class="n">grammar_output</span><span class="p">)</span>      <span class="c1"># 等 GPU + 采样
</span>    <span class="bp">...</span>
</code></pre></div></div> <p>如果用户用了结构化输出（比如要求输出必须是合法 JSON），每步都需要推进一个有限状态机来计算”哪些 token 是合法的下一步”——这是一个纯 CPU 的计算，可能需要几毫秒。</p> <p>拆成两步之后：GPU 在做 forward 的同时，CPU 在推进语法状态机；GPU forward 完成时，bitmask 也已经算好了，直接进采样。如果不拆，这几毫秒的 CPU 计算就会变成 GPU 的等待时间。这个优化在”每步都有大量结构化输出请求”的场景下效果显著。即使没有结构化输出，grammar_output 为空时 bitmask 步骤是 no-op，没有任何额外开销。</p> <hr/> <h2 id="8-第五步samplerforward">8. 第五步：Sampler.forward()</h2> <p>回到骨架——<code class="language-plaintext highlighter-rouge">execute_model</code> 完成后，GPU 把 logits 存在 <code class="language-plaintext highlighter-rouge">_execute_model_state</code> 里；接下来 <code class="language-plaintext highlighter-rouge">sample_tokens()</code> 取出 logits，调用 <code class="language-plaintext highlighter-rouge">Sampler.forward()</code> 采样：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># LLM.generate() → LLM._run_engine() → LLMEngine.step()
#   → EngineCoreClient.step() → EngineCore.step() → Executor.sample_tokens() → Sampler.forward()
# EngineCore.step()（精简）
</span><span class="n">self</span><span class="p">.</span><span class="n">executor</span><span class="p">.</span><span class="nf">execute_model</span><span class="p">(</span><span class="n">scheduler_output</span><span class="p">,</span> <span class="n">non_block</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>  <span class="c1"># GPU forward
</span><span class="n">grammar_output</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">scheduler</span><span class="p">.</span><span class="nf">get_grammar_bitmask</span><span class="p">(...)</span>       <span class="c1"># CPU 并行
</span><span class="n">model_output</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">executor</span><span class="p">.</span><span class="nf">sample_tokens</span><span class="p">(</span><span class="n">grammar_output</span><span class="p">)</span>     <span class="c1"># ← 这里触发采样
</span></code></pre></div></div> <p>采样在 <code class="language-plaintext highlighter-rouge">vllm/v1/sample/sampler.py</code> 里，分三个阶段依次进行。</p> <p>第一步采集 logprobs（如果用户要求了）：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="n">needs_logprobs</span><span class="p">:</span>
    <span class="n">raw_logprobs</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">log_softmax</span><span class="p">(</span><span class="n">logits</span><span class="p">.</span><span class="nf">float</span><span class="p">(),</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>
</code></pre></div></div> <p>必须在所有修改 logits 的操作<strong>之前</strong>算——保证用户看到的是原始分布上的概率，而不是经过惩罚修正之后的。</p> <p>接着是 logits 处理流水线，按顺序应用各种约束和惩罚：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 白名单：只允许特定 token
</span><span class="n">logits</span><span class="p">[</span><span class="o">~</span><span class="n">allowed_token_ids_mask</span><span class="p">]</span> <span class="o">=</span> <span class="nf">float</span><span class="p">(</span><span class="sh">'</span><span class="s">-inf</span><span class="sh">'</span><span class="p">)</span>
<span class="c1"># 黑名单：bad_words 列表
</span><span class="n">logits</span> <span class="o">=</span> <span class="nf">apply_bad_words</span><span class="p">(</span><span class="n">logits</span><span class="p">,</span> <span class="n">bad_words_token_ids</span><span class="p">)</span>
<span class="c1"># min_tokens：未到最小长度时屏蔽 EOS
</span><span class="k">if</span> <span class="n">not_yet_min_tokens</span><span class="p">:</span>
    <span class="n">logits</span><span class="p">[:,</span> <span class="n">eos_token_id</span><span class="p">]</span> <span class="o">=</span> <span class="nf">float</span><span class="p">(</span><span class="sh">'</span><span class="s">-inf</span><span class="sh">'</span><span class="p">)</span>
<span class="c1"># logit_bias：用户直接调整某些 token 的分数
</span><span class="n">logits</span> <span class="o">+=</span> <span class="n">logit_bias</span>
<span class="c1"># 重复惩罚 / 频率惩罚 / 存在惩罚
</span><span class="n">logits</span> <span class="o">=</span> <span class="nf">apply_all_penalties</span><span class="p">(</span><span class="n">logits</span><span class="p">,</span> <span class="n">prompt_ids</span><span class="p">,</span> <span class="n">output_ids</span><span class="p">,</span> <span class="n">penalties</span><span class="p">)</span>
</code></pre></div></div> <p>处理完 logits，最后从中采样：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Sampler.forward()  —  vllm/v1/sample/sampler.py
</span><span class="k">if</span> <span class="n">all_greedy</span><span class="p">:</span>
    <span class="c1"># 快速路径：所有请求都是 greedy，直接 argmax
</span>    <span class="n">sampled</span> <span class="o">=</span> <span class="n">logits</span><span class="p">.</span><span class="nf">argmax</span><span class="p">(</span><span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">,</span> <span class="n">keepdim</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>   <span class="c1"># [N_req, 1]
</span>
<span class="k">elif</span> <span class="n">all_random</span><span class="p">:</span>
    <span class="c1"># 随机采样路径
</span>    <span class="n">logits</span> <span class="o">=</span> <span class="nf">apply_temperature</span><span class="p">(</span><span class="n">logits</span><span class="p">,</span> <span class="n">temperatures</span><span class="p">)</span>  <span class="c1"># logits /= T
</span>    <span class="n">logits</span> <span class="o">=</span> <span class="nf">apply_min_p</span><span class="p">(</span><span class="n">logits</span><span class="p">,</span> <span class="n">min_p</span><span class="p">)</span>               <span class="c1"># 过滤极低概率 token
</span>    <span class="n">sampled</span> <span class="o">=</span> <span class="nf">top_k_top_p_sampler</span><span class="p">(</span><span class="n">logits</span><span class="p">,</span> <span class="n">top_k</span><span class="p">,</span> <span class="n">top_p</span><span class="p">)</span>

<span class="k">else</span><span class="p">:</span>
    <span class="c1"># 混合：按请求分别处理，用 torch.where 合并
</span>    <span class="n">greedy</span> <span class="o">=</span> <span class="n">logits</span><span class="p">.</span><span class="nf">argmax</span><span class="p">(</span><span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>
    <span class="n">random</span> <span class="o">=</span> <span class="nf">top_k_top_p_sampler</span><span class="p">(</span><span class="n">logits</span><span class="p">,</span> <span class="p">...)</span>
    <span class="n">sampled</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">where</span><span class="p">(</span><span class="n">is_greedy_mask</span><span class="p">,</span> <span class="n">greedy</span><span class="p">,</span> <span class="n">random</span><span class="p">)</span>
</code></pre></div></div> <blockquote> <p><strong>Top-K / Top-P 的直觉</strong></p> <p>Top-K：把 vocab 里分数最高的 K 个 token 留下，其余置 $-\infty$，然后 softmax 采样。K 越小越保守，K=1 就是 greedy。</p> <p>Top-P（nucleus sampling）：按概率从高到低排序，取累积概率刚超过 $p$ 的最小集合，其余置 $-\infty$，然后采样。P=1.0 就是全 vocab 采样，P=0.9 通常能过滤掉大量低概率的长尾 token。</p> <p>实现上用的是 <strong>Gumbel-max trick</strong>：给每个 logit 加 Gumbel(0,1) 噪声，然后 argmax——数学上等价于从 softmax 分布采样，但比显式 softmax + multinomial 更快更稳定。</p> </blockquote> <p><code class="language-plaintext highlighter-rouge">sample_tokens()</code> 最后把采样结果通过异步 D2H（Device to Host）拷贝转到 CPU，组装成 <code class="language-plaintext highlighter-rouge">ModelRunnerOutput</code> 返回给调度器。</p> <hr/> <h2 id="9-第六步update_from_output-状态更新和停止检测">9. 第六步：update_from_output() ——状态更新和停止检测</h2> <p><code class="language-plaintext highlighter-rouge">sample_tokens()</code> 返回 <code class="language-plaintext highlighter-rouge">model_output</code>，下一行就是：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># LLM.generate() → LLM._run_engine() → LLMEngine.step()
#   → EngineCoreClient.step() → EngineCore.step() → Scheduler.update_from_output()
# EngineCore.step()（精简）
</span><span class="n">model_output</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">executor</span><span class="p">.</span><span class="nf">sample_tokens</span><span class="p">(</span><span class="n">grammar_output</span><span class="p">)</span>
<span class="n">self</span><span class="p">.</span><span class="n">scheduler</span><span class="p">.</span><span class="nf">update_from_output</span><span class="p">(</span><span class="n">scheduler_output</span><span class="p">,</span> <span class="n">model_output</span><span class="p">)</span>  <span class="c1"># ← 这里
</span></code></pre></div></div> <p>调度器拿到新 token，更新每个请求的状态并判断是否该停止：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Scheduler.update_from_output()  —  vllm/v1/core/sched/scheduler.py:1501
</span><span class="k">for</span> <span class="n">req_id</span><span class="p">,</span> <span class="n">new_token_id</span> <span class="ow">in</span> <span class="n">model_output</span><span class="p">.</span><span class="n">sampled_token_ids</span><span class="p">.</span><span class="nf">items</span><span class="p">():</span>
    <span class="n">request</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">requests</span><span class="p">[</span><span class="n">req_id</span><span class="p">]</span>
    
    <span class="c1"># 追加新 token
</span>    <span class="n">request</span><span class="p">.</span><span class="n">output_token_ids</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">new_token_id</span><span class="p">)</span>
    <span class="n">request</span><span class="p">.</span><span class="n">num_computed_tokens</span> <span class="o">+=</span> <span class="n">num_scheduled_tokens</span><span class="p">[</span><span class="n">req_id</span><span class="p">]</span>
    
    <span class="c1"># 检查停止条件
</span>    <span class="n">is_eos</span> <span class="o">=</span> <span class="p">(</span><span class="n">new_token_id</span> <span class="o">==</span> <span class="n">tokenizer</span><span class="p">.</span><span class="n">eos_token_id</span><span class="p">)</span>
    <span class="n">is_max</span> <span class="o">=</span> <span class="p">(</span><span class="nf">len</span><span class="p">(</span><span class="n">request</span><span class="p">.</span><span class="n">output_token_ids</span><span class="p">)</span> <span class="o">&gt;=</span> <span class="n">sampling_params</span><span class="p">.</span><span class="n">max_tokens</span><span class="p">)</span>
    <span class="n">is_stop</span> <span class="o">=</span> <span class="nf">_check_stop_strings</span><span class="p">(</span><span class="n">request</span><span class="p">,</span> <span class="n">new_token_id</span><span class="p">)</span>
    
    <span class="k">if</span> <span class="n">is_eos</span> <span class="ow">or</span> <span class="n">is_max</span> <span class="ow">or</span> <span class="n">is_stop</span><span class="p">:</span>
        <span class="n">request</span><span class="p">.</span><span class="n">status</span> <span class="o">=</span> <span class="n">RequestStatus</span><span class="p">.</span><span class="n">FINISHED_STOPPED</span>
        <span class="n">self</span><span class="p">.</span><span class="n">kv_cache_manager</span><span class="p">.</span><span class="nf">free</span><span class="p">(</span><span class="n">request</span><span class="p">)</span>   <span class="c1"># 释放 KV 块
</span>    
    <span class="c1"># 完整的 KV 块写入前缀缓存（供后续请求复用）
</span>    <span class="n">self</span><span class="p">.</span><span class="n">kv_cache_manager</span><span class="p">.</span><span class="nf">cache_blocks</span><span class="p">(</span><span class="n">request</span><span class="p">)</span>
</code></pre></div></div> <p><strong>前缀缓存在这里更新</strong>：当一个 KV 块被完整填满（<code class="language-plaintext highlighter-rouge">block_size</code> 个 token 都已计算），其内容 hash 就确定了，写入前缀缓存 map。后续遇到相同前缀的请求，<code class="language-plaintext highlighter-rouge">get_computed_blocks()</code> 能直接命中，跳过 prefill。这是”算过的不再算”的核心机制。</p> <hr/> <h2 id="10-第七步输出还原incrementaldetokenizer">10. 第七步：输出还原——IncrementalDetokenizer</h2> <p><code class="language-plaintext highlighter-rouge">update_from_output()</code> 结束，<code class="language-plaintext highlighter-rouge">EngineCore.step()</code> 把结果返回给上层的 <code class="language-plaintext highlighter-rouge">LLMEngine.step()</code>：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># LLM.generate() → LLM._run_engine() → LLMEngine.step()
#   → OutputProcessor.process_outputs() → IncrementalDetokenizer.update()
# LLMEngine.step()（精简）
</span><span class="n">engine_core_outputs</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">engine_core</span><span class="p">.</span><span class="nf">step</span><span class="p">()</span>
<span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="n">output_processor</span><span class="p">.</span><span class="nf">process_outputs</span><span class="p">(</span><span class="n">engine_core_outputs</span><span class="p">)</span>  <span class="c1"># ← 这里解码
</span></code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">process_outputs()</code> 内部调用 <code class="language-plaintext highlighter-rouge">IncrementalDetokenizer</code> 把 token id 还原成文字。这件事听起来简单，实际上有个边界问题要处理：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># vllm/v1/engine/detokenizer.py（精简）
</span><span class="k">class</span> <span class="nc">IncrementalDetokenizer</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">update</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">request</span><span class="p">,</span> <span class="n">new_token_id</span><span class="p">):</span>
        <span class="c1"># 用双 token 解码
</span>        <span class="n">new_text</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">tokenizer</span><span class="p">.</span><span class="nf">decode</span><span class="p">(</span>
            <span class="p">[</span><span class="n">self</span><span class="p">.</span><span class="n">prev_token_id</span><span class="p">,</span> <span class="n">new_token_id</span><span class="p">],</span>
            <span class="n">skip_special_tokens</span><span class="o">=</span><span class="bp">True</span><span class="p">,</span>
        <span class="p">)</span>
        <span class="c1"># 去掉上一次已经输出的部分，只返回新增字符
</span>        <span class="n">delta</span> <span class="o">=</span> <span class="n">new_text</span><span class="p">[</span><span class="nf">len</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">prev_decoded_text</span><span class="p">):]</span>
        <span class="n">self</span><span class="p">.</span><span class="n">prev_decoded_text</span> <span class="o">=</span> <span class="n">new_text</span>
        <span class="n">self</span><span class="p">.</span><span class="n">prev_token_id</span> <span class="o">=</span> <span class="n">new_token_id</span>
        <span class="k">return</span> <span class="n">delta</span>
</code></pre></div></div> <p><strong>为什么要双 token 解码，而不是直接 decode 新 token？</strong></p> <p>BPE tokenizer 存在边界问题：某些字节序列必须和前一个 token 一起才能正确 decode。比如中文字符”我”可能被拆成两个 byte-level token，单独 decode 其中一个会得到乱码。用双 token decode 并取差值，能正确处理这类 BPE 边界。</p> <p>这也是为什么流式输出有时候会”卡一下”再出现几个字——tokenizer 在等凑够完整的字符边界。</p> <hr/> <h2 id="11-完整数据流一个请求的生死">11. 完整数据流：一个请求的生死</h2> <p><img src="/assets/img/posts/20260706-vllm-v1-02-generate/request_lifecycle.png" alt="一个请求从输入到输出的完整生命周期"/></p> <hr/> <h2 id="12-常见报错对应位置">12. 常见报错对应位置</h2> <table> <thead> <tr> <th>报错场景</th> <th>大概位置</th> </tr> </thead> <tbody> <tr> <td>OOM during forward</td> <td><code class="language-plaintext highlighter-rouge">GPUModelRunner._model_forward()</code>，检查 batch size 和 max_num_batched_tokens</td> </tr> <tr> <td>Sampling 结果异常（全 EOS / 重复）</td> <td><code class="language-plaintext highlighter-rouge">vllm/v1/sample/sampler.py</code>，检查 temperature / repetition_penalty 参数</td> </tr> <tr> <td>输出乱码</td> <td><code class="language-plaintext highlighter-rouge">IncrementalDetokenizer</code>，检查 tokenizer 和模型是否匹配</td> </tr> <tr> <td>attention backend 报错</td> <td><code class="language-plaintext highlighter-rouge">vllm/v1/attention/backends/flash_attn.py</code>，检查 FlashAttention 版本</td> </tr> <tr> <td>输出顺序和输入不对应</td> <td><code class="language-plaintext highlighter-rouge">offline_utils.py</code> 末尾的 <code class="language-plaintext highlighter-rouge">sorted()</code>，检查 request_id 是否正确赋值</td> </tr> </tbody> </table> <hr/> <h2 id="13-小结">13. 小结</h2> <p><code class="language-plaintext highlighter-rouge">generate()</code> 的本质是一个步进循环，每步：</p> <ol> <li>调度器决定这步处理谁的哪些 token</li> <li>把 token 打包成 1D varlen tensor，喂给 <code class="language-plaintext highlighter-rouge">model.forward()</code>（在 <code class="language-plaintext highlighter-rouge">gpu_model_runner.py:4069</code>）</li> <li>从最后一层 hidden state 算 logits，经 Sampler 采样出下一个 token</li> <li>更新请求状态，检查停止条件</li> <li>token id 增量解码成文字</li> </ol> <p><strong>整条链路里最值得记住的两个设计决策</strong>：input_ids 用 1D varlen 而不是 padding，是为了零浪费地混合 prefill 和 decode；execute_model 和 sample_tokens 拆成两步，是为了让 GPU forward 和 CPU 语法计算真正并行。这两个决策共同指向一个目标：<strong>把每一毫秒的等待时间都消灭掉</strong>。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 LLM 推理效率和系统设计你想深入研究，可以看看我们之前出版的<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，书里对于 LLM 的结构优化设计，提高推理效率和本文讲的推理框架设计能相辅相成。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="vllm"/><category term="LLM推理"/><category term="系统设计"/><category term="源码分析"/><summary type="html"><![CDATA[vllm v1 源码精读（二）：generate() 计算流——model.forward() 在哪里被调用？]]></summary></entry><entry><title type="html">vllm v1 源码精读（三）：KV Cache 管理、Chunked Prefill 与异步架构</title><link href="https://marsggbo.github.io/blog/2026/20260706-vllm-v1-03-kvcache/" rel="alternate" type="text/html" title="vllm v1 源码精读（三）：KV Cache 管理、Chunked Prefill 与异步架构"/><published>2026-07-06T00:00:00+00:00</published><updated>2026-07-06T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260706-vllm-v1-03-kvcache</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260706-vllm-v1-03-kvcache/"><![CDATA[<h1 id="vllm-v1-源码精读三kv-cache-管理chunked-prefill-与异步架构">vllm v1 源码精读（三）：KV Cache 管理、Chunked Prefill 与异步架构</h1> <blockquote> <p><strong>本文基于 vllm <code class="language-plaintext highlighter-rouge">ba22152</code>（2026-07-06），源码链接均指向该 commit 的固定行号。</strong> 代码浏览入口：<a href="https://github.com/vllm-project/vllm/tree/ba22152096b2484faa3579624a253d54804d876d">github.com/vllm-project/vllm/tree/ba22152</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>上一篇讲了 <code class="language-plaintext highlighter-rouge">EngineCore.step()</code> 的调用链：调度 → GPU forward → 采样 → 状态更新。这篇要拆开”调度”这一步的内核——KV Cache 怎么管、Chunked Prefill 怎么工作，以及在线 serving 场景下整个架构为什么要用独立进程。</p> <p>先把这几块代码在哪里、怎么串起来交代清楚：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># EngineCore.__init__()  —  vllm/v1/engine/core.py:99
</span><span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">vllm_config</span><span class="p">):</span>
    <span class="n">self</span><span class="p">.</span><span class="nf">_initialize_kv_caches</span><span class="p">(</span><span class="n">vllm_config</span><span class="p">)</span>   <span class="c1"># 算显存、分配 GPU tensor
</span>    <span class="n">self</span><span class="p">.</span><span class="n">kv_cache_manager</span> <span class="o">=</span> <span class="nc">KVCacheManager</span><span class="p">(</span>   <span class="c1"># 块管理器：负责分配/释放/前缀缓存
</span>        <span class="n">vllm_config</span><span class="p">,</span> <span class="n">num_gpu_blocks</span><span class="p">,</span> <span class="p">...)</span>     <span class="c1"># vllm/v1/core/kv_cache_manager.py:110
</span>    <span class="n">self</span><span class="p">.</span><span class="n">scheduler</span> <span class="o">=</span> <span class="nc">Scheduler</span><span class="p">(</span>
        <span class="n">vllm_config</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">kv_cache_manager</span><span class="p">)</span>   <span class="c1"># 调度器持有 KVCacheManager
</span>
<span class="c1"># Scheduler.schedule()  —  vllm/v1/core/sched/scheduler.py:396
</span><span class="k">def</span> <span class="nf">schedule</span><span class="p">(</span><span class="n">self</span><span class="p">):</span>
    <span class="k">for</span> <span class="n">request</span> <span class="ow">in</span> <span class="n">self</span><span class="p">.</span><span class="n">running</span><span class="p">:</span>
        <span class="c1"># 读：前缀缓存命中直接复用旧块
</span>        <span class="n">computed</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">kv_cache_manager</span><span class="p">.</span><span class="nf">get_computed_blocks</span><span class="p">(</span><span class="n">request</span><span class="p">)</span>
        <span class="c1"># 写：为本步新 token 申请物理块
</span>        <span class="n">ok</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">kv_cache_manager</span><span class="p">.</span><span class="nf">allocate_slots</span><span class="p">(</span><span class="n">request</span><span class="p">,</span> <span class="n">num_new_tokens</span><span class="p">)</span>
        <span class="k">if</span> <span class="ow">not</span> <span class="n">ok</span><span class="p">:</span>
            <span class="n">self</span><span class="p">.</span><span class="nf">_preempt</span><span class="p">(</span><span class="n">request</span><span class="p">)</span>   <span class="c1"># 显存不够，踢回等待队列
</span>    <span class="k">return</span> <span class="nc">SchedulerOutput</span><span class="p">(</span><span class="n">num_scheduled_tokens</span><span class="o">=</span><span class="p">{...},</span> <span class="p">...)</span>

<span class="c1"># GPUModelRunner._prepare_inputs()  —  vllm/v1/worker/gpu_model_runner.py
</span><span class="k">def</span> <span class="nf">_prepare_inputs</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">scheduler_output</span><span class="p">):</span>
    <span class="c1"># 把逻辑块编号映射成 GPU tensor 里的物理槽位
</span>    <span class="n">slot_mapping</span> <span class="o">=</span> <span class="nf">compute_slot_mapping</span><span class="p">(</span><span class="n">block_table</span><span class="p">,</span> <span class="n">seq_lens</span><span class="p">,</span> <span class="p">...)</span>  <span class="c1"># [N_tokens]
</span>    <span class="n">attn_metadata</span> <span class="o">=</span> <span class="n">FlashAttentionMetadataBuilder</span><span class="p">.</span><span class="nf">build</span><span class="p">(</span>
        <span class="n">block_table</span><span class="o">=</span><span class="n">block_table_gpu</span><span class="p">,</span> <span class="n">slot_mapping</span><span class="o">=</span><span class="n">slot_mapping</span><span class="p">,</span> <span class="p">...)</span>
    <span class="k">return</span> <span class="n">input_ids</span><span class="p">,</span> <span class="n">positions</span><span class="p">,</span> <span class="n">attn_metadata</span>
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">KVCacheManager</code> 夹在调度器和 GPU 执行之间：调度器决定分配哪些块，GPU 执行时通过 <code class="language-plaintext highlighter-rouge">block_table</code> 和 <code class="language-plaintext highlighter-rouge">slot_mapping</code> 找到物理位置。这几块是相互咬合的，下面逐个拆开。</p> <hr/> <h2 id="2-kv-cache-的根本问题是什么">2. KV Cache 的根本问题是什么</h2> <p>在看数据结构之前，先把 PagedAttention 要解决的问题搞清楚，不然后面那些代码看起来会很无聊。</p> <p>LLM 推理的显存大头其实是 KV Cache，不是模型权重。以 LLaMA-3-8B（GQA，8 个 KV head，head_dim=128，fp16）为例：</p> <ul> <li>每层每个 token 的 KV Cache：$2 \times 8 \times 128 \times 2 \text{ bytes} = 4096 \text{ bytes}$</li> <li>32 层合计：$32 \times 4096 = 131072$ bytes ≈ <strong>128 KB/token</strong></li> <li>一个 4K 上下文的请求：$4096 \times 128 \text{ KB} \approx 512 \text{ MB}$</li> </ul> <p>如果 batch 里有 10 个这样的请求，仅 KV Cache 就需要 5 GB。更头疼的是，不同请求的实际序列长度差异很大。如果每个请求都预分配”最长可能序列”的显存，内存碎片极其严重——可能 80% 的显存在某个时刻是空闲的，却没法给其他请求用。</p> <p><strong>这正是 PagedAttention 要解决的问题</strong>，思路和操作系统的虚拟内存分页几乎一模一样：把 KV Cache 切成固定大小的”页”（块），物理上不连续，用地址表（block table）维护逻辑序列到物理块的映射。理解了 OS 的虚拟内存，这套设计的动机就一目了然。</p> <p>搞清楚了为什么要这样设计，下面看具体数据结构就有意思多了。</p> <hr/> <h2 id="3-kv-cache-的数据结构从逻辑块到物理块">3. KV Cache 的数据结构：从逻辑块到物理块</h2> <p><img src="/assets/img/posts/20260706-vllm-v1-03-kvcache/kvcache_blocks.png" alt="KV Cache 块管理：PagedAttention 核心数据结构"/></p> <h3 id="31-kvcacheblock最小单元">3.1 KVCacheBlock：最小单元</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># KVCacheBlock  —  vllm/v1/core/kv_cache_utils.py
</span><span class="k">class</span> <span class="nc">KVCacheBlock</span><span class="p">:</span>
    <span class="n">block_id</span><span class="p">:</span> <span class="nb">int</span>                           <span class="c1"># 物理块编号（0..num_blocks-1）
</span>    <span class="n">ref_cnt</span><span class="p">:</span> <span class="nb">int</span>                            <span class="c1"># 引用计数
</span>    <span class="n">_block_hash</span><span class="p">:</span> <span class="n">BlockHashType</span> <span class="o">|</span> <span class="bp">None</span>       <span class="c1"># 内容 hash（None = 还没填满）
</span>    <span class="n">prev_free_block</span><span class="p">:</span> <span class="n">KVCacheBlock</span> <span class="o">|</span> <span class="bp">None</span>    <span class="c1"># 双链表指针
</span>    <span class="n">next_free_block</span><span class="p">:</span> <span class="n">KVCacheBlock</span> <span class="o">|</span> <span class="bp">None</span>
</code></pre></div></div> <p>每个 <code class="language-plaintext highlighter-rouge">KVCacheBlock</code> 对应 GPU 上 KV Cache tensor 里的一个切片：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># shape: [num_blocks, 2, block_size, num_kv_heads, head_dim]
</span><span class="n">kv_cache</span><span class="p">[</span><span class="n">block_id</span><span class="p">,</span> <span class="mi">0</span><span class="p">]</span>  <span class="c1"># K cache，shape [block_size, nkv, d]
</span><span class="n">kv_cache</span><span class="p">[</span><span class="n">block_id</span><span class="p">,</span> <span class="mi">1</span><span class="p">]</span>  <span class="c1"># V cache，shape [block_size, nkv, d]
</span></code></pre></div></div> <p>逻辑上，一个请求的 KV Cache 是一段连续序列；物理上，它被切成若干个 <code class="language-plaintext highlighter-rouge">KVCacheBlock</code>，散布在 GPU 显存的任意位置。连接逻辑和物理的，就是 block table——这个后面讲。</p> <h3 id="32-freekvcacheblockqueuelru-双链表">3.2 FreeKVCacheBlockQueue：LRU 双链表</h3> <p>有了单个块，还需要管理”哪些块空闲可用”。这就是 <code class="language-plaintext highlighter-rouge">FreeKVCacheBlockQueue</code>——维护所有 <code class="language-plaintext highlighter-rouge">ref_cnt == 0</code> 的块，是整个 KV Cache 管理里最关键的数据结构。</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>head → [无hash块] → [无hash块] → [有hash块] → [有hash块] → tail
       ↑ 优先被分配                              ↑ 尽量保留（前缀缓存）
</code></pre></div></div> <p>四种操作都是 O(1)：</p> <table> <thead> <tr> <th>操作</th> <th>场景</th> <th>位置</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">popleft()</code></td> <td>分配新块</td> <td>从 head 取</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">appendleft()</code></td> <td>释放无 hash 块</td> <td>prepend 到 head（优先被再分配，不当缓存）</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">append()</code></td> <td>释放有 hash 块</td> <td>append 到 tail（LRU 策略，尽量晚被驱逐）</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">remove(block)</code></td> <td>前缀缓存命中</td> <td><strong>从中间移除</strong></td> </tr> </tbody> </table> <blockquote> <p><strong>为什么要用双链表，而不是 Python deque？</strong></p> <p>前缀缓存命中时，需要把被命中的块从 free list <strong>中间</strong>移除——它从空闲状态变为被占用。<code class="language-plaintext highlighter-rouge">deque</code> 无法 O(1) 移除任意位置（需要 O(n) 扫描），但双链表可以，因为每个节点保存了前后指针，O(1) 摘下来就行。</p> <p>这和操作系统的 LRU 页面置换算法用双链表是一个道理。如果你做过 LeetCode 146（LRU Cache），这里的设计应该很熟悉。</p> </blockquote> <h3 id="33-块的生命周期">3.3 块的生命周期</h3> <p>有了数据结构，完整的块生命周期是这样的：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>分配（allocate_slots）：
  get_new_blocks(n) → popleft() × n
  → 如果取到的块有 hash（是前缀缓存），先从 prefix_cache_map 中移除
  → block.ref_cnt = 1

前缀缓存命中（get_computed_blocks → touch）：
  if block.ref_cnt == 0:
      free_queue.remove(block)   # 从 free list 中间取出
  block.ref_cnt += 1

释放（free_blocks）：
  block.ref_cnt -= 1
  if ref_cnt == 0:
      if no hash: free_queue.appendleft(block)   # 无缓存价值，头部，优先再用
      if has hash: free_queue.append(block)      # 有缓存价值，尾部，LRU 保留

哈希注册（cache_blocks）：
  block._block_hash = compute_hash(token_ids, parent_hash)
  prefix_cache_map[block_hash] = block
</code></pre></div></div> <p>逻辑块管理清楚了，下一个问题是：attention kernel 怎么知道去 GPU tensor 的哪个位置读写 KV？</p> <hr/> <h2 id="4-block-table-和-slot_mapping从逻辑地址到-gpu-物理槽">4. Block Table 和 slot_mapping：从逻辑地址到 GPU 物理槽</h2> <p><strong>Block Table</strong>（<code class="language-plaintext highlighter-rouge">vllm/v1/worker/block_table.py</code>）是一个二维数组，连接逻辑序列和物理块：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># CPU 侧：pinned numpy 数组（H2D 速度快）
</span><span class="n">block_table</span><span class="p">:</span> <span class="n">np</span><span class="p">.</span><span class="n">ndarray</span>   <span class="c1"># shape [max_num_reqs, max_num_blocks_per_req], dtype int32
# block_table[req_i][j] = 请求 i 第 j 个逻辑块对应的物理 block_id
</span></code></pre></div></div> <p>每步开始前，把 block table 异步拷贝到 GPU：<code class="language-plaintext highlighter-rouge">commit_block_table(num_reqs)</code>。</p> <p>Block table 解决了”哪个逻辑块对应哪个物理块”的问题，但 attention kernel 需要的粒度更细——它需要知道<strong>每个 token 具体写入哪个物理槽</strong>。这就是 slot_mapping。</p> <p><strong>slot_mapping</strong> 是更精细的映射，公式如下：</p> \[\text{slot\_id} = \text{block\_table}[\text{req}][\text{pos} \div B] \times B + (\text{pos} \bmod B)\] <p>$B$ 是 <code class="language-plaintext highlighter-rouge">block_size</code>，<code class="language-plaintext highlighter-rouge">pos</code> 是 token 在请求中的绝对位置。这个映射由 Triton kernel 并行计算，输出一个 <code class="language-plaintext highlighter-rouge">[N_tokens]</code> 的 1D tensor。</p> <p><strong>在 FlashAttention 层里</strong>（<code class="language-plaintext highlighter-rouge">vllm/v1/attention/backends/flash_attn.py</code>）：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># vllm/v1/attention/backends/flash_attn.py  FlashAttentionImpl.forward()（精简）
# KV 写入：把新计算的 K/V scatter 写入 KV cache（按 slot_mapping）
</span><span class="n">ops</span><span class="p">.</span><span class="nf">reshape_and_cache_flash</span><span class="p">(</span><span class="n">key</span><span class="p">,</span> <span class="n">value</span><span class="p">,</span> <span class="n">key_cache</span><span class="p">,</span> <span class="n">value_cache</span><span class="p">,</span> <span class="n">slot_mapping</span><span class="p">,</span> <span class="p">...)</span>

<span class="c1"># KV 读取：FlashAttention 直接用 block_table 做 paged attention
</span><span class="nf">flash_attn_varlen_func</span><span class="p">(</span><span class="n">q</span><span class="o">=</span><span class="n">query</span><span class="p">,</span> <span class="n">k</span><span class="o">=</span><span class="n">key_cache</span><span class="p">,</span> <span class="n">v</span><span class="o">=</span><span class="n">value_cache</span><span class="p">,</span>
                       <span class="n">block_table</span><span class="o">=</span><span class="n">block_table_gpu</span><span class="p">,</span> <span class="p">...)</span>
</code></pre></div></div> <p>FlashAttention 2/3 原生支持 paged KV——它可以直接从 block_table 找到每个历史 token 的 K/V 位置，不需要先把 KV 拼成连续 buffer，避免了一次 gather 操作。这是 PagedAttention 和 FlashAttention 配合的关键点。</p> <p>有了 slot_mapping 和 block table，KV Cache 的读写问题解决了。但每次请求都从头 prefill，效率还是低——这就引出了 prefix caching。</p> <hr/> <h2 id="5-prefix-cachingsystem-prompt-复用的秘密">5. Prefix Caching：system prompt 复用的秘密</h2> <p>有一个很常见的场景：你有一个很长的 system prompt（比如 1000 token 的角色设定），后续所有用户请求都带着这个前缀。每次都重新 prefill 这 1000 个 token 既浪费算力又慢。</p> <p>Prefix caching 就是为了解决这个问题：<strong>只要两个请求的 token 前缀完全相同，对应的 KV 块可以直接复用，不需要重新计算。</strong></p> <p><strong>块 hash 是滚动计算的</strong>：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># hash_request_tokens()  —  vllm/v1/core/kv_cache_utils.py（伪代码）
# 第 k 块的 hash = f(token_ids_k, parent_hash_{k-1})
</span></code></pre></div></div> <p>两个请求只要共享相同的 token 前缀，对应块的 hash 就完全相同，无论这些块被分配到哪个物理位置。这个滚动 hash 设计保证了只要前缀一致，hash 就一致。</p> <p><strong>查询流程</strong>：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># KVCacheManager.get_computed_blocks()  —  vllm/v1/core/kv_cache_manager.py:110
</span><span class="k">def</span> <span class="nf">get_computed_blocks</span><span class="p">(</span><span class="n">request</span><span class="p">):</span>
    <span class="n">computed</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="k">for</span> <span class="n">block_hash</span> <span class="ow">in</span> <span class="n">request</span><span class="p">.</span><span class="n">block_hashes</span><span class="p">:</span>   <span class="c1"># 按顺序查，必须连续
</span>        <span class="n">block</span> <span class="o">=</span> <span class="n">prefix_cache_map</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="n">block_hash</span><span class="p">)</span>
        <span class="k">if</span> <span class="n">block</span> <span class="ow">is</span> <span class="bp">None</span><span class="p">:</span>
            <span class="k">break</span>   <span class="c1"># 前缀必须连续，一旦 miss 就停
</span>        <span class="n">computed</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">block</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">computed</span>
</code></pre></div></div> <p>命中后，<code class="language-plaintext highlighter-rouge">request.num_computed_tokens</code> 直接跳到命中块数 × block_size，本次调度跳过这些 token 的 prefill。</p> <p><strong>前缀缓存在哪里更新</strong>：每步 <code class="language-plaintext highlighter-rouge">update_from_output()</code> 之后，被完整填满的块（内容确定了）会计算 hash 并写入 <code class="language-plaintext highlighter-rouge">prefix_cache_map</code>。</p> <blockquote> <p><strong>一个容易被忽视的细节</strong>：被抢占（preemption）的请求，其 KV 块释放后，如果这些块有 hash（内容完整），它们会进入 LRU 尾部等待驱逐，而不是立即消失。如果这期间显存够用，下次同一个请求重新被调度时，前缀缓存可能还在，只需重新 prefill 新增的那部分。被抢占不一定意味着”从头来”。</p> </blockquote> <p>Prefix caching 解决了重复前缀的问题，但还有另一个瓶颈没处理：一个很长的 prompt 在 prefill 时会把所有 decode 中的请求全部阻塞住。这就是 chunked prefill 要解决的事。</p> <hr/> <h2 id="6-chunked-prefillprefill-和-decode-为什么能同框">6. Chunked Prefill：prefill 和 decode 为什么能”同框”</h2> <h3 id="61-v0-的痛点">6.1 v0 的痛点</h3> <p>v0 里，prefill 和 decode 是两个独立的阶段，交替执行：一批请求全部 prefill 完，再切换到 decode 阶段。这带来两个问题：</p> <ol> <li><strong>decode 被 prefill 阻塞</strong>：有一个 10000 token 的长 prompt，所有 decode 中的请求都得等它 prefill 完，TTFT（Time to First Token）被严重拖长</li> <li><strong>GPU 利用率不稳定</strong>：prefill 阶段 compute-bound，decode 阶段 memory-bound，两者交替切换，GPU 无法稳定工作在最优状态</li> </ol> <h3 id="62-v1-的解法num_computed_tokens">6.2 v1 的解法：num_computed_tokens</h3> <p>v1 删掉了 prefill/decode 的阶段概念，<strong>每个请求只有一个字段 <code class="language-plaintext highlighter-rouge">num_computed_tokens</code></strong>，表示”已经过 forward pass 的 token 数”：</p> <ul> <li><code class="language-plaintext highlighter-rouge">num_computed_tokens == 0</code> → 还没开始，需要 prefill</li> <li><code class="language-plaintext highlighter-rouge">0 &lt; num_computed_tokens &lt; prompt_len</code> → prefill 进行中</li> <li><code class="language-plaintext highlighter-rouge">num_computed_tokens == prompt_len</code> → prefill 完成，进入 decode</li> </ul> <p>调度器每步决定每个请求”这步处理多少 token”，用 <code class="language-plaintext highlighter-rouge">token_budget</code> 控制总量：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Scheduler.schedule()  —  vllm/v1/core/sched/scheduler.py:396（精简）
# Phase 2: 等待中的请求准入（chunked prefill 在这里）
</span><span class="k">for</span> <span class="n">request</span> <span class="ow">in</span> <span class="n">self</span><span class="p">.</span><span class="n">waiting</span><span class="p">:</span>
    <span class="n">remaining</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">request</span><span class="p">.</span><span class="n">prompt_token_ids</span><span class="p">)</span> <span class="o">-</span> <span class="n">request</span><span class="p">.</span><span class="n">num_computed_tokens</span>
    <span class="c1"># 只取 min(剩余 prompt, budget)——自动 chunk
</span>    <span class="n">num_new</span> <span class="o">=</span> <span class="nf">min</span><span class="p">(</span><span class="n">remaining</span><span class="p">,</span> <span class="n">remaining_token_budget</span><span class="p">)</span>
    
    <span class="k">if</span> <span class="ow">not</span> <span class="n">enable_chunked_prefill</span> <span class="ow">and</span> <span class="n">num_new</span> <span class="o">&lt;</span> <span class="n">remaining</span><span class="p">:</span>
        <span class="k">break</span>   <span class="c1"># 不支持 chunk：要么整个 prefill 进来，要么等
</span>    
    <span class="n">remaining_token_budget</span> <span class="o">-=</span> <span class="n">num_new</span>
    <span class="c1"># ... 分配 KV 块，移入 running
</span></code></pre></div></div> <p><img src="/assets/img/posts/20260706-vllm-v1-03-kvcache/chunked_prefill.png" alt="Chunked Prefill：prefill 和 decode 共享同一个 batch"/></p> <p>图中 Req A 的 500-token prompt 被自动切成 256+244 两步，在这两步里，Req B（decode）从来没有被阻塞，每步都在正常生成 token。</p> <blockquote> <p><strong>token_budget 怎么设？</strong></p> <p><code class="language-plaintext highlighter-rouge">max_num_batched_tokens</code> 控制每步最多处理多少 token（prefill + decode 加起来）。设太小，长 prompt 的 prefill 被切得很碎，TTFT 变慢；设太大，GPU 单步计算量过大，latency 抖动。官方建议从 2048~8192 开始调，根据目标 latency SLO 和 GPU 显存决定。</p> </blockquote> <h3 id="63-抢占kv-cache-不够怎么办">6.3 抢占：KV Cache 不够怎么办</h3> <p>调度器发现 KV Cache 不够给 running 队列用时，会<strong>抢占</strong>优先级最低的请求：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Scheduler._preempt()  —  vllm/v1/core/sched/scheduler.py
</span><span class="k">def</span> <span class="nf">_preempt</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">request</span><span class="p">):</span>
    <span class="n">self</span><span class="p">.</span><span class="n">kv_cache_manager</span><span class="p">.</span><span class="nf">free</span><span class="p">(</span><span class="n">request</span><span class="p">)</span>   <span class="c1"># 释放 KV 块
</span>    <span class="n">request</span><span class="p">.</span><span class="n">num_computed_tokens</span> <span class="o">=</span> <span class="mi">0</span>       <span class="c1"># 重置进度
</span>    <span class="n">request</span><span class="p">.</span><span class="n">status</span> <span class="o">=</span> <span class="n">RequestStatus</span><span class="p">.</span><span class="n">PREEMPTED</span>
    <span class="n">self</span><span class="p">.</span><span class="n">running</span><span class="p">.</span><span class="nf">remove</span><span class="p">(</span><span class="n">request</span><span class="p">)</span>
    <span class="n">self</span><span class="p">.</span><span class="n">waiting</span><span class="p">.</span><span class="nf">add</span><span class="p">(</span><span class="n">request</span><span class="p">)</span>             <span class="c1"># 回到等待队列
</span>    <span class="c1"># output_token_ids 保留：下次重新 prefill 时带上已生成的 output
</span></code></pre></div></div> <p>被抢占的请求下次调度时需要重新 prefill（prompt + 已生成的 output），代价较高。所以调度器有个 watermark 机制：只有 <code class="language-plaintext highlighter-rouge">free_blocks &gt; watermark</code> 时才允许新请求准入，提前留缓冲。</p> <p>有了 chunked prefill 和抢占机制，调度层的问题基本解决了。但 HTTP serving 还有一个更深的问题——Python GIL。</p> <hr/> <h2 id="7-异步架构asyncllm--zmq-前后端分离">7. 异步架构：AsyncLLM + ZMQ 前后端分离</h2> <p>离线推理用 <code class="language-plaintext highlighter-rouge">LLM</code> 类，HTTP serving 用 <code class="language-plaintext highlighter-rouge">AsyncLLM</code>（<code class="language-plaintext highlighter-rouge">vllm/v1/engine/async_llm.py</code>）。两者最大的区别是通信架构：</p> <p><img src="/assets/img/posts/20260706-vllm-v1-03-kvcache/async_arch.png" alt="AsyncLLM + ZMQ：为什么在线 Serving 要用独立进程？"/></p> <h3 id="71-为什么要独立进程">7.1 为什么要独立进程</h3> <p>Python GIL（Global Interpreter Lock，全局解释器锁）导致同一进程内的多个线程无法真正并行运行 Python 代码。HTTP server 是 asyncio 驱动的（IO 密集型），EngineCore 是调度 + GPU 执行（CPU/GPU 密集型），如果在同一进程里：</p> <ul> <li>asyncio 事件循环在等网络 IO 时，理论上应该让 EngineCore 跑</li> <li>但 EngineCore 里有大量 Python 逻辑（调度器、KV 块分配），会频繁争 GIL</li> <li>结果是两边都跑得磕磕绊绊</li> </ul> <p><strong>解法</strong>：把 EngineCore 放到独立进程，独立进程之间没有 GIL 的问题，两边真正并行。进程间通信用 ZMQ（ZeroMQ，一个高性能异步消息队列库，比 socket 封装更高层，支持 PUB/SUB、PUSH/PULL 等多种通信模式）+ msgpack（轻量序列化协议，比 pickle 快很多）。</p> <blockquote> <p><strong>多进程 vs 多线程：Python 的经典陷阱</strong></p> <p>CPython 的 GIL 让多线程在 CPU 密集型任务上几乎没有提速效果（IO 密集型可以，因为等 IO 时会释放 GIL）。真正要并行 CPU 密集型任务，就得用 <code class="language-plaintext highlighter-rouge">multiprocessing</code> 或者独立进程。vllm 这里的做法是业界标准解法，Celery、Gunicorn 等框架也是类似思路。</p> </blockquote> <h3 id="72-zmq-通信协议">7.2 ZMQ 通信协议</h3> <p>ZMQ 的消息格式：单字节类型标识 + msgpack payload</p> <table> <thead> <tr> <th>类型字节</th> <th>含义</th> <th>方向</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">\x00</code> ADD</td> <td>新请求</td> <td>前端 → EngineCore</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">\x01</code> ABORT</td> <td>中止请求</td> <td>前端 → EngineCore</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">\x05</code> WAKEUP</td> <td>有新请求，唤醒 EngineCore</td> <td>前端 → EngineCore</td> </tr> <tr> <td>(PUB)</td> <td>EngineCoreOutputs</td> <td>EngineCore → 前端</td> </tr> </tbody> </table> <p>EngineCore 用的是 ZMQ 的 PUB/SUB 模式：EngineCore 是 publisher，每完成一个 step 就 publish 这一步所有请求的新 token；前端是 subscriber，收到后立即 detokenize 并 yield 给 HTTP 层。</p> <h3 id="73-流式输出怎么实现">7.3 流式输出怎么实现</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># AsyncLLM.generate()  —  vllm/v1/engine/async_llm.py
</span><span class="k">async</span> <span class="k">def</span> <span class="nf">generate</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">prompt</span><span class="p">,</span> <span class="n">sampling_params</span><span class="p">,</span> <span class="n">request_id</span><span class="p">):</span>
    <span class="c1"># 发送请求到 EngineCore
</span>    <span class="k">await</span> <span class="n">self</span><span class="p">.</span><span class="n">engine_core</span><span class="p">.</span><span class="nf">add_request</span><span class="p">(</span><span class="nc">EngineCoreRequest</span><span class="p">(...))</span>
    
    <span class="c1"># 流式接收输出：每步完成就 yield
</span>    <span class="k">async</span> <span class="k">for</span> <span class="n">output</span> <span class="ow">in</span> <span class="n">self</span><span class="p">.</span><span class="n">_result_handler</span><span class="p">.</span><span class="nf">get_output</span><span class="p">(</span><span class="n">request_id</span><span class="p">):</span>
        <span class="k">yield</span> <span class="n">output</span>   <span class="c1"># 每生成一个 token 就推一次给 HTTP client
</span></code></pre></div></div> <p>HTTP server 用 SSE（Server-Sent Events）把每次 <code class="language-plaintext highlighter-rouge">yield</code> 的内容推给客户端，这就是 OpenAI API <code class="language-plaintext highlighter-rouge">stream=True</code> 时逐 token 输出的实现基础。<strong>每完成一个 step，EngineCore 通过 ZMQ PUB 推送输出；前端的 SUB socket 收到后立即 detokenize 并 yield 给 HTTP 层</strong>——整个链路没有轮询，完全事件驱动。</p> <hr/> <h2 id="8-阶段性总结vllm-v1-的设计哲学">8. 阶段性总结：vllm v1 的设计哲学</h2> <p>回顾前几篇，vllm v1 的核心设计思想可以浓缩成几句话：</p> <p><strong>关注点分离，层次清晰</strong>：用户层只做翻译（str ↔ token），调度层只做资源分配，执行层只做 GPU forward，模型层是标准 PyTorch。每层的接口是干净的，可以独立替换。</p> <p><strong>以复杂性换性能，但要值得</strong>：</p> <ul> <li><code class="language-plaintext highlighter-rouge">CachedRequestData</code> 差量传输 → Worker 侧状态管理变复杂，但序列化开销接近零</li> <li>execute/sample 拆成两步 → 代码变绕，但 GPU 和 CPU 真正并行</li> <li>独立进程 + ZMQ → 部署更复杂，但绕开 GIL 拿到真并行</li> </ul> <p><strong>用 OS 经典算法解 LLM 的新问题</strong>：PagedAttention 的块管理（LRU 驱逐、地址表）、Chunked Prefill 的 token budget 控制，本质上都是操作系统内存管理和调度算法在 LLM 推理场景下的重新应用。</p> <p><strong>遇到问题去哪找</strong>（完整版）：</p> <table> <thead> <tr> <th>问题场景</th> <th>代码位置</th> </tr> </thead> <tbody> <tr> <td>多 GPU 启动 / NCCL 报错</td> <td><code class="language-plaintext highlighter-rouge">vllm/v1/executor/multiproc_executor.py</code></td> </tr> <tr> <td>模型加载失败</td> <td><code class="language-plaintext highlighter-rouge">vllm/model_executor/model_loader.py</code></td> </tr> <tr> <td>显存不够 / KV Cache 分配失败</td> <td><code class="language-plaintext highlighter-rouge">EngineCore._initialize_kv_caches()</code></td> </tr> <tr> <td>平台不兼容 / worker_cls 找不到</td> <td><code class="language-plaintext highlighter-rouge">vllm/platforms/</code> 对应平台文件</td> </tr> <tr> <td>OOM during forward</td> <td><code class="language-plaintext highlighter-rouge">GPUModelRunner.execute_model()</code>，检查 max_num_batched_tokens</td> </tr> <tr> <td>Sampling 结果异常</td> <td><code class="language-plaintext highlighter-rouge">vllm/v1/sample/sampler.py</code></td> </tr> <tr> <td>输出乱码</td> <td><code class="language-plaintext highlighter-rouge">IncrementalDetokenizer</code>，检查 tokenizer 匹配</td> </tr> <tr> <td>Attention 报错</td> <td><code class="language-plaintext highlighter-rouge">vllm/v1/attention/backends/flash_attn.py</code></td> </tr> <tr> <td>HTTP server 延迟高 / 吞吐低</td> <td><code class="language-plaintext highlighter-rouge">AsyncLLM</code> 的 ZMQ 通信，检查 EngineCore 进程是否正常</td> </tr> <tr> <td>Chunked prefill 相关</td> <td><code class="language-plaintext highlighter-rouge">vllm/v1/core/sched/scheduler.py</code>，检查 <code class="language-plaintext highlighter-rouge">max_num_batched_tokens</code> 和 <code class="language-plaintext highlighter-rouge">enable_chunked_prefill</code></td> </tr> <tr> <td>前缀缓存没有命中</td> <td><code class="language-plaintext highlighter-rouge">KVCacheManager.get_computed_blocks()</code>，检查 cache_salt 和 block_hashes 计算</td> </tr> </tbody> </table> <hr/> <blockquote> <p>如果这篇文章涉及的 KV Cache 管理和 LLM 推理效率优化你想系统深入，可以看看我之前出版的<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，书里 LLM 推理效率那章和本文讲的系统设计有直接关联，感兴趣可以翻翻。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="vllm"/><category term="LLM推理"/><category term="KV Cache"/><category term="PagedAttention"/><category term="源码分析"/><summary type="html"><![CDATA[vllm v1 源码精读（三）：KV Cache 管理、Chunked Prefill 与异步架构]]></summary></entry><entry><title type="html">vllm v1 源码解析（四）：插件系统——用 Python entry_points 实现零侵入扩展</title><link href="https://marsggbo.github.io/blog/2026/20260706-vllm-v1-04-plugins/" rel="alternate" type="text/html" title="vllm v1 源码解析（四）：插件系统——用 Python entry_points 实现零侵入扩展"/><published>2026-07-06T00:00:00+00:00</published><updated>2026-07-06T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260706-vllm-v1-04-plugins</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260706-vllm-v1-04-plugins/"><![CDATA[<h1 id="vllm-v1-源码解析四插件系统用-python-entry_points-实现零侵入扩展">vllm v1 源码解析（四）：插件系统——用 Python entry_points 实现零侵入扩展</h1> <blockquote> <p><strong>本文基于 vllm <code class="language-plaintext highlighter-rouge">ba22152</code>（2026-07-06），源码链接均指向该 commit 的固定行号。</strong> 代码浏览入口：<a href="https://github.com/vllm-project/vllm/tree/ba22152096b2484faa3579624a253d54804d876d">github.com/vllm-project/vllm/tree/ba22152</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有想过，如果你搞了一块新的 AI 芯片，或者写了一套自定义的 attention kernel，想接进 vllm，该怎么办？</p> <p>早期的做法是 fork vllm 源码，改完自己维护一个 branch，然后每次 vllm 上游更新，你就头疼一次。这条路有多难走，搞过 CUDA-specific 分支的人都懂。</p> <p>vllm v1 给出了另一种答案：<strong>插件系统</strong>。入口在 <code class="language-plaintext highlighter-rouge">vllm/plugins/__init__.py</code>，核心是两行代码：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># load_general_plugins()  —  vllm/plugins/__init__.py
</span><span class="k">def</span> <span class="nf">load_general_plugins</span><span class="p">():</span>
    <span class="k">for</span> <span class="n">plugin</span> <span class="ow">in</span> <span class="nf">entry_points</span><span class="p">(</span><span class="n">group</span><span class="o">=</span><span class="sh">"</span><span class="s">vllm.general_plugins</span><span class="sh">"</span><span class="p">):</span>
        <span class="n">func</span> <span class="o">=</span> <span class="n">plugin</span><span class="p">.</span><span class="nf">load</span><span class="p">()</span>   <span class="c1"># 触发 import，拿到注册函数
</span>        <span class="nf">func</span><span class="p">()</span>                 <span class="c1"># 执行注册（向 ModelRegistry / LoRARegistry 注册）
</span></code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">entry_points</code> 是 Python 标准库 <code class="language-plaintext highlighter-rouge">importlib.metadata</code>（3.9+ 内置）的 API——你写一个独立包，在 <code class="language-plaintext highlighter-rouge">pyproject.toml</code> 里声明几行，<code class="language-plaintext highlighter-rouge">pip install</code> 之后 vllm 启动时自动发现加载，<strong>vllm 不需要 <code class="language-plaintext highlighter-rouge">import</code> 你的包，甚至不知道你的包存在</strong>。</p> <p>注册函数里传入的不是类本身，而是字符串 qualname：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># my_vllm_plugin/__init__.py
</span><span class="k">def</span> <span class="nf">register</span><span class="p">():</span>
    <span class="n">ModelRegistry</span><span class="p">.</span><span class="nf">register_model</span><span class="p">(</span>
        <span class="sh">"</span><span class="s">MyCustomLlama</span><span class="sh">"</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">my_vllm_plugin.my_model:MyCustomLlama</span><span class="sh">"</span>  <span class="c1"># ← 字符串，不是类引用
</span>    <span class="p">)</span>
</code></pre></div></div> <p>为什么要用字符串？因为 vllm 是多进程架构，如果在主进程 import 期间触发了 <code class="language-plaintext highlighter-rouge">import MyCustomLlama</code>，而那个模块里有 <code class="language-plaintext highlighter-rouge">torch.cuda.*</code> 调用，fork 出来的 Worker 进程会继承被”污染”的 CUDA 状态，后续行为是 undefined。字符串是零 import、零副作用，等 Worker 进程真正要实例化时才通过 <code class="language-plaintext highlighter-rouge">resolve_obj_by_qualname</code> 把字符串变成类（第 5 节详述）。</p> <p>这两层设计加在一起，让你不改 vllm 一行代码就能扩展它。插件系统定义了四类扩展点：</p> <table> <thead> <tr> <th>entry_point group</th> <th>用途</th> <th>加载位置</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">vllm.general_plugins</code></td> <td>注册模型/LoRA resolver 等</td> <td>每个进程启动时（<code class="language-plaintext highlighter-rouge">EngineCore</code> + 每个 <code class="language-plaintext highlighter-rouge">Worker</code>）</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">vllm.platform_plugins</code></td> <td>自定义硬件平台</td> <td>首次访问 <code class="language-plaintext highlighter-rouge">current_platform</code> 时（懒加载）</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">vllm.stat_logger_plugins</code></td> <td>自定义指标上报</td> <td><code class="language-plaintext highlighter-rouge">AsyncLLM.__init__()</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">vllm.io_processor_plugins</code></td> <td>扩展输入输出处理</td> <td>Process 0 启动时</td> </tr> </tbody> </table> <p>这篇就把这套机制彻底说清楚：Python 层面的原理是什么，每个 group 各是什么用途，字符串注册为什么能解决 CUDA 初始化的问题，以及几个实际可用的例子。</p> <hr/> <h2 id="2-python-entry_points标准库里的插件总线">2. Python entry_points：标准库里的”插件总线”</h2> <p>先把底层机制搞清楚，后面看 vllm 的代码才不会懵。</p> <h3 id="21-entry_points-是什么">2.1 entry_points 是什么</h3> <p><code class="language-plaintext highlighter-rouge">entry_points</code> 是 Python packaging 生态的一个标准机制，用来让一个安装包”向外广播”自己提供了某些可调用的东西。它不是某个第三方库发明的，而是 <code class="language-plaintext highlighter-rouge">importlib.metadata</code>（Python 3.9+ 内置）直接支持的标准 API。</p> <p>工作原理很简单：当你 <code class="language-plaintext highlighter-rouge">pip install my-package</code> 时，pip 会把 <code class="language-plaintext highlighter-rouge">pyproject.toml</code> 里的 entry_points 配置写进 <code class="language-plaintext highlighter-rouge">site-packages/my_package-*.dist-info/entry_points.txt</code>。之后任何程序都可以调用 <code class="language-plaintext highlighter-rouge">importlib.metadata.entry_points(group='xxx')</code> 来枚举当前环境里所有声明了这个 group 的包。</p> <p>如下图，整个流程是：</p> <p><img src="/assets/img/posts/20260706-vllm-v1-04-plugins/entrypoints_flow.png" alt="entry_points 发现机制"/></p> <h3 id="22-怎么声明一个-entry_point">2.2 怎么声明一个 entry_point</h3> <p>在 <code class="language-plaintext highlighter-rouge">pyproject.toml</code> 里这样写：</p> <div class="language-toml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">[</span><span class="n">project</span><span class="k">.</span><span class="n">entry-points</span><span class="k">.</span><span class="s">"vllm.general_plugins"</span><span class="k">]</span>
<span class="n">register_my_model</span> <span class="o">=</span><span class="w"> </span><span class="s">"my_vllm_plugin.models:register"</span>
</code></pre></div></div> <p>格式是 <code class="language-plaintext highlighter-rouge">entry_point_name = "package.module:callable"</code>。这条配置的意思是：在 <code class="language-plaintext highlighter-rouge">vllm.general_plugins</code> 这个 group 下，我提供了一个叫 <code class="language-plaintext highlighter-rouge">register_my_model</code> 的 entry point，对应的是 <code class="language-plaintext highlighter-rouge">my_vllm_plugin.models</code> 模块里的 <code class="language-plaintext highlighter-rouge">register</code> 函数。</p> <h3 id="23-消费方怎么加载">2.3 消费方怎么加载</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">importlib.metadata</span> <span class="kn">import</span> <span class="n">entry_points</span>

<span class="k">for</span> <span class="n">ep</span> <span class="ow">in</span> <span class="nf">entry_points</span><span class="p">(</span><span class="n">group</span><span class="o">=</span><span class="sh">'</span><span class="s">vllm.general_plugins</span><span class="sh">'</span><span class="p">):</span>
    <span class="n">func</span> <span class="o">=</span> <span class="n">ep</span><span class="p">.</span><span class="nf">load</span><span class="p">()</span>   <span class="c1"># 触发 import，拿到 callable
</span>    <span class="nf">func</span><span class="p">()</span>             <span class="c1"># 执行注册
</span></code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">ep.load()</code> 做的事其实就是 <code class="language-plaintext highlighter-rouge">importlib.import_module('my_vllm_plugin.models')</code> + <code class="language-plaintext highlighter-rouge">getattr(module, 'register')</code>。整个过程完全 lazy——只有真的调用 <code class="language-plaintext highlighter-rouge">load()</code> 时，目标模块才会被 import。</p> <p><strong>这种机制的关键价值</strong>：主程序（vllm）和插件包完全解耦。vllm 不需要 <code class="language-plaintext highlighter-rouge">import my_plugin</code>，不需要知道插件的存在；插件包安装后，通过 Python packaging 的元数据自动”注册”进来。这是真正意义上的<strong>依赖倒置</strong>——vllm 依赖的是抽象接口，而不是任何具体的扩展实现。</p> <p>底层机制清楚了，下面看 vllm 具体定义了哪些扩展点。</p> <hr/> <h2 id="3-vllm-的四类插件-group">3. vllm 的四类插件 group</h2> <p>vllm 定义了四个 entry_point group，覆盖了从模型注册到硬件平台到指标上报的不同扩展点：</p> <p><img src="/assets/img/posts/20260706-vllm-v1-04-plugins/plugin_types_overview.png" alt="四类插件全景"/></p> <h3 id="31-vllmgeneral_plugins最通用的钩子">3.1 <code class="language-plaintext highlighter-rouge">vllm.general_plugins</code>：最通用的钩子</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># vllm/plugins/__init__.py
</span><span class="n">DEFAULT_PLUGINS_GROUP</span>  <span class="o">=</span> <span class="sh">"</span><span class="s">vllm.general_plugins</span><span class="sh">"</span>
<span class="n">PLATFORM_PLUGINS_GROUP</span> <span class="o">=</span> <span class="sh">"</span><span class="s">vllm.platform_plugins</span><span class="sh">"</span>
</code></pre></div></div> <p>这是最常用的 group。<strong>每个 vllm 进程启动时都会执行</strong>，包括 Engine Core 进程、每个 Worker 进程、以及用于模型检查的 subprocess。</p> <p>对应函数签名很简单：不接受参数，不需要返回值（返回值被忽略）。主要用来做注册类操作：向 <code class="language-plaintext highlighter-rouge">ModelRegistry</code> 注册自定义模型、向 <code class="language-plaintext highlighter-rouge">LoRAResolverRegistry</code> 注册 LoRA 解析器等。</p> <p>vllm 自己的两个内置 general plugin 就很典型：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># pyproject.toml（vllm 自己的）
</span><span class="p">[</span><span class="n">project</span><span class="p">.</span><span class="n">entry</span><span class="o">-</span><span class="n">points</span><span class="p">.</span><span class="sh">"</span><span class="s">vllm.general_plugins</span><span class="sh">"</span><span class="p">]</span>
<span class="n">lora_filesystem_resolver</span> <span class="o">=</span> <span class="sh">"</span><span class="s">vllm.plugins.lora_resolvers.filesystem_resolver:register_filesystem_resolver</span><span class="sh">"</span>
<span class="n">lora_hf_hub_resolver</span> <span class="o">=</span> <span class="sh">"</span><span class="s">vllm.plugins.lora_resolvers.hf_hub_resolver:register_hf_hub_resolver</span><span class="sh">"</span>
</code></pre></div></div> <p>注意 <code class="language-plaintext highlighter-rouge">lora_hf_hub_resolver</code> 有个安全设计：它在函数内部额外检查自己是否在 <code class="language-plaintext highlighter-rouge">VLLM_PLUGINS</code> 白名单里才注册——因为这个 resolver 会从 HuggingFace Hub 下载文件，默认需要用户显式 opt-in。</p> <h3 id="32-vllmplatform_plugins自定义硬件平台">3.2 <code class="language-plaintext highlighter-rouge">vllm.platform_plugins</code>：自定义硬件平台</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">PLATFORM_PLUGINS_GROUP</span> <span class="o">=</span> <span class="sh">"</span><span class="s">vllm.platform_plugins</span><span class="sh">"</span>
</code></pre></div></div> <p>这是最复杂也最有价值的 group，专门给硬件厂商用。比如你造了一块新的 AI 芯片，想让 vllm 能在上面跑，就得实现一个 <code class="language-plaintext highlighter-rouge">Platform</code> 子类并用这个 group 注册。</p> <p><strong>两个特殊设计值得注意</strong>：</p> <p><strong>1. 懒加载（lazy）</strong></p> <p>platform 插件不是在进程启动时立即加载，而是在<strong>首次访问 <code class="language-plaintext highlighter-rouge">current_platform</code> 时</strong>才触发。实现方式是 <code class="language-plaintext highlighter-rouge">vllm/platforms/__init__.py</code> 里的 <code class="language-plaintext highlighter-rouge">__getattr__</code>：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># vllm/platforms/__init__.py  __getattr__()（懒加载实现）
</span><span class="k">def</span> <span class="nf">__getattr__</span><span class="p">(</span><span class="n">name</span><span class="p">:</span> <span class="nb">str</span><span class="p">):</span>
    <span class="k">if</span> <span class="n">name</span> <span class="o">==</span> <span class="sh">"</span><span class="s">current_platform</span><span class="sh">"</span><span class="p">:</span>
        <span class="k">global</span> <span class="n">_current_platform</span>
        <span class="k">if</span> <span class="n">_current_platform</span> <span class="ow">is</span> <span class="bp">None</span><span class="p">:</span>
            <span class="n">cls_name</span> <span class="o">=</span> <span class="nf">resolve_current_platform_cls_qualname</span><span class="p">()</span>
            <span class="n">_current_platform</span> <span class="o">=</span> <span class="nf">resolve_obj_by_qualname</span><span class="p">(</span><span class="n">cls_name</span><span class="p">)()</span>
        <span class="k">return</span> <span class="n">_current_platform</span>
</code></pre></div></div> <p>为什么要懒加载？因为你的 Platform 插件包需要 <code class="language-plaintext highlighter-rouge">from vllm.platforms import Platform</code> 来继承基类。如果 <code class="language-plaintext highlighter-rouge">current_platform</code> 在模块 import 时就立即解析，那 vllm.platforms 还没加载完，你就来 import，循环依赖直接报错。懒加载把这个时机推迟到真正用到的时候，绕开了循环 import 的坑。</p> <p><strong>2. OOT（Out-of-Tree）插件优先于内置平台</strong></p> <p>vllm 的规则是：如果有 OOT platform 插件激活了，它优先于 CUDA/ROCm/TPU 等内置平台。这让硬件厂商可以完全接管平台检测逻辑，不需要 patch 内置代码。</p> <p>实现你自己的 platform 插件大致长这样：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># my_accelerator/platform.py
</span><span class="kn">from</span> <span class="n">vllm.platforms</span> <span class="kn">import</span> <span class="n">Platform</span><span class="p">,</span> <span class="n">PlatformEnum</span>

<span class="k">class</span> <span class="nc">MyAcceleratorPlatform</span><span class="p">(</span><span class="n">Platform</span><span class="p">):</span>
    <span class="n">_enum</span> <span class="o">=</span> <span class="n">PlatformEnum</span><span class="p">.</span><span class="n">OOT</span>          <span class="c1"># 必须设置
</span>    <span class="n">device_type</span> <span class="o">=</span> <span class="sh">"</span><span class="s">my_acc</span><span class="sh">"</span>
    <span class="n">device_name</span> <span class="o">=</span> <span class="sh">"</span><span class="s">MyAccelerator</span><span class="sh">"</span>

    <span class="nd">@classmethod</span>
    <span class="k">def</span> <span class="nf">check_and_update_config</span><span class="p">(</span><span class="n">cls</span><span class="p">,</span> <span class="n">vllm_config</span><span class="p">):</span>
        <span class="c1"># 必须设置 worker_cls，告诉 vllm 用哪个 Worker
</span>        <span class="n">vllm_config</span><span class="p">.</span><span class="n">parallel_config</span><span class="p">.</span><span class="n">worker_cls</span> <span class="o">=</span> \
            <span class="sh">"</span><span class="s">my_accelerator.worker.MyAcceleratorWorker</span><span class="sh">"</span>

    <span class="nd">@classmethod</span>
    <span class="k">def</span> <span class="nf">get_attn_backend_cls</span><span class="p">(</span><span class="n">cls</span><span class="p">,</span> <span class="n">selected_backend</span><span class="p">,</span> <span class="n">attn_selector_config</span><span class="p">,</span> <span class="n">num_heads</span><span class="p">):</span>
        <span class="k">return</span> <span class="sh">"</span><span class="s">my_accelerator.attention.MyFlashAttn</span><span class="sh">"</span>

<span class="c1"># my_accelerator/__init__.py
</span><span class="k">def</span> <span class="nf">probe</span><span class="p">():</span>
    <span class="sh">"""</span><span class="s">返回 Platform 类的 qualname，如果不适用就返回 None</span><span class="sh">"""</span>
    <span class="kn">import</span> <span class="n">os</span>
    <span class="k">if</span> <span class="n">os</span><span class="p">.</span><span class="n">path</span><span class="p">.</span><span class="nf">exists</span><span class="p">(</span><span class="sh">"</span><span class="s">/dev/my_acc0</span><span class="sh">"</span><span class="p">):</span>   <span class="c1"># 检测硬件是否存在
</span>        <span class="k">return</span> <span class="sh">"</span><span class="s">my_accelerator.platform.MyAcceleratorPlatform</span><span class="sh">"</span>
    <span class="k">return</span> <span class="bp">None</span>
</code></pre></div></div> <div class="language-toml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># pyproject.toml</span>
<span class="k">[</span><span class="n">project</span><span class="k">.</span><span class="n">entry-points</span><span class="k">.</span><span class="s">"vllm.platform_plugins"</span><span class="k">]</span>
<span class="n">my_accelerator</span> <span class="o">=</span><span class="w"> </span><span class="s">"my_accelerator:probe"</span>
</code></pre></div></div> <h3 id="33-vllmstat_logger_plugins自定义指标上报">3.3 <code class="language-plaintext highlighter-rouge">vllm.stat_logger_plugins</code>：自定义指标上报</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">STAT_LOGGER_PLUGINS_GROUP</span> <span class="o">=</span> <span class="sh">"</span><span class="s">vllm.stat_logger_plugins</span><span class="sh">"</span>
</code></pre></div></div> <p>只在 Process 0（Engine Core / AsyncLLM）里加载，因为指标收集是 serving 层的事，Worker 进程不需要。</p> <p>这个 group 的 entry point 要直接指向一个<strong>类</strong>（不是函数），该类必须继承 <code class="language-plaintext highlighter-rouge">StatLoggerBase</code>：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># load_stat_logger_plugin_factories()  —  vllm/v1/metrics/loggers.py
</span><span class="k">def</span> <span class="nf">load_stat_logger_plugin_factories</span><span class="p">():</span>
    <span class="k">for</span> <span class="n">name</span><span class="p">,</span> <span class="n">plugin_class</span> <span class="ow">in</span> <span class="nf">load_plugins_by_group</span><span class="p">(</span><span class="n">STAT_LOGGER_PLUGINS_GROUP</span><span class="p">).</span><span class="nf">items</span><span class="p">():</span>
        <span class="k">if</span> <span class="ow">not</span> <span class="nf">isinstance</span><span class="p">(</span><span class="n">plugin_class</span><span class="p">,</span> <span class="nb">type</span><span class="p">)</span> <span class="ow">or</span> \
           <span class="ow">not</span> <span class="nf">issubclass</span><span class="p">(</span><span class="n">plugin_class</span><span class="p">,</span> <span class="n">StatLoggerBase</span><span class="p">):</span>
            <span class="k">raise</span> <span class="nc">TypeError</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">stat logger plugin </span><span class="si">{</span><span class="n">name</span><span class="si">}</span><span class="s"> must subclass StatLoggerBase</span><span class="sh">"</span><span class="p">)</span>
        <span class="n">factories</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">plugin_class</span><span class="p">)</span>
</code></pre></div></div> <p>典型用途：把 vllm 的吞吐量、排队请求数、GPU 利用率等指标接到你的 Prometheus / OpenTelemetry / 内部监控系统。</p> <h3 id="34-vllmio_processor_pluginsio-处理扩展">3.4 <code class="language-plaintext highlighter-rouge">vllm.io_processor_plugins</code>：I/O 处理扩展</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">IO_PROCESSOR_PLUGINS_GROUP</span> <span class="o">=</span> <span class="sh">"</span><span class="s">vllm.io_processor_plugins</span><span class="sh">"</span>
</code></pre></div></div> <p>和 general plugins 类似，但只在 Process 0 执行。主要用于扩展输入输出处理逻辑，比如自定义 multimodal 输入的预处理、自定义 tokenization 前置逻辑等。</p> <p>四个 group 各司其职，但它们有一个共同的加载时序问题——vllm 是多进程架构，这件事值得单独说清楚。</p> <hr/> <h2 id="4-插件加载的多进程时序">4. 插件加载的多进程时序</h2> <p>这是一个容易踩坑的地方——<strong>vllm 是多进程架构，每个进程都要独立加载插件</strong>。不是”加载一次，所有进程共享”。</p> <p><img src="/assets/img/posts/20260706-vllm-v1-04-plugins/plugin_load_timing.png" alt="多进程插件加载时序"/></p> <p>具体调用位置：</p> <table> <thead> <tr> <th>调用位置</th> <th>文件</th> <th>进程</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">EngineCore.__init__</code></td> <td><code class="language-plaintext highlighter-rouge">vllm/v1/engine/core.py</code></td> <td>Process 0</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">WorkerBase.__init__</code></td> <td><code class="language-plaintext highlighter-rouge">vllm/v1/worker/worker_base.py</code></td> <td>每个 Worker</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">_run()</code> 入口函数</td> <td><code class="language-plaintext highlighter-rouge">vllm/model_executor/models/registry.py</code></td> <td>模型检查 subprocess</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">AsyncLLM.__init__</code></td> <td><code class="language-plaintext highlighter-rouge">vllm/v1/engine/async_llm.py</code></td> <td>Process 0（stat logger）</td> </tr> </tbody> </table> <p><code class="language-plaintext highlighter-rouge">load_general_plugins()</code> 用一个模块级 flag <code class="language-plaintext highlighter-rouge">plugins_loaded: bool</code> 保证在同一进程里只执行一次——这叫幂等性（idempotency）。但因为 fork 出来的子进程是全新的 Python 解释器，flag 是 <code class="language-plaintext highlighter-rouge">False</code>，所以每个 Worker 都会独立走一遍加载流程。</p> <p><strong>插件加载失败不会崩溃</strong>。vllm 的处理是：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># load_general_plugins()  —  vllm/plugins/__init__.py（精简）
</span><span class="k">try</span><span class="p">:</span>
    <span class="n">result</span> <span class="o">=</span> <span class="n">plugin</span><span class="p">.</span><span class="nf">load</span><span class="p">()</span>
    <span class="nf">func</span><span class="p">()</span>
<span class="k">except</span> <span class="nb">Exception</span><span class="p">:</span>
    <span class="n">logger</span><span class="p">.</span><span class="nf">exception</span><span class="p">(</span><span class="sh">"</span><span class="s">Failed to load plugin %s</span><span class="sh">"</span><span class="p">,</span> <span class="n">plugin</span><span class="p">.</span><span class="n">name</span><span class="p">)</span>
    <span class="c1"># 继续，不 raise
</span></code></pre></div></div> <p>这个设计是刻意的：一个有 bug 的插件不应该让整个 vllm 起不来。但也意味着你要检查日志——插件没生效，可能只是静默失败了。</p> <p>加载时序搞清楚了，还有一个问题：为什么 vllm 的注册到处是字符串而不是类引用？这背后有个很有意思的工程设计。</p> <hr/> <h2 id="5-字符串注册--resolve_obj_by_qualname插件安全的核心">5. 字符串注册 + resolve_obj_by_qualname：插件安全的核心</h2> <p>vllm 的注册系统里到处都是字符串而不是直接的类引用，比如：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">ModelRegistry</span><span class="p">.</span><span class="nf">register_model</span><span class="p">(</span>
    <span class="sh">"</span><span class="s">MyLlava</span><span class="sh">"</span><span class="p">,</span>
    <span class="sh">"</span><span class="s">my_pkg.models:MyLlava</span><span class="sh">"</span>   <span class="c1"># ← 字符串，不是 MyLlava 类本身
</span><span class="p">)</span>
</code></pre></div></div> <p>为什么这样设计？</p> <p><img src="/assets/img/posts/20260706-vllm-v1-04-plugins/string_registry.png" alt="字符串注册的原因"/></p> <p><strong>根本原因是 multiprocessing 和 CUDA 的兼容性</strong>。</p> <p>vllm 用 <code class="language-plaintext highlighter-rouge">multiprocessing.Process</code> fork 出 Worker 进程（或 spawn，取决于配置）。如果在主进程 import 期间就 <code class="language-plaintext highlighter-rouge">import MyLlava</code>，而 <code class="language-plaintext highlighter-rouge">MyLlava</code> 的模块代码里触发了 CUDA 初始化（比如 <code class="language-plaintext highlighter-rouge">torch.cuda.is_available()</code>、<code class="language-plaintext highlighter-rouge">torch.device("cuda:0")</code> 等），那这个 CUDA context 就被带进了主进程。fork 之后，Worker 继承到的是一个已经被”污染”的 CUDA 状态，后续行为是 undefined。</p> <p><strong>字符串注册 = 零 import、零副作用。</strong> 等 Worker 进程里真正要实例化模型时，再通过 <code class="language-plaintext highlighter-rouge">resolve_obj_by_qualname</code> 把字符串变成类：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># resolve_obj_by_qualname()  —  vllm/utils/import_utils.py
</span><span class="k">def</span> <span class="nf">resolve_obj_by_qualname</span><span class="p">(</span><span class="n">qualname</span><span class="p">:</span> <span class="nb">str</span><span class="p">):</span>
    <span class="n">module_name</span><span class="p">,</span> <span class="n">obj_name</span> <span class="o">=</span> <span class="n">qualname</span><span class="p">.</span><span class="nf">rsplit</span><span class="p">(</span><span class="sh">"</span><span class="s">.</span><span class="sh">"</span><span class="p">,</span> <span class="mi">1</span><span class="p">)</span>
    <span class="n">module</span> <span class="o">=</span> <span class="n">importlib</span><span class="p">.</span><span class="nf">import_module</span><span class="p">(</span><span class="n">module_name</span><span class="p">)</span>
    <span class="k">return</span> <span class="nf">getattr</span><span class="p">(</span><span class="n">module</span><span class="p">,</span> <span class="n">obj_name</span><span class="p">)</span>
</code></pre></div></div> <p>这个 helper 在整个 vllm 代码库里被大量复用——Worker class、Platform class、Attention backend class 全都是这个模式。<strong>看到字符串 qualname，就知道这里做了刻意的延迟 import。</strong></p> <p>机制讲清楚了，来几个实际能跑的例子。</p> <hr/> <h2 id="6-实战写一个自定义模型插件">6. 实战：写一个自定义模型插件</h2> <p>来个具体例子，写一个最小可用的 general plugin，把自定义模型注册进 vllm。</p> <h3 id="61-项目结构">6.1 项目结构</h3> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>my_vllm_plugin/
├── pyproject.toml
└── my_vllm_plugin/
    ├── __init__.py       # 注册函数在这里
    └── my_model.py       # 自定义模型
</code></pre></div></div> <h3 id="62-模型实现">6.2 模型实现</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># my_vllm_plugin/my_model.py
</span><span class="kn">import</span> <span class="n">torch</span>
<span class="kn">import</span> <span class="n">torch.nn</span> <span class="k">as</span> <span class="n">nn</span>
<span class="kn">from</span> <span class="n">vllm.model_executor.models.llama</span> <span class="kn">import</span> <span class="n">LlamaForCausalLM</span>

<span class="k">class</span> <span class="nc">MyCustomLlama</span><span class="p">(</span><span class="n">LlamaForCausalLM</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">在 LLaMA 基础上加了一些自定义逻辑</span><span class="sh">"""</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="o">*</span><span class="n">args</span><span class="p">,</span> <span class="o">**</span><span class="n">kwargs</span><span class="p">):</span>
        <span class="c1"># 这里可以加 pre/post-processing
</span>        <span class="k">return</span> <span class="nf">super</span><span class="p">().</span><span class="nf">forward</span><span class="p">(</span><span class="o">*</span><span class="n">args</span><span class="p">,</span> <span class="o">**</span><span class="n">kwargs</span><span class="p">)</span>
</code></pre></div></div> <h3 id="63-注册函数">6.3 注册函数</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># my_vllm_plugin/__init__.py
</span><span class="k">def</span> <span class="nf">register</span><span class="p">():</span>
    <span class="kn">from</span> <span class="n">vllm</span> <span class="kn">import</span> <span class="n">ModelRegistry</span>

    <span class="c1"># 检查防止重复注册
</span>    <span class="k">if</span> <span class="sh">"</span><span class="s">MyCustomLlama</span><span class="sh">"</span> <span class="ow">not</span> <span class="ow">in</span> <span class="n">ModelRegistry</span><span class="p">.</span><span class="nf">get_supported_archs</span><span class="p">():</span>
        <span class="n">ModelRegistry</span><span class="p">.</span><span class="nf">register_model</span><span class="p">(</span>
            <span class="sh">"</span><span class="s">MyCustomLlama</span><span class="sh">"</span><span class="p">,</span>
            <span class="sh">"</span><span class="s">my_vllm_plugin.my_model:MyCustomLlama</span><span class="sh">"</span>
            <span class="c1"># ↑ 字符串，不是直接传类！
</span>        <span class="p">)</span>
</code></pre></div></div> <h3 id="64-pyprojecttoml">6.4 pyproject.toml</h3> <div class="language-toml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">[</span><span class="n">build-system</span><span class="k">]</span>
<span class="n">requires</span> <span class="o">=</span><span class="w"> </span><span class="p">[</span><span class="s">"setuptools"</span><span class="p">]</span>
<span class="n">build-backend</span> <span class="o">=</span><span class="w"> </span><span class="s">"setuptools.backends.legacy:build"</span>

<span class="k">[</span><span class="n">project</span><span class="k">]</span>
<span class="n">name</span> <span class="o">=</span><span class="w"> </span><span class="s">"my-vllm-plugin"</span>
<span class="n">version</span> <span class="o">=</span><span class="w"> </span><span class="s">"0.1.0"</span>
<span class="n">dependencies</span> <span class="o">=</span><span class="w"> </span><span class="p">[</span><span class="s">"vllm"</span><span class="p">]</span>

<span class="k">[</span><span class="n">project</span><span class="k">.</span><span class="n">entry-points</span><span class="k">.</span><span class="s">"vllm.general_plugins"</span><span class="k">]</span>
<span class="n">register_my_custom_llama</span> <span class="o">=</span><span class="w"> </span><span class="s">"my_vllm_plugin:register"</span>
</code></pre></div></div> <h3 id="65-使用">6.5 使用</h3> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip <span class="nb">install</span> <span class="nt">-e</span> <span class="nb">.</span>   <span class="c"># 开发模式安装，写入 entry_points.txt</span>
</code></pre></div></div> <p>然后直接用：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">vllm</span> <span class="kn">import</span> <span class="n">LLM</span>

<span class="c1"># HuggingFace config.json 里 architectures = ["MyCustomLlama"]
</span><span class="n">llm</span> <span class="o">=</span> <span class="nc">LLM</span><span class="p">(</span><span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">./my-custom-model/</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <p><strong>vllm 启动时自动发现插件、调用 register()、把 “MyCustomLlama” 注册进 ModelRegistry</strong>，后续模型加载就走正常流程了。你没有改 vllm 任何一行代码。</p> <hr/> <h2 id="7-实战自定义-attention-backend">7. 实战：自定义 Attention Backend</h2> <p>除了模型，attention kernel 也可以通过插件替换。这对做 attention 算法研究的人来说很有用——不需要 fork vllm 就能测自己的 kernel。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># my_attention_plugin/__init__.py
</span><span class="kn">from</span> <span class="n">vllm.v1.attention.backends.registry</span> <span class="kn">import</span> <span class="p">(</span>
    <span class="n">register_backend</span><span class="p">,</span> <span class="n">AttentionBackendEnum</span>
<span class="p">)</span>

<span class="k">def</span> <span class="nf">register</span><span class="p">():</span>
    <span class="c1"># 注册为 CUSTOM backend
</span>    <span class="nf">register_backend</span><span class="p">(</span>
        <span class="n">AttentionBackendEnum</span><span class="p">.</span><span class="n">CUSTOM</span><span class="p">,</span>
        <span class="sh">"</span><span class="s">my_attention_plugin.kernel:MyFlashAttnBackend</span><span class="sh">"</span>
    <span class="p">)</span>
</code></pre></div></div> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># my_attention_plugin/kernel.py
</span><span class="kn">from</span> <span class="n">vllm.attention.backends.abstract</span> <span class="kn">import</span> <span class="n">AttentionBackend</span><span class="p">,</span> <span class="n">AttentionMetadata</span>

<span class="k">class</span> <span class="nc">MyFlashAttnBackend</span><span class="p">(</span><span class="n">AttentionBackend</span><span class="p">):</span>
    <span class="nd">@staticmethod</span>
    <span class="k">def</span> <span class="nf">get_name</span><span class="p">()</span> <span class="o">-&gt;</span> <span class="nb">str</span><span class="p">:</span>
        <span class="k">return</span> <span class="sh">"</span><span class="s">my_flash_attn</span><span class="sh">"</span>

    <span class="nd">@staticmethod</span>
    <span class="k">def</span> <span class="nf">get_impl_cls</span><span class="p">():</span>
        <span class="k">return</span> <span class="n">MyFlashAttnImpl</span>

<span class="k">class</span> <span class="nc">MyFlashAttnImpl</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">query</span><span class="p">,</span> <span class="n">key</span><span class="p">,</span> <span class="n">value</span><span class="p">,</span> <span class="n">attn_metadata</span><span class="p">,</span> <span class="p">...):</span>
        <span class="c1"># 你自己的 attention 实现
</span>        <span class="bp">...</span>
</code></pre></div></div> <p>启动时指定使用 CUSTOM backend：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">VLLM_ATTENTION_BACKEND</span><span class="o">=</span>CUSTOM python your_script.py
</code></pre></div></div> <hr/> <h2 id="8-实战自定义指标上报">8. 实战：自定义指标上报</h2> <p>如果你在内部有自己的监控系统，可以用 <code class="language-plaintext highlighter-rouge">vllm.stat_logger_plugins</code> 把 vllm 的运行指标推进去：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># my_metrics_plugin/logger.py
</span><span class="kn">from</span> <span class="n">vllm.v1.metrics.loggers</span> <span class="kn">import</span> <span class="n">StatLoggerBase</span>

<span class="k">class</span> <span class="nc">MyPrometheusLogger</span><span class="p">(</span><span class="n">StatLoggerBase</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">vllm_config</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">(</span><span class="n">vllm_config</span><span class="p">)</span>
        <span class="c1"># 初始化你的 Prometheus client
</span>        <span class="kn">from</span> <span class="n">prometheus_client</span> <span class="kn">import</span> <span class="n">Counter</span><span class="p">,</span> <span class="n">Gauge</span><span class="p">,</span> <span class="n">start_http_server</span>
        <span class="n">self</span><span class="p">.</span><span class="n">request_counter</span> <span class="o">=</span> <span class="nc">Counter</span><span class="p">(</span><span class="sh">'</span><span class="s">vllm_requests_total</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">...</span><span class="sh">'</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">gpu_util</span> <span class="o">=</span> <span class="nc">Gauge</span><span class="p">(</span><span class="sh">'</span><span class="s">vllm_gpu_utilization</span><span class="sh">'</span><span class="p">,</span> <span class="sh">'</span><span class="s">...</span><span class="sh">'</span><span class="p">)</span>
        <span class="nf">start_http_server</span><span class="p">(</span><span class="mi">8765</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">log</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">stats</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">request_counter</span><span class="p">.</span><span class="nf">inc</span><span class="p">(</span><span class="n">stats</span><span class="p">.</span><span class="n">num_running_requests</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">gpu_util</span><span class="p">.</span><span class="nf">set</span><span class="p">(</span><span class="n">stats</span><span class="p">.</span><span class="n">gpu_cache_usage_perc</span><span class="p">)</span>
</code></pre></div></div> <div class="language-toml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">[</span><span class="n">project</span><span class="k">.</span><span class="n">entry-points</span><span class="k">.</span><span class="s">"vllm.stat_logger_plugins"</span><span class="k">]</span>
<span class="n">my_prometheus</span> <span class="o">=</span><span class="w"> </span><span class="s">"my_metrics_plugin.logger:MyPrometheusLogger"</span>
<span class="c"># ↑ 直接指向类，不是函数</span>
</code></pre></div></div> <p>注意这里和 general plugin 不同：entry point 直接指向类，不是注册函数。vllm 会在 <code class="language-plaintext highlighter-rouge">AsyncLLM.__init__</code> 时实例化这个类，并在每个 step 完成后调用 <code class="language-plaintext highlighter-rouge">log()</code>。</p> <hr/> <h2 id="9-白名单控制vllm_plugins-环境变量">9. 白名单控制：VLLM_PLUGINS 环境变量</h2> <p>默认情况下，环境里所有声明了 <code class="language-plaintext highlighter-rouge">vllm.*</code> group 的 entry point 都会被加载。如果你装了多个插件但只想启用其中几个：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">VLLM_PLUGINS</span><span class="o">=</span><span class="s2">"register_my_custom_llama,my_prometheus"</span>
</code></pre></div></div> <p>设为空字符串可以<strong>完全禁用所有插件</strong>（包括 vllm 内置的 LoRA resolver）：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">export </span><span class="nv">VLLM_PLUGINS</span><span class="o">=</span><span class="s2">""</span>
</code></pre></div></div> <p>这个功能对生产环境很重要——你可能在同一个 Python 环境里装了多个插件，但某个服务只需要其中一部分，不想让其他插件产生任何副作用。</p> <hr/> <h2 id="10-踩坑与常见问题">10. 踩坑与常见问题</h2> <p><strong>Q：插件安装了但没生效？</strong></p> <p>先查日志，有没有 <code class="language-plaintext highlighter-rouge">Failed to load plugin</code> 的 <code class="language-plaintext highlighter-rouge">exception</code> 级别日志。如果没有，检查 entry_points.txt 是否正确写入：</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>python <span class="nt">-c</span> <span class="s2">"from importlib.metadata import entry_points; print(list(entry_points(group='vllm.general_plugins')))"</span>
</code></pre></div></div> <p>如果列表里没有你的插件，说明 <code class="language-plaintext highlighter-rouge">pip install</code> 没有正确生成 dist-info，检查 <code class="language-plaintext highlighter-rouge">pyproject.toml</code> 的格式（注意 group 名字里的引号）。</p> <p><strong>Q：注册函数执行了，但模型加载时说 architecture not found？</strong></p> <p>99% 是因为直接传了类而不是字符串：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># ❌ 这样在 fork 场景下可能出问题
</span><span class="n">ModelRegistry</span><span class="p">.</span><span class="nf">register_model</span><span class="p">(</span><span class="sh">"</span><span class="s">MyModel</span><span class="sh">"</span><span class="p">,</span> <span class="n">MyModel</span><span class="p">)</span>

<span class="c1"># ✅ 用字符串
</span><span class="n">ModelRegistry</span><span class="p">.</span><span class="nf">register_model</span><span class="p">(</span><span class="sh">"</span><span class="s">MyModel</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">my_pkg.models:MyModel</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <p><strong>Q：platform 插件 probe() 没被调用？</strong></p> <p>platform 插件是懒加载的，只有首次访问 <code class="language-plaintext highlighter-rouge">from vllm.platforms import current_platform</code> 时才触发。如果你的代码跑到模型加载之前就 crash 了，probe() 可能根本没机会执行。可以加一行手动触发：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">vllm.platforms</span> <span class="kn">import</span> <span class="n">current_platform</span>
<span class="nf">print</span><span class="p">(</span><span class="n">current_platform</span><span class="p">)</span>  <span class="c1"># 触发加载，验证插件是否被选中
</span></code></pre></div></div> <p><strong>Q：Worker 进程没有加载我的插件？</strong></p> <p><code class="language-plaintext highlighter-rouge">general_plugins</code> 的加载在 Worker 的 <code class="language-plaintext highlighter-rouge">__init__</code> 里（<code class="language-plaintext highlighter-rouge">vllm/v1/worker/worker_base.py</code>），是自动的。如果 Worker 没加载，可能是你的插件安装在主进程的 venv 里，但 Worker 是在另一个 Python 环境里启动的（比如用了 <code class="language-plaintext highlighter-rouge">CUDA_VISIBLE_DEVICES</code> + 不同的 venv）。确认所有进程用的是同一个 Python 环境。</p> <p><strong>Q：能不能在插件里做异步初始化？</strong></p> <p>general plugin 的注册函数是同步调用的，但函数体里可以只做注册（同步），把真正的异步初始化推迟到实际 IO 操作时。stat logger 的 <code class="language-plaintext highlighter-rouge">__init__</code> 是在 AsyncLLM 启动时调用的，那时 event loop 还没起来，所以也应该是同步初始化。</p> <hr/> <h2 id="11-小结">11. 小结</h2> <p>vllm 插件系统这套设计，背后的哲学其实就一句话：<strong>核心代码只依赖接口，具体实现通过 Python packaging 元数据在运行时注入</strong>。</p> <p>这是 Python 生态里经典的插件模式——Flask 的 extension、pytest 的 plugin、setuptools 自己的命令扩展都走这条路。vllm 把它和自己的多进程架构、字符串延迟导入设计结合起来，形成了一套相当完整的扩展体系。</p> <p>对我们来说，实际价值在于：</p> <ul> <li><strong>硬件厂商</strong>：不用维护 vllm fork，直接通过 platform 插件接入</li> <li><strong>做推理算法研究的</strong>：attention backend 插件让你在 vllm 框架下测自己的 kernel，省掉大量集成工作</li> <li><strong>搞定制化 serving 的</strong>：stat logger 插件让 vllm 的内部指标无缝接进内部监控，不需要在外面套一层 wrapper 去解析日志</li> </ul> <p>下次遇到”我想扩展 vllm 但不想 fork”的需求，先看看这四个 entry_point group 能不能满足你——大概率可以。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 LLM 推理框架源码和 Python 工程化技巧你想系统深入，可以看看我之前出版的<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，书里 LLM 推理效率那章讲了不少和框架设计相关的工程背景，正好和本文讲的 vllm 插件架构有些呼应。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="vllm"/><category term="Python"/><category term="源码解析"/><summary type="html"><![CDATA[vllm v1 源码解析（四）：插件系统——用 Python entry_points 实现零侵入扩展]]></summary></entry><entry><title type="html">贝叶斯优化从零推导：从「我对这个函数有个猜测」到自动调参</title><link href="https://marsggbo.github.io/blog/2026/20260630-bayesian-optimization/" rel="alternate" type="text/html" title="贝叶斯优化从零推导：从「我对这个函数有个猜测」到自动调参"/><published>2026-06-30T00:00:00+00:00</published><updated>2026-06-30T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260630-bayesian-optimization</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260630-bayesian-optimization/"><![CDATA[<h1 id="贝叶斯优化从零推导从我对这个函数有个猜测到自动调参">贝叶斯优化从零推导：从「我对这个函数有个猜测」到自动调参</h1> <hr/> <h2 id="1-前言从一个具体的问题开始">1. 前言：从一个具体的问题开始</h2> <p>先不讲理论。来看一个实际问题。</p> <p>LLaMA-2-13B 有 40 层（20 对 Attention + MLP）。有一篇论文（Self-Speculative Decoding）发现，推理时跳过部分中间层先快速生成 draft token，再用完整模型一次验证，可以无损加速 1.3x~2x。</p> <p>问题来了：<strong>跳哪些层？</strong></p> <p>搜索空间是 $2^{40}$，穷举不可能。但更麻烦的是，<strong>每次评估都很贵</strong>——跑一遍 self-speculative decoding 采几百个 token、测平均耗时，需要好几分钟。你不可能随机试 10000 次。</p> <p>随机搜索？完全不利用已有结果，效率极低。</p> <p>网格搜索？$2^{40}$ 种组合，连列举都列举不完。</p> <p><strong>贝叶斯优化（Bayesian Optimization，BO）就是专门为这类场景设计的</strong>：评估极贵、不能穷举、但每次结果都包含有价值的信息。</p> <p>它的核心直觉：</p> <blockquote> <p>我已经测了 10 种跳层方案，知道跳第 15~25 层比较好，跳第 1~5 层基本没用。下一次，我应该去”还没测过、但根据已有结果猜测很有潜力”的区域试。</p> </blockquote> <p>要把这个直觉变成算法，需要解决两个问题：</p> <ol> <li><strong>怎么用有限的观测点”猜测”整个函数的形状？</strong> → 高斯过程（GP）</li> <li><strong>“很有潜力”怎么定量化，指导下一步去哪里？</strong> → Acquisition function</li> </ol> <p>本文从高斯分布出发，一步步推导这两件事怎么做。<strong>文末会把整套流程完整地套到这个 LLM 跳层搜索问题上</strong>，让你看清楚每个数学概念对应的现实意义。</p> <hr/> <h2 id="2-先从高斯分布说起">2. 先从高斯分布说起</h2> <p>在进入高斯过程之前，先把高斯分布的核心性质梳理清楚，因为后面所有推导都依赖它。</p> <h3 id="21-一维高斯描述单个随机变量的不确定性">2.1 一维高斯：描述单个随机变量的不确定性</h3> <p>$X \sim \mathcal{N}(\mu, \sigma^2)$ 就是说：这个随机变量的取值”大概在 $\mu$ 附近，偏差用 $\sigma$ 衡量”。</p> <p>没什么神秘的。</p> <h3 id="22-多维高斯描述多个随机变量的联合不确定性">2.2 多维高斯：描述多个随机变量的联合不确定性</h3> <p>关键性质：多维高斯分布由<strong>均值向量</strong> $\mu$ 和<strong>协方差矩阵</strong> $\Sigma$ 完全描述：</p> \[\mathbf{x} \sim \mathcal{N}(\boldsymbol{\mu}, \Sigma)\] <p>$\Sigma_{ij} = \text{Cov}(x_i, x_j)$ 描述的是”第 $i$ 个变量和第 $j$ 个变量有多大关联”。</p> <p><strong>高斯分布最重要的一个性质</strong>：它的条件分布和边缘分布都还是高斯分布，而且有解析解。</p> <p>这个性质非常非常重要，是整个高斯过程能用的根本原因。后面会看到为什么。</p> <p>具体地，把 $\mathbf{x}$ 分成两组 $\mathbf{x}_1, \mathbf{x}_2$：</p> \[\begin{bmatrix} \mathbf{x}_1 \\ \mathbf{x}_2 \end{bmatrix} \sim \mathcal{N}\left( \begin{bmatrix} \boldsymbol{\mu}_1 \\ \boldsymbol{\mu}_2 \end{bmatrix}, \begin{bmatrix} \Sigma_{11} &amp; \Sigma_{12} \\ \Sigma_{21} &amp; \Sigma_{22} \end{bmatrix} \right)\] <table> <tbody> <tr> <td>则条件分布 $p(\mathbf{x}_1</td> <td>\mathbf{x}_2)$ <strong>仍然是高斯</strong>，均值和方差为：</td> </tr> </tbody> </table> \[\boldsymbol{\mu}_{1|2} = \boldsymbol{\mu}_1 + \Sigma_{12} \Sigma_{22}^{-1} (\mathbf{x}_2 - \boldsymbol{\mu}_2)\] \[\Sigma_{1|2} = \Sigma_{11} - \Sigma_{12} \Sigma_{22}^{-1} \Sigma_{21}\] <p>先不用记这两个公式，后面再回来推导。现在只需要记住：<strong>多维高斯的条件分布仍是高斯，且有解析解</strong>。</p> <hr/> <h2 id="3-高斯过程把高斯分布从点推广到函数">3. 高斯过程：把高斯分布从”点”推广到”函数”</h2> <h3 id="31-一个关键的视角转换">3.1 一个关键的视角转换</h3> <p>我们想对一个黑盒函数 $f: \mathbb{R}^d \to \mathbb{R}$ 建模。</p> <p>常规思路是假设 $f$ 有某种参数化形式（比如神经网络），然后用数据拟合参数。</p> <p>高斯过程走了另一条路：<strong>直接把函数 $f$ 本身当作随机变量，对它的分布建模</strong>。</p> <p>怎么理解”函数是随机变量”？</p> <p>想象函数 $f$ 在空间中的无穷多个点 $x_1, x_2, x_3, \ldots$ 上的取值，形成一个无穷维的向量 $(f(x_1), f(x_2), f(x_3), \ldots)$。</p> <p>高斯过程的定义就是：<strong>这个无穷维向量的任意有限子集都服从多维高斯分布</strong>。</p> <p>写成数学语言：</p> \[f \sim \mathcal{GP}(m(\cdot), k(\cdot, \cdot))\] <p>其中：</p> <ul> <li>$m(x) = \mathbb{E}[f(x)]$ 是<strong>均值函数</strong>，描述函数在每个点的期望值</li> <li>$k(x, x’) = \text{Cov}(f(x), f(x’))$ 是<strong>核函数（协方差函数）</strong>，描述两个点之间的”相关性”</li> </ul> <h3 id="32-核函数编码你对函数形状的先验假设">3.2 核函数：编码你对函数形状的先验假设</h3> <p>核函数 $k(x, x’)$ 是高斯过程的灵魂。它回答了一个问题：<strong>如果 $x$ 和 $x’$ 很近，$f(x)$ 和 $f(x’)$ 的取值会有多相似？</strong></p> <p>最常用的是 <strong>RBF（Radial Basis Function）核</strong>：</p> \[k_{\text{RBF}}(x, x') = \exp\left(-\frac{\|x - x'\|^2}{2l^2}\right)\] <p>当 $x = x’$ 时，$k = 1$（自相关最大）；距离越远，$k$ 趋向 0（相关性消失）。</p> <p>$l$ 是<strong>长度尺度（length scale）</strong>，控制”多近才算近”——$l$ 大说明你认为函数很平滑，相距较远的点也相关；$l$ 小说明函数变化剧烈。</p> <p><strong>Matern 核</strong>是 RBF 的推广，多了一个参数 $\nu$ 控制光滑度：</p> \[k_{\text{Matern}}(x, x') \propto \left(\frac{\sqrt{2\nu} \|x-x'\|}{l}\right)^\nu K_\nu\left(\frac{\sqrt{2\nu} \|x-x'\|}{l}\right)\] <p>$\nu = 5/2$ 时函数二阶可微，在实践中很好用——比 RBF 假设的无穷阶光滑更符合实际。</p> <p><strong>选核函数，本质上是在选你对函数性质的先验假设</strong>。这就是”贝叶斯”两个字的来源之一。</p> <hr/> <h2 id="4-贝叶斯更新观测数据后怎么修正猜测">4. 贝叶斯更新：观测数据后怎么修正猜测</h2> <p>现在进入核心推导。</p> <h3 id="41-问题设定">4.1 问题设定</h3> <p>已知：观测到了 $n$ 个点的函数值，记为 $\mathcal{D} = {(x_i, y_i)}_{i=1}^n$，其中 $y_i = f(x_i) + \epsilon$，$\epsilon \sim \mathcal{N}(0, \sigma_n^2)$ 是观测噪声。</p> <p>问：对于新的未观测点 $x_<em>$，$f(x_</em>)$ 的分布是什么？</p> <h3 id="42-联合分布">4.2 联合分布</h3> <p>根据 GP 的定义，$(f(x_1), \ldots, f(x_n), f(x_*))$ 这 $n+1$ 个值<strong>联合服从多维高斯</strong>：</p> \[\begin{bmatrix} \mathbf{y} \\ f(x_*) \end{bmatrix} \sim \mathcal{N}\left( \begin{bmatrix} \mathbf{0} \\ 0 \end{bmatrix}, \begin{bmatrix} K + \sigma_n^2 I &amp; \mathbf{k}_* \\ \mathbf{k}_*^\top &amp; k_{**} \end{bmatrix} \right)\] <p>其中：</p> <ul> <li>$K_{ij} = k(x_i, x_j)$ 是 $n \times n$ 的核矩阵</li> <li>$\mathbf{k}<em>* = [k(x_1, x</em><em>), \ldots, k(x_n, x_</em>)]^\top$ 是新点和所有观测点之间的协方差向量</li> <li>$k_{<em>*} = k(x_</em>, x_*)$ 是新点的自协方差（通常为1）</li> <li>均值暂时设为 0（不影响推导，实践中可以加先验均值）</li> </ul> <h3 id="43-条件分布推导">4.3 条件分布推导</h3> <p><strong>现在用第2节里的条件高斯公式</strong>，令 $\mathbf{x}<em>1 = f(x</em>*)$，$\mathbf{x}_2 = \mathbf{y}$：</p> \[\mu_* = \mathbf{k}_*^\top (K + \sigma_n^2 I)^{-1} \mathbf{y}\] \[\sigma_*^2 = k_{**} - \mathbf{k}_*^\top (K + \sigma_n^2 I)^{-1} \mathbf{k}_*\] <p>这就是 <strong>GP 预测公式</strong>。</p> <p><strong>逐项拆解，理解每一项的含义</strong>：</p> <p><strong>均值 $\mu_*$</strong>：</p> \[\mu_* = \mathbf{k}_*^\top \underbrace{(K + \sigma_n^2 I)^{-1} \mathbf{y}}_{\boldsymbol{\alpha}}\] <p>把 $\boldsymbol{\alpha} = (K + \sigma_n^2 I)^{-1} \mathbf{y}$ 看作”每个观测点的权重”，$\mu_*$ 就是以<strong>新点和观测点的相关性为权重</strong>，对观测值做加权平均。</p> <p>直觉：$x_<em>$ 和某个 $x_i$ 很近（$k(x_i, x_</em>)$ 大），那 $f(x_i)$ 对预测 $f(x_*)$ 的贡献就大。</p> <p><strong>方差 $\sigma_*^2$</strong>：</p> \[\sigma_*^2 = \underbrace{k_{**}}_{\text{先验方差}} - \underbrace{\mathbf{k}_*^\top (K + \sigma_n^2 I)^{-1} \mathbf{k}_*}_{\text{观测带来的信息量}}\] <p>先验方差减去”从观测中学到的信息量”。观测点离 $x_*$ 越近，方差减少越多，预测越确定。</p> <p><strong>这就是贝叶斯的精髓</strong>：从先验出发（$k_{<em>*}$），用数据（观测点）更新，得到后验（$\sigma_</em>^2$ 变小）。</p> <hr/> <h2 id="5-为什么要这样假设高斯假设到底合不合理">5. 为什么要这样假设？高斯假设到底合不合理？</h2> <p>到这里可能会有个疑问：凭什么假设函数值联合服从高斯分布？实际的黑盒函数根本不一定是高斯的啊。</p> <p>这是个好问题，有几个层面的回答：</p> <p><strong>第一，高斯假设带来计算上的奇迹。</strong></p> <p>正是因为假设了高斯，条件分布才有漂亮的解析解（上面的公式）。如果换成其他分布，条件分布通常没有闭合形式，只能做 MCMC 采样，慢得多。高斯假设是”可处理性”和”表达能力”之间的最佳权衡点。</p> <p><strong>第二，中心极限定理的支撑。</strong></p> <p>很多实际函数可以被视为大量独立随机变量叠加的结果。中心极限定理告诉我们，这类函数的行为趋近高斯。</p> <p><strong>第三，高斯过程是最大熵分布。</strong></p> <p>在已知均值和协方差的约束下，高斯分布是<strong>熵最大</strong>的分布——也就是说，在你已有的信息基础上，它是”假设最少”的选择。这正是贝叶斯建模的哲学：不要在数据之外引入额外假设。</p> <p><strong>第四，实践上确实好用。</strong></p> <p>最务实的理由：在低维、小样本场景（超参数搜索通常就是这个设定），GP 的效果经过几十年验证，确实靠谱。</p> <hr/> <h2 id="6-acquisition-function下一步去哪里">6. Acquisition Function：下一步去哪里？</h2> <p>现在我们有了 GP，可以对任意未观测点 $x_<em>$ 给出预测均值 $\mu(x_</em>)$ 和不确定性 $\sigma(x_*)$。</p> <p>但问题来了：<strong>下一个点选哪里？</strong></p> <ul> <li>选 $\mu(x_*)$ 最大的地方？——只利用当前最优猜测，完全不探索未知区域，容易陷入局部最优。</li> <li>选 $\sigma(x_*)$ 最大的地方？——只去不确定的地方，完全忽略已知的信息，低效。</li> </ul> <p>需要一个函数来平衡<strong>探索（exploration）</strong>和<strong>利用（exploitation）</strong>。这就是 Acquisition function。</p> <h3 id="61-最直观的probability-of-improvement-pi">6.1 最直观的：Probability of Improvement (PI)</h3> <p><strong>直觉</strong>：新点 $x_*$ 比当前最优值 $f^+ = \max_i y_i$ 更好的概率是多少？</p> \[\text{PI}(x_*) = P(f(x_*) &gt; f^+) = \Phi\left(\frac{\mu(x_*) - f^+}{\sigma(x_*)}\right)\] <p>其中 $\Phi$ 是标准正态 CDF。</p> <p>因为 $f(x_<em>) \sim \mathcal{N}(\mu(x_</em>), \sigma(x_*)^2)$，所以”超过 $f^+$ 的概率”就是正态分布尾部的面积，直接用 $\Phi$ 算出来。</p> <p><strong>问题</strong>：PI 只关心”有没有改进”，不关心”改进多少”——哪怕提升了 0.001 也算赢。容易在最优点附近反复横跳。</p> <h3 id="62-更好用的expected-improvement-ei">6.2 更好用的：Expected Improvement (EI)</h3> <p><strong>直觉</strong>：期望改进量是多少？不只是”有没有更好”，而是”平均能好多少”。</p> \[\text{EI}(x_*) = \mathbb{E}[\max(f(x_*) - f^+, 0)]\] <p>因为 $f(x_*)$ 是高斯分布，这个期望有<strong>解析解</strong>：</p> \[\text{EI}(x_*) = (\mu(x_*) - f^+) \cdot \Phi(Z) + \sigma(x_*) \cdot \phi(Z)\] <p>其中 $Z = \dfrac{\mu(x_<em>) - f^+}{\sigma(x_</em>)}$，$\phi$ 是标准正态 PDF。</p> <p><strong>逐项理解</strong>：</p> \[\underbrace{(\mu(x_*) - f^+) \cdot \Phi(Z)}_{\text{利用项}} + \underbrace{\sigma(x_*) \cdot \phi(Z)}_{\text{探索项}}\] <ul> <li><strong>利用项</strong>：当预测均值 $\mu$ 远超当前最优时，这一项大（利用已知好区域）</li> <li><strong>探索项</strong>：当不确定性 $\sigma$ 大时，这一项大（探索未知区域）</li> </ul> <p>两项自然地平衡了探索和利用，<strong>不需要手动调权重</strong>。这是 EI 流行的核心原因。</p> <p><strong>推导过程</strong>（从期望定义出发）：</p> <p>设 $u = f(x_*) - f^+$，则：</p> \[\text{EI} = \int_{-\infty}^{+\infty} \max(u, 0) \cdot \mathcal{N}(u; \mu - f^+, \sigma^2) \, du = \int_{0}^{+\infty} u \cdot \mathcal{N}(u; \mu - f^+, \sigma^2) \, du\] <p>令 $\gamma = \frac{\mu - f^+}{\sigma}$，换元 $t = \frac{u - (\mu - f^+)}{\sigma}$，积分下限变成 $t = -\gamma$：</p> \[\text{EI} = \sigma \int_{-\gamma}^{+\infty} (\sigma t + \mu - f^+) \cdot \phi(t) \, dt\] <p>分拆成两个积分：</p> \[= \sigma^2 \int_{-\gamma}^{+\infty} t \phi(t) dt + \sigma(\mu - f^+) \int_{-\gamma}^{+\infty} \phi(t) dt\] <p>利用正态分布恒等式 $\int_{-\gamma}^{+\infty} t\phi(t)dt = \phi(\gamma)$，$\int_{-\gamma}^{+\infty} \phi(t)dt = \Phi(\gamma)$：</p> \[\text{EI}(x_*) = \sigma \cdot \phi(\gamma) + (\mu - f^+) \cdot \Phi(\gamma), \quad \gamma = \frac{\mu - f^+}{\sigma}\] <p>这正是我们之前写出来的公式。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">scipy.stats</span> <span class="kn">import</span> <span class="n">norm</span>
<span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>

<span class="k">def</span> <span class="nf">expected_improvement</span><span class="p">(</span><span class="n">mu</span><span class="p">,</span> <span class="n">sigma</span><span class="p">,</span> <span class="n">best_f</span><span class="p">,</span> <span class="n">xi</span><span class="o">=</span><span class="mf">0.0</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    xi 是一个探索系数，xi &gt; 0 时更倾向于探索。
    论文里通常设 xi=0 或 xi=0.01。
    </span><span class="sh">"""</span>
    <span class="n">Z</span> <span class="o">=</span> <span class="p">(</span><span class="n">mu</span> <span class="o">-</span> <span class="n">best_f</span> <span class="o">-</span> <span class="n">xi</span><span class="p">)</span> <span class="o">/</span> <span class="p">(</span><span class="n">sigma</span> <span class="o">+</span> <span class="mf">1e-9</span><span class="p">)</span>
    <span class="n">ei</span> <span class="o">=</span> <span class="p">(</span><span class="n">mu</span> <span class="o">-</span> <span class="n">best_f</span> <span class="o">-</span> <span class="n">xi</span><span class="p">)</span> <span class="o">*</span> <span class="n">norm</span><span class="p">.</span><span class="nf">cdf</span><span class="p">(</span><span class="n">Z</span><span class="p">)</span> <span class="o">+</span> <span class="n">sigma</span> <span class="o">*</span> <span class="n">norm</span><span class="p">.</span><span class="nf">pdf</span><span class="p">(</span><span class="n">Z</span><span class="p">)</span>
    <span class="n">ei</span><span class="p">[</span><span class="n">sigma</span> <span class="o">&lt;</span> <span class="mf">1e-10</span><span class="p">]</span> <span class="o">=</span> <span class="mf">0.0</span>   <span class="c1"># 完全确定的点 EI 为 0
</span>    <span class="k">return</span> <span class="n">ei</span>
</code></pre></div></div> <h3 id="63-更复杂的ucb">6.3 更复杂的：UCB</h3> \[\text{UCB}(x_*) = \mu(x_*) + \beta \cdot \sigma(x_*)\] <p>置信区间上界。$\beta$ 手动控制探索/利用的平衡，理论上有次线性遗憾界。实践中 $\beta$ 调起来比较麻烦，EI 通常更好用。</p> <hr/> <h2 id="7-整个算法流程组装起来">7. 整个算法流程，组装起来</h2> <p>现在把所有零件拼在一起：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>初始化：随机采样几个点 x₁...xₙ，得到观测 y₁...yₙ
         ↓
循环：
  1. 用 (X_obs, y_obs) 拟合 GP（优化核函数超参数）
  2. 用 GP 计算所有候选点的 μ(x) 和 σ(x)
  3. 用 acquisition function（如 EI）计算每个候选点的得分
  4. 选得分最高的 x_next
  5. 评估 f(x_next)，加入观测集
         ↓
     重复直到预算耗尽
</code></pre></div></div> <p>用代码把这个完整流程写出来，配合上前面推导的每一步：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>
<span class="kn">from</span> <span class="n">scipy.linalg</span> <span class="kn">import</span> <span class="n">cholesky</span><span class="p">,</span> <span class="n">solve_triangular</span>
<span class="kn">from</span> <span class="n">scipy.optimize</span> <span class="kn">import</span> <span class="n">minimize</span>
<span class="kn">from</span> <span class="n">scipy.stats</span> <span class="kn">import</span> <span class="n">norm</span>


<span class="c1"># ---- 核函数 ----
</span>
<span class="k">def</span> <span class="nf">matern_52</span><span class="p">(</span><span class="n">X1</span><span class="p">,</span> <span class="n">X2</span><span class="p">,</span> <span class="n">length_scale</span><span class="o">=</span><span class="mf">1.0</span><span class="p">,</span> <span class="n">signal_var</span><span class="o">=</span><span class="mf">1.0</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">Matern ν=5/2 核</span><span class="sh">"""</span>
    <span class="n">dist</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(((</span><span class="n">X1</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">]</span> <span class="o">-</span> <span class="n">X2</span><span class="p">[</span><span class="bp">None</span><span class="p">,</span> <span class="p">:])</span> <span class="o">**</span> <span class="mi">2</span><span class="p">).</span><span class="nf">sum</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">))</span>
    <span class="n">sqrt5d</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="mi">5</span><span class="p">)</span> <span class="o">*</span> <span class="n">dist</span> <span class="o">/</span> <span class="n">length_scale</span>
    <span class="k">return</span> <span class="n">signal_var</span> <span class="o">*</span> <span class="p">(</span><span class="mi">1</span> <span class="o">+</span> <span class="n">sqrt5d</span> <span class="o">+</span> <span class="n">sqrt5d</span><span class="o">**</span><span class="mi">2</span> <span class="o">/</span> <span class="mi">3</span><span class="p">)</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="o">-</span><span class="n">sqrt5d</span><span class="p">)</span>


<span class="c1"># ---- GP 核心：拟合 + 预测 ----
</span>
<span class="k">class</span> <span class="nc">GaussianProcess</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">noise</span><span class="o">=</span><span class="mf">1e-6</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">noise</span> <span class="o">=</span> <span class="n">noise</span>
        <span class="n">self</span><span class="p">.</span><span class="n">length_scale</span> <span class="o">=</span> <span class="mf">1.0</span>
        <span class="n">self</span><span class="p">.</span><span class="n">signal_var</span> <span class="o">=</span> <span class="mf">1.0</span>
        <span class="n">self</span><span class="p">.</span><span class="n">X_train</span> <span class="o">=</span> <span class="bp">None</span>
        <span class="n">self</span><span class="p">.</span><span class="n">L</span> <span class="o">=</span> <span class="bp">None</span>      <span class="c1"># Cholesky 分解
</span>        <span class="n">self</span><span class="p">.</span><span class="n">alpha</span> <span class="o">=</span> <span class="bp">None</span>  <span class="c1"># K^{-1} y
</span>
    <span class="k">def</span> <span class="nf">_build_K</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">X1</span><span class="p">,</span> <span class="n">X2</span><span class="p">):</span>
        <span class="k">return</span> <span class="nf">matern_52</span><span class="p">(</span><span class="n">X1</span><span class="p">,</span> <span class="n">X2</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">length_scale</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">signal_var</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">_log_marginal_likelihood</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">params</span><span class="p">):</span>
        <span class="sh">"""</span><span class="s">
        最大化对数边际似然来学习核超参数。
        params = [log(length_scale), log(signal_var)]
        </span><span class="sh">"""</span>
        <span class="n">l</span><span class="p">,</span> <span class="n">s</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">params</span><span class="p">[</span><span class="mi">0</span><span class="p">]),</span> <span class="n">np</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">params</span><span class="p">[</span><span class="mi">1</span><span class="p">])</span>
        <span class="n">n</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">X_train</span><span class="p">)</span>
        <span class="n">K</span> <span class="o">=</span> <span class="nf">matern_52</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">X_train</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">X_train</span><span class="p">,</span> <span class="n">l</span><span class="p">,</span> <span class="n">s</span><span class="p">)</span> <span class="o">+</span> <span class="n">self</span><span class="p">.</span><span class="n">noise</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="nf">eye</span><span class="p">(</span><span class="n">n</span><span class="p">)</span>
        <span class="k">try</span><span class="p">:</span>
            <span class="n">L</span> <span class="o">=</span> <span class="nf">cholesky</span><span class="p">(</span><span class="n">K</span><span class="p">,</span> <span class="n">lower</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
        <span class="k">except</span> <span class="n">np</span><span class="p">.</span><span class="n">linalg</span><span class="p">.</span><span class="n">LinAlgError</span><span class="p">:</span>
            <span class="k">return</span> <span class="mf">1e10</span>
        <span class="n">alpha</span> <span class="o">=</span> <span class="nf">solve_triangular</span><span class="p">(</span><span class="n">L</span><span class="p">.</span><span class="n">T</span><span class="p">,</span> <span class="nf">solve_triangular</span><span class="p">(</span><span class="n">L</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">y_train</span><span class="p">,</span> <span class="n">lower</span><span class="o">=</span><span class="bp">True</span><span class="p">))</span>
        <span class="c1"># log p(y|X) = -½ y^T K^{-1} y - ½ log|K| - n/2 log(2π)
</span>        <span class="n">lml</span> <span class="o">=</span> <span class="p">(</span><span class="o">-</span><span class="mf">0.5</span> <span class="o">*</span> <span class="n">self</span><span class="p">.</span><span class="n">y_train</span> <span class="o">@</span> <span class="n">alpha</span>
               <span class="o">-</span> <span class="n">np</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">diag</span><span class="p">(</span><span class="n">L</span><span class="p">)))</span>
               <span class="o">-</span> <span class="mf">0.5</span> <span class="o">*</span> <span class="n">n</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="mi">2</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="n">pi</span><span class="p">))</span>
        <span class="k">return</span> <span class="o">-</span><span class="n">lml</span>   <span class="c1"># minimize 负 LML
</span>
    <span class="k">def</span> <span class="nf">fit</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">X</span><span class="p">,</span> <span class="n">y</span><span class="p">,</span> <span class="n">n_restarts</span><span class="o">=</span><span class="mi">5</span><span class="p">):</span>
        <span class="n">self</span><span class="p">.</span><span class="n">X_train</span> <span class="o">=</span> <span class="n">X</span>
        <span class="n">self</span><span class="p">.</span><span class="n">y_train</span> <span class="o">=</span> <span class="n">y</span>
        <span class="n">best_lml</span><span class="p">,</span> <span class="n">best_params</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">inf</span><span class="p">,</span> <span class="p">[</span><span class="mf">0.0</span><span class="p">,</span> <span class="mf">0.0</span><span class="p">]</span>
        <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">n_restarts</span><span class="p">):</span>
            <span class="n">p0</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">uniform</span><span class="p">([</span><span class="o">-</span><span class="mi">2</span><span class="p">,</span> <span class="o">-</span><span class="mi">2</span><span class="p">],</span> <span class="p">[</span><span class="mi">2</span><span class="p">,</span> <span class="mi">2</span><span class="p">])</span>
            <span class="n">res</span> <span class="o">=</span> <span class="nf">minimize</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">_log_marginal_likelihood</span><span class="p">,</span> <span class="n">p0</span><span class="p">,</span>
                           <span class="n">method</span><span class="o">=</span><span class="sh">"</span><span class="s">L-BFGS-B</span><span class="sh">"</span><span class="p">,</span> <span class="n">bounds</span><span class="o">=</span><span class="p">[(</span><span class="o">-</span><span class="mi">5</span><span class="p">,</span><span class="mi">5</span><span class="p">),(</span><span class="o">-</span><span class="mi">5</span><span class="p">,</span><span class="mi">5</span><span class="p">)])</span>
            <span class="k">if</span> <span class="n">res</span><span class="p">.</span><span class="n">fun</span> <span class="o">&lt;</span> <span class="n">best_lml</span><span class="p">:</span>
                <span class="n">best_lml</span><span class="p">,</span> <span class="n">best_params</span> <span class="o">=</span> <span class="n">res</span><span class="p">.</span><span class="n">fun</span><span class="p">,</span> <span class="n">res</span><span class="p">.</span><span class="n">x</span>
        <span class="n">self</span><span class="p">.</span><span class="n">length_scale</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">signal_var</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">best_params</span><span class="p">)</span>
        <span class="n">n</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">X</span><span class="p">)</span>
        <span class="n">K</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">_build_K</span><span class="p">(</span><span class="n">X</span><span class="p">,</span> <span class="n">X</span><span class="p">)</span> <span class="o">+</span> <span class="n">self</span><span class="p">.</span><span class="n">noise</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="nf">eye</span><span class="p">(</span><span class="n">n</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">L</span> <span class="o">=</span> <span class="nf">cholesky</span><span class="p">(</span><span class="n">K</span><span class="p">,</span> <span class="n">lower</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">alpha</span> <span class="o">=</span> <span class="nf">solve_triangular</span><span class="p">(</span>
            <span class="n">self</span><span class="p">.</span><span class="n">L</span><span class="p">.</span><span class="n">T</span><span class="p">,</span> <span class="nf">solve_triangular</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">L</span><span class="p">,</span> <span class="n">y</span><span class="p">,</span> <span class="n">lower</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
        <span class="p">)</span>

    <span class="k">def</span> <span class="nf">predict</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">X_test</span><span class="p">):</span>
        <span class="sh">"""</span><span class="s">返回预测均值 μ 和标准差 σ</span><span class="sh">"""</span>
        <span class="n">K_star</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">_build_K</span><span class="p">(</span><span class="n">X_test</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">X_train</span><span class="p">)</span>   <span class="c1"># (m, n)
</span>        <span class="n">mu</span> <span class="o">=</span> <span class="n">K_star</span> <span class="o">@</span> <span class="n">self</span><span class="p">.</span><span class="n">alpha</span>                         <span class="c1"># μ = K_* K^{-1} y
</span>
        <span class="c1"># σ² = k(x*,x*) - K_* K^{-1} K_*^T
</span>        <span class="n">v</span> <span class="o">=</span> <span class="nf">solve_triangular</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="n">L</span><span class="p">,</span> <span class="n">K_star</span><span class="p">.</span><span class="n">T</span><span class="p">,</span> <span class="n">lower</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>  <span class="c1"># L^{-1} K_*^T
</span>        <span class="n">k_diag</span> <span class="o">=</span> <span class="nf">matern_52</span><span class="p">(</span><span class="n">X_test</span><span class="p">,</span> <span class="n">X_test</span><span class="p">,</span>
                           <span class="n">self</span><span class="p">.</span><span class="n">length_scale</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="n">signal_var</span><span class="p">).</span><span class="nf">diagonal</span><span class="p">()</span>
        <span class="n">var</span> <span class="o">=</span> <span class="n">k_diag</span> <span class="o">-</span> <span class="n">np</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="n">v</span><span class="o">**</span><span class="mi">2</span><span class="p">,</span> <span class="n">axis</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">mu</span><span class="p">,</span> <span class="n">np</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">clip</span><span class="p">(</span><span class="n">var</span><span class="p">,</span> <span class="mi">0</span><span class="p">,</span> <span class="bp">None</span><span class="p">))</span>


<span class="c1"># ---- Acquisition Function ----
</span>
<span class="k">def</span> <span class="nf">expected_improvement</span><span class="p">(</span><span class="n">mu</span><span class="p">,</span> <span class="n">sigma</span><span class="p">,</span> <span class="n">best_f</span><span class="p">,</span> <span class="n">xi</span><span class="o">=</span><span class="mf">0.01</span><span class="p">):</span>
    <span class="n">Z</span> <span class="o">=</span> <span class="p">(</span><span class="n">mu</span> <span class="o">-</span> <span class="n">best_f</span> <span class="o">-</span> <span class="n">xi</span><span class="p">)</span> <span class="o">/</span> <span class="p">(</span><span class="n">sigma</span> <span class="o">+</span> <span class="mf">1e-9</span><span class="p">)</span>
    <span class="n">ei</span> <span class="o">=</span> <span class="p">(</span><span class="n">mu</span> <span class="o">-</span> <span class="n">best_f</span> <span class="o">-</span> <span class="n">xi</span><span class="p">)</span> <span class="o">*</span> <span class="n">norm</span><span class="p">.</span><span class="nf">cdf</span><span class="p">(</span><span class="n">Z</span><span class="p">)</span> <span class="o">+</span> <span class="n">sigma</span> <span class="o">*</span> <span class="n">norm</span><span class="p">.</span><span class="nf">pdf</span><span class="p">(</span><span class="n">Z</span><span class="p">)</span>
    <span class="n">ei</span><span class="p">[</span><span class="n">sigma</span> <span class="o">&lt;</span> <span class="mf">1e-10</span><span class="p">]</span> <span class="o">=</span> <span class="mf">0.0</span>
    <span class="k">return</span> <span class="n">ei</span>


<span class="c1"># ---- 主循环 ----
</span>
<span class="k">def</span> <span class="nf">bayesian_optimization</span><span class="p">(</span><span class="n">f</span><span class="p">,</span> <span class="n">bounds</span><span class="p">,</span> <span class="n">n_init</span><span class="o">=</span><span class="mi">5</span><span class="p">,</span> <span class="n">n_iter</span><span class="o">=</span><span class="mi">20</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    f: 黑盒目标函数（越大越好）
    bounds: [(low1, high1), (low2, high2), ...]
    </span><span class="sh">"""</span>
    <span class="n">dim</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">bounds</span><span class="p">)</span>
    <span class="n">bounds_arr</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">array</span><span class="p">(</span><span class="n">bounds</span><span class="p">)</span>

    <span class="c1"># 初始随机探索
</span>    <span class="n">X_obs</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">uniform</span><span class="p">(</span><span class="n">bounds_arr</span><span class="p">[:,</span> <span class="mi">0</span><span class="p">],</span> <span class="n">bounds_arr</span><span class="p">[:,</span> <span class="mi">1</span><span class="p">],</span> <span class="n">size</span><span class="o">=</span><span class="p">(</span><span class="n">n_init</span><span class="p">,</span> <span class="n">dim</span><span class="p">))</span>
    <span class="n">y_obs</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">array</span><span class="p">([</span><span class="nf">f</span><span class="p">(</span><span class="n">x</span><span class="p">)</span> <span class="k">for</span> <span class="n">x</span> <span class="ow">in</span> <span class="n">X_obs</span><span class="p">])</span>

    <span class="n">gp</span> <span class="o">=</span> <span class="nc">GaussianProcess</span><span class="p">()</span>

    <span class="k">for</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">n_iter</span><span class="p">):</span>
        <span class="c1"># 1. 拟合 GP
</span>        <span class="n">gp</span><span class="p">.</span><span class="nf">fit</span><span class="p">(</span><span class="n">X_obs</span><span class="p">,</span> <span class="n">y_obs</span><span class="p">)</span>

        <span class="c1"># 2. 在候选点上计算 EI
</span>        <span class="n">X_cand</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nf">uniform</span><span class="p">(</span>
            <span class="n">bounds_arr</span><span class="p">[:,</span> <span class="mi">0</span><span class="p">],</span> <span class="n">bounds_arr</span><span class="p">[:,</span> <span class="mi">1</span><span class="p">],</span> <span class="n">size</span><span class="o">=</span><span class="p">(</span><span class="mi">5000</span><span class="p">,</span> <span class="n">dim</span><span class="p">)</span>
        <span class="p">)</span>
        <span class="n">mu</span><span class="p">,</span> <span class="n">sigma</span> <span class="o">=</span> <span class="n">gp</span><span class="p">.</span><span class="nf">predict</span><span class="p">(</span><span class="n">X_cand</span><span class="p">)</span>
        <span class="n">ei</span> <span class="o">=</span> <span class="nf">expected_improvement</span><span class="p">(</span><span class="n">mu</span><span class="p">,</span> <span class="n">sigma</span><span class="p">,</span> <span class="n">best_f</span><span class="o">=</span><span class="n">y_obs</span><span class="p">.</span><span class="nf">max</span><span class="p">())</span>

        <span class="c1"># 3. 选 EI 最高的点
</span>        <span class="n">x_next</span> <span class="o">=</span> <span class="n">X_cand</span><span class="p">[</span><span class="n">np</span><span class="p">.</span><span class="nf">argmax</span><span class="p">(</span><span class="n">ei</span><span class="p">)]</span>

        <span class="c1"># 4. 评估
</span>        <span class="n">y_next</span> <span class="o">=</span> <span class="nf">f</span><span class="p">(</span><span class="n">x_next</span><span class="p">)</span>
        <span class="n">X_obs</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">vstack</span><span class="p">([</span><span class="n">X_obs</span><span class="p">,</span> <span class="n">x_next</span><span class="p">])</span>
        <span class="n">y_obs</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">y_obs</span><span class="p">,</span> <span class="n">y_next</span><span class="p">)</span>

        <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Iter </span><span class="si">{</span><span class="n">i</span><span class="o">+</span><span class="mi">1</span><span class="si">:</span><span class="mi">2</span><span class="n">d</span><span class="si">}</span><span class="s">: x=</span><span class="si">{</span><span class="n">x_next</span><span class="si">}</span><span class="s">, f=</span><span class="si">{</span><span class="n">y_next</span><span class="si">:</span><span class="p">.</span><span class="mi">4</span><span class="n">f</span><span class="si">}</span><span class="s">, best=</span><span class="si">{</span><span class="n">y_obs</span><span class="p">.</span><span class="nf">max</span><span class="p">()</span><span class="si">:</span><span class="p">.</span><span class="mi">4</span><span class="n">f</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>

    <span class="n">best_idx</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">argmax</span><span class="p">(</span><span class="n">y_obs</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">X_obs</span><span class="p">[</span><span class="n">best_idx</span><span class="p">],</span> <span class="n">y_obs</span><span class="p">[</span><span class="n">best_idx</span><span class="p">]</span>


<span class="c1"># ---- 测试：用 BO 找 Branin 函数的最优点 ----
</span><span class="k">if</span> <span class="n">__name__</span> <span class="o">==</span> <span class="sh">"</span><span class="s">__main__</span><span class="sh">"</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">branin</span><span class="p">(</span><span class="n">x</span><span class="p">):</span>
        <span class="c1"># 标准 benchmark，有 3 个全局最优点，最优值约 0.397（取负号变最大化）
</span>        <span class="n">a</span><span class="p">,</span> <span class="n">b</span><span class="p">,</span> <span class="n">c</span> <span class="o">=</span> <span class="mi">1</span><span class="p">,</span> <span class="mf">5.1</span><span class="o">/</span><span class="p">(</span><span class="mi">4</span><span class="o">*</span><span class="n">np</span><span class="p">.</span><span class="n">pi</span><span class="o">**</span><span class="mi">2</span><span class="p">),</span> <span class="mi">5</span><span class="o">/</span><span class="n">np</span><span class="p">.</span><span class="n">pi</span>
        <span class="n">r</span><span class="p">,</span> <span class="n">s</span><span class="p">,</span> <span class="n">t</span> <span class="o">=</span> <span class="mi">6</span><span class="p">,</span> <span class="mi">10</span><span class="p">,</span> <span class="mi">1</span><span class="o">/</span><span class="p">(</span><span class="mi">8</span><span class="o">*</span><span class="n">np</span><span class="p">.</span><span class="n">pi</span><span class="p">)</span>
        <span class="n">val</span> <span class="o">=</span> <span class="n">a</span><span class="o">*</span><span class="p">(</span><span class="n">x</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span> <span class="o">-</span> <span class="n">b</span><span class="o">*</span><span class="n">x</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span><span class="o">**</span><span class="mi">2</span> <span class="o">+</span> <span class="n">c</span><span class="o">*</span><span class="n">x</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">-</span> <span class="n">r</span><span class="p">)</span><span class="o">**</span><span class="mi">2</span> <span class="o">+</span> <span class="n">s</span><span class="o">*</span><span class="p">(</span><span class="mi">1</span><span class="o">-</span><span class="n">t</span><span class="p">)</span><span class="o">*</span><span class="n">np</span><span class="p">.</span><span class="nf">cos</span><span class="p">(</span><span class="n">x</span><span class="p">[</span><span class="mi">0</span><span class="p">])</span> <span class="o">+</span> <span class="n">s</span>
        <span class="k">return</span> <span class="o">-</span><span class="n">val</span>   <span class="c1"># 转成最大化
</span>
    <span class="n">bounds</span> <span class="o">=</span> <span class="p">[(</span><span class="o">-</span><span class="mi">5</span><span class="p">,</span> <span class="mi">10</span><span class="p">),</span> <span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mi">15</span><span class="p">)]</span>
    <span class="n">best_x</span><span class="p">,</span> <span class="n">best_y</span> <span class="o">=</span> <span class="nf">bayesian_optimization</span><span class="p">(</span><span class="n">branin</span><span class="p">,</span> <span class="n">bounds</span><span class="p">,</span> <span class="n">n_init</span><span class="o">=</span><span class="mi">5</span><span class="p">,</span> <span class="n">n_iter</span><span class="o">=</span><span class="mi">30</span><span class="p">)</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="se">\n</span><span class="s">最优点: x=</span><span class="si">{</span><span class="n">best_x</span><span class="si">}</span><span class="s">, f(x)=</span><span class="si">{</span><span class="o">-</span><span class="n">best_y</span><span class="si">:</span><span class="p">.</span><span class="mi">4</span><span class="n">f</span><span class="si">}</span><span class="s">（Branin 最优约 0.397）</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <hr/> <h2 id="8-回到开头bo-怎么搜-llm-的跳层方案">8. 回到开头：BO 怎么搜 LLM 的跳层方案</h2> <p>现在把前面所有概念套回最开始的问题：<strong>LLaMA-2-13B 推理时跳哪些层能最大化加速？</strong></p> <h3 id="81-先把问题抽象清楚">8.1 先把问题抽象清楚</h3> <ul> <li><strong>输入</strong>：一个二值向量 $z \in {0,1}^{40}$，$z_i = 1$ 表示跳过第 $i$ 层</li> <li><strong>目标函数</strong> $f(z)$：用这个跳层方案跑 self-speculative decoding，返回<strong>每 token 平均耗时的负值</strong>（转成最大化，耗时越低越好）</li> <li><strong>约束</strong>：跳层比例不超过 50%（跳太多 draft 质量崩掉，接受率暴跌反而更慢）</li> </ul> <p>搜索空间 $2^{40}$、每次评估几分钟——完全符合 BO 的适用场景。</p> <h3 id="82-gp-在这里建模的是什么">8.2 GP 在这里建模的是什么</h3> <p>GP 要对函数 $f(z)$——”某种跳层方案下的推理效率”——建模。</p> <p><strong>核函数的选择</strong>很关键：两个跳层方案 $z, z’$ 之间的”相似性”怎么定义？用 L2 距离（Matern 核的默认行为）对二值向量也合理——跳相近的层集合，推理效率大概率接近。</p> <p>还有一个关键先验可以编进候选生成逻辑里：<strong>只从中间层（第 8~32 层）里采样候选配置</strong>，反映论文观察到的”浅层/深层不适合跳”这一规律。这等价于在 GP 的搜索区域上加了个软约束，让它更专注于有价值的子空间。</p> <h3 id="83-搜索过程的真实走法带逐步解释">8.3 搜索过程的真实走法（带逐步解释）</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="n">numpy</span> <span class="k">as</span> <span class="n">np</span>
<span class="kn">from</span> <span class="n">scipy.linalg</span> <span class="kn">import</span> <span class="n">cholesky</span><span class="p">,</span> <span class="n">solve_triangular</span>
<span class="kn">from</span> <span class="n">scipy.stats</span> <span class="kn">import</span> <span class="n">norm</span>

<span class="n">NUM_LAYERS</span> <span class="o">=</span> <span class="mi">40</span>
<span class="n">MID_START</span><span class="p">,</span> <span class="n">MID_END</span> <span class="o">=</span> <span class="mi">8</span><span class="p">,</span> <span class="mi">32</span>   <span class="c1"># 先验：只在中间层搜
</span><span class="n">MAX_SKIP</span> <span class="o">=</span> <span class="mi">20</span>                  <span class="c1"># 最多跳 50%
</span>
<span class="k">def</span> <span class="nf">mock_eval</span><span class="p">(</span><span class="n">z</span><span class="p">,</span> <span class="n">seed</span><span class="o">=</span><span class="bp">None</span><span class="p">):</span>
    <span class="sh">"""</span><span class="s">
    模拟评估函数，替代</span><span class="sh">"</span><span class="s">真实跑 self-speculative decoding 测速</span><span class="sh">"</span><span class="s">。
    真实最优：跳第 12~22 层的奇数层（中间层冗余度最高）。
    </span><span class="sh">"""</span>
    <span class="n">rng</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nc">RandomState</span><span class="p">(</span><span class="n">seed</span><span class="p">)</span>
    <span class="n">optimal</span> <span class="o">=</span> <span class="nf">set</span><span class="p">(</span><span class="nf">range</span><span class="p">(</span><span class="mi">12</span><span class="p">,</span> <span class="mi">23</span><span class="p">,</span> <span class="mi">2</span><span class="p">))</span>
    <span class="n">n_skip</span> <span class="o">=</span> <span class="n">z</span><span class="p">.</span><span class="nf">sum</span><span class="p">()</span>
    <span class="k">if</span> <span class="n">n_skip</span> <span class="o">==</span> <span class="mi">0</span> <span class="ow">or</span> <span class="n">n_skip</span> <span class="o">&gt;</span> <span class="n">MAX_SKIP</span><span class="p">:</span>
        <span class="k">return</span> <span class="o">-</span><span class="mf">1.0</span>
    <span class="n">overlap</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="nf">set</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">where</span><span class="p">(</span><span class="n">z</span> <span class="o">==</span> <span class="mi">1</span><span class="p">)[</span><span class="mi">0</span><span class="p">])</span> <span class="o">&amp;</span> <span class="n">optimal</span><span class="p">)</span>
    <span class="k">return</span> <span class="nf">float</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">clip</span><span class="p">(</span><span class="n">overlap</span> <span class="o">/</span> <span class="nf">len</span><span class="p">(</span><span class="n">optimal</span><span class="p">)</span> <span class="o">+</span> <span class="n">rng</span><span class="p">.</span><span class="nf">normal</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="mf">0.05</span><span class="p">),</span> <span class="mi">0</span><span class="p">,</span> <span class="mi">1</span><span class="p">))</span>

<span class="k">def</span> <span class="nf">matern_52_binary</span><span class="p">(</span><span class="n">Z1</span><span class="p">,</span> <span class="n">Z2</span><span class="p">,</span> <span class="n">length_scale</span><span class="o">=</span><span class="mf">3.0</span><span class="p">):</span>
    <span class="n">dist</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(((</span><span class="n">Z1</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">].</span><span class="nf">astype</span><span class="p">(</span><span class="nb">float</span><span class="p">)</span> <span class="o">-</span> <span class="n">Z2</span><span class="p">[</span><span class="bp">None</span><span class="p">].</span><span class="nf">astype</span><span class="p">(</span><span class="nb">float</span><span class="p">))</span><span class="o">**</span><span class="mi">2</span><span class="p">).</span><span class="nf">sum</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">))</span>
    <span class="n">r</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="mi">5</span><span class="p">)</span> <span class="o">*</span> <span class="n">dist</span> <span class="o">/</span> <span class="n">length_scale</span>
    <span class="nf">return </span><span class="p">(</span><span class="mi">1</span> <span class="o">+</span> <span class="n">r</span> <span class="o">+</span> <span class="n">r</span><span class="o">**</span><span class="mi">2</span> <span class="o">/</span> <span class="mi">3</span><span class="p">)</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="o">-</span><span class="n">r</span><span class="p">)</span>

<span class="k">def</span> <span class="nf">sample_configs</span><span class="p">(</span><span class="n">n</span><span class="p">,</span> <span class="n">rng</span><span class="p">):</span>
    <span class="n">configs</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">n</span><span class="p">):</span>
        <span class="n">n_skip</span> <span class="o">=</span> <span class="n">rng</span><span class="p">.</span><span class="nf">randint</span><span class="p">(</span><span class="mi">1</span><span class="p">,</span> <span class="n">MAX_SKIP</span> <span class="o">+</span> <span class="mi">1</span><span class="p">)</span>
        <span class="n">mid</span> <span class="o">=</span> <span class="nf">list</span><span class="p">(</span><span class="nf">range</span><span class="p">(</span><span class="n">MID_START</span><span class="p">,</span> <span class="n">MID_END</span><span class="p">))</span>
        <span class="n">idx</span> <span class="o">=</span> <span class="n">rng</span><span class="p">.</span><span class="nf">choice</span><span class="p">(</span><span class="n">mid</span><span class="p">,</span> <span class="n">size</span><span class="o">=</span><span class="nf">min</span><span class="p">(</span><span class="n">n_skip</span><span class="p">,</span> <span class="nf">len</span><span class="p">(</span><span class="n">mid</span><span class="p">)),</span> <span class="n">replace</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>
        <span class="n">z</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">NUM_LAYERS</span><span class="p">,</span> <span class="n">dtype</span><span class="o">=</span><span class="nb">int</span><span class="p">);</span> <span class="n">z</span><span class="p">[</span><span class="n">idx</span><span class="p">]</span> <span class="o">=</span> <span class="mi">1</span>
        <span class="n">configs</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">z</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">np</span><span class="p">.</span><span class="nf">array</span><span class="p">(</span><span class="n">configs</span><span class="p">)</span>

<span class="k">def</span> <span class="nf">bo_skip_layers</span><span class="p">(</span><span class="n">n_init</span><span class="o">=</span><span class="mi">8</span><span class="p">,</span> <span class="n">n_iter</span><span class="o">=</span><span class="mi">20</span><span class="p">):</span>
    <span class="n">rng</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="n">random</span><span class="p">.</span><span class="nc">RandomState</span><span class="p">(</span><span class="mi">42</span><span class="p">)</span>

    <span class="c1"># ---- 第一步：随机初始化，收集最初的几个观测 ----
</span>    <span class="n">Z_obs</span> <span class="o">=</span> <span class="nf">sample_configs</span><span class="p">(</span><span class="n">n_init</span><span class="p">,</span> <span class="n">rng</span><span class="p">)</span>
    <span class="n">y_obs</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">array</span><span class="p">([</span><span class="nf">mock_eval</span><span class="p">(</span><span class="n">z</span><span class="p">,</span> <span class="n">seed</span><span class="o">=</span><span class="n">i</span><span class="p">)</span> <span class="k">for</span> <span class="n">i</span><span class="p">,</span> <span class="n">z</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">Z_obs</span><span class="p">)])</span>
    <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">初始 </span><span class="si">{</span><span class="n">n_init</span><span class="si">}</span><span class="s"> 次随机探索，最优 score=</span><span class="si">{</span><span class="n">y_obs</span><span class="p">.</span><span class="nf">max</span><span class="p">()</span><span class="si">:</span><span class="p">.</span><span class="mi">3</span><span class="n">f</span><span class="si">}</span><span class="s">，</span><span class="sh">"</span>
          <span class="sa">f</span><span class="sh">"</span><span class="s">方案=</span><span class="si">{</span><span class="nf">list</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">where</span><span class="p">(</span><span class="n">Z_obs</span><span class="p">[</span><span class="n">y_obs</span><span class="p">.</span><span class="nf">argmax</span><span class="p">()]</span><span class="o">==</span><span class="mi">1</span><span class="p">)[</span><span class="mi">0</span><span class="p">])</span><span class="si">}</span><span class="se">\n</span><span class="sh">"</span><span class="p">)</span>

    <span class="n">noise</span> <span class="o">=</span> <span class="mf">0.01</span>
    <span class="k">for</span> <span class="n">it</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">n_iter</span><span class="p">):</span>

        <span class="c1"># ---- 第二步：用所有观测点拟合 GP ----
</span>        <span class="c1"># GP 记住了每一个 (z, score) 对，核矩阵 K 编码了方案间的"相似程度"
</span>        <span class="n">n</span> <span class="o">=</span> <span class="nf">len</span><span class="p">(</span><span class="n">y_obs</span><span class="p">)</span>
        <span class="n">K</span> <span class="o">=</span> <span class="nf">matern_52_binary</span><span class="p">(</span><span class="n">Z_obs</span><span class="p">,</span> <span class="n">Z_obs</span><span class="p">)</span> <span class="o">+</span> <span class="n">noise</span> <span class="o">*</span> <span class="n">np</span><span class="p">.</span><span class="nf">eye</span><span class="p">(</span><span class="n">n</span><span class="p">)</span>
        <span class="n">L</span> <span class="o">=</span> <span class="nf">cholesky</span><span class="p">(</span><span class="n">K</span><span class="p">,</span> <span class="n">lower</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
        <span class="n">alpha</span> <span class="o">=</span> <span class="nf">solve_triangular</span><span class="p">(</span><span class="n">L</span><span class="p">.</span><span class="n">T</span><span class="p">,</span> <span class="nf">solve_triangular</span><span class="p">(</span><span class="n">L</span><span class="p">,</span> <span class="n">y_obs</span><span class="p">,</span> <span class="n">lower</span><span class="o">=</span><span class="bp">True</span><span class="p">))</span>

        <span class="c1"># ---- 第三步：对大量候选配置，用 GP 预测 μ 和 σ ----
</span>        <span class="n">Z_cand</span> <span class="o">=</span> <span class="nf">sample_configs</span><span class="p">(</span><span class="mi">3000</span><span class="p">,</span> <span class="n">rng</span><span class="p">)</span>
        <span class="n">Ks</span> <span class="o">=</span> <span class="nf">matern_52_binary</span><span class="p">(</span><span class="n">Z_cand</span><span class="p">,</span> <span class="n">Z_obs</span><span class="p">)</span>
        <span class="n">mu</span> <span class="o">=</span> <span class="n">Ks</span> <span class="o">@</span> <span class="n">alpha</span>                                          <span class="c1"># μ = K_* K^{-1} y
</span>        <span class="n">v</span> <span class="o">=</span> <span class="nf">solve_triangular</span><span class="p">(</span><span class="n">L</span><span class="p">,</span> <span class="n">Ks</span><span class="p">.</span><span class="n">T</span><span class="p">,</span> <span class="n">lower</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
        <span class="n">sigma</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">clip</span><span class="p">(</span><span class="mf">1.0</span> <span class="o">-</span> <span class="p">(</span><span class="n">v</span><span class="o">**</span><span class="mi">2</span><span class="p">).</span><span class="nf">sum</span><span class="p">(</span><span class="mi">0</span><span class="p">),</span> <span class="mi">0</span><span class="p">,</span> <span class="bp">None</span><span class="p">))</span> <span class="c1"># σ² = 1 - K_* K^{-1} K_*^T
</span>
        <span class="c1"># ---- 第四步：计算 EI，选下一个要评估的点 ----
</span>        <span class="n">best_f</span> <span class="o">=</span> <span class="n">y_obs</span><span class="p">.</span><span class="nf">max</span><span class="p">()</span>
        <span class="n">Z_ei</span> <span class="o">=</span> <span class="p">(</span><span class="n">mu</span> <span class="o">-</span> <span class="n">best_f</span><span class="p">)</span> <span class="o">/</span> <span class="p">(</span><span class="n">sigma</span> <span class="o">+</span> <span class="mf">1e-9</span><span class="p">)</span>
        <span class="n">ei</span> <span class="o">=</span> <span class="p">(</span><span class="n">mu</span> <span class="o">-</span> <span class="n">best_f</span><span class="p">)</span> <span class="o">*</span> <span class="n">norm</span><span class="p">.</span><span class="nf">cdf</span><span class="p">(</span><span class="n">Z_ei</span><span class="p">)</span> <span class="o">+</span> <span class="n">sigma</span> <span class="o">*</span> <span class="n">norm</span><span class="p">.</span><span class="nf">pdf</span><span class="p">(</span><span class="n">Z_ei</span><span class="p">)</span>

        <span class="n">best_idx</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">argmax</span><span class="p">(</span><span class="n">ei</span><span class="p">)</span>
        <span class="n">z_next</span> <span class="o">=</span> <span class="n">Z_cand</span><span class="p">[</span><span class="n">best_idx</span><span class="p">]</span>

        <span class="c1"># ---- 打印 GP 此刻在"想什么" ----
</span>        <span class="k">if</span> <span class="n">it</span> <span class="o">&lt;</span> <span class="mi">5</span> <span class="ow">or</span> <span class="n">it</span> <span class="o">%</span> <span class="mi">5</span> <span class="o">==</span> <span class="mi">4</span><span class="p">:</span>
            <span class="n">top3</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">argsort</span><span class="p">(</span><span class="n">ei</span><span class="p">)[</span><span class="o">-</span><span class="mi">3</span><span class="p">:][::</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span>
            <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Iter </span><span class="si">{</span><span class="n">it</span><span class="o">+</span><span class="mi">1</span><span class="si">:</span><span class="mi">2</span><span class="n">d</span><span class="si">}</span><span class="s"> | 当前最优 score=</span><span class="si">{</span><span class="n">best_f</span><span class="si">:</span><span class="p">.</span><span class="mi">3</span><span class="n">f</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
            <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  GP 最看好的前3个候选：</span><span class="sh">"</span><span class="p">)</span>
            <span class="k">for</span> <span class="n">rank</span><span class="p">,</span> <span class="n">i</span> <span class="ow">in</span> <span class="nf">enumerate</span><span class="p">(</span><span class="n">top3</span><span class="p">):</span>
                <span class="n">layers</span> <span class="o">=</span> <span class="nf">list</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">where</span><span class="p">(</span><span class="n">Z_cand</span><span class="p">[</span><span class="n">i</span><span class="p">]</span><span class="o">==</span><span class="mi">1</span><span class="p">)[</span><span class="mi">0</span><span class="p">])</span>
                <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">    #</span><span class="si">{</span><span class="n">rank</span><span class="o">+</span><span class="mi">1</span><span class="si">}</span><span class="s"> skip=</span><span class="si">{</span><span class="n">layers</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
                <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">       μ=</span><span class="si">{</span><span class="n">mu</span><span class="p">[</span><span class="n">i</span><span class="p">]</span><span class="si">:</span><span class="p">.</span><span class="mi">3</span><span class="n">f</span><span class="si">}</span><span class="s">  σ=</span><span class="si">{</span><span class="n">sigma</span><span class="p">[</span><span class="n">i</span><span class="p">]</span><span class="si">:</span><span class="p">.</span><span class="mi">3</span><span class="n">f</span><span class="si">}</span><span class="s">  EI=</span><span class="si">{</span><span class="n">ei</span><span class="p">[</span><span class="n">i</span><span class="p">]</span><span class="si">:</span><span class="p">.</span><span class="mi">4</span><span class="n">f</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>

        <span class="c1"># ---- 第五步：实际评估，把新观测加进去 ----
</span>        <span class="n">y_next</span> <span class="o">=</span> <span class="nf">mock_eval</span><span class="p">(</span><span class="n">z_next</span><span class="p">,</span> <span class="n">seed</span><span class="o">=</span><span class="n">n_init</span> <span class="o">+</span> <span class="n">it</span><span class="p">)</span>
        <span class="n">Z_obs</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">vstack</span><span class="p">([</span><span class="n">Z_obs</span><span class="p">,</span> <span class="n">z_next</span><span class="p">])</span>
        <span class="n">y_obs</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">y_obs</span><span class="p">,</span> <span class="n">y_next</span><span class="p">)</span>

        <span class="k">if</span> <span class="n">it</span> <span class="o">&lt;</span> <span class="mi">5</span> <span class="ow">or</span> <span class="n">it</span> <span class="o">%</span> <span class="mi">5</span> <span class="o">==</span> <span class="mi">4</span><span class="p">:</span>
            <span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">  → 评估结果: score=</span><span class="si">{</span><span class="n">y_next</span><span class="si">:</span><span class="p">.</span><span class="mi">3</span><span class="n">f</span><span class="si">}</span><span class="s">，</span><span class="sh">"</span>
                  <span class="sa">f</span><span class="sh">"</span><span class="s">新最优=</span><span class="si">{</span><span class="n">y_obs</span><span class="p">.</span><span class="nf">max</span><span class="p">()</span><span class="si">:</span><span class="p">.</span><span class="mi">3</span><span class="n">f</span><span class="si">}</span><span class="se">\n</span><span class="sh">"</span><span class="p">)</span>

    <span class="n">best_z</span> <span class="o">=</span> <span class="n">Z_obs</span><span class="p">[</span><span class="n">y_obs</span><span class="p">.</span><span class="nf">argmax</span><span class="p">()]</span>
    <span class="k">return</span> <span class="nf">list</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="nf">where</span><span class="p">(</span><span class="n">best_z</span><span class="o">==</span><span class="mi">1</span><span class="p">)[</span><span class="mi">0</span><span class="p">]),</span> <span class="n">y_obs</span><span class="p">.</span><span class="nf">max</span><span class="p">()</span>

<span class="n">best_layers</span><span class="p">,</span> <span class="n">best_score</span> <span class="o">=</span> <span class="nf">bo_skip_layers</span><span class="p">()</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="se">\n</span><span class="s">最终最优跳层方案: </span><span class="si">{</span><span class="n">best_layers</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">最优 score: </span><span class="si">{</span><span class="n">best_score</span><span class="si">:</span><span class="p">.</span><span class="mi">4</span><span class="n">f</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <h3 id="84-打印结果解读μ-和-σ-各自在说什么">8.4 打印结果解读：μ 和 σ 各自在说什么</h3> <p>运行上面代码，会看到类似这样的输出：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>初始 8 次随机探索，最优 score=0.421，方案=[12, 15, 18, 21]

Iter  1 | 当前最优 score=0.421
  GP 最看好的前3个候选：
    #1 skip=[12, 14, 16, 18, 20, 22]
       μ=0.523  σ=0.187  EI=0.1821
    #2 skip=[9, 17, 23, 27, 29]
       μ=0.201  σ=0.412  EI=0.0934
    #3 skip=[12, 15, 18, 21]
       μ=0.468  σ=0.031  EI=0.0089
  → 评估结果: score=0.614，新最优=0.614

Iter  2 | 当前最优 score=0.614
  GP 最看好的前3个候选：
    #1 skip=[12, 14, 16, 18, 20, 22, 24]
       μ=0.651  σ=0.203  EI=0.1423
    ...
</code></pre></div></div> <p>逐行拆解，理解每个数字背后的逻辑：</p> <p><strong>第1轮 #1 候选</strong>：<code class="language-plaintext highlighter-rouge">μ=0.523, σ=0.187, EI=0.1821</code></p> <p>GP 根据 8 个观测点，推断”跳 [12,14,16,18,20,22]”的 score 大概是 0.523。这个配置和初始最优 [12,15,18,21] 有重叠，核矩阵 $K$ 判断它们”相似”，所以 μ 被”拉高”；但这个具体组合没测过，σ 不小。EI = 利用项 + 探索项，两项都有贡献，综合得分最高，<strong>BO 选它</strong>。</p> <p><strong>第1轮 #2 候选</strong>：<code class="language-plaintext highlighter-rouge">μ=0.201, σ=0.412, EI=0.0934</code></p> <p>这个配置和所有观测点都不像（核距离远），GP 没把握预测，σ 很大。EI 的探索项 $\sigma \cdot \phi(Z)$ 贡献了可观的值——BO 觉得”这块完全没探索，万一有惊喜”。但因为 μ 低，综合 EI 不如 #1。</p> <p><strong>第1轮 #3 候选</strong>：<code class="language-plaintext highlighter-rouge">μ=0.468, σ=0.031, EI=0.0089</code></p> <p>和初始最优 [12,15,18,21] 几乎完全一样，GP 预测很有把握（σ 很小）。但正因为 σ 接近零，探索项消失，整体 EI 极低——<strong>BO 不选已知区域附近的点</strong>，那是在原地踏步。</p> <p><strong>第2轮</strong>：BO 在第1轮观测到 [12,14,16,18,20,22] 得分 0.614 之后，GP 更新了对这片区域的估计，下一轮自然地往”在这个方案基础上延伸”的方向搜——这就是贝叶斯更新在驱动搜索逐步收敛到最优区域。</p> <h3 id="85-和随机搜索对比">8.5 和随机搜索对比</h3> <p>同等预算（28 次评估），两者收敛速度的对比：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>评估次数   随机搜索最优   BO最优
    8         0.421       0.421   ← 初始化阶段相同
   15         0.502       0.681   ← BO 锁定了中间偶数层是黄金区间
   21         0.534       0.851   ← 随机还在大海捞针
   28         0.589       0.951   ← BO 已接近最优，随机差了一大截
</code></pre></div></div> <p>差距的本质：随机搜索第 15 次还在不同区域瞎试，BO 第 9 次就已经知道”12~22 层是核心区域”，之后全在这里精细搜索。</p> <hr/> <h2 id="9-贝叶斯在哪里">9. 贝叶斯在哪里？</h2> <p>回头看整个流程，”贝叶斯”到底体现在哪里？</p> \[\underbrace{p(f | \mathcal{D})}_{\text{后验}} \propto \underbrace{p(\mathcal{D} | f)}_{\text{似然}} \cdot \underbrace{p(f)}_{\text{先验}}\] <ul> <li><strong>先验 $p(f)$</strong>：GP 本身就是先验——你通过选择核函数，在所有可能的函数里，指定了哪些函数”更有可能”（平滑的？周期的？）</li> <li> <table> <tbody> <tr> <td>**似然 $p(\mathcal{D}</td> <td>f)$**：观测数据，每个观测点 $y_i = f(x_i) + \epsilon$</td> </tr> </tbody> </table> </li> <li> <table> <tbody> <tr> <td>**后验 $p(f</td> <td>\mathcal{D})$**：更新后的 GP，即 <code class="language-plaintext highlighter-rouge">gp.predict()</code> 给出的 $\mu(x)$ 和 $\sigma(x)$</td> </tr> </tbody> </table> </li> </ul> <table> <tbody> <tr> <td><strong>对数边际似然的优化</strong>（核参数的学习）也是贝叶斯的一部分：通过最大化 $p(\mathcal{D}</td> <td>\theta)$（对函数积分掉，边缘化），从数据中学习核函数的超参数 $\theta = (l, \sigma_n)$。</td> </tr> </tbody> </table> <p>整个过程：先验 → 贝叶斯更新 → 后验 → 用后验指导下一步采样。这是一个完整的贝叶斯推断闭环。</p> <hr/> <h2 id="9-局限性不装">9. 局限性（不装）</h2> <p>BO 不是万能的，有几个很实际的坑：</p> <p><strong>1. 计算复杂度随观测点增长</strong>：GP 的核矩阵是 $n \times n$ 的，Cholesky 分解是 $O(n^3)$。观测点超过几千个，GP 就开始变慢。（解法：sparse GP、inducing points 方法）</p> <p><strong>2. 高维空间效果差</strong>：维度超过 20 之后，BO 的优势基本消失——候选点太稀疏，EI 的指导作用降低。（解法：REMBO 等降维方法）</p> <p><strong>3. 对核函数敏感</strong>：选错核函数，先验和实际函数差太多，收敛很慢。</p> <p><strong>4. 并行化困难</strong>：标准 BO 是串行的，每次评估完再选下一个点。并行版本（qEI）存在，但复杂不少。</p> <p>这也是为什么在超参数搜索这个具体场景，BO 最好用——维度通常不高（5~20 个超参），每次评估极贵（几小时），完全符合 BO 的适用场景。</p> <hr/> <h2 id="10-小结">10. 小结</h2> <p>从头到尾把 BO 的推导走一遍，逻辑线是这样的：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>高斯分布的条件分布有解析解
        ↓
高斯过程：把函数的无穷多个取值建模为联合高斯
        ↓
GP 预测：用条件分布公式，把观测点的信息"传播"到未观测点
   μ* = k_*^T (K + σ²I)^{-1} y       （加权插值）
   σ*² = k** - k_*^T (K + σ²I)^{-1} k*  （先验方差 - 信息增益）
        ↓
Acquisition function：用 μ 和 σ 量化"值得去哪里"
   EI = (μ - f⁺)·Φ(Z) + σ·φ(Z)       （探索+利用的自然权衡）
        ↓
主循环：拟合 GP → 选 EI 最高点 → 评估 → 更新 → 重复
</code></pre></div></div> <p>每一步都有清晰的来源，没有魔法。</p> <p>对应到 LLM 跳层搜索这个具体例子：</p> <table> <thead> <tr> <th>抽象概念</th> <th>在跳层问题里对应的东西</th> </tr> </thead> <tbody> <tr> <td>先验 $p(f)$</td> <td>Matern 核 + 只在中间层采样候选（编码了”中间层冗余”的先验知识）</td> </tr> <tr> <td>观测 $\mathcal{D}$</td> <td>已测的若干个跳层方案和对应的推理耗时</td> </tr> <tr> <td>后验 $p(f|\mathcal{D})$</td> <td>GP 预测的 μ/σ：哪些区域预计耗时低、哪些还不确定</td> </tr> <tr> <td>EI 利用项</td> <td>优先测 GP 预测效率高的配置</td> </tr> <tr> <td>EI 探索项</td> <td>顺带测一些 GP 还没把握的区域，防止错过更好的方案</td> </tr> <tr> <td>贝叶斯更新</td> <td>每测一个新配置，核矩阵和 α 更新，下一轮预测自动变准</td> </tr> </tbody> </table> <p>整个 BO 的核心思想说白了就一句话：<strong>我对这个函数有个猜测（先验），每次实验后更新猜测（贝叶斯更新），下一步去”潜力最大”的地方做实验（EI）</strong>。</p> <p>这套框架不只能用于超参数搜索——任何”评估昂贵、维度不高的黑盒优化”都可以用它：NAS 架构搜索、药物分子设计、A/B 测试参数调优等等。</p> <hr/> <blockquote> <p>如果这篇文章涉及的贝叶斯优化你想在 AutoML / NAS 场景里系统用起来，可以看看我之前出版的<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，书里有专章从贝叶斯优化到进化算法讲超参数搜索的实现，和本文是直接延续。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="贝叶斯优化"/><category term="AutoML"/><category term="数学推导"/><summary type="html"><![CDATA[贝叶斯优化从零推导：从「我对这个函数有个猜测」到自动调参]]></summary></entry><entry><title type="html">从图像到文本：Diffusion 模型原理全解——数学、结构、训练、推理一次讲清</title><link href="https://marsggbo.github.io/blog/2026/20260630-diffusion-from-image-to-text/" rel="alternate" type="text/html" title="从图像到文本：Diffusion 模型原理全解——数学、结构、训练、推理一次讲清"/><published>2026-06-30T00:00:00+00:00</published><updated>2026-06-30T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260630-diffusion-from-image-to-text</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260630-diffusion-from-image-to-text/"><![CDATA[<h1 id="从图像到文本diffusion-模型原理全解数学结构训练推理一次讲清">从图像到文本：Diffusion 模型原理全解——数学、结构、训练、推理一次讲清</h1> <hr/> <h2 id="1-前言从一个直觉开始">1. 前言：从一个直觉开始</h2> <p>你有没有想过，往一张清晰的照片里慢慢加噪声，直到变成纯噪声——这个过程能不能反过来？</p> <p>Diffusion 模型的核心思路就是：<strong>学习「加噪」的逆过程</strong>。先定义一个确定性的加噪过程（从真实数据走向纯噪声），再训练一个神经网络学会逆转它（从纯噪声还原出真实数据）。</p> <p>生成时，从随机噪声出发，一步步”去噪”，最终得到新的样本。</p> <p>这个想法可以用于图像，也可以用于文本——但两者的模型结构、输入输出形式、计算流程差别很大。本文从图像 diffusion 讲起，然后过渡到文本 diffusion，把每一步的维度变化和计算逻辑讲清楚。</p> <hr/> <h2 id="2-先把核心数学搞懂">2. 先把核心数学搞懂</h2> <h3 id="21-符号表读公式前先认清这些变量">2.1 符号表：读公式前先认清这些变量</h3> <p>在进入公式之前，先把所有符号的物理含义说清楚。后面看到这些字母，脑子里要能立刻对应上它们代表什么。</p> <table> <thead> <tr> <th>符号</th> <th>含义</th> <th>直觉</th> </tr> </thead> <tbody> <tr> <td>$x_0$</td> <td>原始干净数据（图像像素、token embedding 等）</td> <td>“未被污染的真相”</td> </tr> <tr> <td>$x_t$</td> <td>第 $t$ 步加噪后的数据</td> <td>“经过 $t$ 轮污染的样本”</td> </tr> <tr> <td>$x_T$</td> <td>最终完全噪声化的数据</td> <td>“几乎纯噪声，看不出原始内容”</td> </tr> <tr> <td>$t$</td> <td>时间步，$t \in {1, 2, \ldots, T}$</td> <td>“当前处于第几轮加噪”</td> </tr> <tr> <td>$T$</td> <td>总步数，通常取 1000</td> <td>“加噪的总轮数”</td> </tr> <tr> <td>$\beta_t$</td> <td>第 $t$ 步的<strong>噪声强度</strong>（noise schedule），$\beta_t \in (0,1)$</td> <td>“这一步要往里加多少噪声”</td> </tr> <tr> <td>$\alpha_t$</td> <td>$\alpha_t = 1 - \beta_t$，信号保留比例</td> <td>“这一步保留多少原来的信号”</td> </tr> <tr> <td>$\bar\alpha_t$</td> <td>$\bar\alpha_t = \prod_{s=1}^{t} \alpha_s$，前 $t$ 步的累积信号保留比例</td> <td>“经过 $t$ 步，原始信号还剩多少”</td> </tr> <tr> <td>$\epsilon$</td> <td>标准高斯噪声，$\epsilon \sim \mathcal{N}(0, I)$</td> <td>“每次加入的随机干扰”</td> </tr> <tr> <td>$\epsilon_\theta$</td> <td>神经网络，参数为 $\theta$</td> <td>“用来预测噪声的模型”</td> </tr> </tbody> </table> <blockquote> <p><strong>关于 noise schedule</strong>：$\beta_t$ 不是随机的，而是人为预先设计好的序列。最简单的是线性 schedule：从 $\beta_1 = 10^{-4}$ 线性增大到 $\beta_T = 0.02$。意思是：<strong>早期步骤（小 $t$）加噪很轻微，后期步骤（大 $t$）加噪越来越猛</strong>。这样设计是为了让逆向去噪在每一步都是一个可处理的小问题，而不是一步从纯噪声跳回清晰图像。</p> </blockquote> <h3 id="22-前向过程加噪">2.2 前向过程（加噪）</h3> <p>前向过程是固定的，<strong>不需要学习</strong>。给定一张真实图像 $x_0$，逐步向它加高斯噪声：</p> \[q(x_t \mid x_{t-1}) = \mathcal{N}\!\left(x_t;\; \sqrt{1-\beta_t}\, x_{t-1},\; \beta_t I\right)\] <p><strong>这个公式的白话翻译</strong>：从 $x_{t-1}$ 生成 $x_t$ 时，先把 $x_{t-1}$ 缩小到 $\sqrt{1-\beta_t}$ 倍（信号衰减），然后加上方差为 $\beta_t$ 的高斯噪声。$\beta_t$ 越大，信号衰减越多，噪声越大。</p> <blockquote> <p>这个公式用到了条件高斯分布：$\mathcal{N}(x; \mu, \sigma^2 I)$ 表示均值为 $\mu$、方差为 $\sigma^2$ 的高斯分布。从这个分布采样等价于：$x = \mu + \sigma \cdot \epsilon$，其中 $\epsilon \sim \mathcal{N}(0, I)$。</p> </blockquote> <p>这个过程有一个非常好用的性质：<strong>可以直接从 $x_0$ 一步跳到任意时刻 $t$</strong>，不需要逐步走 $t$ 次。</p> <p>令 $\alpha_t = 1 - \beta_t$，$\bar\alpha_t = \prod_{s=1}^{t} \alpha_s$，则：</p> \[q(x_t \mid x_0) = \mathcal{N}(x_t;\; \sqrt{\bar\alpha_t}\, x_0,\; (1-\bar\alpha_t) I)\] <p>写成采样形式（实际代码里用这个）：</p> \[\boxed{x_t = \sqrt{\bar\alpha_t}\, x_0 + \sqrt{1 - \bar\alpha_t}\, \epsilon, \quad \epsilon \sim \mathcal{N}(0, I)}\] <p><strong>直觉解读</strong>：</p> <ul> <li>$\sqrt{\bar\alpha_t}\, x_0$：原始信号按 $\sqrt{\bar\alpha_t}$ 缩放后的残留部分</li> <li>$\sqrt{1 - \bar\alpha_t}\, \epsilon$：加入的噪声部分</li> <li>$t$ 越大 → $\bar\alpha_t$ 越小 → 信号保留越少，噪声越多</li> <li>$t = T$ 时 $\bar\alpha_T \approx 0$，于是 $x_T \approx \epsilon \sim \mathcal{N}(0,I)$，彻底变成纯噪声</li> </ul> <p>为什么可以一步跳到？因为多个高斯分布的叠加仍然是高斯分布，可以把递推关系合并成一个解析式。这个性质让训练极其高效——不需要真的走 $t$ 步，只需算一次 $\bar\alpha_t$。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># 预计算 noise schedule（在训练开始前做一次）
</span><span class="n">T</span> <span class="o">=</span> <span class="mi">1000</span>
<span class="n">betas</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">linspace</span><span class="p">(</span><span class="mf">1e-4</span><span class="p">,</span> <span class="mf">0.02</span><span class="p">,</span> <span class="n">T</span><span class="p">)</span>       <span class="c1"># β_1, β_2, ..., β_T，线性增长
</span><span class="n">alphas</span> <span class="o">=</span> <span class="mi">1</span> <span class="o">-</span> <span class="n">betas</span>                            <span class="c1"># α_t = 1 - β_t
</span><span class="n">alpha_bar</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cumprod</span><span class="p">(</span><span class="n">alphas</span><span class="p">,</span> <span class="n">dim</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>      <span class="c1"># ᾱ_t = α_1 * α_2 * ... * α_t
</span>
<span class="c1"># 给定 x0 和时间步 t，一步得到 x_t（即上面的采样公式）
</span><span class="k">def</span> <span class="nf">q_sample</span><span class="p">(</span><span class="n">x0</span><span class="p">,</span> <span class="n">t</span><span class="p">):</span>
    <span class="c1"># x0: [B, C, H, W]，t: [B]
</span>    <span class="n">eps</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn_like</span><span class="p">(</span><span class="n">x0</span><span class="p">)</span>                           <span class="c1"># ε ~ N(0,I)
</span>    <span class="n">sqrt_ab</span>    <span class="o">=</span> <span class="n">alpha_bar</span><span class="p">[</span><span class="n">t</span><span class="p">][:,</span> <span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">].</span><span class="nf">sqrt</span><span class="p">()</span>  <span class="c1"># √ᾱ_t
</span>    <span class="n">sqrt_1_ab</span>  <span class="o">=</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="n">alpha_bar</span><span class="p">[</span><span class="n">t</span><span class="p">])[:,</span> <span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">].</span><span class="nf">sqrt</span><span class="p">()</span>  <span class="c1"># √(1-ᾱ_t)
</span>    <span class="n">x_t</span> <span class="o">=</span> <span class="n">sqrt_ab</span> <span class="o">*</span> <span class="n">x0</span> <span class="o">+</span> <span class="n">sqrt_1_ab</span> <span class="o">*</span> <span class="n">eps</span>                <span class="c1"># 公式直接翻译
</span>    <span class="k">return</span> <span class="n">x_t</span><span class="p">,</span> <span class="n">eps</span>
</code></pre></div></div> <h3 id="23-反向过程去噪">2.3 反向过程（去噪）</h3> <p>反向过程是我们要学习的。我们想要 $q(x_{t-1} \mid x_t)$——已知加了 $t$ 步噪声的 $x_t$，推断出少加一步噪声的 $x_{t-1}$。</p> <p>这个分布依赖于整个数据集，<strong>无法直接计算</strong>。但当 $\beta_t$ 很小时，反向过程也近似是高斯的（这是 diffusion 模型一个关键的数学洞见），于是我们用神经网络来拟合它：</p> \[p_\theta(x_{t-1} \mid x_t) = \mathcal{N}(x_{t-1};\; \mu_\theta(x_t, t),\; \Sigma_\theta(x_t, t))\] <p><strong>训练策略</strong>：不直接预测均值 $\mu_\theta$，而是训练神经网络 $\epsilon_\theta$ 来预测被加入的噪声 $\epsilon$。已知 $x_t$ 和预测的噪声 $\hat\epsilon$，可以反推出 $x_0$，进而推出均值：</p> \[\mu_\theta(x_t, t) = \frac{1}{\sqrt{\alpha_t}}\!\left(x_t - \frac{1-\alpha_t}{\sqrt{1-\bar\alpha_t}}\, \epsilon_\theta(x_t, t)\right)\] <p><strong>这个公式的来源</strong>：把 $x_t = \sqrt{\bar\alpha_t} x_0 + \sqrt{1-\bar\alpha_t}\epsilon$ 代入贝叶斯后验 $q(x_{t-1} \mid x_t, x_0)$ 的均值表达式，再用 $\hat\epsilon$ 替换 $\epsilon$，就得到上式。</p> <h3 id="24-训练目标">2.4 训练目标</h3> <p>经过变分推导，最终的损失函数简化为一个极其简洁的形式：</p> \[\boxed{\mathcal{L} = \mathbb{E}_{x_0,\, \epsilon,\, t}\!\left[\|\epsilon - \epsilon_\theta(x_t, t)\|^2\right]}\] <p><strong>说人话</strong>：</p> <ol> <li>从数据集随机取一个真实样本 $x_0$</li> <li>随机取一个时间步 $t$</li> <li>采样随机噪声 $\epsilon \sim \mathcal{N}(0,I)$</li> <li>算出加噪后的 $x_t = \sqrt{\bar\alpha_t} x_0 + \sqrt{1-\bar\alpha_t} \epsilon$</li> <li>把 $x_t$ 和 $t$ 喂给网络，让它预测 $\hat\epsilon = \epsilon_\theta(x_t, t)$</li> <li>用 MSE 衡量预测误差</li> </ol> <p>整个训练目标就是一个<strong>预测噪声的回归问题</strong>，比 GAN 的博弈稳定得多，比 VAE 的 ELBO 也更简洁。</p> <hr/> <h2 id="3-图像-diffusionunet-结构">3. 图像 Diffusion：UNet 结构</h2> <h3 id="31-为什么用-unet">3.1 为什么用 UNet</h3> <p>图像 diffusion（DDPM、Stable Diffusion 的噪声预测部分）用 UNet 作为去噪网络 $\epsilon_\theta$。</p> <p><strong>输入</strong>：加了噪声的图像 $x_t$，形状 <code class="language-plaintext highlighter-rouge">[B, C, H, W]</code> <strong>输出</strong>：预测的噪声 $\hat\epsilon$，形状 <code class="language-plaintext highlighter-rouge">[B, C, H, W]</code>（与输入完全相同）</p> <p>UNet 是编码器-解码器结构，有跳跃连接（skip connections），能同时处理全局语义（”这里是人脸”）和局部细节（”这几个像素是眼睛”），这正好符合图像去噪的需求。</p> <h3 id="32-时间步-t-怎么注入">3.2 时间步 t 怎么注入</h3> <p>网络需要知道当前是第几步（$t$），因为不同时刻的噪声程度不同，去噪策略也不同（$t=999$ 几乎全是噪声，需要大幅修改；$t=1$ 接近干净图像，只需微调）。</p> <p>做法：把标量 $t$ 编码成一个向量，加进网络的每一层。编码方式和 Transformer 里的位置编码一样——Sinusoidal 编码。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">timestep_embedding</span><span class="p">(</span><span class="n">t</span><span class="p">,</span> <span class="n">dim</span><span class="p">):</span>
    <span class="c1"># t: [B]，整数时间步
</span>    <span class="c1"># 输出: [B, dim]，每个时间步对应一个 dim 维向量
</span>    <span class="n">half</span> <span class="o">=</span> <span class="n">dim</span> <span class="o">//</span> <span class="mi">2</span>
    <span class="c1"># 生成 half 个频率（从高频到低频）
</span>    <span class="n">freqs</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="o">-</span><span class="n">math</span><span class="p">.</span><span class="nf">log</span><span class="p">(</span><span class="mi">10000</span><span class="p">)</span> <span class="o">*</span> <span class="n">torch</span><span class="p">.</span><span class="nf">arange</span><span class="p">(</span><span class="n">half</span><span class="p">)</span> <span class="o">/</span> <span class="n">half</span><span class="p">)</span>
    <span class="n">args</span> <span class="o">=</span> <span class="n">t</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">].</span><span class="nf">float</span><span class="p">()</span> <span class="o">*</span> <span class="n">freqs</span><span class="p">[</span><span class="bp">None</span><span class="p">]</span>   <span class="c1"># [B, half]，时间步 × 频率
</span>    <span class="c1"># 用 cos 和 sin 各表示一半维度，拼在一起
</span>    <span class="n">emb</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">([</span><span class="n">torch</span><span class="p">.</span><span class="nf">cos</span><span class="p">(</span><span class="n">args</span><span class="p">),</span> <span class="n">torch</span><span class="p">.</span><span class="nf">sin</span><span class="p">(</span><span class="n">args</span><span class="p">)],</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>  <span class="c1"># [B, dim]
</span>    <span class="k">return</span> <span class="n">emb</span>

<span class="c1"># 在 UNet 的每个 ResBlock 里，把时间 embedding 加到特征图上
</span><span class="k">class</span> <span class="nc">ResBlock</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">channels</span><span class="p">,</span> <span class="n">t_dim</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">conv</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Conv2d</span><span class="p">(</span><span class="n">channels</span><span class="p">,</span> <span class="n">channels</span><span class="p">,</span> <span class="mi">3</span><span class="p">,</span> <span class="n">padding</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>
        <span class="c1"># 把 t_dim 维的时间向量投影到 channels 维，然后加到特征图
</span>        <span class="n">self</span><span class="p">.</span><span class="n">time_proj</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">t_dim</span><span class="p">,</span> <span class="n">channels</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">,</span> <span class="n">t_emb</span><span class="p">):</span>
        <span class="c1"># x:     [B, C, H, W]  当前特征图
</span>        <span class="c1"># t_emb: [B, t_dim]    时间步 embedding
</span>        <span class="n">scale</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">time_proj</span><span class="p">(</span><span class="n">t_emb</span><span class="p">)[:,</span> <span class="p">:,</span> <span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">]</span>  <span class="c1"># [B, C, 1, 1]
</span>        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">conv</span><span class="p">(</span><span class="n">x</span><span class="p">)</span> <span class="o">+</span> <span class="n">scale</span>   <span class="c1"># 广播加到每个空间位置
</span></code></pre></div></div> <h3 id="33-完整训练公式对照代码">3.3 完整训练：公式对照代码</h3> <p>下面把训练流程的数学和代码并排放，方便对照：</p> <p><strong>数学步骤：</strong></p> <p>\(t \sim \text{Uniform}\{1,\ldots,T\}, \quad \epsilon \sim \mathcal{N}(0,I)\) \(x_t = \sqrt{\bar\alpha_t}\, x_0 + \sqrt{1-\bar\alpha_t}\, \epsilon\) \(\mathcal{L} = \|\epsilon - \epsilon_\theta(x_t, t)\|^2\)</p> <p><strong>对应代码：</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">T</span> <span class="o">=</span> <span class="mi">1000</span>
<span class="n">betas</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">linspace</span><span class="p">(</span><span class="mf">1e-4</span><span class="p">,</span> <span class="mf">0.02</span><span class="p">,</span> <span class="n">T</span><span class="p">)</span>   <span class="c1"># β_t，线性 schedule
</span><span class="n">alphas</span> <span class="o">=</span> <span class="mi">1</span> <span class="o">-</span> <span class="n">betas</span>                        <span class="c1"># α_t = 1 - β_t
</span><span class="n">alpha_bar</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cumprod</span><span class="p">(</span><span class="n">alphas</span><span class="p">,</span> <span class="n">dim</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>  <span class="c1"># ᾱ_t = ∏α_s
</span>
<span class="k">def</span> <span class="nf">train_step</span><span class="p">(</span><span class="n">x0</span><span class="p">,</span> <span class="n">unet</span><span class="p">):</span>
    <span class="c1"># x0: [B, C, H, W]，像素值归一化到 [-1, 1]
</span>    <span class="n">B</span> <span class="o">=</span> <span class="n">x0</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">0</span><span class="p">]</span>

    <span class="c1"># 步骤1：t ~ Uniform{1,...,T}
</span>    <span class="n">t</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randint</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">T</span><span class="p">,</span> <span class="p">(</span><span class="n">B</span><span class="p">,))</span>               <span class="c1"># [B]，每个样本随机一个时间步
</span>
    <span class="c1"># 步骤2：ε ~ N(0,I)，然后算 x_t（前向公式的直接实现）
</span>    <span class="n">eps</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn_like</span><span class="p">(</span><span class="n">x0</span><span class="p">)</span>                  <span class="c1"># [B, C, H, W]
</span>    <span class="n">sqrt_ab</span>   <span class="o">=</span> <span class="n">alpha_bar</span><span class="p">[</span><span class="n">t</span><span class="p">][:,</span> <span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">].</span><span class="nf">sqrt</span><span class="p">()</span>      <span class="c1"># √ᾱ_t，形状 [B,1,1,1]
</span>    <span class="n">sqrt_1_ab</span> <span class="o">=</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="n">alpha_bar</span><span class="p">[</span><span class="n">t</span><span class="p">])[:,</span> <span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">].</span><span class="nf">sqrt</span><span class="p">()</span>
    <span class="n">x_t</span> <span class="o">=</span> <span class="n">sqrt_ab</span> <span class="o">*</span> <span class="n">x0</span> <span class="o">+</span> <span class="n">sqrt_1_ab</span> <span class="o">*</span> <span class="n">eps</span>        <span class="c1"># [B, C, H, W]，加噪后的图像
</span>
    <span class="c1"># 步骤3：网络预测噪声 ε_θ(x_t, t)
</span>    <span class="n">eps_pred</span> <span class="o">=</span> <span class="nf">unet</span><span class="p">(</span><span class="n">x_t</span><span class="p">,</span> <span class="n">t</span><span class="p">)</span>                     <span class="c1"># [B, C, H, W]
</span>
    <span class="c1"># 步骤4：MSE 损失，即 ||ε - ε_θ||²
</span>    <span class="n">loss</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">mse_loss</span><span class="p">(</span><span class="n">eps_pred</span><span class="p">,</span> <span class="n">eps</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">loss</span>
</code></pre></div></div> <h3 id="34-推理从噪声还原图像">3.4 推理：从噪声还原图像</h3> <p><strong>数学步骤：</strong></p> <p>从 $x_T \sim \mathcal{N}(0,I)$ 出发，每一步：</p> \[\mu_t = \frac{1}{\sqrt{\alpha_t}}\!\left(x_t - \frac{1-\alpha_t}{\sqrt{1-\bar\alpha_t}}\hat\epsilon\right), \quad \hat\epsilon = \epsilon_\theta(x_t, t)\] \[x_{t-1} = \mu_t + \sqrt{\beta_t}\, z, \quad z \sim \mathcal{N}(0,I) \quad (t &gt; 0)\] <p><strong>对应代码：</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@torch.no_grad</span><span class="p">()</span>
<span class="k">def</span> <span class="nf">ddpm_sample</span><span class="p">(</span><span class="n">unet</span><span class="p">,</span> <span class="n">shape</span><span class="p">):</span>
    <span class="c1"># shape: (B, C, H, W)
</span>    <span class="n">x</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="n">shape</span><span class="p">)</span>   <span class="c1"># x_T ~ N(0, I)，从纯噪声出发
</span>
    <span class="k">for</span> <span class="n">t</span> <span class="ow">in</span> <span class="nf">reversed</span><span class="p">(</span><span class="nf">range</span><span class="p">(</span><span class="n">T</span><span class="p">)):</span>    <span class="c1"># t = T-1, T-2, ..., 1, 0
</span>        <span class="n">t_batch</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">full</span><span class="p">((</span><span class="n">shape</span><span class="p">[</span><span class="mi">0</span><span class="p">],),</span> <span class="n">t</span><span class="p">)</span>

        <span class="c1"># 用网络预测噪声 ε̂ = ε_θ(x_t, t)
</span>        <span class="n">eps_pred</span> <span class="o">=</span> <span class="nf">unet</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">t_batch</span><span class="p">)</span>         <span class="c1"># [B, C, H, W]
</span>
        <span class="c1"># 计算均值 μ_t（对应上面公式）
</span>        <span class="n">a</span>  <span class="o">=</span> <span class="n">alphas</span><span class="p">[</span><span class="n">t</span><span class="p">]</span>        <span class="c1"># α_t
</span>        <span class="n">ab</span> <span class="o">=</span> <span class="n">alpha_bar</span><span class="p">[</span><span class="n">t</span><span class="p">]</span>     <span class="c1"># ᾱ_t
</span>        <span class="n">mu</span> <span class="o">=</span> <span class="p">(</span><span class="n">x</span> <span class="o">-</span> <span class="p">(</span><span class="mi">1</span><span class="o">-</span><span class="n">a</span><span class="p">)</span> <span class="o">/</span> <span class="p">(</span><span class="mi">1</span><span class="o">-</span><span class="n">ab</span><span class="p">).</span><span class="nf">sqrt</span><span class="p">()</span> <span class="o">*</span> <span class="n">eps_pred</span><span class="p">)</span> <span class="o">/</span> <span class="n">a</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">()</span>

        <span class="c1"># x_{t-1} = μ_t + √β_t * z（最后一步 t=0 不加噪声）
</span>        <span class="k">if</span> <span class="n">t</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">:</span>
            <span class="n">z</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn_like</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
            <span class="n">x</span> <span class="o">=</span> <span class="n">mu</span> <span class="o">+</span> <span class="n">betas</span><span class="p">[</span><span class="n">t</span><span class="p">].</span><span class="nf">sqrt</span><span class="p">()</span> <span class="o">*</span> <span class="n">z</span>
        <span class="k">else</span><span class="p">:</span>
            <span class="n">x</span> <span class="o">=</span> <span class="n">mu</span>    <span class="c1"># t=0 时直接返回均值，不加额外噪声
</span>
    <span class="k">return</span> <span class="n">x</span>   <span class="c1"># [B, C, H, W]，去噪后的图像
</span></code></pre></div></div> <p><strong>维度流</strong>（以 <code class="language-plaintext highlighter-rouge">C=3, H=W=64</code> 为例）：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>输入：[B, 3, 64, 64]（纯噪声 x_T）
  ↓ UNet encoder（下采样，增加通道数）
  [B,  64, 64, 64]
  [B, 128, 32, 32]
  [B, 256, 16, 16]
  ↓ bottleneck（可加 self-attention）
  [B, 256, 16, 16]
  ↓ UNet decoder（上采样 + skip connection，恢复空间分辨率）
  [B, 128, 32, 32]
  [B,  64, 64, 64]
  ↓ 输出头（1×1 卷积，把通道数变回 C=3）
输出：[B, 3, 64, 64]（预测的噪声 ε̂，与输入同形状）
</code></pre></div></div> <hr/> <h2 id="4-从图像到文本最大的区别在哪里">4. 从图像到文本：最大的区别在哪里</h2> <p>图像是<strong>连续空间</strong>里的信号，像素值是实数，加高斯噪声天然合法。</p> <p>文本是<strong>离散空间</strong>——token id 是整数（比如”猫”对应 id=3421），<strong>你不能在两个整数之间做线性插值</strong>。想象一下 <code class="language-plaintext highlighter-rouge">"猫" × 0.3 + "狗" × 0.7</code>——这在整数空间毫无意义。</p> <p>这就是文本 diffusion 最核心的挑战。目前主要有两条技术路线：</p> <table> <thead> <tr> <th> </th> <th>图像 Diffusion</th> <th>连续文本 Diffusion</th> <th>离散 Masked Diffusion</th> </tr> </thead> <tbody> <tr> <td>操作对象</td> <td>像素值（连续）</td> <td>token embedding（连续）</td> <td>token id（离散）</td> </tr> <tr> <td>噪声类型</td> <td>高斯噪声</td> <td>高斯噪声</td> <td>Mask / 替换</td> </tr> <tr> <td>网络结构</td> <td>UNet</td> <td>Transformer</td> <td>Transformer</td> </tr> <tr> <td>输出</td> <td>预测噪声 $\hat\epsilon$，形状同输入</td> <td>预测噪声（或 $x_0$），形状同输入</td> <td>每个位置的 token logits</td> </tr> <tr> <td>损失函数</td> <td>MSE</td> <td>MSE</td> <td>Cross Entropy</td> </tr> <tr> <td>解码方式</td> <td>直接读像素值</td> <td>找最近邻 token</td> <td>argmax</td> </tr> </tbody> </table> <hr/> <h2 id="5-连续空间文本-diffusionembedding-space">5. 连续空间文本 Diffusion（Embedding Space）</h2> <h3 id="51-思路在-embedding-上加噪声">5.1 思路：在 embedding 上加噪声</h3> <p>既然 token id 是离散的，加不了噪声，那就把每个 token 先转换成连续的 embedding 向量，然后在 embedding 空间里做扩散。</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>输入序列（token ids）：[B, L]
      ↓ embedding 层（查表，每个 id 变成 d 维向量）
token embeddings：    [B, L, d_model]   ← 在这里做 diffusion
      ↓ 加噪 / 去噪（T 步）
去噪后的 embeddings：  [B, L, d_model]
      ↓ 解码回 token id（见 5.5 节）
输出序列（token ids）：[B, L]
</code></pre></div></div> <p><strong>注意 embedding 归一化</strong>：训练时通常把 embedding 归一化到单位球面（$|x_0| = 1$），这样能让信号和噪声的量级相当——否则如果 embedding 的模很大，噪声占比会很小，信噪比失衡，训练不稳定。</p> <h3 id="52-前向加噪文本版数学和代码对照">5.2 前向加噪（文本版）：数学和代码对照</h3> <p>文本版的前向过程数学公式和图像版<strong>完全相同</strong>，只是维度从 <code class="language-plaintext highlighter-rouge">[B, C, H, W]</code> 变成了 <code class="language-plaintext highlighter-rouge">[B, L, d]</code>：</p> <p>\(q(x_t \mid x_0) = \mathcal{N}(x_t;\; \sqrt{\bar\alpha_t}\, x_0,\; (1-\bar\alpha_t) I)\) \(x_t = \sqrt{\bar\alpha_t}\, x_0 + \sqrt{1-\bar\alpha_t}\, \epsilon, \quad \epsilon \sim \mathcal{N}(0, I)\)</p> <p>这里 $x_0 \in \mathbb{R}^{L \times d}$ 是归一化后的 token embedding 矩阵，$\epsilon \in \mathbb{R}^{L \times d}$ 是同形状的高斯噪声。序列中每个位置、每个维度独立地加噪声。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">forward_diffuse_text</span><span class="p">(</span><span class="n">x0</span><span class="p">,</span> <span class="n">t</span><span class="p">,</span> <span class="n">alpha_bar</span><span class="p">):</span>
    <span class="c1"># x0: [B, L, d]，归一化的 token embeddings
</span>    <span class="c1"># t:  [B]
</span>    <span class="n">eps</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn_like</span><span class="p">(</span><span class="n">x0</span><span class="p">)</span>              <span class="c1"># [B, L, d]，ε ~ N(0,I)
</span>    <span class="n">ab</span>  <span class="o">=</span> <span class="n">alpha_bar</span><span class="p">[</span><span class="n">t</span><span class="p">][:,</span> <span class="bp">None</span><span class="p">,</span> <span class="bp">None</span><span class="p">]</span>       <span class="c1"># [B, 1, 1]，广播到 [B, L, d]
</span>    <span class="c1"># 公式直接翻译：x_t = √ᾱ_t * x0 + √(1-ᾱ_t) * ε
</span>    <span class="n">x_t</span> <span class="o">=</span> <span class="n">ab</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">()</span> <span class="o">*</span> <span class="n">x0</span> <span class="o">+</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="n">ab</span><span class="p">).</span><span class="nf">sqrt</span><span class="p">()</span> <span class="o">*</span> <span class="n">eps</span>
    <span class="k">return</span> <span class="n">x_t</span><span class="p">,</span> <span class="n">eps</span>   <span class="c1"># x_t: [B, L, d]，形状始终不变
</span></code></pre></div></div> <blockquote> <p><strong>图像 vs 文本</strong>：形状的区别是 <code class="language-plaintext highlighter-rouge">[B, C, H, W]</code> vs <code class="language-plaintext highlighter-rouge">[B, L, d]</code>。图像的空间结构是二维的（H×W），文本的是一维序列（L）。加噪公式和 $\alpha/\beta$ 的含义完全一样，只是张量维度不同。</p> </blockquote> <h3 id="53-去噪网络为什么换成-transformer">5.3 去噪网络：为什么换成 Transformer</h3> <p>图像 UNet 用局部卷积（每个像素只看周围的邻居）处理空间结构，而文本需要<strong>全局依赖</strong>——理解”猫”这个词的含义，需要看整句话的上下文，不是只看左右两个位置。</p> <p>因此去噪网络 $\epsilon_\theta$ 换成 Transformer（BERT 风格的双向 Encoder），用 self-attention 让序列中每个位置都能看到所有其他位置。</p> <p><strong>数学上</strong>，去噪网络 $\epsilon_\theta(x_t, t)$ 的接口没有变：输入是加噪后的 $x_t$ 和时间步 $t$，输出是预测的噪声（形状与 $x_t$ 相同）。变的只是内部实现（从 UNet 换成 Transformer）。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">TextDiffusionTransformer</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">d_model</span><span class="p">,</span> <span class="n">nhead</span><span class="p">,</span> <span class="n">num_layers</span><span class="p">,</span> <span class="n">vocab_size</span><span class="p">,</span> <span class="n">max_len</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">embed</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Embedding</span><span class="p">(</span><span class="n">vocab_size</span><span class="p">,</span> <span class="n">d_model</span><span class="p">)</span>   <span class="c1"># token id → embedding
</span>        <span class="n">self</span><span class="p">.</span><span class="n">pos_embed</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Embedding</span><span class="p">(</span><span class="n">max_len</span><span class="p">,</span> <span class="n">d_model</span><span class="p">)</span>  <span class="c1"># 位置 embedding
</span>        <span class="n">self</span><span class="p">.</span><span class="n">time_embed</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Sequential</span><span class="p">(</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">d_model</span><span class="p">,</span> <span class="n">d_model</span> <span class="o">*</span> <span class="mi">4</span><span class="p">),</span> <span class="n">nn</span><span class="p">.</span><span class="nc">SiLU</span><span class="p">(),</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">d_model</span> <span class="o">*</span> <span class="mi">4</span><span class="p">,</span> <span class="n">d_model</span><span class="p">),</span>
        <span class="p">)</span>
        <span class="n">encoder_layer</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">TransformerEncoderLayer</span><span class="p">(</span><span class="n">d_model</span><span class="p">,</span> <span class="n">nhead</span><span class="p">,</span> <span class="n">batch_first</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">transformer</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">TransformerEncoder</span><span class="p">(</span><span class="n">encoder_layer</span><span class="p">,</span> <span class="n">num_layers</span><span class="p">)</span>
        <span class="c1"># 输出仍是 embedding 空间（d_model 维），不是 vocab logits
</span>        <span class="n">self</span><span class="p">.</span><span class="n">out_proj</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">d_model</span><span class="p">,</span> <span class="n">d_model</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x_t</span><span class="p">,</span> <span class="n">t</span><span class="p">):</span>
        <span class="c1"># x_t: [B, L, d]  加噪后的 embedding（连续值，不是 token id）
</span>        <span class="c1"># t:   [B]         时间步
</span>        <span class="n">B</span><span class="p">,</span> <span class="n">L</span><span class="p">,</span> <span class="n">d</span> <span class="o">=</span> <span class="n">x_t</span><span class="p">.</span><span class="n">shape</span>

        <span class="c1"># 时间步编码：标量 t → d 维向量，广播加到序列每个位置
</span>        <span class="n">t_emb</span> <span class="o">=</span> <span class="nf">timestep_embedding</span><span class="p">(</span><span class="n">t</span><span class="p">,</span> <span class="n">d</span><span class="p">)</span>        <span class="c1"># [B, d]
</span>        <span class="n">t_emb</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">time_embed</span><span class="p">(</span><span class="n">t_emb</span><span class="p">)</span>          <span class="c1"># [B, d]，经过 MLP 变换
</span>        <span class="n">t_emb</span> <span class="o">=</span> <span class="n">t_emb</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">,</span> <span class="p">:]</span>               <span class="c1"># [B, 1, d] → 广播到 [B, L, d]
</span>
        <span class="c1"># 位置编码
</span>        <span class="n">pos</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">arange</span><span class="p">(</span><span class="n">L</span><span class="p">,</span> <span class="n">device</span><span class="o">=</span><span class="n">x_t</span><span class="p">.</span><span class="n">device</span><span class="p">)</span>
        <span class="n">p_emb</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">pos_embed</span><span class="p">(</span><span class="n">pos</span><span class="p">)[</span><span class="bp">None</span><span class="p">]</span>       <span class="c1"># [1, L, d]
</span>
        <span class="c1"># 融合：加噪 embedding + 时间信息 + 位置信息
</span>        <span class="n">h</span> <span class="o">=</span> <span class="n">x_t</span> <span class="o">+</span> <span class="n">t_emb</span> <span class="o">+</span> <span class="n">p_emb</span>                <span class="c1"># [B, L, d]
</span>
        <span class="c1"># 双向 self-attention：每个位置都能看到整个序列
</span>        <span class="n">h</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">transformer</span><span class="p">(</span><span class="n">h</span><span class="p">)</span>                 <span class="c1"># [B, L, d]
</span>
        <span class="c1"># 输出：预测噪声 ε̂（也在 embedding 空间，和 x_t 同维度）
</span>        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">out_proj</span><span class="p">(</span><span class="n">h</span><span class="p">)</span>                 <span class="c1"># [B, L, d]
</span></code></pre></div></div> <h3 id="54-训练文本连续-diffusion数学和代码对照">5.4 训练（文本连续 diffusion）：数学和代码对照</h3> <p><strong>数学：</strong></p> <p>\(x_0 = \text{normalize}(E[\text{token\_ids}]) \quad \in \mathbb{R}^{L \times d}\) \(t \sim \text{Uniform}\{1,\ldots,T\}, \quad \epsilon \sim \mathcal{N}(0,I)\) \(x_t = \sqrt{\bar\alpha_t}\, x_0 + \sqrt{1-\bar\alpha_t}\, \epsilon\) \(\mathcal{L} = \|\epsilon - \epsilon_\theta(x_t, t)\|^2\)</p> <p><strong>代码：</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">train_step_text_continuous</span><span class="p">(</span><span class="n">token_ids</span><span class="p">,</span> <span class="n">model</span><span class="p">,</span> <span class="n">alpha_bar</span><span class="p">):</span>
    <span class="c1"># token_ids: [B, L]，整数 token id
</span>    <span class="n">B</span><span class="p">,</span> <span class="n">L</span> <span class="o">=</span> <span class="n">token_ids</span><span class="p">.</span><span class="n">shape</span>

    <span class="c1"># 步骤1：token id → embedding，并归一化（让量级和噪声匹配）
</span>    <span class="n">x0</span> <span class="o">=</span> <span class="n">model</span><span class="p">.</span><span class="nf">embed</span><span class="p">(</span><span class="n">token_ids</span><span class="p">)</span>              <span class="c1"># [B, L, d]
</span>    <span class="n">x0</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">normalize</span><span class="p">(</span><span class="n">x0</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>            <span class="c1"># 归一化到单位球面
</span>
    <span class="c1"># 步骤2：随机时间步 + 加噪
</span>    <span class="n">t</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randint</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">T</span><span class="p">,</span> <span class="p">(</span><span class="n">B</span><span class="p">,))</span>
    <span class="n">x_t</span><span class="p">,</span> <span class="n">eps</span> <span class="o">=</span> <span class="nf">forward_diffuse_text</span><span class="p">(</span><span class="n">x0</span><span class="p">,</span> <span class="n">t</span><span class="p">,</span> <span class="n">alpha_bar</span><span class="p">)</span>   <span class="c1"># [B, L, d]
</span>
    <span class="c1"># 步骤3：预测噪声
</span>    <span class="n">eps_pred</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">x_t</span><span class="p">,</span> <span class="n">t</span><span class="p">)</span>                <span class="c1"># [B, L, d]
</span>
    <span class="c1"># 步骤4：MSE 损失（和图像版完全相同的公式）
</span>    <span class="n">loss</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">mse_loss</span><span class="p">(</span><span class="n">eps_pred</span><span class="p">,</span> <span class="n">eps</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">loss</span>
</code></pre></div></div> <h3 id="55-推理--解码回-token最近邻是怎么回事">5.5 推理 + 解码回 token：最近邻是怎么回事？</h3> <p><strong>去噪过程</strong>（和图像版数学完全相同，只是维度不同）：</p> \[\mu_t = \frac{1}{\sqrt{\alpha_t}}\!\left(x_t - \frac{1-\alpha_t}{\sqrt{1-\bar\alpha_t}}\hat\epsilon\right), \quad x_{t-1} = \mu_t + \sqrt{\beta_t}\, z\] <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@torch.no_grad</span><span class="p">()</span>
<span class="k">def</span> <span class="nf">text_diffusion_sample</span><span class="p">(</span><span class="n">model</span><span class="p">,</span> <span class="n">B</span><span class="p">,</span> <span class="n">L</span><span class="p">,</span> <span class="n">d</span><span class="p">):</span>
    <span class="n">x</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn</span><span class="p">(</span><span class="n">B</span><span class="p">,</span> <span class="n">L</span><span class="p">,</span> <span class="n">d</span><span class="p">)</span>    <span class="c1"># 从噪声出发，x_T ~ N(0, I)
</span>
    <span class="k">for</span> <span class="n">t</span> <span class="ow">in</span> <span class="nf">reversed</span><span class="p">(</span><span class="nf">range</span><span class="p">(</span><span class="n">T</span><span class="p">)):</span>
        <span class="n">t_batch</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">full</span><span class="p">((</span><span class="n">B</span><span class="p">,),</span> <span class="n">t</span><span class="p">)</span>
        <span class="n">eps_pred</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">t_batch</span><span class="p">)</span>         <span class="c1"># [B, L, d]
</span>
        <span class="c1"># DDPM 去噪公式（与图像版完全相同）
</span>        <span class="n">a</span>  <span class="o">=</span> <span class="n">alphas</span><span class="p">[</span><span class="n">t</span><span class="p">]</span>
        <span class="n">ab</span> <span class="o">=</span> <span class="n">alpha_bar</span><span class="p">[</span><span class="n">t</span><span class="p">]</span>
        <span class="n">mu</span> <span class="o">=</span> <span class="p">(</span><span class="n">x</span> <span class="o">-</span> <span class="p">(</span><span class="mi">1</span><span class="o">-</span><span class="n">a</span><span class="p">)</span><span class="o">/</span><span class="p">(</span><span class="mi">1</span><span class="o">-</span><span class="n">ab</span><span class="p">).</span><span class="nf">sqrt</span><span class="p">()</span> <span class="o">*</span> <span class="n">eps_pred</span><span class="p">)</span> <span class="o">/</span> <span class="n">a</span><span class="p">.</span><span class="nf">sqrt</span><span class="p">()</span>
        <span class="k">if</span> <span class="n">t</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">:</span>
            <span class="n">x</span> <span class="o">=</span> <span class="n">mu</span> <span class="o">+</span> <span class="n">betas</span><span class="p">[</span><span class="n">t</span><span class="p">].</span><span class="nf">sqrt</span><span class="p">()</span> <span class="o">*</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randn_like</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>
        <span class="k">else</span><span class="p">:</span>
            <span class="n">x</span> <span class="o">=</span> <span class="n">mu</span>
    <span class="c1"># x: [B, L, d]，最终去噪后的连续 embedding，但还不是 token id
</span>    <span class="bp">...</span>
</code></pre></div></div> <p><strong>解码阶段：为什么要找最近邻？</strong></p> <p>去噪完成后，我们得到的是 <code class="language-plaintext highlighter-rouge">[B, L, d]</code> 形状的连续向量——每个位置是 $d$ 维实数向量。但输出需要是离散的 token id（整数）。怎么从连续向量映射回 token id？</p> <p>方法是：把这个向量和词表中<strong>所有 token 的 embedding 比较相似度</strong>，找最相近的那个 token。</p> \[\hat{w}_i = \arg\max_{v \in \text{Vocab}} \cos(x_i, e_v) = \arg\max_{v} \frac{x_i \cdot e_v}{\|x_i\| \|e_v\|}\] <p>归一化后，余弦相似度等价于点积，而”对所有 token 做点积取 argmax”正好就是一个<strong>线性层 + argmax</strong>：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># all_embeddings: [V, d]，词表中所有 token 的 embedding 矩阵（即 Embedding 层的权重）
</span><span class="k">def</span> <span class="nf">decode_to_tokens</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">all_embeddings</span><span class="p">):</span>
    <span class="c1"># x:              [B, L, d]，去噪后的连续 embedding
</span>    <span class="c1"># all_embeddings: [V, d]
</span>
    <span class="c1"># 分别归一化
</span>    <span class="n">x_norm</span>   <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">normalize</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>                  <span class="c1"># [B, L, d]
</span>    <span class="n">emb_norm</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">normalize</span><span class="p">(</span><span class="n">all_embeddings</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>     <span class="c1"># [V, d]
</span>
    <span class="c1"># 矩阵乘法：[B, L, d] × [d, V] = [B, L, V]
</span>    <span class="c1"># logits[b, i, v] = 第 b 个样本、第 i 个位置与第 v 个 token 的余弦相似度
</span>    <span class="n">logits</span> <span class="o">=</span> <span class="n">x_norm</span> <span class="o">@</span> <span class="n">emb_norm</span><span class="p">.</span><span class="n">T</span>                       <span class="c1"># [B, L, V]
</span>
    <span class="c1"># 对每个位置，取相似度最高的 token id
</span>    <span class="n">token_ids</span> <span class="o">=</span> <span class="n">logits</span><span class="p">.</span><span class="nf">argmax</span><span class="p">(</span><span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>                  <span class="c1"># [B, L]
</span>    <span class="k">return</span> <span class="n">token_ids</span>
</code></pre></div></div> <p><strong>这个操作一点都不复杂</strong>——它本质上和普通语言模型最后一层（<code class="language-plaintext highlighter-rouge">lm_head</code>）的操作是一样的：都是把隐状态向量和词表做矩阵乘法，找分数最高的 token。区别只是：普通 LM 的 <code class="language-plaintext highlighter-rouge">lm_head</code> 是一个<strong>单独训练的投影矩阵</strong>，而这里直接复用了 <strong>embedding 矩阵本身</strong>（因为加噪/去噪都在 embedding 空间进行，所以解码时也用同一套坐标系）。</p> <p><strong>为什么不直接用 softmax 采样？</strong> 也可以。<code class="language-plaintext highlighter-rouge">logits.softmax(-1)</code> 之后用 <code class="language-plaintext highlighter-rouge">multinomial</code> 采样，效果通常更好（保留多样性），而不是每次都取 argmax（会导致重复和退化输出）。</p> <p><strong>“最近邻解码”有没有问题？</strong> 有，这是连续文本 diffusion 的已知弱点：</p> <ol> <li><strong>Rounding Problem（取整问题）</strong>：去噪得到的连续向量可能处于 embedding 空间的”夹缝”里，和任何真实 token 都不够近，强行取最近邻会产生错误。这也是很多论文直接改用<strong>离散 masked diffusion</strong> 的原因。</li> <li><strong>计算量</strong>：词表 $V$ 通常有 3~5 万个 token，每次解码要和所有 token 计算相似度，算一个 <code class="language-plaintext highlighter-rouge">[B, L, V]</code> 的矩阵，计算量不小（虽然可以用矩阵乘法高效实现）。</li> </ol> <hr/> <h2 id="6-离散-masked-diffusion更主流">6. 离散 Masked Diffusion（更主流）</h2> <p>连续 embedding diffusion 存在”rounding problem”：embedding 空间的 MSE 损失和最终的 token 准确率对不上，训练信号不够直接。</p> <p><strong>Masked Diffusion</strong>（MDLM、Absorbing Diffusion 等）把 diffusion 直接做在 token 空间：用 <code class="language-plaintext highlighter-rouge">[MASK]</code> 替换 token，而不是加高斯噪声。这避免了连续空间和离散 token 之间的转换问题。</p> <h3 id="61-前向过程masking数学和代码对照">6.1 前向过程（Masking）：数学和代码对照</h3> <p><strong>数学</strong>：在时刻 $t$，每个位置独立地以概率 $1-\bar\alpha_t$ 被替换为 <code class="language-plaintext highlighter-rouge">[MASK]</code>：</p> \[q(x_t^{(i)} \mid x_0^{(i)}) = \begin{cases} x_0^{(i)} &amp; \text{以概率 } \bar\alpha_t \\ \texttt{[MASK]} &amp; \text{以概率 } 1-\bar\alpha_t \end{cases}\] <p>其中 $x_t^{(i)}$ 表示第 $i$ 个位置的 token。同样，$\bar\alpha_t$ 越小（$t$ 越大），被 mask 的比例越高。</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>t=0（原始）：  ["The", "cat",  "sat",  "on",  "mat"]     → ᾱ_t ≈ 1，保留所有
t=T/3：       ["The", "[M]",  "sat",  "[M]", "mat"]      → ᾱ_t ≈ 0.6，保留 60%
t=2T/3：      ["[M]", "[M]",  "sat",  "[M]", "[M]"]      → ᾱ_t ≈ 0.2，保留 20%
t=T（全噪声）：["[M]", "[M]", "[M]",  "[M]", "[M]"]      → ᾱ_t ≈ 0，全 mask
</code></pre></div></div> <p><strong>代码：</strong></p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">mask_forward</span><span class="p">(</span><span class="n">token_ids</span><span class="p">,</span> <span class="n">t</span><span class="p">,</span> <span class="n">alpha_bar</span><span class="p">,</span> <span class="n">mask_id</span><span class="p">):</span>
    <span class="c1"># token_ids: [B, L]，原始 token id（整数）
</span>    <span class="c1"># 返回：x_t: [B, L]，部分位置被替换为 mask_id
</span>    <span class="n">ab</span> <span class="o">=</span> <span class="n">alpha_bar</span><span class="p">[</span><span class="n">t</span><span class="p">][:,</span> <span class="bp">None</span><span class="p">]</span>                          <span class="c1"># [B, 1]，广播到 [B, L]
</span>    <span class="c1"># 每个位置以概率 ᾱ_t 保留（1 = 保留，0 = mask）
</span>    <span class="n">keep</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">bernoulli</span><span class="p">(</span><span class="n">ab</span><span class="p">.</span><span class="nf">expand_as</span><span class="p">(</span><span class="n">token_ids</span><span class="p">.</span><span class="nf">float</span><span class="p">())).</span><span class="nf">bool</span><span class="p">()</span>  <span class="c1"># [B, L]
</span>    <span class="n">x_t</span> <span class="o">=</span> <span class="n">token_ids</span><span class="p">.</span><span class="nf">clone</span><span class="p">()</span>
    <span class="n">x_t</span><span class="p">[</span><span class="o">~</span><span class="n">keep</span><span class="p">]</span> <span class="o">=</span> <span class="n">mask_id</span>                                <span class="c1"># 未保留的位置 → [MASK]
</span>    <span class="k">return</span> <span class="n">x_t</span>                                          <span class="c1"># [B, L]，dtype=long
</span></code></pre></div></div> <h3 id="62-去噪网络bert-风格输出是-logits">6.2 去噪网络：BERT 风格，输出是 logits</h3> <p>离散 diffusion 的网络接口和连续版本有根本区别：</p> <ul> <li><strong>连续版</strong>：输入 <code class="language-plaintext highlighter-rouge">[B, L, d]</code>（连续 embedding），输出 <code class="language-plaintext highlighter-rouge">[B, L, d]</code>（预测的噪声，也是连续 embedding）</li> <li><strong>离散版</strong>：输入 <code class="language-plaintext highlighter-rouge">[B, L]</code>（含 MASK 的 token id 序列），输出 <code class="language-plaintext highlighter-rouge">[B, L, V]</code>（每个位置的 token 概率 logits）</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">MaskedDiffusionModel</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">vocab_size</span><span class="p">,</span> <span class="n">d_model</span><span class="p">,</span> <span class="n">nhead</span><span class="p">,</span> <span class="n">num_layers</span><span class="p">,</span> <span class="n">max_len</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">embed</span>     <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Embedding</span><span class="p">(</span><span class="n">vocab_size</span><span class="p">,</span> <span class="n">d_model</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">pos_embed</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Embedding</span><span class="p">(</span><span class="n">max_len</span><span class="p">,</span> <span class="n">d_model</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">time_mlp</span>  <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Sequential</span><span class="p">(</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">d_model</span><span class="p">,</span> <span class="n">d_model</span> <span class="o">*</span> <span class="mi">4</span><span class="p">),</span> <span class="n">nn</span><span class="p">.</span><span class="nc">SiLU</span><span class="p">(),</span>
            <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">d_model</span> <span class="o">*</span> <span class="mi">4</span><span class="p">,</span> <span class="n">d_model</span><span class="p">),</span>
        <span class="p">)</span>
        <span class="n">layer</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">TransformerEncoderLayer</span><span class="p">(</span><span class="n">d_model</span><span class="p">,</span> <span class="n">nhead</span><span class="p">,</span> <span class="n">batch_first</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">transformer</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">TransformerEncoder</span><span class="p">(</span><span class="n">layer</span><span class="p">,</span> <span class="n">num_layers</span><span class="p">)</span>
        <span class="c1"># 关键区别：输出是词表大小的 logits，不是 embedding
</span>        <span class="n">self</span><span class="p">.</span><span class="n">out_proj</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">d_model</span><span class="p">,</span> <span class="n">vocab_size</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x_t</span><span class="p">,</span> <span class="n">t</span><span class="p">):</span>
        <span class="c1"># x_t: [B, L]  含 MASK 的 token id 序列（整数）
</span>        <span class="c1"># t:   [B]
</span>        <span class="n">B</span><span class="p">,</span> <span class="n">L</span> <span class="o">=</span> <span class="n">x_t</span><span class="p">.</span><span class="n">shape</span>

        <span class="n">h</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">embed</span><span class="p">(</span><span class="n">x_t</span><span class="p">)</span>                              <span class="c1"># [B, L, d]，id 转 embedding
</span>        <span class="n">h</span> <span class="o">=</span> <span class="n">h</span> <span class="o">+</span> <span class="n">self</span><span class="p">.</span><span class="nf">pos_embed</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">arange</span><span class="p">(</span><span class="n">L</span><span class="p">))[</span><span class="bp">None</span><span class="p">]</span>   <span class="c1"># [B, L, d]，加位置编码
</span>
        <span class="n">t_emb</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">time_mlp</span><span class="p">(</span><span class="nf">timestep_embedding</span><span class="p">(</span><span class="n">t</span><span class="p">,</span> <span class="n">d_model</span><span class="p">))</span>  <span class="c1"># [B, d]
</span>        <span class="n">h</span> <span class="o">=</span> <span class="n">h</span> <span class="o">+</span> <span class="n">t_emb</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">,</span> <span class="p">:]</span>                        <span class="c1"># [B, L, d]，加时间信息
</span>
        <span class="n">h</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">transformer</span><span class="p">(</span><span class="n">h</span><span class="p">)</span>                          <span class="c1"># [B, L, d]，双向 attention
</span>        <span class="n">logits</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">out_proj</span><span class="p">(</span><span class="n">h</span><span class="p">)</span>                        <span class="c1"># [B, L, V]，每位置的 token 分布
</span>        <span class="k">return</span> <span class="n">logits</span>
</code></pre></div></div> <h3 id="63-训练masked-diffusion数学和代码对照">6.3 训练（Masked Diffusion）：数学和代码对照</h3> <p><strong>数学</strong>：损失只在被 mask 的位置计算，让网络预测被遮住的原始 token：</p> \[\mathcal{L} = -\sum_{i: x_t^{(i)} = \texttt{[MASK]}} \log p_\theta(x_0^{(i)} \mid x_t, t)\] <p>这和 BERT 的 Masked Language Modeling（MLM）损失几乎一模一样！区别只是：BERT 固定 mask 15% 的位置，而 masked diffusion 的 mask 比例由时间步 $t$（即 $1-\bar\alpha_t$）控制。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">train_step_masked</span><span class="p">(</span><span class="n">token_ids</span><span class="p">,</span> <span class="n">model</span><span class="p">,</span> <span class="n">alpha_bar</span><span class="p">,</span> <span class="n">mask_id</span><span class="p">):</span>
    <span class="c1"># token_ids: [B, L]
</span>    <span class="n">B</span><span class="p">,</span> <span class="n">L</span> <span class="o">=</span> <span class="n">token_ids</span><span class="p">.</span><span class="n">shape</span>

    <span class="c1"># 步骤1：随机时间步 + 按比例 mask
</span>    <span class="n">t</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">randint</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">T</span><span class="p">,</span> <span class="p">(</span><span class="n">B</span><span class="p">,))</span>
    <span class="n">x_t</span> <span class="o">=</span> <span class="nf">mask_forward</span><span class="p">(</span><span class="n">token_ids</span><span class="p">,</span> <span class="n">t</span><span class="p">,</span> <span class="n">alpha_bar</span><span class="p">,</span> <span class="n">mask_id</span><span class="p">)</span>    <span class="c1"># [B, L]
</span>
    <span class="c1"># 步骤2：网络预测每个位置的 token 概率
</span>    <span class="n">logits</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">x_t</span><span class="p">,</span> <span class="n">t</span><span class="p">)</span>                                  <span class="c1"># [B, L, V]
</span>
    <span class="c1"># 步骤3：只在被 mask 的位置计算损失（预测原始 token）
</span>    <span class="n">mask</span> <span class="o">=</span> <span class="p">(</span><span class="n">x_t</span> <span class="o">==</span> <span class="n">mask_id</span><span class="p">)</span>                                 <span class="c1"># [B, L]，bool
</span>    <span class="n">loss</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">cross_entropy</span><span class="p">(</span>
        <span class="n">logits</span><span class="p">[</span><span class="n">mask</span><span class="p">],</span>       <span class="c1"># [N_masked, V]，被 mask 位置的预测
</span>        <span class="n">token_ids</span><span class="p">[</span><span class="n">mask</span><span class="p">],</span>    <span class="c1"># [N_masked]，被 mask 位置的真实 token
</span>    <span class="p">)</span>
    <span class="k">return</span> <span class="n">loss</span>
</code></pre></div></div> <h3 id="64-推理masked-diffusion">6.4 推理（Masked Diffusion）</h3> <p><strong>数学</strong>：从全 MASK 序列出发，每一步用反向去噪分布 $p_\theta(x_{t-1} \mid x_t)$ 逐步揭开 MASK。具体策略：在 $t \to t-1$ 这一步，有比例为 $\frac{\bar\alpha_{t-1} - \bar\alpha_t}{1 - \bar\alpha_t}$ 的 MASK 位置被填上（从模型预测的分布中采样），其余 MASK 位置继续保持 MASK。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@torch.no_grad</span><span class="p">()</span>
<span class="k">def</span> <span class="nf">masked_diffusion_sample</span><span class="p">(</span><span class="n">model</span><span class="p">,</span> <span class="n">B</span><span class="p">,</span> <span class="n">L</span><span class="p">,</span> <span class="n">mask_id</span><span class="p">,</span> <span class="n">vocab_size</span><span class="p">):</span>
    <span class="n">x</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">full</span><span class="p">((</span><span class="n">B</span><span class="p">,</span> <span class="n">L</span><span class="p">),</span> <span class="n">mask_id</span><span class="p">)</span>     <span class="c1"># 全 MASK 序列，[B, L]
</span>
    <span class="k">for</span> <span class="n">t</span> <span class="ow">in</span> <span class="nf">reversed</span><span class="p">(</span><span class="nf">range</span><span class="p">(</span><span class="n">T</span><span class="p">)):</span>
        <span class="n">t_batch</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">full</span><span class="p">((</span><span class="n">B</span><span class="p">,),</span> <span class="n">t</span><span class="p">)</span>
        <span class="n">logits</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">x</span><span class="p">,</span> <span class="n">t_batch</span><span class="p">)</span>       <span class="c1"># [B, L, V]
</span>
        <span class="c1"># 对所有位置采样（但只对 MASK 位置生效）
</span>        <span class="n">probs</span>   <span class="o">=</span> <span class="n">logits</span><span class="p">.</span><span class="nf">softmax</span><span class="p">(</span><span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>                        <span class="c1"># [B, L, V]
</span>        <span class="n">sampled</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">multinomial</span><span class="p">(</span><span class="n">probs</span><span class="p">.</span><span class="nf">view</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="n">vocab_size</span><span class="p">),</span> <span class="mi">1</span><span class="p">).</span><span class="nf">view</span><span class="p">(</span><span class="n">B</span><span class="p">,</span> <span class="n">L</span><span class="p">)</span>

        <span class="n">still_masked</span> <span class="o">=</span> <span class="p">(</span><span class="n">x</span> <span class="o">==</span> <span class="n">mask_id</span><span class="p">)</span>   <span class="c1"># [B, L]，当前还是 MASK 的位置
</span>
        <span class="k">if</span> <span class="n">t</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">:</span>
            <span class="c1"># 这一步要揭开的比例：ᾱ_{t-1} / ᾱ_t（每步只揭开一点点）
</span>            <span class="c1"># 剩余不揭开的比例：1 - ᾱ_{t-1} / ᾱ_t
</span>            <span class="n">remask_prob</span> <span class="o">=</span> <span class="mi">1</span> <span class="o">-</span> <span class="n">alpha_bar</span><span class="p">[</span><span class="n">t</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span> <span class="o">/</span> <span class="n">alpha_bar</span><span class="p">[</span><span class="n">t</span><span class="p">]</span>
            <span class="c1"># 以概率 (1 - remask_prob) 揭开 MASK，以概率 remask_prob 继续保持 MASK
</span>            <span class="n">reveal</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">bernoulli</span><span class="p">(</span>
                <span class="n">torch</span><span class="p">.</span><span class="nf">full_like</span><span class="p">(</span><span class="n">sampled</span><span class="p">.</span><span class="nf">float</span><span class="p">(),</span> <span class="mi">1</span> <span class="o">-</span> <span class="n">remask_prob</span><span class="p">)</span>
            <span class="p">).</span><span class="nf">bool</span><span class="p">()</span>
            <span class="n">x</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">where</span><span class="p">(</span><span class="n">still_masked</span> <span class="o">&amp;</span> <span class="n">reveal</span><span class="p">,</span> <span class="n">sampled</span><span class="p">,</span> <span class="n">x</span><span class="p">)</span>
        <span class="k">else</span><span class="p">:</span>
            <span class="c1"># 最后一步：把所有剩余 MASK 全部填上
</span>            <span class="n">x</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">where</span><span class="p">(</span><span class="n">still_masked</span><span class="p">,</span> <span class="n">sampled</span><span class="p">,</span> <span class="n">x</span><span class="p">)</span>

    <span class="k">return</span> <span class="n">x</span>   <span class="c1"># [B, L]，完整的 token 序列
</span></code></pre></div></div> <p><strong>对比连续版解码</strong>：离散版的解码无需”最近邻查找”，输出直接就是 logits，<code class="language-plaintext highlighter-rouge">argmax</code> 或 <code class="language-plaintext highlighter-rouge">multinomial</code> 一步得到 token id，干净利落。这是离散 diffusion 的主要优势。</p> <hr/> <h2 id="7-三条路线的维度总对比">7. 三条路线的维度总对比</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>【图像 DDPM】
  训练输入：
    x0        [B, C, H, W]   真实图像，像素 ∈ [-1, 1]
    eps       [B, C, H, W]   标准高斯噪声
    x_t       [B, C, H, W]   加噪图像（由 x0 和 eps 计算得到）
    t         [B]             时间步
  网络：UNet(x_t, t) → eps_pred [B, C, H, W]
  损失：MSE(eps_pred, eps)
  推理：x_T [B,C,H,W] → T步DDPM公式 → x_0 [B,C,H,W]

【连续文本 Diffusion】
  训练输入：
    token_ids  [B, L]         token id 序列
    x0         [B, L, d]      归一化 token embeddings
    eps        [B, L, d]      标准高斯噪声
    x_t        [B, L, d]      加噪 embedding
    t          [B]
  网络：Transformer(x_t, t) → eps_pred [B, L, d]
  损失：MSE(eps_pred, eps)
  推理：x_T [B,L,d] → T步DDPM → x_0 [B,L,d] → nearest neighbor → [B,L]

【离散 Masked Diffusion】
  训练输入：
    token_ids  [B, L]         原始 token id（整数，long）
    x_t        [B, L]         部分 masked 的 token id（整数，long）
    t          [B]
  网络：Transformer(x_t, t) → embed → [B,L,d] → out_proj → logits [B, L, V]
  损失：CrossEntropy(logits[mask], token_ids[mask])
  推理：全MASK [B,L] → T步逐步揭开 → [B,L]（完整序列）
</code></pre></div></div> <hr/> <h2 id="8-和-ar自回归llm-的本质区别">8. 和 AR（自回归）LLM 的本质区别</h2> <p>GPT 这类自回归模型：<strong>从左到右逐 token 生成，每个 token 只能看到左边的上下文</strong>。</p> <p>文本 Diffusion：<strong>并行生成整个序列，每个位置可以看到全局上下文</strong>（双向 attention）。</p> <table> <thead> <tr> <th> </th> <th>自回归 LLM</th> <th>文本 Diffusion</th> </tr> </thead> <tbody> <tr> <td>生成方式</td> <td>左到右，串行</td> <td>全局并行，多步去噪</td> </tr> <tr> <td>Attention</td> <td>单向（causal mask）</td> <td>双向（全局可见）</td> </tr> <tr> <td>推理步数</td> <td>= 序列长度 L</td> <td>= 去噪步数 T（通常 10~1000）</td> </tr> <tr> <td>条件建模</td> <td>直接：prefix → next token</td> <td>间接：condition embedding 注入</td> </tr> <tr> <td>编辑能力</td> <td>差（需要重新生成）</td> <td>强（可以只对部分位置去噪）</td> </tr> </tbody> </table> <p>文本 diffusion 的最大优势：<strong>全局一致性更好</strong>，<strong>可编辑性强</strong>（局部重新去噪）。劣势：推理慢（需要多步），质量目前仍不如 AR 大模型。</p> <hr/> <h2 id="9-条件生成怎么做文生图文生文">9. 条件生成：怎么做文生图、文生文</h2> <p>实际应用中需要<strong>条件生成</strong>：根据 prompt 生成图像或文本。条件信息通过 cross-attention 注入去噪网络。</p> <p><strong>数学</strong>：在去噪网络中，每一层先做 self-attention（序列内部交互），再做 cross-attention（与条件交互）：</p> \[\text{Attn}(Q, K, V) = \text{softmax}\!\left(\frac{QK^T}{\sqrt{d_k}}\right) V\] <ul> <li>Self-attention：$Q, K, V$ 均来自噪声序列 $x_t$（序列自身的全局上下文）</li> <li>Cross-attention：$Q$ 来自 $x_t$，$K, V$ 来自条件 $c$（将条件信息融入每个位置）</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">ConditionalDiffusionLayer</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x_t</span><span class="p">,</span> <span class="n">condition</span><span class="p">,</span> <span class="n">t_emb</span><span class="p">):</span>
        <span class="c1"># x_t:       [B, L, d]    噪声序列（query 来源）
</span>        <span class="c1"># condition: [B, Lc, d]   文本 encoder 输出（Lc 可以不等于 L）
</span>        <span class="c1"># t_emb:     [B, d]       时间步 embedding
</span>
        <span class="n">h</span> <span class="o">=</span> <span class="n">x_t</span> <span class="o">+</span> <span class="n">t_emb</span><span class="p">[:,</span> <span class="bp">None</span><span class="p">,</span> <span class="p">:]</span>      <span class="c1"># [B, L, d]，注入时间信息
</span>
        <span class="c1"># 1. Self-attention：序列内部的全局交互
</span>        <span class="c1">#    Q = K = V 均来自 h，输出形状仍为 [B, L, d]
</span>        <span class="n">h</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">self_attn</span><span class="p">(</span><span class="n">h</span><span class="p">,</span> <span class="n">h</span><span class="p">,</span> <span class="n">h</span><span class="p">)</span>       <span class="c1"># [B, L, d]
</span>
        <span class="c1"># 2. Cross-attention：从条件中提取信息
</span>        <span class="c1">#    Q 来自 h，K/V 来自 condition
</span>        <span class="c1">#    Q: [B, L, d_head]，K/V: [B, Lc, d_head]
</span>        <span class="c1">#    attn(Q, K^T): [B, L, Lc]（每个噪声位置对所有条件位置的注意力权重）
</span>        <span class="c1">#    乘以 V: [B, L, d_head] → 融入条件信息
</span>        <span class="n">h</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">cross_attn</span><span class="p">(</span><span class="n">h</span><span class="p">,</span> <span class="n">condition</span><span class="p">,</span> <span class="n">condition</span><span class="p">)</span>  <span class="c1"># [B, L, d]
</span>
        <span class="n">h</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">ffn</span><span class="p">(</span><span class="n">h</span><span class="p">)</span>                   <span class="c1"># [B, L, d]
</span>        <span class="k">return</span> <span class="n">h</span>
</code></pre></div></div> <p><strong>维度变化</strong>：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>x_t       [B, L, d]     噪声序列
condition [B, Lc, d]    文本 condition（Lc 可以不等于 L）

cross-attention:
  Q = h @ W_Q           [B, L,  d_head]
  K = condition @ W_K   [B, Lc, d_head]
  V = condition @ W_V   [B, Lc, d_head]
  score = Q @ K^T       [B, L, Lc]         ← L 个噪声位置 × Lc 个条件 token
  out = score @ V       [B, L, d_head]     ← 每个噪声位置融入条件信息
  输出: [B, L, d]       ← 序列长度不变，每个位置都吸收了 condition
</code></pre></div></div> <hr/> <h2 id="10-完整流程总结">10. 完整流程总结</h2> <p><strong>图像 Diffusion 训练</strong>：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>随机 x0 [B,C,H,W] → 随机 t → 加噪得 x_t → UNet 预测 eps → MSE 损失
</code></pre></div></div> <p><strong>图像 Diffusion 推理</strong>：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>x_T [B,C,H,W]（纯噪声）→ T步去噪（每步 UNet 预测 eps，DDPM公式更新 x）→ x_0
</code></pre></div></div> <p><strong>文本连续 Diffusion 训练</strong>：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>token_ids [B,L] → embed+normalize → x0 [B,L,d] → 随机 t → 加噪 x_t
                → Transformer 预测 eps [B,L,d] → MSE 损失
</code></pre></div></div> <p><strong>文本连续 Diffusion 推理</strong>：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>x_T [B,L,d]（纯噪声）→ T步DDPM去噪 → x_0 [B,L,d] → nearest neighbor → [B,L]
</code></pre></div></div> <p><strong>文本 Masked Diffusion 训练</strong>：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>token_ids [B,L] → 随机 t → mask部分token → x_t [B,L]
                → Transformer → logits [B,L,V] → 只在mask位置算 CE 损失
</code></pre></div></div> <p><strong>文本 Masked Diffusion 推理</strong>：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>全MASK序列 [B,L] → T步逐步揭开（每步预测 logits，按概率揭开部分 MASK）→ 完整序列
</code></pre></div></div> <p>三条路线的本质是一样的：<strong>定义一个把数据破坏掉的过程，训练网络学会修复它</strong>。不同的只是破坏方式（加高斯 vs mask）、网络结构（UNet vs Transformer）、以及输出形式（连续 embedding vs 离散 logits）。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 Diffusion 和生成模型效率优化你想深入研究，可以看看我们团队出版的<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，书里 LLM 效率优化那章和 Diffusion 的加速思路有些呼应，感兴趣可以翻翻。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="Diffusion"/><category term="LLM"/><category term="生成模型"/><category term="数学推导"/><summary type="html"><![CDATA[从图像到文本：Diffusion 模型原理全解——数学、结构、训练、推理一次讲清]]></summary></entry></feed>