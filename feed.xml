<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://marsggbo.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://marsggbo.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-07-23T02:58:52+00:00</updated><id>https://marsggbo.github.io/feed.xml</id><title type="html">HE Xin</title><subtitle>&quot;SSS: Study, Sleep, Slim&quot; </subtitle><entry><title type="html"></title><link href="https://marsggbo.github.io/blog/2026/2026-06-07-2605_29350/" rel="alternate" type="text/html" title=""/><published>2026-07-23T02:58:52+00:00</published><updated>2026-07-23T02:58:52+00:00</updated><id>https://marsggbo.github.io/blog/2026/2026-06-07-2605_29350</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/2026-06-07-2605_29350/"><![CDATA[<blockquote> <p>插播：之前写的<a href="https://item.jd.com/14945889.html">《动手学 AutoML》</a>终于出版了，从 NAS 到超参优化都有覆盖，适合想系统入门 AutoML 的同学。好了广告结束，现在进入正题。</p> <p><img src="/assets/img/book_cover_automl.png" alt="动手学AutoML书籍封面"/></p> </blockquote> <h1 id="moe-压缩新思路别删专家别合并专家把它们重映射就够了">MoE 压缩新思路：别删专家、别合并专家，把它们”重映射”就够了</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2605.29350">ConMoE: Expert-Pool Consolidation via Prototype Reassignment for MoE Compression</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有想过，MoE 架构最骄傲的地方，恰恰也是最烫手的地方？</p> <p>MoE（Mixture-of-Experts）的核心设计是稀疏激活——每个 token 只走少数几个 expert，理论上参数量很大但计算量很小。Qwen3-30B-A3B 的名字里 A3B 的意思就是”激活参数 3B”，但总参数有 30B。听起来很美，对吧？</p> <p>但问题来了：<strong>这 30B 参数，你一个都不能少地加载进显存</strong>。</p> <p>不管一个 token 实际上只用了 3B 参数，那剩下 27B 的专家还是得老老实实待在显存里候命。这就是 MoE 推理的核心矛盾：稀疏激活省了算力，但显存一分没省。随着模型越做越大，expert 存储变成了 MoE 落地的最大障碍之一。</p> <p>现有的压缩思路主要两条路：<strong>剪掉专家（pruning）</strong> 和 <strong>合并专家（merging）</strong>。前者直接删掉一些专家，后者把多个专家的权重”揉”成一个新专家。</p> <p>今天要聊的这篇论文提了第三条路：<strong>什么都不删，什么都不合并，只是把原来的专家引用”重新指向”一个更小的专家池</strong>。</p> <p>听起来像绕口令？别急，慢慢来。</p> <hr/> <h2 id="2-现有方案的痛点">2. 现有方案的痛点</h2> <p>在聊 ConMoE 之前，先交代下背景，说说 pruning 和 merging 各自的问题。</p> <p><strong>Expert Pruning（专家剪枝）</strong>：直接删掉贡献低的专家。简单粗暴，但问题是删了就是删了，原来指向这个专家的 router 决策没有任何”容错”——被删专家的那些 token 只能丢给其他留下来的专家，不管合不合适。</p> <p><strong>Expert Merging（专家合并）</strong>：把多个专家的权重融合成一个新专家。听起来更优雅，但实际上构造了一个”原来不存在”的新权重，这个新权重可能引入了原本没有的错误，而且融合方式很敏感，调参难度大。</p> <p>这两类方法有一个共同问题：<strong>把”保留哪些专家参数”和”原来的 expert slot 该怎么被表示”这两件事耦合在一起了</strong>。</p> <p>ConMoE 的作者们说，等等，这两个问题应该分开来看。</p> <hr/> <h2 id="3-conmoe-的核心思路专家池整合expert-pool-consolidation">3. ConMoE 的核心思路：专家池整合（Expert-Pool Consolidation）</h2> <p>论文提出了一个新的框架视角，叫 <strong>expert-pool consolidation</strong>（专家池整合）。</p> <p>核心思想拆成两步：</p> <ol> <li>从原来的专家池里<strong>挑出一个更小的子集</strong>作为”原型（prototype）”</li> <li>把每个原来的专家引用<strong>确定性地重映射</strong>到某个 prototype 上</li> </ol> <p>这样，router 对外还是看到原来那些 expert slot，内部只是把调用转发给对应的 prototype。<strong>专家的权重没有被修改，没有被融合，prototype 就是原来的 pretrained 专家</strong>。</p> <p>如下图，直观地展示了 ConMoE 的整体流程：</p> <p><img src="/assets/img/posts/2605_29350/figure1_p3.png" alt="ConMoE 整体架构示意"/></p> <p>图里的流程很清晰：左边是预训练的 MoE，有若干层，每层有自己的专家池。ConMoE 在若干相邻层构成的 local scope 里，用 calibration 统计数据和专家间的距离来选出 prototype，然后把原来的每个专家 slot 都确定性地指向某个 prototype。最右边压缩后的 MoE 对外接口和原来一样（router 不变），只是内部的专家池变小了。</p> <p>这里有一个很巧妙的地方：<strong>prototype 可以跨层共享</strong>。也就是说，相邻几层的专家可以共用一个 prototype 池，这让压缩比更高，同时也利用了相邻层之间专家的冗余性。</p> <hr/> <h2 id="4-怎么挑-prototype两个信号的乘积">4. 怎么挑 prototype？两个信号的乘积</h2> <p>这是方法里最关键的设计。对于每个 local scope $G$，ConMoE 给每个专家算一个分数 $s_e$，然后取 top-K 作为 prototype。</p> <p>分数由两个信号构成：</p> <p><strong>信号一：路由条件贡献分（contribution score）$a_e$</strong></p> \[a_e = \frac{1}{|\mathcal{D}_e|} \sum_{t \in \mathcal{D}_e} g_e(t) \cdot \|e(h_t)\|_2\] <p>说人话：在 calibration 数据上，这个专家被 router 选中的那些 token，它的输出强度乘以路由权重的平均值。分数高 = 这个专家被用到时贡献大。</p> <p><strong>信号二：可替换性分（replaceability score）$b_e$</strong></p> \[b_e = \min_{e' \in \mathcal{E}_G \setminus \{e\}} d(e, e')\] <p>就是这个专家在 scope 内，到其最近邻专家的距离。距离越大 = 越难被其他专家替代 = 越不可替换。</p> <p>两个分数各自在 scope 内做 min-max 归一化后相乘：</p> \[s_e = \bar{a}_e \cdot \bar{b}_e\] <p><strong>直觉很清晰</strong>：一个好的 prototype，既要在原来模型里有实质性的贡献（不能是闲置专家），又要在参数空间里相对独特（不能被其他专家轻易替代）。两个维度同时满足，才是值得被保留的 prototype。</p> <p>选好 prototype 后，每个原始专家就找自己最近的 prototype：</p> \[m_G(e) = \arg\min_{p \in P_G} d(e, p)\] <p>这就是重映射。干净，确定，不需要梯度，不需要 fine-tune。</p> <hr/> <h2 id="5-跨层共享scope-大了不一定好">5. 跨层共享：scope 大了不一定好</h2> <p>ConMoE 允许相邻几层共享一个 prototype 候选池（local scope），这让压缩更灵活。但 scope 到底设多大合适？</p> <p>如下图是在 Qwen3-30B-A3B 上做的消融，50% expert reduction 的情况下，不同 scope size 的效果：</p> <p><img src="/assets/img/posts/2605_29350/figure2_p7.png" alt="scope size 对性能的影响"/></p> <p>结果很有趣：scope 1（只在单层内选）、scope 2、scope 4 的表现非常接近，scope 4 在这个代表性子集上平均分最高。但 scope 一旦扩到 8 或 16 层，三个任务的性能都出现明显下降，HellaSwag 和 MMLU 掉得尤其多。</p> <p>这说明什么？<strong>相邻层之间确实存在可复用的冗余，但这种冗余是局部的</strong>。距离太远的层，对应的是模型不同深度的语义转换，强行共享 prototype 会引入深度不匹配的问题。</p> <p>这也解释了为什么 ConMoE 选择 bounded local scope，而不是一个全局的 expert pool。</p> <hr/> <h2 id="6-主实验结果">6. 主实验结果</h2> <p>实验在三个 MoE 模型上做了对比：Qwen3-30B-A3B、deepseek-moe-16b-base、OLMoE-1B-7B-0125，各自测了 25% 和 50% 的 expert reduction。Baseline 包括：</p> <ul> <li><strong>Pruning 类</strong>：Frequency pruning（按路由频率删）、REAP pruning（基于贡献分删）</li> <li><strong>Merging 类</strong>：M-SMoE（路由统计引导合并）、HC-SMoE（输出特征聚类合并）</li> </ul> <p>如下表是主要实验结果（6 个多选题 benchmark 的平均分）：</p> <p><img src="/assets/img/posts/2605_29350/table1_p6.png" alt="主实验结果表"/></p> <p>几个关键观察：</p> <ul> <li><strong>DeepSeek 上，ConMoE 在 25% 和 50% reduction 两个设置下都拿到最佳平均分</strong>（0.691 vs 0.681/0.676，0.651 vs 0.642/0.613），这是最亮眼的结果。</li> <li><strong>OLMoE 上，25% reduction 同样是 ConMoE 最好</strong>（0.676 vs 0.669/0.662），50% 时和最强 baseline 很接近。</li> <li><strong>Qwen3 上，pruning 方法特别强</strong>，尤其是 50% reduction 时 Frequency pruning 达到 0.733，ConMoE 是 0.717。但 ConMoE 在 WinoGrande 和 BoolQ 上保持了很强的表现。</li> </ul> <p>整体来说，ConMoE 作为一种全新的”不删不合并、只重映射”的压缩范式，<strong>在和 pruning/merging 同等压缩比下能打平甚至超过</strong>，这本身就是一个有意义的结论。</p> <hr/> <h2 id="7-消融实验哪个设计最重要">7. 消融实验：哪个设计最重要？</h2> <h3 id="71-整合结构consolidation-structure">7.1 整合结构（Consolidation Structure）</h3> <p>如下表对比了不同的整合方式：</p> <p><img src="/assets/img/posts/2605_29350/table2_p6_04.png" alt="整合结构消融实验"/></p> <p>三种 variant：</p> <ul> <li><strong>Layer-local</strong>：只在单层内选 prototype，不跨层</li> <li><strong>Cross-layer fixed-k</strong>：跨层候选池，但每层的 prototype 数量固定</li> <li><strong>Adaptive prototype</strong>：ConMoE 默认方案，用贡献分+可替换性自适应分配</li> </ul> <p>有意思的是，layer-local 已经很强了（ARC-C 0.515，HellaSwag 0.715，MMLU 0.651）。允许跨层候选池（Cross-layer fixed-k）在 HellaSwag 上略有提升，Adaptive prototype 和 Cross-layer fixed-k 表现接近。</p> <p><strong>结论：确定性重映射本身才是最重要的成分</strong>，跨层和自适应选择是锦上添花。</p> <h3 id="72-prototype-选择策略">7.2 Prototype 选择策略</h3> <p>如下表比较了不同的 prototype 选择信号：</p> <p><img src="/assets/img/posts/2605_29350/table3_p7_08.png" alt="prototype 选择消融"/></p> <ul> <li><strong>Distance-only</strong>（只看可替换性）：ARC-C 0.242，HellaSwag 0.319，MMLU 0.231 ——直接崩了</li> <li><strong>Usage top-k</strong>（只看路由频率）：HellaSwag 掉到 0.586，比较弱</li> <li><strong>REAP top-k</strong>（基于贡献分）：HellaSwag 回到 0.718，接近 ConMoE 默认</li> <li><strong>Adaptive prototype</strong>（ConMoE 默认）：HellaSwag 0.716，与 REAP top-k 相当</li> </ul> <p><strong>路由条件贡献分是主导信号</strong>，可替换性是辅助正则化（防止选出重复的 prototype），单独用距离作为选择标准完全行不通。</p> <h3 id="73-事后权重融合诊断">7.3 事后权重融合诊断</h3> <p>有人会问：选好 prototype 之后，能不能再做一下权重融合（把分配给同一 prototype 的那些原始专家的权重平均一下），效果会不会更好？</p> <p>如下表给出了答案：</p> <p><img src="/assets/img/posts/2605_29350/table4_p7.png" alt="事后权重融合诊断"/></p> <p>对比三种方案：</p> <ul> <li><strong>None（ConMoE 默认）</strong>：不做任何融合，ARC-C 0.504，HellaSwag 0.716，MMLU 0.639</li> <li><strong>Arcee fusion</strong>：HellaSwag 从 0.716 直接掉到 0.531，MMLU 从 0.639 掉到 0.512</li> <li><strong>Weighted average</strong>：更惨，HellaSwag 0.469，MMLU 0.358</li> </ul> <p><strong>ConMoE 的收益来自确定性重映射，而不是权重融合</strong>。直接复用原始的 pretrained prototype 才是最稳的策略，加了融合反而破坏了原始权重。</p> <hr/> <h2 id="8-跨层专家可替换性分析">8. 跨层专家可替换性分析</h2> <p>为什么跨层 scope 有效，但又不能太大？论文专门做了一个几何分析。</p> <p>在 Qwen3-30B-A3B 上，用 4 层 scope 计算每个 expert 在参数空间的最近邻，统计最近邻是否在同一层还是不同层：</p> <p><img src="/assets/img/posts/2605_29350/figure3_p8.png" alt="跨层最近邻分析"/></p> <p>左图是热力图，展示从 source layer 到最近邻 layer 的分布。可以看到主要沿对角线集中，说明最近邻大概率在附近的层，而非全局随机分布。</p> <p>右图是每层中”最近邻在不同层”的比例。<strong>整体上有 50.4% 的 expert，其参数空间的最近邻在另一层</strong>，部分中间层超过 70%。</p> <p>这两个观察共同说明：<strong>专家的可替换性不是严格层内的，相邻层之间存在真实的冗余</strong>——这是跨层共享 prototype 的几何依据。但这种冗余是局部的，不支持全局共享。</p> <hr/> <h2 id="9-总结">9. 总结</h2> <p>ConMoE 提供了一个很干净的框架来思考 MoE 压缩：</p> <ul> <li><strong>不删</strong>（pruning 的做法）</li> <li><strong>不改权重</strong>（merging 的做法）</li> <li><strong>只重映射</strong>：选出 pretrained 专家子集作为 prototype，把其他专家的引用指过去</li> </ul> <p>三个设计原则从实验里验证：</p> <ol> <li><strong>确定性重映射是最稳定的成分</strong>，单层内做都已经很强</li> <li><strong>跨层复用要保持局部</strong>，相邻层可以，太远的层会因深度不匹配掉性能</li> <li><strong>路由条件贡献分是主导信号</strong>，可替换性是辅助的正则化</li> </ol> <p>个人觉得这篇论文最有价值的点，是把”保留哪些专家参数”和”原来的 expert slot 怎么被表示”这两个问题<strong>显式地解耦</strong>了。这个视角在之前的 pruning/merging 框架里是隐含的，ConMoE 把它明确化之后，整个设计空间就清晰多了。</p> <p>当然 limitation 也直接：这套方案依赖 calibration 数据，跨层 scope 不能太大，而且现在的”logical reduction ratio”和实际的显存节省还需要专门的 runtime 支持（shared-prototype checkpoint 格式）才能落地。最后这点是整个 prototype sharing 方案的工程挑战，不只是这篇论文的问题。</p> <p>感兴趣的可以去翻原文，代码和复现脚本作者也有提到会开源。</p> <p>欢迎评论区交流，有什么遗漏或者理解有误的地方也欢迎指出~</p>]]></content><author><name></name></author></entry><entry><title type="html">arXiv’26 | Cassandra：不训练、不加显存，把 draft model 从 target 模型的 bit 里抠出来</title><link href="https://marsggbo.github.io/blog/2026/20260722-cassandra/" rel="alternate" type="text/html" title="arXiv’26 | Cassandra：不训练、不加显存，把 draft model 从 target 模型的 bit 里抠出来"/><published>2026-07-22T00:00:00+00:00</published><updated>2026-07-22T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260722-cassandra</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260722-cassandra/"><![CDATA[<h1 id="isca26--cassandra不训练不加显存把-draft-model-从-target-模型的-bit-里抠出来">ISCA26 | Cassandra：不训练、不加显存，把 draft model 从 target 模型的 bit 里抠出来</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2605.26558">Cassandra: Enabling Reasoning LLMs at Edge via Self-Speculative Decoding</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>先交代下背景：我自己最近正好在 vLLM 里折腾 self-speculative decoding，对这个方向的痛点算是结结实实体会过——<strong>训练一个好用的 draft model 很贵（EAGLE 系列要单独训练），不训练的方法（layer skipping、KV 压缩）在低 batch 下加速又很有限</strong>。所以看到这篇 Cassandra 时眼睛一亮：它给出的答案是，draft model 根本不用另外造，<strong>直接从 target 模型的权重里按 bit 粒度”抠”一个出来</strong>。</p> <p>场景设定很明确：<strong>消费级设备上的 reasoning LLM</strong>。Reasoning 模型的特点是 decode 阶段特别长（一道题思考几千 token），decode 又是 memory-bound 的，权重加载是大头；同时边缘设备 batch size 就是 1 或 2，量化这类有损方法在长推理链上的精度损失会被放大。无损加速里最顺手的武器就是 speculative decoding：</p> <p><img src="/assets/img/posts/20260722-cassandra/figure2_p3.png" alt="自回归解码 vs 投机解码"/></p> <p>draft 模型先猜 N 个 token，target 模型并行验证，猜对了就白赚——输出分布和原模型严格一致，无损。问题在于 draft model 从哪来。</p> <h2 id="2-核心矛盾draft-model-的出身问题">2. 核心矛盾：draft model 的”出身”问题</h2> <p>现有方案大致三类，各有各的坑：</p> <ol> <li><strong>训练式（EAGLE-3 等）</strong>：效果好，但要训练，而且 draft 权重要额外占显存——边缘设备本来显存就抠抠搜搜</li> <li><strong>Layer skipping（Draft&amp;Verify、Swift）</strong>：训练-free，但跳层对模型质量伤害大，acceptance rate 上不去</li> <li><strong>KV 压缩式（MagicDec、QuantSpec）</strong>：优化的是 attention/KV 路径，可低 batch + 中等长度序列时，<strong>decode 的瓶颈根本不在 attention，在 FFN 权重加载</strong>——药不对症</li> </ol> <p>Cassandra 的 insight 是：投机解码里 draft model 不需要”自己能写出正确答案”，只要”接下来几个 token 大概率猜对”。这个要求比有损压缩的”保持任务精度”低得多，所以<strong>可以把多种有损压缩狠狠地叠加起来用</strong>——精度崩了没关系，反正有 target 模型兜底验证。</p> <h2 id="3-方案">3. 方案</h2> <h2 id="31-先补个基础一个-bf16-权重里有什么">3.1 先补个基础：一个 bf16 权重里有什么</h2> <p>后面所有操作都在 bit 层面进行，所以先把 BFloat16 的位组成摆清楚。一个 bf16 数占 16 bit，分三段：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[1 bit 符号位 S] [8 bit 指数位 E] [7 bit 尾数位 M]
数值 = (−1)^S × 2^(E−127) × 1.M
</code></pre></div></div> <ul> <li><strong>符号位</strong>：正负，1 bit 动不得</li> <li><strong>指数位</strong>：决定数量级（这个数是 0.001 量级还是 100 量级），8 bit。改动 1 bit 数值直接翻倍/减半，<strong>最敏感</strong></li> <li><strong>尾数位</strong>：决定精度（小数点后的细节），7 bit。砍掉低位尾数≈四舍五入，<strong>最不敏感</strong></li> </ul> <p>一个例子：0.15625 的 bf16 表示是 <code class="language-plaintext highlighter-rouge">0 01111100 0100000</code>（S=0，E=124 即 2^-3，M=0.25，即 1.25×2^-3=0.15625）。把尾数砍到只剩 3 bit <code class="language-plaintext highlighter-rouge">010</code>，还原时低位补零，得到的还是 0.15625；即使原来尾数是 <code class="language-plaintext highlighter-rouge">0100111</code>，砍完还原成 <code class="language-plaintext highlighter-rouge">0100000</code>，数值只差 0.5% 左右——<strong>数量级不变，只丢精度尾巴</strong>。这就是后面”截尾数”敢下手的原因。</p> <h2 id="32-三步变换把模型切成骨架和细节补丁">3.2 三步变换：把模型切成”骨架”和”细节补丁”</h2> <p>有了这个基础，看 Cassandra 怎么把一个模型切成两份。它的存储格式设计遵循一个不变式：<strong>speculation data（draft 用的骨架）+ verification data（补丁）= 原始模型的每一个 bit</strong>，两份拼起来严格无损。</p> <p><img src="/assets/img/posts/20260722-cassandra/figure4_p5_05.png" alt="Cassandra 的格式变换流程"/></p> <p>上图（a）是离线变换流程，从左到右走读：</p> <p><strong>第一步：非结构化 value pruning（选哪些权重进骨架）</strong>。权重用 Wanda 的准则打分——不是单看权重绝对值，而是”权重 × 对应激活的 L2 范数”（激活在某些 channel 上天然幅值大，这些位置的权重再小也重要），分数高的整值保留进 speculation data；被剪掉的权重<strong>不丢弃</strong>，归入 verification data，同时用一张 bitmap（图中蓝色块）记录”谁被留下了”。KV Cache 则用 per-token 幅值剪枝（Key cache 有明显的 channel 级 outlier，Mustafar 的结论）。之所以选非结构化剪枝而不是结构化，是因为它保输出能力更强——同样压缩比下 acceptance rate 更高，而这两种剪枝都几乎不需要校准，贴合 training-free 的定位。</p> <p><strong>第二步：mantissa truncation（骨架内部再瘦身）</strong>。留下来的权重，7 bit 尾数只保留高位若干 bit 进 speculation data，<strong>砍掉的低位尾数原封不动存进 verification data</strong>。这里有个容易错过的对比：为什么用”截断”而不是更常见的量化（如 INT4）？因为量化是换一套数值表示（原始 bf16 的 bit 在量化值里不复存在，还原要做反量化计算），而截断<strong>保留的是原始 bit 的子集</strong>——还原时把两段尾数拼回去就行，零计算开销。”draft 是 target 的严格子集”这个关键性质就是这么来的。</p> <p><strong>第三步：exponent 压缩</strong>（下一节展开）。剪枝砍掉了一部分权重、截断砍掉了尾数，这时看剩下的 speculation data 的构成：1 bit 符号 + 8 bit 指数 + 3~4 bit 尾数——<strong>8 bit 的指数占了一半以上</strong>，成了新的大头。指数又动不得（前面说了，错 1 bit 数值翻倍）。这就是第三步存在的原因。</p> <p>再看图（b）（c）的两条推理路径：</p> <ul> <li><strong>Draft 推理（图 b）</strong>：只从外存加载 speculation data。被剪掉的权重位置按 bitmap <strong>补零</strong>（zero padding），被截掉的尾数低位也补零，恢复成标准 bf16 张量后正常跑 forward。注意这里没有省计算——张量尺寸和原模型一样，FLOPs 一点没少——<strong>省的是从内存搬进来的字节数</strong>。decode 阶段是 memory-bound（算力等内存），搬得少就是跑得快</li> <li><strong>Target 验证（图 c）</strong>：speculation + verification 两份都加载，剪掉的权重拼回原位、截掉的尾数低位接回去、指数解压——<strong>逐 bit 还原出原始模型</strong>做并行验证。所以最终输出的分布和原始模型完全一致，无损</li> </ul> <p>对比 EAGLE 这类方案的账本：EAGLE 要在显存里额外塞一个 draft 网络；Cassandra 的 draft 和 target 共享同一份存储（draft 读其中一部分，target 读全部），<strong>额外参数存储为零</strong>。</p> <h2 id="33-指数位怎么压不能改但可以换个编码">3.3 指数位怎么压：不能改，但可以换个编码</h2> <p>指数不能改值，但<strong>“不能改值”和”不能压缩”是两回事</strong>。突破口在于指数值的分布：LLM 权重高度集中在 0 附近（比如绝大多数权重在 0.001~0.1 这个区间），对应的指数值就扎堆在 119、120、121 这么几个数上，126、127 这种大指数几乎不出现。分布越集中，Shannon 熵越低——实测：</p> <p><img src="/assets/img/posts/20260722-cassandra/fig6_full.png" alt="权重和 KV Cache 的 exponent 熵都只有 2.6-2.8 bit"/></p> <p>bf16 权重 exponent 的 Shannon 熵只有约 <strong>2.58-2.64 bit</strong>，KV Cache 也就 2.7-2.8 bit。8 bit 的存储格子里装着不到 3 bit 的信息，<strong>六成是水分</strong>。水分可以用熵编码无损榨掉：高频指数用短码，低频用长码，每个值都能精确还原——值一个没改，占的 bit 少了。</p> <p>选哪种熵编码？论文对比了三条路（对应原文 Fig.5）：</p> <ul> <li><strong>Huffman 编码</strong>：按频率建最优二叉树，压缩率最好（贴近 2.6 bit 的熵界）。但解码要么查一张 2^N 项的 LUT（N 是最长码长，LLM 里可到 32，表大到不现实），要么沿树逐 bit 走（纯串行）。<strong>压缩率赢了，硬件输了</strong></li> <li><strong>Unary coding（一元码，Cassandra-1 选的）</strong>：按出现频率排序，最高频的指数编成 <code class="language-plaintext highlighter-rouge">1</code>，次高频 <code class="language-plaintext highlighter-rouge">01</code>，第三 <code class="language-plaintext highlighter-rouge">001</code>……即”N 个 0 跟一个 1”。妙处在于<strong>每个码字都以 1 结尾</strong>，从压缩流里辨认码字边界只需要”数零、遇一停”——这是纯组合逻辑，可以大规模并行。代价是压缩率略逊：实测平均 <strong>2.85 bit</strong>，比 Huffman 差零点几 bit，但换来了硬件解码的全并行。作者的取舍很明确：解码开销省下来的，比压缩率上让出去的多</li> <li><strong>MX 格式（Cassandra-2 选的，微损）</strong>：不做逐值编码，而是让相邻一组值（如 32 个）<strong>共享一个最大指数</strong>，组内每个值的尾数按自己与共享指数的差右移对齐。压得更狠、解码更快，但右移会丢掉小值的低位精度——轻微有损</li> </ul> <p>于是形成两个版本：<strong>Cassandra-1 = unary 无损路线</strong>（精度零风险），<strong>Cassandra-2 = MX 微损路线</strong>（更快，精度小让步）。用户按需选。</p> <h2 id="34-剪枝和截断的配比为什么要两个都用">3.4 剪枝和截断的配比：为什么要两个都用</h2> <p>压缩总预算固定时，是全砍权重（激进剪枝）还是全砍精度（激进截尾数）？论文扫了整条 trade-off 曲线：</p> <p><img src="/assets/img/posts/20260722-cassandra/fig7_full.png" alt="剪枝+截尾数联合使用时 acceptance rate 更稳"/></p> <p>左图横轴是压缩比、纵轴是 acceptance rate（draft 猜的 token 被 target 接受的比例）：单用 value pruning（VP，蓝线）压过 60% 就跳水，单用 mantissa truncation（MT，黄线）55% 就崩，<strong>两者联用（VP+MT，红线）能扛到 80% 压缩比</strong>。右图换算成理想加速：VP+MT 在压缩比 75% 附近达到峰值约 2.5 倍。</p> <p>直觉解释：剪枝伤的是”哪些神经元还在”，截断伤的是”每个神经元有多精确”，两种损伤模式作用在不同维度上，各砍一半比在单一维度上砍到底更温和——和”鸡蛋不放一个篮子”一个道理。</p> <h2 id="35-硬件侧2-面积换掉格式转换开销">3.5 硬件侧：2% 面积换掉格式转换开销</h2> <p>最后一块拼图：这套自定义 bit 格式，xPU 的浮点单元不认识，算之前必须转回标准 bf16。转换涉及三类位级操作——按 bitmap 把剪掉的位置补零（de-sparsification）、unary 解码指数、拼接尾数——用通用 SIMD 核串行做，省下的带宽会被转换开销吐回去。</p> <p>所以配了专用 encoder/decoder。核心部件是<strong>并行 zero counter</strong>：把 unary 码流切成 8-bit chunk，对每个 bit 位置并行输出”到此为止累计了几个零”，配合前一 chunk 传来的边界状态（最后一位是啥、连零数多少），一个周期内并行辨认出多个码字边界，再查一张小 codebook 还原指数值。SystemVerilog 实现、28nm 综合，<strong>在 64 TFLOPS 的 NPU 上面积开销约 2%</strong>。</p> <p>为什么 encoder 也不能省？权重可以离线压好，但 <strong>KV Cache 是推理时在线生成的</strong>——每生成一个 token 就产出一批新的 K/V，要现场剪枝、截断、编码后再存显存。这条在线路径没有硬件兜底就会变成新瓶颈。</p> <h2 id="4-效果">4. 效果</h2> <p>三个 reasoning 模型（DeepSeek-R1-Distill-Llama-8B、Qwen3-8B-Thinking、Qwen3-4B-Thinking）、三种硬件（RTX 4090、Jetson AGX Orin、systolic NPU）：</p> <p><img src="/assets/img/posts/20260722-cassandra/fig12_full.png" alt="各硬件各模型上的吞吐提升"/></p> <ul> <li>对 BFloat16 baseline 加速 <strong>1.78-2.41 倍</strong>（INT8 量化只有 1.25-1.42 倍，还掉精度）</li> <li>Acceptance rate 稳定在 <strong>0.74-0.91</strong>，DeepSeek 蒸馏模型最高</li> </ul> <p>和其他投机解码方法对刚：</p> <p><img src="/assets/img/posts/20260722-cassandra/figure13_p12.png" alt="与各类投机解码方法的对比"/></p> <p>Cassandra-1 全面压过训练-free 的同行（Draft&amp;Verify 约 1.1 倍、MagicDec 低 batch 下甚至负优化 0.9 倍、Lookahead 1.1-1.46 倍），和要训练的 EAGLE-3 打得有来有回（GPQA 上 1.93 vs 2.38，LiveCodeBench 上 2.10 vs 2.12，AIME 上 1.88 vs 1.99）——<strong>不训练能摸到训练式方法的天花板，这就是最大卖点</strong>。</p> <p>还有个容易被忽略的赢点——显存：</p> <p><img src="/assets/img/posts/20260722-cassandra/fig14_full.png" alt="各方案的显存需求对比"/></p> <p>由于 exponent 压缩是实打实作用在整个模型上的，Cassandra 的显存占用<strong>比 BF16 baseline 还低</strong>：同样 24 GB 的 RTX 4090，BF16 和 Eagle-3 在 64K 序列附近撞墙，Cassandra 能撑到 128K，相同显存下能生成 <strong>11.59 倍于 Llama3-based 投机解码、1.81 倍于 Eagle-3</strong> 的 token 数。加速的同时还省显存，这在”加速方法普遍要多吃显存”的投机解码赛道里是个异类。</p> <h2 id="5讨论">5.讨论</h2> <p>作为最近在 vLLM 里亲手搓过 self-spec decoding 的牛马，说几句体感：</p> <ol> <li><strong>“draft 不需要对，只需要像”这个观察是整篇文章的灵魂</strong>。有损压缩的评价体系（perplexity、任务精度）在投机解码语境下完全换了坐标系——换成 acceptance rate vs compression ratio 的 trade-off。想通这一点，各种”平时不敢用”的激进压缩手段全部解锁。我自己实验里用 layer-skip 路线在 Qwen3-8B 上只拿到 1.1-1.2 倍，看到 bit 级方案能到 2 倍上下，只能说粒度选对了确实不一样：跳层是把某些”完整的思考步骤”整个扔掉，bit 级修剪是全体权重”均匀降精度”，后者对 next-token 预测的伤害显然更温和。</li> <li><strong>Exponent 熵只有 2.6 bit 这个数字值得单独记住</strong>。这和前几天那篇逼近 Shannon 界的无损压缩论文（bf16 有效熵 10-12 bit）互相印证：浮点格式的 exponent 是整个 LLM 存储里水分最大的部位。以后看到任何压缩方案，先问一句”exponent 怎么处理的”。</li> <li>泼冷水的部分：核心收益依赖<strong>自定义硬件解码器</strong>。RTX 4090 上的数字是模拟器给的，真实 GPU 没有那个并行 unary decoder，落地要等硬件厂商买账，或者看有没有人能用 tensor core + bit 操作 kernel 逼近这个效果——这其实是个挺有意思的开源坑位。</li> </ol> <hr/> <blockquote> <p>顺带扯一句题外话：Cassandra 本质上是在”剪枝 + 截断 + 熵编码”的组合空间里找 acceptance rate 和压缩比的最优 trade-off——这个”在离散设计空间里搜索最优配置”的问题形态，和 NAS/AutoML 是同源的。我们把相关积累整理成了《动手学 AutoML：从 NAS 到大语言模型优化实战》，书里第 8 章讲 LLM 压缩（剪枝/量化），实战篇还有 LLM 后训练剪枝的完整实现，和这篇论文的 value pruning 思路可以对照着看。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="Speculative Decoding"/><category term="边缘推理"/><category term="论文解读"/><summary type="html"><![CDATA[ISCA26 | Cassandra：不训练、不加显存，把 draft model 从 target 模型的 bit 里抠出来]]></summary></entry><entry><title type="html">arXiv’26 | DySHARP：MoE 通信里一半流量是重复的，让 NVSwitch 帮你去重</title><link href="https://marsggbo.github.io/blog/2026/20260722-dysharp/" rel="alternate" type="text/html" title="arXiv’26 | DySHARP：MoE 通信里一半流量是重复的，让 NVSwitch 帮你去重"/><published>2026-07-22T00:00:00+00:00</published><updated>2026-07-22T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260722-dysharp</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260722-dysharp/"><![CDATA[<h1 id="arxiv26--dysharpmoe-通信里一半流量是重复的让-nvswitch-帮你去重">arXiv’26 | DySHARP：MoE 通信里一半流量是重复的，让 NVSwitch 帮你去重</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2605.05607">Accelerating MoE with Dynamic In-Switch Computing on Multi-GPUs</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>作为研究 MoE 系统的牛马，expert parallelism（EP）的通信开销是我绕不开的老对手：DeepSeek-V3 这种规模的模型，MoE 层里 <strong>Dispatch + Combine 能吃掉 70% 的时间</strong>，GEMM 反而只占三成。这篇 HKUST + 上交 + 华为 + 北大的工作提出了一个我之前没认真想过的角度：<strong>这些通信流量里有接近一半是重复的，而去重这件事可以让 NVSwitch 交换机来干</strong>。</p> <p>先解释两个概念，后面反复用：</p> <ul> <li><strong>Dispatch</strong>：token 经过 router 选中 top-k 个 expert，要把这个 token 的激活发到 k 个 expert 所在的 GPU——注意，是<strong>同一份数据发给多个目的地</strong></li> <li><strong>Combine</strong>：k 个 expert 算完，各自把结果发回 token 的原 GPU 做加权求和——是<strong>多份数据回来后被归约成一份</strong></li> </ul> <p>看出问题了吗？Dispatch 时同一个 token 的数据从 GPU 到交换机传了 k 次（内容一模一样）；Combine 时 k 份”注定要被加起来”的结果分别占用 k 次交换机到 GPU 的传输。<strong>从交换机的视角看，这些都是冗余</strong>：</p> <p><img src="/assets/img/posts/20260722-dysharp/figure1_p1.png" alt="MoE 的通信冗余与 in-switch computing 机会"/></p> <p>理想情况：Dispatch 时 GPU 只发一份给交换机，<strong>交换机负责多播</strong>；Combine 时<strong>交换机先归约再回传</strong>一份结果。这就是 in-switch computing。冗余量化一下：</p> <p><img src="/assets/img/posts/20260722-dysharp/figure2_p2.png" alt="冗余流量占比与消除冗余的理想加速"/></p> <p>DeepSeek-V3 在模拟的 GH200 NVL32 32-GPU 系统上，冗余流量占 25-50%，消除后通信理想加速最高接近 2 倍。</p> <h2 id="2-核心矛盾nvls-只会做静态的事">2. 核心矛盾：NVLS 只会做”静态”的事</h2> <p>NVIDIA 其实已经有 in-switch computing 了——NVLink SHARP（NVLS），AllReduce 就靠它加速。那直接用不就完了？</p> <p>不行，而且原因很本质。NVLS 是为<strong>静态集合通信</strong>设计的：</p> <p><img src="/assets/img/posts/20260722-dysharp/figure3_p3.png" alt="静态 vs 动态通信模式，NVLS 的适用边界"/></p> <ul> <li><strong>静态（AllReduce/AllGather）</strong>：目标集固定（永远是全体 GPU），地址对称（每个 GPU 上同一个 multimem 地址映射到相同偏移）</li> <li><strong>动态（Dispatch/Combine）</strong>：目标集由 router 逐 token 决定（这个 token 发给 GPU {0,2}，下个发给 {1,3}），地址不对称（每个 GPU 收到的 token 数不同，紧凑排布后同一 token 在不同 GPU 上的偏移完全不同）</li> </ul> <p>硬套 NVLS 的后果是什么？把动态通信重写成静态集合操作（比如把 Dispatch 当 AllGather 发全量），<strong>无用流量暴涨 340%</strong>，in-switch 省下的还不够填这个坑——论文实验里 NVLS 方案比 DeepEP 还慢一截（0.3-0.6 倍）就是这么来的。</p> <p><strong>这就是这篇文章要补的功能缺口：让 in-switch computing 学会处理”目标不固定、地址不对称”的动态通信。</strong></p> <h2 id="3-方案两条腿缺一不可">3. 方案：两条腿缺一不可</h2> <h2 id="31-dynamic-multimem-addressingisa架构运行时三层协同">3.1 Dynamic multimem addressing：ISA/架构/运行时三层协同</h2> <p>核心思路：数据包携带一个 multimem 地址 + 一份轻量目标列表，<strong>“发给谁”由包自己说，”存在哪”由每个 GPU 本地管</strong>。这解开了静态 NVLS 的两个死结（固定目标、对称地址）。</p> <p>新增两条指令：</p> <ul> <li><code class="language-plaintext highlighter-rouge">dymultimem.st</code>：Dispatch 用，交换机按目标列表多播</li> <li><code class="language-plaintext highlighter-rouge">dymultimem.ld_reduce</code>：Combine 用，交换机收齐 k 份后归约，只回传最终结果</li> </ul> <p>三个硬件触点的改动如下：</p> <p><img src="/assets/img/posts/20260722-dysharp/fig9_full.png" alt="dynamic multimem 寻址框架的架构设计"/></p> <p>对照上图从左到右走一遍：</p> <ol> <li><strong>源 GPU</strong>：SM 里的 LSU 加了一个 MultimemQ。常规指令读了操作数就发射，dymultimem 指令要先按”目标数 + 基地址”从内存取目标列表，凑齐了才把完整请求包发上 NVLink——不能堵住原来的 LSQ，所以单独排队</li> <li><strong>交换机</strong>：Route 模块按目标列表复制包，每个副本只保留发往该端口的目标（OutPort = Target / 每 GPU expert 数）；Combine 方向 Reduction Logic 给每个请求记目标数，部分响应到一个减一个，清零时把归约结果发回源 GPU</li> <li><strong>目的 GPU</strong>：Hub 里加了硬件 memory manager，维护一张 AL Table（algebraic index → layout index），把”逻辑上第 i 个 token”翻译成本地紧凑内存里的实际位置，配 AL TLB 加速。Dispatch 时增量分配 layout block，保证碎片化的 token 存成致密张量；Combine 复用同一张映射表反查</li> </ol> <p>这里的关键设计判断是：<strong>地址翻译放目的端硬件做，而不是源端软件做</strong>。源端软件管理要来回同步元数据（token 到达计数之类），DeepEP 的部分开销正来自于此。</p> <h2 id="32-token-centric-kernel-fusion不对称的流量省不出加速">3.2 Token-centric kernel fusion：不对称的流量省不出加速</h2> <p>第一条腿做完，发现一个反直觉的事：<strong>流量砍半了，速度没快多少</strong>。为什么？</p> <p>Dispatch 的冗余在 GPU→switch 方向被消除，Combine 的冗余在 switch→GPU 方向被消除。但传统调度里 Dispatch、GEMM、Combine 是串行的三个阶段——Dispatch 阶段只用上行带宽（下行闲着），Combine 阶段只用下行带宽（上行闲着）。<strong>流量减少是方向不对称的，串行执行时每个阶段的瓶颈方向并没有变短多少</strong>。</p> <p>解法：把整条 Dispatch → GEMM-1 → GEMM-2 → Combine 链按 <strong>token 粒度</strong>流水线化，让 Dispatch 和 Combine 同时在跑，上下行带宽互补着用：</p> <p><img src="/assets/img/posts/20260722-dysharp/figure12_p8.png" alt="token 级数据依赖链与 SM 分组"/></p> <p>实现要点：</p> <ul> <li><strong>SM 分组</strong>：SM 划成四组分别跑 Dispatch / GEMM-1 / GEMM-2 / Combine 的 persistent thread block（megakernel 绕过硬件 TB 调度器），两个 GEMM 组可以互借空闲 SM</li> <li><strong>readiness 门控</strong>：token tracker 硬件表跟踪每个 token 的就绪状态；GEMM 的某行 tile 只有等它依赖的 token 全部到齐才发射，Combine 只有等某 token 的 top-k 个 expert 输出全就绪才发 <code class="language-plaintext highlighter-rouge">ld_reduce</code></li> <li>同步 tile 尺寸取 128，正好对齐 GEMM tile——再细会拖累 GEMM 效率，再粗会损失 overlap</li> </ul> <h2 id="4-效果">4. 效果</h2> <p>模拟平台是 GH200 NVL32（32 GPU，9 台 NVSwitch，cycle-accurate：BookSim2 + 魔改 Accel-Sim，H200 规格）。Baseline 阵容很全：DeepEP、NVLS、FasterMoE、Tutel、CCFuser、COMET、DualPipe。</p> <p>训练端到端：</p> <p><img src="/assets/img/posts/20260722-dysharp/figure14_p10.png" alt="端到端训练加速"/></p> <p>对 DeepEP 几何平均 <strong>1.93 倍</strong>（最大 2.3 倍左右，Large-32 配置），对 COMET 也有 1.63 倍；MoE 层单独看，对 DeepEP 几何平均 <strong>2.26 倍</strong>。NVLS 那根矮柱（几何平均反而只有 DeepEP 的三成）就是”静态方案硬扛动态通信”的代价，非常有画面感。</p> <p>消融实验值得细看，它验证了”两条腿缺一不可”：</p> <p><img src="/assets/img/posts/20260722-dysharp/fig16_full.png" alt="时间分解与消融：两个技术单独都不够"/></p> <ul> <li><strong>DySHARP-Basic</strong>（只有动态寻址，无 overlap）：流量砍了，但 Dispatch+Combine 时间只从 0.494 降到 0.462——不对称问题活生生摆在这</li> <li><strong>Fusion Only</strong>（只有 kernel fusion，无 in-switch）：0.512，比 COMET 还略差，fusion 自己不产生流量减少</li> <li><strong>完整 DySHARP</strong>：通信时间压到 <strong>0.061（ideal）/ 完全被计算掩盖（eval 里 exposed communication 几乎为零）</strong>，MoE 层最高 2.8 倍于 DeepEP</li> </ul> <p>推理端到端也测了（Large-32 配置约 1.9 倍 vs DeepEP）：</p> <p><img src="/assets/img/posts/20260722-dysharp/figure27_p13.png" alt="推理端到端加速"/></p> <p>硬件开销方面，交换机和 GPU 的增量逻辑都很轻（AL Table 放 DRAM，TLB 和 buffer 是小 SRAM），论文还讨论了经由 IB Quantum Switch 的多节点扩展，思路是把整个集群抽象成共享内存，NVSwitch 管节点内、IB Switch 管节点间的多播/归约。</p> <h2 id="5-一点个人-take">5. 一点个人 take</h2> <ol> <li><strong>“冗余在交换机视角下才可见”是个很好的抽象升维</strong>。GPU 侧的通信优化（DeepEP 的分层通信、COMET 的重叠）做得再好，也只是把冗余流量传得更高效，没有消除冗余本身。换到交换机视角，Dispatch 是多播、Combine 是归约，两者都是网络设备的经典能力——<strong>MoE 的 all-to-all 从来就不是真正的 all-to-all，是被迫用 all-to-all 表达的 multicast + reduce</strong>。这个 framing 我很喜欢。</li> <li>消融里”流量减半但不加速”的教训值得所有做通信优化的人记住：<strong>带宽是有方向的，节省也是有方向的</strong>。不对称的节省要靠调度上的重叠来”兑现”，否则就是纸面收益。这和我们做 offloading 时”PCIe 上下行错峰”是同一个道理。</li> <li>冷静的部分：这是需要改 NVSwitch 和 GPU 硬件的方案，模拟器数字再漂亮，落地取决于 NVIDIA 愿不愿意把 NVLS 做成动态的。不过考虑到 MoE 已经是主流架构、且论文展示的改动相对克制（复用 NVLS 的骨架做动态扩展），我觉得这个方向进厂商 roadmap 的概率不低。倒是软件侧可以先抄作业：token-centric 的 readiness 门控流水线不依赖新硬件，DeepEP 们完全可以借鉴。</li> </ol> <p>欢迎评论区交流，尤其是搞 EP 通信的朋友。</p> <hr/> <p>顺带一提，MoE 系统优化是我自己的主研究方向之一（ExpertFlow 就是做稀疏 MoE 推理的）。我们也把 LLM 效率优化这条线的方法论整理进了《动手学 AutoML：从 NAS 到大语言模型优化实战》——书里第 8 章讲 LLM 压缩与模型融合，和通信优化角度不同，但目标一致：让大模型跑得起、跑得快。对 MoE 感兴趣的读者可以两条线对照着看。</p> <blockquote> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="MoE"/><category term="分布式训练"/><category term="NVLink"/><category term="论文解读"/><summary type="html"><![CDATA[arXiv’26 | DySHARP：MoE 通信里一半流量是重复的，让 NVSwitch 帮你去重]]></summary></entry><entry><title type="html">arXiv’26 | ST-MoE：expert 激活不是随机的——用时空相关性把 expert 提前搬上片</title><link href="https://marsggbo.github.io/blog/2026/20260722-expert-prefetch/" rel="alternate" type="text/html" title="arXiv’26 | ST-MoE：expert 激活不是随机的——用时空相关性把 expert 提前搬上片"/><published>2026-07-22T00:00:00+00:00</published><updated>2026-07-22T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260722-expert-prefetch</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260722-expert-prefetch/"><![CDATA[<h1 id="arxiv26--st-moeexpert-激活不是随机的用时空相关性把-expert-提前搬上片">arXiv’26 | ST-MoE：expert 激活不是随机的——用时空相关性把 expert 提前搬上片</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2606.15453">A Spatio-Temporal Expert Prefetching Framework for Efficient MoE-based LLM Inference</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>MoE 的老矛盾我在专栏里念叨过很多次：<strong>稀疏激活省的是计算，省不了内存</strong>——每个 token 只激活 top-k 个 expert，但你事先不知道是哪 k 个，router 出结果才知道，于是 expert 权重只能按需现搬。搬运在关键路径上，decode 阶段一层一停顿，90% 以上的参数全程闲着还得占着位置或者反复过总线。</p> <p>这篇来自 UNC Charlotte 和 George Washington 大学的工作（作者里有 Ahmed Louri、Avinash Karanth 这些做片上网络的老牌体系结构学者）问了一个很自然的问题：<strong>expert 的选择真的是不可预测的吗？</strong> 答案是否定的，而且规律有两个维度。</p> <h2 id="2-观察expert-激活的时空规律">2. 观察：expert 激活的时空规律</h2> <p>作者在 DeepSeek、Qwen、Mixtral 等多个 MoE 模型、10 万 token 的语料上做了统计：</p> <p><img src="/assets/img/posts/20260722-expert-prefetch/figure2_p2.png" alt="跨层与跨 token 的 expert 共激活热力图"/></p> <p>两条规律：</p> <ol> <li><strong>空间相关（跨层）</strong>：同一个 token 在第 l 层选了 expert e，那它在第 l+1 层的 expert 选择有明显的统计偏好——左图热力图上那些深色条纹就是高频”expert 组合”。直觉解释：transformer 逐层精炼的是<strong>同一个 token 的表征</strong>，语义特征是连续的，路由决策自然层间相关。卡方检验 p 值全部 &lt; 0.01</li> <li><strong>时间相关（跨 token）</strong>：同一层里，相邻 decode token 的 expert 选择重合度是随机基线（E(N)=K²/N）的<strong>接近 2 倍</strong>。因为自回归解码的每个 token 都基于前文生成，邻近 token 语义依赖强，触发的 expert 自然也像</li> </ol> <p><strong>既然可预测，就可以 prefetch</strong>——在第 l 层还在算的时候，提前把第 l+1 层大概率要用的 expert 搬上片，加载延迟就被计算掩盖了。</p> <h2 id="3-方案st-moe">3. 方案：ST-MoE</h2> <p>整体框架分 profiling 和 runtime 两阶段：</p> <p><img src="/assets/img/posts/20260722-expert-prefetch/fig3_full.png" alt="ST-MoE 框架总览：profiling 建表，runtime 查表预测"/></p> <h2 id="31-两张表--一个打分函数">3.1 两张表 + 一个打分函数</h2> <p>预测机制轻得出奇，就两张查找表：</p> <ul> <li><strong>CCT（Cross-layer Correlation Table）</strong>：profiling 阶段离线统计”第 l 层选 e → 第 l+1 层选 f”的共现频率，每层一张</li> <li><strong>HT（History Table）</strong>：runtime 在线记录<strong>上一个 decode token</strong> 在各层实际选了哪些 expert</li> </ul> <p>预测下一层 expert 时，两路信源打分融合：当前层实际选中的 expert 去查 CCT，得到空间相关候选及其置信分；HT 提供时间相关候选（上个 token 同层用过的）加 1 分；两边都出现的分数相加，<strong>总分 ≥ 2 的进 prefetch 集合</strong>。表会在线更新（Table Update 函数），路由行为漂移时预测跟着适应——所以它是”应用感知”的：不同任务（摘要/数学/代码）的路由模式不同，表学到的就不同。</p> <p>预测准确率相当能打：</p> <p><img src="/assets/img/posts/20260722-expert-prefetch/figure7_p8.png" alt="各模型各应用上的 expert 预测准确率"/></p> <p><strong>普遍超过 80%，DeepSeek 系平均 85-89%</strong>。有个很有意思的细节：MATH 任务的准确率稳定高于其他任务——数学推理的解码模式更结构化、token 间依赖更规整，路由行为也就更可预测。CNN/DM 摘要任务 token 转移更发散，预测最难。<strong>“任务的语义规整度决定路由可预测度”</strong>，这个观察值得记住。</p> <h2 id="32-硬件平台与流水线">3.2 硬件平台与流水线</h2> <p>配套的加速器由四部分组成：EPU（预测单元，存 CCT/HT + 算置信分）、Router（gating 网络的 MAC 阵列）、EMU（expert 到 PE 的动态映射 + permutation network）、可重构 PE 阵列（dataflow 可按数据复用机会切换）：</p> <p><img src="/assets/img/posts/20260722-expert-prefetch/figure4_p6.png" alt="ST-MoE 硬件平台总览"/></p> <p>执行流水线是精髓，看下图：</p> <p><img src="/assets/img/posts/20260722-expert-prefetch/figure6_p7.png" alt="三个连续 MoE 层的流水线工作流"/></p> <p>对着图走一遍：Layer 1 的 gating 结果一出来，Predictor（P）立刻预测 Layer 2 的 expert 并启动 prefetch（EP）——<strong>这段搬运和 Layer 1 的 expert 计算、Layer 2 的 attention 完全重叠</strong>。Layer 2 的 router 出真实结果后做验证（V）：预测对的 expert 已经在片上，直接开算；预测漏的走 Missed Loading（ML）现补。miss 的代价有界——最坏退化成按需加载，不会比 baseline 更差，模型精度则完全不受影响（<strong>预测只影响搬运时机，不改变路由结果</strong>，这是和 Adap-G 那类”少激活 expert”的有损方法的本质区别）。</p> <h2 id="4-效果">4. 效果</h2> <p>四个 MoE 模型（Qwen1.5/2.0-MoE、DeepSeek-V2、DeepSeek-MoE）× 三个任务（CNN/DM、MATH、HumanEval），对比 GPU 基线、Adap-G、Pre-gated MoE：</p> <p><img src="/assets/img/posts/20260722-expert-prefetch/fig8_full.png" alt="归一化执行时间对比及时间分解"/></p> <ul> <li>执行时间比 GPU 基线平均降 <strong>60%</strong>，比 Adap-G 降 56%，比 Pre-gated 降 33%</li> <li>右边的时间分解图说明收益来源：GPU 基线里 <strong>Data Comm（灰色）占七成</strong>，ST-MoE 把这块几乎压没了——预测 + 重叠正是打在数据搬运上</li> <li>对比 Pre-gated MoE 的优势也讲得清楚：Pre-gated 要额外训练一个 pre-gating 函数，ST-MoE 纯查表零训练，还能利用跨 token 相关性</li> </ul> <p>能耗方面比较诚实：比 GPU 基线<strong>多 10%</strong>（miss 补搬 expert 的代价），但 EDP（能耗延迟积）平均只有 GPU 的 <strong>0.4 倍</strong>——时间省得多，能耗多一点，综合效率大赚。</p> <p>消融实验把贡献拆开（五个配置递进）：动态 dataflow 贡献 1.1→1.4 倍，加时间相关（HT）到 2.0 倍，再加空间相关（CCT）到 <strong>2.33 倍</strong>。空间和时间两路信源确实互补，谁也替代不了谁。</p> <h2 id="5-一点个人-take">5. 一点个人 take</h2> <p>Expert prefetching 这个方向我一直在跟（自己做 ExpertFlow 时也深挖过 expert 激活的可预测性），说几点看法：</p> <ol> <li><strong>这篇的价值在于把”预测”做得极轻</strong>。之前的方案要么训练额外的预测网络（Pre-gated），要么用 LSTM/attention 做序列预测——预测器本身的开销和部署复杂度就把收益吃掉一块。ST-MoE 用两张查找表 + 阈值打分做到 80%+ 准确率，说明 expert 路由的可预测性是<strong>浅层统计规律</strong>，杀鸡不需要牛刀。</li> <li><strong>跨 token 相关性被严重低估了</strong>。大部分 prefetch 工作只用跨层信号（毕竟 gating 结果天然按层出），但消融显示 HT 这路信号贡献了 1.4→2.0 倍的跳变，比 CCT 的增量还大。上个 token 用过的 expert 大概率还热——这其实暗示 expert cache 的 LRU 策略天然就有效，两者是一体两面。</li> <li>保留意见：实验是自研加速器 + 模拟器上的结果，profiling 阶段的领域依赖性也值得注意——表是按应用域建的，跨域 serving（一个服务同时接代码和闲聊请求）时 CCT 的质量会打折扣，这在真实多租户场景是个实际问题。另外 batch 变大后不同请求的 expert 需求会天然”并集化”，预测的价值会被稀释——这也是所有 expert 预测方法共同的适用边界：<strong>低 batch、边缘侧最香</strong>。</li> </ol> <p>欢迎评论区交流，做 MoE 推理的朋友应该会有共鸣。</p> <hr/> <p>顺带扯一句题外话：ST-MoE 的 profiling-预测-验证闭环，本质是在”为每个负载自动适配系统行为”，这和 AutoML “为每个任务自动适配模型结构”是同一种思维方式。我们把 AutoML 方向的积累整理成了《动手学 AutoML：从 NAS 到大语言模型优化实战》，应用篇里 LLM 架构自动化、MoE 相关的模型融合都有覆盖，对”让系统自己学会调优”感兴趣的读者可以翻翻。</p> <blockquote> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="MoE"/><category term="LLM"/><category term="Expert Prefetching"/><category term="论文解读"/><summary type="html"><![CDATA[arXiv’26 | ST-MoE：expert 激活不是随机的——用时空相关性把 expert 提前搬上片]]></summary></entry><entry><title type="html">arXiv’26 | PLENA：Agent 时代的 LLM 推理，被两堵 Memory Wall 卡住了</title><link href="https://marsggbo.github.io/blog/2026/20260722-plena/" rel="alternate" type="text/html" title="arXiv’26 | PLENA：Agent 时代的 LLM 推理，被两堵 Memory Wall 卡住了"/><published>2026-07-22T00:00:00+00:00</published><updated>2026-07-22T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260722-plena</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260722-plena/"><![CDATA[<h1 id="arxiv26--plenaagent-时代的-llm-推理被两堵-memory-wall-卡住了">arXiv’26 | PLENA：Agent 时代的 LLM 推理，被两堵 Memory Wall 卡住了</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2509.09505">Combating the Memory Walls: Optimization Pathways for Long-Context Agentic LLM Inference</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有想过，同样一个 70B 模型，跑 chatbot 和跑 agent，硬件的”体感”完全是两回事？</p> <p>Chatbot 场景：用户问一句，模型答几百 token，皆大欢喜。Agent 场景：模型要吞下一整个网页 DOM、一长串 tool-call 轨迹，动辄 90K token 的 context，再慢慢往外吐几千 token。这篇来自剑桥、Imperial College London 和爱丁堡的工作先甩了一个数据：<strong>agentic workload 的单次推理 token 消耗平均是 chatbot 的 100 倍，极端情况 1000 倍</strong>。</p> <p><img src="/assets/img/posts/20260722-plena/fig1_full.png" alt="agentic 负载的 token 用量远超 chatbot"/></p> <p>上图（a）里 OSWorld、Chrome 这些 Computer Use Agent 任务的 token 用量直接飙到 100K 量级。更有意思的是（b）和（c）：随着 context 变长，<strong>计算热点从 FFN 迁移到 Attention</strong>（LLaMA-3-70B 上交叉点约在 19K 生成 token），而显存占用则从 weight-dominant 切换到 KV-dominant——128K context 下单个 batch 的 FP16 KV Cache 就要 <strong>39 GB</strong>，比很多模型权重本身还大。</p> <p>于是就撞上了论文标题里的两堵墙：</p> <ul> <li><strong>带宽墙（bandwidth wall）</strong>：KV 和权重要以极高带宽反复读，HBM 带宽喂不饱计算单元</li> <li><strong>容量墙（capacity wall）</strong>：KV Cache 随 context 疯长，很快吃光 HBM，batch size 被压到个位数</li> </ul> <p>这两堵墙一起把 GPU/TPU 的算力利用率摁在地上摩擦。作者的回应是 PLENA——一套软硬件协同设计的推理加速器系统，从三条 pathway 同时拆墙。</p> <h2 id="2-为什么现有硬件在-agent-负载上躺平">2. 为什么现有硬件在 agent 负载上躺平</h2> <p>先说一个容易被忽略的架构错配。TPU 这类加速器用的是<strong>方形 systolic array</strong>（比如 128×128），它假设 GEMM 的 M、K、N 三个维度尺寸相近。但 agent 推理是什么情况？容量墙压着 batch size 起不来，decode 阶段的 GEMM 是典型的 <strong>fat GEMM</strong>：M（batch 相关维度）只有个位数到几十，K、N 却是几千上万。方形阵列里大片 PE 全程围观，利用率惨不忍睹。</p> <p><img src="/assets/img/posts/20260722-plena/fig2_full.png" alt="方形阵列 vs 扁平阵列的可达 FLOPs 对比"/></p> <p>上图（a）很直观：同样数量的乘法器，方形阵列一半 PE 在闲着，扁平（flattened）阵列全部激活。（b）则展示了两条优化路径叠加的效果——扁平化抬高带宽墙下的利用率，量化把容量墙往右推（batch 能开更大）。</p> <h2 id="3-plena-的三条-pathway">3. PLENA 的三条 pathway</h2> <h2 id="31-pathway-1扁平-systolic-array">3.1 Pathway 1：扁平 systolic array</h2> <p>把 64×64 的方阵拍扁成 4×1024 这类形状（<code class="language-plaintext highlighter-rouge">BLEN×MLEN</code>，BLEN 对齐 batch 维度 M）。fat GEMM 的 M 小，那就让阵列的短边等于 M，长边吃 K/N 维度的并行。每个 cycle 从 Matrix SRAM 和 Vector SRAM 各取一条 MLEN 宽的输入，切成子向量喂给各个 sub-array，输出走 adder tree 归约。</p> <p>对 FlashAttention 还有个额外彩蛋：扁平阵列可以<strong>多个 attention head 并行处理</strong>，长 context + 小 batch 的场景下利用率反而更高。</p> <h2 id="32-pathway-2非对称量化">3.2 Pathway 2：非对称量化</h2> <p>这里”非对称”不是指 zero-point，而是指<strong>权重（W）/激活（A）/KV Cache 三者用不同精度</strong>的策略：</p> <ul> <li>激活对量化误差最敏感 → 片上用高精度 FP 存在 Vector SRAM</li> <li>权重和 KV 不那么敏感 → 用低精度 MX 格式（MXFP/MXINT）压进 Matrix SRAM 和 HBM</li> <li>对 KV 追加 Hadamard 旋转压异常值，K/V 只被 attention GEMM 消费，所以 inverse transform 可以推迟到进 Matrix SRAM 时再做</li> </ul> <p>算法侧配了两个 trick：<strong>output-norm 引导的分块 clipping 搜索</strong>和<strong>选择性旋转</strong>（不像 QuaRot 全局把旋转融进权重，而是逐层搜索要不要在线旋转）。效果：W4A4KV4 下 LLaMA-3-8B 六个零样本任务平均 70.39 分，QuaRot 只有 65.18（FP16 是 73.22）；70B 上差距更大（76.20 vs 69.21）。<strong>微缩放算术和旋转并不天然兼容</strong>，论文的消融实验证明盲套 QuaRot 反而伤精度，这个观察挺有价值。</p> <h2 id="33-pathway-3原生-flashattention-支持">3.3 Pathway 3：原生 FlashAttention 支持</h2> <p>FlashAttention 在 GPU 上是软件功劳，在专用加速器上得硬件配合：PLENA 设计了 IO-aware 的融合 attention 数据通路，中间激活（S、P 矩阵）全程留在片上，配合 transposable matrix SRAM（转置/非转置访问都不加开销）和 memory prefetch，把 off-chip 流量砍到最低。</p> <p>整体架构如下，Matrix Unit（扁平阵列）+ Vector Unit（归约/逐元素）+ Scalar Unit 三件套，由 decoder 的 system-pipeline controller 统一调度：</p> <p><img src="/assets/img/posts/20260722-plena/fig4_full.png" alt="PLENA 加速器整体架构"/></p> <h2 id="4-不止是芯片全栈--自动设计空间探索">4. 不止是芯片：全栈 + 自动设计空间探索</h2> <p>我个人认为这篇工作最”论文之外”的价值在工具链：PLENA 配了<strong>自定义 ISA、编译器、事务级模拟器和自动化 DSE（设计空间探索）流程</strong>，且承诺全部开源。</p> <p>DSE 用 BoTorch 做多目标贝叶斯优化，在 perplexity / latency / 面积三个目标上搜 Pareto 前沿：</p> <p><img src="/assets/img/posts/20260722-plena/figure11_p10.png" alt="多目标协同设计的经验可达曲面"/></p> <p>BoTorch 明显优于随机采样和 TPE。搜出来的设计点很有意思：追极致低延迟的配置用 MXINT8 + 1024 宽 MLEN（面积 203 mm²），追小面积的配置只要 23.6 mm² 还能保住 6.60 的 perplexity——<strong>架构参数和量化格式是联动搜索的</strong>，这其实就是把 NAS 那套方法论搬到了加速器设计上。</p> <h2 id="5-效果">5. 效果</h2> <p>三条 pathway 的消融看下面这张时序分解图（LLaMA-3.3-70B，batch 16，prefill 90K token / decode 98K context）：</p> <p><img src="/assets/img/posts/20260722-plena/fig13_full.png" alt="三种配置下 prefill/decode 的时序分解"/></p> <p>从 Config 1（朴素 self-attention、无 prefetch、128×128 方阵）到 Config 3（FlashAttention + prefetch + 1024×16 扁平阵列）：</p> <ul> <li><strong>Prefill：2.2 倍加速</strong>，平均计算利用率从 30.5% 提到 68.1%，FFN 阶段的计算利用率干到 99.4%</li> <li><strong>Decode：10.2 倍加速</strong>——方阵配置下 decode 的平均计算利用率只有可怜的 0.7%，扁平化后 attention 部分的 SA 利用率从 0.5% 提到 6.1%（decode 本质 memory-bound，这已经是把带宽用满后的结果）</li> </ul> <p>系统级对比（相同 HBM 容量预算、batch 开到各自能塞下的最大值）：</p> <ul> <li>LLaMA-3.1-8B agentic 负载（90K 输入/8K 输出）：吞吐 <strong>1.45 倍于 A100</strong>，能效 <strong>2.65 倍</strong></li> <li>LLaMA-3.3-70B 同负载：吞吐 <strong>2.21 倍于 A100</strong>（也高于 H100 的 2.04 倍），能效 <strong>4.04 倍</strong></li> <li>对 TPU v6e 的优势更夸张，agentic 场景下 TPU 只有 A100 的 0.47-0.53 倍吞吐，PLENA 是它的 4.7 倍</li> </ul> <p>注意一个诚实的细节：PLENA 的 TTFT（首 token 延迟）普遍比 GPU 长（70B 上 43s vs A100 的 29s），它赢在大 batch 吞吐和能效。<strong>这是给云端 agent 批量服务设计的芯片，不是给交互式对话设计的</strong>。</p> <h2 id="6-一点个人-take">6. 一点个人 take</h2> <p>这篇文章我最喜欢的其实是第一章的 workload 分析——它把”agent 推理和 chatbot 推理是两种硬件负载”这件事用数据钉死了：token 量差两个数量级、计算热点从 FFN 挪到 attention、显存从 weight-dominant 变成 KV-dominant。做推理优化的同行不妨把这三条当 checklist，很多为 chatbot 调的系统配置（比如 batch 策略、KV Cache 配额）直接搬到 agent 场景就是负优化。</p> <p>第二个感想：<strong>“batch size 由容量决定、利用率由形状决定”</strong> 这个链条值得反复咀嚼。我们在 GPU 上做推理优化时习惯把 batch 当自由变量，但 128K context 下 KV Cache 一口气吃掉 39 GB，batch 根本不是你想开就能开的。PLENA 的解法是量化推容量墙 + 扁平阵列适配小 M，两手抓。GPU 上对应的思路其实就是 KV 量化 + 针对小 batch 的 kernel 特化，方向是相通的。</p> <p>最后泼点冷水：这是模拟器 + RTL 层面的结果，没有流片；而且 fat GEMM 的形状随模型架构变化（MoE、GQA 比例都会影响），固定 4×1024 的阵列形状能不能吃遍未来模型，要打个问号——好在他们的 DSE 框架本身就是拿来回答这个问题的。</p> <p>欢迎评论区交流。</p> <hr/> <p>顺带一提，PLENA 用 BoTorch 在 perplexity/延迟/面积三个目标上自动搜索加速器配置，这套多目标贝叶斯优化的方法论和 NAS/AutoML 是同一套内功。我们把这个方向的积累写成了一本书《动手学 AutoML：从 NAS 到大语言模型优化实战》，进阶篇专门讲搜索策略（进化/贝叶斯/强化学习/DARTS），应用篇也覆盖了 LLM 量化压缩——读完你会发现”搜网络架构”和”搜芯片架构”在方法上惊人地一致。</p> <blockquote> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="Agent"/><category term="硬件加速"/><category term="FlashAttention"/><category term="论文解读"/><summary type="html"><![CDATA[arXiv’26 | PLENA：Agent 时代的 LLM 推理，被两堵 Memory Wall 卡住了]]></summary></entry><entry><title type="html">arXiv’26 | 无损压缩逼近 Shannon 极限：你的 LLM 权重里一半以上的 bit 是白存的</title><link href="https://marsggbo.github.io/blog/2026/20260722-shannon-compress/" rel="alternate" type="text/html" title="arXiv’26 | 无损压缩逼近 Shannon 极限：你的 LLM 权重里一半以上的 bit 是白存的"/><published>2026-07-22T00:00:00+00:00</published><updated>2026-07-22T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260722-shannon-compress</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260722-shannon-compress/"><![CDATA[<h1 id="arxiv26--无损压缩逼近-shannon-极限你的-llm-权重里一半以上的-bit-是白存的">arXiv’26 | 无损压缩逼近 Shannon 极限：你的 LLM 权重里一半以上的 bit 是白存的</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2606.15789">Approaching Shannon Bound with Lossless LLM Weight Compression</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有想过，当你把一个 bf16 的模型权重加载进显存时，这 16 个 bit 里到底有多少是真正”有信息量”的？</p> <p>这篇来自 NUS、华中科技大学和 ETH Zurich 的工作给出了一个让人有点意外的答案：<strong>LLM 权重的有效熵比存储位宽低 2-10 倍</strong>。换句话说，一个 bf16 权重名义上占 16 bit，但从信息论角度看，它实际只携带 10-12 bit 的信息；int4 更夸张，4 bit 的存储里真实熵只有 0.6-1.0 bit。剩下的全是统计冗余——理论上可以<strong>一个数值都不改</strong>地压掉。</p> <p>注意关键词：<strong>无损（lossless）</strong>。不是量化，不是剪枝，解压出来的权重和原始权重逐 bit 相同，推理结果完全一致。这对 reasoning 模型特别重要——有损压缩带来的精度损失在长推理链上会累积放大，而无损压缩没有这个顾虑。</p> <p>先看大背景，模型规模和单卡显存的剪刀差越拉越大：</p> <p><img src="/assets/img/posts/20260722-shannon-compress/fig1_full.png" alt="模型规模增长远快于显存增长"/></p> <p>Llama 4 已经摸到 2T 参数，而单卡 HBM 才刚爬到 288 GB。显存不够，要么加卡（贵），要么量化（掉精度），要么 offload（慢）。这篇论文给了第四条路：把权重里的统计冗余榨干。</p> <h2 id="2-权重里到底有多少水分">2. 权重里到底有多少水分</h2> <h2 id="21-熵分析有效-bit-数到底怎么算">2.1 熵分析：有效 bit 数到底怎么算</h2> <p>先把”有效 bit”这个概念掰开。信息论里 Shannon 熵回答的问题是：<strong>一堆符号，平均每个符号最少要多少 bit 才能无损表示？</strong> 答案由符号的出现频率决定——出现越频繁的符号，理论上可以用越短的编码。</p> <p>具体到 LLM 权重，计算分三步：</p> <p><strong>第一步：把权重当成离散符号</strong>。一个 bf16 权重就是一个 16-bit 的位模式（bit pattern），总共有 2^16 = 65536 种可能取值，每种取值就是一个”符号”；int4 权重则只有 2^4 = 16 种符号。注意这里<strong>不关心数值大小，只看位模式</strong>——0.5 和 0.4999 是两个不同的符号。</p> <p><strong>第二步：对每一层做经验直方图，套熵公式</strong>。对第 l 层的权重矩阵 W^(l)，数每个符号 i 出现的次数，除以总数得到经验概率 p_i，然后：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>H(l) = − Σ_i p_i · log2(p_i)
</code></pre></div></div> <p>举个玩具例子体会一下这个公式：假设某个 int4 层的 16 种符号里，符号 <code class="language-plaintext highlighter-rouge">0000</code> 出现概率 0.5，<code class="language-plaintext highlighter-rouge">0001</code> 和 <code class="language-plaintext highlighter-rouge">1111</code> 各 0.25，其余 13 种从不出现。那么：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>H = −(0.5·log2(0.5) + 0.25·log2(0.25) + 0.25·log2(0.25))
  = −(0.5·(−1) + 0.25·(−2) + 0.25·(−2))
  = 0.5 + 0.5 + 0.5 = 1.5 bit
</code></pre></div></div> <p>名义上每个权重占 4 bit，但信息量只有 1.5 bit——<strong>理想编码下（高频符号用短码，低频用长码）平均 1.5 bit/权重就够了，剩下 2.5 bit 是纯冗余</strong>。这就是”有效 bit”的含义。</p> <p>为什么 LLM 权重会这样？因为训练出来的权重<strong>高度集中在 0 附近、呈重尾分布</strong>：绝大多数权重的指数位落在少数几个值上，量化后的 code 也扎堆在中间几档。分布越尖，熵越低，可压空间越大。</p> <p><strong>第三步：按参数量加权平均到整个模型</strong>。不同层参数量不同，所以：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>H_model = Σ_l H(l)·|W(l)| / Σ_l |W(l)|
</code></pre></div></div> <p>这就是整个模型的”平均有效 bit 数”。注意这套算法给出的是<strong>无损压缩的理论下界</strong>（Shannon 源编码定理）：任何无损编码方案，平均码长不可能低于这个数；而好的熵编码器可以无限逼近它。</p> <p>作者用这套方法扫了从 Qwen-1.5B 到 Llama-405B 共 6 个模型、7 种数值格式（bf16 / fp8 / int8 / sq8 / fp4 / int4 / awq4）：</p> <p><img src="/assets/img/posts/20260722-shannon-compress/figure2_p3_02.png" alt="各模型各数值格式的熵差距"/></p> <p>怎么读这张图：每组两根柱子，蓝色是名义存储位宽（bf16 就是 16），红色是按上面方法算出的 Shannon 熵界。<strong>两根柱子的差就是可无损压掉的水分</strong>。几个值得记住的数字：</p> <ul> <li><strong>bf16 名义 16 bit，实际熵只有 10-12 bit</strong>，即 4-5 bit 的冗余，对应约 1.5 倍压缩空间</li> <li><strong>int4 冗余率最高</strong>：名义 4 bit，实际熵只有 0.6-1.0 bit，理论压缩比可达 6.3-10.4 倍（Mistral-7B 最狠）——量化把连续权重塞进 16 个档位后，分布反而更尖了，大量 code 几乎不出现</li> <li><strong>SmoothQuant 的 sq8 冗余最少</strong>（1.1 倍）。这不难理解：SmoothQuant 的设计目标之一就是把激活的 outlier “迁移”到权重上、再把数值范围铺满量化区间，符号分布被人为抹平了，熵自然接近满载</li> </ul> <p>细思极恐的是：即便是 AWQ、int4 这些已经压得很狠的量化格式，里面仍然趴着大量统计冗余。<strong>量化和无损压缩是正交的</strong>，可以叠着用。</p> <h2 id="22-为什么大家不用无损压缩">2.2 为什么大家不用无损压缩？</h2> <p>无损压缩在存储系统、多媒体领域用得飞起，在 LLM 系统里却几乎缺席，原因不在压缩比，在<strong>解压路径</strong>。现有方案（比如 ZipNN）是层级粒度的：先把一整层解压到 global memory，再启动 GEMM。</p> <p><img src="/assets/img/posts/20260722-shannon-compress/figure3_p4.png" alt="现有粗粒度流水线存在层级同步屏障"/></p> <p>解压在关键路径上，每层一个同步屏障，计算单元干等，还要来回搬运 global memory。这就是<strong>核心矛盾：压缩比再高，解压跟不上 GPU 的消费速度，一切白搭</strong>。</p> <p>作者据此提炼了三条设计原则：</p> <ol> <li><strong>逼近 Shannon 极限 + 硬件级吞吐</strong>：解码速度必须跟得上 GPU 显存带宽，否则解压自己成瓶颈</li> <li><strong>tile 粒度随机访问</strong>：GEMM 是一个 tile 一个 tile 消费权重的，codec 必须支持任意 tile 独立解码，不能”想解第 100 个 tile 得先解完前 99 个”</li> <li><strong>解压深度融合进 GEMM dataflow</strong>：解码结果直接写 shared memory，不落 global memory</li> </ol> <p>对照这三条筛一遍经典压缩算法，答案几乎唯一：<strong>rANS（range Asymmetric Numeral Systems）</strong>。</p> <p>简单科普一下 ANS 是什么。熵编码的家族谱系大致是：Huffman 编码给每个符号分配整数个 bit（频率 1/2 → 1 bit，1/4 → 2 bit），实现简单但压不到理论极限（符号概率不是 2 的负整数次幂时就有浪费）；算术编码可以给符号分配”分数个 bit”，能贴着 Shannon 界走，但解码是串行的位级操作，速度感人。<strong>ANS 是 2014 年前后出现的新贵，压缩率贴近算术编码（熵效率 &gt;99%），解码速度却接近 Huffman</strong>——它把整个编码状态维护成一个整数，编码/解码就是对这个整数做乘除和查表，天然适合查表并行化。Facebook 的 Zstd、苹果的 LZFSE 内核用的都是它的变体（FSE/tANS）。对 GPU 场景更重要的是：ANS 的状态可以随意切分成多条独立子流，正好把串行解码摊到 warp 的 32 个 lane 上。</p> <p>gzip、Zstd 这些为什么不行？前者熵效率只有 80-90%，后者虽然内核也是 rANS，但访问粒度是 64KB 起的 chunk——GEMM 要的是任意 tile 随机可解，这些通用格式做不到。</p> <h2 id="3-方案tile-级-on-the-fly-ans-解压">3. 方案：tile 级 on-the-fly ANS 解压</h2> <h2 id="31-整体执行模型">3.1 整体执行模型</h2> <p>核心思路一句话：<strong>把压缩 bitstream 按 GEMM 的 tile 切分，每个 tile 独立编码，运行时在 shared memory 里”边解边算”</strong>。</p> <p><img src="/assets/img/posts/20260722-shannon-compress/fig4_full.png" alt="tile 对齐的 on-the-fly 解压执行模型"/></p> <p>离线阶段，每层权重按下游 GEMM 的 tiling 几何（如 128×32、256×64）切成 tile，同层所有 tile 共享一个紧凑的 ANS codebook（分摊元数据开销），每个 tile 的起始偏移记进一张轻量索引表。这样任意 tile 都是自包含的子流，GEMM kernel 要哪块就解哪块。</p> <p>运行时的流水线看下图，第 i 层的计算不用等整层解压完，第一个 tile 解好就能开算，后面的 tile 并发解码：</p> <p><img src="/assets/img/posts/20260722-shannon-compress/fig5_full.png" alt="GPU kernel 全景：离线压缩 + 在线解压与 swizzled GEMM"/></p> <p>几个关键工程点：</p> <ul> <li><strong>warp 分工</strong>：每个 thread block 里 warp 0 负责流式解码压缩 tile，解出的权重<strong>直接写进 shared memory</strong>（不经过 global memory），其余 warp 立刻拿去喂 tensor core</li> <li><strong>双缓冲（ping-pong）</strong>：解 tile k+1 和算 tile k 重叠，用 <code class="language-plaintext highlighter-rouge">cuda::atomic_ref</code> 做生产者-消费者同步，解压延迟几乎全被藏掉</li> <li><strong>warp 级并行 rANS</strong>：ANS 状态机本质是串行的，作者把每个 tile 的 bitstream 切成 R 条独立子流，每个 warp lane 维护自己的 rANS 状态，把串行解码摊到整个 warp 上</li> <li><strong>确定性驱逐</strong>：shared memory 放不下时，按 tile-swizzle 遍历顺序静态决定驱逐谁，不需要运行时引用计数</li> </ul> <p>整套东西做成了 CUTLASS 之上的 plugin 式 kernel，PyTorch 可以直接调，改动极小。</p> <h2 id="4-效果">4. 效果</h2> <h2 id="41-压缩率离-shannon-极限只差-001-01-bit">4.1 压缩率：离 Shannon 极限只差 0.01-0.1 bit</h2> <p><img src="/assets/img/posts/20260722-shannon-compress/figure6_p9.png" alt="tile 级 ANS 压缩率与 Shannon 熵界几乎重合"/></p> <p>绿色柱（Ours）和红色柱（Shannon bound）几乎贴在一起——<strong>低位宽格式（int8/sq8/fp4/int4）上基本编到了理论最优</strong>。唯一的例外是 bf16，16-bit 符号表太大，频率表的有限精度归一化引入约 0.1-0.2 bit 开销，但也在 ANS 理论预期之内。实际压缩比：bf16 约 1.4-1.5 倍，int4 最高 10.4 倍。</p> <h2 id="42-kernel-层面比-sota-无损方案快最多-11-倍">4.2 kernel 层面：比 SOTA 无损方案快最多 11 倍</h2> <p>和两个最新的无损压缩推理系统对比（都是层级解压路线）：</p> <p><img src="/assets/img/posts/20260722-shannon-compress/figure7_p10.png" alt="对比 NeuZip：低 batch 下最高 11.1 倍加速"/></p> <p>对 NeuZip 峰值加速 <strong>10.1-11.1 倍</strong>，对 DFloat11 也有 <strong>6.4-6.9 倍</strong>。差距的来源正是第 2.2 节说的那个矛盾：它们都要先把整层 materialize 到 global memory，而本文的 tile 级路径完全绕开了这一步。</p> <p>消融实验把收益拆开看：</p> <p><img src="/assets/img/posts/20260722-shannon-compress/figure9_p11.png" alt="从 naive ANS 解码到融合 kernel 的逐步加速"/></p> <p>从 naive 实现（DietGPU 解码 + CUTLASS GEMM 两段式）出发，tile 对齐流水线贡献 3.3-8.2 倍，双缓冲再往上叠，最终 <strong>4.4-10.1 倍</strong>（Llama-405B 的大矩阵上收益最明显，10.06 倍）。和原生 CUTLASS 比，A100 上开销基本打平（1.0-1.1 倍），H200 上由于片上内存更大，某些配置甚至反超 baseline——<strong>解压不再是税，偶尔还是补贴</strong>。</p> <h2 id="43-端到端省下的显存全喂给-kv-cache">4.3 端到端：省下的显存全喂给 KV Cache</h2> <p>集成进 SGLang 后，在固定显存预算下测端到端 serving（数据来自论文 Table II）：</p> <ul> <li><strong>Qwen-14B</strong>（单卡 A100 80GB）：权重从 27.5 GB 压到 18.1 GB，省下的 9.4 GB 全部让给 KV Cache，最大 batch 从 47 涨到 75（1.3 倍），吞吐提升至 1.2 倍</li> <li><strong>Mixtral-176B</strong>（4×A100）：权重 261.9 GB → 163.7 GB，KV Cache 空间从 26.3 GB 暴涨到 124.6 GB，<strong>可行 batch size 从 20 涨到 95（4.8 倍）</strong>，吞吐 241 → 391 tokens/s（1.6 倍）</li> </ul> <p>这里有个很本质的观察：<strong>压缩把 LLM 推理的瓶颈从”显存容量”挪回了”计算吞吐”</strong>。权重瘦身 → KV Cache 变大 → batch 变大 → tensor core 吃得更饱。代价是 TPOT（每 token 延迟）略涨——这是给吞吐导向 serving 场景设计的，追求单请求低延迟的场景要自己掂量一下。</p> <h2 id="5-一点个人-take">5. 一点个人 take</h2> <p>这篇工作我觉得最值得借鉴的不是 ANS 本身（熵编码是老技术了），而是<strong>“压缩格式必须迁就计算的访问模式”</strong>这个视角。以前大家做权重压缩，习惯把压缩当成存储问题——压缩比越高越好；这篇明确指出真正的门槛在解压路径和 GEMM dataflow 的融合。tile 粒度、shared memory 直写、双缓冲，每一步都是在把解压往 GEMM 的固有节奏里塞。</p> <p>另外两个我比较在意的点：</p> <ol> <li><strong>和量化正交</strong>是这个方法最大的实用卖点。AWQ 完模型还能再无损压 1.4 倍，白捡的显存为什么不要？</li> <li>bf16 上 1.4-1.5 倍的收益说实话不算惊艳，但 MoE 场景（Mixtral-176B batch 4.8 倍）的收益是实打实的——MoE 的痛点恰恰是权重占用大而单 token 激活少，压权重换 KV Cache 空间这笔账在 MoE 上最划算。作为长期做 MoE 推理优化的牛马，看到这个数字还是挺兴奋的。</li> </ol> <p>局限也说一句：目前收益集中在 memory-bound 和容量受限的场景，compute-bound 的大 batch prefill 阶段，压缩带来的是”能跑更大 batch”而非”单次 GEMM 更快”；另外每种 tiling 几何都要离线预处理一遍，模型切换的工程成本还在。</p> <p>欢迎评论区交流，有 kernel 细节问题也可以一起讨论。</p> <hr/> <p>顺带扯一句题外话：这篇论文属于”不改数值的压缩”，而我们平时更常打交道的是”改数值的压缩”——剪枝、量化这些有损路线。我们把这个方向的积累整理成了一本书《动手学 AutoML：从 NAS 到大语言模型优化实战》，第 8 章和第 11 章专门讲 LLM 压缩（剪枝/量化/模型融合）和后训练剪枝实战。读完这篇再去看有损压缩，你会对”每个 bit 到底值多少信息”这件事有完全不同的体感。</p> <blockquote> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="模型压缩"/><category term="推理优化"/><category term="论文解读"/><summary type="html"><![CDATA[arXiv’26 | 无损压缩逼近 Shannon 极限：你的 LLM 权重里一半以上的 bit 是白存的]]></summary></entry><entry><title type="html">arXiv’25 | SMoE：expert 不在 GPU 上？别搬了，找个’平替’直接算</title><link href="https://marsggbo.github.io/blog/2026/20260722-smoe/" rel="alternate" type="text/html" title="arXiv’25 | SMoE：expert 不在 GPU 上？别搬了，找个’平替’直接算"/><published>2026-07-22T00:00:00+00:00</published><updated>2026-07-22T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260722-smoe</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260722-smoe/"><![CDATA[<h1 id="arxiv25--smoeexpert-不在-gpu-上别搬了找个平替直接算">arXiv’25 | SMoE：expert 不在 GPU 上？别搬了，找个”平替”直接算</h1> <blockquote> <p>原文：<a href="https://arxiv.org/pdf/2508.18983">SMoE: An Algorithm-System Co-Design for Pushing MoE to the Edge via Expert Substitution</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>MoE offloading 这个赛道我很熟：GPU 显存装不下所有 expert，只能把大部分放 CPU 内存，router 选中谁就现从 PCIe 搬谁。所有已有工作——MoE-Infinity、HybriMoE、各种 prefetch/cache 方案——都在回答同一个问题：<strong>怎么把”搬运”调度得更聪明</strong>。</p> <p>这篇南京大学（联合清华、荣耀）的工作换了一个问法：<strong>这个 expert 真的非搬不可吗？</strong></p> <p>它的观察起点很扎心。看三个 MoE 模型（DeepSeekMoE、XverseMoE、Qwen2MoE）的 gate score 分布：</p> <p><img src="/assets/img/posts/20260722-smoe/fig5_full.png" alt="各排名位置的平均 expert score，长尾明显"/></p> <p>Top-k 选出来的 expert，score 分布是<strong>陡峭的长尾</strong>：排第一的 expert score 是 0.13，排在 active/inactive 边界附近的只有 0.02-0.03——<strong>同为”被激活的 expert”，对输出的贡献差了一个数量级</strong>。而现有 offloading 系统对它们一视同仁：不管你贡献大小，选中了就得从 PCIe 搬过来（DeepSeekMoE 上搬一个 expert 要 0.75ms，比 GPU 算一次贵 7 倍还多）。</p> <p>为一个贡献 2% 的 expert 付一次全价 PCIe 运费，这个账怎么算都不划算。</p> <h2 id="2-核心思路低分-expert-用平替">2. 核心思路：低分 expert 用”平替”</h2> <p>SMoE 的做法说人话就是：<strong>router 选中的低分 expert，如果 GPU 缓存里恰好有一个 score 相近的其他 expert，直接拿后者顶替，不搬了</strong>。</p> <p><img src="/assets/img/posts/20260722-smoe/figure2_p2.png" alt="替换策略示意：raw 选择 vs 带替换的调度"/></p> <p>看上图左侧的例子：gate 排序后选中 a、b、c、d、e 五个 expert，其中 d、e 是低分的，且不在 GPU 上；GPU 缓存里躺着 f、g 两个没被选中但 score 接近的 expert。传统做法要走 3 次 PCIe（c、d、e），SMoE 用 f、g 顶替 d、e，只需要 1 次 PCIe。表格里的数字：<strong>GPU 命中从 2/5 提到 4/5</strong>。</p> <p>凭什么敢替？两个依据：</p> <ol> <li><strong>gate score 本身就是重要性度量</strong>——低分 expert 对加权求和的贡献小，替换引入的扰动有限</li> <li><strong>替换有严格的分数带约束</strong>：设 S(k+1) 是第 k+1 名的分数，被替换的 expert 分数必须落在 (S(k+1), (1+α)·S(k+1))，替补的分数必须落在 ((1-α)·S(k+1), S(k+1)]——两边都贴着 top-k 边界，<strong>α 这个超参控制”平替”的容忍度</strong>。找不到满足约束的替补就老老实实回退到 PCIe 搬运或 CPU 计算，不硬替</li> </ol> <p>这套逻辑在 CPU 上跑，每层搜索空间就几十个 expert，完全和 GPU 计算流水线重叠，开销可忽略。</p> <h2 id="3-配套系统设计">3. 配套系统设计</h2> <p>光有替换还不够，SMoE 是一套组合拳，目标是最大化”router 命中 GPU 缓存”的概率：</p> <h2 id="31-score-感知的缓存淘汰">3.1 Score 感知的缓存淘汰</h2> <p>要让”平替”可行，GPU 里得囤着<strong>分数带附近的 inactive expert</strong>。传统 LRU 只看访问时间，SMoE 改成按<strong>近 n 轮平均激活分数</strong>淘汰——历史贡献高的留下，不管你最近有没有被用到。同时有个保护机制：本层被 router 选中的 expert 在计算完成前免疫淘汰，避免”刚要用就被踢”。</p> <p>再看一个支撑这个策略的数据：</p> <p><img src="/assets/img/posts/20260722-smoe/figure7_p6.png" alt="expert 复用概率随 score 排名递减"/></p> <p>高分 expert 的跨 token 复用概率高达 0.4-0.6，长尾 expert 只有 0.1 出头——<strong>按分数囤货比按时间囤货更接近未来需求</strong>。</p> <h2 id="32-top-score-expert-预取--cpu-协同">3.2 top-score expert 预取 + CPU 协同</h2> <ul> <li><strong>预取只押注高分 expert</strong>：用 GPU 上已有的参数提前预测下一层的 top expert（GPU 相对空闲，预测不占关键路径），预取带宽花在刀刃上——高分 expert 替不得，必须真身上场</li> <li><strong>CPU 也是算力</strong>：低 batch 下 CPU 直接算一个 expert（0.56-2.2ms）和 PCIe 搬运（0.75-2.3ms）是同一量级，与其干等搬运，不如让 CPU 就地算。SMoE 用双指针策略在”PCIe 搬到 GPU 算”和”CPU 就地算”之间做负载均衡</li> </ul> <p>三条流水线（GPU 计算 / CPU 计算 / PCIe 加载）的编排如下：</p> <p><img src="/assets/img/posts/20260722-smoe/figure10_p7.png" alt="GPU、CPU、加载三条流水线的跨层编排"/></p> <h2 id="4-效果">4. 效果</h2> <p>三个平台组合（DeepSeekMoE+3080Ti、XverseMoE+4060Ti、Qwen2MoE+A6000），五个任务，对比 llama.cpp、MoE-Infinity、DeepSpeed、HybriMoE：</p> <p><img src="/assets/img/posts/20260722-smoe/fig12_full.png" alt="五个 workload 上的 TPOT 对比"/></p> <ul> <li><strong>decode 延迟（TPOT）比最优 baseline 平均降 24%（batch=1）/ 35%（batch=3）</strong>，摘要里那个 48% 是相对更广基线的总体口径；绝对值看，S3（Qwen+A6000）batch=3 时 SMoE 跑到 0.09s/token，HybriMoE 要 0.16s</li> <li><strong>GPU cache 命中率 70% 上下</strong>，比 baseline 们的 30-40% 高出一截：</li> </ul> <p><img src="/assets/img/posts/20260722-smoe/figure13_p10.png" alt="GPU 缓存命中率对比"/></p> <p>精度是替换方案最容易被质疑的地方，论文扫了 α 从 0 到 0.6 的全谱（8 个 benchmark × 3 个模型）。结论：<strong>α ≤ 0.3 时精度基本无损</strong>——GaoKao 上 Qwen 的精度甚至从 73.5% 涨到 76.1%（α=0.25），作者归因于轻微扰动起了类似正则化的作用；α 拉到 0.5 以上才开始看到一致的下降（掉 1-2 个点）。MT-Bench、HumanEval 这类生成任务也是同样的模式。</p> <p>消融显示替换（CE）、预取、CPU 协同三个组件各自贡献都在，其中<strong>替换是命中率提升的主力</strong>。</p> <h2 id="5-一点个人-take">5. 一点个人 take</h2> <ol> <li><strong>这篇论文动了一个大家默认不敢动的东西：路由结果的忠实性</strong>。所有 offloading 工作都把 router 的输出当圣旨，只优化执行；SMoE 说低分 expert 的选择本来就带噪声，在分数带内换一个功能相近的，输出扰动比量化还小。从 ExpertFlow 时期我就有个体感——<strong>gate score 的信息量远没被榨干</strong>，大家只拿它做 top-k 排序，其实它还告诉你”这个选择有多重要”。SMoE 是我看到的第一个把这层信息用在调度决策上的系统工作。</li> <li>和前几天解读的 ST-MoE（时空相关性预取）对照着看很有意思：ST-MoE 赌”我能预测你要谁”，SMoE 赌”你要的那个其实可以不给”。<strong>预取是把搬运藏起来，替换是把搬运消灭掉</strong>，两者完全正交，理论上可以叠加——预取管高分 expert，替换管低分 expert，正好是同一个分数轴的两端。</li> <li>冷静的部分：α 的最优值有模型依赖性（精度-速度 trade-off 曲线不同模型形状不一样），实际部署要自己扫一遍；另外”score 相近 ≈ 功能相似”这个假设在论文里是用端到端精度间接验证的，没有从表征层面深挖——万一某些模型的 expert 特化程度很高（比如代码 expert 和数学 expert 分数相近但功能迥异），替换的风险会大一些。HumanEval 上 DeepSeek 的 pass@1 波动（26.7→20.7 @ α=0.6）可能就是这个信号。</li> </ol> <p>欢迎评论区交流，尤其是在边缘设备上折腾 MoE 的朋友。</p> <hr/> <p>顺带扯一句题外话：SMoE 的”expert 平替”思路——用功能相似的组件替换开销大的组件、再用端到端指标验证损失——和模型压缩里的结构化剪枝、模型融合是同一个方法论家族。我们把这条线的积累整理成了《动手学 AutoML：从 NAS 到大语言模型优化实战》，第 8 章讲 LLM 压缩与模型融合，实战篇有 LLM 后训练剪枝的完整代码，感兴趣的读者可以对照着看：剪枝是”永久删掉不重要的”，SMoE 是”临时替换不重要的”，殊途同归。</p> <blockquote> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="MoE"/><category term="Expert Offloading"/><category term="边缘推理"/><category term="论文解读"/><summary type="html"><![CDATA[arXiv’25 | SMoE：expert 不在 GPU 上？别搬了，找个”平替”直接算]]></summary></entry><entry><title type="html">DAC’26 | SlideFormer 让单张 4090 全参微调 123B 模型</title><link href="https://marsggbo.github.io/blog/2026/20260721-slideformer/" rel="alternate" type="text/html" title="DAC’26 | SlideFormer 让单张 4090 全参微调 123B 模型"/><published>2026-07-21T00:00:00+00:00</published><updated>2026-07-21T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260721-slideformer</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260721-slideformer/"><![CDATA[<h1 id="dac26--slideformer-让单张-4090-全参微调-123b-模型">DAC’26 | SlideFormer 让单张 4090 全参微调 123B 模型</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2603.16428">An Efficient Heterogeneous Co-Design for Fine-Tuning on a Single GPU</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有算过一笔账：全参微调一个 8B 模型，到底需要多少显存？</p> <p>答案可能比你想的夸张——<strong>混合精度训练下超过 128 GB</strong>，一张 24GB 的 RTX 4090 连零头都装不下。这也是为什么大家一提微调就默认 LoRA：不是 LoRA 有多好，而是全参微调根本跑不起来。但 LoRA 在很多 domain-critical 任务上打不过全参微调，这一点已经有不少工作（比如 “LoRA vs Full Fine-tuning: An Illusion of Equivalence”）实锤了。</p> <p>今天想和大家聊聊 HKUST(GZ) 发表在 DAC’26 的这篇 SlideFormer：<strong>在单张 RTX 4090 上全参微调 123B+ 的模型</strong>，吞吐比 ZeRO-Offload / ZeRO-Infinity / ColossalAI 高 1.40~6.27 倍，CPU/GPU 内存还各省一半左右。代码已开源：<a href="https://github.com/RegiaYoung/SlideFormer">github.com/RegiaYoung/SlideFormer</a>。</p> <p>这篇论文最打动我的地方是它的出发点：<strong>GPU 显存涨得慢，但 CPU 内存涨得飞快</strong>。如下图，消费级 GPU 从 2020 年的 RTX 3090（24GB）到 2025 年的 RTX 5090 也就 32GB；而同期消费级平台的 DDR5 内存已经干到 256GB 了。这个”剪刀差”意味着：单卡微调本质上不是一个 GPU 问题，而是一个<strong>异构系统设计问题</strong>——GPU、CPU、RAM、NVMe 要当成一个整体来调度。</p> <p><img src="/assets/img/posts/20260721-slideformer/figure1_full.png" alt="CPU 与 GPU 内存容量差距逐年拉大"/></p> <h2 id="2-先算账显存都去哪了">2. 先算账：显存都去哪了</h2> <p>对一个 N 参数的模型做混合精度全参微调，内存开销大致是：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Mem = 2N (BF16 参数) + 2N (BF16 梯度) + 4N (FP32 master copy) + 8N (Adam momentum + variance) + 激活值
</code></pre></div></div> <p>前面静态部分加起来就是 <strong>16N 字节</strong>——8B 模型就是 128GB，这还没算激活值（正比于 层数×hidden×seq_len×batch）。<strong>其中 12N 是优化器相关的（master copy + Adam 状态），是最大的一块，而且只在参数更新那一刻才用得上</strong>——这就是 offloading 的机会所在。</p> <p>现有方案的问题在哪？</p> <ul> <li><strong>ZeRO-Offload / ZeRO-Infinity</strong>：为多卡分布式设计，单卡跑的时候那些多卡通信机制还挂在那里空转，额外内存和延迟白白浪费。更致命的是它们在 update 阶段是<strong>同步的</strong>：CPU 更新参数时 GPU 干等着。</li> <li><strong>ColossalAI（Gemini）</strong>：chunk 粒度的动态内存管理，对单卡效率不友好，同样是同步 update。</li> <li><strong>LoHan</strong>（近期最接近的工作）：用多进程做异步更新，IPC 开销不小；on-demand 内存分配容易碎片化；而且只支持 GPT-2，评估时还把 loss 换成 MSE 来回避 logits 显存问题——这在真实任务里是不可用的。</li> </ul> <h2 id="3-slideformer-的解法三根支柱">3. SlideFormer 的解法：三根支柱</h2> <p>SlideFormer 的整体架构如下图：GPU 上只保留一个滑动窗口大小的活跃层，CPU 常驻 FP32 master 参数和优化器状态，NVMe 作为可选的第三层，激活值可以通过 GPUDirect 直接在 GPU 和 NVMe 之间搬运，完全绕过 CPU。</p> <p><img src="/assets/img/posts/20260721-slideformer/figure2_full.png" alt="SlideFormer 系统总览"/></p> <h3 id="31-layer-sliding把-gpu-当滑动窗口">3.1 Layer-Sliding：把 GPU 当滑动窗口</h3> <p>核心思想一句话：<strong>GPU 不需要装下整个模型，只需要装下”正在算的那一层”</strong>。</p> <p>反直觉的地方在于粒度选择。直觉上窗口开大点（一次放多层）应该更好？论文的答案是<strong>恰恰相反</strong>：层是串行计算的，多放几层在 GPU 上纯属占着显存不干活，这些显存拿去加大 batch size 收益大得多。而层（Layer）是 LLM 里最小的重复结构单元，用它做粒度天然规整——不像 ZeRO-Offload 或 LoHan 用 param-group，各种大小不一的组件需要复杂的手动配置。</p> <p>真正的性能来源是<strong>backward 和参数更新的重叠</strong>。看下图这个对比：ZeRO-Offload 是”GPU 算完 backward → 梯度全部搬到 CPU → CPU 统一 update”，三个阶段串行，GPU 在 update 阶段完全 IDLE；SlideFormer 则是层粒度流水线——第 i 层 backward 一算完，梯度立刻异步 d2h，CPU 马上开始更新第 i 层参数，与此同时 GPU 继续算第 i-1 层的 backward，还顺手预取第 i-2 层的参数。仅这一项就带来约 <strong>40%</strong> 的性能提升。</p> <p><img src="/assets/img/posts/20260721-slideformer/figure3_full.png" alt="ZeRO-Offload 与 SlideFormer 的 backward 时间线对比"/></p> <p>更细的重叠时序如下图：GPU compute、d2h stream、h2d stream、CPU compute 四条轨道同时在跑，梯度下传、激活值/参数上传、CPU 更新全部藏在 backward 计算的影子里。</p> <p><img src="/assets/img/posts/20260721-slideformer/figure5_p3.png" alt="GPU-CPU 流水线中的计算-通信重叠"/></p> <p><strong>什么时候能完全藏住？</strong> 论文给了一个很实用的量化指标——hiding factor：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>η = T_bwd / (T_d2h + T_update)
</code></pre></div></div> <p>η ≥ 1 表示 offloading 零开销。看实测数据（Qwen2.5-14B）：</p> <p><img src="/assets/img/posts/20260721-slideformer/table1_p4.png" alt="backward 阶段各环节耗时与 GPU 利用率"/></p> <p>规律很清晰：<strong>batch size 是决定性变量</strong>。4090 上 batch=16 时 η 只有 0.66（T_bwd=170ms 藏不住 22+175ms 的传输和更新），GPU 利用率 93.1%；batch 拉到 64，T_bwd 涨到 660ms 而 T_update 几乎不变（195ms），η=3.00，GPU 利用率 98.4%。这也解释了为什么”支持更大 batch”本身就是个性能 feature——batch 不够大，流水线就填不满。</p> <p>还有个有意思的观察：达到完全重叠所需的临界 batch size 几乎<strong>不随模型大小变化</strong>（如下图，从 3B 到 72B 都稳定在 4090≈15、A100≈21 附近）。原因是 T_bwd、T_d2h、T_update 都正比于层的大小，比值自然就和层大小无关了。</p> <p><img src="/assets/img/posts/20260721-slideformer/figure4_full.png" alt="不同模型大小下的临界 batch size"/></p> <p>顺带一提工程实现：SlideFormer 用的是<strong>线程 + CUDA Stream</strong> 的轻量组合（一个线程管 h2d/d2h 传输，一个线程跑 Layer-Adam 更新），而不是 LoHan 的多进程方案——省掉了 IPC 开销。这个选择看似平平无奇，但对流水线这种延迟敏感的场景，进程间通信的开销真的会咬人。</p> <h3 id="32-内存管理预分配--共享-buffer">3.2 内存管理：预分配 + 共享 buffer</h3> <ul> <li><strong>GPU 侧</strong>：一个预分配的 cache unit 队列，每个 unit 刚好装下一层的参数和梯度，层滑进来算完就把 unit 让给下一层。<strong>固定 footprint、零碎片、零重分配</strong>——对比 LoHan/StrongHold 的 on-demand 分配，这是典型的”空间确定性换运行时开销”。</li> <li><strong>CPU 侧</strong>：FP32 master 参数存成 flattened pinned tensor 方便 h2d；梯度用一个<strong>全层共享</strong>的 pinned buffer 接收——梯度的 CPU 占用直接从 2N 降到 2N/num_layers；FP32→BF16 的类型转换也在 CPU 侧用共享 buffer 完成，GPU 上不再需要为转换付出额外显存和传输。这套布局让 CPU 峰值内存降了 25% 以上。</li> <li><strong>激活值</strong>：Sliding checkpointing——每层 forward 完激活值异步下放到 CPU/NVMe，backward 前再预取回来重算，显存里永远只有一个小窗口的激活值。</li> </ul> <h3 id="33-io-与-kernel两个容易被忽视的坑">3.3 I/O 与 kernel：两个容易被忽视的坑</h3> <p><strong>GPUDirect Storage（GDS）</strong>：模型超过 CPU 内存怎么办？上 NVMe。SlideFormer 是第一个把 GDS 用于微调 offload 的工作——激活值直接在 GPU 和 NVMe 之间走 DMA，绕过 CPU bounce buffer，把 CPU 和 PCIe 带宽留给流水线里更紧要的参数传输和更新。</p> <p>有个设计决策值得细品：<strong>参数不下放 NVMe</strong>。参数的 h2d 传输在重叠的关键路径上，换到 NVMe 速度立刻从 PCIe 掉到 SSD 档位，整条流水线都得跟着降速；而且强行加第三层会让数据路径变复杂。<strong>每种数据只在两层之间移动</strong>——这种克制反而是好的系统设计。</p> <p><strong>Fused LinearCrossEntropy（LCE）kernel</strong>：这是我觉得最”实诚”的一个优化点。现在的模型词表越来越大（Llama-3.1 词表 128K），输出层那个 logits tensor（B×S×V）的显存<strong>可以超过前面所有激活值的总和</strong>——这个瓶颈很多系统工作直接装看不见（LoHan 干脆换 MSE loss 绕过去）。SlideFormer 把 projection 和 loss 融合成一个 Triton kernel，分 chunk 算梯度，从头到尾不物化完整 logits。效果如下图：batch=64 时 torch 标准做法要近 50GB，LCE 不到 6GB，<strong>输出层显存降 88.9%</strong>，速度还基本不变。</p> <p><img src="/assets/img/posts/20260721-slideformer/figure6_p4.png" alt="torch 标准方法与 LCE 的显存和耗时对比"/></p> <p>除了 LCE，RoPE、RMSNorm、SwiGLU 也都换成了优化的 Triton kernel，加上 FlashAttention，intra-layer 的计算效率也补齐了。</p> <h2 id="4-实验数字说话">4. 实验：数字说话</h2> <p>实验平台两套：高端 PC（RTX 4090 24GB 或 AMD RX 7900XT 20GB + Ryzen 9 9950X + 256GB DDR5）和服务器（A100 80GB + 1024GB DDR4）。对比对象是 ZeRO-Offload、ZeRO-Infinity、ColossalAI、LoHan。</p> <p><strong>吞吐 vs batch size</strong>（Llama-3.1-8B on 4090）：如下图，SlideFormer 全 batch 领先，相对三个 baseline 分别是 1.39×、2.82×、6.34×。注意 batch=64 时其他框架全部画 ×（跑不动了），只有 SlideFormer 还在继续涨——回到前面说的，大 batch 才能填满流水线，而能开大 batch 本身就是内存管理带来的红利。下半张图还有个扎眼的细节：ZeRO-Offload 的 CPU 内存占用（黄色）比 SlideFormer 高出一大截，这就是”分布式架构在单卡空转”的代价。</p> <p><img src="/assets/img/posts/20260721-slideformer/figure7_full.png" alt="Llama-3.1-8B 各框架吞吐与 CPU 内存对比"/></p> <p><strong>吞吐 vs 模型大小</strong>（Qwen2.5 3B~72B on 4090）：ZeRO 系列 14B 就跑不动了，ColossalAI 也撑不到 72B，SlideFormer 一路跑到 72B+，而且<strong>吞吐始终贴着”不做 offload 的理论峰值”（红虚线）的 90~95%</strong>。也就是说 offloading 的开销几乎被完全藏掉了。</p> <p><img src="/assets/img/posts/20260721-slideformer/figure8_full.png" alt="Qwen2.5 各尺寸模型的吞吐与内存对比"/></p> <p><strong>GPU 显存</strong>：同样是 Llama-3.1-8B，SlideFormer 的显存占用比 ZeRO-Offload 低 50% 以上，而且随 batch 增长的斜率也最缓——预分配 cache 队列 + LCE kernel 的合力。</p> <p><img src="/assets/img/posts/20260721-slideformer/figure9_full.png" alt="各框架 GPU 显存随 batch size 的变化"/></p> <p><strong>跨平台</strong>：AMD RX 7900XT 和 A100 上同样保持 95%+ 峰值性能（如下图）。能同时把 N 卡和 A 卡都伺候好的 offloading 系统真不多，这说明设计本身没有绑死在 CUDA 特有机制上。</p> <p><img src="/assets/img/posts/20260721-slideformer/figure10_p5.png" alt="AMD 和 NVIDIA 平台上的微调吞吐"/></p> <p><strong>NVMe offloading 的取舍</strong>：如下图，开满所有 offload 选项能把 CPU 内存再降 60~80%，代价是吞吐掉 30~50%；I/O 带宽是瓶颈，所以性能随 SSD 数量近线性提升。论文还给了个实用结论：<strong>小模型优先 offload 优化器状态，激活值别动</strong>（激活值 offload 会同时拖累 forward 和 backward）；<strong>只有最大的模型或 CPU 内存实在紧张时才 offload 激活值</strong>。</p> <p><img src="/assets/img/posts/20260721-slideformer/figure11_p6.png" alt="不同 NVMe 数量与 offload 策略的性能对比"/></p> <p><strong>能跑多大的模型</strong>：这张图是全文的”结论图”。横轴 CPU 内存，纵轴最大可训练模型。ZeRO-Offload 上限 8B，ColossalAI 上限 32B，而 SlideFormer 开满 NVMe offload 后<strong>在 256GB 内存的 PC 上能微调 90B，服务器上超过 123B</strong>（Mistral-Large 级别）。瓶颈成功从 GPU 显存转移到了 CPU 内存——而后者便宜得多。</p> <p><img src="/assets/img/posts/20260721-slideformer/figure12_full.png" alt="各框架最大可训练模型大小对比"/></p> <p><strong>和 LoHan 正面刚</strong>：GPT2-13B（LoHan 只支持 GPT-2，只能迁就它）上用标准 loss 对比，SlideFormer 吞吐更高、GPU 显存不到对方一半、CPU 内存省 30%。</p> <p><img src="/assets/img/posts/20260721-slideformer/figure13_p6.png" alt="SlideFormer 与 LoHan 在 GPT2-13B 上的对比"/></p> <h2 id="5-我的一些-take">5. 我的一些 take</h2> <ol> <li> <p><strong>这篇论文的”新”不在单点技术，而在 co-design 的完整度</strong>。异步 offload、层粒度流水线、共享 buffer、fused kernel，每一样单拎出来都有前人的影子（Stronghold、LoHan、Liger Kernel），但把它们在单卡场景下捏成一个自洽的系统，并且每个设计决策都有量化依据（η 因子、临界 batch size、参数为什么不下 NVMe），这才是系统论文该有的样子。</p> </li> <li> <p><strong>η = T_bwd / (T_d2h + T_update) 这个指标很值得偷学</strong>。做任何 overlap 类优化，先把各环节耗时 profile 出来算个比值，就知道还有多少油水、瓶颈在哪一侧。比”我们做了 overlap，快了 X 倍”这种黑盒叙事有营养得多。</p> </li> <li> <p><strong>LCE 那个点提醒我们：显存瓶颈会迁移</strong>。前几年大家都在卷激活值 checkpoint、参数 offload，词表膨胀之后 logits 反而成了大头。做系统优化不能只盯着经典瓶颈，要定期重新 profile——瓶颈永远在你不看的地方长大。</p> </li> <li> <p>对个人研究者来说这是实打实的福音：一台 128GB 内存的 PC + 一张 4080 就能全参微调 Llama-3.1-8B，吞吐几乎无损。全参微调的门槛第一次降到了”高配游戏 PC”的水位。当然也要实事求是：123B 那个数字是开满 NVMe offload 跑出来的，吞吐会打折扣，别指望拿它日常炼丹；但”能跑”和”不能跑”是质的区别。</p> </li> </ol> <p>欢迎评论区交流，如果有跑过 ZeRO-Offload / LoHan 踩坑经历的朋友也欢迎分享。</p> <hr/> <p>扯一句题外话。SlideFormer 解决的是”全参微调跑不起来”的问题，而我自己更多在琢磨另一条路：能不能通过剪枝、量化这类压缩手段，让模型本身变小，从源头缓解内存压力？两条路角度不同但目标一致。我们把这个方向的积累整理成了《动手学 AutoML：从 NAS 到大语言模型优化实战》，里面有 LLM 压缩（剪枝/量化/模型融合）的原理章节和后训练剪枝的完整实战，对”穷人玩大模型”这个课题感兴趣的朋友可以翻翻。</p> <blockquote> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="微调"/><category term="Offloading"/><category term="论文解读"/><summary type="html"><![CDATA[DAC’26 | SlideFormer 让单张 4090 全参微调 123B 模型]]></summary></entry><entry><title type="html">大厂 ASR 技术报告横评：从 Seed-ASR 到 StepAudio 2.5，两年时间行业共识是怎么形成的</title><link href="https://marsggbo.github.io/blog/2026/20260715-asr-landscape/" rel="alternate" type="text/html" title="大厂 ASR 技术报告横评：从 Seed-ASR 到 StepAudio 2.5，两年时间行业共识是怎么形成的"/><published>2026-07-15T00:00:00+00:00</published><updated>2026-07-15T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260715-asr-landscape</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260715-asr-landscape/"><![CDATA[<h1 id="大厂-asr-技术报告横评从-seed-asr-到-stepaudio-25两年时间行业共识是怎么形成的">大厂 ASR 技术报告横评：从 Seed-ASR 到 StepAudio 2.5，两年时间行业共识是怎么形成的</h1> <blockquote> <p>本文是大厂 ASR 技术报告解读系列的收官篇。前七篇解读： <a href="https://arxiv.org/abs/2407.04675">Seed-ASR（字节）</a>、<a href="https://arxiv.org/abs/2407.04051">SenseVoice（阿里通义）</a>、<a href="https://arxiv.org/abs/2501.14350">FireRedASR（小红书）</a>、<a href="https://arxiv.org/abs/2504.18425">Kimi-Audio（月之暗面）</a>、<a href="https://arxiv.org/abs/2512.23808">MiMo-Audio（小米）</a>、<a href="https://arxiv.org/abs/2601.21337">Qwen3-ASR（阿里 Qwen）</a>、<a href="https://arxiv.org/abs/2605.23463">StepAudio 2.5（阶跃星辰）</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>过去几周我把 2024 年中到 2026 年中各大厂发布的 ASR / 音频大模型技术报告系统读了一遍，每篇写了单独的解读。这篇收官文把七份报告放到一条时间线上，回答三个问题：</p> <ol> <li><strong>各家的技术路线到底差在哪？</strong></li> <li><strong>两年里行业共识是怎么一步步形成的？</strong></li> <li><strong>下一步会卷什么？</strong></li> </ol> <p>先交代一个调研中的发现：<strong>百度、腾讯、讯飞这三家传统语音强厂，在这个时间窗口内没有发布过对标级别的 ASR 技术报告</strong>。百度上一份重量级报告还是 Deep Speech（2014/2015），腾讯只有零散的单点论文（语音 tokenizer 等），讯飞的技术基本以产品和比赛系统形式存在。这本身就是个信号：<strong>这一轮 ASR 军备竞赛的主角，是 LLM 时代的新玩家（字节、阶跃、月之暗面、小米）和转型成功的老玩家（阿里）</strong>，而不是上一代语音巨头。</p> <h2 id="2-时间线全景">2. 时间线全景</h2> <p><img src="/assets/img/posts/20260715-asr-landscape/timeline.png" alt="大厂 ASR/音频大模型技术报告时间线"/></p> <p>七份报告按时间排开，能清晰看到三波浪潮：</p> <ul> <li><strong>2024.07：路线分岔点</strong>。同一个月，字节 Seed-ASR 宣告”ASR 归入 LLM”，阿里 SenseVoice 坚持”NAR 小模型 + 系统级组合”。两条路线的对峙贯穿了后面两年。</li> <li><strong>2025：数据与底座之年</strong>。FireRedASR 用数据质量掀翻参数量神话；Kimi-Audio、MiMo-Audio 把战场从 ASR 抬到通用音频底座，数据规模从百万小时卷到一亿小时。</li> <li><strong>2026：效率收敛之年</strong>。Qwen3-ASR 和 StepAudio 2.5 殊途同归——底座统一、模型瘦身、把 RTF/TTFT/并发吞吐写进主表。<strong>精度竞赛基本结束，成本竞赛开始</strong>。</li> </ul> <h2 id="3-逐维度对比">3. 逐维度对比</h2> <h3 id="31-架构路线encoder-adapter-llm-的胜利与例外">3.1 架构路线：Encoder-Adapter-LLM 的胜利与例外</h3> <table> <thead> <tr> <th>系统</th> <th>路线</th> <th>声学侧</th> <th>语言侧</th> <th>参数量</th> </tr> </thead> <tbody> <tr> <td>Seed-ASR</td> <td>LLM-based</td> <td>LUISE 2B (Conformer, SSL)</td> <td>10B+ MoE（冻结）</td> <td>~12B+</td> </tr> <tr> <td>SenseVoice-S</td> <td>NAR</td> <td>SAN-M encoder + CTC</td> <td>无</td> <td>234M</td> </tr> <tr> <td>FireRedASR-AED</td> <td>AED</td> <td>Conformer 16L</td> <td>Transformer decoder 16L</td> <td>1.1B</td> </tr> <tr> <td>FireRedASR-LLM</td> <td>LLM-based</td> <td>AED encoder 复用 710M</td> <td>Qwen2-7B + LoRA</td> <td>8.3B</td> </tr> <tr> <td>Kimi-Audio</td> <td>通用底座</td> <td>Whisper 特征 + GLM-4-Voice tokenizer</td> <td>Qwen2.5-7B 改双头</td> <td>~9B</td> </tr> <tr> <td>MiMo-Audio</td> <td>通用底座</td> <td>自研 1.2B RVQ tokenizer + patch</td> <td>MiMo-7B</td> <td>~9B</td> </tr> <tr> <td>Qwen3-ASR</td> <td>LLM-based</td> <td>AuT 180M/300M</td> <td>Qwen3 0.6B/1.7B</td> <td>0.8B/2B</td> </tr> <tr> <td>StepAudio 2.5</td> <td>底座+模式分化</td> <td>冻结 encoder + adaptor</td> <td>MoE LLM + MTP</td> <td>未披露</td> </tr> </tbody> </table> <p>几个结论：</p> <p><strong>Encoder-Adapter-LLM 成了绝对主流</strong>，但内部分化出了两个关键参数的共识：</p> <ol> <li> <p><strong>LLM 侧音频帧率收敛到 6.25-12.5Hz</strong>。Seed-ASR 160ms/帧（6.25Hz）、FireRedASR 80ms（12.5Hz）、Qwen3-ASR 和 Kimi-Audio 12.5Hz、MiMo-Audio patch 后 6.25Hz。两年里各家独立摸索，最后都落在这个区间——<strong>这基本就是”语义不丢、序列最短”的物理甜点区</strong>，后来者可以直接抄。</p> </li> <li> <p><strong>“冻结 LLM / LoRA”是训练配方的共识</strong>：语音侧模块负责把声学翻译到 LLM 语义空间，LLM 的文本能力碰都不要碰。Seed-ASR 全冻结、FireRedASR 用 LoRA、通用底座们用文本任务高权重（Kimi 1:7、MiMo 权重 100）保底，手段不同，恐惧相同——<strong>多模态训练毁掉语言能力，是所有人都踩过或怕踩的坑</strong>。</p> </li> </ol> <p><strong>例外者 SenseVoice 活得很好</strong>。NAR + CTC 的 234M 模型，10 秒音频 70ms，今天仍是”海量短音频、成本敏感”场景的默认选择。它证明了行业不是单线程进化——<strong>LLM 化解决的是难场景（口语、方言、上下文），不是所有场景</strong>。</p> <h3 id="32-数据三种哲学">3.2 数据：三种哲学</h3> <table> <thead> <tr> <th>系统</th> <th>规模</th> <th>哲学</th> </tr> </thead> <tbody> <tr> <td>FireRedASR</td> <td>7 万小时</td> <td><strong>人工标注，质量至上</strong>：”一千小时高质量好过一万小时弱标注”</td> </tr> <tr> <td>Seed-ASR</td> <td>770 万小时 SSL + 56 万 SFT</td> <td><strong>自监督规模 + 监督精调</strong></td> </tr> <tr> <td>Kimi-Audio</td> <td>1300 万小时</td> <td><strong>全自动流水线</strong>（BSRNN 增强→diarization→双系统转写），240 张 L20 日产 20 万小时</td> </tr> <tr> <td>StepAudio 2.5</td> <td>2.2T token 续训 + 50K 小时长音频伪标签</td> <td><strong>多系统投票</strong>（ROVER + 分歧率过滤 ê&gt;0.05）</td> </tr> <tr> <td>MiMo-Audio</td> <td>1 亿小时</td> <td><strong>规模换涌现</strong></td> </tr> </tbody> </table> <p>这是分歧最大的维度，但仔细看是<strong>场景决定哲学</strong>：</p> <ul> <li>FireRedASR 只做中文普通话+唱歌，任务面窄，人工标注花得起，公开 benchmark 上 7 万小时打赢 770 万小时；</li> <li>Seed/Kimi/MiMo 要的是方言、口音、多语言、副语言的长尾覆盖，这些<strong>长尾能力只能靠规模喂出来</strong>，人工标不动；</li> <li>StepAudio 的多系统投票流水线是中间路线：用已有 ASR 系统的一致性代替人工置信度，把伪标签质量做到可控。</li> </ul> <p>我的判断：<strong>benchmark 精度已经证明可以靠数据质量以小博大，但产品端的长尾鲁棒性仍然是规模的领地</strong>。两条路线都不会死。</p> <h3 id="33-推理效率2026-年的主战场">3.3 推理效率:2026 年的主战场</h3> <p>把各家公布的效率数字放一起（注意硬件和口径不完全可比，看量级）：</p> <table> <thead> <tr> <th>系统</th> <th>单并发 RTF</th> <th>首包延迟</th> <th>高并发数据</th> </tr> </thead> <tbody> <tr> <td>SenseVoice-S (2024)</td> <td>10s 音频 70ms（≈0.007）</td> <td>天然低（NAR）</td> <td>未公布</td> </tr> <tr> <td>Seed-ASR (2024)</td> <td>未公布</td> <td>未公布</td> <td>未公布</td> </tr> <tr> <td>FireRedASR (2025)</td> <td>未公布</td> <td>未公布</td> <td>未公布</td> </tr> <tr> <td>Qwen3-ASR-0.6B (2026)</td> <td>0.00923</td> <td>TTFT 92ms</td> <td>128 并发吞吐 2000 秒/秒</td> </tr> <tr> <td>StepAudio 2.5 (2026)</td> <td><strong>0.0053</strong>（H800）</td> <td>未公布</td> <td>未公布</td> </tr> </tbody> </table> <p>趋势一目了然：<strong>2024-2025 年的报告根本不谈效率，2026 年的报告把效率当主菜</strong>。原因很简单——精度差距缩小到 benchmark 上 0.1-0.3 个点之后，商业竞争的变量就只剩下每小时音频的转写成本。</p> <p>两家的加速武器还不一样，而且都源自文本 LLM 的推理优化工具箱：</p> <ul> <li><strong>StepAudio 2.5 用 MTP-5</strong>（multi-token prediction + 自回归验证），吃的是”ASR 输出被音频锚死、语义分支少、投机接受率高”的红利——第一位置接受率 0.95+，平均一次接受 5 个 token；</li> <li><strong>Qwen3-ASR 用系统工程</strong>：vLLM + CUDA Graph + bf16 + 动态 attention 窗口，靠小模型 + 成熟 serving 栈拿吞吐。</li> </ul> <p><strong>ASR 推理优化已经和文本 LLM 推理优化完全同源了</strong>——speculative decoding、KV cache 管理、continuous batching，一套工具箱通吃。这对做推理系统的同学是好消息：你的技能栈直接迁移。</p> <h3 id="34-能力半径专才与通才的分界线">3.4 能力半径：专才与通才的分界线</h3> <p>七份报告按目标可以切成两个阵营：</p> <p><strong>专用 ASR</strong>（Seed-ASR、FireRedASR、Qwen3-ASR、StepAudio-ASR 模式）：目标是转写这一件事的极致——WER、RTF、方言、热词。</p> <p><strong>通用音频底座</strong>（Kimi-Audio、MiMo-Audio，以及作为系统的 StepAudio 2.5）：ASR 只是能力之一，目标是理解+生成+对话的统一。</p> <p>数据不会说谎，把 AISHELL-1 和 LibriSpeech test-clean 拉出来看：</p> <table> <thead> <tr> <th> </th> <th>AISHELL-1 CER</th> <th>LibriSpeech clean WER</th> </tr> </thead> <tbody> <tr> <td>专用：FireRedASR-AED</td> <td><strong>0.55</strong></td> <td>—</td> </tr> <tr> <td>专用：StepAudio 2.5 ASR</td> <td>0.71</td> <td>1.27（long）/1.38</td> </tr> <tr> <td>通用：Kimi-Audio</td> <td>0.60</td> <td>1.28</td> </tr> <tr> <td>通用：MiMo-Audio</td> <td>1.65</td> <td>3.50</td> </tr> <tr> <td>专用：Qwen3-ASR-1.7B</td> <td>1.49*</td> <td>1.63</td> </tr> </tbody> </table> <p>（*Qwen3-ASR 的对比表来自 StepAudio 2.5 报告）</p> <p>Kimi-Audio 证明通用模型的 ASR 可以摸到专用第一梯队；MiMo-Audio 证明如果主攻方向是通用智能（它的 MMAU 74.9、模态差距 3.4 分是全场最佳），ASR 会付出 1-2 倍错误率的代价。<strong>“什么都要”在音频领域目前仍然做不到，你必须选</strong>。</p> <p>有意思的是 StepAudio 2.5 给出的第三种答案：<strong>底座通用，模式特化</strong>——同一个底座上用不同的后训练和解码策略分化出 ASR/TTS/Realtime 三个”人格”，每个单项都打专用系统。如果这条路线被验证可持续，专才/通才的二分法就过时了。</p> <h3 id="35-商业策略开源成了中国厂商的集体选择">3.5 商业策略：开源成了中国厂商的集体选择</h3> <p>七份报告里，<strong>只有 Seed-ASR 完全闭源</strong>。FireRedASR、SenseVoice、Kimi-Audio、MiMo-Audio、Qwen3-ASR 全部开源（Qwen3-ASR 直接 Apache 2.0），StepAudio 2.5 部分开源。</p> <p>对照 Qwen3-ASR 报告里的数字：GPT-4o-Transcribe 在 WenetSpeech 上 15.30/32.27，Qwen3-ASR-1.7B 是 4.97/5.88——<strong>国际闭源 API 在中文场景差 3-6 倍，而中文最强的模型是免费的</strong>。中文 ASR API 这个市场基本被开源模型定价归零了，商业空间被挤压到私有化部署、定制热词、行业合规这些服务层。字节的闭源策略能撑多久，取决于豆包生态的内部消化能力。</p> <h2 id="4-两年共识清单">4. 两年共识清单</h2> <p>把七份报告沉淀下来的行业共识列一遍，2026 年入场做 ASR 的团队可以直接拿走：</p> <ol> <li><strong>架构</strong>：Encoder-Adapter-LLM，encoder 用 Conformer/Transformer 系 + 大规模（伪标签或 SSL）预训练，adapter 做帧率压缩，LLM 冻结或 LoRA</li> <li><strong>帧率</strong>：LLM 侧 6.25-12.5Hz</li> <li><strong>数据</strong>：伪标签流水线（多系统投票 + 一致性过滤）是规模化正解，人工标注留给核心域</li> <li><strong>训练</strong>：SFT → context/功能 SFT → RL（reward 直接用 WER/加权 WER，不需要 reward model）</li> <li><strong>推理</strong>：vLLM 系 serving + speculative decoding/MTP，ASR 的投机接受率天然高于自由文本</li> <li><strong>评测</strong>：WER 之外必须报 RTF/TTFT/并发吞吐，通用模型加报模态差距</li> <li><strong>产品</strong>：热词/上下文注入是工业刚需，时间戳对齐用 NAR 小模型单独解决更香</li> </ol> <h2 id="5-下一步卷什么我的四个判断">5. 下一步卷什么？我的四个判断</h2> <ol> <li> <p><strong>成本战争进入深水区</strong>。单并发 RTF 已经卷到 0.005 量级，下一个战场是高并发下的单卡吞吐和 MTP/speculative 在 batch 场景的收益保持——StepAudio 2.5 没公布并发数据不是忘了，是这块还没做好。</p> </li> <li> <p><strong>流式与离线的界限会彻底消失</strong>。Qwen3-ASR 的动态 attention 窗口、Uni-ASR 的统一训练范式都在指向同一个终点：一个模型、一份权重，延迟-精度按需调节。维护两套模型的时代结束了。</p> </li> <li> <p><strong>富转写（rich transcription）会成为标配</strong>。SenseVoice 的情感/事件标签、Qwen3-ForcedAligner 的时间戳、Seed-ASR 的上下文感知——转写的定义正在从”文字”扩展到”文字+时间+说话人+情绪+场景”。纯 WER 的竞争叙事已经讲完了。</p> </li> <li> <p><strong>通用底座的 ASR 会继续逼近专用系统</strong>。Kimi-Audio 已经摸到第一梯队，StepAudio 2.5 的”模式分化”给出了工程路径。两三年内，”专门训一个 ASR 模型”可能会变成”从音频底座上后训练出一个 ASR 模式”——就像今天没人从零训文本分类模型一样。</p> </li> </ol> <p>这个系列到此完结。七篇单篇解读都在博客里，欢迎交叉阅读，也欢迎评论区拍砖——尤其是一线做 ASR 落地的同学，你们对”benchmark 数字 vs 真实场景”的体感比论文更有说服力。</p> <hr/> <p>最后扯一句题外话：写完这个系列最大的感受是，ASR 的技术演化正在变成一个”配置搜索”问题——帧率选多少、encoder 多大、LLM 冻不冻、MTP 挂几个头、任务权重怎么配，每家报告本质上都是在这个高维空间里给出自己的采样点。把这类问题做系统化，正是我们在《动手学 AutoML：从 NAS 到大语言模型优化实战》里讨论的主题——从架构搜索到 LLM 压缩，方法论是通的。对这个视角感兴趣的朋友可以翻翻。</p> <blockquote> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="ASR"/><category term="音频大模型"/><category term="论文解读"/><category term="综述"/><summary type="html"><![CDATA[大厂 ASR 技术报告横评：从 Seed-ASR 到 StepAudio 2.5，两年时间行业共识是怎么形成的]]></summary></entry><entry><title type="html">arXiv’25 | FireRedASR：小红书用 7 万小时人工标注告诉你，数据质量能打赢参数量</title><link href="https://marsggbo.github.io/blog/2026/20260715-fireredasr/" rel="alternate" type="text/html" title="arXiv’25 | FireRedASR：小红书用 7 万小时人工标注告诉你，数据质量能打赢参数量"/><published>2026-07-15T00:00:00+00:00</published><updated>2026-07-15T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260715-fireredasr</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260715-fireredasr/"><![CDATA[<h1 id="arxiv25--fireredasr小红书用-7-万小时人工标注告诉你数据质量能打赢参数量">arXiv’25 | FireRedASR：小红书用 7 万小时人工标注告诉你，数据质量能打赢参数量</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2501.14350">FireRedASR: Open-Source Industrial-Grade Mandarin Speech Recognition Models from Encoder-Decoder to LLM Integration</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>继续大厂 ASR 技术报告系列。今天这篇是小红书 FireRed 团队 2025 年 1 月放出的 FireRedASR，一篇非常”实诚”的工业报告——<strong>模型开源、架构常规、数字硬核</strong>。</p> <p>它最抓眼球的结论有两个：</p> <ol> <li><strong>FireRedASR-LLM（8.3B）在四个中文公开 benchmark 上平均 CER 3.05%</strong>，把当时的 SOTA、字节的 Seed-ASR（12B+，闭源）拉下马，相对降低 8.4%</li> <li><strong>FireRedASR-AED 只有 1.1B 参数，平均 CER 3.18%，也比 Seed-ASR 好</strong>——用不到十分之一的参数量打赢了 12B+ 的模型</li> </ol> <p>一个团队同时放出两条技术路线（传统 AED 和 LLM-based），还都开源，这在中文 ASR 圈里是头一份。更有意思的是，这篇报告用结果暗示了一个朴素但经常被忽略的道理：<strong>当你的数据足够好，参数量的差距是可以被抹平的</strong>。</p> <h2 id="2-两条路线aed-和-llm">2. 两条路线：AED 和 LLM</h2> <p>先看架构全景图，一张图把两个模型都画清楚了：</p> <p><img src="/assets/img/posts/20260715-fireredasr/001_Figure_1.png" alt="FireRedASR-LLM（左）与 FireRedASR-AED（右下）架构"/></p> <h3 id="21-fireredasr-aed常规但扎实的-encoder-decoder">2.1 FireRedASR-AED：常规但扎实的 Encoder-Decoder</h3> <p>AED（Attention-based Encoder-Decoder）就是最经典的端到端 ASR 范式：</p> <ul> <li><strong>Encoder</strong>：Conformer，16 层。先用两层步长为 2 的卷积做下采样，把时间分辨率从 10ms/帧 降到 40ms/帧，再过 Conformer block（Macaron 式 FFN + 相对位置编码的 self-attention + GLU 卷积模块，卷积核 33）</li> <li><strong>Decoder</strong>：标准 Transformer，16 层，正弦位置编码，输入输出 embedding 权重共享</li> <li><strong>输入特征</strong>：80 维 log Mel filterbank，25ms 窗，10ms 帧移</li> <li><strong>建模单元</strong>：混合 tokenization——中文用字（6827 个汉字），英文用 BPE（1000 个），总词表 7832</li> </ul> <p>没有任何花活，就是把每一个组件都做扎实。模型从 XS 到 L 四个规格：</p> <p><img src="/assets/img/posts/20260715-fireredasr/table1_arch.png" alt="两个模型的架构规格"/></p> <h3 id="22-fireredasr-llmencoder-adapter-llm-三件套">2.2 FireRedASR-LLM：Encoder-Adapter-LLM 三件套</h3> <p>LLM 路线也是现在的标准配方：</p> <ul> <li><strong>Encoder</strong>：直接用训练好的 FireRedASR-AED 的 encoder 初始化（710M）</li> <li><strong>Adapter</strong>：Linear-ReLU-Linear，把 encoder 输出投影到 LLM 的 embedding 维度。这里有个实用细节：encoder 输出 40ms/帧 对 LLM 还是太长，adapter 前面加了 <strong>frame splicing，把分辨率进一步压到 80ms/帧</strong>，直接砍半序列长度</li> <li><strong>LLM</strong>：Qwen2-7B-Instruct</li> </ul> <p><strong>训练策略是这篇报告的关键细节</strong>：encoder 和 adapter 全量训练，LLM 冻结主体、只加 LoRA。训练输入是 (prompt, speech, transcript) 三元组，loss 只算 transcript 部分。这个配比的逻辑是：<strong>让语音侧的模块负责”把声学特征翻译到 LLM 的语义空间”，LLM 自己的文本能力尽量不动</strong>。</p> <p>整个 8.3B 里，真正为 ASR 训练的参数只有 encoder 710M + adapter 22M + LoRA，不到 10%。</p> <h2 id="3-数据一句话价值百万">3. 数据：一句话价值百万</h2> <p>报告在数据部分说了一句很有态度的话，大意是：<strong>一千小时高质量人工标注数据，好过一万小时弱标注数据</strong>。</p> <p>FireRedASR 的训练数据约 <strong>70K 小时</strong>，其中中文部分绝大多数是<strong>专业标注员手工转录</strong>的真实场景音频（不是朗读式录音），另有约 11K 小时英文数据。</p> <p>对比一下同行的数据规模：</p> <ul> <li>Whisper：680K 小时弱监督数据（爬来的网络字幕对）</li> <li>Seed-ASR：自监督阶段 7.7M 小时 + 监督微调 562K 小时</li> <li>FireRedASR：70K 小时，人工标注</li> </ul> <p><strong>数据量差了一到两个数量级，但公开 benchmark 上 FireRedASR 全面领先</strong>。当然这里有个隐藏变量：FireRedASR 主攻中文普通话，任务面比 Whisper（99 种语言）和 Seed-ASR（多域多语言上下文感知）窄很多。聚焦本身就是一种优化。</p> <p>另一个训练细节：<strong>Progressive Regularization Training</strong>。1.1B 的 AED 模型不好训，他们的做法是先不加正则快速收敛，再逐步引入 dropout 和 SpecAugment。之前用 Two-pass Transducer 架构时过拟合和内存开销都有问题，换 AED + 渐进正则才把 1.1B 训稳。</p> <h2 id="4-实验结果">4. 实验结果</h2> <h3 id="41-公开-benchmark双双超过-seed-asr">4.1 公开 benchmark：双双超过 Seed-ASR</h3> <p><img src="/assets/img/posts/20260715-fireredasr/table2_public.png" alt="四个公开中文测试集上的 CER 对比"/></p> <p>关键数字：</p> <ul> <li><strong>FireRedASR-LLM 平均 3.05%</strong>，比 Seed-ASR 的 3.33% 相对降低 8.4%</li> <li><strong>FireRedASR-AED 平均 3.18%</strong>，1.1B 参数打赢 12B+ 的 Seed-ASR</li> <li>对比其它开源模型更是碾压：Qwen-Audio (8.4B) 6.19%、SenseVoice-L (1.6B) 4.47%、Whisper-Large-v3 9.86%、Paraformer-Large 4.56%</li> <li>细看单项很有意思：<strong>aishell1 上 AED（0.55）比 LLM（0.76）还好</strong>，朗读式干净语音里 LLM 的语义先验帮不上忙；但 ws_meeting（会议场景）上 LLM 明显占优（4.67 vs 5.69 的 Seed-ASR），<strong>越难、越口语化的场景，LLM 的语言建模能力越值钱</strong></li> </ul> <p>脚注里还有一个数字：如果按 Seed-ASR 报告的六个测试集平均（Average-6）算，FireRedASR-LLM 是 2.86% vs Seed-ASR 的 2.98%，依然领先。</p> <h3 id="42-scaling-规律两条路线都还没到头">4.2 Scaling 规律：两条路线都还没到头</h3> <p><img src="/assets/img/posts/20260715-fireredasr/table3_scaling.png" alt="不同规模下的平均 CER"/></p> <ul> <li>AED 从 140M 一路扩到 1.1B：3.79% → 3.56% → 3.37% → 3.18%，每次翻倍稳定拿 5-6% 的相对提升</li> <li>LLM 路线固定 Qwen2-7B 不动，只扩 encoder（86M → 710M）：3.29% → 3.05%，整体 7.3% CERR</li> </ul> <p><strong>adapter 参数几乎不变（17M → 22M），收益全部来自 encoder 变大</strong>。这个消融很说明问题：LLM-based ASR 的瓶颈不在语言模型，而在声学表征的质量。</p> <h3 id="43-真实场景和唱歌识别工业模型的私货">4.3 真实场景和唱歌识别：工业模型的”私货”</h3> <p>公开 benchmark 之外，报告还测了自建的多源场景测试集（短视频、直播、自动字幕、语音输入、智能助手五个场景）和<strong>唱歌歌词识别</strong>：</p> <p><img src="/assets/img/posts/20260715-fireredasr/table4_multisource.png" alt="多源场景与唱歌识别的 CER 对比"/></p> <ul> <li>语音场景：FireRedASR-LLM 3.48%，比某头部商业 API（ProviderA-Large）的 4.56% 相对好 23.7%，比 Paraformer-Large 好 40%</li> <li><strong>唱歌场景差距直接拉爆：7.05% vs ProviderA 的 14.16%、Paraformer 的 21.19%</strong>，相对提升 50-67%</li> </ul> <p>唱歌识别这个测试项很”小红书”——短视频平台的核心场景就是带 BGM 的人声和唱歌内容，这是通用 ASR 模型的盲区，也是自建数据的价值所在。<strong>工业报告和学术论文的区别就在这：benchmark 是拿来对外交代的，自家场景的测试集才是模型真正被优化的方向</strong>。</p> <h3 id="44-泛化方言和英文">4.4 泛化：方言和英文</h3> <ul> <li><strong>KeSpeech 中文方言</strong>：FireRedASR-LLM 3.56%，远好于 Baichuan-omni 的 6.7%</li> <li><strong>LibriSpeech 英文</strong>：test-clean 1.73% / test-other 3.67%，和 Whisper-Large-v3（1.82% / 3.50%）基本打平——考虑到英文训练数据只有 11K 小时（Whisper 的英文数据是它的几十倍），这个结果算相当能打</li> </ul> <h2 id="5-我的-take">5. 我的 take</h2> <ol> <li> <p><strong>AED 1.1B 打赢 12B+ 这个结果，比 LLM 版本夺冠更值得琢磨</strong>。它说明在数据质量足够高、任务域聚焦的前提下，传统架构的天花板远没到。直接结论：不是所有场景都需要为 ASR 挂一个 7B 的 LLM——AED 版本推理成本低得多，精度只差 4%（3.18 vs 3.05），生产环境里这笔账很好算。</p> </li> <li> <p><strong>LLM 路线的收益是”场景选择性”的</strong>。干净朗读语音（aishell1）里 LLM 反而更差，嘈杂口语场景（会议、唱歌）里 LLM 优势巨大。LLM 带来的不是均匀提升，而是<strong>用语言先验补声学信号的洞</strong>——信号越烂，先验越值钱。</p> </li> <li> <p><strong>encoder scaling 消融是给后来者的路标</strong>：固定 LLM 扩 encoder 有效，说明声学表征仍是第一瓶颈。后面 Qwen3-ASR 用 AuT encoder、StepAudio 2.5 用大规模 audio 续训，本质上都在回应同一个判断。</p> </li> <li> <p>报告没讲推理效率（RTF、延迟），这是和后来的 Qwen3-ASR、StepAudio 2.5 报告比明显缺的一块——2025 年初大家还在卷精度，2026 年的报告已经把 RTF 放进主表了，一年时间竞争维度就变了。</p> </li> </ol> <p>这个系列的最后我会写一篇横向对比，把 Seed-ASR、FireRedASR、Qwen3-ASR、StepAudio 2.5、Kimi-Audio 这些放一起讲发展脉络，欢迎关注。</p> <hr/> <p>顺带提一句，FireRedASR 的 scaling 实验（AED 四个尺寸、encoder 四个尺寸）本质上是一次手动的架构-规模搜索，这类”在给定预算下找最优模型配置”的问题正是 AutoML/NAS 的经典命题。我们把这个方向的方法论整理成了一本书《动手学 AutoML：从 NAS 到大语言模型优化实战》，里面搜索空间设计、架构评估那几章讲的就是怎么把这种实验做得更系统，感兴趣可以翻翻。</p> <blockquote> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="ASR"/><category term="论文解读"/><summary type="html"><![CDATA[arXiv’25 | FireRedASR：小红书用 7 万小时人工标注告诉你，数据质量能打赢参数量]]></summary></entry></feed>