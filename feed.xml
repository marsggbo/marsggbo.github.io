<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://marsggbo.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://marsggbo.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-08-16T18:54:52+00:00</updated><id>https://marsggbo.github.io/feed.xml</id><title type="html">HE Xin</title><subtitle>&quot;SSS: Study, Sleep, Slim&quot; </subtitle><entry><title type="html">arXiv’26 | APEX：端侧 MoE 的专家预取，别再 top-k 一刀切了</title><link href="https://marsggbo.github.io/blog/2026/20260816-apex/" rel="alternate" type="text/html" title="arXiv’26 | APEX：端侧 MoE 的专家预取，别再 top-k 一刀切了"/><published>2026-08-16T00:00:00+00:00</published><updated>2026-08-16T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260816-apex</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260816-apex/"><![CDATA[<h1 id="arxiv26--apex端侧-moe-的专家预取别再-top-k-一刀切了">arXiv’26 | APEX：端侧 MoE 的专家预取，别再 top-k 一刀切了</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2608.11688">APEX: Adaptive Expert Prefetching for Edge MoE Inference</a></p> </blockquote> <hr/> <h2 id="1-前言先把背景交代清楚">1. 前言：先把背景交代清楚</h2> <p>你有没有想过这样一个问题：<strong>MoE 模型号称”稀疏激活、天生适合端侧”，为什么真放到手机/边缘盒子上，还是慢得像老牛拉车？</strong></p> <p>先说清楚 MoE 的工作方式，这是理解全文的前提。每个 token 进到 MoE 层，router（一个很小的分类头）从 N 个专家里挑出 top-k 个（比如 Granite-3B 是 40 选 8），只有这 k 个专家的 FFN 参与计算——<strong>这就是”稀疏激活”：计算量只跟 k 走，跟 N 无关</strong>。所以一个 3B 总参的 MoE，每 token 实际只动 800M 参数。</p> <p>听起来是端侧的完美配方？坏就坏在<strong>“激活稀疏”不等于”加载稀疏”</strong>。端侧芯片的片上内存（SRAM/封装内存）只有几个 GB，装不下全部专家权重，绝大多数专家只能躺在片外的 LPDDR5X 里。于是每来一个 token，流程是：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>route（挑专家）→ load（从 LPDDR5X 搬权重）→ execute（算）
                    ↑
              这一段是纯等待，计算阵列全程闲置
</code></pre></div></div> <p>论文在 Granite-3.1-3B-A800M（1024 token 上下文）上量了这笔账：<strong>专家加载占延迟的 43%、总能耗的 29%</strong>。搬权重的时间比算权重的时间还长，而且计算阵列闲置期间还在烧静态功耗——花钱买了个电暖气。</p> <p><img src="/assets/img/posts/20260816-apex/figure1_p1_01.png" alt="端侧 MoE 的延迟与能耗拆解"/></p> <p>标准解法是<strong>预取（prefetching）</strong>：趁当前层还在算，提前把下一层要用的专家搬进来，把 load 藏进 execute 里。这条线我之前写过一篇：<a href="https://marsggbo.github.io/blog/2026/20260722-expert-prefetch/">《ST-MoE expert-prefetch》</a>——它的核心洞察是 <strong>expert 激活不是随机的，有时空相关性</strong>（相邻 token、相邻层倾向复用同一批专家），所以可以拿历史激活模式预测下一层要哪些专家，提前搬上片。另一篇 <a href="https://marsggbo.github.io/blog/2026/20260722-smoe/">《SMoE》</a>走的是完全不同的路：专家不在 GPU 上就别死等，<strong>找个”平替”专家直接算</strong>——用一点精度换完全不 stall。</p> <p>今天这篇 APEX（arXiv 2608.11688）要指出的是：<strong>现有预取的”固定 top-k”策略，在端侧是一个两头堵的死局</strong>，而它的答案是让<strong>预取预算本身随 token 自适应</strong>。在模拟的高端端侧平台上：重叠准确率保住 97–98%（ProMoE 会掉到 79%），延迟比无预取低 42%，能耗比固定超采低 21.8%，EDP 最高改善 41%。</p> <hr/> <h2 id="2-病根平均准确率是个谎言">2. 病根：平均准确率是个谎言</h2> <p>固定 top-k 预取的代表是 ProMoE：预取时多搬 δ 个”预测最可能的”专家。它的问题是 <strong>δ 是个全局常数</strong>：</p> <ul> <li>δ 给小了（比如 +2）：难 token 的预测 miss，router 要的专家不在片上，照样 stall；</li> <li>δ 给大了（比如 +8）：简单 token 白搬一堆用不上的专家，能耗爆炸。</li> </ul> <p>你可能会问：预测准确率 70–85%（ProMoE 的水平）听起来也不差啊？<strong>这里有个很隐蔽的度量陷阱</strong>。论文强调 overlap 必须按 <strong>per-layer、per-token</strong> 算：每个 token 的每一层都要求”router 选的 k 个专家 ⊆ 预取集合”才算成功。为什么这么苛刻？因为平均数会骗人——k=4 时哪怕每层平均重叠 75%（4 个里对 3 个），<strong>每个 token 每层都缺一个专家，每个 token 都 stall</strong>。串行系统里，99 层完美 + 1 层 miss = 全部白干，这一点我在<a href="https://marsggbo.github.io/blog/2026/kvcomm/">《KVCOMM》</a>那篇讲依赖链的时候就提过：<strong>流水线的吞吐由最慢的一环决定，不由平均环决定</strong>。</p> <p>而 token 之间的”预测难度”差异极大——论文 profiling 发现：<strong>大多数 token 只要 δ=2 或 δ=4 的小额预算就能全覆盖，少数难 token 才需要大预算</strong>。这个长尾分布就是”固定 δ 必然两头堵”的实证根据，也是自适应方案的立足点。</p> <hr/> <h2 id="3-平台先把端侧设备说清楚">3. 平台：先把”端侧设备”说清楚</h2> <p>这是篇<strong>算法-硬件协同设计</strong>的论文，评估平台是 CHIPSIM 综合校准联合仿真器（不是真机）。读这类论文第一步永远是搞清楚平台假设，这里的目标设备定位在”近端旗舰边缘加速器”（Jetson Thor / Hailo-10H 一档），chiplet 架构：</p> <p><strong>计算 chiplet</strong>：</p> <ul> <li>4 个 vector processing array，每个是 16×16 的 vector unit 阵列、vector length 32，BF16 matmul @ 750MHz → 峰值 <strong>24 TFLOPS</strong>；</li> <li>每阵列 2MB SRAM（共 8MB）+ 32 个 SFU 处理非线性；</li> <li>阵列是 <strong>RTL 级实现、TSMC 28nm 综合、Synopsys PrimeTime 功耗表征</strong>——不是拍脑袋的性能模型。</li> </ul> <p><strong>内存层级</strong>（这是全文的关键变量）：</p> <ul> <li>封装内：HBM3-class <strong>819 GB/s</strong>（7 pJ/bit）——放共享的热数据；</li> <li>片外：专家权重在 LPDDR5X（3 pJ/bit），走 PCIe 6.0 ×16、<strong>256 GB/s</strong>（5 pJ/bit）；</li> <li>片间 UCIe 0.5 pJ/bit。</li> </ul> <p>预取按<strong>真实 DMA 建模</strong>——受带宽竞争和排队延迟影响，不是理想化的”并行搬运不要钱”。内存用 Ramulator 2.0 建模，整体是 cycle-accurate 的联合仿真。这个严谨度比”在 A100 上模拟手机”高一档。</p> <hr/> <h2 id="4-方法一个会看人下菜的预取路由器">4. 方法：一个会看人下菜的预取路由器</h2> <p>APEX 的 pipeline 把 <code class="language-plaintext highlighter-rouge">route → load → execute</code> 改成 <code class="language-plaintext highlighter-rouge">predict → prefetch → execute</code>，由三个组件构成。</p> <h3 id="41-prefetch-router预测本层专家">4.1 Prefetch Router：预测本层专家</h3> <p>一个线性层 + softmax，从冻结的原 router 蒸馏（KL loss）。有两个设计决策值得划重点：</p> <p><strong>（1）它放在本层 attention 之前</strong>，用当前层的 hidden state 预测<strong>本层 FFN</strong> 将要路由的专家。这和 Pre-gated MoE 的”用第 L 层预测第 L+1 层”不同——APEX 的理由是同层内的 hidden state 和最终路由决策相关性最强（跨层预测等于又叠了一层不确定性）。而且预取发起得早，DMA 传输可以藏进 attention 的计算时间里。</p> <p><strong>（2）原 router 保留，依然是最终决策者</strong>。prefetch router 只负责内存调度（搬谁），不参与计算路由（用谁）——这样预测错了最多是多搬/漏搬，<strong>不会改变模型的计算语义</strong>。这一点和 Pre-gated MoE 直接用预测结果做路由的激进路线形成对比。</p> <p>蒸馏 loss 就是标准 KL：</p> \[\mathcal{L}_\text{KL} = \sum_i q_r(i) \log \frac{q_r(i)}{q_p(i)}\] <p>$q_r$ 是原 router 的专家分布，$q_p$ 是 prefetch router 的，梯度只更新后者，<strong>基座模型完全不动</strong>。</p> <h3 id="42-自适应预算-δ̂x这篇的灵魂">4.2 自适应预算 δ̂(x)：这篇的灵魂</h3> <p>固定 δ 的替代方案：给每个 token 实时决定”多取几个”。先定义 oracle——<strong>δ*</strong>（这个 token 的真实难度）：</p> \[\delta^* = \min\{\delta \in \{0, \dots, N-k\} : \mathcal{K}_r \subseteq \mathcal{K}_p^{(\delta)}\}\] <p>即”让 router 真正选的 k 个专家全部落进 top-(k+δ) 预取列表”所需的最小额外预算。δ*=0 表示预测全对，δ* 大表示这个 token 难猜。</p> <p>然后训一个<strong>序数 logistic CDF 模型</strong>来预测 δ* 的分布：</p> \[p_\delta(x) = \Pr(\delta \geq \delta^* \mid x) = \sigma(\theta_\delta - w^\top x)\] <p>$x$ 是该层 attention 之前的 token 表征，$w$ 是一个权重向量（就这么简单，一个线性打分器），$\theta_0 \leq \theta_1 \leq \dots \leq \theta_{N-k}$ 是有序阈值。推理时的规则：</p> \[\hat{\delta}(x) = \min\{\delta : p_\delta(x) \geq \tau\}\] <p><strong>“取最小的 δ 使覆盖率有 τ 的把握”</strong>——τ=0.90 是默认值。有把握的 token 只多取一两个，没把握的 token 才大手笔超采，预算精确花在长尾上。</p> <p>训练分两阶段（都是离线的，且训一次全任务通用）：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Stage 1: KL 蒸馏 prefetch router（见上式）
# Stage 2: 用累积二元交叉熵拟合 CDF
</span><span class="k">def</span> <span class="nf">cdf_loss</span><span class="p">(</span><span class="n">theta</span><span class="p">,</span> <span class="n">w</span><span class="p">,</span> <span class="n">x</span><span class="p">,</span> <span class="n">delta_star</span><span class="p">):</span>
    <span class="c1"># delta_star: profiling 得到的 oracle 标签
</span>    <span class="c1"># 对每个候选预算 delta，目标是 1[delta &gt;= delta_star]
</span>    <span class="n">loss</span> <span class="o">=</span> <span class="mi">0</span>
    <span class="k">for</span> <span class="n">delta</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">N</span> <span class="o">-</span> <span class="n">k</span> <span class="o">+</span> <span class="mi">1</span><span class="p">):</span>
        <span class="n">p</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">sigmoid</span><span class="p">(</span><span class="n">theta</span><span class="p">[</span><span class="n">delta</span><span class="p">]</span> <span class="o">-</span> <span class="n">w</span> <span class="o">@</span> <span class="n">x</span><span class="p">)</span>        <span class="c1"># P(delta* &lt;= delta)
</span>        <span class="n">target</span> <span class="o">=</span> <span class="mf">1.0</span> <span class="k">if</span> <span class="n">delta</span> <span class="o">&gt;=</span> <span class="n">delta_star</span> <span class="k">else</span> <span class="mf">0.0</span>
        <span class="n">loss</span> <span class="o">+=</span> <span class="n">F</span><span class="p">.</span><span class="nf">binary_cross_entropy</span><span class="p">(</span><span class="n">p</span><span class="p">,</span> <span class="n">torch</span><span class="p">.</span><span class="nf">tensor</span><span class="p">(</span><span class="n">target</span><span class="p">))</span>
    <span class="k">return</span> <span class="n">loss</span>
</code></pre></div></div> <p>训练配置很轻：WikiText 上 lr 5e-4、batch 8、序列长 1024、1000 步，<strong>每个模型 10 分钟到 1 小时</strong>。整个 APEX 新增参数 0.79M–34.11M，只占模型参数的 <strong>0.022%–0.060%</strong>，推理性能开销 &lt;0.051%。</p> <h3 id="43-两种执行模式语义严格-vs-绝不等待">4.3 两种执行模式：语义严格 vs 绝不等待</h3> <p>预取 miss 了怎么办？两种模式：</p> <p><strong>Correctness-preserving（默认）</strong>：router 要的专家有缺失时，<strong>先把已就绪的专家算起来，缺失的异步补取</strong>，聚合前补齐——最终聚合用的是完整的原始专家集合，<strong>语义与原 MoE 严格一致</strong>，代价只是没藏住的纠错延迟。</p> <p><strong>Stall-free（可选）</strong>：绝不等待。缺失的专家直接用”已预取集合里、<strong>原 router softmax 分数最高的候选</strong>“顶替——这就是 SMoE 那篇”平替”思想的端侧版。Algorithm 1 的逻辑：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">stall_free_select</span><span class="p">(</span><span class="n">K_routed</span><span class="p">,</span> <span class="n">q_r</span><span class="p">,</span> <span class="n">K_prefetched</span><span class="p">):</span>
    <span class="c1"># K_routed: 原 router 选的 k 个专家; q_r: 原 router 的 logits
</span>    <span class="c1"># K_prefetched: 按 delta_hat(x) 预取的 k+delta 个专家
</span>    <span class="n">alpha</span> <span class="o">=</span> <span class="nf">softmax</span><span class="p">(</span><span class="n">q_r</span><span class="p">)</span>                          <span class="c1"># 用原 router 的权重当替补排序依据
</span>    <span class="k">if</span> <span class="n">K_routed</span> <span class="o">&lt;=</span> <span class="n">K_prefetched</span><span class="p">:</span>                  <span class="c1"># 全命中，直接用
</span>        <span class="k">return</span> <span class="n">K_routed</span>
    <span class="n">keep</span>  <span class="o">=</span> <span class="n">K_routed</span> <span class="o">&amp;</span> <span class="n">K_prefetched</span>               <span class="c1"># 预测对的保留
</span>    <span class="n">missing</span> <span class="o">=</span> <span class="n">K_routed</span> <span class="o">-</span> <span class="n">K_prefetched</span>             <span class="c1"># 缺失的
</span>    <span class="n">subs</span>  <span class="o">=</span> <span class="nf">top_n_by_weight</span><span class="p">(</span><span class="n">K_prefetched</span> <span class="o">-</span> <span class="n">keep</span><span class="p">,</span> <span class="n">alpha</span><span class="p">,</span> <span class="n">n</span><span class="o">=</span><span class="nf">len</span><span class="p">(</span><span class="n">missing</span><span class="p">))</span>
    <span class="k">return</span> <span class="n">keep</span> <span class="o">|</span> <span class="n">subs</span>                            <span class="c1"># 用预取池里分数最高的顶上
</span></code></pre></div></div> <p>注意替补的排序依据是<strong>原 router</strong> 的分数（不是 prefetch router 的）——原 router 才知道”哪些专家真的合适”，prefetch router 只管”搬谁”。</p> <hr/> <h2 id="5-效果">5. 效果</h2> <h3 id="51-重叠准确率自适应的碾压局">5.1 重叠准确率：自适应的碾压局</h3> <p>τ=0.90 下，APEX 在<strong>所有层</strong>保持 <strong>&gt;97–98%</strong> 的专家重叠率；ProMoE 在 Granite-1B 的第 5 层会掉到 <strong>79%</strong>。平均每个 token 额外预取的专家数：Granite-1B 4.17、Granite-3B 2.86、Phi-7B 0.67、DeepSeek-V2-Lite 1.98——<strong>模型越”好预测”，预算自动越小</strong>，这就是自适应的意义（Phi 只有 16 个专家、路由集中，0.67 个额外预算就够 98%）。</p> <p>τ 的扫描直接展示”把握-预算”的交换关系：Granite-1B 上 τ 从 0.60→0.90→0.97，重叠率 93.4%→98.2%→99.4%，平均 δ̂ 从 1.36→4.17→6.70——<strong>想要多稳，明码标价</strong>。</p> <p><img src="/assets/img/posts/20260816-apex/figure9_p9.png" alt="重叠准确率随预算的变化"/></p> <h3 id="52-延迟与能耗预取生意的本质">5.2 延迟与能耗：预取生意的本质</h3> <p>Granite-3B、512 token、correctness-preserving 模式：</p> <ul> <li>延迟 <strong>11.41ms</strong>：比无预取（19.77ms）<strong>低 42%</strong>，比 ProMoE（15.39ms）低 26%，比固定超采 (k+4)/(k+8) 分别低 20%/40%；</li> <li>能耗 <strong>287.3mJ</strong>：比无预取低 9.5%、比 ProMoE 低 5.8%；<strong>固定 k+8 超采最惨（比 APEX 多 21.8%）</strong>——固定预算省下的延迟全在能耗上还回去了；</li> <li>Stall-free 再加 2.0–2.8% 的延迟改善，聊胜于无。</li> </ul> <p><img src="/assets/img/posts/20260816-apex/figure12_p11_18.png" alt="延迟与能耗对比"/></p> <p>能耗的拆解（1024 context）最能说明”预取生意”的本质：开 APEX 后，片外 I/O 从 48mJ 涨到 56mJ（多搬了 8mJ），但计算阵列的<strong>闲置漏电从 57mJ 崩到 8mJ</strong>（少烧 49mJ）——<strong>用 8mJ 的搬运换 49mJ 的干等</strong>，净赚 41mJ。这就是所有预取技术的账本：带宽是便宜的，闲置是昂贵的。</p> <h3 id="53-鲁棒性与精度代价">5.3 鲁棒性与精度代价</h3> <ul> <li><strong>EDP</strong>（延迟×能量积）：相对无预取改善 36–49%、相对 ProMoE 改善 16–30%（视模型而定），Phi-7B 上 stall-free 的增益尤其大（再 +14/8/7%）——因为它 k=2，miss 一个就废一个，绝不等候的价值最大；</li> <li><strong>带宽扫描</strong>：32–1024 GB/s 全区间延迟收益 14–42% 成立——带宽越紧张，预取越值钱；</li> <li><strong>精度扫描</strong>：4/8/16/32-bit 专家权重下收益都在——<strong>和量化正交可叠加</strong>（量化缩体积、APEX 缩次数）；</li> <li><strong>Stall-free 的精度代价</strong>（明码标价）：Granite-1B PPL 7.88→7.99、平均分 43.3→42.8；DeepSeek-V2-Lite PPL 7.02→7.10。<strong>Phi-7B 掉得明显（64.6→60.4）</strong>——16 选 2 的模型每个专家都是命根子，替代一个就伤筋动骨。论文因此建议 k 小的模型关掉 stall-free，用 correctness-preserving——这个诚实的边界标注很加分。</li> </ul> <hr/> <h2 id="6-我的-take">6. 我的 Take</h2> <p><strong>一，预取问题的正确度量是尾部覆盖率，不是平均准确率。</strong> ProMoE 的 80% 平均准确率听起来能打，但任何一层的 miss 都会把前面所有层的完美预取全部白干。”平均”在串行依赖链上是个没有意义的统计量——这个认识对做流水线/调度系统的同学是通用的。APEX 用一个 ordinal logistic CDF 把”这个 token 需要多少预算”建模出来，本质上是把<strong>置信度校准</strong>这套工具用到了内存搬运调度上，这个迁移本身就很漂亮。</p> <p><strong>二，端侧 MoE 的解题空间基本铺开了，三个角各站一篇。</strong> 预测得准（ST-MoE 时空相关性）、取得巧（APEX 自适应预算）、算得动（SMoE 平替）——三者还是正交的：APEX 的 prefetch router 可以吃任何更好的预测器，stall-free 的替补池也可以换成 SMoE 式的更聪明的替代选择。往后端侧 MoE 的论文大概率是在这三个角之间做组合。</p> <p><strong>三，对”仿真论文”的一点辩护。</strong> 这篇没有真机，但 RTL 综合 + PrimeTime 功耗 + Ramulator 内存 + DMA 排队的建模链，比多数”在服务器 GPU 上假装端侧”的论文可信得多。硬件协同设计类工作本来就该先在仿真里把设计空间扫清楚——当然，上真机验证仍是它（和所有同类工作）欠社区的一步。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 MoE 推理优化你想系统深入，可以看看我之前出版的<a href="https://item.jd.com/14945889.html">《动手学AutoML：从 NAS 到大语言模型优化实战》</a>，书里讲 NAS 在大模型上的延伸时正好覆盖稀疏模型结构搜索这条线，和 MoE 效率优化是同一套设计思路。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="MoE"/><category term="端侧推理"/><category term="推理优化"/><category term="论文解读"/><summary type="html"><![CDATA[arXiv’26 | APEX：端侧 MoE 的专家预取，别再 top-k 一刀切了]]></summary></entry><entry><title type="html">arXiv’26 | EcoSpec：MoE 投机解码别只顾接受率，草稿会把专家撒得到处都是</title><link href="https://marsggbo.github.io/blog/2026/20260816-ecospec/" rel="alternate" type="text/html" title="arXiv’26 | EcoSpec：MoE 投机解码别只顾接受率，草稿会把专家撒得到处都是"/><published>2026-08-16T00:00:00+00:00</published><updated>2026-08-16T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260816-ecospec</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260816-ecospec/"><![CDATA[<h1 id="arxiv26--ecospecmoe-投机解码别只顾接受率草稿会把专家撒得到处都是">arXiv’26 | EcoSpec：MoE 投机解码别只顾接受率，草稿会把专家撒得到处都是</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2607.12696">Less Experts, Faster Decoding: Cost-Aware Speculative Decoding for Mixture-of-Experts</a></p> </blockquote> <hr/> <h2 id="1-前言先把背景交代清楚">1. 前言：先把背景交代清楚</h2> <p>你有没有想过这样一个问题：<strong>在 MoE 模型上做投机解码，”验收”到底要付多少钱？</strong></p> <p>先把投机解码的账本摆出来。每轮解码：drafter 打出 γ 个草稿 token，target 模型一次并行 forward 验收，验收通过的直接收下。标准叙事里这笔账很好算：target 的验收是 prefill 式的批量计算，本来就要跑一遍，多算几个 token 的边际成本很低——所以只要草稿接受率够高就稳赚。<a href="https://marsggbo.github.io/blog/2026/20260814-dspark/">DSpark</a>（DeepSeek 线上方案：半自回归草稿 + 置信度决定每轮验收多长）优化的就是”验收预算怎么花”这个问题。</p> <p>但这个”边际成本很低”的前提，在 <strong>MoE</strong> 上悄悄变质了。原因一句话：<strong>MoE 层的计算成本不由 token 数决定，而由这批 token 激活的专家并集决定。</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Dense 模型验收 γ 个 token：多算几个 token ≈ 多一点 FLOPs，几乎免费
MoE  模型验收 γ 个 token：每个 token 各自路由 top-k 专家，
                          需要加载的权重 = 这 γ+1 组专家的并集
</code></pre></div></div> <p>如果这 γ 个 token 路由高度重叠（都走同样 8 个专家），验收确实只多花一点 FLOPs；如果互不相交，专家加载量直接 ×γ。而坏消息是：<strong>按”接受概率最高”选出来的草稿 token，天然往第二种情况跑偏</strong>——概率高的候选分布在语义空间的不同区域，路由自然分散。论文给这个现象起了名字：<strong>expert scattering（专家散射）</strong>——草稿在概率空间里抱团，在专家空间里四散开花。</p> <p>量化一下有多疼：EAGLE-3 在 Qwen3-235B 上，每个验收步平均激活 <strong>23.7 个专家/层</strong>（AR 解码 top-8 只需 8 个，3 倍带宽）；DeepSeek-V3.1 配 MTP 更是 31.4 个。一个 DeepSeek 专家（FP8）就是 <strong>44MB</strong> 的 HBM 流量——在 61 层 MoE 上，<strong>每层每步只要多”撒”出 0.2 个专家，一轮投机解码就多烧约 0.5GB 的带宽</strong>。</p> <p>EcoSpec（arXiv 2607.12696）做的事一句话讲完：<strong>在选草稿的时候，把”会激活哪些专家”算进成本函数</strong>——验收规则一个字不动（严格无损），在 DeepSeek-V3.1（671B）、Qwen3-235B、GPT-OSS-120B 上端到端最高 <strong>1.62×</strong> 加速。</p> <p>这条问题和两篇旧文有直接的血缘：<a href="https://marsggbo.github.io/blog/2026/20260722-expert-prefetch/">《ST-MoE expert-prefetch》</a>的洞察是<strong>expert 激活有很强的时空相关性、可预测</strong>——那篇用它做”提前把专家搬上片”的预取，这篇用它做”选草稿时避开冷门专家”的调度，同一个洞察的两种变现；而”MoE 推理被内存带宽卡死”这个大前提，是 <a href="https://marsggbo.github.io/blog/2026/20260722-smoe/">《SMoE》</a>那篇的主线——expert 权重搬不动，才是 MoE 部署的核心矛盾，本文不过是把这个矛盾推进到了投机解码场景。</p> <hr/> <h2 id="2-病根验收成本--专家并集而草稿选择对并集一无所知">2. 病根：验收成本 = 专家并集，而草稿选择对并集一无所知</h2> <p>把这笔账算细一点。投机解码每轮的流程：</p> <ol> <li>drafter 打出一棵草稿树（EAGLE-3：3 步 forward、每步留 top-2，长出候选树）；</li> <li>从树里选 γ 个节点送去 target 验收（γ=4 是常用值）；</li> <li>target 一次 forward 验收这 γ+1 个 token。</li> </ol> <p>第 3 步在 MoE 层 $\ell$ 上需要加载的专家集合：</p> \[\mathcal{E}_\text{verify}^{\ell} = \bigcup_{t \in S} \mathcal{S}_\ell(x_t)\] <table> <tbody> <tr> <td>$S$ 是被验收的 token 集合，$\mathcal{S}_\ell(x_t)$ 是 token $t$ 在第 $\ell$ 层路由到的专家。**现有多数工作的草稿选择只优化 $</td> <td>S</td> <td>$ 里被接受的期望 token 数，对 $\mathcal{E}_\text{verify}$ 的大小完全无感知。**</td> </tr> </tbody> </table> <p>更糟的是 batch 场景。论文扫了 batch size：<strong>batch=4 时 EAGLE-3 在 Qwen3-235B 上的加速直接掉到 1.00×</strong>（不赚不赔），因为每步的专家足迹膨胀到 54.9 个/层，撞上 HBM 带宽墙——验收慢到把投机赚的 时间 全吐回去。<strong>带宽是 MoE 投机解码的隐形天花板，而现有方法连这个天花板的存在都没意识到。</strong></p> <p><img src="/assets/img/posts/20260816-ecospec/figure1_p2.png" alt="MoE 投机解码的带宽瓶颈"/></p> <hr/> <h2 id="3-方法把专家成本写进草稿选择">3. 方法：把专家成本写进草稿选择</h2> <p>EcoSpec 在 drafter 和验收之间插了三个组件，<strong>target 的验证规则原封不动</strong>——所以输出分布和标准投机解码严格一致，无损保证照单全收。</p> <p><img src="/assets/img/posts/20260816-ecospec/figure2_p4.png" alt="EcoSpec 框架"/></p> <h3 id="31-轻量-expert-predictor预测每个草稿-token-会路由到哪">3.1 轻量 Expert Predictor：预测每个草稿 token 会路由到哪</h3> <p>第一个组件是一个小 decoder LM（Qwen3-0.6B 或 DeepSeek-R1-Distill-Qwen-1.5B，视目标模型家族而定），输入草稿 token（用目标模型的 embedding 层投影到自己的 hidden 空间），<strong>一次性输出所有 MoE 层的路由 logits</strong> $z \in \mathbb{R}^{L \times E}$，softmax 后每层取 Top-K（K 与目标模型的路由配置一致，如 Qwen3 的 Top-8、GPT-OSS 的 Top-4）得到预测专家集 $\mathcal{E}_\text{pred}$。</p> <p>训练是纯监督的：把目标模型在七个评测数据集上跑出来的<strong>真实 router 轨迹</strong>收集起来，逐层交叉熵训练——监督目标是个把质量 1/K 均摊到 k 个被激活专家上的归一化分布。80/20 划分、100 epochs、Adam lr=1e-5。测试集上的 Top-K 路由预测准确率：<strong>GPT-OSS-120B 93%、Qwen3-235B 82%、DeepSeek-V3.1 80%</strong>。</p> <p>开销：预测器每步约 <strong>4ms</strong>（对比：Qwen3 的验收本身 830ms），可以忽略。</p> <h3 id="32-全局-expert-buffer只有新专家才算成本">3.2 全局 Expert Buffer：只有”新”专家才算成本</h3> <p>第二个组件维护一个集合 $\mathcal{B}$：<strong>已被本轮已选草稿覆盖的专家</strong>。它把成本从”绝对量”变成”边际量”——第一个走某专家的 token 付全款，后面的搭便车。这个设计直接决定了打分函数的性质（见 3.3 的前缀一致性）。</p> <p>注意 $\mathcal{B}$ 是<strong>每个投机步重建的</strong>（从空集 $\mathcal{B}_0$ 开始，随贪心选择增长），不跨步持久——跨步的复用由系统层的 expert cache 负责，那不是这篇的战场。</p> <h3 id="33-cost-aware-draft-selection贪心--边际成本">3.3 Cost-Aware Draft Selection：贪心 + 边际成本</h3> <p>第三个组件是核心打分。对草稿树里每个候选节点 $t_i$：</p> \[S(t_i) = \frac{P(t_i)}{\Delta\text{Cost}(t_i \mid \mathcal{B}) + \epsilon}\] <ul> <li>$P(t_i)$：从根到 $t_i$ 路径上草稿概率的连乘——”这条路被接受的概率”代理（分子：接受率收益）；</li> <li> <table> <tbody> <tr> <td>$\Delta\text{Cost}(t_i \mid \mathcal{B}) =</td> <td>\mathcal{E}_\text{traj}(t_i) \setminus \mathcal{B}</td> <td>$：$t_i$ 整条路径的预测专家足迹中，<strong>还没被 buffer 覆盖的新增专家数</strong>（分母：带宽成本）；</td> </tr> </tbody> </table> </li> <li>$\epsilon$ 防除零。</li> </ul> <p>选择是标准贪心：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">ecospec_select</span><span class="p">(</span><span class="n">draft_tree</span><span class="p">,</span> <span class="n">P</span><span class="p">,</span> <span class="n">E_pred</span><span class="p">,</span> <span class="n">gamma</span><span class="p">):</span>
    <span class="c1"># P[node]: 根到该节点路径的草稿概率连乘
</span>    <span class="c1"># E_pred[node]: 该节点全路径的预测专家足迹 (layer, expert) 集合
</span>    <span class="n">S</span><span class="p">,</span> <span class="n">B</span> <span class="o">=</span> <span class="nf">set</span><span class="p">(),</span> <span class="nf">set</span><span class="p">()</span>                    <span class="c1"># 选中的节点 / 专家 buffer
</span>    <span class="k">while</span> <span class="nf">len</span><span class="p">(</span><span class="n">S</span><span class="p">)</span> <span class="o">&lt;</span> <span class="n">gamma</span><span class="p">:</span>
        <span class="n">best</span> <span class="o">=</span> <span class="nf">argmax</span><span class="p">(</span><span class="n">t</span> <span class="k">for</span> <span class="n">t</span> <span class="ow">in</span> <span class="n">tree</span> <span class="o">-</span> <span class="n">S</span><span class="p">,</span>
                      <span class="n">key</span><span class="o">=</span><span class="k">lambda</span> <span class="n">t</span><span class="p">:</span> <span class="n">P</span><span class="p">[</span><span class="n">t</span><span class="p">]</span> <span class="o">/</span> <span class="p">(</span><span class="nf">len</span><span class="p">(</span><span class="n">E_pred</span><span class="p">[</span><span class="n">t</span><span class="p">]</span> <span class="o">-</span> <span class="n">B</span><span class="p">)</span> <span class="o">+</span> <span class="n">eps</span><span class="p">))</span>
        <span class="n">S</span><span class="p">.</span><span class="nf">add</span><span class="p">(</span><span class="n">best</span><span class="p">)</span>
        <span class="n">B</span> <span class="o">|=</span> <span class="n">E_pred</span><span class="p">[</span><span class="n">best</span><span class="p">]</span>                  <span class="c1"># 后来的候选共享这些专家 = 边际成本归零
</span>    <span class="k">return</span> <span class="n">S</span>                               <span class="c1"># 一次 target forward 验收
</span></code></pre></div></div> <table> <tbody> <tr> <td>这个打分有一个很妙的结构性质——<strong>天然前缀一致（prefix-consistent）</strong>。因为子节点的路径概率 $P(\text{child}) = P(\text{parent}) \cdot P(\text{child}</td> <td>\text{parent}) \leq P(\text{parent})$（条件概率 ≤ 1），而父节点的专家足迹是子节点足迹的子集（$\Delta\text{Cost}$ 只会更小），所以<strong>子节点的分数不可能超过父节点</strong>（除并列外）。这意味着贪心选出的集合天然前缀闭合，和树形验证的 attention mask 结构严丝合缝，<strong>不需要任何额外的结构约束或修补项</strong>。</td> </tr> </tbody> </table> <h3 id="34-和-eagle-3--gto-的关系正交可叠加">3.4 和 EAGLE-3 / GTO 的关系：正交可叠加</h3> <p>EcoSpec 不动 drafter 本身——EAGLE-3 照常生成草稿树和候选概率，EcoSpec 只接管”从树里挑哪些节点去验收”这一步。对比实验里 drafter 配置在 baseline 和 EcoSpec 之间严格保持一致，把变量隔离在选择阶段。它甚至可以叠加在 GTO（一种改进 drafter 训练的方法）上：GTO+EcoSpec 在 Qwen3 HumanEval 上 1.13×→1.29×——<strong>改草稿生成（GTO）和改验收选择（EcoSpec）是两个正交的轴</strong>。</p> <hr/> <h2 id="4-效果">4. 效果</h2> <h3 id="41-主结果8h200γ4贪心-t07-个-benchmark">4.1 主结果（8×H200、γ=4、贪心 T=0、7 个 benchmark）</h3> <table> <thead> <tr> <th>模型</th> <th>baseline</th> <th>EcoSpec</th> <th>专家/层</th> </tr> </thead> <tbody> <tr> <td>Qwen3-235B-A22B</td> <td>EAGLE-3 1.22×</td> <td><strong>1.36×</strong>（MTBench 最高 <strong>1.62×</strong>）</td> <td>23.7→20.5</td> </tr> <tr> <td>GPT-OSS-120B</td> <td>EAGLE-3 1.14×</td> <td><strong>1.31×</strong>（MMStar/AMC 最高 1.45×）</td> <td>11.6→10.6</td> </tr> <tr> <td>DeepSeek-V3.1</td> <td>MTP 1.10×</td> <td><strong>1.15×</strong></td> <td>31.4→31.2</td> </tr> </tbody> </table> <p>采样（T=1）下同样成立：Qwen3 1.28×→1.38×、GPT-OSS 1.18×→1.30×、DeepSeek 1.28×→1.33×。</p> <p><img src="/assets/img/posts/20260816-ecospec/table1_p7.png" alt="主结果表"/></p> <p><strong>最关键的佐证是接受长度几乎没动</strong>（Qwen3 贪心 2.41→2.32，GPT-OSS 1.88→1.86）：提速全部来自”验收变便宜”，而不是”草稿变准”。这和 EAGLE 系列纯提 τ 的路线是<strong>完全不同的另一条轴</strong>——以前大家以为投机解码只有”接受率”一个旋钮，现在多了”验收成本”这个旋钮。</p> <h3 id="42-hbm-流量与延迟拆解">4.2 HBM 流量与延迟拆解</h3> <p>Table 2（每验收步的 HBM 读取量估算）：</p> <table> <thead> <tr> <th>模型</th> <th>baseline</th> <th>EcoSpec</th> <th>降幅</th> </tr> </thead> <tbody> <tr> <td>Qwen3-235B</td> <td>99.3 GB</td> <td>88.1 GB</td> <td><strong>−11.2 GB（11.3%）</strong></td> </tr> <tr> <td>GPT-OSS-120B</td> <td>6.0 GB</td> <td>5.5 GB</td> <td>−8.0%</td> </tr> <tr> <td>DeepSeek-V3.1</td> <td>97.3 GB</td> <td>96.8 GB</td> <td>−1.0%</td> </tr> </tbody> </table> <p>Qwen3 的验收延迟从 0.832s 降到 0.730s（每步总延迟 0.840→0.742s）。三个模型的差异很有信息量：<strong>Qwen3 和 GPT-OSS 的专家使用模式集中</strong>（token 倾向复用热门专家），选择空间大，省得多；<strong>DeepSeek 的均衡路由</strong>（训得好的负载均衡反而让专家使用更分散）最难省——但它的单专家最大（44MB），每层每步省 0.2 个专家就值 0.5GB，所以哪怕百分比只有 1%，绝对收益仍然划算。</p> <h3 id="43-消融边际成本预测器batch">4.3 消融：边际成本、预测器、batch</h3> <p><strong>边际成本 vs 静态全局成本</strong>：把 buffer 增量换成”路径足迹大小”的静态打分（不随选择更新），Qwen3 GSM8K 只有 1.28×；换成边际成本打分升到 <strong>1.39×</strong>（GPT-OSS 1.06×→1.11×、DeepSeek 1.16×→1.19× 同向）。<strong>“谁先来谁付全款”的建模是必要的</strong>，静态成本看不到候选之间的共享结构。</p> <p><strong>预测器鲁棒性</strong>：预测准确率从收敛值掉到 50%，Qwen3 GSM8K 加速从 1.39× 退到 1.17×——有影响但 gracefully degrade；更重要的是 <strong>oracle 专家集的结果（20.4 专家/层）和收敛预测器（20.5）几乎一样</strong>——说明 82–93% 的准确率已经不是瓶颈，继续卷预测器精度没有边际收益。</p> <p><strong>batch 扩展</strong>：batch=4 时 EAGLE-3 归零（1.00×），EcoSpec 保住 <strong>1.09×</strong>（专家足迹 45.7 vs 54.9）——<strong>带宽越紧张的并发场景，成本感知越值钱</strong>。这对线上部署（几乎永远有 batch）是最有说服力的一组数字。</p> <p><strong>γ 扫描</strong>：γ=3/4/5/6 上 EcoSpec 全区间领先，γ=4 是甜点——γ 再大，验收的专家并集膨胀，收益反而缩水。<strong>“多验收几个”在 MoE 上不是免费的</strong>，这条曲线就是证据。</p> <hr/> <h2 id="5-我的-take">5. 我的 Take</h2> <p><strong>一，这篇换掉的是投机解码的那把尺子。</strong> 收益函数从”接受了几个 token”换成”每 GB 带宽换几个 token”。在 dense 模型上这两者单调对应，在 MoE 上中间隔着一个路由并集——以前的论文不是没优化成本，是这个成本项在 dense 假设下根本不存在。类似的”隐形式成本”在其他系统里也常见（比如 prefill 的 KV 产出、allreduce 的小包合并），<strong>每换一次底层架构，成本模型就得重写一遍</strong>——这是系统研究永远有的可做之处。</p> <p><strong>二，和 DSpark 是公式里的两个不同变量，可以叠。</strong> DSpark 用置信度管理 verify 预算（分子上的 $T_\text{verify}$ 分配），EcoSpec 用专家成本管理 draft 选择（分母上的验收成本）——一个决定”验收多长”，一个决定”验收哪些”，组合起来就是完整的 MoE 投机解码调度器。我预期很快会有人把这两件事放进同一个目标函数里做联合优化。</p> <p><strong>三，”草稿的专家足迹”会成为一等公民。</strong> 随着 MoE 成为旗舰标配、投机解码成为默认配置，每个草稿 token 附带的”预计激活专家”会变成推理栈里需要显式管理的量——就像 KV Cache 的显存、batch 的槽位一样。预测路由这个组件（80–93% 准确率、4ms 开销）已经在 expert-prefetch（预取侧）、EcoSpec（选择侧）两篇里出场了，往后大概会变成 MoE 推理引擎的标准件。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 MoE 推理、投机解码优化你想系统深入，可以看看我之前出版的<a href="https://item.jd.com/14945889.html">《动手学AutoML：从 NAS 到大语言模型优化实战》</a>，书里大模型优化章节讲的稀疏化与效率权衡，和本文”专家足迹管理”是同一套设计思路。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="MoE"/><category term="投机解码"/><category term="推理优化"/><category term="论文解读"/><summary type="html"><![CDATA[arXiv’26 | EcoSpec：MoE 投机解码别只顾接受率，草稿会把专家撒得到处都是]]></summary></entry><entry><title type="html">arXiv’26 | Cross-Model KV Transfer：小模型算好的 KV Cache，大模型直接接着用</title><link href="https://marsggbo.github.io/blog/2026/20260816-kvtransfer/" rel="alternate" type="text/html" title="arXiv’26 | Cross-Model KV Transfer：小模型算好的 KV Cache，大模型直接接着用"/><published>2026-08-16T00:00:00+00:00</published><updated>2026-08-16T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260816-kvtransfer</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260816-kvtransfer/"><![CDATA[<h1 id="arxiv26--cross-model-kv-transfer小模型算好的-kv-cache大模型直接接着用">arXiv’26 | Cross-Model KV Transfer：小模型算好的 KV Cache，大模型直接接着用</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2608.03893">Cross-Model KV Cache Transfer in LLM Families: A Closed-Form Linear Mapping for Prefill Reuse</a></p> </blockquote> <hr/> <h2 id="1-前言先把背景交代清楚">1. 前言：先把背景交代清楚</h2> <p>你有没有想过这样一个问题：<strong>同一个对话里，为什么从小模型切换到大模型，前面聊过的内容要整个重新算一遍？</strong></p> <p>先把”重新算一遍”这件事的代价说具体。LLM 推理分两个阶段：prefill 阶段把整段 prompt 并行喂进去，产出两样东西——第一个 token 的概率分布，和<strong>每个 token 位置的 K/V 向量</strong>（也就是 KV Cache）；decode 阶段每生成一个 token，都要把历史 KV 从显存里读一遍。注意一个常被忽略的事实：<strong>prefill 的计算产出物本质上就是 KV Cache</strong>。也就是说，”重新 prefill”和”重新算一遍 KV Cache”是同一件事。</p> <p>那为什么会有”重新 prefill”的需求？因为现在的线上系统越来越离不开<strong>模型家族</strong>这个概念。Qwen3 有 4B/8B/14B/32B 一整排，Llama 3.1 有 8B/70B，Ministral 有 3B/8B/14B。围绕家族出现了三种很自然的玩法：</p> <ul> <li><strong>级联调度（cascade）</strong>：简单问题走小模型省钱，检测到难题再升级大模型；</li> <li><strong>会话中途换模型</strong>：对话前几轮用便宜模型顶着，用户开始问硬核问题了再切大模型；</li> <li><strong>家族内路由（routing）</strong>：一个 router 在家族成员间动态选模型。</li> </ul> <p>这三种玩法有一个共同的隐形税：<strong>每次切换，接收方模型都要把累积的上下文从头 prefill 一遍。</strong> 长对话切一次模型，TTFT 直接多几秒——这在产品体验上是很难接受的。</p> <p>所以这个问题可以抽象得非常干净：<strong>A 模型算好的 KV Cache，能不能直接变换成 B 模型的 KV Cache？</strong> 而且注意这是双向需求的：小→大是质量升级（升级后不用重算），大→小是成本降级（长上下文阶段切回小模型省 decode 成本）。</p> <p>在讲这篇论文的方法之前，先把我之前写过的两篇 KV 复用文章放这，它们和本文正好构成一条完整的线：</p> <ul> <li><a href="https://marsggbo.github.io/blog/2026/kvcomm/">《KVCOMM》</a>：<strong>同一个模型</strong>内部，多个 Agent 共享相同前缀的 KV——它的洞察是不同 Agent 的 system prompt / 工具描述高度重叠，把这份 KV 在 Agent 间”通起来”，TTFT 砍 7.8 倍；</li> <li><a href="https://marsggbo.github.io/blog/2026/semsharekv/">《SemShareKV》</a>：更进一步，用 LSH 做<strong>语义级</strong>共享——不用前缀完全相同，意思相近的段落也能共享 KV。</li> </ul> <p>这两篇的共同前提都是”<strong>同一个模型</strong>“：KV Cache 是模型私有的记忆，换个模型就作废。今天这篇（arXiv 2608.03893）补的就是最后这块拼图：<strong>跨模型</strong>的 KV 空间对齐。它的结论出人意料地简单粗暴：<strong>同一家族的两个模型之间，KV Cache 近似差一个线性映射——而线性映射有闭式解，一行矩阵求逆就能算出来，完全不需要训练。</strong></p> <p>在 Qwen3 14B→32B 上，转换后的 KV 直接喂给 32B，五个 benchmark 平均保住 <strong>97.6%</strong> 的准确率；32K token 的上下文，重新 prefill 要 6975ms，KV 转换只要 <strong>277.6ms，快 25 倍</strong>。</p> <hr/> <h2 id="2-前置知识跨模型-kv-复用前人卡在哪">2. 前置知识：跨模型 KV 复用，前人卡在哪</h2> <p>跨模型 KV 这个方向其实已经有几篇工作了，先看它们为什么不够用，才能理解这篇的定位：</p> <table> <thead> <tr> <th>方法</th> <th>核心思路</th> <th>问题</th> </tr> </thead> <tbody> <tr> <td><strong>C2C</strong>（Cache-to-Cache）</td> <td>给每个模型对训练一个神经 fuser，融合两个模型的 KV</td> <td>每一对模型都要梯度训练，成本高、泛化差</td> </tr> <tr> <td><strong>LatentAlign</strong></td> <td>给每个模型学一个 adapter，把各自 KV 投到共享 latent 空间</td> <td>同样需要训练每个模型的 adapter</td> </tr> <tr> <td><strong>IAM</strong></td> <td>不搬 KV 值，把小模型的 <strong>attention pattern</strong>（注意力模式）代入大模型</td> <td>无梯度，但传的是 pattern 不是 cache，复用度有限</td> </tr> <tr> <td><strong>DroidSpeak</strong></td> <td>在<strong>架构完全相同</strong>的微调变体之间（如同 base 模型和它的 SFT 版）直接共享 KV</td> <td>无梯度，但只支持同构微调对，不支持跨规模（8B→70B 这种）</td> </tr> </tbody> </table> <p>理想形态需要同时满足四条：<strong>无梯度训练、跨规模、传输 KV 值本身、闭式解</strong>。前四篇各占一到两条，这篇是第一个全占的。为什么”无梯度”这么重要？因为跨模型的组合是 $O(n^2)$ 的——一个四模型家族就有 12 个有序对，如果每对都要训练，这个方法就只能是论文 demo；如果是闭式解，每对就是”跑一次最小二乘”的事，部署成本完全不同量级。</p> <hr/> <h2 id="3-关键观察跨模型的-kv-有线性结构">3. 关键观察：跨模型的 KV 有线性结构</h2> <p>整个方法能成立，全压在一个实证观察上：<strong>同一家族里，两个模型的 KV Cache 之间存在显著的线性关系。</strong></p> <p>怎么验证？把源模型第 $l$ 层的（处理过的）key/value 作为自变量 $X$，目标模型对应位置的 key/value 作为因变量 $Y$，做线性回归，看 $R^2$（方差解释率）。在 Qwen3 14B→32B 上：</p> <table> <thead> <tr> <th>源层数 k</th> <th>K 的 R²</th> <th>V 的 R²</th> </tr> </thead> <tbody> <tr> <td>1</td> <td>0.5572</td> <td>0.3249</td> </tr> <tr> <td>8</td> <td>0.7914</td> <td>0.6541</td> </tr> <tr> <td>全部</td> <td>0.8451</td> <td>0.7645</td> </tr> </tbody> </table> <p><img src="/assets/img/posts/20260816-kvtransfer/figure2_p3.png" alt="跨模型 KV 的线性结构"/></p> <p>三个值得划重点的细节：</p> <p><strong>（1）单层就能解释 56% 的 key 方差</strong>。这说明两个模型的中间表征不是”语义上有点像”那种模糊的相似，而是<strong>近似线性可达</strong>的——这个数字给了线性映射合法性。</p> <p><strong>（2）key 比 value 好预测约 0.2 个 R²</strong>。为什么？key 承载的主要是”检索信息”（这个 token 该被谁注意到），在同族模型里检索结构高度保守；value 承载的是”内容信息”，模型规模变大后内容表征会重新组织，漂移更大。这个不对称后面选 MLP 补救时还会用到。</p> <p><strong>（3）RoPE 会污染拟合</strong>。key 在存进 cache 前被 RoPE 旋转过——位置信息乘在内容上，导致”第 5 个 token 的 key”和”第 5000 个 token 的 key”即使内容一样，数值也完全不同。直接拿带 RoPE 的 key 拟合，$R^2$ 会显著掉，而且”哪层源最相关”的结构变得模糊；<strong>把 RoPE 剥掉再拟合，层与层之间的对角结构立刻清晰</strong>。这个观察直接催生了方法的第三个组件。</p> <p>为什么同族模型的 KV 会有线性结构？我的理解是：<strong>家族模型本来就是同一份数据、相近的 tokenizer、相近的配方练出来的</strong>，中间层学到的特征空间高度同构——尤其 key 这种低频检索结构，几乎是”家族指纹”级别的保守量。论文没有从理论上证明这一点（也不需要），但把它量化到”一个 per-head 线性映射能搬 97% 的信息”，本身就是很有价值的实证贡献。</p> <hr/> <h2 id="4-方法每头一个闭式-ridge三个组件拼起来">4. 方法：每头一个闭式 Ridge，三个组件拼起来</h2> <p>整条 pipeline 长这样：源模型正常 prefill 出 KV Cache → <strong>per-head 线性映射</strong>把它变换成目标模型的 KV 格式 → 目标模型跳过 prefill 直接 decode：</p> <p><img src="/assets/img/posts/20260816-kvtransfer/figure1_p2.png" alt="KV 转换 pipeline"/></p> <p>方法由三个组件构成，逐个拆。</p> <h3 id="41-per-head-ridge-回归一行闭式解">4.1 Per-head Ridge 回归：一行闭式解</h3> <p>对目标模型的每个 (layer $l$, head $h$)，K 和 V 各学一个独立的线性映射。设计矩阵 $X$ 是源模型 top-k 个源层的 KV 拼接（$N \times (k \cdot n_{kv} \cdot d_h)$ 维），$Y$ 是目标模型该 head 的 K 或 V（$N \times d_h^t$ 维）。解就是标准 ridge：</p> \[W^* = (X^\top X + \lambda I)^{-1} X^\top Y, \quad \lambda = 0.01\] <p>几个工程细节值得说：</p> <ul> <li><strong>为什么是 ridge 不是 OLS</strong>：top-k 源层是按”最相关”挑出来的，它们彼此特征高度相关，$X^\top X$ 接近奇异，裸 OLS 数值上会炸。加 $\lambda I$ 正则后稳定，且 $\lambda$ 在四个数量级上扫描结果都很平（只有 $\lambda=1$ 这种离谱值才崩，HellaSwag 掉 15.79pp）——说明这个超参很钝，不挑；</li> <li><strong>bias 的处理</strong>：$X$、$Y$ 先中心化再求解，$W^<em>$ 只估斜率，事后按 $\ b = \bar{Y} - \bar{X}W^</em>$ 把偏置补回来；</li> <li><strong>计算量的去向</strong>：拟合的大头是 $X^\top X$（$O(N d_s^2)$），<strong>每个目标层只算一次、层内所有 head 共享</strong>——这就是”47 分钟拟合一整对模型”的来源；</li> <li><strong>head 之间零共享</strong>：每个 (l, h) 一个独立的 $W_K^{l,h}$ 和 $W_V^{l,h}$，不跨 head、不跨 K/V 共享参数。线性映射本来就便宜，没必要共享。</li> </ul> <p>翻译成代码，核心就是这几行（拟合阶段）：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">fit_head_mapper</span><span class="p">(</span><span class="n">X</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> <span class="n">Y</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">,</span> <span class="n">lam</span><span class="p">:</span> <span class="nb">float</span> <span class="o">=</span> <span class="mf">0.01</span><span class="p">):</span>
    <span class="c1"># X: [N, d_src]   源模型 top-k 层拼接的 K（或 V），已剥 RoPE、已中心化
</span>    <span class="c1"># Y: [N, d_tgt]   目标模型同位置的 K（或 V），已剥 RoPE、已中心化
</span>    <span class="n">d_src</span> <span class="o">=</span> <span class="n">X</span><span class="p">.</span><span class="n">shape</span><span class="p">[</span><span class="mi">1</span><span class="p">]</span>
    <span class="n">XtX</span> <span class="o">=</span> <span class="n">X</span><span class="p">.</span><span class="n">T</span> <span class="o">@</span> <span class="n">X</span>                          <span class="c1"># 每层一次，层内所有 head 共享
</span>    <span class="n">XtY</span> <span class="o">=</span> <span class="n">X</span><span class="p">.</span><span class="n">T</span> <span class="o">@</span> <span class="n">Y</span>
    <span class="n">W</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="n">linalg</span><span class="p">.</span><span class="nf">solve</span><span class="p">(</span><span class="n">XtX</span> <span class="o">+</span> <span class="n">lam</span> <span class="o">*</span> <span class="n">torch</span><span class="p">.</span><span class="nf">eye</span><span class="p">(</span><span class="n">d_src</span><span class="p">),</span> <span class="n">XtY</span><span class="p">)</span>   <span class="c1"># 闭式解，无梯度
</span>    <span class="k">return</span> <span class="n">W</span>                                <span class="c1"># 推理时: Y_hat = X_centered @ W + b
</span></code></pre></div></div> <h3 id="42-跨层源选择不是同层对同层">4.2 跨层源选择：不是同层对同层</h3> <p>第二个反直觉的组件：<strong>目标层的 KV 不是只从源模型的同层映射，而是从”最有预测力”的 k 个源层拼起来映射。</strong></p> <p>注意这和我们训练模型时的直觉相反——第 20 层的表征不一定最像另一个模型的第 20 层（两模型深度、宽度都不同），真正该问的是”目标第 20 层的 K 能被源模型的哪几层线性表出”。论文用<strong>贪心前向选择（greedy forward selection）</strong>回答这个问题：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">greedy_forward_selection</span><span class="p">(</span><span class="n">X_layers</span><span class="p">,</span> <span class="n">Y</span><span class="p">,</span> <span class="n">max_k</span><span class="p">):</span>
    <span class="c1"># X_layers: {源层id: 该层所有 token 的 K}   Y: 目标层所有 token 的 K
</span>    <span class="n">selected</span> <span class="o">=</span> <span class="p">[</span><span class="nf">best_single_layer_by_R2</span><span class="p">(</span><span class="n">X_layers</span><span class="p">,</span> <span class="n">Y</span><span class="p">)]</span>   <span class="c1"># 1. 从单层 R² 最高的源层起步
</span>    <span class="k">while</span> <span class="nf">len</span><span class="p">(</span><span class="n">selected</span><span class="p">)</span> <span class="o">&lt;</span> <span class="n">max_k</span><span class="p">:</span>
        <span class="c1"># 2. 每轮加入能让"联合 R²"提升最大的那个源层
</span>        <span class="n">gains</span> <span class="o">=</span> <span class="p">{</span><span class="n">j</span><span class="p">:</span> <span class="nf">joint_r2</span><span class="p">(</span><span class="n">X_layers</span><span class="p">[</span><span class="n">selected</span> <span class="o">+</span> <span class="p">[</span><span class="n">j</span><span class="p">]],</span> <span class="n">Y</span><span class="p">)</span> <span class="k">for</span> <span class="n">j</span> <span class="ow">in</span> <span class="n">all_layers</span> <span class="o">-</span> <span class="nf">set</span><span class="p">(</span><span class="n">selected</span><span class="p">)}</span>
        <span class="n">selected</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="nf">argmax</span><span class="p">(</span><span class="n">gains</span><span class="p">))</span>
    <span class="k">return</span> <span class="n">selected</span>                                        <span class="c1"># 3. 产出 k=1..all 的整条曲线
</span></code></pre></div></div> <p>结论：<strong>收益集中在 k=1→4，k=6 就到全层 R² 的 92.3%（key）/ 87.7%（value）</strong>，之后再堆源层是边际递减。生产上不用逐对跑贪心（那是分析工具），直接按”单源 head 平均 R² 排序取 top-k”固定下来，同层所有 head 共享一套源层选择。</p> <p>消融里这个组件是<strong>贡献最大的单项</strong>：k 从 8 砍到 1，ARC-C 从 61.60 崩到 27.65，PPL 从 7.33 崩到 22.73——单层信息量远不够，跨层拼接才是主力。</p> <h3 id="43-rope-因式分解让映射与位置解耦">4.3 RoPE 因式分解：让映射与位置解耦</h3> <p>第三个组件解决”位置编码污染”。源 key 存的是 $K_s R_{\Theta_s}$（内容乘上源模型的 RoPE），直接映射会把”位置分布”也学进权重里——校准用 1024 token 的短序列，权重就绑死在这个位置分布上，换上下文长度就失效。</p> <p>做法是把位置”剥掉-映射-再穿上”：</p> \[\hat{K}_t = \big(K_s\, R_{\Theta_s}^{-1}\, W_K + b_K\big)\, R_{\Theta_t}\] <ul> <li>$R_{\Theta_s}^{-1}$：源模型 RoPE 的逆旋转——RoPE 是正交旋转，<strong>求逆是精确且免费的</strong>（转置即可），剥完得到无位置的 content key；</li> <li>$W_K$：在无位置空间里做线性映射（校准时目标侧同样剥 RoPE，权重完全 position-free）；</li> <li>$R_{\Theta_t}$：套上<strong>目标模型自己的</strong> RoPE——两模型 RoPE 配置不同也无所谓，反正位置是在最后一步才穿上的。</li> </ul> <p><strong>value 不编码位置</strong>，直接 $ \hat{V}_t = V_s W_V + b_V$。</p> <p>这一步的必要性有硬数据：推理时不做重旋转，MMLU 从 78.09 崩到 <strong>25.79</strong>（随机水平）、GSM8K 崩到 4.17；而 HellaSwag 只掉 5pp——因为 HellaSwag 这种局部语言建模任务对位置不敏感，MMLU/GSM8K 这种需要全局结构的任务会立刻暴露位置的错位。这个对比也侧面说明：<strong>评测集选不对，坏掉的组件根本测不出来。</strong></p> <h3 id="44-校准与部署开销">4.4 校准与部署开销</h3> <ul> <li><strong>数据</strong>：FineWeb-Edu 500 条 × 1024 token，stride-4 子采样后每个目标 head 约 <strong>128K 个 token 级观测</strong>。稳健性：200 条之后饱和，50 条也只差 1.6pp；<strong>校准域比数据量重要</strong>（CodeAlpaca 校准比 FineWeb-Edu 掉 5.24pp，Wikipedia 只掉 1.05pp）；</li> <li><strong>拟合算力</strong>：单节点 8×H100，每对模型 <strong>47–87 分钟</strong>（Qwen3 14B→32B 约 52 分钟），全程无反向传播；精度上前向 bf16、协方差 fp32；</li> <li><strong>mapper 体积</strong>：每对 1.01–3.36B 参数（4–12GB）——不小，但<strong>不需要常驻 GPU</strong>，可以从 CPU/磁盘按需 page in（80–480ms，每次切换付一次），因为切换模型本来就是低频事件；</li> <li><strong>适用边界</strong>：同家族、KV head 数与 head dim 匹配、dense full attention。GQA 结构不匹配的不行，MLA 这种 latent 压缩的更不行；每个方向各需一个 mapper（14B→32B 和 32B→14B 是两个不同的映射）。</li> </ul> <hr/> <h2 id="5-实验保真度延迟漂移三本账">5. 实验：保真度、延迟、漂移三本账</h2> <h3 id="51-保真度能保住多少table-1">5.1 保真度：能保住多少？（Table 1）</h3> <p>先定义指标。<strong>Retention = 转换后准确率 ÷ 目标模型独立 prefill 的准确率 × 100%</strong>。还有个更严苛的 <strong>floor-normalized</strong> 版本：把随机_chance 定为 0%、目标独立运行定为 100%，专治”低分对看起来还有 40%”的错觉。</p> <table> <thead> <tr> <th>迁移方向</th> <th>k</th> <th>平均 retention</th> <th>关键项</th> </tr> </thead> <tbody> <tr> <td>Qwen3 14B→32B</td> <td>8</td> <td><strong>97.6%</strong></td> <td>HellaSwag 101.0%（超过独立运行），GSM8K 95.6%</td> </tr> <tr> <td>Qwen3 8B→32B</td> <td>12</td> <td>87.5%</td> <td>GSM8K 掉到 68.8%</td> </tr> <tr> <td>Llama 3.1 8B→70B</td> <td>20</td> <td>72.8%</td> <td>HellaSwag 94.4%，但 GSM8K 只有 18.2%</td> </tr> <tr> <td>Ministral 3 3B→8B</td> <td>all</td> <td>76.2%</td> <td> </td> </tr> <tr> <td>Ministral 3 3B→14B</td> <td>20</td> <td>44.2%</td> <td>floor-normalized 只剩 14.7%，基本不可用</td> </tr> <tr> <td>Ministral 3 8B→14B</td> <td>12</td> <td>41.6%</td> <td>floor-normalized 11.1%，不可用</td> </tr> </tbody> </table> <p><img src="/assets/img/posts/20260816-kvtransfer/table1_p6.png" alt="主结果表：各迁移对的保真度"/></p> <p>规律很清楚：<strong>同代、尺寸相近的家族对是甜点</strong>（14B→32B 几乎无损），跨得越远崩得越狠；推理类任务（GSM8K）比语言建模类任务（HellaSwag）敏感得多。这个边界本身就有工程价值——级联系统可以据此决定哪几档之间值得开 KV 转移、哪几档老实重新 prefill。</p> <h3 id="52-延迟省多少table-5">5.2 延迟：省多少？（Table 5）</h3> <p>Qwen3 14B↔32B、32K token：重新 prefill <strong>6975ms</strong> vs mapper <strong>277.6ms = 25×</strong>（小→大）；大→小是 2952.7ms vs 427.1ms = <strong>7×</strong>。整个 64–32K 区间、七个迁移对、<strong>70 个测试格 mapper 全部更快</strong>，总范围 2.7×–25.1×。</p> <p><img src="/assets/img/posts/20260816-kvtransfer/table5_p9.png" alt="重 prefill vs KV 转换延迟"/></p> <p>为什么大→小省得少？因为小模型的 prefill 本来就便宜，能省的基数小。这也符合业务直觉：升级方向的收益（用户等不起大模型 7 秒 prefill）恰恰是最大的。</p> <h3 id="53-多轮会话会不会漂">5.3 多轮会话会不会漂？</h3> <p>CoQA 十轮多轮 handoff（14B↔32B 交替使用对方的 KV）：小→大的差距从第 1 轮到第 10 轮只扩大 1.7pp；大→小每轮线性漂移 0.33pp。<strong>十轮之内不会级联崩坏</strong>——误差没有复利效应，这是”会话中途换模型”能上线的安全垫。</p> <h3 id="54-闭式解不够的地方mlp-补位以及一个有趣的归因">5.4 闭式解不够的地方：MLP 补位，以及一个有趣的归因</h3> <p>Ridge 在它擅长的对上和 MLP 打平（97.6% vs 97.3%），但在崩掉的对上 MLP 能救回来很多：Ministral 3B→14B 从 68.0% 拉到 92.3%（<strong>+24.3pp</strong>）、8B→14B 从 58.7% 到 95.5%（<strong>+36.8pp</strong>）。</p> <p>更有意思的是论文对”为什么 MLP 能救”的归因分析：<strong>决定保真度的不是误差的大小，而是误差落在哪个子空间</strong>。MLP 的增益恰好对应把残差从 attention 敏感的方向挪开（失败对上 K-concentration Δ≈−2.5、cosine Δ≈+0.45）。两个佐证：</p> <ul> <li>attention 输出的 cosine 相似度与最终 retention 的相关性 <strong>r=+0.57</strong>；</li> <li>校准时的 R² 与 retention 的相关性 <strong>r=−0.20</strong>——基本不相关！</li> </ul> <p><strong>“拟合得好”和”用得好”是两件事</strong>：R² 衡量的是全空间平均误差，而 attention 只对某些子空间敏感。这个结论对以后所有做 KV 压缩/量化的工作都适用：<strong>评测拟合质量请直接测下游，别只看 R²</strong>。顺带地，attention-output cosine 可以当一个便宜的”这个模型对能不能转移”的事前预测器。</p> <hr/> <h2 id="6-我的-take">6. 我的 Take</h2> <p><strong>一，KV 复用这条线的版图补全了。</strong> 从”同模型同前缀”（prefix caching，vLLM/SGLang 标配）→”同模型语义共享”（SemShareKV）→”多 Agent 共享”（KVCOMM）→”跨模型搬运”（本文），每一层的适用范围和代价都清晰了。工程组合拳也顺理成章：同族切换用 KV Transfer，跨族切换乖乖 prefill。</p> <p><strong>二，”闭式解”这个形态选对了。</strong> 它把跨模型 KV 复用从”研究问题”压成了”部署时的一次性成本”（一对模型 52 分钟 + 一次磁盘存放），这是能进推理框架的形态。哪天 vLLM 里出现 <code class="language-plaintext highlighter-rouge">--kv-transfer-adapter</code> 这种启动参数，我不会意外。</p> <p><strong>三，边界要诚实。</strong> 只支持 dense attention 同构 KV 家族对，GQA 不匹配、MLA 压缩都无解；Ministral 那两个失败对说明”家族”的假设并不总成立。它买到的是<strong>家族内</strong>的廉价切换，不是万能 KV 翻译器——但就这个受限场景（而它恰好是 cascade/routing 的主流场景），97.6% retention + 25× prefill 节省，这笔账很划算。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 KV Cache、LLM 推理优化你想系统深入，可以看看我之前出版的<a href="https://item.jd.com/14945889.html">《动手学AutoML：从 NAS 到大语言模型优化实战》</a>，书里有专门一章讲 LLM 推理效率与 KV Cache 优化，本文的 prefill 复用正是那一章的主线问题。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="KV Cache"/><category term="推理优化"/><category term="论文解读"/><summary type="html"><![CDATA[arXiv’26 | Cross-Model KV Transfer：小模型算好的 KV Cache，大模型直接接着用]]></summary></entry><entry><title type="html">arXiv’26 | Wind-MTP：百万上下文里，MTP 草稿头才是那个隐形税</title><link href="https://marsggbo.github.io/blog/2026/20260816-windmtp/" rel="alternate" type="text/html" title="arXiv’26 | Wind-MTP：百万上下文里，MTP 草稿头才是那个隐形税"/><published>2026-08-16T00:00:00+00:00</published><updated>2026-08-16T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260816-windmtp</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260816-windmtp/"><![CDATA[<h1 id="arxiv26--wind-mtp百万上下文里mtp-草稿头才是那个隐形税">arXiv’26 | Wind-MTP：百万上下文里，MTP 草稿头才是那个隐形税</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2607.21535">Wind-MTP: Removing the Full-Context Draft-KV Tax</a></p> </blockquote> <hr/> <h2 id="1-前言先把背景交代清楚">1. 前言：先把背景交代清楚</h2> <p>你有没有想过这样一个问题：<strong>投机解码里的草稿模型，真的是”几乎免费”的吗？</strong></p> <p>先把投机解码的账本摆出来。每轮解码：</p> \[L = \frac{T_\text{draft} + T_\text{verify}}{\tau}\] <p>$T_\text{draft}$ 是打草稿的时间，$T_\text{verify}$ 是大模型验收的时间，$\tau$ 是平均每轮被接受的 token 数。过去两年这个领域的工作几乎都能塞进这个公式：</p> <ul> <li><a href="https://marsggbo.github.io/blog/2026/20260814-dflash/">DFlash</a>（ICML’26）：用 Diffusion drafter 替掉 AR drafter，一次 forward 并行草拟整个 block，把 $T_\text{draft}$ 从”随草稿长度线性涨”压成常数；</li> <li><a href="https://marsggbo.github.io/blog/2026/20260814-dspark/">DSpark</a>：DeepSeek 线上方案，半自回归 + 置信度调度，同时优化 $\tau$ 和验收预算的分配；</li> <li><a href="https://marsggbo.github.io/blog/2026/20260722-cassandra/">Cassandra</a>：不训练新模型，直接从 target 模型的权重 bitstream 里抠出一个 drafter，把草稿的<strong>训练成本</strong>都省了。</li> </ul> <p>这条线的共识是：<strong>草稿阶段必须便宜</strong>。但所有这些工作（包括这些论文自己）都默认了一个前提——<strong>验收那边（target）才是大头，草稿头那点计算可以忽略</strong>。</p> <p>这个前提在 dense 模型 + 短上下文时代基本成立。但这篇 Wind-MTP（arXiv 2607.21535）指出，它正在两个趋势的夹击下失效：</p> <ol> <li><strong>上下文爆炸</strong>：旗舰模型的上下文已经冲到 1M token。草稿头再轻，它用的也是<strong>全量 attention</strong>——每猜一个草稿 token，都要把全部历史 KV 读一遍；</li> <li><strong>target 越来越快</strong>：新一代模型的 target 主干纷纷换上 GDN（Gated DeltaNet）、Mamba2 这类线性/递归注意力，decode 一步的成本本来就被压得很低。</li> </ol> <p>一边膨胀、一边瘦身，结果就是倒挂。论文给出的数字：<strong>1M context、γ=6 时，草稿阶段给每个 decode step 白加 92%–138% 的时间——比验收本身还贵</strong>。在难任务上原生 MTP 甚至跑输完全不开投机解码的 dense 解码（Qwen-122B code QA 只有 0.80×）——<strong>开了投机解码反而更慢</strong>，这是最打脸的情况。</p> <p>解法说破了不值钱：<strong>给草稿头的 attention 开一个 4K 的滑动窗口，验收侧原封不动</strong>。零训练、无新 kernel、严格无损，1M context 端到端再提速 <strong>11%–53%</strong>。但”为什么敢砍”和”为什么砍了不崩”，里面的分析做得非常扎实，这才是这篇论文的含金量。</p> <hr/> <h2 id="2-病根草稿头也在读全量-kv而且读-γ-遍">2. 病根：草稿头也在读全量 KV，而且读 γ 遍</h2> <p>先把这笔税的来源拆开。设上下文长度 $S$、草稿深度 $d$（drafter 层数）、每轮草稿长度 $\gamma$。一个 decode 迭代：</p> \[T_\text{iter} = \underbrace{t_\text{verify}}_{\text{验收 1 次}} + \underbrace{t_\text{draft}^{\text{ctx}}}_{\text{每步读历史 KV}} + \gamma \cdot \underbrace{t_\text{draft}^{\text{fwd}}}_{\text{草稿层 forward}}\] <p>关键在中间那项：MTP 头虽然是”挂在主干上的小头”，但它的 attention 是标准的 full attention——<strong>每生成一个草稿 token，都要对全部 $S$ 个历史位置做 attention，把它们的 KV 从 HBM 读一遍</strong>。$\gamma$ 个草稿 token 就是 $\gamma$ 遍。</p> <p>短上下文时这笔开销无感：单层 draft 的 KV 每 token 只有 1–2KB，8–32K 上下文里整个 draft KV 就 8–64MB，全在 L2 cache 里躺着，读了也不心疼。但到 1M context：</p> <ul> <li>draft 的 KV 膨胀到 <strong>1–2GB</strong>，每一步 γ 次从 HBM 全量读；</li> <li>论文实测三个模型的 draft KV pool 已占总 KV 显存的 <strong>7.7%–11.1%</strong>（Qwen-122B 7.7%、Qwen-35B 9.1%、Nemotron 11.1%，正好等于架构里 $1/(F+1)$，F 是 full-attention 层数）。</li> </ul> <p>Table 1 给了三个模型的每步成本（1M、d=7、B=1、bf16 KV、单张 B200）：</p> <table> <thead> <tr> <th>模型</th> <th>原生 MTP</th> <th>窗口化后</th> <th>草稿税</th> </tr> </thead> <tbody> <tr> <td>Qwen3.6-35B-A3B</td> <td>26.4 ms</td> <td>18.3 ms</td> <td>+44.3%</td> </tr> <tr> <td>Qwen3.5-122B-A10B</td> <td>34.5 ms</td> <td>26.5 ms</td> <td>+30.2%</td> </tr> <tr> <td>Nemotron-3-120B-A12B</td> <td>33.1 ms</td> <td>25.8 ms</td> <td>+28.3%</td> </tr> </tbody> </table> <p><img src="/assets/img/posts/20260816-windmtp/table1_p5.png" alt="每步成本对比"/></p> <p>nsys 的 kernel 级分解（Table 7，γ=6）更能说明钱的去向——每个迭代拆成 $t_\text{verify}^\text{base}$（裸验收）、$t_\text{verify}^\text{ovh}$（投机记账）和 $t_\text{draft}$ 三段，以 Qwen-35B 为例：12.5 / 4.6 / <strong>12.7</strong> ms，总计 29.7ms。<strong>草稿和裸验收几乎一样贵</strong>（12.7 vs 12.5）！窗口化后 $t_\text{draft}$ 从 12.7ms 掉到 4.3ms，验收两段纹丝不动——省的全是草稿。</p> <p>而 $t_\text{draft}$ 里面最大的单个 kernel 是什么？<code class="language-plaintext highlighter-rouge">generate_draft_decode_kv_indices</code>——<strong>生成草稿 attention 的 KV 索引</strong>这一个 kernel 就吃掉 3588μs/iter（Qwen-35B），窗口化后 3.0μs，<strong>三个数量级</strong>。这个 kernel 干的事就是”枚举全部历史位置给草稿头做 attention”——1M 上下文下枚举本身就是灾难。</p> <hr/> <h2 id="3-解法只给草稿开窗口">3. 解法：只给草稿开窗口</h2> <h3 id="31-改动本体小到一个-pr">3.1 改动本体：小到一个 PR</h3> <p>Wind-MTP 的实现是<strong>把草稿头的 paged block table 截短为 sink 块 + 最近 W 块</strong>：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>原生草稿 attention 的 block table:
[1, 2, 3, ..., 999993, 999994, ..., 1000000]   ← 全部 1M 块

窗口化后:
[1, 2, ..., 64, 995969, 995970, ..., 1000000]   ← 64 个 sink + 最近 W=4032 块
</code></pre></div></div> <p>W=4032、n_sink=64，即 StreamingLLM 式的”attention sink + 滑动窗口”——4K 窗口只占 1M 的 <strong>0.4%</strong>，等于把 99% 的草稿 KV 从读取路径上删掉。<strong>target 的验收 attention 一个字不动。</strong></p> <p>配套的第二个改动是 <strong>ring buffer</strong>：既然草稿只读 n_sink+W 个位置，那 draft KV pool 就没必要按 1M 分配——物理上只开 <code class="language-plaintext highlighter-rouge">n_sink + W + d</code> 个槽位循环使用（树宽 k 的 drafter 就是 n_sink + W + d·k），逻辑位置映射到 <code class="language-plaintext highlighter-rouge">ring_base(r) + pos mod (n_sink+W+d)</code>。RoPE 已经烘焙在存好的 key 里，块在缩减后的表里排第几不影响得分，所以这个替换对数值完全透明，而且 CUDA-graph 安全。<strong>省下来的 7.7–11.1% 显存直接变成多塞一个 1M 请求的能力</strong>：原生 MTP 在 B=6 就 OOM 的批次，窗口化能接着跑。</p> <p>唯一的实现代价：需要跑 Triton 后端做 ring 索引重映射（原生跑 FlashInfer）。后文有消融证明赢的是窗口不是后端。</p> <h3 id="32-为什么严格无损">3.2 为什么严格无损</h3> <p>这点必须单独讲清楚，因为它是所有”改草稿”方案里最容易被质疑的地方。</p> <p>投机解码的数学保证：<strong>输出分布永远由 target 的验收决定</strong>——每个草稿 token 都要过 target 的 rejection sampling，被接受的 token 是 target 自己采样的。草稿头只影响”提议什么”，<strong>不影响”接受什么”</strong>。</p> <p>所以改草稿头的 attention 不存在分布层面的损失。实测 greedy 输出的差异全部落在验收侧本来就有的 bf16 batched-verify 噪声带内。<strong>这是”改草稿”类方案的红利：可以放心对草稿做各种激进的近似，只要验收不动，正确性就有底。</strong>（对比那些改验收侧做 lossy verification 的工作，性质完全不同。）</p> <p><img src="/assets/img/posts/20260816-windmtp/figure1_p6.png" alt="草稿 attention 窗口化前后"/></p> <h3 id="33-为什么-acceptance-几乎不掉三层证据">3.3 为什么 acceptance 几乎不掉：三层证据</h3> <p>真正的问题是：把草稿的视野从 1M 砍到 4K，$\tau$ 为什么不崩？这是论文做得最扎实的部分。</p> <p><strong>第一层：剂量反应曲线（dose-response）。</strong> 窗口从 1K/2K/4K/8K/原生 扫过去，Qwen-35B（d=7，多 needle 检索任务）的接受长度 AL = 3.79 / 4.74 / 4.74 / 4.16 / <strong>4.49</strong>，TPOT = 4.89 / 3.85 / 3.85 / 4.50 / 6.04 ms。</p> <p>注意两个反直觉的点：<strong>4K 窗口的 AL（4.74）比原生全量（4.49）还高一点</strong>，同时 TPOT 从 6.04 降到 3.85。窗口不是”伤质量换速度”，而是存在一个<strong>联合最优</strong>——太小（1K）不够用，太大（8K/原生）除了变慢没有额外收益。</p> <p><strong>第二层：逐位置条件接受率 $\alpha_j$。</strong> 论文用 SGLang 的逐请求接受直方图 $H[n]$（每轮恰好接受 n 个草稿 token 的步数）定义：</p> \[\alpha_j = \Pr[\text{accept } j \mid \text{reached } j] = \frac{\sum_{n \ge j} H[n]}{\sum_{n \ge j-1} H[n]}, \qquad \text{AL} = 1 + \sum_{j \ge 1} \prod_{k \le j} \alpha_k\] <p>窗口化后的 $\alpha_j$ 曲线在所有位置上都落在原生曲线的 95% Wilson 置信区间内；$\alpha_1$（只依赖局部预测的第一个位置）完全不变，差距只出现在需要远距离信息的深位置。<strong>伤不到要害。</strong></p> <p><strong>第三层：运行中 A/B 探针——草稿到底改没改主意？</strong> 直接在推理过程中对比窗口化前后草稿的 top-1 提议：<strong>86%–94% 的步骤完全不变</strong>。翻转发生在哪里？统计 flip margin（翻转处 top-1 与 top-2 的概率差）：Qwen-35B 和 Nemotron 的翻转集中在 margin≈0.49/0.54 的<strong>接近平局</strong>位置（全平均 margin 是 5.16/3.02）——本来就要掷硬币的地方，换不换窗口无所谓。唯一的例外是 Qwen-122B：它的翻转发生在 margin 6.80 的<strong>高置信</strong>位置——这个 drafter 是真的在用远距离上下文（它的 AL 也确实从 5.58 掉到 4.73），但 TPOT 依然净赚。</p> <p><strong>机制解释</strong>收尾：三个模型的草稿头都架在递归混合底座上（Qwen 系是 GDN-MoE，Nemotron 是 Mamba2-hybrid）。<strong>远距离信息本来就由递归状态携带</strong>，attention 只需要管局部。一个漂亮的证据：窗口内 attention 质量占比，Qwen-35B 0.748、Qwen-122B 0.805，而 Nemotron（NoPE）只有 <strong>0.160</strong>——它的草稿 attention 本来就不怎么依赖位置，top-1 提议照样保住 86%，因为远处信息在 Mamba2 状态里。</p> <p>一句话总结这个洞察：<strong>这类混合架构的草稿头根本不怎么用长程 attention，却一直在为它付全量的钱。</strong></p> <hr/> <h2 id="4-端到端效果">4. 端到端效果</h2> <h3 id="41-主结果1md7b1ruler-niah_multiquery">4.1 主结果（1M、d=7、B=1、RULER niah_multiquery）</h3> <table> <thead> <tr> <th>模型</th> <th>vs 原生 MTP</th> <th>vs dense</th> <th>AL 变化</th> <th>TPOT</th> </tr> </thead> <tbody> <tr> <td>Qwen3.6-35B</td> <td><strong>+53%</strong></td> <td>2.55×</td> <td>4.49→4.74</td> <td>5.94→3.89 ms</td> </tr> <tr> <td>Qwen3.5-122B</td> <td>+11%</td> <td>1.75×</td> <td>5.57→4.74</td> <td>6.09→5.46 ms</td> </tr> <tr> <td>Nemotron-3-120B</td> <td>+23%</td> <td>1.58×</td> <td>3.75→3.61</td> <td>8.77→7.15 ms</td> </tr> </tbody> </table> <p><img src="/assets/img/posts/20260816-windmtp/table2_p10.png" alt="端到端加速"/></p> <p>122B 那行值得单独看：AL 明明掉了（5.57→4.74），TPOT 还是降了 11%。<strong>成本端的赢大于接受端的输</strong>——这也是为什么不能只盯着 acceptance length 评价投机解码系统。</p> <p>更值得注意的是任务难度维度的规律：窗口优势随难度<strong>单调放大</strong>——简单多值任务 +4%（AL 5.9）、中等 VT 任务 +17%（AL 4.2）、困难 CWE 任务 <strong>+38%</strong>（AL 2.8）。原因很简单：低接受率场景原生 MTP 本来就在亏本，窗口把它的成本砍下来，等于把负收益救成正收益。<strong>越难的任务越该开窗口</strong>，这个方向性结论对线上配置很有指导意义。</p> <h3 id="42-窗口楔子随上下文增长">4.2 “窗口楔子”随上下文增长</h3> <p>Figure 3 定义了一个干净的指标——matched-acceptance 成本比（用 $\text{AL} \cdot \text{TPOT} = T_\text{iter}$ 消掉接受率，只留纯成本侧的 step_nat/step_win − 1），从 261K 扫到 1M：</p> <ul> <li>Qwen-35B：+27% → <strong>+43%</strong></li> <li>Qwen-122B：+24% → +30%</li> <li>Nemotron：+17% → +26%</li> <li>每步绝对节省接近翻倍（Qwen-35B：3.4→7.9ms）</li> </ul> <p><img src="/assets/img/posts/20260816-windmtp/figure3_p10.png" alt="窗口优势随上下文增长"/></p> <p><strong>上下文越长，省得越多</strong>——和行业”上下文越来越长”的方向完全同向。这也是为什么我说这个技巧会快速变成默认配置。</p> <h3 id="43-批量张量并行与硬件复现">4.3 批量、张量并行与硬件复现</h3> <ul> <li><strong>单卡 B200 批量扫描</strong>（Figure 4）：6 个面板里 5 个窗口化 Pareto 占优。Qwen-35B 单用户 218 tok/s（dense 的 2.2×），B=5 峰值吞吐 473 tok/s/GPU（dense 2.1×、原生 1.5×）。唯一例外是 Nemotron + FWE（接受率最低的任务）高并发下 dense 赢峰值吞吐——接受率太低时投机本身就不划算，这锅不该窗口背；</li> <li><strong>2×B200 TP2</strong>（Figure 5）：全批量、全模型 Pareto 占优，单用户比 dense 快 1.4–2.5×，吞吐天花板比原生再抬 1.15–1.25×；</li> <li><strong>H100 + FP8 复现</strong>（Table 4，公开 checkpoint RedHatAI/Qwen3.6-35B-A3B-FP8）：dense 12.26ms → 原生 7.48ms（1.64×）→ 窗口化 <strong>5.05ms（2.43×）</strong>，AL 还从 4.34 升到 4.53——换卡、换精度结论不变；</li> <li><strong>fp8-KV 扫描</strong>：窗口化全 cell 赢（+14–46%），但 KV 减半后 Qwen 的百分比收益按比例缩水（1M 处 +46%→+22%）——符合”税 ∝ KV 字节数”的预测，Nemotron 反而几乎不缩水。</li> </ul> <h3 id="44-后端消融赢的是窗口不是-triton">4.4 后端消融：赢的是窗口，不是 Triton</h3> <p>一个必须做的诚实消融：原生 MTP 跑 FlashInfer（快后端）8.13ms；原生切到 Triton 50.59ms（<strong>6 倍 handicap</strong>）；窗口化跑 Triton 5.37ms。<strong>窗口化在最慢的后端上仍然赢过原生在最快后端上的成绩</strong>——收益 attribution 清清楚楚。</p> <h3 id="45-best-depth-对比">4.5 best-depth 对比</h3> <p>每个方法都扫 $d \in {3,5,7,9}$ 取各自最优深度：窗口化对原生的优势区间 <strong>1.22–1.58×</strong>（Qwen-35B Code-QA 7.95→5.03ms）。没有任何一格的最优深度是 d=9——更深更慢；且 d=9 在 1M 下对 122B/Nemotron 直接 OOM 不可行，窗口化 + ring buffer 反而跑得动。</p> <hr/> <h2 id="5-我的-take">5. 我的 Take</h2> <p><strong>一，这篇是”草稿成本经济学”的当头一棒。</strong> 投机解码这个领域卷了两年”草稿怎么打”（打得更准、打得更长、打得更便宜），这篇提醒大家：<strong>草稿的执行成本本身也在被上下文长度通货膨胀侵蚀</strong>。$T_\text{draft}$ 在公式里从来不是常数——它是 $O(S)$ 的函数，只是短上下文时代把它当常数没出事而已。1M 时代，”草稿头自带全量 attention”就是个 bug 级的设计遗留。</p> <p><strong>二，它顺手示范了”改草稿”方案的正确姿势。</strong> 验收侧不动 = 无损保证白拿；然后剂量反应、逐位置 $\alpha_j$、A/B 探针三层证据把”为什么敢砍”论证到 kernel 级——尤其是”递归底座已经携带远距离信息，attention 只管局部”这个机制发现，对后续所有混合架构的草稿设计都有参考价值。</p> <p><strong>三，工程上这是个”没理由不合并”的 PR。</strong> 零训练、无新 kernel、严格无损、显存还倒赚 8–11%。我预期”草稿侧窗口化/稀疏化”会像 target 侧的窗口化一样迅速变成推理栈默认配置。和 target 侧的稀疏化（我在 <a href="https://marsggbo.github.io/blog/2026/20260814-nsa/">NSA</a> 和 DeepSeek V3.2 DSA 的解读里聊过）对照着看会发现一个对称的图景：<strong>target 的 attention 在变稀，drafter 的 attention 也在变稀，两头一起把 decode 从 memory-bound 里往外拽。</strong></p> <hr/> <blockquote> <p>如果这篇文章涉及的投机解码、长上下文推理优化你想系统深入，可以看看我之前出版的<a href="https://item.jd.com/14945889.html">《动手学AutoML：从 NAS 到大语言模型优化实战》</a>，书里有专门一章讲 LLM 推理效率与 KV Cache 优化，本文讨论的 decode 阶段 memory-bound 问题正是那一章的主线。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="投机解码"/><category term="长上下文"/><category term="推理优化"/><category term="论文解读"/><summary type="html"><![CDATA[arXiv’26 | Wind-MTP：百万上下文里，MTP 草稿头才是那个隐形税]]></summary></entry><entry><title type="html">arXiv’26 | PCTree：DSpark 的草稿链换成树，接受长度白赚 30%</title><link href="https://marsggbo.github.io/blog/2026/20260815-chainstotrees/" rel="alternate" type="text/html" title="arXiv’26 | PCTree：DSpark 的草稿链换成树，接受长度白赚 30%"/><published>2026-08-15T00:00:00+00:00</published><updated>2026-08-15T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260815-chainstotrees</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260815-chainstotrees/"><![CDATA[<h1 id="arxiv26--pctreedspark-的草稿链换成树接受长度白赚-30">arXiv’26 | PCTree：DSpark 的草稿链换成树，接受长度白赚 30%</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2608.02123">From Chains to Trees: Parent-Conditioned Drafting for Semi-Autoregressive Speculative Decoding</a></p> </blockquote> <hr/> <h2 id="1-前言先把背景交代清楚">1. 前言：先把背景交代清楚</h2> <p>你有没有想过这样一个问题：<strong>同一个草稿模型，同一份计算预算，能不能在”草稿阶段”就把多点可能性留住？</strong></p> <p>接着<a href="https://marsggbo.github.io/blog/2026/20260814-dflash/">上篇 DFlash</a> 的坑说。投机解码的两条路线我们都很熟了：</p> <ul> <li><strong>AR drafter（如 EAGLE-3）</strong>：逐 token 串行打草稿，质量高，但打草稿的时间随草稿长度线性涨；</li> <li><strong>Parallel drafter（如 DFlash）</strong>：一个 block 一次并行算完，草稿极快，但 block 内部每个 token 都是”盲猜”——<strong>suffix decay</strong>，越靠后越不靠谱。</li> </ul> <p><a href="https://marsggbo.github.io/blog/2026/20260814-dspark/">DSpark</a>（DeepSeek 线上跑的半自回归方案，一句话回顾：一次并行 backbone 前向 + 轻量 Markov head 逐位修正，再用置信度调度决定每轮验收多长）走的是第三条路：<strong>半自回归</strong>——一次并行 backbone 前向给出 block 的 base logits，再用一个轻量 Markov head 逐位置串行修正，把”看着当前位置之前已知的 token”这个条件依赖补进去。思路很优雅，但 DSpark 有个没解决的结构问题：<strong>它打出来的是一根链。</strong></p> <p>链就链吧，有什么不好？问题在于投机解码的验收规则很残酷：<strong>一旦链上某个位置被 target 拒绝，后面所有草稿全作废</strong>（rejection sampling）。所以链越长，前面的一个错越贵；而 DSpark 又不得不把块设得长一点才划算——这就是 DSpark 里”草稿块越大 = 越划算的假象”背后埋的雷。</p> <p>今天这篇 PCTree（arXiv 2608.02123）做的事一句话讲完：<strong>一个人可以靠修正头支持多条互不冲突的草稿路径，把它从”一条链”扩展成”一棵树”，且不重新训练、不多跑一次 backbone 前向。</strong> 在 Qwen3-4B/8B/14B、九个 benchmark 上，相对同配置 DSpark 的加速提升 <strong>3.1%–29.5%</strong>；GSM8K 上平均每轮接受的 token 从 9.41 涨到 11.16，端到端 AR 加速 6.14×→6.60×。听起来增益不大？注意：<strong>这是把别人已有的草稿白捡出 30% 的边际收益，不花一分训练钱。</strong></p> <p>先说清楚它的前提——”Markov head 可以按父节点分别打分”这件事，到底为什么成立。</p> <hr/> <h2 id="2-dspark-的限制条件结构被当成了单链在用">2. DSpark 的限制：条件结构被当成了单链在用</h2> <p>DSpark 的打草稿流程：anchor token $x_t$ 输入并行 backbone，一步前向算出 block 的 base logits $L_0 \ldots L_{B-1}$；然后 Markov head 在第 $d$ 步做：</p> \[p_d(y \mid y_{&lt;d}) = \mathrm{softmax}(L_d + W \cdot \mathrm{emb}(y_{d-1}))\] <p>也就是<strong>只用前一个已选 token 修正当前位置的分布</strong>。这个公式看着平平无奇，但它有一个被 DSpark 浪费掉的特性：<strong>每个位置的分布，其实可以针对每一个具体父 token 分别计算</strong>——因为修正项只依赖父 token 的 embedding，而 embedding 是共享的、已经算好的。</p> <p>DSpark 的用法是：第 $d$ 位只取前一个位置选中的那个 token 当父节点，一条路走到黑。而 PCTree 的观察是：<strong>父节点集合本质上是一个 batch</strong>，把 $L_d$ 广播开，<code class="language-plaintext highlighter-rouge">Z_d = L_d + W^T P_{d-1}</code> 一次矩阵算就能拿到”当前层所有父节点各自对应的子分布”。于是草稿从一条链，顺理成章变成一棵可以分叉的树——代价只是个 batch matmul。</p> <p>想清楚这点的确挺妙：<strong>能力的载体（修正头）没变，变的是我们用它的方式。</strong></p> <hr/> <h2 id="3-pctree预算约束下的树构建">3. PCTree：预算约束下的树构建</h2> <h3 id="31-三个要点">3.1 三个要点</h3> <ul> <li><strong>父条件打分（Parent-Conditioned Expansion）</strong>：每个父节点 $p$ 对它自己的候选子节点计算条件概率 $\pi_d(\cdot \mid p)$，路径得分就是各步条件概率连乘——注意是<strong>真条件</strong>，不是把 block 的平行草稿缝起来那种伪条件；</li> <li><strong>预算约束</strong>：每层每个父只取 top-k 子节点（k=4 默认），层内再做全局 top-k 剪枝（frontier 不超过 k）；所有 B 层扩展完后，把所有节点按”路径得分降序、深度升序、稳定 ID”排序，截取 top-N（N=32 默认）作为最终验证树；</li> <li><strong>前缀闭合</strong>：由于子节点得分必低于父节点，top-N 集合天然前缀闭合（prefix-closed）——被选的节点要么是根路径上的，要么是根路径的祖先节点都在；于是可以生成一个<strong>祖先限制的 tree attention mask</strong>，target model 一次 forward 验证整棵树。</li> </ul> <p>由于 k=1 时（每父节点只留一个孩子、每层不剪枝）PCTree 数学上完全退化回 DSpark——这篇论文的所有实验都以”k=1 即 DSpark”为对照点，非常干净。流程图如下：左边是 DSpark 的单链草稿，右边是 PCT 的树草稿与一次验收：</p> <p><img src="/assets/img/posts/20260815-chainstotrees/figure1_p2.png" alt="PCTree 与 DSpark 的草稿结构对比"/></p> <hr/> <h2 id="4-效果主要是白赚和不亏">4. 效果：主要是”白赚”和”不亏”</h2> <h3 id="41-主结果">4.1 主结果</h3> <p>论文表格的核心其实是<strong>“同配置 DSpark vs PCT 的差”</strong>。抽取三个代表性任务（Qwen3-4B, k=4, N=32）：</p> <table> <thead> <tr> <th>任务</th> <th>B</th> <th>DSpark τ</th> <th>PCTree τ</th> <th>τ 增幅</th> <th>AR 加速 DSpark→PCTree</th> </tr> </thead> <tbody> <tr> <td>GSM8K</td> <td>7</td> <td>6.31</td> <td>7.24</td> <td>+14.8%</td> <td>4.24x→4.50x（+6.1%）</td> </tr> <tr> <td>GSM8K</td> <td>16</td> <td>9.41</td> <td>11.16</td> <td>+18.6%</td> <td>6.14x→6.60x（+7.5%）</td> </tr> <tr> <td>HumanEval</td> <td>7</td> <td>5.60</td> <td>6.78</td> <td>+21.2%</td> <td>3.74x→4.27x（+14.3%）</td> </tr> <tr> <td>MT-Bench</td> <td>7</td> <td>3.82</td> <td>5.07</td> <td>+32.5%</td> <td>2.48x→3.20x（+29.3%）</td> </tr> <tr> <td>MT-Bench</td> <td>16</td> <td>4.28</td> <td>5.50</td> <td>+28.8%</td> <td>2.82x→3.25x（+15.1%）</td> </tr> </tbody> </table> <blockquote> <p>所有数据来自论文 Table 3，速度为 AR baseline 的相对提速；τ 为平均接受长度。</p> </blockquote> <p>不难看出规律：<strong>对话类任务（MT-Bench）增益最大</strong>——因为聊天场景分布更散、分支的空间更大；数学任务相对集中，但也稳定 + 6~7%。9 个 benchmark × 3 个模型规模的完整表在论文 Table 8。</p> <p><img src="/assets/img/posts/20260815-chainstotrees/table3_p8.png" alt="主结果表：与 DSpark 的配对增益"/></p> <h3 id="42-机制隔离真条件-vs-假联动">4.2 机制隔离：真条件 vs 假联动</h3> <p>论文 Table 4 做了个很干净的实验：把”父条件”换成 block 共享分布（shared-Markov tree，即所有分支共享同一套位置分布，只是把 top-k 铺开）。结果非常有层次：</p> <ul> <li>裸 DSpark 链：τ=9.410；</li> <li><strong>共享分布树</strong>：τ=10.225——有点涨，但这是”把 marginal 铺开”的自然结果；</li> <li><strong>PCTree 真父条件树</strong>：τ=11.156（再 +9.1%），每 sample 的 round 数从 24.7 降到 22.6；</li> <li>对照组 DFlash+DDTree（并行分布硬铺树）：τ=7.485，垫底。</li> </ul> <p>注意共享树和真条件树在树形状（都一样稠密）下的差距，说明<strong>分支能不能生钱，取决于条件质量</strong>：父依赖的真条件 &gt; 共享 marginal 树 &gt; 并行分布硬铺树。这也是对”给 DFlash 加个树就有用”这类朴素想法的直接泼冷水。</p> <h3 id="43-结构消融">4.3 结构消融</h3> <ul> <li><strong>k（每父节点的分支数）</strong>：k=1（=DSpark）9.41 → k=2：10.85 → k=4：11.16 → k=8：11.14。即 k=4 就是甜点，再大没有新东西——节点预算 N 是硬约束；</li> <li><strong>节点预算 N</strong>：τ 随 N 单调涨，但端到端加速在 N=32（B=7）或 N=64（B=16）附近达峰——树太大，验证成本反超（论文 Figure 5）；</li> <li><strong>成本分解</strong>（Table 6，GSM8K B=16）：PCT 每 round 的 Markov+tree 时间从 DSpark 的 1.09ms 升到 5.24ms，但 round 数从 26.8 降到 22.6，净赚 7.5% 的 AR 加速——<strong>这是拿增量验证开销换 τ 的正常生意</strong>。</li> </ul> <h3 id="44-一图流生存曲线">4.4 一图流：生存曲线</h3> <p>论文 Figure 4 画了”草稿深度上的存活概率”：DSpark 到深度 8 就掉到 57.1%，PCT 不但整体抬高，尾部（深度 12）还能留下 46.8%——树把”链断即血亏”变成了”此路不通，另辟歧路”。</p> <p><img src="/assets/img/posts/20260815-chainstotrees/figure4_p10.png" alt="草稿深度上的存活率对比"/></p> <hr/> <h2 id="5-我的-take">5. 我的 Take</h2> <p>这篇和 <a href="https://marsggbo.github.io/blog/2026/20260815-dartree/">DARTree</a>（给 diffusion drafter 装 AR 树，我前天刚写过解读）是同一个大潮里的两块拼图：<strong>投机解码的 DRF 之争告一段落，改动都收敛到”草稿内部怎么结构”了。</strong> DARTree 给 diffusion drafter 装 AR 树，PCTree 给 DSpark 这个”半 AR 链”扩展成真条件树——两者都主打”零训练、不碰 backbone”，把提速算盘打在<strong>草稿阶段的确定性</strong>上。</p> <p>个人觉得 PCTree 最值得学的是它的”破题角度”：Markov head 的父条件能力是 DSpark 自己就已经具备的，只是被单链用法封印了。<strong>推理优化的高杠杆工作，往往不是引入新能力，而是把已有能力的表达方式解开恢复</strong>——这和当年的 MTP、EAGLE 用最后一层 hidden state 是同一个道理。</p> <p>要说局限，也是诚实的：增益集中在分支丰富的对话/代码任务，最利的数学题（GSM8K 类）稳定但幅度有限；树验证在长上下文服务端会吃掉部分收益（N 达峰后回落）。但作为”上生产之前最后一块拼图”，DSpark + PCTree 这个组合已经足够体面了。</p> <hr/> <blockquote> <p>如果这篇文章涉及的投机解码、LLM 推理优化你想系统深入，可以看看我之前推出的<a href="https://item.jd.com/14945889.html">《动手学AutoML：从 NAS 到大语言模型优化实战》</a>，书里有专门一章讲 LLM 推理效率和 KV Cache 优化，和本文的加速思路是同一套工程背景。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="投机解码"/><category term="推理优化"/><category term="论文解读"/><summary type="html"><![CDATA[arXiv’26 | PCTree：DSpark 的草稿链换成树，接受长度白赚 30%]]></summary></entry><entry><title type="html">arXiv’26 | DARTree：给 Diffusion Drafter 填一棵 AR 树，投机解码飙到 9.7×</title><link href="https://marsggbo.github.io/blog/2026/20260815-dartree/" rel="alternate" type="text/html" title="arXiv’26 | DARTree：给 Diffusion Drafter 填一棵 AR 树，投机解码飙到 9.7×"/><published>2026-08-15T00:00:00+00:00</published><updated>2026-08-15T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260815-dartree</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260815-dartree/"><![CDATA[<h1 id="arxiv26--dartree给-diffusion-drafter-填一棵-ar-树投机解码飙到-97">arXiv’26 | DARTree：给 Diffusion Drafter 填一棵 AR 树，投机解码飙到 9.7×</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2608.13524">DARTree: Speculative Diffusion Decoding with Autoregressive Draft Trees</a></p> </blockquote> <hr/> <h2 id="1-前言先把背景交代清楚">1. 前言：先把背景交代清楚</h2> <p>你有没有想过这样一个问题：<strong>投机解码的草稿模型，到底什么样的才算”好”？</strong></p> <p>一星期前我们刚聊过 DFlash——它把 drafter 从自回归（AR）换成了 Diffusion，一次 forward 直接把一整块 token 并行草拟出来，”打草稿”从串行苦力变成了一次性操作，给投机解码刷出了 6× 以上的加速。</p> <p>但 DFlash 这种”并行整块猜”的草稿有个天生缺陷：<strong>block 内部的 token 之间没有因果依赖</strong>。每个位置都是独立取边际分布里的最高概率——单独看每个位置都没毛病，合起来却可能拼出 “no course” 这种鬼话。我在 DSpark 的解读里管这叫 <strong>suffix decay</strong>：草稿越长，越靠后的 token 越不靠谱。</p> <p>接下来这个领域几乎所有的修修补补，本质都是同一件事：<strong>并行草拟块里缺的”序列条件依赖”，怎么低成本地补回来。</strong></p> <p>今天这篇 DARTree（arXiv 2608.13524，8 月 13 号刚挂出来）给的答案很妙：<strong>你手上已经有一个能把单条链修好的 AR correction head，为什么不让它去填一棵树？</strong> 草稿做完后，树里保留多条互不冲突的候选路径，一次性验收；接受率直接起飞——在 GSM8K 上每轮平均收下 <strong>12.97 个 token</strong>，比 DFlash 多 98.6%，比目前最强的单链方案 Domino 多 27.9%，无损加速最高 <strong>9.73×</strong>。</p> <p>听起来像又一次”把链换成树”的常规操作？往下看它的动机——树的好处大家都懂，<strong>之前的难点在于没人付得起树的代价</strong>。</p> <hr/> <h2 id="2-为什么之前没人做串行-correction-head--树--打草稿比生成还慢">2. 为什么之前没人做：串行 correction head + 树 = 打草稿比生成还慢</h2> <p>先说 DFlash 系 diffusion drafter 的完整流程：一次前向给出 block 的公共表征和 base logits，但想在 block 内部补上序列依赖，还得靠一个轻量的 <strong>AR correction head</strong>——每出一个草稿 token，就用 head 修正一次，这步本身是串行的。</p> <p>这块 head 很轻，单次推理很便宜。问题出在你想在<strong>多棵候选路径</strong>上使用它的时刻：</p> <ul> <li><strong>Domino</strong> 用 correction head 修一条链——链上有了依赖，但一条链赌错一个位置就全盘皆输；</li> <li><strong>DDTree</strong> 先在 block 上用 heap 构建草稿树，但<strong>每从 heap 弹出一个节点，都要跟着跑一次 correction head 推理</strong>——这是逐节点串行计算，树里的节点一多，延迟当场爆炸。</li> </ul> <p>论文里拆了一组数字：在 Qwen3-4B 上，逐节点串行的 correction 版本打一个草稿 round 要 <strong>62.7ms</strong>，而 DARTree 只要 <strong>28.5ms</strong>，前者换来的 τ 还更少。也就是说：<strong>“树”本身是好东西，”串行地建树”才是毒瘤</strong>——这组数据看着小，其实就是这整条线的拦路虎。</p> <p>如下两图，动机拆解（左）和 DARTree 的总体流程（右）：一次 block 并行草拟 + 条件化树扩展 + 延迟裁剪（deferred pruning），最后 target 一次性验收：</p> <p><img src="/assets/img/posts/20260815-dartree/figure3_p4.png" alt="DARTree 整体框架"/></p> <p><img src="/assets/img/posts/20260815-dartree/figure1_p1.png" alt="延迟拆解：树构建方式决定了每轮开销"/></p> <hr/> <h2 id="3-dartree把-ar-correction-head-从链迁移到树">3. DARTree：把 AR Correction Head 从链迁移到树</h2> <h3 id="31-基本流程">3.1 基本流程</h3> <p>输入一个 anchor token，DARTree 沿用 DFlash 的一次 block-parallel 前向，得到共享的表征 ${H_d}$ 和 base logits ${L_d^{base}}$。关键在第二步：</p> <ul> <li><strong>候选生成</strong>：先解码 base logits，每个位置只保留 top-K 候选（默认 K=64），这等于预剪掉了所有”不可能成为树节点”的对象；</li> <li><strong>深度逐层扩展</strong>：第 $d$ 层时，把上一层存活的所有父节点<strong>批量</strong>喂给 correction head（一次矩阵运算算完），输出子节点打分；</li> <li><strong>打分函数</strong>：</li> </ul> \[s_\beta(u_{1:d}) = \sum_{i=1}^{d} \log q_i(u_i) + \beta \cdot d\] <p>其中 $q_i(u_i)$ 是修正后的位置条件概率，$\beta \le 0$ 是深度惩罚——<strong>惩罚树”长而浅”比”长而宽”更值</strong>，因为验收只看最长合法前缀，深度才是直接收益；</p> <ul> <li><strong>逐层裁剪</strong>：每层只留 top-W 个节点（修剪版 W=12 / 固定版 W=4），把宽度压住；</li> <li><strong>延迟裁剪（deferred pruning）</strong>：先扩一棵比最终更大的 supertree，再一次性全局选出 top-B 个节点（B=64）——论文 Lemma 1 保证在 $\beta \le 0$ 下，这个全局 top-B 等价于 heap 的 best-first 弹出，<strong>树重建一遍 + 一次裁剪</strong>，杜绝逐节点串行。</li> </ul> <h3 id="32-为什么这能跑起来树是一次前向的副产品">3.2 为什么这能跑起来：树是一次前向的副产品</h3> <p>验收一步全是标准操作：所有 draft token 一次性喂给 target model，用 tree attention mask 并行验证所有路径，取最长合法前缀。注意关键点——<strong>DARTree 全程只多了一次块并行前向 + 一次批量树修正前向</strong>，correction head 的逐节点串行开销正式取消，变成整层批处理 + 一次前向。</p> <hr/> <h2 id="4-效果acceptance-length-和-speedup-全面开火">4. 效果：Acceptance Length 和 Speedup 全面开火</h2> <h3 id="41-主结果">4.1 主结果</h3> <p>Table 1 对比 DARTree 与四条基线（AR baseline、DFlash、Domino、DDTree），在 Qwen3-4B/8B、温度 0 和 1、七个 benchmark 上全部打平。简单摘录 Qwen3-4B temp=0 的关键行：</p> <ul> <li><strong>GSM8K：τ=12.97，speedup 9.73×</strong>，比 DFlash 多收 98.6% 的 token，比 Domino 多 27.9%；</li> <li>平均 τ=9.81，平均 speedup 6.99×；固定版（等宽 W=4）也有 9.36 τ / 6.55×；</li> <li>温度 1 也不崩：平均 8.48 τ / 5.93×；</li> <li>同比树方案 DDTree：τ 最多多 28.9%，speedup 最多多 22.7%；对比单链的 Domino：τ 最多多 46.8%，speedup 最多多 40.1%。</li> </ul> <p><img src="/assets/img/posts/20260815-dartree/table1_p6.png" alt="主结果：Qwen3-4B/8B 上的 τ 与 speedup"/></p> <h3 id="42-位置接受率两头的好都占了">4.2 位置接受率：两头的好都占了</h3> <p>如下图，按草稿位置画接受率：DDTree 赢在早期（前几个位置接受率高），Domino 赢在后期（靠链式修正兜底），而 <strong>DARTree 把两头的长处都占了</strong>——GSM8K 上近半的 round 几乎整块被接受：</p> <p><img src="/assets/img/posts/20260815-dartree/figure5_p7_12.png" alt="位置条件接受率对比"/></p> <h3 id="43-机制隔离与-dspark-兼容">4.3 机制隔离与 DSpark 兼容</h3> <p>论文 Table 2 做了一次非常诚实的机制隔离：把 DARTree 的”延迟裁剪”换回朴素的”heap 弹出→修正”组合，GSM8K 上每 round 延迟从 <strong>28.5ms 飙回 62.7ms</strong>，speedup 从 9.7× 掉到 4.38×——<strong>真正的瓶颈是树的构建方式，不是树本身</strong>。</p> <p>更实用的是 Table 3：把 correction head 换成 <strong>DSpark 开源的 Markov head</strong>，不重新训练、不多跑 backbone forward，DARTree 直接在 GSM8K/HumanEval/MT-Bench 上再赚 <strong>14.6%–40.6% 的 τ</strong>，speedup 最多再涨 <strong>34.3%</strong>。这说明 DARTree 不是 DFlash 专属补丁，而是给”任意 diffusion drafter 装树”的通用配件。</p> <h3 id="44-并发服务">4.4 并发服务</h3> <p>单请求快不算数，服务端收益才是真金白银。在 RTX 6000 Ada + SGLang 上，并发度 C=2 时 DARTree 跑出 <strong>6.19×</strong> 提速（同期 DFlash 4.10×、Domino 5.79×）；并发到 C=4 时，配合自适应预算（B 从 64 收到 32、宽度从 12 收到 3）仍保持 5.66×，吞吐从 311.9 TPS 拉到 1766.2 TPS。另外深度惩罚 β 取 −0.2~−0.1、top-K 从 32 加到 512 都没什么影响——超参很钝，不挑环境。</p> <hr/> <h2 id="5-我的-take">5. 我的 Take</h2> <p>投机解码卷到现在，方向已经不再是”换个更聪明的 drafter”，而是<strong>在同一份草稿上把筹码铺开</strong>：DSpark 用置信度决定草稿长块，DARTree 用一棵树铺满块内所有可能路径。这篇最难得的地方是<strong>在 DSpark 的 Markov head 上零训练直接平放</strong>（Table 3），等于给整个 diffusion drafter 家族留了一条现成的加速通道。</p> <p>要不要担心树的验收 overhead？树大了 target 一次 forward 全验完，代价主要是 tree attention mask——超长上下文里会略有感，但论文在预算被压到 16 个节点的极端情况下依然是正收益。更关键的是，<strong>这篇顺手证明了”并行草拟块的软肋，可以用一棵树来治”</strong>——方向感比具体数字更重要。</p> <hr/> <blockquote> <p>如果这篇文章涉及的投机解码、LLM 推理优化你想系统深入，可以看看我之前推出的<a href="https://item.jd.com/14945889.html">《动手学AutoML：从 NAS 到大语言模型优化实战》</a>，书里有专门一章讲 LLM 推理效率和 KV Cache 优化，和本文的推理加速是同一套工程背景。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="投机解码"/><category term="Diffusion"/><category term="推理优化"/><category term="论文解读"/><summary type="html"><![CDATA[arXiv’26 | DARTree：给 Diffusion Drafter 填一棵 AR 树，投机解码飙到 9.7×]]></summary></entry><entry><title type="html">DeepSeek V3.2 | DSA：Sparse Attention 这次真正写进了生产模型</title><link href="https://marsggbo.github.io/blog/2026/20260815-deepseek-v32/" rel="alternate" type="text/html" title="DeepSeek V3.2 | DSA：Sparse Attention 这次真正写进了生产模型"/><published>2026-08-15T00:00:00+00:00</published><updated>2026-08-15T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260815-deepseek-v32</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260815-deepseek-v32/"><![CDATA[<h1 id="deepseek-v32--dsasparse-attention-这次真正写进了生产模型">DeepSeek V3.2 | DSA：Sparse Attention 这次真正写进了生产模型</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2512.02556">DeepSeek-V3.2: Pushing the Frontier of Open Large Language Models</a></p> </blockquote> <hr/> <h2 id="1-前言先把背景交代清楚">1. 前言：先把背景交代清楚</h2> <p>你有没有想过一个问题：<strong>NSA 那类”trainable sparse attention”论文里说的”可以直接从零预训练”，和”全球头部模型真的换掉 attention”之间，隔了多少步？</strong></p> <p>上个月聊 NSA 时我给的判断是：它证明了两件事，一是稀疏模式可以训出来不掉点，二是硬件对齐的 kernel 能让稀疏真正快起来。但”论文里能训”离”生产 API 默认开稀疏”还有一道天堑——你动的是 attention，是全模型每个 token 都要过的最核心计算，改坏了性能用户直接跑路，而且训练成本是几千万美元的级别，没人敢赌。</p> <p><strong>DeepSeek-V3.2 的回答是：赌了，而且赌赢了。</strong> 这是目前第一个（至少是目前最公开的）把<strong>训练时的稀疏 attention（DSA，DeepSeek Sparse Attention）</strong>写进主力开源大模型的技术报告：</p> <ul> <li>相比 V3.1-Terminus，构架的<strong>唯一改动</strong>就是 attention——其他全部不动；</li> <li>推理时核心 attention 复杂度从 $O(L^2)$ 降到 $O(L \cdot k)$，$k=2048$；</li> <li>训练分两步走：先用 21 亿 token 把”检索器”（lightning indexer）对齐到完整 attention 的分布，再在 943.7B token 上训练带稀疏 attention 的全体模型；</li> <li>最终效果不只是”没变慢”——常规版对标 GPT-5，高算力版 DeepSeek-V3.2-Speciale 超越 GPT-5、追平 Gemini-3.0-Pro，还拿了 IMO 2025 和 IOI 2025 的双金。</li> </ul> <p>数据说话之前，先把最重要的架构问题讲清楚：<strong>稀疏 selection 这件事，DSA 具体是怎么做的。</strong></p> <hr/> <h2 id="2-dsalightning-indexer--动态-top-k-选择">2. DSA：Lightning Indexer + 动态 top-k 选择</h2> <p>DSA 的总体结构一句话：<strong>在标准 attention 之外，加一个轻量”打分器”（indexer），按它给的分数为每个 query 动态挑 top-k 个历史 KV，然后只在这 k 个上做 attention。</strong></p> <p>拆分结构：</p> <ul> <li><strong>Lightning Indexer</strong>：极轻的多头 ReLU 网络，对每个 (query token, 前序 token) 算一个标量 index score，全部浮点在 FP8 上算、head 数很少——作者明确说它的计算量远小于 V3.1-Terminus 里 MLA 本身；</li> <li><strong>Fine-grained 选择</strong>：每个 query 独立取出 top-k（k=2048）个 index score 最高的 KV 条目，其他一律不看。这是<strong>动态的、query 相关的选择</strong>，不是固定图样（窗口/全局 anchor 那种）——这是和 2020 年那批 sparse attention 最根本的分野；</li> <li><strong>在 MLA 上的实例化</strong>：MLA 的 MQA 模式下，所有 query head 共享同一份 latent key-value，所以 indexer 只需要对”这份共享 latent”打分一次，检索出来的 top-k 天然被所有 head 复用，省查询次数也省带宽。</li> </ul> <p>一句话总结 DSA 的精髓：<strong>把”稀疏模式”从手工设计的静态结构，降级成一个可以用梯度训练的小网络。</strong> 模式本身是学出来的，所以不需要你手写 “which tokens are important” 的启发式。</p> <p>架构长这样（论文 Figure 2，DSA 挂在 MLA 上，右下角就是 indexer/top-k 检索那条小径）：</p> <p><img src="/assets/img/posts/20260815-deepseek-v32/figure2_p4.png" alt="DSA 注意力架构"/></p> <p>我喜欢把 DSA 和 NSA 放在一起看构图：NSA 强调”hardware-aligned”（kernel 一起设计），DSA 强调”indexer + top-k 检索直接对到矩阵乘/检索硬件”——学术上是用两套说法，工程上其实是同一个目标：<strong>让稀疏 pattern 真的变成 GPU 上的高效内存访问，而不是理论 FLOPs</strong>。</p> <hr/> <h2 id="3-怎么训两阶段把稀疏当成训练目标本身">3. 怎么训：两阶段，把”稀疏”当成训练目标本身</h2> <p>DSA 的训练协议是我认为全篇最有工程价值的地方，它回答了直觉性疑问——”排名网络自己还没训好，稀疏 attention 怎么敢回传梯度？”答案是：<strong>两个阶段解耦。</strong></p> <p><strong>阶段一：indexer 对齐（dense warm-up）</strong>。冻结整个主模型，只训 lightning indexer，用 KL 散度让它的打分分布收敛到完整 attention 的分布。量级：1000 步 × 每步 16 条 128K 序列 ≈ <strong>2.1B token</strong>，学习率 1e-3。此时模型主体完全不稀疏。</p> <p><strong>阶段二：稀疏训练（sparse training）</strong>。解冻所有参数，主模型在”每 query 只看 indexer 挑出的 top-2048 个 KV”的条件下继续预训练，LM loss 只算主模型那路。量级：15,000 步 × 每步 480 条 128K 序列 ≈ <strong>943.7B token</strong>，学习率 7.3e-6。</p> <p>两个细节值得划重点：</p> <ul> <li><strong>任何 step 都不存在”满了再稀疏”</strong>：模型从头到尾在稀疏约束下训练，训练分布 ≈ 推理分布——这正是 NSA 那篇说的”natively trainable”的题中之义，也是把 H2O/StreamingLLM 那种”训练完再硬剪”甩开一个时代的原因；</li> <li><strong>没有 load-balancing 辅助 loss</strong>（就我读到的架构描述）：准确性全部压在 indexer 对齐的质量上，训练能收敛，说明”用一个小模型预测 attention 冲量”这条路的假设是成立的。这也从侧面说明：<strong>sparse attention 的难点根本不在 kernel，在 selection 的训练信号怎么设计</strong>——和 MoE router 的训练是如出一辙的题。</li> </ul> <hr/> <h2 id="4-效率账从-ol2-到-olk看图说明一切">4. 效率账：从 $O(L^2)$ 到 $O(Lk)$，看图说明一切</h2> <p>论文 Figure 3 直接放了推理成本随序列位置的曲线，对比 V3.1-Terminus（紫，dense MLA）和 V3.2（青，DSA）。两条线的差别不只是”矮了一点”，而是<strong>斜率</strong>的差别：dense 侧每 token 成本随 context 长度线性涨（要走全量 KV），稀疏侧涨的梯度明显平——长上下文吃掉的墙钟时间,一小段话就能看出来。</p> <p><img src="/assets/img/posts/20260815-deepseek-v32/figure3_p6.png" alt="推理成本对比：dense MLA vs DSA"/></p> <p>数字上：核心 attention 从 $O(L^2)$ 变成 $O(L \cdot 2048)$。也就是说，64K context 时 32K 个 KV 只需要检索 2K 个，剩下 30K 的 HBM 读取直接免单——<strong>decode 阶段每个 token 把过去的 KV 全从 HBM 读一遍的伤,终于不再随上下文线性爆炸</strong>。配合 MLA 的 latent 共享，检索返回的还是压缩后的 k 语义向量，带宽又省一层。</p> <p>顺带一提：论文把 RL 的后训练 compute 提到了 预训练成本的 <strong>10%+</strong>，这也是 V3.2 相比 V3.1 提升的主因之一——但这不是本文重点，略过。</p> <hr/> <h2 id="5-效果与-takeaway">5. 效果与 Takeaway</h2> <h3 id="51-性能没掉还更好了">5.1 性能没掉，还更好了</h3> <p>V3.2 在主流 benchmark 上追平 GPT-5；高算力变体 Speciale 超过 GPT-5、逼近 Gemini-3.0-Pro，IMO 2025 与 IOI 2025 双金。更值得玩味的对比是：<strong>DeepSeek-V3.2-Exp 上一代（九月的 sparse 实验版）ChatbotArena Elo 只比 V3.1-Terminus 高一点，AA-LCR 在 reasoning 模式下 +4 分</strong>——也就是说稀疏不是”保住了性能”，而是”顺手更好了”，因为整个 pipeline（更长 RL、更高效上下文）都因此受益。</p> <h3 id="52-个人-take私货时间">5.2 个人 take（私货时间）</h3> <p>每次写这种生产型 tech report，最值钱的不是它又刷了几个榜，而是它暴露的”工程常识”：</p> <ol> <li><strong>“只改 attention”是 blockbuster 级的产品决策</strong>。MoE、MLA、FP8，DeepSeek 都验证过了；最后一块要动刀的地方就是 attention，而 sparse 是被证明了”能训”之后才敢动的。NSA → DSA 这条线把”稀疏”从有论文争议推进到”旗舰模型默认配置”。</li> <li><strong>训练信号决定一切</strong>：indexer 用 KL 对齐、稀疏训练期 top-k 是 checkpoint 友好的，整个架构没有任何”硬凑”的辅助 loss。技术报告里每一处 math 都极俭省，这是很典型”系统出活”的气质。</li> <li><strong>对想跟风做 sparse 的工作室</strong>：直接抄 DSA 不一定行——它吃 128K 长序列预训练预算（943.7B token 稀疏训练 + 更早的 dense warm-up）。小预算团队想复刻，得默认”训练顺序 + 对齐方式”整套搬走，少哪一块都会掉点。</li> </ol> <p>一句话：<strong>NSA 证明稀疏 attention “可以做对”，DSA 证明”生产线上也确实用起了”。</strong> 这中间差的那一大截，才是这一两年 LLM 推理论文最好写的地方。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 Long Context、稀疏注意力、LLM 推理效率优化你想系统深入，可以看看我之前推出的<a href="https://item.jd.com/14945889.html">《动手学AutoML：从 NAS 到大语言模型优化实战》</a>，书里有专门章节系统讲 LLM 推理效率、KV Cache 与注意力机制优化，和本文分析的 DSA 是同一张蓝图上的内容。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="稀疏注意力"/><category term="长上下文"/><category term="推理优化"/><category term="DeepSeek"/><category term="论文解读"/><summary type="html"><![CDATA[DeepSeek V3.2 | DSA：Sparse Attention 这次真正写进了生产模型]]></summary></entry><entry><title type="html">ICML’26 | DFlash：用 Diffusion 模型干掉 AR Drafter，投机解码提速 6×</title><link href="https://marsggbo.github.io/blog/2026/20260814-dflash/" rel="alternate" type="text/html" title="ICML’26 | DFlash：用 Diffusion 模型干掉 AR Drafter，投机解码提速 6×"/><published>2026-08-14T00:00:00+00:00</published><updated>2026-08-14T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260814-dflash</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260814-dflash/"><![CDATA[<h1 id="icml26--dflash用-diffusion-模型干掉-ar-drafter投机解码提速-6">ICML’26 | DFlash：用 Diffusion 模型干掉 AR Drafter，投机解码提速 6×</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2602.06036">DFlash: Block Diffusion for Flash Speculative Decoding</a></p> </blockquote> <hr/> <h2 id="1-前言先把背景交代清楚">1. 前言：先把背景交代清楚</h2> <p>你有没有想过这样一个问题：<strong>为什么大模型”想”问题很快，但”说”出来却一个 token 一个 token 往外蹦，急死人？</strong></p> <p>原因是硬约束：LLM 的生成是自回归的（autoregressive decoding），每生成一个 token，都要把整张网络从头到尾 forward 一遍，相当于把模型的所有参数从显存里读一遍，才换得来下一个字。而这段”搬参数”的时间由内存带宽决定，显卡算力再猛也帮不上忙——decode 阶段是 memory-bound，不是 compute-bound。</p> <p>所以大家都在想怎么绕过”逐字生成”，最主流的招数就是<strong>投机解码（Speculative Decoding）</strong>，说人话：</p> <p><strong>让小模型（draft model）先打草稿，一口气猜出后面好几个 token；大模型再”批量验收”这串草稿，验收通过的直接收下。</strong></p> <p>为什么这套能成立？因为大模型的”验收”本质上就是一次并行 forward（prefill），它本来就支持同时处理多个位置，多验几个 token 的边际成本很低；再配合 rejection sampling，<strong>输出分布和主模型逐字自回归采样完全一致</strong>——提速是免费的，模型不会因为打草稿而”变笨”。</p> <p>接下来是算账时刻。每一轮投机解码的开销 = 打草稿的时间 $T_\text{draft}$ + 大模型验收的时间 $T_\text{verify}$，换来的收益是平均每轮被接受的 token 数 $\tau$（acceptance length）：</p> \[L = \frac{T_\text{draft} + T_\text{verify}}{\tau}\] <p>想让它变快，无非两条路：把 $\tau$ 做大（草稿质量更准），或者把分子两项压小（打草稿和验收都更快）。这个领域过去的大部分力气都花在前者——比如 EAGLE-3 想尽办法让草稿更聪明、草稿树更大；至于分子里的 $T_\text{draft}$，几乎被当成一个”不得不付的常数”。</p> <p><strong>可 $T_\text{draft}$ 恰恰是 AR drafter 的死穴：它打草稿也是逐 token 串行的。</strong>主流 AR drafter 一次只生成 1 个 token，想草拟 16 个就得跑 16 次串行 forward；序列越长，drafting 延迟越高，慢慢地就把验收那边省下来的时间全部吃回去了。这个约束也反过来卡死了草稿长度——不是不想多草拟，是草拟不起。</p> <p>DFlash（ICML 2026）给出了一个相当”反主流”的解法：<strong>与其在 AR 头上修修补补，不如把这个串行引擎整个换掉——用 Diffusion 模型当 drafter。</strong>一个 block 的 token 一次 forward pass 全部并行生成，drafting latency 和生成长度彻底解耦。代价当然也有：block 内部 token 之间没有自回归依赖，草稿质量会打折——DFlash 的补救是从目标模型注入 context feature，把丢掉的”上下文感知”补回来。</p> <p>最终在 Qwen3 系列上，对比 AR baseline <strong>加速超过 6×</strong>，比树大小为 60 的 EAGLE-3 还快 <strong>2.5×</strong>。下面先看标杆 EAGLE-3 到底慢在哪，再拆 DFlash 的两个关键设计。</p> <hr/> <h2 id="2-背景标杆-eagle-3-是怎么做的">2. 背景：标杆 EAGLE-3 是怎么做的</h2> <p>DFlash 的主要对比对象是 EAGLE-3，先把它的原理交代一下。</p> <p>EAGLE-3 是目前最强的 AR drafter 之一，核心思路是：用目标模型最后一层的 hidden state 作为 draft 模型的额外输入（拼在 token embedding 上），让 draft 模型知道目标模型当前的”理解状态”，从而提升草稿质量。</p> <p>推理时用<strong>树状草稿</strong>（draft tree）：不只草拟一条序列，而是并行草拟多条候选路径，目标模型用 tree attention 一次验证所有路径，接受最长的合法前缀。树大小越大（如 EAGLE-3 (60) 表示树节点数 60），接受的 token 越多，但验证 overhead 也越大。</p> <p><strong>EAGLE-3 的核心矛盾</strong>：AR 生成是串行的，每生成一个草稿 token 就要跑一次 draft forward。树大小 60 意味着要跑 60 次 forward pass，即使每次只有 1 层，延迟也不可忽视。草拟 16 个 token 需要 16 次串行 forward，延迟随草拟长度线性增长。</p> <hr/> <h2 id="3-为什么换成-diffusion">3. 为什么换成 Diffusion</h2> <p>这个决定看着激进，其实逻辑很顺。关键在两种 drafter 的生成过程有本质区别：</p> <p><strong>AR drafter</strong> 的生成过程：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>t=0: 已知 [x₁, x₂, x₃]（prompt），预测 x₄
t=1: 已知 [x₁, x₂, x₃, x₄]，预测 x₅
t=2: 已知 [x₁, ..., x₅]，预测 x₆
...（草拟 γ 个 token 需要 γ 次 forward）
</code></pre></div></div> <p><strong>Diffusion drafter</strong> 的生成过程：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>初始化：把 γ 个待生成位置全部设为 &lt;mask&gt;
       输入 = [x₁, x₂, x₃, &lt;mask&gt;, &lt;mask&gt;, &lt;mask&gt;]
一次 forward：同时预测 x₄, x₅, x₆ 的 logits
             γ 个 token，1 次 forward
</code></pre></div></div> <p>Diffusion 路线的代价：block 内部 token 之间没有自回归依赖——预测 x₆ 时不知道 x₄、x₅ 是什么，只能靠全局 context 盲猜。但 DFlash 用 target context feature 注入来弥补这个缺陷（后面展开）。</p> <p>收益也直接：草稿延迟和草拟长度解耦。如下图实测，5 层 DFlash 草拟 16 个 token 只需 ~5ms，而 1 层 EAGLE-3 草拟 16 个 token 需要 ~26ms：</p> <p><img src="/assets/img/posts/20260814-dflash/figure3_p3.png" alt="EAGLE-3 vs DFlash drafting 延迟对比"/></p> <p>这个延迟优势意味着 DFlash 可以在不超 latency budget 的前提下，用更深的 drafter（更高质量），这就是它能超过 AR drafter 的根本原因——不是预测得更准，而是把省下的时间换成了更贵的模型。</p> <hr/> <h2 id="4-核心设计从目标模型注入-context-feature">4. 核心设计：从目标模型注入 Context Feature</h2> <p>换掉引擎只是第一步，diffusion 单靠自己能做到的 acceptance length 很有限——论文里专门验证过，不加任何 conditioning 的 5 层 diffusion drafter，speedup 只有 2-3×。</p> <p>原因直接：drafter 必须在不知道目标模型内部状态的情况下凭空预测未来 token，等于从零开始猜。</p> <p>DFlash 的解法：<strong>在目标模型 prefill 的时候，把它的中间层 hidden state 抽出来，注入到 drafter 的每一层 KV cache 里</strong>。</p> <h3 id="41-提取-target-context-feature">4.1 提取 Target Context Feature</h3> <p>目标模型跑 prefill 时，从若干中间层（均匀采样 5 层，从第 2 层到倒数第 3 层）抽取 hidden state，拼接后过一个轻量投影层：</p> \[H_t = \text{RMSNorm}\left(W_c[H^{(l_1)}; \ldots; H^{(l_5)}]\right)\] <p>$W_c \in \mathbb{R}^{D_{d} \times 5D_t}$，把多层信息压缩到 draft 模型的 hidden dimension，得到 context feature $H_t$。</p> <p>用 PyTorch 写大概是这样：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">ContextFeatureExtractor</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">target_hidden</span><span class="p">,</span> <span class="n">draft_hidden</span><span class="p">,</span> <span class="n">num_layers</span><span class="o">=</span><span class="mi">5</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">proj</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">target_hidden</span> <span class="o">*</span> <span class="n">num_layers</span><span class="p">,</span> <span class="n">draft_hidden</span><span class="p">,</span> <span class="n">bias</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">norm</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">RMSNorm</span><span class="p">(</span><span class="n">draft_hidden</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">hidden_states</span><span class="p">:</span> <span class="nb">list</span><span class="p">[</span><span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">])</span> <span class="o">-&gt;</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">:</span>
        <span class="c1"># hidden_states: list[N, target_hidden], len=num_layers
</span>        <span class="n">fused</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">(</span><span class="n">hidden_states</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>   <span class="c1"># [N, target_hidden * num_layers]
</span>        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">norm</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">proj</span><span class="p">(</span><span class="n">fused</span><span class="p">))</span>          <span class="c1"># [N, draft_hidden]
</span></code></pre></div></div> <p>这个 context feature 只需要计算一次，之后所有 drafting iteration 都复用。</p> <h3 id="42-kv-injection注入到每一层">4.2 KV Injection：注入到每一层</h3> <p>拿到 $H_t$ 后，不是把它和 draft token embedding 拼在一起作为输入（EAGLE-3 的做法），而是直接注入到 drafter 每一层的 KV 里：</p> <p>\(Q_i = W^Q_i H_d\) \(K_i = [W^K_i H_t;\ W^K_i H_d]_{\text{seq}}\) \(V_i = [W^V_i H_t;\ W^V_i H_d]_{\text{seq}}\)</p> <p>$H_d$ 是 draft token 的 hidden state，$[\ ;\ ]_{\text{seq}}$ 表示在 sequence 维度拼接。</p> <p>代码大概长这样：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">DraftAttentionLayer</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">h_draft</span><span class="p">,</span> <span class="n">h_ctx</span><span class="p">,</span> <span class="n">attn_mask</span><span class="o">=</span><span class="bp">None</span><span class="p">):</span>
        <span class="c1"># h_draft: [batch, γ, d]  ← γ 个 mask token 的 hidden states
</span>        <span class="c1"># h_ctx:   [batch, N, d]  ← target context feature（整个序列）
</span>        <span class="n">Q</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">wq</span><span class="p">(</span><span class="n">h_draft</span><span class="p">)</span>                      <span class="c1"># [batch, γ, d]
</span>
        <span class="n">K_ctx</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">wk</span><span class="p">(</span><span class="n">h_ctx</span><span class="p">)</span>                    <span class="c1"># [batch, N, d]
</span>        <span class="n">K_draft</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">wk</span><span class="p">(</span><span class="n">h_draft</span><span class="p">)</span>                <span class="c1"># [batch, γ, d]
</span>        <span class="n">K</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">([</span><span class="n">K_ctx</span><span class="p">,</span> <span class="n">K_draft</span><span class="p">],</span> <span class="n">dim</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>    <span class="c1"># [batch, N+γ, d]
</span>
        <span class="n">V_ctx</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">wv</span><span class="p">(</span><span class="n">h_ctx</span><span class="p">)</span>
        <span class="n">V_draft</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">wv</span><span class="p">(</span><span class="n">h_draft</span><span class="p">)</span>
        <span class="n">V</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">([</span><span class="n">V_ctx</span><span class="p">,</span> <span class="n">V_draft</span><span class="p">],</span> <span class="n">dim</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>    <span class="c1"># [batch, N+γ, d]
</span>
        <span class="c1"># bidirectional attention：block 内部 mask token 互相可见
</span>        <span class="n">out</span> <span class="o">=</span> <span class="n">F</span><span class="p">.</span><span class="nf">scaled_dot_product_attention</span><span class="p">(</span><span class="n">Q</span><span class="p">,</span> <span class="n">K</span><span class="p">,</span> <span class="n">V</span><span class="p">,</span> <span class="n">attn_mask</span><span class="o">=</span><span class="n">attn_mask</span><span class="p">)</span>
        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">wo</span><span class="p">(</span><span class="n">out</span><span class="p">)</span>                       <span class="c1"># [batch, γ, d]
</span></code></pre></div></div> <p>关键点：<strong>draft token 只产生 Query，context feature 只作为额外的 KV entry</strong>——不参与 Q、output projection、FFN，只相当于给每层 attention 提供了更丰富的 key-value 池。</p> <p>好处是无论 drafter 有多少层，每一层都能直接 attend 到目标模型的 context，信息不会随深度衰减。整体推理设计如下图（可以看 context feature 如何注入 KV cache）：</p> <p><img src="/assets/img/posts/20260814-dflash/figure2_full.png" alt="DFlash 推理设计：context feature 注入 KV cache"/></p> <h3 id="43-一次-forward-完成-block-预测">4.3 一次 Forward 完成 Block 预测</h3> <p>有了 context feature 之后，整个 drafting 过程是：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">draft_block</span><span class="p">(</span><span class="n">anchor_token</span><span class="p">,</span> <span class="n">ctx_feature</span><span class="p">,</span> <span class="n">drafter</span><span class="p">,</span> <span class="n">block_size</span><span class="o">=</span><span class="mi">16</span><span class="p">):</span>
    <span class="c1"># anchor_token: 目标模型上一轮验证后产生的最后一个 token
</span>    <span class="c1"># ctx_feature:  从目标模型 prefill 时提取的 context feature
</span>
    <span class="c1"># 初始化：anchor + γ 个 mask token
</span>    <span class="n">mask_token_id</span> <span class="o">=</span> <span class="n">tokenizer</span><span class="p">.</span><span class="n">mask_token_id</span>
    <span class="n">draft_input</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">([</span>
        <span class="n">anchor_token</span><span class="p">.</span><span class="nf">unsqueeze</span><span class="p">(</span><span class="mi">1</span><span class="p">),</span>                              <span class="c1"># [B, 1]
</span>        <span class="n">torch</span><span class="p">.</span><span class="nf">full</span><span class="p">((</span><span class="n">B</span><span class="p">,</span> <span class="n">block_size</span><span class="p">),</span> <span class="n">mask_token_id</span><span class="p">)</span>              <span class="c1"># [B, γ]
</span>    <span class="p">],</span> <span class="n">dim</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>                                                   <span class="c1"># [B, 1+γ]
</span>
    <span class="c1"># embed → drafter forward（bidirectional attention within block）
</span>    <span class="n">h</span> <span class="o">=</span> <span class="n">drafter</span><span class="p">.</span><span class="nf">embed</span><span class="p">(</span><span class="n">draft_input</span><span class="p">)</span>                             <span class="c1"># [B, 1+γ, d]
</span>    <span class="k">for</span> <span class="n">layer</span> <span class="ow">in</span> <span class="n">drafter</span><span class="p">.</span><span class="n">layers</span><span class="p">:</span>
        <span class="n">h</span> <span class="o">=</span> <span class="nf">layer</span><span class="p">(</span><span class="n">h</span><span class="p">,</span> <span class="n">ctx_feature</span><span class="p">)</span>                              <span class="c1"># KV injection
</span>
    <span class="c1"># 只取 mask 位置的 logits
</span>    <span class="n">logits</span> <span class="o">=</span> <span class="n">drafter</span><span class="p">.</span><span class="nf">lm_head</span><span class="p">(</span><span class="n">h</span><span class="p">[:,</span> <span class="mi">1</span><span class="p">:,</span> <span class="p">:])</span>                     <span class="c1"># [B, γ, V]
</span>
    <span class="c1"># 并行采样（或 argmax）
</span>    <span class="n">draft_tokens</span> <span class="o">=</span> <span class="n">logits</span><span class="p">.</span><span class="nf">argmax</span><span class="p">(</span><span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>                       <span class="c1"># [B, γ]
</span>    <span class="k">return</span> <span class="n">draft_tokens</span>
</code></pre></div></div> <p>整个 block 的 γ 个 token 一次 forward 搞定，无论 γ 多大延迟都不变——这就是分子 $T_\text{draft}$ 被压扁的全过程。</p> <hr/> <h2 id="5-训练block-masking">5. 训练：Block Masking</h2> <p>推理时 drafter 拿到的是全部 mask 的 block，训练时也要对齐这个行为。</p> <p>如下图，DFlash 的训练时 attention mask 设计：<img src="/assets/img/posts/20260814-dflash/figure4_p5.png" alt="DFlash 训练时的 attention mask 设计"/></p> <p>具体做法：</p> <ol> <li>从 response 序列里随机采样 anchor token 位置</li> <li>以 anchor 为起点，把接下来 <code class="language-plaintext highlighter-rouge">block_size - 1</code> 个 token 随机 mask 掉一部分（绿色）</li> <li>drafter 的任务：给定 anchor + 剩余可见 token + context feature，预测所有 mask 位置</li> </ol> <p>训练时多个 block 拼在一起，用 block 内部双向 attention + 跨 block 单向 causal mask（Flex Attention 实现），可以在一次 forward + backward 里处理整个长序列，效率很高。</p> <p>损失函数带位置衰减 $w_k = e^{-(k-1)/\gamma}$，让靠前的位置有更高的权重——因为前几个 token 被接受的概率更高，对整体 acceptance length 的贡献更大：</p> \[\mathcal{L} = -\sum_{k=1}^{\gamma} w_k \log p_d(x_k^*)\] <hr/> <h2 id="6-实验结果">6. 实验结果</h2> <h3 id="51-主结果">5.1 主结果</h3> <p>如下表，在 Qwen3-4B/8B（thinking 模式关闭）上，DFlash 对比 AR baseline 平均加速 <strong>4.9×（greedy）</strong>，在 Math 任务上 acceptance length $\tau$ 最高达 <strong>7.87</strong>：</p> <p><img src="/assets/img/posts/20260814-dflash/table1_full.png" alt="主结果表：DFlash vs EAGLE-3 speedup 对比"/></p> <p>注意一个细节：EAGLE-3 树大小 60（验证 overhead 很大），DFlash 只用 block size 16，speedup 反而更高。</p> <h3 id="52-为什么-diffusion-能反超-ar">5.2 为什么 Diffusion 能反超 AR</h3> <p>这里有个反直觉的地方：diffusion 没有 block 内依赖，质量理论上比 AR 差，为什么 acceptance length 反而更高？</p> <p>根本原因是 <strong>latency budget 的不同分配</strong>：</p> <ul> <li>EAGLE-3 草拟 16 个 token 需要 16 次 forward 串行，延迟 ~26ms，只能用浅层 drafter（再深会更慢）</li> <li>DFlash 草拟 16 个 token 只需 1 次 forward，延迟 ~5ms，省下的预算可以养一个 5 层更深的 drafter</li> </ul> <p>同样的 latency budget，DFlash 能用更大、更深的模型，综合质量反而更高。<strong>AR 输给 Diffusion，输的不是准确性，是预算被串行延迟吃光了</strong>——这是典型的”用延迟换质量”的 Pareto 优化。</p> <hr/> <h2 id="7-个人-take">7. 个人 Take</h2> <p>DFlash 抓到了一个真实的系统瓶颈：之前大家都在研究怎么让 drafter 预测得更准（更大的 tree、更好的 AR 头），但核心矛盾是<strong>串行 drafting 的延迟随草拟长度线性增长</strong>，从根本上限制了你能草拟多少 token、敢用多深的 drafter。</p> <p>换成 diffusion 之后，drafting latency 和草拟长度解耦，这个约束就没了。可以放心堆更深的 drafter、更大的 block，在相同延迟下拿到更高的 acceptance length。</p> <p>当然，diffusion drafter 的训练比 AR 复杂——block masking 设计、context conditioning、位置加权 loss，每个细节都有讲究，开箱复现的成本不低；目前结果也主要在 Qwen3 系列上，跨架构的泛化性还需要更多验证。</p> <p>但”用并行替换串行”这个方向是对的，DFlash 算是把这条路走通的第一个工作。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 LLM 推理效率优化你想系统深入，可以看看我之前出版的<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大模型优化实战》</a>，书里有专章讲 LLM 推理效率和参数高效微调，和本文的工程背景有直接关联。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="投机解码"/><category term="Diffusion"/><category term="推理优化"/><category term="论文解读"/><summary type="html"><![CDATA[ICML’26 | DFlash：用 Diffusion 模型干掉 AR Drafter，投机解码提速 6×]]></summary></entry><entry><title type="html">DSpark：DeepSeek 线上跑的投机解码，半自回归 + 置信度调度怎么做到的</title><link href="https://marsggbo.github.io/blog/2026/20260814-dspark/" rel="alternate" type="text/html" title="DSpark：DeepSeek 线上跑的投机解码，半自回归 + 置信度调度怎么做到的"/><published>2026-08-14T00:00:00+00:00</published><updated>2026-08-14T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260814-dspark</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260814-dspark/"><![CDATA[<h1 id="dsparkdeepseek-线上跑的投机解码半自回归--置信度调度怎么做到的">DSpark：DeepSeek 线上跑的投机解码，半自回归 + 置信度调度怎么做到的</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2607.05147">DSpark: Confidence-Scheduled Speculative Decoding with Semi-Autoregressive Generation</a></p> </blockquote> <hr/> <h2 id="1-前言先把背景交代清楚">1. 前言：先把背景交代清楚</h2> <p>你有没有想过这样一个问题：<strong>为什么大模型”算”得那么快，但生成一段话，却要一个字一个字往外蹦？</strong></p> <p>先说结论。因为 LLM 的生成是自回归的（autoregressive decoding）——每生成一个 token，都要把完整的网络从头到尾 forward 一遍，相当于把模型的所有参数从显存里读一遍。而 decode 阶段 token 之间严格串行，上一步没算完，下一步没法开始，所以显卡算力再强，也只能干等”读一遍参数”的时间：<strong>搬一遍权重，换回来一个字。</strong></p> <p>这个瓶颈在系统领域有个专门的说法：decode 阶段是 <strong>memory-bound</strong>（内存带宽受限），不是 compute-bound。翻译成大白话就是——<strong>生成一个 token 需要的计算量其实很小，真正卡你的是把全部参数读出来这件事</strong>；而显存带宽这些年涨得比算力慢多了，所以模型越做越大、算力越来越猛，生成速度却始终被拉在后头。</p> <p>在生产环境里，这个瓶颈会直接变成账单：decode 的吞吐 = 实打实的运营成本。DeepSeek 的 API 每天服务着海量请求，多快、多久、能扛多少并发，都是钱。所以”怎么让解码变快”是 LLM 推理优化里最核心的课题之一，而其中最主流的一路解法，就是今天要聊的<strong>投机解码（Speculative Decoding）</strong>。</p> <p>投机解码说人话就是：</p> <p><strong>让小模型（draft model）先打草稿，一口气把后面好几个 token 猜出来；再让大模型批量”验收”这串草稿，验收通过的 token 直接全部收下。</strong></p> <p>乍一听像个作弊技巧——草稿不靠谱怎么办？其实它的数学是完备的：大模型在验收时会并行为每个草稿 token 算概率，从第一个被拒绝的位置开始，把后面的草稿全部作废，并用大模型自己采样的结果替换。这套 rejection sampling 机制保证了<strong>输出分布和大模型逐字自回归生成时完全一致</strong>——也就是说，投机解码提速是”免费”的，模型不会因为打草稿而”变笨”。</p> <p>概念说清楚了，但落地没那么简单：每一轮解码的花销其实是两笔账，打草稿的时间 $T_\text{draft}$，加上大模型验收的时间 $T_\text{verify}$，换来的收益是这轮平均被接受的 token 数，记为 $\tau$（acceptance length）。所以平均每生成一个 token 的延迟是：</p> \[L = \frac{T_\text{draft} + T_\text{verify}}{\tau}\] <p>这个公式看着不起眼，但它把整个投机解码家族的设计空间都压缩到一行了：想让 $L$ 变小，只有两条路——<strong>要么压分子</strong>（打草稿更快、验收更便宜），<strong>要么拉分母</strong>（每轮多验收几个 token）。顺着这两条路，现有工作分成两派，各有各的坑：</p> <ul> <li><strong>AR drafter（逐 token 串行打草稿，如 EAGLE-3）</strong>：打草稿同样是一个个 token 生成的，质量高，当然 $\tau$ 很漂亮；但打草稿的时间随草稿长度 $\gamma$ 线性上涨，草稿越长越慢，所以只能被迫用小的 $\gamma$ 来维持动量。</li> <li><strong>Parallel drafter（并行整块打草稿，如 DFlash）</strong>：一次 forward 直接并行算出整个 block 的草稿，$T_\text{draft}$ 基本不随 $\gamma$ 涨，延迟极低；但代价是 block 里靠后的每个 token 都是”盲猜”——它们根本没见过 block 里前面的 token 长什么样，只能按全局 context 猜，所以越靠后的位置 acceptance rate 掉得越狠。</li> </ul> <p>这个”block 越靠后越不靠谱”的现象，论文里有名字，叫 <strong>suffix decay</strong>，理解它等于理解这篇论文的一半。</p> <p>现在主角登场。<strong>DSpark 是 DeepSeek 部署在自己 V4 生产系统上的实际方案</strong>，它想做的事一句话讲完：<strong>把 parallel drafter 的低延迟保留下来，但往 block 内部补上序列依赖，把 suffix decay 治好；同时不是所有草稿 token 都值得拿去大模型验收——多加一个置信度打分器，动态决定每轮验证几个。</strong>这两点看着都不难，难的是它在 DeepSeek V4 的线上系统真实跑过、用生产流量测过。下面拆开来讲。</p> <hr/> <h2 id="2-suffix-decay并行-drafter-的病根在哪">2. Suffix Decay：并行 Drafter 的病根在哪</h2> <p>先把病根看清楚。假设要草拟一个 block <code class="language-plaintext highlighter-rouge">[x₁, x₂, x₃, x₄]</code>，并行 drafter 一次 forward 同时预测这 4 个位置：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>x₁ = argmax P(x₁ | context)      ← 只依赖完整 context，还好
x₂ = argmax P(x₂ | context)      ← 不知道 x₁ 是什么！
x₃ = argmax P(x₃ | context)      ← 不知道 x₁, x₂ 是什么！
x₄ = argmax P(x₄ | context)      ← 完全盲猜 x₁ ~ x₃
</code></pre></div></div> <p>问题就在这：每个位置只知道全局 context，不知道 block 内部”前面已经说出来的是什么”。举例：假设模型在这个位置有两种等大概率的续写，<code class="language-plaintext highlighter-rouge">"of course"</code> 和 <code class="language-plaintext highlighter-rouge">"no problem"</code> 的语境。并行 drafter 对每个位置独立取最高概率，完全可能拼出 <code class="language-plaintext highlighter-rouge">"of problem"</code> 或者 <code class="language-plaintext highlighter-rouge">"no course"</code>——每个位置单独看都对，组合起来语句不通，大模型一验收就被否。</p> <p>下图对 DFlash（蓝色，纯并行）和 DSpark（绿色，加序列依赖）在不同草稿位置上的<strong>条件接受率</strong>做了对比：</p> <p><img src="/assets/img/posts/20260814-dspark/figure2_p12.png" alt="位置条件 acceptance rate 对比"/></p> <p>蓝色曲线非常典型：第 1 个位置还行，第 4、5 个位置明显往下掉。而绿色的 DSpark 曲线平坦甚至略升——原因就是它给 block 内部做了序列依赖，让每个位置”看”到了前面已经采样的 token。</p> <h2 id="3-semi-autoregressive-架构并行打草稿--顺序轻修正">3. Semi-Autoregressive 架构：并行打草稿 + 顺序轻修正</h2> <p>DSpark 的 drafter 是两阶段结构，整个架构和 decode 循环如下：</p> <p><img src="/assets/img/posts/20260814-dspark/figure1_p5.png" alt="DSpark 整体架构和 decoding 循环"/></p> <h3 id="31-parallel-stage并行骨干先跑一遍">3.1 Parallel Stage：并行骨干先跑一遍</h3> <p>第一阶段直接采用类似 DFlash 的 parallel backbone，一次 forward 并行算出整个 block 的 hidden states 和 base logits：</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>输入：anchor token x₀ + γ 个 &lt;mask&gt;
一次 forward → hidden_states: h₁, ..., hᵧ
              base logits:   U₁, ..., Uᵧ   [γ, V]
</code></pre></div></div> <p>为什么要个 anchor？因为上一轮大模型验收完之后，必然留下一个已确认的 token（图中把它记为 D，作为下一轮草稿的锚点）——它给 drafter 提供”从我这往后可能是”的全局位置信息。这一步 $T_\text{draft}$ 极低，不随草稿长度增长。</p> <h3 id="32-sequential-stage轻量序列头修正">3.2 Sequential Stage：轻量序列头修正</h3> <p>第二阶段是一个很轻的模块，从左到右逐给每个位置的 logits 加一个”转移修正” $B_k$，让第 $k$ 个 token 的预测真正依赖前面已经采样出来的 token：</p> \[p_k(v \mid x_0, x_{&lt;k}) = \frac{\exp(U_k(v) + B_k(x_0, x_{&lt;k}, v))}{\sum_{u \in \mathcal{V}} \exp(U_k(u) + B_k(x_0, x_{&lt;k}, u))}\] <p>$U_k$是并行阶段给的 base logit（已经感知全局 context），$B_k$ 是对 block 内依赖的修正，两种实现：</p> <p><strong>Markov Head（默认）</strong>——最简，只依赖前一个 token，低秩分解 $B = W_1 W_2$：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">MarkovHead</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">vocab_size</span><span class="p">,</span> <span class="n">rank</span><span class="o">=</span><span class="mi">256</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">W1</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Embedding</span><span class="p">(</span><span class="n">vocab_size</span><span class="p">,</span> <span class="n">rank</span><span class="p">)</span>   <span class="c1"># 上一个 token -&gt; rank 维向量
</span>        <span class="n">self</span><span class="p">.</span><span class="n">W2</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">rank</span><span class="p">,</span> <span class="n">vocab_size</span><span class="p">,</span> <span class="n">bias</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>  <span class="c1"># rank -&gt; V
</span>
    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">prev_token_id</span><span class="p">:</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="n">torch</span><span class="p">.</span><span class="n">Tensor</span><span class="p">:</span>
        <span class="c1"># prev_token_id: [batch]
</span>        <span class="n">embed</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nc">W1</span><span class="p">(</span><span class="n">prev_token_id</span><span class="p">)</span>   <span class="c1"># [batch, rank]
</span>        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nc">W2</span><span class="p">(</span><span class="n">embed</span><span class="p">)</span>            <span class="c1"># [batch, V]  ← 转移偏置 B(x_{k-1}, ·)
</span></code></pre></div></div> <p><strong>RNN Head</strong>——带一个跨位置的 recurrent state $s_k$，能看到整个 prefix 历史：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">RNNHead</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">vocab_size</span><span class="p">,</span> <span class="n">backbone_dim</span><span class="p">,</span> <span class="n">rank</span><span class="o">=</span><span class="mi">256</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">W1</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Embedding</span><span class="p">(</span><span class="n">vocab_size</span><span class="p">,</span> <span class="n">rank</span><span class="p">)</span>
        <span class="c1"># 单个 linear 拆成 gate / candidate / output 三部分
</span>        <span class="n">self</span><span class="p">.</span><span class="n">Wgco</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="mi">2</span> <span class="o">*</span> <span class="n">rank</span> <span class="o">+</span> <span class="n">backbone_dim</span><span class="p">,</span> <span class="mi">3</span> <span class="o">*</span> <span class="n">rank</span><span class="p">,</span> <span class="n">bias</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">W2</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">rank</span><span class="p">,</span> <span class="n">vocab_size</span><span class="p">,</span> <span class="n">bias</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">step</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">s_prev</span><span class="p">,</span> <span class="n">prev_token_id</span><span class="p">,</span> <span class="n">h_k</span><span class="p">):</span>
        <span class="c1"># s_prev: [batch, rank]，h_k: [batch, backbone_dim]
</span>        <span class="n">z</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">([</span><span class="n">s_prev</span><span class="p">,</span> <span class="n">self</span><span class="p">.</span><span class="nc">W1</span><span class="p">(</span><span class="n">prev_token_id</span><span class="p">),</span> <span class="n">h_k</span><span class="p">],</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>  <span class="c1"># [batch, 2r+d]
</span>        <span class="n">gates</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nc">Wgco</span><span class="p">(</span><span class="n">z</span><span class="p">).</span><span class="nf">chunk</span><span class="p">(</span><span class="mi">3</span><span class="p">,</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">)</span>   <span class="c1"># gate, cand, out 各 [batch, rank]
</span>        <span class="n">g</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">sigmoid</span><span class="p">(</span><span class="n">gates</span><span class="p">[</span><span class="mi">0</span><span class="p">])</span>
        <span class="n">s_k</span> <span class="o">=</span> <span class="n">g</span> <span class="o">*</span> <span class="n">s_prev</span> <span class="o">+</span> <span class="p">(</span><span class="mi">1</span> <span class="o">-</span> <span class="n">g</span><span class="p">)</span> <span class="o">*</span> <span class="n">torch</span><span class="p">.</span><span class="nf">tanh</span><span class="p">(</span><span class="n">gates</span><span class="p">[</span><span class="mi">1</span><span class="p">])</span>             <span class="c1"># GRU-like 更新
</span>        <span class="n">B_k</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nc">W2</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">tanh</span><span class="p">(</span><span class="n">gates</span><span class="p">[</span><span class="mi">2</span><span class="p">]))</span>                            <span class="c1"># [batch, V]
</span>        <span class="k">return</span> <span class="n">s_k</span><span class="p">,</span> <span class="n">B_k</span>
</code></pre></div></div> <p>整个 sequential stage 的推理是个串行的循环，伪代码理解：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">sequential_stage</span><span class="p">(</span><span class="n">base_logits</span><span class="p">,</span> <span class="n">hidden_states</span><span class="p">,</span> <span class="n">seq_head</span><span class="p">,</span> <span class="n">anchor_id</span><span class="p">):</span>
    <span class="c1"># base_logits:   [batch, γ, V]  ← 来自 parallel stage
</span>    <span class="c1"># hidden_states: [batch, γ, d]  ← 来自 parallel stage
</span>    <span class="n">B</span><span class="p">,</span> <span class="n">gamma</span><span class="p">,</span> <span class="n">V</span> <span class="o">=</span> <span class="n">base_logits</span><span class="p">.</span><span class="n">shape</span>
    <span class="n">draft_tokens</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="n">prev_id</span> <span class="o">=</span> <span class="n">anchor_id</span>           <span class="c1"># 第一个"前序 token"就是 anchor
</span>    <span class="n">s</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">B</span><span class="p">,</span> <span class="n">seq_head</span><span class="p">.</span><span class="n">rank</span><span class="p">)</span>

    <span class="k">for</span> <span class="n">k</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">gamma</span><span class="p">):</span>
        <span class="n">s</span><span class="p">,</span> <span class="n">bias</span> <span class="o">=</span> <span class="n">seq_head</span><span class="p">.</span><span class="nf">step</span><span class="p">(</span><span class="n">s</span><span class="p">,</span> <span class="n">prev_id</span><span class="p">,</span> <span class="n">hidden_states</span><span class="p">[:,</span> <span class="n">k</span><span class="p">,</span> <span class="p">:])</span>
        <span class="n">logits_k</span> <span class="o">=</span> <span class="n">base_logits</span><span class="p">[:,</span> <span class="n">k</span><span class="p">,</span> <span class="p">:]</span> <span class="o">+</span> <span class="n">bias</span>     <span class="c1"># 并行 logit + 转移修正
</span>        <span class="n">x_k</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">multinomial</span><span class="p">(</span><span class="n">logits_k</span><span class="p">.</span><span class="nf">softmax</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">),</span> <span class="mi">1</span><span class="p">).</span><span class="nf">squeeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>
        <span class="n">draft_tokens</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">x_k</span><span class="p">)</span>
        <span class="n">prev_id</span> <span class="o">=</span> <span class="n">x_k</span>              <span class="c1"># 下一步就看到当前采样结果
</span>
    <span class="k">return</span> <span class="n">torch</span><span class="p">.</span><span class="nf">stack</span><span class="p">(</span><span class="n">draft_tokens</span><span class="p">,</span> <span class="n">dim</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>  <span class="c1"># [batch, γ]
</span></code></pre></div></div> <p>虽然这个循环是串行的，但 seq head 实在太轻（embedding 查表 + 低秩矩阵乘），每步计算量远小于并行骨干，所以 $T_\text{sequential} \ll T_\text{parallel}$，总延迟仍然由并行阶段主导，这一点论文里也是实测过的：如下图，草稿长度从 2 拉到 8，sequential head 带来的额外延迟几乎看不见（最右面板）。</p> <p><img src="/assets/img/posts/20260814-dspark/figure4_p13.png" alt="proposal length 和 latency overhead"/></p> <p>而序列依赖带来的收益也立竿见影——下图对比 drafter 深度对 acceptance length 的影响，<strong>2 层的 DSpark 已经超过了 5 层的 DFlash</strong>，说明花最便宜的成本把”block 内依赖”补上，比无脑加深并行骨干划算得多：</p> <p><img src="/assets/img/posts/20260814-dspark/figure3_p13.png" alt="drafter 深度对 acceptance length 的影响"/></p> <h2 id="4-confidence-scheduled-verification不是所有草稿都值得验收">4. Confidence-Scheduled Verification：不是所有草稿都值得验收</h2> <p>个阶段解决了”草稿质量”的问题，但还有一个独立的问题没人管——<strong>验证浪费</strong>。</p> <p>投机解码送进大模型验证的 token 越多，一旦某个位置被拒、后面全部作废，前面的验证预算就白花了。如果草稿质量本来就差，每次送 8 个去验收、平均只打回 2 个，剩下 6 个 token 验证纯属浪费。更要命的是在高并发系统里：<strong>每个白白验证的 token 都占着 target model 的 batch capacity，而这部分 capacity 本该用来服务其他用户的请求。</strong> 所以关键是要做到”择优验证”——即先估计草稿 token 能不能被接住，再决定送几个。</p> <h3 id="41-confidence-head预测我这份草稿被接受的概率">4.1 Confidence Head：预测”我这份草稿被接受的概率”</h3> <p>DSpark 在 sequential stage 之外，加了一个 confidence head，为每个草稿 token 输出 $c_k \in (0,1)$：在有前面的 token 全被接受的条件下，第 $k$ 个 token 也会被接受的概率：</p> \[c_k = \sigma(w^\top [h_k; W_1[x_{k-1}]])\] <p>监督信号不是简单地”这个 token 对没对”，而是 draft 和 target 两个分布之间的 TV 距离：</p> \[c_k^* = 1 - \frac{1}{2} \|p_k^d - p_k^t\|_1\] <p>这里的 $p_k^d$ 是 drafter 在第 $k$ 个位置给出的分布，$p_k^t$ 是目标模型在同一位置的分布，$|\cdot|_1$ 是 L1 范数（逐元素取绝对差求和）。为什么要 TV 距离当监督信号？因为投机解码的 rejection sampling 机制保证：<strong>单步接受概率恰好等于 $1 - \frac{1}{2}|p^d - p^t|_1$</strong>——两个分布越接近，TV 距离越小，被接受的概率越高。所以模型学会的是”我的草稿和真正的大模型有多像”，而不是”我猜的 token 对不对”。</p> <p>这两件事的区别很关键：<strong>accuracy 高不等于 acceptance rate 高</strong>。就算你 top-1 猜对了，但如果整个分布和大模型偏差很大，rejection sampling 依然可能拒绝你。很多人在跑投机解码时会忽略这层。</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">ConfidenceHead</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">backbone_dim</span><span class="p">,</span> <span class="n">markov_rank</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">w</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">backbone_dim</span> <span class="o">+</span> <span class="n">markov_rank</span><span class="p">,</span> <span class="mi">1</span><span class="p">,</span> <span class="n">bias</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>

    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">h_k</span><span class="p">,</span> <span class="n">prev_embed</span><span class="p">):</span>
        <span class="c1"># h_k: [batch, d], prev_embed: [batch, rank]
</span>        <span class="k">return</span> <span class="n">torch</span><span class="p">.</span><span class="nf">sigmoid</span><span class="p">(</span><span class="n">self</span><span class="p">.</span><span class="nf">w</span><span class="p">(</span><span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">([</span><span class="n">h_k</span><span class="p">,</span> <span class="n">prev_embed</span><span class="p">],</span> <span class="n">dim</span><span class="o">=-</span><span class="mi">1</span><span class="p">))).</span><span class="nf">squeeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>
</code></pre></div></div> <p>训练时用 BCE loss：</p> \[\mathcal{L}_\text{conf} = -\sum_{k=1}^\gamma w_k \left[ c_k^* \log c_k + (1-c_k^*) \log(1 - c_k) \right]\] <h3 id="42-sequential-temperature-scaling给置信度校准">4.2 Sequential Temperature Scaling：给置信度”校准”</h3> <p>神经网络输出的概率天然贴向 0/1 两边（overconfident），直接把 $c_k$ 当作接受概率用，误差会很大。DSpark 用了一个挺工程化的手段——<strong>Sequential Temperature Scaling（STS）</strong>：prefix survival probability 是各位置置信度的连乘 $\prod_{i \le k} c_i$，所以校准也是从左到右逐位置来的，每个位置用一维 grid search，在固定前面已校准位置的前提下，找一个让该位置 ECE 最小的温度参数。</p> <p>这还是个 order-preserving 的变换：<strong>它不改变各个草稿之间质量高低的相对排序，只是把绝对概率尺子校正对</strong>，让后面的调度器算得实。</p> <h3 id="43-hardware-aware-prefix-scheduler把该验证几个变成调度问题">4.3 Hardware-Aware Prefix Scheduler：把”该验证几个”变成调度问题</h3> <p>有了校准后的 $c_k$，一条请求（记为 $r$）在第 $j$ 处的 prefix survival probability 就是：</p> \[a_{r,j} = \prod_{i \le j} c_{r,i}\] <p>含义是”前 $j$ 个草稿全部被接受的概率”，天然单调递减——这给贪心算法提供了支撑。</p> <p>现在的问题是，一批请求 $r=1..R$ 在排队，该给每个请求塞多少 token 去验证？DSpark 的答案是把这当成一个<strong>在整个 batch 上的吞吐优化</strong>：</p> \[\Theta = \tau \cdot \text{SPS}(B)\] <p>其中 $B = \sum_r (1 + \ell_r)$ 是送进 target model 的 token 总数，$\text{SPS}(B)$ 是引擎在 batch size $B$ 下的每步步数（提前 profile 好的函数）。实际里 $\text{SPS}(B)$ 随着 batch 变大单调下降（batch 越大，每一步的耗时越长），$\tau$ 却随着每个请求的 $\ell_r$ 拉长而变大（多验收几轮多收益）——所以两头有个最优值。而 $a_{r,j}$ 的单调性又能保证贪心成立：把所有候选 (request, position) 按 $a_{r,j}$ 从大到小排序，依次塞进验证 batch，每塞一个算一次当前吞吐，吞吐不再变大就停。论文里 Algorithm 1 的完整流程截图如下，主体就是上面这套贪心：</p> <p><img src="/assets/img/posts/20260814-dspark/algorithm1_p8.png" alt="Hardware-Aware Prefix Scheduler 伪代码"/></p> <p>上面那段逻辑，翻译成 Python 大约是这样：</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">hardware_aware_scheduler</span><span class="p">(</span><span class="n">confidence_scores</span><span class="p">,</span> <span class="n">sps_table</span><span class="p">,</span> <span class="n">R</span><span class="p">,</span> <span class="n">gamma</span><span class="p">):</span>
    <span class="c1"># confidence_scores: [R, gamma]
</span>    <span class="c1"># sps_table: dict {batch_size -&gt; steps_per_sec}
</span>
    <span class="c1"># 计算每个请求每个位置的 survival probability
</span>    <span class="n">a</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cumprod</span><span class="p">(</span><span class="n">confidence_scores</span><span class="p">,</span> <span class="n">dim</span><span class="o">=</span><span class="mi">1</span><span class="p">)</span>  <span class="c1"># [R, gamma]
</span>
    <span class="c1"># 所有候选 (request, position) 按 a 从大到小排序
</span>    <span class="n">candidates</span> <span class="o">=</span> <span class="p">[(</span><span class="n">a</span><span class="p">[</span><span class="n">r</span><span class="p">,</span> <span class="n">j</span><span class="p">].</span><span class="nf">item</span><span class="p">(),</span> <span class="n">r</span><span class="p">,</span> <span class="n">j</span><span class="p">)</span>
                  <span class="k">for</span> <span class="n">r</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">R</span><span class="p">)</span> <span class="k">for</span> <span class="n">j</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">gamma</span><span class="p">)</span> <span class="k">if</span> <span class="n">a</span><span class="p">[</span><span class="n">r</span><span class="p">,</span> <span class="n">j</span><span class="p">]</span> <span class="o">&gt;</span> <span class="mi">0</span><span class="p">]</span>
    <span class="n">candidates</span><span class="p">.</span><span class="nf">sort</span><span class="p">(</span><span class="n">reverse</span><span class="o">=</span><span class="bp">True</span><span class="p">)</span>

    <span class="n">lengths</span> <span class="o">=</span> <span class="p">[</span><span class="mi">0</span><span class="p">]</span> <span class="o">*</span> <span class="n">R</span>          <span class="c1"># 初始每个请求验证长度为 0
</span>    <span class="n">B</span> <span class="o">=</span> <span class="n">R</span>                      <span class="c1"># 初始 batch size（每个请求至少发 anchor）
</span>    <span class="n">tau_expected</span> <span class="o">=</span> <span class="n">R</span>
    <span class="n">best_theta</span> <span class="o">=</span> <span class="n">tau_expected</span> <span class="o">*</span> <span class="n">sps_table</span><span class="p">[</span><span class="n">B</span><span class="p">]</span>
    <span class="n">best_lengths</span> <span class="o">=</span> <span class="n">lengths</span><span class="p">.</span><span class="nf">copy</span><span class="p">()</span>

    <span class="k">for</span> <span class="n">a_val</span><span class="p">,</span> <span class="n">r</span><span class="p">,</span> <span class="n">j</span> <span class="ow">in</span> <span class="n">candidates</span><span class="p">:</span>
        <span class="k">if</span> <span class="n">j</span> <span class="o">!=</span> <span class="n">lengths</span><span class="p">[</span><span class="n">r</span><span class="p">]:</span>    <span class="c1"># prefix 约束，必须按顺序扩展
</span>            <span class="k">continue</span>
        <span class="n">lengths</span><span class="p">[</span><span class="n">r</span><span class="p">]</span> <span class="o">+=</span> <span class="mi">1</span>
        <span class="n">B</span> <span class="o">+=</span> <span class="mi">1</span>
        <span class="n">tau_expected</span> <span class="o">+=</span> <span class="n">a_val</span>
        <span class="n">theta</span> <span class="o">=</span> <span class="n">tau_expected</span> <span class="o">*</span> <span class="n">sps_table</span><span class="p">.</span><span class="nf">get</span><span class="p">(</span><span class="n">B</span><span class="p">,</span> <span class="mi">0</span><span class="p">)</span>
        <span class="k">if</span> <span class="n">theta</span> <span class="o">&gt;</span> <span class="n">best_theta</span><span class="p">:</span>
            <span class="n">best_theta</span> <span class="o">=</span> <span class="n">theta</span>
            <span class="n">best_lengths</span> <span class="o">=</span> <span class="n">lengths</span><span class="p">.</span><span class="nf">copy</span><span class="p">()</span>
        <span class="k">else</span><span class="p">:</span>
            <span class="k">break</span>              <span class="c1"># 单调性保证可以提前终止
</span>
    <span class="k">return</span> <span class="n">best_lengths</span>
</code></pre></div></div> <p>这个设计的妙处在于：<strong>系统负载高时 $\text{SPS}(B)$ 往下掉得快，scheduler 就自动收紧各请求的验证长度；负载低时再放开，让每请求验证更多 token。</strong> 相比之下，虽然 DeepSeek 之前的生产 baseline（MTP-1）也是”多 token 预测”一家人，但每轮固定验证 2 个 token，没有任何弹性。</p> <p>置信度也真的有用。如下图为 confidence threshold 的扫参结果——随着阈值提高，acceptance rate 从 ~77% 上升到 92.5%，代价是每步平均接受的 token 略降，因为”会被拒的草稿”提前被筛掉了：</p> <p><img src="/assets/img/posts/20260814-dspark/figure5_p15.png" alt="置信度阈值"/></p> <h2 id="5-训练目标三项损失">5. 训练目标：三项损失</h2> <p>DSpark 的 drafter 加起来一起训练，损失由三项组成（每一项都带位置衰减权重 $w_k = e^{-(k-1)/\gamma}$，越靠前的 token 权重越大——因为位置靠前一旦出错，浪费的更多）：</p> \[\mathcal{L} = \alpha_{ce} \mathcal{L}_{ce} + \alpha_{tv} \mathcal{L}_{tv} + \alpha_{conf} \mathcal{L}_{conf}\] <table> <thead> <tr> <th>项</th> <th>作用</th> <th>权重</th> </tr> </thead> <tbody> <tr> <td>$\mathcal{L}_{ce}$</td> <td>交叉熵，让 drafter 逐 token 预测正确</td> <td>0.1</td> </tr> <tr> <td>$\mathcal{L}_{tv}$</td> <td>TV 距离，直接最小化 draft/target 分布差异</td> <td>0.9</td> </tr> <tr> <td>$\mathcal{L}_{conf}$</td> <td>置信度 head，BCE</td> <td>1.0</td> </tr> </tbody> </table> <p>$\mathcal{L}_{tv}$ 权重是最高的，有意的设计：<strong>因为 acceptance rate 与 TV 距离直接挂钩，最小化 TV 比单纯猜对 token 标准化更直接地提升加速比</strong>。</p> <h2 id="6-线上部署结果deepseek-v4">6. 线上部署结果（DeepSeek V4）</h2> <p>这篇论文最有说服力的部分在真实生产数据，前面讲的所有设计都跑在 DeepSeek V4 的线上系统里。</p> <p>下图是 throughput（系统总吞吐）和 TPS（单用户生成速度）的 Pareto 前沿对比：</p> <p><img src="/assets/img/posts/20260814-dspark/figure7_p18.png" alt="Throughput vs TPS：DeepSeek V4 线上表现"/></p> <p>与 MTP-1（production baseline）比，DSpark 把 Pareto 曲线整体向右上凸：<strong>相同 TPS 下系统吞吐提升 51%~52%</strong>，<strong>相同吞吐下每用户 TPS 提升 57%~60%</strong>，在低负载高 TPS 区间提升高达 <strong>+85%</strong>。</p> <p>再看 load-adaptive 的效果：</p> <p><img src="/assets/img/posts/20260814-dspark/figure8_p19.png" alt="负载自适应吞吐和 verification budget"/></p> <p>并发请求数上涨时，DSpark 的验证预算（下、折叠线）自动从 ~5 收缩到 ~3.5，始终把系统吞吐保在最优位置；相比之下 MTP-1 永远固定在 2，任凭负载变化。</p> <h2 id="7-offline-benchmark换个模型也成立">7. Offline Benchmark：换个模型也成立</h2> <p>生产流量好说，但有人会问：是不是只有 DeepSeek 自家模型调得好？论文在 Qwen3-4B/8B/14B 和 Gemma4-12B 上也做了完整对照：</p> <p><img src="/assets/img/posts/20260814-dspark/table1_p11.png" alt="主结果：acceptance length 对比"/></p> <p>以 Qwen3-8B + GSM8K 为例：Eagle3 是 5.30，DFlash 是 5.33，DSpark 达 6.17。而 Chat 类任务（MT-Bench、Alpaca）提升更刺眼——也符合直觉，<strong>链上对话需要的”语境连贯性”越多，序列依赖建模就越值钱</strong>。</p> <h2 id="8-个人-take">8. 个人 Take</h2> <p>DSpark 有两处我觉得设计得很干净的地方：</p> <p>一是 semi-autoregressive 架构中的分工：<strong>重骨干做粗预测，轻 head 做小修正</strong>。这个”把重活留给并行、把依赖补丁下放给一个几乎免费的顺序头”的范式，本身可搬走，不一定要配投机解码这一整套。</p> <p>二是 confidence-scheduled verification 的视角转换：它把投机解码从”单请求 drafting”兜底扩大到了<strong>“多请求 batch 调度”</strong>的框架。在真正的生产系统里，一个 draft token 的边际价值不是固定的——它取决于当前 batch size 和系统负载。把”草稿值不值得送”和”送多少进去”这两个以前被拆开考虑的问题放在一起联合优化，这才是投机解码在生产环境里真正发挥价值的地方。</p> <p>最后聊点感受。投机解码过去常被吐槽”理论全对、工程没法用”，但问题往往不在草稿模型本身，而是栽在”验收浪费”和”调度僵化”这两个工程细节上。DSpark 能把这些细节做到生产级，并拿出真实线上数据，说服力就不是一般 benchmark 能量化的了。这类思路对做 LLM 推理的朋友，值得读一遍原文。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 LLM 推理效率优化你想系统深入，可以看看我之前出版的<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，书里有专章讲 LLM 推理效率和参数高效微调，和本文的工程背景有直接关联。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="投机解码"/><category term="推理优化"/><category term="DeepSeek"/><category term="论文解读"/><summary type="html"><![CDATA[DSpark：DeepSeek 线上跑的投机解码，半自回归 + 置信度调度怎么做到的]]></summary></entry><entry><title type="html">DeepSeek NSA：Sparse Attention 不是新概念，但这次真的做对了</title><link href="https://marsggbo.github.io/blog/2026/20260814-nsa/" rel="alternate" type="text/html" title="DeepSeek NSA：Sparse Attention 不是新概念，但这次真的做对了"/><published>2026-08-14T00:00:00+00:00</published><updated>2026-08-14T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260814-nsa</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260814-nsa/"><![CDATA[<h1 id="deepseek-nsasparse-attention-不是新概念但这次真的做对了">DeepSeek NSA：Sparse Attention 不是新概念，但这次真的做对了</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2502.11089">Native Sparse Attention: Hardware-Aligned and Natively Trainable Sparse Attention</a></p> </blockquote> <hr/> <h2 id="1-前言先把背景交代清楚">1. 前言：先把背景交代清楚</h2> <p>你有没有想过这样一个问题：<strong>为什么模型上下文越长，生成越慢、服务器账单越贵？</strong></p> <p>Attention 机制在这里有两笔躲不掉的账，都跟序列长度强相关：</p> <ul> <li><strong>计算量</strong>：理论上限是 $O(N^2)$，序列翻倍，prefill 的计算量翻四倍；</li> <li><strong>内存访问</strong>：每一个历史 token 的 K/V 都要保存，decode 阶段每生成一个新 token，都得把过去所有的 K/V 从 HBM 里读一遍——而 decode 恰恰是 memory-bound（内存带宽受限）的，读多少数据，就要等多久。</li> </ul> <p>所以上下文能力是”真香”的，attention 成本也”真贵”，64k、128k 这样的长上下文场景里，attention 的账单几乎肉眼可见地涨。大家的第一个念头很自然：<strong>反正也不是每个历史 token 都有用，跳过不重要的，是不是就能便宜下来？</strong></p> <p>这个想法有个正式名字：<strong>Sparse Attention（稀疏注意力）</strong>。而且它一点都不新——BigBird、Longformer、Reformer，2020 年那波论文基本把”哪些 token 可以不算”这件事在算法层面研究透了：局部窗口、块状稀疏、全局哨兵 token，该有的花样都有了。</p> <p><strong>但真正好用的 sparse attention 工程实现，现在才开始出现。</strong></p> <p>这不是贬低前人，是实事求是：以前的 sparse attention 在 GPU 上的落地大多很尴尬——计算上确实跳过了部分 token，但内存访问 pattern 还是按 dense 来的，HBM 带宽该占多少占多少，SRAM 利用率也差。结果就是理论 FLOPs 降了不少，实际 wall-clock time 没快多少。而且更麻烦的是可训练性：很多方案是拿 full attention 训练完的模型，推理时再做近似（H2O、StreamingLLM 都走这条路线），模型从来没为稀疏模式优化过，近似误差是固定的，质量必然有折扣。</p> <p>大模型时代的 attention 优化有个公认的分水岭——FlashAttention 证明了一件事：<strong>attention 慢的根子不在 FLOPs，在 HBM 访问；搞定内存访问比省计算量更重要。</strong>DeepSeek 这篇 NSA（Native Sparse Attention）做的事，大致可以理解成”把 FlashAttention 的这条路在稀疏场景下重新走一遍”：</p> <p><strong>算法设计和 GPU kernel 实现一起考虑，让 sparsity 真正落地为实际加速；同时支持直接从零预训练稀疏模型，而不是事后近似。</strong></p> <p>64k 长度序列上，对比 FlashAttention-2：decode <strong>快 11.6×</strong>，forward <strong>快 9×</strong>，backward <strong>快 6×</strong>，而且模型质量不降反升——在通用 benchmark 平均分上超过了 Full Attention baseline。</p> <p>这三个数字里 decode 反而提升最大，原因值得先说一下，它也是理解这篇论文的关键：</p> <ul> <li><strong>Decode</strong>：每生成一个 token，都把历史 K/V 从 HBM 读一遍，是 memory-bandwidth bound。稀疏化直接跳过大部分 KV 的 load，HBM 读取量大幅下降，对带宽受限的操作收益最大，所以反而最快——11.6×</li> <li><strong>Forward（prefill）</strong>：整个 prompt 并行处理，是 compute-bound，省下的 FLOPs 直接转化为加速——9×</li> <li><strong>Backward</strong>：还要算梯度，attention 之外的大量操作没法稀疏，折扣最大——6×</li> </ul> <p>好，背景交代完毕。下面先把”老 sparse attention 为什么没做对”拆开讲，再看 NSA 的算法和 kernel 是怎么对症下药的。</p> <hr/> <h2 id="2-老-sparse-attention-为什么没做好算法对硬件不认">2. 老 Sparse Attention 为什么没做好：算法对，硬件不认</h2> <p>先说清楚问题在哪，这样才能理解 NSA 解决了什么。</p> <p>Attention 的计算复杂度是 $O(N^2)$，序列长度翻倍，计算量翻四倍。Sparse attention 的思路是：不是所有 token 对之间的 attention 都有意义，跳过那些”不重要的”token，把 $O(N^2)$ 压下来。</p> <p>算法层面没问题。但落到 GPU 上：</p> <p><strong>GPU 喜欢连续、规整的内存访问。</strong> 你告诉它”第 17、43、128、512 号 token 的 KV 我要，其他不要”，GPU 就得在 HBM 里东一块西一块地 load，内存访问变成 random access，带宽利用率极差。哪怕你 FLOPs 少了 90%，memory bound 没解决，速度未必快多少。</p> <p><strong>另一个问题是可训练性。</strong> 很多 sparse attention 是训练完一个 full attention 模型，推理时再做近似（比如 H2O、StreamingLLM）。这种方式不可能做到最优——模型本身没有针对 sparse pattern 训练优化过，近似误差是固定存在的。</p> <p>NSA 把这两个问题一起解决。</p> <hr/> <h2 id="3-nsa-的三路并行架构">3. NSA 的三路并行架构</h2> <p>算法层面，NSA 的第一步是把 attention 拆成三个并行分支，每个分支处理不同粒度的信息，最后用 gated 加权合并。说人话就是：”全局走势靠压缩总结、关键位置靠精读、近处靠逐词看”，三种阅读策略各司其职：</p> <p><img src="/assets/img/posts/20260814-nsa/figure2_p3.png" alt="NSA 三路并行架构概览"/></p> <h3 id="31-compressed-attention粗粒度的全局感知">3.1 Compressed Attention（粗粒度的全局感知）</h3> <p>这一路把整个序列的 KV 按块压缩——每个块的 token 用一个 MLP 压成一个代表向量，然后对这些压缩后的 KV 做 attention。</p> <p>作用：<strong>保留全局上下文的粗粒度感知</strong>。模型能”看到”整个历史序列的大致走向，不会因为 sparsity 完全丢掉远距离信息。</p> <p>计算复杂度：从 $O(N^2)$ 降到了 $O(N \cdot N/b)$，$b$ 是块大小。</p> <h3 id="32-selected-attention精细-top-n-块选择">3.2 Selected Attention（精细 top-n 块选择）</h3> <p>这一路做精细选择：先用 compressed attention 的分数估算哪些块”最重要”，然后 top-n 挑出来，对这选定块的原始 KV 做 full attention。</p> <p>作用：<strong>精确捕捉真正重要的长距离依赖</strong>。比如你在写代码，当前 token 可能需要精确 attend 到几百行之前定义的变量——compressed 那路能粗略定位它在哪，selected 这路负责精确计算。</p> <p>关键设计：选择的粒度是<strong>块</strong>而非单个 token，这就是 hardware-aware 的核心。块级选择意味着内存访问是连续的，GPU 可以高效地 load 整块 KV，而不会退化成 random access 单 token。</p> <h3 id="33-sliding-window-attention近处的精确阅读">3.3 Sliding Window Attention（近处的精确阅读）</h3> <p>这一路对当前 token 最近的 $w$ 个 token 做完整 attention。</p> <p>作用：<strong>保局部连贯性</strong>。语言本质上是局部依赖的，相邻 token 的关联最强。这一路保证不会因为 selected 块选择遗漏紧邻上下文。</p> <p>实现上这也是最简单的一路：标准 causal attention + fixed window，FlashAttention 直接支持。</p> <hr/> <h2 id="4-hardware-aware-kernel-设计把稀疏变成可执行的快">4. Hardware-Aware Kernel 设计：把稀疏变成”可执行的快”</h2> <p>三路架构是 NSA 能快起来的算法基础，但真正的加速来自 kernel 实现。如下图，NSA 的 Triton kernel 设计图：</p> <p><img src="/assets/img/posts/20260814-nsa/figure3_full.png" alt="NSA Kernel 设计：GQA 分组 load，SRAM 计算"/></p> <p>两个关键设计：</p> <p><strong>1. GQA 分组共享 KV load（Grid Loop）</strong></p> <p>NSA 基于 GQA（Grouped Query Attention）架构，一组 query head 共享同一套 KV。Kernel 的 Grid Loop 按 GQA group 遍历 query，这样同一组内所有 query head 对同一块 KV 的 load 只发生一次，然后 SRAM 里的 KV 被整组复用。</p> <p><strong>结果：HBM → SRAM 的 load 次数大幅减少，带宽利用率显著提升。</strong></p> <p><strong>2. 块级 KV 选择（Inner Loop）</strong></p> <p>Selected attention 的稀疏索引是按块存的连续 index，Inner Loop 按这个 index 顺序 load 对应 KV 块进 SRAM 再计算。因为选中的是整块，load 全是顺序 coalesced access，不存在 random access 的问题。</p> <p>这和”每次 load 一个 token 的 KV”有本质区别——前者是顺序化的结构，后者是跳变的随机。</p> <p>Table 4 的分析显示，64k 序列下 NSA 的 memory access volume 只有 FlashAttention-2 的约 1/9，正好对应 forward 9× 的实测加速——一个数字直接对上，说明快得是真的省流量，不是靠偷工减料。</p> <hr/> <h2 id="5-端到端可训练不是事后近似">5. 端到端可训练：不是事后近似</h2> <p>NSA 支持<strong>从头预训练</strong>，这一点值得单独说一句。</p> <p>三路分支的 gated 合并参数、selected attention 的块选择逻辑，训练时全部参与梯度更新。模型在训练过程中就学会了”哪些块重要、怎么分配三路权重”，而不是推理时用规则去近似一个 full attention 模型——这是和 H2O/StreamingLLM 这类方案的路线之争。</p> <p>实验用的是 27B 总参数（3B active，MoE 架构），预训练同等 token 下，NSA 的 loss 曲线和 Full Attention 几乎重合，甚至略低。</p> <hr/> <h2 id="6-实验结果">6. 实验结果</h2> <h3 id="61-通用-benchmark">6.1 通用 Benchmark</h3> <p>如下表，NSA 在大多数通用 benchmark 上<strong>超过</strong>了同等规模的 Full Attention baseline：平均分 <strong>0.456</strong> vs Full Attention 的 0.443：</p> <p><img src="/assets/img/posts/20260814-nsa/table1_p10.png" alt="通用 benchmark 对比"/></p> <p>这是在整个模型计算量大幅下探的情况下做到的。核心原因：稀疏注意力过滤了大量无意义 token 对的干扰，某种程度上相当于对 attention pattern 做了一次”降噪”。</p> <h3 id="62-长上下文needle-in-a-haystack">6.2 长上下文：Needle-in-a-Haystack</h3> <p>64k 上下文的 Needle-in-a-Haystack，NSA 全绿：所有位置、所有深度 accuracy = 1.0，没有任何折损。说明 Compressed + Selected 的双路设计确实兜住了长距离依赖，没有因为稀疏而丢关键信息：</p> <p><img src="/assets/img/posts/20260814-nsa/figure5_p12.png" alt="Needle-in-a-Haystack 64k 满分"/></p> <h3 id="63-实际加速对比-triton-版-flashattention-2">6.3 实际加速（对比 Triton 版 FlashAttention-2）</h3> <p>如下图：</p> <p><img src="/assets/img/posts/20260814-nsa/figure6_p13.png" alt="NSA vs FlashAttention-2 实际速度对比"/></p> <ul> <li>8k 序列：forward 2.1×，backward 1.1×（短序列时 sparsity 收益还不明显）</li> <li>32k 序列：forward 6.3×，backward 3.4×</li> <li>64k 序列：forward <strong>9.0×</strong>，backward <strong>6.0×</strong></li> </ul> <p>序列越长，收益越大——这正是 sparse attention 应该有的表现：$O(N^2)$ vs $O(N \cdot k)$ 的差距要序列够长才放得出来。</p> <hr/> <h2 id="7-和以往-sparse-attention-的本质区别">7. 和以往 Sparse Attention 的本质区别</h2> <table> <thead> <tr> <th>维度</th> <th>老方案（H2O/StreamingLLM 等）</th> <th>NSA</th> </tr> </thead> <tbody> <tr> <td>训练方式</td> <td>训练 Full Attention，推理时近似</td> <td>原生稀疏预训练</td> </tr> <tr> <td>sparsity 粒度</td> <td>token 级（random access）</td> <td>block 级（coalesced access）</td> </tr> <tr> <td>实际加速</td> <td>理论 FLOPs 降，wall-clock 未必</td> <td>显著真实加速（6-9×）</td> </tr> <tr> <td>质量</td> <td>通常有精度损失</td> <td>达到甚至超过 Full Attention</td> </tr> </tbody> </table> <p>最本质的区别就一句话：<strong>以前是把 dense 模型想办法变稀疏，NSA 是把稀疏作为训练目标本身。</strong></p> <hr/> <h2 id="8-个人-take">8. 个人 Take</h2> <p>这篇工作让我想起 FlashAttention 刚出来时的感觉：大家都知道 attention 慢，也都在想各种算法层面的优化，但 FlashAttention 的核心洞察是”<strong>IO-awareness 比 FLOPs 更重要</strong>“——用 tiling 和 online softmax 在不改变结果的前提下大幅减少 HBM 访问，才真正快了。</p> <p>NSA 做的类似：sparse attention 的算法价值业内早就认可，但落地的 gap 在于 memory access pattern。NSA 用块级选择把 random access 变成 sequential access，再配合 GQA 的 KV 共享，让 GPU 真正能高效执行稀疏计算。</p> <p><strong>算法 + 硬件要一起考虑</strong>，这条路 DeepSeek 走得很稳。</p> <p>当然，现在 NSA 的实验主要在 27B MoE 模型上，更大规模 dense 模型（百亿级）和更长序列（128k+）的表现还有待观察；块选择也依赖 compressed attention 的估算，某些极端 attention pattern 下会不会翻车，是值得继续追的问题。</p> <p>但作为”把 sparse attention 真正做成生产可用”的较早工作，NSA 都值得关注。</p> <hr/> <blockquote> <p>如果这篇文章涉及的 LLM 推理效率优化你想系统深入，可以看看我之前出版<a href="https://item.jd.com/14945889.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>，书里有专章讲 LLM 推理效率和 attention 优化工程背景，正好和本文的场景呼应。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="Attention"/><category term="长上下文"/><category term="推理优化"/><category term="论文解读"/><summary type="html"><![CDATA[DeepSeek NSA：Sparse Attention 不是新概念，但这次真的做对了]]></summary></entry></feed>