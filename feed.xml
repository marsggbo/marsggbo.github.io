<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://marsggbo.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://marsggbo.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-06-12T02:31:10+00:00</updated><id>https://marsggbo.github.io/feed.xml</id><title type="html">HE Xin</title><subtitle>&quot;SSS: Study, Sleep, Slim&quot; </subtitle><entry><title type="html"></title><link href="https://marsggbo.github.io/blog/2026/2026-06-07-2605_29350/" rel="alternate" type="text/html" title=""/><published>2026-06-12T02:31:10+00:00</published><updated>2026-06-12T02:31:10+00:00</updated><id>https://marsggbo.github.io/blog/2026/2026-06-07-2605_29350</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/2026-06-07-2605_29350/"><![CDATA[<blockquote> <p>插播：之前写的<a href="https://item.jd.com/14945889.html">《动手学 AutoML》</a>终于出版了，从 NAS 到超参优化都有覆盖，适合想系统入门 AutoML 的同学。好了广告结束，现在进入正题。</p> <p><img src="/assets/img/book_cover_automl.png" alt="动手学AutoML书籍封面"/></p> </blockquote> <h1 id="moe-压缩新思路别删专家别合并专家把它们重映射就够了">MoE 压缩新思路：别删专家、别合并专家，把它们”重映射”就够了</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2605.29350">ConMoE: Expert-Pool Consolidation via Prototype Reassignment for MoE Compression</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有想过，MoE 架构最骄傲的地方，恰恰也是最烫手的地方？</p> <p>MoE（Mixture-of-Experts）的核心设计是稀疏激活——每个 token 只走少数几个 expert，理论上参数量很大但计算量很小。Qwen3-30B-A3B 的名字里 A3B 的意思就是”激活参数 3B”，但总参数有 30B。听起来很美，对吧？</p> <p>但问题来了：<strong>这 30B 参数，你一个都不能少地加载进显存</strong>。</p> <p>不管一个 token 实际上只用了 3B 参数，那剩下 27B 的专家还是得老老实实待在显存里候命。这就是 MoE 推理的核心矛盾：稀疏激活省了算力，但显存一分没省。随着模型越做越大，expert 存储变成了 MoE 落地的最大障碍之一。</p> <p>现有的压缩思路主要两条路：<strong>剪掉专家（pruning）</strong> 和 <strong>合并专家（merging）</strong>。前者直接删掉一些专家，后者把多个专家的权重”揉”成一个新专家。</p> <p>今天要聊的这篇论文提了第三条路：<strong>什么都不删，什么都不合并，只是把原来的专家引用”重新指向”一个更小的专家池</strong>。</p> <p>听起来像绕口令？别急，慢慢来。</p> <hr/> <h2 id="2-现有方案的痛点">2. 现有方案的痛点</h2> <p>在聊 ConMoE 之前，先交代下背景，说说 pruning 和 merging 各自的问题。</p> <p><strong>Expert Pruning（专家剪枝）</strong>：直接删掉贡献低的专家。简单粗暴，但问题是删了就是删了，原来指向这个专家的 router 决策没有任何”容错”——被删专家的那些 token 只能丢给其他留下来的专家，不管合不合适。</p> <p><strong>Expert Merging（专家合并）</strong>：把多个专家的权重融合成一个新专家。听起来更优雅，但实际上构造了一个”原来不存在”的新权重，这个新权重可能引入了原本没有的错误，而且融合方式很敏感，调参难度大。</p> <p>这两类方法有一个共同问题：<strong>把”保留哪些专家参数”和”原来的 expert slot 该怎么被表示”这两件事耦合在一起了</strong>。</p> <p>ConMoE 的作者们说，等等，这两个问题应该分开来看。</p> <hr/> <h2 id="3-conmoe-的核心思路专家池整合expert-pool-consolidation">3. ConMoE 的核心思路：专家池整合（Expert-Pool Consolidation）</h2> <p>论文提出了一个新的框架视角，叫 <strong>expert-pool consolidation</strong>（专家池整合）。</p> <p>核心思想拆成两步：</p> <ol> <li>从原来的专家池里<strong>挑出一个更小的子集</strong>作为”原型（prototype）”</li> <li>把每个原来的专家引用<strong>确定性地重映射</strong>到某个 prototype 上</li> </ol> <p>这样，router 对外还是看到原来那些 expert slot，内部只是把调用转发给对应的 prototype。<strong>专家的权重没有被修改，没有被融合，prototype 就是原来的 pretrained 专家</strong>。</p> <p>如下图，直观地展示了 ConMoE 的整体流程：</p> <p><img src="/assets/img/posts/2605_29350/figure1_p3.png" alt="ConMoE 整体架构示意"/></p> <p>图里的流程很清晰：左边是预训练的 MoE，有若干层，每层有自己的专家池。ConMoE 在若干相邻层构成的 local scope 里，用 calibration 统计数据和专家间的距离来选出 prototype，然后把原来的每个专家 slot 都确定性地指向某个 prototype。最右边压缩后的 MoE 对外接口和原来一样（router 不变），只是内部的专家池变小了。</p> <p>这里有一个很巧妙的地方：<strong>prototype 可以跨层共享</strong>。也就是说，相邻几层的专家可以共用一个 prototype 池，这让压缩比更高，同时也利用了相邻层之间专家的冗余性。</p> <hr/> <h2 id="4-怎么挑-prototype两个信号的乘积">4. 怎么挑 prototype？两个信号的乘积</h2> <p>这是方法里最关键的设计。对于每个 local scope $G$，ConMoE 给每个专家算一个分数 $s_e$，然后取 top-K 作为 prototype。</p> <p>分数由两个信号构成：</p> <p><strong>信号一：路由条件贡献分（contribution score）$a_e$</strong></p> \[a_e = \frac{1}{|\mathcal{D}_e|} \sum_{t \in \mathcal{D}_e} g_e(t) \cdot \|e(h_t)\|_2\] <p>说人话：在 calibration 数据上，这个专家被 router 选中的那些 token，它的输出强度乘以路由权重的平均值。分数高 = 这个专家被用到时贡献大。</p> <p><strong>信号二：可替换性分（replaceability score）$b_e$</strong></p> \[b_e = \min_{e' \in \mathcal{E}_G \setminus \{e\}} d(e, e')\] <p>就是这个专家在 scope 内，到其最近邻专家的距离。距离越大 = 越难被其他专家替代 = 越不可替换。</p> <p>两个分数各自在 scope 内做 min-max 归一化后相乘：</p> \[s_e = \bar{a}_e \cdot \bar{b}_e\] <p><strong>直觉很清晰</strong>：一个好的 prototype，既要在原来模型里有实质性的贡献（不能是闲置专家），又要在参数空间里相对独特（不能被其他专家轻易替代）。两个维度同时满足，才是值得被保留的 prototype。</p> <p>选好 prototype 后，每个原始专家就找自己最近的 prototype：</p> \[m_G(e) = \arg\min_{p \in P_G} d(e, p)\] <p>这就是重映射。干净，确定，不需要梯度，不需要 fine-tune。</p> <hr/> <h2 id="5-跨层共享scope-大了不一定好">5. 跨层共享：scope 大了不一定好</h2> <p>ConMoE 允许相邻几层共享一个 prototype 候选池（local scope），这让压缩更灵活。但 scope 到底设多大合适？</p> <p>如下图是在 Qwen3-30B-A3B 上做的消融，50% expert reduction 的情况下，不同 scope size 的效果：</p> <p><img src="/assets/img/posts/2605_29350/figure2_p7.png" alt="scope size 对性能的影响"/></p> <p>结果很有趣：scope 1（只在单层内选）、scope 2、scope 4 的表现非常接近，scope 4 在这个代表性子集上平均分最高。但 scope 一旦扩到 8 或 16 层，三个任务的性能都出现明显下降，HellaSwag 和 MMLU 掉得尤其多。</p> <p>这说明什么？<strong>相邻层之间确实存在可复用的冗余，但这种冗余是局部的</strong>。距离太远的层，对应的是模型不同深度的语义转换，强行共享 prototype 会引入深度不匹配的问题。</p> <p>这也解释了为什么 ConMoE 选择 bounded local scope，而不是一个全局的 expert pool。</p> <hr/> <h2 id="6-主实验结果">6. 主实验结果</h2> <p>实验在三个 MoE 模型上做了对比：Qwen3-30B-A3B、deepseek-moe-16b-base、OLMoE-1B-7B-0125，各自测了 25% 和 50% 的 expert reduction。Baseline 包括：</p> <ul> <li><strong>Pruning 类</strong>：Frequency pruning（按路由频率删）、REAP pruning（基于贡献分删）</li> <li><strong>Merging 类</strong>：M-SMoE（路由统计引导合并）、HC-SMoE（输出特征聚类合并）</li> </ul> <p>如下表是主要实验结果（6 个多选题 benchmark 的平均分）：</p> <p><img src="/assets/img/posts/2605_29350/table1_p6.png" alt="主实验结果表"/></p> <p>几个关键观察：</p> <ul> <li><strong>DeepSeek 上，ConMoE 在 25% 和 50% reduction 两个设置下都拿到最佳平均分</strong>（0.691 vs 0.681/0.676，0.651 vs 0.642/0.613），这是最亮眼的结果。</li> <li><strong>OLMoE 上，25% reduction 同样是 ConMoE 最好</strong>（0.676 vs 0.669/0.662），50% 时和最强 baseline 很接近。</li> <li><strong>Qwen3 上，pruning 方法特别强</strong>，尤其是 50% reduction 时 Frequency pruning 达到 0.733，ConMoE 是 0.717。但 ConMoE 在 WinoGrande 和 BoolQ 上保持了很强的表现。</li> </ul> <p>整体来说，ConMoE 作为一种全新的”不删不合并、只重映射”的压缩范式，<strong>在和 pruning/merging 同等压缩比下能打平甚至超过</strong>，这本身就是一个有意义的结论。</p> <hr/> <h2 id="7-消融实验哪个设计最重要">7. 消融实验：哪个设计最重要？</h2> <h3 id="71-整合结构consolidation-structure">7.1 整合结构（Consolidation Structure）</h3> <p>如下表对比了不同的整合方式：</p> <p><img src="/assets/img/posts/2605_29350/table2_p6_04.png" alt="整合结构消融实验"/></p> <p>三种 variant：</p> <ul> <li><strong>Layer-local</strong>：只在单层内选 prototype，不跨层</li> <li><strong>Cross-layer fixed-k</strong>：跨层候选池，但每层的 prototype 数量固定</li> <li><strong>Adaptive prototype</strong>：ConMoE 默认方案，用贡献分+可替换性自适应分配</li> </ul> <p>有意思的是，layer-local 已经很强了（ARC-C 0.515，HellaSwag 0.715，MMLU 0.651）。允许跨层候选池（Cross-layer fixed-k）在 HellaSwag 上略有提升，Adaptive prototype 和 Cross-layer fixed-k 表现接近。</p> <p><strong>结论：确定性重映射本身才是最重要的成分</strong>，跨层和自适应选择是锦上添花。</p> <h3 id="72-prototype-选择策略">7.2 Prototype 选择策略</h3> <p>如下表比较了不同的 prototype 选择信号：</p> <p><img src="/assets/img/posts/2605_29350/table3_p7_08.png" alt="prototype 选择消融"/></p> <ul> <li><strong>Distance-only</strong>（只看可替换性）：ARC-C 0.242，HellaSwag 0.319，MMLU 0.231 ——直接崩了</li> <li><strong>Usage top-k</strong>（只看路由频率）：HellaSwag 掉到 0.586，比较弱</li> <li><strong>REAP top-k</strong>（基于贡献分）：HellaSwag 回到 0.718，接近 ConMoE 默认</li> <li><strong>Adaptive prototype</strong>（ConMoE 默认）：HellaSwag 0.716，与 REAP top-k 相当</li> </ul> <p><strong>路由条件贡献分是主导信号</strong>，可替换性是辅助正则化（防止选出重复的 prototype），单独用距离作为选择标准完全行不通。</p> <h3 id="73-事后权重融合诊断">7.3 事后权重融合诊断</h3> <p>有人会问：选好 prototype 之后，能不能再做一下权重融合（把分配给同一 prototype 的那些原始专家的权重平均一下），效果会不会更好？</p> <p>如下表给出了答案：</p> <p><img src="/assets/img/posts/2605_29350/table4_p7.png" alt="事后权重融合诊断"/></p> <p>对比三种方案：</p> <ul> <li><strong>None（ConMoE 默认）</strong>：不做任何融合，ARC-C 0.504，HellaSwag 0.716，MMLU 0.639</li> <li><strong>Arcee fusion</strong>：HellaSwag 从 0.716 直接掉到 0.531，MMLU 从 0.639 掉到 0.512</li> <li><strong>Weighted average</strong>：更惨，HellaSwag 0.469，MMLU 0.358</li> </ul> <p><strong>ConMoE 的收益来自确定性重映射，而不是权重融合</strong>。直接复用原始的 pretrained prototype 才是最稳的策略，加了融合反而破坏了原始权重。</p> <hr/> <h2 id="8-跨层专家可替换性分析">8. 跨层专家可替换性分析</h2> <p>为什么跨层 scope 有效，但又不能太大？论文专门做了一个几何分析。</p> <p>在 Qwen3-30B-A3B 上，用 4 层 scope 计算每个 expert 在参数空间的最近邻，统计最近邻是否在同一层还是不同层：</p> <p><img src="/assets/img/posts/2605_29350/figure3_p8.png" alt="跨层最近邻分析"/></p> <p>左图是热力图，展示从 source layer 到最近邻 layer 的分布。可以看到主要沿对角线集中，说明最近邻大概率在附近的层，而非全局随机分布。</p> <p>右图是每层中”最近邻在不同层”的比例。<strong>整体上有 50.4% 的 expert，其参数空间的最近邻在另一层</strong>，部分中间层超过 70%。</p> <p>这两个观察共同说明：<strong>专家的可替换性不是严格层内的，相邻层之间存在真实的冗余</strong>——这是跨层共享 prototype 的几何依据。但这种冗余是局部的，不支持全局共享。</p> <hr/> <h2 id="9-总结">9. 总结</h2> <p>ConMoE 提供了一个很干净的框架来思考 MoE 压缩：</p> <ul> <li><strong>不删</strong>（pruning 的做法）</li> <li><strong>不改权重</strong>（merging 的做法）</li> <li><strong>只重映射</strong>：选出 pretrained 专家子集作为 prototype，把其他专家的引用指过去</li> </ul> <p>三个设计原则从实验里验证：</p> <ol> <li><strong>确定性重映射是最稳定的成分</strong>，单层内做都已经很强</li> <li><strong>跨层复用要保持局部</strong>，相邻层可以，太远的层会因深度不匹配掉性能</li> <li><strong>路由条件贡献分是主导信号</strong>，可替换性是辅助的正则化</li> </ol> <p>个人觉得这篇论文最有价值的点，是把”保留哪些专家参数”和”原来的 expert slot 怎么被表示”这两个问题<strong>显式地解耦</strong>了。这个视角在之前的 pruning/merging 框架里是隐含的，ConMoE 把它明确化之后，整个设计空间就清晰多了。</p> <p>当然 limitation 也直接：这套方案依赖 calibration 数据，跨层 scope 不能太大，而且现在的”logical reduction ratio”和实际的显存节省还需要专门的 runtime 支持（shared-prototype checkpoint 格式）才能落地。最后这点是整个 prototype sharing 方案的工程挑战，不只是这篇论文的问题。</p> <p>感兴趣的可以去翻原文，代码和复现脚本作者也有提到会开源。</p> <p>欢迎评论区交流，有什么遗漏或者理解有误的地方也欢迎指出~</p>]]></content><author><name></name></author></entry><entry><title type="html">UPenn &amp;amp; Meta | NF-CoT：当 LLM 的思维链不再是文字，而是连续概率流</title><link href="https://marsggbo.github.io/blog/2026/20260612-nfcot/" rel="alternate" type="text/html" title="UPenn &amp;amp; Meta | NF-CoT：当 LLM 的思维链不再是文字，而是连续概率流"/><published>2026-06-12T00:00:00+00:00</published><updated>2026-06-12T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260612-nfcot</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260612-nfcot/"><![CDATA[<blockquote> <p>插播：之前写的<a href="https://item.jd.com/14945889.html">《动手学 AutoML》</a>终于出版了，从 NAS 到超参优化都有覆盖，适合想系统入门 AutoML 的同学。好了广告结束，现在进入正题。</p> <p><img src="/assets/img/book_cover_automl.png" alt="动手学AutoML书籍封面"/></p> </blockquote> <h1 id="nf-cot当-llm-的思维链不再是文字而是连续概率流">NF-CoT：当 LLM 的思维链不再是文字，而是连续概率流</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2606.06447">Latent Reasoning with Normalizing Flows</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有想过一个问题：大模型在”思考”的时候，为什么非得一个字一个字把推理过程说出来？</p> <p>Chain-of-Thought（CoT）确实让模型变聪明了，但代价也很离谱——<strong>每一步推理都必须变成人话才能继续</strong>。即使模型内部只是做了一个简单的语义更新，它也得把这个更新”翻译”成一串 token 输出到序列里，然后才能看到自己刚才在想什么。</p> <p>这就好比：你做数学题时，每写一步草稿都要先把它翻译成完整的中文句子，才能继续往下算。效率当然低。</p> <p>那有没有办法让模型<strong>在连续空间里”想”</strong>，想完了直接输出答案？这就是 latent reasoning（潜在推理）想做的事。但之前的 latent reasoning 方法各有各的问题——有的是确定性的（Coconut，没有概率分布，只有一条路径），有的需要迭代去噪（LaDiR，用 diffusion model，慢）。</p> <p>今天介绍的这篇 NF-CoT，来自 UPenn + UC San Diego + Meta，给了一个很漂亮的解：<strong>用 Normalizing Flow 在 LLM 内部建模连续思维链的概率分布</strong>，既有精确的 likelihood，又能像普通 token 一样从左到右采样，还能复用 KV cache。代码推理任务上比 LaDiR 强 7%，推理速度快 1.92 倍。</p> <hr/> <h2 id="2-背景铺垫cot-的四种范式">2. 背景铺垫：CoT 的四种范式</h2> <p>在讲 NF-CoT 之前，先帮大家理清当前 CoT 推理的四种主流范式，如下图：</p> <p><img src="/assets/img/posts/20260612-nfcot/fig1_paradigms.png" alt="四种 CoT 推理范式对比"/></p> <h3 id="21-explicit-cot显式思维链">2.1 Explicit CoT（显式思维链）</h3> <p>最经典的做法，模型把推理过程逐步写出来：Q → d₁ → d₂ → … → dₗ → A。每个 d 都是离散 token，可以采样、打分、用 KV cache。但问题是：<strong>文本是一种信息密度极低的”思考”介质</strong>，很多内部计算用自然语言表达既冗余又不自然。</p> <h3 id="22-coconut确定性隐状态">2.2 Coconut（确定性隐状态）</h3> <p>把推理过程压缩到 hidden state 里，直接回传给模型作为”上下文”。但问题是：<strong>这些 hidden state 是确定性的，不是概率分布</strong>。后续研究发现它实际上只走一条路径（single-threaded），不能表达多种推理策略。</p> <h3 id="23-ladirdiffusion-去噪-latent">2.3 LaDiR（Diffusion 去噪 latent）</h3> <p>用 VAE 把 CoT 编码成连续向量，再用 diffusion model 从噪声去噪生成 latent。有概率性，但<strong>需要 30 步迭代去噪</strong>，推理时很慢，也丧失了 LLM 原生的从左到右 likelihood 接口。</p> <h3 id="24-nf-cot本文">2.4 NF-CoT（本文）</h3> <p>用 Normalizing Flow 建模连续思维 token，在 LLM 内部从左到右自回归采样，有精确 likelihood。集四家之长：<strong>概率性 + 自回归 + KV cache 兼容 + 可精确计算 log-likelihood</strong>。</p> <hr/> <h2 id="3-先聊聊-normalizing-flow-是什么">3. 先聊聊 Normalizing Flow 是什么</h2> <p>如果你对 Normalizing Flow（NF）不太熟，这里简单过一下核心思想。</p> <p>NF 的本质是：<strong>用一系列可逆变换，把一个简单的分布（如标准高斯）映射到一个复杂的目标分布</strong>。因为变换可逆，你既可以从简单分布采样（生成），也可以把数据点映射回简单分布（计算 likelihood）。</p> <p>具体来说，如果有一个可逆函数 f⁻¹ 把数据 y 映射到高斯噪声 z：</p> <ul> <li>z = f⁻¹(y; c)</li> <li> <table> <tbody> <tr> <td>p(y</td> <td>c) = p(z) ·</td> <td>det J_{f⁻¹}(y;c)</td> </tr> </tbody> </table> </li> </ul> <p>其中 J 是 Jacobian 矩阵。只要 f 可逆且 Jacobian 行列式可算，你就同时拥有了：</p> <ol> <li> <table> <tbody> <tr> <td><strong>精确 likelihood</strong>（可以直接算 log p(y</td> <td>c)）</td> </tr> </tbody> </table> </li> <li><strong>直接采样</strong>（从 z~N(0,I) 采样后通过 f 变换得到 y）</li> </ol> <p>TARFlow / STARFlow 是近期把 NF 扩展到高维序列数据的工作，用 Transformer 做自回归 flow——每个位置的变换只依赖之前的位置，Jacobian 是三角矩阵（行列式好算），天然和 causal attention 兼容。</p> <p><strong>NF-CoT 正是把这种自回归 flow “植入”到 LLM 的 causal stream 里</strong>，让连续思维 token 拥有和文本 token 一样的建模接口。</p> <hr/> <h2 id="4-nf-cot-的架构与训练">4. NF-CoT 的架构与训练</h2> <p>如下图是 NF-CoT 的训练和推理流程：</p> <p><img src="/assets/img/posts/20260612-nfcot/fig2_pipeline.png" alt="NF-CoT 训练与推理流程"/></p> <h3 id="41-整体思路">4.1 整体思路</h3> <p>NF-CoT 做了一件很巧妙的事：<strong>把 LLM 的 backbone 本身当作 deep autoregressive flow 的一部分</strong>。</p> <p>具体来说，NF-CoT 包含两个 flow 组件：</p> <ol> <li> <p><strong>Shallow Flow Blocks</strong>（浅层 flow 块）：5 个轻量的 MetaBlock，负责把 VAE 编码空间 e 重新参数化到一个更适合自回归建模的空间 u。这是一个可逆映射 u = F(e; q)。</p> </li> <li> <p><strong>Deep Flow</strong>（深层 flow）：就是 LLM backbone 本身。连续思维 u₁:K 被投影到 token embedding 维度后，跟答案 token 一起放进同一个 causal stream 处理。在连续思维位置，NF head 输出高斯参数 (μ, σ)；在文本位置，LM head 输出 token logits。</p> </li> </ol> <h3 id="42-训练目标">4.2 训练目标</h3> <p>训练时，先用一个冻结的 VAE encoder 把显式 CoT 编码成连续 target e₁:K（64 个 latent slot，维度 2560）。然后优化一个联合目标：</p> <p><strong>L = λ_flow · L_flow + λ_text · L_text</strong></p> <ul> <li>L_flow：NF 精确 likelihood，给连续思维分配概率</li> <li>L_text：标准 cross-entropy，给答案 token 分配概率</li> </ul> <p>两个 loss 在同一个 causal forward pass 里算出来——<strong>flow head 打分连续思维，LM head 打分答案 token</strong>，共享一个 backbone。</p> <h3 id="43-两阶段训练课程">4.3 两阶段训练课程</h3> <ul> <li><strong>Stage 1</strong>：冻住 LLM backbone，只训练 shallow flow blocks 和投影层。让 flow 组件先学会一个合理的 latent 接口。</li> <li><strong>Stage 2</strong>：解冻所有参数，端到端联合训练。</li> </ul> <p>Ablation 表明，跳过 Stage 1 直接端到端训练会掉 2-3 个点——因为 backbone 会被随机初始化的 flow 组件发出的”噪声梯度”干扰。</p> <h3 id="44-推理">4.4 推理</h3> <p>推理时最优雅的地方：<strong>shallow flow blocks 完全不需要运行</strong>。模型直接在 u 空间从左到右采样：</p> <ol> <li>给定 prompt q，从自回归高斯密度逐步采样连续思维 ũ₁:K</li> <li>采样完成后，切换到 LM head，在同一个 causal stream 里继续解码答案 token</li> <li><strong>KV cache 无缝复用</strong>——因为连续思维和答案 token 共享同一个 backbone</li> </ol> <p>不需要迭代去噪（LaDiR 需要 30 步），不需要额外的 prefix recomputation。一次性从左到右过完。</p> <hr/> <h2 id="5-强化学习在连续空间做-policy-gradient">5. 强化学习：在连续空间做 Policy Gradient</h2> <p>NF-CoT 的一个核心优势是<strong>连续思维有精确的 log-likelihood</strong>，这意味着可以直接对 latent 推理路径做 policy gradient 优化。</p> <p>具体做法是 GRPO 风格的 RL：对每个 prompt 采样一组 trajectory (ũ, x̂)，用执行奖励（代码通过单元测试 = reward 1，否则 = 0）计算 group-normalized advantage，然后用 PPO 的 clipped surrogate 更新：</p> <ul> <li><strong>Token PPO</strong>：标准 token-level 策略梯度（更新 LM head 的答案解码）</li> <li><strong>Latent PPO</strong>：sequence-level 策略梯度（更新 NF head 的连续思维采样）</li> </ul> <p>关键发现：<strong>token-space GRPO 会导致 pass@k collapse</strong>（pass@1 提了，但 pass@128 不涨甚至下降），而 <strong>latent-space RL 不会</strong>。如下图对比：</p> <p><img src="/assets/img/posts/20260612-nfcot/fig4_rl_diversity.png" alt="RL 前后 pass@k 对比"/></p> <p>原因很直观：token-space RL 会让模型把概率集中在少数”正确模板”上；而 latent-space RL 在连续空间做优化，不同的 latent 采样可以 steer 到不同的实现策略，多样性被保留。</p> <hr/> <h2 id="6-实验结果">6. 实验结果</h2> <h3 id="61-主结果代码生成-pass1">6.1 主结果：代码生成 pass@1</h3> <p>基于 Qwen3-8B-Base backbone，在 5 个代码生成 benchmark 上的结果：</p> <table> <thead> <tr> <th>方法</th> <th>MBPP</th> <th>MBPP+</th> <th>HumanEval</th> <th>HumanEval+</th> <th>LCB v6</th> <th>平均</th> </tr> </thead> <tbody> <tr> <td>Base Model</td> <td>60.5</td> <td>53.8</td> <td>78.2</td> <td>68.6</td> <td>17.7</td> <td>55.8</td> </tr> <tr> <td>Standard SFT</td> <td>63.3</td> <td>52.7</td> <td>84.6</td> <td>69.5</td> <td>-</td> <td>-</td> </tr> <tr> <td>LaDiR</td> <td>66.8</td> <td>59.5</td> <td>87.4</td> <td>73.2</td> <td>21.3</td> <td>61.6</td> </tr> <tr> <td><strong>NF-CoT (Unified)</strong></td> <td><strong>83.9</strong></td> <td><strong>72.1</strong></td> <td><strong>85.8</strong></td> <td><strong>78.3</strong></td> <td><strong>23.7</strong></td> <td><strong>68.8</strong></td> </tr> <tr> <td><strong>NF-CoT + RL</strong></td> <td><strong>85.4</strong></td> <td><strong>73.3</strong></td> <td><strong>86.7</strong></td> <td><strong>80.2</strong></td> <td><strong>25.1</strong></td> <td><strong>70.1</strong></td> </tr> </tbody> </table> <p>几个关键数字：</p> <ul> <li>NF-CoT (Unified) 比 base model <strong>+13.0%</strong>，比 LaDiR <strong>+7.1%</strong></li> <li>加上 RL 后进一步到 <strong>+14.3%</strong></li> <li>比 OlympicCoder（目前最强的开源自回归代码模型之一）还高 0.3%</li> </ul> <h3 id="62-passk-扩展性">6.2 pass@k 扩展性</h3> <p>如下图，NF-CoT 在 MBPP+ 上 pass@1 (72.1) 就已经 match 了 base model 的 pass@128 (72.0)。随着 k 增大，NF-CoT 的 pass@k 持续上升到 87.5（k=128），而 LaDiR 在 90.2 就开始饱和。</p> <p><img src="/assets/img/posts/20260612-nfcot/fig3_passk.png" alt="pass@k 扩展对比"/></p> <p>这说明 flow noise 采样确实产生了<strong>结构性多样</strong>的连续思维路径，而非坍缩到单一模式。</p> <h3 id="63-推理效率">6.3 推理效率</h3> <p>这是我最关心的部分——都是在 HumanEval 上，16 candidates/problem，Qwen3-8B backbone，vLLM 解码：</p> <table> <thead> <tr> <th>方法</th> <th>Latent 生成 (s)</th> <th>解码 (s)</th> <th>总时间 (s)</th> <th>FLOPs/样本</th> </tr> </thead> <tbody> <tr> <td><strong>NF-CoT (Unified)</strong></td> <td><strong>173.5</strong></td> <td>152.1</td> <td><strong>325.6</strong></td> <td><strong>19.9T</strong></td> </tr> <tr> <td>LaDiR</td> <td>468.2</td> <td>157.1</td> <td>625.3</td> <td>49.3T</td> </tr> </tbody> </table> <ul> <li>Latent 生成快 <strong>2.70×</strong>（自回归一次 pass vs. 30 步去噪）</li> <li>总体快 <strong>1.92×</strong></li> <li>Per-sample compute 省 <strong>2.48×</strong></li> </ul> <p>训练同样显著：NF-CoT 比 LaDiR sample throughput 高 2.85×，total FLOPs 低 6.66×。</p> <h3 id="64-压缩率">6.4 压缩率</h3> <p>64 个 latent token 平均编码 385 个文本 token 的 CoT，<strong>有效压缩率约 6.0×</strong>。也就是说，模型用 64 步连续”想法”做完了原本需要 385 个 token 才能表达的推理。</p> <hr/> <h2 id="7-一个有意思的分析latent-空间是平滑的">7. 一个有意思的分析：latent 空间是平滑的</h2> <p>论文做了一个 perturbation 实验：对采样出的连续思维加高斯噪声 ε，然后看 pass@1 怎么变。</p> <p>当噪声 σ 从 0 涨到 3.0 时：</p> <ul> <li>cosine similarity 从 1.000 掉到 0.116（几乎正交了）</li> <li>但 <strong>pass@1 只从 86.0 掉到 83.6</strong>（置信区间重叠）</li> <li>exact-text match 从 0.973 掉到 0.278</li> </ul> <p>这说明什么？<strong>NF-CoT 学到的 latent 空间是局部平滑的</strong>。扰动改变的是生成程序的”表面形式”（用哪种算法实现、变量名怎么起），而不是”能否正确解题”。连续思维更像是<strong>分布式控制变量</strong>——控制走哪条路，而非能否到达目的地。</p> <hr/> <h2 id="8-我的-take">8. 我的 Take</h2> <p>作为一个做 LLM 推理效率的研究者，我觉得这篇工作有几个点值得关注：</p> <p><strong>1. 把 NF 和 LLM 统一到一个 causal stream 里是一个很优雅的设计</strong>。之前 latent reasoning 的方案要么跟 LLM 的推理接口割裂（diffusion 需要额外模型），要么损失概率性（Coconut 类方法）。NF-CoT 的统一架构让 latent 和 text 共享一个 backbone + KV cache，既干净又高效。</p> <table> <tbody> <tr> <td><strong>2. 精确 likelihood 打开了 RL 的大门</strong>。这是相比 LaDiR 的核心优势——有了 tractable log p(u</td> <td>q)，policy gradient 就可以直接作用于连续推理空间，而不只是答案 token。更重要的是，这种 RL 不会导致 pass@k collapse，这对 best-of-N 采样场景很关键。</td> </tr> </tbody> </table> <p><strong>3. 6x 压缩 + 2x 加速的组合很实用</strong>。64 个 latent token 替代 385 个文本 token，加上不需要迭代去噪，实际部署时对 KV cache 内存和推理延迟都有直接好处。</p> <p><strong>局限性也很明显</strong>：目前只在代码生成上验证，对数学推理、自然语言推理等场景还没有数据。另外 latent 不可解释——解码出来的”思维链”只能作为定性 probe，不能当作 faithful explanation。</p> <p>总的来说，NF-CoT 展示了一条很有前景的路线：<strong>用概率生成模型（而非确定性隐状态）来做 latent reasoning</strong>，且不牺牲 LLM 原生的自回归接口。期待后续在更多推理任务上的验证。</p> <hr/> <p>欢迎评论区交流。如果你也在做 LLM 推理效率相关的工作，可以聊聊你怎么看 latent reasoning 这个方向。</p>]]></content><author><name></name></author><category term="LLM"/><category term="推理优化"/><category term="Normalizing Flow"/><category term="论文解读"/><summary type="html"><![CDATA[插播：之前写的《动手学 AutoML》终于出版了，从 NAS 到超参优化都有覆盖，适合想系统入门 AutoML 的同学。好了广告结束，现在进入正题。]]></summary></entry><entry><title type="html">ICML’26 | Transformer 真的需要三个投影矩阵吗？Q-K=V 让 KV Cache 直接砍半</title><link href="https://marsggbo.github.io/blog/2026/20260612-qkv-sharing/" rel="alternate" type="text/html" title="ICML’26 | Transformer 真的需要三个投影矩阵吗？Q-K=V 让 KV Cache 直接砍半"/><published>2026-06-12T00:00:00+00:00</published><updated>2026-06-12T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260612-qkv-sharing</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260612-qkv-sharing/"><![CDATA[<blockquote> <p>插播：之前写的<a href="https://item.jd.com/14945889.html">《动手学 AutoML》</a>终于出版了，从 NAS 到超参优化都有覆盖，适合想系统入门 AutoML 的同学。好了广告结束，现在进入正题。</p> <p><img src="/assets/img/book_cover_automl.png" alt="动手学AutoML书籍封面"/></p> </blockquote> <h1 id="transformer-真的需要三个投影矩阵吗q-kv-让-kv-cache-直接砍半">Transformer 真的需要三个投影矩阵吗？Q-K=V 让 KV Cache 直接砍半</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2606.04032">Do Transformers Need Three Projections? Systematic Study of QKV Variants</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有想过，Transformer 里每个 attention head 都要算 Q、K、V 三个投影，<strong>这三个真的缺一不可吗？</strong></p> <p>CNN 用的是共享卷积核，SSM（Mamba）用的是统一的状态表示，为什么独独 Transformer 非得维护三份独立的投影矩阵？要知道，在 LLM 推理的时候，K 和 V 需要全量缓存（KV Cache），这玩意儿在长上下文场景下是真正的显存杀手——32k context 下一个用户就要吃掉 2.6 GB。</p> <p>今天想和大家聊聊这篇 ICML 2026 的工作。作者提了一个很朴素的问题：<strong>如果我们让 K 和 V 共享同一个投影矩阵（K=V），会怎样？</strong> 答案让人惊喜——perplexity 只涨了 3.1%，但 KV Cache 直接砍半。更狠的是，这个方法和 GQA/MQA 正交，两者叠加可以把 cache 压到只剩 3.1%。</p> <h2 id="2-核心思路哪个投影矩阵可以省">2. 核心思路：哪个投影矩阵可以省？</h2> <p>标准 attention 的公式大家都熟：</p> \[A = \text{Softmax}(\alpha \cdot Q K^T) V\] <p>其中 $Q = XW_q$，$K = XW_k$，$V = XW_v$，三个投影矩阵各管各的。作者系统性地测试了三种”省矩阵”的方案：</p> <p><img src="/assets/img/posts/20260612-qkv-sharing/fig1_qkv_variants.jpg" alt="QKV共享方案总览"/></p> <p><strong>方案 1：Q=K-V</strong>（合并 Q 和 K，保留独立 V）</p> \[A = \text{Softmax}(\alpha \cdot K K^T) V\] <p>问题：$KK^T$ 是对称矩阵，attention map 变成对称的了。对于需要因果关系的语言模型来说，<strong>方向性被破坏了</strong>。</p> <p><strong>方案 2：Q-K=V</strong>（保留独立 Q，合并 K 和 V）</p> \[A = \text{Softmax}(\alpha \cdot Q K^T) K\] <p>注意这里 V 直接用 K 代替。$QK^T$ 仍然是非对称的，attention 的方向性完全保留。</p> <p><strong>方案 3：Q=K=V</strong>（三合一，只有一个投影矩阵）</p> \[A = \text{Softmax}(\alpha \cdot K K^T) K\] <p>最极端的简化——既对称又共享 value，约束太强。</p> <p><strong>直觉上哪个能行？</strong> 想想 KV Cache 的工作原理：推理时需要缓存每个 token 的 K 和 V 向量。如果 K=V（方案 2），那我们只需要缓存 K 就行了——<strong>KV Cache 直接减半</strong>。而方案 1（Q=K）虽然也省了一个矩阵，但推理时还是得分别存 K 和 V，<strong>没有任何推理收益</strong>。</p> <p>这也是整个工作<strong>最核心的 insight</strong>：不是所有投影共享都能带来推理加速，<strong>只有共享 K 和 V 才能直接转化为 KV Cache 的减少</strong>。</p> <h2 id="3-为什么-kv-能-work">3. 为什么 K=V 能 work？</h2> <p>作者分析了训练好的标准 QKV 模型，发现一个有意思的现象：</p> <ul> <li>K 和 V 投影矩阵的 <strong>cosine similarity 高达 0.73</strong></li> <li>K 和 V 的 effective rank 也很接近（687 vs 702，总维度 1024）</li> <li>相比之下，Q 和 K 的 cosine similarity 只有 0.42，Q 和 V 更低只有 0.31</li> </ul> <p><strong>说人话：K 和 V 本来就在学很相似的表示空间</strong>。强制让它们共享一个投影矩阵，只是把一个已经存在的”软约束”变成了”硬约束”，质量损失自然很小。</p> <p>而 Q 和 K 的表示空间差异大得多——Q 负责”我要查什么”，K 负责”我是什么”，这两个角色本质上需要不对称性来支撑 attention 的方向性。合并 Q 和 K 会把 attention map 变对称，对因果语言模型是致命的。</p> <h2 id="4-实验从-300m-到-12b-全面验证">4. 实验：从 300M 到 1.2B 全面验证</h2> <p>作者用 SlimPajama 数据集训练了 300M 和 1.2B 参数的 GPT 风格模型（10B tokens），实验非常扎实。</p> <h3 id="41-300m-模型主实验">4.1 300M 模型主实验</h3> <table> <thead> <tr> <th>模型</th> <th>Val PPL</th> <th>PPL 退化</th> <th>KV Cache 缩减</th> </tr> </thead> <tbody> <tr> <td>QKV（baseline）</td> <td>5.11</td> <td>—</td> <td>0%</td> </tr> <tr> <td><strong>Q-K=V</strong></td> <td><strong>5.27</strong></td> <td><strong>+3.1%</strong></td> <td><strong>50%</strong></td> </tr> <tr> <td>Q=K-V</td> <td>5.36</td> <td>+4.9%</td> <td>0%</td> </tr> <tr> <td>Q=K=V</td> <td>6.41</td> <td>+25.4%</td> <td>50%</td> </tr> <tr> <td>GQA-4</td> <td>5.15</td> <td>+0.7%</td> <td>75%</td> </tr> <tr> <td>MQA</td> <td>5.19</td> <td>+1.5%</td> <td>93.8%</td> </tr> <tr> <td><strong>Q-GQA-4</strong></td> <td><strong>5.32</strong></td> <td><strong>+3.9%</strong></td> <td><strong>87.5%</strong></td> </tr> <tr> <td><strong>Q-MQA</strong></td> <td><strong>5.36</strong></td> <td><strong>+4.8%</strong></td> <td><strong>96.9%</strong></td> </tr> </tbody> </table> <p>几个关键发现：</p> <ol> <li><strong>Q-K=V 是投影共享方案里的明确赢家</strong>：3.1% 的 perplexity 退化换来 50% 的 cache 缩减</li> <li><strong>Q=K-V 看起来质量还行（+4.9%），但没有任何推理收益</strong>——因为它还是得缓存 K 和 V</li> <li><strong>Q=K=V 直接崩了</strong>（+25.4%），三合一太激进</li> <li><strong>投影共享和 head sharing 正交且可叠加</strong>：Q-K=V + MQA 直接把 cache 压到只剩 3.1%</li> </ol> <p><img src="/assets/img/posts/20260612-qkv-sharing/fig8_projection_sharing.png" alt="投影共享对比"/></p> <p><img src="/assets/img/posts/20260612-qkv-sharing/fig9_head_sharing_combined.png" alt="Head sharing 叠加效果"/></p> <h3 id="42-12b-模型验证">4.2 1.2B 模型验证</h3> <table> <thead> <tr> <th>模型</th> <th>Val PPL</th> <th>PPL 退化</th> <th>KV Cache (32K ctx)</th> </tr> </thead> <tbody> <tr> <td>QKV</td> <td>5.004</td> <td>—</td> <td>5,900 MB</td> </tr> <tr> <td>Q-K=V</td> <td>5.128</td> <td>+2.48%</td> <td>2,950 MB</td> </tr> <tr> <td>GQA-8</td> <td>5.030</td> <td>+0.52%</td> <td>1,408 MB</td> </tr> <tr> <td>MQA</td> <td>5.057</td> <td>+1.06%</td> <td>176 MB</td> </tr> <tr> <td>Q-GQA-8</td> <td>5.158</td> <td>+3.08%</td> <td>704 MB</td> </tr> <tr> <td>Q-MQA</td> <td>5.212</td> <td>+4.16%</td> <td>88 MB</td> </tr> </tbody> </table> <p>1.2B 的结果更让人兴奋——<strong>Q-K=V 的退化从 300M 的 3.1% 降到了 2.48%</strong>，模型越大对投影约束越鲁棒。如果这个趋势在 7B+ 继续保持，那投影共享对大规模生产模型就更有吸引力了。</p> <p>Q-MQA 的数字尤其夸张：<strong>只用 88 MB 的 KV Cache 就能服务 32K context</strong>，相比 QKV baseline 的 5.9 GB 压缩了 67 倍。对 batch=32 的并发场景，从 189 GB 降到 2.8 GB——这是”能不能跑”和”跑不起来”的区别。</p> <h3 id="43-downstream-任务不掉点">4.3 Downstream 任务不掉点</h3> <p>更实用的发现：虽然 Q-K=V 的 perplexity 比 baseline 差了 2.48%，但在 5-shot downstream 评测（HellaSwag、PIQA、ARC 等）上，<strong>平均准确率只掉了 0.41%</strong>（35.99% vs 36.40%）。Perplexity 的小幅退化并没有转化为下游任务上的等比损失。</p> <h2 id="5-pareto-前沿投影共享填补了效率-质量的空白">5. Pareto 前沿：投影共享填补了效率-质量的空白</h2> <p><img src="/assets/img/posts/20260612-qkv-sharing/fig10_pareto.png" alt="效率-质量 Pareto 前沿"/></p> <p>这张图把所有方案放在一起看就很清楚了：</p> <ul> <li><strong>GQA/MQA</strong> 在右上角——高 cache 压缩、低质量损失</li> <li><strong>Q-K=V 填补了 QKV baseline 和 GQA 之间的空白</strong>——提供了一个 50% cache reduction 的中间选项</li> <li><strong>组合方案</strong>（Q-GQA、Q-MQA）进一步推高了效率前沿</li> </ul> <p>作者也给出了部署建议：</p> <table> <thead> <tr> <th>场景</th> <th>推荐方案</th> <th>Cache 缩减</th> <th>PPL 退化</th> </tr> </thead> <tbody> <tr> <td>云端（质量优先）</td> <td>GQA-4</td> <td>75%</td> <td>+0.7%</td> </tr> <tr> <td>边缘（平衡）</td> <td>Q-K=V</td> <td>50%</td> <td>+3.1%</td> </tr> <tr> <td>边缘（激进）</td> <td>Q-GQA-4</td> <td>87.5%</td> <td>+3.9%</td> </tr> <tr> <td>IoT/移动端</td> <td>Q-MQA</td> <td>96.9%</td> <td>+4.8%</td> </tr> </tbody> </table> <h2 id="6-训练曲线稳不稳">6. 训练曲线稳不稳？</h2> <p><img src="/assets/img/posts/20260612-qkv-sharing/fig11_val_curves_300m.png" alt="300M 验证曲线"/></p> <p><img src="/assets/img/posts/20260612-qkv-sharing/fig12_val_curves_1b.png" alt="1.2B 验证曲线"/></p> <p>值得一提的是所有变体训练都很稳定——<strong>不需要特殊的初始化、学习率调度或架构修改</strong>，直接把 attention 里的投影矩阵改一下就行。训练速度也基本没差（423k-460k tokens/s），Q=K=V 因为少算了投影甚至稍微快一点点。</p> <h2 id="7-和-deepseek-v2-mla-的区别">7. 和 DeepSeek-V2 MLA 的区别</h2> <p>看到 K=V 你可能会想到 DeepSeek-V2 的 Multi-Head Latent Attention（MLA）。两者都是为了压 KV Cache，但路线完全不同：</p> <ul> <li><strong>MLA</strong>：把 K 和 V 压缩到一个共享的低维 latent 向量，推理时再展开。K 和 V 功能上仍然独立，只是压缩了存储。代价是额外的 up-projection 参数。</li> <li><strong>Q-K=V</strong>：直接硬约束 K=V，不需要额外参数，简单粗暴。</li> </ul> <p>两者是正交的思路，理论上甚至可以组合——先用 MLA 压缩，再在压缩后的 latent 里做 K=V 共享。</p> <h2 id="8-我的-take">8. 我的 take</h2> <p>这篇文章的亮点不是方法有多 fancy（让 K=V 这件事本身并不复杂），而是<strong>系统性地回答了一个基础问题</strong>：Transformer 的三个投影矩阵到底各自贡献了什么？</p> <p>几个我觉得有意思的点：</p> <ol> <li> <p><strong>V 的重要性被高估了</strong>。K 已经足够 rich 来同时承担”寻址”和”内容”两个角色。这和 He &amp; Hofmann (ICLR 2024) 说 V 的角色没那么关键是一致的。</p> </li> <li> <p><strong>投影共享和 head sharing 是完全正交的优化轴</strong>。以前大家只在”减少 KV head 数量”这一个维度上使劲（GQA、MQA），这篇文章开辟了另一个维度。两者叠加的效果是乘法关系，不是加法。</p> </li> <li> <p><strong>这对已有模型可能有 fine-tuning 的意义</strong>。虽然论文是 from scratch 训练的，但既然预训练好的模型里 K 和 V 本身就高度相似（cosine similarity 0.73），那是不是可以 post-hoc 做 K=V merging + 少量 fine-tune？值得探索。</p> </li> <li> <p><strong>实际部署时的收益</strong>：32K context 下每用户省 1.31 GB，batch=32 省 42 GB，折算下来一年省 $72K 的 GPU 成本。这种数字对工程团队很有说服力。</p> </li> </ol> <p>总的来说，方法简单、实验扎实、insight 清晰。<strong>做 LLM 推理效率优化的同学建议读一读，尤其是同时做 serving 优化的。</strong> 把 Q-K=V 加到现有的 GQA 模型里可能是一个低成本高收益的事情。</p>]]></content><author><name></name></author><category term="LLM"/><category term="Attention"/><category term="KV Cache"/><category term="推理优化"/><category term="论文解读"/><summary type="html"><![CDATA[插播：之前写的《动手学 AutoML》终于出版了，从 NAS 到超参优化都有覆盖，适合想系统入门 AutoML 的同学。好了广告结束，现在进入正题。]]></summary></entry><entry><title type="html">arXiv’26 | FlashMemory-DeepSeek-V4：用 13.5% 的显存干 100% 的活，超长上下文推理的 less is more</title><link href="https://marsggbo.github.io/blog/2026/20260611-flashmemory/" rel="alternate" type="text/html" title="arXiv’26 | FlashMemory-DeepSeek-V4：用 13.5% 的显存干 100% 的活，超长上下文推理的 less is more"/><published>2026-06-11T00:00:00+00:00</published><updated>2026-06-11T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260611-flashmemory</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260611-flashmemory/"><![CDATA[<h1 id="flashmemory-deepseek-v4用-135-的显存干-100-的活超长上下文推理的-less-is-more">FlashMemory-DeepSeek-V4：用 13.5% 的显存干 100% 的活，超长上下文推理的 less is more</h1> <blockquote> <p>插播：之前写的<a href="https://item.jd.com/14945889.html">《动手学 AutoML》</a>终于出版了，从 NAS 到超参优化都有覆盖，适合想系统入门 AutoML 的同学。好了广告结束，现在进入正题。</p> <p><img src="https://github.com/marsggbo/marsggbo.github.io/blob/master/assets/img/book_cover_automl.png?raw=true" alt="动手学AutoML书籍封面"/></p> </blockquote> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2606.09079">FlashMemory-DeepSeek-V4: Lightning Index Ultra-Long Context via Lookahead Sparse Attention</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有想过，当一个 LLM 在处理一段 500K token 的超长上下文时，<strong>GPU 上那些 KV cache 里有多少是真正被用到的？</strong></p> <p>答案是：<strong>90% 以上的请求，光靠最后 8K token 就能准确回答。</strong></p> <p>这就是这个领域最核心的矛盾——你为了应对那 10% 的”真需要回忆全文”的情况，不得不把整个 KV cache 全量加载到 GPU 显存里。500K token 的 context，KV cache 动辄 1.8GB+，而大部分时候你只需要其中很小一部分。</p> <p>传统做法要么”全部加载”（浪费显存），要么”sliding window 只看最近的”（丢掉远处关键信息）。这两个极端，都不是答案。</p> <p>今天想和大家聊聊这篇来自腾讯 AI Lab 的工作——<strong>FlashMemory-DeepSeek-V4</strong>——它在 DeepSeek-V4 的架构上做了一个非常优雅的改动：用一个超轻量的 Neural Memory Indexer，<strong>提前预测</strong>未来 64 步需要哪些历史 KV chunk，按需从 CPU 加载到 GPU，其余的全部留在 CPU 冷存储。</p> <p>结果：<strong>GPU 显存占用降到 baseline 的 13.5%，准确率反而还涨了 0.6%。</strong> 对，没看错——”less is more”。500K context 下显存压缩超过 90%。</p> <p>但要读懂这篇工作，你得先搞明白 DeepSeek-V4 本身的 attention 架构设计。这部分我会花较多篇幅讲清楚，因为不懂 V4 的底层，后面的 FlashMemory 就是空中楼阁。</p> <hr/> <h2 id="2-deepseek-v4-的-hybrid-attention-架构">2. DeepSeek-V4 的 Hybrid Attention 架构</h2> <p>先交代下背景。DeepSeek-V4 是 DeepSeek-AI 2026 年发布的最新一代大模型，分两个规格：</p> <ul> <li><strong>DeepSeek-V4-Pro</strong>：1.6T 参数（49B 激活），MoE 架构</li> <li><strong>DeepSeek-V4-Flash</strong>：284B 参数（13B 激活），MoE 架构</li> </ul> <p>两者都支持 <strong>100 万 token</strong> 的上下文窗口。</p> <p>这么长的 context，如果用普通的 full attention，KV cache 的线性增长会直接把 GPU 撑爆。DeepSeek-V4 的核心创新就在于它的 <strong>Hybrid Attention Architecture</strong>——把 attention 层分成两种类型，各自承担不同的任务：</p> <h3 id="21-hcaheavily-compressed-attention">2.1 HCA（Heavily Compressed Attention）</h3> <p>HCA 层的压缩比是 <strong>128:1</strong>。</p> <p>什么意思？假设原始 context 有 128K token，经过 HCA 压缩后，变成大约 1K 个 compressed entry。每个 entry 代表 128 个连续 token 的”语义摘要”。</p> <p>HCA 层的作用是<strong>维持全局上下文感知</strong>——即使在超长序列下，模型也能通过这些高度压缩的 entry 感知到远处发生了什么。代价是精度有损：你知道”远处发生了某件事”，但不知道具体细节。</p> <p>这些 HCA chunk 体积极小（128:1 压缩），<strong>始终保留在 GPU 显存中</strong>，不会被 offload。</p> <h3 id="22-csacompressed-sparse-attention">2.2 CSA（Compressed Sparse Attention）</h3> <p>CSA 层是 DeepSeek-V4 的”精细回忆”通道。</p> <p>和 HCA 不同，CSA 的压缩比低得多（保留更多 token 级细节），但不会对所有历史 token 都做 attention——它用一个 <strong>Lightning Indexer</strong> 来选择性地关注最相关的那些。</p> <p>Lightning Indexer 的工作方式是：</p> <ol> <li>把当前 query token 投射到一组低秩 indexer query 上</li> <li>和所有历史 compressed entry 的 indexer key（$K^{IComp}$）做内积</li> <li>用 ReLU 激活 + Top-$k$ 选取分数最高的若干历史 entry</li> <li>只对这 Top-$k$ 个 entry 做完整的 Multi-Query Attention</li> </ol> <p>这样，CSA 层不需要对全量历史做 attention，只扫描 Top-$k$ 个最相关的 chunk。</p> <h3 id="23-mlamulti-head-latent-attention">2.3 MLA（Multi-head Latent Attention）</h3> <p>DeepSeek-V4 延续了 V3 的 MLA 设计。核心思路是对 query 和 key-value 都做低秩分解：</p> <ul> <li>Query 先经过 down-projection 到一个低维 latent space（<code class="language-plaintext highlighter-rouge">q_lora_rank = 1536</code>），再 up-project 回多头</li> <li>Key-Value 也做了类似的压缩，这样 KV cache 存储的是压缩后的 latent 向量，而不是原始的高维 KV</li> </ul> <p>这个设计直接决定了 DeepSeek-V4 的 KV cache 本身就比标准 MHA 模型小很多——但在 100 万 token 下，即便压缩后，依然是线性增长的。</p> <h3 id="24-整体-kv-cache-开销">2.4 整体 KV cache 开销</h3> <p>在 1M token 上下文设置下，DeepSeek-V4-Pro 相比 V3 只需要：</p> <ul> <li><strong>27% 的单 token 推理 FLOPs</strong></li> <li><strong>10% 的 KV cache</strong></li> </ul> <p>但即便如此，CSA 层的 KV cache 在 500K token 下仍然占据 1.87GB（8×H20 部署环境）。而且这个占用是<strong>不管你当前 token 用不用那些远处的 context，都全部加载</strong>。</p> <p>这就是 FlashMemory 要解决的问题。</p> <hr/> <h2 id="3-flashmemory-的核心思路lookahead-sparse-attention">3. FlashMemory 的核心思路：Lookahead Sparse Attention</h2> <p>FlashMemory 的设计理念是一句话：<strong>不要等用到的时候才去回忆，提前预测未来 64 步需要什么。</strong></p> <p>如下图，左侧是 DeepSeek-V4 原生的 CSA pipeline（黑线），右侧是 FlashMemory 提出的 LSA（Lookahead Sparse Attention）机制（红线）：</p> <p><img src="/assets/img/posts/20260611-flashmemory/crop_fig2_architecture.png" alt="LSA 与 CSA 架构对比"/></p> <p>原生 CSA 的流程是：每一步 decode 都用 Lightning Indexer 做一次 Top-$k$ 选择，从全量 compressed KV 中选出最相关的 entry。这意味着<strong>所有历史 compressed KV 都要常驻 GPU 显存</strong>，否则 Lightning Indexer 没法实时做选择。</p> <p>LSA 的改动看起来很小，但影响是根本性的：</p> <ol> <li>引入一个 <strong>Memory Indexer</strong>，每隔 $\tau = 64$ 个 decoding step 触发一次</li> <li>Memory Indexer 预测：未来 64 步<strong>可能会用到哪些</strong>历史 compressed KV chunk</li> <li>只把这些被预测为”关键”的 chunk 从 CPU cold pool 加载到 GPU</li> <li>在这 64 步内，原生的 Lightning Indexer 在这个<strong>缩小的搜索范围</strong>内做正常的 Top-$k$ attention</li> </ol> <p>这样一来，绝大部分历史 KV cache 就可以安全地留在 CPU 内存里，不占 GPU 显存。</p> <h3 id="31-memory-indexer-的数学">3.1 Memory Indexer 的数学</h3> <p>Memory Indexer 复用了 DeepSeek-V4 原生 Lightning Indexer 的架构（低秩投影 + 内积打分），只做了一个关键改动：</p> <p><strong>把 ReLU + Top-$k$ 换成了 Sigmoid + 阈值。</strong></p> <p>原生 Lightning Indexer 的行为是：</p> <ul> <li>ReLU 激活后取分数最高的 Top-$k$ 个 entry（固定数量）</li> </ul> <p>Memory Indexer 的行为是：</p> <ul> <li>Sigmoid 激活，把分数映射到 $(0, 1)$ 区间</li> <li>设定阈值 $\geq 0.5$ 的所有 entry 都被认为”关键”</li> <li>关键 entry 从 CPU 拉到 GPU</li> </ul> <p>为什么用 Sigmoid + 阈值而不是 Top-$k$？因为不同的 query，真正需要的历史 chunk 数量是<strong>动态变化</strong>的。有些 token 只需要 local context 就够了（0 个远处 chunk），有些 token 需要综合全文（大量 chunk）。Top-$k$ 的固定数量无法表达这种差异。</p> <p>具体公式：给定 decoding step $t$，当前 hidden state $\mathbf{h}_t$，先做低秩投射得到 indexer query：</p> <p>\(\mathbf{c}_t^Q = \mathbf{h}_t \cdot W^{DQ}\) \(\mathbf{q}_t^l = \mathbf{c}_t^Q \cdot W^{IUQ}\)</p> <p>同时计算动态 head routing weights：</p> \[\mathbf{w}_t^l = \mathbf{h}_t \cdot W^w\] <p>最终的 lookahead index score：</p> \[I_{t,s} = \sigma\left(\sum_{h=1}^{n_h^l} \mathbf{w}_{t,h}^l \cdot \text{ReLU}\left(\mathbf{q}_{t,h}^l \cdot (K_s^{IComp})^T\right)\right)\] <p>满足 $I_{t,s} \geq 0.5$ 的 entry $s$ 被加载到 GPU。</p> <h3 id="32-两层选择机制">3.2 两层选择机制</h3> <p>FlashMemory 实际上是一个<strong>两级筛选</strong>：</p> <ol> <li><strong>第一级（Memory Indexer）</strong>：粗粒度，每 64 步一次，从全量历史中选出”可能需要”的 chunk 集合 $C_t^{MemComp}$，从 CPU 加载到 GPU</li> <li><strong>第二级（原生 Lightning Indexer）</strong>：细粒度，每步都做，在 $C_t^{MemComp}$ 这个缩小的范围内做正常的 ReLU Top-$k$ 选择，得到最终参与 attention 的 $C_i^{CoreComp}$</li> </ol> <p>这种分层设计保证了：即使 Memory Indexer 的 recall 不完美，只要关键 chunk 被”宽松地”装进 GPU，原生的精确选择机制仍然能正常工作。</p> <hr/> <h2 id="4-训练一张-h20-卡训一小时">4. 训练：一张 H20 卡训一小时</h2> <p>这是这篇工作最让我印象深刻的地方。</p> <p>Memory Indexer 的训练和<strong>整个 backbone 完全解耦</strong>。具体来说：</p> <p><strong>关键洞察</strong>：历史 entry 的 compressed indexer key $K_s^{IComp}$ 是在 backbone forward 时预计算好的、<strong>完全固定的</strong>。训练 Memory Indexer 本质上只是在训练一个 dual-encoder 的 query encoder——把当前 hidden state 映射到一个能正确匹配固定 key 的空间。</p> <p>训练 pipeline：</p> <ol> <li>离线跑一遍 DeepSeek-V4-Flash backbone，收集每个 token 的 hidden state 和 indexer key</li> <li>构造 golden label（哪些 entry 是”真正需要的”）</li> <li>只训练 query 端的低秩投影矩阵（$W^{DQ}$, $W^{IUQ}$, $W^w$），用 Focal Loss</li> </ol> <p>训练过程中 <strong>完全不加载 backbone 模型</strong>。参数量不到整个模型的 0.1%。</p> <p>结果：<strong>在一张 H20 上跑 1 小时就收敛了。</strong> 8 张 H20 一周跑了 500 次实验扫参数空间。</p> <p>这比传统的 end-to-end self-distillation（原生 Lightning Indexer 的训练方式）快了不知道多少个量级。</p> <h3 id="41-golden-label-怎么造">4.1 Golden Label 怎么造</h3> <p>直接用原生 Lightning Indexer 的 Top-$k$ 选择结果作为 label 会有严重的 inflation 问题——Top-$k$ 是固定数量的，不管这个 entry 有多不相关都会被选上。</p> <p>FlashMemory 提出了 <strong>Cross-Layer Majority Voting</strong> 去噪：</p> <ol> <li>对未来 window $[t, t+\tau-1]$ 内的每个 token $i$，收集 21 层 CSA 的原始 indexer logit</li> <li>对每层做 Softmax + Top-$p$（$p=0.6$）动态截断（而非固定 Top-$k$）</li> <li>跨层投票：只有在 $\geq 3$ 层中都被独立选中的 entry 才认为是”golden”</li> <li>对整个 window 取 union 作为最终正样本集合</li> </ol> <p>这个 pipeline 把原始的约 10,000 个 pseudo-positive 过滤到 100-1,000 个 high-confidence entry。</p> <h3 id="42-最优配置3-层就够了">4.2 最优配置：3 层就够了</h3> <p>并不是每一层 transformer 都适合做 lookahead prediction。浅层的表示主要是 low-level token statistics，做长程预测效果差。</p> <p>通过 500 次 Pareto 优化，最终确定在 <strong>第 10、12、20 层</strong> 放置独立的 Memory Indexer，用 <strong>OR-mode 路由</strong>（三层中任何一层预测 $\geq 0.5$，就加载该 chunk）。</p> <p>用 8 层？recall 太宽松，会把 30-49% 的历史 chunk 加载回来，失去了省显存的意义。用 1 层？代表能力不够。3 层是 sweet spot。</p> <hr/> <h2 id="5-实验结果">5. 实验结果</h2> <p><img src="/assets/img/posts/20260611-flashmemory/crop_table1_results.png" alt="主实验结果"/></p> <p>在三个主要长上下文 benchmark 上（LongBench-v2、LongMemEval、RULER）：</p> <table> <thead> <tr> <th>指标</th> <th>DS-V4-Flash</th> <th>FM-DS-V4</th> <th>Recency Only</th> <th>Random 10%</th> </tr> </thead> <tbody> <tr> <td><strong>平均准确率</strong></td> <td>76.9%</td> <td><strong>77.5%</strong> (+0.6)</td> <td>33.3%</td> <td>38.7%</td> </tr> <tr> <td><strong>平均显存</strong></td> <td>0.93 GB (100%)</td> <td><strong>0.10 GB (13.5%)</strong></td> <td>0.04 GB</td> <td>0.12 GB</td> </tr> </tbody> </table> <p>几个关键数字：</p> <ul> <li><strong>LongBench-v2-L（493K context）</strong>：FM-DS-V4 在只用 10% 显存的情况下比 baseline <strong>高了 1.9 分</strong>。Less is more 在这里被最极端地验证——去掉噪声 chunk 反而减少了 attention 中的干扰</li> <li><strong>RULER 512K</strong>：89.6 vs 88.3，FM-DS-V4 用 0.18GB vs baseline 的 1.87GB，<strong>显存压缩超过 90%</strong></li> <li><strong>Recency Only 和 Random 10% 全面崩溃</strong>（准确率只有 baseline 的一半不到），说明 Memory Indexer 确实学到了有意义的 temporal routing，不是随机碰运气</li> </ul> <p>如下图是性能和显存的直观对比：</p> <p><img src="/assets/img/posts/20260611-flashmemory/crop_fig1_performance.png" alt="性能与显存效率对比"/></p> <hr/> <h2 id="6-局限性不是万能的">6. 局限性：不是万能的</h2> <p>这篇工作非常坦诚地列出了三个 failure case，我觉得这比单纯报正面结果有价值得多。</p> <h3 id="61-context-independent-overhead">6.1 Context-Independent Overhead</h3> <p>理想状态下，如果当前 query 和历史 context 完全无关，Sigmoid gating 应该输出接近零的分数，不加载任何 chunk。但实际上 <strong>Sigmoid 存在”背景泄漏”</strong>——在 500K context 下，模型还是会误加载约 8.4% 的历史 chunk（虽然准确率没受影响）。</p> <h3 id="62-mrcr-崩溃dense-global-memory-任务">6.2 MRCR 崩溃（Dense Global Memory 任务）</h3> <p>在 Multi-Range Context Retrieval（MRCR）benchmark 上，准确率从 baseline 的 76.0% <strong>暴跌到 48.0%</strong>。</p> <p>原因：MRCR 要求模型同时精确回忆分散在全文多个位置的信息——这是一个”dense global memory”任务。即便给 oracle 50% 的 golden chunk，准确率也会下降 2%。这说明某些任务<strong>天生就不适合 sparse 策略</strong>，它们需要”全量密集记忆”。</p> <p>Memory Indexer 基于浅层 dot-product 相似度做决策，缺乏 Late-Interaction（如 ColBERT 式 token 级交互），面对这类复杂的多点检索任务，能力有限。</p> <h3 id="63-长度泛化天花板">6.3 长度泛化天花板</h3> <p>Memory Indexer 只能安全泛化到<strong>训练长度的 2 倍</strong>。超过这个边界，lookahead 选择退化为接近随机。作者把最终模型训练到 512K，推测超过 1M 就会失效。</p> <p>本质原因是 positional embedding 的 out-of-distribution——这不是一般的检索模型，它依赖位置信息来建模”远处 vs 近处”的优先级。</p> <hr/> <h2 id="7-我的看法">7. 我的看法</h2> <p>先说我觉得好的部分。</p> <p><strong>“Backbone-free decoupled training”是这篇工作最值得学习的工程范式。</strong> 把一个复杂系统的优化分解为：固定 key representation + 只训 query encoder，这样一来训练成本从”需要加载整个千亿参数模型”变成”单张 H20 一小时”。这个思路对很多 retrieval-augmented 的系统设计都有启发意义。</p> <p><strong>Less is more 的实验结果很有意思。</strong> FM-DS-V4 在 LongBench-v2-L 上不仅没掉点还涨了 1.9%，说明原生 full attention 确实存在”注意力噪声”问题——太多不相关的 chunk 参与 attention dot-product 会干扰模型决策。这个发现和 sparse attention 文献中的 “attention denoising” 观察一致。</p> <p><strong>但项目被暂停是遗憾的。</strong> 论文明确说了因为组织调整（project lead 离开腾讯），项目已经 suspended。很多关键 ablation 没做完——$\tau=64$ 和阈值 0.5 都是初步探索的值，没有系统对比。MRCR 上的 failure 也没来得及修（作者提出了 Late-Interaction 方向但未验证）。</p> <p>从实际 serving 角度看，FlashMemory 的价值在于：<strong>如果你的业务以”长 context 但大部分回答不需要精确全文回忆”为主</strong>（这是绝大多数 RAG/agent 场景），那么它能用极低的显存服务超长 context。但如果你的场景是”必须精确检索全文多个位置的信息”（MRCR 类任务），目前的 Memory Indexer 还不够。</p> <p>代码和模型都已开源：<a href="https://github.com/libertywing/FlashMemory-Deepseek-V4">GitHub</a> / <a href="https://huggingface.co/libertywing/FlashMemory-Deepseek-V4">HuggingFace</a>。值得拉下来看看 Memory Indexer 的实现细节。</p> <hr/> <blockquote> <p>欢迎评论区交流，或者指出理解有偏差的地方。</p> </blockquote>]]></content><author><name></name></author><category term="LLM"/><category term="KV Cache"/><category term="长上下文"/><category term="DeepSeek"/><category term="Sparse Attention"/><category term="论文解读"/><summary type="html"><![CDATA[FlashMemory-DeepSeek-V4：用 13.5% 的显存干 100% 的活，超长上下文推理的 less is more]]></summary></entry><entry><title type="html">arXiv’26 | Mirage：把世界模型的 3D 记忆搬进 Latent Space，快 10 倍还省 55 倍显存</title><link href="https://marsggbo.github.io/blog/2026/20260611-mirage/" rel="alternate" type="text/html" title="arXiv’26 | Mirage：把世界模型的 3D 记忆搬进 Latent Space，快 10 倍还省 55 倍显存"/><published>2026-06-11T00:00:00+00:00</published><updated>2026-06-11T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260611-mirage</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260611-mirage/"><![CDATA[<blockquote> <p>插播：之前写的<a href="https://item.jd.com/14945889.html">《动手学 AutoML》</a>终于出版了，从 NAS 到超参优化都有覆盖，适合想系统入门 AutoML 的同学。好了广告结束，现在进入正题。</p> <p><img src="/assets/img/book_cover_automl.png" alt="动手学AutoML书籍封面"/></p> </blockquote> <h1 id="mirage把世界模型的-3d-记忆搬进-latent-space快-10-倍还省-55-倍显存">Mirage：把世界模型的 3D 记忆搬进 Latent Space，快 10 倍还省 55 倍显存</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2606.09828">Latent Spatial Memory for Video World Models</a></p> </blockquote> <hr/> <h2 id="1-什么是世界模型从-llm-到脑中模拟器">1. 什么是世界模型？从 LLM 到”脑中模拟器”</h2> <p>你有没有想过，人类为什么能在脑子里”预演”未来？</p> <p>比如你在一个陌生的房间里走了半圈，转身回来——你会”预期”看到刚才路过的沙发、桌子还在原位。你甚至可以闭着眼睛想象：如果我从这个角度看过去，那个柜子应该被墙挡住了。</p> <p>这种能力，本质上就是你大脑里维护了一个<strong>世界的内部模型</strong>：它记住了场景的空间结构，能根据你的”动作”（移动、转头）预测下一步会看到什么。</p> <p><strong>世界模型（World Model）</strong>就是 AI 试图复刻这种能力的尝试。</p> <h3 id="11-世界模型的正式定义">1.1 世界模型的正式定义</h3> <p>简单来说，世界模型是一个系统，它能够：</p> <ol> <li><strong>观察</strong>当前环境状态（一帧图像、一段视频）</li> <li><strong>记忆</strong>已经看过的场景信息</li> <li><strong>预测/生成</strong>在给定动作（如相机轨迹）下的未来观察</li> </ol> <p>用数学来说，给定初始帧 $I_0$ 和一条相机轨迹 ${(\mathbf{E}<em>t, K_t)}$（位姿+内参），世界模型需要生成一系列多视角一致的帧 ${I_t}</em>{t=1}^T$。</p> <p>这跟纯粹的”视频生成”有什么区别？<strong>核心区别在于 3D 一致性</strong>。普通的视频生成模型（如 Sora、Wan）可以生成漂亮的视频，但如果你让相机绕回起点，生成的画面大概率跟开头完全对不上——因为它没有维护一个持久的 3D 空间表示。</p> <h3 id="12-世界模型的应用场景">1.2 世界模型的应用场景</h3> <p>世界模型为什么重要？因为它是很多下游应用的底座：</p> <ul> <li><strong>自动驾驶模拟</strong>：给定车辆轨迹，生成逼真的驾驶场景视频用于训练和测试</li> <li><strong>游戏/交互环境生成</strong>：玩家在虚拟世界里自由探索，系统实时生成一致的新场景</li> <li><strong>机器人导航规划</strong>：机器人在脑中”预演”路线，预判会遇到什么</li> <li><strong>3D 内容创作</strong>：从单张图片出发，探索式地生成完整的 3D 场景</li> </ul> <h3 id="13-视频世界模型的演进路线">1.3 视频世界模型的演进路线</h3> <p>近年来，随着 video diffusion model 的突破（Sora、CogVideoX、Wan2.1 等），研究者开始把这些强大的视频生成器当作”世界模拟器”来用。大致经历了三个阶段：</p> <p><strong>阶段一：纯视频生成器</strong></p> <p>直接用 video diffusion model 生成视频，可以条件在相机轨迹上。问题是：<strong>没有持久记忆</strong>。每次生成只看有限的时间窗口，超出窗口的信息就丢了。相机走远了再回来，场景大概率”漂”了。</p> <p><strong>阶段二：带 RGB 点云记忆的世界模型</strong></p> <p>为了解决 3D 一致性问题，一系列工作（WonderJourney、WonderWorld、Voyager、Spatia 等）给视频生成器加了一个<strong>持久的 RGB 点云</strong>作为空间记忆：</p> <ul> <li>把每一帧用深度估计 lift 成 3D 点云</li> <li>点云存储每个点的 3D 坐标 + RGB 颜色</li> <li>生成新视角时，先把点云渲染成目标视角的 RGB 图像，再 encode 成 latent 送给 diffusion backbone</li> </ul> <p>这确实有效——有了 3D 锚点，相机回访时能”记住”之前看到了什么。</p> <p><strong>但问题来了。</strong></p> <h3 id="14-rgb-点云记忆的核心瓶颈">1.4 RGB 点云记忆的核心瓶颈</h3> <p>这也是整个领域在 Mirage 之前面临的<strong>核心矛盾</strong>：</p> <blockquote> <p><strong>video diffusion model 在 latent space 工作，但空间记忆在 pixel space 工作。每生成一帧，都要做一次”latent → pixel → latent”的来回折腾。</strong></p> </blockquote> <p>具体来说：</p> <ol> <li><strong>计算代价爆炸</strong>：每一步 conditioning 都要先把百万级的点云渲染成全分辨率 RGB 图像，再用 VAE encoder 编码回 latent。渲染和编码的分辨率是 pixel 级的（如 704×1280），而 diffusion backbone 只需要 latent 级的（如 44×80）。相当于每步都做了一大堆无用功。</li> <li><strong>信息损失</strong>：latent → pixel → latent 的 round trip 并不是无损的。VAE 重建有误差，光栅化有 artifacts，可见性有空洞。原本 latent 里丰富的语义/纹理特征，经过 RGB 这一趟就”打了折”。</li> <li><strong>内存随 rollout 线性增长</strong>：点云越来越大，渲染越来越慢，到最后 GPU 直接 OOM。</li> </ol> <p><strong>一句话总结：你的生成器在 latent 世界里干活，但你非要让它的记忆在 pixel 世界里存取，来回倒腾既慢又损。</strong></p> <p>这就是 Mirage 要解决的核心问题。</p> <hr/> <h2 id="2-miragelatent-spatial-memory">2. Mirage：Latent Spatial Memory</h2> <p>今天想聊的这篇工作，核心 idea 出奇地自然：</p> <blockquote> <p><strong>既然 diffusion backbone 消费的是 latent，那空间记忆为什么不直接存 latent？</strong></p> </blockquote> <p>如下图，上半部分是之前的 RGB 点云方案：存颜色 → 渲染成图 → 再 encode 回 latent；下半部分是 Mirage 的 latent spatial memory：存 latent 特征 → 直接投影到目标视角的 latent grid 上。<strong>省掉了 pixel space 的来回。</strong></p> <p><img src="/assets/img/posts/20260611-mirage/fig2_latent_vs_rgb.png" alt="Latent Spatial Memory 与 RGB 点云方案对比"/></p> <h3 id="21-latent-spatial-memory-的定义">2.1 Latent Spatial Memory 的定义</h3> <p>Mirage 的记忆不再是 $\mathcal{M}_{\text{rgb}} = {(\mathbf{p}_i, \mathbf{c}_i)}$（坐标 + 颜色），而是：</p> \[\mathcal{M} = \{(\mathbf{p}_i, \mathbf{f}_i)\}, \quad \mathbf{p}_i \in \mathbb{R}^3, \quad \mathbf{f}_i \in \mathbb{R}^C\] <p>每个点存的是一个 $C=48$ 维的 latent feature vector（对应 Wan2.2 的 VAE latent channel 数），而不是 3 维的 RGB。</p> <p>这意味着什么？<strong>记忆里存的是 diffusion model 的”母语”</strong>——跟 backbone 直接对接，不需要翻译。</p> <h3 id="22-记忆的构建depth-guided-back-projection">2.2 记忆的构建：Depth-Guided Back-Projection</h3> <p>构建过程和 RGB 方案类似，只是存的内容不同：</p> <ol> <li>输入帧 $I$ 通过 VAE encoder 得到 latent tensor $\mathbf{z} \in \mathbb{R}^{C \times h \times w}$</li> <li>估计深度图 $D$，下采样到 latent 分辨率</li> <li>每个 latent cell $(u,v)$ 通过 pinhole 反投影到世界坐标系：</li> </ol> \[\mathbf{p}_{uv} = \pi^{-1}(u, v, D(u,v); K, \mathbf{E})\] <ol> <li>对应的 latent token $\mathbf{F}_{uv} = \mathbf{z}[:,v,u]$ 存入记忆</li> </ol> <p>关键点：<strong>整个 lifting 在 latent 分辨率进行</strong>（44×80），不是 pixel 分辨率（704×1280）。空间点数直接少了 $s^2 = 16^2 = 256$ 倍。</p> <h3 id="23-记忆的读取latent-space-projection">2.3 记忆的读取：Latent-Space Projection</h3> <p>读取时同样高效——目标视角下的 conditioning signal 通过一次 latent 分辨率的投影就得到了：</p> <ol> <li>把记忆中所有点投影到目标相机的 latent grid 上</li> <li>每个 cell 保留深度最近的点（z-buffering）</li> <li>取出对应的 latent token 组成 target-view latent tensor $\hat{\mathbf{z}}_t$</li> <li>同时生成 visibility mask $\mathbf{m}_t$ 标记哪些位置有信息</li> </ol> \[i_t(u,v) = \arg\min_{i \in \Omega_t(u,v)} [\mathbf{E}_t \mathbf{p}_i]_z, \quad \hat{\mathbf{z}}_t(u,v) = \mathbf{F}_{i_t(u,v)}\] <p>整个 readout <strong>不涉及任何 pixel 空间操作</strong>。没有渲染，没有 VAE encode。就是一次 latent 分辨率的投影。</p> <hr/> <h2 id="3-mirage-的完整流程">3. Mirage 的完整流程</h2> <p>如下图，Mirage 以 chunk-by-chunk 的方式自回归生成长视频，每个 chunk 经历三步循环：</p> <p><img src="/assets/img/posts/20260611-mirage/fig3_overview.png" alt="Mirage 整体架构与流程"/></p> <h3 id="31-initialize--readout--update-循环">3.1 Initialize → Readout → Update 循环</h3> <p><strong>Step 1：初始化记忆</strong></p> <ul> <li>对初始帧 $I_0$ encode 得到 latent，估计深度，back-project 到 3D → 得到初始 cache $\mathcal{M}$</li> </ul> <p><strong>Step 2：Readout + Denoising（逐 chunk）</strong></p> <ul> <li>把 $\mathcal{M}$ 投影到当前 chunk 每个目标视角 → 得到 latent conditioning tensors</li> <li>通过 ControlNet-style 的 side branch 注入 diffusion backbone</li> <li>Backbone 在 latent space 做 denoising → 生成当前 chunk</li> </ul> <p><strong>Step 3：Cache Update</strong></p> <ul> <li>对生成的帧估计深度、re-encode 为 latent</li> <li>用分割模型检测动态物体和天空，排除这些区域</li> <li>将静态区域的 latent tokens back-project 进记忆</li> </ul> <p>然后回到 Step 2，处理下一个 chunk。</p> <h3 id="32-关键设计细节">3.2 关键设计细节</h3> <p><strong>ControlNet-style 注入</strong>：readout 的 latent tensor 通过一个跟 backbone 结构对齐的 side branch 注入。因为 readout 已经在 backbone 的 latent space 里了，所以<strong>不需要额外的 bridging encoder</strong>——这是 RGB 方案做不到的。</p> <p><strong>动态物体过滤</strong>：用 Qwen3-VL 做开放词汇实体检测 + SAM3 做分割，把移动物体和天空从记忆更新中排除。因为这些东西的 geometry 不靠谱，放进 persistent cache 会污染记忆。</p> <p><strong>两阶段训练</strong>：</p> <ul> <li>第一阶段冻结 backbone 和 VAE，只训练 side branch</li> <li>第二阶段加 rank-64 LoRA 在 self-attention 上，跟 side branch 联合优化</li> </ul> <p>这种策略避免了 backbone 在早期适配一个还不成熟的 conditioning signal。</p> <hr/> <h2 id="4-实验结果">4. 实验结果</h2> <h3 id="41-worldscore-benchmark">4.1 WorldScore Benchmark</h3> <p>WorldScore 是一个专门评估 world generation 的 benchmark，涵盖可控性、一致性、质量、运动四大维度。Mirage 在 Average Score 上达到 <strong>70.36</strong>，超过所有对比方法：</p> <table> <thead> <tr> <th>方法</th> <th>Average</th> <th>Static</th> <th>Dynamic</th> <th>3D Cons</th> <th>Photo Cons</th> </tr> </thead> <tbody> <tr> <td>WonderJourney</td> <td>54.19</td> <td>63.75</td> <td>44.63</td> <td>80.60</td> <td>79.03</td> </tr> <tr> <td>Voyager</td> <td>66.08</td> <td>77.62</td> <td>54.53</td> <td>81.56</td> <td>85.99</td> </tr> <tr> <td>Spatia</td> <td>69.73</td> <td>72.63</td> <td>66.82</td> <td>86.40</td> <td>89.10</td> </tr> <tr> <td><strong>Mirage</strong></td> <td><strong>70.36</strong></td> <td><strong>73.60</strong></td> <td><strong>67.11</strong></td> <td><strong>92.21</strong></td> <td><strong>93.95</strong></td> </tr> </tbody> </table> <p>特别注意 3D Consistency 和 Photometric Consistency——Mirage 分别达到 92.21 和 93.95，大幅领先 RGB cache 方案。这直接验证了 latent spatial memory 比 RGB 点云能更好地保持几何一致性。</p> <h3 id="42-realestate10k">4.2 RealEstate10K</h3> <p>在 RealEstate10K 上，Mirage 在 Novel View Synthesis 和 Closed-Loop（相机走一圈回来的一致性测试）上均达到最优或接近最优：</p> <table> <thead> <tr> <th>方法</th> <th>PSNR↑</th> <th>SSIM↑</th> <th>LPIPS↓</th> <th>PSNR_C↑</th> <th>SSIM_C↑</th> </tr> </thead> <tbody> <tr> <td>ViewCrafter</td> <td>15.78</td> <td>0.580</td> <td>0.396</td> <td>14.79</td> <td>0.481</td> </tr> <tr> <td>Voyager</td> <td>17.79</td> <td>0.636</td> <td>0.297</td> <td>17.66</td> <td>0.540</td> </tr> <tr> <td>Spatia</td> <td>18.58</td> <td>0.646</td> <td>0.254</td> <td>19.38</td> <td>0.579</td> </tr> <tr> <td><strong>Mirage</strong></td> <td>18.38</td> <td><strong>0.779</strong></td> <td><strong>0.250</strong></td> <td><strong>20.05</strong></td> <td><strong>0.825</strong></td> </tr> </tbody> </table> <p>Closed-Loop 指标尤其有说服力——相机绕了一圈回来，Mirage 的 SSIM_C 达到 0.825，远超 Spatia 的 0.579。这说明 latent 记忆真的在长距离 rollout 下保持了场景一致性。</p> <h3 id="43-效率这才是-latent-spatial-memory-的杀手锏">4.3 效率：这才是 Latent Spatial Memory 的杀手锏</h3> <p>如下图，随着 rollout 长度增加，Mirage 的每帧 cache 读取时间和内存占用几乎不增长，而 RGB 方案则爆炸式上升：</p> <p><img src="/assets/img/posts/20260611-mirage/fig5_efficiency.png" alt="效率对比：cache 读取时间与内存占用"/></p> <p>具体数字：</p> <ul> <li><strong>端到端生成速度</strong>：比 RGB cache 快 <strong>10.57×</strong></li> <li><strong>3D cache 显存占用</strong>：比 RGB 方案少 <strong>55×</strong></li> </ul> <p>为什么差这么多？因为：</p> <ul> <li>RGB 方案每步要在 $H \times W$（如 704×1280）分辨率做光栅化 + VAE encode</li> <li>Mirage 只需要在 $h \times w$（如 44×80）分辨率做一次投影</li> <li>cache 本身的 footprint 也小了 $s^2 = 256$ 倍（latent stride 16）</li> </ul> <p>而且这个差距随着 rollout 变长会越来越大——RGB 方案最终会 OOM，Mirage 可以轻松完成。</p> <h3 id="44-消融实验">4.4 消融实验</h3> <table> <thead> <tr> <th>变体</th> <th>Avg</th> <th>3D Cons</th> <th>Photo Cons</th> </tr> </thead> <tbody> <tr> <td>Mirage (full)</td> <td>70.36</td> <td>92.21</td> <td>93.95</td> </tr> <tr> <td>换成 RGB 点云</td> <td>67.71</td> <td>90.75</td> <td>91.10</td> </tr> <tr> <td>Feature Upsample</td> <td>60.85</td> <td>84.90</td> <td>79.81</td> </tr> <tr> <td>去掉动态物体过滤</td> <td>61.20</td> <td>80.88</td> <td>76.10</td> </tr> <tr> <td>单阶段训练</td> <td>63.18</td> <td>87.11</td> <td>84.47</td> </tr> </tbody> </table> <p>几个关键结论：</p> <ul> <li><strong>Latent vs RGB</strong>：同样的 backbone + 训练策略，仅把 cache 从 latent 换成 RGB，Average Score 从 70.36 降到 67.71。说明 RGB round trip 确实损失了信息。</li> <li><strong>动态物体过滤很重要</strong>：去掉后 3D Consistency 从 92.21 暴降到 80.88。移动物体的 stale 几何如果留在 cache 里，后面生成时会被错误地 splat 回来。</li> <li><strong>两阶段训练有效</strong>：让 side branch 先稳定了再解锁 LoRA，比一起从头训收敛更好。</li> </ul> <hr/> <h2 id="5-局限性与思考">5. 局限性与思考</h2> <p><strong>动态物体没有被记忆</strong>：当前方案直接把动态物体从 cache 中排除了。这意味着如果场景里有个人走过，相机回来时那个人不会”被记住”。这是一个简化假设——persistent memory 目前只服务于静态几何。</p> <p><strong>对深度估计的依赖</strong>：整个 lifting/update 依赖 metric depth。虽然消融实验显示换不同 depth estimator 影响不大（因为 ControlNet 把 readout 当作 soft hint），但在极端场景下深度估计失准还是会影响 cache 质量。</p> <p><strong>我的 take</strong>：</p> <p>这篇工作的核心贡献其实是一个很自然的 insight——<strong>空间记忆应该和生成器在同一个表示空间</strong>。之前的 RGB 点云方案之所以又慢又损，本质上是因为”存储空间”和”工作空间”不匹配。Mirage 把记忆搬进 latent space，从根源上消除了 pixel-latent 的来回折腾。</p> <p>10× 加速和 55× 内存节省不是靠什么 trick 堆出来的，是 <strong>表示层面的结构性改进</strong> 带来的自然结果。这种”对问题本质的重新思考”往往比在现有框架上做 engineering optimization 更有效。</p> <hr/> <h2 id="6-总结">6. 总结</h2> <table> <thead> <tr> <th>维度</th> <th>RGB 点云方案</th> <th>Mirage（Latent Spatial Memory）</th> </tr> </thead> <tbody> <tr> <td>记忆内容</td> <td>3D 坐标 + RGB 颜色</td> <td>3D 坐标 + Latent Feature</td> </tr> <tr> <td>Readout 方式</td> <td>渲染 → VAE Encode</td> <td>直接 latent 投影</td> </tr> <tr> <td>每步操作分辨率</td> <td>Pixel ($H \times W$)</td> <td>Latent ($h \times w$)</td> </tr> <tr> <td>端到端速度</td> <td>1×</td> <td><strong>10.57×</strong></td> </tr> <tr> <td>Cache 显存</td> <td>1×</td> <td><strong>1/55</strong></td> </tr> <tr> <td>信息保真</td> <td>有 VAE 重建损失</td> <td>无损（native latent）</td> </tr> <tr> <td>3D Consistency</td> <td>86-91</td> <td><strong>92.21</strong></td> </tr> </tbody> </table> <p>一句话：<strong>世界模型的 3D 记忆，应该说”latent 的语言”，而不是 pixel 的语言。</strong></p> <p>Mirage 用一个干净的 idea 把 video world model 的效率和质量同时拉上来了。对于做 LLM/视频生成/3D 相关的同学，这个”把记忆搬进 native representation space”的思路很有借鉴意义。</p> <hr/> <p><em>Project Page: <a href="https://aka.ms/latent-spatial-memory">aka.ms/latent-spatial-memory</a></em></p>]]></content><author><name></name></author><category term="World Model"/><category term="Video Generation"/><category term="3D Consistency"/><category term="Diffusion Model"/><category term="论文解读"/><summary type="html"><![CDATA[插播：之前写的《动手学 AutoML》终于出版了，从 NAS 到超参优化都有覆盖，适合想系统入门 AutoML 的同学。好了广告结束，现在进入正题。]]></summary></entry><entry><title type="html">Anthropic | 当 AI 开始造自己：递归自我改进离我们有多远？</title><link href="https://marsggbo.github.io/blog/2026/20260611-rsi/" rel="alternate" type="text/html" title="Anthropic | 当 AI 开始造自己：递归自我改进离我们有多远？"/><published>2026-06-11T00:00:00+00:00</published><updated>2026-06-11T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260611-rsi</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260611-rsi/"><![CDATA[<blockquote> <p>插播：之前写的<a href="https://item.jd.com/14945889.html">《动手学 AutoML》</a>终于出版了，从 NAS 到超参优化都有覆盖，适合想系统入门 AutoML 的同学。好了广告结束，现在进入正题。</p> <p><img src="/assets/img/book_cover_automl.png" alt="动手学AutoML书籍封面"/></p> </blockquote> <h1 id="当-ai-开始造自己anthropic-递归自我改进报告深度解读">当 AI 开始造自己：Anthropic 递归自我改进报告深度解读</h1> <blockquote> <p>原文：<a href="https://www.anthropic.com/institute/recursive-self-improvement">When AI builds itself</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有想过这样一个问题：如果 AI 写代码的能力已经超过了写它的人，那下一代 AI 是不是也可以由 AI 自己来造？</p> <p>这不是科幻。Anthropic 在 2026 年 6 月发了一篇博客，标题极其直白——<strong>When AI builds itself</strong>（当 AI 造自己）。文中用大量内部数据展示了一件事：<strong>AI 已经在加速 AI 自身的开发</strong>，且趋势在加速。</p> <p>今天想和大家聊聊这篇文章到底讲了什么、哪些数字值得细品、以及作为一个做 LLM 推理效率的研究者，我自己怎么看这件事。</p> <hr/> <h2 id="2-外部证据能力增长有多快">2. 外部证据：能力增长有多快？</h2> <p>Anthropic 引用的第一组数据来自 METR（一个专做 AI 长任务评测的机构）：</p> <ul> <li>2024 年 3 月，Claude Opus 3 能完成<strong>人类 4 分钟能做完</strong>的任务</li> <li>一年后，Claude Sonnet 3.7 做到了 <strong>1.5 小时</strong>级别</li> <li>又一年，Claude Opus 4.6 做到了 <strong>12 小时</strong>级别</li> </ul> <p>任务时间跨度翻倍周期从 7 个月缩短到 <strong>4 个月</strong>。如果这个趋势不停，2027 年 AI 就能处理人类需要数周完成的任务。</p> <p>另外两个 benchmark：</p> <ul> <li><strong>SWE-bench</strong>（真实开源项目 bug 修复）：两年内从个位数正确率飙升到饱和</li> <li><strong>CORE-Bench</strong>（复现已有研究）：从 2024 年 20% 成功率到 2026 年饱和</li> </ul> <p>这些数字说明的不只是”模型越来越强”，而是<strong>能力增长本身在加速</strong>——一个二阶效应。</p> <hr/> <h2 id="3-内部证据anthropic-自己的数据才是重头戏">3. 内部证据：Anthropic 自己的数据才是重头戏</h2> <p>外部 benchmark 大家或多或少都知道，真正让人”细思极恐”的是 Anthropic 放出来的内部数据。</p> <h3 id="31-代码产出8-倍加速">3.1 代码产出：8 倍加速</h3> <p>截至 2026 年 5 月：</p> <ul> <li><strong>超过 80% 的 merged 代码</strong>是 Claude 写的（不是人写的！）</li> <li>2025 年 2 月 Claude Code 发布之前，这个数字还是个位数</li> <li>Q2 2026，工程师人均每天 merge 的代码量是 2024 年的 <strong>8 倍</strong></li> </ul> <p>文章也很老实地说了，lines of code 不等于”质量”，8 倍是一个上界估计。但即使打个折，4-5 倍的实际生产力提升也是很惊人的。</p> <p>这里我想多说一句。8 倍代码产出意味着什么？意味着<strong>人的角色从”写代码”变成了”审代码”和”定方向”</strong>。这已经不是”AI 辅助编程”了，这是”人辅助 AI 编程”。</p> <h3 id="32-代码质量从不如人到平起平坐">3.2 代码质量：从不如人到平起平坐</h3> <p>Anthropic 内部的评估是：</p> <ul> <li>2025 年底，Claude 写的代码质量<strong>还不如</strong>人写的</li> <li>2026 年中，<strong>大致持平</strong></li> <li>预计年内会<strong>超过人类</strong></li> </ul> <p>更有意思的数据：他们用 Claude 做自动 code review，回溯分析发现，<strong>能提前拦住 1/3 的历史线上事故</strong>。写那些代码的工程师可是全世界最顶尖的那批人。</p> <h3 id="33-开放式问题解决成功率从-26-到-76">3.3 开放式问题解决：成功率从 26% 到 76%</h3> <p>Claude Code 在”最开放、最不确定”的任务上（比如线上 incident 根因定位），成功率从 2025 年底的约 26% 跃升到 2026 年 5 月的 <strong>76%</strong>，半年涨了 50 个百分点。</p> <p>文中举了个具体例子：一次常规升级导致数万个训练任务崩溃，工程师只给了 Claude 一些日志和集群 access，Claude 自己排查、定位到一个 obscure debugging flag、复现问题、验证修复方案。<strong>2 小时干完了人类 2-3 天的活。</strong></p> <h3 id="34-实验优化从-3x-到-52x">3.4 实验优化：从 3x 到 52x</h3> <p>Anthropic 每发一个新模型都做一个固定测试：给 Claude 一段训练代码，让它尽可能加速但保持正确性。</p> <ul> <li>2025 年 5 月（Opus 4）：平均加速 <strong>~3x</strong></li> <li>2026 年 4 月（Mythos Preview）：平均加速 <strong>~52x</strong></li> </ul> <p>人类 skilled researcher 花 4-8 小时大概能做到 4x。Claude 现在做到 52x。<strong>在”给定目标、优化执行”这个维度上，Claude 已经 superhuman。</strong></p> <h3 id="35-研究方向判断从-51-到-64">3.5 研究方向判断：从 51% 到 64%</h3> <p>这可能是最重要的一个数字。Anthropic 做了一个实验：找出真实研究 session 中人类走弯路的时刻（n=129），看 Claude 在那个时间点会选择什么 next step，然后用一个”看过全局”的 judge 来评判谁的选择更好。</p> <ul> <li>2025 年 11 月（Opus 4.5）：Claude 的选择优于人类的比例 <strong>51%</strong></li> <li>2026 年 4 月（Mythos Preview）：<strong>64%</strong></li> </ul> <p>这个数字很微妙。它不是说 Claude 全面超越人类研究者，因为选取的都是”人走弯路”的时刻。但它说明一件事：<strong>在”接下来该干啥”这个决策层面，模型在变好。</strong> 而研究工作本质上就是一连串 next-step decisions。</p> <hr/> <h2 id="4-三个可能的未来">4. 三个可能的未来</h2> <p>Anthropic 描述了三条路径：</p> <p><strong>路径 1：趋势停滞。</strong> 可能我们已经接近 S-curve 的拐点，”研究品味”这种东西无法从更多 compute 和 data 中学到。或者，算力供应链（芯片、电力）成为硬约束。</p> <p><strong>路径 2：持续复合加速，但人类保持方向设定权。</strong> 100 人的公司做出 10 万人公司的产出。人的角色变成”定方向 + 验证结果”。</p> <p><strong>路径 3：完全递归自我改进。</strong> AI 系统自主设计和训练自己的下一代。人类退到”监督和验证”的角色。</p> <p>Anthropic 认为他们目前处于路径 2，但路径 3 <strong>不是不可能</strong>。</p> <hr/> <h2 id="5-我的几个-take">5. 我的几个 Take</h2> <h3 id="51-amdahl-定律无处不在">5.1 Amdahl 定律无处不在</h3> <p>文章里反复提到 Amdahl’s law：加速一个环节，瓶颈只会转移到别处。Anthropic 自己就遇到了：代码产出暴增之后，<strong>human code review 变成了新瓶颈</strong>。新 idea 爆炸式涌现之后，<strong>决定做哪些事</strong>变成了新瓶颈。</p> <p>这跟做 LLM 推理优化的经验是一致的。你把 attention 算快了，memory bandwidth 就是瓶颈；你把 KV cache 压缩了，scheduling overhead 就冒出来。系统性能优化永远是打地鼠。</p> <p>但这里有个核心区别：物理硬件的瓶颈有物理定律兜底，而”研究品味”和”方向判断”是不是也有一个类似的”物理定律”上限？我倾向于认为没有。如果它本质上也是一种 pattern recognition + long-horizon planning 的能力，scaling 早晚能覆盖。</p> <h3 id="52-研究品味真的不可学吗">5.2 “研究品味”真的不可学吗？</h3> <p>Anthropic 目前的立场是：Claude 的比较优势在执行，人类的比较优势在 taste 和 judgment。但文章自己也说了一句非常耐人寻味的话：</p> <blockquote> <p>“Research taste” might be just another AI capability that AI systems fail at for a time, then get good at.</p> </blockquote> <p>我比较认同这个判断。”研究品味”这种听起来很虚的东西，拆开看无非是：</p> <ol> <li>什么问题重要（需要对 field 的 landscape 有全局理解）</li> <li>什么方向能走通（需要对方法论的直觉和约束条件的判断）</li> <li>什么结果值得信任（需要统计素养和实验设计经验）</li> </ol> <p>这三个哪一个不是可以从海量 research trajectory data 里学的？当模型见过足够多的”提出假设→验证→失败/成功”的循环之后，它对”下一步该干什么”的判断就会越来越好。上面 51% → 64% 的数字已经是早期信号了。</p> <h3 id="53-递归自我改进的验证问题">5.3 递归自我改进的”验证问题”</h3> <p>全文最让我不安的一段是关于 alignment 的：</p> <blockquote> <p>Models could prove to be sufficiently aligned… Alternatively, the rare occurrences of misalignment present in today’s models could compound as the models build their successors, growing more frequent but less understood until we lose control of them.</p> </blockquote> <p>这也是整个递归自我改进叙事里<strong>最核心的矛盾</strong>：如果模型能改进自己，那人类怎么验证改进后的模型仍然是安全的？每一代模型都比上一代更强，验证难度也在增加。到某个临界点，人类可能已经不具备验证能力了。</p> <p>Anthropic 提出的解法是”可验证暂停”（verifiable pause）：所有 frontier lab 同时停下来，并且能互相验证对方确实停了。类比核军控条约。但他们自己也承认：训练跑比导弹发射井好藏得多，违约动机极强（谁偷偷继续谁就继承领先地位），而且建立信任机制需要时间——而时间恰恰是最稀缺的。</p> <h3 id="54-对我们做效率优化的意味什么">5.4 对我们做效率优化的意味什么？</h3> <p>作为一个做 MoE 推理优化的人，我的直观感受是：<strong>推理效率的重要性会进一步放大。</strong> 如果未来 AI 系统需要大量做实验（跑实验、改代码、再跑实验），那 inference 的成本直接决定了递归循环的速度。文中提到自动化 weak-to-strong 研究花了 800 小时 compute、$18,000。如果推理效率翻倍，这个循环就快一倍。</p> <p>换句话说，做推理效率优化的人，某种意义上是在做”加速 AI 递归自我改进”的基础设施。这既让人兴奋，也让人有点复杂的感受。</p> <hr/> <h2 id="6-结语">6. 结语</h2> <p>Anthropic 这篇文章的核心信息其实就一句话：<strong>递归自我改进不是遥远的科幻，而是一个正在展开的连续体。</strong> 我们已经处于”AI 显著加速 AI 开发”的阶段，距离”AI 完全自主开发 AI”还有一些 gap（主要在方向判断上），但这个 gap 在以可度量的速度缩小。</p> <p>作为研究者，我觉得最值得警惕的不是某一天突然出现”超级 AI”（大概率不会是一个跳变），而是这个<strong>渐变过程中，人类逐步丧失验证能力</strong>的问题。每一步看起来都是 incremental improvement，但累积效应可能在某个时点突破我们的理解边界。</p> <p>这大概是我们这一代 AI 研究者需要认真思考的问题。不只是”怎么让模型更快更好”，还有”快到什么程度之后，我们该停下来想想”。</p> <hr/> <p>欢迎评论区聊聊你怎么看递归自我改进这件事。是 overhyped 还是 underhyped？</p>]]></content><author><name></name></author><category term="LLM"/><category term="AI Safety"/><category term="行业观察"/><summary type="html"><![CDATA[插播：之前写的《动手学 AutoML》终于出版了，从 NAS 到超参优化都有覆盖，适合想系统入门 AutoML 的同学。好了广告结束，现在进入正题。]]></summary></entry><entry><title type="html">arXiv’26 | Self-Harness：让 Agent 自己改自己的 harness，pass rate 最高翻倍</title><link href="https://marsggbo.github.io/blog/2026/20260611-selfharness/" rel="alternate" type="text/html" title="arXiv’26 | Self-Harness：让 Agent 自己改自己的 harness，pass rate 最高翻倍"/><published>2026-06-11T00:00:00+00:00</published><updated>2026-06-11T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/20260611-selfharness</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/20260611-selfharness/"><![CDATA[<blockquote> <p>插播：之前写的<a href="https://item.jd.com/14945889.html">《动手学 AutoML》</a>终于出版了，从 NAS 到超参优化都有覆盖，适合想系统入门 AutoML 的同学。好了广告结束，现在进入正题。</p> <p><img src="/assets/img/book_cover_automl.png" alt="动手学AutoML书籍封面"/></p> </blockquote> <h1 id="self-harness让-agent-自己改自己的-harnesspass-rate-最高翻倍">Self-Harness：让 Agent 自己改自己的 harness，pass rate 最高翻倍</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2606.09498">Self-Harness: Harnesses That Improve Themselves</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有想过，你的 AI Agent 到底是被谁”调教”出来的？</p> <p>做过 Agent 开发的应该都有体感——一个 LLM 能不能干活，只有一半取决于模型本身。另一半取决于你包在它外面那层东西：system prompt 怎么写、tools 怎么配、出错了走什么恢复逻辑、context 怎么管理。这层东西，业内现在越来越多叫它 <strong>harness</strong>。</p> <p>同一个模型，换一套 harness，性能能差出一倍。这不是夸张——Claude Code、Codex、OpenHands，本质上都是不同的 harness 套在相同或相似的模型上。</p> <p>但问题来了：<strong>这层 harness 目前全靠人肉工程。</strong> 你反复跑 case，看 trace，发现”模型老是忘记写输出文件”，手动加一行 instruction；发现”模型死循环了”，手动加一个 max retry。这种模式对一个模型还凑合，对十个不同 backend 呢？根本不 scale。</p> <p>更离谱的是，不同模型的”坏习惯”完全不同。MiniMax 容易忘记创建 artifact，Qwen 容易在 edit 失败后把文件删了，GLM 容易丢环境变量。你没法写一套通用 harness 解决所有模型的问题——<strong>这本质上是 model-specific 的。</strong></p> <p>今天要聊的这篇来自上海 AI Lab 的工作——Self-Harness——提了一个思路：<strong>让 Agent 自己改自己的 harness</strong>。不需要人类工程师，也不需要一个更强的外部模型来指导。</p> <hr/> <h2 id="2-三种-harness-改进范式">2. 三种 harness 改进范式</h2> <p>先交代下背景。目前 harness 改进存在三种范式：</p> <p><img src="/assets/img/posts/20260611-selfharness/emb_001_p2.png" alt="三种 harness 改进范式对比"/></p> <p><strong>Human Harness Engineering</strong>：人类专家手动检查 trace、改 prompt、加 tool——就是我们现在在做的事。问题是不 scale。</p> <p><strong>Meta-Harness</strong>：用一个更强的外部模型去优化弱模型的 harness。比如用 Claude 4 去帮 Qwen3.5 改 harness。问题是：对 frontier model 来说没有”更强的”了，而且外部模型不一定理解目标模型的真实 failure mode。</p> <p><strong>Self-Harness（本文）</strong>：让模型自己改自己的 harness。用同一个固定模型，基于自己执行失败的证据，提出 harness 修改方案，再用 regression test 验证。</p> <p>这也是整个 Agent 工程化方向的一个核心趋势：从”人调 prompt”到”模型调 harness”。</p> <hr/> <h2 id="3-方法三步自改进循环">3. 方法：三步自改进循环</h2> <p>Self-Harness 的核心 idea 很朴素——一个三步迭代循环：</p> <p><img src="/assets/img/posts/20260611-selfharness/emb_002_p9.png" alt="Self-Harness 优化循环总览"/></p> <h3 id="31-weakness-mining从-trace-里挖-failure-pattern">3.1 Weakness Mining：从 trace 里挖 failure pattern</h3> <p>第一步不是直接改 harness，而是<strong>先搞清楚模型到底在哪里反复犯错</strong>。</p> <p>具体做法：把模型在一组 held-in task 上跑一遍，收集所有失败的 execution trace，然后按 failure signature 聚类。一个 failure signature 包含三个维度：</p> <ul> <li><strong>Verifier 拒绝的原因</strong>（timeout？missing artifact？assertion failure？）</li> <li><strong>Agent 行为与该失败的因果关系</strong>（直接导致？间接贡献？）</li> <li><strong>背后的抽象行为机制</strong>（death loop？忘记创建文件？环境变量丢失？）</li> </ul> <p>关键点：<strong>不是看单个 case 挂了，而是找”这类问题反复挂在同一个机制上”</strong>。两个 case 可能都 timeout 了，但一个是因为死循环，另一个是因为等外部下载——它们需要不同的 harness fix。</p> <h3 id="32-harness-proposal同一个模型提修改方案">3.2 Harness Proposal：同一个模型提修改方案</h3> <p>第二步用<strong>同一个模型</strong>（不是更强的模型！），基于聚类出来的 failure pattern，生成多个并行的 harness 修改候选。</p> <p>每个候选有几个硬约束：</p> <ul> <li>必须绑定到一个具体的 failure mechanism</li> <li>只能改一个具体的可编辑表面（instruction、tool config、middleware 等）</li> <li>多个候选之间必须 materially distinct——不能换个说法重复同一个修改</li> <li><strong>最小化编辑</strong>：不能大改架构，只做 narrow change</li> </ul> <h3 id="33-proposal-validationregression-test-保驾护航">3.3 Proposal Validation：regression test 保驾护航</h3> <p>第三步是验证。改完的 harness 不能光解决已知问题，还不能把之前能跑过的搞挂。</p> <p>接受规则很保守：</p> <ul> <li>held-in 和 held-out 两个 split 都不能降</li> <li>至少一个 split 要提升</li> <li>如果只是”在 A 上涨了但 B 上跌了”——<strong>不接受</strong></li> </ul> <p>多个通过验证的候选可以 merge 到下一版 harness。没通过的不会被采用，但会被记录下来供后续参考。</p> <hr/> <h2 id="4-实验三个模型三套不同的进化路径">4. 实验：三个模型，三套不同的进化路径</h2> <p>他们在 Terminal-Bench-2.0（89 个容器化终端任务）上做了实验，用了三个差异很大的模型：MiniMax M2.5、Qwen3.5-35B-A3B、GLM-5。初始 harness 是一个极简的 DeepAgent baseline——只有一段简短的 system prompt 加默认的文件和 shell 工具。</p> <p>结果如下：</p> <p><img src="/assets/img/posts/20260611-selfharness/emb_004_p11.png" alt="三个模型的 pass rate 提升"/></p> <p>数字说话：</p> <table> <thead> <tr> <th>模型</th> <th>held-in（初始→最终）</th> <th>held-out（初始→最终）</th> <th>相对提升</th> </tr> </thead> <tbody> <tr> <td>MiniMax M2.5</td> <td>43.0% → 50.0%</td> <td>40.5% → 61.9%</td> <td>+53%</td> </tr> <tr> <td>Qwen3.5-35B-A3B</td> <td>15.1% → 36.0%</td> <td>23.8% → 38.1%</td> <td>+60%（held-out），+138%（held-in）</td> </tr> <tr> <td>GLM-5</td> <td>47.7% → 57.0%</td> <td>42.9% → 57.1%</td> <td>+33%</td> </tr> </tbody> </table> <p><strong>几个关键观察：</strong></p> <ol> <li><strong>held-out 也涨了</strong>——说明改进不是 overfit 到已知 case，而是捕捉到了通用的行为机制</li> <li><strong>三个模型都涨了</strong>——证明方法不依赖特定模型能力</li> <li><strong>Qwen3.5 的提升最大（138%）</strong>——说明初始 harness 越不适配模型特点，Self-Harness 的改进空间越大</li> </ol> <hr/> <h2 id="5-最有意思的部分不同模型进化出了完全不同的-harness">5. 最有意思的部分：不同模型进化出了完全不同的 harness</h2> <p>这篇文章最让我觉得有意思的地方，不是那几个 percentage point 的提升，而是<strong>三个模型进化出来的 harness 长得完全不一样</strong>。</p> <p>如下图是 Qwen3.5 经过 Self-Harness 后保留的四个 code-level diff：</p> <p><img src="/assets/img/posts/20260611-selfharness/emb_006_p13.png" alt="Qwen3.5 的四个 harness 修改"/></p> <p>逐个看：</p> <p><strong>MiniMax M2.5 学会了什么：</strong></p> <ul> <li>“先建输出文件再干活”——因为它老是探索到最后忘记 write artifact 就 timeout 了</li> <li>限制 tool call 总数——防止进入”无效 tool-use loop”</li> <li>更小心地处理 structured tool content 的 schema</li> </ul> <p><strong>Qwen3.5 学会了什么：</strong></p> <ul> <li>Dependency precheck——执行前先检查 Python module 是否存在</li> <li>不重复执行完全相同的失败命令（”do not retry the exact same command with the exact same parameters”）</li> <li>限制连续 explore 步数不超过 3 步就要做 change</li> <li>Tool error 后触发 artifact recovery middleware——<strong>这个最骚</strong>，它自己给自己造了一个 middleware，当检测到 tool error 时自动注入”你必须立即创建缺失的 artifact”这条 system prompt</li> </ul> <p><strong>GLM-5 学会了什么：</strong></p> <ul> <li>让环境变量和 PATH 修改跨 shell session 持久化——因为它每次开新 shell 都丢状态</li> <li>从”无止境的 exploration”转向”实现 + 测试”——给自己设了一个阶段切换点</li> </ul> <p><strong>本质上，不同模型有不同的”坏习惯”，需要不同的 harness 来矫正。</strong> MiniMax 的核心问题是”做太多探索忘了交作业”，Qwen 的核心问题是”edit 失败后走向自毁”，GLM 的核心问题是”环境状态不持久”。人类手动做这事成本极高，但让模型自己诊断自己的毛病、自己开药方，这条路看起来是通的。</p> <hr/> <h2 id="6-case-study从失败到成功的行为对比">6. Case Study：从失败到成功的行为对比</h2> <p>如下图展示了 MiniMax M2.5 在一个 <code class="language-plaintext highlighter-rouge">count-dataset-tokens</code> 任务上的 before/after 对比：</p> <p><img src="/assets/img/posts/20260611-selfharness/emb_007_p18.png" alt="MiniMax M2.5 行为对比"/></p> <p>左边（初始 harness）：模型找到了 metadata 配置，但之后继续无意义的 dataset exploration，最终 timeout 没有写出 answer.txt。</p> <p>右边（改进后 harness）：模型定位到 metadata-backed subset，计算 token count，<strong>立即写 /app/answer.txt</strong>，然后 read back 验证。</p> <p>这个行为变化的根源就是 Self-Harness 给 bootstrap instruction 加了一条：”identify the required output artifact and create an initial version as early as possible”。一行改动，一类问题解决。</p> <hr/> <h2 id="7-局限与思考">7. 局限与思考</h2> <p>当然，这篇的局限也很明确：</p> <ol> <li><strong>在一个固定 benchmark 上做的</strong>——Terminal-Bench-2.0 只有 64 个 task，promotion rule 可能对这个规模很敏感</li> <li><strong>Edit 是 bounded 的</strong>——只能改声明好的 editable surfaces，不能动模型参数或工具实现</li> <li><strong>不是开放式自我进化</strong>——它没法给自己加新 tool 或改变控制架构，只是在已有框架内微调执行策略</li> <li><strong>依赖 verifier 质量</strong>——如果 verifier 本身有噪声，promotion gate 会出问题</li> </ol> <p>但作为一个 proof of concept，它回答了一个关键问题：<strong>固定参数的模型能不能通过改 harness 把自己变强？能。</strong></p> <p>我个人的 take：这条路和 Meta-Harness（外部模型优化）不矛盾。对于 frontier model，Self-Harness 是唯一选择（没有更强的了）；对于中小模型，也许两者结合效果更好。另外，Self-Harness 的 weakness mining + proposal + validation 这个 pattern，其实可以迁移到很多其他”系统配置自优化”的场景——你有 trace、有 verifier、有可编辑表面，你就能跑这个循环。</p> <hr/> <h2 id="8-总结">8. 总结</h2> <p>一句话概括：<strong>Self-Harness 让 LLM Agent 不再只是被动接受人类设计的 harness，而是能基于自己的执行失败证据，自己提出、验证、并接受 harness 修改。</strong></p> <p>核心贡献：</p> <ul> <li>提出了 Self-Harness 范式：模型自己改自己的 harness</li> <li>三步循环：Weakness Mining → Harness Proposal → Proposal Validation</li> <li>在三个不同模型上都有效，held-out pass rate 最高相对提升 138%</li> <li>定性分析表明不同模型进化出不同的 harness——不是加了一堆通用废话，而是针对性地解决了 model-specific 的行为缺陷</li> </ul> <p>这也是整个 Agent 工程化方向的一个趋势信号：harness 不再是写死的，它可以演化。从 prompt engineering 到 context engineering 到 harness engineering，下一步可能就是 <strong>harness self-evolution</strong>。</p> <p>欢迎评论区讨论：你觉得让 Agent 自己改自己的 harness，边界在哪里？</p>]]></content><author><name></name></author><category term="LLM"/><category term="Agent"/><category term="Harness Engineering"/><category term="自改进"/><category term="论文解读"/><summary type="html"><![CDATA[插播：之前写的《动手学 AutoML》终于出版了，从 NAS 到超参优化都有覆盖，适合想系统入门 AutoML 的同学。好了广告结束，现在进入正题。]]></summary></entry><entry><title type="html">MoE 变 Dense：剪枝+蒸馏能救内存瓶颈吗？</title><link href="https://marsggbo.github.io/blog/2026/2605_28207/" rel="alternate" type="text/html" title="MoE 变 Dense：剪枝+蒸馏能救内存瓶颈吗？"/><published>2026-06-07T00:00:00+00:00</published><updated>2026-06-07T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/2605_28207</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/2605_28207/"><![CDATA[<h1 id="moe-变-dense剪枝蒸馏能救内存瓶颈吗">MoE 变 Dense：剪枝+蒸馏能救内存瓶颈吗？</h1> <blockquote> <p>📖 <strong>新书推荐</strong>：如果你对 MoE 部署优化、模型压缩、自动搜索这类话题感兴趣，欢迎看看我们刚出版的<a href="https://item.jd.com/15384990.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a>——系统讲透搜索空间设计、效率评估、LLM 压缩与微调等核心方法，配有完整可运行代码。京东搜索「动手学 AutoML」即可。</p> </blockquote> <hr/> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2605.28207">Pruning and Distilling Mixture-of-Experts into Dense Language Models</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你是否想过这个问题：</p> <p><strong>Qwen3-30B-A3B 推理起来爽，但你得先把 30B 参数全部塞进显存——哪怕每个 token 只激活 3B。</strong></p> <p>这就是整个 MoE 时代最核心的矛盾：稀疏激活带来了计算上的高效，但内存瓶颈根本没有解决。MoE 能让你省算力，却没法省显存。</p> <p>所以自然就有人问：<strong>能不能把训练好的 MoE 直接”变成”一个 dense 模型？</strong> 不是从头训 dense，也不是压缩成小 MoE，而是把 expert 的知识直接浓缩到一个标准的 FFN 里？</p> <p>今天想和大家聊聊这篇来自 KRAFTON 的工作——据我所知，这是第一个系统性研究 MoE-to-dense 转换的框架，而且结论相当有意思。</p> <hr/> <h2 id="2-背景moe-的内存困境">2. 背景：MoE 的内存困境</h2> <p>先交代下背景。</p> <p>现在主流的大模型基本都是 MoE 架构：DeepSeek-V3/V4、Qwen3、Llama 4、GLM-5……这些模型的共同设计思路是：<strong>有很多 expert，但每个 token 只路由到其中少数几个</strong>。</p> <p>以 Qwen3-30B-A3B 为例：</p> <ul> <li>总参数：30B</li> <li>每个 MoE 层有 128 个 expert</li> <li>每个 token 激活其中 8 个</li> <li>实际参与计算的参数：~3B</li> </ul> <p>从计算量看，30B 的 MoE 用起来接近 3B dense 的成本。但从内存来看——你得把 30B 全部加载进来，不然 router 不知道往哪路由。</p> <p>这就结结实实踩了个大坑：<strong>你买单卡 A100 跑不了，买多卡又觉得没必要</strong>，因为大多数参数在推理时根本用不上，就是坐在那里吃显存。</p> <hr/> <h2 id="3-现有方案的痛点">3. 现有方案的痛点</h2> <p>面对这个问题，学术界已经有不少工作了：</p> <h3 id="31-expert-剪枝把-moe-压缩成小-moe">3.1 Expert 剪枝（把 MoE 压缩成小 MoE）</h3> <p>REAP、MC-SMoE、HC-SMoE、Sub-MoE、MergeMoE……这些工作的思路是<strong>减少 expert 数量</strong>，把 128 个 expert 压缩成更少。但有一个本质问题没有解决：</p> <blockquote> <p><strong>输出还是 MoE 模型</strong>。你还是需要 router，还是需要把所有保留的 expert 全部加载进显存。内存瓶颈的根本矛盾没有消失。</p> </blockquote> <p>如下图，Table 1 对现有方法做了梳理——所有 prior work 都只能产出更小的 MoE，而这篇工作第一个把目标定在了完全的 dense 模型上：</p> <p><img src="/assets/img/posts/2605_28207/table1_p2.png" alt="现有MoE压缩方法对比"/></p> <h3 id="32-dense-模型剪枝dense-to-dense">3.2 Dense 模型剪枝（Dense-to-Dense）</h3> <p>另一条路是先训一个大 dense 模型（比如 Qwen3-32B），再剪枝蒸馏到小 dense 模型。Minitron 就是走这条路。</p> <p>问题是：</p> <ol> <li>你得先有个大 dense teacher，训练成本本身就不低</li> <li>大 dense teacher（32B）在推理时每个 token 全量激活，给学生做蒸馏时计算效率低</li> <li>dense 剪枝在高压缩比下效果很差（后面实验数据会说明）</li> </ol> <hr/> <h2 id="4-这篇工作的核心思路">4. 这篇工作的核心思路</h2> <p>这篇论文提出了一个三步走的框架，把 MoE 直接转成 dense：</p> <p><strong>第一步</strong>：对 MoE 层里的所有 expert 打分，选出最重要的 K 个。</p> <p><strong>第二步</strong>：把 K 个 expert 分成 k 组（k 是原来 MoE 的 top-k 数），每组内做 score-weighted 平均得到一个代表性 FFN 块，再把 k 个 FFN 块拼接成一个完整的 dense FFN。</p> <p><strong>第三步</strong>：用原始 MoE 作为 teacher，对 dense 学生做知识蒸馏，把蒸馏过程中的精度损失补回来。</p> <p>如下图是完整的 pipeline 和与其他方法的对比：</p> <p><img src="/assets/img/posts/2605_28207/figure1_p1.png" alt="MoE-to-dense pipeline 和效果对比"/></p> <p>这张图非常关键——左边对比了三种产出 3B dense 模型的方式（MoE-to-dense、Dense-to-dense pruning、Random Init），MoE-to-dense 大幅领先；右边是 pipeline 示意图，三步转换一目了然。</p> <hr/> <h2 id="5-方法细节打分才是关键">5. 方法细节：打分才是关键</h2> <h3 id="51-expert-打分七种方法的对决">5.1 Expert 打分：七种方法的对决</h3> <p>这篇工作花了大量篇幅研究<strong>如何给 expert 打分</strong>——这是整个 pipeline 中最关键的决策，比怎么分组更重要。</p> <p>论文一共评测了 7 种打分方法，大致分三类：</p> <p><strong>频率类</strong>（SF/PP/PS）：看 expert 被选中了多少次、路由概率多高。这类方法偏爱”generalist”expert——服务的 token 多，但选的 expert 高度重叠，没什么多样性。</p> <p><strong>条件概率类</strong>（CP/ACP）：CP 只统计被路由到时的平均概率，过滤掉频率信息，专注于 router 真正”有把握”时的分数；ACP 在 CP 基础上再乘以输出范数，把 expert 的实际输出贡献也算进去。</p> <p><strong>D-Optimal 类</strong>（DO-CP/DO-ACP）：这是这篇论文自己提出的方法。核心思想是：独立给每个 expert 打分会选出很多”同质”的 expert，浪费了 dense FFN 的表示能力。D-Optimal 准则用 Gram 矩阵的 log-determinant 来同时衡量每个 expert 的重要性和 expert 之间的多样性，保证选出来的 k 个 expert 尽可能”各司其职”。</p> <p>如下图展示了不同打分方法选出的 top-8 expert 之间的重叠度：</p> <p><img src="/assets/img/posts/2605_28207/figure2_p5.png" alt="不同打分方法选择的expert重叠度"/></p> <p>频率类方法（SF/PP/PS）选出来的 expert 几乎完全重叠——本质上它们在选同一批”万金油”expert。DO-ACP 和其他方法的重叠度只有 ≤0.08，选出的 expert 要多样得多。</p> <p>下图则是 effective rank（有效秩，衡量多样性的指标，满分 8）的对比：</p> <p><img src="/assets/img/posts/2605_28207/figure3_p5.png" alt="Expert集合的有效秩对比"/></p> <p>DO-Optimal 选出的 expert 集合有效秩从 6.07 提升到 7.37（基于 CP），或从 6.31 提升到 6.93（基于 ACP）。换句话说，它真的让 dense FFN 的每个”槽位”都发挥了不同的作用。</p> <h3 id="52-分组策略五种方法差距不大">5.2 分组策略：五种方法，差距不大</h3> <p>把 K 个 expert 分成 k 组，每组合并为一个 FFN 块，共有五种策略：轮询（RR）、权重聚类（WC）、router 聚类（RC）、Anchor-based（AB）、输出聚类（OC）。</p> <p>实验结论很干脆：<strong>分组策略对最终结果的影响远小于打分方法</strong>，只贡献约 1 pp 的差距。而且当 K=k 时（纯剪枝，不做合并），分组根本不重要——因为每组只有一个 expert，没有”组内平均”这回事。</p> <h3 id="53-down-projection-缩放弥合静态权重和动态路由的差距">5.3 Down-projection 缩放：弥合静态权重和动态路由的差距</h3> <p>MoE 里 router 是 token-dependent 的，而 dense FFN 的权重是静态的。怎么弥合？</p> <p>论文用了两种简单的静态缩放方案：均匀缩放（每组权重 ×1/k）和比例缩放（按 expert 重要性分配权重）。实验发现差距很小，但打分越极端（比如 CP 选的 expert 概率差异大），比例缩放更好。</p> <hr/> <h2 id="6-实验结果350-种配置的大扫描">6. 实验结果：350 种配置的大扫描</h2> <p>这篇工作一口气跑了 <strong>350 种组合</strong>（7 打分 × 5 分组 × 2 缩放 × 5 个 K 值），主要在 Qwen3-30B-A3B 上做评测，然后在 DeepSeek-V2-Lite 和 GPT-OSS-20B 上做验证。</p> <h3 id="61-打分策略决定上限">6.1 打分策略决定上限</h3> <p>如下图展示了各配置的下游任务平均准确率（0.3B token 蒸馏后）：</p> <p><img src="/assets/img/posts/2605_28207/figure4_p8.png" alt="打分和分组方法的边际效应"/></p> <p>结论非常清晰：</p> <ul> <li>最好的打分（DO-ACP）vs 最差的打分（PP），差距 <strong>5.7 pp</strong></li> <li>最好的分组（RR）vs 最差的分组（AB），差距只有 <strong>1.2 pp</strong></li> </ul> <p>打分是核心。</p> <h3 id="62-纯剪枝kk胜过合并">6.2 纯剪枝（K=k）胜过合并</h3> <p>最好的 7 个配置全部使用 K=8 的纯剪枝——就是选出最好的 8 个 expert，直接拼接，完全不做组内平均。虽然合并（K&gt;k）在蒸馏前 PPL 更低，但蒸馏之后排名逆转：DO-ACP 在 K=8 下达到 43.41% 平均准确率，而 ACP×OC K=16 只有 40.50%。</p> <p>为什么？直觉上，DO-ACP 选出的 expert 本来就已经足够多样，强行合并反而把有用的个性化信息抹掉了；而频率类方法选出一堆同质 expert，合并反而有助于去冗余。</p> <h3 id="63-和其他方法的对比">6.3 和其他方法的对比</h3> <p>如下表，跟各种 baseline 的硬碰硬对比：</p> <p><img src="/assets/img/posts/2605_28207/table3_p8.png" alt="与其他方法的对比"/></p> <ul> <li>DO-ACP（43.41%）vs MC-SMoE 打分方式（36.31%）：<strong>+7.1 pp</strong></li> <li>DO-ACP vs Dense-to-Dense 剪枝（33.28%）：<strong>+10.1 pp</strong></li> <li>DO-ACP vs Random FFN（32.70%）：<strong>+10.7 pp</strong></li> </ul> <p>Dense-to-Dense 剪枝在这个压缩比（30B→3B）下几乎和随机初始化一样，只比 random 好 0.6 pp。这说明 dense 剪枝在极高压缩比下结构优势很弱，而 MoE expert 提供了一个强得多的初始化。</p> <h3 id="64-扩展训练4b-token-后效果">6.4 扩展训练：4B token 后效果</h3> <p>蒸馏量放大到 4B token，结论依然稳定：</p> <p><img src="/assets/img/posts/2605_28207/table6_p10.png" alt="扩展训练结果"/></p> <p>DO-ACP 达到 <strong>58.10%</strong> 平均准确率，比 D2D 剪枝（51.84%）高 <strong>+6.3 pp</strong>，而且训练速度还快 <strong>1.6 倍</strong>。</p> <p>速度快这件事很重要：MoE teacher（Qwen3-30B-A3B）每个 token 只激活 3B 参数，而 Dense teacher（Qwen3-32B）需要跑全量 32B——MoE teacher 蒸馏更省算力。</p> <p>验证损失曲线如下：</p> <p><img src="/assets/img/posts/2605_28207/figure5_p10.png" alt="扩展训练的验证损失曲线"/></p> <p>DO-ACP 在整个训练过程中始终保持最低的验证损失，优势没有随训练量扩大而消失。</p> <h3 id="65-定性分析错误模式对比">6.5 定性分析：错误模式对比</h3> <p>论文还做了 MMLU chain-of-thought 的定性分析，用 Claude Opus 4.6 作为 judge 对 567 道题的回答做分类。</p> <p>如下图展示了各模型的错误分布：</p> <p><img src="/assets/img/posts/2605_28207/figure6_p11.png" alt="MMLU错误分布分析"/></p> <p>从左到右模型质量提升，灾难性失败（语无伦次、重复循环）比例下降。DO-ACP 的灾难失败率（54.5%）是所有方法中最低的，同时知识错误率也最低（4.2% vs D2D 的 12.5%）。</p> <p>MMLU 人文类题目上差距最大：DO-ACP 达到 49.2%，SF 只有 25.4%，D2D 只有 19.2%。知识密集型任务下多样性打分的优势更明显——选出多样的 expert 确实更好地保留了世界知识。</p> <h3 id="66-跨模型验证">6.6 跨模型验证</h3> <p>在 DeepSeek-V2-Lite 和 GPT-OSS-20B 上的验证结果：</p> <p><img src="/assets/img/posts/2605_28207/figure7_p12.png" alt="跨模型验证结果"/></p> <p>核心结论在三个模型上都成立：DO-ACP 最好，纯剪枝（K=k）最好。但有一个有趣的规律：<strong>expert 池越大，多样性打分的优势越明显</strong>。</p> <ul> <li>Qwen3（128 experts）：最好 vs 最差打分差 <strong>7.1 pp</strong></li> <li>DeepSeek-V2-Lite（64 experts）：差距 <strong>4.3 pp</strong></li> <li>GPT-OSS-20B（32 experts）：差距 <strong>1.6 pp</strong></li> </ul> <p>直觉上也说得通：expert 越多，高质量 expert 之间冗余越严重，多样性筛选的价值越大；expert 数少时每个 expert 覆盖的 token 模式更不一样，独立打分也能选出比较多样的集合。</p> <hr/> <h2 id="7-这个工作值不值得前景如何">7. 这个工作值不值得？前景如何？</h2> <p>说几点个人判断。</p> <h3 id="71-内存问题确实是真问题">7.1 内存问题确实是真问题</h3> <p>这不是为了发论文而造的问题。MoE 在实际部署中内存占用确实是卡脖子的点——尤其是单卡 serving、边缘设备、on-device 推理。把 30B MoE 变成等效 3B dense，这个需求非常真实。</p> <h3 id="72-moe-是更好的起点">7.2 MoE 是更好的起点</h3> <p>这篇论文最重要的发现不是某个具体方法，而是<strong>MoE-to-dense 比 Dense-to-dense 剪枝强得多</strong>这个事实。</p> <p>在相同参数量下，MoE 的 expert 天然提供了更好的初始化：每个 expert 虽然参数少，但它们捕捉了不同的 token 模式，把它们拼在一起就已经构成了一个质量不错的 dense FFN，蒸馏只是锦上添花。而 dense 剪枝从一个”大而全”的网络暴力剪到小，在极高压缩比下结构信息丢失严重。</p> <h3 id="73-d-optimal-的思路有普适性">7.3 D-Optimal 的思路有普适性</h3> <p>把 D-optimality（实验设计里的经典准则）用到 expert 选择上，是个挺优雅的迁移。它本质上是在问”我用有限的 budget，怎么选一个信息量最大的子集”——这个问题在 active learning、coreset 选取、模型压缩里都有对应。</p> <p>这篇工作的理论部分也是认真的——证明了 greedy 选择的 (1-1/e) 近似比，有冗余时独立打分会失败的反例，以及有限样本下的稳定性保证。不是随便套了个公式。</p> <h3 id="74-局限和还没解决的问题">7.4 局限和还没解决的问题</h3> <p>说实话，这篇工作还有几个明显的限制：</p> <p><strong>压缩比很高</strong>：Qwen3-30B-A3B → 3.3B，压缩了近 10 倍。4B token 蒸馏之后 58.10% 的均分，而同规模 Qwen3-1.7B（比这个学生还小）预训练后能到 66.63%。差距还有 8.5 pp。如果目标是部署，还需要更多的蒸馏数据。</p> <p><strong>蒸馏量有限</strong>：4B token 对于 LLM 来说仍然是很少的量。论文自己也承认，需要扩到数十亿 token 级别才能真正评估质量天花板。</p> <p><strong>专家池大小影响收益</strong>：GPT-OSS-20B（32 experts）上 MoE-to-dense 相对 random baseline 只有 +3.7 pp，而 Qwen3 上有 +10.7 pp。如果 MoE 的 expert 数本来就不多，转换成 dense 的优势就没那么显著了。</p> <p><strong>合并策略还可以优化</strong>：当前方案把合并权重和打分权重绑定，论文承认解耦可能更好。</p> <h3 id="75-这个方向有没有前景">7.5 这个方向有没有前景？</h3> <p>我觉得是有的，但需要看清楚它解决的是哪个问题。</p> <p>它<strong>不是</strong>想训练出一个最强的 3B 模型——从头预训练肯定能做得更好。它解决的是”<strong>我已经有了一个训练好的强 MoE，我不想重新训，但我需要一个能跑在内存受限环境里的 dense 版本</strong>“这个问题。</p> <p>这个场景在 on-device、私有部署、边缘计算场景下非常常见。而且随着 MoE 成为旗舰模型的标配，MoE-to-dense 的”原材料”会越来越多——每一个 MoE 都是潜在的 dense 小模型工厂。</p> <p>比较有意思的后续方向：</p> <ul> <li>能不能在蒸馏时引入更多数据（domain-specific）来做领域适配？</li> <li>MoE-to-dense 和 LoRA 微调结合，能不能进一步弥合质量差距？</li> <li>expert 粒度更细时（如 DeepSeek 的 fine-grained MoE），转换策略需要怎么调整？</li> </ul> <hr/> <h2 id="8-总结">8. 总结</h2> <p>一句话：<strong>把 MoE 剪成 dense，打分方法是核心，多样性比频率更重要，最终产物比 Dense-to-Dense 剪枝强 +6.3 pp，还快 1.6 倍。</strong></p> <p>这篇工作做的事情很朴素——系统地扫了一遍所有可能的设计选择，找出哪个因素最关键，然后把结论在多个模型上验证了。没有花哨的创新，但结论实在，实验扎实。</p> <p>MoE 的内存瓶颈是整个行业的核心矛盾之一，MoE-to-dense 提供了一个新的思路。值得关注。</p> <p>欢迎评论区交流，如果有踩过类似坑的也欢迎分享。</p> <hr/> <p><em>不排除穿插了个人私货哈哈，有问题欢迎指出</em></p>]]></content><author><name></name></author><category term="LLM"/><category term="MoE"/><category term="论文解读"/><category term="模型压缩"/><summary type="html"><![CDATA[MoE 变 Dense：剪枝+蒸馏能救内存瓶颈吗？]]></summary></entry><entry><title type="html">ReMoE：只动 Router 就让 MoE 推理快 2 倍？这才是端侧 MoE 部署该有的姿势</title><link href="https://marsggbo.github.io/blog/2026/2605_27081/" rel="alternate" type="text/html" title="ReMoE：只动 Router 就让 MoE 推理快 2 倍？这才是端侧 MoE 部署该有的姿势"/><published>2026-06-03T00:00:00+00:00</published><updated>2026-06-03T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/2605_27081</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/2605_27081/"><![CDATA[<blockquote> <p>插播：之前写的<a href="https://item.jd.com/14945889.html">《动手学 AutoML》</a>终于出版了，从 NAS 到超参优化都有覆盖，适合想系统入门 AutoML 的同学。好了广告结束，现在进入正题。</p> <p><img src="/assets/img/book_cover_automl.png" alt="动手学AutoML书籍封面"/></p> </blockquote> <h1 id="remoe只动-router-就让-moe-推理快-2-倍这才是端侧-moe-部署该有的姿势">ReMoE：只动 Router 就让 MoE 推理快 2 倍？这才是端侧 MoE 部署该有的姿势</h1> <blockquote> <p>原文：<a href="https://arxiv.org/abs/2605.27081">ReMoE: Boosting Expert Reuse through Router Fine-Tuning in Memory-Constrained MoE LLM Inference</a></p> </blockquote> <hr/> <h2 id="1-前言">1. 前言</h2> <p>你有没有想过一个问题：MoE 模型的”稀疏激活”理论上是推理效率的银弹——每个 token 只用 Top-K 个 expert，计算量小得很。但<strong>当你把 MoE 模型塞到手机/边缘设备上的时候</strong>，为什么体验反而比同等计算量的 dense 模型还卡？</p> <p>答案是<strong>内存瓶颈</strong>。</p> <p>以 DeepSeek-V2-Lite 为例，它有 64 个 routed expert，每个 token 选 6 个。但如果你设备的快速内存只能装下 6 个 expert 的权重，那每生成一个 token，只要选中的 expert 集合跟上一步不一样，就得从慢速存储（UFS/NVMe SSD）把新 expert 搬进来。搬一次就是几百 MB 的权重 I/O。</p> <p><strong>这也是整个 MoE 端侧部署的核心矛盾：模型的稀疏激活降低了计算量，但 expert 的频繁切换导致 I/O 成了新的瓶颈。</strong></p> <p>今天想和大家聊聊这篇被 ICML 2026 接收的工作——<strong>ReMoE</strong>。它的思路非常优雅：不改模型结构、不改推理引擎、不加运行时策略，<strong>只 fine-tune router（gate 参数）</strong>，就让 MoE 在边缘设备上的推理速度提升 1.77–1.99×。</p> <hr/> <h2 id="2-问题定义moe-offloading-的-locality-gap">2. 问题定义：MoE Offloading 的 Locality Gap</h2> <h3 id="21-端侧-moe-推理的困境">2.1 端侧 MoE 推理的困境</h3> <p>先交代下背景。在内存受限的设备上跑 MoE 模型，通常只有少量 expert 能常驻快速内存（比如 GPU 显存、DRAM），剩下的 expert 要从慢速存储（UFS 4.0、NVMe SSD）按需加载。这种模式叫 <strong>expert offloading</strong>。</p> <p>问题是：标准 MoE 训练时用了 load-balancing loss，目的是让 token 尽量均匀分配给所有 expert，防止训练时 expert parallelism 出问题。这个优化目标在训练时很合理，但到了内存受限的单请求推理场景，就变成了灾难——router 被训练得尽可能”散”地选 expert，导致相邻 token 几乎不复用同一批 expert。</p> <p>如下图，对比了 Baseline 和 ReMoE 的 expert 访问模式：</p> <p><img src="/assets/img/posts/2605_27081/figure1_p2.png" alt="Expert I/O 动态对比"/></p> <p>左边是标准 router 的行为：每一步选的 expert 集合和上一步差异很大，cache 被频繁淘汰替换，产生大量 I/O thrashing。右边是 ReMoE：相邻步之间 expert 复用率大幅提高，cache 替换频率下降，I/O overhead 显著减少。</p> <h3 id="22-量化指标eor">2.2 量化指标：EOR</h3> <p>为了量化这个”缓存友好度”，论文定义了两个指标：</p> <p><strong>Instantaneous Reuse (IR)</strong>：相邻两步 expert 集合的重叠比例：</p> \[IR_t = \frac{|E_t \cap E_{t-1}|}{K}\] <p><strong>Expert Overlap Ratio (EOR)</strong>：整个序列的平均 IR：</p> \[EOR = \frac{1}{T-1}\sum_{t=2}^{T} IR_t\] <p>EOR 越高，意味着 cache miss 越少。论文还给出了一个关键命题：在标准 LRU cache（容量 $C \geq K$）下，平均 expert fetch 次数的上界是 $K(1 - EOR)$。<strong>所以提高 EOR，就是直接减少 I/O。</strong></p> <h3 id="23-训练-推理的-mismatch">2.3 训练-推理的 Mismatch</h3> <p>如下图，可以清楚看到 baseline router 的 routing trajectory：</p> <p><img src="/assets/img/posts/2605_27081/figure2_p5.png" alt="Routing trajectory 对比"/></p> <p>这是 DeepSeek-V2-Lite 第 21 层在 teacher forcing 下的 expert 选择轨迹。Baseline（左）可以看到短暂的复用，但频繁切换。ReMoE（右）通过 gate-only fine-tuning 把这些短暂的复用”拉长”了，形成明显的水平条纹——同一个 expert 被连续多步选中。</p> <p>重点：输入是固定的（teacher forcing），所以差异完全来自 routing policy 的改变，不是生成内容不同。</p> <hr/> <h2 id="3-remoe-方法详解">3. ReMoE 方法详解</h2> <h3 id="31-总览冻结一切只调-gate">3.1 总览：冻结一切，只调 Gate</h3> <p>ReMoE 的核心思路极其简洁：<strong>冻结所有非 router 参数</strong>（embedding、attention、expert FFN 全部不动），只 fine-tune gate 参数 $\theta_{\text{gate}}$。</p> <p>如下图展示了 ReMoE 在单个 MoE 层内的工作流程：</p> <p><img src="/assets/img/posts/2605_27081/figure3_p5.png" alt="ReMoE 方法概览"/></p> <p>具体步骤：</p> <ol> <li> <p><strong>Step 1</strong>：给定当前 hidden state $h_t$，同时跑两个 router——一个是冻结的 reference router（用预训练权重 $\theta^0_{\text{gate}}$ 的 FP32 快照），产出 $P^{\text{ref}}_t$；一个是可训练的 router，产出 $P_t$。</p> </li> <li><strong>Step 2</strong>：用两个信号优化可训练 router： <ul> <li><strong>Semantic Anchor</strong>（Trust-KL）：让 $P_t$ 不要偏离 $P^{\text{ref}}_t$ 太远</li> <li><strong>Temporal Locality Signal</strong>：让 $P_t$ 倾向于复用最近几步选过的 expert</li> </ul> </li> <li> <p><strong>Step 3-4</strong>：记录当前选择的 Top-K expert 到历史 buffer，供后续步骤使用</p> </li> <li><strong>Step 5</strong>：正常执行 Top-K expert，和 baseline 完全一致</li> </ol> <p><strong>关键洞察：训练时多了 reference router 的前向计算，但部署时完全没有任何额外开销</strong>——用的就是标准 MoE 推理图，只是 gate 权重被替换成了 fine-tune 后的版本。</p> <h3 id="32-训练目标">3.2 训练目标</h3> <p>总目标函数：</p> \[\mathcal{L} = L_{\text{CE}} + \lambda_{\text{KL}} L_{\text{Trust}} + \alpha_t L_{\text{Loc}}\] <p>其中 $L_{\text{CE}}$ 是标准 next-token cross entropy，$L_{\text{Trust}}$ 是语义锚点，$L_{\text{Loc}}$ 是时域局部性正则，$\alpha_t$ 是 warmup 系数。</p> <p>注意一个细节：<strong>训练时把标准 MoE load-balancing loss 关掉了</strong>（$L_{\text{aux}} = 0$），因为 load-balancing 的目标是让 expert 使用尽可能均匀——这和 cache locality 的目标直接冲突。</p> <h3 id="33-semantic-anchortrust-kl-loss">3.3 Semantic Anchor：Trust-KL Loss</h3> <p>说人话就是：不能让 router 为了提高复用率就乱飘，得”锚住”预训练学到的路由语义。</p> <p>具体做法是用 KL 散度约束当前 $P_t$ 和冻结参考分布 $P^{\text{ref}}_t$ 的距离：</p> \[L_{\text{Trust}} = \frac{1}{T}\sum_{t=1}^{T} D_{\text{KL}}(P_t \| \text{stop\_gradient}(P^{\text{ref}}_t))\] <p>这里用 stop_gradient 是因为 $P^{\text{ref}}_t$ 来自冻结分支，不需要梯度回传。</p> <p>为什么选 KL 而不是 L2 之类的？因为 routing 本身就是概率分布，KL 散度天然适合衡量分布偏移，而且对高概率 expert（实际会被 Top-K 选中的）赋予更大权重。</p> <h3 id="34-temporal-locality-regularization">3.4 Temporal Locality Regularization</h3> <p>这是 ReMoE 的核心技术贡献，由四个子项组成：</p> \[L_{\text{Loc}} = \lambda_{\text{Reuse}} L_{\text{Reuse}} + \lambda_{\text{Smooth}} L_{\text{Smooth}} + \lambda_{\text{Lag}} L_{\text{Lag}} + \lambda_{\text{WS}} L_{\text{WS}}\] <p>逐一解释：</p> <p><strong>① Reuse Loss $L_{\text{Reuse}}$</strong>——直接提高短期复用</p> <p>先定义”reuse mass”：当前步分配给上一步选中 expert 的概率质量：</p> \[m_t = \frac{1}{K}\sum_{k \in \tilde{E}_{t-1}} P_t(k)\] <p>其中 $\tilde{E}<em>{t-1} = \text{stop_gradient}(E</em>{t-1})$，即上一步的 Top-K 集合作为固定目标。然后对整个序列取平均并取负对数：</p> \[\rho = \frac{1}{T-1}\sum_{t=2}^{T} m_t, \quad L_{\text{Reuse}} = -\log(\rho + 10^{-8})\] <p>直觉上就是：鼓励当前步把更多概率分配给上一步用过的 expert，增加复用的可能性。</p> <p><strong>② Smoothness Loss $L_{\text{Smooth}}$</strong>——抑制高频抖动</p> <p>用对称 KL 散度惩罚相邻步分布的突变：</p> \[L_{\text{Smooth}} = \frac{1}{T-1}\sum_{t=2}^{T} \text{SymKL}(P_t, P_{t-1})\] <p>如果 $P_t$ 相邻步变化越小，Top-K 边界被跨越的可能性就越低。注意这里<strong>没有</strong> stop_gradient，因为希望双向约束：$P_t$ 要靠近 $P_{t-1}$，反过来也一样。</p> <p><strong>③ Lagged Inertia Loss $L_{\text{Lag}}$</strong>——抑制慢漂移</p> <p>只看相邻步可能忽略”温水煮青蛙”的漂移——每步变化很小但累积起来 expert 集合完全不同了。用一个 lag set $\mathcal{D} = {1, 2, 4, 8, 16}$ 对齐多个历史步：</p> \[L_{\text{Lag}} = \frac{1}{T-1}\sum_{t=2}^{T} \frac{1}{|\mathcal{D}|}\sum_{d \in \mathcal{D}, t-d \geq 1} \text{SymKL}(P_t, P_{t-d})\] <p><strong>④ Working-Set Compression Loss $L_{\text{WS}}$</strong>——压缩局部工作集</p> <p>前三个都不能防止 router 在一个稍大的窗口内访问太多不同 expert。这个 loss 对窗口内平均分布做熵最小化：</p> \[\bar{P}_b = \frac{1}{W}\sum_{j=1}^{W} P_{(b-1)W+j}, \quad L_{\text{WS}} = \frac{1}{n}\sum_{b=1}^{n} H(\bar{P}_b)\] <p>鼓励局部窗口内只依赖少量 expert，让 routing 适配小 cache 容量。</p> <hr/> <h2 id="4-实验设置">4. 实验设置</h2> <ul> <li><strong>模型</strong>：DeepSeek-V2-Lite（15.7B 总参数，2.4B 激活参数，64 routed expert + 2 shared expert，Top-6 routing）</li> <li><strong>数据</strong>：OpenHermes-2.5，100K 样本训练，1K 验证</li> <li><strong>训练</strong>：2000 步，AdamW，lr=5e-5，序列长度 2048，micro-batch=1 + grad_accum=8</li> <li><strong>超参</strong>：$\lambda_{\text{KL}}=0.45$，$\lambda_{\text{reuse}}=0.2$，$\lambda_{\text{smooth}}=0.05$，$\lambda_{\text{lag}}=0.05$，$\lambda_{\text{ws}}=0.01$</li> <li><strong>训练成本</strong>：单张 80GB GPU 约 7.9 小时。<strong>一次性成本，推理时零开销。</strong></li> </ul> <hr/> <h2 id="5-实验结果">5. 实验结果</h2> <h3 id="51-routing-locality-提升">5.1 Routing Locality 提升</h3> <p>如下图展示了定量结果：</p> <p><img src="/assets/img/posts/2605_27081/table2_p7.png" alt="Routing 指标对比"/></p> <p>核心数字：</p> <ul> <li><strong>EOR 从 27.3% 提升到 34.5%</strong>，相对提升 +26.4%</li> <li>作为对照，CE-only（用相同数据和设置只优化交叉熵，不加 locality loss）的 EOR 反而降到了 22.9%——说明单纯 continued fine-tuning 不仅没用，反而有害</li> <li>Routing entropy 从 0.9998 降到 0.9971（微降），负载均衡 CV 从 0.04 升到 0.16（可接受，因为 B=1 场景没有 expert parallelism）</li> <li><strong>序列级 expert 多样性几乎不变</strong>（64.000 → 63.997），说明 concentration 是 step-level 的局部集中，而不是全局 routing collapse</li> </ul> <h3 id="52-cache-效率提升">5.2 Cache 效率提升</h3> <p><img src="/assets/img/posts/2605_27081/table9_p22.png" alt="Cache 效率数据"/></p> <p>在 LRU cache 下，不同容量 $C$ 的结果：</p> <ul> <li>$C=6$（和 Top-K 匹配）：uHR 从 0.3187 提升到 0.3687（+15.7%），#uMiss 从 0.87M 降到 0.81M（-7.3%）</li> <li>$C=12$：uHR 从 0.4519 提升到 0.5035（+11.4%），#uMiss 从 0.70M 降到 0.63M（-9.4%）</li> <li>在 LFU、FIFO、甚至 Belady 最优策略下都有类似提升</li> </ul> <h3 id="53-真实系统评测">5.3 真实系统评测</h3> <p><strong>vLLM Expert Offloading（RTX 3090）</strong></p> <p><img src="/assets/img/posts/2605_27081/table4_p8.png" alt="vLLM 评测结果"/></p> <ul> <li>输出吞吐从 3.58 提升到 3.88 tok/s（<strong>+8.4%</strong>）</li> <li>TPOT 从 254.31ms 降到 242.99ms（-4.5%）</li> <li>uHR 从 39.4% 提升到 43.4%</li> <li>CE-only 反而比 baseline 还差（2.95 tok/s），再次证明 locality loss 的必要性</li> </ul> <p><strong>Jetson Orin NX 16GB 边缘设备（llama.cpp + NVMe SSD）</strong></p> <p><img src="/assets/img/posts/2605_27081/table5_p8.png" alt="Jetson 评测结果"/></p> <p>这才是 ReMoE 真正大显身手的场景——SSD 后端的 expert miss 代价更高：</p> <ul> <li><strong>ShareGPT</strong>：TPOT 从 554.69ms 降到 306.27ms，<strong>减少 44.8%</strong>，decode speedup <strong>1.81×</strong></li> <li><strong>GSM8K</strong>：TPOT 从 613.73ms 降到 346.04ms，<strong>减少 43.6%</strong>，decode speedup <strong>1.77×</strong></li> <li><strong>HumanEval</strong>：TPOT 从 672.68ms 降到 337.61ms，<strong>减少 49.8%</strong>，decode speedup <strong>1.99×</strong></li> </ul> <p>细思极恐：<strong>只改了 router 的权重，没动任何其他东西</strong>，推理速度翻倍。</p> <h3 id="54-模型能力保持">5.4 模型能力保持</h3> <p><img src="/assets/img/posts/2605_27081/table6_p8.png" alt="Benchmark 结果"/></p> <ul> <li>MMLU：57.72 → 57.81（+0.09 pp），几乎不变</li> <li>HumanEval：26.83 → 29.27（+2.44 pp），反而提升了</li> <li>GSM8K（strict）：38.89 → 38.13（-0.76 pp），在误差范围内</li> <li>IFEval：基本持平</li> </ul> <p><strong>结论：locality gain 不以牺牲模型能力为代价。</strong></p> <hr/> <h2 id="6-消融实验">6. 消融实验</h2> <p><img src="/assets/img/posts/2605_27081/table7_p9.png" alt="Ablation 结果"/></p> <p>消融研究逐一移除各组件，结论清晰：</p> <table> <thead> <tr> <th>变体</th> <th>EOR</th> <th>PPL</th> <th>解读</th> </tr> </thead> <tbody> <tr> <td>Full ReMoE</td> <td>0.3453</td> <td>3.2280</td> <td>完整方法</td> </tr> <tr> <td>w/o Reuse</td> <td>0.2831</td> <td>3.2222</td> <td>EOR 暴跌 -0.062，<strong>Reuse 是核心驱动力</strong></td> </tr> <tr> <td>w/o Consistency</td> <td>0.3290</td> <td>3.2254</td> <td>EOR 小降 -0.016，辅助稳定作用</td> </tr> <tr> <td>w/o Trust</td> <td>0.3877</td> <td>3.2629</td> <td>EOR 最高但 PPL 劣化，<strong>Trust 防止过度集中</strong></td> </tr> </tbody> </table> <p>三个关键发现：</p> <ol> <li><strong>Reuse loss 是 locality 提升的主力</strong>：去掉它 EOR 直接回到接近 baseline 水平</li> <li><strong>Consistency terms（Smooth + Lag + WS）起辅助稳定作用</strong>：主要抑制 routing 抖动和慢漂移</li> <li><strong>Trust-KL 是质量守门员</strong>：没有它 EOR 能更高（0.3877），但 PPL 开始劣化（3.2280 → 3.2629），routing 过度集中（CV: 0.16 → 0.21）</li> </ol> <hr/> <h2 id="7-超参敏感性">7. 超参敏感性</h2> <p><img src="/assets/img/posts/2605_27081/figure5_p25.png" alt="超参敏感性分析"/></p> <p>论文做了 $\lambda_{\text{reuse}}$ 和 $\lambda_{\text{KL}}$ 的 sweep：</p> <ul> <li> <p><strong>增大 $\lambda_{\text{reuse}}$</strong>：reuse score 从 0.283 线性增长到 0.370，同时 trust deviation 也增大（0.0098 → 0.0667）。但 PPL 和 Acc@1 在整个范围内保持稳定（PPL ≈ 3.22-3.24），说明在合理范围内提高复用不影响能力。</p> </li> <li> <p><strong>增大 $\lambda_{\text{KL}}$</strong>：trust deviation 从 0.308（$\lambda_{\text{KL}}=0$）降到 0.016（$\lambda_{\text{KL}}=0.7$），但 reuse 也从 0.386 降到 0.321。$\lambda_{\text{KL}}=0.45$ 是一个甜点：既有强复用增益，drift 也可控。</p> </li> </ul> <hr/> <h2 id="8-泛化性验证">8. 泛化性验证</h2> <p>论文不只在 DeepSeek 上做了验证：</p> <ul> <li> <p><strong>Qwen1.5-MoE-A2.7B</strong>：同样的 recipe，EOR 从 0.1695 提升到 0.2156（+27.2%），下游 benchmark 保持（MMLU 61.10 → 61.20，HumanEval 35.37 → 35.98）</p> </li> <li> <p><strong>Iluvatar BI-V150 GPU（国产卡）</strong>：EOR +25.0%，prefill 吞吐 2.47×，decode 吞吐 1.54×</p> </li> <li> <p><strong>华为昇腾 910B3 NPU + 鲲鹏 920</strong>：EOR +30.2%，生成吞吐从 4.3 提升到 4.8 tok/s（+11.6%）</p> </li> </ul> <p>跨模型、跨硬件平台都能 work，这说明方法的 generalization 不错。</p> <hr/> <h2 id="9-与推理时-cache-aware-rerouting-的比较">9. 与推理时 Cache-Aware Rerouting 的比较</h2> <p>一个自然的问题：为什么不在推理时直接给 cache 里已有的 expert 加分？</p> <p>论文做了对比实验：在推理时给 cached expert 的 router score 加一个 bonus $\beta$：</p> <ul> <li>$\beta=1.0$：uHR 41.66%，但 PPL 从 6.35 飙到 10.60</li> <li>$\beta=4.0$：uHR 63.88%，但 <strong>PPL 炸到 3607.92</strong>——模型直接废了</li> </ul> <p>而 ReMoE 的 learned router 不加任何推理时干预，uHR 23.74%，PPL 6.35——在一个更安全的 quality-locality trade-off 点上。</p> <p>更有趣的是，<strong>两者可以叠加</strong>：ReMoE + mild rerouting（$\beta=0.5$）比 Baseline + 同等 rerouting 效果更好（uHR 34.07% vs 32.07%，PPL 6.51 vs 6.97）。</p> <hr/> <h2 id="10-个人-take">10. 个人 Take</h2> <p>这篇论文让我印象深刻的几点：</p> <ol> <li> <p><strong>问题定义精准</strong>：把 MoE 训练时的 load-balancing 和部署时的 cache locality 之间的 mismatch 抽出来，定义为一个可优化的问题。这个 insight 本身就很有价值。</p> </li> <li> <p><strong>方法极其轻量</strong>：只动 gate 参数，2000 步，7.9 小时，一张卡。推理时零开销。这种”训练-部署”分离的思路非常工程友好。</p> </li> <li> <p><strong>效果惊人</strong>：在 Jetson 上接近 2× 的 decode speedup，而且<strong>没有牺牲模型能力</strong>。这在 MoE efficiency 方向是很少见的”白捡”收益。</p> </li> <li> <p><strong>与现有系统正交</strong>：ReMoE 改的是上游 routing trace，和下游的 cache 策略（LRU/LFU）、prefetch 策略、推理时 rerouting 都是互补的，可以叠加。</p> </li> </ol> <p>如果你在做 MoE 模型的端侧部署，或者对 expert offloading 系统感兴趣，这篇值得仔细读。代码和 checkpoint 已开源：<a href="https://github.com/BUAA-OSCAR/ReMoE">https://github.com/BUAA-OSCAR/ReMoE</a>。</p> <hr/> <p>欢迎评论区交流 / 指出问题。</p>]]></content><author><name></name></author><category term="LLM"/><category term="MoE"/><category term="推理优化"/><category term="论文解读"/><category term="ICML"/><summary type="html"><![CDATA[插播：之前写的《动手学 AutoML》终于出版了，从 NAS 到超参优化都有覆盖，适合想系统入门 AutoML 的同学。好了广告结束，现在进入正题。]]></summary></entry><entry><title type="html">LLM 时代，还有人搞 AutoML 吗？有，而且变得更难了</title><link href="https://marsggbo.github.io/blog/2026/automl-book/" rel="alternate" type="text/html" title="LLM 时代，还有人搞 AutoML 吗？有，而且变得更难了"/><published>2026-06-02T00:00:00+00:00</published><updated>2026-06-02T00:00:00+00:00</updated><id>https://marsggbo.github.io/blog/2026/automl-book</id><content type="html" xml:base="https://marsggbo.github.io/blog/2026/automl-book/"><![CDATA[<h1 id="llm-时代还有人搞-automl-吗有而且变得更难了">LLM 时代，还有人搞 AutoML 吗？有，而且变得更难了</h1> <hr/> <p>前几天有人在评论区问我：「现在都是 prompt engineering 和 fine-tune，NAS / AutoML 这条路还有人走吗？」</p> <p>我当时回了一句：「你说的那些，本质上就是 AutoML。」</p> <p>对方沉默了。</p> <hr/> <h2 id="1-一个被误解了十年的领域">1. 一个被误解了十年的领域</h2> <p>AutoML 这个词在国内有点尴尬——说出去，一半人以为是”傻瓜调参工具”，另一半人觉得这玩意儿已经过时了。</p> <p>但如果你真的做过这个方向，你会发现它背后在问的是一个<strong>永不过时的问题</strong>：</p> <blockquote> <p><strong>在一个复杂的搜索空间里，怎么用最少的代价找到最好的配置？</strong></p> </blockquote> <p>这个问题从来没有消失，只是换了马甲——</p> <ul> <li>2016 年以前，它叫”超参数优化”</li> <li>2017-2020 年，它叫”神经架构搜索（NAS）”</li> <li>2021 年以后，它叫”LLM 结构设计、量化策略搜索、LoRA rank 选择、KV cache 压缩”</li> </ul> <p><strong>换了个名字，核心矛盾还是那个</strong>：搜索空间太大，评估太贵，资源有限。</p> <hr/> <h2 id="2-写这本书之前我踩了什么坑">2. 写这本书之前，我踩了什么坑</h2> <p><img src="/assets/img/posts/automl-book/book_cover.jpg" alt="书封面"/></p> <p>说回这本书：<strong>《动手学 AutoML：从 NAS 到大语言模型优化实战》</strong>，机械工业出版社。</p> <p>我在 A*STAR CFAR 做 LLM 效率研究，之前在 HKBU 读博期间做了好几年 NAS。现在回头看，入门这个方向的时候，中文资料的确很匮乏——有的只讲概念，没有代码；有的代码能跑，但不知道在搜索什么；还有一类，就是把英文综述直接翻译一遍，读完仍然一脸懵。</p> <p>我自己摸清楚这个领域，靠的是啃论文、开源 <code class="language-plaintext highlighter-rouge">hyperbox</code> 框架、在知乎写了一堆文章——基本上是把别人踩过的坑再踩一遍，然后记下来。</p> <p>写书的念头就是这么来的：<strong>同样的坑，没必要让后来的人再踩一遍</strong>。</p> <hr/> <h2 id="3-llm-时代-automl-的新挑战">3. LLM 时代 AutoML 的新挑战</h2> <p>如果你觉得”NAS 就是搜网络结构、和 LLM 没什么关系”，那我得在这里纠正一下。</p> <p>GPT 系列刚出来的时候，大家都在想：模型越来越大，怎么让它跑得更快、用更少资源？</p> <p>这就引出了几类核心问题：</p> <p><strong>量化（Quantization）</strong>：把 float32 的权重压成 INT8 甚至 INT4，精度损失有多少？能不能自动找到每层的最优 bit-width？这是个搜索问题。</p> <p><strong>剪枝（Pruning）</strong>：Transformer 里有多少头是冗余的？注意力层可以砍多少？哪些 FFN 神经元可以移除？哪些不能？还是个搜索问题。</p> <p><strong>MoE 架构设计</strong>：DeepSeek、Mixtral 这类 MoE 模型，expert 的数量、每次激活几个、router 怎么设计——这些超参数怎么定？One-shot NAS 的逻辑可以直接迁移过来。</p> <p><strong>LoRA / PEFT 的配置</strong>：rank 选多少？用哪些层？不同任务的最优配置是不一样的，但你总不能每次都手调吧——这又是个 AutoML 问题。</p> <p><strong>本质上，LLM 时代反而把 AutoML 的重要性放大了</strong>，因为搜索空间更大、评估更贵、错误成本更高。以前跑错一次 NAS 实验，顶多浪费几张 GPU；现在搜错一次 LLM 量化策略，可能是几百张 A100 的时间。</p> <hr/> <h2 id="4-这本书讲了什么">4. 这本书讲了什么</h2> <p>书的结构大致是三层递进：从”为什么要自动化”，到 NAS 的各个范式，再到 LLM 时代的具体落地。</p> <p><strong>第一层：AutoML 基础</strong></p> <p>问题定义、搜索空间、优化策略——贝叶斯优化、进化算法、强化学习各自的适用场景和局限。这部分是打底，不会写成 wikipedia 词条那种风格，会告诉你每个方法在什么条件下会出问题。</p> <p><strong>第二层：NAS 核心范式</strong></p> <p>这是全书最厚的部分，因为这里有最多坑：</p> <ul> <li><strong>Black-box NAS</strong>：每个候选架构独立训练评估。最准，最贵，搜一次可能要 3000 GPU-days（谷歌当年 NASNet 就是这么出来的）</li> <li><strong>One-shot NAS / Weight Sharing</strong>：让所有候选共享一套 supernet 权重，把搜索成本压到几十个 GPU-hours。但有个核心问题：supernet 里排名第一的架构，独立训练后不一定还是第一。这个”ranking correlation”问题到现在还没有完美的解法</li> <li><strong>Predictor-based NAS</strong>：训练一个代理模型去预测架构的性能，少做几百次昂贵的评估。样本效率高，但代理模型本身的泛化性是个问题</li> <li><strong>Efficiency-aware NAS</strong>：把 latency、内存占用这些硬件约束直接加进搜索目标。这是最靠近工业落地的方向</li> </ul> <p>书里专门有一章讲 NAS-Bench 系列——这是这个领域少有的可以在上面做可重复实验的 benchmark，想做 NAS 研究但没有大量 GPU 的同学，强烈建议把这章读透。</p> <p><strong>第三层：LLM 时代的模型压缩与优化</strong></p> <p>涵盖量化、剪枝、知识蒸馏、PEFT（LoRA / Adapter / Prefix-Tuning）。每种方法都有配套的可运行代码，不是 toy demo，是在真实模型上跑的。</p> <hr/> <h2 id="5-得说几句实在话">5. 得说几句实在话</h2> <p>写了这么多，有几点必须说清楚，不然你买了之后骂我。</p> <p><strong>一，领域跑太快了。</strong> 书稿定稿到现在，又出了不少新工作。书里的内容是截止写作时的主流范式，可以给你打下系统性基础，但不代表”最新”。书是跑不过 arxiv 的。</p> <p><strong>二，LLM 那部分是全书相对薄弱的地方。</strong> 我的研究重心在 NAS 和系统效率，LLM 压缩那块写得没有前面几章深。如果你是冲着”LLM 推理系统优化”（vLLM、PagedAttention 那个层级）来的，这本书不是最对口的选择。</p> <p><strong>三，有门槛。</strong> 假设你有基础的 Python 和 PyTorch 使用经验，以及机器学习基础知识。纯零基础建议先打好底再来。</p> <hr/> <h2 id="6-写在最后">6. 写在最后</h2> <p>我最近在做 MoE 系统的推理效率优化，ExpertFlow 刚在 DAC 2026 被接收，下周可能会单独写篇文章聊聊这个。</p> <p>顺带一提，书里有个我一直觉得被低估的观点——</p> <p><strong>NAS 和 RLHF 的优化逻辑，在结构上是同构的。</strong></p> <p>NAS：用 reward（架构性能）去指导搜索策略更新，最终收敛到好架构。<br/> RLHF：用 reward（人类偏好）去更新模型参数，最终让模型”更像人类期望的样子”。</p> <p>两者都是在高维空间里用稀疏 reward 做策略优化，只是搜索对象不同。这个角度如果你从来没有想过，书里第三章有专门的讨论，可以翻翻。</p> <hr/> <p>书在京东有售：<a href="https://item.jd.com/15384990.html">《动手学 AutoML：从 NAS 到大语言模型优化实战》</a></p> <p>欢迎评论区聊，有任何问题——包括书里写得不清楚的地方——都可以直接来问。</p> <hr/> <p><em>作者：贺鑫（marsggbo），新加坡 A*STAR CFAR 研究员</em><br/> <em>知乎 / GitHub: marsggbo</em></p>]]></content><author><name></name></author><category term="AutoML"/><category term="NAS"/><category term="LLM"/><category term="论文解读"/><category term="新书"/><summary type="html"><![CDATA[LLM 时代，还有人搞 AutoML 吗？有，而且变得更难了]]></summary></entry></feed>